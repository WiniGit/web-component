(function(){"use strict";try{if(typeof document<"u"){var e=document.createElement("style");e.appendChild(document.createTextNode(`._checkbox-container_1749q_1{position:relative;border-radius:.4rem;border:var(--neutral-bolder-border);background-color:transparent;transition:border,background-color .4s;cursor:pointer}._checkbox-container_1749q_1>input{position:absolute;opacity:0;cursor:pointer;height:0;width:0}._checkbox-container_1749q_1>svg{width:100%;height:100%;visibility:hidden;opacity:0!important;transition:visibility,opacity .4s}._checkbox-container_1749q_1>svg>path{fill:var(--check-color, #fff)}._checkbox-container_1749q_1>input:checked~svg,._checkbox-container_1749q_1[is-null-value=true]>svg{visibility:visible;opacity:1!important}._checkbox-container_1749q_1:has(>input:checked),._checkbox-container_1749q_1[is-null-value=true]{background-color:var(--primary-main-color);border:none!important}._checkbox-container_1749q_1:has(>input:disabled){background-color:var(--neutral-disable-background-color)!important}._checkbox-container_1749q_1:has(>input:disabled)>svg>path{fill:var(--neutral-text-disabled-color)!important}._select1-container_t49wa_1{width:100%;box-sizing:border-box;padding:.8rem 1.6rem;border:var(--neutral-bolder-border);border-radius:.8rem;gap:.8rem;position:relative;cursor:context-menu}._select1-container_t49wa_1:focus-within{border-color:var(--primary-main-color)}._select1-container_t49wa_1._disabled_t49wa_33{background-color:var(--neutral-disable-background-color)!important;pointer-events:none!important}._select1-container_t49wa_1._helper-text_t49wa_43{overflow:visible!important;border-color:var(--helper-text-color)!important}._select1-container_t49wa_1._helper-text_t49wa_43:after{content:attr(helper-text);color:var(--helper-text-color);position:absolute;left:0;bottom:-.4rem;width:max-content;font-size:1.2rem;line-height:1.6rem;font-family:inherit;transform:translateY(100%)}._select1-container_t49wa_1>input{flex:1;width:100%;border:none;outline:none;padding:0;background-color:transparent!important;font:inherit;color:inherit;font-size:inherit;font-family:inherit;font-weight:inherit;line-height:inherit;text-align:inherit;text-overflow:inherit}._select1-container_t49wa_1 input:read-only{cursor:context-menu}._select1-popup_t49wa_121{position:absolute;z-index:10;background-color:var(--neutral-absolute-background-color);border-radius:.8rem;border:var(--neutral-main-border);height:fit-content;box-shadow:0 0 12px #00204d1f;align-items:center}._select1-popup_t49wa_121>._select-body_t49wa_143{max-height:100%;flex:1;align-items:start;max-height:28rem;min-height:8rem;width:100%;padding:.4rem 0;overflow:hidden auto}._select1-popup_t49wa_121>._select-body_t49wa_143 ._select-tile_t49wa_165{justify-content:start;align-items:center;padding:.8rem 1.6rem;gap:.8rem;width:100%;cursor:context-menu}._select1-popup_t49wa_121>._select-body_t49wa_143 ._select-tile_t49wa_165:hover{background-color:var(--neutral-hover-background-color)}._select1-popup_t49wa_121>._select-body_t49wa_143 ._select-tile_t49wa_165._disabled_t49wa_33{background-color:var(--neutral-disable-background-color)!important}._select1-popup_t49wa_121>._select-body_t49wa_143 ._select-tile_t49wa_165._disabled_t49wa_33 *{color:var(--neutral-text-disabled-color)!important}._no-results-found_t49wa_207{padding:.8rem;font:400 1.4rem/2rem Inter;color:var(--neutral-text-title-reverse-color)}._wini-icon_1sizd_1{display:flex;justify-content:center;align-items:center;width:fit-content;height:fit-content;padding:.2rem;--size: 1.6rem;--color: var(--neutral-text-subtitle-color)}._wini-icon_1sizd_1[class~=link-icon]{--size: "undefined"}._wini-icon_1sizd_1>svg{width:var(--size, inherit)!important;height:var(--size, inherit)!important}._wini-icon_1sizd_1[class*=fill-icon]>svg>*[fill^="#"]{fill:var(--color, "#61616B")}._wini-icon_1sizd_1[class*=outline-icon]>svg>*[stroke^="#"]{stroke:var(--color, "#61616B")}._wini-icon_1sizd_1._clickable_1sizd_57{cursor:pointer}._wini-icon_1sizd_1[class~=icon-button]{border-radius:50%;background-color:var(--neutral-main-background-color)}._wini-icon_1sizd_1[class~=border]{border:var(--neutral-bolder-border)}._wini-icon_1sizd_1[class~=dashed]{border-style:dashed}._wini-icon_1sizd_1[class~=size64]{width:6.4rem;height:6.4rem;--size: 1.6rem}._wini-icon_1sizd_1[class~=size56]{width:5.6rem;height:5.6rem;--size: 1.6rem}._wini-icon_1sizd_1[class~=size48]{width:4.8rem;height:4.8rem;--size: 1.6rem}._wini-icon_1sizd_1[class~=size40]{width:4rem;height:4rem;--size: 1.4rem}._wini-icon_1sizd_1[class~=size32]{width:3.2rem;height:3.2rem;--size: 1.4rem}._wini-icon_1sizd_1[class~=size24]{width:2.4rem;height:2.4rem;--size: 1.2rem}@keyframes _on-open-tooltip_1sizd_1{0%{opacity:0}to{opacity:1}}._tooltip-container_1sizd_183{position:fixed;z-index:999;animation:_on-open-tooltip_1sizd_1 .2s ease-in-out;animation-fill-mode:forwards}._tooltip-container_1sizd_183>._tooltip-message_1sizd_197{color:var(--neutral-text-body-reverse-color);background-color:var(--neutral-main-reverse-background-color);padding:.4rem 1.2rem;border-radius:.8rem;max-width:21.2rem;text-align:center}._wini-icon_1sizd_1[class~=icon-button]:hover{background-color:var(--neutral-hover-background-color)}.comp-text{display:inline-block;-webkit-box-orient:vertical;-webkit-line-clamp:var(--max-line);overflow:hidden;text-overflow:ellipsis;-webkit-box-pack:center;text-align:start;vertical-align:middle;display:-webkit-box}.comp-text.type-button{cursor:pointer}.comp-text.innerhtml *{-webkit-box-orient:vertical;overflow:hidden;text-overflow:ellipsis;text-align:inherit;margin:0;padding:0;font-family:inherit;font-size:inherit;-webkit-box-pack:inherit}.popup-overlay{position:fixed;top:0!important;left:0!important;right:0!important;bottom:0!important;width:100dvw!important;height:100dvh!important;background-color:#000000b2;display:flex!important;justify-content:center;align-items:center;z-index:99}.popup-container{position:relative;background-color:var(--neutral-absolute-background-color);border-radius:.8rem;border:var(--neutral-main-border);overflow:visible;max-width:86%;max-height:80%}.popup-header{align-items:center;justify-content:start;padding:1.6rem 2.4rem;border-bottom:var(--neutral-main-border)}.popup-footer{column-gap:.8rem;justify-content:end;padding:1.2rem 2.4rem;border-top:var(--neutral-main-border)}.popup-container>.popup-close-btn{position:absolute;right:0;top:0;transform:translate(50%,-50%);width:3.2rem;height:3.2rem;padding:.6rem;border-radius:50%;align-items:center;justify-content:center;background-color:var(--neutral-absolute-background-color);box-shadow:0 15px 50px #1b20321a}.popup-overlay.hidden-overlay{background-color:transparent;width:fit-content!important;height:fit-content!important}.popup-overlay.hidden-overlay>*{position:fixed!important;z-index:99;border-radius:.8rem;background-color:var(--neutral-absolute-background-color)}.popup-overlay>*{transition:width .8s ease-in-out,max-width .8s ease-in-out,height .8s ease-in-out,max-height .8s ease-in-out,border-radius .8s ease-in-out;box-shadow:2px 0 16px #0000000a}.popup-overlay.hidden-overlay>.popup-container{position:absolute;width:max-content;height:max-content;background-color:transparent}.popup-overlay.hidden-overlay>.popup-container .popup-close-btn{display:none!important}@keyframes open-drawer-right-to-left{0%{transform:translate(100%)}to{transform:translate(0)}}.right-drawer{top:0;right:0;height:100dvh;animation:open-drawer-right-to-left .6s ease-in-out;animation-fill-mode:forwards}@keyframes open-drawer-left-to-right{0%{transform:translate(-100%)}to{transform:translate(0)}}.left-drawer{top:0;left:0;height:100dvh;animation:open-drawer-left-to-right .6s ease-in-out;animation-fill-mode:forwards}@keyframes show-dropdown-popup{0%{opacity:0;height:0;width:0;transform:scale(0)}to{opacity:1;transform:scale(1)}}.dropdown-popup{animation:show-dropdown-popup .4s ease-in-out}._switch-container_zx9ru_1{position:relative;display:inline-block;background-color:var(--off-bg)!important;border-radius:50vw!important;border:none;outline:none}._switch-container_zx9ru_1>input{opacity:0;width:0;height:0;border:none;outline:none}._switch-container_zx9ru_1>._slider_zx9ru_35{font-size:var(--size);position:absolute;height:calc(100% - .2em);aspect-ratio:1 / 1;left:.1em;top:.1em;background-color:var(--dot-color);transition:.4s;border-radius:50%}._switch-container_zx9ru_1>input:checked+._slider_zx9ru_35{-webkit-transform:translateX(calc(.8*(100% + .2em)));-ms-transform:translateX(calc(.8*(100% + .2em)));transform:translate(calc(.8*(100% + .2em)))}._switch-container_zx9ru_1:has(>input:checked){background-color:var(--on-bg)!important}._switch-container_zx9ru_1:has(>input:disabled:not(*:checked)){pointer-events:none;border:var(--neutral-bolder-border);background-color:var(--neutral-disable-background-color)!important}._switch-container_zx9ru_1:has(>input:disabled:not(*:checked)) ._slider_zx9ru_35{background-color:var(--neutral-text-disabled-color)!important}._switch-container_zx9ru_1:has(>input:disabled:checked){pointer-events:none;background-color:var(--primary-lighter-color)!important}._dialog-overlay_19n83_1{position:fixed;top:0;left:0;width:100dvw;height:100dvh;background-color:#00000080;display:flex;justify-content:center;align-items:center;z-index:100}._dialog-container_19n83_27{position:relative;background-color:var(--neutral-absolute-background-color);padding:2.4rem;border-radius:8px;box-shadow:0 2px 8px #00000040;gap:2.4rem;overflow:visible}._dialog-container_19n83_27 button{outline:none;padding:0;border:none}._dialog-body_19n83_59{gap:1.6rem}._dialog-body_19n83_59>div:last-child{width:100%;align-items:inherit;gap:8px}._dialog-body_19n83_59>div:last-child>*{width:100%}._dialog-body_19n83_59>._dialog-status_19n83_87{width:5.6rem;height:5.6rem;border-radius:50%;align-items:center;justify-content:center}._dialog-footer_19n83_103{gap:8px;justify-content:end;width:100%}._dialog-footer_19n83_103>._dialog-action_19n83_115{height:4rem;justify-content:center;padding:0 1.6rem;border-radius:.8rem;background-color:var(--neutral-main-background-color)}._dialog-footer_19n83_103>._dialog-action_19n83_115>*{color:var(--neutral-text-subtitle-color)}._dialog-footer_19n83_103>._dialog-submit_19n83_139>*{color:var(--neutral-text-title-reverse-color)}._dialog-container_19n83_27[dialog-type="1"] ._dialog-status_19n83_87{background-color:var(--infor-background)}._dialog-container_19n83_27[dialog-type="2"] ._dialog-status_19n83_87{background-color:var(--error-background)}._dialog-container_19n83_27[dialog-type="3"] ._dialog-status_19n83_87{background-color:var(--warning-background)}._dialog-container_19n83_27[dialog-type="4"] ._dialog-status_19n83_87{background-color:var(--success-background)}._dialog-container_19n83_27[dialog-type="1"] ._dialog-footer_19n83_103>._dialog-submit_19n83_139{background-color:var(--infor-main-color)}._dialog-container_19n83_27[dialog-type="2"] ._dialog-footer_19n83_103>._dialog-submit_19n83_139{background-color:var(--error-main-color)}._dialog-container_19n83_27[dialog-type="3"] ._dialog-footer_19n83_103>._dialog-submit_19n83_139{background-color:var(--warning-main-color)}._dialog-container_19n83_27[dialog-type="4"] ._dialog-footer_19n83_103>._dialog-submit_19n83_139{background-color:var(--success-main-color)}._date-time-picker_9nzyy_1{padding:.8rem 1.6rem;gap:.4rem;border-radius:.8rem;text-align:start}._date-time-picker_9nzyy_1>._prefix-icon_9nzyy_15{padding:.5rem;margin-right:.8rem;border-radius:50%;border:1px dashed var(--neutral-text-subtitle-color)}._date-time-picker_9nzyy_1>._value_9nzyy_29{flex:1;width:100%;font:inherit;color:inherit;font-size:inherit;font-family:inherit;font-weight:inherit;line-height:inherit;text-align:inherit;text-overflow:inherit}._date-time-picker_9nzyy_1>input{outline:none;border:none;background-color:transparent!important}._date-time-picker_9nzyy_1>._prefix-icon_9nzyy_15+._value_9nzyy_29{width:calc(100% - .8rem)}._date-time-picker_9nzyy_1:has(>input){padding:.7rem 1.6rem;border:var(--neutral-main-border)}._date-time-picker_9nzyy_1:has(>input):focus-within{border-color:var(--primary-main-color)}._date-time-picker_9nzyy_1:has(>input:disabled),._date-time-picker_9nzyy_1:disabled{background-color:var(--neutral-disable-background-color)!important;pointer-events:none!important}button._date-time-picker_9nzyy_1:hover{background-color:var(--neutral-hover-background-color)}button._date-time-picker_9nzyy_1:focus{box-shadow:0 4px 20px #0000001a inset}._date-time-picker_9nzyy_1._helper-text_9nzyy_121{position:relative;overflow:visible!important;border-color:var(--helper-text-color)!important}._date-time-picker_9nzyy_1._helper-text_9nzyy_121:after{content:attr(helper-text);color:var(--helper-text-color);position:absolute;left:0;bottom:-.4rem;width:max-content;font-size:1.2rem;line-height:1.6rem;font-family:inherit;transform:translateY(100%)}._popup-actions_9nzyy_159{padding:.4rem 0;max-height:28rem;min-width:10rem;background-color:var(--neutral-absolute-background-color)}._popup-actions_9nzyy_159>button{padding:.8rem;gap:.8rem}._popup-actions_9nzyy_159>button:hover{background-color:var(--neutral-hover-background-color)}._text-field-container_17xun_1{position:relative;box-sizing:border-box;padding:.8rem 1.5rem;border:var(--neutral-main-border);border-radius:.8rem;gap:.8rem}input::-webkit-outer-spin-button,input::-webkit-inner-spin-button{-webkit-appearance:none;margin:0}._text-field-container_17xun_1[class~=size48]{padding:1.2rem 1.5rem}._text-field-container_17xun_1[class~=size32]{padding:.4rem 1.1rem}._text-field-container_17xun_1[class~=size24]{padding:0 .7rem}._text-field-container_17xun_1:has(>input:disabled){background-color:var(--neutral-disable-background-color)!important;color:var(--neutral-text-disabled-color)!important;pointer-events:none!important}._text-field-container_17xun_1:focus-within{border-color:var(--primary-main-color)}._text-field-container_17xun_1>input{flex:1;width:100%;border:none;outline:none;padding:0;background-color:transparent!important;font:inherit;color:inherit;font-size:inherit;font-family:inherit;font-weight:inherit;line-height:inherit;text-align:inherit;text-overflow:inherit}._text-field-container_17xun_1._helper-text_17xun_109{overflow:visible!important;border-color:var(--helper-text-color)!important}._text-field-container_17xun_1._helper-text_17xun_109:after{content:attr(helper-text);color:var(--helper-text-color);position:absolute;left:0;bottom:-.4rem;width:max-content;font-size:1.2rem;line-height:1.6rem;font-family:inherit;transform:translateY(100%)}._calendar-container_1hxld_1{background-color:var(--neutral-absolute-background-color);border-radius:.8rem;align-items:stretch;border:var(--neutral-main-border);width:fit-content}._calendar-sidebar-options_1hxld_17{padding:.8rem 2.4rem .8rem 0;border-right:var(--neutral-main-border)}._calendar-sidebar-options_1hxld_17>._calendar-sidebar-option-buttton_1hxld_27{color:var(--neutral-text-title-color);padding:.8rem 1.6rem}._picker-time-container_1hxld_37{border-left:var(--neutral-main-border);height:34.2rem;align-items:center}._picker-time-container_1hxld_37>div[class*=heading-7]:first-child{width:100%;padding:1.2rem 1.6rem;border-bottom:var(--neutral-main-border);text-align:center}._picker-time-container_1hxld_37 div[class*=scroll-picker-]{flex:1;height:100%;gap:.4rem;padding:.4rem 0;overflow:hidden auto;scrollbar-width:thin}._picker-time-container_1hxld_37 ._scroll-picker-minutes_1hxld_81{border-left:var(--neutral-main-border);border-right:var(--neutral-main-border)}._picker-time-container_1hxld_37 div[class*=scroll-picker-]>button{width:5.6rem;padding:.4rem 0}._picker-time-container_1hxld_37 div[class*=scroll-picker-]>button._selected_1hxld_101{background-color:var(--primary-background);color:var(--primary-main-color)}._picker-date-header_1hxld_111{padding:1.2rem 1.6rem;gap:1.2rem;background-color:transparent!important;border-bottom:var(--neutral-main-border);justify-content:space-between}._picker-date-header_1hxld_111>span{width:100%;max-width:15rem;flex:1;text-align:center;cursor:pointer;border-radius:.8rem}._picker-date-header_1hxld_111>span:hover{background-color:var(--neutral-hover-background-color)}._picker-date-header_1hxld_111>button{width:1.6rem;height:1.6rem;display:flex;align-items:center;justify-content:center}._picker-date-header_1hxld_111>button>svg{font-size:1.4rem;color:var(--neutral-text-title-color);pointer-events:none}._picker-date-body_1hxld_181{padding:1.2rem 1.6rem;width:31.2rem;flex-wrap:wrap;gap:.8rem 0}._picker-date-body_1hxld_181>div{height:3.2rem;padding:0 .4rem;display:flex}._picker-date-body_1hxld_181>div>*{border:1px solid transparent;justify-content:center;height:100%;width:100%}._date-picker-circle_1hxld_221{width:calc(100% / 7)}._date-picker-circle_1hxld_221>*{border-radius:50%}._month-picker-circle_1hxld_237,._year-picker-circle_1hxld_239{width:calc(100% / 3)}._month-picker-circle_1hxld_237>*,._year-picker-circle_1hxld_239>*{border-radius:2.4rem}._picker-date-body_1hxld_181>._in-range_1hxld_257{background-color:var(--neutral-main-background-color)}._picker-date-body_1hxld_181:has(>._in-range_1hxld_257,>._end-range_1hxld_265)>._start-range_1hxld_265{background-image:linear-gradient(to right,transparent 50%,var(--neutral-main-background-color) 50%)}._end-range_1hxld_265{background-image:linear-gradient(to left,transparent 50%,var(--neutral-main-background-color) 50%)!important}._picker-date-body_1hxld_181>._today_1hxld_281>*{border-color:var(--primary-main-color)}._picker-date-body_1hxld_181>._selected_1hxld_101>*{background-color:var(--primary-main-color);color:var(--neutral-absolute-background-color)}._picker-date-body_1hxld_181>._invalid_1hxld_299{opacity:.24!important;pointer-events:none!important}._button-container_gfxps_1{padding:.4rem 1.2rem;border-radius:.8rem;gap:.8rem;justify-content:center;color:var(--neutral-text-subtitle-color, #61616B);width:fit-content;border:1px solid transparent;outline:none}._button-container_gfxps_1[class~=border]{border:var(--neutral-bolder-border)}._button-container_gfxps_1[class~=dashed]{border-style:dashed;color:hsla(hue,saturation,lightness,alpha)}._button-container_gfxps_1[class~=size64]{padding:1.9rem 2.4rem}._button-container_gfxps_1[class~=size56]{padding:1.5rem 2rem}._button-container_gfxps_1[class~=size48]{padding:1.1rem 2rem}._button-container_gfxps_1[class~=size40]{padding:.8rem 1.6rem}._button-container_gfxps_1[class~=size24]{padding:.3rem .8rem}._button-container_gfxps_1>._button-label_gfxps_81{color:inherit;font:inherit;font-size:inherit;font-family:inherit;line-height:inherit;font-weight:inherit;text-align:inherit;letter-spacing:inherit;text-decoration:inherit;font-style:inherit}button[class~=button-grey],a[class~=button-grey]{background-color:var(--neutral-main-background-color)!important;border-color:var(--neutral-main-background-color)!important}button[class~=button-grey] *,a[class~=button-grey] *{color:var(--neutral-text-subtitle-color)!important;--color: var(--neutral-text-subtitle-color) !important}button[class~=button-primary],a[class~=button-primary]{background-color:var(--primary-main-color)!important;border-color:var(--primary-main-color)!important}button[class~=button-primary] *,a[class~=button-primary] *{color:var(--neutral-text-stable-color)!important;--color: var(--neutral-text-stable-color) !important}button[class~=button-neutral] *,a[class~=button-neutral] *{color:var(--neutral-text-subtitle-color)!important;--color: var(--neutral-text-subtitle-color) !important}button[class~=button-neutral],a[class~=button-neutral]{border-color:var(--neutral-bolder-border-color)!important}button[class~=button-black] *,a[class~=button-black] *{color:var(--neutral-text-title-reverse-color)!important;--color: var(--neutral-text-title-reverse-color) !important}button[class~=button-black],a[class~=button-black]{background-color:var(--neutral-absolute-reverse-background-color)!important;border-color:var(--neutral-bolder-reverse-border-color)!important}button[class~=button-white] *,a[class~=button-white] *{color:var(--neutral-text-title-reverse-color)!important;--color: var(--neutral-text-title-reverse-color) !important}button[class~=button-white],a[class~=button-white]{background-color:var(--neutral-absolute-background-color)!important;border-color:var(--neutral-bolder-border-color)!important}button[class~=button-infor],a[class~=button-infor]{border-color:var(--infor-main-color)!important}button[class~=button-infor] *,a[class~=button-infor] *{color:var(--infor-main-color)!important;--color: var(--infor-main-color) !important}button[class~=button-warning],a[class~=button-warning]{border-color:var(--warning-main-color)!important}button[class~=button-warning] *,a[class~=button-warning] *{color:var(--warning-main-color)!important;--color: var(--warning-main-color) !important}button[class~=button-error],a[class~=button-error]{border-color:var(--error-main-color)!important}button[class~=button-error] *,a[class~=button-error] *{color:var(--error-main-color)!important;--color: var(--error-main-color) !important}button[class~=button-success],a[class~=button-success]{border-color:var(--success-main-color)!important}button[class~=button-success] *,a[class~=button-success] *{color:var(--success-main-color)!important;--color: var(--success-main-color) !important}button[class~=button-infor-main],a[class~=button-infor-main]{background-color:var(--infor-main-color)!important;border-color:var(--infor-main-color)!important}button[class~=button-warning-main],a[class~=button-warning-main]{background-color:var(--warning-main-color)!important;border-color:var(--warning-main-color)!important}button[class~=button-error-main],a[class~=button-error-main]{background-color:var(--error-main-color)!important;border-color:var(--error-main-color)!important}button[class~=button-success-main],a[class~=button-success-main]{background-color:var(--success-main-color)!important;border-color:var(--success-main-color)!important}button[class~=button-infor-main] *,a[class~=button-infor-main] *,button[class~=button-warning-main] *,a[class~=button-warning-main] *,button[class~=button-error-main] *,a[class~=button-error-main] *,button[class~=button-success-main] *,a[class~=button-success-main] *{color:var(--neutral-text-stable-color)!important;--color: var(--neutral-text-stable-color) !important}button._button-container_gfxps_1:hover{opacity:.86}button._button-container_gfxps_1:focus{box-shadow:0 4px 20px #0000001a inset}._button-container_gfxps_1:disabled{pointer-events:none;background-color:var(--neutral-disable-background-color, #dddde1)!important;border-color:var(--neutral-bolder-border-color, #D7D7DB)!important}._button-container_gfxps_1:disabled *{color:var(--neutral-text-disabled-color)!important;--color: var(--neutral-text-disabled-color) !important}._select-multi-container_15r79_1{width:100%;max-height:100%;box-sizing:border-box;padding:.8rem 1.6rem;border:var(--neutral-bolder-border);border-radius:.8rem;gap:.8rem;position:relative;cursor:context-menu}._select-multi-container_15r79_1:focus-within{border-color:var(--primary-main-color)}._select-multi-container_15r79_1._disabled_15r79_35{background-color:var(--neutral-disable-background-color)!important;pointer-events:none!important}._select-multi-container_15r79_1._helper-text_15r79_45{overflow:visible!important;border-color:var(--helper-text-color)!important}._select-multi-container_15r79_1._helper-text_15r79_45:after{content:attr(helper-text);color:var(--helper-text-color);position:absolute;left:0;bottom:-.4rem;width:max-content;font-size:1.2rem;line-height:1.6rem;font-family:inherit;transform:translateY(100%)}._select-multi-container_15r79_1 *:has(input),._select-multi-container_15r79_1 *:has(._selected-item-value_15r79_83),._select-multi-container_15r79_1 input{font:inherit;color:inherit;font-size:inherit;font-family:inherit;font-weight:inherit;line-height:inherit;text-align:inherit;text-overflow:inherit}._select-multi-container_15r79_1 input{flex:1;min-width:6rem;border:none;outline:none;padding:0;background-color:transparent}._select-multi-container_15r79_1 ._selected-item-value_15r79_83{border-radius:10rem;padding:.4rem .8rem;background-color:var(--neutral-selected-background-color);gap:.8rem}._select-multi-popup_15r79_139{position:absolute;z-index:10;background-color:var(--neutral-absolute-background-color);border-radius:.8rem;border:var(--neutral-main-border);height:fit-content;box-shadow:0 0 12px #00204d1f;align-items:center}._select-multi-popup_15r79_139>._select-body_15r79_161{max-height:100%;flex:1;align-items:start;max-height:28rem;min-height:8rem;width:100%;padding:.4rem 0;overflow:hidden auto}._select-multi-popup_15r79_139>._select-body_15r79_161 ._select-tile_15r79_183{justify-content:start;align-items:center;padding:.8rem 1.6rem;gap:.8rem;width:100%;cursor:context-menu}._select-multi-popup_15r79_139>._select-body_15r79_161 ._select-tile_15r79_183:hover{background-color:var(--neutral-hover-background-color)}._select-multi-popup_15r79_139>._select-body_15r79_161 ._select-tile_15r79_183:has(input:checked){background-color:var(--neutral-selected-background-color)}._select-multi-popup_15r79_139>._select-body_15r79_161 ._select-tile_15r79_183._disabled_15r79_35{background-color:var(--neutral-disable-background-color)!important}._select-multi-popup_15r79_139>._select-body_15r79_161 ._select-tile_15r79_183._disabled_15r79_35 *{color:var(--neutral-text-disabled-color)!important}._no-results-found_15r79_233{padding:.8rem;font:400 1.4rem/2rem Inter;color:var(--neutral-text-title-reverse-color)}._progress-bar-container_1ddy5_1{row-gap:1.2rem;width:38.2rem}._progress-bar-container_1ddy5_1>._progress-bar-title_1ddy5_11{justify-content:space-between;width:100%}._progress-bar-container_1ddy5_1>._progress-bar-tile_1ddy5_21{column-gap:1.6rem;width:100%;height:2.2rem}._progress-bar-container_1ddy5_1>._progress-bar-tile_1ddy5_21>._progress-bar-value_1ddy5_33{border-radius:10rem;width:100%;flex:1;height:.6rem;position:relative;overflow:hidden;background-color:var(--full-color)}._progress-bar-container_1ddy5_1>._progress-bar-tile_1ddy5_21>._progress-bar-value_1ddy5_33:after{content:"";position:absolute;top:0;left:0;width:var(--percent);height:.6rem;border-radius:10rem;background-color:var(--percent-color);transition:width ease-in-out 1s}._status-icon_1ddy5_77>div{width:1.6rem!important;height:1.6rem!important}._custom-pagination_1ovf2_1{width:100%;height:100%;justify-content:space-between;gap:1.6rem}._custom-pagination_1ovf2_1>._row_1ovf2_15{gap:1.6rem;height:100%}._items-per-page-container_1ovf2_25{gap:8px;height:100%}._pagination_1ovf2_35{gap:.8rem}._pagination_1ovf2_35>li{width:2.4rem;height:2.4rem;color:#161c2499;justify-content:center;border-radius:.4rem;font-weight:600}._pagination_1ovf2_35>li *{color:inherit;font:inherit;font-size:inherit;font-family:inherit;line-height:inherit;font-weight:inherit;text-align:inherit;letter-spacing:inherit}._pagination_1ovf2_35>li._active_1ovf2_83{background-color:var(--infor-background)}._pagination_1ovf2_35>li._active_1ovf2_83{color:var(--infor-main-color)}.custom-table{border:none;min-width:100%;border-collapse:collapse}.custom-table>.tb-header{top:-1px;z-index:4;position:sticky;background-color:#f9fafb}.custom-table>.tb-header>tr{border-top:var(--neutral-bolder-border);border-bottom:var(--neutral-bolder-border)}.custom-table .tb-cell{height:4.4rem;text-align:left;padding:0 2.4rem;font:400 1.4rem/2.2rem Inter;color:#00204dcc;vertical-align:middle}.custom-table tr>td{background-color:#fff;overflow:hidden;text-overflow:ellipsis;max-width:52rem}.custom-table>.tb-header>tr>td{font:600 1.2rem/2rem Inter;white-space:nowrap;color:#00204d99;background-color:#f9fafb}.custom-table tr.selected{background-color:#f5f7fa}.custom-table tr.selected>td{background-color:#f5f7fa}.tb-row>.tb-cell.tb-cell-fixed{z-index:2;position:sticky;background-color:#fff}.tb-header .tb-cell.tb-cell-fixed{z-index:2;position:sticky;background-color:#f9fafb}.tb-cell[align-cell=start]{text-align:start}.tb-cell[align-cell=center]{text-align:center}.tb-cell[align-cell=end]{text-align:end}._radio-btn-container_xv7x5_1{height:max(var(--size),14px);width:max(var(--size),14px);aspect-ratio:1 / 1!important;border-radius:50%;box-sizing:border-box;border:2px solid var(--off-color);background-color:transparent!important;transition:border-color ease-in-out .5s;justify-content:center;cursor:pointer}._radio-btn-container_xv7x5_1>input{display:none;opacity:0;cursor:pointer}._radio-btn-container_xv7x5_1>._checkmark_xv7x5_39{height:inherit;width:inherit;scale:.5;transform-origin:center center;border-radius:50%;aspect-ratio:1 / 1;visibility:hidden;opacity:0;transition:visibility,opacity ease-in-out .5s}._radio-btn-container_xv7x5_1>input:checked~._checkmark_xv7x5_39{visibility:visible;opacity:1;background-color:var(--active-color)}._radio-btn-container_xv7x5_1:has(>input:checked){border-color:var(--active-color)!important}._radio-btn-container_xv7x5_1:has(input:disabled){pointer-events:none!important;border-color:var(--off-color)!important;background-color:var(--neutral-disable-background-color)!important}._radio-btn-container_xv7x5_1>input:disabled~._checkmark_xv7x5_39{display:block!important;background-color:var(--neutral-text-disabled-color)}._text-area-container_11acu_1{height:4rem;position:relative;gap:.8rem;border-radius:.8rem;border:var(--neutral-bolder-border);padding:.8rem 1.6rem;align-items:start}._text-area-container_11acu_1:has(>textarea:disabled){background-color:var(--neutral-disable-background-color)!important;color:var(--neutral-text-disabled-color)!important;pointer-events:none!important}._text-area-container_11acu_1:focus-within{border-color:var(--primary-main-color)}._text-area-container_11acu_1>textarea{flex:1;padding:0;width:100%;height:inherit;max-height:100%;border:none;outline:none;background-color:transparent!important;font:inherit;color:inherit;font-size:inherit;font-family:inherit;font-weight:inherit;line-height:inherit;text-align:inherit;text-overflow:inherit;resize:inherit}._text-area-container_11acu_1._helper-text_11acu_81{overflow:visible!important;border-color:var(--helper-text-color)!important}._text-area-container_11acu_1._helper-text_11acu_81:after{content:attr(helper-text);color:var(--helper-text-color);position:absolute;left:0;bottom:-.4rem;width:max-content;font-size:1.2rem;line-height:1.6rem;font-family:inherit;transform:translateY(100%)}._import-file-container_121cp_1{position:relative;align-items:center;justify-content:center}._import-file-container_121cp_1 input{display:none}._import-file-container_121cp_1:has(input:disabled){background-color:var(--neutral-disable-background-color)!important;pointer-events:none!important}._import-file-container_121cp_1:not(._button-only_121cp_31){max-width:57rem;padding:2.4rem;gap:1.6rem;border-radius:.8rem;border:var(--neutral-bolder-border)}._import-file-container_121cp_1:not(._button-only_121cp_31):has(svg._preview-icon_121cp_47){border-style:dashed}._import-file-container_121cp_1>._import-file-prefix_121cp_55:not(*:has(>img)){width:4.8rem;height:4.8rem;border-radius:50%}._import-file-container_121cp_1>._import-file-prefix_121cp_55{background-color:var(--primary-background);align-items:center;justify-content:center}._import-file-container_121cp_1>._import-file-prefix_121cp_55>img{max-width:14rem;max-height:8.8rem;border-radius:.8rem}._import-file-container_121cp_1>._file-preview-content_121cp_91{max-width:100%;flex:1;row-gap:.4rem;align-items:start}._import-file-container_121cp_1._col_121cp_105>._file-preview-content_121cp_91{align-items:center}._remove-preview-file_121cp_113{width:1.6rem;height:1.6rem;margin-left:.6rem}div[class*=Toastify__toast-body]>div:last-child{font:500 1.2rem/1.6rem}._import-file-container_121cp_1._helper-text_121cp_133{overflow:visible!important;border-color:var(--helper-text-color)!important}._import-file-container_121cp_1._helper-text_121cp_133:after{content:attr(helper-text);color:var(--helper-text-color);position:absolute;left:0;bottom:-.4rem;width:max-content;font-size:1.2rem;line-height:1.6rem;font-family:inherit;transform:translateY(100%)}:root{--toastify-color-light: #fff;--toastify-color-dark: #121212;--toastify-color-info: #287CF0;--toastify-color-success: #3AAC6D;--toastify-color-warning: #FC7A1C;--toastify-color-error: #E14337;--toastify-color-transparent: rgba(255, 255, 255, .7);--toastify-icon-color-info: var(--toastify-color-info);--toastify-icon-color-success: var(--toastify-color-success);--toastify-icon-color-warning: var(--toastify-color-warning);--toastify-icon-color-error: var(--toastify-color-error);--toastify-toast-width: 320px;--toastify-toast-offset: 16px;--toastify-toast-top: max(var(--toastify-toast-offset), env(safe-area-inset-top));--toastify-toast-right: max(var(--toastify-toast-offset), env(safe-area-inset-right));--toastify-toast-left: max(var(--toastify-toast-offset), env(safe-area-inset-left));--toastify-toast-bottom: max(var(--toastify-toast-offset), env(safe-area-inset-bottom));--toastify-toast-background: #fff;--toastify-toast-min-height: 64px;--toastify-toast-max-height: 800px;--toastify-toast-bd-radius: 6px;--toastify-font-family: sans-serif;--toastify-z-index: 9999;--toastify-text-color-light: #757575;--toastify-text-color-dark: #fff;--toastify-text-color-info: #fff;--toastify-text-color-success: #fff;--toastify-text-color-warning: #fff;--toastify-text-color-error: #fff;--toastify-spinner-color: #616161;--toastify-spinner-color-empty-area: #e0e0e0;--toastify-color-progress-light: linear-gradient(to right, #4cd964, #5ac8fa, #007aff, #34aadc, #5856d6, #ff2d55);--toastify-color-progress-dark: #bb86fc;--toastify-color-progress-info: var(--toastify-color-info);--toastify-color-progress-success: var(--toastify-color-success);--toastify-color-progress-warning: var(--toastify-color-warning);--toastify-color-progress-error: var(--toastify-color-error);--toastify-color-progress-bgo: .2}.Toastify__toast-container{z-index:var(--toastify-z-index);transform:translateZ(var(--toastify-z-index));position:fixed;padding:4px;width:var(--toastify-toast-width);box-sizing:border-box;color:#fff}.Toastify__toast-container--top-left{top:var(--toastify-toast-top);left:var(--toastify-toast-left)}.Toastify__toast-container--top-center{top:var(--toastify-toast-top);left:50%;transform:translate(-50%)}.Toastify__toast-container--top-right{top:var(--toastify-toast-top);right:var(--toastify-toast-right)}.Toastify__toast-container--bottom-left{bottom:var(--toastify-toast-bottom);left:var(--toastify-toast-left)}.Toastify__toast-container--bottom-center{bottom:var(--toastify-toast-bottom);left:50%;transform:translate(-50%)}.Toastify__toast-container--bottom-right{bottom:var(--toastify-toast-bottom);right:var(--toastify-toast-right)}@media only screen and (max-width : 480px){.Toastify__toast-container{width:100vw;padding:0;left:env(safe-area-inset-left);margin:0}.Toastify__toast-container--top-left,.Toastify__toast-container--top-center,.Toastify__toast-container--top-right{top:env(safe-area-inset-top);transform:translate(0)}.Toastify__toast-container--bottom-left,.Toastify__toast-container--bottom-center,.Toastify__toast-container--bottom-right{bottom:env(safe-area-inset-bottom);transform:translate(0)}.Toastify__toast-container--rtl{right:env(safe-area-inset-right);left:initial}}.Toastify__toast{--y: 0;position:relative;-ms-touch-action:none;touch-action:none;min-height:var(--toastify-toast-min-height);box-sizing:border-box;margin-bottom:1rem;padding:8px;border-radius:var(--toastify-toast-bd-radius);box-shadow:0 4px 12px #0000001a;display:-ms-flexbox;display:flex;-ms-flex-pack:justify;justify-content:space-between;max-height:var(--toastify-toast-max-height);font-family:var(--toastify-font-family);cursor:default;direction:ltr;z-index:0;overflow:hidden}.Toastify__toast--stacked{position:absolute;width:100%;transform:translate3d(0,var(--y),0) scale(var(--s));transition:transform .3s}.Toastify__toast--stacked[data-collapsed] .Toastify__toast-body,.Toastify__toast--stacked[data-collapsed] .Toastify__close-button{transition:opacity .1s}.Toastify__toast--stacked[data-collapsed=false]{overflow:visible}.Toastify__toast--stacked[data-collapsed=true]:not(:last-child)>*{opacity:0}.Toastify__toast--stacked:after{content:"";position:absolute;left:0;right:0;height:calc(var(--g) * 1px);bottom:100%}.Toastify__toast--stacked[data-pos=top]{top:0}.Toastify__toast--stacked[data-pos=bot]{bottom:0}.Toastify__toast--stacked[data-pos=bot].Toastify__toast--stacked:before{transform-origin:top}.Toastify__toast--stacked[data-pos=top].Toastify__toast--stacked:before{transform-origin:bottom}.Toastify__toast--stacked:before{content:"";position:absolute;left:0;right:0;bottom:0;height:100%;transform:scaleY(3);z-index:-1}.Toastify__toast--rtl{direction:rtl}.Toastify__toast--close-on-click{cursor:pointer}.Toastify__toast-body{margin:auto 0;-ms-flex:1 1 auto;flex:1 1 auto;padding:6px;display:-ms-flexbox;display:flex;-ms-flex-align:center;align-items:center}.Toastify__toast-body>div:last-child{word-break:break-word;-ms-flex:1;flex:1}.Toastify__toast-icon{-webkit-margin-end:10px;margin-inline-end:10px;width:20px;-ms-flex-negative:0;flex-shrink:0;display:-ms-flexbox;display:flex}.Toastify--animate{animation-fill-mode:both;animation-duration:.5s}.Toastify--animate-icon{animation-fill-mode:both;animation-duration:.3s}@media only screen and (max-width : 480px){.Toastify__toast{margin-bottom:0;border-radius:0}}.Toastify__toast-theme--dark{background:var(--toastify-color-dark);color:var(--toastify-text-color-dark)}.Toastify__toast-theme--light,.Toastify__toast-theme--colored.Toastify__toast--default{background:var(--toastify-color-light);color:var(--toastify-text-color-light)}.Toastify__toast-theme--colored.Toastify__toast--info{color:var(--toastify-text-color-info);background:var(--toastify-color-info)}.Toastify__toast-theme--colored.Toastify__toast--success{color:var(--toastify-text-color-success);background:var(--toastify-color-success)}.Toastify__toast-theme--colored.Toastify__toast--warning{color:var(--toastify-text-color-warning);background:var(--toastify-color-warning)}.Toastify__toast-theme--colored.Toastify__toast--error{color:var(--toastify-text-color-error);background:var(--toastify-color-error)}.Toastify__progress-bar-theme--light{background:var(--toastify-color-progress-light)}.Toastify__progress-bar-theme--dark{background:var(--toastify-color-progress-dark)}.Toastify__progress-bar--info{background:var(--toastify-color-progress-info)}.Toastify__progress-bar--success{background:var(--toastify-color-progress-success)}.Toastify__progress-bar--warning{background:var(--toastify-color-progress-warning)}.Toastify__progress-bar--error{background:var(--toastify-color-progress-error)}.Toastify__progress-bar-theme--colored.Toastify__progress-bar--info,.Toastify__progress-bar-theme--colored.Toastify__progress-bar--success,.Toastify__progress-bar-theme--colored.Toastify__progress-bar--warning,.Toastify__progress-bar-theme--colored.Toastify__progress-bar--error{background:var(--toastify-color-transparent)}.Toastify__close-button{color:#fff;background:transparent;outline:none;border:none;padding:0;cursor:pointer;opacity:.7;transition:.3s ease;-ms-flex-item-align:start;align-self:flex-start;z-index:1}.Toastify__close-button--light{color:#000;opacity:.3}.Toastify__close-button>svg{fill:currentColor;height:16px;width:14px}.Toastify__close-button:hover,.Toastify__close-button:focus{opacity:1}@keyframes Toastify__trackProgress{0%{transform:scaleX(1)}to{transform:scaleX(0)}}.Toastify__progress-bar{position:absolute;bottom:0;left:0;width:100%;height:100%;z-index:var(--toastify-z-index);opacity:.7;transform-origin:left;border-bottom-left-radius:var(--toastify-toast-bd-radius)}.Toastify__progress-bar--animated{animation:Toastify__trackProgress linear 1 forwards}.Toastify__progress-bar--controlled{transition:transform .2s}.Toastify__progress-bar--rtl{right:0;left:initial;transform-origin:right;border-bottom-left-radius:initial;border-bottom-right-radius:var(--toastify-toast-bd-radius)}.Toastify__progress-bar--wrp{position:absolute;bottom:0;left:0;width:100%;height:5px;border-bottom-left-radius:var(--toastify-toast-bd-radius)}.Toastify__progress-bar--wrp[data-hidden=true]{opacity:0}.Toastify__progress-bar--bg{opacity:var(--toastify-color-progress-bgo);width:100%;height:100%}.Toastify__spinner{width:20px;height:20px;box-sizing:border-box;border:2px solid;border-radius:100%;border-color:var(--toastify-spinner-color-empty-area);border-right-color:var(--toastify-spinner-color);animation:Toastify__spin .65s linear infinite}@keyframes Toastify__bounceInRight{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(3000px,0,0)}60%{opacity:1;transform:translate3d(-25px,0,0)}75%{transform:translate3d(10px,0,0)}90%{transform:translate3d(-5px,0,0)}to{transform:none}}@keyframes Toastify__bounceOutRight{20%{opacity:1;transform:translate3d(-20px,var(--y),0)}to{opacity:0;transform:translate3d(2000px,var(--y),0)}}@keyframes Toastify__bounceInLeft{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(-3000px,0,0)}60%{opacity:1;transform:translate3d(25px,0,0)}75%{transform:translate3d(-10px,0,0)}90%{transform:translate3d(5px,0,0)}to{transform:none}}@keyframes Toastify__bounceOutLeft{20%{opacity:1;transform:translate3d(20px,var(--y),0)}to{opacity:0;transform:translate3d(-2000px,var(--y),0)}}@keyframes Toastify__bounceInUp{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(0,3000px,0)}60%{opacity:1;transform:translate3d(0,-20px,0)}75%{transform:translate3d(0,10px,0)}90%{transform:translate3d(0,-5px,0)}to{transform:translateZ(0)}}@keyframes Toastify__bounceOutUp{20%{transform:translate3d(0,calc(var(--y) - 10px),0)}40%,45%{opacity:1;transform:translate3d(0,calc(var(--y) + 20px),0)}to{opacity:0;transform:translate3d(0,-2000px,0)}}@keyframes Toastify__bounceInDown{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(0,-3000px,0)}60%{opacity:1;transform:translate3d(0,25px,0)}75%{transform:translate3d(0,-10px,0)}90%{transform:translate3d(0,5px,0)}to{transform:none}}@keyframes Toastify__bounceOutDown{20%{transform:translate3d(0,calc(var(--y) - 10px),0)}40%,45%{opacity:1;transform:translate3d(0,calc(var(--y) + 20px),0)}to{opacity:0;transform:translate3d(0,2000px,0)}}.Toastify__bounce-enter--top-left,.Toastify__bounce-enter--bottom-left{animation-name:Toastify__bounceInLeft}.Toastify__bounce-enter--top-right,.Toastify__bounce-enter--bottom-right{animation-name:Toastify__bounceInRight}.Toastify__bounce-enter--top-center{animation-name:Toastify__bounceInDown}.Toastify__bounce-enter--bottom-center{animation-name:Toastify__bounceInUp}.Toastify__bounce-exit--top-left,.Toastify__bounce-exit--bottom-left{animation-name:Toastify__bounceOutLeft}.Toastify__bounce-exit--top-right,.Toastify__bounce-exit--bottom-right{animation-name:Toastify__bounceOutRight}.Toastify__bounce-exit--top-center{animation-name:Toastify__bounceOutUp}.Toastify__bounce-exit--bottom-center{animation-name:Toastify__bounceOutDown}@keyframes Toastify__zoomIn{0%{opacity:0;transform:scale3d(.3,.3,.3)}50%{opacity:1}}@keyframes Toastify__zoomOut{0%{opacity:1}50%{opacity:0;transform:translate3d(0,var(--y),0) scale3d(.3,.3,.3)}to{opacity:0}}.Toastify__zoom-enter{animation-name:Toastify__zoomIn}.Toastify__zoom-exit{animation-name:Toastify__zoomOut}@keyframes Toastify__flipIn{0%{transform:perspective(400px) rotateX(90deg);animation-timing-function:ease-in;opacity:0}40%{transform:perspective(400px) rotateX(-20deg);animation-timing-function:ease-in}60%{transform:perspective(400px) rotateX(10deg);opacity:1}80%{transform:perspective(400px) rotateX(-5deg)}to{transform:perspective(400px)}}@keyframes Toastify__flipOut{0%{transform:translate3d(0,var(--y),0) perspective(400px)}30%{transform:translate3d(0,var(--y),0) perspective(400px) rotateX(-20deg);opacity:1}to{transform:translate3d(0,var(--y),0) perspective(400px) rotateX(90deg);opacity:0}}.Toastify__flip-enter{animation-name:Toastify__flipIn}.Toastify__flip-exit{animation-name:Toastify__flipOut}@keyframes Toastify__slideInRight{0%{transform:translate3d(110%,0,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideInLeft{0%{transform:translate3d(-110%,0,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideInUp{0%{transform:translate3d(0,110%,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideInDown{0%{transform:translate3d(0,-110%,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideOutRight{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(110%,var(--y),0)}}@keyframes Toastify__slideOutLeft{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(-110%,var(--y),0)}}@keyframes Toastify__slideOutDown{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(0,500px,0)}}@keyframes Toastify__slideOutUp{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(0,-500px,0)}}.Toastify__slide-enter--top-left,.Toastify__slide-enter--bottom-left{animation-name:Toastify__slideInLeft}.Toastify__slide-enter--top-right,.Toastify__slide-enter--bottom-right{animation-name:Toastify__slideInRight}.Toastify__slide-enter--top-center{animation-name:Toastify__slideInDown}.Toastify__slide-enter--bottom-center{animation-name:Toastify__slideInUp}.Toastify__slide-exit--top-left,.Toastify__slide-exit--bottom-left{animation-name:Toastify__slideOutLeft;animation-timing-function:ease-in;animation-duration:.3s}.Toastify__slide-exit--top-right,.Toastify__slide-exit--bottom-right{animation-name:Toastify__slideOutRight;animation-timing-function:ease-in;animation-duration:.3s}.Toastify__slide-exit--top-center{animation-name:Toastify__slideOutUp;animation-timing-function:ease-in;animation-duration:.3s}.Toastify__slide-exit--bottom-center{animation-name:Toastify__slideOutDown;animation-timing-function:ease-in;animation-duration:.3s}@keyframes Toastify__spin{0%{transform:rotate(0)}to{transform:rotate(360deg)}}._infinite-scroll_1o3yx_1._loading_1o3yx_1:after{content:"";position:sticky;width:1.2rem;height:1.2rem;border:.4rem solid var(--neutral-absolute-background-color);border-radius:50%;border-top:.4rem solid var(--infor-main-color);-webkit-animation:_spin_1o3yx_1 2s linear infinite;animation:_spin_1o3yx_1 2s linear infinite;order:100000}@-webkit-keyframes _spin_1o3yx_1{0%{-webkit-transform:rotate(0deg)}to{-webkit-transform:rotate(360deg)}}@keyframes _spin_1o3yx_1{0%{transform:rotate(0)}to{transform:rotate(360deg)}}._rating-container_170xt_1{gap:.8rem}._rating-container_170xt_1 svg{cursor:pointer}._rating-container_170xt_1 svg>*{pointer-events:none!important}.awssld{--organic-arrow-thickness: 4px;--organic-arrow-height: 40px;--slider-height-percentage: 60%;--loader-bar-color: #851515;--loader-bar-height: 6px;--control-button-width: 10%;--control-button-height: 25%;--control-button-opacity: .5;--control-button-hover-opacity: .75;--control-button-background: transparent;--transition-bezier: cubic-bezier(.5, .075, .25, .95);--slider-transition-duration: 575ms;--organic-arrow-color: #6a6a6a;--organic-arrow-border-radius: 0;--control-bullet-color: #6a6a6a;--control-bullet-active-color: #6a6a6a;--content-background-color: #2f2f2f}.awssld__container figure,.awssld__content,.awssld__box{display:block;position:absolute;width:100%;height:100%;top:0;left:0}.awssld__controls button,.awssld__bullets button{outline-color:0;outline-style:none;outline-width:0}.awssld{--organic-arrow-thickness: 4px;--organic-arrow-height: 40px;--slider-height-percentage: 60%;--loader-bar-color: var(--primary-main-color, #851515);--loader-bar-height: 6px;--control-button-width: 10%;--control-button-height: 25%;--control-button-opacity: .5;--control-button-hover-opacity: .75;--control-button-background: transparent;--transition-bezier: cubic-bezier(.5, .075, .25, .95);--slider-transition-duration: 575ms;--organic-arrow-color: #6a6a6a;--organic-arrow-border-radius: 0;--control-bullet-color: #6a6a6a;--control-bullet-active-color: #6a6a6a;--content-background-color: #2f2f2f}.awssld{display:block;position:relative;width:100%;max-width:100%}.awssld__wrapper{display:block;overflow:hidden;position:relative;height:100%;width:100%}.awssld__container{display:block;width:100%;height:0;padding-bottom:var(--slider-height-percentage)}@media all and (max-width: 500px){.awssld__container{padding-bottom:calc(var(--slider-height-percentage) * 1.25)}}.awssld__startUp{background-color:red;height:100%;width:100%}.awssld__startUp>div{height:100%;width:100%;display:-webkit-box;display:-ms-flexbox;display:flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center}.awssld__startUp img{width:35%;height:auto}.awssld__content{background-color:var(--content-background-color);overflow:hidden;display:-webkit-box;display:-ms-flexbox;display:flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center}.awssld__content>img,.awssld__content>video{-o-object-fit:cover;object-fit:cover;width:100%;height:100%;position:absolute;top:0;left:0}.awssld__controls button{display:-webkit-box;display:-ms-flexbox;display:flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center;width:var(--control-button-width);height:var(--control-button-height);position:absolute;z-index:10;top:calc(50% - (.5 * var(--control-button-height)));border:none;background-color:var(--control-button-background);color:#fff;cursor:pointer}.awssld__controls button .awssld__controls__arrow-left,.awssld__controls button .awssld__controls__arrow-right{opacity:var(--control-button-opacity)}.awssld__controls button:hover .awssld__controls__arrow-left,.awssld__controls button:hover .awssld__controls__arrow-right{opacity:var(--control-button-opacity-hover)}.awssld__controls--active .awssld__controls__arrow-left{opacity:var(--control-button-opacity-hover);-webkit-transform:translate3d(-100%,0,0);transform:translate3d(-100%,0,0)}.awssld__controls--active .awssld__controls__arrow-right{opacity:var(--control-button-opacity-hover);-webkit-transform:translate3d(100%,0,0);transform:translate3d(100%,0,0)}.awssld__controls--hidden{display:none}@media all and (max-width: 520px){.awssld__controls{visibility:hidden}}.awssld__bar{display:block;width:100%;height:var(--loader-bar-height);background-color:var(--loader-bar-color);position:absolute;top:0;left:0;z-index:101;-webkit-transition:-webkit-transform 3s var(--transition-bezier);transition:-webkit-transform 3s var(--transition-bezier);transition:transform 3s var(--transition-bezier);transition:transform 3s var(--transition-bezier),-webkit-transform 3s var(--transition-bezier);-webkit-transform:translate3d(-100%,0,0);transform:translate3d(-100%,0,0)}.awssld__bar--active{-webkit-transform:translate3d(-20%,0,0);transform:translate3d(-20%,0,0)}.awssld__bar--end{-webkit-transition-duration:.3s;transition-duration:.3s;-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}.awssld__next{right:0}.awssld__prev{left:0}.awssld__box{z-index:1;visibility:hidden}.awssld--animated{will-change:transform;visibility:visible}.awssld--animated-mobile{will-change:transform;-webkit-transition:-webkit-transform 325ms cubic-bezier(.15,.65,.1,1);transition:-webkit-transform 325ms cubic-bezier(.15,.65,.1,1);transition:transform 325ms cubic-bezier(.15,.65,.1,1);transition:transform 325ms cubic-bezier(.15,.65,.1,1),-webkit-transform 325ms cubic-bezier(.15,.65,.1,1)}.awssld--active{visibility:visible;z-index:2;-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}.awssld--moveRight,.awssld--moveLeft{-webkit-backface-visibility:hidden;backface-visibility:hidden}.awssld--moveRight{-webkit-animation:slideFromRight var(--slider-transition-duration) both var(--transition-bezier);animation:slideFromRight var(--slider-transition-duration) both var(--transition-bezier)}.awssld--moveLeft{-webkit-animation:slideFromLeft var(--slider-transition-duration) both var(--transition-bezier);animation:slideFromLeft var(--slider-transition-duration) both var(--transition-bezier)}.awssld--exit{z-index:0}.awssld--exit.awssld--moveLeft{-webkit-animation:slideToLeft var(--slider-transition-duration) both var(--transition-bezier);animation:slideToLeft var(--slider-transition-duration) both var(--transition-bezier)}.awssld--exit.awssld--moveRight{-webkit-animation:slideToRight var(--slider-transition-duration) both var(--transition-bezier);animation:slideToRight var(--slider-transition-duration) both var(--transition-bezier)}.awssld--first .awssld__prev,.awssld--last .awssld__next{visibility:hidden}.awssld--fill-parent{position:absolute!important;width:100%!important;height:100%!important;top:0;left:0}.awssld--fill-parent .awssld__container{height:100%;padding:0}.awssld__bullets{position:absolute;bottom:-40px;width:100%;display:-webkit-box;display:-ms-flexbox;display:flex;-webkit-box-align:center;-ms-flex-align:center;align-items:center;-webkit-box-pack:center;-ms-flex-pack:center;justify-content:center}.awssld__bullets button{padding:0;display:block;width:16px;height:16px;margin:5px;border-radius:50%;background:var(--control-bullet-color);text-indent:-9999px;overflow:hidden;cursor:pointer;border:none;-webkit-transition:background-color .175s ease-out,-webkit-transform .225s cubic-bezier(.8,1.35,.75,1.45);transition:background-color .175s ease-out,-webkit-transform .225s cubic-bezier(.8,1.35,.75,1.45);transition:transform .225s cubic-bezier(.8,1.35,.75,1.45),background-color .175s ease-out;transition:transform .225s cubic-bezier(.8,1.35,.75,1.45),background-color .175s ease-out,-webkit-transform .225s cubic-bezier(.8,1.35,.75,1.45)}.awssld__bullets button:hover,.awssld__bullets .awssld__bullets--loading{-webkit-transform:scale(1.2);transform:scale(1.2)}.awssld__bullets .awssld__bullets--active{-webkit-transform:scale(1.5);transform:scale(1.5);background:var(--control-bullet-active-color)}.awssld__bullets .awssld__bullets--active:hover{-webkit-transform:scale(1.5);transform:scale(1.5)}.awssld__controls__arrow-left,.awssld__controls__arrow-right{width:100%;height:var(--organic-arrow-height);position:relative;display:block;-webkit-transition:opacity .2s ease-out,-webkit-transform .2s ease-out .125s;transition:opacity .2s ease-out,-webkit-transform .2s ease-out .125s;transition:transform .2s ease-out .125s,opacity .2s ease-out;transition:transform .2s ease-out .125s,opacity .2s ease-out,-webkit-transform .2s ease-out .125s}.awssld__controls__arrow-left:before,.awssld__controls__arrow-left:after,.awssld__controls__arrow-right:before,.awssld__controls__arrow-right:after{content:" ";position:absolute;right:calc(50% - (.7071 * (var(--organic-arrow-height) + var(--organic-arrow-thickness))) / 2);height:100%;border-radius:var(--organic-arrow-border-radius);width:var(--organic-arrow-thickness);background-color:var(--organic-arrow-color);-webkit-transition:background-color .15s ease-out,-webkit-transform .15s ease-out;transition:background-color .15s ease-out,-webkit-transform .15s ease-out;transition:transform .15s ease-out,background-color .15s ease-out;transition:transform .15s ease-out,background-color .15s ease-out,-webkit-transform .15s ease-out}.awssld__controls__arrow-left:before,.awssld__controls__arrow-right:before{-webkit-transform-origin:100% 100% 0;transform-origin:100% 100% 0;top:-50%;-webkit-transform:rotate(-45deg);transform:rotate(-45deg)}.awssld__controls__arrow-left:after,.awssld__controls__arrow-right:after{-webkit-transform-origin:100% 0% 0;transform-origin:100% 0% 0;top:50%;-webkit-transform:rotate(45deg);transform:rotate(45deg)}.awssld__controls__arrow-right--active{-webkit-transform:translate3d(100%,0,0);transform:translate3d(100%,0,0)}.awssld__controls__arrow-right--active:after{-webkit-transform:rotate(90deg) translate3d(50%,0,0)!important;transform:rotate(90deg) translate3d(50%,0,0)!important}.awssld__controls__arrow-right--active:before{-webkit-transform:rotate(-90deg) translate3d(50%,0,0)!important;transform:rotate(-90deg) translate3d(50%,0,0)!important}.awssld__controls__arrow-left:before,.awssld__controls__arrow-left:after{right:auto;left:calc(50% - (.7071 * (var(--organic-arrow-height) + var(--organic-arrow-thickness))) / 2)}.awssld__controls__arrow-left:before{-webkit-transform-origin:0 100% 0;transform-origin:0 100% 0;top:-50%;-webkit-transform:rotate(45deg);transform:rotate(45deg)}.awssld__controls__arrow-left:after{-webkit-transform-origin:0 0 0;transform-origin:0 0 0;top:50%;-webkit-transform:rotate(-45deg);transform:rotate(-45deg)}.awssld__controls__arrow-left--active{-webkit-transform:translate3d(-100%,0,0);transform:translate3d(-100%,0,0)}.awssld__controls__arrow-left--active:after{-webkit-transform:rotate(-90deg) translate3d(-50%,0,0)!important;transform:rotate(-90deg) translate3d(-50%,0,0)!important}.awssld__controls__arrow-left--active:before{-webkit-transform:rotate(90deg) translate3d(-50%,0,0)!important;transform:rotate(90deg) translate3d(-50%,0,0)!important}.awssld__controls button:hover .awssld__controls__arrow-left:before{opacity:1;-webkit-transform:rotate(30deg);transform:rotate(30deg)}.awssld__controls button:hover .awssld__controls__arrow-left:after{opacity:1;-webkit-transform:rotate(-30deg);transform:rotate(-30deg)}.awssld__controls button:hover .awssld__controls__arrow-right:before{opacity:1;-webkit-transform:rotate(-30deg);transform:rotate(-30deg)}.awssld__controls button:hover .awssld__controls__arrow-right:after{opacity:1;-webkit-transform:rotate(30deg);transform:rotate(30deg)}.awssld__timer{--timer-delay: 2s;--timer-release: .2s;--timer-height: 4px;--timer-background-color: rgba(0, 0, 0, .15);width:100%;height:var(--timer-height);background-color:var(--timer-background-color);position:absolute;top:0;left:0;z-index:100;-webkit-transform:translateX(-100%);transform:translate(-100%)}.awssld__timer--animated{will-change:transform;-webkit-transition:-webkit-transform var(--timer-delay) linear;transition:-webkit-transform var(--timer-delay) linear;transition:transform var(--timer-delay) linear;transition:transform var(--timer-delay) linear,-webkit-transform var(--timer-delay) linear}.awssld__timer--run{-webkit-transform:translateX(0);transform:translate(0)}.awssld__timer--fast{-webkit-transition:-webkit-transform calc(var(--timer-release) / 2) linear;transition:-webkit-transform calc(var(--timer-release) / 2) linear;transition:transform calc(var(--timer-release) / 2) linear;transition:transform calc(var(--timer-release) / 2) linear,-webkit-transform calc(var(--timer-release) / 2) linear;-webkit-transform:translateX(-.00001px);transform:translate(-.00001px)}.awssld__timer--end{-webkit-transform:translateX(-.0001px);transform:translate(-.0001px);-webkit-transition:-webkit-transform var(--timer-release) linear;transition:-webkit-transform var(--timer-release) linear;transition:transform var(--timer-release) linear;transition:transform var(--timer-release) linear,-webkit-transform var(--timer-release) linear}.awssld__timer--hidden{display:none}@-webkit-keyframes slideFromLeft{0%{-webkit-transform:translate3d(-100%,0,0);transform:translate3d(-100%,0,0)}to{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}}@keyframes slideFromLeft{0%{-webkit-transform:translate3d(-100%,0,0);transform:translate3d(-100%,0,0)}to{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}}@-webkit-keyframes slideFromRight{0%{-webkit-transform:translate3d(100%,0,0);transform:translate3d(100%,0,0)}to{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}}@keyframes slideFromRight{0%{-webkit-transform:translate3d(100%,0,0);transform:translate3d(100%,0,0)}to{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}}@-webkit-keyframes slideToLeft{0%{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}to{-webkit-transform:translate3d(-100%,0,0);transform:translate3d(-100%,0,0)}}@keyframes slideToLeft{0%{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}to{-webkit-transform:translate3d(-100%,0,0);transform:translate3d(-100%,0,0)}}@-webkit-keyframes slideToRight{0%{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}to{-webkit-transform:translate3d(100%,0,0);transform:translate3d(100%,0,0)}}@keyframes slideToRight{0%{-webkit-transform:translate3d(0,0,0);transform:translateZ(0)}to{-webkit-transform:translate3d(100%,0,0);transform:translate3d(100%,0,0)}}.custom-slider-container{--content-background-color: #00000000 !important}.custom-slider-container .awssld__content>*{width:100%;height:100%}.custom-slider-container .awssld__controls button{padding:2.4rem;width:fit-content;height:fit-content;top:50%;transform:translateY(-50%)}.awssld--scaleOutAnimation{--scale-out-scale: .85}.awssld--scaleOutAnimation .awssld--exit.awssld--moveRight,.awssld--scaleOutAnimation .awssld--exit.awssld--moveLeft{-webkit-animation:scaleOut var(--slider-transition-duration) both var(--transition-bezier);animation:scaleOut var(--slider-transition-duration) both var(--transition-bezier)}@-webkit-keyframes scaleOut{to{-webkit-transform:scale(var(--scale-out-scale));transform:scale(var(--scale-out-scale))}}@keyframes scaleOut{to{-webkit-transform:scale(var(--scale-out-scale));transform:scale(var(--scale-out-scale))}}.awssld--foldOutAnimation{--fold-out-perspective: 2000px;--fold-out-angle: 90deg}.awssld--foldOutAnimation .awssld__container{-webkit-perspective:var(--fold-out-perspective);perspective:var(--fold-out-perspective)}.awssld--foldOutAnimation .awssld--exit.awssld--moveLeft,.awssld--foldOutAnimation .awssld--exit.awssld--moveRight{-webkit-transform-style:preserve-3d;transform-style:preserve-3d}.awssld--foldOutAnimation .awssld--exit.awssld--moveLeft{-webkit-transform-origin:100% 50%;transform-origin:100% 50%;-webkit-animation:foldLeft var(--slider-transition-duration) both var(--transition-bezier);animation:foldLeft var(--slider-transition-duration) both var(--transition-bezier)}.awssld--foldOutAnimation .awssld--exit.awssld--moveRight{-webkit-transform-origin:0% 50%;transform-origin:0% 50%;-webkit-animation:foldRight var(--slider-transition-duration) both var(--transition-bezier);animation:foldRight var(--slider-transition-duration) both var(--transition-bezier)}@-webkit-keyframes foldLeft{0%{-webkit-transform:translate3d(0,0,0) rotateY(0deg);transform:translateZ(0) rotateY(0)}to{-webkit-transform:translate3d(-100%,0,0) rotateY(calc(var(--fold-out-angle) * -1));transform:translate3d(-100%,0,0) rotateY(calc(var(--fold-out-angle) * -1))}}@keyframes foldLeft{0%{-webkit-transform:translate3d(0,0,0) rotateY(0deg);transform:translateZ(0) rotateY(0)}to{-webkit-transform:translate3d(-100%,0,0) rotateY(calc(var(--fold-out-angle) * -1));transform:translate3d(-100%,0,0) rotateY(calc(var(--fold-out-angle) * -1))}}@-webkit-keyframes foldRight{0%{-webkit-transform:translate3d(0,0,0) rotateY(0deg);transform:translateZ(0) rotateY(0)}to{-webkit-transform:translate3d(100%,0,0) rotateY(var(--fold-out-angle));transform:translate3d(100%,0,0) rotateY(var(--fold-out-angle))}}@keyframes foldRight{0%{-webkit-transform:translate3d(0,0,0) rotateY(0deg);transform:translateZ(0) rotateY(0)}to{-webkit-transform:translate3d(100%,0,0) rotateY(var(--fold-out-angle));transform:translate3d(100%,0,0) rotateY(var(--fold-out-angle))}}.awssld--cubeAnimation{--cube-animation-duration: 675ms;--cube-animation-perspective: 1800px;--cube-animation-ease-in: cubic-bezier(.8, 0, 1, .8);--cube-animation-ease-out: cubic-bezier(0, .2, .2, 1);--cube-translateZ-distance: -225px}.awssld--cubeAnimation .awssld__container{-webkit-perspective:var(--cube-animation-perspective);perspective:var(--cube-animation-perspective)}.awssld--cubeAnimation .awssld--moveRight{-webkit-transform-origin:0% 50%;transform-origin:0% 50%;-webkit-animation:cubeLeftIn var(--cube-animation-duration) both var(--cube-animation-ease-in);animation:cubeLeftIn var(--cube-animation-duration) both var(--cube-animation-ease-in)}.awssld--cubeAnimation .awssld--moveLeft{-webkit-transform-origin:100% 50%;transform-origin:100% 50%;-webkit-animation:cubeRightIn var(--cube-animation-duration) both var(--cube-animation-ease-in);animation:cubeRightIn var(--cube-animation-duration) both var(--cube-animation-ease-in)}.awssld--cubeAnimation .awssld--exit.awssld--moveLeft{-webkit-transform-origin:100% 50%;transform-origin:100% 50%;-webkit-animation:cubeLeftOut var(--cube-animation-duration) both var(--cube-animation-ease-in);animation:cubeLeftOut var(--cube-animation-duration) both var(--cube-animation-ease-in)}.awssld--cubeAnimation .awssld--exit.awssld--moveRight{-webkit-transform-origin:0% 50%;transform-origin:0% 50%;-webkit-animation:cubeRightOut var(--cube-animation-duration) both var(--cube-animation-ease-in);animation:cubeRightOut var(--cube-animation-duration) both var(--cube-animation-ease-in)}@-webkit-keyframes cubeLeftOut{50%{-webkit-animation-timing-function:var(--cube-animation-ease-out);animation-timing-function:var(--cube-animation-ease-out);-webkit-transform:translateX(-50%) translateZ(var(--cube-translateZ-distance)) rotateY(-45deg);transform:translate(-50%) translateZ(var(--cube-translateZ-distance)) rotateY(-45deg)}to{-webkit-transform:translateX(-100%) rotateY(-90deg);transform:translate(-100%) rotateY(-90deg)}}@keyframes cubeLeftOut{50%{-webkit-animation-timing-function:var(--cube-animation-ease-out);animation-timing-function:var(--cube-animation-ease-out);-webkit-transform:translateX(-50%) translateZ(var(--cube-translateZ-distance)) rotateY(-45deg);transform:translate(-50%) translateZ(var(--cube-translateZ-distance)) rotateY(-45deg)}to{-webkit-transform:translateX(-100%) rotateY(-90deg);transform:translate(-100%) rotateY(-90deg)}}@-webkit-keyframes cubeRightOut{50%{-webkit-animation-timing-function:var(--cube-animation-ease-out);animation-timing-function:var(--cube-animation-ease-out);-webkit-transform:translateX(50%) translateZ(var(--cube-translateZ-distance)) rotateY(45deg);transform:translate(50%) translateZ(var(--cube-translateZ-distance)) rotateY(45deg)}to{-webkit-transform:translateX(100%) rotateY(90deg);transform:translate(100%) rotateY(90deg)}}@keyframes cubeRightOut{50%{-webkit-animation-timing-function:var(--cube-animation-ease-out);animation-timing-function:var(--cube-animation-ease-out);-webkit-transform:translateX(50%) translateZ(var(--cube-translateZ-distance)) rotateY(45deg);transform:translate(50%) translateZ(var(--cube-translateZ-distance)) rotateY(45deg)}to{-webkit-transform:translateX(100%) rotateY(90deg);transform:translate(100%) rotateY(90deg)}}@-webkit-keyframes cubeLeftIn{0%{-webkit-transform:translateX(100%) rotateY(90deg);transform:translate(100%) rotateY(90deg)}50%{-webkit-animation-timing-function:var(--cube-animation-ease-out);animation-timing-function:var(--cube-animation-ease-out);-webkit-transform:translateX(50%) translateZ(var(--cube-translateZ-distance)) rotateY(45deg);transform:translate(50%) translateZ(var(--cube-translateZ-distance)) rotateY(45deg)}}@keyframes cubeLeftIn{0%{-webkit-transform:translateX(100%) rotateY(90deg);transform:translate(100%) rotateY(90deg)}50%{-webkit-animation-timing-function:var(--cube-animation-ease-out);animation-timing-function:var(--cube-animation-ease-out);-webkit-transform:translateX(50%) translateZ(var(--cube-translateZ-distance)) rotateY(45deg);transform:translate(50%) translateZ(var(--cube-translateZ-distance)) rotateY(45deg)}}@-webkit-keyframes cubeRightIn{0%{-webkit-transform:translateX(-100%) rotateY(-90deg);transform:translate(-100%) rotateY(-90deg)}50%{-webkit-animation-timing-function:var(--cube-animation-ease-out);animation-timing-function:var(--cube-animation-ease-out);-webkit-transform:translateX(-50%) translateZ(var(--cube-translateZ-distance)) rotateY(-45deg);transform:translate(-50%) translateZ(var(--cube-translateZ-distance)) rotateY(-45deg)}}@keyframes cubeRightIn{0%{-webkit-transform:translateX(-100%) rotateY(-90deg);transform:translate(-100%) rotateY(-90deg)}50%{-webkit-animation-timing-function:var(--cube-animation-ease-out);animation-timing-function:var(--cube-animation-ease-out);-webkit-transform:translateX(-50%) translateZ(var(--cube-translateZ-distance)) rotateY(-45deg);transform:translate(-50%) translateZ(var(--cube-translateZ-distance)) rotateY(-45deg)}}.awssld--fallAnimation{--fall-animation-angle: 16deg;--fall-scaling-in-from: .9}.awssld--fallAnimation .awssld--moveRight,.awssld--fallAnimation .awssld--moveLeft{-webkit-animation:scaleUp calc(var(--slider-transition-duration) / 2) var(--transition-bezier) both;animation:scaleUp calc(var(--slider-transition-duration) / 2) var(--transition-bezier) both}.awssld--fallAnimation .awssld--exit{z-index:100}.awssld--fallAnimation .awssld--exit.awssld--moveLeft{-webkit-transform-origin:0% 0%;transform-origin:0% 0%;-webkit-animation:rotateFallRight var(--slider-transition-duration) both ease-in-out;animation:rotateFallRight var(--slider-transition-duration) both ease-in-out}.awssld--fallAnimation .awssld--exit.awssld--moveRight{-webkit-transform-origin:100% 0%;transform-origin:100% 0%;-webkit-animation:rotateFallLeft var(--slider-transition-duration) both ease-in-out;animation:rotateFallLeft var(--slider-transition-duration) both ease-in-out}@-webkit-keyframes scaleUp{0%{-webkit-transform:scale(var(--fall-scaling-in-from));transform:scale(var(--fall-scaling-in-from))}75%{-webkit-transform:scale(1);transform:scale(1)}}@keyframes scaleUp{0%{-webkit-transform:scale(var(--fall-scaling-in-from));transform:scale(var(--fall-scaling-in-from))}75%{-webkit-transform:scale(1);transform:scale(1)}}@-webkit-keyframes rotateFallRight{0%{-webkit-transform:rotateZ(0deg);transform:rotate(0)}40%{-webkit-transform:rotateZ(var(--fall-animation-angle));transform:rotate(var(--fall-animation-angle))}60%{-webkit-transform:rotateZ(calc(var(--fall-animation-angle) - 2deg));transform:rotate(calc(var(--fall-animation-angle) - 2deg))}to{-webkit-transform:translateY(100%) rotateZ(calc(var(--fall-animation-angle) * 2));transform:translateY(100%) rotate(calc(var(--fall-animation-angle) * 2))}}@keyframes rotateFallRight{0%{-webkit-transform:rotateZ(0deg);transform:rotate(0)}40%{-webkit-transform:rotateZ(var(--fall-animation-angle));transform:rotate(var(--fall-animation-angle))}60%{-webkit-transform:rotateZ(calc(var(--fall-animation-angle) - 2deg));transform:rotate(calc(var(--fall-animation-angle) - 2deg))}to{-webkit-transform:translateY(100%) rotateZ(calc(var(--fall-animation-angle) * 2));transform:translateY(100%) rotate(calc(var(--fall-animation-angle) * 2))}}@-webkit-keyframes rotateFallLeft{0%{-webkit-transform:rotateZ(0deg);transform:rotate(0)}40%{-webkit-transform:rotateZ(calc(var(--fall-animation-angle) * -1));transform:rotate(calc(var(--fall-animation-angle) * -1))}60%{-webkit-transform:rotateZ(calc((var(--fall-animation-angle) - 2deg) * -1));transform:rotate(calc((var(--fall-animation-angle) - 2deg) * -1))}to{-webkit-transform:translateY(100%) rotateZ(calc(var(--fall-animation-angle) * -2));transform:translateY(100%) rotate(calc(var(--fall-animation-angle) * -2))}}@keyframes rotateFallLeft{0%{-webkit-transform:rotateZ(0deg);transform:rotate(0)}40%{-webkit-transform:rotateZ(calc(var(--fall-animation-angle) * -1));transform:rotate(calc(var(--fall-animation-angle) * -1))}60%{-webkit-transform:rotateZ(calc((var(--fall-animation-angle) - 2deg) * -1));transform:rotate(calc((var(--fall-animation-angle) - 2deg) * -1))}to{-webkit-transform:translateY(100%) rotateZ(calc(var(--fall-animation-angle) * -2));transform:translateY(100%) rotate(calc(var(--fall-animation-angle) * -2))}}._tag-container_13a7m_1{padding:3px 7px;border-radius:100rem;gap:6px;justify-content:center;color:var(--neutral-text-subtitle-color, #61616B);border:var(--neutral-bolder-border)}._tag-container_13a7m_1[class~=dashed]{border-style:dashed}._tag-container_13a7m_1[class~=size32]{padding:4px 11px;gap:8px}._tag-container_13a7m_1._type-button_13a7m_37{cursor:pointer}._tag-container_13a7m_1>._tag-label_13a7m_45{color:inherit;font:inherit;font-size:inherit;font-family:inherit;line-height:inherit;font-weight:inherit;text-align:inherit;letter-spacing:inherit}._tag-container_13a7m_1._disabled_13a7m_67{color:var(--neutral-text-disabled-color);background-color:var(--neutral-disable-background-color)!important;border-color:var(--neutral-disable-background-color)!important;pointer-events:none!important}._tag-container_13a7m_1[class~=tag-primary]{background-color:var(--primary-main-color)!important;border-color:var(--primary-main-color)!important}._tag-container_13a7m_1[class~=tag-primary] *{color:var(--neutral-text-stable-color)!important;--color: var(--neutral-text-stable-color) !important}._tag-container_13a7m_1[class~=tag-grey]{border-color:var(--neutral-bolder-border-color)!important;background-color:var(--neutral-main-background-color)!important}._tag-container_13a7m_1[class~=tag-grey] *{color:var(--neutral-text-subtitle-color)!important;--color: var(--neutral-text-subtitle-color) !important}._tag-container_13a7m_1[class~=tag-neutral]{border-color:var(--neutral-bolder-border-color)!important}._tag-container_13a7m_1[class~=tag-neutral] *{color:var(--neutral-text-subtitle-color)!important;--color: var(--neutral-text-subtitle-color) !important}._tag-container_13a7m_1[class~=tag-infor]{background-color:var(--infor-background);border-color:var(--infor-main-color)!important}._tag-container_13a7m_1[class~=tag-infor] *{color:var(--infor-main-color)!important;--color: var(--infor-main-color) !important}._tag-container_13a7m_1[class~=tag-warning]{background-color:var(--warning-background);border-color:var(--warning-main-color)!important}._tag-container_13a7m_1[class~=tag-warning] *{color:var(--warning-main-color)!important;--color: var(--warning-main-color) !important}._tag-container_13a7m_1[class~=tag-error]{background-color:var(--error-background);border-color:var(--error-main-color)!important}._tag-container_13a7m_1[class~=tag-error] *{color:var(--error-main-color)!important;--color: var(--error-main-color) !important}._tag-container_13a7m_1[class~=tag-success]{background-color:var(--success-background);border-color:var(--success-main-color)!important}._tag-container_13a7m_1[class~=tag-success] *{color:var(--success-main-color)!important;--color: var(--success-main-color) !important}._number-picker-container_26bbi_1{position:relative;align-items:stretch;gap:.4rem}._number-picker-container_26bbi_1[number-picker-type=outline]{border-radius:.8rem;padding:0 .6rem;border:var(--neutral-bolder-border)}._number-picker-container_26bbi_1>div:has(>svg){padding:.7rem;border:var(--neutral-bolder-border);border-radius:50%;aspect-ratio:1 / 1;height:100%;cursor:pointer}._number-picker-container_26bbi_1[number-picker-type=outline]>div:has(>svg){padding:.6rem}._number-picker-container_26bbi_1[class~=size40]>div>svg{width:1.6rem;height:1.6rem}._number-picker-container_26bbi_1[class~=size40]>div:has(>svg){padding:1.1rem}._number-picker-container_26bbi_1[class~=size40][number-picker-type=outline]>div:has(>svg){padding:1rem}._number-picker-container_26bbi_1[class~=size24]>div:has(>svg){padding:.5rem}._number-picker-container_26bbi_1[class~=size24][number-picker-type=outline]>div:has(>svg){padding:.4rem}._number-picker-container_26bbi_1[class~=size24]>div>svg{width:1.2rem;height:1.2rem}._number-picker-container_26bbi_1[number-picker-type=outline]>div:has(>svg){border-color:transparent;border-radius:0}._number-picker-container_26bbi_1>div>svg{height:100%;flex:1;aspect-ratio:1 / 1}._number-picker-container_26bbi_1>div>svg *{fill:var(--neutral-text-subtitle-color)}._number-picker-container_26bbi_1>input{flex:1;width:100%;min-width:5.6rem;border:var(--neutral-bolder-border);border-color:transparent;outline:none;padding:0 .4rem;background-color:transparent!important;font:inherit;color:inherit;font-size:inherit;font-family:inherit;font-weight:inherit;line-height:inherit;text-align:center;text-overflow:inherit}._number-picker-container_26bbi_1>input:focus{border-color:var(--primary-main-color);border-radius:.8rem}._number-picker-container_26bbi_1[number-picker-type=outline] input{border:none}._number-picker-container_26bbi_1[number-picker-type=outline]:focus-within{border-color:var(--primary-main-color)}._number-picker-container_26bbi_1:has(>input:disabled){pointer-events:none!important;color:var(--neutral-text-disabled-color)}._number-picker-container_26bbi_1[number-picker-type=outline]:has(>input:disabled){background-color:var(--neutral-disable-background-color)}._number-picker-container_26bbi_1:has(>input:disabled)>div:has(>svg){background-color:var(--neutral-disable-background-color)}._number-picker-container_26bbi_1:has(>input:disabled) svg *{fill:var(--neutral-text-disabled-color)}._number-picker-container_26bbi_1._helper-text_26bbi_231{overflow:visible!important;border-color:var(--helper-text-color)!important}._number-picker-container_26bbi_1._helper-text_26bbi_231:after{content:attr(helper-text);color:var(--helper-text-color);position:absolute;left:0;bottom:-.4rem;width:max-content;font-size:1.2rem;line-height:1.6rem;font-family:inherit;transform:translateY(100%)}._input-opt-container_gu2t8_1{height:4.8rem;gap:.8rem}._input-opt-container_gu2t8_1>input{border:var(--neutral-bolder-border);height:100%;aspect-ratio:1 / 1;border-radius:.8rem;text-align:center;min-width:3.2rem;font:inherit;font-size:inherit;font-family:inherit;font-weight:inherit;line-height:inherit;font-style:inherit}._input-opt-container_gu2t8_1:focus-within>input{border-color:var(--primary-main-color)}._input-opt-container_gu2t8_1._helper-text_gu2t8_49{overflow:visible!important;border-color:var(--helper-text-color)!important}._input-opt-container_gu2t8_1._helper-text_gu2t8_49:after{content:attr(helper-text);color:var(--helper-text-color);position:absolute;left:0;bottom:-.4rem;width:max-content;font-size:1.2rem;line-height:1.6rem;font-family:inherit;transform:translateY(100%)}._login-view-container_161cl_1{gap:4.8rem;align-items:center;min-width:320px;max-width:460px}._login-view-form-container_161cl_15{gap:2rem;align-items:inherit;width:100%}._login-view-form-container_161cl_15>div:last-child{padding:.4rem 3.2rem;gap:inherit;align-items:inherit;width:100%}._login-view-form-container_161cl_15>div:last-child>div{width:100%}button._login-btn_161cl_49{height:4.8rem;width:100%;justify-content:center;background-color:var(--primary-main-color);color:var(--neutral-text-stable-color)}button:disabled._login-btn_161cl_49{background-color:var(--neutral-disable-background-color);color:var(--neutral-text-disabled-color);cursor:context-menu}._forgot-password-btn_161cl_77{color:var(--neutral-text-subtitle-color)}._forgot-password-btn_161cl_77:hover,._register-btn_161cl_95:hover{text-decoration:underline;cursor:pointer}._login-social-media_161cl_105{gap:1.6rem;flex-wrap:wrap}._login-social-media_161cl_105>button{height:4.8rem;width:calc((100% - 1.6rem)/2);gap:1.6rem;justify-content:center;border:var(--neutral-bolder-border);background-color:transparent}._or-spacing_161cl_133{gap:1.2rem}._or-spacing_161cl_133>div:nth-child(odd){height:1px;width:100%;flex:1;background-color:var(--neutral-text-body-reverse-color)}._or-spacing_161cl_133>div:nth-child(2n){color:var(--neutral-text-subtitle-color)}.editor-container{position:relative;--ck-color-text: var(--neutral-text-title-color);width:100%;height:400px;max-height:600px;border-radius:.8rem;border:var(--neutral-bolder-border);overflow:hidden visible}.editor-container:has(.ck-read-only){background-color:var(--neutral-disable-background-color)!important;pointer-events:none!important}.editor-container:focus-within{border-color:var(--primary-main-color)}.editor-container.helper-text{overflow:visible!important;border-color:var(--helper-text-color)!important}.editor-container.helper-text:after{content:attr(helper-text);color:var(--helper-text-color);position:absolute;left:0;bottom:-.4rem;width:max-content;font-size:1.2rem;line-height:1.6rem;font-family:inherit;transform:translateY(100%)}.ck-content{font-family:Inter;line-height:1.6;word-break:break-word}.ck-content h3.category{font-family:Inter;font-size:20px;font-weight:700;color:#555;letter-spacing:10px;margin:0;padding:0}.ck-content h2.document-title{font-family:Inter;font-size:50px;font-weight:700;margin:0;padding:0;border:0}.ck-content h3.document-subtitle{font-family:Inter;font-size:20px;color:#555;margin:0 0 1em;font-weight:700;padding:0}.ck-content blockquote.side-quote{font-family:Inter;font-style:normal;float:right;width:35%;position:relative;border:0;overflow:visible;z-index:1;margin-left:1em}.ck-content blockquote.side-quote:before{content:"“";position:absolute;top:-37px;left:-10px;display:block;font-size:200px;color:#e7e7e7;z-index:-1;line-height:1}.ck-content blockquote.side-quote p{font-size:2em;line-height:1}.ck-content blockquote.side-quote p:last-child:not(:first-child){font-size:1.3em;text-align:right;color:#555}.ck-content span.marker{background:#ff0}.ck-content span.spoiler{background:#000;color:#000}.ck-content span.spoiler:hover{background:#000;color:#fff}.ck-content pre.fancy-code{border:0;margin-left:2em;margin-right:2em;border-radius:10px}.ck-content pre.fancy-code:before{content:"";display:block;height:13px;background:url(data:image/svg+xml;base64,);margin-bottom:8px;background-repeat:no-repeat}.ck-content pre.fancy-code-dark{background:#272822;color:#fff;box-shadow:5px 5px #0000001f}.ck-content pre.fancy-code-bright{background:#dddfe0;color:#000;box-shadow:5px 5px #b3b3b3}.editor-container_classic-editor .editor-container__editor .ck-editor .ck-editor__editable{border-radius:0}.ck-editor-container{--ck-font-size-base: 1.4rem}.editor-container__editor{flex:1;height:100%;overflow:hidden!important;display:flex;flex-direction:column}.editor-container__editor>div,.ck.ck-reset.ck-editor.ck-rounded-corners{flex:1;height:100%;overflow:hidden!important;display:flex;flex-direction:column}.ck.ck-editor__main>.ck.ck-content.ck-rounded-corners{min-height:100%}.ck.ck-toolbar{background-color:var(--neutral-absolute-background-color);border-radius:.8rem .8rem 0 0!important}.ck.ck-editor__top .ck-sticky-panel .ck-sticky-panel__content{height:40px;border:none!important;border-radius:.8rem .8rem 0 0!important;border-bottom:var(--neutral-bolder-border)!important}.ck.ck-editor__main{flex:1;height:100%!important;overflow:hidden auto!important}.ck.ck-editor__main>div{background-color:transparent!important;border:none!important;font-size:16px!important}.ck.ck-color-grid>button{border:var(--neutral-bolder-border);border-radius:.4rem!important}/**
* @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
* For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
*/:root{--ck-color-base-foreground: hsl(0, 0%, 98%);--ck-color-base-background: hsl(0, 0%, 100%);--ck-color-base-border: hsl(220, 6%, 81%);--ck-color-base-action: hsl(104, 50.2%, 42.5%);--ck-color-base-focus: hsl(209, 92%, 70%);--ck-color-base-text: hsl(0, 0%, 20%);--ck-color-base-active: hsl(218.1, 100%, 58%);--ck-color-base-active-focus: hsl(218.2, 100%, 52.5%);--ck-color-base-error: hsl(15, 100%, 43%);--ck-color-focus-border-coordinates: 218, 81.8%, 56.9%;--ck-color-focus-border: hsl(var(--ck-color-focus-border-coordinates));--ck-color-focus-outer-shadow: hsl(212.4, 89.3%, 89%);--ck-color-focus-disabled-shadow: hsla(209, 90%, 72%,.3);--ck-color-focus-error-shadow: hsla(9,100%,56%,.3);--ck-color-text: var(--ck-color-base-text);--ck-color-shadow-drop: hsla(0, 0%, 0%, .15);--ck-color-shadow-drop-active: hsla(0, 0%, 0%, .2);--ck-color-shadow-inner: hsla(0, 0%, 0%, .1);--ck-color-button-default-background: transparent;--ck-color-button-default-hover-background: hsl(0, 0%, 94.1%);--ck-color-button-default-active-background: hsl(0, 0%, 94.1%);--ck-color-button-default-disabled-background: transparent;--ck-color-button-on-background: hsl(212, 100%, 97.1%);--ck-color-button-on-hover-background: hsl(211.7, 100%, 92.9%);--ck-color-button-on-active-background: hsl(211.7, 100%, 92.9%);--ck-color-button-on-disabled-background: hsl(211, 15%, 95%);--ck-color-button-on-color: hsl(218.1, 100%, 58%);--ck-color-button-action-background: var(--ck-color-base-action);--ck-color-button-action-hover-background: hsl(104, 53.2%, 40.2%);--ck-color-button-action-active-background: hsl(104, 53.2%, 40.2%);--ck-color-button-action-disabled-background: hsl(104, 44%, 58%);--ck-color-button-action-text: var(--ck-color-base-background);--ck-color-button-save: hsl(120, 100%, 27%);--ck-color-button-cancel: hsl(15, 100%, 43%);--ck-color-switch-button-off-background: hsl(0, 0%, 57.6%);--ck-color-switch-button-off-hover-background: hsl(0, 0%, 49%);--ck-color-switch-button-on-background: var(--ck-color-button-action-background);--ck-color-switch-button-on-hover-background: hsl(104, 53.2%, 40.2%);--ck-color-switch-button-inner-background: var(--ck-color-base-background);--ck-color-switch-button-inner-shadow: hsla(0, 0%, 0%, .1);--ck-color-dropdown-panel-background: var(--ck-color-base-background);--ck-color-dropdown-panel-border: var(--ck-color-base-border);--ck-color-dialog-background: var(--ck-custom-background);--ck-color-dialog-form-header-border: var(--ck-custom-border);--ck-color-input-background: var(--ck-color-base-background);--ck-color-input-border: var(--ck-color-base-border);--ck-color-input-error-border: var(--ck-color-base-error);--ck-color-input-text: var(--ck-color-base-text);--ck-color-input-disabled-background: hsl(0, 0%, 95%);--ck-color-input-disabled-border: var(--ck-color-base-border);--ck-color-input-disabled-text: hsl(0, 0%, 46%);--ck-color-list-background: var(--ck-color-base-background);--ck-color-list-button-hover-background: var(--ck-color-button-default-hover-background);--ck-color-list-button-on-background: var(--ck-color-button-on-color);--ck-color-list-button-on-background-focus: var(--ck-color-button-on-color);--ck-color-list-button-on-text: var(--ck-color-base-background);--ck-color-panel-background: var(--ck-color-base-background);--ck-color-panel-border: var(--ck-color-base-border);--ck-color-toolbar-background: var(--ck-color-base-background);--ck-color-toolbar-border: var(--ck-color-base-border);--ck-color-tooltip-background: var(--ck-color-base-text);--ck-color-tooltip-text: var(--ck-color-base-background);--ck-color-engine-placeholder-text: hsl(0, 0%, 44%);--ck-color-upload-bar-background: hsl(209, 92%, 70%);--ck-color-link-default: hsl(240, 100%, 47%);--ck-color-link-selected-background: hsla(201, 100%, 56%, .1);--ck-color-link-fake-selection: hsla(201, 100%, 56%, .3);--ck-color-highlight-background: hsl(60, 100%, 50%);--ck-color-light-red: hsl(0, 100%, 90%)}:root{--ck-disabled-opacity: .5}:root{--ck-focus-outer-shadow-geometry: 0 0 0 3px;--ck-focus-outer-shadow: var(--ck-focus-outer-shadow-geometry) var(--ck-color-focus-outer-shadow);--ck-focus-disabled-outer-shadow: var(--ck-focus-outer-shadow-geometry) var(--ck-color-focus-disabled-shadow);--ck-focus-error-outer-shadow: var(--ck-focus-outer-shadow-geometry) var(--ck-color-focus-error-shadow);--ck-focus-ring: 1px solid var(--ck-color-focus-border)}:root{--ck-font-size-base: 13px;--ck-line-height-base: 1.84615;--ck-font-face: Helvetica, Arial, Tahoma, Verdana, Sans-Serif;--ck-font-size-tiny: .7em;--ck-font-size-small: .75em;--ck-font-size-normal: 1em;--ck-font-size-big: 1.4em;--ck-font-size-large: 1.8em}:root{--ck-ui-component-min-height: 2.3em}.ck.ck-reset,.ck.ck-reset_all,.ck-reset_all *:not(.ck-reset_all-excluded *){box-sizing:border-box;width:auto;height:auto;position:static;margin:0;padding:0;border:0;background:transparent;text-decoration:none;vertical-align:middle;transition:none;word-wrap:break-word}.ck.ck-reset_all,.ck-reset_all *:not(.ck-reset_all-excluded *){border-collapse:collapse;font:normal normal normal var(--ck-font-size-base)/var(--ck-line-height-base) var(--ck-font-face);color:var(--ck-color-text);text-align:left;white-space:nowrap;cursor:auto;float:none}.ck-reset_all .ck-rtl *:not(.ck-reset_all-excluded *){text-align:right}.ck-reset_all iframe:not(.ck-reset_all-excluded *){vertical-align:inherit}.ck-reset_all textarea:not(.ck-reset_all-excluded *){white-space:pre-wrap}.ck-reset_all textarea:not(.ck-reset_all-excluded *),.ck-reset_all input[type=text]:not(.ck-reset_all-excluded *),.ck-reset_all input[type=password]:not(.ck-reset_all-excluded *){cursor:text}.ck-reset_all textarea[disabled]:not(.ck-reset_all-excluded *),.ck-reset_all input[type=text][disabled]:not(.ck-reset_all-excluded *),.ck-reset_all input[type=password][disabled]:not(.ck-reset_all-excluded *){cursor:default}.ck-reset_all fieldset:not(.ck-reset_all-excluded *){padding:10px;border:2px groove hsl(255,7%,88%)}.ck-reset_all button:not(.ck-reset_all-excluded *)::-moz-focus-inner{padding:0;border:0}.ck[dir=rtl],.ck[dir=rtl] .ck{text-align:right}:root{--ck-border-radius: 2px}:root{--ck-inner-shadow: 2px 2px 3px var(--ck-color-shadow-inner) inset;--ck-drop-shadow: 0 1px 2px 1px var(--ck-color-shadow-drop);--ck-drop-shadow-active: 0 3px 6px 1px var(--ck-color-shadow-drop-active)}:root{--ck-spacing-unit: .6em;--ck-spacing-large: calc(var(--ck-spacing-unit) * 1.5);--ck-spacing-standard: var(--ck-spacing-unit);--ck-spacing-medium: calc(var(--ck-spacing-unit) * .8);--ck-spacing-small: calc(var(--ck-spacing-unit) * .5);--ck-spacing-tiny: calc(var(--ck-spacing-unit) * .3);--ck-spacing-extra-tiny: calc(var(--ck-spacing-unit) * .16)}.ck.ck-autocomplete>.ck-search__results{border-radius:0;max-height:200px;overflow-y:auto;background:var(--ck-color-base-background);border:1px solid var(--ck-color-dropdown-panel-border);min-width:auto}.ck-rounded-corners .ck.ck-autocomplete>.ck-search__results,.ck.ck-autocomplete>.ck-search__results.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-autocomplete>.ck-search__results{box-shadow:var(--ck-drop-shadow),0 0}.ck.ck-autocomplete>.ck-search__results.ck-search__results_n{border-bottom-left-radius:0;border-bottom-right-radius:0;margin-bottom:-1px}.ck.ck-autocomplete>.ck-search__results.ck-search__results_s{border-top-left-radius:0;border-top-right-radius:0;margin-top:-1px}.ck.ck-button,a.ck.ck-button{background:var(--ck-color-button-default-background);border-radius:0;white-space:nowrap;cursor:default;vertical-align:middle;padding:var(--ck-spacing-tiny);text-align:center;min-width:var(--ck-ui-component-min-height);min-height:var(--ck-ui-component-min-height);line-height:1;font-size:inherit;border:1px solid transparent;transition:box-shadow .2s ease-in-out,border .2s ease-in-out;-webkit-appearance:none}.ck.ck-button:not(.ck-disabled):hover,a.ck.ck-button:not(.ck-disabled):hover{background:var(--ck-color-button-default-hover-background)}.ck.ck-button:not(.ck-disabled):active,a.ck.ck-button:not(.ck-disabled):active{background:var(--ck-color-button-default-active-background)}.ck.ck-button.ck-disabled,a.ck.ck-button.ck-disabled{background:var(--ck-color-button-default-disabled-background)}.ck-rounded-corners .ck.ck-button,.ck-rounded-corners a.ck.ck-button,.ck.ck-button.ck-rounded-corners,a.ck.ck-button.ck-rounded-corners{border-radius:var(--ck-border-radius)}@media (prefers-reduced-motion: reduce){.ck.ck-button,a.ck.ck-button{transition:none}}.ck.ck-button:active,a.ck.ck-button:active,.ck.ck-button:focus,a.ck.ck-button:focus{outline:none;border:var(--ck-focus-ring);box-shadow:var(--ck-focus-outer-shadow),0 0}.ck.ck-button .ck-button__icon use,a.ck.ck-button .ck-button__icon use,.ck.ck-button .ck-button__icon use *,a.ck.ck-button .ck-button__icon use *{color:inherit}.ck.ck-button .ck-button__label,a.ck.ck-button .ck-button__label{font-size:inherit;font-weight:inherit;color:inherit;cursor:inherit;vertical-align:middle}[dir=ltr] .ck.ck-button .ck-button__label,[dir=ltr] a.ck.ck-button .ck-button__label{text-align:left}[dir=rtl] .ck.ck-button .ck-button__label,[dir=rtl] a.ck.ck-button .ck-button__label{text-align:right}.ck.ck-button .ck-button__keystroke,a.ck.ck-button .ck-button__keystroke{color:inherit;opacity:.5}[dir=ltr] .ck.ck-button .ck-button__keystroke,[dir=ltr] a.ck.ck-button .ck-button__keystroke{margin-left:var(--ck-spacing-large)}[dir=rtl] .ck.ck-button .ck-button__keystroke,[dir=rtl] a.ck.ck-button .ck-button__keystroke{margin-right:var(--ck-spacing-large)}.ck.ck-button.ck-disabled:active,a.ck.ck-button.ck-disabled:active,.ck.ck-button.ck-disabled:focus,a.ck.ck-button.ck-disabled:focus{box-shadow:var(--ck-focus-disabled-outer-shadow),0 0}.ck.ck-button.ck-disabled .ck-button__icon,a.ck.ck-button.ck-disabled .ck-button__icon,.ck.ck-button.ck-disabled .ck-button__label,a.ck.ck-button.ck-disabled .ck-button__label{opacity:var(--ck-disabled-opacity)}.ck.ck-button.ck-disabled .ck-button__keystroke,a.ck.ck-button.ck-disabled .ck-button__keystroke{opacity:.3}.ck.ck-button.ck-button_with-text,a.ck.ck-button.ck-button_with-text{padding:var(--ck-spacing-tiny) var(--ck-spacing-standard)}[dir=ltr] .ck.ck-button.ck-button_with-text .ck-button__icon,[dir=ltr] a.ck.ck-button.ck-button_with-text .ck-button__icon{margin-right:var(--ck-spacing-medium)}[dir=rtl] .ck.ck-button.ck-button_with-text .ck-button__icon,[dir=rtl] a.ck.ck-button.ck-button_with-text .ck-button__icon{margin-left:var(--ck-spacing-medium)}.ck.ck-button.ck-button_with-keystroke .ck-button__label,a.ck.ck-button.ck-button_with-keystroke .ck-button__label{flex-grow:1}.ck.ck-button.ck-on,a.ck.ck-button.ck-on{background:var(--ck-color-button-on-background);color:var(--ck-color-button-on-color)}.ck.ck-button.ck-on:not(.ck-disabled):hover,a.ck.ck-button.ck-on:not(.ck-disabled):hover{background:var(--ck-color-button-on-hover-background)}.ck.ck-button.ck-on:not(.ck-disabled):active,a.ck.ck-button.ck-on:not(.ck-disabled):active{background:var(--ck-color-button-on-active-background)}.ck.ck-button.ck-on.ck-disabled,a.ck.ck-button.ck-on.ck-disabled{background:var(--ck-color-button-on-disabled-background)}.ck.ck-button.ck-button-save,a.ck.ck-button.ck-button-save{color:var(--ck-color-button-save)}.ck.ck-button.ck-button-cancel,a.ck.ck-button.ck-button-cancel{color:var(--ck-color-button-cancel)}.ck.ck-button-action,a.ck.ck-button-action{background:var(--ck-color-button-action-background);color:var(--ck-color-button-action-text)}.ck.ck-button-action:not(.ck-disabled):hover,a.ck.ck-button-action:not(.ck-disabled):hover{background:var(--ck-color-button-action-hover-background)}.ck.ck-button-action:not(.ck-disabled):active,a.ck.ck-button-action:not(.ck-disabled):active{background:var(--ck-color-button-action-active-background)}.ck.ck-button-action.ck-disabled,a.ck.ck-button-action.ck-disabled{background:var(--ck-color-button-action-disabled-background)}.ck.ck-button-bold,a.ck.ck-button-bold{font-weight:700}:root{--ck-switch-button-toggle-width: 2.6153846154em;--ck-switch-button-toggle-inner-size: calc(1.0769230769em + 1px);--ck-switch-button-translation: calc( var(--ck-switch-button-toggle-width) - var(--ck-switch-button-toggle-inner-size) - 2px );--ck-switch-button-inner-hover-shadow: 0 0 0 5px var(--ck-color-switch-button-inner-shadow)}.ck.ck-button.ck-switchbutton,.ck.ck-button.ck-switchbutton:hover,.ck.ck-button.ck-switchbutton:focus,.ck.ck-button.ck-switchbutton:active,.ck.ck-button.ck-switchbutton.ck-on:hover,.ck.ck-button.ck-switchbutton.ck-on:focus,.ck.ck-button.ck-switchbutton.ck-on:active{color:inherit;background:transparent}[dir=ltr] .ck.ck-button.ck-switchbutton .ck-button__label{margin-right:calc(2 * var(--ck-spacing-large))}[dir=rtl] .ck.ck-button.ck-switchbutton .ck-button__label{margin-left:calc(2 * var(--ck-spacing-large))}.ck.ck-button.ck-switchbutton .ck-button__toggle{border-radius:0;transition:background .4s ease,box-shadow .2s ease-in-out,outline .2s ease-in-out;border:1px solid transparent;width:var(--ck-switch-button-toggle-width);background:var(--ck-color-switch-button-off-background)}.ck-rounded-corners .ck.ck-button.ck-switchbutton .ck-button__toggle,.ck.ck-button.ck-switchbutton .ck-button__toggle.ck-rounded-corners{border-radius:var(--ck-border-radius)}[dir=ltr] .ck.ck-button.ck-switchbutton .ck-button__toggle{margin-left:auto}[dir=rtl] .ck.ck-button.ck-switchbutton .ck-button__toggle{margin-right:auto}.ck.ck-button.ck-switchbutton .ck-button__toggle .ck-button__toggle__inner{border-radius:0;width:var(--ck-switch-button-toggle-inner-size);height:var(--ck-switch-button-toggle-inner-size);background:var(--ck-color-switch-button-inner-background);transition:all .3s ease}.ck-rounded-corners .ck.ck-button.ck-switchbutton .ck-button__toggle .ck-button__toggle__inner,.ck.ck-button.ck-switchbutton .ck-button__toggle .ck-button__toggle__inner.ck-rounded-corners{border-radius:var(--ck-border-radius);border-radius:calc(.5 * var(--ck-border-radius))}@media (prefers-reduced-motion: reduce){.ck.ck-button.ck-switchbutton .ck-button__toggle .ck-button__toggle__inner{transition:none}}.ck.ck-button.ck-switchbutton .ck-button__toggle:hover{background:var(--ck-color-switch-button-off-hover-background)}.ck.ck-button.ck-switchbutton .ck-button__toggle:hover .ck-button__toggle__inner{box-shadow:var(--ck-switch-button-inner-hover-shadow)}.ck.ck-button.ck-switchbutton.ck-disabled .ck-button__toggle{opacity:var(--ck-disabled-opacity)}.ck.ck-button.ck-switchbutton:focus{border-color:transparent;outline:none;box-shadow:none}.ck.ck-button.ck-switchbutton:focus .ck-button__toggle{box-shadow:0 0 0 1px var(--ck-color-base-background),0 0 0 5px var(--ck-color-focus-outer-shadow);outline-offset:1px;outline:var(--ck-focus-ring)}.ck.ck-button.ck-switchbutton.ck-on .ck-button__toggle{background:var(--ck-color-switch-button-on-background)}.ck.ck-button.ck-switchbutton.ck-on .ck-button__toggle:hover{background:var(--ck-color-switch-button-on-hover-background)}[dir=ltr] .ck.ck-button.ck-switchbutton.ck-on .ck-button__toggle .ck-button__toggle__inner{transform:translate(var( --ck-switch-button-translation ))}[dir=rtl] .ck.ck-button.ck-switchbutton.ck-on .ck-button__toggle .ck-button__toggle__inner{transform:translate(calc(-1 * var( --ck-switch-button-translation )))}.ck.ck-button.ck-list-item-button{padding:var(--ck-spacing-tiny) calc(2 * var(--ck-spacing-standard))}.ck.ck-button.ck-list-item-button,.ck.ck-button.ck-list-item-button.ck-on{background:var(--ck-color-list-background);color:var(--ck-color-text)}[dir=ltr] .ck.ck-button.ck-list-item-button:has(.ck-list-item-button__check-holder){padding-left:var(--ck-spacing-small)}[dir=rtl] .ck.ck-button.ck-list-item-button:has(.ck-list-item-button__check-holder){padding-right:var(--ck-spacing-small)}.ck.ck-button.ck-list-item-button:hover:not(.ck-disabled),.ck.ck-button.ck-list-item-button.ck-button.ck-on:hover,.ck.ck-button.ck-list-item-button.ck-on:not(.ck-list-item-button_toggleable),.ck.ck-button.ck-list-item-button.ck-on:hover{background:var(--ck-color-list-button-hover-background)}.ck.ck-button.ck-list-item-button:hover:not(.ck-disabled):not(.ck-disabled),.ck.ck-button.ck-list-item-button.ck-button.ck-on:hover:not(.ck-disabled),.ck.ck-button.ck-list-item-button.ck-on:not(.ck-list-item-button_toggleable):not(.ck-disabled),.ck.ck-button.ck-list-item-button.ck-on:hover:not(.ck-disabled){color:var(--ck-color-text)}:root{--ck-collapsible-arrow-size: calc(.5 * var(--ck-icon-size))}.ck.ck-collapsible>.ck.ck-button{width:100%;font-weight:700;border-radius:0;color:inherit}.ck.ck-collapsible>.ck.ck-button:focus{background:transparent}.ck.ck-collapsible>.ck.ck-button:active,.ck.ck-collapsible>.ck.ck-button:not(:focus),.ck.ck-collapsible>.ck.ck-button:hover:not(:focus){background:transparent;border-color:transparent;box-shadow:none}.ck.ck-collapsible>.ck.ck-button>.ck-icon{margin-right:var(--ck-spacing-medium);width:var(--ck-collapsible-arrow-size)}.ck.ck-collapsible>.ck-collapsible__children{padding:var(--ck-spacing-medium) var(--ck-spacing-large) var(--ck-spacing-large)}.ck.ck-collapsible.ck-collapsible_collapsed>.ck.ck-button .ck-icon{transform:rotate(-90deg)}:root{--ck-color-grid-tile-size: 24px;--ck-color-color-grid-check-icon: hsl(212, 81%, 46%)}.ck.ck-color-grid{grid-gap:5px;padding:8px}.ck.ck-color-grid__tile{transition:.2s ease box-shadow}@media (forced-colors: none){.ck.ck-color-grid__tile{width:var(--ck-color-grid-tile-size);height:var(--ck-color-grid-tile-size);min-width:var(--ck-color-grid-tile-size);min-height:var(--ck-color-grid-tile-size);padding:0;border:0}.ck.ck-color-grid__tile.ck-on,.ck.ck-color-grid__tile:focus:not(.ck-disabled),.ck.ck-color-grid__tile:hover:not(.ck-disabled){border:0}.ck.ck-color-grid__tile.ck-color-selector__color-tile_bordered{box-shadow:0 0 0 1px var(--ck-color-base-border)}.ck.ck-color-grid__tile.ck-on{box-shadow:inset 0 0 0 1px var(--ck-color-base-background),0 0 0 2px var(--ck-color-base-text)}.ck.ck-color-grid__tile:focus:not(.ck-disabled),.ck.ck-color-grid__tile:hover:not(.ck-disabled){box-shadow:inset 0 0 0 1px var(--ck-color-base-background),0 0 0 2px var(--ck-color-focus-border)}}@media (forced-colors: active){.ck.ck-color-grid__tile{width:unset;height:unset;min-width:unset;min-height:unset;padding:0 var(--ck-spacing-small)}.ck.ck-color-grid__tile .ck-button__label{display:inline-block}}@media (prefers-reduced-motion: reduce){.ck.ck-color-grid__tile{transition:none}}.ck.ck-color-grid__tile.ck-disabled{cursor:unset;transition:unset}.ck.ck-color-grid__tile .ck.ck-icon{display:none;color:var(--ck-color-color-grid-check-icon)}.ck.ck-color-grid__tile.ck-on .ck.ck-icon{display:block}.ck.ck-color-grid__label{padding:0 var(--ck-spacing-standard)}.ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__remove-color,.ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__color-picker{width:100%}.ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__color-picker{padding:calc(var(--ck-spacing-standard) / 2) var(--ck-spacing-standard);border-bottom-left-radius:0;border-bottom-right-radius:0}.ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__color-picker:not(:focus){border-top:1px solid var(--ck-color-base-border)}[dir=ltr] .ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__color-picker .ck.ck-icon{margin-right:var(--ck-spacing-standard)}[dir=rtl] .ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__color-picker .ck.ck-icon{margin-left:var(--ck-spacing-standard)}.ck.ck-color-selector .ck-color-grids-fragment label.ck.ck-color-grid__label{font-weight:unset}.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-picker{padding:8px}.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-picker .hex-color-picker{height:100px;min-width:180px}.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-picker .hex-color-picker::part(saturation){border-radius:var(--ck-border-radius) var(--ck-border-radius) 0 0}.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-picker .hex-color-picker::part(hue){border-radius:0 0 var(--ck-border-radius) var(--ck-border-radius)}.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-picker .hex-color-picker::part(saturation-pointer),.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-picker .hex-color-picker::part(hue-pointer){width:15px;height:15px}.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-selector_action-bar{padding:0 8px 8px}:root{--ck-dialog-overlay-background-color: hsla( 0, 0%, 0%, .5 );--ck-dialog-drop-shadow: 0px 0px 6px 2px hsl(0deg 0% 0% / 15%);--ck-dialog-max-width: 100vw;--ck-dialog-max-height: 90vh;--ck-color-dialog-background: var(--ck-color-base-background);--ck-color-dialog-form-header-border: var(--ck-color-base-border)}.ck.ck-dialog-overlay{animation:ck-dialog-fade-in .3s;background:var(--ck-dialog-overlay-background-color);z-index:var(--ck-z-dialog)}.ck.ck-dialog{border-radius:0;--ck-drop-shadow: var(--ck-dialog-drop-shadow);background:var(--ck-color-dialog-background);max-height:var(--ck-dialog-max-height);max-width:var(--ck-dialog-max-width);border:1px solid var(--ck-color-base-border);overscroll-behavior:contain}.ck-rounded-corners .ck.ck-dialog,.ck.ck-dialog.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-dialog{box-shadow:var(--ck-drop-shadow),0 0}.ck.ck-dialog .ck.ck-form__header{border-bottom:1px solid var(--ck-color-dialog-form-header-border)}.ck-dialog-scroll-locked{overflow:hidden}@keyframes ck-dialog-fade-in{0%{background:#0000}to{background:var(--ck-dialog-overlay-background-color)}}.ck.ck-dialog .ck.ck-dialog__actions{padding:var(--ck-spacing-large)}.ck.ck-dialog .ck.ck-dialog__actions>*+*{margin-left:var(--ck-spacing-large)}:root{--ck-dropdown-arrow-size: calc(.5 * var(--ck-icon-size))}.ck.ck-dropdown{font-size:inherit}.ck.ck-dropdown .ck-dropdown__arrow{width:var(--ck-dropdown-arrow-size)}[dir=ltr] .ck.ck-dropdown .ck-dropdown__arrow{right:var(--ck-spacing-standard);margin-left:var(--ck-spacing-standard)}[dir=rtl] .ck.ck-dropdown .ck-dropdown__arrow{left:var(--ck-spacing-standard);margin-right:var(--ck-spacing-small)}.ck.ck-dropdown.ck-disabled .ck-dropdown__arrow{opacity:var(--ck-disabled-opacity)}[dir=ltr] .ck.ck-dropdown .ck-button.ck-dropdown__button:not(.ck-button_with-text){padding-left:var(--ck-spacing-small)}[dir=rtl] .ck.ck-dropdown .ck-button.ck-dropdown__button:not(.ck-button_with-text){padding-right:var(--ck-spacing-small)}.ck.ck-dropdown .ck-button.ck-dropdown__button .ck-button__label{width:7em;overflow:hidden;text-overflow:ellipsis}.ck.ck-dropdown .ck-button.ck-dropdown__button.ck-disabled .ck-button__label{opacity:var(--ck-disabled-opacity)}.ck.ck-dropdown .ck-button.ck-dropdown__button.ck-on{border-bottom-left-radius:0;border-bottom-right-radius:0}.ck.ck-dropdown .ck-button.ck-dropdown__button.ck-dropdown__button_label-width_auto .ck-button__label{width:auto}.ck.ck-dropdown .ck-button.ck-dropdown__button.ck-off:active,.ck.ck-dropdown .ck-button.ck-dropdown__button.ck-on:active{box-shadow:none}.ck.ck-dropdown .ck-button.ck-dropdown__button.ck-off:active:focus,.ck.ck-dropdown .ck-button.ck-dropdown__button.ck-on:active:focus{box-shadow:var(--ck-focus-outer-shadow),0 0}.ck.ck-dropdown__panel{border-radius:0;background:var(--ck-color-dropdown-panel-background);border:1px solid var(--ck-color-dropdown-panel-border);bottom:0;min-width:100%}.ck-rounded-corners .ck.ck-dropdown__panel,.ck.ck-dropdown__panel.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-dropdown__panel{box-shadow:var(--ck-drop-shadow),0 0}.ck.ck-dropdown__panel.ck-dropdown__panel_se{border-top-left-radius:0}.ck.ck-dropdown__panel.ck-dropdown__panel_sw{border-top-right-radius:0}.ck.ck-dropdown__panel.ck-dropdown__panel_ne{border-bottom-left-radius:0}.ck.ck-dropdown__panel.ck-dropdown__panel_nw{border-bottom-right-radius:0}.ck.ck-dropdown__panel:focus{outline:none}.ck.ck-dropdown>.ck-dropdown__panel>.ck-list{border-radius:0}.ck-rounded-corners .ck.ck-dropdown>.ck-dropdown__panel>.ck-list,.ck.ck-dropdown>.ck-dropdown__panel>.ck-list.ck-rounded-corners{border-radius:var(--ck-border-radius);border-top-left-radius:0}.ck.ck-dropdown>.ck-dropdown__panel>.ck-list .ck-list__item:first-child>.ck-button{border-radius:0}.ck-rounded-corners .ck.ck-dropdown>.ck-dropdown__panel>.ck-list .ck-list__item:first-child>.ck-button,.ck.ck-dropdown>.ck-dropdown__panel>.ck-list .ck-list__item:first-child>.ck-button.ck-rounded-corners{border-radius:var(--ck-border-radius);border-top-left-radius:0;border-bottom-left-radius:0;border-bottom-right-radius:0}.ck.ck-dropdown>.ck-dropdown__panel>.ck-list .ck-list__item:last-child>.ck-button{border-radius:0}.ck-rounded-corners .ck.ck-dropdown>.ck-dropdown__panel>.ck-list .ck-list__item:last-child>.ck-button,.ck.ck-dropdown>.ck-dropdown__panel>.ck-list .ck-list__item:last-child>.ck-button.ck-rounded-corners{border-radius:var(--ck-border-radius);border-top-left-radius:0;border-top-right-radius:0}:root{--ck-color-split-button-hover-background: hsl(0, 0%, 92%);--ck-color-split-button-hover-border: hsl(0, 0%, 70%)}[dir=ltr] .ck.ck-splitbutton:hover>.ck-splitbutton__action,[dir=ltr] .ck.ck-splitbutton.ck-splitbutton_open>.ck-splitbutton__action{border-top-right-radius:unset;border-bottom-right-radius:unset}[dir=rtl] .ck.ck-splitbutton:hover>.ck-splitbutton__action,[dir=rtl] .ck.ck-splitbutton.ck-splitbutton_open>.ck-splitbutton__action{border-top-left-radius:unset;border-bottom-left-radius:unset}.ck.ck-splitbutton>.ck-splitbutton__arrow{min-width:unset}[dir=ltr] .ck.ck-splitbutton>.ck-splitbutton__arrow{border-top-left-radius:unset;border-bottom-left-radius:unset}[dir=rtl] .ck.ck-splitbutton>.ck-splitbutton__arrow{border-top-right-radius:unset;border-bottom-right-radius:unset}.ck.ck-splitbutton>.ck-splitbutton__arrow svg{width:var(--ck-dropdown-arrow-size)}.ck.ck-splitbutton>.ck-splitbutton__arrow:not(:focus){border-top-width:0px;border-bottom-width:0px}.ck.ck-splitbutton.ck-splitbutton_open{border-radius:0}.ck-rounded-corners .ck.ck-splitbutton.ck-splitbutton_open,.ck.ck-splitbutton.ck-splitbutton_open.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck-rounded-corners .ck.ck-splitbutton.ck-splitbutton_open>.ck-splitbutton__action,.ck.ck-splitbutton.ck-splitbutton_open.ck-rounded-corners>.ck-splitbutton__action{border-bottom-left-radius:0}.ck-rounded-corners .ck.ck-splitbutton.ck-splitbutton_open>.ck-splitbutton__arrow,.ck.ck-splitbutton.ck-splitbutton_open.ck-rounded-corners>.ck-splitbutton__arrow{border-bottom-right-radius:0}.ck.ck-splitbutton.ck-splitbutton_open>.ck-button:not(.ck-on):not(.ck-disabled):not(:hover),.ck.ck-splitbutton:hover>.ck-button:not(.ck-on):not(.ck-disabled):not(:hover){background:var(--ck-color-split-button-hover-background)}.ck.ck-splitbutton.ck-splitbutton_open>.ck-splitbutton__arrow:not(.ck-disabled):after,.ck.ck-splitbutton:hover>.ck-splitbutton__arrow:not(.ck-disabled):after{content:"";position:absolute;width:1px;height:100%;background-color:var(--ck-color-split-button-hover-border)}.ck.ck-splitbutton.ck-splitbutton_open>.ck-splitbutton__arrow:focus:after,.ck.ck-splitbutton:hover>.ck-splitbutton__arrow:focus:after{--ck-color-split-button-hover-border: var(--ck-color-focus-border)}[dir=ltr] .ck.ck-splitbutton.ck-splitbutton_open>.ck-splitbutton__arrow:not(.ck-disabled):after,[dir=ltr] .ck.ck-splitbutton:hover>.ck-splitbutton__arrow:not(.ck-disabled):after{left:-1px}[dir=rtl] .ck.ck-splitbutton.ck-splitbutton_open>.ck-splitbutton__arrow:not(.ck-disabled):after,[dir=rtl] .ck.ck-splitbutton:hover>.ck-splitbutton__arrow:not(.ck-disabled):after{right:-1px}.ck.ck-toolbar-dropdown .ck-toolbar{border:0}.ck.ck-button.ck-dropdown-menu-list__nested-menu__button{width:100%;padding:var(--ck-spacing-tiny) calc(2 * var(--ck-spacing-standard));border-radius:0}.ck.ck-button.ck-dropdown-menu-list__nested-menu__button:focus{border-color:transparent;box-shadow:none}.ck.ck-button.ck-dropdown-menu-list__nested-menu__button:focus:not(.ck-on){background:var(--ck-color-button-default-hover-background)}.ck.ck-button.ck-dropdown-menu-list__nested-menu__button>.ck-button__label{flex-grow:1;overflow:hidden;text-overflow:ellipsis}.ck.ck-button.ck-dropdown-menu-list__nested-menu__button.ck-disabled>.ck-button__label{opacity:var(--ck-disabled-opacity)}.ck.ck-button.ck-dropdown-menu-list__nested-menu__button.ck-icon-spacing:not(:has(.ck-button__icon))>.ck-button__label{margin-left:calc(var(--ck-icon-size) - var(--ck-spacing-small))}.ck.ck-button.ck-dropdown-menu-list__nested-menu__button>.ck-dropdown-menu-list__nested-menu__button__arrow{width:var(--ck-dropdown-arrow-size)}[dir=ltr] .ck.ck-button.ck-dropdown-menu-list__nested-menu__button>.ck-dropdown-menu-list__nested-menu__button__arrow{transform:rotate(-90deg);margin-right:calc(-1 * var(--ck-spacing-small))}[dir=rtl] .ck.ck-button.ck-dropdown-menu-list__nested-menu__button>.ck-dropdown-menu-list__nested-menu__button__arrow{transform:rotate(90deg);margin-left:calc(-1 * var(--ck-spacing-small))}.ck.ck-button.ck-dropdown-menu-list__nested-menu__button.ck-disabled>.ck-dropdown-menu-list__nested-menu__button__arrow{opacity:var(--ck-disabled-opacity)}[dir=ltr] .ck.ck-button.ck-dropdown-menu-list__nested-menu__button:not(.ck-button_with-text){padding-left:var(--ck-spacing-small)}[dir=ltr] .ck.ck-button.ck-dropdown-menu-list__nested-menu__button>.ck-dropdown-menu-list__nested-menu__button__arrow{right:var(--ck-spacing-standard);margin-left:var(--ck-spacing-standard)}[dir=rtl] .ck.ck-button.ck-dropdown-menu-list__nested-menu__button:not(.ck-button_with-text){padding-right:var(--ck-spacing-small)}[dir=rtl] .ck.ck-button.ck-dropdown-menu-list__nested-menu__button>.ck-dropdown-menu-list__nested-menu__button__arrow{left:var(--ck-spacing-standard);margin-right:var(--ck-spacing-small)}:root{--ck-dropdown-menu-menu-item-min-width: 18em}.ck.ck-dropdown-menu-list__nested-menu__item{min-width:var(--ck-dropdown-menu-menu-item-min-width)}.ck-button.ck-dropdown-menu-list__nested-menu__item__button{border-radius:0}.ck-button.ck-dropdown-menu-list__nested-menu__item__button>.ck-spinner-container,.ck-button.ck-dropdown-menu-list__nested-menu__item__button>.ck-spinner-container .ck-spinner{--ck-toolbar-spinner-size: 20px}.ck-button.ck-dropdown-menu-list__nested-menu__item__button>.ck-spinner-container{margin-left:calc(-1 * var(--ck-spacing-small));margin-right:var(--ck-spacing-small)}.ck-button.ck-dropdown-menu-list__nested-menu__item__button:focus{border-color:transparent;box-shadow:none}.ck-button.ck-dropdown-menu-list__nested-menu__item__button:focus:not(.ck-on){background:var(--ck-color-button-default-hover-background)}:root{--ck-dropdown-menu-menu-panel-max-width: 75vw}.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel{box-shadow:var(--ck-drop-shadow),0 0;background:var(--ck-color-dropdown-panel-background);border:1px solid var(--ck-color-dropdown-panel-border);bottom:0;height:fit-content;max-width:var(--ck-dropdown-menu-menu-panel-max-width)}.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel:after,.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel:before{display:none}.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel.ck-balloon-panel_es,.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel.ck-balloon-panel_se{border-top-left-radius:0}.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel.ck-balloon-panel_ws,.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel.ck-balloon-panel_sw{border-top-right-radius:0}.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel.ck-balloon-panel_en,.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel.ck-balloon-panel_ne{border-bottom-left-radius:0}.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel.ck-balloon-panel_wn,.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel.ck-balloon-panel_nw{border-bottom-right-radius:0}.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel:focus{outline:none}:root{--ck-accessibility-help-dialog-max-width: 600px;--ck-accessibility-help-dialog-max-height: 400px;--ck-accessibility-help-dialog-border-color: hsl(220, 6%, 81%);--ck-accessibility-help-dialog-code-background-color: hsl(0deg 0% 92.94%);--ck-accessibility-help-dialog-kbd-shadow-color: hsl(0deg 0% 61%)}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content{padding:var(--ck-spacing-large);max-width:var(--ck-accessibility-help-dialog-max-width);max-height:var(--ck-accessibility-help-dialog-max-height);overflow:auto;-webkit-user-select:text;user-select:text;border:1px solid transparent}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content:focus{outline:none;border:var(--ck-focus-ring);box-shadow:var(--ck-focus-outer-shadow),0 0}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content *{white-space:normal}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content .ck-label{display:none}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content h3{font-weight:700;font-size:1.2em}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content h4{font-weight:700;font-size:1em}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content p,.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content h3,.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content h4,.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content table{margin:1em 0}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content dl{display:grid;grid-template-columns:2fr 1fr;border-top:1px solid var(--ck-accessibility-help-dialog-border-color);border-bottom:none}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content dl dt,.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content dl dd{border-bottom:1px solid var(--ck-accessibility-help-dialog-border-color);padding:.4em 0}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content dl dt{grid-column-start:1}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content dl dd{grid-column-start:2;text-align:right}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content kbd,.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content code{display:inline-block;background:var(--ck-accessibility-help-dialog-code-background-color);padding:.4em;vertical-align:middle;line-height:1;border-radius:2px;text-align:center;font-size:.9em}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content code{font-family:monospace}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content kbd{min-width:1.8em;box-shadow:0 1px 1px var(--ck-accessibility-help-dialog-kbd-shadow-color);margin:0 1px}.ck.ck-accessibility-help-dialog .ck-accessibility-help-dialog__content kbd+kbd{margin-left:2px}:root{--ck-color-editable-blur-selection: hsl(0, 0%, 85%)}.ck.ck-editor__editable:not(.ck-editor__nested-editable){border-radius:0}.ck-rounded-corners .ck.ck-editor__editable:not(.ck-editor__nested-editable),.ck.ck-editor__editable.ck-rounded-corners:not(.ck-editor__nested-editable){border-radius:var(--ck-border-radius)}.ck.ck-editor__editable.ck-focused:not(.ck-editor__nested-editable){outline:none;border:var(--ck-focus-ring);box-shadow:var(--ck-inner-shadow),0 0}.ck.ck-editor__editable_inline{overflow:auto;padding:0 var(--ck-spacing-standard);border:1px solid transparent}.ck.ck-editor__editable_inline[dir=ltr]{text-align:left}.ck.ck-editor__editable_inline[dir=rtl]{text-align:right}.ck.ck-editor__editable_inline>*:first-child{margin-top:var(--ck-spacing-large)}.ck.ck-editor__editable_inline>*:last-child{margin-bottom:var(--ck-spacing-large)}.ck.ck-editor__editable_inline.ck-blurred ::selection{background:var(--ck-color-editable-blur-selection)}.ck.ck-balloon-panel.ck-toolbar-container[class*=arrow_n]:after{border-bottom-color:var(--ck-color-panel-background)}.ck.ck-balloon-panel.ck-toolbar-container[class*=arrow_s]:after{border-top-color:var(--ck-color-panel-background)}:root{--ck-form-header-height: 44px}.ck.ck-form__header{padding:var(--ck-spacing-small) var(--ck-spacing-large);height:var(--ck-form-header-height);line-height:var(--ck-form-header-height);border-bottom:1px solid var(--ck-color-base-border)}[dir=ltr] .ck.ck-form__header>.ck-icon{margin-right:var(--ck-spacing-medium)}[dir=rtl] .ck.ck-form__header>.ck-icon{margin-left:var(--ck-spacing-medium)}.ck.ck-form__header .ck-form__header__label{--ck-font-size-base: 15px;font-weight:700}:root{--ck-icon-size: calc(var(--ck-line-height-base) * var(--ck-font-size-normal));--ck-icon-font-size: .8333350694em}.ck.ck-icon{width:var(--ck-icon-size);height:var(--ck-icon-size);font-size:var(--ck-icon-font-size);cursor:inherit}.ck.ck-icon *{cursor:inherit}.ck.ck-icon.ck-icon_inherit-color,.ck.ck-icon.ck-icon_inherit-color *{color:inherit}.ck.ck-icon.ck-icon_inherit-color *:not([fill]){fill:currentColor}:root{--ck-input-width: 18em;--ck-input-text-width: var(--ck-input-width)}.ck.ck-input{border-radius:0;background:var(--ck-color-input-background);border:1px solid var(--ck-color-input-border);padding:var(--ck-spacing-extra-tiny) var(--ck-spacing-medium);min-width:var(--ck-input-width);min-height:var(--ck-ui-component-min-height);transition:box-shadow .1s ease-in-out,border .1s ease-in-out}.ck-rounded-corners .ck.ck-input,.ck.ck-input.ck-rounded-corners{border-radius:var(--ck-border-radius)}@media (prefers-reduced-motion: reduce){.ck.ck-input{transition:none}}.ck.ck-input:focus{outline:none;border:var(--ck-focus-ring);box-shadow:var(--ck-focus-outer-shadow),0 0}.ck.ck-input[readonly]{border:1px solid var(--ck-color-input-disabled-border);background:var(--ck-color-input-disabled-background);color:var(--ck-color-input-disabled-text)}.ck.ck-input[readonly]:focus{box-shadow:var(--ck-focus-disabled-outer-shadow),0 0}.ck.ck-input.ck-error{border-color:var(--ck-color-input-error-border);animation:ck-input-shake .3s ease both}@media (prefers-reduced-motion: reduce){.ck.ck-input.ck-error{animation:none}}.ck.ck-input.ck-error:focus{box-shadow:var(--ck-focus-error-outer-shadow),0 0}@keyframes ck-input-shake{20%{transform:translate(-2px)}40%{transform:translate(2px)}60%{transform:translate(-1px)}80%{transform:translate(1px)}}.ck.ck-label{font-weight:700}:root{--ck-labeled-field-view-transition: .1s cubic-bezier(0, 0, .24, .95);--ck-labeled-field-empty-unfocused-max-width: 100% - 2 * var(--ck-spacing-medium);--ck-labeled-field-label-default-position-x: var(--ck-spacing-medium);--ck-labeled-field-label-default-position-y: calc(.6 * var(--ck-font-size-base));--ck-color-labeled-field-label-background: var(--ck-color-base-background)}.ck.ck-labeled-field-view{border-radius:0}.ck-rounded-corners .ck.ck-labeled-field-view,.ck.ck-labeled-field-view.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-labeled-field-view>.ck.ck-labeled-field-view__input-wrapper{width:100%}.ck.ck-labeled-field-view>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label{top:0;pointer-events:none;background:var(--ck-color-labeled-field-label-background);padding:0 calc(.5 * var(--ck-font-size-tiny));line-height:initial;font-weight:400;text-overflow:ellipsis;overflow:hidden;max-width:100%;transition:transform var(--ck-labeled-field-view-transition),padding var(--ck-labeled-field-view-transition),background var(--ck-labeled-field-view-transition)}[dir=ltr] .ck.ck-labeled-field-view>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label{left:0;transform-origin:0 0;transform:translate(var(--ck-spacing-medium),-6px) scale(.75)}[dir=rtl] .ck.ck-labeled-field-view>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label{right:0;transform-origin:100% 0;transform:translate(calc(-1 * var(--ck-spacing-medium)),-6px) scale(.75)}@media (prefers-reduced-motion: reduce){.ck.ck-labeled-field-view>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label{transition:none}}.ck.ck-labeled-field-view.ck-error>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label{color:var(--ck-color-base-error)}.ck.ck-labeled-field-view.ck-error .ck-input:not([readonly])+.ck.ck-label{color:var(--ck-color-base-error)}.ck.ck-labeled-field-view .ck-labeled-field-view__status{font-size:var(--ck-font-size-small);margin-top:var(--ck-spacing-small);white-space:normal}.ck.ck-labeled-field-view .ck-labeled-field-view__status.ck-labeled-field-view__status_error{color:var(--ck-color-base-error)}.ck.ck-labeled-field-view.ck-disabled>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label,.ck.ck-labeled-field-view.ck-labeled-field-view_empty:not(.ck-labeled-field-view_focused)>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label{color:var(--ck-color-input-disabled-text)}.ck.ck-labeled-field-view.ck-disabled.ck-labeled-field-view_empty:not(.ck-labeled-field-view_placeholder)>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label,.ck.ck-labeled-field-view.ck-labeled-field-view_empty:not(.ck-labeled-field-view_focused):not(.ck-labeled-field-view_placeholder):not(.ck-error)>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label{max-width:calc(var(--ck-labeled-field-empty-unfocused-max-width));background:transparent;padding:0}[dir=ltr] .ck.ck-labeled-field-view.ck-disabled.ck-labeled-field-view_empty:not(.ck-labeled-field-view_placeholder)>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label,[dir=ltr] .ck.ck-labeled-field-view.ck-labeled-field-view_empty:not(.ck-labeled-field-view_focused):not(.ck-labeled-field-view_placeholder):not(.ck-error)>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label{transform:translate(var(--ck-labeled-field-label-default-position-x),var(--ck-labeled-field-label-default-position-y)) scale(1)}[dir=rtl] .ck.ck-labeled-field-view.ck-disabled.ck-labeled-field-view_empty:not(.ck-labeled-field-view_placeholder)>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label,[dir=rtl] .ck.ck-labeled-field-view.ck-labeled-field-view_empty:not(.ck-labeled-field-view_focused):not(.ck-labeled-field-view_placeholder):not(.ck-error)>.ck.ck-labeled-field-view__input-wrapper>.ck.ck-label{transform:translate(calc(-1 * var(--ck-labeled-field-label-default-position-x)),var(--ck-labeled-field-label-default-position-y)) scale(1)}.ck.ck-labeled-field-view>.ck.ck-labeled-field-view__input-wrapper>.ck-dropdown>.ck.ck-button{background:transparent}.ck.ck-labeled-field-view.ck-labeled-field-view_empty>.ck.ck-labeled-field-view__input-wrapper>.ck-dropdown>.ck-button>.ck-button__label{opacity:0}.ck.ck-labeled-field-view.ck-labeled-field-view_empty:not(.ck-labeled-field-view_focused):not(.ck-labeled-field-view_placeholder)>.ck.ck-labeled-field-view__input-wrapper>.ck-dropdown+.ck-label{max-width:calc(var(--ck-labeled-field-empty-unfocused-max-width) - var(--ck-dropdown-arrow-size) - var(--ck-spacing-standard))}.ck.ck-labeled-input .ck-labeled-input__status{font-size:var(--ck-font-size-small);margin-top:var(--ck-spacing-small);white-space:normal}.ck.ck-labeled-input .ck-labeled-input__status_error{color:var(--ck-color-base-error)}.ck.ck-list{border-radius:0;list-style-type:none;background:var(--ck-color-list-background);padding:var(--ck-spacing-small) 0}.ck-rounded-corners .ck.ck-list,.ck.ck-list.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-list__item{cursor:default;min-width:15em}.ck.ck-list__item>.ck-button:not(.ck-list-item-button){padding:var(--ck-spacing-tiny) calc(2 * var(--ck-spacing-standard));min-height:unset;width:100%;border-radius:0}[dir=ltr] .ck.ck-list__item>.ck-button:not(.ck-list-item-button){text-align:left}[dir=rtl] .ck.ck-list__item>.ck-button:not(.ck-list-item-button){text-align:right}.ck.ck-list__item>.ck-button:not(.ck-list-item-button) .ck-button__label{line-height:calc(var(--ck-line-height-base) * var(--ck-font-size-base))}.ck.ck-list__item>.ck-button:not(.ck-list-item-button):active{box-shadow:none}.ck.ck-list__item>.ck-button.ck-on:not(.ck-list-item-button){background:var(--ck-color-list-button-on-background);color:var(--ck-color-list-button-on-text)}.ck.ck-list__item>.ck-button.ck-on:not(.ck-list-item-button):active{box-shadow:none}.ck.ck-list__item>.ck-button.ck-on:not(.ck-list-item-button):hover:not(.ck-disabled){background:var(--ck-color-list-button-on-background-focus)}.ck.ck-list__item>.ck-button.ck-on:not(.ck-list-item-button):focus:not(.ck-disabled){border-color:var(--ck-color-base-background)}.ck.ck-list__item>.ck-button:not(.ck-list-item-button):hover:not(.ck-disabled){background:var(--ck-color-list-button-hover-background)}.ck.ck-list__item>.ck-button.ck-switchbutton.ck-on{background:var(--ck-color-list-background);color:inherit}.ck.ck-list__item>.ck-button.ck-switchbutton.ck-on:hover:not(.ck-disabled){background:var(--ck-color-list-button-hover-background);color:inherit}.ck-list .ck-list__group{padding-top:var(--ck-spacing-medium)}.ck-list .ck-list__group:first-child{padding-top:0}*:not(.ck-hidden)~.ck-list .ck-list__group{border-top:1px solid var(--ck-color-base-border)}.ck-list .ck-list__group>.ck-label{font-size:11px;font-weight:700;padding:var(--ck-spacing-medium) var(--ck-spacing-large) 0}.ck.ck-list__separator{height:1px;width:100%;background:var(--ck-color-base-border);margin:var(--ck-spacing-small) 0}.ck.ck-menu-bar{display:flex;flex-wrap:wrap;justify-content:flex-start;background:var(--ck-color-base-background);padding:var(--ck-spacing-small);gap:var(--ck-spacing-small);border:1px solid var(--ck-color-toolbar-border);width:100%}.ck.ck-menu-bar__menu{font-size:inherit}.ck.ck-menu-bar__menu.ck-menu-bar__menu_top-level{max-width:100%}.ck.ck-menu-bar__menu>.ck-menu-bar__menu__button{width:100%}.ck.ck-menu-bar__menu>.ck-menu-bar__menu__button>.ck-button__label{flex-grow:1;overflow:hidden;text-overflow:ellipsis}.ck.ck-menu-bar__menu>.ck-menu-bar__menu__button.ck-disabled>.ck-button__label{opacity:var(--ck-disabled-opacity)}[dir=ltr] .ck.ck-menu-bar__menu>.ck-menu-bar__menu__button:not(.ck-button_with-text){padding-left:var(--ck-spacing-small)}[dir=rtl] .ck.ck-menu-bar__menu>.ck-menu-bar__menu__button:not(.ck-button_with-text){padding-right:var(--ck-spacing-small)}.ck.ck-menu-bar__menu.ck-menu-bar__menu_top-level>.ck-menu-bar__menu__button{padding:var(--ck-spacing-small) var(--ck-spacing-medium);min-height:unset}.ck.ck-menu-bar__menu.ck-menu-bar__menu_top-level>.ck-menu-bar__menu__button .ck-button__label{width:unset;line-height:unset;overflow:visible}.ck.ck-menu-bar__menu.ck-menu-bar__menu_top-level>.ck-menu-bar__menu__button.ck-on{border-bottom-left-radius:0;border-bottom-right-radius:0}.ck.ck-menu-bar__menu.ck-menu-bar__menu_top-level>.ck-menu-bar__menu__button .ck-icon{display:none}.ck.ck-menu-bar__menu:not(.ck-menu-bar__menu_top-level) .ck-menu-bar__menu__button{border-radius:0}.ck.ck-menu-bar__menu:not(.ck-menu-bar__menu_top-level) .ck-menu-bar__menu__button>.ck-menu-bar__menu__button__arrow{width:var(--ck-dropdown-arrow-size)}[dir=ltr] .ck.ck-menu-bar__menu:not(.ck-menu-bar__menu_top-level) .ck-menu-bar__menu__button>.ck-menu-bar__menu__button__arrow{transform:rotate(-90deg);margin-left:var(--ck-spacing-standard);margin-right:calc(-1 * var(--ck-spacing-small))}[dir=rtl] .ck.ck-menu-bar__menu:not(.ck-menu-bar__menu_top-level) .ck-menu-bar__menu__button>.ck-menu-bar__menu__button__arrow{transform:rotate(90deg);left:var(--ck-spacing-standard);margin-right:var(--ck-spacing-small);margin-left:calc(-1 * var(--ck-spacing-small))}.ck.ck-menu-bar__menu:not(.ck-menu-bar__menu_top-level) .ck-menu-bar__menu__button.ck-disabled>.ck-menu-bar__menu__button__arrow{opacity:var(--ck-disabled-opacity)}:root{--ck-menu-bar-menu-item-min-width: 18em}.ck.ck-menu-bar__menu .ck.ck-menu-bar__menu__item{min-width:var(--ck-menu-bar-menu-item-min-width)}.ck.ck-menu-bar__menu .ck-button.ck-menu-bar__menu__item__button{border-radius:0}.ck.ck-menu-bar__menu .ck-button.ck-menu-bar__menu__item__button>.ck-spinner-container,.ck.ck-menu-bar__menu .ck-button.ck-menu-bar__menu__item__button>.ck-spinner-container .ck-spinner{--ck-toolbar-spinner-size: 20px}.ck.ck-menu-bar__menu .ck-button.ck-menu-bar__menu__item__button>.ck-spinner-container{font-size:var(--ck-icon-font-size)}[dir=ltr] .ck.ck-menu-bar__menu .ck-button.ck-menu-bar__menu__item__button>.ck-spinner-container{margin-right:var(--ck-spacing-medium)}[dir=rtl] .ck.ck-menu-bar__menu .ck-button.ck-menu-bar__menu__item__button>.ck-spinner-container{margin-left:var(--ck-spacing-medium)}:root{--ck-menu-bar-menu-panel-max-width: 75vw}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel{border-radius:0;background:var(--ck-color-dropdown-panel-background);border:1px solid var(--ck-color-dropdown-panel-border);bottom:0;height:fit-content;max-width:var(--ck-menu-bar-menu-panel-max-width)}.ck-rounded-corners .ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel{box-shadow:var(--ck-drop-shadow),0 0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_es,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_se{border-top-left-radius:0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_ws,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_sw{border-top-right-radius:0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_en,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_ne{border-bottom-left-radius:0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_wn,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_nw{border-bottom-right-radius:0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel:focus{outline:none}.ck.ck-menu-bar .ck-list-item-button:focus,.ck.ck-menu-bar .ck-list-item-button:active{border-color:transparent;box-shadow:none}.ck.ck-menu-bar.ck-menu-bar_focus-border-enabled .ck-list-item-button:focus,.ck.ck-menu-bar.ck-menu-bar_focus-border-enabled .ck-list-item-button:active{position:relative;z-index:2;outline:none;border:var(--ck-focus-ring);box-shadow:var(--ck-focus-outer-shadow),0 0}:root{--ck-balloon-border-width: 1px;--ck-balloon-arrow-offset: 2px;--ck-balloon-arrow-height: 10px;--ck-balloon-arrow-half-width: 8px;--ck-balloon-arrow-drop-shadow: 0 2px 2px var(--ck-color-shadow-drop)}.ck.ck-balloon-panel{border-radius:0;min-height:15px;background:var(--ck-color-panel-background);border:var(--ck-balloon-border-width) solid var(--ck-color-panel-border)}.ck-rounded-corners .ck.ck-balloon-panel,.ck.ck-balloon-panel.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-balloon-panel{box-shadow:var(--ck-drop-shadow),0 0}.ck.ck-balloon-panel.ck-balloon-panel_with-arrow:before,.ck.ck-balloon-panel.ck-balloon-panel_with-arrow:after{width:0;height:0;border-style:solid}.ck.ck-balloon-panel[class*=arrow_n]:before,.ck.ck-balloon-panel[class*=arrow_n]:after{border-width:0 var(--ck-balloon-arrow-half-width) var(--ck-balloon-arrow-height) var(--ck-balloon-arrow-half-width)}.ck.ck-balloon-panel[class*=arrow_n]:before{border-color:transparent transparent var(--ck-color-panel-border) transparent;margin-top:calc(-1 * var(--ck-balloon-border-width))}.ck.ck-balloon-panel[class*=arrow_n]:after{border-color:transparent transparent var(--ck-color-panel-background) transparent;margin-top:calc(var(--ck-balloon-arrow-offset) - var(--ck-balloon-border-width))}.ck.ck-balloon-panel[class*=arrow_s]:before,.ck.ck-balloon-panel[class*=arrow_s]:after{border-width:var(--ck-balloon-arrow-height) var(--ck-balloon-arrow-half-width) 0 var(--ck-balloon-arrow-half-width)}.ck.ck-balloon-panel[class*=arrow_s]:before{border-color:var(--ck-color-panel-border) transparent transparent;filter:drop-shadow(var(--ck-balloon-arrow-drop-shadow));margin-bottom:calc(-1 * var(--ck-balloon-border-width))}.ck.ck-balloon-panel[class*=arrow_s]:after{border-color:var(--ck-color-panel-background) transparent transparent transparent;margin-bottom:calc(var(--ck-balloon-arrow-offset) - var(--ck-balloon-border-width))}.ck.ck-balloon-panel[class*=arrow_e]:before,.ck.ck-balloon-panel[class*=arrow_e]:after{border-width:var(--ck-balloon-arrow-half-width) 0 var(--ck-balloon-arrow-half-width) var(--ck-balloon-arrow-height)}.ck.ck-balloon-panel[class*=arrow_e]:before{border-color:transparent transparent transparent var(--ck-color-panel-border);margin-right:calc(-1 * var(--ck-balloon-border-width))}.ck.ck-balloon-panel[class*=arrow_e]:after{border-color:transparent transparent transparent var(--ck-color-panel-background);margin-right:calc(var(--ck-balloon-arrow-offset) - var(--ck-balloon-border-width))}.ck.ck-balloon-panel[class*=arrow_w]:before,.ck.ck-balloon-panel[class*=arrow_w]:after{border-width:var(--ck-balloon-arrow-half-width) var(--ck-balloon-arrow-height) var(--ck-balloon-arrow-half-width) 0}.ck.ck-balloon-panel[class*=arrow_w]:before{border-color:transparent var(--ck-color-panel-border) transparent transparent;margin-left:calc(-1 * var(--ck-balloon-border-width))}.ck.ck-balloon-panel[class*=arrow_w]:after{border-color:transparent var(--ck-color-panel-background) transparent transparent;margin-left:calc(var(--ck-balloon-arrow-offset) - var(--ck-balloon-border-width))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_n:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_n:after{left:50%;margin-left:calc(-1 * var(--ck-balloon-arrow-half-width));top:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_nw:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_nw:after{left:calc(2 * var(--ck-balloon-arrow-half-width));top:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_ne:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_ne:after{right:calc(2 * var(--ck-balloon-arrow-half-width));top:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_s:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_s:after{left:50%;margin-left:calc(-1 * var(--ck-balloon-arrow-half-width));bottom:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_sw:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_sw:after{left:calc(2 * var(--ck-balloon-arrow-half-width));bottom:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_se:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_se:after{right:calc(2 * var(--ck-balloon-arrow-half-width));bottom:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_sme:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_sme:after{right:25%;margin-right:calc(2 * var(--ck-balloon-arrow-half-width));bottom:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_smw:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_smw:after{left:25%;margin-left:calc(2 * var(--ck-balloon-arrow-half-width));bottom:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_nme:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_nme:after{right:25%;margin-right:calc(2 * var(--ck-balloon-arrow-half-width));top:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_nmw:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_nmw:after{left:25%;margin-left:calc(2 * var(--ck-balloon-arrow-half-width));top:calc(-1 * var(--ck-balloon-arrow-height))}.ck.ck-balloon-panel.ck-balloon-panel_arrow_e:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_e:after{right:calc(-1 * var(--ck-balloon-arrow-height));margin-top:calc(-1 * var(--ck-balloon-arrow-half-width));top:50%}.ck.ck-balloon-panel.ck-balloon-panel_arrow_w:before,.ck.ck-balloon-panel.ck-balloon-panel_arrow_w:after{left:calc(-1 * var(--ck-balloon-arrow-height));margin-top:calc(-1 * var(--ck-balloon-arrow-half-width));top:50%}.ck .ck-balloon-rotator__navigation{background:var(--ck-color-toolbar-background);border-bottom:1px solid var(--ck-color-toolbar-border);padding:0 var(--ck-spacing-small)}.ck .ck-balloon-rotator__navigation>*{margin-right:var(--ck-spacing-small);margin-top:var(--ck-spacing-small);margin-bottom:var(--ck-spacing-small)}.ck .ck-balloon-rotator__navigation .ck-balloon-rotator__counter{margin-right:var(--ck-spacing-standard);margin-left:var(--ck-spacing-small)}.ck .ck-balloon-rotator__content .ck.ck-annotation-wrapper{box-shadow:none}:root{--ck-balloon-fake-panel-offset-horizontal: 6px;--ck-balloon-fake-panel-offset-vertical: 6px}.ck .ck-fake-panel div{box-shadow:var(--ck-drop-shadow),0 0;min-height:15px;background:var(--ck-color-panel-background);border:1px solid var(--ck-color-panel-border);border-radius:var(--ck-border-radius);width:100%;height:100%}.ck .ck-fake-panel div:nth-child(1){margin-left:var(--ck-balloon-fake-panel-offset-horizontal);margin-top:var(--ck-balloon-fake-panel-offset-vertical)}.ck .ck-fake-panel div:nth-child(2){margin-left:calc(var(--ck-balloon-fake-panel-offset-horizontal) * 2);margin-top:calc(var(--ck-balloon-fake-panel-offset-vertical) * 2)}.ck .ck-fake-panel div:nth-child(3){margin-left:calc(var(--ck-balloon-fake-panel-offset-horizontal) * 3);margin-top:calc(var(--ck-balloon-fake-panel-offset-vertical) * 3)}.ck .ck-balloon-panel_arrow_s+.ck-fake-panel,.ck .ck-balloon-panel_arrow_se+.ck-fake-panel,.ck .ck-balloon-panel_arrow_sw+.ck-fake-panel{--ck-balloon-fake-panel-offset-vertical: -6px}.ck.ck-sticky-panel .ck-sticky-panel__content_sticky{box-shadow:var(--ck-drop-shadow),0 0;border-width:0 1px 1px;border-top-left-radius:0;border-top-right-radius:0}.ck-vertical-form>.ck-button:nth-last-child(2):after{border-right:1px solid var(--ck-color-base-border)}.ck.ck-responsive-form{padding:var(--ck-spacing-large)}.ck.ck-responsive-form:focus{outline:none}[dir=ltr] .ck.ck-responsive-form>:not(:first-child){margin-left:var(--ck-spacing-standard)}[dir=rtl] .ck.ck-responsive-form>:not(:last-child){margin-left:var(--ck-spacing-standard)}@media screen and (max-width: 600px){.ck.ck-responsive-form{padding:0;width:calc(.8 * var(--ck-input-width))}.ck.ck-responsive-form .ck-labeled-field-view{margin:var(--ck-spacing-large) var(--ck-spacing-large) 0}.ck.ck-responsive-form .ck-labeled-field-view .ck-input-text,.ck.ck-responsive-form .ck-labeled-field-view .ck-input-number{min-width:0;width:100%}.ck.ck-responsive-form .ck-labeled-field-view .ck-labeled-field-view__error{white-space:normal}.ck.ck-responsive-form>.ck-button:nth-last-child(2):after{border-right:1px solid var(--ck-color-base-border)}.ck.ck-responsive-form>.ck-button:nth-last-child(1),.ck.ck-responsive-form>.ck-button:nth-last-child(2){padding:var(--ck-spacing-standard);margin-top:var(--ck-spacing-large);border-radius:0}.ck.ck-responsive-form>.ck-button:nth-last-child(1):not(:focus),.ck.ck-responsive-form>.ck-button:nth-last-child(2):not(:focus){border-top:1px solid var(--ck-color-base-border)}[dir=ltr] .ck.ck-responsive-form>.ck-button:nth-last-child(1),[dir=ltr] .ck.ck-responsive-form>.ck-button:nth-last-child(2){margin-left:0}[dir=rtl] .ck.ck-responsive-form>.ck-button:nth-last-child(1),[dir=rtl] .ck.ck-responsive-form>.ck-button:nth-last-child(2){margin-left:0}[dir=rtl] .ck.ck-responsive-form>.ck-button:nth-last-child(1):last-of-type,[dir=rtl] .ck.ck-responsive-form>.ck-button:nth-last-child(2):last-of-type{border-right:1px solid var(--ck-color-base-border)}}:root{--ck-search-field-view-horizontal-spacing: calc(var(--ck-icon-size) + var(--ck-spacing-medium))}.ck.ck-search>.ck-labeled-field-view .ck-input{width:100%}.ck.ck-search>.ck-labeled-field-view.ck-search__query_with-icon{--ck-labeled-field-label-default-position-x: var(--ck-search-field-view-horizontal-spacing)}.ck.ck-search>.ck-labeled-field-view.ck-search__query_with-icon>.ck-labeled-field-view__input-wrapper>.ck-icon{opacity:.5;pointer-events:none}.ck.ck-search>.ck-labeled-field-view.ck-search__query_with-icon .ck-input{width:100%}[dir=ltr] .ck.ck-search>.ck-labeled-field-view.ck-search__query_with-icon .ck-input{padding-left:var(--ck-search-field-view-horizontal-spacing)}[dir=rtl] .ck.ck-search>.ck-labeled-field-view.ck-search__query_with-icon .ck-input:not(.ck-input-text_empty){padding-left:var(--ck-search-field-view-horizontal-spacing)}.ck.ck-search>.ck-labeled-field-view.ck-search__query_with-reset{--ck-labeled-field-empty-unfocused-max-width: 100% - 2 * var(--ck-search-field-view-horizontal-spacing)}.ck.ck-search>.ck-labeled-field-view.ck-search__query_with-reset.ck-labeled-field-view_empty{--ck-labeled-field-empty-unfocused-max-width: 100% - var(--ck-search-field-view-horizontal-spacing) - var(--ck-spacing-medium)}.ck.ck-search>.ck-labeled-field-view.ck-search__query_with-reset .ck-search__reset{min-width:auto;min-height:auto;background:none;opacity:.5;padding:0}[dir=ltr] .ck.ck-search>.ck-labeled-field-view.ck-search__query_with-reset .ck-search__reset{right:var(--ck-spacing-medium)}[dir=rtl] .ck.ck-search>.ck-labeled-field-view.ck-search__query_with-reset .ck-search__reset{left:var(--ck-spacing-medium)}.ck.ck-search>.ck-labeled-field-view.ck-search__query_with-reset .ck-search__reset:hover{opacity:1}.ck.ck-search>.ck-labeled-field-view.ck-search__query_with-reset .ck-input{width:100%}[dir=ltr] .ck.ck-search>.ck-labeled-field-view.ck-search__query_with-reset .ck-input:not(.ck-input-text_empty){padding-right:var(--ck-search-field-view-horizontal-spacing)}[dir=rtl] .ck.ck-search>.ck-labeled-field-view.ck-search__query_with-reset .ck-input{padding-right:var(--ck-search-field-view-horizontal-spacing)}.ck.ck-search>.ck-search__results{min-width:100%}.ck.ck-search>.ck-search__results>.ck-search__info{width:100%;padding:var(--ck-spacing-medium) var(--ck-spacing-large)}.ck.ck-search>.ck-search__results>.ck-search__info *{white-space:normal}.ck.ck-search>.ck-search__results>.ck-search__info>span:first-child{font-weight:700}.ck.ck-search>.ck-search__results>.ck-search__info>span:last-child{margin-top:var(--ck-spacing-medium)}.ck.ck-spinner-container{width:var(--ck-toolbar-spinner-size);height:var(--ck-toolbar-spinner-size);animation:1.5s infinite ck-spinner-rotate linear}@media (prefers-reduced-motion: reduce){.ck.ck-spinner-container{animation-duration:3s}}.ck.ck-spinner{width:var(--ck-toolbar-spinner-size);height:var(--ck-toolbar-spinner-size);border-radius:50%;border:2px solid var(--ck-color-text);border-top-color:transparent}@keyframes ck-spinner-rotate{to{transform:rotate(360deg)}}.ck-textarea{overflow-x:hidden}:root{--ck-color-block-toolbar-button: var(--ck-color-text);--ck-block-toolbar-button-size: var(--ck-font-size-normal)}.ck.ck-block-toolbar-button{color:var(--ck-color-block-toolbar-button);font-size:var(--ck-block-toolbar-size)}.ck.ck-toolbar{border-radius:0;background:var(--ck-color-toolbar-background);padding:0 var(--ck-spacing-small);border:1px solid var(--ck-color-toolbar-border)}.ck-rounded-corners .ck.ck-toolbar,.ck.ck-toolbar.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-toolbar .ck.ck-toolbar__separator{height:var(--ck-icon-size);width:1px;min-width:1px;background:var(--ck-color-toolbar-border);margin-top:var(--ck-spacing-small);margin-bottom:var(--ck-spacing-small)}.ck.ck-toolbar .ck-toolbar__line-break{height:0}.ck.ck-toolbar>.ck-toolbar__items>*:not(.ck-toolbar__line-break){margin-right:var(--ck-spacing-small)}.ck.ck-toolbar>.ck-toolbar__items:empty+.ck.ck-toolbar__separator{display:none}.ck.ck-toolbar>.ck-toolbar__items>*:not(.ck-toolbar__line-break),.ck.ck-toolbar>.ck.ck-toolbar__grouped-dropdown{margin-top:var(--ck-spacing-small);margin-bottom:var(--ck-spacing-small)}.ck.ck-toolbar.ck-toolbar_vertical{padding:0}.ck.ck-toolbar.ck-toolbar_vertical>.ck-toolbar__items>.ck{width:100%;margin:0;border-radius:0}.ck.ck-toolbar.ck-toolbar_compact{padding:0}.ck.ck-toolbar.ck-toolbar_compact>.ck-toolbar__items>*{margin:0}.ck.ck-toolbar.ck-toolbar_compact>.ck-toolbar__items>*:not(:first-child):not(:last-child){border-radius:0}.ck.ck-toolbar>.ck.ck-toolbar__grouped-dropdown>.ck.ck-button.ck-dropdown__button{padding-left:var(--ck-spacing-tiny)}.ck.ck-toolbar .ck-toolbar__nested-toolbar-dropdown>.ck-dropdown__panel{min-width:auto}.ck.ck-toolbar .ck-toolbar__nested-toolbar-dropdown>.ck-button>.ck-button__label{max-width:7em;width:auto}.ck.ck-toolbar:focus{outline:none}.ck-toolbar-container .ck.ck-toolbar{border:0}[dir=rtl] .ck.ck-toolbar>.ck-toolbar__items>.ck,.ck.ck-toolbar[dir=rtl]>.ck-toolbar__items>.ck{margin-right:0}[dir=rtl] .ck.ck-toolbar:not(.ck-toolbar_compact)>.ck-toolbar__items>.ck,.ck.ck-toolbar[dir=rtl]:not(.ck-toolbar_compact)>.ck-toolbar__items>.ck{margin-left:var(--ck-spacing-small)}[dir=rtl] .ck.ck-toolbar>.ck-toolbar__items>.ck:last-child,.ck.ck-toolbar[dir=rtl]>.ck-toolbar__items>.ck:last-child{margin-left:0}[dir=rtl] .ck.ck-toolbar.ck-toolbar_compact>.ck-toolbar__items>.ck:first-child,.ck.ck-toolbar.ck-toolbar_compact[dir=rtl]>.ck-toolbar__items>.ck:first-child{border-top-left-radius:0;border-bottom-left-radius:0}[dir=rtl] .ck.ck-toolbar.ck-toolbar_compact>.ck-toolbar__items>.ck:last-child,.ck.ck-toolbar.ck-toolbar_compact[dir=rtl]>.ck-toolbar__items>.ck:last-child{border-top-right-radius:0;border-bottom-right-radius:0}[dir=rtl] .ck.ck-toolbar>.ck.ck-toolbar__separator,.ck.ck-toolbar[dir=rtl]>.ck.ck-toolbar__separator{margin-left:var(--ck-spacing-small)}[dir=rtl] .ck.ck-toolbar.ck-toolbar_grouping>.ck-toolbar__items:not(:empty):not(:only-child),.ck.ck-toolbar.ck-toolbar_grouping[dir=rtl]>.ck-toolbar__items:not(:empty):not(:only-child){margin-left:var(--ck-spacing-small)}[dir=ltr] .ck.ck-toolbar>.ck-toolbar__items>.ck:last-child,.ck.ck-toolbar[dir=ltr]>.ck-toolbar__items>.ck:last-child{margin-right:0}[dir=ltr] .ck.ck-toolbar.ck-toolbar_compact>.ck-toolbar__items>.ck:first-child,.ck.ck-toolbar.ck-toolbar_compact[dir=ltr]>.ck-toolbar__items>.ck:first-child{border-top-right-radius:0;border-bottom-right-radius:0}[dir=ltr] .ck.ck-toolbar.ck-toolbar_compact>.ck-toolbar__items>.ck:last-child,.ck.ck-toolbar.ck-toolbar_compact[dir=ltr]>.ck-toolbar__items>.ck:last-child{border-top-left-radius:0;border-bottom-left-radius:0}[dir=ltr] .ck.ck-toolbar>.ck.ck-toolbar__separator,.ck.ck-toolbar[dir=ltr]>.ck.ck-toolbar__separator{margin-right:var(--ck-spacing-small)}[dir=ltr] .ck.ck-toolbar.ck-toolbar_grouping>.ck-toolbar__items:not(:empty):not(:only-child),.ck.ck-toolbar.ck-toolbar_grouping[dir=ltr]>.ck-toolbar__items:not(:empty):not(:only-child){margin-right:var(--ck-spacing-small)}.ck.ck-balloon-panel.ck-tooltip{--ck-balloon-border-width: 0px;--ck-balloon-arrow-offset: 0px;--ck-balloon-arrow-half-width: 4px;--ck-balloon-arrow-height: 4px;--ck-tooltip-text-padding: 4px;--ck-color-panel-background: var(--ck-color-tooltip-background);padding:0 var(--ck-spacing-medium);box-shadow:none}.ck.ck-balloon-panel.ck-tooltip .ck-tooltip__text{font-size:.9em;line-height:1.5;color:var(--ck-color-tooltip-text)}.ck.ck-balloon-panel.ck-tooltip.ck-tooltip_multi-line .ck-tooltip__text{white-space:break-spaces;display:inline-block;padding:var(--ck-tooltip-text-padding) 0;max-width:200px}.ck.ck-balloon-panel.ck-tooltip:before{display:none}.ck.ck-editor__top .ck-sticky-panel .ck-sticky-panel__content{border-radius:0;border:1px solid var(--ck-color-base-border);border-bottom-width:0}.ck-rounded-corners .ck.ck-editor__top .ck-sticky-panel .ck-sticky-panel__content,.ck.ck-editor__top .ck-sticky-panel .ck-sticky-panel__content.ck-rounded-corners{border-radius:var(--ck-border-radius);border-bottom-left-radius:0;border-bottom-right-radius:0}.ck.ck-editor__top .ck-sticky-panel .ck-sticky-panel__content.ck-sticky-panel__content_sticky{border-bottom-width:1px}.ck.ck-editor__top .ck-sticky-panel .ck-sticky-panel__content .ck-menu-bar{border:0;border-bottom:1px solid var(--ck-color-base-border)}.ck.ck-editor__top .ck-sticky-panel .ck-sticky-panel__content .ck-toolbar{border:0}.ck.ck-editor__main>.ck-editor__editable{background:var(--ck-color-base-background);border-radius:0}.ck-rounded-corners .ck.ck-editor__main>.ck-editor__editable,.ck.ck-editor__main>.ck-editor__editable.ck-rounded-corners{border-radius:var(--ck-border-radius);border-top-left-radius:0;border-top-right-radius:0}.ck.ck-editor__main>.ck-editor__editable:not(.ck-focused){border-color:var(--ck-color-base-border)}.ck .ck-fake-bookmark-selection{background:var(--ck-color-link-fake-selection)}.ck .ck-fake-bookmark-selection_collapsed{height:100%;border-right:1px solid var(--ck-color-base-text);margin-right:-1px;outline:solid 1px hsla(0,0%,100%,.5)}:root{--ck-clipboard-drop-target-dot-width: 12px;--ck-clipboard-drop-target-dot-height: 8px;--ck-clipboard-drop-target-color: var(--ck-color-focus-border)}.ck.ck-editor__editable .ck.ck-clipboard-drop-target-position span{bottom:calc(-.5 * var(--ck-clipboard-drop-target-dot-height));top:calc(-.5 * var(--ck-clipboard-drop-target-dot-height));border:1px solid var(--ck-clipboard-drop-target-color);background:var(--ck-clipboard-drop-target-color);margin-left:-1px}.ck.ck-editor__editable .ck.ck-clipboard-drop-target-position span:after{content:"";width:0;height:0;display:block;position:absolute;left:50%;top:calc(-.5 * var(--ck-clipboard-drop-target-dot-height));transform:translate(-50%);border-color:var(--ck-clipboard-drop-target-color) transparent transparent transparent;border-width:calc(var(--ck-clipboard-drop-target-dot-height)) calc(.5 * var(--ck-clipboard-drop-target-dot-width)) 0 calc(.5 * var(--ck-clipboard-drop-target-dot-width));border-style:solid}.ck.ck-editor__editable .ck-widget.ck-clipboard-drop-target-range{outline:var(--ck-widget-outline-thickness) solid var(--ck-clipboard-drop-target-color)!important}.ck.ck-editor__editable .ck-widget:-webkit-drag{zoom:.6;outline:none!important}.ck.ck-clipboard-drop-target-line{height:0;border:1px solid var(--ck-clipboard-drop-target-color);background:var(--ck-clipboard-drop-target-color);margin-top:-1px}.ck.ck-clipboard-drop-target-line:before{content:"";position:absolute;top:calc(-.5 * var(--ck-clipboard-drop-target-dot-width));width:0;height:0;border-style:solid}[dir=ltr] .ck.ck-clipboard-drop-target-line:before{left:-1px;border-width:calc(.5 * var(--ck-clipboard-drop-target-dot-width)) 0 calc(.5 * var(--ck-clipboard-drop-target-dot-width)) var(--ck-clipboard-drop-target-dot-height);border-color:transparent transparent transparent var(--ck-clipboard-drop-target-color)}[dir=rtl] .ck.ck-clipboard-drop-target-line:before{right:-1px;border-width:calc(.5 * var(--ck-clipboard-drop-target-dot-width)) var(--ck-clipboard-drop-target-dot-height) calc(.5 * var(--ck-clipboard-drop-target-dot-width)) 0;border-color:transparent var(--ck-clipboard-drop-target-color) transparent transparent}:root{--ck-color-code-block-label-background: hsl(0, 0%, 46%)}.ck.ck-editor__editable pre[data-language]:after{top:-1px;right:10px;background:var(--ck-color-code-block-label-background);font-size:10px;font-family:var(--ck-font-face);line-height:16px;padding:var(--ck-spacing-tiny) var(--ck-spacing-medium);color:#fff;white-space:nowrap}.ck.ck-code-block-dropdown .ck-dropdown__panel{max-height:250px;overflow-y:auto;overflow-x:hidden}@media (forced-colors: active){.ck.ck-placeholder,.ck .ck-placeholder{forced-color-adjust:preserve-parent-color}}.ck.ck-placeholder:before,.ck .ck-placeholder:before{cursor:text}@media (forced-colors: none){.ck.ck-placeholder:before,.ck .ck-placeholder:before{color:var(--ck-color-engine-placeholder-text)}}@media (forced-colors: active){.ck.ck-placeholder:before,.ck .ck-placeholder:before{font-style:italic;margin-left:1px}}.ck.ck-find-and-replace-form{width:400px}.ck.ck-find-and-replace-form:focus{outline:none}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs,.ck.ck-find-and-replace-form .ck-find-and-replace-form__actions{flex:1 1 auto;flex-direction:row;flex-wrap:wrap;align-items:center;align-content:stretch;padding:var(--ck-spacing-large);margin:0}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs>.ck-button,.ck.ck-find-and-replace-form .ck-find-and-replace-form__actions>.ck-button{flex:0 0 auto}[dir=ltr] .ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs>*+*,[dir=ltr] .ck.ck-find-and-replace-form .ck-find-and-replace-form__actions>*+*{margin-left:var(--ck-spacing-standard)}[dir=rtl] .ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs>*+*,[dir=rtl] .ck.ck-find-and-replace-form .ck-find-and-replace-form__actions>*+*{margin-right:var(--ck-spacing-standard)}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs .ck-labeled-field-view,.ck.ck-find-and-replace-form .ck-find-and-replace-form__actions .ck-labeled-field-view{flex:1 1 auto}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs .ck-labeled-field-view .ck-input,.ck.ck-find-and-replace-form .ck-find-and-replace-form__actions .ck-labeled-field-view .ck-input{width:100%;min-width:50px}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs{align-items:flex-start}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs>.ck-button-prev>.ck-icon{transform:rotate(90deg)}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs>.ck-button-next>.ck-icon{transform:rotate(-90deg)}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs .ck-results-counter{top:50%;transform:translateY(-50%);color:var(--ck-color-base-border)}[dir=ltr] .ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs .ck-results-counter{right:var(--ck-spacing-standard)}[dir=rtl] .ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs .ck-results-counter{left:var(--ck-spacing-standard)}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs>.ck-labeled-field-replace{flex:0 0 100%;padding-top:var(--ck-spacing-standard)}[dir=ltr] .ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs>.ck-labeled-field-replace{margin-left:0}[dir=rtl] .ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs>.ck-labeled-field-replace{margin-right:0}.ck.ck-find-and-replace-form .ck-find-and-replace-form__actions{flex-wrap:wrap;justify-content:flex-end;margin-top:calc(-1 * var(--ck-spacing-large))}.ck.ck-find-and-replace-form .ck-find-and-replace-form__actions>.ck-button-find{font-weight:700}.ck.ck-find-and-replace-form .ck-find-and-replace-form__actions>.ck-button-find .ck-button__label{padding-left:var(--ck-spacing-large);padding-right:var(--ck-spacing-large)}.ck.ck-find-and-replace-form .ck-switchbutton{width:100%;display:flex;flex-direction:row;flex-wrap:nowrap;justify-content:space-between;align-items:center}@media screen and (max-width: 600px){.ck.ck-find-and-replace-form{width:300px;max-width:100%}.ck.ck-find-and-replace-form.ck-find-and-replace-form__input{flex-wrap:wrap}.ck.ck-find-and-replace-form.ck-find-and-replace-form__input .ck-labeled-field-view{flex:1 0 auto;width:100%;margin-bottom:var(--ck-spacing-standard)}.ck.ck-find-and-replace-form.ck-find-and-replace-form__input>.ck-button{text-align:center}.ck.ck-find-and-replace-form.ck-find-and-replace-form__input>.ck-button:first-of-type{flex:1 1 auto}[dir=ltr] .ck.ck-find-and-replace-form.ck-find-and-replace-form__input>.ck-button:first-of-type{margin-left:0}[dir=rtl] .ck.ck-find-and-replace-form.ck-find-and-replace-form__input>.ck-button:first-of-type{margin-right:0}.ck.ck-find-and-replace-form.ck-find-and-replace-form__input>.ck-button:first-of-type .ck-button__label{width:100%;text-align:center}.ck.ck-find-and-replace-form.ck-find-and-replace-form__actions>:not(.ck-labeled-field-view){flex-wrap:wrap;flex:1 1 auto}.ck.ck-find-and-replace-form.ck-find-and-replace-form__actions>:not(.ck-labeled-field-view)>.ck-button{text-align:center}.ck.ck-find-and-replace-form.ck-find-and-replace-form__actions>:not(.ck-labeled-field-view)>.ck-button:first-of-type{flex:1 1 auto}[dir=ltr] .ck.ck-find-and-replace-form.ck-find-and-replace-form__actions>:not(.ck-labeled-field-view)>.ck-button:first-of-type{margin-left:0}[dir=rtl] .ck.ck-find-and-replace-form.ck-find-and-replace-form__actions>:not(.ck-labeled-field-view)>.ck-button:first-of-type{margin-right:0}.ck.ck-find-and-replace-form.ck-find-and-replace-form__actions>:not(.ck-labeled-field-view)>.ck-button .ck-button__label{width:100%;text-align:center}}.ck.ck-dropdown.ck-heading-dropdown .ck-dropdown__button .ck-button__label{width:8em}.ck.ck-dropdown.ck-heading-dropdown .ck-dropdown__panel .ck-list__item{min-width:18em}:root{--ck-html-embed-content-width: calc(100% - 1.5 * var(--ck-icon-size));--ck-html-embed-source-height: 10em;--ck-html-embed-unfocused-outline-width: 1px;--ck-html-embed-content-min-height: calc(var(--ck-icon-size) + var(--ck-spacing-standard));--ck-html-embed-source-disabled-background: var(--ck-color-base-foreground);--ck-html-embed-source-disabled-color: hsl(0deg 0% 45%)}.ck-widget.raw-html-embed{font-size:var(--ck-font-size-base);background-color:var(--ck-color-base-foreground)}.ck-widget.raw-html-embed:not(.ck-widget_selected):not(:hover){outline:var(--ck-html-embed-unfocused-outline-width) dashed var(--ck-color-widget-blurred-border)}.ck-widget.raw-html-embed[dir=ltr]{text-align:left}.ck-widget.raw-html-embed[dir=rtl]{text-align:right}.ck-widget.raw-html-embed:before{content:attr(data-html-embed-label);top:calc(-1 * var(--ck-html-embed-unfocused-outline-width));left:var(--ck-spacing-standard);background:#999;transition:background var(--ck-widget-handler-animation-duration) var(--ck-widget-handler-animation-curve);padding:calc(var(--ck-spacing-tiny) + var(--ck-html-embed-unfocused-outline-width)) var(--ck-spacing-small) var(--ck-spacing-tiny);border-radius:0 0 var(--ck-border-radius) var(--ck-border-radius);color:var(--ck-color-base-background);font-size:var(--ck-font-size-tiny);font-family:var(--ck-font-face)}.ck-widget.raw-html-embed[dir=rtl]:before{left:auto;right:var(--ck-spacing-standard)}.ck-widget.raw-html-embed[dir=ltr] .ck-widget__type-around .ck-widget__type-around__button.ck-widget__type-around__button_before{margin-left:50px}.ck.ck-editor__editable.ck-blurred .ck-widget.raw-html-embed.ck-widget_selected:before{top:0;padding:var(--ck-spacing-tiny) var(--ck-spacing-small)}.ck.ck-editor__editable:not(.ck-blurred) .ck-widget.raw-html-embed.ck-widget_selected:before{top:0;padding:var(--ck-spacing-tiny) var(--ck-spacing-small);background:var(--ck-color-focus-border)}.ck.ck-editor__editable .ck-widget.raw-html-embed:not(.ck-widget_selected):hover:before{top:0;padding:var(--ck-spacing-tiny) var(--ck-spacing-small)}.ck-widget.raw-html-embed .raw-html-embed__content-wrapper{padding:var(--ck-spacing-standard)}.ck-widget.raw-html-embed .raw-html-embed__buttons-wrapper{top:var(--ck-spacing-standard);right:var(--ck-spacing-standard)}.ck-widget.raw-html-embed .raw-html-embed__buttons-wrapper .ck-button.raw-html-embed__save-button{color:var(--ck-color-button-save)}.ck-widget.raw-html-embed .raw-html-embed__buttons-wrapper .ck-button.raw-html-embed__cancel-button{color:var(--ck-color-button-cancel)}.ck-widget.raw-html-embed .raw-html-embed__buttons-wrapper .ck-button:not(:first-child){margin-top:var(--ck-spacing-small)}.ck-widget.raw-html-embed[dir=rtl] .raw-html-embed__buttons-wrapper{left:var(--ck-spacing-standard);right:auto}.ck-widget.raw-html-embed .raw-html-embed__source{box-sizing:border-box;height:var(--ck-html-embed-source-height);width:var(--ck-html-embed-content-width);resize:none;min-width:0;padding:var(--ck-spacing-standard);font-family:monospace;-moz-tab-size:4;tab-size:4;white-space:pre-wrap;font-size:var(--ck-font-size-base);text-align:left;direction:ltr}.ck-widget.raw-html-embed .raw-html-embed__source[disabled]{background:var(--ck-html-embed-source-disabled-background);color:var(--ck-html-embed-source-disabled-color);-webkit-text-fill-color:var(--ck-html-embed-source-disabled-color);opacity:1}.ck-widget.raw-html-embed .raw-html-embed__preview{min-height:var(--ck-html-embed-content-min-height);width:var(--ck-html-embed-content-width)}.ck-editor__editable:not(.ck-read-only) .ck-widget.raw-html-embed .raw-html-embed__preview{pointer-events:none}.ck-widget.raw-html-embed .raw-html-embed__preview-content{box-sizing:border-box;background-color:var(--ck-color-base-foreground)}.ck-widget.raw-html-embed .raw-html-embed__preview-content>*{margin-left:auto;margin-right:auto}.ck-widget.raw-html-embed .raw-html-embed__preview-placeholder{color:var(--ck-html-embed-source-disabled-color)}:root{--ck-image-insert-insert-by-url-width: 250px}.ck.ck-image-insert-url{--ck-input-width: 100%}.ck.ck-image-insert-url .ck-image-insert-url__action-row{grid-column-gap:var(--ck-spacing-large);margin-top:var(--ck-spacing-large)}.ck.ck-image-insert-url .ck-image-insert-url__action-row .ck-button-save,.ck.ck-image-insert-url .ck-image-insert-url__action-row .ck-button-cancel{justify-content:center;min-width:auto}.ck.ck-image-insert-url .ck-image-insert-url__action-row .ck-button .ck-button__label{color:var(--ck-color-text)}.ck.ck-image-insert-form>.ck.ck-button{display:block;width:100%}[dir=ltr] .ck.ck-image-insert-form>.ck.ck-button{text-align:left}[dir=rtl] .ck.ck-image-insert-form>.ck.ck-button{text-align:right}.ck.ck-image-insert-form>.ck.ck-collapsible{min-width:var(--ck-image-insert-insert-by-url-width)}.ck.ck-image-insert-form>.ck.ck-collapsible:not(:first-child){border-top:1px solid var(--ck-color-base-border)}.ck.ck-image-insert-form>.ck.ck-collapsible:not(:last-child){border-bottom:1px solid var(--ck-color-base-border)}.ck.ck-image-insert-form>.ck.ck-image-insert-url{min-width:var(--ck-image-insert-insert-by-url-width);padding:var(--ck-spacing-large)}.ck.ck-image-insert-form:focus{outline:none}:root{--ck-color-image-upload-icon: hsl(0, 0%, 100%);--ck-color-image-upload-icon-background: hsl(120, 100%, 27%);--ck-image-upload-icon-size: 20;--ck-image-upload-icon-width: 2px;--ck-image-upload-icon-is-visible: clamp(0px, 100% - 50px, 1px)}.ck-image-upload-complete-icon{opacity:0;background:var(--ck-color-image-upload-icon-background);animation-name:ck-upload-complete-icon-show,ck-upload-complete-icon-hide;animation-fill-mode:forwards,forwards;animation-duration:.5s,.5s;font-size:calc(1px * var(--ck-image-upload-icon-size));animation-delay:0ms,3s;overflow:hidden;width:calc(var(--ck-image-upload-icon-is-visible) * var(--ck-image-upload-icon-size));height:calc(var(--ck-image-upload-icon-is-visible) * var(--ck-image-upload-icon-size))}.ck-image-upload-complete-icon:after{left:25%;top:50%;opacity:0;height:0;width:0;transform:scaleX(-1) rotate(135deg);transform-origin:left top;border-top:var(--ck-image-upload-icon-width) solid var(--ck-color-image-upload-icon);border-right:var(--ck-image-upload-icon-width) solid var(--ck-color-image-upload-icon);animation-name:ck-upload-complete-icon-check;animation-duration:.5s;animation-delay:.5s;animation-fill-mode:forwards;box-sizing:border-box}@media (prefers-reduced-motion: reduce){.ck-image-upload-complete-icon{animation-duration:0ms}.ck-image-upload-complete-icon:after{animation:none;opacity:1;width:.3em;height:.45em}}@keyframes ck-upload-complete-icon-show{0%{opacity:0}to{opacity:1}}@keyframes ck-upload-complete-icon-hide{0%{opacity:1}to{opacity:0}}@keyframes ck-upload-complete-icon-check{0%{opacity:1;width:0;height:0}33%{width:.3em;height:0}to{opacity:1;width:.3em;height:.45em}}:root{--ck-color-upload-placeholder-loader: hsl(0, 0%, 70%);--ck-upload-placeholder-loader-size: 32px;--ck-upload-placeholder-image-aspect-ratio: 2.8}.ck .ck-image-upload-placeholder{width:100%;margin:0}.ck .ck-image-upload-placeholder.image-inline{width:calc(2 * var(--ck-upload-placeholder-loader-size) * var(--ck-upload-placeholder-image-aspect-ratio))}.ck .ck-image-upload-placeholder img{aspect-ratio:var(--ck-upload-placeholder-image-aspect-ratio)}.ck .ck-upload-placeholder-loader{width:100%;height:100%}.ck .ck-upload-placeholder-loader:before{width:var(--ck-upload-placeholder-loader-size);height:var(--ck-upload-placeholder-loader-size);border-radius:50%;border-top:3px solid var(--ck-color-upload-placeholder-loader);border-right:2px solid transparent;animation:ck-upload-placeholder-loader 1s linear infinite}@keyframes ck-upload-placeholder-loader{to{transform:rotate(360deg)}}.ck.ck-editor__editable .image.ck-appear,.ck.ck-editor__editable .image-inline.ck-appear{animation:fadeIn .7s}@media (prefers-reduced-motion: reduce){.ck.ck-editor__editable .image.ck-appear,.ck.ck-editor__editable .image-inline.ck-appear{opacity:1;animation:none}}.ck.ck-editor__editable .image .ck-progress-bar,.ck.ck-editor__editable .image-inline .ck-progress-bar{height:2px;width:0;background:var(--ck-color-upload-bar-background);transition:width .1s}@keyframes fadeIn{0%{opacity:0}to{opacity:1}}.ck .ck-link_selected{background:var(--ck-color-link-selected-background)}.ck .ck-link_selected span.image-inline{outline:var(--ck-widget-outline-thickness) solid var(--ck-color-link-selected-background)}.ck .ck-fake-link-selection{background:var(--ck-color-link-fake-selection)}.ck .ck-fake-link-selection_collapsed{height:100%;border-right:1px solid var(--ck-color-base-text);margin-right:-1px;outline:solid 1px hsla(0,0%,100%,.5)}.ck.ck-link-actions .ck-button.ck-link-actions__preview{padding-left:0;padding-right:0}.ck.ck-link-actions .ck-button.ck-link-actions__preview .ck-button__label{padding:0 var(--ck-spacing-medium);color:var(--ck-color-link-default);text-overflow:ellipsis;cursor:pointer;max-width:var(--ck-input-width);min-width:3em;text-align:center}.ck.ck-link-actions .ck-button.ck-link-actions__preview .ck-button__label:hover{text-decoration:underline}.ck.ck-link-actions .ck-button.ck-link-actions__preview,.ck.ck-link-actions .ck-button.ck-link-actions__preview:hover,.ck.ck-link-actions .ck-button.ck-link-actions__preview:focus,.ck.ck-link-actions .ck-button.ck-link-actions__preview:active{background:none}.ck.ck-link-actions .ck-button.ck-link-actions__preview:active{box-shadow:none}.ck.ck-link-actions .ck-button.ck-link-actions__preview:focus .ck-button__label{text-decoration:underline}[dir=ltr] .ck.ck-link-actions .ck-button:not(:first-child){margin-left:var(--ck-spacing-standard)}[dir=rtl] .ck.ck-link-actions .ck-button:not(:last-child){margin-left:var(--ck-spacing-standard)}@media screen and (max-width: 600px){.ck.ck-link-actions .ck-button.ck-link-actions__preview{margin:var(--ck-spacing-standard) var(--ck-spacing-standard) 0}.ck.ck-link-actions .ck-button.ck-link-actions__preview .ck-button__label{min-width:0;max-width:100%}[dir=ltr] .ck.ck-link-actions .ck-button:not(.ck-link-actions__preview){margin-left:0}[dir=rtl] .ck.ck-link-actions .ck-button:not(.ck-link-actions__preview){margin-left:0}}.ck.ck-link-form_layout-vertical{padding:0;min-width:var(--ck-input-width)}.ck.ck-link-form_layout-vertical .ck-labeled-field-view{margin:var(--ck-spacing-large) var(--ck-spacing-large) var(--ck-spacing-small)}.ck.ck-link-form_layout-vertical .ck-labeled-field-view .ck-input-text{min-width:0;width:100%}.ck.ck-link-form_layout-vertical>.ck-button{padding:var(--ck-spacing-standard);margin:0;width:50%;border-radius:0}.ck.ck-link-form_layout-vertical>.ck-button:not(:focus){border-top:1px solid var(--ck-color-base-border)}[dir=ltr] .ck.ck-link-form_layout-vertical>.ck-button{margin-left:0}[dir=rtl] .ck.ck-link-form_layout-vertical>.ck-button{margin-left:0}[dir=rtl] .ck.ck-link-form_layout-vertical>.ck-button:last-of-type{border-right:1px solid var(--ck-color-base-border)}.ck.ck-link-form_layout-vertical .ck.ck-list{margin:0 var(--ck-spacing-large)}.ck.ck-link-form_layout-vertical .ck.ck-list .ck-button.ck-switchbutton{padding:0;width:100%}.ck.ck-link-form_layout-vertical .ck.ck-list .ck-button.ck-switchbutton:hover{background:none}:root{--ck-link-image-indicator-icon-size: 20;--ck-link-image-indicator-icon-is-visible: clamp(0px, 100% - 50px, 1px)}.ck.ck-editor__editable figure.image>a:after,.ck.ck-editor__editable a span.image-inline:after{content:"";top:min(var(--ck-spacing-medium),6%);right:min(var(--ck-spacing-medium),6%);background-color:#0006;background-image:url(data:image/svg+xml;base64,);background-size:14px;background-repeat:no-repeat;background-position:center;border-radius:100%;overflow:hidden;width:calc(var(--ck-link-image-indicator-icon-is-visible) * var(--ck-link-image-indicator-icon-size));height:calc(var(--ck-link-image-indicator-icon-is-visible) * var(--ck-link-image-indicator-icon-size))}.ck.ck-list-properties.ck-list-properties_without-styles{padding:var(--ck-spacing-large)}.ck.ck-list-properties.ck-list-properties_without-styles>*{min-width:14em}.ck.ck-list-properties.ck-list-properties_without-styles>*+*{margin-top:var(--ck-spacing-standard)}.ck.ck-list-properties.ck-list-properties_with-numbered-properties>.ck-list-styles-list{grid-template-columns:repeat(4,auto)}.ck.ck-list-properties.ck-list-properties_with-numbered-properties>.ck-collapsible{border-top:1px solid var(--ck-color-base-border)}.ck.ck-list-properties.ck-list-properties_with-numbered-properties>.ck-collapsible>.ck-collapsible__children>*{width:100%}.ck.ck-list-properties.ck-list-properties_with-numbered-properties>.ck-collapsible>.ck-collapsible__children>*+*{margin-top:var(--ck-spacing-standard)}.ck.ck-list-properties .ck.ck-numbered-list-properties__start-index .ck-input{min-width:auto;width:100%}.ck.ck-list-properties .ck.ck-numbered-list-properties__reversed-order{background:transparent;padding-left:0;padding-right:0;margin-bottom:calc(-1 * var(--ck-spacing-tiny))}.ck.ck-list-properties .ck.ck-numbered-list-properties__reversed-order:active,.ck.ck-list-properties .ck.ck-numbered-list-properties__reversed-order:hover{box-shadow:none;border-color:transparent;background:none}:root{--ck-list-style-button-size: 44px}.ck.ck-list-styles-list{grid-template-columns:repeat(3,auto);row-gap:var(--ck-spacing-medium);column-gap:var(--ck-spacing-medium);padding:var(--ck-spacing-large)}.ck.ck-list-styles-list .ck-button{width:var(--ck-list-style-button-size);height:var(--ck-list-style-button-size);padding:0;margin:0;box-sizing:content-box}.ck.ck-list-styles-list .ck-button .ck-icon{width:var(--ck-list-style-button-size);height:var(--ck-list-style-button-size)}:root{--ck-media-embed-placeholder-icon-size: 3em;--ck-color-media-embed-placeholder-url-text: hsl(0, 0%, 46%);--ck-color-media-embed-placeholder-url-text-hover: var(--ck-color-base-text)}.ck-media__wrapper{margin:0 auto}.ck-media__wrapper .ck-media__placeholder{padding:calc(3 * var(--ck-spacing-standard));background:var(--ck-color-base-foreground)}.ck-media__wrapper .ck-media__placeholder .ck-media__placeholder__icon{min-width:var(--ck-media-embed-placeholder-icon-size);height:var(--ck-media-embed-placeholder-icon-size);margin-bottom:var(--ck-spacing-large);background-position:center;background-size:cover}.ck-media__wrapper .ck-media__placeholder .ck-media__placeholder__icon .ck-icon{width:100%;height:100%}.ck-media__wrapper .ck-media__placeholder .ck-media__placeholder__url__text{color:var(--ck-color-media-embed-placeholder-url-text);white-space:nowrap;text-align:center;font-style:italic;text-overflow:ellipsis}.ck-media__wrapper .ck-media__placeholder .ck-media__placeholder__url__text:hover{color:var(--ck-color-media-embed-placeholder-url-text-hover);cursor:pointer;text-decoration:underline}.ck-media__wrapper[data-oembed-url*="open.spotify.com"]{max-width:300px;max-height:380px}.ck-media__wrapper[data-oembed-url*="google.com/maps"] .ck-media__placeholder__icon,.ck-media__wrapper[data-oembed-url*="goo.gl/maps"] .ck-media__placeholder__icon,.ck-media__wrapper[data-oembed-url*="maps.google.com"] .ck-media__placeholder__icon,.ck-media__wrapper[data-oembed-url*="maps.app.goo.gl"] .ck-media__placeholder__icon{background-image:url(data:image/svg+xml;base64,)}.ck-media__wrapper[data-oembed-url*="facebook.com"] .ck-media__placeholder{background:#4268b3}.ck-media__wrapper[data-oembed-url*="facebook.com"] .ck-media__placeholder .ck-media__placeholder__icon{background-image:url(data:image/svg+xml;base64,)}.ck-media__wrapper[data-oembed-url*="facebook.com"] .ck-media__placeholder .ck-media__placeholder__url__text{color:#cdf}.ck-media__wrapper[data-oembed-url*="facebook.com"] .ck-media__placeholder .ck-media__placeholder__url__text:hover{color:#fff}.ck-media__wrapper[data-oembed-url*="instagram.com"] .ck-media__placeholder{background:linear-gradient(-135deg,#1400c7,#b800b1,#f50000)}.ck-media__wrapper[data-oembed-url*="instagram.com"] .ck-media__placeholder .ck-media__placeholder__icon{background-image:url(data:image/svg+xml;base64,)}.ck-media__wrapper[data-oembed-url*="instagram.com"] .ck-media__placeholder .ck-media__placeholder__url__text{color:#ffe0fe}.ck-media__wrapper[data-oembed-url*="instagram.com"] .ck-media__placeholder .ck-media__placeholder__url__text:hover{color:#fff}.ck-media__wrapper[data-oembed-url*="twitter.com"] .ck.ck-media__placeholder{background:linear-gradient(to right,#71c6f4,#0d70a5)}.ck-media__wrapper[data-oembed-url*="twitter.com"] .ck.ck-media__placeholder .ck-media__placeholder__icon{background-image:url(data:image/svg+xml;base64,)}.ck-media__wrapper[data-oembed-url*="twitter.com"] .ck.ck-media__placeholder .ck-media__placeholder__url__text{color:#b8e6ff}.ck-media__wrapper[data-oembed-url*="twitter.com"] .ck.ck-media__placeholder .ck-media__placeholder__url__text:hover{color:#fff}:root{--ck-color-mention-background: hsla(341, 100%, 30%, .1);--ck-color-mention-text: hsl(341, 100%, 30%)}.ck-content .mention{background:var(--ck-color-mention-background);color:var(--ck-color-mention-text)}:root{--ck-color-restricted-editing-exception-background: hsla(31, 100%, 65%, .2);--ck-color-restricted-editing-exception-hover-background: hsla(31, 100%, 65%, .35);--ck-color-restricted-editing-exception-brackets: hsla(31, 100%, 40%, .4);--ck-color-restricted-editing-selected-exception-background: hsla(31, 100%, 65%, .5);--ck-color-restricted-editing-selected-exception-brackets: hsla(31, 100%, 40%, .6)}.ck-editor__editable .restricted-editing-exception{transition:.2s ease-in-out background;background-color:var(--ck-color-restricted-editing-exception-background);border:1px solid;border-image:linear-gradient(to right,var(--ck-color-restricted-editing-exception-brackets) 0%,var(--ck-color-restricted-editing-exception-brackets) 5px,hsla(0,0%,0%,0) 6px,hsla(0,0%,0%,0) calc(100% - 6px),var(--ck-color-restricted-editing-exception-brackets) calc(100% - 5px),var(--ck-color-restricted-editing-exception-brackets) 100%) 1}@media (prefers-reduced-motion: reduce){.ck-editor__editable .restricted-editing-exception{transition:none}}.ck-editor__editable .restricted-editing-exception.restricted-editing-exception_selected{background-color:var(--ck-color-restricted-editing-selected-exception-background);border-image:linear-gradient(to right,var(--ck-color-restricted-editing-selected-exception-brackets) 0%,var(--ck-color-restricted-editing-selected-exception-brackets) 5px,var(--ck-color-restricted-editing-selected-exception-brackets) calc(100% - 5px),var(--ck-color-restricted-editing-selected-exception-brackets) 100%) 1}.ck-editor__editable .restricted-editing-exception.restricted-editing-exception_collapsed{padding-left:1ch}.ck-restricted-editing_mode_restricted,.ck-restricted-editing_mode_restricted *{cursor:default}.ck-restricted-editing_mode_restricted .restricted-editing-exception,.ck-restricted-editing_mode_restricted .restricted-editing-exception *{cursor:text}.ck-restricted-editing_mode_restricted .restricted-editing-exception:hover{background:var(--ck-color-restricted-editing-exception-hover-background)}:root{--ck-character-grid-tile-size: 24px}.ck.ck-character-grid{overflow-y:auto;overflow-x:hidden}.ck.ck-character-grid .ck-character-grid__tiles{grid-template-columns:repeat(auto-fill,minmax(var(--ck-character-grid-tile-size),1fr));margin:var(--ck-spacing-standard) var(--ck-spacing-large);grid-gap:var(--ck-spacing-standard)}.ck.ck-character-grid .ck-character-grid__tile{width:var(--ck-character-grid-tile-size);height:var(--ck-character-grid-tile-size);min-width:var(--ck-character-grid-tile-size);min-height:var(--ck-character-grid-tile-size);font-size:1.5em;padding:0;transition:.2s ease box-shadow;border:0}@media (prefers-reduced-motion: reduce){.ck.ck-character-grid .ck-character-grid__tile{transition:none}}.ck.ck-character-grid .ck-character-grid__tile:focus:not(.ck-disabled),.ck.ck-character-grid .ck-character-grid__tile:hover:not(.ck-disabled){border:0;box-shadow:inset 0 0 0 1px var(--ck-color-base-background),0 0 0 2px var(--ck-color-focus-border)}.ck.ck-character-grid .ck-character-grid__tile .ck-button__label{line-height:var(--ck-character-grid-tile-size);width:100%;text-align:center}.ck.ck-character-info{padding:var(--ck-spacing-small) var(--ck-spacing-large);border-top:1px solid var(--ck-color-base-border)}.ck.ck-character-info>*{text-transform:uppercase;font-size:var(--ck-font-size-small)}.ck.ck-character-info .ck-character-info__name{max-width:280px;text-overflow:ellipsis;overflow:hidden}.ck.ck-character-info .ck-character-info__code{opacity:.6}.ck.ck-special-characters-navigation>.ck-label{max-width:160px;text-overflow:ellipsis;overflow:hidden}.ck.ck-special-characters-navigation>.ck-dropdown .ck-dropdown__panel{max-height:250px;overflow-y:auto;overflow-x:hidden}@media screen and (max-width: 600px){.ck.ck-special-characters-navigation{max-width:190px}.ck.ck-special-characters-navigation>.ck-form__header__label{text-overflow:ellipsis;overflow:hidden}}.ck.ck-dropdown.ck-style-dropdown.ck-style-dropdown_multiple-active>.ck-button>.ck-button__label{font-style:italic}:root{--ck-style-panel-button-width: 120px;--ck-style-panel-button-height: 80px;--ck-style-panel-button-label-background: hsl(0, 0%, 94.1%);--ck-style-panel-button-hover-label-background: hsl(0, 0%, 92.1%);--ck-style-panel-button-hover-border-color: hsl(0, 0%, 70%)}.ck.ck-style-panel .ck-style-grid{row-gap:var(--ck-spacing-large);column-gap:var(--ck-spacing-large)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button{--ck-color-button-default-hover-background: var(--ck-color-base-background);--ck-color-button-default-active-background: var(--ck-color-base-background);padding:0;width:var(--ck-style-panel-button-width);height:var(--ck-style-panel-button-height)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button:not(:focus){border:1px solid var(--ck-color-base-border)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button .ck-button__label{height:22px;line-height:22px;width:100%;padding:0 var(--ck-spacing-medium);overflow:hidden;text-overflow:ellipsis;flex-shrink:0}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button .ck-style-grid__button__preview{width:100%;overflow:hidden;opacity:.9;padding:var(--ck-spacing-medium);background:var(--ck-color-base-background);border:2px solid var(--ck-color-base-background)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button.ck-disabled{--ck-color-button-default-disabled-background: var(--ck-color-base-foreground)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button.ck-disabled:not(:focus){border-color:var(--ck-style-panel-button-label-background)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button.ck-disabled .ck-style-grid__button__preview{opacity:.4;border-color:var(--ck-color-base-foreground);filter:saturate(.3)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button.ck-on{border-color:var(--ck-color-base-active)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button.ck-on .ck-button__label{box-shadow:0 -1px 0 var(--ck-color-base-active);z-index:1}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button.ck-on:hover{border-color:var(--ck-color-base-active-focus)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button:not(.ck-on) .ck-button__label{background:var(--ck-style-panel-button-label-background)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button:not(.ck-on):hover .ck-button__label{background:var(--ck-style-panel-button-hover-label-background)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button:hover:not(.ck-disabled):not(.ck-on){border-color:var(--ck-style-panel-button-hover-border-color)}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button:hover:not(.ck-disabled):not(.ck-on) .ck-style-grid__button__preview{opacity:1}.ck.ck-style-panel .ck-style-panel__style-group>.ck-label{margin:var(--ck-spacing-large) 0}.ck.ck-style-panel .ck-style-panel__style-group:first-child>.ck-label{margin-top:0}:root{--ck-style-panel-max-height: 470px}.ck.ck-style-panel{padding:var(--ck-spacing-large);overflow-y:auto;max-height:var(--ck-style-panel-max-height)}[dir=ltr] .ck.ck-input-color>.ck.ck-input-text{border-top-right-radius:0;border-bottom-right-radius:0}[dir=rtl] .ck.ck-input-color>.ck.ck-input-text{border-top-left-radius:0;border-bottom-left-radius:0}.ck.ck-input-color>.ck.ck-input-text:focus{z-index:0}.ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button{padding:0}[dir=ltr] .ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button{border-top-left-radius:0;border-bottom-left-radius:0}[dir=ltr] .ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button:not(:focus){border-left:1px solid transparent}[dir=rtl] .ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button{border-top-right-radius:0;border-bottom-right-radius:0}[dir=rtl] .ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button:not(:focus){border-right:1px solid transparent}.ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button.ck-disabled{background:var(--ck-color-input-disabled-background)}.ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button>.ck.ck-input-color__button__preview{border-radius:0;width:20px;height:20px;border:1px solid var(--ck-color-input-border)}.ck-rounded-corners .ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button>.ck.ck-input-color__button__preview,.ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button>.ck.ck-input-color__button__preview.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-input-color>.ck.ck-dropdown>.ck.ck-button.ck-input-color__button>.ck.ck-input-color__button__preview>.ck.ck-input-color__button__preview__no-color-indicator{top:-30%;left:50%;height:150%;width:8%;background:red;border-radius:2px;transform:rotate(45deg);transform-origin:50%}.ck.ck-input-color .ck.ck-input-color__remove-color{width:100%;padding:calc(var(--ck-spacing-standard) / 2) var(--ck-spacing-standard);border-bottom-left-radius:0;border-bottom-right-radius:0}.ck.ck-input-color .ck.ck-input-color__remove-color:not(:focus){border-bottom:1px solid var(--ck-color-input-border)}[dir=ltr] .ck.ck-input-color .ck.ck-input-color__remove-color{border-top-right-radius:0}[dir=rtl] .ck.ck-input-color .ck.ck-input-color__remove-color{border-top-left-radius:0}.ck.ck-input-color .ck.ck-input-color__remove-color .ck.ck-icon{margin-right:var(--ck-spacing-standard)}[dir=rtl] .ck.ck-input-color .ck.ck-input-color__remove-color .ck.ck-icon{margin-right:0;margin-left:var(--ck-spacing-standard)}.ck.ck-form{padding:0 0 var(--ck-spacing-large)}.ck.ck-form:focus{outline:none}.ck.ck-form .ck.ck-input-text{min-width:100%;width:0}.ck.ck-form .ck.ck-dropdown{min-width:100%}.ck.ck-form .ck.ck-dropdown .ck-dropdown__button:not(:focus){border:1px solid var(--ck-color-base-border)}.ck.ck-form .ck.ck-dropdown .ck-dropdown__button .ck-button__label{width:100%}.ck.ck-form__row{padding:var(--ck-spacing-standard) var(--ck-spacing-large) 0}[dir=ltr] .ck.ck-form__row>*:not(.ck-label)+*{margin-left:var(--ck-spacing-large)}[dir=rtl] .ck.ck-form__row>*:not(.ck-label)+*{margin-right:var(--ck-spacing-large)}.ck.ck-form__row>.ck-label{width:100%;min-width:100%}.ck.ck-form__row.ck-table-form__action-row{margin-top:var(--ck-spacing-large)}.ck.ck-form__row.ck-table-form__action-row .ck-button .ck-button__label{color:var(--ck-color-text)}:root{--ck-insert-table-dropdown-padding: 10px;--ck-insert-table-dropdown-box-height: 11px;--ck-insert-table-dropdown-box-width: 12px;--ck-insert-table-dropdown-box-margin: 1px}.ck .ck-insert-table-dropdown__grid{width:calc(var(--ck-insert-table-dropdown-box-width) * 10 + var(--ck-insert-table-dropdown-box-margin) * 20 + var(--ck-insert-table-dropdown-padding) * 2);padding:var(--ck-insert-table-dropdown-padding) var(--ck-insert-table-dropdown-padding) 0}.ck .ck-insert-table-dropdown__label,.ck[dir=rtl] .ck-insert-table-dropdown__label{text-align:center}.ck .ck-insert-table-dropdown-grid-box{min-width:var(--ck-insert-table-dropdown-box-width);min-height:var(--ck-insert-table-dropdown-box-height);margin:var(--ck-insert-table-dropdown-box-margin);border:1px solid var(--ck-color-base-border);border-radius:1px;outline:none;transition:none}@media (prefers-reduced-motion: reduce){.ck .ck-insert-table-dropdown-grid-box{transition:none}}.ck .ck-insert-table-dropdown-grid-box:focus{box-shadow:none}.ck .ck-insert-table-dropdown-grid-box.ck-on{border-color:var(--ck-color-focus-border);background:var(--ck-color-focus-outer-shadow)}.ck.ck-table-cell-properties-form{width:320px}.ck.ck-table-cell-properties-form .ck-form__row.ck-table-cell-properties-form__padding-row{align-self:flex-end;padding:0;width:25%}.ck.ck-table-cell-properties-form .ck-form__row.ck-table-cell-properties-form__alignment-row .ck.ck-toolbar{background:none;margin-top:var(--ck-spacing-standard)}:root{--ck-color-selector-focused-cell-background: hsla(212, 90%, 80%, .3)}.ck-widget.table td.ck-editor__nested-editable.ck-editor__nested-editable_focused,.ck-widget.table th.ck-editor__nested-editable.ck-editor__nested-editable_focused,.ck-widget.table td.ck-editor__nested-editable:focus,.ck-widget.table th.ck-editor__nested-editable:focus{background:var(--ck-color-selector-focused-cell-background);outline:1px solid var(--ck-color-focus-border);outline-offset:-1px}:root{--ck-table-properties-error-arrow-size: 6px;--ck-table-properties-min-error-width: 150px}.ck.ck-table-form .ck-form__row.ck-table-form__border-row .ck-labeled-field-view>.ck-label{font-size:var(--ck-font-size-tiny);text-align:center}.ck.ck-table-form .ck-form__row.ck-table-form__border-row .ck-table-form__border-style,.ck.ck-table-form .ck-form__row.ck-table-form__border-row .ck-table-form__border-width{width:80px;min-width:80px;max-width:80px}.ck.ck-table-form .ck-form__row.ck-table-form__dimensions-row{padding:0}.ck.ck-table-form .ck-form__row.ck-table-form__dimensions-row .ck-table-form__dimensions-row__width,.ck.ck-table-form .ck-form__row.ck-table-form__dimensions-row .ck-table-form__dimensions-row__height{margin:0}.ck.ck-table-form .ck-form__row.ck-table-form__dimensions-row .ck-table-form__dimension-operator{align-self:flex-end;display:inline-block;height:var(--ck-ui-component-min-height);line-height:var(--ck-ui-component-min-height);margin:0 var(--ck-spacing-small)}.ck.ck-table-form .ck.ck-labeled-field-view{padding-top:var(--ck-spacing-standard)}.ck.ck-table-form .ck.ck-labeled-field-view .ck.ck-labeled-field-view__status{border-radius:0;background:var(--ck-color-base-error);color:var(--ck-color-base-background);padding:var(--ck-spacing-small) var(--ck-spacing-medium);min-width:var(--ck-table-properties-min-error-width);text-align:center;animation:ck-table-form-labeled-view-status-appear .15s ease both}.ck-rounded-corners .ck.ck-table-form .ck.ck-labeled-field-view .ck.ck-labeled-field-view__status,.ck.ck-table-form .ck.ck-labeled-field-view .ck.ck-labeled-field-view__status.ck-rounded-corners{border-radius:var(--ck-border-radius)}.ck.ck-table-form .ck.ck-labeled-field-view .ck.ck-labeled-field-view__status:after{border-color:transparent transparent var(--ck-color-base-error) transparent;border-width:0 var(--ck-table-properties-error-arrow-size) var(--ck-table-properties-error-arrow-size) var(--ck-table-properties-error-arrow-size);border-style:solid}@media (prefers-reduced-motion: reduce){.ck.ck-table-form .ck.ck-labeled-field-view .ck.ck-labeled-field-view__status{animation:none}}.ck.ck-table-form .ck.ck-labeled-field-view .ck-input.ck-error:not(:focus)+.ck.ck-labeled-field-view__status{display:none}@keyframes ck-table-form-labeled-view-status-appear{0%{opacity:0}to{opacity:1}}.ck.ck-table-properties-form{width:320px}.ck.ck-table-properties-form .ck-form__row.ck-table-properties-form__alignment-row{align-self:flex-end;padding:0}.ck.ck-table-properties-form .ck-form__row.ck-table-properties-form__alignment-row .ck.ck-toolbar{background:none;margin-top:var(--ck-spacing-standard)}.ck.ck-table-properties-form .ck-form__row.ck-table-properties-form__alignment-row .ck.ck-toolbar .ck-toolbar__items>*{width:40px}:root{--ck-table-selected-cell-background: hsla(208, 90%, 80%, .3)}.ck.ck-editor__editable .table table td.ck-editor__editable_selected,.ck.ck-editor__editable .table table th.ck-editor__editable_selected{position:relative;caret-color:transparent;outline:unset;box-shadow:unset}.ck.ck-editor__editable .table table td.ck-editor__editable_selected:after,.ck.ck-editor__editable .table table th.ck-editor__editable_selected:after{content:"";pointer-events:none;background-color:var(--ck-table-selected-cell-background);position:absolute;top:0;left:0;right:0;bottom:0}.ck.ck-editor__editable .table table td.ck-editor__editable_selected ::selection,.ck.ck-editor__editable .table table th.ck-editor__editable_selected ::selection,.ck.ck-editor__editable .table table td.ck-editor__editable_selected:focus,.ck.ck-editor__editable .table table th.ck-editor__editable_selected:focus{background-color:transparent}.ck.ck-editor__editable .table table td.ck-editor__editable_selected .ck-widget,.ck.ck-editor__editable .table table th.ck-editor__editable_selected .ck-widget{outline:unset}.ck.ck-editor__editable .table table td.ck-editor__editable_selected .ck-widget>.ck-widget__selection-handle,.ck.ck-editor__editable .table table th.ck-editor__editable_selected .ck-widget>.ck-widget__selection-handle{display:none}:root{--ck-widget-outline-thickness: 3px;--ck-widget-handler-icon-size: 16px;--ck-widget-handler-animation-duration: .2s;--ck-widget-handler-animation-curve: ease;--ck-color-widget-blurred-border: hsl(0, 0%, 87%);--ck-color-widget-hover-border: hsl(43, 100%, 62%);--ck-color-widget-editable-focus-background: var(--ck-color-base-background);--ck-color-widget-drag-handler-icon-color: var(--ck-color-base-background)}.ck .ck-widget{outline-width:var(--ck-widget-outline-thickness);outline-style:solid;outline-color:transparent;transition:outline-color var(--ck-widget-handler-animation-duration) var(--ck-widget-handler-animation-curve)}@media (prefers-reduced-motion: reduce){.ck .ck-widget{transition:none}}.ck .ck-widget.ck-widget_selected,.ck .ck-widget.ck-widget_selected:hover{outline:var(--ck-widget-outline-thickness) solid var(--ck-color-focus-border)}.ck .ck-widget:hover{outline-color:var(--ck-color-widget-hover-border)}.ck .ck-editor__nested-editable{border:1px solid transparent}.ck .ck-editor__nested-editable.ck-editor__nested-editable_focused,.ck .ck-editor__nested-editable:focus{box-shadow:var(--ck-inner-shadow),0 0}@media (forced-colors: none){.ck .ck-editor__nested-editable.ck-editor__nested-editable_focused,.ck .ck-editor__nested-editable:focus{background-color:var(--ck-color-widget-editable-focus-background)}}.ck .ck-editor__nested-editable.ck-editor__nested-editable_focused:not(td,th),.ck .ck-editor__nested-editable:focus:not(td,th){outline:none;border:var(--ck-focus-ring)}.ck .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle{padding:4px;box-sizing:border-box;background-color:transparent;opacity:0;transition:background-color var(--ck-widget-handler-animation-duration) var(--ck-widget-handler-animation-curve),visibility var(--ck-widget-handler-animation-duration) var(--ck-widget-handler-animation-curve),opacity var(--ck-widget-handler-animation-duration) var(--ck-widget-handler-animation-curve);border-radius:var(--ck-border-radius) var(--ck-border-radius) 0 0;transform:translateY(-100%);left:calc(0px - var(--ck-widget-outline-thickness));top:0}@media (prefers-reduced-motion: reduce){.ck .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle{transition:none}}.ck .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle .ck-icon{width:var(--ck-widget-handler-icon-size);height:var(--ck-widget-handler-icon-size);color:var(--ck-color-widget-drag-handler-icon-color)}.ck .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle .ck-icon .ck-icon__selected-indicator{opacity:0;transition:opacity .3s var(--ck-widget-handler-animation-curve)}@media (prefers-reduced-motion: reduce){.ck .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle .ck-icon .ck-icon__selected-indicator{transition:none}}.ck .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle:hover .ck-icon .ck-icon__selected-indicator{opacity:1}.ck .ck-widget.ck-widget_with-selection-handle:hover>.ck-widget__selection-handle{opacity:1;background-color:var(--ck-color-widget-hover-border)}.ck .ck-widget.ck-widget_with-selection-handle.ck-widget_selected>.ck-widget__selection-handle,.ck .ck-widget.ck-widget_with-selection-handle.ck-widget_selected:hover>.ck-widget__selection-handle{opacity:1;background-color:var(--ck-color-focus-border)}.ck .ck-widget.ck-widget_with-selection-handle.ck-widget_selected>.ck-widget__selection-handle .ck-icon .ck-icon__selected-indicator,.ck .ck-widget.ck-widget_with-selection-handle.ck-widget_selected:hover>.ck-widget__selection-handle .ck-icon .ck-icon__selected-indicator{opacity:1}.ck[dir=rtl] .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle{left:auto;right:calc(0px - var(--ck-widget-outline-thickness))}.ck.ck-editor__editable.ck-read-only .ck-widget{transition:none}.ck.ck-editor__editable.ck-read-only .ck-widget:not(.ck-widget_selected){--ck-widget-outline-thickness: 0px}.ck.ck-editor__editable.ck-read-only .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle,.ck.ck-editor__editable.ck-read-only .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle:hover{background:var(--ck-color-widget-blurred-border)}.ck.ck-editor__editable.ck-blurred .ck-widget.ck-widget_selected,.ck.ck-editor__editable.ck-blurred .ck-widget.ck-widget_selected:hover{outline-color:var(--ck-color-widget-blurred-border)}.ck.ck-editor__editable.ck-blurred .ck-widget.ck-widget_selected.ck-widget_with-selection-handle>.ck-widget__selection-handle,.ck.ck-editor__editable.ck-blurred .ck-widget.ck-widget_selected.ck-widget_with-selection-handle:hover>.ck-widget__selection-handle,.ck.ck-editor__editable.ck-blurred .ck-widget.ck-widget_selected.ck-widget_with-selection-handle>.ck-widget__selection-handle:hover,.ck.ck-editor__editable.ck-blurred .ck-widget.ck-widget_selected.ck-widget_with-selection-handle:hover>.ck-widget__selection-handle:hover{background:var(--ck-color-widget-blurred-border)}.ck.ck-editor__editable>.ck-widget.ck-widget_with-selection-handle:first-child,.ck.ck-editor__editable blockquote>.ck-widget.ck-widget_with-selection-handle:first-child{margin-top:calc(1em + var(--ck-widget-handler-icon-size))}:root{--ck-resizer-size: 10px;--ck-resizer-offset: calc( ( var(--ck-resizer-size) / -2 ) - 2px);--ck-resizer-border-width: 1px}.ck .ck-widget__resizer{outline:1px solid var(--ck-color-resizer)}.ck .ck-widget__resizer__handle{width:var(--ck-resizer-size);height:var(--ck-resizer-size);background:var(--ck-color-focus-border);border:var(--ck-resizer-border-width) solid hsl(0,0%,100%);border-radius:var(--ck-resizer-border-radius)}.ck .ck-widget__resizer__handle.ck-widget__resizer__handle-top-left{top:var(--ck-resizer-offset);left:var(--ck-resizer-offset)}.ck .ck-widget__resizer__handle.ck-widget__resizer__handle-top-right{top:var(--ck-resizer-offset);right:var(--ck-resizer-offset)}.ck .ck-widget__resizer__handle.ck-widget__resizer__handle-bottom-right{bottom:var(--ck-resizer-offset);right:var(--ck-resizer-offset)}.ck .ck-widget__resizer__handle.ck-widget__resizer__handle-bottom-left{bottom:var(--ck-resizer-offset);left:var(--ck-resizer-offset)}:root{--ck-widget-type-around-button-size: 20px;--ck-color-widget-type-around-button-active: var(--ck-color-focus-border);--ck-color-widget-type-around-button-hover: var(--ck-color-widget-hover-border);--ck-color-widget-type-around-button-blurred-editable: var(--ck-color-widget-blurred-border);--ck-color-widget-type-around-button-radar-start-alpha: 0;--ck-color-widget-type-around-button-radar-end-alpha: .3;--ck-color-widget-type-around-button-icon: var(--ck-color-base-background)}.ck .ck-widget .ck-widget__type-around__button{width:var(--ck-widget-type-around-button-size);height:var(--ck-widget-type-around-button-size);background:var(--ck-color-widget-type-around-button);border-radius:100px;transition:opacity var(--ck-widget-handler-animation-duration) var(--ck-widget-handler-animation-curve),background var(--ck-widget-handler-animation-duration) var(--ck-widget-handler-animation-curve);opacity:0;pointer-events:none}@media (prefers-reduced-motion: reduce){.ck .ck-widget .ck-widget__type-around__button{transition:none}}.ck .ck-widget .ck-widget__type-around__button svg{width:10px;height:8px;transform:translate(-50%,-50%);transition:transform .5s ease;margin-top:1px}@media (prefers-reduced-motion: reduce){.ck .ck-widget .ck-widget__type-around__button svg{transition:none}}.ck .ck-widget .ck-widget__type-around__button svg *{stroke-dasharray:10;stroke-dashoffset:0;fill:none;stroke:var(--ck-color-widget-type-around-button-icon);stroke-width:1.5px;stroke-linecap:round;stroke-linejoin:round}.ck .ck-widget .ck-widget__type-around__button svg line{stroke-dasharray:7}.ck .ck-widget .ck-widget__type-around__button:hover{animation:ck-widget-type-around-button-sonar 1s ease infinite}.ck .ck-widget .ck-widget__type-around__button:hover svg polyline{animation:ck-widget-type-around-arrow-dash 2s linear}.ck .ck-widget .ck-widget__type-around__button:hover svg line{animation:ck-widget-type-around-arrow-tip-dash 2s linear}@media (prefers-reduced-motion: reduce){.ck .ck-widget .ck-widget__type-around__button:hover{animation:none}.ck .ck-widget .ck-widget__type-around__button:hover svg polyline{animation:none}.ck .ck-widget .ck-widget__type-around__button:hover svg line{animation:none}}.ck .ck-widget.ck-widget_selected>.ck-widget__type-around>.ck-widget__type-around__button,.ck .ck-widget:hover>.ck-widget__type-around>.ck-widget__type-around__button{opacity:1;pointer-events:auto}.ck .ck-widget:not(.ck-widget_selected)>.ck-widget__type-around>.ck-widget__type-around__button{background:var(--ck-color-widget-type-around-button-hover)}.ck .ck-widget.ck-widget_selected>.ck-widget__type-around>.ck-widget__type-around__button,.ck .ck-widget>.ck-widget__type-around>.ck-widget__type-around__button:hover{background:var(--ck-color-widget-type-around-button-active)}.ck .ck-widget.ck-widget_selected>.ck-widget__type-around>.ck-widget__type-around__button:after,.ck .ck-widget>.ck-widget__type-around>.ck-widget__type-around__button:hover:after{width:calc(var(--ck-widget-type-around-button-size) - 2px);height:calc(var(--ck-widget-type-around-button-size) - 2px);border-radius:100px;background:linear-gradient(135deg,#fff0,#ffffff4d)}.ck .ck-widget.ck-widget_with-selection-handle>.ck-widget__type-around>.ck-widget__type-around__button_before{margin-left:20px}.ck .ck-widget .ck-widget__type-around__fake-caret{pointer-events:none;height:1px;animation:ck-widget-type-around-fake-caret-pulse linear 1s infinite normal forwards;outline:solid 1px hsla(0,0%,100%,.5);background:var(--ck-color-base-text)}.ck .ck-widget.ck-widget_selected.ck-widget_type-around_show-fake-caret_before,.ck .ck-widget.ck-widget_selected.ck-widget_type-around_show-fake-caret_after{outline-color:transparent}.ck .ck-widget.ck-widget_type-around_show-fake-caret_before.ck-widget_selected:hover,.ck .ck-widget.ck-widget_type-around_show-fake-caret_after.ck-widget_selected:hover{outline-color:var(--ck-color-widget-hover-border)}.ck .ck-widget.ck-widget_type-around_show-fake-caret_before>.ck-widget__type-around>.ck-widget__type-around__button,.ck .ck-widget.ck-widget_type-around_show-fake-caret_after>.ck-widget__type-around>.ck-widget__type-around__button{opacity:0;pointer-events:none}.ck .ck-widget.ck-widget_type-around_show-fake-caret_before.ck-widget_with-selection-handle.ck-widget_selected>.ck-widget__selection-handle,.ck .ck-widget.ck-widget_type-around_show-fake-caret_after.ck-widget_with-selection-handle.ck-widget_selected>.ck-widget__selection-handle,.ck .ck-widget.ck-widget_type-around_show-fake-caret_before.ck-widget_with-selection-handle.ck-widget_selected:hover>.ck-widget__selection-handle,.ck .ck-widget.ck-widget_type-around_show-fake-caret_after.ck-widget_with-selection-handle.ck-widget_selected:hover>.ck-widget__selection-handle{opacity:0}.ck .ck-widget.ck-widget_type-around_show-fake-caret_before.ck-widget_selected.ck-widget_with-resizer>.ck-widget__resizer,.ck .ck-widget.ck-widget_type-around_show-fake-caret_after.ck-widget_selected.ck-widget_with-resizer>.ck-widget__resizer{opacity:0}.ck[dir=rtl] .ck-widget.ck-widget_with-selection-handle .ck-widget__type-around>.ck-widget__type-around__button_before{margin-left:0;margin-right:20px}.ck-editor__nested-editable.ck-editor__editable_selected .ck-widget.ck-widget_selected>.ck-widget__type-around>.ck-widget__type-around__button,.ck-editor__nested-editable.ck-editor__editable_selected .ck-widget:hover>.ck-widget__type-around>.ck-widget__type-around__button{opacity:0;pointer-events:none}.ck-editor__editable.ck-blurred .ck-widget.ck-widget_selected>.ck-widget__type-around>.ck-widget__type-around__button:not(:hover){background:var(--ck-color-widget-type-around-button-blurred-editable)}.ck-editor__editable.ck-blurred .ck-widget.ck-widget_selected>.ck-widget__type-around>.ck-widget__type-around__button:not(:hover) svg *{stroke:#999}@keyframes ck-widget-type-around-arrow-dash{0%{stroke-dashoffset:10}20%,to{stroke-dashoffset:0}}@keyframes ck-widget-type-around-arrow-tip-dash{0%,20%{stroke-dashoffset:7}40%,to{stroke-dashoffset:0}}@keyframes ck-widget-type-around-button-sonar{0%{box-shadow:0 0 hsla(var(--ck-color-focus-border-coordinates),var(--ck-color-widget-type-around-button-radar-start-alpha))}50%{box-shadow:0 0 0 5px hsla(var(--ck-color-focus-border-coordinates),var(--ck-color-widget-type-around-button-radar-end-alpha))}to{box-shadow:0 0 0 5px hsla(var(--ck-color-focus-border-coordinates),var(--ck-color-widget-type-around-button-radar-start-alpha))}}@keyframes ck-widget-type-around-fake-caret-pulse{0%{opacity:1}49%{opacity:1}50%{opacity:0}99%{opacity:0}to{opacity:1}}.ck-content code{background-color:#c7c7c74d;padding:.15em;border-radius:2px}.ck.ck-editor__editable .ck-code_selected{background-color:#c7c7c780}.ck-content blockquote{overflow:hidden;padding-right:1.5em;padding-left:1.5em;margin-left:0;margin-right:0;font-style:italic;border-left:solid 5px hsl(0,0%,80%)}.ck-content[dir=rtl] blockquote{border-left:0;border-right:solid 5px hsl(0,0%,80%)}:root{--ck-bookmark-icon-hover-fill-color: var(--ck-color-widget-hover-border);--ck-bookmark-icon-selected-fill-color: var(--ck-color-focus-border);--ck-bookmark-icon-animation-duration: var(--ck-widget-handler-animation-duration);--ck-bookmark-icon-animation-curve: var(--ck-widget-handler-animation-curve)}.ck-bookmark.ck-widget{outline:none}.ck-bookmark.ck-widget .ck-bookmark__icon .ck-icon__fill{transition:fill var(--ck-bookmark-icon-animation-duration) var(--ck-bookmark-icon-animation-curve)}.ck-bookmark.ck-widget:hover .ck-bookmark__icon .ck-icon__fill{fill:var(--ck-bookmark-icon-hover-fill-color)}.ck-bookmark.ck-widget.ck-widget_selected .ck-bookmark__icon .ck-icon__fill{fill:var(--ck-bookmark-icon-selected-fill-color)}.ck-bookmark.ck-widget.ck-widget_selected,.ck-bookmark.ck-widget.ck-widget_selected:hover{outline:none}.ck-bookmark.ck-widget .ck-bookmark__icon{position:relative;top:-.1em}.ck-bookmark.ck-widget .ck-bookmark__icon .ck-icon{height:1.2em;width:auto;vertical-align:middle}.ck.ck-bookmark-view:focus{outline:none}.ck.ck-bookmark-form{display:flex;align-items:flex-start}@media screen and (max-width: 600px){.ck.ck-bookmark-form{flex-wrap:wrap}.ck.ck-bookmark-form .ck-button,.ck.ck-bookmark-form .ck-labeled-field-view{flex-basis:100%}.ck.ck-bookmark-form .ck-button{justify-content:center}}@media screen and (max-width: 600px){.ck.ck-bookmark-form.ck-responsive-form>.ck-button:nth-last-child(1){margin:var(--ck-spacing-large);padding:0 var(--ck-spacing-standard);border-radius:var(--ck-border-radius)}}.ck.ck-bookmark-actions{display:flex;align-items:center}.ck.ck-bookmark-actions .ck-bookmark-actions__preview{max-width:var(--ck-input-width);min-width:3em;font-weight:400;text-overflow:ellipsis;text-align:center;overflow:hidden;-moz-user-select:none;-webkit-user-select:none;-ms-user-select:none;user-select:none;cursor:default}@media screen and (max-width: 600px){.ck.ck-bookmark-actions{display:flex;flex-wrap:wrap}.ck.ck-bookmark-actions .ck-bookmark-actions__preview{flex-basis:100%;margin:var(--ck-spacing-standard) var(--ck-spacing-standard) 0;min-width:auto}}@media screen and (max-width: 600px){.ck.ck-bookmark-actions.ck-responsive-form .ck-button{flex-basis:50%;margin-top:var(--ck-spacing-standard)}}:root{--ck-image-processing-highlight-color: hsl(220, 10%, 98%);--ck-image-processing-background-color: hsl(220, 10%, 90%)}.ck.ck-editor__editable .image.image-processing{position:relative}.ck.ck-editor__editable .image.image-processing:before{content:"";position:absolute;top:0;left:0;z-index:1;height:100%;width:100%;background:linear-gradient(90deg,var(--ck-image-processing-background-color),var(--ck-image-processing-highlight-color),var(--ck-image-processing-background-color));background-size:200% 100%;animation:ck-image-processing-animation 2s linear infinite}.ck.ck-editor__editable .image.image-processing img{height:100%}@keyframes ck-image-processing-animation{0%{background-position:200% 0}to{background-position:-200% 0}}.ck.ck-editor__editable .ck.ck-clipboard-drop-target-position{display:inline;position:relative;pointer-events:none}.ck.ck-editor__editable .ck.ck-clipboard-drop-target-position span{position:absolute;width:0}.ck.ck-editor__editable .ck-widget:-webkit-drag>.ck-widget__selection-handle{display:none}.ck.ck-editor__editable .ck-widget:-webkit-drag>.ck-widget__type-around{display:none}.ck.ck-clipboard-drop-target-line{position:absolute;pointer-events:none}.ck-content pre{padding:1em;color:#353535;background:#c7c7c74d;border:1px solid hsl(0,0%,77%);border-radius:2px;text-align:left;direction:ltr;-moz-tab-size:4;tab-size:4;white-space:pre-wrap;font-style:normal;min-width:200px}.ck-content pre code{background:unset;padding:0;border-radius:0}.ck.ck-editor__editable pre{position:relative}.ck.ck-editor__editable pre[data-language]:after{content:attr(data-language);position:absolute}.ck.ck-editor{position:relative}.ck.ck-editor .ck-editor__top .ck-sticky-panel .ck-toolbar{z-index:var(--ck-z-panel)}.ck.ck-menu-bar{border:none;border-bottom:1px solid var(--ck-color-toolbar-border)}:root{--ck-emoji-grid-tile-size: 27px}.ck.ck-emoji .ck.ck-emoji__tiles{max-width:100%;max-height:min(265px,40vh);overflow-y:auto;overflow-x:hidden;border-top:1px solid var(--ck-color-base-border)}.ck.ck-emoji .ck.ck-emoji__tiles .ck-emoji__grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(var(--ck-emoji-grid-tile-size),1fr));margin:var(--ck-spacing-standard) var(--ck-spacing-large);grid-gap:var(--ck-spacing-small)}.ck.ck-emoji .ck.ck-emoji__tiles .ck-emoji__tile{width:var(--ck-emoji-grid-tile-size);height:var(--ck-emoji-grid-tile-size);min-width:var(--ck-emoji-grid-tile-size);min-height:var(--ck-emoji-grid-tile-size);font-size:1.5em;padding:0;transition:.2s ease box-shadow;border:0}@media (prefers-reduced-motion: reduce){.ck.ck-emoji .ck.ck-emoji__tiles .ck-emoji__tile{transition:none}}.ck.ck-emoji .ck.ck-emoji__tiles .ck-emoji__tile:focus:not(.ck-disabled),.ck.ck-emoji .ck.ck-emoji__tiles .ck-emoji__tile:hover:not(.ck-disabled){border:0;box-shadow:inset 0 0 0 1px var(--ck-color-base-background),0 0 0 2px var(--ck-color-focus-border)}.ck.ck-emoji .ck.ck-emoji__tiles .ck-emoji__tile .ck-button__label{line-height:var(--ck-emoji-grid-tile-size);width:100%;text-align:center}.ck.ck-emoji__categories-list{display:flex;justify-content:space-between;margin:0 var(--ck-spacing-large)}.ck.ck-emoji__categories-list>.ck.ck-button.ck-button_with-text{border-width:0;border-bottom-width:2px;border-bottom-style:solid;border-bottom-color:transparent;padding:0;font-size:var(--ck-font-size-big);min-width:var(--ck-font-size-big);min-height:var(--ck-font-size-big)}.ck.ck-emoji__categories-list>.ck.ck-button.ck-button_with-text.ck-emoji__category-item.ck-on{border-bottom-color:var(--ck-color-base-active)}.ck.ck-emoji__categories-list>.ck.ck-button.ck-button_with-text>span{margin:auto}.ck.ck-emoji__skin-tone{margin-left:var(--ck-spacing-standard)}.ck.ck-emoji__skin-tone>.ck.ck-dropdown .ck.ck-list__item{min-width:1em}.ck.ck-emoji__skin-tone>.ck.ck-dropdown .ck-button.ck-dropdown__button .ck-button__label{width:initial}.ck.ck-emoji{width:320px}.ck .ck.ck-emoji__search{display:flex;padding:var(--ck-spacing-large);padding-bottom:var(--ck-spacing-medium);justify-content:space-between;align-items:center}.ck .ck-fake-emoji-selection{background:var(--ck-color-link-fake-selection)}.ck .ck-fake-emoji-selection_collapsed{height:100%;border-right:1px solid var(--ck-color-base-text);margin-right:-1px;outline:solid 1px hsla(0,0%,100%,.5)}.ck.ck-placeholder,.ck .ck-placeholder{position:relative}.ck.ck-placeholder:before,.ck .ck-placeholder:before{position:absolute;left:0;right:0;content:attr(data-placeholder);pointer-events:none}.ck.ck-read-only .ck-placeholder:before{display:none}.ck.ck-reset_all .ck-placeholder{position:relative}.ck.ck-editor__editable span[data-ck-unsafe-element]{display:none}.ck-find-result{background:var(--ck-color-highlight-background);color:var(--ck-color-text)}.ck-find-result_selected{background:#ff9633}.ck.ck-find-and-replace-form{max-width:100%}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs,.ck.ck-find-and-replace-form .ck-find-and-replace-form__actions{display:flex}.ck.ck-find-and-replace-form .ck-find-and-replace-form__inputs.ck-find-and-replace-form__inputs .ck-results-counter,.ck.ck-find-and-replace-form .ck-find-and-replace-form__actions.ck-find-and-replace-form__inputs .ck-results-counter{position:absolute}.ck-content .text-tiny{font-size:.7em}.ck-content .text-small{font-size:.85em}.ck-content .text-big{font-size:1.4em}.ck-content .text-huge{font-size:1.8em}.ck.ck-heading_heading1 .ck-button__label{font-size:20px}.ck.ck-heading_heading2 .ck-button__label{font-size:17px}.ck.ck-heading_heading3 .ck-button__label{font-size:14px}.ck[class*=ck-heading_heading]{font-weight:700}:root{--ck-highlight-marker-yellow: hsl(60, 97%, 73%);--ck-highlight-marker-green: hsl(120, 93%, 68%);--ck-highlight-marker-pink: hsl(345, 96%, 73%);--ck-highlight-marker-blue: hsl(201, 97%, 72%);--ck-highlight-pen-red: hsl(0, 85%, 49%);--ck-highlight-pen-green: hsl(112, 100%, 27%)}.ck-content .marker-yellow{background-color:var(--ck-highlight-marker-yellow)}.ck-content .marker-green{background-color:var(--ck-highlight-marker-green)}.ck-content .marker-pink{background-color:var(--ck-highlight-marker-pink)}.ck-content .marker-blue{background-color:var(--ck-highlight-marker-blue)}.ck-content .pen-red{color:var(--ck-highlight-pen-red);background-color:transparent}.ck-content .pen-green{color:var(--ck-highlight-pen-green);background-color:transparent}.ck-editor__editable .ck-horizontal-line{display:flow-root}.ck-content hr{margin:15px 0;height:4px;background:#dedede;border:0}.ck-widget.raw-html-embed{margin:.9em auto;position:relative;display:flow-root;min-width:15em;font-style:normal}.ck-widget.raw-html-embed:before{position:absolute;z-index:1}.ck-widget.raw-html-embed .raw-html-embed__buttons-wrapper{position:absolute;display:flex;flex-direction:column}.ck-widget.raw-html-embed .raw-html-embed__preview{position:relative;overflow:hidden;display:flex}.ck-widget.raw-html-embed .raw-html-embed__preview-content{width:100%;position:relative;margin:auto;display:table;border-collapse:separate;border-spacing:7px}.ck-widget.raw-html-embed .raw-html-embed__preview-placeholder{position:absolute;left:0;top:0;right:0;bottom:0;display:flex;align-items:center;justify-content:center}:root{--ck-html-object-embed-unfocused-outline-width: 1px}.ck-widget.html-object-embed{font-size:var(--ck-font-size-base);background-color:var(--ck-color-base-foreground);padding:var(--ck-spacing-small);padding-top:calc(var(--ck-font-size-tiny) + var(--ck-spacing-large));min-width:calc(76px + var(--ck-spacing-standard))}.ck-widget.html-object-embed:not(.ck-widget_selected):not(:hover){outline:var(--ck-html-object-embed-unfocused-outline-width) dashed var(--ck-color-widget-blurred-border)}.ck-widget.html-object-embed:before{font-weight:400;font-style:normal;position:absolute;content:attr(data-html-object-embed-label);top:0;left:var(--ck-spacing-standard);background:#999;transition:background var(--ck-widget-handler-animation-duration) var(--ck-widget-handler-animation-curve);padding:calc(var(--ck-spacing-tiny) + var(--ck-html-object-embed-unfocused-outline-width)) var(--ck-spacing-small) var(--ck-spacing-tiny);border-radius:0 0 var(--ck-border-radius) var(--ck-border-radius);color:var(--ck-color-base-background);font-size:var(--ck-font-size-tiny);font-family:var(--ck-font-face)}.ck-widget.html-object-embed .ck-widget__type-around .ck-widget__type-around__button.ck-widget__type-around__button_before{margin-left:50px}.ck-widget.html-object-embed .html-object-embed__content{pointer-events:none}div.ck-widget.html-object-embed{margin:1em auto}span.ck-widget.html-object-embed{display:inline-block}:root{--ck-color-image-caption-background: hsl(0, 0%, 97%);--ck-color-image-caption-text: hsl(0, 0%, 20%);--ck-color-image-caption-highlighted-background: hsl(52deg 100% 50%)}.ck-content .image>figcaption{display:table-caption;caption-side:bottom;word-break:break-word;color:var(--ck-color-image-caption-text);background-color:var(--ck-color-image-caption-background);padding:.6em;font-size:.75em;outline-offset:-1px}@media (forced-colors: active){.ck-content .image>figcaption{background-color:unset;color:unset}}@media (forced-colors: none){.ck.ck-editor__editable .image>figcaption.image__caption_highlighted{animation:ck-image-caption-highlight .6s ease-out}}@media (prefers-reduced-motion: reduce){.ck.ck-editor__editable .image>figcaption.image__caption_highlighted{animation:none}}@keyframes ck-image-caption-highlight{0%{background-color:var(--ck-color-image-caption-highlighted-background)}to{background-color:var(--ck-color-image-caption-background)}}.ck.ck-image-insert-url{width:400px;padding:var(--ck-spacing-large) var(--ck-spacing-large) 0}.ck.ck-image-insert-url .ck-image-insert-url__action-row{display:grid;grid-template-columns:repeat(2,1fr)}.ck-content img.image_resized{height:auto}.ck-content .image.image_resized{max-width:100%;display:block;box-sizing:border-box}.ck-content .image.image_resized img{width:100%}.ck-content .image.image_resized>figcaption{display:block}.ck.ck-editor__editable td .image-inline.image_resized img,.ck.ck-editor__editable th .image-inline.image_resized img{max-width:100%}[dir=ltr] .ck.ck-button.ck-button_with-text.ck-resize-image-button .ck-button__icon{margin-right:var(--ck-spacing-standard)}[dir=rtl] .ck.ck-button.ck-button_with-text.ck-resize-image-button .ck-button__icon{margin-left:var(--ck-spacing-standard)}.ck.ck-dropdown .ck-button.ck-resize-image-button .ck-button__label{width:4em}.ck.ck-image-custom-resize-form{display:flex;flex-direction:row;flex-wrap:nowrap;align-items:flex-start}.ck.ck-image-custom-resize-form .ck-labeled-field-view{display:inline-block}.ck.ck-image-custom-resize-form .ck-label{display:none}@media screen and (max-width: 600px){.ck.ck-image-custom-resize-form{flex-wrap:wrap}.ck.ck-image-custom-resize-form .ck-labeled-field-view{flex-basis:100%}.ck.ck-image-custom-resize-form .ck-button{flex-basis:50%}}:root{--ck-image-style-spacing: 1.5em;--ck-inline-image-style-spacing: calc(var(--ck-image-style-spacing) / 2)}.ck-content .image.image-style-block-align-left,.ck-content .image.image-style-block-align-right{max-width:calc(100% - var(--ck-image-style-spacing))}.ck-content .image.image-style-align-left,.ck-content .image.image-style-align-right{clear:none}.ck-content .image.image-style-side{float:right;margin-left:var(--ck-image-style-spacing);max-width:50%}.ck-content .image.image-style-align-left{float:left;margin-right:var(--ck-image-style-spacing)}.ck-content .image.image-style-align-right{float:right;margin-left:var(--ck-image-style-spacing)}.ck-content .image.image-style-block-align-right{margin-right:0;margin-left:auto}.ck-content .image.image-style-block-align-left{margin-left:0;margin-right:auto}.ck-content .image-style-align-center{margin-left:auto;margin-right:auto}.ck-content .image-style-align-left{float:left;margin-right:var(--ck-image-style-spacing)}.ck-content .image-style-align-right{float:right;margin-left:var(--ck-image-style-spacing)}.ck-content p+.image.image-style-align-left,.ck-content p+.image.image-style-align-right,.ck-content p+.image.image-style-side{margin-top:0}.ck-content .image-inline.image-style-align-left,.ck-content .image-inline.image-style-align-right{margin-top:var(--ck-inline-image-style-spacing);margin-bottom:var(--ck-inline-image-style-spacing)}.ck-content .image-inline.image-style-align-left{margin-right:var(--ck-inline-image-style-spacing)}.ck-content .image-inline.image-style-align-right{margin-left:var(--ck-inline-image-style-spacing)}.ck.ck-splitbutton.ck-splitbutton_flatten:hover>.ck-splitbutton__action:not(.ck-disabled),.ck.ck-splitbutton.ck-splitbutton_flatten.ck-splitbutton_open>.ck-splitbutton__action:not(.ck-disabled),.ck.ck-splitbutton.ck-splitbutton_flatten:hover>.ck-splitbutton__arrow:not(.ck-disabled),.ck.ck-splitbutton.ck-splitbutton_flatten.ck-splitbutton_open>.ck-splitbutton__arrow:not(.ck-disabled),.ck.ck-splitbutton.ck-splitbutton_flatten:hover>.ck-splitbutton__arrow:not(.ck-disabled):not(:hover),.ck.ck-splitbutton.ck-splitbutton_flatten.ck-splitbutton_open>.ck-splitbutton__arrow:not(.ck-disabled):not(:hover){background-color:var(--ck-color-button-on-background)}.ck.ck-splitbutton.ck-splitbutton_flatten:hover>.ck-splitbutton__action:not(.ck-disabled):after,.ck.ck-splitbutton.ck-splitbutton_flatten.ck-splitbutton_open>.ck-splitbutton__action:not(.ck-disabled):after,.ck.ck-splitbutton.ck-splitbutton_flatten:hover>.ck-splitbutton__arrow:not(.ck-disabled):after,.ck.ck-splitbutton.ck-splitbutton_flatten.ck-splitbutton_open>.ck-splitbutton__arrow:not(.ck-disabled):after,.ck.ck-splitbutton.ck-splitbutton_flatten:hover>.ck-splitbutton__arrow:not(.ck-disabled):not(:hover):after,.ck.ck-splitbutton.ck-splitbutton_flatten.ck-splitbutton_open>.ck-splitbutton__arrow:not(.ck-disabled):not(:hover):after{display:none}.ck.ck-splitbutton.ck-splitbutton_flatten.ck-splitbutton_open:hover>.ck-splitbutton__action:not(.ck-disabled),.ck.ck-splitbutton.ck-splitbutton_flatten.ck-splitbutton_open:hover>.ck-splitbutton__arrow:not(.ck-disabled),.ck.ck-splitbutton.ck-splitbutton_flatten.ck-splitbutton_open:hover>.ck-splitbutton__arrow:not(.ck-disabled):not(:hover){background-color:var(--ck-color-button-on-hover-background)}.ck.ck-text-alternative-form{display:flex;flex-direction:row;flex-wrap:nowrap}.ck.ck-text-alternative-form .ck-labeled-field-view{display:inline-block}.ck.ck-text-alternative-form .ck-label{display:none}@media screen and (max-width: 600px){.ck.ck-text-alternative-form{flex-wrap:wrap}.ck.ck-text-alternative-form .ck-labeled-field-view{flex-basis:100%}.ck.ck-text-alternative-form .ck-button{flex-basis:50%}}.ck.ck-editor__editable .image,.ck.ck-editor__editable .image-inline{position:relative}.ck.ck-editor__editable .image .ck-progress-bar,.ck.ck-editor__editable .image-inline .ck-progress-bar{position:absolute;top:0;left:0}.ck-image-upload-complete-icon{display:block;position:absolute;top:min(var(--ck-spacing-medium),6%);right:min(var(--ck-spacing-medium),6%);border-radius:50%;z-index:1}.ck-image-upload-complete-icon:after{content:"";position:absolute}.ck .ck-upload-placeholder-loader{position:absolute;display:flex;align-items:center;justify-content:center;top:0;left:0}.ck .ck-upload-placeholder-loader:before{content:"";position:relative}.ck-content .image{display:table;clear:both;text-align:center;margin:.9em auto;min-width:50px}.ck-content .image img{display:block;margin:0 auto;max-width:100%;min-width:100%;height:auto}.ck-content .image-inline{display:inline-flex;max-width:100%;align-items:flex-start}.ck-content .image-inline picture{display:flex}.ck-content .image-inline picture,.ck-content .image-inline img{flex-grow:1;flex-shrink:1;max-width:100%}.ck.ck-editor__editable .image>figcaption.ck-placeholder:before{padding-left:inherit;padding-right:inherit;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.ck.ck-editor__editable .image{z-index:1}.ck.ck-editor__editable .image.ck-widget_selected{z-index:2}.ck.ck-editor__editable .image-inline{z-index:1}.ck.ck-editor__editable .image-inline.ck-widget_selected{z-index:2}.ck.ck-editor__editable .image-inline.ck-widget_selected ::selection{display:none}.ck.ck-editor__editable .image-inline img{height:auto}.ck.ck-editor__editable td .image-inline img,.ck.ck-editor__editable th .image-inline img{max-width:none}.ck.ck-editor__editable img.image_placeholder{background-size:100% 100%}.ck.ck-editor__editable figure.image>a:after,.ck.ck-editor__editable a span.image-inline:after{display:block;position:absolute}.ck.ck-link-actions{display:flex;flex-direction:row;flex-wrap:nowrap}.ck.ck-link-actions .ck-link-actions__preview{display:inline-block}.ck.ck-link-actions .ck-link-actions__preview .ck-button__label{overflow:hidden}@media screen and (max-width: 600px){.ck.ck-link-actions{flex-wrap:wrap}.ck.ck-link-actions .ck-link-actions__preview{flex-basis:100%}.ck.ck-link-actions .ck-button:not(.ck-link-actions__preview){flex-basis:50%}}.ck.ck-link-form{display:flex;align-items:flex-start}.ck.ck-link-form .ck-label{display:none}@media screen and (max-width: 600px){.ck.ck-link-form{flex-wrap:wrap}.ck.ck-link-form .ck-labeled-field-view{flex-basis:100%}.ck.ck-link-form .ck-button{flex-basis:50%}}.ck.ck-link-form_layout-vertical{display:block}.ck.ck-link-form_layout-vertical .ck-button.ck-button-save,.ck.ck-link-form_layout-vertical .ck-button.ck-button-cancel{margin-top:var(--ck-spacing-medium)}.ck-editor__editable .ck-list-bogus-paragraph{display:block}.ck.ck-list-styles-list{display:grid}.ck-content ol{list-style-type:decimal}.ck-content ol ol{list-style-type:lower-latin}.ck-content ol ol ol{list-style-type:lower-roman}.ck-content ol ol ol ol{list-style-type:upper-latin}.ck-content ol ol ol ol ol{list-style-type:upper-roman}.ck-content ul{list-style-type:disc}.ck-content ul ul{list-style-type:circle}.ck-content ul ul ul,.ck-content ul ul ul ul{list-style-type:square}:root{--ck-todo-list-checkmark-size: 16px}.ck-content .todo-list{list-style:none}.ck-content .todo-list li{position:relative;margin-bottom:5px}.ck-content .todo-list li .todo-list{margin-top:5px}.ck-content .todo-list .todo-list__label>input{-webkit-appearance:none;display:inline-block;position:relative;width:var(--ck-todo-list-checkmark-size);height:var(--ck-todo-list-checkmark-size);vertical-align:middle;border:0;left:-25px;margin-right:-15px;right:0;margin-left:0}.ck-content[dir=rtl] .todo-list .todo-list__label>input{left:0;margin-right:0;right:-25px;margin-left:-15px}.ck-content .todo-list .todo-list__label>input:before{display:block;position:absolute;box-sizing:border-box;content:"";width:100%;height:100%;border:1px solid hsl(0,0%,20%);border-radius:2px;transition:.25s ease-in-out box-shadow}@media (prefers-reduced-motion: reduce){.ck-content .todo-list .todo-list__label>input:before{transition:none}}.ck-content .todo-list .todo-list__label>input:after{display:block;position:absolute;box-sizing:content-box;pointer-events:none;content:"";left:calc(var(--ck-todo-list-checkmark-size) / 3);top:calc(var(--ck-todo-list-checkmark-size) / 5.3);width:calc(var(--ck-todo-list-checkmark-size) / 5.3);height:calc(var(--ck-todo-list-checkmark-size) / 2.6);border-style:solid;border-color:transparent;border-width:0 calc(var(--ck-todo-list-checkmark-size) / 8) calc(var(--ck-todo-list-checkmark-size) / 8) 0;transform:rotate(45deg)}.ck-content .todo-list .todo-list__label>input[checked]:before{background:#26ab33;border-color:#26ab33}.ck-content .todo-list .todo-list__label>input[checked]:after{border-color:#fff}.ck-content .todo-list .todo-list__label .todo-list__label__description{vertical-align:middle}.ck-content .todo-list .todo-list__label.todo-list__label_without-description input[type=checkbox]{position:absolute}.ck-editor__editable.ck-content .todo-list .todo-list__label>input,.ck-editor__editable.ck-content .todo-list .todo-list__label>span[contenteditable=false]>input{cursor:pointer}.ck-editor__editable.ck-content .todo-list .todo-list__label>input:hover:before,.ck-editor__editable.ck-content .todo-list .todo-list__label>span[contenteditable=false]>input:hover:before{box-shadow:0 0 0 5px #0000001a}.ck-editor__editable.ck-content .todo-list .todo-list__label>span[contenteditable=false]>input{-webkit-appearance:none;display:inline-block;position:relative;width:var(--ck-todo-list-checkmark-size);height:var(--ck-todo-list-checkmark-size);vertical-align:middle;border:0;left:-25px;margin-right:-15px;right:0;margin-left:0}.ck-editor__editable.ck-content[dir=rtl] .todo-list .todo-list__label>span[contenteditable=false]>input{left:0;margin-right:0;right:-25px;margin-left:-15px}.ck-editor__editable.ck-content .todo-list .todo-list__label>span[contenteditable=false]>input:before{display:block;position:absolute;box-sizing:border-box;content:"";width:100%;height:100%;border:1px solid hsl(0,0%,20%);border-radius:2px;transition:.25s ease-in-out box-shadow}@media (prefers-reduced-motion: reduce){.ck-editor__editable.ck-content .todo-list .todo-list__label>span[contenteditable=false]>input:before{transition:none}}.ck-editor__editable.ck-content .todo-list .todo-list__label>span[contenteditable=false]>input:after{display:block;position:absolute;box-sizing:content-box;pointer-events:none;content:"";left:calc(var(--ck-todo-list-checkmark-size) / 3);top:calc(var(--ck-todo-list-checkmark-size) / 5.3);width:calc(var(--ck-todo-list-checkmark-size) / 5.3);height:calc(var(--ck-todo-list-checkmark-size) / 2.6);border-style:solid;border-color:transparent;border-width:0 calc(var(--ck-todo-list-checkmark-size) / 8) calc(var(--ck-todo-list-checkmark-size) / 8) 0;transform:rotate(45deg)}.ck-editor__editable.ck-content .todo-list .todo-list__label>span[contenteditable=false]>input[checked]:before{background:#26ab33;border-color:#26ab33}.ck-editor__editable.ck-content .todo-list .todo-list__label>span[contenteditable=false]>input[checked]:after{border-color:#fff}.ck-editor__editable.ck-content .todo-list .todo-list__label.todo-list__label_without-description input[type=checkbox]{position:absolute}.ck-content .media{clear:both;margin:.9em 0;display:block;min-width:15em}.ck-media__wrapper .ck-media__placeholder{display:flex;flex-direction:column;align-items:center}.ck-media__wrapper .ck-media__placeholder .ck-media__placeholder__url{max-width:100%;position:relative}.ck-media__wrapper .ck-media__placeholder .ck-media__placeholder__url .ck-media__placeholder__url__text{overflow:hidden;display:block}.ck-media__wrapper[data-oembed-url*="twitter.com"] .ck-media__placeholder__icon *,.ck-media__wrapper[data-oembed-url*="google.com/maps"] .ck-media__placeholder__icon *,.ck-media__wrapper[data-oembed-url*="goo.gl/maps"] .ck-media__placeholder__icon *,.ck-media__wrapper[data-oembed-url*="maps.google.com"] .ck-media__placeholder__icon *,.ck-media__wrapper[data-oembed-url*="maps.app.goo.gl"] .ck-media__placeholder__icon *,.ck-media__wrapper[data-oembed-url*="facebook.com"] .ck-media__placeholder__icon *,.ck-media__wrapper[data-oembed-url*="instagram.com"] .ck-media__placeholder__icon *{display:none}.ck-editor__editable:not(.ck-read-only) .ck-media__wrapper>*:not(.ck-media__placeholder){pointer-events:none}.ck-editor__editable:not(.ck-read-only) .ck-widget:not(.ck-widget_selected) .ck-media__placeholder{pointer-events:none}.ck-vertical-form .ck-button:after{content:"";width:0;position:absolute;right:-1px;top:-1px;bottom:-1px;z-index:1}.ck-vertical-form .ck-button:focus:after{display:none}@media screen and (max-width: 600px){.ck.ck-responsive-form .ck-button:after{content:"";width:0;position:absolute;right:-1px;top:-1px;bottom:-1px;z-index:1}.ck.ck-responsive-form .ck-button:focus:after{display:none}}.ck.ck-media-form{display:flex;align-items:flex-start;flex-direction:row;flex-wrap:nowrap;width:400px}.ck.ck-media-form .ck-labeled-field-view{display:inline-block;width:100%}.ck.ck-media-form .ck-label{display:none}.ck.ck-media-form .ck-input{width:100%}@media screen and (max-width: 600px){.ck.ck-media-form{flex-wrap:wrap}.ck.ck-media-form .ck-labeled-field-view{flex-basis:100%}.ck.ck-media-form .ck-button{flex-basis:50%}}:root{--ck-mention-list-max-height: 300px}.ck.ck-mentions{max-height:var(--ck-mention-list-max-height);overflow-y:auto;overflow-x:hidden;overscroll-behavior:contain}.ck.ck-mentions>.ck-list__item{overflow:hidden;flex-shrink:0}:root{--ck-color-minimap-tracker-background: 208, 0%, 51%;--ck-color-minimap-iframe-outline: hsl(0deg 0% 75%);--ck-color-minimap-iframe-shadow: hsl(0deg 0% 0% / 11%);--ck-color-minimap-progress-background: hsl(0,0%,40%)}.ck.ck-minimap{position:absolute;-webkit-user-select:none;user-select:none;background:var(--ck-color-base-background)}.ck.ck-minimap,.ck.ck-minimap iframe{width:100%;height:100%}.ck.ck-minimap iframe{border:0;pointer-events:none;position:relative;outline:1px solid var(--ck-color-minimap-iframe-outline);box-shadow:0 2px 5px var(--ck-color-minimap-iframe-shadow);margin:0}.ck.ck-minimap .ck.ck-minimap__position-tracker{position:absolute;width:100%;top:0;background:hsla(var(--ck-color-minimap-tracker-background),.2);z-index:1;transition:background .1s ease-in-out}@media (prefers-reduced-motion: reduce){.ck.ck-minimap .ck.ck-minimap__position-tracker{transition:none}}.ck.ck-minimap .ck.ck-minimap__position-tracker:hover{background:hsla(var(--ck-color-minimap-tracker-background),.3)}.ck.ck-minimap .ck.ck-minimap__position-tracker.ck-minimap__position-tracker_dragging,.ck.ck-minimap .ck.ck-minimap__position-tracker.ck-minimap__position-tracker_dragging:hover{background:hsla(var(--ck-color-minimap-tracker-background),.4)}.ck.ck-minimap .ck.ck-minimap__position-tracker.ck-minimap__position-tracker_dragging:after,.ck.ck-minimap .ck.ck-minimap__position-tracker.ck-minimap__position-tracker_dragging:hover:after{opacity:1}.ck.ck-minimap .ck.ck-minimap__position-tracker:after{content:attr(data-progress) "%";position:absolute;top:5px;right:5px;background:var(--ck-color-minimap-progress-background);color:var(--ck-color-base-background);border:1px solid var(--ck-color-base-background);padding:2px 4px;font-size:10px;border-radius:3px;opacity:0;transition:opacity .1s ease-in-out}@media (prefers-reduced-motion: reduce){.ck.ck-minimap .ck.ck-minimap__position-tracker:after{transition:none}}.ck-content .page-break{position:relative;clear:both;padding:5px 0;display:flex;align-items:center;justify-content:center}.ck-content .page-break:after{content:"";position:absolute;border-bottom:2px dashed hsl(0,0%,77%);width:100%}.ck-content .page-break__label{position:relative;z-index:1;padding:.3em .6em;display:block;text-transform:uppercase;border:1px solid hsl(0,0%,77%);border-radius:2px;font-family:Helvetica,Arial,Tahoma,Verdana,Sans-Serif;font-size:.75em;font-weight:700;color:#333;background:#fff;box-shadow:2px 2px 1px #00000026;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none}@media print{.ck-content .page-break{padding:0}.ck-content .page-break:after{display:none}.ck-content *:has(+.page-break){margin-bottom:0}}:root{--ck-show-blocks-border-color: hsl(0, 0%, 46%)}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) address{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) address{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>ADDRESS</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) address{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>ADDRESS</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) address:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) address{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>ADDRESS</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) address{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>ADDRESS</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) aside{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) aside{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>ASIDE</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) aside{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>ASIDE</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) aside:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) aside{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>ASIDE</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) aside{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>ASIDE</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) blockquote{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) blockquote{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>BLOCKQUOTE</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) blockquote{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>BLOCKQUOTE</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) blockquote:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) blockquote{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>BLOCKQUOTE</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) blockquote{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>BLOCKQUOTE</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) details{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) details{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>DETAILS</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) details{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>DETAILS</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) details:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) details{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>DETAILS</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) details{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>DETAILS</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) div:not(.ck-widget,.ck-widget *){background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) div:not(.ck-widget,.ck-widget *){background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>DIV</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) div:not(.ck-widget,.ck-widget *){background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>DIV</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) div:not(.ck-widget,.ck-widget *):not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) div:not(.ck-widget,.ck-widget *){background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>DIV</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) div:not(.ck-widget,.ck-widget *){background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>DIV</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) footer{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) footer{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>FOOTER</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) footer{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>FOOTER</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) footer:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) footer{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>FOOTER</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) footer{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>FOOTER</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h1{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h1{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H1</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h1{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H1</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h1:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h1{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H1</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h1{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H1</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h2{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h2{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H2</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h2{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H2</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h2:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h2{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H2</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h2{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H2</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h3{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h3{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H3</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h3{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H3</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h3:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h3{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H3</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h3{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H3</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h4{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h4{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H4</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h4{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H4</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h4:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h4{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H4</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h4{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H4</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h5{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h5{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H5</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h5{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H5</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h5:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h5{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H5</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h5{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H5</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h6{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h6{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H6</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h6{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H6</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h6:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h6{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H6</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) h6{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>H6</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) header{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) header{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>HEADER</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) header{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>HEADER</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) header:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) header{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>HEADER</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) header{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>HEADER</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) main{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) main{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>MAIN</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) main{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>MAIN</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) main:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) main{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>MAIN</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) main{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>MAIN</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) nav{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) nav{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>NAV</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) nav{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>NAV</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) nav:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) nav{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>NAV</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) nav{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>NAV</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) pre{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) pre{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>PRE</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) pre{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>PRE</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) pre:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) pre{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>PRE</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) pre{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>PRE</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ol{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ol{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>OL</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ol{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>OL</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ol:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ol{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>OL</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ol{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>OL</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ul{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ul{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>UL</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ul{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>UL</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ul:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ul{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>UL</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) ul{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>UL</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) p{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) p{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>P</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) p{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>P</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) p:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) p{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>P</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) p{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>P</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) section{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) section{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>SECTION</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) section{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>SECTION</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) section:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) section{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>SECTION</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) section{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>SECTION</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) :where(figure.image,figure.table) figcaption{background-repeat:no-repeat;padding-top:15px}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) :where(figure.image,figure.table) figcaption{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>FIGCAPTION</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) :where(figure.image,figure.table) figcaption{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>FIGCAPTION</text></svg>");background-position:calc(100% - 1px) 1px}.ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) :where(figure.image,figure.table) figcaption:not(.ck-widget_selected):not(.ck-widget:hover){outline:1px dashed var(--ck-show-blocks-border-color)}[dir=ltr] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) :where(figure.image,figure.table) figcaption{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='3' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>FIGCAPTION</text></svg>");background-position:1px 1px}[dir=rtl] .ck.ck-editor__editable.ck-editor__editable_inline.ck-show-blocks:not(.ck-widget) :where(figure.image,figure.table) figcaption{background-image:url("data:image/svg+xml;utf8,<svg width='120' height='12' xmlns='http://www.w3.org/2000/svg' ><text style='paint-order:stroke fill; clip-path: inset(-3px); transform:translate(-2px, 0)' stroke='%23EAEAEA' stroke-width='13' dominant-baseline='middle' fill='black' x='100%' text-anchor='end' y='7' font-size='9px' font-family='Consolas, %22Lucida Console%22, %22Lucida Sans Typewriter%22, %22DejaVu Sans Mono%22, %22Bitstream Vera Sans Mono%22, %22Liberation Mono%22, Monaco, %22Courier New%22, Courier, monospace'>FIGCAPTION</text></svg>");background-position:calc(100% - 1px) 1px}.ck-source-editing-area{position:relative;overflow:hidden}.ck-source-editing-area:after,.ck-source-editing-area textarea{padding:var(--ck-spacing-large);margin:0;border:1px solid transparent;line-height:var(--ck-line-height-base);font-size:var(--ck-font-size-normal);font-family:monospace;white-space:pre-wrap}.ck-source-editing-area:after{content:attr(data-value) " ";visibility:hidden;display:block}.ck-source-editing-area textarea{position:absolute;width:100%;height:100%;resize:none;outline:none;overflow:hidden;box-sizing:border-box;border-color:var(--ck-color-base-border);border-radius:0}.ck-rounded-corners .ck-source-editing-area textarea,.ck-source-editing-area textarea.ck-rounded-corners{border-radius:var(--ck-border-radius);border-top-left-radius:0;border-top-right-radius:0}.ck-source-editing-area textarea:not([readonly]):focus{outline:none;border:var(--ck-focus-ring);box-shadow:var(--ck-inner-shadow),0 0}.ck.ck-character-grid{max-width:100%}.ck.ck-character-grid .ck-character-grid__tiles{display:grid}.ck.ck-character-info{display:flex;justify-content:space-between}.ck.ck-special-characters>.ck-dialog__content>div{width:350px;max-width:100%;height:100%;display:grid;grid-column-gap:0px;grid-row-gap:0px;grid-template-columns:1fr;grid-template-rows:auto 1fr auto}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories{grid-area:1 / 1 / 2 / 2;padding:var(--ck-spacing-medium) var(--ck-spacing-large)}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories>.ck-labeled-field-view{padding-top:var(--ck-spacing-standard);width:100%}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories>.ck-labeled-field-view .ck.ck-labeled-field-view__status{background:var(--ck-color-base-error);color:var(--ck-color-base-background);padding:var(--ck-spacing-small) var(--ck-spacing-medium);min-width:var(--ck-table-properties-min-error-width);text-align:center;animation:ck-table-form-labeled-view-status-appear .15s ease both}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories>.ck-labeled-field-view .ck.ck-labeled-field-view__status:after{border-color:transparent transparent var(--ck-color-base-error) transparent;border-width:0 var(--ck-table-properties-error-arrow-size) var(--ck-table-properties-error-arrow-size) var(--ck-table-properties-error-arrow-size);border-style:solid}@media (prefers-reduced-motion: reduce){.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories>.ck-labeled-field-view .ck.ck-labeled-field-view__status{animation:none}}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories>.ck-labeled-field-view .ck-input.ck-error:not(:focus)+.ck.ck-labeled-field-view__status{display:none}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories>.ck-labeled-field-view>.ck-label{font-size:var(--ck-font-size-tiny);text-align:center}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories .ck-dropdown{display:block;width:100%}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories .ck-dropdown>button:not(:focus){border:1px solid var(--ck-color-base-border)}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-categories .ck-dropdown>button>span{width:100%}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-grid{grid-area:2 / 1 / 3 / 2;max-height:200px}.ck.ck-special-characters>.ck-dialog__content>div>.ck-character-info{grid-area:3 / 1 / 4 / 2}:root{--ck-style-panel-columns: 3}.ck.ck-style-panel .ck-style-grid{display:grid;grid-template-columns:repeat(var(--ck-style-panel-columns),auto);justify-content:start}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button{display:flex;justify-content:space-between;flex-direction:column}.ck.ck-style-panel .ck-style-grid .ck-style-grid__button .ck-style-grid__button__preview{display:flex;align-content:center;justify-content:flex-start;align-items:center;flex-grow:1;flex-basis:100%}.ck-content .table{margin:.9em auto;display:table}.ck-content .table table{border-collapse:collapse;border-spacing:0;width:100%;height:100%;border:1px double hsl(0,0%,70%)}.ck-content .table table td,.ck-content .table table th{min-width:2em;padding:.4em;border:1px solid hsl(0,0%,75%)}.ck-content .table table th{font-weight:700;background:#0000000d}@media print{.ck-content .table table{height:initial}}.ck-content[dir=rtl] .table th{text-align:right}.ck-content[dir=ltr] .table th{text-align:left}.ck-editor__editable .ck-table-bogus-paragraph{display:inline-block;width:100%}.ck .ck-insert-table-dropdown__grid{display:flex;flex-direction:row;flex-wrap:wrap}.ck.ck-form__row{display:flex;flex-direction:row;flex-wrap:nowrap;justify-content:space-between}.ck.ck-form__row>*:not(.ck-label){flex-grow:1}.ck.ck-form__row.ck-table-form__action-row .ck-button-save,.ck.ck-form__row.ck-table-form__action-row .ck-button-cancel{justify-content:center}.ck.ck-table-cell-properties-form .ck-form__row.ck-table-cell-properties-form__alignment-row{flex-wrap:wrap}.ck.ck-table-cell-properties-form .ck-form__row.ck-table-cell-properties-form__alignment-row .ck.ck-toolbar:first-of-type{flex-grow:.57}.ck.ck-table-cell-properties-form .ck-form__row.ck-table-cell-properties-form__alignment-row .ck.ck-toolbar:last-of-type{flex-grow:.43}.ck.ck-table-cell-properties-form .ck-form__row.ck-table-cell-properties-form__alignment-row .ck.ck-toolbar .ck-button{flex-grow:1}.ck.ck-input-color{width:100%;display:flex;flex-direction:row-reverse}.ck.ck-input-color>input.ck.ck-input-text{min-width:auto;flex-grow:1}.ck.ck-input-color>div.ck.ck-dropdown{min-width:auto}.ck.ck-input-color>div.ck.ck-dropdown>.ck-input-color__button .ck-dropdown__arrow{display:none}.ck.ck-input-color .ck.ck-input-color__button{display:flex}.ck.ck-input-color .ck.ck-input-color__button .ck.ck-input-color__button__preview{position:relative;overflow:hidden}.ck.ck-input-color .ck.ck-input-color__button .ck.ck-input-color__button__preview>.ck.ck-input-color__button__preview__no-color-indicator{position:absolute;display:block}.ck.ck-table-form .ck-form__row.ck-table-form__border-row,.ck.ck-table-form .ck-form__row.ck-table-form__background-row{flex-wrap:wrap}.ck.ck-table-form .ck-form__row.ck-table-form__dimensions-row{flex-wrap:wrap;align-items:center}.ck.ck-table-form .ck-form__row.ck-table-form__dimensions-row .ck-labeled-field-view{display:flex;flex-direction:column-reverse;align-items:center}.ck.ck-table-form .ck-form__row.ck-table-form__dimensions-row .ck-labeled-field-view .ck.ck-dropdown,.ck.ck-table-form .ck-form__row.ck-table-form__dimensions-row .ck-table-form__dimension-operator{flex-grow:0}.ck.ck-table-form .ck.ck-labeled-field-view{position:relative}.ck.ck-table-form .ck.ck-labeled-field-view .ck.ck-labeled-field-view__status{position:absolute;left:50%;bottom:calc(-1 * var(--ck-table-properties-error-arrow-size));transform:translate(-50%,100%);z-index:1}.ck.ck-table-form .ck.ck-labeled-field-view .ck.ck-labeled-field-view__status:after{content:"";position:absolute;top:calc(-1 * var(--ck-table-properties-error-arrow-size));left:50%;transform:translate(-50%)}.ck.ck-table-properties-form .ck-form__row.ck-table-properties-form__alignment-row{flex-wrap:wrap;flex-basis:0;align-content:baseline}.ck.ck-table-properties-form .ck-form__row.ck-table-properties-form__alignment-row .ck.ck-toolbar .ck-toolbar__items{flex-wrap:nowrap}:root{--ck-color-selector-caption-background: hsl(0, 0%, 97%);--ck-color-selector-caption-text: hsl(0, 0%, 20%);--ck-color-selector-caption-highlighted-background: hsl(52deg 100% 50%)}.ck-content .table>figcaption{display:table-caption;caption-side:top;word-break:break-word;text-align:center;color:var(--ck-color-selector-caption-text);background-color:var(--ck-color-selector-caption-background);padding:.6em;font-size:.75em;outline-offset:-1px}@media (forced-colors: active){.ck-content .table>figcaption{background-color:unset;color:unset}}@media (forced-colors: none){.ck.ck-editor__editable .table>figcaption.table__caption_highlighted{animation:ck-table-caption-highlight .6s ease-out}}.ck.ck-editor__editable .table>figcaption.ck-placeholder:before{padding-left:inherit;padding-right:inherit;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}@keyframes ck-table-caption-highlight{0%{background-color:var(--ck-color-selector-caption-highlighted-background)}to{background-color:var(--ck-color-selector-caption-background)}}:root{--ck-color-selector-column-resizer-hover: var(--ck-color-base-active);--ck-table-column-resizer-width: 7px;--ck-table-column-resizer-position-offset: calc(var(--ck-table-column-resizer-width) * -.5 - .5px)}.ck-content .table .ck-table-resized{table-layout:fixed}.ck-content .table table{overflow:hidden}.ck-content .table td,.ck-content .table th{overflow-wrap:break-word;position:relative}.ck.ck-editor__editable .table .ck-table-column-resizer{position:absolute;top:0;bottom:0;right:var(--ck-table-column-resizer-position-offset);width:var(--ck-table-column-resizer-width);cursor:col-resize;-webkit-user-select:none;user-select:none;z-index:var(--ck-z-default)}.ck.ck-editor__editable.ck-column-resize_disabled .table .ck-table-column-resizer,.ck.ck-editor__editable .table[draggable] .ck-table-column-resizer{display:none}.ck.ck-editor__editable .table .ck-table-column-resizer:hover,.ck.ck-editor__editable .table .ck-table-column-resizer__active{background-color:var(--ck-color-selector-column-resizer-hover);opacity:.25;top:-999999px;bottom:-999999px}.ck.ck-editor__editable[dir=rtl] .table .ck-table-column-resizer{left:var(--ck-table-column-resizer-position-offset);right:unset}.ck-hidden{display:none!important}:root{--ck-z-default: 1;--ck-z-panel: calc( var(--ck-z-default) + 999 );--ck-z-dialog: 9999}.ck-transitions-disabled,.ck-transitions-disabled *{transition:none!important}:root{--ck-powered-by-font-size: calc(var(--ck-font-size-base) * 7.5 / 13);--ck-powered-by-line-height: calc(var(--ck-font-size-base) * 10 / 13);--ck-powered-by-letter-spacing: calc(var(--ck-font-size-base) * -.2 / 13);--ck-powered-by-padding-vertical: 2px;--ck-powered-by-padding-horizontal: 4px;--ck-powered-by-text-color: hsl(0, 0%, 31%);--ck-powered-by-border-radius: var(--ck-border-radius);--ck-powered-by-background: hsl(0, 0%, 100%);--ck-powered-by-border-color: var(--ck-color-focus-border);--ck-powered-by-svg-width: 53;--ck-powered-by-svg-height: 10;--ck-powered-by-icon-width: calc(var(--ck-font-size-base) * var(--ck-powered-by-svg-width) / 13);--ck-powered-by-icon-height: calc(var(--ck-font-size-base) * var(--ck-powered-by-svg-height) / 13)}.ck.ck-balloon-panel.ck-powered-by-balloon{--ck-border-radius: var(--ck-powered-by-border-radius);box-shadow:none;background:var(--ck-powered-by-background);min-height:unset;z-index:calc(var(--ck-z-panel) - 1)}.ck.ck-balloon-panel.ck-powered-by-balloon .ck.ck-powered-by{line-height:var(--ck-powered-by-line-height)}.ck.ck-balloon-panel.ck-powered-by-balloon .ck.ck-powered-by a{cursor:pointer;display:flex;align-items:center;opacity:.66;filter:grayscale(80%);line-height:var(--ck-powered-by-line-height);padding:var(--ck-powered-by-padding-vertical) var(--ck-powered-by-padding-horizontal)}.ck.ck-balloon-panel.ck-powered-by-balloon .ck.ck-powered-by .ck-powered-by__label{font-size:var(--ck-powered-by-font-size);letter-spacing:var(--ck-powered-by-letter-spacing);padding-left:2px;text-transform:uppercase;font-weight:700;margin-right:4px;cursor:pointer;line-height:normal;color:var(--ck-powered-by-text-color)}.ck.ck-balloon-panel.ck-powered-by-balloon .ck.ck-powered-by .ck-icon{display:block;cursor:pointer;width:var(--ck-powered-by-icon-width);height:var(--ck-powered-by-icon-height)}.ck.ck-balloon-panel.ck-powered-by-balloon .ck.ck-powered-by:hover a{filter:grayscale(0%);opacity:1}.ck.ck-balloon-panel.ck-powered-by-balloon[class*=position_inside]{border-color:transparent}.ck.ck-balloon-panel.ck-powered-by-balloon[class*=position_border]{border:var(--ck-focus-ring);border-color:var(--ck-powered-by-border-color)}:root{--ck-evaluation-badge-font-size: calc(var(--ck-font-size-base) * 7.5 / 13);--ck-evaluation-badge-line-height: calc(var(--ck-font-size-base) * 7.5 / 13);--ck-evaluation-badge-letter-spacing: calc(var(--ck-font-size-base) * -.2 / 13);--ck-evaluation-badge-padding-vertical: 2px;--ck-evaluation-badge-padding-horizontal: 4px;--ck-evaluation-badge-text-color: hsl(0, 0%, 31%);--ck-evaluation-badge-border-radius: var(--ck-border-radius);--ck-evaluation-badge-background: hsl(0, 0%, 100%);--ck-evaluation-badge-border-color: var(--ck-color-focus-border)}.ck.ck-balloon-panel.ck-evaluation-badge-balloon{--ck-border-radius: var(--ck-evaluation-badge-border-radius);box-shadow:none;background:var(--ck-evaluation-badge-background);min-height:unset;z-index:calc(var(--ck-z-panel) - 1)}.ck.ck-balloon-panel.ck-evaluation-badge-balloon .ck.ck-evaluation-badge{line-height:var(--ck-evaluation-badge-line-height);padding:var(--ck-evaluation-badge-padding-vertical) var(--ck-evaluation-badge-padding-horizontal)}.ck.ck-balloon-panel.ck-evaluation-badge-balloon .ck.ck-evaluation-badge .ck-evaluation-badge__label{display:block;padding:0 2px;font-size:var(--ck-evaluation-badge-font-size);letter-spacing:var(--ck-evaluation-badge-letter-spacing);font-weight:700;line-height:normal;text-transform:uppercase;color:var(--ck-evaluation-badge-text-color)}.ck.ck-balloon-panel.ck-evaluation-badge-balloon[class*=position_inside]{border-color:transparent}.ck.ck-balloon-panel.ck-evaluation-badge-balloon[class*=position_border]{border:var(--ck-focus-ring);border-color:var(--ck-evaluation-badge-border-color)}.ck.ck-button,a.ck.ck-button{-moz-user-select:none;-webkit-user-select:none;-ms-user-select:none;user-select:none;position:relative;display:inline-flex;align-items:center}[dir=ltr] .ck.ck-button,[dir=ltr] a.ck.ck-button{justify-content:left}[dir=rtl] .ck.ck-button,[dir=rtl] a.ck.ck-button{justify-content:right}.ck.ck-button .ck-button__label,a.ck.ck-button .ck-button__label{display:none}.ck.ck-button.ck-button_with-text .ck-button__label,a.ck.ck-button.ck-button_with-text .ck-button__label{display:inline-block}.ck.ck-button:not(.ck-button_with-text),a.ck.ck-button:not(.ck-button_with-text){justify-content:center}.ck.ck-button.ck-switchbutton .ck-button__toggle,.ck.ck-button.ck-switchbutton .ck-button__toggle .ck-button__toggle__inner{display:block}.ck.ck-list-item-button{min-height:unset;width:100%;border-radius:0}[dir=ltr] .ck.ck-list-item-button{text-align:left}[dir=rtl] .ck.ck-list-item-button{text-align:right}[dir=ltr] .ck.ck-list-item-button.ck-list-item-button_toggleable{padding-left:var(--ck-spacing-small)}[dir=rtl] .ck.ck-list-item-button.ck-list-item-button_toggleable{padding-right:var(--ck-spacing-small)}.ck.ck-list-item-button .ck-list-item-button__check-holder{display:inline-flex;width:.9em;height:.9em}[dir=ltr] .ck.ck-list-item-button .ck-list-item-button__check-holder{margin-right:var(--ck-spacing-small)}[dir=rtl] .ck.ck-list-item-button .ck-list-item-button__check-holder{margin-left:var(--ck-spacing-small)}.ck.ck-list-item-button .ck-list-item-button__check-icon{height:100%}.ck.ck-collapsible.ck-collapsible_collapsed>.ck-collapsible__children{display:none}.ck.ck-color-grid{display:grid}.color-picker-hex-input{width:max-content}.color-picker-hex-input .ck.ck-input{min-width:unset}.ck.ck-color-picker__row{display:flex;flex-direction:row;flex-wrap:nowrap;justify-content:space-between;margin:var(--ck-spacing-large) 0 0;width:unset}.ck.ck-color-picker__row .ck.ck-labeled-field-view{padding-top:unset}.ck.ck-color-picker__row .ck.ck-input-text{width:unset}.ck.ck-color-picker__row .ck-color-picker__hash-view{padding-top:var(--ck-spacing-tiny);padding-right:var(--ck-spacing-medium)}.ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__remove-color,.ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__color-picker{display:flex;align-items:center}[dir=rtl] .ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__remove-color,[dir=rtl] .ck.ck-color-selector .ck-color-grids-fragment .ck-button.ck-color-selector__color-picker{justify-content:flex-start}.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-selector_action-bar{display:flex;flex-direction:row;justify-content:space-around}.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-selector_action-bar .ck-button-save,.ck.ck-color-selector .ck-color-picker-fragment .ck.ck-color-selector_action-bar .ck-button-cancel{flex:1}.ck.ck-dialog .ck.ck-dialog__actions{display:flex;justify-content:flex-end}.ck.ck-dialog-overlay{-webkit-user-select:none;user-select:none;overscroll-behavior:none;position:fixed;bottom:0;left:0;right:0;top:0}.ck.ck-dialog-overlay.ck-dialog-overlay__transparent{pointer-events:none;animation:none;background:none}.ck.ck-dialog{overscroll-behavior:none;width:fit-content;position:absolute}.ck.ck-dialog .ck.ck-form__header{flex-shrink:0}.ck.ck-dialog:not(.ck-dialog_modal) .ck.ck-form__header .ck-form__header__label{cursor:grab}.ck.ck-dialog-overlay.ck-dialog-overlay__transparent .ck.ck-dialog{pointer-events:all}:root{--ck-dropdown-max-width: 75vw}.ck.ck-dropdown{display:inline-block;position:relative}.ck.ck-dropdown .ck-dropdown__arrow{pointer-events:none;z-index:var(--ck-z-default)}.ck.ck-dropdown .ck-button.ck-dropdown__button{width:100%}.ck.ck-dropdown .ck-dropdown__panel{display:none;z-index:var(--ck-z-panel);max-width:var(--ck-dropdown-max-width);position:absolute}.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel-visible{display:inline-block}.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_ne,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_nw,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_n,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_nmw,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_nme{bottom:100%}.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_se,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_sw,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_smw,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_sme,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_s{top:100%;bottom:auto}.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_ne,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_se{left:0}.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_nw,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_sw{right:0}.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_s,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_n{left:50%;transform:translate(-50%)}.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_nmw,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_smw{left:75%;transform:translate(-75%)}.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_nme,.ck.ck-dropdown .ck-dropdown__panel.ck-dropdown__panel_sme{left:25%;transform:translate(-25%)}.ck.ck-toolbar .ck-dropdown__panel{z-index:calc(var(--ck-z-panel) + 1)}.ck.ck-splitbutton{font-size:inherit}.ck.ck-splitbutton .ck-splitbutton__action:focus{z-index:calc(var(--ck-z-default) + 1)}:root{--ck-toolbar-dropdown-max-width: 60vw}.ck.ck-toolbar-dropdown>.ck-dropdown__panel{width:max-content;max-width:var(--ck-toolbar-dropdown-max-width)}.ck.ck-toolbar-dropdown>.ck-dropdown__panel .ck-button:focus{z-index:calc(var(--ck-z-default) + 1)}.ck.ck-dropdown-menu-list__nested-menu__button>.ck-dropdown-menu-list__nested-menu__button__arrow{pointer-events:none;z-index:var(--ck-z-default)}.ck.ck-balloon-panel.ck-dropdown-menu__nested-menu__panel{position:absolute;max-height:314px;overflow-y:auto;z-index:calc(var(--ck-z-panel) + 1)}.ck.ck-dropdown-menu-list__nested-menu{display:block}.ck.ck-aria-live-announcer{position:absolute;left:-10000px;top:-10000px}.ck.ck-aria-live-region-list{list-style-type:none}.ck.ck-form__header{display:flex;flex-direction:row;flex-wrap:nowrap;align-items:center;justify-content:space-between}.ck.ck-form__header h2.ck-form__header__label{flex-grow:1}.ck.ck-icon{vertical-align:middle}.ck.ck-label{display:block}.ck.ck-voice-label{display:none}.ck.ck-labeled-field-view>.ck.ck-labeled-field-view__input-wrapper{display:flex;position:relative}.ck.ck-labeled-field-view .ck.ck-label{display:block;position:absolute}.ck.ck-list{-moz-user-select:none;-webkit-user-select:none;-ms-user-select:none;user-select:none;display:flex;flex-direction:column}.ck.ck-list .ck-list__item,.ck.ck-list .ck-list__separator{display:block}.ck.ck-list .ck-list__item>*:focus{position:relative;z-index:var(--ck-z-default)}:root{--ck-balloon-panel-arrow-z-index: calc(var(--ck-z-default) - 3)}.ck.ck-balloon-panel{display:none;position:absolute;z-index:var(--ck-z-panel)}.ck.ck-balloon-panel.ck-balloon-panel_with-arrow:before,.ck.ck-balloon-panel.ck-balloon-panel_with-arrow:after{content:"";position:absolute}.ck.ck-balloon-panel.ck-balloon-panel_with-arrow:before{z-index:var(--ck-balloon-panel-arrow-z-index)}.ck.ck-balloon-panel.ck-balloon-panel_with-arrow:after{z-index:calc(var(--ck-balloon-panel-arrow-z-index) + 1)}.ck.ck-balloon-panel[class*=arrow_n]:before{z-index:var(--ck-balloon-panel-arrow-z-index)}.ck.ck-balloon-panel[class*=arrow_n]:after{z-index:calc(var(--ck-balloon-panel-arrow-z-index) + 1)}.ck.ck-balloon-panel[class*=arrow_s]:before{z-index:var(--ck-balloon-panel-arrow-z-index)}.ck.ck-balloon-panel[class*=arrow_s]:after{z-index:calc(var(--ck-balloon-panel-arrow-z-index) + 1)}.ck.ck-balloon-panel.ck-balloon-panel_visible{display:block}.ck .ck-balloon-rotator__navigation{display:flex;align-items:center;justify-content:center}.ck .ck-balloon-rotator__content .ck-toolbar{justify-content:center}.ck .ck-fake-panel{position:absolute;z-index:calc(var(--ck-z-panel) - 1)}.ck .ck-fake-panel div{position:absolute}.ck .ck-fake-panel div:nth-child(1){z-index:2}.ck .ck-fake-panel div:nth-child(2){z-index:1}.ck.ck-sticky-panel .ck-sticky-panel__content_sticky{z-index:var(--ck-z-panel);position:fixed;top:0}.ck.ck-sticky-panel .ck-sticky-panel__content_sticky_bottom-limit{top:auto;position:absolute}.ck.ck-autocomplete{position:relative}.ck.ck-autocomplete>.ck-search__results{position:absolute;z-index:var(--ck-z-panel)}.ck.ck-autocomplete>.ck-search__results.ck-search__results_n{bottom:100%}.ck.ck-autocomplete>.ck-search__results.ck-search__results_s{top:100%;bottom:auto}.ck.ck-search>.ck-labeled-field-view>.ck-labeled-field-view__input-wrapper>.ck-icon{position:absolute;top:50%;transform:translateY(-50%)}[dir=ltr] .ck.ck-search>.ck-labeled-field-view>.ck-labeled-field-view__input-wrapper>.ck-icon{left:var(--ck-spacing-medium)}[dir=rtl] .ck.ck-search>.ck-labeled-field-view>.ck-labeled-field-view__input-wrapper>.ck-icon{right:var(--ck-spacing-medium)}.ck.ck-search>.ck-labeled-field-view .ck-search__reset{position:absolute;top:50%;transform:translateY(-50%)}.ck.ck-search>.ck-search__results>.ck-search__info>span:first-child{display:block}.ck.ck-search>.ck-search__results>.ck-search__info:not(.ck-hidden)~*{display:none}.ck.ck-highlighted-text mark{background:var(--ck-color-highlight-background);vertical-align:initial;font-weight:inherit;line-height:inherit;font-size:inherit}.ck.ck-balloon-panel.ck-tooltip{-moz-user-select:none;-webkit-user-select:none;-ms-user-select:none;user-select:none;z-index:calc(var(--ck-z-dialog) + 100)}:root{--ck-toolbar-spinner-size: 18px}.ck.ck-spinner-container{display:block;position:relative}.ck.ck-spinner{position:absolute;top:50%;left:0;right:0;margin:0 auto;transform:translateY(-50%);z-index:1}.ck.ck-toolbar{-moz-user-select:none;-webkit-user-select:none;-ms-user-select:none;user-select:none;display:flex;flex-flow:row nowrap;align-items:center}.ck.ck-toolbar>.ck-toolbar__items{display:flex;flex-flow:row wrap;align-items:center;flex-grow:1}.ck.ck-toolbar .ck.ck-toolbar__separator{display:inline-block}.ck.ck-toolbar .ck.ck-toolbar__separator:first-child,.ck.ck-toolbar .ck.ck-toolbar__separator:last-child{display:none}.ck.ck-toolbar .ck-toolbar__line-break{flex-basis:100%}.ck.ck-toolbar.ck-toolbar_grouping>.ck-toolbar__items{flex-wrap:nowrap}.ck.ck-toolbar.ck-toolbar_vertical>.ck-toolbar__items{flex-direction:column}.ck.ck-toolbar.ck-toolbar_floating>.ck-toolbar__items{flex-wrap:nowrap}.ck.ck-toolbar>.ck.ck-toolbar__grouped-dropdown>.ck-dropdown__button .ck-dropdown__arrow{display:none}.ck.ck-block-toolbar-button{position:absolute;z-index:var(--ck-z-default)}.ck.ck-menu-bar__menu>.ck-menu-bar__menu__button>.ck-menu-bar__menu__button__arrow{pointer-events:none;z-index:var(--ck-z-default)}:root{--ck-menu-bar-menu-max-width: 75vw;--ck-menu-bar-nested-menu-horizontal-offset: 5px}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel{z-index:var(--ck-z-panel);max-width:var(--ck-menu-bar-menu-max-width);position:absolute}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_ne,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_nw{bottom:100%}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_se,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_sw{top:100%;bottom:auto}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_ne,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_se{left:0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_nw,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_sw{right:0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_es,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_en{left:calc(100% - var(--ck-menu-bar-nested-menu-horizontal-offset))}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_es{top:0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_en{bottom:0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_ws,.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_wn{right:calc(100% - var(--ck-menu-bar-nested-menu-horizontal-offset))}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_ws{top:0}.ck.ck-menu-bar__menu>.ck.ck-menu-bar__menu__panel.ck-menu-bar__menu__panel_position_wn{bottom:0}.ck.ck-menu-bar__menu{display:block;position:relative}:root{--ck-color-resizer: var(--ck-color-focus-border);--ck-color-resizer-tooltip-background: hsl(0, 0%, 15%);--ck-color-resizer-tooltip-text: hsl(0, 0%, 95%);--ck-resizer-border-radius: var(--ck-border-radius);--ck-resizer-tooltip-offset: 10px;--ck-resizer-tooltip-height: calc(var(--ck-spacing-small) * 2 + 10px)}.ck .ck-widget,.ck .ck-widget.ck-widget_with-selection-handle{position:relative}.ck .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle{position:absolute}.ck .ck-widget.ck-widget_with-selection-handle .ck-widget__selection-handle .ck-icon{display:block}.ck .ck-widget.ck-widget_with-selection-handle:hover>.ck-widget__selection-handle{visibility:visible}.ck .ck-widget.ck-widget_with-selection-handle.ck-widget_selected>.ck-widget__selection-handle{visibility:visible}.ck .ck-size-view{background:var(--ck-color-resizer-tooltip-background);color:var(--ck-color-resizer-tooltip-text);border:1px solid var(--ck-color-resizer-tooltip-text);border-radius:var(--ck-resizer-border-radius);font-size:var(--ck-font-size-tiny);display:block;padding:0 var(--ck-spacing-small);height:var(--ck-resizer-tooltip-height);line-height:var(--ck-resizer-tooltip-height)}.ck .ck-size-view.ck-orientation-top-left,.ck .ck-size-view.ck-orientation-top-right,.ck .ck-size-view.ck-orientation-bottom-right,.ck .ck-size-view.ck-orientation-bottom-left,.ck .ck-size-view.ck-orientation-above-center{position:absolute}.ck .ck-size-view.ck-orientation-top-left{top:var(--ck-resizer-tooltip-offset);left:var(--ck-resizer-tooltip-offset)}.ck .ck-size-view.ck-orientation-top-right{top:var(--ck-resizer-tooltip-offset);right:var(--ck-resizer-tooltip-offset)}.ck .ck-size-view.ck-orientation-bottom-right{bottom:var(--ck-resizer-tooltip-offset);right:var(--ck-resizer-tooltip-offset)}.ck .ck-size-view.ck-orientation-bottom-left{bottom:var(--ck-resizer-tooltip-offset);left:var(--ck-resizer-tooltip-offset)}.ck .ck-size-view.ck-orientation-above-center{top:calc(var(--ck-resizer-tooltip-height) * -1);left:50%;transform:translate(-50%)}.ck .ck-widget_with-resizer{position:relative}.ck .ck-widget__resizer{display:none;position:absolute;pointer-events:none;left:0;top:0}.ck-focused .ck-widget_with-resizer.ck-widget_selected>.ck-widget__resizer{display:block}.ck .ck-widget__resizer__handle{position:absolute;pointer-events:all}.ck .ck-widget__resizer__handle.ck-widget__resizer__handle-top-left,.ck .ck-widget__resizer__handle.ck-widget__resizer__handle-bottom-right{cursor:nwse-resize}.ck .ck-widget__resizer__handle.ck-widget__resizer__handle-top-right,.ck .ck-widget__resizer__handle.ck-widget__resizer__handle-bottom-left{cursor:nesw-resize}.ck .ck-widget .ck-widget__type-around__button{display:block;position:absolute;overflow:hidden;z-index:var(--ck-z-default)}.ck .ck-widget .ck-widget__type-around__button svg{position:absolute;top:50%;left:50%;z-index:calc(var(--ck-z-default) + 2)}.ck .ck-widget .ck-widget__type-around__button.ck-widget__type-around__button_before{top:calc(-.5 * var(--ck-widget-outline-thickness));left:min(10%,30px);transform:translateY(-50%)}.ck .ck-widget .ck-widget__type-around__button.ck-widget__type-around__button_after{bottom:calc(-.5 * var(--ck-widget-outline-thickness));right:min(10%,30px);transform:translateY(50%)}.ck .ck-widget.ck-widget_selected>.ck-widget__type-around>.ck-widget__type-around__button:after,.ck .ck-widget>.ck-widget__type-around>.ck-widget__type-around__button:hover:after{content:"";display:block;position:absolute;top:1px;left:1px;z-index:calc(var(--ck-z-default) + 1)}.ck .ck-widget>.ck-widget__type-around>.ck-widget__type-around__fake-caret{display:none;position:absolute;left:0;right:0}.ck .ck-widget:hover>.ck-widget__type-around>.ck-widget__type-around__fake-caret{left:calc(-1 * var(--ck-widget-outline-thickness));right:calc(-1 * var(--ck-widget-outline-thickness))}.ck .ck-widget.ck-widget_type-around_show-fake-caret_before>.ck-widget__type-around>.ck-widget__type-around__fake-caret{top:calc(-1 * var(--ck-widget-outline-thickness) - 1px);display:block}.ck .ck-widget.ck-widget_type-around_show-fake-caret_after>.ck-widget__type-around>.ck-widget__type-around__fake-caret{bottom:calc(-1 * var(--ck-widget-outline-thickness) - 1px);display:block}.ck.ck-editor__editable.ck-read-only .ck-widget__type-around,.ck.ck-editor__editable.ck-restricted-editing_mode_restricted .ck-widget__type-around,.ck.ck-editor__editable.ck-widget__type-around_disabled .ck-widget__type-around{display:none}._slider-container_15o51_1{position:relative;height:1.6rem}._slider-container_15o51_1{height:unset;width:1.6rem}._range-bar_15o51_21{position:absolute;top:50%;transform:translateY(-50%);width:100%;height:var(--range-width, 6px);border-radius:10rem;background:var(--neutral-main-background-color)}._value-bar_15o51_41{border-radius:10rem;height:100%;background:var(--primary-main-color)}._slider-container_15o51_1._vertical_15o51_53{top:unset;left:50%;transform:translate(-50%);width:var(--range-width, 6px);height:100%}._thumb-dot_15o51_69{top:50%;left:0;transform:translate(-50%,-50%);position:absolute;height:inherit;aspect-ratio:1 / 1;border-radius:50%;border:2px solid var(--primary-main-color);background-color:var(--neutral-absolute-background-color)}._thumb-dot_15o51_69:focus{box-shadow:0 0 0 4px var(--primary-background)}._slider-container_15o51_1._vertical_15o51_53{top:0;left:50%;height:unset;width:inherit}@keyframes _on-open-tooltip_15o51_1{0%{opacity:0}to{opacity:1}}._tooltip-container_15o51_135{position:fixed;z-index:999;animation:_on-open-tooltip_15o51_1 .2s ease-in-out;animation-fill-mode:forwards}._tooltip-container_15o51_135>._tooltip-message_15o51_149{color:var(--neutral-text-body-reverse-color);background-color:var(--neutral-main-reverse-background-color);padding:.4rem 1.2rem;border-radius:.8rem;max-width:21.2rem;text-align:center}._mark-point_15o51_167{position:absolute;top:50%;transform:translate(-50%,-50%);height:calc(var(--range-width, 6px) + max(.3rem,1.5px));width:calc(var(--range-width, 6px) + max(.3rem,1.5px));border-radius:50%;border:2px solid var(--primary-main-color);background-color:var(--neutral-absolute-background-color)}._mark-text_15o51_189{position:absolute;bottom:-2px;transform:translate(-50%,100%);font:inherit;color:inherit;font-size:inherit;font-family:inherit;font-weight:inherit;line-height:inherit;text-align:inherit;text-overflow:inherit}`)),document.head.appendChild(e)}}catch(t){console.error("vite-plugin-css-injected-by-js",t)}})();
var O2 = Object.defineProperty;
var V2 = (n, t, e) => t in n ? O2(n, t, { enumerable: !0, configurable: !0, writable: !0, value: e }) : n[t] = e;
var f = (n, t, e) => V2(n, typeof t != "symbol" ? t + "" : t, e);
import ce, { createRef as vn, useRef as yt, useState as zt, useMemo as Ca, useEffect as ht, createContext as L2, useContext as B2, useCallback as M2, forwardRef as Ic, createElement as Pm, useLayoutEffect as o0, isValidElement as Pc, useSyncExternalStore as N2, cloneElement as Kh, memo as F2, useDeferredValue as D2 } from "react";
import Zh from "react-dom";
function $2(n, t) {
  for (var e = 0; e < t.length; e++) {
    const i = t[e];
    if (typeof i != "string" && !Array.isArray(i)) {
      for (const s in i)
        if (s !== "default" && !(s in n)) {
          const r = Object.getOwnPropertyDescriptor(i, s);
          r && Object.defineProperty(n, s, r.get ? r : {
            enumerable: !0,
            get: () => i[s]
          });
        }
    }
  }
  return Object.freeze(Object.defineProperty(n, Symbol.toStringTag, { value: "Module" }));
}
function Rc(n) {
  return n && n.__esModule && Object.prototype.hasOwnProperty.call(n, "default") ? n.default : n;
}
var Ka = { exports: {} }, oo = {};
/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var Rm;
function z2() {
  if (Rm) return oo;
  Rm = 1;
  var n = ce, t = Symbol.for("react.element"), e = Symbol.for("react.fragment"), i = Object.prototype.hasOwnProperty, s = n.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner, r = { key: !0, ref: !0, __self: !0, __source: !0 };
  function o(a, l, c) {
    var u, d = {}, h = null, m = null;
    c !== void 0 && (h = "" + c), l.key !== void 0 && (h = "" + l.key), l.ref !== void 0 && (m = l.ref);
    for (u in l) i.call(l, u) && !r.hasOwnProperty(u) && (d[u] = l[u]);
    if (a && a.defaultProps) for (u in l = a.defaultProps, l) d[u] === void 0 && (d[u] = l[u]);
    return { $$typeof: t, type: a, key: h, ref: m, props: d, _owner: s.current };
  }
  return oo.Fragment = e, oo.jsx = o, oo.jsxs = o, oo;
}
var ao = {};
/**
 * @license React
 * react-jsx-runtime.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var Om;
function H2() {
  return Om || (Om = 1, process.env.NODE_ENV !== "production" && function() {
    var n = ce, t = Symbol.for("react.element"), e = Symbol.for("react.portal"), i = Symbol.for("react.fragment"), s = Symbol.for("react.strict_mode"), r = Symbol.for("react.profiler"), o = Symbol.for("react.provider"), a = Symbol.for("react.context"), l = Symbol.for("react.forward_ref"), c = Symbol.for("react.suspense"), u = Symbol.for("react.suspense_list"), d = Symbol.for("react.memo"), h = Symbol.for("react.lazy"), m = Symbol.for("react.offscreen"), g = Symbol.iterator, p = "@@iterator";
    function w(P) {
      if (P === null || typeof P != "object")
        return null;
      var Q = g && P[g] || P[p];
      return typeof Q == "function" ? Q : null;
    }
    var y = n.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
    function k(P) {
      {
        for (var Q = arguments.length, ue = new Array(Q > 1 ? Q - 1 : 0), Te = 1; Te < Q; Te++)
          ue[Te - 1] = arguments[Te];
        C("error", P, ue);
      }
    }
    function C(P, Q, ue) {
      {
        var Te = y.ReactDebugCurrentFrame, We = Te.getStackAddendum();
        We !== "" && (Q += "%s", ue = ue.concat([We]));
        var st = ue.map(function(De) {
          return String(De);
        });
        st.unshift("Warning: " + Q), Function.prototype.apply.call(console[P], console, st);
      }
    }
    var E = !1, T = !1, L = !1, O = !1, N = !1, M;
    M = Symbol.for("react.module.reference");
    function J(P) {
      return !!(typeof P == "string" || typeof P == "function" || P === i || P === r || N || P === s || P === c || P === u || O || P === m || E || T || L || typeof P == "object" && P !== null && (P.$$typeof === h || P.$$typeof === d || P.$$typeof === o || P.$$typeof === a || P.$$typeof === l || // This needs to include all possible module reference object
      // types supported by any Flight configuration anywhere since
      // we don't know which Flight build this will end up being used
      // with.
      P.$$typeof === M || P.getModuleId !== void 0));
    }
    function D(P, Q, ue) {
      var Te = P.displayName;
      if (Te)
        return Te;
      var We = Q.displayName || Q.name || "";
      return We !== "" ? ue + "(" + We + ")" : ue;
    }
    function B(P) {
      return P.displayName || "Context";
    }
    function $(P) {
      if (P == null)
        return null;
      if (typeof P.tag == "number" && k("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), typeof P == "function")
        return P.displayName || P.name || null;
      if (typeof P == "string")
        return P;
      switch (P) {
        case i:
          return "Fragment";
        case e:
          return "Portal";
        case r:
          return "Profiler";
        case s:
          return "StrictMode";
        case c:
          return "Suspense";
        case u:
          return "SuspenseList";
      }
      if (typeof P == "object")
        switch (P.$$typeof) {
          case a:
            var Q = P;
            return B(Q) + ".Consumer";
          case o:
            var ue = P;
            return B(ue._context) + ".Provider";
          case l:
            return D(P, P.render, "ForwardRef");
          case d:
            var Te = P.displayName || null;
            return Te !== null ? Te : $(P.type) || "Memo";
          case h: {
            var We = P, st = We._payload, De = We._init;
            try {
              return $(De(st));
            } catch {
              return null;
            }
          }
        }
      return null;
    }
    var W = Object.assign, Y = 0, ye, Pe, Be, te, R, K, X;
    function ie() {
    }
    ie.__reactDisabledLog = !0;
    function oe() {
      {
        if (Y === 0) {
          ye = console.log, Pe = console.info, Be = console.warn, te = console.error, R = console.group, K = console.groupCollapsed, X = console.groupEnd;
          var P = {
            configurable: !0,
            enumerable: !0,
            value: ie,
            writable: !0
          };
          Object.defineProperties(console, {
            info: P,
            log: P,
            warn: P,
            error: P,
            group: P,
            groupCollapsed: P,
            groupEnd: P
          });
        }
        Y++;
      }
    }
    function de() {
      {
        if (Y--, Y === 0) {
          var P = {
            configurable: !0,
            enumerable: !0,
            writable: !0
          };
          Object.defineProperties(console, {
            log: W({}, P, {
              value: ye
            }),
            info: W({}, P, {
              value: Pe
            }),
            warn: W({}, P, {
              value: Be
            }),
            error: W({}, P, {
              value: te
            }),
            group: W({}, P, {
              value: R
            }),
            groupCollapsed: W({}, P, {
              value: K
            }),
            groupEnd: W({}, P, {
              value: X
            })
          });
        }
        Y < 0 && k("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
      }
    }
    var pe = y.ReactCurrentDispatcher, ve;
    function _e(P, Q, ue) {
      {
        if (ve === void 0)
          try {
            throw Error();
          } catch (We) {
            var Te = We.stack.trim().match(/\n( *(at )?)/);
            ve = Te && Te[1] || "";
          }
        return `
` + ve + P;
      }
    }
    var Ee = !1, we;
    {
      var Ne = typeof WeakMap == "function" ? WeakMap : Map;
      we = new Ne();
    }
    function se(P, Q) {
      if (!P || Ee)
        return "";
      {
        var ue = we.get(P);
        if (ue !== void 0)
          return ue;
      }
      var Te;
      Ee = !0;
      var We = Error.prepareStackTrace;
      Error.prepareStackTrace = void 0;
      var st;
      st = pe.current, pe.current = null, oe();
      try {
        if (Q) {
          var De = function() {
            throw Error();
          };
          if (Object.defineProperty(De.prototype, "props", {
            set: function() {
              throw Error();
            }
          }), typeof Reflect == "object" && Reflect.construct) {
            try {
              Reflect.construct(De, []);
            } catch (ci) {
              Te = ci;
            }
            Reflect.construct(P, [], De);
          } else {
            try {
              De.call();
            } catch (ci) {
              Te = ci;
            }
            P.call(De.prototype);
          }
        } else {
          try {
            throw Error();
          } catch (ci) {
            Te = ci;
          }
          P();
        }
      } catch (ci) {
        if (ci && Te && typeof ci.stack == "string") {
          for (var Me = ci.stack.split(`
`), Qt = Te.stack.split(`
`), kt = Me.length - 1, St = Qt.length - 1; kt >= 1 && St >= 0 && Me[kt] !== Qt[St]; )
            St--;
          for (; kt >= 1 && St >= 0; kt--, St--)
            if (Me[kt] !== Qt[St]) {
              if (kt !== 1 || St !== 1)
                do
                  if (kt--, St--, St < 0 || Me[kt] !== Qt[St]) {
                    var Ri = `
` + Me[kt].replace(" at new ", " at ");
                    return P.displayName && Ri.includes("<anonymous>") && (Ri = Ri.replace("<anonymous>", P.displayName)), typeof P == "function" && we.set(P, Ri), Ri;
                  }
                while (kt >= 1 && St >= 0);
              break;
            }
        }
      } finally {
        Ee = !1, pe.current = st, de(), Error.prepareStackTrace = We;
      }
      var rr = P ? P.displayName || P.name : "", vs = rr ? _e(rr) : "";
      return typeof P == "function" && we.set(P, vs), vs;
    }
    function Qe(P, Q, ue) {
      return se(P, !1);
    }
    function q(P) {
      var Q = P.prototype;
      return !!(Q && Q.isReactComponent);
    }
    function U(P, Q, ue) {
      if (P == null)
        return "";
      if (typeof P == "function")
        return se(P, q(P));
      if (typeof P == "string")
        return _e(P);
      switch (P) {
        case c:
          return _e("Suspense");
        case u:
          return _e("SuspenseList");
      }
      if (typeof P == "object")
        switch (P.$$typeof) {
          case l:
            return Qe(P.render);
          case d:
            return U(P.type, Q, ue);
          case h: {
            var Te = P, We = Te._payload, st = Te._init;
            try {
              return U(st(We), Q, ue);
            } catch {
            }
          }
        }
      return "";
    }
    var V = Object.prototype.hasOwnProperty, _ = {}, b = y.ReactDebugCurrentFrame;
    function x(P) {
      if (P) {
        var Q = P._owner, ue = U(P.type, P._source, Q ? Q.type : null);
        b.setExtraStackFrame(ue);
      } else
        b.setExtraStackFrame(null);
    }
    function A(P, Q, ue, Te, We) {
      {
        var st = Function.call.bind(V);
        for (var De in P)
          if (st(P, De)) {
            var Me = void 0;
            try {
              if (typeof P[De] != "function") {
                var Qt = Error((Te || "React class") + ": " + ue + " type `" + De + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof P[De] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                throw Qt.name = "Invariant Violation", Qt;
              }
              Me = P[De](Q, De, Te, ue, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
            } catch (kt) {
              Me = kt;
            }
            Me && !(Me instanceof Error) && (x(We), k("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", Te || "React class", ue, De, typeof Me), x(null)), Me instanceof Error && !(Me.message in _) && (_[Me.message] = !0, x(We), k("Failed %s type: %s", ue, Me.message), x(null));
          }
      }
    }
    var z = Array.isArray;
    function j(P) {
      return z(P);
    }
    function H(P) {
      {
        var Q = typeof Symbol == "function" && Symbol.toStringTag, ue = Q && P[Symbol.toStringTag] || P.constructor.name || "Object";
        return ue;
      }
    }
    function ne(P) {
      try {
        return me(P), !1;
      } catch {
        return !0;
      }
    }
    function me(P) {
      return "" + P;
    }
    function Oe(P) {
      if (ne(P))
        return k("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", H(P)), me(P);
    }
    var Le = y.ReactCurrentOwner, Rt = {
      key: !0,
      ref: !0,
      __self: !0,
      __source: !0
    }, Ot, An;
    function ws(P) {
      if (V.call(P, "ref")) {
        var Q = Object.getOwnPropertyDescriptor(P, "ref").get;
        if (Q && Q.isReactWarning)
          return !1;
      }
      return P.ref !== void 0;
    }
    function lu(P) {
      if (V.call(P, "key")) {
        var Q = Object.getOwnPropertyDescriptor(P, "key").get;
        if (Q && Q.isReactWarning)
          return !1;
      }
      return P.key !== void 0;
    }
    function qa(P, Q) {
      typeof P.ref == "string" && Le.current;
    }
    function cu(P, Q) {
      {
        var ue = function() {
          Ot || (Ot = !0, k("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", Q));
        };
        ue.isReactWarning = !0, Object.defineProperty(P, "key", {
          get: ue,
          configurable: !0
        });
      }
    }
    function Ga(P, Q) {
      {
        var ue = function() {
          An || (An = !0, k("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", Q));
        };
        ue.isReactWarning = !0, Object.defineProperty(P, "ref", {
          get: ue,
          configurable: !0
        });
      }
    }
    var _2 = function(P, Q, ue, Te, We, st, De) {
      var Me = {
        // This tag allows us to uniquely identify this as a React Element
        $$typeof: t,
        // Built-in properties that belong on the element
        type: P,
        key: Q,
        ref: ue,
        props: De,
        // Record the component responsible for creating this element.
        _owner: st
      };
      return Me._store = {}, Object.defineProperty(Me._store, "validated", {
        configurable: !1,
        enumerable: !1,
        writable: !0,
        value: !1
      }), Object.defineProperty(Me, "_self", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: Te
      }), Object.defineProperty(Me, "_source", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: We
      }), Object.freeze && (Object.freeze(Me.props), Object.freeze(Me)), Me;
    };
    function y2(P, Q, ue, Te, We) {
      {
        var st, De = {}, Me = null, Qt = null;
        ue !== void 0 && (Oe(ue), Me = "" + ue), lu(Q) && (Oe(Q.key), Me = "" + Q.key), ws(Q) && (Qt = Q.ref, qa(Q, We));
        for (st in Q)
          V.call(Q, st) && !Rt.hasOwnProperty(st) && (De[st] = Q[st]);
        if (P && P.defaultProps) {
          var kt = P.defaultProps;
          for (st in kt)
            De[st] === void 0 && (De[st] = kt[st]);
        }
        if (Me || Qt) {
          var St = typeof P == "function" ? P.displayName || P.name || "Unknown" : P;
          Me && cu(De, St), Qt && Ga(De, St);
        }
        return _2(P, Me, Qt, We, Te, Le.current, De);
      }
    }
    var uu = y.ReactCurrentOwner, Cm = y.ReactDebugCurrentFrame;
    function sr(P) {
      if (P) {
        var Q = P._owner, ue = U(P.type, P._source, Q ? Q.type : null);
        Cm.setExtraStackFrame(ue);
      } else
        Cm.setExtraStackFrame(null);
    }
    var du;
    du = !1;
    function hu(P) {
      return typeof P == "object" && P !== null && P.$$typeof === t;
    }
    function xm() {
      {
        if (uu.current) {
          var P = $(uu.current.type);
          if (P)
            return `

Check the render method of \`` + P + "`.";
        }
        return "";
      }
    }
    function k2(P) {
      return "";
    }
    var Tm = {};
    function C2(P) {
      {
        var Q = xm();
        if (!Q) {
          var ue = typeof P == "string" ? P : P.displayName || P.name;
          ue && (Q = `

Check the top-level render call using <` + ue + ">.");
        }
        return Q;
      }
    }
    function Em(P, Q) {
      {
        if (!P._store || P._store.validated || P.key != null)
          return;
        P._store.validated = !0;
        var ue = C2(Q);
        if (Tm[ue])
          return;
        Tm[ue] = !0;
        var Te = "";
        P && P._owner && P._owner !== uu.current && (Te = " It was passed a child from " + $(P._owner.type) + "."), sr(P), k('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', ue, Te), sr(null);
      }
    }
    function Sm(P, Q) {
      {
        if (typeof P != "object")
          return;
        if (j(P))
          for (var ue = 0; ue < P.length; ue++) {
            var Te = P[ue];
            hu(Te) && Em(Te, Q);
          }
        else if (hu(P))
          P._store && (P._store.validated = !0);
        else if (P) {
          var We = w(P);
          if (typeof We == "function" && We !== P.entries)
            for (var st = We.call(P), De; !(De = st.next()).done; )
              hu(De.value) && Em(De.value, Q);
        }
      }
    }
    function x2(P) {
      {
        var Q = P.type;
        if (Q == null || typeof Q == "string")
          return;
        var ue;
        if (typeof Q == "function")
          ue = Q.propTypes;
        else if (typeof Q == "object" && (Q.$$typeof === l || // Note: Memo only checks outer props here.
        // Inner props are checked in the reconciler.
        Q.$$typeof === d))
          ue = Q.propTypes;
        else
          return;
        if (ue) {
          var Te = $(Q);
          A(ue, P.props, "prop", Te, P);
        } else if (Q.PropTypes !== void 0 && !du) {
          du = !0;
          var We = $(Q);
          k("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", We || "Unknown");
        }
        typeof Q.getDefaultProps == "function" && !Q.getDefaultProps.isReactClassApproved && k("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
      }
    }
    function T2(P) {
      {
        for (var Q = Object.keys(P.props), ue = 0; ue < Q.length; ue++) {
          var Te = Q[ue];
          if (Te !== "children" && Te !== "key") {
            sr(P), k("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", Te), sr(null);
            break;
          }
        }
        P.ref !== null && (sr(P), k("Invalid attribute `ref` supplied to `React.Fragment`."), sr(null));
      }
    }
    var Am = {};
    function Im(P, Q, ue, Te, We, st) {
      {
        var De = J(P);
        if (!De) {
          var Me = "";
          (P === void 0 || typeof P == "object" && P !== null && Object.keys(P).length === 0) && (Me += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.");
          var Qt = k2();
          Qt ? Me += Qt : Me += xm();
          var kt;
          P === null ? kt = "null" : j(P) ? kt = "array" : P !== void 0 && P.$$typeof === t ? (kt = "<" + ($(P.type) || "Unknown") + " />", Me = " Did you accidentally export a JSX literal instead of a component?") : kt = typeof P, k("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", kt, Me);
        }
        var St = y2(P, Q, ue, We, st);
        if (St == null)
          return St;
        if (De) {
          var Ri = Q.children;
          if (Ri !== void 0)
            if (Te)
              if (j(Ri)) {
                for (var rr = 0; rr < Ri.length; rr++)
                  Sm(Ri[rr], P);
                Object.freeze && Object.freeze(Ri);
              } else
                k("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
            else
              Sm(Ri, P);
        }
        if (V.call(Q, "key")) {
          var vs = $(P), ci = Object.keys(Q).filter(function(R2) {
            return R2 !== "key";
          }), fu = ci.length > 0 ? "{key: someKey, " + ci.join(": ..., ") + ": ...}" : "{key: someKey}";
          if (!Am[vs + fu]) {
            var P2 = ci.length > 0 ? "{" + ci.join(": ..., ") + ": ...}" : "{}";
            k(`A props object containing a "key" prop is being spread into JSX:
  let props = %s;
  <%s {...props} />
React keys must be passed directly to JSX without using spread:
  let props = %s;
  <%s key={someKey} {...props} />`, fu, vs, P2, vs), Am[vs + fu] = !0;
          }
        }
        return P === i ? T2(St) : x2(St), St;
      }
    }
    function E2(P, Q, ue) {
      return Im(P, Q, ue, !0);
    }
    function S2(P, Q, ue) {
      return Im(P, Q, ue, !1);
    }
    var A2 = S2, I2 = E2;
    ao.Fragment = i, ao.jsx = A2, ao.jsxs = I2;
  }()), ao;
}
var Vm;
function j2() {
  return Vm || (Vm = 1, process.env.NODE_ENV === "production" ? Ka.exports = z2() : Ka.exports = H2()), Ka.exports;
}
var v = j2();
const U2 = {
  "checkbox-container": "_checkbox-container_1749q_1"
};
class a0 extends ce.Component {
  constructor(e) {
    super(e);
    f(this, "ref", vn());
    f(this, "onChange", () => {
      const e = !this.state.value;
      this.setState({ value: e }), this.props.onChange && this.ref.current && this.props.onChange(e, this.ref.current.querySelector("input"));
    });
    this.state = {
      value: this.props.value ?? !1
    };
  }
  componentDidUpdate(e) {
    e.value !== this.props.value && this.setState({ value: this.props.value });
  }
  render() {
    let e = {
      width: this.props.size ?? "2.4rem",
      height: this.props.size ?? "2.4rem"
    };
    return this.props.style && (delete this.props.style.width, delete this.props.style.minWidth, delete this.props.style.maxWidth, delete this.props.style.height, delete this.props.style.minHeight, delete this.props.style.maxHeight, e = {
      ...this.props.style,
      ...e
    }), /* @__PURE__ */ v.jsxs("label", { ref: this.ref, id: this.props.id, className: `${U2["checkbox-container"]} row ${this.props.className ?? ""}`, style: e, "is-null-value": `${this.state.value == null}`, onClick: this.props.onClick, children: [
      /* @__PURE__ */ v.jsx(
        "input",
        {
          type: "checkbox",
          checked: !!this.state.value,
          disabled: this.props.disabled,
          onChange: (i) => {
            i.stopPropagation();
            const s = !this.state.value;
            this.setState({ value: s }), this.props.onChange && this.props.onChange(s, i.target);
          }
        }
      ),
      /* @__PURE__ */ v.jsx("svg", { width: "100%", height: "100%", viewBox: "0 0 20 20", fill: "none", xmlns: "http://www.w3.org/2000/svg", style: { "--check-color": this.props.checkColor }, children: this.state.value === void 0 ? /* @__PURE__ */ v.jsx("path", { fillRule: "evenodd", clipRule: "evenodd", d: "M4.79199 9.95298C4.79199 9.69148 5.00398 9.47949 5.26548 9.47949H14.7352C14.9967 9.47949 15.2087 9.69148 15.2087 9.95298C15.2087 10.2145 14.9967 10.4265 14.7352 10.4265H5.26548C5.00398 10.4265 4.79199 10.2145 4.79199 9.95298Z" }) : /* @__PURE__ */ v.jsx("path", { fillRule: "evenodd", clipRule: "evenodd", d: "M15.07 6.49317C15.2549 6.67808 15.2549 6.97787 15.07 7.16278L8.91467 13.3181C8.72977 13.503 8.42997 13.503 8.24507 13.3181L4.93067 10.0037C4.74577 9.81878 4.74577 9.51899 4.93067 9.33408C5.11558 9.14917 5.41537 9.14917 5.60028 9.33408L8.57987 12.3137L14.4004 6.49317C14.5853 6.30827 14.8851 6.30827 15.07 6.49317Z" }) })
    ] });
  }
}
const W2 = "_disabled_t49wa_33", as = {
  "select1-container": "_select1-container_t49wa_1",
  disabled: W2,
  "helper-text": "_helper-text_t49wa_43",
  "select1-popup": "_select1-popup_t49wa_121",
  "select-body": "_select-body_t49wa_143",
  "select-tile": "_select-tile_t49wa_165",
  "no-results-found": "_no-results-found_t49wa_207"
}, q2 = "_clickable_1sizd_57", cn = {
  "wini-icon": "_wini-icon_1sizd_1",
  clickable: q2,
  "tooltip-container": "_tooltip-container_1sizd_183",
  "tooltip-message": "_tooltip-message_1sizd_197"
};
let Ae = class extends ce.Component {
  render() {
    let t = this.props.style ?? {};
    return this.props.maxLine && (t = { ...t, "--max-line": this.props.maxLine }), this.props.html ? /* @__PURE__ */ v.jsx("div", { dangerouslySetInnerHTML: { __html: this.props.html }, id: this.props.id, onMouseOver: this.props.onHover, onClick: this.props.onClick, className: `comp-text innerhtml ${this.props.onClick ? "type-button" : ""} ${this.props.className ?? ""}`, style: t }) : /* @__PURE__ */ v.jsx("div", { id: this.props.id, onMouseOver: this.props.onHover, onClick: this.props.onClick, className: `comp-text ${this.props.onClick ? "type-button" : ""} ${this.props.className ?? ""}`, style: t, children: this.props.children });
  }
};
function rt({ id: n, src: t, link: e, className: i, style: s, size: r, color: o, alt: a, onClick: l, tooltip: c }) {
  const u = yt(null), [d, h] = zt(), [m, g] = zt(!1), p = "https://cdn.jsdelivr.net/gh/WiniGit/icon-library@latest/", w = Ca(() => c ? {
    "tooltip-value": c,
    onMouseOver: () => {
      g(!0);
    },
    onMouseOut: () => {
      g(!1);
    }
  } : {}, [c]);
  return ht(() => {
    t ? fetch(p + t + ".svg").then(async (y) => {
      h(await y.text());
    }).catch(() => {
      h(a ?? "error");
    }) : e && fetch(e).then(async (y) => {
      h(await y.text());
    });
  }, [t, e]), /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
    /* @__PURE__ */ v.jsx(
      "div",
      {
        ref: u,
        id: n,
        onClick: l,
        className: `${cn["wini-icon"]} ${d ? "" : "skeleton-loading"} ${l ? cn.clickable : ""} ${i ?? ""} ${t ? t.split("/").map((y, k) => k === 0 ? `${y}-icon` : y.replace(" ", "-")).join(" ") : ""}${e ? " link-icon" : ""}`,
        style: s ? { ...s, "--size": r, "--color": o } : { "--size": r, "--color": o },
        dangerouslySetInnerHTML: { __html: d ?? "" },
        ...w
      }
    ),
    c && m && Zh.createPortal(G2({ element: u.current, tooltip: c }), document.body)
  ] });
}
const G2 = ({ element: n, tooltip: t }) => {
  if (!n) return null;
  const e = n.getBoundingClientRect();
  let i = t.position ?? "bottom";
  switch (document.body.offsetHeight - e.bottom < 100 && i === "bottom" ? i = "top" : e.top < 100 && i === "top" && (i = "bottom"), document.body.offsetWidth - e.right < 100 && i === "right" ? i = "left" : e.left < 100 && i === "left" && (i = "right"), i) {
    case "top":
      return /* @__PURE__ */ v.jsxs("div", { ref: (s) => {
        if (s) {
          const r = s.getBoundingClientRect();
          r.x < 0 ? (s.style.left = e.x + e.width / 2 + "px", s.style.transform = "translateX(-1.8rem)", s.style.alignItems = "start") : r.right > document.body.offsetWidth && (s.style.left = "unset", s.style.right = document.body.offsetWidth - e.right - e.width / 2 + "px", s.style.transform = "translateX(-1.4rem)", s.style.alignItems = "end");
        }
      }, className: `col ${cn["tooltip-container"]}`, style: { alignItems: "center", bottom: document.body.offsetHeight - e.top - 4, left: e.left + e.width / 2, transform: "translateX(-50%)" }, children: [
        /* @__PURE__ */ v.jsx(Ae, { className: `body-3 ${cn["tooltip-message"]}`, maxLine: 2, children: t.message }),
        /* @__PURE__ */ v.jsx("div", { className: "row", style: { padding: "0 1.2rem", transform: "translateY(-0.2rem)" }, children: /* @__PURE__ */ v.jsx("div", { style: { borderLeft: "0.6rem solid transparent", borderRight: "0.6rem solid transparent", borderTop: "0.8rem solid var(--neutral-main-reverse-background-color)", borderRadius: 2 } }) })
      ] });
    case "bottom":
      return /* @__PURE__ */ v.jsxs("div", { ref: (s) => {
        if (s) {
          const r = s.getBoundingClientRect();
          r.x < 0 ? (s.style.left = e.x + e.width / 2 + "px", s.style.transform = "translateX(-1.8rem)", s.style.alignItems = "start") : r.right > document.body.offsetWidth && (s.style.left = "unset", s.style.right = document.body.offsetWidth - e.right - e.width / 2 + "px", s.style.transform = "translateX(-1.4rem)", s.style.alignItems = "end");
        }
      }, className: `col ${cn["tooltip-container"]}`, style: { alignItems: "center", top: e.bottom + 4, left: e.left + e.width / 2, transform: "translateX(-50%)" }, children: [
        /* @__PURE__ */ v.jsx("div", { className: "row", style: { padding: "0 1.2rem", transform: "translateY(0.2rem)" }, children: /* @__PURE__ */ v.jsx("div", { style: { borderLeft: "0.6rem solid transparent", borderRight: "0.6rem solid transparent", borderBottom: "0.8rem solid var(--neutral-main-reverse-background-color)", borderRadius: 2 } }) }),
        /* @__PURE__ */ v.jsx(Ae, { className: `body-3 ${cn["tooltip-message"]}`, maxLine: 2, children: t.message })
      ] });
    case "left":
      return /* @__PURE__ */ v.jsxs("div", { ref: (s) => {
        if (s) {
          const r = s.getBoundingClientRect();
          r.y < 0 ? (s.style.top = e.y + e.height / 2 + "px", s.style.transform = "translateY(-1.8rem)", s.style.alignItems = "start") : r.bottom > document.body.offsetHeight && (s.style.top = "unset", s.style.bottom = document.body.offsetHeight - e.bottom - e.height / 2 + "px", s.style.transform = "translateY(-1.4rem)", s.style.alignItems = "end");
        }
      }, className: `row ${cn["tooltip-container"]}`, style: { top: e.top + e.height / 2, right: document.body.offsetWidth - e.left - 4, transform: "translateY(-50%)" }, children: [
        /* @__PURE__ */ v.jsx(Ae, { className: `body-3 ${cn["tooltip-message"]}`, maxLine: 2, children: t.message }),
        /* @__PURE__ */ v.jsx("div", { className: "row", style: { padding: "1.2rem 0", transform: "translateX(-0.2rem)" }, children: /* @__PURE__ */ v.jsx("div", { style: { borderTop: "0.6rem solid transparent", borderBottom: "0.6rem solid transparent", borderLeft: "0.8rem solid var(--neutral-main-reverse-background-color)", borderRadius: 2 } }) })
      ] });
    case "right":
      return /* @__PURE__ */ v.jsxs("div", { ref: (s) => {
        if (s) {
          const r = s.getBoundingClientRect();
          r.y < 0 ? (s.style.top = e.y + e.height / 2 + "px", s.style.transform = "translateY(-1.8rem)", s.style.alignItems = "start") : r.bottom > document.body.offsetHeight && (s.style.top = "unset", s.style.bottom = document.body.offsetHeight - e.bottom - e.height / 2 + "px", s.style.transform = "translateY(-1.4rem)", s.style.alignItems = "end");
        }
      }, className: `row ${cn["tooltip-container"]}`, style: { top: e.top + e.height / 2, left: e.right + 4, transform: "translateY(-50%)" }, children: [
        /* @__PURE__ */ v.jsx("div", { className: "row", style: { padding: "1.2rem 0", transform: "translateX(0.2rem)" }, children: /* @__PURE__ */ v.jsx("div", { style: { borderTop: "0.6rem solid transparent", borderBottom: "0.6rem solid transparent", borderRight: "0.8rem solid var(--neutral-main-reverse-background-color)", borderRadius: 2 } }) }),
        /* @__PURE__ */ v.jsx(Ae, { className: `body-3 ${cn["tooltip-message"]}`, maxLine: 2, children: t.message })
      ] });
    default:
      return /* @__PURE__ */ v.jsx("div", {});
  }
}, K2 = (n, t, e, i) => {
  var r, o, a, l;
  const s = [e, {
    code: t,
    ...i || {}
  }];
  if ((o = (r = n == null ? void 0 : n.services) == null ? void 0 : r.logger) != null && o.forward)
    return n.services.logger.forward(s, "warn", "react-i18next::", !0);
  ds(s[0]) && (s[0] = `react-i18next:: ${s[0]}`), (l = (a = n == null ? void 0 : n.services) == null ? void 0 : a.logger) != null && l.warn ? n.services.logger.warn(...s) : console != null && console.warn && console.warn(...s);
}, Lm = {}, Ld = (n, t, e, i) => {
  ds(e) && Lm[e] || (ds(e) && (Lm[e] = /* @__PURE__ */ new Date()), K2(n, t, e, i));
}, l0 = (n, t) => () => {
  if (n.isInitialized)
    t();
  else {
    const e = () => {
      setTimeout(() => {
        n.off("initialized", e);
      }, 0), t();
    };
    n.on("initialized", e);
  }
}, Bd = (n, t, e) => {
  n.loadNamespaces(t, l0(n, e));
}, Bm = (n, t, e, i) => {
  if (ds(e) && (e = [e]), n.options.preload && n.options.preload.indexOf(t) > -1) return Bd(n, e, i);
  e.forEach((s) => {
    n.options.ns.indexOf(s) < 0 && n.options.ns.push(s);
  }), n.loadLanguages(t, l0(n, i));
}, Z2 = (n, t, e = {}) => !t.languages || !t.languages.length ? (Ld(t, "NO_LANGUAGES", "i18n.languages were undefined or empty", {
  languages: t.languages
}), !0) : t.hasLoadedNamespace(n, {
  lng: e.lng,
  precheck: (i, s) => {
    var r;
    if (((r = e.bindI18n) == null ? void 0 : r.indexOf("languageChanging")) > -1 && i.services.backendConnector.backend && i.isLanguageChangingTo && !s(i.isLanguageChangingTo, n)) return !1;
  }
}), Y2 = (n) => n.displayName || n.name || (ds(n) && n.length > 0 ? n : "Unknown"), ds = (n) => typeof n == "string", J2 = (n) => typeof n == "object" && n !== null, X2 = /&(?:amp|#38|lt|#60|gt|#62|apos|#39|quot|#34|nbsp|#160|copy|#169|reg|#174|hellip|#8230|#x2F|#47);/g, Q2 = {
  "&amp;": "&",
  "&#38;": "&",
  "&lt;": "<",
  "&#60;": "<",
  "&gt;": ">",
  "&#62;": ">",
  "&apos;": "'",
  "&#39;": "'",
  "&quot;": '"',
  "&#34;": '"',
  "&nbsp;": " ",
  "&#160;": " ",
  "&copy;": "©",
  "&#169;": "©",
  "&reg;": "®",
  "&#174;": "®",
  "&hellip;": "…",
  "&#8230;": "…",
  "&#x2F;": "/",
  "&#47;": "/"
}, ex = (n) => Q2[n], tx = (n) => n.replace(X2, ex);
let Md = {
  bindI18n: "languageChanged",
  bindI18nStore: "",
  transEmptyNodeValue: "",
  transSupportBasicHtmlNodes: !0,
  transWrapTextNodes: "",
  transKeepBasicHtmlNodesFor: ["br", "strong", "i", "p"],
  useSuspense: !0,
  unescape: tx
};
const ix = (n = {}) => {
  Md = {
    ...Md,
    ...n
  };
}, nx = () => Md;
let c0;
const sx = (n) => {
  c0 = n;
}, rx = () => c0, ox = {
  type: "3rdParty",
  init(n) {
    ix(n.options.react), sx(n);
  }
}, ax = L2();
class lx {
  constructor() {
    this.usedNamespaces = {};
  }
  addUsedNamespaces(t) {
    t.forEach((e) => {
      this.usedNamespaces[e] || (this.usedNamespaces[e] = !0);
    });
  }
  getUsedNamespaces() {
    return Object.keys(this.usedNamespaces);
  }
}
const cx = (n, t) => {
  const e = yt();
  return ht(() => {
    e.current = n;
  }, [n, t]), e.current;
}, u0 = (n, t, e, i) => n.getFixedT(t, e, i), ux = (n, t, e, i) => M2(u0(n, t, e, i), [n, t, e, i]), Yr = (n, t = {}) => {
  var E, T, L, O;
  const {
    i18n: e
  } = t, {
    i18n: i,
    defaultNS: s
  } = B2(ax) || {}, r = e || i || rx();
  if (r && !r.reportNamespaces && (r.reportNamespaces = new lx()), !r) {
    Ld(r, "NO_I18NEXT_INSTANCE", "useTranslation: You will need to pass in an i18next instance by using initReactI18next");
    const N = (J, D) => ds(D) ? D : J2(D) && ds(D.defaultValue) ? D.defaultValue : Array.isArray(J) ? J[J.length - 1] : J, M = [N, {}, !1];
    return M.t = N, M.i18n = {}, M.ready = !1, M;
  }
  (E = r.options.react) != null && E.wait && Ld(r, "DEPRECATED_OPTION", "useTranslation: It seems you are still using the old wait option, you may migrate to the new useSuspense behaviour.");
  const o = {
    ...nx(),
    ...r.options.react,
    ...t
  }, {
    useSuspense: a,
    keyPrefix: l
  } = o;
  let c = s || ((T = r.options) == null ? void 0 : T.defaultNS);
  c = ds(c) ? [c] : c || ["translation"], (O = (L = r.reportNamespaces).addUsedNamespaces) == null || O.call(L, c);
  const u = (r.isInitialized || r.initializedStoreOnce) && c.every((N) => Z2(N, r, o)), d = ux(r, t.lng || null, o.nsMode === "fallback" ? c : c[0], l), h = () => d, m = () => u0(r, t.lng || null, o.nsMode === "fallback" ? c : c[0], l), [g, p] = zt(h);
  let w = c.join();
  t.lng && (w = `${t.lng}${w}`);
  const y = cx(w), k = yt(!0);
  ht(() => {
    const {
      bindI18n: N,
      bindI18nStore: M
    } = o;
    k.current = !0, !u && !a && (t.lng ? Bm(r, t.lng, c, () => {
      k.current && p(m);
    }) : Bd(r, c, () => {
      k.current && p(m);
    })), u && y && y !== w && k.current && p(m);
    const J = () => {
      k.current && p(m);
    };
    return N && (r == null || r.on(N, J)), M && (r == null || r.store.on(M, J)), () => {
      k.current = !1, r && (N == null || N.split(" ").forEach((D) => r.off(D, J))), M && r && M.split(" ").forEach((D) => r.store.off(D, J));
    };
  }, [r, w]), ht(() => {
    k.current && u && p(h);
  }, [r, l, u]);
  const C = [g, r, u];
  if (C.t = g, C.i18n = r, C.ready = u, u || !u && !a) return C;
  throw new Promise((N) => {
    t.lng ? Bm(r, t.lng, c, () => N()) : Bd(r, c, () => N());
  });
}, Oc = (n, t = {}) => function(i) {
  function s({
    forwardedRef: o,
    ...a
  }) {
    const [l, c, u] = Yr(n, {
      ...a,
      keyPrefix: t.keyPrefix
    }), d = {
      ...a,
      t: l,
      i18n: c,
      tReady: u
    };
    return t.withRef && o ? d.ref = o : !t.withRef && o && (d.forwardedRef = o), Pm(i, d);
  }
  s.displayName = `withI18nextTranslation(${Y2(i)})`, s.WrappedComponent = i;
  const r = (o, a) => Pm(s, Object.assign({}, o, {
    forwardedRef: a
  }));
  return t.withRef ? Ic(r) : s;
}, Io = (n) => {
  var t, e;
  (e = (t = n.ref) == null ? void 0 : t.current) == null || e.onOpen({
    heading: n.heading,
    content: n.content,
    body: n.body,
    footer: n.footer,
    clickOverlayClosePopup: n.clickOverlayClosePopup,
    style: n.style,
    className: n.className,
    hideButtonClose: n.hideButtonClose
  });
}, Ss = (n) => {
  n.current.onClose();
};
class d0 extends ce.Component {
  constructor(e) {
    super(e);
    f(this, "state", {
      open: !1
    });
  }
  onOpen(e) {
    this.setState({ open: !0, ...e });
  }
  onClose() {
    this.setState({ open: !1 });
  }
  render() {
    return /* @__PURE__ */ v.jsx(v.Fragment, { children: this.state.open && /* @__PURE__ */ v.jsx(Yh, { className: this.state.clickOverlayClosePopup ? "hidden-overlay" : "", onClose: this.state.clickOverlayClosePopup ? () => {
      this.onClose();
    } : void 0, children: this.state.content ?? /* @__PURE__ */ v.jsxs("div", { className: `popup-container col ${this.state.className ?? ""}`, onClick: (e) => e.stopPropagation(), style: this.state.style, children: [
      this.state.heading,
      this.state.body,
      this.state.footer,
      this.state.hideButtonClose ? null : /* @__PURE__ */ v.jsx("button", { type: "button", onClick: () => this.onClose(), className: "popup-close-btn row", children: /* @__PURE__ */ v.jsx("svg", { width: "100%", height: "100%", viewBox: "0 0 20 20", fill: "none", xmlns: "http://www.w3.org/2000/svg", style: { width: "2rem", height: "2rem" }, children: /* @__PURE__ */ v.jsx("path", { fillRule: "evenodd", clipRule: "evenodd", d: "M16.4223 4.7559C16.7477 4.43047 16.7477 3.90283 16.4223 3.57739C16.0968 3.25195 15.5692 3.25195 15.2438 3.57739L9.99967 8.82147L4.7556 3.57739C4.43016 3.25195 3.90252 3.25195 3.57709 3.57739C3.25165 3.90283 3.25165 4.43047 3.57709 4.7559L8.82116 9.99998L3.57709 15.2441C3.25165 15.5695 3.25165 16.0971 3.57709 16.4226C3.90252 16.748 4.43016 16.748 4.7556 16.4226L9.99967 11.1785L15.2438 16.4226C15.5692 16.748 16.0968 16.748 16.4223 16.4226C16.7477 16.0971 16.7477 15.5695 16.4223 15.2441L11.1782 9.99998L16.4223 4.7559Z", fill: "#00204D", fillOpacity: 0.6 }) }) })
    ] }) }) });
  }
}
function Yh({ children: n, onClose: t, className: e, style: i, onOpen: s }) {
  const r = yt(null);
  return ht(() => {
    if (r.current && t) {
      const o = (a) => {
        (a.target === r.current || !r.current.contains(a.target)) && t(a);
      };
      return window.document.body.addEventListener("mousedown", o), () => {
        window.document.body.removeEventListener("mousedown", o);
      };
    }
  }, [r.current]), ht(() => {
    r.current && s && s(r.current);
  }, [r.current, s]), ht(() => {
    if (r.current && r.current.firstChild) {
      const o = r.current.firstChild, a = o.getBoundingClientRect();
      a.x < 0 ? (o.style.left = "0px", o.style.right = "unset") : a.right > document.body.offsetWidth && (o.style.right = "0px", o.style.left = "unset"), a.y < 0 ? (o.style.top = "0px", o.style.bottom = "unset") : a.bottom > document.body.offsetHeight && (o.style.bottom = "0px", o.style.top = "unset");
    }
  }, [r]), /* @__PURE__ */ v.jsx(
    "div",
    {
      ref: r,
      className: `popup-overlay ${e ?? ""}`,
      style: i,
      children: n
    }
  );
}
class dx extends ce.Component {
  constructor(e) {
    var i;
    super(e);
    f(this, "containerRef", vn());
    f(this, "inputRef", vn());
    f(this, "onKeyDown", (e) => {
      var i, s, r, o;
      if (((i = this.state.options) != null && i.length || (s = this.state.search) != null && s.length) && this.state.isOpen)
        switch (e.key.toLowerCase()) {
          case "enter":
            e.preventDefault();
            const a = (this.state.search ?? this.state.options).find((l) => l.id === this.state.selected);
            a && this.onSelect(a);
            break;
          case "arrowup":
            if (e.preventDefault(), this.state.selected) {
              let l = (this.state.search ?? this.state.options).findIndex((c) => c.id === this.state.selected);
              l = (l === 0 ? this.props.options.length : l) - 1, this.setState({ ...this.state, selected: (r = this.props.options[l]) == null ? void 0 : r.id });
            }
            break;
          case "arrowdown":
            if (e.preventDefault(), this.state.selected) {
              let l = (this.state.search ?? this.state.options).findIndex((c) => c.id === this.state.selected);
              l = (l + 1 === this.props.options.length ? -1 : l) + 1, this.setState({ ...this.state, selected: (o = this.props.options[l]) == null ? void 0 : o.id });
            }
            break;
        }
    });
    this.state = {
      value: e.value,
      options: e.options,
      offset: {
        x: 0,
        y: 0,
        height: 0,
        width: 0,
        bottom: 0,
        left: 0,
        right: 0,
        top: 0,
        toJSON: function() {
          throw new Error("Function not implemented.");
        }
      },
      isOpen: !1,
      onSelect: null
    }, this.search = this.search.bind(this), this.onSelect = this.onSelect.bind(this), this.onKeyDown = this.onKeyDown.bind(this), this.inputRef.current && (this.inputRef.current.value = `${((i = this.state.options.find((s) => s.id === this.state.value)) == null ? void 0 : i.name) ?? ""}`);
  }
  async search(e) {
    var i;
    if (e.target.value.trim().length)
      if ((i = this.props) != null && i.handleSearch) {
        const s = await this.props.handleSearch(e.target.value.trim());
        this.setState({ ...this.state, search: s });
      } else
        this.setState({
          ...this.state,
          search: this.props.options.filter((s) => typeof s.name == "string" && s.name.toLowerCase().includes(e.target.value.trim().toLowerCase()))
        });
    else
      this.setState({ ...this.state, search: void 0 });
  }
  onSelect(e) {
    var i, s;
    if (e.disabled)
      this.setState({ ...this.state, isOpen: !1, onSelect: void 0, selected: void 0 }), (i = this.inputRef.current) == null || i.blur();
    else {
      let r = { ...this.state, isOpen: !1, value: e.id, onSelect: void 0, selected: void 0 };
      r.options.some((o) => o.id === e.id) || r.options.push(e), this.setState(r), (s = this.inputRef.current) == null || s.blur();
    }
    this.props.onChange && this.props.onChange(e);
  }
  componentDidUpdate(e, i) {
    var s, r, o, a;
    if (e.options !== this.props.options && (this.setState({ ...this.state, options: this.props.options }), this.inputRef.current && (this.inputRef.current.value = `${((s = this.props.options.find((l) => l.id === this.state.value)) == null ? void 0 : s.name) ?? ""}`)), e.value !== this.props.value && this.setState({ ...this.state, value: this.props.value }), i.value !== this.state.value && this.inputRef.current && (this.inputRef.current.value = `${((r = this.state.options.find((l) => l.id === this.state.value)) == null ? void 0 : r.name) ?? ""}`), this.state.isOpen && i.isOpen !== this.state.isOpen) {
      const l = (o = this.containerRef.current.querySelector(".select1-popup")) == null ? void 0 : o.getBoundingClientRect();
      if (l) {
        let c;
        l.right > document.body.offsetWidth && (c = {
          top: this.state.offset.y + this.state.offset.height + 2 + "px",
          right: document.body.offsetWidth - this.state.offset.right + "px"
        });
        let u = l.bottom - 8;
        const d = (a = this.containerRef.current) == null ? void 0 : a.getBoundingClientRect();
        d && u > document.body.offsetHeight && (c = { ...c ?? {}, top: `${d.y - 2 - l.height}px` }), c && (c.left ?? (c.left = c.right ? void 0 : `${this.state.offset.x}px`), c.width ?? (c.width = `${this.state.offset.width}px`), this.setState({ ...this.state, style: c }));
      }
    }
  }
  componentDidMount() {
    var e;
    this.inputRef.current && (this.inputRef.current.value = `${((e = this.state.options.find((i) => i.id === this.state.value)) == null ? void 0 : e.name) ?? ""}`);
  }
  render() {
    var s, r, o;
    const { t: e } = this.props, i = this.state.options.find((a) => a.id === this.state.value);
    return /* @__PURE__ */ v.jsxs(
      "div",
      {
        id: this.props.id,
        ref: this.containerRef,
        className: `${as["select1-container"]} row ${this.props.disabled ? as.disabled : ""} ${((s = this.props.helperText) == null ? void 0 : s.length) && as["helper-text"]} ${this.props.className ?? "body-3"}`,
        "helper-text": this.props.helperText,
        style: this.props.style ? { "--helper-text-color": this.props.helperTextColor ?? "#e14337", ...this.props.style } : { "--helper-text-color": this.props.helperTextColor ?? "#e14337" },
        onClick: () => {
          var a, l, c;
          this.state.isOpen || (this.setState({
            ...this.state,
            isOpen: !0,
            style: void 0,
            offset: (l = (a = this.containerRef) == null ? void 0 : a.current) == null ? void 0 : l.getBoundingClientRect()
          }), (c = this.inputRef.current) == null || c.focus());
        },
        children: [
          this.props.prefix,
          !i || typeof i.name == "string" || typeof i.name == "number" ? /* @__PURE__ */ v.jsx(
            "input",
            {
              ref: this.inputRef,
              readOnly: this.props.readOnly,
              onChange: this.search,
              placeholder: this.props.placeholder,
              onBlur: (a) => {
                this.state.onSelect && !this.props.readOnly && a.target.focus();
              }
            }
          ) : i.name,
          this.props.suffix ?? /* @__PURE__ */ v.jsx("div", { ref: (a) => {
            a != null && a.parentElement && a.parentElement.getBoundingClientRect().width < 88 && (a.style.display = "none");
          }, className: "row", children: /* @__PURE__ */ v.jsx(rt, { src: this.state.isOpen ? "fill/arrows/up-arrow" : "fill/arrows/down-arrow", size: "1.2rem" }) }),
          this.state.isOpen && /* @__PURE__ */ v.jsx(
            Yh,
            {
              onOpen: this.props.onOpenOptions,
              className: "hidden-overlay",
              onClose: (a) => {
                a.target !== this.inputRef.current && this.setState({ ...this.state, isOpen: !1 });
              },
              children: /* @__PURE__ */ v.jsx("div", { className: `${as["select1-popup"]} select1-popup col ${this.props.popupClassName ?? ""}`, style: this.state.style ?? {
                top: this.state.offset.y + this.state.offset.height + 2 + "px",
                left: this.state.offset.x + "px",
                width: this.state.offset.width
              }, children: /* @__PURE__ */ v.jsxs("div", { className: `col ${as["select-body"]}`, onScroll: this.props.handleLoadmore ? (a) => {
                if (this.props.handleLoadmore) {
                  let l = a.target;
                  this.props.handleLoadmore(Math.round(l.offsetHeight + l.scrollTop) >= l.scrollHeight - 1, a);
                }
              } : void 0, children: [
                (this.state.search ?? this.state.options).filter((a) => !a.parentId).map((a) => /* @__PURE__ */ v.jsx(
                  h0,
                  {
                    item: a,
                    children: (this.state.search ?? this.state.options).filter((l) => l.parentId === a.id),
                    selected: this.state.selected === a.id,
                    onClick: this.onSelect,
                    treeData: (this.state.search ?? this.state.options).some((l) => l.parentId)
                  },
                  a.id
                )),
                !((r = this.state.search) != null && r.length) && !((o = this.props.options) != null && o.length) && /* @__PURE__ */ v.jsx("div", { className: as["no-results-found"], children: e("noResultFound") })
              ] }) })
            }
          )
        ]
      }
    );
  }
}
function h0({ item: n, children: t, selected: e, onClick: i, treeData: s }) {
  const [r, o] = zt(!1);
  return n.title && typeof n.title != "string" ? /* @__PURE__ */ v.jsx(v.Fragment, { children: n.title(i) }) : /* @__PURE__ */ v.jsxs("div", { className: "col", style: { width: "100%" }, children: [
    /* @__PURE__ */ v.jsxs("div", { className: `${as["select-tile"]} row ${n.disabled ? as.disabled : ""}`, style: { paddingLeft: n.parentId ? "4.4rem" : void 0, backgroundColor: e ? "var(--neutral-selected-background-color)" : void 0 }, onClick: () => {
      t != null && t.length ? o(!r) : i(n);
    }, children: [
      s ? /* @__PURE__ */ v.jsx("div", { className: "row", style: { width: "1.4rem", height: "1.4rem" }, children: t != null && t.length ? /* @__PURE__ */ v.jsx(rt, { src: r ? "fill/arrows/triangle-down" : "fill/arrows/triangle-right", size: "1.2rem" }) : null }) : void 0,
      n.title && typeof n.title == "string" || typeof n.name == "string" ? /* @__PURE__ */ v.jsx(Ae, { className: "body-3", children: n.title && typeof n.title == "string" ? n.title : n.name }) : n.name
    ] }),
    t != null && t.length ? /* @__PURE__ */ v.jsx("div", { className: "col", style: { display: r ? "flex" : "none", width: "100%" }, children: t.map((a) => /* @__PURE__ */ v.jsx(h0, { item: a, onClick: i }, a.id)) }) : void 0
  ] });
}
const hx = Oc()(dx), fx = "_slider_zx9ru_35", Mm = {
  "switch-container": "_switch-container_zx9ru_1",
  slider: fx
};
class RZ extends ce.Component {
  constructor() {
    super(...arguments);
    f(this, "state", {
      value: this.props.value ?? !1
    });
  }
  componentDidUpdate(e) {
    e.value !== this.props.value && this.setState({ value: this.props.value });
  }
  render() {
    const e = {
      "--off-bg": this.props.offBackground ?? "var(--neutral-main-background-color)",
      "--on-bg": this.props.onBackground ?? "var(--primary-main-color)",
      "--dot-color": this.props.dotColor ?? "#ffffff",
      "--size": this.props.size ? typeof this.props.size == "number" ? `${this.props.size}px` : this.props.size : "2rem"
    };
    let i = {
      height: this.props.size ?? "2rem",
      width: `calc(${this.props.size ? typeof this.props.size == "number" ? `${this.props.size}px` : this.props.size : "2rem"} * 9 / 5)`,
      ...e
    };
    return this.props.style && (delete this.props.style.width, delete this.props.style.minWidth, delete this.props.style.maxWidth, delete this.props.style.height, delete this.props.style.minHeight, delete this.props.style.maxHeight, i = {
      ...this.props.style,
      ...i
    }), /* @__PURE__ */ v.jsxs("label", { id: this.props.id, className: `${Mm["switch-container"]} row ${this.props.className ?? ""}`, style: i, children: [
      /* @__PURE__ */ v.jsx(
        "input",
        {
          type: "checkbox",
          checked: this.state.value,
          name: this.props.name,
          disabled: this.props.disabled,
          onChange: () => {
            const s = !this.state.value;
            this.setState({ value: s }), this.props.onChange && this.props.onChange(s);
          }
        }
      ),
      /* @__PURE__ */ v.jsx("span", { className: Mm.slider })
    ] });
  }
}
const ts = {
  "dialog-overlay": "_dialog-overlay_19n83_1",
  "dialog-container": "_dialog-container_19n83_27",
  "dialog-body": "_dialog-body_19n83_59",
  "dialog-status": "_dialog-status_19n83_87",
  "dialog-footer": "_dialog-footer_19n83_103",
  "dialog-action": "_dialog-action_19n83_115",
  "dialog-submit": "_dialog-submit_19n83_139"
};
var mx = /* @__PURE__ */ ((n) => (n.start = "start", n.center = "center", n.end = "end", n))(mx || {});
class gx extends ce.Component {
  constructor(t) {
    super(t), this.state = {
      open: !1,
      title: "",
      status: Xo.INFOR,
      content: "",
      onSubmit: () => {
      }
    };
  }
  showDialogNoti(t) {
    this.setState({ open: !0, ...t });
  }
  closeDialog() {
    this.setState({ open: !1 });
  }
  render() {
    const { t } = this.props;
    return /* @__PURE__ */ v.jsx(v.Fragment, { children: this.state.open && Zh.createPortal(
      /* @__PURE__ */ v.jsx("div", { className: ts["dialog-overlay"], children: /* @__PURE__ */ v.jsxs("div", { className: `${ts["dialog-container"]} col`, style: { width: "41.4rem", alignItems: this.state.alignment }, "dialog-type": this.state.status, onClick: (e) => e.stopPropagation(), children: [
        /* @__PURE__ */ v.jsxs("div", { className: `${ts["dialog-body"]} col`, style: { alignItems: "inherit" }, children: [
          /* @__PURE__ */ v.jsx("div", { className: `${ts["dialog-status"]} row`, children: x0(this.state.status) }),
          /* @__PURE__ */ v.jsxs("div", { className: "col", children: [
            /* @__PURE__ */ v.jsx(Ae, { className: "heading-6", style: { textAlign: this.state.alignment === "center" ? "center" : "start" }, children: this.state.title }),
            /* @__PURE__ */ v.jsx(Ae, { className: "body-3", style: { textAlign: this.state.alignment === "center" ? "center" : "start" }, children: this.state.content })
          ] })
        ] }),
        /* @__PURE__ */ v.jsxs("div", { className: `${ts["dialog-footer"]} row`, children: [
          /* @__PURE__ */ v.jsx("button", { type: "button", style: this.state.alignment === "center" ? { flex: 1, width: "100%" } : void 0, onClick: () => {
            this.state.onCancel && this.state.onCancel(), this.setState({ open: !1 });
          }, className: `${ts["dialog-action"]} row`, children: /* @__PURE__ */ v.jsx(Ae, { className: "button-text-3", children: this.state.cancelTitle ?? t("cancel") }) }),
          /* @__PURE__ */ v.jsx("button", { type: "button", style: this.state.alignment === "center" ? { flex: 1, width: "100%" } : void 0, onClick: () => {
            this.state.onSubmit(), this.setState({ open: !1 });
          }, className: `${ts["dialog-action"]} row ${ts["dialog-submit"]}`, children: /* @__PURE__ */ v.jsx(Ae, { className: "button-text-3", children: this.state.submitTitle ?? t("submit") }) })
        ] })
      ] }) }),
      document.body
    ) });
  }
}
const Nd = vn(), OZ = () => {
  const { t: n, i18n: t } = Yr();
  return /* @__PURE__ */ v.jsx(gx, { ref: Nd, t: n, i18n: t, tReady: !0 });
}, VZ = (n) => {
  Nd.current && Nd.current.showDialogNoti({
    title: n.title ?? "",
    status: n.status ?? Xo.INFOR,
    content: n.content ?? "",
    onSubmit: n.onSubmit ?? (() => {
    }),
    onCancel: n.onCancel,
    submitTitle: n.submitTitle,
    cancelTitle: n.cancelTitle,
    alignment: n.alignment
  });
}, px = "_value_9nzyy_29", zi = {
  "date-time-picker": "_date-time-picker_9nzyy_1",
  "prefix-icon": "_prefix-icon_9nzyy_15",
  value: px,
  "helper-text": "_helper-text_9nzyy_121",
  "popup-actions": "_popup-actions_9nzyy_159"
};
var xa = (n) => n.type === "checkbox", Is = (n) => n instanceof Date, ei = (n) => n == null;
const f0 = (n) => typeof n == "object";
var xt = (n) => !ei(n) && !Array.isArray(n) && f0(n) && !Is(n), bx = (n) => xt(n) && n.target ? xa(n.target) ? n.target.checked : n.target.value : n, wx = (n) => n.substring(0, n.search(/\.\d+(\.|$)/)) || n, vx = (n, t) => n.has(wx(t)), _x = (n) => {
  const t = n.constructor && n.constructor.prototype;
  return xt(t) && t.hasOwnProperty("isPrototypeOf");
}, Jh = typeof window < "u" && typeof window.HTMLElement < "u" && typeof document < "u";
function Vi(n) {
  let t;
  const e = Array.isArray(n), i = typeof FileList < "u" ? n instanceof FileList : !1;
  if (n instanceof Date)
    t = new Date(n);
  else if (n instanceof Set)
    t = new Set(n);
  else if (!(Jh && (n instanceof Blob || i)) && (e || xt(n)))
    if (t = e ? [] : {}, !e && !_x(n))
      t = n;
    else
      for (const s in n)
        n.hasOwnProperty(s) && (t[s] = Vi(n[s]));
  else
    return n;
  return t;
}
var Vc = (n) => Array.isArray(n) ? n.filter(Boolean) : [], It = (n) => n === void 0, ge = (n, t, e) => {
  if (!t || !xt(n))
    return e;
  const i = Vc(t.split(/[,[\].]+?/)).reduce((s, r) => ei(s) ? s : s[r], n);
  return It(i) || i === n ? It(n[t]) ? e : n[t] : i;
}, un = (n) => typeof n == "boolean", Xh = (n) => /^\w*$/.test(n), m0 = (n) => Vc(n.replace(/["|']|\]/g, "").split(/\.|\[/)), ut = (n, t, e) => {
  let i = -1;
  const s = Xh(t) ? [t] : m0(t), r = s.length, o = r - 1;
  for (; ++i < r; ) {
    const a = s[i];
    let l = e;
    if (i !== o) {
      const c = n[a];
      l = xt(c) || Array.isArray(c) ? c : isNaN(+s[i + 1]) ? {} : [];
    }
    if (a === "__proto__" || a === "constructor" || a === "prototype")
      return;
    n[a] = l, n = n[a];
  }
  return n;
};
const Nm = {
  BLUR: "blur",
  FOCUS_OUT: "focusout"
}, Wi = {
  onBlur: "onBlur",
  onChange: "onChange",
  onSubmit: "onSubmit",
  onTouched: "onTouched",
  all: "all"
}, In = {
  max: "max",
  min: "min",
  maxLength: "maxLength",
  minLength: "minLength",
  pattern: "pattern",
  required: "required",
  validate: "validate"
};
ce.createContext(null);
var yx = (n, t, e, i = !0) => {
  const s = {
    defaultValues: t._defaultValues
  };
  for (const r in n)
    Object.defineProperty(s, r, {
      get: () => {
        const o = r;
        return t._proxyFormState[o] !== Wi.all && (t._proxyFormState[o] = !i || Wi.all), n[o];
      }
    });
  return s;
}, ui = (n) => xt(n) && !Object.keys(n).length, kx = (n, t, e, i) => {
  e(n);
  const { name: s, ...r } = n;
  return ui(r) || Object.keys(r).length >= Object.keys(t).length || Object.keys(r).find((o) => t[o] === Wi.all);
}, Al = (n) => Array.isArray(n) ? n : [n];
function Cx(n) {
  const t = ce.useRef(n);
  t.current = n, ce.useEffect(() => {
    const e = !n.disabled && t.current.subject && t.current.subject.subscribe({
      next: t.current.next
    });
    return () => {
      e && e.unsubscribe();
    };
  }, [n.disabled]);
}
var gn = (n) => typeof n == "string", xx = (n, t, e, i, s) => gn(n) ? (i && t.watch.add(n), ge(e, n, s)) : Array.isArray(n) ? n.map((r) => (i && t.watch.add(r), ge(e, r))) : (i && (t.watchAll = !0), e), Tx = (n, t, e, i, s) => t ? {
  ...e[n],
  types: {
    ...e[n] && e[n].types ? e[n].types : {},
    [i]: s || !0
  }
} : {}, Fm = (n) => ({
  isOnSubmit: !n || n === Wi.onSubmit,
  isOnBlur: n === Wi.onBlur,
  isOnChange: n === Wi.onChange,
  isOnAll: n === Wi.all,
  isOnTouch: n === Wi.onTouched
}), Dm = (n, t, e) => !e && (t.watchAll || t.watch.has(n) || [...t.watch].some((i) => n.startsWith(i) && /^\.\w+/.test(n.slice(i.length))));
const Mo = (n, t, e, i) => {
  for (const s of e || Object.keys(n)) {
    const r = ge(n, s);
    if (r) {
      const { _f: o, ...a } = r;
      if (o) {
        if (o.refs && o.refs[0] && t(o.refs[0], s) && !i)
          return !0;
        if (o.ref && t(o.ref, o.name) && !i)
          return !0;
        if (Mo(a, t))
          break;
      } else if (xt(a) && Mo(a, t))
        break;
    }
  }
};
var Ex = (n, t, e) => {
  const i = Al(ge(n, e));
  return ut(i, "root", t[e]), ut(n, e, i), n;
}, Qh = (n) => n.type === "file", fn = (n) => typeof n == "function", Hl = (n) => {
  if (!Jh)
    return !1;
  const t = n ? n.ownerDocument : 0;
  return n instanceof (t && t.defaultView ? t.defaultView.HTMLElement : HTMLElement);
}, Il = (n) => gn(n), ef = (n) => n.type === "radio", jl = (n) => n instanceof RegExp;
const $m = {
  value: !1,
  isValid: !1
}, zm = { value: !0, isValid: !0 };
var g0 = (n) => {
  if (Array.isArray(n)) {
    if (n.length > 1) {
      const t = n.filter((e) => e && e.checked && !e.disabled).map((e) => e.value);
      return { value: t, isValid: !!t.length };
    }
    return n[0].checked && !n[0].disabled ? (
      // @ts-expect-error expected to work in the browser
      n[0].attributes && !It(n[0].attributes.value) ? It(n[0].value) || n[0].value === "" ? zm : { value: n[0].value, isValid: !0 } : zm
    ) : $m;
  }
  return $m;
};
const Hm = {
  isValid: !1,
  value: null
};
var p0 = (n) => Array.isArray(n) ? n.reduce((t, e) => e && e.checked && !e.disabled ? {
  isValid: !0,
  value: e.value
} : t, Hm) : Hm;
function jm(n, t, e = "validate") {
  if (Il(n) || Array.isArray(n) && n.every(Il) || un(n) && !n)
    return {
      type: e,
      message: Il(n) ? n : "",
      ref: t
    };
}
var or = (n) => xt(n) && !jl(n) ? n : {
  value: n,
  message: ""
}, Um = async (n, t, e, i, s, r) => {
  const { ref: o, refs: a, required: l, maxLength: c, minLength: u, min: d, max: h, pattern: m, validate: g, name: p, valueAsNumber: w, mount: y } = n._f, k = ge(e, p);
  if (!y || t.has(p))
    return {};
  const C = a ? a[0] : o, E = (B) => {
    s && C.reportValidity && (C.setCustomValidity(un(B) ? "" : B || ""), C.reportValidity());
  }, T = {}, L = ef(o), O = xa(o), N = L || O, M = (w || Qh(o)) && It(o.value) && It(k) || Hl(o) && o.value === "" || k === "" || Array.isArray(k) && !k.length, J = Tx.bind(null, p, i, T), D = (B, $, W, Y = In.maxLength, ye = In.minLength) => {
    const Pe = B ? $ : W;
    T[p] = {
      type: B ? Y : ye,
      message: Pe,
      ref: o,
      ...J(B ? Y : ye, Pe)
    };
  };
  if (r ? !Array.isArray(k) || !k.length : l && (!N && (M || ei(k)) || un(k) && !k || O && !g0(a).isValid || L && !p0(a).isValid)) {
    const { value: B, message: $ } = Il(l) ? { value: !!l, message: l } : or(l);
    if (B && (T[p] = {
      type: In.required,
      message: $,
      ref: C,
      ...J(In.required, $)
    }, !i))
      return E($), T;
  }
  if (!M && (!ei(d) || !ei(h))) {
    let B, $;
    const W = or(h), Y = or(d);
    if (!ei(k) && !isNaN(k)) {
      const ye = o.valueAsNumber || k && +k;
      ei(W.value) || (B = ye > W.value), ei(Y.value) || ($ = ye < Y.value);
    } else {
      const ye = o.valueAsDate || new Date(k), Pe = (R) => /* @__PURE__ */ new Date((/* @__PURE__ */ new Date()).toDateString() + " " + R), Be = o.type == "time", te = o.type == "week";
      gn(W.value) && k && (B = Be ? Pe(k) > Pe(W.value) : te ? k > W.value : ye > new Date(W.value)), gn(Y.value) && k && ($ = Be ? Pe(k) < Pe(Y.value) : te ? k < Y.value : ye < new Date(Y.value));
    }
    if ((B || $) && (D(!!B, W.message, Y.message, In.max, In.min), !i))
      return E(T[p].message), T;
  }
  if ((c || u) && !M && (gn(k) || r && Array.isArray(k))) {
    const B = or(c), $ = or(u), W = !ei(B.value) && k.length > +B.value, Y = !ei($.value) && k.length < +$.value;
    if ((W || Y) && (D(W, B.message, $.message), !i))
      return E(T[p].message), T;
  }
  if (m && !M && gn(k)) {
    const { value: B, message: $ } = or(m);
    if (jl(B) && !k.match(B) && (T[p] = {
      type: In.pattern,
      message: $,
      ref: o,
      ...J(In.pattern, $)
    }, !i))
      return E($), T;
  }
  if (g) {
    if (fn(g)) {
      const B = await g(k, e), $ = jm(B, C);
      if ($ && (T[p] = {
        ...$,
        ...J(In.validate, $.message)
      }, !i))
        return E($.message), T;
    } else if (xt(g)) {
      let B = {};
      for (const $ in g) {
        if (!ui(B) && !i)
          break;
        const W = jm(await g[$](k, e), C, $);
        W && (B = {
          ...W,
          ...J($, W.message)
        }, E(W.message), i && (T[p] = B));
      }
      if (!ui(B) && (T[p] = {
        ref: C,
        ...B
      }, !i))
        return T;
    }
  }
  return E(!0), T;
};
function Sx(n, t) {
  const e = t.slice(0, -1).length;
  let i = 0;
  for (; i < e; )
    n = It(n) ? i++ : n[t[i++]];
  return n;
}
function Ax(n) {
  for (const t in n)
    if (n.hasOwnProperty(t) && !It(n[t]))
      return !1;
  return !0;
}
function Vt(n, t) {
  const e = Array.isArray(t) ? t : Xh(t) ? [t] : m0(t), i = e.length === 1 ? n : Sx(n, e), s = e.length - 1, r = e[s];
  return i && delete i[r], s !== 0 && (xt(i) && ui(i) || Array.isArray(i) && Ax(i)) && Vt(n, e.slice(0, -1)), n;
}
var mu = () => {
  let n = [];
  return {
    get observers() {
      return n;
    },
    next: (s) => {
      for (const r of n)
        r.next && r.next(s);
    },
    subscribe: (s) => (n.push(s), {
      unsubscribe: () => {
        n = n.filter((r) => r !== s);
      }
    }),
    unsubscribe: () => {
      n = [];
    }
  };
}, Fd = (n) => ei(n) || !f0(n);
function ls(n, t) {
  if (Fd(n) || Fd(t))
    return n === t;
  if (Is(n) && Is(t))
    return n.getTime() === t.getTime();
  const e = Object.keys(n), i = Object.keys(t);
  if (e.length !== i.length)
    return !1;
  for (const s of e) {
    const r = n[s];
    if (!i.includes(s))
      return !1;
    if (s !== "ref") {
      const o = t[s];
      if (Is(r) && Is(o) || xt(r) && xt(o) || Array.isArray(r) && Array.isArray(o) ? !ls(r, o) : r !== o)
        return !1;
    }
  }
  return !0;
}
var b0 = (n) => n.type === "select-multiple", Ix = (n) => ef(n) || xa(n), gu = (n) => Hl(n) && n.isConnected, w0 = (n) => {
  for (const t in n)
    if (fn(n[t]))
      return !0;
  return !1;
};
function Ul(n, t = {}) {
  const e = Array.isArray(n);
  if (xt(n) || e)
    for (const i in n)
      Array.isArray(n[i]) || xt(n[i]) && !w0(n[i]) ? (t[i] = Array.isArray(n[i]) ? [] : {}, Ul(n[i], t[i])) : ei(n[i]) || (t[i] = !0);
  return t;
}
function v0(n, t, e) {
  const i = Array.isArray(n);
  if (xt(n) || i)
    for (const s in n)
      Array.isArray(n[s]) || xt(n[s]) && !w0(n[s]) ? It(t) || Fd(e[s]) ? e[s] = Array.isArray(n[s]) ? Ul(n[s], []) : { ...Ul(n[s]) } : v0(n[s], ei(t) ? {} : t[s], e[s]) : e[s] = !ls(n[s], t[s]);
  return e;
}
var lo = (n, t) => v0(n, t, Ul(t)), _0 = (n, { valueAsNumber: t, valueAsDate: e, setValueAs: i }) => It(n) ? n : t ? n === "" ? NaN : n && +n : e && gn(n) ? new Date(n) : i ? i(n) : n;
function pu(n) {
  const t = n.ref;
  return Qh(t) ? t.files : ef(t) ? p0(n.refs).value : b0(t) ? [...t.selectedOptions].map(({ value: e }) => e) : xa(t) ? g0(n.refs).value : _0(It(t.value) ? n.ref.value : t.value, n);
}
var Px = (n, t, e, i) => {
  const s = {};
  for (const r of n) {
    const o = ge(t, r);
    o && ut(s, r, o._f);
  }
  return {
    criteriaMode: e,
    names: [...n],
    fields: s,
    shouldUseNativeValidation: i
  };
}, co = (n) => It(n) ? n : jl(n) ? n.source : xt(n) ? jl(n.value) ? n.value.source : n.value : n;
const Wm = "AsyncFunction";
var Rx = (n) => !!n && !!n.validate && !!(fn(n.validate) && n.validate.constructor.name === Wm || xt(n.validate) && Object.values(n.validate).find((t) => t.constructor.name === Wm)), Ox = (n) => n.mount && (n.required || n.min || n.max || n.maxLength || n.minLength || n.pattern || n.validate);
function qm(n, t, e) {
  const i = ge(n, e);
  if (i || Xh(e))
    return {
      error: i,
      name: e
    };
  const s = e.split(".");
  for (; s.length; ) {
    const r = s.join("."), o = ge(t, r), a = ge(n, r);
    if (o && !Array.isArray(o) && e !== r)
      return { name: e };
    if (a && a.type)
      return {
        name: r,
        error: a
      };
    s.pop();
  }
  return {
    name: e
  };
}
var Vx = (n, t, e, i, s) => s.isOnAll ? !1 : !e && s.isOnTouch ? !(t || n) : (e ? i.isOnBlur : s.isOnBlur) ? !n : (e ? i.isOnChange : s.isOnChange) ? n : !0, Lx = (n, t) => !Vc(ge(n, t)).length && Vt(n, t);
const Bx = {
  mode: Wi.onSubmit,
  reValidateMode: Wi.onChange,
  shouldFocusError: !0
};
function Mx(n = {}) {
  let t = {
    ...Bx,
    ...n
  }, e = {
    submitCount: 0,
    isDirty: !1,
    isLoading: fn(t.defaultValues),
    isValidating: !1,
    isSubmitted: !1,
    isSubmitting: !1,
    isSubmitSuccessful: !1,
    isValid: !1,
    touchedFields: {},
    dirtyFields: {},
    validatingFields: {},
    errors: t.errors || {},
    disabled: t.disabled || !1
  }, i = {}, s = xt(t.defaultValues) || xt(t.values) ? Vi(t.defaultValues || t.values) || {} : {}, r = t.shouldUnregister ? {} : Vi(s), o = {
    action: !1,
    mount: !1,
    watch: !1
  }, a = {
    mount: /* @__PURE__ */ new Set(),
    disabled: /* @__PURE__ */ new Set(),
    unMount: /* @__PURE__ */ new Set(),
    array: /* @__PURE__ */ new Set(),
    watch: /* @__PURE__ */ new Set()
  }, l, c = 0;
  const u = {
    isDirty: !1,
    dirtyFields: !1,
    validatingFields: !1,
    touchedFields: !1,
    isValidating: !1,
    isValid: !1,
    errors: !1
  }, d = {
    values: mu(),
    array: mu(),
    state: mu()
  }, h = Fm(t.mode), m = Fm(t.reValidateMode), g = t.criteriaMode === Wi.all, p = (b) => (x) => {
    clearTimeout(c), c = setTimeout(b, x);
  }, w = async (b) => {
    if (!t.disabled && (u.isValid || b)) {
      const x = t.resolver ? ui((await N()).errors) : await J(i, !0);
      x !== e.isValid && d.state.next({
        isValid: x
      });
    }
  }, y = (b, x) => {
    !t.disabled && (u.isValidating || u.validatingFields) && ((b || Array.from(a.mount)).forEach((A) => {
      A && (x ? ut(e.validatingFields, A, x) : Vt(e.validatingFields, A));
    }), d.state.next({
      validatingFields: e.validatingFields,
      isValidating: !ui(e.validatingFields)
    }));
  }, k = (b, x = [], A, z, j = !0, H = !0) => {
    if (z && A && !t.disabled) {
      if (o.action = !0, H && Array.isArray(ge(i, b))) {
        const ne = A(ge(i, b), z.argA, z.argB);
        j && ut(i, b, ne);
      }
      if (H && Array.isArray(ge(e.errors, b))) {
        const ne = A(ge(e.errors, b), z.argA, z.argB);
        j && ut(e.errors, b, ne), Lx(e.errors, b);
      }
      if (u.touchedFields && H && Array.isArray(ge(e.touchedFields, b))) {
        const ne = A(ge(e.touchedFields, b), z.argA, z.argB);
        j && ut(e.touchedFields, b, ne);
      }
      u.dirtyFields && (e.dirtyFields = lo(s, r)), d.state.next({
        name: b,
        isDirty: B(b, x),
        dirtyFields: e.dirtyFields,
        errors: e.errors,
        isValid: e.isValid
      });
    } else
      ut(r, b, x);
  }, C = (b, x) => {
    ut(e.errors, b, x), d.state.next({
      errors: e.errors
    });
  }, E = (b) => {
    e.errors = b, d.state.next({
      errors: e.errors,
      isValid: !1
    });
  }, T = (b, x, A, z) => {
    const j = ge(i, b);
    if (j) {
      const H = ge(r, b, It(A) ? ge(s, b) : A);
      It(H) || z && z.defaultChecked || x ? ut(r, b, x ? H : pu(j._f)) : Y(b, H), o.mount && w();
    }
  }, L = (b, x, A, z, j) => {
    let H = !1, ne = !1;
    const me = {
      name: b
    };
    if (!t.disabled) {
      const Oe = !!(ge(i, b) && ge(i, b)._f && ge(i, b)._f.disabled);
      if (!A || z) {
        u.isDirty && (ne = e.isDirty, e.isDirty = me.isDirty = B(), H = ne !== me.isDirty);
        const Le = Oe || ls(ge(s, b), x);
        ne = !!(!Oe && ge(e.dirtyFields, b)), Le || Oe ? Vt(e.dirtyFields, b) : ut(e.dirtyFields, b, !0), me.dirtyFields = e.dirtyFields, H = H || u.dirtyFields && ne !== !Le;
      }
      if (A) {
        const Le = ge(e.touchedFields, b);
        Le || (ut(e.touchedFields, b, A), me.touchedFields = e.touchedFields, H = H || u.touchedFields && Le !== A);
      }
      H && j && d.state.next(me);
    }
    return H ? me : {};
  }, O = (b, x, A, z) => {
    const j = ge(e.errors, b), H = u.isValid && un(x) && e.isValid !== x;
    if (t.delayError && A ? (l = p(() => C(b, A)), l(t.delayError)) : (clearTimeout(c), l = null, A ? ut(e.errors, b, A) : Vt(e.errors, b)), (A ? !ls(j, A) : j) || !ui(z) || H) {
      const ne = {
        ...z,
        ...H && un(x) ? { isValid: x } : {},
        errors: e.errors,
        name: b
      };
      e = {
        ...e,
        ...ne
      }, d.state.next(ne);
    }
  }, N = async (b) => {
    y(b, !0);
    const x = await t.resolver(r, t.context, Px(b || a.mount, i, t.criteriaMode, t.shouldUseNativeValidation));
    return y(b), x;
  }, M = async (b) => {
    const { errors: x } = await N(b);
    if (b)
      for (const A of b) {
        const z = ge(x, A);
        z ? ut(e.errors, A, z) : Vt(e.errors, A);
      }
    else
      e.errors = x;
    return x;
  }, J = async (b, x, A = {
    valid: !0
  }) => {
    for (const z in b) {
      const j = b[z];
      if (j) {
        const { _f: H, ...ne } = j;
        if (H) {
          const me = a.array.has(H.name), Oe = j._f && Rx(j._f);
          Oe && u.validatingFields && y([z], !0);
          const Le = await Um(j, a.disabled, r, g, t.shouldUseNativeValidation && !x, me);
          if (Oe && u.validatingFields && y([z]), Le[H.name] && (A.valid = !1, x))
            break;
          !x && (ge(Le, H.name) ? me ? Ex(e.errors, Le, H.name) : ut(e.errors, H.name, Le[H.name]) : Vt(e.errors, H.name));
        }
        !ui(ne) && await J(ne, x, A);
      }
    }
    return A.valid;
  }, D = () => {
    for (const b of a.unMount) {
      const x = ge(i, b);
      x && (x._f.refs ? x._f.refs.every((A) => !gu(A)) : !gu(x._f.ref)) && pe(b);
    }
    a.unMount = /* @__PURE__ */ new Set();
  }, B = (b, x) => !t.disabled && (b && x && ut(r, b, x), !ls(K(), s)), $ = (b, x, A) => xx(b, a, {
    ...o.mount ? r : It(x) ? s : gn(b) ? { [b]: x } : x
  }, A, x), W = (b) => Vc(ge(o.mount ? r : s, b, t.shouldUnregister ? ge(s, b, []) : [])), Y = (b, x, A = {}) => {
    const z = ge(i, b);
    let j = x;
    if (z) {
      const H = z._f;
      H && (!H.disabled && ut(r, b, _0(x, H)), j = Hl(H.ref) && ei(x) ? "" : x, b0(H.ref) ? [...H.ref.options].forEach((ne) => ne.selected = j.includes(ne.value)) : H.refs ? xa(H.ref) ? H.refs.length > 1 ? H.refs.forEach((ne) => (!ne.defaultChecked || !ne.disabled) && (ne.checked = Array.isArray(j) ? !!j.find((me) => me === ne.value) : j === ne.value)) : H.refs[0] && (H.refs[0].checked = !!j) : H.refs.forEach((ne) => ne.checked = ne.value === j) : Qh(H.ref) ? H.ref.value = "" : (H.ref.value = j, H.ref.type || d.values.next({
        name: b,
        values: { ...r }
      })));
    }
    (A.shouldDirty || A.shouldTouch) && L(b, j, A.shouldTouch, A.shouldDirty, !0), A.shouldValidate && R(b);
  }, ye = (b, x, A) => {
    for (const z in x) {
      const j = x[z], H = `${b}.${z}`, ne = ge(i, H);
      (a.array.has(b) || xt(j) || ne && !ne._f) && !Is(j) ? ye(H, j, A) : Y(H, j, A);
    }
  }, Pe = (b, x, A = {}) => {
    const z = ge(i, b), j = a.array.has(b), H = Vi(x);
    ut(r, b, H), j ? (d.array.next({
      name: b,
      values: { ...r }
    }), (u.isDirty || u.dirtyFields) && A.shouldDirty && d.state.next({
      name: b,
      dirtyFields: lo(s, r),
      isDirty: B(b, H)
    })) : z && !z._f && !ei(H) ? ye(b, H, A) : Y(b, H, A), Dm(b, a) && d.state.next({ ...e }), d.values.next({
      name: o.mount ? b : void 0,
      values: { ...r }
    });
  }, Be = async (b) => {
    o.mount = !0;
    const x = b.target;
    let A = x.name, z = !0;
    const j = ge(i, A), H = () => x.type ? pu(j._f) : bx(b), ne = (me) => {
      z = Number.isNaN(me) || Is(me) && isNaN(me.getTime()) || ls(me, ge(r, A, me));
    };
    if (j) {
      let me, Oe;
      const Le = H(), Rt = b.type === Nm.BLUR || b.type === Nm.FOCUS_OUT, Ot = !Ox(j._f) && !t.resolver && !ge(e.errors, A) && !j._f.deps || Vx(Rt, ge(e.touchedFields, A), e.isSubmitted, m, h), An = Dm(A, a, Rt);
      ut(r, A, Le), Rt ? (j._f.onBlur && j._f.onBlur(b), l && l(0)) : j._f.onChange && j._f.onChange(b);
      const ws = L(A, Le, Rt, !1), lu = !ui(ws) || An;
      if (!Rt && d.values.next({
        name: A,
        type: b.type,
        values: { ...r }
      }), Ot)
        return u.isValid && (t.mode === "onBlur" && Rt ? w() : Rt || w()), lu && d.state.next({ name: A, ...An ? {} : ws });
      if (!Rt && An && d.state.next({ ...e }), t.resolver) {
        const { errors: qa } = await N([A]);
        if (ne(Le), z) {
          const cu = qm(e.errors, i, A), Ga = qm(qa, i, cu.name || A);
          me = Ga.error, A = Ga.name, Oe = ui(qa);
        }
      } else
        y([A], !0), me = (await Um(j, a.disabled, r, g, t.shouldUseNativeValidation))[A], y([A]), ne(Le), z && (me ? Oe = !1 : u.isValid && (Oe = await J(i, !0)));
      z && (j._f.deps && R(j._f.deps), O(A, Oe, me, ws));
    }
  }, te = (b, x) => {
    if (ge(e.errors, x) && b.focus)
      return b.focus(), 1;
  }, R = async (b, x = {}) => {
    let A, z;
    const j = Al(b);
    if (t.resolver) {
      const H = await M(It(b) ? b : j);
      A = ui(H), z = b ? !j.some((ne) => ge(H, ne)) : A;
    } else b ? (z = (await Promise.all(j.map(async (H) => {
      const ne = ge(i, H);
      return await J(ne && ne._f ? { [H]: ne } : ne);
    }))).every(Boolean), !(!z && !e.isValid) && w()) : z = A = await J(i);
    return d.state.next({
      ...!gn(b) || u.isValid && A !== e.isValid ? {} : { name: b },
      ...t.resolver || !b ? { isValid: A } : {},
      errors: e.errors
    }), x.shouldFocus && !z && Mo(i, te, b ? j : a.mount), z;
  }, K = (b) => {
    const x = {
      ...o.mount ? r : s
    };
    return It(b) ? x : gn(b) ? ge(x, b) : b.map((A) => ge(x, A));
  }, X = (b, x) => ({
    invalid: !!ge((x || e).errors, b),
    isDirty: !!ge((x || e).dirtyFields, b),
    error: ge((x || e).errors, b),
    isValidating: !!ge(e.validatingFields, b),
    isTouched: !!ge((x || e).touchedFields, b)
  }), ie = (b) => {
    b && Al(b).forEach((x) => Vt(e.errors, x)), d.state.next({
      errors: b ? e.errors : {}
    });
  }, oe = (b, x, A) => {
    const z = (ge(i, b, { _f: {} })._f || {}).ref, j = ge(e.errors, b) || {}, { ref: H, message: ne, type: me, ...Oe } = j;
    ut(e.errors, b, {
      ...Oe,
      ...x,
      ref: z
    }), d.state.next({
      name: b,
      errors: e.errors,
      isValid: !1
    }), A && A.shouldFocus && z && z.focus && z.focus();
  }, de = (b, x) => fn(b) ? d.values.subscribe({
    next: (A) => b($(void 0, x), A)
  }) : $(b, x, !0), pe = (b, x = {}) => {
    for (const A of b ? Al(b) : a.mount)
      a.mount.delete(A), a.array.delete(A), x.keepValue || (Vt(i, A), Vt(r, A)), !x.keepError && Vt(e.errors, A), !x.keepDirty && Vt(e.dirtyFields, A), !x.keepTouched && Vt(e.touchedFields, A), !x.keepIsValidating && Vt(e.validatingFields, A), !t.shouldUnregister && !x.keepDefaultValue && Vt(s, A);
    d.values.next({
      values: { ...r }
    }), d.state.next({
      ...e,
      ...x.keepDirty ? { isDirty: B() } : {}
    }), !x.keepIsValid && w();
  }, ve = ({ disabled: b, name: x, field: A, fields: z }) => {
    (un(b) && o.mount || b || a.disabled.has(x)) && (b ? a.disabled.add(x) : a.disabled.delete(x), L(x, pu(A ? A._f : ge(z, x)._f), !1, !1, !0));
  }, _e = (b, x = {}) => {
    let A = ge(i, b);
    const z = un(x.disabled) || un(t.disabled);
    return ut(i, b, {
      ...A || {},
      _f: {
        ...A && A._f ? A._f : { ref: { name: b } },
        name: b,
        mount: !0,
        ...x
      }
    }), a.mount.add(b), A ? ve({
      field: A,
      disabled: un(x.disabled) ? x.disabled : t.disabled,
      name: b
    }) : T(b, !0, x.value), {
      ...z ? { disabled: x.disabled || t.disabled } : {},
      ...t.progressive ? {
        required: !!x.required,
        min: co(x.min),
        max: co(x.max),
        minLength: co(x.minLength),
        maxLength: co(x.maxLength),
        pattern: co(x.pattern)
      } : {},
      name: b,
      onChange: Be,
      onBlur: Be,
      ref: (j) => {
        if (j) {
          _e(b, x), A = ge(i, b);
          const H = It(j.value) && j.querySelectorAll && j.querySelectorAll("input,select,textarea")[0] || j, ne = Ix(H), me = A._f.refs || [];
          if (ne ? me.find((Oe) => Oe === H) : H === A._f.ref)
            return;
          ut(i, b, {
            _f: {
              ...A._f,
              ...ne ? {
                refs: [
                  ...me.filter(gu),
                  H,
                  ...Array.isArray(ge(s, b)) ? [{}] : []
                ],
                ref: { type: H.type, name: b }
              } : { ref: H }
            }
          }), T(b, !1, void 0, H);
        } else
          A = ge(i, b, {}), A._f && (A._f.mount = !1), (t.shouldUnregister || x.shouldUnregister) && !(vx(a.array, b) && o.action) && a.unMount.add(b);
      }
    };
  }, Ee = () => t.shouldFocusError && Mo(i, te, a.mount), we = (b) => {
    un(b) && (d.state.next({ disabled: b }), Mo(i, (x, A) => {
      const z = ge(i, A);
      z && (x.disabled = z._f.disabled || b, Array.isArray(z._f.refs) && z._f.refs.forEach((j) => {
        j.disabled = z._f.disabled || b;
      }));
    }, 0, !1));
  }, Ne = (b, x) => async (A) => {
    let z;
    A && (A.preventDefault && A.preventDefault(), A.persist && A.persist());
    let j = Vi(r);
    if (a.disabled.size)
      for (const H of a.disabled)
        ut(j, H, void 0);
    if (d.state.next({
      isSubmitting: !0
    }), t.resolver) {
      const { errors: H, values: ne } = await N();
      e.errors = H, j = ne;
    } else
      await J(i);
    if (Vt(e.errors, "root"), ui(e.errors)) {
      d.state.next({
        errors: {}
      });
      try {
        await b(j, A);
      } catch (H) {
        z = H;
      }
    } else
      x && await x({ ...e.errors }, A), Ee(), setTimeout(Ee);
    if (d.state.next({
      isSubmitted: !0,
      isSubmitting: !1,
      isSubmitSuccessful: ui(e.errors) && !z,
      submitCount: e.submitCount + 1,
      errors: e.errors
    }), z)
      throw z;
  }, se = (b, x = {}) => {
    ge(i, b) && (It(x.defaultValue) ? Pe(b, Vi(ge(s, b))) : (Pe(b, x.defaultValue), ut(s, b, Vi(x.defaultValue))), x.keepTouched || Vt(e.touchedFields, b), x.keepDirty || (Vt(e.dirtyFields, b), e.isDirty = x.defaultValue ? B(b, Vi(ge(s, b))) : B()), x.keepError || (Vt(e.errors, b), u.isValid && w()), d.state.next({ ...e }));
  }, Qe = (b, x = {}) => {
    const A = b ? Vi(b) : s, z = Vi(A), j = ui(b), H = j ? s : z;
    if (x.keepDefaultValues || (s = A), !x.keepValues) {
      if (x.keepDirtyValues) {
        const ne = /* @__PURE__ */ new Set([
          ...a.mount,
          ...Object.keys(lo(s, r))
        ]);
        for (const me of Array.from(ne))
          ge(e.dirtyFields, me) ? ut(H, me, ge(r, me)) : Pe(me, ge(H, me));
      } else {
        if (Jh && It(b))
          for (const ne of a.mount) {
            const me = ge(i, ne);
            if (me && me._f) {
              const Oe = Array.isArray(me._f.refs) ? me._f.refs[0] : me._f.ref;
              if (Hl(Oe)) {
                const Le = Oe.closest("form");
                if (Le) {
                  Le.reset();
                  break;
                }
              }
            }
          }
        i = {};
      }
      r = t.shouldUnregister ? x.keepDefaultValues ? Vi(s) : {} : Vi(H), d.array.next({
        values: { ...H }
      }), d.values.next({
        values: { ...H }
      });
    }
    a = {
      mount: x.keepDirtyValues ? a.mount : /* @__PURE__ */ new Set(),
      unMount: /* @__PURE__ */ new Set(),
      array: /* @__PURE__ */ new Set(),
      disabled: /* @__PURE__ */ new Set(),
      watch: /* @__PURE__ */ new Set(),
      watchAll: !1,
      focus: ""
    }, o.mount = !u.isValid || !!x.keepIsValid || !!x.keepDirtyValues, o.watch = !!t.shouldUnregister, d.state.next({
      submitCount: x.keepSubmitCount ? e.submitCount : 0,
      isDirty: j ? !1 : x.keepDirty ? e.isDirty : !!(x.keepDefaultValues && !ls(b, s)),
      isSubmitted: x.keepIsSubmitted ? e.isSubmitted : !1,
      dirtyFields: j ? {} : x.keepDirtyValues ? x.keepDefaultValues && r ? lo(s, r) : e.dirtyFields : x.keepDefaultValues && b ? lo(s, b) : x.keepDirty ? e.dirtyFields : {},
      touchedFields: x.keepTouched ? e.touchedFields : {},
      errors: x.keepErrors ? e.errors : {},
      isSubmitSuccessful: x.keepIsSubmitSuccessful ? e.isSubmitSuccessful : !1,
      isSubmitting: !1
    });
  }, q = (b, x) => Qe(fn(b) ? b(r) : b, x);
  return {
    control: {
      register: _e,
      unregister: pe,
      getFieldState: X,
      handleSubmit: Ne,
      setError: oe,
      _executeSchema: N,
      _getWatch: $,
      _getDirty: B,
      _updateValid: w,
      _removeUnmounted: D,
      _updateFieldArray: k,
      _updateDisabledField: ve,
      _getFieldArray: W,
      _reset: Qe,
      _resetDefaultValues: () => fn(t.defaultValues) && t.defaultValues().then((b) => {
        q(b, t.resetOptions), d.state.next({
          isLoading: !1
        });
      }),
      _updateFormState: (b) => {
        e = {
          ...e,
          ...b
        };
      },
      _disableForm: we,
      _subjects: d,
      _proxyFormState: u,
      _setErrors: E,
      get _fields() {
        return i;
      },
      get _formValues() {
        return r;
      },
      get _state() {
        return o;
      },
      set _state(b) {
        o = b;
      },
      get _defaultValues() {
        return s;
      },
      get _names() {
        return a;
      },
      set _names(b) {
        a = b;
      },
      get _formState() {
        return e;
      },
      set _formState(b) {
        e = b;
      },
      get _options() {
        return t;
      },
      set _options(b) {
        t = {
          ...t,
          ...b
        };
      }
    },
    trigger: R,
    register: _e,
    handleSubmit: Ne,
    watch: de,
    setValue: Pe,
    getValues: K,
    reset: q,
    resetField: se,
    clearErrors: ie,
    unregister: pe,
    setError: oe,
    setFocus: (b, x = {}) => {
      const A = ge(i, b), z = A && A._f;
      if (z) {
        const j = z.refs ? z.refs[0] : z.ref;
        j.focus && (j.focus(), x.shouldSelect && fn(j.select) && j.select());
      }
    },
    getFieldState: X
  };
}
function y0(n = {}) {
  const t = ce.useRef(void 0), e = ce.useRef(void 0), [i, s] = ce.useState({
    isDirty: !1,
    isValidating: !1,
    isLoading: fn(n.defaultValues),
    isSubmitted: !1,
    isSubmitting: !1,
    isSubmitSuccessful: !1,
    isValid: !1,
    submitCount: 0,
    dirtyFields: {},
    touchedFields: {},
    validatingFields: {},
    errors: n.errors || {},
    disabled: n.disabled || !1,
    defaultValues: fn(n.defaultValues) ? void 0 : n.defaultValues
  });
  t.current || (t.current = {
    ...Mx(n),
    formState: i
  });
  const r = t.current.control;
  return r._options = n, Cx({
    subject: r._subjects.state,
    next: (o) => {
      kx(o, r._proxyFormState, r._updateFormState) && s({ ...r._formState });
    }
  }), ce.useEffect(() => r._disableForm(n.disabled), [r, n.disabled]), ce.useEffect(() => {
    if (r._proxyFormState.isDirty) {
      const o = r._getDirty();
      o !== i.isDirty && r._subjects.state.next({
        isDirty: o
      });
    }
  }, [r, i.isDirty]), ce.useEffect(() => {
    n.values && !ls(n.values, e.current) ? (r._reset(n.values, r._options.resetOptions), e.current = n.values, s((o) => ({ ...o }))) : r._resetDefaultValues();
  }, [n.values, r]), ce.useEffect(() => {
    n.errors && r._setErrors(n.errors);
  }, [n.errors, r]), ce.useEffect(() => {
    r._state.mount || (r._updateValid(), r._state.mount = !0), r._state.watch && (r._state.watch = !1, r._subjects.state.next({ ...r._formState })), r._removeUnmounted();
  }), ce.useEffect(() => {
    n.shouldUnregister && r._subjects.values.next({
      values: r._getWatch()
    });
  }, [n.shouldUnregister, r]), t.current.formState = yx(i, r), t.current;
}
const Nx = 864e5, Gm = Symbol.for("constructDateFrom");
function k0(n, t) {
  return typeof n == "function" ? n(t) : n && typeof n == "object" && Gm in n ? n[Gm](t) : n instanceof Date ? new n.constructor(t) : new Date(t);
}
function C0(n, t) {
  return k0(n, n);
}
function Km(n) {
  const t = C0(n), e = new Date(
    Date.UTC(
      t.getFullYear(),
      t.getMonth(),
      t.getDate(),
      t.getHours(),
      t.getMinutes(),
      t.getSeconds(),
      t.getMilliseconds()
    )
  );
  return e.setUTCFullYear(t.getFullYear()), +n - +e;
}
function Fx(n, ...t) {
  const e = k0.bind(
    null,
    t.find((i) => typeof i == "object")
  );
  return t.map(e);
}
function Zm(n, t) {
  const e = C0(n);
  return e.setHours(0, 0, 0, 0), e;
}
function Zi(n, t, e) {
  const [i, s] = Fx(
    e == null ? void 0 : e.in,
    n,
    t
  ), r = Zm(i), o = Zm(s), a = +r - Km(r), l = +o - Km(o);
  return Math.round((a - l) / Nx);
}
const Ym = {
  "text-field-container": "_text-field-container_17xun_1",
  "helper-text": "_helper-text_17xun_109"
};
class Ps extends ce.Component {
  constructor() {
    super(...arguments);
    f(this, "containerRef", vn());
    f(this, "getInput", () => {
      var e;
      return (e = this.containerRef.current) == null ? void 0 : e.querySelector("input");
    });
  }
  render() {
    var e;
    return /* @__PURE__ */ v.jsxs(
      "div",
      {
        ref: this.containerRef,
        id: this.props.id,
        className: `${Ym["text-field-container"]} row ${this.props.className ?? "body-3"} ${(e = this.props.helperText) != null && e.length ? Ym["helper-text"] : ""}`,
        "helper-text": this.props.helperText,
        style: this.props.style ? { "--helper-text-color": this.props.helperTextColor ?? "#e14337", ...this.props.style } : { "--helper-text-color": this.props.helperTextColor ?? "#e14337" },
        children: [
          this.props.prefix,
          this.props.register ? /* @__PURE__ */ v.jsx(
            "input",
            {
              name: this.props.name,
              ...this.props.register,
              autoComplete: this.props.autoComplete,
              autoFocus: this.props.autoFocus,
              maxLength: this.props.maxLength,
              type: this.props.type ?? "text",
              placeholder: this.props.placeholder,
              readOnly: this.props.readOnly,
              disabled: this.props.disabled,
              onFocus: this.props.onFocus,
              onKeyDown: this.props.onComplete ? (i) => {
                if (this.props.onComplete)
                  switch (i.key.toLowerCase()) {
                    case "enter":
                      this.props.onComplete(i);
                      break;
                  }
              } : void 0
            }
          ) : /* @__PURE__ */ v.jsx(
            "input",
            {
              autoComplete: this.props.autoComplete,
              autoFocus: this.props.autoFocus,
              maxLength: this.props.maxLength,
              name: this.props.name,
              type: this.props.type ?? "text",
              defaultValue: this.props.defaultValue,
              value: this.props.value,
              placeholder: this.props.placeholder,
              readOnly: this.props.readOnly,
              disabled: this.props.disabled,
              onChange: this.props.onChange,
              onFocus: this.props.onFocus,
              onBlur: this.props.onBlur,
              onKeyDown: this.props.onComplete ? (i) => {
                if (this.props.onComplete)
                  switch (i.key.toLowerCase()) {
                    case "enter":
                      this.props.onComplete(i);
                      break;
                  }
              } : void 0
            }
          ),
          this.props.suffix
        ]
      }
    );
  }
}
const Dx = "_selected_1hxld_101", $x = "_today_1hxld_281", zx = "_invalid_1hxld_299", ct = {
  "calendar-container": "_calendar-container_1hxld_1",
  "calendar-sidebar-options": "_calendar-sidebar-options_1hxld_17",
  "calendar-sidebar-option-buttton": "_calendar-sidebar-option-buttton_1hxld_27",
  "picker-time-container": "_picker-time-container_1hxld_37",
  "scroll-picker-minutes": "_scroll-picker-minutes_1hxld_81",
  selected: Dx,
  "picker-date-header": "_picker-date-header_1hxld_111",
  "picker-date-body": "_picker-date-body_1hxld_181",
  "date-picker-circle": "_date-picker-circle_1hxld_221",
  "month-picker-circle": "_month-picker-circle_1hxld_237",
  "year-picker-circle": "_year-picker-circle_1hxld_239",
  "in-range": "_in-range_1hxld_257",
  "end-range": "_end-range_1hxld_265",
  "start-range": "_start-range_1hxld_265",
  today: $x,
  invalid: zx
}, ti = /* @__PURE__ */ new Date(), is = new Date(
  ti.getFullYear() - 100,
  ti.getMonth(),
  ti.getDate()
), ar = new Date(
  ti.getFullYear() + 100,
  ti.getMonth(),
  ti.getDate()
), Za = (n, t, e) => Zi(n, t) > -1 && Zi(e, n) > -1, Jm = (n, t, e, i) => {
  let s;
  e ? i ? (e instanceof Date ? s = { sTime: e, eTime: e } : s = e, s.sTime.getTime() < n.getTime() && (s.sTime = n), s.eTime.getTime() > t.getTime() && (s.eTime = t)) : (e instanceof Date ? s = e : s = e.sTime, s.getTime() < n.getTime() && (s = n), s.getTime() > t.getTime() && (s = t)) : s = i ? { sTime: ti, eTime: ti } : ti;
  const r = s instanceof Date ? s.getMonth() : s.sTime.getMonth(), o = s instanceof Date ? s.getFullYear() : s.sTime.getFullYear();
  return {
    value: e ? s : void 0,
    selectMonth: r,
    selectYear: o,
    tab: 0
    /* DATE */
  };
};
class Hx extends ce.Component {
  constructor(e) {
    super(e);
    f(this, "minDate");
    f(this, "maxDate");
    this.minDate = !this.props.min || this.props.min.getTime() < is.getTime() ? is : this.props.min, this.maxDate = !this.props.max || this.props.max.getTime() > ar.getTime() ? ar : this.props.max, this.state = Jm(this.minDate, this.maxDate, this.props.value, this.props.range), this.showDateInMonth = this.showDateInMonth.bind(this), this.showMonthInYear = this.showMonthInYear.bind(this), this.showYearInRange = this.showYearInRange.bind(this), this.getTitle = this.getTitle.bind(this);
  }
  componentDidUpdate(e) {
    e.value !== this.props.value && this.setState(Jm(this.minDate, this.maxDate, this.props.value, this.props.range));
  }
  showDateInMonth() {
    let e = new Date(this.state.selectYear, this.state.selectMonth, 1);
    return /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
      Array.from({ length: 7 }).map((i, s) => {
        switch (s) {
          case 0:
            var r = this.props.t("su");
            break;
          case 1:
            r = this.props.t("mo");
            break;
          case 2:
            r = this.props.t("tu");
            break;
          case 3:
            r = this.props.t("we");
            break;
          case 4:
            r = this.props.t("th");
            break;
          case 5:
            r = this.props.t("fr");
            break;
          case 6:
            r = this.props.t("sa");
            break;
          default:
            r = "";
            break;
        }
        return /* @__PURE__ */ v.jsx("div", { className: `${ct["date-picker-circle"]} date-picker-circle`, children: /* @__PURE__ */ v.jsx("span", { className: "label-4 row", children: r }) }, "dtwk-" + s);
      }),
      Array.from({ length: 42 }).map((i, s) => {
        var u, d, h;
        let r = s % 7 + Math.floor(s / 7) * 7 - e.getDay();
        const o = new Date(this.state.selectYear, this.state.selectMonth, r + 1);
        let a = `${ct["date-picker-circle"]} date-picker-circle`, l = "body-3";
        r + 1 === ti.getDate() && this.state.selectMonth === ti.getMonth() && this.state.selectYear === ti.getFullYear() && (a += ` ${ct.today}`);
        let c;
        return Za(o, this.minDate, this.maxDate) ? this.state.value instanceof Date ? this.state.value.getTime() === o.getTime() && (a += ` ${ct.selected}`) : ((u = this.state.value) == null ? void 0 : u.sTime.getDate()) === o.getDate() && Math.abs(Zi(o, this.state.value.sTime)) < 1 || ((d = this.state.value) == null ? void 0 : d.eTime.getDate()) === o.getDate() && Math.abs(Zi(o, this.state.value.eTime)) < 1 ? a += ` ${ct.selected} ${ct[`${((h = this.state.value) == null ? void 0 : h.sTime.getDate()) === o.getDate() && Math.abs(Zi(o, this.state.value.sTime)) < 1 ? "start" : "end"}-range`]}` : this.state.value && Za(o, this.state.value.sTime, this.state.value.eTime) && (a += ` ${ct["in-range"]}`) : a += ` ${ct.invalid}`, o.getMonth() !== this.state.selectMonth && (l = "placeholder-2"), /* @__PURE__ */ v.jsx("div", { className: a, style: c, children: /* @__PURE__ */ v.jsx(
          "button",
          {
            type: "button",
            className: `${l} row`,
            onClick: () => {
              const m = this.state.value;
              if (this.props.range) {
                const g = !m || o.getTime() < m.sTime.getTime() ? { sTime: o, eTime: o } : { sTime: m.sTime, eTime: o };
                this.setState({ ...this.state, value: g }), this.props.onSelect && this.props.onSelect(g);
              } else
                this.setState({ ...this.state, value: o }), this.props.onSelect && this.props.onSelect(o);
            },
            children: o.getDate()
          }
        ) }, o.toString());
      })
    ] });
  }
  showMonthInYear() {
    return /* @__PURE__ */ v.jsx(v.Fragment, { children: Array.from({ length: 12 }).map((e, i) => {
      switch (i) {
        case 0:
          var s = this.props.i18n.language === "en" ? "Jan" : this.props.t("january");
          break;
        case 1:
          s = this.props.i18n.language === "en" ? "Feb" : this.props.t("february");
          break;
        case 2:
          s = this.props.i18n.language === "en" ? "Mar" : this.props.t("march");
          break;
        case 3:
          s = this.props.i18n.language === "en" ? "Apr" : this.props.t("april");
          break;
        case 4:
          s = this.props.i18n.language === "en" ? "May" : this.props.t("may");
          break;
        case 5:
          s = this.props.i18n.language === "en" ? "Jun" : this.props.t("june");
          break;
        case 6:
          s = this.props.i18n.language === "en" ? "Jul" : this.props.t("july");
          break;
        case 7:
          s = this.props.i18n.language === "en" ? "Aug" : this.props.t("august");
          break;
        case 8:
          s = this.props.i18n.language === "en" ? "Sep" : this.props.t("september");
          break;
        case 9:
          s = this.props.i18n.language === "en" ? "Oct" : this.props.t("october");
          break;
        case 10:
          s = this.props.i18n.language === "en" ? "Nov" : this.props.t("november");
          break;
        case 11:
          s = this.props.i18n.language === "en" ? "Dec" : this.props.t("december");
          break;
        default:
          s = "";
          break;
      }
      const r = new Date(this.state.selectYear, i);
      let o = `${ct["month-picker-circle"]} month-picker-circle`;
      return this.state.selectYear === ti.getFullYear() && ti.getMonth() === i && (o += ` ${ct.today}`), Za(r, this.minDate, this.maxDate) ? this.state.value instanceof Date ? this.state.selectYear === this.state.value.getFullYear() && i === this.state.value.getMonth() && (o += ` ${ct.selected}`) : this.state.value && (i === this.state.value.sTime.getMonth() && this.state.value.sTime.getFullYear() === this.state.selectYear || i === this.state.value.eTime.getMonth() && this.state.value.eTime.getFullYear() === this.state.selectYear) ? o += ` ${ct.selected} ${ct[`${i === this.state.value.sTime.getMonth() && this.state.value.sTime.getFullYear() === this.state.selectYear ? "start" : "end"}-range`]}` : this.state.value && Za(r, this.state.value.sTime, this.state.value.eTime) && (o += ` ${ct["in-range"]}`) : o += ` ${ct.invalid}`, /* @__PURE__ */ v.jsx("div", { className: o, children: /* @__PURE__ */ v.jsx("button", { type: "button", className: "body-3 row", onClick: () => {
        this.setState({
          ...this.state,
          selectMonth: i,
          tab: 0
          /* DATE */
        });
      }, children: s }) }, r.toString());
    }) });
  }
  showYearInRange() {
    return Array.from({ length: 12 }).map((e, i) => {
      var a, l, c;
      let s = this.state.selectYear - (this.state.selectYear - is.getFullYear()) % 12, r = i + s, o = `${ct["year-picker-circle"]} year-picker-circle`;
      return r === ti.getFullYear() && (o += ` ${ct.today}`), r < this.minDate.getFullYear() || r > this.maxDate.getFullYear() ? o += ` ${ct.invalid}` : this.state.value instanceof Date ? r === this.state.value.getFullYear() && (o += ` ${ct.selected}`) : r === ((a = this.state.value) == null ? void 0 : a.sTime.getFullYear()) || r === ((l = this.state.value) == null ? void 0 : l.eTime.getFullYear()) ? o += ` ${ct.selected} ${ct[`${r === ((c = this.state.value) == null ? void 0 : c.sTime.getFullYear()) ? "start" : "end"}-range`]}` : this.state.value && r > this.state.value.sTime.getFullYear() && r < this.state.value.eTime.getFullYear() && (o += ` ${ct["in-range"]}`), /* @__PURE__ */ v.jsx("div", { className: o, children: /* @__PURE__ */ v.jsx("button", { type: "button", className: "body-3 row", onClick: () => {
        this.setState({ ...this.state, tab: 1, selectYear: r });
      }, children: r }) }, r.toString());
    });
  }
  getTitle() {
    switch (this.state.tab) {
      case 2:
        let i = this.state.selectYear - (this.state.selectYear - is.getFullYear()) % 12;
        return `${i}-${i + 11}`;
      case 1:
        return this.state.selectYear;
      default:
        switch (this.state.selectMonth) {
          case 0:
            var e = this.props.t("january");
            break;
          case 1:
            e = this.props.t("february");
            break;
          case 2:
            e = this.props.t("march");
            break;
          case 3:
            e = this.props.t("april");
            break;
          case 4:
            e = this.props.t("may");
            break;
          case 5:
            e = this.props.t("june");
            break;
          case 6:
            e = this.props.t("july");
            break;
          case 7:
            e = this.props.t("august");
            break;
          case 8:
            e = this.props.t("september");
            break;
          case 9:
            e = this.props.t("october");
            break;
          case 10:
            e = this.props.t("november");
            break;
          case 11:
            e = this.props.t("december");
            break;
          default:
            e = "";
            break;
        }
        return `${e}${this.props.i18n.language === "en" ? " " : "/"}${this.state.selectYear}`;
    }
  }
  render() {
    return /* @__PURE__ */ v.jsxs("div", { className: `${ct["calendar-container"]} col ${this.props.className ?? ""}`, style: this.props.style, children: [
      this.props.header,
      /* @__PURE__ */ v.jsxs("div", { className: `${ct["picker-date-header"]} row`, children: [
        /* @__PURE__ */ v.jsx(
          "button",
          {
            type: "button",
            onClick: () => {
              switch (this.state.tab) {
                case 2:
                  this.state.selectYear - 10 < is.getFullYear() ? this.setState({ ...this.state, selectYear: is.getFullYear() }) : this.setState({ ...this.state, selectYear: this.state.selectYear - 10 });
                  break;
                case 1:
                  new Date(this.state.selectYear, this.state.selectMonth - 1).getTime() >= is.getTime() && this.setState({ ...this.state, selectYear: this.state.selectYear - 1 });
                  break;
                default:
                  const i = new Date(this.state.selectYear, this.state.selectMonth - 1);
                  i.getTime() >= is.getTime() && this.setState({ ...this.state, selectMonth: i.getMonth(), selectYear: i.getFullYear() });
                  break;
              }
            },
            children: /* @__PURE__ */ v.jsx(rt, { src: "fill/arrows/left-arrow", size: "1.4rem" })
          }
        ),
        /* @__PURE__ */ v.jsx("span", { className: "heading-7", onClick: () => {
          this.state.tab !== 2 && this.setState({
            ...this.state,
            tab: this.state.tab === 0 ? 1 : 2
            /* YEAR */
          });
        }, children: this.getTitle() }),
        /* @__PURE__ */ v.jsx(
          "button",
          {
            type: "button",
            onClick: () => {
              switch (this.state.tab) {
                case 2:
                  this.state.selectYear + 10 > ar.getFullYear() ? this.setState({ ...this.state, selectYear: ar.getFullYear() }) : this.setState({ ...this.state, selectYear: this.state.selectYear + 10 });
                  break;
                case 1:
                  new Date(this.state.selectYear, this.state.selectMonth + 1).getTime() <= ar.getTime() && this.setState({ ...this.state, selectYear: this.state.selectYear + 1 });
                  break;
                default:
                  const i = new Date(this.state.selectYear, this.state.selectMonth + 1);
                  i.getTime() <= ar.getTime() && this.setState({ ...this.state, selectMonth: i.getMonth(), selectYear: i.getFullYear() });
                  break;
              }
            },
            children: /* @__PURE__ */ v.jsx(rt, { src: "fill/arrows/right-arrow", size: "1.4rem" })
          }
        )
      ] }),
      /* @__PURE__ */ v.jsx("div", { className: `${ct["picker-date-body"]} row`, children: this.state.tab === 2 ? this.showYearInRange() : this.state.tab === 1 ? this.showMonthInYear() : this.showDateInMonth() }),
      this.props.footer
    ] });
  }
}
const jx = Oc()(Hx), Ya = {
  "button-container": "_button-container_gfxps_1",
  "button-label": "_button-label_gfxps_81"
};
function mn(n) {
  const t = yt(null);
  return ht(() => {
    if (t.current)
      switch (n.type) {
        case "submit":
          let e = function(i) {
            switch (i.key.toLowerCase()) {
              case "enter":
                t.current.click();
                break;
            }
          };
          return window.addEventListener("keydown", e), () => {
            window.removeEventListener("keydown", e);
          };
      }
  }, [n.type, t.current]), n.linkTo ? /* @__PURE__ */ v.jsxs("a", { id: n.id, href: n.disabled ? void 0 : n.linkTo, target: n.target, className: `${Ya["button-container"]} row ${n.className ?? "button-text-3"}`, style: n.style, onClick: n.onClick, children: [
    n.prefix,
    /* @__PURE__ */ v.jsx(Ae, { maxLine: 1, className: Ya["button-label"], children: n.label }),
    n.suffix
  ] }) : /* @__PURE__ */ v.jsxs("button", { ref: t, id: n.id, type: n.type ?? "button", disabled: n.disabled, className: `${Ya["button-container"]} row ${n.className ?? "button-text-3"}`, style: n.style, onClick: n.onClick, children: [
    n.prefix,
    /* @__PURE__ */ v.jsx(Ae, { maxLine: 1, className: Ya["button-label"], children: n.label }),
    n.suffix
  ] });
}
const Vr = /* @__PURE__ */ new Date(), bu = new Date(
  Vr.getFullYear() - 100,
  Vr.getMonth(),
  Vr.getDate()
), wu = new Date(
  Vr.getFullYear() + 100,
  Vr.getMonth(),
  Vr.getDate()
), ki = (n, t = "dd/mm/yyyy") => {
  let e = t.split(" "), i = e[0], s = e[1];
  i.includes("hh") && (i = e[1], s = e[0]);
  let r = i.split(t.includes("/") ? "/" : "-").map((o) => {
    switch (o.toLowerCase()) {
      case "dd":
        return n.getDate() < 10 ? `0${n.getDate()}` : `${n.getDate()}`;
      case "mm":
        return n.getMonth() + 1 < 10 ? `0${n.getMonth() + 1}` : `${n.getMonth() + 1}`;
      case "yyyy":
        return `${n.getFullYear()}`;
      default:
        return "";
    }
  }).join(t.includes("/") ? "/" : "-");
  if (s) {
    let o = s.split(":").map((a) => {
      switch (a) {
        case "hh":
          return n.getHours() < 10 ? `0${n.getHours()}` : `${n.getHours()}`;
        case "mm":
          return n.getMinutes() < 10 ? `0${n.getMinutes()}` : `${n.getMinutes()}`;
        case "ss":
          return n.getSeconds() < 10 ? `0${n.getSeconds()}` : `${n.getSeconds()}`;
        default:
          return "";
      }
    }).join(":");
    return r + " " + o;
  }
  return r;
}, Dd = (n, t = "dd/mm/yyyy", e = "/") => {
  let i = t, s = "", r = n, o = "", a = !1;
  t.trim().indexOf(" ") > -1 && (i = t.trim().split(" ")[0], s = t.trim().split(" ")[1], r = n.trim().split(" ")[0], o = n.trim().split(" ")[1] ?? "00:00:00", a = !0);
  let c = i.toLowerCase().split(e), u = r.split(e), d = c.indexOf("mm"), h = c.indexOf("dd"), m = c.indexOf("yyyy"), g = 0, p = 0, w = 0;
  if (a) {
    let C = s.split(":"), E = C.indexOf("HH");
    E < 0 && (E = C.indexOf("hh"));
    let T = C.indexOf("mm"), L = C.indexOf("ss"), O = o.split(":");
    g = parseInt(O[E] ?? "0"), p = parseInt(O[T] ?? "0"), w = parseInt(O[L] ?? "0");
  }
  let y = parseInt(u[d]);
  y -= 1;
  var k = new Date(parseInt(u[m]), y, parseInt(u[h] ?? "0"), g, p, w);
  return k;
}, Ux = (n, t, e) => Zi(n, t) > -1 && Zi(e, n) > -1;
function LZ(n) {
  const t = yt(null), e = yt(null), [i, s] = zt(), r = Ca(() => {
    var l, c, u;
    return i ? i instanceof Date ? /* @__PURE__ */ v.jsx(Ae, { className: zi.value, children: ki(i, `dd/mm/yyyy${(l = n.pickerType) != null && l.includes("time") ? " hh:mm" : ""}`) }) : /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
      /* @__PURE__ */ v.jsxs(Ae, { className: zi.value, style: { flex: "none", width: "fit-content" }, children: [
        ki(i.start ?? /* @__PURE__ */ new Date(), `dd/mm/yyyy${(c = n.pickerType) != null && c.includes("time") || n.pickerType === "auto" ? " hh:mm" : ""}`),
        " - ",
        ki(i.end ?? /* @__PURE__ */ new Date(), `dd/mm/yyyy${(u = n.pickerType) != null && u.includes("time") || n.pickerType === "auto" ? " hh:mm" : ""}`)
      ] }),
      i.repeatData && /* @__PURE__ */ v.jsx(rt, { src: "outline/arrows/loop-2", size: "1.2rem" })
    ] }) : /* @__PURE__ */ v.jsx(Ae, { className: zi.value, style: { color: "var(--neutral-text-subtitle-color)" }, children: n.placeholder ?? "" });
  }, [i]);
  ht(() => {
    e.current && (i && i instanceof Date ? e.current.value = ki(i, "dd/mm/yyyy") : e.current.value = "");
  }, [i, e.current]), ht(() => {
    switch (n.pickerType) {
      case "date":
      case "datetime":
        s(n.value);
        break;
      default:
        s(!n.value || !n.endValue ? void 0 : { start: n.value, end: n.endValue, repeatData: n.pickerType === "auto" || !n.pickerType ? n.repeatValue : void 0 });
        break;
    }
  }, [n.value, n.endValue, n.repeatValue, n.pickerType]);
  const o = (l) => {
    Io({
      ref: t,
      clickOverlayClosePopup: !0,
      content: /* @__PURE__ */ v.jsx(
        Wx,
        {
          ref: t,
          max: n.max,
          min: n.min,
          value: i instanceof Date ? i : i == null ? void 0 : i.start,
          endValue: i instanceof Date || i == null ? void 0 : i.end,
          pickerType: n.pickerType,
          enableRepeat: n.enableRepeat || !!(!(i instanceof Date) && (i != null && i.repeatData)),
          repeatValue: i instanceof Date || i == null ? void 0 : i.repeatData,
          style: { top: l.bottom + 2, left: l.left + 16 },
          onApply: (c) => {
            s(c), Ss(t), n.onChange && n.onChange(c), e.current && e.current.focus();
          }
        }
      )
    });
  }, a = () => {
    var l;
    switch (n.pickerType) {
      case "date":
        return /* @__PURE__ */ v.jsxs(
          "div",
          {
            id: n.id,
            className: `row ${zi["date-time-picker"]} ${n.className ?? "body-3"} ${(l = n.helperText) != null && l.length ? zi["helper-text"] : ""}`,
            "helper-text": n.helperText,
            style: n.style ? { "--helper-text-color": n.helperTextColor ?? "#e14337", ...n.style } : { "--helper-text-color": n.helperTextColor ?? "#e14337" },
            onClick: (c) => {
              const u = c.target.closest("div").getBoundingClientRect();
              o(u);
            },
            children: [
              n.prefix ?? /* @__PURE__ */ v.jsx(rt, { className: zi["prefix-icon"], src: "outline/user interface/calendar-date", size: "1.2rem" }),
              /* @__PURE__ */ v.jsx(
                "input",
                {
                  className: zi.value,
                  ref: e,
                  autoComplete: "off",
                  disabled: n.disabled,
                  placeholder: n.placeholder,
                  readOnly: n.pickOnly,
                  onKeyDown: (c) => {
                    switch (c.key.toLowerCase()) {
                      case "enter":
                        c.target.blur();
                        break;
                    }
                  },
                  onBlur: n.pickOnly ? void 0 : (c) => {
                    const u = c.target.value.trim();
                    let d;
                    u.match(/[0-9]{1,2}(\/|-)[0-9]{1,2}(\/|-)[0-9]{4}/g) && (d = Dd(u, "dd/MM/yyyy", "/"), Ux(d, n.min ?? bu, n.min ?? wu) || (Zi(n.min ?? bu, d) > -1 ? d = n.min ?? bu : Zi(d, n.min ?? wu) > -1 ? d = n.max ?? wu : d = void 0)), s(d), n.onChange && n.onChange(d);
                  }
                }
              )
            ]
          }
        );
      default:
        return /* @__PURE__ */ v.jsxs("button", { id: n.id, type: "button", disabled: n.disabled, className: `row ${zi["date-time-picker"]} ${n.className ?? "body-3"}`, style: n.style, onClick: (c) => {
          const u = c.target.closest("button").getBoundingClientRect();
          o(u);
        }, children: [
          n.prefix ?? /* @__PURE__ */ v.jsx(rt, { className: zi["prefix-icon"], src: "outline/user interface/calendar-date", size: "1.2rem" }),
          r
        ] });
    }
  };
  return /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
    /* @__PURE__ */ v.jsx(d0, { ref: t }),
    a()
  ] });
}
const Wx = Ic(function({ value: t, style: e, endValue: i, repeatValue: s, onApply: r, pickerType: o = "auto", enableRepeat: a = !1, min: l, max: c }, u) {
  const d = y0({ shouldFocusError: !1 }), [h, m] = zt(!1), [g, p] = zt(!1), w = /* @__PURE__ */ new Date(), [y, k] = zt({ type: 1, value: ["everyday"] }), C = yt(null), E = yt(null), T = yt(null), { t: L } = Yr(), O = /[0-9]{1,2}(\/|-)[0-9]{1,2}(\/|-)[0-9]{4}/g, N = /^(?:[01]\d|2[0-3]):[0-5]\d(?:[:][0-5]\d)?$/g;
  ht(() => {
    s && a ? (p(!0), k(s)) : p(!1);
  }, [s]), ht(() => {
    h && o !== "auto" && !o.includes("time") ? (m(!1), d.setValue("time-start", null), d.setValue("time-end", null)) : !h && o.includes("time") && m(!0);
  }, [o]);
  const M = () => {
    if (t) {
      const D = new Date(t);
      d.setValue("date-start", D), E.current.getInput().value = ki(D), (o.includes("time") || D.getSeconds() === 1) && (m(!0), d.setValue("time-start", `${D.getHours() < 9 ? `0${D.getHours()}` : D.getHours()}:${D.getMinutes() < 9 ? `0${D.getMinutes()}` : D.getMinutes()}`));
    } else E.current.getInput().value = "";
  }, J = () => {
    if ((o != null && o.includes("range") || o === "auto") && T.current)
      if (i) {
        const D = new Date(i);
        d.setValue("date-end", D), T.current.getInput().value = ki(D), (o.includes("time") || D.getSeconds() === 59) && d.setValue("time-end", `${D.getHours() < 9 ? `0${D.getHours()}` : D.getHours()}:${D.getMinutes() < 9 ? `0${D.getMinutes()}` : D.getMinutes()}`);
      } else T.current.getInput().value = "";
  };
  return ht(() => {
    t && E.current && M();
  }, [t, E]), ht(() => {
    J();
  }, [i, T, o]), /* @__PURE__ */ v.jsxs("div", { className: "col", style: { width: "31.2rem", ...e }, children: [
    /* @__PURE__ */ v.jsx(d0, { ref: C }),
    /* @__PURE__ */ v.jsx(
      jx,
      {
        min: l,
        max: c,
        range: o.includes("range") || o === "auto",
        value: o === "date" || o === "datetime" ? d.watch("date-start") : d.watch("date-start") && d.watch("date-end") ? { sTime: d.watch("date-start"), eTime: d.watch("date-end") } : void 0,
        header: o !== "date" && /* @__PURE__ */ v.jsxs("div", { className: "row", style: { flexWrap: "wrap", gap: "0.8rem 1.2rem", padding: "1.6rem", borderBottom: "var(--neutral-main-border)" }, children: [
          /* @__PURE__ */ v.jsx(
            Ps,
            {
              ref: E,
              autoComplete: "off",
              className: "col12 body-3",
              style: { "--gutter": "1.2rem", padding: "0.4rem 1.2rem" },
              placeholder: o.includes("range") || o === "auto" ? L("start-date") : "dd/mm/yyyy",
              onComplete: (D) => D.target.blur(),
              onBlur: (D) => {
                const B = D.target.value;
                if (O.test(B)) {
                  const $ = Dd(B, "dd/mm/yyyy", "/");
                  (o.includes("range") || o === "auto") && Zi(d.getValues("date-end"), $) < 0 && (d.setValue("date-end", $), T.current.getInput().value = ki($)), d.setValue("date-start", $);
                } else D.target.value = d.getValues("date-start") ? ki(d.getValues("date-start")) : "";
              }
            }
          ),
          (o.includes("range") || o === "auto") && /* @__PURE__ */ v.jsx(
            Ps,
            {
              ref: T,
              autoComplete: "off",
              className: "col12 body-3",
              style: { "--gutter": "1.2rem", padding: "0.4rem 1.2rem" },
              placeholder: L("end-date"),
              onComplete: (D) => D.target.blur(),
              onBlur: (D) => {
                const B = D.target.value;
                if (O.test(B)) {
                  const $ = Dd(B, "dd/mm/yyyy", "/");
                  Zi($, d.getValues("date-start")) < 0 && (d.setValue("date-start", $), E.current.getInput().value = ki($)), d.setValue("date-end", $);
                } else D.target.value = d.getValues("date-end") ? ki(d.getValues("date-end")) : "";
              }
            }
          ),
          h && /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
            /* @__PURE__ */ v.jsx(
              Ps,
              {
                autoComplete: "off",
                name: "time-start",
                style: { "--gutter": "1.2rem", padding: "0.4rem 1.2rem" },
                onComplete: (D) => {
                  D.target.blur();
                },
                register: d.register("time-start", {
                  onChange: (D) => D.target.value = D.target.value.trim(),
                  onBlur: (D) => {
                    N.test(D.target.value) ? d.setValue("time-start", D.target.value) : D.target.value = "";
                  }
                }),
                className: "col12 body-3",
                placeholder: "hh:mm",
                onFocus: (D) => {
                  const B = D.target.closest("div").getBoundingClientRect();
                  Io({
                    ref: C,
                    clickOverlayClosePopup: !0,
                    content: /* @__PURE__ */ v.jsx("div", { className: `col ${zi["popup-actions"]}`, style: { maxHeight: "24rem", top: B.bottom + 2, right: document.body.offsetWidth - B.right, width: B.width, overflow: "hidden auto", border: "var(--neutral-main-border)" }, children: Array.from({ length: 48 }).map(($, W) => {
                      if (W % 2 === 0) var Y = `${W / 2 < 9 ? `0${W / 2}` : W / 2}:00`;
                      else Y = `${(W - 1) / 2 < 9 ? `0${(W - 1) / 2}` : (W - 1) / 2}:30`;
                      return /* @__PURE__ */ v.jsx("button", { type: "button", className: "row", onClick: () => {
                        d.setValue("time-start", Y), Ss(C);
                      }, children: /* @__PURE__ */ v.jsx(Ae, { className: "body-3", children: Y }) }, "time-" + W);
                    }) })
                  });
                }
              }
            ),
            (o.includes("range") || o === "auto") && /* @__PURE__ */ v.jsx(
              Ps,
              {
                autoComplete: "off",
                name: "time-end",
                style: { "--gutter": "1.2rem", padding: "0.4rem 1.2rem" },
                onComplete: (D) => {
                  D.target.blur();
                },
                register: d.register("time-end", {
                  onChange: (D) => D.target.value = D.target.value.trim(),
                  onBlur: (D) => {
                    N.test(D.target.value) ? d.setValue("time-end", D.target.value) : D.target.value = "";
                  }
                }),
                className: "col12 body-3",
                placeholder: "hh:mm",
                onFocus: (D) => {
                  const B = D.target.closest("div").getBoundingClientRect();
                  Io({
                    ref: C,
                    clickOverlayClosePopup: !0,
                    content: /* @__PURE__ */ v.jsx("div", { className: `col ${zi["popup-actions"]}`, style: { maxHeight: "24rem", top: B.bottom + 2, right: document.body.offsetWidth - B.right, width: B.width, overflow: "hidden auto", border: "var(--neutral-main-border)" }, children: Array.from({ length: 48 }).map(($, W) => {
                      if (W % 2 === 0) var Y = `${W / 2 < 9 ? `0${W / 2}` : W / 2}:00`;
                      else Y = `${(W - 1) / 2 < 9 ? `0${(W - 1) / 2}` : (W - 1) / 2}:30`;
                      return /* @__PURE__ */ v.jsx("button", { type: "button", className: "row", onClick: () => {
                        d.setValue("time-end", Y), Ss(C);
                      }, children: /* @__PURE__ */ v.jsx(Ae, { className: "body-3", children: Y }) }, "time-" + W);
                    }) })
                  });
                }
              }
            )
          ] })
        ] }),
        footer: o !== "date" && /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
          g && /* @__PURE__ */ v.jsxs("div", { className: "col", style: { borderTop: "var(--neutral-main-border)" }, children: [
            /* @__PURE__ */ v.jsxs("div", { className: "row", style: { gap: 4, padding: "1.2rem 1.6rem" }, children: [
              /* @__PURE__ */ v.jsx(Ae, { className: "heading-8", style: { flex: 1 }, children: "Lặp lại" }),
              /* @__PURE__ */ v.jsx(
                mn,
                {
                  style: { padding: 0 },
                  label: (() => {
                    switch (y.type) {
                      case 1:
                        return L("daily");
                      case 2:
                        return L("weekly");
                      case 3:
                        return L("monthly");
                      default:
                        return "";
                    }
                  })(),
                  suffix: /* @__PURE__ */ v.jsx(rt, { src: "outline/arrows/down-arrow", size: "1.4rem", style: { padding: "0.2rem" } }),
                  onClick: (D) => {
                    const B = D.target.closest("button").getBoundingClientRect();
                    Io({
                      ref: C,
                      clickOverlayClosePopup: !0,
                      style: { position: "absolute", top: B.bottom + 2, left: B.x + 8 },
                      body: /* @__PURE__ */ v.jsx("div", { className: "col popup-actions", children: Array.from({ length: 3 }).map(($, W) => {
                        let Y = "";
                        switch (W) {
                          case 0:
                            Y = L("daily");
                            break;
                          case 1:
                            Y = L("weekly");
                            break;
                          case 2:
                            Y = L("monthly");
                            break;
                        }
                        return /* @__PURE__ */ v.jsx("button", { type: "button", className: "row", onClick: () => {
                          let ye = ["everyday"];
                          switch (W) {
                            case 0:
                              ye = ["everyday"];
                              break;
                            case 1:
                              ye = w.getDay();
                              break;
                            case 2:
                              ye = w.getDate();
                              break;
                          }
                          k({ type: W + 1, value: [ye] }), Ss(C);
                        }, children: /* @__PURE__ */ v.jsx(Ae, { className: "button-text-3", children: Y }) }, "tStatus-" + W);
                      }) })
                    });
                  }
                }
              )
            ] }),
            (() => {
              switch (y.type) {
                case 2:
                  return /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
                    /* @__PURE__ */ v.jsx(Ae, { className: "heading-8", style: { padding: "0 1.6rem" }, children: L("on") + " " + L("date").toLowerCase() }),
                    /* @__PURE__ */ v.jsx("div", { className: "row", style: { justifyContent: "space-between", padding: "0.4rem 1.6rem" }, children: Array.from({ length: 7 }).map((D, B) => {
                      switch (B) {
                        case 0:
                          var $ = L("su");
                          break;
                        case 1:
                          $ = L("mo");
                          break;
                        case 2:
                          $ = L("tu");
                          break;
                        case 3:
                          $ = L("we");
                          break;
                        case 4:
                          $ = L("th");
                          break;
                        case 5:
                          $ = L("fr");
                          break;
                        case 6:
                          $ = L("sa");
                          break;
                        default:
                          $ = "";
                          break;
                      }
                      return /* @__PURE__ */ v.jsxs("div", { className: "col", style: { gap: 4, alignItems: "center" }, children: [
                        /* @__PURE__ */ v.jsx(a0, { size: "1.8rem", value: y.value.includes(B), disabled: y.value.includes(B) && y.value.length === 1, onChange: (W) => {
                          k(W ? { type: 2, value: [...y.value, B] } : { type: 2, value: y.value.filter((Y) => Y !== B) });
                        } }),
                        /* @__PURE__ */ v.jsx(Ae, { className: "placeholder-2", children: $ })
                      ] }, "weekday-" + B);
                    }) })
                  ] });
                case 3:
                  return /* @__PURE__ */ v.jsxs("div", { className: "row", style: { justifyContent: "space-between", padding: "0.4rem 1.6rem", gap: "1.2rem" }, children: [
                    /* @__PURE__ */ v.jsx(Ae, { className: "heading-8", style: { flex: 1 }, children: L("on") + " " + L("date").toLowerCase() }),
                    /* @__PURE__ */ v.jsx(
                      mn,
                      {
                        style: { padding: 0 },
                        label: y.value[0] === "last" ? L("Last") : `${y.value[0]}`,
                        suffix: /* @__PURE__ */ v.jsx(rt, { src: "outline/arrows/down-arrow", size: "1.4rem", style: { padding: "0.2rem" } }),
                        onClick: (D) => {
                          const B = D.target.closest("button").getBoundingClientRect();
                          Io({
                            ref: C,
                            clickOverlayClosePopup: !0,
                            style: { top: B.bottom + 2, right: document.body.offsetWidth - B.right, maxHeight: "30.4rem" },
                            body: /* @__PURE__ */ v.jsx("div", { className: "col popup-actions", style: { flex: 1, overflow: "hidden auto" }, children: Array.from({ length: 29 }).map(($, W) => {
                              switch (W) {
                                case 28:
                                  var Y = L("Last");
                                  break;
                                default:
                                  Y = `${W + 1}`;
                                  break;
                              }
                              return /* @__PURE__ */ v.jsx("button", { type: "button", className: "row", onClick: () => {
                                k({ type: 3, value: [W === 28 ? "last" : W + 1] }), Ss(C);
                              }, children: /* @__PURE__ */ v.jsx(Ae, { className: "button-text-3", children: Y }) }, "date-" + W);
                            }) })
                          });
                        }
                      }
                    )
                  ] });
                default:
                  return null;
              }
            })()
          ] }),
          r && /* @__PURE__ */ v.jsxs("div", { className: "row", style: { gap: "0.8rem", padding: "1.2rem 1.6rem", borderTop: "var(--neutral-main-border)" }, children: [
            o === "auto" && /* @__PURE__ */ v.jsxs("div", { className: "row", style: { gap: 4 }, children: [
              /* @__PURE__ */ v.jsx(
                rt,
                {
                  src: "outline/user interface/time-alarm",
                  size: "1.6rem",
                  style: { padding: "0.7rem", borderRadius: "50%", backgroundColor: h ? "var(--neutral-disable-background-color)" : void 0 },
                  onClick: () => {
                    m(!h);
                  }
                }
              ),
              (a || o === "auto") && /* @__PURE__ */ v.jsx(
                rt,
                {
                  src: "outline/arrows/loop-2",
                  size: "1.6rem",
                  style: { padding: "0.7rem", borderRadius: "50%", backgroundColor: g ? "var(--neutral-disable-background-color)" : void 0 },
                  onClick: () => {
                    p(!g);
                  }
                }
              )
            ] }),
            /* @__PURE__ */ v.jsx("div", { style: { flex: 1 } }),
            /* @__PURE__ */ v.jsx(
              mn,
              {
                label: L("reset"),
                onClick: () => {
                  d.setValue("date-start", null), d.setValue("date-end", null), d.setValue("time-start", null), d.setValue("time-end", null), M(), J();
                }
              }
            ),
            /* @__PURE__ */ v.jsx(
              mn,
              {
                label: L("apply"),
                disabled: !d.watch("date-start") || !d.watch("date-end") && (o.includes("range") || o === "auto"),
                className: "button-text-3 button-primary",
                onClick: () => {
                  var W, Y;
                  let D = d.getValues("date-start"), B = h && (W = d.getValues("time-start")) != null && W.length ? d.getValues("time-start") : "00:00";
                  if (D.setHours(parseInt(B.split(":")[0]), parseInt(B.split(":")[1]), h ? 1 : 0, 0), o.includes("range") || o === "auto") {
                    var $ = d.getValues("date-end");
                    let ye = h && (Y = d.getValues("time-end")) != null && Y.length ? d.getValues("time-end") : "23:59";
                    $.setHours(parseInt(ye.split(":")[0]), parseInt(ye.split(":")[1]), h ? 59 : 0, 0);
                  }
                  r(!o.includes("range") && o !== "auto" ? D : { start: D, end: $, repeatData: g ? y : void 0 }), Ss(u);
                }
              }
            )
          ] })
        ] }),
        onSelect: (D) => {
          o !== "date" ? D instanceof Date ? (d.setValue("date-start", D), E.current && (E.current.getInput().value = ki(D))) : (d.setValue("date-start", D.sTime), E.current && (E.current.getInput().value = ki(D.sTime)), (o.includes("range") || o === "auto") && (d.setValue("date-end", D.eTime), T.current && (T.current.getInput().value = ki(D.eTime)))) : r && (r(D), Ss(u));
        }
      }
    )
  ] });
}), qx = "_disabled_15r79_35", Pn = {
  "select-multi-container": "_select-multi-container_15r79_1",
  disabled: qx,
  "helper-text": "_helper-text_15r79_45",
  "selected-item-value": "_selected-item-value_15r79_83",
  "select-multi-popup": "_select-multi-popup_15r79_139",
  "select-body": "_select-body_15r79_161",
  "select-tile": "_select-tile_15r79_183",
  "no-results-found": "_no-results-found_15r79_233"
};
class Gx extends ce.Component {
  constructor(e) {
    super(e);
    f(this, "containerRef", vn());
    f(this, "inputRef", vn());
    this.state = {
      value: e.value ?? [],
      options: e.options,
      offset: {
        x: 0,
        y: 0,
        height: 0,
        width: 0,
        bottom: 0,
        left: 0,
        right: 0,
        top: 0,
        toJSON: function() {
          throw new Error("Function not implemented.");
        }
      },
      isOpen: !1,
      onSelect: null
    }, this.onCheck = this.onCheck.bind(this), this.search = this.search.bind(this), this.onClickItem = this.onClickItem.bind(this);
  }
  onCheck(e, i) {
    let s = [];
    e ? s = [...this.state.value, ...i.map((r) => r.id)] : s = this.state.value.filter((r) => i.every((o) => r !== o.id)), this.setState({ ...this.state, value: s }), this.props.onChange && this.props.onChange(s);
  }
  async search(e) {
    var i;
    if (e.target.value.trim().length)
      if ((i = this.props) != null && i.handleSearch) {
        const s = await this.props.handleSearch(e.target.value.trim());
        this.setState({ ...this.state, search: s });
      } else
        this.setState({
          ...this.state,
          search: this.props.options.filter((s) => typeof s.name == "string" && s.name.toLowerCase().includes(e.target.value.trim().toLowerCase()))
        });
    else
      this.setState({ ...this.state, search: void 0 });
  }
  onClickItem(e, i) {
    var r, o;
    e.stopPropagation();
    let s = this.state.value.filter((a) => a !== i);
    this.setState({
      ...this.state,
      value: s,
      ...this.state.isOpen ? {} : {
        isOpen: !0,
        style: void 0,
        offset: (o = (r = this.containerRef) == null ? void 0 : r.current) == null ? void 0 : o.getBoundingClientRect()
      }
    }), this.props.onChange && this.props.onChange(s);
  }
  renderOptions(e) {
    let i = [];
    return e.parentId || (i = (this.state.search ?? this.state.options).filter((s) => s.parentId === e.id)), /* @__PURE__ */ v.jsxs("div", { className: "col", style: { width: "100%" }, children: [
      /* @__PURE__ */ v.jsxs("div", { className: `${Pn["select-tile"]} row ${e.disabled ? Pn.disabled : ""}`, style: { paddingLeft: e.parentId ? "4.4rem" : void 0 }, onClick: i.length ? () => {
        this.state.search ? this.setState({
          ...this.state,
          search: this.state.search.map((s) => s.id === e.id ? { ...s, isOpen: !e.isOpen } : s)
        }) : this.setState({
          ...this.state,
          options: this.state.options.map((s) => s.id === e.id ? { ...s, isOpen: !e.isOpen } : s)
        });
      } : void 0, children: [
        (this.state.search ?? this.state.options).some((s) => s.parentId) && /* @__PURE__ */ v.jsx("div", { className: "row", style: { width: "1.4rem", height: "1.4rem" }, children: i.length ? /* @__PURE__ */ v.jsx(rt, { src: e.isOpen ? "fill/arrows/triangle-down" : "fill/arrows/triangle-right", size: "1.2rem" }) : null }),
        /* @__PURE__ */ v.jsx(a0, { disabled: e.disabled, value: i.length ? i.every((s) => this.state.value.includes(s.id)) ? !0 : i.some((s) => this.state.value.includes(s.id)) ? void 0 : !1 : this.state.value.includes(e.id), onChange: (s) => {
          this.onCheck(s, [e, ...i]);
        }, size: "2rem" }),
        /* @__PURE__ */ v.jsx(Ae, { className: "body-3", children: e.name })
      ] }),
      /* @__PURE__ */ v.jsx("div", { className: "col", style: { display: e.isOpen ? "flex" : "none", width: "100%" }, children: i.map((s) => this.renderOptions(s)) })
    ] }, e.id);
  }
  componentDidUpdate(e, i) {
    var s, r;
    if (e.options !== this.props.options && this.setState({ ...this.state, options: this.props.options }), e.value !== this.props.value && this.setState({ ...this.state, value: this.props.value ?? [] }), this.state.isOpen && (i.isOpen !== this.state.isOpen || i.value.length !== this.state.value.length)) {
      const o = (s = this.containerRef.current.querySelector(".select-multi-popup")) == null ? void 0 : s.getBoundingClientRect();
      if (o) {
        let a;
        i.isOpen !== this.state.isOpen && o.right > document.body.offsetWidth && (a = {
          top: this.state.offset.y + this.state.offset.height + 2 + "px",
          right: document.body.offsetWidth - this.state.offset.right + "px"
        });
        let l = o.bottom - 8;
        const c = (r = this.containerRef.current) == null ? void 0 : r.getBoundingClientRect();
        c && (i.value.length !== this.state.value.length && (l = c.bottom + 2 + o.height, a = { ...a ?? {}, top: `${c.bottom + 2}px` }), l > document.body.offsetHeight && (a = { ...a ?? {}, top: `${c.y - 2 - o.height}px` })), a && (a.left ?? (a.left = a.right ? void 0 : `${this.state.offset.x}px`), a.width ?? (a.width = `${this.state.offset.width}px`), this.setState({ ...this.state, style: a }));
      }
    }
  }
  render() {
    var i, s, r;
    const { t: e } = this.props;
    return /* @__PURE__ */ v.jsxs(
      "div",
      {
        id: this.props.id,
        ref: this.containerRef,
        className: `${Pn["select-multi-container"]} row ${this.props.disabled ? Pn.disabled : ""} ${((i = this.props.helperText) == null ? void 0 : i.length) && Pn["helper-text"]} ${this.props.className ?? "body-3"}`,
        "helper-text": this.props.helperText,
        style: this.props.style ? { "--helper-text-color": this.props.helperTextColor ?? "#e14337", ...this.props.style } : { "--helper-text-color": this.props.helperTextColor ?? "#e14337" },
        onClick: () => {
          var o, a;
          this.state.isOpen || this.setState({
            ...this.state,
            isOpen: !0,
            style: void 0,
            offset: (a = (o = this.containerRef) == null ? void 0 : o.current) == null ? void 0 : a.getBoundingClientRect()
          });
        },
        children: [
          /* @__PURE__ */ v.jsxs("div", { className: "row", style: { flexWrap: "wrap", flex: 1, width: "100%", gap: "0.6rem 0.4rem" }, children: [
            this.state.value.map((o) => {
              const a = this.props.options.find((l) => l.id === o);
              return /* @__PURE__ */ v.jsxs("div", { className: `row ${Pn["selected-item-value"]}`, onClick: a != null && a.disabled ? void 0 : (l) => this.onClickItem(l, o), children: [
                /* @__PURE__ */ v.jsx(Ae, { style: { color: "var(--neutral-text-title-color)", fontSize: "1.2rem", lineHeight: "1.4rem" }, children: a == null ? void 0 : a.name }),
                /* @__PURE__ */ v.jsx(rt, { src: "outline/user interface/e-remove", size: "1.2rem" })
              ] }, o);
            }),
            (!this.state.value.length || this.state.isOpen) && /* @__PURE__ */ v.jsx(
              "input",
              {
                ref: this.inputRef,
                autoFocus: this.state.value.length > 0,
                onChange: this.search,
                placeholder: this.state.value.length ? void 0 : this.props.placeholder,
                onBlur: (o) => {
                  this.state.isOpen && o.target.focus();
                }
              }
            )
          ] }),
          this.props.showClearValueButton && this.state.value.length ? /* @__PURE__ */ v.jsx("button", { type: "button", className: "row", style: { padding: "0.4rem" }, onClick: (o) => {
            o.stopPropagation(), this.state.value.length && this.setState({ ...this.state, isOpen: !0, value: [] });
          }, children: /* @__PURE__ */ v.jsx(rt, { src: "outline/user interface/c-remove", size: "1.6rem" }) }) : /* @__PURE__ */ v.jsx("div", { ref: (o) => {
            o != null && o.parentElement && o.parentElement.getBoundingClientRect().width < 100 && (o.style.display = "none");
          }, className: "row", children: /* @__PURE__ */ v.jsx(rt, { src: this.state.isOpen ? "fill/arrows/up-arrow" : "fill/arrows/down-arrow", size: "1.2rem" }) }),
          this.state.isOpen && /* @__PURE__ */ v.jsx(
            Yh,
            {
              className: "hidden-overlay",
              onClose: (o) => {
                o.target !== this.inputRef.current && this.setState({ ...this.state, isOpen: !1 });
              },
              children: /* @__PURE__ */ v.jsxs(
                "div",
                {
                  className: `${Pn["select-multi-popup"]} select-multi-popup col ${this.props.popupClassName ?? ""}`,
                  style: this.state.style ?? {
                    top: this.state.offset.y + this.state.offset.height + 2 + "px",
                    left: this.state.offset.x + "px",
                    width: this.state.offset.width
                  },
                  children: [
                    /* @__PURE__ */ v.jsx("div", { style: { padding: "1.2rem 1.6rem", width: "100%", borderBottom: "var(--neutral-main-border)" }, children: (() => {
                      const o = this.state.search ?? this.props.options ?? [], a = o.every((l) => this.state.value.some((c) => c === l.id));
                      return /* @__PURE__ */ v.jsx(Ae, { onClick: () => {
                        let l = [];
                        o.length && (a ? l = this.state.value.filter((c) => o.every((u) => c !== u.id || u.disabled)) : l = [...this.state.value, ...o.filter((c) => this.state.value.every((u) => u !== c.id) && !c.disabled).map((c) => c.id)]), this.setState({ ...this.state, value: l }), this.props.onChange && this.props.onChange(l);
                      }, className: "button-text-3", style: { color: o.length ? void 0 : "var(--neutral-text-title-color)" }, children: o.length && a ? `${e("remove")} ${e("all").toLowerCase()}` : `${e("select")} ${e("all").toLowerCase()}` });
                    })() }),
                    /* @__PURE__ */ v.jsxs("div", { className: `col ${Pn["select-body"]}`, onScroll: this.props.handleLoadmore ? (o) => {
                      if (this.props.handleLoadmore) {
                        let a = o.target;
                        this.props.handleLoadmore(Math.round(a.offsetHeight + a.scrollTop) >= a.scrollHeight - 1, o);
                      }
                    } : void 0, children: [
                      (this.state.search ?? this.state.options).filter((o) => !o.parentId).map((o) => this.renderOptions(o)),
                      !((s = this.state.search) != null && s.length) && !((r = this.props.options) != null && r.length) && /* @__PURE__ */ v.jsx("div", { className: Pn["no-results-found"], children: e("noResultFound") })
                    ] })
                  ]
                }
              )
            }
          )
        ]
      }
    );
  }
}
const BZ = Oc()(Gx), uo = {
  "progress-bar-container": "_progress-bar-container_1ddy5_1",
  "progress-bar-title": "_progress-bar-title_1ddy5_11",
  "progress-bar-tile": "_progress-bar-tile_1ddy5_21",
  "progress-bar-value": "_progress-bar-value_1ddy5_33",
  "status-icon": "_status-icon_1ddy5_77"
};
function MZ({ id: n, status: t = Xo.INFOR, percent: e = 100, titleText: i, title: s, hideTitle: r = !1, progressBarOnly: o = !1, fullColor: a = "var(--neutral-main-background-color)", percentColor: l = "var(--primary-main-color)", style: c, progressBarStyle: u }) {
  const [d, h] = zt(!0);
  return /* @__PURE__ */ v.jsxs("div", { id: n, className: `col ${uo["progress-bar-container"]}`, style: c ? { padding: o ? "0" : "1.6rem 2.4rem", ...c } : { padding: o ? "0" : "1.6rem 2.4rem" }, children: [
    r || o ? null : s ?? /* @__PURE__ */ v.jsxs("div", { className: `row ${uo["progress-bar-title"]}`, children: [
      /* @__PURE__ */ v.jsx("div", { className: "heading-8", children: i }),
      /* @__PURE__ */ v.jsx(rt, { src: d ? "fill/arrows/down-arrow" : "fill/arrows/up-arrow", onClick: () => {
        h(!d);
      } })
    ] }),
    d ? /* @__PURE__ */ v.jsxs("div", { className: `row ${uo["progress-bar-tile"]}`, children: [
      /* @__PURE__ */ v.jsx("div", { className: uo["progress-bar-value"], style: { "--percent-color": l, "--full-color": a, "--percent": `${e}%`, ...u ?? {} } }),
      o || t === Xo.INFOR ? null : /* @__PURE__ */ v.jsx("div", { className: `${uo["status-icon"]}`, children: x0(t) }),
      o ? null : /* @__PURE__ */ v.jsxs("div", { className: "label-4", children: [
        e,
        "/100"
      ] })
    ] }) : null
  ] });
}
var Xo = /* @__PURE__ */ ((n) => (n[n.INFOR = 1] = "INFOR", n[n.ERROR = 2] = "ERROR", n[n.WARNING = 3] = "WARNING", n[n.SUCCSESS = 4] = "SUCCSESS", n))(Xo || {});
const x0 = (n) => {
  switch (n) {
    case 2:
      return Zx;
    case 3:
      return Yx;
    case 4:
      return Jx;
    default:
      return Kx;
  }
}, Kx = /* @__PURE__ */ v.jsx("div", { style: { width: "3.6rem", height: "3.6rem" }, children: /* @__PURE__ */ v.jsx("svg", { width: "100%", height: "100%", viewBox: "0 0 36 36", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: /* @__PURE__ */ v.jsx("path", { d: "M18.0003 3.41669C15.116 3.41669 12.2965 4.27198 9.89826 5.87442C7.50005 7.47686 5.63087 9.75446 4.52709 12.4192C3.42331 15.084 3.13451 18.0162 3.69721 20.8451C4.25991 23.674 5.64884 26.2725 7.68836 28.312C9.72787 30.3515 12.3264 31.7404 15.1553 32.3031C17.9842 32.8658 20.9164 32.577 23.5811 31.4733C26.2459 30.3695 28.5235 28.5003 30.1259 26.1021C31.7284 23.7039 32.5837 20.8843 32.5837 18C32.5795 14.1336 31.0417 10.4267 28.3077 7.69266C25.5737 4.95867 21.8668 3.42087 18.0003 3.41669V3.41669ZM19.2496 26.1715H16.7401V15.5695H19.2496V26.1715ZM19.0418 12.2384C18.9034 12.3653 18.7407 12.463 18.5637 12.5256C18.3866 12.5883 18.1987 12.6146 18.0113 12.603C17.8204 12.6157 17.6289 12.5899 17.4481 12.5273C17.2673 12.4647 17.101 12.3664 16.9588 12.2384C16.8333 12.1032 16.7363 11.9441 16.6737 11.7706C16.611 11.5971 16.584 11.4127 16.5943 11.2285C16.5815 11.04 16.6073 10.8509 16.67 10.6727C16.7327 10.4945 16.8309 10.3309 16.9588 10.1919C17.1013 10.0644 17.2678 9.96649 17.4484 9.9039C17.6291 9.84131 17.8204 9.81526 18.0113 9.82728C18.1987 9.81637 18.3864 9.843 18.5633 9.90561C18.7403 9.96822 18.903 10.0655 19.0418 10.1919C19.1697 10.3309 19.268 10.4945 19.3307 10.6727C19.3934 10.8509 19.4191 11.04 19.4064 11.2285C19.4166 11.4127 19.3896 11.5971 19.327 11.7706C19.2644 11.9441 19.1674 12.1032 19.0418 12.2384Z", fill: "#366AE2" }) }) }), Zx = /* @__PURE__ */ v.jsx("div", { style: { width: "3.6rem", height: "3.6rem" }, children: /* @__PURE__ */ v.jsx("svg", { width: "100%", height: "100%", viewBox: "0 0 36 36", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: /* @__PURE__ */ v.jsx("path", { d: "M18.0003 3.41669C15.116 3.41669 12.2965 4.27198 9.89826 5.87442C7.50005 7.47686 5.63087 9.75446 4.52709 12.4192C3.42331 15.084 3.13451 18.0162 3.69721 20.8451C4.25991 23.674 5.64884 26.2725 7.68836 28.312C9.72787 30.3515 12.3264 31.7404 15.1553 32.3031C17.9842 32.8658 20.9164 32.577 23.5811 31.4733C26.2459 30.3695 28.5235 28.5003 30.1259 26.1021C31.7284 23.7039 32.5837 20.8843 32.5837 18C32.5724 14.1357 31.0324 10.4329 28.2999 7.70044C25.5674 4.96797 21.8646 3.42791 18.0003 3.41669V3.41669ZM24.016 22.2972L22.2976 24.0156L18.0003 19.7184L13.7031 24.0156L11.9847 22.2972L16.2819 18L11.9847 13.7028L13.7031 11.9844L18.0003 16.2816L22.2976 11.9844L24.016 13.7028L19.7187 18L24.016 22.2972Z", fill: "#E14337" }) }) }), Yx = /* @__PURE__ */ v.jsx("div", { style: { width: "3.6rem", height: "3.6rem" }, children: /* @__PURE__ */ v.jsx("svg", { width: "100%", height: "100%", viewBox: "0 0 36 36", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: /* @__PURE__ */ v.jsx("path", { d: "M32.1516 25.8877L21.3215 5.41379C21.0014 4.81074 20.5231 4.30622 19.938 3.95437C19.3529 3.60252 18.6831 3.41663 18.0003 3.41663C17.3176 3.41663 16.6477 3.60252 16.0626 3.95437C15.4775 4.30622 14.9993 4.81074 14.6792 5.41379L3.84901 25.8877C3.54781 26.459 3.39954 27.0984 3.41863 27.744C3.43771 28.3895 3.6235 29.0191 3.95793 29.5716C4.29235 30.1241 4.76404 30.5806 5.32713 30.8969C5.89022 31.2131 6.52555 31.3783 7.17136 31.3763H28.8293C29.4751 31.3783 30.1104 31.2131 30.6735 30.8969C31.2366 30.5806 31.7083 30.1241 32.0427 29.5716C32.3771 29.0191 32.5629 28.3895 32.582 27.744C32.6011 27.0984 32.4528 26.459 32.1516 25.8877ZM18.0003 27.7294C17.6397 27.7294 17.2871 27.6224 16.9873 27.4221C16.6874 27.2217 16.4537 26.9369 16.3157 26.6037C16.1776 26.2705 16.1415 25.9039 16.2119 25.5502C16.2823 25.1965 16.4559 24.8715 16.7109 24.6165C16.966 24.3615 17.2909 24.1878 17.6446 24.1175C17.9983 24.0471 18.3649 24.0832 18.6981 24.2212C19.0313 24.3593 19.3161 24.593 19.5165 24.8928C19.7168 25.1927 19.8238 25.5453 19.8238 25.9059C19.8238 26.3895 19.6317 26.8533 19.2897 27.1953C18.9477 27.5373 18.4839 27.7294 18.0003 27.7294ZM19.216 21.6512H16.7847L16.1769 11.926H19.8238L19.216 21.6512Z", fill: "#FC6B03" }) }) }), Jx = /* @__PURE__ */ v.jsx("div", { style: { width: "3.6rem", height: "3.6rem" }, children: /* @__PURE__ */ v.jsx("svg", { width: "100%", height: "100%", viewBox: "0 0 36 36", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: /* @__PURE__ */ v.jsx("path", { d: "M18.0003 3.41669C15.116 3.41669 12.2965 4.27198 9.89826 5.87442C7.50005 7.47686 5.63087 9.75446 4.52709 12.4192C3.42331 15.084 3.13451 18.0162 3.69721 20.8451C4.25991 23.674 5.64884 26.2725 7.68836 28.312C9.72787 30.3515 12.3264 31.7404 15.1553 32.3031C17.9842 32.8658 20.9164 32.577 23.5811 31.4733C26.2459 30.3695 28.5235 28.5003 30.1259 26.1021C31.7284 23.7039 32.5837 20.8843 32.5837 18C32.5724 14.1357 31.0324 10.4329 28.2999 7.70044C25.5674 4.96797 21.8646 3.42791 18.0003 3.41669V3.41669ZM15.5698 24.5795L8.99026 18L10.7087 16.2816L15.5698 21.1427L25.292 11.4205L27.0104 13.1389L15.5698 24.5795Z", fill: "#39AC6D" }) }) });
var Pl = { exports: {} }, Xx = Pl.exports, Xm;
function Qx() {
  return Xm || (Xm = 1, function(n, t) {
    (function(e, i) {
      n.exports = i(ce);
    })(Xx, (e) => (() => {
      var i = { 703: (a, l, c) => {
        var u = c(414);
        function d() {
        }
        function h() {
        }
        h.resetWarningCache = d, a.exports = function() {
          function m(w, y, k, C, E, T) {
            if (T !== u) {
              var L = new Error("Calling PropTypes validators directly is not supported by the `prop-types` package. Use PropTypes.checkPropTypes() to call them. Read more at http://fb.me/use-check-prop-types");
              throw L.name = "Invariant Violation", L;
            }
          }
          function g() {
            return m;
          }
          m.isRequired = m;
          var p = { array: m, bigint: m, bool: m, func: m, number: m, object: m, string: m, symbol: m, any: m, arrayOf: g, element: m, elementType: m, instanceOf: g, node: m, objectOf: g, oneOf: g, oneOfType: g, shape: g, exact: g, checkPropTypes: h, resetWarningCache: d };
          return p.PropTypes = p, p;
        };
      }, 697: (a, l, c) => {
        a.exports = c(703)();
      }, 414: (a) => {
        a.exports = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
      }, 98: (a) => {
        a.exports = e;
      } }, s = {};
      function r(a) {
        var l = s[a];
        if (l !== void 0) return l.exports;
        var c = s[a] = { exports: {} };
        return i[a](c, c.exports, r), c.exports;
      }
      r.n = (a) => {
        var l = a && a.__esModule ? () => a.default : () => a;
        return r.d(l, { a: l }), l;
      }, r.d = (a, l) => {
        for (var c in l) r.o(l, c) && !r.o(a, c) && Object.defineProperty(a, c, { enumerable: !0, get: l[c] });
      }, r.o = (a, l) => Object.prototype.hasOwnProperty.call(a, l), r.r = (a) => {
        typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
      };
      var o = {};
      return (() => {
        r.r(o), r.d(o, { default: () => D });
        var a = r(98), l = r.n(a), c = r(697), u = r.n(c);
        function d() {
          return d = Object.assign ? Object.assign.bind() : function(B) {
            for (var $ = 1; $ < arguments.length; $++) {
              var W = arguments[$];
              for (var Y in W) Object.prototype.hasOwnProperty.call(W, Y) && (B[Y] = W[Y]);
            }
            return B;
          }, d.apply(this, arguments);
        }
        var h = function(B) {
          var $ = B.pageClassName, W = B.pageLinkClassName, Y = B.page, ye = B.selected, Pe = B.activeClassName, Be = B.activeLinkClassName, te = B.getEventListener, R = B.pageSelectedHandler, K = B.href, X = B.extraAriaContext, ie = B.pageLabelBuilder, oe = B.rel, de = B.ariaLabel || "Page " + Y + (X ? " " + X : ""), pe = null;
          return ye && (pe = "page", de = B.ariaLabel || "Page " + Y + " is your current page", $ = $ !== void 0 ? $ + " " + Pe : Pe, W !== void 0 ? Be !== void 0 && (W = W + " " + Be) : W = Be), l().createElement("li", { className: $ }, l().createElement("a", d({ rel: oe, role: K ? void 0 : "button", className: W, href: K, tabIndex: ye ? "-1" : "0", "aria-label": de, "aria-current": pe, onKeyPress: R }, te(R)), ie(Y)));
        };
        h.propTypes = { pageSelectedHandler: u().func.isRequired, selected: u().bool.isRequired, pageClassName: u().string, pageLinkClassName: u().string, activeClassName: u().string, activeLinkClassName: u().string, extraAriaContext: u().string, href: u().string, ariaLabel: u().string, page: u().number.isRequired, getEventListener: u().func.isRequired, pageLabelBuilder: u().func.isRequired, rel: u().string };
        const m = h;
        function g() {
          return g = Object.assign ? Object.assign.bind() : function(B) {
            for (var $ = 1; $ < arguments.length; $++) {
              var W = arguments[$];
              for (var Y in W) Object.prototype.hasOwnProperty.call(W, Y) && (B[Y] = W[Y]);
            }
            return B;
          }, g.apply(this, arguments);
        }
        var p = function(B) {
          var $ = B.breakLabel, W = B.breakAriaLabel, Y = B.breakClassName, ye = B.breakLinkClassName, Pe = B.breakHandler, Be = B.getEventListener, te = Y || "break";
          return l().createElement("li", { className: te }, l().createElement("a", g({ className: ye, role: "button", tabIndex: "0", "aria-label": W, onKeyPress: Pe }, Be(Pe)), $));
        };
        p.propTypes = { breakLabel: u().oneOfType([u().string, u().node]), breakAriaLabel: u().string, breakClassName: u().string, breakLinkClassName: u().string, breakHandler: u().func.isRequired, getEventListener: u().func.isRequired };
        const w = p;
        function y(B) {
          var $ = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "";
          return B ?? $;
        }
        function k(B) {
          return k = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function($) {
            return typeof $;
          } : function($) {
            return $ && typeof Symbol == "function" && $.constructor === Symbol && $ !== Symbol.prototype ? "symbol" : typeof $;
          }, k(B);
        }
        function C() {
          return C = Object.assign ? Object.assign.bind() : function(B) {
            for (var $ = 1; $ < arguments.length; $++) {
              var W = arguments[$];
              for (var Y in W) Object.prototype.hasOwnProperty.call(W, Y) && (B[Y] = W[Y]);
            }
            return B;
          }, C.apply(this, arguments);
        }
        function E(B, $) {
          for (var W = 0; W < $.length; W++) {
            var Y = $[W];
            Y.enumerable = Y.enumerable || !1, Y.configurable = !0, "value" in Y && (Y.writable = !0), Object.defineProperty(B, Y.key, Y);
          }
        }
        function T(B, $) {
          return T = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(W, Y) {
            return W.__proto__ = Y, W;
          }, T(B, $);
        }
        function L(B, $) {
          if ($ && (k($) === "object" || typeof $ == "function")) return $;
          if ($ !== void 0) throw new TypeError("Derived constructors may only return object or undefined");
          return O(B);
        }
        function O(B) {
          if (B === void 0) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
          return B;
        }
        function N(B) {
          return N = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function($) {
            return $.__proto__ || Object.getPrototypeOf($);
          }, N(B);
        }
        function M(B, $, W) {
          return $ in B ? Object.defineProperty(B, $, { value: W, enumerable: !0, configurable: !0, writable: !0 }) : B[$] = W, B;
        }
        var J = function(B) {
          (function(te, R) {
            if (typeof R != "function" && R !== null) throw new TypeError("Super expression must either be null or a function");
            te.prototype = Object.create(R && R.prototype, { constructor: { value: te, writable: !0, configurable: !0 } }), Object.defineProperty(te, "prototype", { writable: !1 }), R && T(te, R);
          })(Be, B);
          var $, W, Y, ye, Pe = (Y = Be, ye = function() {
            if (typeof Reflect > "u" || !Reflect.construct || Reflect.construct.sham) return !1;
            if (typeof Proxy == "function") return !0;
            try {
              return Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
              })), !0;
            } catch {
              return !1;
            }
          }(), function() {
            var te, R = N(Y);
            if (ye) {
              var K = N(this).constructor;
              te = Reflect.construct(R, arguments, K);
            } else te = R.apply(this, arguments);
            return L(this, te);
          });
          function Be(te) {
            var R, K;
            return function(X, ie) {
              if (!(X instanceof ie)) throw new TypeError("Cannot call a class as a function");
            }(this, Be), M(O(R = Pe.call(this, te)), "handlePreviousPage", function(X) {
              var ie = R.state.selected;
              R.handleClick(X, null, ie > 0 ? ie - 1 : void 0, { isPrevious: !0 });
            }), M(O(R), "handleNextPage", function(X) {
              var ie = R.state.selected, oe = R.props.pageCount;
              R.handleClick(X, null, ie < oe - 1 ? ie + 1 : void 0, { isNext: !0 });
            }), M(O(R), "handlePageSelected", function(X, ie) {
              if (R.state.selected === X) return R.callActiveCallback(X), void R.handleClick(ie, null, void 0, { isActive: !0 });
              R.handleClick(ie, null, X);
            }), M(O(R), "handlePageChange", function(X) {
              R.state.selected !== X && (R.setState({ selected: X }), R.callCallback(X));
            }), M(O(R), "getEventListener", function(X) {
              return M({}, R.props.eventListener, X);
            }), M(O(R), "handleClick", function(X, ie, oe) {
              var de = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {}, pe = de.isPrevious, ve = pe !== void 0 && pe, _e = de.isNext, Ee = _e !== void 0 && _e, we = de.isBreak, Ne = we !== void 0 && we, se = de.isActive, Qe = se !== void 0 && se;
              X.preventDefault ? X.preventDefault() : X.returnValue = !1;
              var q = R.state.selected, U = R.props.onClick, V = oe;
              if (U) {
                var _ = U({ index: ie, selected: q, nextSelectedPage: oe, event: X, isPrevious: ve, isNext: Ee, isBreak: Ne, isActive: Qe });
                if (_ === !1) return;
                Number.isInteger(_) && (V = _);
              }
              V !== void 0 && R.handlePageChange(V);
            }), M(O(R), "handleBreakClick", function(X, ie) {
              var oe = R.state.selected;
              R.handleClick(ie, X, oe < X ? R.getForwardJump() : R.getBackwardJump(), { isBreak: !0 });
            }), M(O(R), "callCallback", function(X) {
              R.props.onPageChange !== void 0 && typeof R.props.onPageChange == "function" && R.props.onPageChange({ selected: X });
            }), M(O(R), "callActiveCallback", function(X) {
              R.props.onPageActive !== void 0 && typeof R.props.onPageActive == "function" && R.props.onPageActive({ selected: X });
            }), M(O(R), "getElementPageRel", function(X) {
              var ie = R.state.selected, oe = R.props, de = oe.nextPageRel, pe = oe.prevPageRel, ve = oe.selectedPageRel;
              return ie - 1 === X ? pe : ie === X ? ve : ie + 1 === X ? de : void 0;
            }), M(O(R), "pagination", function() {
              var X = [], ie = R.props, oe = ie.pageRangeDisplayed, de = ie.pageCount, pe = ie.marginPagesDisplayed, ve = ie.breakLabel, _e = ie.breakClassName, Ee = ie.breakLinkClassName, we = ie.breakAriaLabels, Ne = R.state.selected;
              if (de <= oe) for (var se = 0; se < de; se++) X.push(R.getPageElement(se));
              else {
                var Qe = oe / 2, q = oe - Qe;
                Ne > de - oe / 2 ? Qe = oe - (q = de - Ne) : Ne < oe / 2 && (q = oe - (Qe = Ne));
                var U, V, _ = function(z) {
                  return R.getPageElement(z);
                }, b = [];
                for (U = 0; U < de; U++) {
                  var x = U + 1;
                  if (x <= pe) b.push({ type: "page", index: U, display: _(U) });
                  else if (x > de - pe) b.push({ type: "page", index: U, display: _(U) });
                  else if (U >= Ne - Qe && U <= Ne + (Ne === 0 && oe > 1 ? q - 1 : q)) b.push({ type: "page", index: U, display: _(U) });
                  else if (ve && b.length > 0 && b[b.length - 1].display !== V && (oe > 0 || pe > 0)) {
                    var A = U < Ne ? we.backward : we.forward;
                    V = l().createElement(w, { key: U, breakAriaLabel: A, breakLabel: ve, breakClassName: _e, breakLinkClassName: Ee, breakHandler: R.handleBreakClick.bind(null, U), getEventListener: R.getEventListener }), b.push({ type: "break", index: U, display: V });
                  }
                }
                b.forEach(function(z, j) {
                  var H = z;
                  z.type === "break" && b[j - 1] && b[j - 1].type === "page" && b[j + 1] && b[j + 1].type === "page" && b[j + 1].index - b[j - 1].index <= 2 && (H = { type: "page", index: z.index, display: _(z.index) }), X.push(H.display);
                });
              }
              return X;
            }), te.initialPage !== void 0 && te.forcePage !== void 0 && console.warn("(react-paginate): Both initialPage (".concat(te.initialPage, ") and forcePage (").concat(te.forcePage, ") props are provided, which is discouraged.") + ` Use exclusively forcePage prop for a controlled component.
See https://reactjs.org/docs/forms.html#controlled-components`), K = te.initialPage ? te.initialPage : te.forcePage ? te.forcePage : 0, R.state = { selected: K }, R;
          }
          return $ = Be, (W = [{ key: "componentDidMount", value: function() {
            var te = this.props, R = te.initialPage, K = te.disableInitialCallback, X = te.extraAriaContext, ie = te.pageCount, oe = te.forcePage;
            R === void 0 || K || this.callCallback(R), X && console.warn("DEPRECATED (react-paginate): The extraAriaContext prop is deprecated. You should now use the ariaLabelBuilder instead."), Number.isInteger(ie) || console.warn("(react-paginate): The pageCount prop value provided is not an integer (".concat(ie, "). Did you forget a Math.ceil()?")), R !== void 0 && R > ie - 1 && console.warn("(react-paginate): The initialPage prop provided is greater than the maximum page index from pageCount prop (".concat(R, " > ").concat(ie - 1, ").")), oe !== void 0 && oe > ie - 1 && console.warn("(react-paginate): The forcePage prop provided is greater than the maximum page index from pageCount prop (".concat(oe, " > ").concat(ie - 1, ")."));
          } }, { key: "componentDidUpdate", value: function(te) {
            this.props.forcePage !== void 0 && this.props.forcePage !== te.forcePage && (this.props.forcePage > this.props.pageCount - 1 && console.warn("(react-paginate): The forcePage prop provided is greater than the maximum page index from pageCount prop (".concat(this.props.forcePage, " > ").concat(this.props.pageCount - 1, ").")), this.setState({ selected: this.props.forcePage })), Number.isInteger(te.pageCount) && !Number.isInteger(this.props.pageCount) && console.warn("(react-paginate): The pageCount prop value provided is not an integer (".concat(this.props.pageCount, "). Did you forget a Math.ceil()?"));
          } }, { key: "getForwardJump", value: function() {
            var te = this.state.selected, R = this.props, K = R.pageCount, X = te + R.pageRangeDisplayed;
            return X >= K ? K - 1 : X;
          } }, { key: "getBackwardJump", value: function() {
            var te = this.state.selected - this.props.pageRangeDisplayed;
            return te < 0 ? 0 : te;
          } }, { key: "getElementHref", value: function(te) {
            var R = this.props, K = R.hrefBuilder, X = R.pageCount, ie = R.hrefAllControls;
            if (K) return ie || te >= 0 && te < X ? K(te + 1, X, this.state.selected) : void 0;
          } }, { key: "ariaLabelBuilder", value: function(te) {
            var R = te === this.state.selected;
            if (this.props.ariaLabelBuilder && te >= 0 && te < this.props.pageCount) {
              var K = this.props.ariaLabelBuilder(te + 1, R);
              return this.props.extraAriaContext && !R && (K = K + " " + this.props.extraAriaContext), K;
            }
          } }, { key: "getPageElement", value: function(te) {
            var R = this.state.selected, K = this.props, X = K.pageClassName, ie = K.pageLinkClassName, oe = K.activeClassName, de = K.activeLinkClassName, pe = K.extraAriaContext, ve = K.pageLabelBuilder;
            return l().createElement(m, { key: te, pageSelectedHandler: this.handlePageSelected.bind(null, te), selected: R === te, rel: this.getElementPageRel(te), pageClassName: X, pageLinkClassName: ie, activeClassName: oe, activeLinkClassName: de, extraAriaContext: pe, href: this.getElementHref(te), ariaLabel: this.ariaLabelBuilder(te), page: te + 1, pageLabelBuilder: ve, getEventListener: this.getEventListener });
          } }, { key: "render", value: function() {
            var te = this.props.renderOnZeroPageCount;
            if (this.props.pageCount === 0 && te !== void 0) return te && te(this.props);
            var R = this.props, K = R.disabledClassName, X = R.disabledLinkClassName, ie = R.pageCount, oe = R.className, de = R.containerClassName, pe = R.previousLabel, ve = R.previousClassName, _e = R.previousLinkClassName, Ee = R.previousAriaLabel, we = R.prevRel, Ne = R.nextLabel, se = R.nextClassName, Qe = R.nextLinkClassName, q = R.nextAriaLabel, U = R.nextRel, V = this.state.selected, _ = V === 0, b = V === ie - 1, x = "".concat(y(ve)).concat(_ ? " ".concat(y(K)) : ""), A = "".concat(y(se)).concat(b ? " ".concat(y(K)) : ""), z = "".concat(y(_e)).concat(_ ? " ".concat(y(X)) : ""), j = "".concat(y(Qe)).concat(b ? " ".concat(y(X)) : ""), H = _ ? "true" : "false", ne = b ? "true" : "false";
            return l().createElement("ul", { className: oe || de, role: "navigation", "aria-label": "Pagination" }, l().createElement("li", { className: x }, l().createElement("a", C({ className: z, href: this.getElementHref(V - 1), tabIndex: _ ? "-1" : "0", role: "button", onKeyPress: this.handlePreviousPage, "aria-disabled": H, "aria-label": Ee, rel: we }, this.getEventListener(this.handlePreviousPage)), pe)), this.pagination(), l().createElement("li", { className: A }, l().createElement("a", C({ className: j, href: this.getElementHref(V + 1), tabIndex: b ? "-1" : "0", role: "button", onKeyPress: this.handleNextPage, "aria-disabled": ne, "aria-label": q, rel: U }, this.getEventListener(this.handleNextPage)), Ne)));
          } }]) && E($.prototype, W), Object.defineProperty($, "prototype", { writable: !1 }), Be;
        }(a.Component);
        M(J, "propTypes", { pageCount: u().number.isRequired, pageRangeDisplayed: u().number, marginPagesDisplayed: u().number, previousLabel: u().node, previousAriaLabel: u().string, prevPageRel: u().string, prevRel: u().string, nextLabel: u().node, nextAriaLabel: u().string, nextPageRel: u().string, nextRel: u().string, breakLabel: u().oneOfType([u().string, u().node]), breakAriaLabels: u().shape({ forward: u().string, backward: u().string }), hrefBuilder: u().func, hrefAllControls: u().bool, onPageChange: u().func, onPageActive: u().func, onClick: u().func, initialPage: u().number, forcePage: u().number, disableInitialCallback: u().bool, containerClassName: u().string, className: u().string, pageClassName: u().string, pageLinkClassName: u().string, pageLabelBuilder: u().func, activeClassName: u().string, activeLinkClassName: u().string, previousClassName: u().string, nextClassName: u().string, previousLinkClassName: u().string, nextLinkClassName: u().string, disabledClassName: u().string, disabledLinkClassName: u().string, breakClassName: u().string, breakLinkClassName: u().string, extraAriaContext: u().string, ariaLabelBuilder: u().func, eventListener: u().string, renderOnZeroPageCount: u().func, selectedPageRel: u().string }), M(J, "defaultProps", { pageRangeDisplayed: 2, marginPagesDisplayed: 3, activeClassName: "selected", previousLabel: "Previous", previousClassName: "previous", previousAriaLabel: "Previous page", prevPageRel: "prev", prevRel: "prev", nextLabel: "Next", nextClassName: "next", nextAriaLabel: "Next page", nextPageRel: "next", nextRel: "next", breakLabel: "...", breakAriaLabels: { forward: "Jump forward", backward: "Jump backward" }, disabledClassName: "disabled", disableInitialCallback: !1, pageLabelBuilder: function(B) {
          return B;
        }, eventListener: "onClick", renderOnZeroPageCount: void 0, selectedPageRel: "canonical", hrefAllControls: !1 });
        const D = J;
      })(), o;
    })());
  }(Pl)), Pl.exports;
}
var e5 = Qx();
const t5 = /* @__PURE__ */ Rc(e5), i5 = "_pagination_1ovf2_35", n5 = "_active_1ovf2_83", vu = {
  "custom-pagination": "_custom-pagination_1ovf2_1",
  pagination: i5,
  active: n5
};
function NZ({ id: n, currentPage: t, itemPerPage: e, totalItem: i, onChangePage: s, hidePageSize: r = !1, hideGoToPage: o = !1, style: a }) {
  const l = yt(null), { t: c } = Yr();
  return ht(() => {
    if (l.current) {
      const u = l.current.getInput();
      u && (u.value = t.toString());
    }
  }, [t]), t > 1 && (i === 0 || Math.floor(i / e) + (i % e === 0 ? 0 : 1) < t) ? (s(1, e), /* @__PURE__ */ v.jsx("div", {})) : i > 0 ? /* @__PURE__ */ v.jsxs("div", { id: n, className: `${vu["custom-pagination"]} row`, style: a, children: [
    r ? null : /* @__PURE__ */ v.jsxs("div", { className: "row", style: { gap: "0.8rem" }, children: [
      /* @__PURE__ */ v.jsx(
        hx,
        {
          readOnly: !0,
          placeholder: e.toString(),
          options: [10, 20, 50, 80, 100, 150, 200].map((u, d) => ({ id: u, name: u })),
          style: { borderRadius: "0.4rem", width: "5.6rem", padding: "0 0.8rem", height: "2.4rem" },
          onChange: (u) => {
            s(t, isNaN(parseInt(u.id)) ? e : parseInt(u.id));
          }
        }
      ),
      /* @__PURE__ */ v.jsx(Ae, { className: "body-3", children: c("ofItems", { totalItem: i }) })
    ] }),
    /* @__PURE__ */ v.jsx("div", { style: { flex: 1 } }),
    /* @__PURE__ */ v.jsx(
      t5,
      {
        onPageChange: (u) => {
          s(u.selected + 1, e);
        },
        forcePage: t - 1,
        breakClassName: "row button-text-3",
        breakLabel: "...",
        pageCount: Math.ceil(i / e),
        previousClassName: "row",
        previousLabel: /* @__PURE__ */ v.jsx(rt, { src: "fill/arrows/left-arrow", size: "1.4rem" }),
        nextClassName: "row",
        nextLabel: /* @__PURE__ */ v.jsx(rt, { src: "fill/arrows/right-arrow", size: "1.4rem" }),
        containerClassName: `${vu.pagination} row`,
        pageClassName: "row button-text-3",
        activeClassName: vu.active,
        hrefBuilder: (u) => u >= 1 && u <= Math.ceil(i / e) ? `/page/${u}` : "#",
        renderOnZeroPageCount: null
      }
    ),
    o ? null : /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
      /* @__PURE__ */ v.jsx("div", { style: { height: "1.6rem", backgroundColor: "var(--neutral-bolder-border-color)", width: 1 } }),
      /* @__PURE__ */ v.jsxs(Ae, { className: "label-3", children: [
        c("go"),
        " ",
        c("page").toLowerCase()
      ] }),
      /* @__PURE__ */ v.jsx(
        Ps,
        {
          ref: l,
          style: { width: "4.8rem", textAlign: "center", padding: 0, height: "2.4rem", borderRadius: "0.4rem" },
          className: "body-3",
          type: "number",
          onBlur: (u) => {
            const d = u.target.value.trim().length ? parseInt(u.target.value.trim()) : void 0;
            d && !isNaN(d) && d > 0 && d <= Math.ceil(i / e) ? s(d, e) : u.target.value = "";
          }
        }
      )
    ] })
  ] }) : /* @__PURE__ */ v.jsx("div", { id: n });
}
var s5 = /* @__PURE__ */ ((n) => (n.start = "start", n.center = "center", n.end = "end", n))(s5 || {});
class T0 extends ce.Component {
  render() {
    return /* @__PURE__ */ v.jsx("td", { id: this.props.id, onClick: this.props.onClick, style: this.props.style, "align-cell": this.props.align ?? "start", className: `tb-cell ${this.props.className ?? ""} ${this.props.fixed ? "tb-cell-fixed" : ""}`, children: this.props.children });
  }
}
class FZ extends ce.Component {
  render() {
    return /* @__PURE__ */ v.jsx("tr", { id: this.props.id, style: this.props.style, className: `tb-row ${this.props.className ?? ""}`, onClick: this.props.onClick, children: (this.props.children ?? []).map((t, e) => {
      let i = 0;
      return this.props.children && e > 0 && e < this.props.children.length - 1 && (i = `calc(${this.props.children.slice(0, e).map((s) => {
        var o, a;
        const r = ((o = s.props.style) == null ? void 0 : o.minWidth) ?? ((a = s.props.style) == null ? void 0 : a.width);
        return r ? typeof r == "number" ? `${r}px` : r : "60px";
      }).join(" + ")})`), /* @__PURE__ */ v.jsx(
        T0,
        {
          id: t.props.id,
          align: t.props.align,
          children: t.props.children,
          fixed: t.props.fixed,
          onClick: t.props.onClick,
          style: t.props.fixed ? this.props.children && e === this.props.children.length - 1 ? { right: 0, ...t.props.style ?? {} } : { left: i, ...t.props.style ?? {} } : t.props.style,
          className: t.props.className
        },
        `tb-cell-${e}`
      );
    }) });
  }
}
class DZ extends ce.Component {
  render() {
    return /* @__PURE__ */ v.jsx("thead", { style: this.props.style, className: `tb-header ${this.props.className ?? ""}`, children: /* @__PURE__ */ v.jsx("tr", { children: (this.props.children ?? []).map((t, e) => {
      let i = 0;
      return this.props.children && e > 0 && e < this.props.children.length - 1 && (i = `calc(${this.props.children.slice(0, e).map((s) => {
        var r;
        return (r = s.props.style) != null && r.width ? typeof s.props.style.width == "number" ? `${s.props.style.width}px` : s.props.style.width : "60px";
      }).join(" + ")})`), /* @__PURE__ */ v.jsx(
        T0,
        {
          id: t.props.id,
          align: t.props.align,
          children: t.props.children,
          onClick: t.props.onClick,
          fixed: t.props.fixed,
          style: t.props.fixed ? this.props.children && e === this.props.children.length - 1 ? { right: 0, ...t.props.style ?? {} } : { left: i, ...t.props.style ?? {} } : t.props.style,
          className: t.props.className
        },
        `tb-cell-${e}`
      );
    }) }) });
  }
}
class $Z extends ce.Component {
  render() {
    return /* @__PURE__ */ v.jsx("tbody", { id: this.props.id, children: this.props.children });
  }
}
let zZ = class extends ce.Component {
  render() {
    return /* @__PURE__ */ v.jsx("table", { id: this.props.id, className: `custom-table ${this.props.className}`, style: this.props.style, children: this.props.children });
  }
};
const r5 = "_checkmark_xv7x5_39", Qm = {
  "radio-btn-container": "_radio-btn-container_xv7x5_1",
  checkmark: r5
};
class jZ extends ce.Component {
  render() {
    let t = {
      "--off-color": this.props.offColor ?? "var(--neutral-bolder-border-color)",
      "--active-color": this.props.activeColor ?? "var(--primary-main-color)",
      "--size": this.props.size ? typeof this.props.size == "number" ? `${this.props.size}px` : this.props.size : "20px"
    };
    return this.props.style && (delete this.props.style.width, delete this.props.style.minWidth, delete this.props.style.maxWidth, delete this.props.style.height, delete this.props.style.minHeight, delete this.props.style.maxHeight, t = {
      ...this.props.style,
      ...t
    }), /* @__PURE__ */ v.jsxs("label", { id: this.props.id, className: `row ${Qm["radio-btn-container"]} ${this.props.className ?? ""}`, style: t, children: [
      this.props.register ? /* @__PURE__ */ v.jsx(
        "input",
        {
          ...this.props.register,
          type: "radio",
          value: this.props.value,
          disabled: this.props.disabled
        }
      ) : /* @__PURE__ */ v.jsx(
        "input",
        {
          type: "radio",
          name: this.props.name,
          value: this.props.value,
          defaultChecked: this.props.defaultChecked,
          disabled: this.props.disabled,
          onChange: this.props.onChange
        }
      ),
      /* @__PURE__ */ v.jsx("span", { className: Qm.checkmark })
    ] });
  }
}
const eg = {
  "text-area-container": "_text-area-container_11acu_1",
  "helper-text": "_helper-text_11acu_81"
};
class UZ extends ce.Component {
  constructor() {
    super(...arguments);
    f(this, "containerRef", vn());
    f(this, "getTextarea", () => {
      var e;
      return (e = this.containerRef.current) == null ? void 0 : e.querySelector("textarea");
    });
  }
  render() {
    var e;
    return /* @__PURE__ */ v.jsx(
      "div",
      {
        ref: this.containerRef,
        id: this.props.id,
        className: `${eg["text-area-container"]} row ${this.props.className ?? "body-3"} ${(e = this.props.helperText) != null && e.length ? eg["helper-text"] : ""}`,
        "helper-text": this.props.helperText,
        style: this.props.style ? { "--helper-text-color": this.props.helperTextColor ?? "#e14337", ...this.props.style } : { "--helper-text-color": this.props.helperTextColor ?? "#e14337" },
        children: this.props.register ? /* @__PURE__ */ v.jsx(
          "textarea",
          {
            name: this.props.name,
            autoFocus: this.props.autoFocus,
            ...this.props.register,
            maxLength: this.props.maxLength,
            placeholder: this.props.placeholder,
            readOnly: this.props.readOnly,
            disabled: this.props.disabled,
            onFocus: this.props.onFocus
          }
        ) : /* @__PURE__ */ v.jsx(
          "textarea",
          {
            autoFocus: this.props.autoFocus,
            maxLength: this.props.maxLength,
            name: this.props.name,
            defaultValue: this.props.defaultValue,
            value: this.props.value,
            placeholder: this.props.placeholder,
            readOnly: this.props.readOnly,
            disabled: this.props.disabled,
            onChange: this.props.onChange,
            onFocus: this.props.onFocus,
            onBlur: this.props.onBlur
          }
        )
      }
    );
  }
}
const Vn = {
  "import-file-container": "_import-file-container_121cp_1",
  "button-only": "_button-only_121cp_31",
  "preview-icon": "_preview-icon_121cp_47",
  "import-file-prefix": "_import-file-prefix_121cp_55",
  "file-preview-content": "_file-preview-content_121cp_91",
  "remove-preview-file": "_remove-preview-file_121cp_113",
  "helper-text": "_helper-text_121cp_133"
}, o5 = /* @__PURE__ */ v.jsxs("svg", { width: "100%", height: "100%", style: { width: "3rem", height: "3rem" }, viewBox: "0 0 36 36", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: [
  /* @__PURE__ */ v.jsx("path", { d: "M22.5312 6.51941C20.3258 6.12929 18.0555 6.35518 15.9702 7.1722C13.8849 7.98923 12.0654 9.36573 10.712 11.1502C9.53042 12.7081 8.74407 14.5243 8.41412 16.4432C6.99557 16.9154 5.7486 17.8144 4.85059 19.0274C3.77621 20.4786 3.27749 22.2764 3.45068 24.0737C3.62388 25.871 4.45672 27.5405 5.78845 28.7599C7.12018 29.9792 8.85639 30.6621 10.662 30.6766H13.1063C13.7786 30.6766 14.3236 30.1316 14.3236 29.4594C14.3236 28.7871 13.7786 28.2421 13.1063 28.2421H10.6769C9.47485 28.2313 8.31921 27.7762 7.43253 26.9643C6.54471 26.1514 5.98948 25.0384 5.87402 23.8402C5.75855 22.642 6.09103 21.4435 6.80729 20.476C7.52354 19.5085 8.57279 18.8406 9.75252 18.6013C10.2753 18.4952 10.6682 18.061 10.7216 17.5303C10.9012 15.7476 11.5691 14.049 12.6518 12.6214C13.7345 11.1938 15.1901 10.0926 16.8583 9.43899C18.5266 8.78536 20.3428 8.60466 22.1071 8.91675C23.8715 9.22884 25.5155 10.0216 26.8583 11.2079C28.2011 12.3941 29.1905 13.9278 29.7178 15.6402C30.2451 17.3526 30.2898 19.1772 29.8469 20.9134C29.404 22.6495 28.4907 24.2297 27.2075 25.4802C25.9244 26.7308 24.3211 27.603 22.5742 28.001C21.9187 28.1504 21.5084 28.8028 21.6577 29.4583C21.807 30.1138 22.4595 30.5241 23.115 30.3748C25.2987 29.8772 27.3028 28.7869 28.9067 27.2238C30.5107 25.6606 31.6523 23.6853 32.2059 21.5152C32.7595 19.345 32.7037 17.0642 32.0446 14.9237C31.3855 12.7833 30.1486 10.8661 28.4701 9.38333C26.7916 7.90052 24.7366 6.90953 22.5312 6.51941Z", style: { fill: "var(--primary-main-color)" } }),
  /* @__PURE__ */ v.jsx("path", { d: "M17.1146 17.6431C17.2313 17.5264 17.3658 17.4384 17.5094 17.379C17.6513 17.3201 17.8067 17.2874 17.9697 17.2866L17.9753 17.2866L17.9809 17.2866C18.2906 17.288 18.5998 17.4069 18.8361 17.6431L23.7052 22.5123C24.1806 22.9876 24.1806 23.7584 23.7052 24.2338C23.2298 24.7091 22.4591 24.7091 21.9837 24.2338L19.1926 21.4427V29.4594C19.1926 30.1317 18.6476 30.6767 17.9753 30.6767C17.303 30.6767 16.758 30.1317 16.758 29.4594V21.4427L13.9669 24.2338C13.4916 24.7091 12.7208 24.7091 12.2455 24.2338C11.7701 23.7584 11.7701 22.9876 12.2455 22.5123L17.1146 17.6431Z", style: { fill: "var(--primary-main-color)" } })
] }), a5 = /* @__PURE__ */ v.jsxs("svg", { className: Vn["preview-icon"], width: "100%", height: "100%", style: { width: "3rem", height: "3rem" }, viewBox: "0 0 36 36", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: [
  /* @__PURE__ */ v.jsx("path", { d: "M20.9163 3.41669H7.54829C7.22597 3.41669 6.91686 3.54472 6.68895 3.77263C6.46105 4.00054 6.33301 4.30965 6.33301 4.63196V31.3681C6.33301 31.6904 6.46105 31.9995 6.68895 32.2274C6.91686 32.4553 7.22597 32.5834 7.54829 32.5834H29.4233C29.7456 32.5834 30.0547 32.4553 30.2826 32.2274C30.5105 31.9995 30.6386 31.6904 30.6386 31.3681V13.1389H22.1316C21.8093 13.1389 21.5002 13.0109 21.2723 12.783C21.0444 12.5551 20.9163 12.2459 20.9163 11.9236V3.41669Z", style: { fill: "var(--primary-main-color)" } }),
  /* @__PURE__ */ v.jsx("path", { d: "M29.9264 10.7084H23.3469V4.12884L29.9264 10.7084Z", style: { fill: "var(--primary-main-color)" } })
] }), l5 = /* @__PURE__ */ v.jsx("svg", { width: "100%", height: "100%", style: { width: "2.4rem", height: "2.4rem" }, fill: "none", xmlns: "http://www.w3.org/2000/svg", children: /* @__PURE__ */ v.jsx("path", { d: "M13.4144 12.0002L20.4144 5.00015L19.0002 3.58594L12.0002 10.5859L5.00015 3.58594L3.58594 5.00015L10.5859 12.0002L3.58594 19.0002L5.00015 20.4144L12.0002 13.4144L19.0002 20.4144L20.4144 19.0002L13.4144 12.0002Z", style: { fill: "var(--error-main-color)" } }) }), c5 = (n, t) => {
  if (n == 0) return "0 Bytes";
  var e = 1e3, i = 2, s = ["Bytes", "KB", "MB", "GB", "TB", "PB", "EB", "ZB", "YB"], r = Math.floor(Math.log(n) / Math.log(e));
  return parseFloat((n / Math.pow(e, r)).toFixed(i)) + " " + s[r];
};
class u5 extends ce.Component {
  constructor(e) {
    super(e);
    f(this, "fileRef", vn());
    this.state = {
      preview: this.props.value ? Array.isArray(this.props.value) ? this.props.value : [this.props.value] : void 0
    };
  }
  showFilePicker() {
    var e;
    (e = this.fileRef.current) == null || e.click();
  }
  componentDidUpdate(e) {
    (e.value !== this.props.value || e.status !== this.props.status) && this.setState({ ...this.state, status: this.props.status, preview: this.props.value ? Array.isArray(this.props.value) ? this.props.value : [this.props.value] : void 0 });
  }
  render() {
    var r, o, a, l, c, u, d, h, m, g, p, w, y, k;
    const { t: e } = this.props;
    let i;
    this.props.maxSize && (i = c5(this.props.maxSize));
    let s = this.state.preview ? this.props.style ?? {} : { cursor: "pointer", ...this.props.style ?? {} };
    return /* @__PURE__ */ v.jsxs(
      "div",
      {
        id: this.props.id,
        className: `${Vn["import-file-container"]} ${this.props.className ?? "row"} ${this.props.buttonOnly ? Vn["button-only"] : ""} ${(r = this.props.helperText) != null && r.length ? Vn["helper-text"] : ""}`,
        style: { "--helper-text-color": this.props.helperTextColor ?? "#e14337", ...s },
        "helper-text": this.props.helperText,
        onClick: () => {
          !this.state.preview && !this.props.buttonOnly && this.showFilePicker();
        },
        children: [
          /* @__PURE__ */ v.jsx("input", { disabled: this.props.disabled, type: "file", multiple: this.props.multiple, accept: (this.props.allowType ?? []).join(","), ref: this.fileRef, onChange: (C) => {
            var T, L, O;
            let E;
            if ((T = C.target.files) != null && T.length && (E = [...C.target.files], this.props.maxSize && E.some((N) => N.size > this.props.maxSize * 1024) && (j5.errors(e("limitFileError", { name: (L = E.find((N) => N.size > this.props.maxSize * 1024)) == null ? void 0 : L.name, sizeTitle: i })), E = E.filter((N) => N.size <= this.props.maxSize * 1024))), E)
              if (this.props.multiple) {
                const N = ((O = this.state.preview) == null ? void 0 : O.filter((M) => E.every((J) => M.name !== J.name && M.size !== J.size && M.lastModified !== J.lastModified))) ?? [];
                this.setState({ ...this.state, preview: [...N, ...E] }), this.props.onChange && this.props.onChange([...N, ...E]);
              } else
                this.setState({ ...this.state, preview: E }), this.props.onChange && this.props.onChange(E);
          } }),
          this.props.buttonOnly ? null : this.props.multiple && ((o = this.state.preview) != null && o.length) ? /* @__PURE__ */ v.jsx("div", { className: "row", style: { flex: 1, flexWrap: "wrap", gap: "0.8rem" }, children: this.state.preview.map((C) => {
            var E;
            return /* @__PURE__ */ v.jsxs("div", { className: "row", style: { gap: "0.8rem", padding: "0.6rem 0.8rem", borderRadius: "0.4rem", border: "var(--neutral-main-border)", flex: "0 calc((100% * 6 / 24) - 0.8rem * 3 / 4)", width: "auto", minWidth: "11.4rem", ...this.props.fileTagStyle ?? {} }, children: [
              /* @__PURE__ */ v.jsx(rt, { src: `outline/${(E = C.type) != null && E.includes("image") ? "multimedia/image" : "files/file-export"}`, size: "1.4rem" }),
              /* @__PURE__ */ v.jsx(Ae, { className: "subtitle-4", style: { flex: 1, width: "100%" }, maxLine: 1, children: C.name }),
              /* @__PURE__ */ v.jsx(rt, { src: "fill/user interface/e-remove", size: "1.4rem", onClick: () => {
                var L;
                const T = (L = this.state.preview) == null ? void 0 : L.filter((O) => O.name !== C.name && O.size !== C.size && O.lastModified !== C.lastModified);
                this.setState({ ...this.state, preview: T }), this.props.onChange && this.props.onChange(T);
              }, color: "#E14337" })
            ] }, `${C.name}-${C.size}-${C.lastModified}`);
          }) }) : /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
            /* @__PURE__ */ v.jsx("div", { className: `${Vn["import-file-prefix"]} row`, children: (a = this.state.preview) != null && a.length ? (l = this.state.preview[0].type) != null && l.includes("image") ? /* @__PURE__ */ v.jsx("img", { src: this.state.preview[0] instanceof File ? URL.createObjectURL(this.state.preview[0]) : (u = (c = this.state.preview) == null ? void 0 : c[0]) == null ? void 0 : u.url }) : a5 : o5 }),
            /* @__PURE__ */ v.jsxs("div", { className: `${Vn["file-preview-content"]} col`, children: [
              /* @__PURE__ */ v.jsx(Ae, { className: `${Vn["title-file"]} heading-8`, style: { maxWidth: "100%" }, children: ((h = (d = this.state.preview) == null ? void 0 : d[0]) == null ? void 0 : h.name) ?? this.props.label ?? e("uploadFileAction") }),
              /* @__PURE__ */ v.jsx(Ae, { className: `${Vn["subtitle-file"]} subtitle-3`, style: { maxWidth: "100%" }, children: (g = (m = this.state.preview) == null ? void 0 : m[0]) != null && g.size ? `${(p = this.state.preview) == null ? void 0 : p[0].size}KB` : this.props.subTitle ?? (i ? e("limitFileWarning", { sizeTitle: i }) : "") })
            ] })
          ] }),
          (w = this.state.preview) != null && w.length && this.props.buttonOnly && !this.props.multiple ? /* @__PURE__ */ v.jsxs("div", { className: "row", style: { gap: "0.4rem" }, children: [
            /* @__PURE__ */ v.jsx(Ae, { className: "button-text-6", children: ((y = this.state.preview) == null ? void 0 : y[0].name) ?? "" }),
            /* @__PURE__ */ v.jsx("button", { type: "button", className: `${Vn["remove-preview-file"]}`, onClick: () => {
              this.setState({ ...this.state, preview: void 0 }), this.props.onChange && this.props.onChange(void 0);
            }, children: l5 })
          ] }) : /* @__PURE__ */ v.jsx(
            mn,
            {
              label: (k = this.state.preview) != null && k.length ? this.props.multiple ? `${e("add")} ${e("file").toLowerCase()}` : `${e("remove")} ${e("file").toLowerCase()}` : `${e("choose")} ${e("file").toLowerCase()}`,
              style: { padding: "1.2rem", backgroundColor: "var(--neutral-main-background-color)" },
              className: "button-text-4",
              onClick: () => {
                this.state.preview && !this.props.multiple ? (this.setState({ ...this.state, preview: void 0 }), this.props.onChange && this.props.onChange(void 0)) : (this.props.buttonOnly || this.state.preview) && this.showFilePicker();
              }
            }
          )
        ]
      }
    );
  }
}
const WZ = Oc()(u5);
function E0(n) {
  var t, e, i = "";
  if (typeof n == "string" || typeof n == "number") i += n;
  else if (typeof n == "object") if (Array.isArray(n)) {
    var s = n.length;
    for (t = 0; t < s; t++) n[t] && (e = E0(n[t])) && (i && (i += " "), i += e);
  } else for (e in n) n[e] && (i && (i += " "), i += e);
  return i;
}
function Os() {
  for (var n, t, e = 0, i = "", s = arguments.length; e < s; e++) (n = arguments[e]) && (t = E0(n)) && (i && (i += " "), i += t);
  return i;
}
function d5(n) {
  if (typeof document > "u") return;
  let t = document.head || document.getElementsByTagName("head")[0], e = document.createElement("style");
  e.type = "text/css", t.firstChild ? t.insertBefore(e, t.firstChild) : t.appendChild(e), e.styleSheet ? e.styleSheet.cssText = n : e.appendChild(document.createTextNode(n));
}
d5(`:root{--toastify-color-light: #fff;--toastify-color-dark: #121212;--toastify-color-info: #3498db;--toastify-color-success: #07bc0c;--toastify-color-warning: #f1c40f;--toastify-color-error: hsl(6, 78%, 57%);--toastify-color-transparent: rgba(255, 255, 255, .7);--toastify-icon-color-info: var(--toastify-color-info);--toastify-icon-color-success: var(--toastify-color-success);--toastify-icon-color-warning: var(--toastify-color-warning);--toastify-icon-color-error: var(--toastify-color-error);--toastify-container-width: fit-content;--toastify-toast-width: 320px;--toastify-toast-offset: 16px;--toastify-toast-top: max(var(--toastify-toast-offset), env(safe-area-inset-top));--toastify-toast-right: max(var(--toastify-toast-offset), env(safe-area-inset-right));--toastify-toast-left: max(var(--toastify-toast-offset), env(safe-area-inset-left));--toastify-toast-bottom: max(var(--toastify-toast-offset), env(safe-area-inset-bottom));--toastify-toast-background: #fff;--toastify-toast-padding: 14px;--toastify-toast-min-height: 64px;--toastify-toast-max-height: 800px;--toastify-toast-bd-radius: 6px;--toastify-toast-shadow: 0px 4px 12px rgba(0, 0, 0, .1);--toastify-font-family: sans-serif;--toastify-z-index: 9999;--toastify-text-color-light: #757575;--toastify-text-color-dark: #fff;--toastify-text-color-info: #fff;--toastify-text-color-success: #fff;--toastify-text-color-warning: #fff;--toastify-text-color-error: #fff;--toastify-spinner-color: #616161;--toastify-spinner-color-empty-area: #e0e0e0;--toastify-color-progress-light: linear-gradient(to right, #4cd964, #5ac8fa, #007aff, #34aadc, #5856d6, #ff2d55);--toastify-color-progress-dark: #bb86fc;--toastify-color-progress-info: var(--toastify-color-info);--toastify-color-progress-success: var(--toastify-color-success);--toastify-color-progress-warning: var(--toastify-color-warning);--toastify-color-progress-error: var(--toastify-color-error);--toastify-color-progress-bgo: .2}.Toastify__toast-container{z-index:var(--toastify-z-index);-webkit-transform:translate3d(0,0,var(--toastify-z-index));position:fixed;width:var(--toastify-container-width);box-sizing:border-box;color:#fff;display:flex;flex-direction:column}.Toastify__toast-container--top-left{top:var(--toastify-toast-top);left:var(--toastify-toast-left)}.Toastify__toast-container--top-center{top:var(--toastify-toast-top);left:50%;transform:translate(-50%);align-items:center}.Toastify__toast-container--top-right{top:var(--toastify-toast-top);right:var(--toastify-toast-right);align-items:end}.Toastify__toast-container--bottom-left{bottom:var(--toastify-toast-bottom);left:var(--toastify-toast-left)}.Toastify__toast-container--bottom-center{bottom:var(--toastify-toast-bottom);left:50%;transform:translate(-50%);align-items:center}.Toastify__toast-container--bottom-right{bottom:var(--toastify-toast-bottom);right:var(--toastify-toast-right);align-items:end}.Toastify__toast{--y: 0;position:relative;touch-action:none;width:var(--toastify-toast-width);min-height:var(--toastify-toast-min-height);box-sizing:border-box;margin-bottom:1rem;padding:var(--toastify-toast-padding);border-radius:var(--toastify-toast-bd-radius);box-shadow:var(--toastify-toast-shadow);max-height:var(--toastify-toast-max-height);font-family:var(--toastify-font-family);z-index:0;display:flex;flex:1 auto;align-items:center;word-break:break-word}@media only screen and (max-width: 480px){.Toastify__toast-container{width:100vw;left:env(safe-area-inset-left);margin:0}.Toastify__toast-container--top-left,.Toastify__toast-container--top-center,.Toastify__toast-container--top-right{top:env(safe-area-inset-top);transform:translate(0)}.Toastify__toast-container--bottom-left,.Toastify__toast-container--bottom-center,.Toastify__toast-container--bottom-right{bottom:env(safe-area-inset-bottom);transform:translate(0)}.Toastify__toast-container--rtl{right:env(safe-area-inset-right);left:initial}.Toastify__toast{--toastify-toast-width: 100%;margin-bottom:0;border-radius:0}}.Toastify__toast-container[data-stacked=true]{width:var(--toastify-toast-width)}.Toastify__toast--stacked{position:absolute;width:100%;transform:translate3d(0,var(--y),0) scale(var(--s));transition:transform .3s}.Toastify__toast--stacked[data-collapsed] .Toastify__toast-body,.Toastify__toast--stacked[data-collapsed] .Toastify__close-button{transition:opacity .1s}.Toastify__toast--stacked[data-collapsed=false]{overflow:visible}.Toastify__toast--stacked[data-collapsed=true]:not(:last-child)>*{opacity:0}.Toastify__toast--stacked:after{content:"";position:absolute;left:0;right:0;height:calc(var(--g) * 1px);bottom:100%}.Toastify__toast--stacked[data-pos=top]{top:0}.Toastify__toast--stacked[data-pos=bot]{bottom:0}.Toastify__toast--stacked[data-pos=bot].Toastify__toast--stacked:before{transform-origin:top}.Toastify__toast--stacked[data-pos=top].Toastify__toast--stacked:before{transform-origin:bottom}.Toastify__toast--stacked:before{content:"";position:absolute;left:0;right:0;bottom:0;height:100%;transform:scaleY(3);z-index:-1}.Toastify__toast--rtl{direction:rtl}.Toastify__toast--close-on-click{cursor:pointer}.Toastify__toast-icon{margin-inline-end:10px;width:22px;flex-shrink:0;display:flex}.Toastify--animate{animation-fill-mode:both;animation-duration:.5s}.Toastify--animate-icon{animation-fill-mode:both;animation-duration:.3s}.Toastify__toast-theme--dark{background:var(--toastify-color-dark);color:var(--toastify-text-color-dark)}.Toastify__toast-theme--light,.Toastify__toast-theme--colored.Toastify__toast--default{background:var(--toastify-color-light);color:var(--toastify-text-color-light)}.Toastify__toast-theme--colored.Toastify__toast--info{color:var(--toastify-text-color-info);background:var(--toastify-color-info)}.Toastify__toast-theme--colored.Toastify__toast--success{color:var(--toastify-text-color-success);background:var(--toastify-color-success)}.Toastify__toast-theme--colored.Toastify__toast--warning{color:var(--toastify-text-color-warning);background:var(--toastify-color-warning)}.Toastify__toast-theme--colored.Toastify__toast--error{color:var(--toastify-text-color-error);background:var(--toastify-color-error)}.Toastify__progress-bar-theme--light{background:var(--toastify-color-progress-light)}.Toastify__progress-bar-theme--dark{background:var(--toastify-color-progress-dark)}.Toastify__progress-bar--info{background:var(--toastify-color-progress-info)}.Toastify__progress-bar--success{background:var(--toastify-color-progress-success)}.Toastify__progress-bar--warning{background:var(--toastify-color-progress-warning)}.Toastify__progress-bar--error{background:var(--toastify-color-progress-error)}.Toastify__progress-bar-theme--colored.Toastify__progress-bar--info,.Toastify__progress-bar-theme--colored.Toastify__progress-bar--success,.Toastify__progress-bar-theme--colored.Toastify__progress-bar--warning,.Toastify__progress-bar-theme--colored.Toastify__progress-bar--error{background:var(--toastify-color-transparent)}.Toastify__close-button{color:#fff;position:absolute;top:6px;right:6px;background:transparent;outline:none;border:none;padding:0;cursor:pointer;opacity:.7;transition:.3s ease;z-index:1}.Toastify__toast--rtl .Toastify__close-button{left:6px;right:unset}.Toastify__close-button--light{color:#000;opacity:.3}.Toastify__close-button>svg{fill:currentColor;height:16px;width:14px}.Toastify__close-button:hover,.Toastify__close-button:focus{opacity:1}@keyframes Toastify__trackProgress{0%{transform:scaleX(1)}to{transform:scaleX(0)}}.Toastify__progress-bar{position:absolute;bottom:0;left:0;width:100%;height:100%;z-index:1;opacity:.7;transform-origin:left}.Toastify__progress-bar--animated{animation:Toastify__trackProgress linear 1 forwards}.Toastify__progress-bar--controlled{transition:transform .2s}.Toastify__progress-bar--rtl{right:0;left:initial;transform-origin:right;border-bottom-left-radius:initial}.Toastify__progress-bar--wrp{position:absolute;overflow:hidden;bottom:0;left:0;width:100%;height:5px;border-bottom-left-radius:var(--toastify-toast-bd-radius);border-bottom-right-radius:var(--toastify-toast-bd-radius)}.Toastify__progress-bar--wrp[data-hidden=true]{opacity:0}.Toastify__progress-bar--bg{opacity:var(--toastify-color-progress-bgo);width:100%;height:100%}.Toastify__spinner{width:20px;height:20px;box-sizing:border-box;border:2px solid;border-radius:100%;border-color:var(--toastify-spinner-color-empty-area);border-right-color:var(--toastify-spinner-color);animation:Toastify__spin .65s linear infinite}@keyframes Toastify__bounceInRight{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(3000px,0,0)}60%{opacity:1;transform:translate3d(-25px,0,0)}75%{transform:translate3d(10px,0,0)}90%{transform:translate3d(-5px,0,0)}to{transform:none}}@keyframes Toastify__bounceOutRight{20%{opacity:1;transform:translate3d(-20px,var(--y),0)}to{opacity:0;transform:translate3d(2000px,var(--y),0)}}@keyframes Toastify__bounceInLeft{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(-3000px,0,0)}60%{opacity:1;transform:translate3d(25px,0,0)}75%{transform:translate3d(-10px,0,0)}90%{transform:translate3d(5px,0,0)}to{transform:none}}@keyframes Toastify__bounceOutLeft{20%{opacity:1;transform:translate3d(20px,var(--y),0)}to{opacity:0;transform:translate3d(-2000px,var(--y),0)}}@keyframes Toastify__bounceInUp{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(0,3000px,0)}60%{opacity:1;transform:translate3d(0,-20px,0)}75%{transform:translate3d(0,10px,0)}90%{transform:translate3d(0,-5px,0)}to{transform:translateZ(0)}}@keyframes Toastify__bounceOutUp{20%{transform:translate3d(0,calc(var(--y) - 10px),0)}40%,45%{opacity:1;transform:translate3d(0,calc(var(--y) + 20px),0)}to{opacity:0;transform:translate3d(0,-2000px,0)}}@keyframes Toastify__bounceInDown{0%,60%,75%,90%,to{animation-timing-function:cubic-bezier(.215,.61,.355,1)}0%{opacity:0;transform:translate3d(0,-3000px,0)}60%{opacity:1;transform:translate3d(0,25px,0)}75%{transform:translate3d(0,-10px,0)}90%{transform:translate3d(0,5px,0)}to{transform:none}}@keyframes Toastify__bounceOutDown{20%{transform:translate3d(0,calc(var(--y) - 10px),0)}40%,45%{opacity:1;transform:translate3d(0,calc(var(--y) + 20px),0)}to{opacity:0;transform:translate3d(0,2000px,0)}}.Toastify__bounce-enter--top-left,.Toastify__bounce-enter--bottom-left{animation-name:Toastify__bounceInLeft}.Toastify__bounce-enter--top-right,.Toastify__bounce-enter--bottom-right{animation-name:Toastify__bounceInRight}.Toastify__bounce-enter--top-center{animation-name:Toastify__bounceInDown}.Toastify__bounce-enter--bottom-center{animation-name:Toastify__bounceInUp}.Toastify__bounce-exit--top-left,.Toastify__bounce-exit--bottom-left{animation-name:Toastify__bounceOutLeft}.Toastify__bounce-exit--top-right,.Toastify__bounce-exit--bottom-right{animation-name:Toastify__bounceOutRight}.Toastify__bounce-exit--top-center{animation-name:Toastify__bounceOutUp}.Toastify__bounce-exit--bottom-center{animation-name:Toastify__bounceOutDown}@keyframes Toastify__zoomIn{0%{opacity:0;transform:scale3d(.3,.3,.3)}50%{opacity:1}}@keyframes Toastify__zoomOut{0%{opacity:1}50%{opacity:0;transform:translate3d(0,var(--y),0) scale3d(.3,.3,.3)}to{opacity:0}}.Toastify__zoom-enter{animation-name:Toastify__zoomIn}.Toastify__zoom-exit{animation-name:Toastify__zoomOut}@keyframes Toastify__flipIn{0%{transform:perspective(400px) rotateX(90deg);animation-timing-function:ease-in;opacity:0}40%{transform:perspective(400px) rotateX(-20deg);animation-timing-function:ease-in}60%{transform:perspective(400px) rotateX(10deg);opacity:1}80%{transform:perspective(400px) rotateX(-5deg)}to{transform:perspective(400px)}}@keyframes Toastify__flipOut{0%{transform:translate3d(0,var(--y),0) perspective(400px)}30%{transform:translate3d(0,var(--y),0) perspective(400px) rotateX(-20deg);opacity:1}to{transform:translate3d(0,var(--y),0) perspective(400px) rotateX(90deg);opacity:0}}.Toastify__flip-enter{animation-name:Toastify__flipIn}.Toastify__flip-exit{animation-name:Toastify__flipOut}@keyframes Toastify__slideInRight{0%{transform:translate3d(110%,0,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideInLeft{0%{transform:translate3d(-110%,0,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideInUp{0%{transform:translate3d(0,110%,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideInDown{0%{transform:translate3d(0,-110%,0);visibility:visible}to{transform:translate3d(0,var(--y),0)}}@keyframes Toastify__slideOutRight{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(110%,var(--y),0)}}@keyframes Toastify__slideOutLeft{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(-110%,var(--y),0)}}@keyframes Toastify__slideOutDown{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(0,500px,0)}}@keyframes Toastify__slideOutUp{0%{transform:translate3d(0,var(--y),0)}to{visibility:hidden;transform:translate3d(0,-500px,0)}}.Toastify__slide-enter--top-left,.Toastify__slide-enter--bottom-left{animation-name:Toastify__slideInLeft}.Toastify__slide-enter--top-right,.Toastify__slide-enter--bottom-right{animation-name:Toastify__slideInRight}.Toastify__slide-enter--top-center{animation-name:Toastify__slideInDown}.Toastify__slide-enter--bottom-center{animation-name:Toastify__slideInUp}.Toastify__slide-exit--top-left,.Toastify__slide-exit--bottom-left{animation-name:Toastify__slideOutLeft;animation-timing-function:ease-in;animation-duration:.3s}.Toastify__slide-exit--top-right,.Toastify__slide-exit--bottom-right{animation-name:Toastify__slideOutRight;animation-timing-function:ease-in;animation-duration:.3s}.Toastify__slide-exit--top-center{animation-name:Toastify__slideOutUp;animation-timing-function:ease-in;animation-duration:.3s}.Toastify__slide-exit--bottom-center{animation-name:Toastify__slideOutDown;animation-timing-function:ease-in;animation-duration:.3s}@keyframes Toastify__spin{0%{transform:rotate(0)}to{transform:rotate(360deg)}}
`);
var Ta = (n) => typeof n == "number" && !isNaN(n), Ns = (n) => typeof n == "string", zn = (n) => typeof n == "function", h5 = (n) => Ns(n) || Ta(n), $d = (n) => Ns(n) || zn(n) ? n : null, f5 = (n, t) => n === !1 || Ta(n) && n > 0 ? n : t, zd = (n) => Pc(n) || Ns(n) || zn(n) || Ta(n);
function m5(n, t, e = 300) {
  let { scrollHeight: i, style: s } = n;
  requestAnimationFrame(() => {
    s.minHeight = "initial", s.height = i + "px", s.transition = `all ${e}ms`, requestAnimationFrame(() => {
      s.height = "0", s.padding = "0", s.margin = "0", setTimeout(t, e);
    });
  });
}
function S0({ enter: n, exit: t, appendPosition: e = !1, collapse: i = !0, collapseDuration: s = 300 }) {
  return function({ children: r, position: o, preventExitTransition: a, done: l, nodeRef: c, isIn: u, playToast: d }) {
    let h = e ? `${n}--${o}` : n, m = e ? `${t}--${o}` : t, g = yt(0);
    return o0(() => {
      let p = c.current, w = h.split(" "), y = (k) => {
        k.target === c.current && (d(), p.removeEventListener("animationend", y), p.removeEventListener("animationcancel", y), g.current === 0 && k.type !== "animationcancel" && p.classList.remove(...w));
      };
      p.classList.add(...w), p.addEventListener("animationend", y), p.addEventListener("animationcancel", y);
    }, []), ht(() => {
      let p = c.current, w = () => {
        p.removeEventListener("animationend", w), i ? m5(p, l, s) : l();
      };
      u || (a ? w() : (g.current = 1, p.className += ` ${m}`, p.addEventListener("animationend", w)));
    }, [u]), ce.createElement(ce.Fragment, null, r);
  };
}
function tg(n, t) {
  return { content: A0(n.content, n.props), containerId: n.props.containerId, id: n.props.toastId, theme: n.props.theme, type: n.props.type, data: n.props.data || {}, isLoading: n.props.isLoading, icon: n.props.icon, reason: n.removalReason, status: t };
}
function A0(n, t, e = !1) {
  return Pc(n) && !Ns(n.type) ? Kh(n, { closeToast: t.closeToast, toastProps: t, data: t.data, isPaused: e }) : zn(n) ? n({ closeToast: t.closeToast, toastProps: t, data: t.data, isPaused: e }) : n;
}
function g5({ closeToast: n, theme: t, ariaLabel: e = "close" }) {
  return ce.createElement("button", { className: `Toastify__close-button Toastify__close-button--${t}`, type: "button", onClick: (i) => {
    i.stopPropagation(), n(!0);
  }, "aria-label": e }, ce.createElement("svg", { "aria-hidden": "true", viewBox: "0 0 14 16" }, ce.createElement("path", { fillRule: "evenodd", d: "M7.71 8.23l3.75 3.75-1.48 1.48-3.75-3.75-3.75 3.75L1 11.98l3.75-3.75L1 4.48 2.48 3l3.75 3.75L9.98 3l1.48 1.48-3.75 3.75z" })));
}
function p5({ delay: n, isRunning: t, closeToast: e, type: i = "default", hide: s, className: r, controlledProgress: o, progress: a, rtl: l, isIn: c, theme: u }) {
  let d = s || o && a === 0, h = { animationDuration: `${n}ms`, animationPlayState: t ? "running" : "paused" };
  o && (h.transform = `scaleX(${a})`);
  let m = Os("Toastify__progress-bar", o ? "Toastify__progress-bar--controlled" : "Toastify__progress-bar--animated", `Toastify__progress-bar-theme--${u}`, `Toastify__progress-bar--${i}`, { "Toastify__progress-bar--rtl": l }), g = zn(r) ? r({ rtl: l, type: i, defaultClassName: m }) : Os(m, r), p = { [o && a >= 1 ? "onTransitionEnd" : "onAnimationEnd"]: o && a < 1 ? null : () => {
    c && e();
  } };
  return ce.createElement("div", { className: "Toastify__progress-bar--wrp", "data-hidden": d }, ce.createElement("div", { className: `Toastify__progress-bar--bg Toastify__progress-bar-theme--${u} Toastify__progress-bar--${i}` }), ce.createElement("div", { role: "progressbar", "aria-hidden": d ? "true" : "false", "aria-label": "notification timer", className: g, style: h, ...p }));
}
var b5 = 1, I0 = () => `${b5++}`;
function w5(n, t, e) {
  let i = 1, s = 0, r = [], o = [], a = t, l = /* @__PURE__ */ new Map(), c = /* @__PURE__ */ new Set(), u = (k) => (c.add(k), () => c.delete(k)), d = () => {
    o = Array.from(l.values()), c.forEach((k) => k());
  }, h = ({ containerId: k, toastId: C, updateId: E }) => {
    let T = k ? k !== n : n !== 1, L = l.has(C) && E == null;
    return T || L;
  }, m = (k, C) => {
    l.forEach((E) => {
      var T;
      (C == null || C === E.props.toastId) && ((T = E.toggle) == null || T.call(E, k));
    });
  }, g = (k) => {
    var C, E;
    (E = (C = k.props) == null ? void 0 : C.onClose) == null || E.call(C, k.removalReason), k.isActive = !1;
  }, p = (k) => {
    if (k == null) l.forEach(g);
    else {
      let C = l.get(k);
      C && g(C);
    }
    d();
  }, w = () => {
    s -= r.length, r = [];
  }, y = (k) => {
    var C, E;
    let { toastId: T, updateId: L } = k.props, O = L == null;
    k.staleId && l.delete(k.staleId), k.isActive = !0, l.set(T, k), d(), e(tg(k, O ? "added" : "updated")), O && ((E = (C = k.props).onOpen) == null || E.call(C));
  };
  return { id: n, props: a, observe: u, toggle: m, removeToast: p, toasts: l, clearQueue: w, buildToast: (k, C) => {
    if (h(C)) return;
    let { toastId: E, updateId: T, data: L, staleId: O, delay: N } = C, M = T == null;
    M && s++;
    let J = { ...a, style: a.toastStyle, key: i++, ...Object.fromEntries(Object.entries(C).filter(([B, $]) => $ != null)), toastId: E, updateId: T, data: L, isIn: !1, className: $d(C.className || a.toastClassName), progressClassName: $d(C.progressClassName || a.progressClassName), autoClose: C.isLoading ? !1 : f5(C.autoClose, a.autoClose), closeToast(B) {
      l.get(E).removalReason = B, p(E);
    }, deleteToast() {
      let B = l.get(E);
      if (B != null) {
        if (e(tg(B, "removed")), l.delete(E), s--, s < 0 && (s = 0), r.length > 0) {
          y(r.shift());
          return;
        }
        d();
      }
    } };
    J.closeButton = a.closeButton, C.closeButton === !1 || zd(C.closeButton) ? J.closeButton = C.closeButton : C.closeButton === !0 && (J.closeButton = zd(a.closeButton) ? a.closeButton : !0);
    let D = { content: k, props: J, staleId: O };
    a.limit && a.limit > 0 && s > a.limit && M ? r.push(D) : Ta(N) ? setTimeout(() => {
      y(D);
    }, N) : y(D);
  }, setProps(k) {
    a = k;
  }, setToggle: (k, C) => {
    let E = l.get(k);
    E && (E.toggle = C);
  }, isToastActive: (k) => {
    var C;
    return (C = l.get(k)) == null ? void 0 : C.isActive;
  }, getSnapshot: () => o };
}
var ii = /* @__PURE__ */ new Map(), Qo = [], Hd = /* @__PURE__ */ new Set(), v5 = (n) => Hd.forEach((t) => t(n)), P0 = () => ii.size > 0;
function _5() {
  Qo.forEach((n) => O0(n.content, n.options)), Qo = [];
}
var y5 = (n, { containerId: t }) => {
  var e;
  return (e = ii.get(t || 1)) == null ? void 0 : e.toasts.get(n);
};
function R0(n, t) {
  var e;
  if (t) return !!((e = ii.get(t)) != null && e.isToastActive(n));
  let i = !1;
  return ii.forEach((s) => {
    s.isToastActive(n) && (i = !0);
  }), i;
}
function k5(n) {
  if (!P0()) {
    Qo = Qo.filter((t) => n != null && t.options.toastId !== n);
    return;
  }
  if (n == null || h5(n)) ii.forEach((t) => {
    t.removeToast(n);
  });
  else if (n && ("containerId" in n || "id" in n)) {
    let t = ii.get(n.containerId);
    t ? t.removeToast(n.id) : ii.forEach((e) => {
      e.removeToast(n.id);
    });
  }
}
var C5 = (n = {}) => {
  ii.forEach((t) => {
    t.props.limit && (!n.containerId || t.id === n.containerId) && t.clearQueue();
  });
};
function O0(n, t) {
  zd(n) && (P0() || Qo.push({ content: n, options: t }), ii.forEach((e) => {
    e.buildToast(n, t);
  }));
}
function x5(n) {
  var t;
  (t = ii.get(n.containerId || 1)) == null || t.setToggle(n.id, n.fn);
}
function V0(n, t) {
  ii.forEach((e) => {
    (t == null || !(t != null && t.containerId) || (t == null ? void 0 : t.containerId) === e.id) && e.toggle(n, t == null ? void 0 : t.id);
  });
}
function T5(n) {
  let t = n.containerId || 1;
  return { subscribe(e) {
    let i = w5(t, n, v5);
    ii.set(t, i);
    let s = i.observe(e);
    return _5(), () => {
      s(), ii.delete(t);
    };
  }, setProps(e) {
    var i;
    (i = ii.get(t)) == null || i.setProps(e);
  }, getSnapshot() {
    var e;
    return (e = ii.get(t)) == null ? void 0 : e.getSnapshot();
  } };
}
function E5(n) {
  return Hd.add(n), () => {
    Hd.delete(n);
  };
}
function S5(n) {
  return n && (Ns(n.toastId) || Ta(n.toastId)) ? n.toastId : I0();
}
function Ea(n, t) {
  return O0(n, t), t.toastId;
}
function Lc(n, t) {
  return { ...t, type: t && t.type || n, toastId: S5(t) };
}
function Bc(n) {
  return (t, e) => Ea(t, Lc(n, e));
}
function Ye(n, t) {
  return Ea(n, Lc("default", t));
}
Ye.loading = (n, t) => Ea(n, Lc("default", { isLoading: !0, autoClose: !1, closeOnClick: !1, closeButton: !1, draggable: !1, ...t }));
function A5(n, { pending: t, error: e, success: i }, s) {
  let r;
  t && (r = Ns(t) ? Ye.loading(t, s) : Ye.loading(t.render, { ...s, ...t }));
  let o = { isLoading: null, autoClose: null, closeOnClick: null, closeButton: null, draggable: null }, a = (c, u, d) => {
    if (u == null) {
      Ye.dismiss(r);
      return;
    }
    let h = { type: c, ...o, ...s, data: d }, m = Ns(u) ? { render: u } : u;
    return r ? Ye.update(r, { ...h, ...m }) : Ye(m.render, { ...h, ...m }), d;
  }, l = zn(n) ? n() : n;
  return l.then((c) => a("success", i, c)).catch((c) => a("error", e, c)), l;
}
Ye.promise = A5;
Ye.success = Bc("success");
Ye.info = Bc("info");
Ye.error = Bc("error");
Ye.warning = Bc("warning");
Ye.warn = Ye.warning;
Ye.dark = (n, t) => Ea(n, Lc("default", { theme: "dark", ...t }));
function I5(n) {
  k5(n);
}
Ye.dismiss = I5;
Ye.clearWaitingQueue = C5;
Ye.isActive = R0;
Ye.update = (n, t = {}) => {
  let e = y5(n, t);
  if (e) {
    let { props: i, content: s } = e, r = { delay: 100, ...i, ...t, toastId: t.toastId || n, updateId: I0() };
    r.toastId !== n && (r.staleId = n);
    let o = r.render || s;
    delete r.render, Ea(o, r);
  }
};
Ye.done = (n) => {
  Ye.update(n, { progress: 1 });
};
Ye.onChange = E5;
Ye.play = (n) => V0(!0, n);
Ye.pause = (n) => V0(!1, n);
function P5(n) {
  var t;
  let { subscribe: e, getSnapshot: i, setProps: s } = yt(T5(n)).current;
  s(n);
  let r = (t = N2(e, i, i)) == null ? void 0 : t.slice();
  function o(a) {
    if (!r) return [];
    let l = /* @__PURE__ */ new Map();
    return n.newestOnTop && r.reverse(), r.forEach((c) => {
      let { position: u } = c.props;
      l.has(u) || l.set(u, []), l.get(u).push(c);
    }), Array.from(l, (c) => a(c[0], c[1]));
  }
  return { getToastToRender: o, isToastActive: R0, count: r == null ? void 0 : r.length };
}
function R5(n) {
  let [t, e] = zt(!1), [i, s] = zt(!1), r = yt(null), o = yt({ start: 0, delta: 0, removalDistance: 0, canCloseOnClick: !0, canDrag: !1, didMove: !1 }).current, { autoClose: a, pauseOnHover: l, closeToast: c, onClick: u, closeOnClick: d } = n;
  x5({ id: n.toastId, containerId: n.containerId, fn: e }), ht(() => {
    if (n.pauseOnFocusLoss) return h(), () => {
      m();
    };
  }, [n.pauseOnFocusLoss]);
  function h() {
    document.hasFocus() || y(), window.addEventListener("focus", w), window.addEventListener("blur", y);
  }
  function m() {
    window.removeEventListener("focus", w), window.removeEventListener("blur", y);
  }
  function g(O) {
    if (n.draggable === !0 || n.draggable === O.pointerType) {
      k();
      let N = r.current;
      o.canCloseOnClick = !0, o.canDrag = !0, N.style.transition = "none", n.draggableDirection === "x" ? (o.start = O.clientX, o.removalDistance = N.offsetWidth * (n.draggablePercent / 100)) : (o.start = O.clientY, o.removalDistance = N.offsetHeight * (n.draggablePercent === 80 ? n.draggablePercent * 1.5 : n.draggablePercent) / 100);
    }
  }
  function p(O) {
    let { top: N, bottom: M, left: J, right: D } = r.current.getBoundingClientRect();
    O.nativeEvent.type !== "touchend" && n.pauseOnHover && O.clientX >= J && O.clientX <= D && O.clientY >= N && O.clientY <= M ? y() : w();
  }
  function w() {
    e(!0);
  }
  function y() {
    e(!1);
  }
  function k() {
    o.didMove = !1, document.addEventListener("pointermove", E), document.addEventListener("pointerup", T);
  }
  function C() {
    document.removeEventListener("pointermove", E), document.removeEventListener("pointerup", T);
  }
  function E(O) {
    let N = r.current;
    if (o.canDrag && N) {
      o.didMove = !0, t && y(), n.draggableDirection === "x" ? o.delta = O.clientX - o.start : o.delta = O.clientY - o.start, o.start !== O.clientX && (o.canCloseOnClick = !1);
      let M = n.draggableDirection === "x" ? `${o.delta}px, var(--y)` : `0, calc(${o.delta}px + var(--y))`;
      N.style.transform = `translate3d(${M},0)`, N.style.opacity = `${1 - Math.abs(o.delta / o.removalDistance)}`;
    }
  }
  function T() {
    C();
    let O = r.current;
    if (o.canDrag && o.didMove && O) {
      if (o.canDrag = !1, Math.abs(o.delta) > o.removalDistance) {
        s(!0), n.closeToast(!0), n.collapseAll();
        return;
      }
      O.style.transition = "transform 0.2s, opacity 0.2s", O.style.removeProperty("transform"), O.style.removeProperty("opacity");
    }
  }
  let L = { onPointerDown: g, onPointerUp: p };
  return a && l && (L.onMouseEnter = y, n.stacked || (L.onMouseLeave = w)), d && (L.onClick = (O) => {
    u && u(O), o.canCloseOnClick && c(!0);
  }), { playToast: w, pauseToast: y, isRunning: t, preventExitTransition: i, toastRef: r, eventHandlers: L };
}
var O5 = typeof window < "u" ? o0 : ht, Mc = ({ theme: n, type: t, isLoading: e, ...i }) => ce.createElement("svg", { viewBox: "0 0 24 24", width: "100%", height: "100%", fill: n === "colored" ? "currentColor" : `var(--toastify-icon-color-${t})`, ...i });
function V5(n) {
  return ce.createElement(Mc, { ...n }, ce.createElement("path", { d: "M23.32 17.191L15.438 2.184C14.728.833 13.416 0 11.996 0c-1.42 0-2.733.833-3.443 2.184L.533 17.448a4.744 4.744 0 000 4.368C1.243 23.167 2.555 24 3.975 24h16.05C22.22 24 24 22.044 24 19.632c0-.904-.251-1.746-.68-2.44zm-9.622 1.46c0 1.033-.724 1.823-1.698 1.823s-1.698-.79-1.698-1.822v-.043c0-1.028.724-1.822 1.698-1.822s1.698.79 1.698 1.822v.043zm.039-12.285l-.84 8.06c-.057.581-.408.943-.897.943-.49 0-.84-.367-.896-.942l-.84-8.065c-.057-.624.25-1.095.779-1.095h1.91c.528.005.84.476.784 1.1z" }));
}
function L5(n) {
  return ce.createElement(Mc, { ...n }, ce.createElement("path", { d: "M12 0a12 12 0 1012 12A12.013 12.013 0 0012 0zm.25 5a1.5 1.5 0 11-1.5 1.5 1.5 1.5 0 011.5-1.5zm2.25 13.5h-4a1 1 0 010-2h.75a.25.25 0 00.25-.25v-4.5a.25.25 0 00-.25-.25h-.75a1 1 0 010-2h1a2 2 0 012 2v4.75a.25.25 0 00.25.25h.75a1 1 0 110 2z" }));
}
function B5(n) {
  return ce.createElement(Mc, { ...n }, ce.createElement("path", { d: "M12 0a12 12 0 1012 12A12.014 12.014 0 0012 0zm6.927 8.2l-6.845 9.289a1.011 1.011 0 01-1.43.188l-4.888-3.908a1 1 0 111.25-1.562l4.076 3.261 6.227-8.451a1 1 0 111.61 1.183z" }));
}
function M5(n) {
  return ce.createElement(Mc, { ...n }, ce.createElement("path", { d: "M11.983 0a12.206 12.206 0 00-8.51 3.653A11.8 11.8 0 000 12.207 11.779 11.779 0 0011.8 24h.214A12.111 12.111 0 0024 11.791 11.766 11.766 0 0011.983 0zM10.5 16.542a1.476 1.476 0 011.449-1.53h.027a1.527 1.527 0 011.523 1.47 1.475 1.475 0 01-1.449 1.53h-.027a1.529 1.529 0 01-1.523-1.47zM11 12.5v-6a1 1 0 012 0v6a1 1 0 11-2 0z" }));
}
function N5() {
  return ce.createElement("div", { className: "Toastify__spinner" });
}
var jd = { info: L5, warning: V5, success: B5, error: M5, spinner: N5 }, F5 = (n) => n in jd;
function D5({ theme: n, type: t, isLoading: e, icon: i }) {
  let s = null, r = { theme: n, type: t };
  return i === !1 || (zn(i) ? s = i({ ...r, isLoading: e }) : Pc(i) ? s = Kh(i, r) : e ? s = jd.spinner() : F5(t) && (s = jd[t](r))), s;
}
var $5 = (n) => {
  let { isRunning: t, preventExitTransition: e, toastRef: i, eventHandlers: s, playToast: r } = R5(n), { closeButton: o, children: a, autoClose: l, onClick: c, type: u, hideProgressBar: d, closeToast: h, transition: m, position: g, className: p, style: w, progressClassName: y, updateId: k, role: C, progress: E, rtl: T, toastId: L, deleteToast: O, isIn: N, isLoading: M, closeOnClick: J, theme: D, ariaLabel: B } = n, $ = Os("Toastify__toast", `Toastify__toast-theme--${D}`, `Toastify__toast--${u}`, { "Toastify__toast--rtl": T }, { "Toastify__toast--close-on-click": J }), W = zn(p) ? p({ rtl: T, position: g, type: u, defaultClassName: $ }) : Os($, p), Y = D5(n), ye = !!E || !l, Pe = { closeToast: h, type: u, theme: D }, Be = null;
  return o === !1 || (zn(o) ? Be = o(Pe) : Pc(o) ? Be = Kh(o, Pe) : Be = g5(Pe)), ce.createElement(m, { isIn: N, done: O, position: g, preventExitTransition: e, nodeRef: i, playToast: r }, ce.createElement("div", { id: L, tabIndex: 0, onClick: c, "data-in": N, className: W, ...s, style: w, ref: i, ...N && { role: C, "aria-label": B } }, Y != null && ce.createElement("div", { className: Os("Toastify__toast-icon", { "Toastify--animate-icon Toastify__zoom-enter": !M }) }, Y), A0(a, n, !t), Be, !n.customProgressBar && ce.createElement(p5, { ...k && !ye ? { key: `p-${k}` } : {}, rtl: T, theme: D, delay: l, isRunning: t, isIn: N, closeToast: h, hide: d, type: u, className: y, controlledProgress: ye, progress: E || 0 })));
}, L0 = (n, t = !1) => ({ enter: `Toastify--animate Toastify__${n}-enter`, exit: `Toastify--animate Toastify__${n}-exit`, appendPosition: t }), z5 = S0(L0("bounce", !0)), ig = S0(L0("slide", !0)), H5 = { position: "top-right", transition: z5, autoClose: 5e3, closeButton: !0, pauseOnHover: !0, pauseOnFocusLoss: !0, draggable: "touch", draggablePercent: 80, draggableDirection: "x", role: "alert", theme: "light", "aria-label": "Notifications Alt+T", hotKeys: (n) => n.altKey && n.code === "KeyT" };
function qZ(n) {
  let t = { ...H5, ...n }, e = n.stacked, [i, s] = zt(!0), r = yt(null), { getToastToRender: o, isToastActive: a, count: l } = P5(t), { className: c, style: u, rtl: d, containerId: h, hotKeys: m } = t;
  function g(w) {
    let y = Os("Toastify__toast-container", `Toastify__toast-container--${w}`, { "Toastify__toast-container--rtl": d });
    return zn(c) ? c({ position: w, rtl: d, defaultClassName: y }) : Os(y, $d(c));
  }
  function p() {
    e && (s(!0), Ye.play());
  }
  return O5(() => {
    var w;
    if (e) {
      let y = r.current.querySelectorAll('[data-in="true"]'), k = 12, C = (w = t.position) == null ? void 0 : w.includes("top"), E = 0, T = 0;
      Array.from(y).reverse().forEach((L, O) => {
        let N = L;
        N.classList.add("Toastify__toast--stacked"), O > 0 && (N.dataset.collapsed = `${i}`), N.dataset.pos || (N.dataset.pos = C ? "top" : "bot");
        let M = E * (i ? 0.2 : 1) + (i ? 0 : k * O);
        N.style.setProperty("--y", `${C ? M : M * -1}px`), N.style.setProperty("--g", `${k}`), N.style.setProperty("--s", `${1 - (i ? T : 0)}`), E += N.offsetHeight, T += 0.025;
      });
    }
  }, [i, l, e]), ht(() => {
    function w(y) {
      var k;
      let C = r.current;
      m(y) && ((k = C.querySelector('[tabIndex="0"]')) == null || k.focus(), s(!1), Ye.pause()), y.key === "Escape" && (document.activeElement === C || C != null && C.contains(document.activeElement)) && (s(!0), Ye.play());
    }
    return document.addEventListener("keydown", w), () => {
      document.removeEventListener("keydown", w);
    };
  }, [m]), ce.createElement("section", { ref: r, className: "Toastify", id: h, onMouseEnter: () => {
    e && (s(!1), Ye.pause());
  }, onMouseLeave: p, "aria-live": "polite", "aria-atomic": "false", "aria-relevant": "additions text", "aria-label": t["aria-label"] }, o((w, y) => {
    let k = y.length ? { ...u } : { ...u, pointerEvents: "none" };
    return ce.createElement("div", { tabIndex: -1, className: g(w), "data-stacked": e, style: k, key: `c-${w}` }, y.map(({ content: C, props: E }) => ce.createElement($5, { ...E, stacked: e, collapseAll: p, isIn: a(E.toastId, E.containerId), key: `t-${E.key}` }, C)));
  }));
}
class j5 {
  static success(t) {
    Ye.success(t, {
      hideProgressBar: !0,
      transition: ig,
      autoClose: 800,
      theme: "colored"
    });
  }
  static errors(t) {
    Ye.error(t, {
      theme: "colored",
      pauseOnHover: !1,
      hideProgressBar: !0,
      transition: ig,
      autoClose: 800
    });
  }
}
const U5 = "_loading_1o3yx_1", ng = {
  "infinite-scroll": "_infinite-scroll_1o3yx_1",
  loading: U5
};
class GZ extends ce.Component {
  constructor() {
    super(...arguments);
    f(this, "state", {
      loading: !1
    });
  }
  render() {
    return /* @__PURE__ */ v.jsx("div", { id: this.props.id, onScroll: async (e) => {
      if (this.props.handleScroll) {
        this.setState({ ...this.state, loading: !0 });
        let i = e.target;
        await this.props.handleScroll(Math.round(i.offsetHeight + i.scrollTop) >= i.scrollHeight - 1, e), this.setState({ loading: !1 });
      }
    }, className: `${ng["infinite-scroll"]} ${this.state.loading ? ng.loading : ""} ${this.props.className ?? "col"}`, style: this.props.style ?? { overflow: "hidden auto" }, children: this.props.children });
  }
}
const W5 = {
  "rating-container": "_rating-container_170xt_1"
}, q5 = () => window.crypto.randomUUID().replace(/-/g, "");
class KZ extends ce.Component {
  constructor() {
    super(...arguments);
    f(this, "state", {
      value: this.props.value ?? 0
    });
  }
  componentDidUpdate(e) {
    e.value !== this.props.value && this.setState({ value: this.props.value ?? 0 });
  }
  render() {
    return /* @__PURE__ */ v.jsx("div", { id: this.props.id, className: `row ${W5["rating-container"]} ${this.props.className ?? ""}`, style: this.props.style, children: Array.from({ length: 5 }).map((e, i) => {
      let s = "rating-star-grad-0", r = 0;
      return this.state.value >= 5 ? (s = "rating-star-grad-5", r = 100) : this.state.value >= i && (s = q5(), r = (this.state.value - i) * 100), /* @__PURE__ */ v.jsxs("svg", { onClick: () => {
        this.props.onChange && (this.setState({ value: i + 1 }), this.props.onChange(i + 1));
      }, width: "100%", height: "100%", style: { width: this.props.size ?? "2rem", height: this.props.size ?? "2rem" }, viewBox: "0 0 20 20", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: [
        /* @__PURE__ */ v.jsx("defs", { children: /* @__PURE__ */ v.jsxs("linearGradient", { id: s, x1: "0%", x2: "100%", y1: "0%", y2: "0%", children: [
          /* @__PURE__ */ v.jsx("stop", { offset: "0%", style: { stopColor: this.props.fillColor ?? "var(--secondary3-main-color,#FAAD1E)" } }),
          /* @__PURE__ */ v.jsx("stop", { offset: `${r}%`, style: { stopColor: this.props.fillColor ?? "var(--secondary3-main-color,#FAAD1E)" } }),
          /* @__PURE__ */ v.jsx("stop", { offset: `${r}%`, style: { stopColor: "#00000000" } })
        ] }) }),
        /* @__PURE__ */ v.jsx("path", { fillRule: "evenodd", clipRule: "evenodd", d: "M10 1.66667C10.2884 1.66667 10.5518 1.82993 10.6794 2.0878L12.844 6.46194L17.6847 7.16325C17.97 7.20459 18.2071 7.4039 18.2962 7.67736C18.3853 7.95082 18.311 8.25101 18.1045 8.45172L14.6018 11.8563L15.4285 16.6636C15.4772 16.947 15.3604 17.2334 15.127 17.4024C14.8937 17.5714 14.5844 17.5937 14.3292 17.4599L10 15.1897L5.67081 17.4599C5.41557 17.5937 5.10627 17.5714 4.87295 17.4024C4.63964 17.2334 4.52278 16.947 4.57151 16.6636L5.39815 11.8563L1.89545 8.45172C1.68896 8.25101 1.61465 7.95082 1.70377 7.67736C1.79288 7.4039 2.02996 7.20459 2.31533 7.16325L7.15599 6.46194L9.32063 2.0878C9.44825 1.82993 9.71162 1.66667 10 1.66667ZM10 4.12915L8.33846 7.48665C8.22811 7.70963 8.01479 7.86418 7.76802 7.89993L4.05223 8.43827L6.74094 11.0517C6.91947 11.2252 7.00094 11.4752 6.95881 11.7203L6.3243 15.4102L9.64738 13.6676C9.86813 13.5519 10.1319 13.5519 10.3526 13.6676L13.6757 15.4102L13.0412 11.7203C12.9991 11.4752 13.0805 11.2252 13.2591 11.0517L15.9478 8.43827L12.232 7.89993C11.9852 7.86418 11.7719 7.70963 11.6615 7.48665L10 4.12915Z", style: { fill: this.props.strokeColor ?? "var(--neutral-text-placeholder-color,#878792)" } }),
        /* @__PURE__ */ v.jsx("path", { d: "M17.738 7.18949L12.8212 6.47499L10.6249 2.02268C10.5611 1.91426 10.47 1.82438 10.3608 1.76194C10.2515 1.6995 10.1279 1.66666 10.0021 1.66666C9.87623 1.66666 9.75259 1.6995 9.64335 1.76194C9.53411 1.82438 9.44306 1.91426 9.37921 2.02268L7.17875 6.47499L2.26191 7.18949C2.13368 7.208 2.0132 7.26201 1.91406 7.34542C1.81493 7.42882 1.74111 7.5383 1.70095 7.66147C1.66078 7.78463 1.65588 7.91658 1.68678 8.04239C1.71769 8.1682 1.78317 8.28286 1.87583 8.3734L5.43449 11.8411L4.59499 16.7385C4.57311 16.8662 4.58739 16.9975 4.63622 17.1175C4.68505 17.2375 4.76648 17.3414 4.8713 17.4175C4.97612 17.4937 5.10016 17.539 5.22938 17.5483C5.3586 17.5577 5.48785 17.5306 5.60252 17.4704L9.99998 15.1588L14.3974 17.4704C14.5121 17.5306 14.6414 17.5577 14.7706 17.5483C14.8998 17.539 15.0238 17.4937 15.1286 17.4175C15.2335 17.3414 15.3149 17.2375 15.3637 17.1175C15.4126 16.9975 15.4268 16.8662 15.405 16.7385L14.5655 11.8411L18.1241 8.3734C18.2168 8.28295 18.2823 8.16841 18.3132 8.0427C18.3442 7.91699 18.3394 7.78512 18.2994 7.66199C18.2594 7.53886 18.1858 7.42937 18.0868 7.34587C17.9879 7.26238 17.8675 7.20822 17.7394 7.18949H17.738Z", fill: `url(#${s})` })
      ] }, "rate-" + i);
    }) });
  }
}
function ZZ(n) {
  const t = 30 - (n.strokeWidth ?? 4), e = Math.PI * 2 * t, i = (1 - (n.percent ?? 0) / 100) * e;
  return /* @__PURE__ */ v.jsxs("svg", { id: n.id, width: "100%", height: "100%", viewBox: "0 0 60 60", fill: "none", xmlns: "http://www.w3.org/2000/svg", style: { width: "6rem", height: "6rem", ...n.style ?? {} }, children: [
    /* @__PURE__ */ v.jsx("path", { d: `M 30,30 m 0,-${t} a ${t},${t} 0 1 1 0,${2 * t} a ${t},${t} 0 1 1 0,-${2 * t}`, style: { fill: "none", stroke: n.strokeColor ?? "var(--neutral-main-background-color)", strokeWidth: n.strokeWidth ?? "4px" } }),
    /* @__PURE__ */ v.jsx("path", { d: `M 30,30 m 0,-${t} a ${t},${t} 0 1 1 0,${2 * t} a ${t},${t} 0 1 1 0,-${2 * t}`, style: { fill: n.fillColor ?? "none", stroke: n.percentColor ?? "var(--primary-main-color)", strokeWidth: n.strokeWidth ?? "4px", strokeLinecap: "round", strokeDasharray: `${e}px ${e}px`, strokeDashoffset: `${i}px` } }),
    /* @__PURE__ */ v.jsxs("text", { x: "50%", y: "50%", dy: ".3em", textAnchor: "middle", fontSize: "1.6rem", fontWeight: "600", style: { fill: "var(neutral-text-title-color)", ...n.textStyle ?? {} }, children: [
      n.percent ?? 0,
      "%"
    ] })
  ] });
}
var Rl = { exports: {} }, Ja = { exports: {} }, Xa = { exports: {} }, qe = {};
/** @license React v16.13.1
 * react-is.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var sg;
function G5() {
  if (sg) return qe;
  sg = 1;
  var n = typeof Symbol == "function" && Symbol.for, t = n ? Symbol.for("react.element") : 60103, e = n ? Symbol.for("react.portal") : 60106, i = n ? Symbol.for("react.fragment") : 60107, s = n ? Symbol.for("react.strict_mode") : 60108, r = n ? Symbol.for("react.profiler") : 60114, o = n ? Symbol.for("react.provider") : 60109, a = n ? Symbol.for("react.context") : 60110, l = n ? Symbol.for("react.async_mode") : 60111, c = n ? Symbol.for("react.concurrent_mode") : 60111, u = n ? Symbol.for("react.forward_ref") : 60112, d = n ? Symbol.for("react.suspense") : 60113, h = n ? Symbol.for("react.suspense_list") : 60120, m = n ? Symbol.for("react.memo") : 60115, g = n ? Symbol.for("react.lazy") : 60116, p = n ? Symbol.for("react.block") : 60121, w = n ? Symbol.for("react.fundamental") : 60117, y = n ? Symbol.for("react.responder") : 60118, k = n ? Symbol.for("react.scope") : 60119;
  function C(T) {
    if (typeof T == "object" && T !== null) {
      var L = T.$$typeof;
      switch (L) {
        case t:
          switch (T = T.type, T) {
            case l:
            case c:
            case i:
            case r:
            case s:
            case d:
              return T;
            default:
              switch (T = T && T.$$typeof, T) {
                case a:
                case u:
                case g:
                case m:
                case o:
                  return T;
                default:
                  return L;
              }
          }
        case e:
          return L;
      }
    }
  }
  function E(T) {
    return C(T) === c;
  }
  return qe.AsyncMode = l, qe.ConcurrentMode = c, qe.ContextConsumer = a, qe.ContextProvider = o, qe.Element = t, qe.ForwardRef = u, qe.Fragment = i, qe.Lazy = g, qe.Memo = m, qe.Portal = e, qe.Profiler = r, qe.StrictMode = s, qe.Suspense = d, qe.isAsyncMode = function(T) {
    return E(T) || C(T) === l;
  }, qe.isConcurrentMode = E, qe.isContextConsumer = function(T) {
    return C(T) === a;
  }, qe.isContextProvider = function(T) {
    return C(T) === o;
  }, qe.isElement = function(T) {
    return typeof T == "object" && T !== null && T.$$typeof === t;
  }, qe.isForwardRef = function(T) {
    return C(T) === u;
  }, qe.isFragment = function(T) {
    return C(T) === i;
  }, qe.isLazy = function(T) {
    return C(T) === g;
  }, qe.isMemo = function(T) {
    return C(T) === m;
  }, qe.isPortal = function(T) {
    return C(T) === e;
  }, qe.isProfiler = function(T) {
    return C(T) === r;
  }, qe.isStrictMode = function(T) {
    return C(T) === s;
  }, qe.isSuspense = function(T) {
    return C(T) === d;
  }, qe.isValidElementType = function(T) {
    return typeof T == "string" || typeof T == "function" || T === i || T === c || T === r || T === s || T === d || T === h || typeof T == "object" && T !== null && (T.$$typeof === g || T.$$typeof === m || T.$$typeof === o || T.$$typeof === a || T.$$typeof === u || T.$$typeof === w || T.$$typeof === y || T.$$typeof === k || T.$$typeof === p);
  }, qe.typeOf = C, qe;
}
var Ge = {};
/** @license React v16.13.1
 * react-is.development.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var rg;
function K5() {
  return rg || (rg = 1, process.env.NODE_ENV !== "production" && function() {
    var n = typeof Symbol == "function" && Symbol.for, t = n ? Symbol.for("react.element") : 60103, e = n ? Symbol.for("react.portal") : 60106, i = n ? Symbol.for("react.fragment") : 60107, s = n ? Symbol.for("react.strict_mode") : 60108, r = n ? Symbol.for("react.profiler") : 60114, o = n ? Symbol.for("react.provider") : 60109, a = n ? Symbol.for("react.context") : 60110, l = n ? Symbol.for("react.async_mode") : 60111, c = n ? Symbol.for("react.concurrent_mode") : 60111, u = n ? Symbol.for("react.forward_ref") : 60112, d = n ? Symbol.for("react.suspense") : 60113, h = n ? Symbol.for("react.suspense_list") : 60120, m = n ? Symbol.for("react.memo") : 60115, g = n ? Symbol.for("react.lazy") : 60116, p = n ? Symbol.for("react.block") : 60121, w = n ? Symbol.for("react.fundamental") : 60117, y = n ? Symbol.for("react.responder") : 60118, k = n ? Symbol.for("react.scope") : 60119;
    function C(se) {
      return typeof se == "string" || typeof se == "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
      se === i || se === c || se === r || se === s || se === d || se === h || typeof se == "object" && se !== null && (se.$$typeof === g || se.$$typeof === m || se.$$typeof === o || se.$$typeof === a || se.$$typeof === u || se.$$typeof === w || se.$$typeof === y || se.$$typeof === k || se.$$typeof === p);
    }
    function E(se) {
      if (typeof se == "object" && se !== null) {
        var Qe = se.$$typeof;
        switch (Qe) {
          case t:
            var q = se.type;
            switch (q) {
              case l:
              case c:
              case i:
              case r:
              case s:
              case d:
                return q;
              default:
                var U = q && q.$$typeof;
                switch (U) {
                  case a:
                  case u:
                  case g:
                  case m:
                  case o:
                    return U;
                  default:
                    return Qe;
                }
            }
          case e:
            return Qe;
        }
      }
    }
    var T = l, L = c, O = a, N = o, M = t, J = u, D = i, B = g, $ = m, W = e, Y = r, ye = s, Pe = d, Be = !1;
    function te(se) {
      return Be || (Be = !0, console.warn("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.")), R(se) || E(se) === l;
    }
    function R(se) {
      return E(se) === c;
    }
    function K(se) {
      return E(se) === a;
    }
    function X(se) {
      return E(se) === o;
    }
    function ie(se) {
      return typeof se == "object" && se !== null && se.$$typeof === t;
    }
    function oe(se) {
      return E(se) === u;
    }
    function de(se) {
      return E(se) === i;
    }
    function pe(se) {
      return E(se) === g;
    }
    function ve(se) {
      return E(se) === m;
    }
    function _e(se) {
      return E(se) === e;
    }
    function Ee(se) {
      return E(se) === r;
    }
    function we(se) {
      return E(se) === s;
    }
    function Ne(se) {
      return E(se) === d;
    }
    Ge.AsyncMode = T, Ge.ConcurrentMode = L, Ge.ContextConsumer = O, Ge.ContextProvider = N, Ge.Element = M, Ge.ForwardRef = J, Ge.Fragment = D, Ge.Lazy = B, Ge.Memo = $, Ge.Portal = W, Ge.Profiler = Y, Ge.StrictMode = ye, Ge.Suspense = Pe, Ge.isAsyncMode = te, Ge.isConcurrentMode = R, Ge.isContextConsumer = K, Ge.isContextProvider = X, Ge.isElement = ie, Ge.isForwardRef = oe, Ge.isFragment = de, Ge.isLazy = pe, Ge.isMemo = ve, Ge.isPortal = _e, Ge.isProfiler = Ee, Ge.isStrictMode = we, Ge.isSuspense = Ne, Ge.isValidElementType = C, Ge.typeOf = E;
  }()), Ge;
}
var og;
function B0() {
  return og || (og = 1, process.env.NODE_ENV === "production" ? Xa.exports = G5() : Xa.exports = K5()), Xa.exports;
}
/*
object-assign
(c) Sindre Sorhus
@license MIT
*/
var _u, ag;
function Z5() {
  if (ag) return _u;
  ag = 1;
  var n = Object.getOwnPropertySymbols, t = Object.prototype.hasOwnProperty, e = Object.prototype.propertyIsEnumerable;
  function i(r) {
    if (r == null)
      throw new TypeError("Object.assign cannot be called with null or undefined");
    return Object(r);
  }
  function s() {
    try {
      if (!Object.assign)
        return !1;
      var r = new String("abc");
      if (r[5] = "de", Object.getOwnPropertyNames(r)[0] === "5")
        return !1;
      for (var o = {}, a = 0; a < 10; a++)
        o["_" + String.fromCharCode(a)] = a;
      var l = Object.getOwnPropertyNames(o).map(function(u) {
        return o[u];
      });
      if (l.join("") !== "0123456789")
        return !1;
      var c = {};
      return "abcdefghijklmnopqrst".split("").forEach(function(u) {
        c[u] = u;
      }), Object.keys(Object.assign({}, c)).join("") === "abcdefghijklmnopqrst";
    } catch {
      return !1;
    }
  }
  return _u = s() ? Object.assign : function(r, o) {
    for (var a, l = i(r), c, u = 1; u < arguments.length; u++) {
      a = Object(arguments[u]);
      for (var d in a)
        t.call(a, d) && (l[d] = a[d]);
      if (n) {
        c = n(a);
        for (var h = 0; h < c.length; h++)
          e.call(a, c[h]) && (l[c[h]] = a[c[h]]);
      }
    }
    return l;
  }, _u;
}
var yu, lg;
function tf() {
  if (lg) return yu;
  lg = 1;
  var n = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
  return yu = n, yu;
}
var ku, cg;
function M0() {
  return cg || (cg = 1, ku = Function.call.bind(Object.prototype.hasOwnProperty)), ku;
}
var Cu, ug;
function Y5() {
  if (ug) return Cu;
  ug = 1;
  var n = function() {
  };
  if (process.env.NODE_ENV !== "production") {
    var t = /* @__PURE__ */ tf(), e = {}, i = /* @__PURE__ */ M0();
    n = function(r) {
      var o = "Warning: " + r;
      typeof console < "u" && console.error(o);
      try {
        throw new Error(o);
      } catch {
      }
    };
  }
  function s(r, o, a, l, c) {
    if (process.env.NODE_ENV !== "production") {
      for (var u in r)
        if (i(r, u)) {
          var d;
          try {
            if (typeof r[u] != "function") {
              var h = Error(
                (l || "React class") + ": " + a + " type `" + u + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof r[u] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`."
              );
              throw h.name = "Invariant Violation", h;
            }
            d = r[u](o, u, l, a, null, t);
          } catch (g) {
            d = g;
          }
          if (d && !(d instanceof Error) && n(
            (l || "React class") + ": type specification of " + a + " `" + u + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof d + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument)."
          ), d instanceof Error && !(d.message in e)) {
            e[d.message] = !0;
            var m = c ? c() : "";
            n(
              "Failed " + a + " type: " + d.message + (m ?? "")
            );
          }
        }
    }
  }
  return s.resetWarningCache = function() {
    process.env.NODE_ENV !== "production" && (e = {});
  }, Cu = s, Cu;
}
var xu, dg;
function J5() {
  if (dg) return xu;
  dg = 1;
  var n = B0(), t = Z5(), e = /* @__PURE__ */ tf(), i = /* @__PURE__ */ M0(), s = /* @__PURE__ */ Y5(), r = function() {
  };
  process.env.NODE_ENV !== "production" && (r = function(a) {
    var l = "Warning: " + a;
    typeof console < "u" && console.error(l);
    try {
      throw new Error(l);
    } catch {
    }
  });
  function o() {
    return null;
  }
  return xu = function(a, l) {
    var c = typeof Symbol == "function" && Symbol.iterator, u = "@@iterator";
    function d(R) {
      var K = R && (c && R[c] || R[u]);
      if (typeof K == "function")
        return K;
    }
    var h = "<<anonymous>>", m = {
      array: y("array"),
      bigint: y("bigint"),
      bool: y("boolean"),
      func: y("function"),
      number: y("number"),
      object: y("object"),
      string: y("string"),
      symbol: y("symbol"),
      any: k(),
      arrayOf: C,
      element: E(),
      elementType: T(),
      instanceOf: L,
      node: J(),
      objectOf: N,
      oneOf: O,
      oneOfType: M,
      shape: B,
      exact: $
    };
    function g(R, K) {
      return R === K ? R !== 0 || 1 / R === 1 / K : R !== R && K !== K;
    }
    function p(R, K) {
      this.message = R, this.data = K && typeof K == "object" ? K : {}, this.stack = "";
    }
    p.prototype = Error.prototype;
    function w(R) {
      if (process.env.NODE_ENV !== "production")
        var K = {}, X = 0;
      function ie(de, pe, ve, _e, Ee, we, Ne) {
        if (_e = _e || h, we = we || ve, Ne !== e) {
          if (l) {
            var se = new Error(
              "Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types"
            );
            throw se.name = "Invariant Violation", se;
          } else if (process.env.NODE_ENV !== "production" && typeof console < "u") {
            var Qe = _e + ":" + ve;
            !K[Qe] && // Avoid spamming the console because they are often not actionable except for lib authors
            X < 3 && (r(
              "You are manually calling a React.PropTypes validation function for the `" + we + "` prop on `" + _e + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details."
            ), K[Qe] = !0, X++);
          }
        }
        return pe[ve] == null ? de ? pe[ve] === null ? new p("The " + Ee + " `" + we + "` is marked as required " + ("in `" + _e + "`, but its value is `null`.")) : new p("The " + Ee + " `" + we + "` is marked as required in " + ("`" + _e + "`, but its value is `undefined`.")) : null : R(pe, ve, _e, Ee, we);
      }
      var oe = ie.bind(null, !1);
      return oe.isRequired = ie.bind(null, !0), oe;
    }
    function y(R) {
      function K(X, ie, oe, de, pe, ve) {
        var _e = X[ie], Ee = ye(_e);
        if (Ee !== R) {
          var we = Pe(_e);
          return new p(
            "Invalid " + de + " `" + pe + "` of type " + ("`" + we + "` supplied to `" + oe + "`, expected ") + ("`" + R + "`."),
            { expectedType: R }
          );
        }
        return null;
      }
      return w(K);
    }
    function k() {
      return w(o);
    }
    function C(R) {
      function K(X, ie, oe, de, pe) {
        if (typeof R != "function")
          return new p("Property `" + pe + "` of component `" + oe + "` has invalid PropType notation inside arrayOf.");
        var ve = X[ie];
        if (!Array.isArray(ve)) {
          var _e = ye(ve);
          return new p("Invalid " + de + " `" + pe + "` of type " + ("`" + _e + "` supplied to `" + oe + "`, expected an array."));
        }
        for (var Ee = 0; Ee < ve.length; Ee++) {
          var we = R(ve, Ee, oe, de, pe + "[" + Ee + "]", e);
          if (we instanceof Error)
            return we;
        }
        return null;
      }
      return w(K);
    }
    function E() {
      function R(K, X, ie, oe, de) {
        var pe = K[X];
        if (!a(pe)) {
          var ve = ye(pe);
          return new p("Invalid " + oe + " `" + de + "` of type " + ("`" + ve + "` supplied to `" + ie + "`, expected a single ReactElement."));
        }
        return null;
      }
      return w(R);
    }
    function T() {
      function R(K, X, ie, oe, de) {
        var pe = K[X];
        if (!n.isValidElementType(pe)) {
          var ve = ye(pe);
          return new p("Invalid " + oe + " `" + de + "` of type " + ("`" + ve + "` supplied to `" + ie + "`, expected a single ReactElement type."));
        }
        return null;
      }
      return w(R);
    }
    function L(R) {
      function K(X, ie, oe, de, pe) {
        if (!(X[ie] instanceof R)) {
          var ve = R.name || h, _e = te(X[ie]);
          return new p("Invalid " + de + " `" + pe + "` of type " + ("`" + _e + "` supplied to `" + oe + "`, expected ") + ("instance of `" + ve + "`."));
        }
        return null;
      }
      return w(K);
    }
    function O(R) {
      if (!Array.isArray(R))
        return process.env.NODE_ENV !== "production" && (arguments.length > 1 ? r(
          "Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z])."
        ) : r("Invalid argument supplied to oneOf, expected an array.")), o;
      function K(X, ie, oe, de, pe) {
        for (var ve = X[ie], _e = 0; _e < R.length; _e++)
          if (g(ve, R[_e]))
            return null;
        var Ee = JSON.stringify(R, function(Ne, se) {
          var Qe = Pe(se);
          return Qe === "symbol" ? String(se) : se;
        });
        return new p("Invalid " + de + " `" + pe + "` of value `" + String(ve) + "` " + ("supplied to `" + oe + "`, expected one of " + Ee + "."));
      }
      return w(K);
    }
    function N(R) {
      function K(X, ie, oe, de, pe) {
        if (typeof R != "function")
          return new p("Property `" + pe + "` of component `" + oe + "` has invalid PropType notation inside objectOf.");
        var ve = X[ie], _e = ye(ve);
        if (_e !== "object")
          return new p("Invalid " + de + " `" + pe + "` of type " + ("`" + _e + "` supplied to `" + oe + "`, expected an object."));
        for (var Ee in ve)
          if (i(ve, Ee)) {
            var we = R(ve, Ee, oe, de, pe + "." + Ee, e);
            if (we instanceof Error)
              return we;
          }
        return null;
      }
      return w(K);
    }
    function M(R) {
      if (!Array.isArray(R))
        return process.env.NODE_ENV !== "production" && r("Invalid argument supplied to oneOfType, expected an instance of array."), o;
      for (var K = 0; K < R.length; K++) {
        var X = R[K];
        if (typeof X != "function")
          return r(
            "Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + Be(X) + " at index " + K + "."
          ), o;
      }
      function ie(oe, de, pe, ve, _e) {
        for (var Ee = [], we = 0; we < R.length; we++) {
          var Ne = R[we], se = Ne(oe, de, pe, ve, _e, e);
          if (se == null)
            return null;
          se.data && i(se.data, "expectedType") && Ee.push(se.data.expectedType);
        }
        var Qe = Ee.length > 0 ? ", expected one of type [" + Ee.join(", ") + "]" : "";
        return new p("Invalid " + ve + " `" + _e + "` supplied to " + ("`" + pe + "`" + Qe + "."));
      }
      return w(ie);
    }
    function J() {
      function R(K, X, ie, oe, de) {
        return W(K[X]) ? null : new p("Invalid " + oe + " `" + de + "` supplied to " + ("`" + ie + "`, expected a ReactNode."));
      }
      return w(R);
    }
    function D(R, K, X, ie, oe) {
      return new p(
        (R || "React class") + ": " + K + " type `" + X + "." + ie + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + oe + "`."
      );
    }
    function B(R) {
      function K(X, ie, oe, de, pe) {
        var ve = X[ie], _e = ye(ve);
        if (_e !== "object")
          return new p("Invalid " + de + " `" + pe + "` of type `" + _e + "` " + ("supplied to `" + oe + "`, expected `object`."));
        for (var Ee in R) {
          var we = R[Ee];
          if (typeof we != "function")
            return D(oe, de, pe, Ee, Pe(we));
          var Ne = we(ve, Ee, oe, de, pe + "." + Ee, e);
          if (Ne)
            return Ne;
        }
        return null;
      }
      return w(K);
    }
    function $(R) {
      function K(X, ie, oe, de, pe) {
        var ve = X[ie], _e = ye(ve);
        if (_e !== "object")
          return new p("Invalid " + de + " `" + pe + "` of type `" + _e + "` " + ("supplied to `" + oe + "`, expected `object`."));
        var Ee = t({}, X[ie], R);
        for (var we in Ee) {
          var Ne = R[we];
          if (i(R, we) && typeof Ne != "function")
            return D(oe, de, pe, we, Pe(Ne));
          if (!Ne)
            return new p(
              "Invalid " + de + " `" + pe + "` key `" + we + "` supplied to `" + oe + "`.\nBad object: " + JSON.stringify(X[ie], null, "  ") + `
Valid keys: ` + JSON.stringify(Object.keys(R), null, "  ")
            );
          var se = Ne(ve, we, oe, de, pe + "." + we, e);
          if (se)
            return se;
        }
        return null;
      }
      return w(K);
    }
    function W(R) {
      switch (typeof R) {
        case "number":
        case "string":
        case "undefined":
          return !0;
        case "boolean":
          return !R;
        case "object":
          if (Array.isArray(R))
            return R.every(W);
          if (R === null || a(R))
            return !0;
          var K = d(R);
          if (K) {
            var X = K.call(R), ie;
            if (K !== R.entries) {
              for (; !(ie = X.next()).done; )
                if (!W(ie.value))
                  return !1;
            } else
              for (; !(ie = X.next()).done; ) {
                var oe = ie.value;
                if (oe && !W(oe[1]))
                  return !1;
              }
          } else
            return !1;
          return !0;
        default:
          return !1;
      }
    }
    function Y(R, K) {
      return R === "symbol" ? !0 : K ? K["@@toStringTag"] === "Symbol" || typeof Symbol == "function" && K instanceof Symbol : !1;
    }
    function ye(R) {
      var K = typeof R;
      return Array.isArray(R) ? "array" : R instanceof RegExp ? "object" : Y(K, R) ? "symbol" : K;
    }
    function Pe(R) {
      if (typeof R > "u" || R === null)
        return "" + R;
      var K = ye(R);
      if (K === "object") {
        if (R instanceof Date)
          return "date";
        if (R instanceof RegExp)
          return "regexp";
      }
      return K;
    }
    function Be(R) {
      var K = Pe(R);
      switch (K) {
        case "array":
        case "object":
          return "an " + K;
        case "boolean":
        case "date":
        case "regexp":
          return "a " + K;
        default:
          return K;
      }
    }
    function te(R) {
      return !R.constructor || !R.constructor.name ? h : R.constructor.name;
    }
    return m.checkPropTypes = s, m.resetWarningCache = s.resetWarningCache, m.PropTypes = m, m;
  }, xu;
}
var Tu, hg;
function X5() {
  if (hg) return Tu;
  hg = 1;
  var n = /* @__PURE__ */ tf();
  function t() {
  }
  function e() {
  }
  return e.resetWarningCache = t, Tu = function() {
    function i(o, a, l, c, u, d) {
      if (d !== n) {
        var h = new Error(
          "Calling PropTypes validators directly is not supported by the `prop-types` package. Use PropTypes.checkPropTypes() to call them. Read more at http://fb.me/use-check-prop-types"
        );
        throw h.name = "Invariant Violation", h;
      }
    }
    i.isRequired = i;
    function s() {
      return i;
    }
    var r = {
      array: i,
      bigint: i,
      bool: i,
      func: i,
      number: i,
      object: i,
      string: i,
      symbol: i,
      any: i,
      arrayOf: s,
      element: i,
      elementType: i,
      instanceOf: s,
      node: i,
      objectOf: s,
      oneOf: s,
      oneOfType: s,
      shape: s,
      exact: s,
      checkPropTypes: e,
      resetWarningCache: t
    };
    return r.PropTypes = r, r;
  }, Tu;
}
var fg;
function Q5() {
  if (fg) return Ja.exports;
  if (fg = 1, process.env.NODE_ENV !== "production") {
    var n = B0(), t = !0;
    Ja.exports = /* @__PURE__ */ J5()(n.isElement, t);
  } else
    Ja.exports = /* @__PURE__ */ X5()();
  return Ja.exports;
}
var eT = Rl.exports, mg;
function tT() {
  return mg || (mg = 1, function(n, t) {
    (function(i, s) {
      n.exports = s(/* @__PURE__ */ Q5(), ce);
    })(eT, function(e, i) {
      return function(s) {
        var r = {};
        function o(a) {
          if (r[a]) return r[a].exports;
          var l = r[a] = { i: a, l: !1, exports: {} };
          return s[a].call(l.exports, l, l.exports, o), l.l = !0, l.exports;
        }
        return o.m = s, o.c = r, o.d = function(a, l, c) {
          o.o(a, l) || Object.defineProperty(a, l, { enumerable: !0, get: c });
        }, o.r = function(a) {
          typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(a, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(a, "__esModule", { value: !0 });
        }, o.t = function(a, l) {
          if (1 & l && (a = o(a)), 8 & l || 4 & l && typeof a == "object" && a && a.__esModule) return a;
          var c = /* @__PURE__ */ Object.create(null);
          if (o.r(c), Object.defineProperty(c, "default", { enumerable: !0, value: a }), 2 & l && typeof a != "string") for (var u in a) o.d(c, u, (function(d) {
            return a[d];
          }).bind(null, u));
          return c;
        }, o.n = function(a) {
          var l = a && a.__esModule ? function() {
            return a.default;
          } : function() {
            return a;
          };
          return o.d(l, "a", l), l;
        }, o.o = function(a, l) {
          return Object.prototype.hasOwnProperty.call(a, l);
        }, o.p = "", o(o.s = 5);
      }([function(s, r, o) {
        function a(d, h) {
          for (var m = 0; m < h.length; m++) {
            var g = h[m];
            g.enumerable = g.enumerable || !1, g.configurable = !0, "value" in g && (g.writable = !0), Object.defineProperty(d, g.key, g);
          }
        }
        o.d(r, "a", function() {
          return l;
        }), o.d(r, "b", function() {
          return c;
        }), o.d(r, "c", function() {
          return u;
        });
        var l = function() {
          function d() {
            (function(m, g) {
              if (!(m instanceof g)) throw new TypeError("Cannot call a class as a function");
            })(this, d), typeof window < "u" && (this.image = new Image(), this.resolve = null, this.video = document.createElement("video"), this.events());
          }
          return function(m, g, p) {
            return g && a(m.prototype, g), m;
          }(d, [{ key: "events", value: function() {
            var m = this;
            this.video.addEventListener("loadeddata", function() {
              return m.resolve && m.resolve(!0);
            }), this.video.addEventListener("loadeddata", function() {
              return m.resolve && m.resolve(!1);
            }), this.image.onload = function() {
              return m.resolve && m.resolve(!0);
            }, this.image.onerror = function() {
              return m.resolve && m.resolve(!1);
            };
          } }, { key: "load", value: function(m) {
            var g = this;
            return new Promise(function(p) {
              m || p(!0), g.resolve = p, g.loading = !0, g.ended = !1, m.match(/\.(mp4|webm)/i) && g.video.setAttribute("src", m), m.match(/\.(png|jp(e)?g|gif|webp)/i) && (g.image.src = m, (g.image.width > 0 || g.image.height > 0) && p(!0));
            });
          } }, { key: "loadImage", value: function(m) {
            var g = this, p = new Image(), w = !1;
            p.onload = function() {
              w || g.pumpLoaded();
            }, p.onerror = function() {
              w || g.pumpLoaded();
            }, p.src = m, w === !1 && (p.width > 0 || p.height > 0) && (w = !0, this.pumpLoaded());
          } }, { key: "loadVideo", value: function(m) {
            var g = this, p = document.createElement("video");
            p.addEventListener("loadeddata", function() {
              g.pumpLoaded();
            }), p.addEventListener("error", function() {
              g.pumpLoaded();
            }), p.setAttribute("src", m);
          } }, { key: "pumpLoaded", value: function() {
            this.loaded += 1, this.loaded === this.toLoad && this.resolver(!0);
          } }, { key: "startLoad", value: function(m) {
            m.match(/\.(mp4|webm)/i) && this.loadVideo(m), m.match(/\.(png|jp(e)?g|gif|webp)/i) && this.loadImage(m);
          } }, { key: "loadMultiple", value: function(m) {
            var g = this;
            return this.loaded = 0, this.toLoad = m.length, new Promise(function(p) {
              g.resolver = p, m.forEach(function(w) {
                g.startLoad(w);
              });
            });
          } }]), d;
        }();
        function c() {
          var d = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : [], h = arguments.length > 1 ? arguments[1] : void 0;
          if (!h) return d.join(" ").trim();
          for (var m = [], g = d.length; g--; ) h[d[g]] && m.push(h[d[g]]);
          return m;
        }
        function u() {
          var d = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "", h = arguments.length > 1 ? arguments[1] : void 0;
          return h && h[d] || d;
        }
      }, function(s, r, o) {
        o.d(r, "a", function() {
          return h;
        }), o.d(r, "b", function() {
          return m;
        }), o.d(r, "c", function() {
          return g;
        }), o.d(r, "e", function() {
          return p;
        }), o.d(r, "d", function() {
          return w;
        }), o.d(r, "g", function() {
          return y;
        }), o.d(r, "f", function() {
          return k;
        });
        var a = o(0);
        function l(C) {
          return function(T) {
            if (Array.isArray(T)) {
              for (var L = 0, O = new Array(T.length); L < T.length; L++) O[L] = T[L];
              return O;
            }
          }(C) || function(T) {
            if (Symbol.iterator in Object(T) || Object.prototype.toString.call(T) === "[object Arguments]") return Array.from(T);
          }(C) || function() {
            throw new TypeError("Invalid attempt to spread non-iterable instance");
          }();
        }
        function c(C, E) {
          var T = Object.keys(C);
          if (Object.getOwnPropertySymbols) {
            var L = Object.getOwnPropertySymbols(C);
            E && (L = L.filter(function(O) {
              return Object.getOwnPropertyDescriptor(C, O).enumerable;
            })), T.push.apply(T, L);
          }
          return T;
        }
        function u(C) {
          for (var E = 1; E < arguments.length; E++) {
            var T = arguments[E] != null ? arguments[E] : {};
            E % 2 ? c(Object(T), !0).forEach(function(L) {
              d(C, L, T[L]);
            }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(C, Object.getOwnPropertyDescriptors(T)) : c(Object(T)).forEach(function(L) {
              Object.defineProperty(C, L, Object.getOwnPropertyDescriptor(T, L));
            });
          }
          return C;
        }
        function d(C, E, T) {
          return E in C ? Object.defineProperty(C, E, { value: T, enumerable: !0, configurable: !0, writable: !0 }) : C[E] = T, C;
        }
        var h = function(E, T) {
          typeof T == "string" && E && T.split(" ").forEach(function(L) {
            E.classList.add(L);
          });
        }, m = function(E, T) {
          typeof T == "string" && E && T.split(" ").forEach(function(L) {
            E.classList.remove(L);
          });
        }, g = function(E) {
          return typeof E == "string" && E.split(" ")[0] || "";
        }, p = function(E) {
          var T = Array.isArray(E) ? E : [E];
          if (T.length === 1) return T[0];
          for (var L = u({}, T[0]), O = 1; O < T.length; O += 1) {
            for (var N in L) T[O][N] && (L[N] = [L[N], T[O][N]].join(" "));
            for (var M in T[O]) L[M] || (L[M] = T[O][M]);
          }
          return L;
        };
        function w(C) {
          var E, T = C.rootElement, L = C.cssModule, O = C.disabled, N = C.organicArrows, M = C.className, J = C.total, D = C.current, B = C.infinite, $ = C.animation, W = C.fillParent, Y = [T];
          return $ && Y.push("".concat(T, "--").concat($)), N === !0 && Y.push("".concat(T, "--organic-arrows")), O === !0 && Y.push("".concat(T, "--disabled")), W && Y.push("".concat(T, "--fill-parent")), B === !1 && (D === 0 && Y.push("".concat(T, "--first")), D === J - 1 && Y.push("".concat(T, "--last"))), L && L[T] && (Y = Object(a.b)(Y, L)), M && (E = Y).push.apply(E, l(M.split(" "))), Y.join(" ").trim().replace(/[\s]+/gi, " ");
        }
        function y(C) {
          var E = [];
          return (C.constructor === Array ? C : [C]).forEach(function(T) {
            var L = u({}, T.props);
            T.props["data-src"] && (L.source = T.props["data-src"]), T.props["data-slug"] && (L.slug = T.props["data-slug"]), E.push(L);
          }), E;
        }
        function k(C, E) {
          return { boxA: Object(a.c)("".concat(C, "__boxA"), E), boxB: Object(a.c)("".concat(C, "__boxB"), E), box: Object(a.c)("".concat(C, "__box"), E), container: Object(a.c)("".concat(C, "__container"), E), wrapper: Object(a.c)("".concat(C, "__wrapper"), E), bar: Object(a.c)("".concat(C, "__bar"), E), barActive: Object(a.c)("".concat(C, "__bar--active"), E), barEnd: Object(a.c)("".concat(C, "__bar--end"), E), content: Object(a.c)("".concat(C, "__content"), E), contentStatic: Object(a.c)("".concat(C, "__content--static"), E), contentMoveLeft: Object(a.c)("".concat(C, "__content--moveLeft"), E), contentMoveRight: Object(a.c)("".concat(C, "__content--moveRight"), E), controlsHidden: Object(a.c)("".concat(C, "__controls--hidden"), E), controlsActive: Object(a.c)("".concat(C, "__controls--active"), E), animated: Object(a.c)("".concat(C, "--animated"), E), animatedMobile: Object(a.c)("".concat(C, "--animated-mobile"), E), contentExit: Object(a.c)("".concat(C, "__content--exit"), E), exit: Object(a.c)("".concat(C, "--exit"), E), active: Object(a.c)("".concat(C, "--active"), E), moveLeft: Object(a.c)("".concat(C, "--moveLeft"), E), moveRight: Object(a.c)("".concat(C, "--moveRight"), E), startUp: Object(a.c)("".concat(C, "__startUp"), E), bulletsLoading: Object(a.c)("".concat(C, "__bullets--loading"), E) };
        }
      }, function(s, r) {
        s.exports = e;
      }, function(s, r) {
        s.exports = i;
      }, function(s, r, o) {
        s.exports = function(a) {
          var l = {};
          function c(u) {
            if (l[u]) return l[u].exports;
            var d = l[u] = { i: u, l: !1, exports: {} };
            return a[u].call(d.exports, d, d.exports, c), d.l = !0, d.exports;
          }
          return c.m = a, c.c = l, c.d = function(u, d, h) {
            c.o(u, d) || Object.defineProperty(u, d, { enumerable: !0, get: h });
          }, c.r = function(u) {
            typeof Symbol < "u" && Symbol.toStringTag && Object.defineProperty(u, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(u, "__esModule", { value: !0 });
          }, c.t = function(u, d) {
            if (1 & d && (u = c(u)), 8 & d || 4 & d && typeof u == "object" && u && u.__esModule) return u;
            var h = /* @__PURE__ */ Object.create(null);
            if (c.r(h), Object.defineProperty(h, "default", { enumerable: !0, value: u }), 2 & d && typeof u != "string") for (var m in u) c.d(h, m, (function(g) {
              return u[g];
            }).bind(null, m));
            return h;
          }, c.n = function(u) {
            var d = u && u.__esModule ? function() {
              return u.default;
            } : function() {
              return u;
            };
            return c.d(d, "a", d), d;
          }, c.o = function(u, d) {
            return Object.prototype.hasOwnProperty.call(u, d);
          }, c.p = "", c(c.s = 0);
        }([function(a, l, c) {
          function u(h, m) {
            var g = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, p = g.tolerance, w = p === void 0 ? 0 : p, y = g.propertyName;
            return new Promise(function(k) {
              if (h) {
                var C = null, E = m.charAt(0).toUpperCase() + m.slice(1), T = 0;
                h.style["Webkit" + E] !== void 0 && (C = "webkit" + E + "End"), h.style.OTransition !== void 0 && (C = "o" + m + "End"), h.style[m] !== void 0 && (C = m + "end"), h.clearCssEndEvent && h.clearCssEndEvent(), h.clearCssEndEvent = function() {
                  h.removeEventListener(C, L);
                }, h.addEventListener(C, L);
              } else k(!1);
              function L(O) {
                if ((O.srcElement || O.target) === h) {
                  if (T >= w) {
                    if (y && y !== O.propertyName) return;
                    h.removeEventListener(C, L), k(O);
                  }
                  T += 1;
                }
              }
            });
          }
          function d(h) {
            window && window.requestAnimationFrame(function() {
              window.requestAnimationFrame(h);
            });
          }
          Object.defineProperty(l, "__esModule", { value: !0 }), l.setCssEndEvent = u, l.beforeCssLayout = function(h) {
            window && window.requestAnimationFrame(h);
          }, l.beforeNextCssLayout = d, l.beforeFutureCssLayout = function(h, m) {
            (function g(p, w) {
              window && p && Number.isInteger(p) && p > 0 ? window.requestAnimationFrame(function() {
                g(p - 1, w);
              }) : w();
            })(h + 1, m);
          }, l.onceNextCssLayout = function() {
            return new Promise(function(h) {
              d(h);
            });
          }, l.onceTransitionEnd = function(h) {
            var m = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
            return new Promise(function(g) {
              u(h, "transition", m).then(g);
            });
          }, l.onceAnimationEnd = function(h) {
            var m = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
            return new Promise(function(g) {
              u(h, "animation", m).then(g);
            });
          };
        }]);
      }, function(s, r, o) {
        s.exports = o(13);
      }, , , , , , , , function(s, r, o) {
        o.r(r);
        var a = o(3), l = o.n(a), c = o(2), u = o.n(c), d = o(4), h = o(0), m = o(1);
        function g(q) {
          return (g = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(V) {
            return typeof V;
          } : function(V) {
            return V && typeof Symbol == "function" && V.constructor === Symbol && V !== Symbol.prototype ? "symbol" : typeof V;
          })(q);
        }
        function p(q, U) {
          for (var V = 0; V < U.length; V++) {
            var _ = U[V];
            _.enumerable = _.enumerable || !1, _.configurable = !0, "value" in _ && (_.writable = !0), Object.defineProperty(q, _.key, _);
          }
        }
        function w(q) {
          return (w = Object.setPrototypeOf ? Object.getPrototypeOf : function(V) {
            return V.__proto__ || Object.getPrototypeOf(V);
          })(q);
        }
        function y(q) {
          if (q === void 0) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
          return q;
        }
        function k(q, U) {
          return (k = Object.setPrototypeOf || function(_, b) {
            return _.__proto__ = b, _;
          })(q, U);
        }
        function C(q, U, V) {
          return U in q ? Object.defineProperty(q, U, { value: V, enumerable: !0, configurable: !0, writable: !0 }) : q[U] = V, q;
        }
        var E = function(q) {
          function U(V) {
            var _;
            return function(x, A) {
              if (!(x instanceof A)) throw new TypeError("Cannot call a class as a function");
            }(this, U), _ = function(x, A) {
              return !A || g(A) !== "object" && typeof A != "function" ? y(x) : A;
            }(this, w(U).call(this, V)), C(y(_), "bulletClick", function(b) {
              var x = b.currentTarget;
              x.classList.add(Object(h.c)("".concat(_.rootElement, "__bullets--loading"), _.props.cssModule));
              var A = parseInt(x.getAttribute("data-index"), 10), z = !(_.props.selected > A);
              _.props.onClick({ index: A, direction: z });
            }), _.rootElement = V.rootElement, _;
          }
          return function(_, b) {
            if (typeof b != "function" && b !== null) throw new TypeError("Super expression must either be null or a function");
            _.prototype = Object.create(b && b.prototype, { constructor: { value: _, writable: !0, configurable: !0 } }), b && k(_, b);
          }(U, q), function(_, b, x) {
            return b && p(_.prototype, b), _;
          }(U, [{ key: "renderBullets", value: function() {
            var _ = this, b = this.props, x = b.cssModule, A = b.selected, z = b.media;
            return (z === void 0 ? [] : z).map(function(j, H) {
              var ne = H === A ? Object(h.c)("".concat(_.rootElement, "__bullets--active"), x) : null;
              return l.a.createElement("button", { key: "bullet-".concat(H), "data-index": H, onClick: _.bulletClick, className: ne }, H);
            });
          } }, { key: "render", value: function() {
            var _ = this.props, b = _.cssModule, x = _.rootElement;
            return l.a.createElement("nav", { className: Object(h.c)("".concat(x, "__bullets"), b) }, this.renderBullets());
          } }]), U;
        }(l.a.Component);
        function T(q) {
          return (T = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(V) {
            return typeof V;
          } : function(V) {
            return V && typeof Symbol == "function" && V.constructor === Symbol && V !== Symbol.prototype ? "symbol" : typeof V;
          })(q);
        }
        function L(q, U) {
          for (var V = 0; V < U.length; V++) {
            var _ = U[V];
            _.enumerable = _.enumerable || !1, _.configurable = !0, "value" in _ && (_.writable = !0), Object.defineProperty(q, _.key, _);
          }
        }
        function O(q, U) {
          return !U || T(U) !== "object" && typeof U != "function" ? function(_) {
            if (_ === void 0) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return _;
          }(q) : U;
        }
        function N(q) {
          return (N = Object.setPrototypeOf ? Object.getPrototypeOf : function(V) {
            return V.__proto__ || Object.getPrototypeOf(V);
          })(q);
        }
        function M(q, U) {
          return (M = Object.setPrototypeOf || function(_, b) {
            return _.__proto__ = b, _;
          })(q, U);
        }
        function J(q, U, V) {
          return U in q ? Object.defineProperty(q, U, { value: V, enumerable: !0, configurable: !0, writable: !0 }) : q[U] = V, q;
        }
        C(E, "propTypes", { cssModule: u.a.object, rootElement: u.a.string.isRequired, media: u.a.array, onClick: u.a.func, selected: u.a.number }), C(E, "defaultProps", { cssModule: null, selected: 0, media: [], onClick: function() {
        } });
        var D = function(q) {
          function U() {
            return function(_, b) {
              if (!(_ instanceof b)) throw new TypeError("Cannot call a class as a function");
            }(this, U), O(this, N(U).apply(this, arguments));
          }
          return function(_, b) {
            if (typeof b != "function" && b !== null) throw new TypeError("Super expression must either be null or a function");
            _.prototype = Object.create(b && b.prototype, { constructor: { value: _, writable: !0, configurable: !0 } }), b && M(_, b);
          }(U, q), function(_, b, x) {
            return b && L(_.prototype, b), _;
          }(U, [{ key: "componentDidMount", value: function() {
            this.props.onMount({ element: this.controls, next: this.next, prev: this.prev });
          } }, { key: "render", value: function() {
            var _ = this, b = this.props, x = b.rootElement, A = b.cssModule, z = b.organicArrows, j = b.buttonContentLeft, H = b.buttonContentRight, ne = b.onNext, me = b.onPrev;
            return l.a.createElement("div", { ref: function(Le) {
              _.controls = Le, _.props.onMount({ element: _.controls, next: _.next || null, prev: _.prev || null });
            }, className: [Object(h.c)("".concat(x, "__controls"), A), Object(h.c)("".concat(x, "__controls--hidden"), A)].join(" ") }, l.a.createElement("button", { ref: function(Le) {
              _.next = Le;
            }, "aria-label": "next", className: Object(h.c)("".concat(x, "__next"), A), onClick: ne }, z ? l.a.createElement("span", { className: Object(h.c)("".concat(x, "__controls__arrow-right"), A) }) : H), l.a.createElement("button", { ref: function(Le) {
              _.prev = Le;
            }, "aria-label": "previous", className: Object(h.c)("".concat(x, "__prev"), A), onClick: me }, z ? l.a.createElement("span", { className: Object(h.c)("".concat(x, "__controls__arrow-left"), A) }) : j));
          } }]), U;
        }(l.a.Component);
        function B(q) {
          return (B = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(V) {
            return typeof V;
          } : function(V) {
            return V && typeof Symbol == "function" && V.constructor === Symbol && V !== Symbol.prototype ? "symbol" : typeof V;
          })(q);
        }
        function $() {
          return ($ = Object.assign || function(q) {
            for (var U = 1; U < arguments.length; U++) {
              var V = arguments[U];
              for (var _ in V) Object.prototype.hasOwnProperty.call(V, _) && (q[_] = V[_]);
            }
            return q;
          }).apply(this, arguments);
        }
        function W(q, U) {
          if (q == null) return {};
          var V, _, b = function(z, j) {
            if (z == null) return {};
            var H, ne, me = {}, Oe = Object.keys(z);
            for (ne = 0; ne < Oe.length; ne++) H = Oe[ne], j.indexOf(H) >= 0 || (me[H] = z[H]);
            return me;
          }(q, U);
          if (Object.getOwnPropertySymbols) {
            var x = Object.getOwnPropertySymbols(q);
            for (_ = 0; _ < x.length; _++) V = x[_], U.indexOf(V) >= 0 || Object.prototype.propertyIsEnumerable.call(q, V) && (b[V] = q[V]);
          }
          return b;
        }
        function Y(q, U) {
          for (var V = 0; V < U.length; V++) {
            var _ = U[V];
            _.enumerable = _.enumerable || !1, _.configurable = !0, "value" in _ && (_.writable = !0), Object.defineProperty(q, _.key, _);
          }
        }
        function ye(q) {
          return (ye = Object.setPrototypeOf ? Object.getPrototypeOf : function(V) {
            return V.__proto__ || Object.getPrototypeOf(V);
          })(q);
        }
        function Pe(q) {
          if (q === void 0) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
          return q;
        }
        function Be(q, U) {
          return (Be = Object.setPrototypeOf || function(_, b) {
            return _.__proto__ = b, _;
          })(q, U);
        }
        function te(q, U, V) {
          return U in q ? Object.defineProperty(q, U, { value: V, enumerable: !0, configurable: !0, writable: !0 }) : q[U] = V, q;
        }
        J(D, "propTypes", { cssModule: u.a.object, rootElement: u.a.string.isRequired, onMount: u.a.func.isRequired, onNext: u.a.func.isRequired, onPrev: u.a.func.isRequired, buttonContentLeft: u.a.node, buttonContentRight: u.a.node, organicArrows: u.a.bool }), J(D, "defaultProps", { cssModule: null, organicArrows: !0, buttonContentLeft: null, buttonContentRight: null });
        var R = function(q) {
          function U() {
            var V, _;
            (function(j, H) {
              if (!(j instanceof H)) throw new TypeError("Cannot call a class as a function");
            })(this, U);
            for (var b = arguments.length, x = new Array(b), A = 0; A < b; A++) x[A] = arguments[A];
            return _ = function(j, H) {
              return !H || B(H) !== "object" && typeof H != "function" ? Pe(j) : H;
            }(this, (V = ye(U)).call.apply(V, [this].concat(x))), te(Pe(_), "state", {}), _;
          }
          return function(_, b) {
            if (typeof b != "function" && b !== null) throw new TypeError("Super expression must either be null or a function");
            _.prototype = Object.create(b && b.prototype, { constructor: { value: _, writable: !0, configurable: !0 } }), b && Be(_, b);
          }(U, q), function(_, b, x) {
            return b && Y(_.prototype, b), _;
          }(U, [{ key: "render", value: function() {
            var _ = this.props, b = _.media, x = _.className, A = b.source, z = b.children, j = b.style, H = (b.loader, b["data-src"], b["data-alt"], b.className), ne = (b.onTransitionEnd, b.onTransitionStartOut, b.onTransitionStartIn, b.onTransitionRequestOut, b.onTransitionRequestIn, W(b, ["source", "children", "style", "loader", "data-src", "data-alt", "className", "onTransitionEnd", "onTransitionStartOut", "onTransitionStartIn", "onTransitionRequestOut", "onTransitionRequestIn"])), me = null;
            return A && (me = A.match(/\.(mp4|webm)/) ? l.a.createElement("video", { title: b.title || b["data-title"], src: A, type: "video/mp4", controls: !0 }) : l.a.createElement("img", { alt: b.alt || b.title || b["data-alt"] || null, src: A })), l.a.createElement("div", $({ className: x, style: j || null }, ne), me, z && l.a.createElement("div", { className: H }, b.children));
          } }]), U;
        }(l.a.Component);
        function K(q) {
          return (K = typeof Symbol == "function" && typeof Symbol.iterator == "symbol" ? function(V) {
            return typeof V;
          } : function(V) {
            return V && typeof Symbol == "function" && V.constructor === Symbol && V !== Symbol.prototype ? "symbol" : typeof V;
          })(q);
        }
        function X() {
          return (X = Object.assign || function(q) {
            for (var U = 1; U < arguments.length; U++) {
              var V = arguments[U];
              for (var _ in V) Object.prototype.hasOwnProperty.call(V, _) && (q[_] = V[_]);
            }
            return q;
          }).apply(this, arguments);
        }
        function ie(q) {
          return function(V) {
            if (Array.isArray(V)) {
              for (var _ = 0, b = new Array(V.length); _ < V.length; _++) b[_] = V[_];
              return b;
            }
          }(q) || function(V) {
            if (Symbol.iterator in Object(V) || Object.prototype.toString.call(V) === "[object Arguments]") return Array.from(V);
          }(q) || function() {
            throw new TypeError("Invalid attempt to spread non-iterable instance");
          }();
        }
        function oe(q, U) {
          var V = Object.keys(q);
          if (Object.getOwnPropertySymbols) {
            var _ = Object.getOwnPropertySymbols(q);
            U && (_ = _.filter(function(b) {
              return Object.getOwnPropertyDescriptor(q, b).enumerable;
            })), V.push.apply(V, _);
          }
          return V;
        }
        function de(q) {
          for (var U = 1; U < arguments.length; U++) {
            var V = arguments[U] != null ? arguments[U] : {};
            U % 2 ? oe(Object(V), !0).forEach(function(_) {
              we(q, _, V[_]);
            }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(q, Object.getOwnPropertyDescriptors(V)) : oe(Object(V)).forEach(function(_) {
              Object.defineProperty(q, _, Object.getOwnPropertyDescriptor(V, _));
            });
          }
          return q;
        }
        function pe(q, U) {
          for (var V = 0; V < U.length; V++) {
            var _ = U[V];
            _.enumerable = _.enumerable || !1, _.configurable = !0, "value" in _ && (_.writable = !0), Object.defineProperty(q, _.key, _);
          }
        }
        function ve(q) {
          return (ve = Object.setPrototypeOf ? Object.getPrototypeOf : function(V) {
            return V.__proto__ || Object.getPrototypeOf(V);
          })(q);
        }
        function _e(q) {
          if (q === void 0) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
          return q;
        }
        function Ee(q, U) {
          return (Ee = Object.setPrototypeOf || function(_, b) {
            return _.__proto__ = b, _;
          })(q, U);
        }
        function we(q, U, V) {
          return U in q ? Object.defineProperty(q, U, { value: V, enumerable: !0, configurable: !0, writable: !0 }) : q[U] = V, q;
        }
        te(R, "propTypes", { media: u.a.object.isRequired, className: u.a.string.isRequired });
        var Ne = "awssld", se = new h.a(), Qe = function(q) {
          function U(V) {
            var _;
            return function(x, A) {
              if (!(x instanceof A)) throw new TypeError("Cannot call a class as a function");
            }(this, U), _ = function(x, A) {
              return !A || K(A) !== "object" && typeof A != "function" ? _e(x) : A;
            }(this, ve(U).call(this, V)), we(_e(_), "clickNext", function() {
              var b = _.index === null ? 0 : _.index + 1;
              _.onTransitionRequest("next", b), _.goTo({ index: b, direction: !0 });
            }), we(_e(_), "clickPrev", function() {
              var b = _.index - 1;
              _.onTransitionRequest("prev", b), _.goTo({ index: b, direction: !1 });
            }), we(_e(_), "touchStart", function(b) {
              if (!_.animating && _.index !== null) {
                var x = b.nativeEvent;
                _.touchStartPoint = x.touches[0].clientX;
              }
            }), we(_e(_), "touchMove", function(b) {
              if (!_.animating && _.touchStartPoint) {
                var x = b.nativeEvent, A = x.touches[0].clientX - _.touchStartPoint, z = _[_.active], j = _[_.loader], H = !(A > 0), ne = Math.abs(A);
                _.touchEnabled !== !1 ? ne >= 10 && (_.loading === !1 ? _.goTo({ index: H ? _.index + 1 : _.index - 1, direction: H, touch: !0 }) : _.direction === !0 ? (A += 10, Math.abs(A) > z.offsetWidth ? A = -z.offsetWidth : A > 0 && (A = 0), z.style.transform = "translate3d(".concat(A, "px, 0, 0)"), j.style.transform = "translate3d(calc(100% + ".concat(A, "px), 0, 0)")) : (A -= 10, Math.abs(A) > z.offsetWidth ? A = z.offsetWidth : A < 0 && (A = 0), z.style.transform = "translate3d(".concat(A, "px, 0, 0)"), j.style.transform = "translate3d(calc(-100% + ".concat(A, "px), 0, 0)"))) : ne > 20 && (_.touchEnabled = !0, _.touchStartPoint = x.touches[0].clientX);
              }
            }), we(_e(_), "touchEnd", function() {
              !_.animating && _.touchStartPoint && _.loading && (_.touchStartPoint = null, _.animating = !0, _.touchEnabled = !1, _.animateMobileEnd(function() {
                _.index = _.nextIndex, _.setState({ index: _.index }), _.onTransitionEnd(), _.animating = !1, _.loading = !1, _.unchargeIndex();
              }));
            }), we(_e(_), "bulletClick", function(b) {
              var x = b.currentTarget, A = parseInt(x.getAttribute("data-index"), 10);
              _.goTo({ index: A, direction: !(_.index > A) }, function() {
                Object(d.onceNextCssLayout)().then(function() {
                  Object(m.a)(x, _.classNames.bulletsLoading);
                });
              });
            }), _.rootElement = V.rootElement || Ne, _.boxA = null, _.boxB = null, _.loaded = [], _.active = "boxA", _.loader = "boxB", _.nextIndex = null, _.loading = !1, _.media = null, _.started = !1, _.touchEnabled = !1, _.setupStartup(V), _;
          }
          return function(_, b) {
            if (typeof b != "function" && b !== null) throw new TypeError("Super expression must either be null or a function");
            _.prototype = Object.create(b && b.prototype, { constructor: { value: _, writable: !0, configurable: !0 } }), b && Ee(_, b);
          }(U, q), function(_, b, x) {
            return b && pe(_.prototype, b), _;
          }(U, [{ key: "componentDidMount", value: function() {
            var _ = this;
            Object(m.a)(this.boxA, this.classNames.active), this.props.startupScreen && (this.buttons && (Object(m.a)(this.buttons.element, this.classNames.controlsHidden), Object(m.a)(this.buttons.element, this.classNames.controlsActive)), this.props.startup === !0 && this.media.length > 0 && this.startup()), this.props.onFirstMount && this.props.onFirstMount(de({}, this.getInfo())), this.buttons && Object(d.onceNextCssLayout)().then(function() {
              _.buttons && _.buttons.element && Object(m.b)(_.buttons.element, _.classNames.controlsHidden);
            });
          } }, { key: "UNSAFE_componentWillReceiveProps", value: function(_) {
            if (this.checkChildren(_), this.setupClassNames(Object(m.e)(_.cssModule)), _.name === this.props.name) if (_.startup !== !0 || this.started !== !1) if (_.selected === this.props.selected) this.refreshSlider();
            else {
              var b = this.getIndex(_.selected), x = _.infinite === !0 && b === 0 && this.index === this.media.length - 1 || !(this.index > b);
              this.goTo({ index: b, direction: x });
            }
            else this.startup();
            else this.resetSlider(_.selected);
          } }, { key: "onTransitionStart", value: function() {
            var _ = this.media[this.index], b = this.media[this.nextIndex], x = de({}, this.getInfo(), { nextSlide: this[this.loader], nextIndex: this.nextIndex, nextMedia: b });
            this.props.onTransitionStart && this.props.onTransitionStart(x), _ && _.onTransitionStartOut && _.onTransitionStartOut(x), b && b.onTransitionStartIn && b.onTransitionStartIn(x);
          } }, { key: "onTransitionRequest", value: function(_, b) {
            var x = this.media[this.index], A = this.checkIndex(b), z = this.media[A], j = de({ eventName: _ }, this.getInfo(), { nextSlide: null, nextIndex: A, nextMedia: z });
            this.props.onTransitionRequest && this.props.onTransitionRequest(j), x && x.onTransitionRequestOut && x.onTransitionRequestOut(j), z && z.onTransitionRequestIn && z.onTransitionRequestIn(j);
          } }, { key: "onTransitionEnd", value: function() {
            var _ = this.media[this.index], b = de({}, this.getInfo());
            this.props.onTransitionEnd && this.props.onTransitionEnd(b), _ && _.onTransitionEnd && _.onTransitionEnd(b);
          } }, { key: "getRootClassName", value: function() {
            var _ = this.props, b = _.animation, x = _.className, A = _.cssModule, z = _.disabled, j = _.fillParent, H = _.infinite, ne = _.organicArrows;
            return Object(m.d)({ animation: b, className: x, cssModule: Object(m.e)(A), current: this.state.index, disabled: z, fillParent: j, infinite: H, organicArrows: ne, rootElement: this.rootElement, total: this.media.length });
          } }, { key: "setupStartup", value: function(_) {
            if (this.checkChildren(_), this.setupClassNames(Object(m.e)(_.cssModule)), _.startupScreen) {
              var b = this.getIndex(this.props.selected);
              this.index = null, this.state = { index: this.index, boxA: { className: this.classNames.startUp, children: _.startupScreen }, boxB: this.media[b] || null };
            } else this.started = !0, this.index = this.getIndex(this.props.selected), this.state = { index: this.index, boxA: this.media[this.index] || null, boxB: null };
          } }, { key: "getInfo", value: function() {
            return { slides: this.media.length, currentIndex: this.index, currentSlide: this[this.active], currentMedia: this.media[this.index], element: this.slider };
          } }, { key: "getProgressBar", value: function() {
            if (!document) return {};
            var _ = document.createElement("div");
            return _.className = this.classNames.bar, _;
          } }, { key: "setupClassNames", value: function(_) {
            this.classNames = Object(m.f)(this.rootElement, _);
          } }, { key: "getIndex", value: function(_) {
            var b = 0;
            return typeof _ == "number" ? _ : (typeof _ == "string" && this.media.forEach(function(x, A) {
              x.slug === _ && (b = A);
            }), b);
          } }, { key: "refreshSlider", value: function() {
            var _;
            if (this.loading !== !0 && this.props.startup !== !1 && this.index !== null) {
              var b = this.index;
              this.setState((we(_ = { index: b }, this.active, this.media[this.getIndex(b)]), we(_, this.loader, null), _));
            }
          } }, { key: "startup", value: function() {
            var _ = this;
            this.started = !0, setTimeout(function() {
              _.goTo({ index: _.props.selected, direction: !0, touch: !1 });
            }, this.props.startupDelay || 75);
          } }, { key: "resetSlider", value: function() {
            var _, b = this, x = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0;
            this.index = x, this.setState((we(_ = { index: x }, this.active, this.media[this.getIndex(x)]), we(_, this.loader, null), _), function() {
              b.props.onResetSlider && b.props.onResetSlider(de({}, b.getInfo()));
            });
          } }, { key: "checkChildren", value: function(_) {
            _.children && (_.children !== this.props.children || this.props.children && !this.media) ? this.media = Object(m.g)(_.children) : _.media && _.media.length ? this.media = _.media : this.media || (this.media = []);
          } }, { key: "startBarAnimation", value: function(_) {
            var b = this, x = _.active;
            return new Promise(function(A) {
              b.bar = b.getProgressBar(), x.appendChild(b.bar), Object(d.onceNextCssLayout)().then(function() {
                Object(d.onceNextCssLayout)().then(function() {
                  Object(m.a)(b.bar, b.classNames.barActive), A();
                });
              });
            });
          } }, { key: "endBarAnimation", value: function(_) {
            var b = this;
            this.bar && Object(d.onceNextCssLayout)().then(function() {
              Object(d.onceTransitionEnd)(b.bar).then(function() {
                _();
              }), Object(m.a)(b.bar, b.classNames.barEnd);
            });
          } }, { key: "loadContent", value: function(_, b) {
            var x = this;
            return new Promise(function(A, z) {
              if (x.props.onLoadStart || b && b.onLoadStart) {
                var j = x.props.onLoadStart || b && b.onLoadStart;
                return x.startBarAnimation({ active: _ }), void j(de({ next: function() {
                  x.endBarAnimation(function() {
                    A(x.bar);
                  });
                }, error: z }, x.getInfo()));
              }
              if (b && (b.source || b.preload)) {
                var H = b.preload ? b.preload : b.source && [b.source] || [];
                return x.checkLoadedUrls(H) === !0 ? void A(null) : (x.startBarAnimation({ active: _ }), void se.loadMultiple(H).then(function() {
                  x.pushLoaded(H), x.endBarAnimation(function() {
                    A(x.bar);
                  });
                }));
              }
              A(null);
            });
          } }, { key: "pushLoaded", value: function(_) {
            this.loaded = [].concat(ie(this.loaded), ie(_));
          } }, { key: "checkLoadedUrls", value: function(_) {
            var b = this, x = !0;
            return _.forEach(function(A) {
              b.loaded.includes(A) || (x = !1);
            }), x;
          } }, { key: "startAnimationMobile", value: function() {
            var _ = this.direction, b = this[this.active], x = this[this.loader], A = _ ? this.classNames.contentMoveRight : this.classNames.contentMoveLeft, z = _ ? this.classNames.contentMoveLeft : this.classNames.contentMoveRight;
            this.props.onTransitionStart && this.props.onTransitionStart(de({}, this.getInfo(), { nextSlide: this[this.loader], nextIndex: this.nextIndex, nextMedia: this.media[this.nextIndex] }));
            var j = b.querySelector(".".concat(Object(m.c)(this.classNames.content)));
            Object(m.a)(j, z), Object(m.a)(j, this.classNames.contentExit);
            var H = x.querySelector(".".concat(Object(m.c)(this.classNames.content)));
            Object(m.a)(H, A), Object(m.a)(H, this.classNames.contentStatic), Object(m.a)(b, this.classNames.animated), Object(m.a)(x, this.classNames.animated);
          } }, { key: "animateMobileEnd", value: function(_) {
            var b = this, x = this.direction, A = this[this.active], z = this[this.loader], j = x ? this.classNames.moveLeft : this.classNames.moveRight, H = x ? this.classNames.contentMoveRight : this.classNames.contentMoveLeft, ne = x ? this.classNames.contentMoveLeft : this.classNames.contentMoveRight, me = z.querySelector(".".concat(Object(m.c)(this.classNames.content))), Oe = A.querySelector(".".concat(Object(m.c)(this.classNames.content)));
            Object(m.b)(me, this.classNames.contentStatic), Object(m.a)(z, this.classNames.animatedMobile), Object(m.a)(A, this.classNames.animatedMobile), Object(d.onceNextCssLayout)().then(function() {
              z.style.transform = "translate3d(0, 0, 0)", A.style.transform = "translate3d(".concat(b.direction ? "-" : "", "100%, 0, 0)"), Object(d.onceTransitionEnd)(A).then(function() {
                b.loading && (Object(m.b)(A, b.classNames.animated), Object(m.b)(z, b.classNames.animated), Object(m.a)(z, b.classNames.active), Object(m.b)(A, b.classNames.active), Object(m.b)(A, j), Object(m.b)(z, b.classNames.animatedMobile), Object(m.b)(A, b.classNames.animatedMobile), Object(m.b)(Oe, ne), Object(m.b)(Oe, b.classNames.contentExit), Object(m.b)(me, H), b.buttons && setTimeout(function() {
                  b.buttons && Object(m.b)(b.buttons.element, b.classNames.controlsActive);
                }, b.props.controlsReturnDelay), b.activeArrow && (Object(m.b)(b.activeArrow, b.activeArrowClass), b.activeArrow = null, b.activeArrowClass = null), b.active = b.active === "boxA" ? "boxB" : "boxA", b.loader = b.active === "boxA" ? "boxB" : "boxA", _ && _());
              });
            });
          } }, { key: "runAnimation", value: function(_) {
            var b = this, x = _.active, A = _.media, z = _.contentExitMoveClass, j = _.contentEnterMoveClass, H = _.activeContentElement, ne = _.loaderContentElement, me = _.loader, Oe = _.loaderPosition, Le = _.exitPosition, Rt = _.callback, Ot = _.transitionDelay;
            this.loadContent(x, A).then(function(An) {
              Object(m.a)(H, z), Object(m.a)(H, b.classNames.contentExit), Object(m.a)(ne, j), Object(m.a)(ne, b.classNames.contentStatic), setTimeout(function() {
                Object(d.onceNextCssLayout)().then(function() {
                  Object(m.a)(x, b.classNames.animated), Object(m.a)(me, b.classNames.animated), Object(m.b)(ne, b.classNames.contentStatic), Object(m.a)(x, b.classNames.exit), Object(m.a)(me, Oe), Object(m.a)(x, Le), Object(d.onceAnimationEnd)(x).then(function() {
                    Object(m.a)(me, b.classNames.active), Object(m.b)(me, Oe), Object(m.b)(me, b.classNames.animated), Object(m.b)(x, b.classNames.animated), Object(m.b)(x, b.classNames.active), Object(m.b)(x, Le), Object(m.b)(x, b.classNames.exit), Object(m.b)(H, z), Object(m.b)(H, b.classNames.contentExit), Object(m.b)(ne, j), An && x.removeChild(An), b.buttons && setTimeout(function() {
                      b.buttons && Object(m.b)(b.buttons.element, b.classNames.controlsActive);
                    }, b.props.controlsReturnDelay), b.active = b.active === "boxA" ? "boxB" : "boxA", b.loader = b.active === "boxA" ? "boxB" : "boxA";
                    var ws = !b.activeArrow;
                    b.activeArrow && (Object(d.onceTransitionEnd)(b.activeArrow, { tolerance: b.index === null ? 0 : 2 }).then(function() {
                      b.releaseTransition();
                    }), Object(m.b)(b.activeArrow, b.activeArrowClass), b.activeArrow = null, b.activeArrowClass = null), Rt({ release: ws });
                  });
                });
              }, Ot);
            });
          } }, { key: "releaseTransition", value: function() {
            this.loading = !1;
          } }, { key: "startAnimation", value: function(_, b, x) {
            var A = this.props.transitionDelay, z = this[this.active], j = this[this.loader], H = _ ? this.classNames.moveRight : this.classNames.moveLeft, ne = _ ? this.classNames.moveLeft : this.classNames.moveRight, me = _ ? this.classNames.contentMoveRight : this.classNames.contentMoveLeft, Oe = _ ? this.classNames.contentMoveLeft : this.classNames.contentMoveRight, Le = z.querySelector(".".concat(Object(m.c)(this.classNames.content))), Rt = j.querySelector(".".concat(Object(m.c)(this.classNames.content)));
            z.style.removeProperty("transform"), j.style.removeProperty("transform"), this.onTransitionStart();
            var Ot = { active: z, media: b, contentExitMoveClass: Oe, contentEnterMoveClass: me, activeContentElement: Le, loaderContentElement: Rt, loader: j, loaderPosition: H, exitPosition: ne, callback: x, transitionDelay: A };
            this.runAnimation(Ot);
          } }, { key: "goTo", value: function(_) {
            var b = this, x = _.index, A = _.direction, z = _.touch, j = z !== void 0 && z, H = this.getIndex(x);
            this.loading !== !0 && x !== this.index ? (this.loading = !0, this.direction = A, j !== !0 ? this.activateArrows(A, function() {
              b.chargeIndex(H, function(ne) {
                b.renderedLoader = !0, b.startAnimation(A, ne, function(me) {
                  var Oe = me.release, Le = Oe === void 0 || Oe;
                  b.index = b.nextIndex, b.setState({ index: b.index }, function() {
                    b.onTransitionEnd(), Le === !0 && b.releaseTransition();
                  });
                });
              });
            }) : this.chargeIndex(H, function() {
              b.activateArrows(A), b.startAnimationMobile();
            })) : this.props.onTransitionReject && this.props.onTransitionReject(de({}, this.getInfo(), { forceTransition: function() {
              b.goTo({ index: x, direction: A, touch: j });
            } }));
          } }, { key: "checkIndex", value: function(_) {
            return _ > this.media.length - 1 ? 0 : _ < 0 ? this.media.length - 1 : _;
          } }, { key: "chargeIndex", value: function(_, b) {
            this.nextIndex = this.checkIndex(_);
            var x = {}, A = this.media[this.nextIndex];
            x[this.loader] = de({ loader: !0 }, A), this.setState(x, function() {
              b(A);
            });
          } }, { key: "unchargeIndex", value: function() {
            var _ = {};
            _[this.loader] = null, this.setState(_, function() {
            });
          } }, { key: "activateArrows", value: function(_, b) {
            var x = _ ? "right" : "left", A = Object(m.e)(this.props.cssModule), z = Object(m.c)(Object(h.c)("".concat(this.rootElement, "__controls__arrow-").concat(x), A));
            if (this.buttons) {
              var j = _ ? this.buttons.next : this.buttons.prev;
              this.activeArrow = j.querySelector(".".concat(z));
            }
            !this.activeArrow || this.buttons && this.buttons.element && this.buttons.element.classList.contains(this.classNames.controlsActive) ? b && b() : (this.activeArrowClass = Object(h.c)("".concat(this.rootElement, "__controls__arrow-").concat(x, "--active"), A), Object(d.onceTransitionEnd)(this.activeArrow, { tolerance: this.index === null ? 0 : 2 }).then(function() {
              b && b();
            }), this.buttons && this.buttons.element && (Object(m.a)(this.buttons.element, this.classNames.controlsActive), Object(m.a)(this.activeArrow, this.activeArrowClass)));
          } }, { key: "renderBox", value: function(_) {
            var b = this, x = {};
            return this.props.mobileTouch && (x.onTouchStart = this.touchStart, x.onTouchMove = this.touchMove, x.onTouchEnd = this.touchEnd), l.a.createElement("div", X({ ref: function(z) {
              b["box".concat(_)] = z;
            }, className: this.classNames.box }, x), this.state["box".concat(_)] && l.a.createElement(R, { media: this.state["box".concat(_)], className: this.classNames.content }));
          } }, { key: "render", value: function() {
            var _ = this, b = this.props, x = b.cssModule, A = b.organicArrows, z = b.bullets, j = b.style, H = b.customContent, ne = b.buttons, me = b.buttonContentLeft, Oe = b.buttonContentRight, Le = this.rootElement;
            return l.a.createElement("div", { ref: function(Ot) {
              _.slider = Ot;
            }, className: this.getRootClassName(), style: j }, l.a.createElement("div", { ref: function(Ot) {
              _.wrapper = Ot;
            }, className: this.classNames.wrapper }, l.a.createElement("div", { ref: function(Ot) {
              _.container = Ot;
            }, className: this.classNames.container }, this.renderBox("A"), this.renderBox("B")), ne && l.a.createElement(D, { rootElement: Le, cssModule: Object(m.e)(x), onMount: function(Ot) {
              _.buttons = Ot;
            }, onNext: this.clickNext, onPrev: this.clickPrev, organicArrows: A, buttonContentLeft: me, buttonContentRight: Oe }), H), z && l.a.createElement(E, { cssModule: Object(m.e)(x), rootElement: Le, media: this.media, selected: this.state.index, onClick: function(Ot) {
              _.onTransitionRequest("bullet", Ot.index), _.goTo(Ot);
            } }));
          } }]), U;
        }(l.a.Component);
        we(Qe, "propTypes", { animation: u.a.string, bullets: u.a.bool, buttonContentLeft: u.a.node, buttonContentRight: u.a.node, buttons: u.a.bool, children: u.a.node, className: u.a.string, controlsReturnDelay: u.a.number, cssModule: u.a.any, customContent: u.a.node, onLoadStart: u.a.func, disabled: u.a.bool, fillParent: u.a.bool, infinite: u.a.bool, media: u.a.array, name: u.a.string, onFirstMount: u.a.func, onResetSlider: u.a.func, onStartupRelease: u.a.func, onTransitionEnd: u.a.func, onTransitionRequest: u.a.func, onTransitionStart: u.a.func, organicArrows: u.a.bool, rootElement: u.a.string, selected: u.a.any, startup: u.a.bool, startupDelay: u.a.number, startupScreen: u.a.object, style: u.a.object, transitionDelay: u.a.number, mobileTouch: u.a.bool }), we(Qe, "defaultProps", { animation: null, bullets: !0, buttonContentLeft: null, buttonContentRight: null, buttons: !0, children: null, className: null, controlsReturnDelay: 0, cssModule: null, customContent: null, onLoadStart: null, disabled: !1, fillParent: !1, infinite: !0, media: [], name: "awesome-slider", onFirstMount: null, onResetSlider: null, onStartupRelease: null, onTransitionEnd: null, onTransitionRequest: null, onTransitionStart: null, organicArrows: !0, rootElement: Ne, selected: 0, startup: !0, startupDelay: 0, startupScreen: null, style: {}, transitionDelay: 0, mobileTouch: !0 }), r.default = Qe;
      }]);
    });
  }(Rl)), Rl.exports;
}
var iT = tT();
const nT = /* @__PURE__ */ Rc(iT);
class YZ extends ce.Component {
  constructor(e) {
    super(e);
    f(this, "intervalPlay");
    f(this, "nextPage", () => {
      var i, s;
      let e = ((i = this.state) == null ? void 0 : i.page) ?? 0;
      (s = this.props) != null && s.children && e + 1 < this.props.children.length && (this.setState({ page: e + 1 }), this.props.onChage && this.props.onChage(e + 1));
    });
    f(this, "previousPage", () => {
      var i, s;
      let e = ((i = this.state) == null ? void 0 : i.page) ?? 0;
      (s = this.props) != null && s.children && e > 0 && (this.setState({ page: e - 1 }), this.props.onChage && this.props.onChage(e - 1));
    });
    f(this, "autoPlay", () => {
      var i, s;
      let e = ((i = this.state) == null ? void 0 : i.page) ?? 0;
      (s = this.props) != null && s.children && e + 1 === this.props.children.length && (e = -1), this.setState({ page: e + 1 }), this.props.onChage && this.props.onChage(e + 1);
    });
    e.buttons ?? (e.buttons = !0), this.state = {
      page: 0
    }, this.autoPlay = this.autoPlay.bind(this), this.nextPage = this.nextPage.bind(this), this.previousPage = this.previousPage.bind(this);
  }
  componentDidMount() {
    this.props.autoPlay && (this.intervalPlay = setInterval(this.autoPlay, this.props.duration ?? 2e3));
  }
  componentDidUpdate(e) {
    this.props.autoPlay !== e.autoPlay && !this.props.autoPlay && clearInterval(this.intervalPlay);
  }
  render() {
    var e;
    return /* @__PURE__ */ v.jsx(
      nT,
      {
        animation: this.props.animation,
        style: this.props.style,
        className: `custom-slider-container ${this.props.className ?? ""}`,
        selected: this.state.page,
        bullets: !1,
        buttons: this.props.buttons ? this.props.children && ((e = this.props.children) == null ? void 0 : e.length) > 1 : !1,
        organicArrows: !1,
        buttonContentLeft: this.props.prevButton ?? /* @__PURE__ */ v.jsx(rt, { src: "fill/arrows/circle-ctrl-left", size: "2.4rem", color: this.props.iconColor ?? "var(--neutral-absolute-background-color)" }),
        buttonContentRight: this.props.nextButton ?? /* @__PURE__ */ v.jsx(rt, { src: "fill/arrows/circle-ctrl-right", size: "2.4rem", color: this.props.iconColor ?? "var(--neutral-absolute-background-color)" }),
        children: this.props.children
      }
    );
  }
}
const sT = "_disabled_13a7m_67", Qa = {
  "tag-container": "_tag-container_13a7m_1",
  "type-button": "_type-button_13a7m_37",
  "tag-label": "_tag-label_13a7m_45",
  disabled: sT
};
class JZ extends ce.Component {
  render() {
    return /* @__PURE__ */ v.jsxs("div", { id: this.props.id, "tag-type": this.props.status ?? "default", className: `${Qa["tag-container"]} row ${this.props.onClick ? Qa["type-button"] : ""} ${this.props.disabled ? Qa.disabled : ""} ${this.props.className ?? "button-text-6"} `, style: this.props.style, onClick: this.props.onClick, children: [
      this.props.prefix,
      /* @__PURE__ */ v.jsx(Ae, { maxLine: 1, className: Qa["tag-label"], children: this.props.title }),
      this.props.suffix
    ] });
  }
}
const gg = {
  "number-picker-container": "_number-picker-container_26bbi_1",
  "helper-text": "_helper-text_26bbi_231"
}, XZ = ({ id: n, value: t, onChange: e, disabled: i, readOnly: s, className: r, helperText: o, helperTextColor: a, max: l, min: c, style: u, type: d = "icon-button", volume: h = 1 }) => {
  const [m, g] = zt(0), p = yt(null);
  return ht(() => {
    p.current && (g(t ?? 0), p.current.value = `${t ?? 0}`);
  }, [t, p]), /* @__PURE__ */ v.jsxs(
    "div",
    {
      id: n,
      className: `row ${gg["number-picker-container"]} ${r ?? "body-2"} ${(o == null ? void 0 : o.length) && gg["helper-text"]}`,
      "number-picker-type": d ?? "icon-button",
      "helper-text": o,
      style: u ? { "--helper-text-color": a ?? "#e14337", ...u } : { "--helper-text-color": a ?? "#e14337" },
      children: [
        /* @__PURE__ */ v.jsx("div", { className: "row", onClick: () => {
          let w = m - h;
          (c === void 0 || w >= c) && (h % 1 === 0 ? w = Math.round(w) : w = parseFloat(w.toFixed(1)), g(w), p.current && (p.current.value = `${w}`), e && e(w));
        }, children: /* @__PURE__ */ v.jsx("svg", { width: "16", height: "16", viewBox: "0 0 16 16", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: /* @__PURE__ */ v.jsx("path", { fillRule: "evenodd", clipRule: "evenodd", d: "M1.3335 7.93907C1.3335 7.60435 1.60484 7.33301 1.93956 7.33301H14.0608C14.3955 7.33301 14.6668 7.60435 14.6668 7.93907C14.6668 8.27379 14.3955 8.54513 14.0608 8.54513H1.93956C1.60484 8.54513 1.3335 8.27379 1.3335 7.93907Z" }) }) }),
        /* @__PURE__ */ v.jsx(
          "input",
          {
            ref: p,
            readOnly: s,
            disabled: i,
            onKeyDown: (w) => {
              switch (w.key.toLowerCase()) {
                case "enter":
                  w.target.blur();
                  break;
              }
            },
            onFocus: (w) => {
              w.target.select();
            },
            onBlur: (w) => {
              let y = h % 1 === 0 ? parseInt(w.target.value.trim()) : parseFloat(w.target.value.trim());
              isNaN(y) ? w.target.value = `${m}` : (h % 1 === 0 ? y = Math.round(y) : y = parseFloat(y.toFixed(1)), c !== void 0 && y < c ? (g(c), p.current && (p.current.value = `${c}`), e && e(c)) : l !== void 0 && y > l ? (g(l), p.current && (p.current.value = `${l}`), e && e(l)) : (g(y), p.current && (p.current.value = `${y}`), e && e(y)));
            }
          }
        ),
        /* @__PURE__ */ v.jsx("div", { className: "row", onClick: () => {
          let w = m + h;
          (l === void 0 || w <= l) && (h % 1 === 0 ? w = Math.round(w) : w = parseFloat(w.toFixed(1)), g(w), p.current && (p.current.value = `${w}`), e && e(w));
        }, children: /* @__PURE__ */ v.jsx("svg", { width: "16", height: "16", viewBox: "0 0 16 16", fill: "none", xmlns: "http://www.w3.org/2000/svg", children: /* @__PURE__ */ v.jsx("path", { d: "M8.60622 1.93907C8.60622 1.60435 8.33488 1.33301 8.00016 1.33301C7.66544 1.33301 7.3941 1.60435 7.3941 1.93907V7.39361H1.93956C1.60484 7.39361 1.3335 7.66496 1.3335 7.99967C1.3335 8.33439 1.60484 8.60574 1.93956 8.60574H7.3941V14.0603C7.3941 14.395 7.66544 14.6663 8.00016 14.6663C8.33488 14.6663 8.60622 14.395 8.60622 14.0603V8.60574H14.0608C14.3955 8.60574 14.6668 8.33439 14.6668 7.99967C14.6668 7.66496 14.3955 7.39361 14.0608 7.39361H8.60622V1.93907Z" }) }) })
      ]
    }
  );
}, rT = {
  "input-opt-container": "_input-opt-container_gu2t8_1"
};
class QZ extends ce.Component {
  constructor(e) {
    super(e);
    f(this, "containerRef", vn());
    f(this, "getValue", () => this.containerRef.current ? [...this.containerRef.current.querySelectorAll("input")].map((e) => e.value).join("") : this.props.value ?? "");
    this.getValue = this.getValue.bind(this);
  }
  componentDidUpdate(e) {
    var i;
    if (e.value !== this.props.value && this.containerRef.current) {
      const s = [...this.containerRef.current.querySelectorAll("input")];
      if ((i = this.props.value) != null && i.length)
        for (let r = 0; r < s.length; r++) s[r].value = this.props.value[r];
      else
        for (let r = 0; r < s.length; r++) s[r].value = "";
    }
  }
  render() {
    var e;
    return /* @__PURE__ */ v.jsx(
      "div",
      {
        id: this.props.id,
        ref: this.containerRef,
        "helper-text": this.props.helperText,
        style: this.props.style ? { "--helper-text-color": this.props.helperTextColor ?? "#e14337", ...this.props.style } : { "--helper-text-color": this.props.helperTextColor ?? "#e14337" },
        className: `row body-1 ${rT["input-opt-container"]} ${((e = this.props.helperText) == null ? void 0 : e.length) && "helper-text"} ${this.props.className ?? ""}`,
        onMouseDown: (i) => {
          i.stopPropagation(), i.preventDefault();
          const s = [...i.target.closest("div").childNodes];
          for (const [r, o] of s.entries())
            if (!o.value.length || r === s.length - 1) {
              o.focus();
              break;
            }
        },
        children: Array.from({ length: this.props.length ?? 6 }).map((i, s) => /* @__PURE__ */ v.jsx(
          "input",
          {
            autoFocus: s === 0 && this.props.autoFocus,
            disabled: this.props.disabled,
            style: this.props.inputStyle,
            onKeyDown: (r) => {
              var a, l;
              const o = r.key.toLowerCase();
              switch (o) {
                case "backspace":
                  r.target.value.length ? r.target.value = "" : ((a = r.target.previousSibling) == null ? void 0 : a.localName) === "input" ? r.target.previousSibling.focus() : r.target.blur();
                  break;
                case "delete":
                  r.target.value = "";
                  break;
                default:
                  if (r.preventDefault(), r.stopPropagation(), o === "v" && r.ctrlKey)
                    return navigator.clipboard.readText().then((c) => {
                      /^\d{6}$/g.test(c) && [...r.target.closest("div").childNodes].forEach((h, m) => {
                        h.value = c[m], h.focus();
                      });
                    });
                  /[0-9]/g.test(o) && !o.startsWith("f") && (r.target.value.length || (r.target.value = o), ((l = r.target.nextSibling) == null ? void 0 : l.localName) === "input" && !r.target.nextSibling.value.length ? r.target.nextSibling.focus() : r.target.blur());
                  break;
              }
            },
            onBlur: () => {
              this.props.onChange && this.props.onChange(this.getValue(), this.containerRef.current);
            }
          },
          "opt-" + s
        ))
      }
    );
  }
}
const _s = {
  "login-view-container": "_login-view-container_161cl_1",
  "login-view-form-container": "_login-view-form-container_161cl_15",
  "login-btn": "_login-btn_161cl_49",
  "forgot-password-btn": "_forgot-password-btn_161cl_77",
  "register-btn": "_register-btn_161cl_95",
  "login-social-media": "_login-social-media_161cl_105",
  "or-spacing": "_or-spacing_161cl_133"
};
function eY(n) {
  var o, a, l, c, u, d;
  const t = y0({ shouldFocusError: !1 }), [e, i] = zt(!1), { t: s } = Yr(), r = (h) => {
    n.onSubmit && n.onSubmit(h, t);
  };
  return /* @__PURE__ */ v.jsxs("form", { id: n.id, className: `col login-view-container ${_s["login-view-container"]} ${n.className ?? ""}`, style: n.style, children: [
    typeof n.logo == "string" ? /* @__PURE__ */ v.jsx("img", { alt: "logo", src: n.logo, height: "36rem" }) : n.logo,
    /* @__PURE__ */ v.jsxs("div", { className: `col login-view-form-container ${_s["login-view-form-container"]}`, children: [
      /* @__PURE__ */ v.jsx(Ae, { className: "heading-4", children: n.title ?? `${s("loginTo")} Wini` }),
      /* @__PURE__ */ v.jsxs("div", { className: "col", children: [
        /* @__PURE__ */ v.jsxs("div", { className: "col", style: { gap: "0.8rem", overflow: "visible" }, children: [
          /* @__PURE__ */ v.jsx(Ae, { className: "label-3", children: n.formData.username.label ?? s("username") }),
          /* @__PURE__ */ v.jsx(
            Ps,
            {
              autoComplete: "username",
              className: "placeholder-2",
              placeholder: n.formData.username.label,
              style: { height: "4.8rem" },
              prefix: n.formData.username.prefix,
              name: n.formData.username.name,
              register: (n.methods ?? t).register(n.formData.username.name, {
                onChange: (h) => {
                  h.target.value = h.target.value.trim();
                },
                onBlur: n.formData.username.onValidate
              }),
              onComplete: (h) => {
                h.target.blur();
              },
              helperText: (a = (o = (n.methods ?? t).formState.errors) == null ? void 0 : o[n.formData.username.name]) == null ? void 0 : a.message
            }
          )
        ] }),
        /* @__PURE__ */ v.jsxs("div", { className: "col", style: { gap: "0.8rem", overflow: "visible" }, children: [
          /* @__PURE__ */ v.jsx(Ae, { className: "label-3", children: n.formData.password.label ?? s("password") }),
          /* @__PURE__ */ v.jsx(
            Ps,
            {
              autoComplete: "current-password",
              className: "placeholder-2",
              placeholder: n.formData.password.label,
              style: { height: "4.8rem" },
              prefix: n.formData.password.prefix,
              suffix: /* @__PURE__ */ v.jsx("button", { type: "button", onClick: () => {
                i(!e);
              }, children: /* @__PURE__ */ v.jsx(rt, { src: `outline/user interface/${e ? "view" : "hide"}`, size: "1.6rem" }) }),
              name: n.formData.password.name,
              type: e ? "text" : "password",
              register: (n.methods ?? t).register(n.formData.password.name, {
                onChange: (h) => {
                  h.target.value = h.target.value.trim();
                },
                onBlur: n.formData.password.onValidate
              }),
              onComplete: (h) => {
                var m;
                (m = (n.methods ?? t).watch(n.formData.password.name)) != null && m.length ? (h.target.blur(), !n.formData.password.onValidate && n.onSubmit && r((n.methods ?? t).getValues())) : h.target.blur();
              },
              helperText: (c = (l = (n.methods ?? t).formState.errors) == null ? void 0 : l[n.formData.password.name]) == null ? void 0 : c.message
            }
          )
        ] }),
        /* @__PURE__ */ v.jsx(Ae, { className: `button-text-3 ${_s["forgot-password-btn"]}`, onClick: n.onForgotPassword, children: n.forgotPasswordText ?? s("forgotPassword") }),
        /* @__PURE__ */ v.jsxs("div", { className: "col", style: { gap: "1.6rem" }, children: [
          /* @__PURE__ */ v.jsx(
            mn,
            {
              disabled: !((u = (n.methods ?? t).watch(n.formData.username.name)) != null && u.length && ((d = (n.methods ?? t).watch(n.formData.password.name)) != null && d.length)),
              className: `button-text-1 ${_s["login-btn"]}`,
              onClick: n.onSubmit && (n.methods ?? t).handleSubmit(r),
              label: n.buttonLoginLabel ?? s("login")
            }
          ),
          /* @__PURE__ */ v.jsxs("div", { className: "row", style: { justifyContent: "center", gap: "0.4rem" }, children: [
            /* @__PURE__ */ v.jsx(Ae, { className: "label-4", children: n.registerPrefixText ?? s("dontHaveAccount") }),
            /* @__PURE__ */ v.jsx(Ae, { className: `button-text-3 ${_s["register-btn"]}`, onClick: n.onRegister, children: n.registerText ?? `${s("signupFor")} Wini` })
          ] })
        ] }),
        n.loginWithGoogle || n.loginWithFacebook || n.loginWithApple || n.loginWithMicrosoft ? /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
          /* @__PURE__ */ v.jsxs("div", { className: `row ${_s["or-spacing"]}`, children: [
            /* @__PURE__ */ v.jsx("div", {}),
            /* @__PURE__ */ v.jsx(Ae, { className: "label-4", children: n.orText ?? s("or") }),
            /* @__PURE__ */ v.jsx("div", {})
          ] }),
          /* @__PURE__ */ v.jsxs("div", { className: `row ${_s["login-social-media"]}`, children: [
            n.loginWithGoogle && /* @__PURE__ */ v.jsx(
              mn,
              {
                className: "label-1",
                onClick: n.loginWithGoogle,
                prefix: /* @__PURE__ */ v.jsx(rt, { src: "color/social media/google", size: "2rem" }),
                label: "Google"
              }
            ),
            n.loginWithFacebook && /* @__PURE__ */ v.jsx(
              mn,
              {
                className: "label-1",
                onClick: n.loginWithFacebook,
                prefix: /* @__PURE__ */ v.jsx(rt, { src: "color/social media/logo-facebook", size: "2rem" }),
                label: "Facebook"
              }
            ),
            n.loginWithApple && /* @__PURE__ */ v.jsx(
              mn,
              {
                className: "label-1",
                onClick: n.loginWithApple,
                prefix: /* @__PURE__ */ v.jsx(rt, { src: "color/development/apple", size: "2rem" }),
                label: "Apple"
              }
            ),
            n.loginWithMicrosoft && /* @__PURE__ */ v.jsx(
              mn,
              {
                className: "label-1",
                onClick: n.loginWithMicrosoft,
                prefix: /* @__PURE__ */ v.jsx(rt, { src: "color/development/microsoft", size: "2rem" }),
                label: "Microsoft"
              }
            )
          ] })
        ] }) : null
      ] })
    ] })
  ] });
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function oT() {
  const n = {
    resolve: null,
    promise: null
  };
  return n.promise = new Promise((t) => {
    n.resolve = t;
  }), n;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function aT(n) {
  let t = null;
  return (...e) => (t || (t = {
    current: n(...e)
  }), t.current);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const Gt = new Array(256).fill("").map((n, t) => ("0" + t.toString(16)).slice(-2));
function lT() {
  const [n, t, e, i] = crypto.getRandomValues(new Uint32Array(4));
  return "e" + Gt[n >> 0 & 255] + Gt[n >> 8 & 255] + Gt[n >> 16 & 255] + Gt[n >> 24 & 255] + Gt[t >> 0 & 255] + Gt[t >> 8 & 255] + Gt[t >> 16 & 255] + Gt[t >> 24 & 255] + Gt[e >> 0 & 255] + Gt[e >> 8 & 255] + Gt[e >> 16 & 255] + Gt[e >> 24 & 255] + Gt[i >> 0 & 255] + Gt[i >> 8 & 255] + Gt[i >> 16 & 255] + Gt[i >> 24 & 255];
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function cT(n, t) {
  const e = n.extraPlugins || [];
  return {
    ...n,
    extraPlugins: [
      ...e,
      ...t.filter((i) => !e.includes(i))
    ]
  };
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function N0(n) {
  return !!n && /^\d+\.\d+\.\d+/.test(n);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function F0(n) {
  return n ? ["nightly", "alpha", "internal", "nightly-", "staging"].some((t) => n.includes(t)) : !1;
}
function uT(n) {
  return N0(n) || F0(n);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function dT(n) {
  if (!N0(n))
    throw new Error(`Invalid semantic version: ${n || "<blank>"}.`);
  const [t, e, i] = n.split(".");
  return {
    major: Number.parseInt(t, 10),
    minor: Number.parseInt(e, 10),
    patch: Number.parseInt(i, 10)
  };
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function hT(n) {
  if (F0(n))
    return 3;
  const { major: t } = dT(n);
  switch (!0) {
    case t >= 44:
      return 3;
    case t >= 38:
      return 2;
    default:
      return 1;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function fT() {
  const { CKEDITOR_VERSION: n, CKEDITOR: t } = window;
  return uT(n) ? {
    source: t ? "cdn" : "npm",
    version: n
  } : null;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function mT() {
  const n = fT();
  return n ? hT(n.version) : null;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function D0(n, t) {
  switch (t || (t = mT() || void 0), t) {
    case 1:
    case 2:
      return n === void 0;
    case 3:
      return n === "GPL";
    default:
      return !1;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function gT(n, t) {
  return function(i) {
    D0(i.config.get("licenseKey")) || i.on("collectUsageData", (s, { setUsageData: r }) => {
      r(`integration.${n}`, t);
    });
  };
}
var pT = Object.defineProperty, bT = (n, t, e) => t in n ? pT(n, t, { enumerable: !0, configurable: !0, writable: !0, value: e }) : n[t] = e, fi = (n, t, e) => bT(n, typeof t != "symbol" ? t + "" : t, e);
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const $0 = class z0 {
  constructor(t, e) {
    fi(this, "_lifecycle"), fi(this, "_element"), fi(this, "_releaseLock", null), fi(this, "_value", null), fi(this, "_afterMountCallbacks", []), fi(this, "_state", {
      destroyedBeforeInitialization: !1,
      mountingInProgress: null
    }), fi(this, "release", aT(() => {
      const { _releaseLock: i, _state: s, _element: r, _lifecycle: o } = this;
      s.mountingInProgress ? s.mountingInProgress.then(() => o.unmount({
        element: r,
        // Mount result might be overridden by watchdog during restart so use instance variable.
        mountResult: this.value
      })).catch((a) => {
        console.error("Semaphore unmounting error:", a);
      }).then(i.resolve).then(() => {
        this._value = null;
      }) : (s.destroyedBeforeInitialization = !0, i.resolve());
    })), this._element = t, this._lifecycle = e, this._lock();
  }
  /**
   * Getter for {@link #_value}.
   */
  get value() {
    return this._value;
  }
  /**
   * Occasionally, the Watchdog restarts the editor instance, resulting in a new instance being assigned to the semaphore.
   * In terms of race conditions, it's generally safer to simply override the semaphore value rather than recreating it
   * with a different one.
   */
  unsafeSetValue(t) {
    this._value = t, this._afterMountCallbacks.forEach((e) => e(t)), this._afterMountCallbacks = [];
  }
  /**
   * This registers a callback that will be triggered after the editor has been successfully mounted.
   *
   * 	* If the editor is already mounted, the callback will be executed immediately.
   *	* If the editor is in the process of mounting, the callback will be executed upon successful mounting.
  * 	* If the editor is never mounted, the passed callback will not be executed.
  * 	* If an exception is thrown within the callback, it will be re-thrown in the semaphore.
  */
  runAfterMount(t) {
    const { _value: e, _afterMountCallbacks: i } = this;
    e ? t(e) : i.push(t);
  }
  /**
   * This method is used to inform other components that the {@link #_element} will be used by the editor,
   * which is initialized by the {@link #_lifecycle} methods.
   *
   * 	* If an editor is already present on the provided element, the initialization of the current one
   * 	  will be postponed until the previous one is destroyed.
   *
   * 	* If the element is empty and does not have an editor attached to it, the currently locked editor will
   * 	  be mounted immediately.
   *
   * After the successful initialization of the editor and the assignment of the {@link #_value} member,
   * the `onReady` lifecycle method is called.
   *
   * *Important note:*
   *
   * It’s really important to keep this method *sync*. If we make this method *async*, it won’t work well because
   * it will cause problems when we’re trying to set up the {@link LifeCycleEditorElementSemaphore#_semaphores} map entries.
   */
  _lock() {
    const { _semaphores: t } = z0, { _state: e, _element: i, _lifecycle: s } = this, r = t.get(i) || Promise.resolve(null), o = oT();
    this._releaseLock = o;
    const a = r.then(() => e.destroyedBeforeInitialization ? Promise.resolve(void 0) : (e.mountingInProgress = s.mount().then((l) => (l && this.unsafeSetValue(l), l)), e.mountingInProgress)).then(async (l) => {
      l && s.afterMount && await s.afterMount({
        element: i,
        mountResult: l
      });
    }).then(() => o.promise).catch((l) => {
      console.error("Semaphore mounting error:", l);
    }).then(() => {
      t.get(i) === a && t.delete(i);
    });
    t.set(i, a);
  }
};
fi($0, "_semaphores", /* @__PURE__ */ new Map());
let wT = $0;
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const vT = "$__CKEditorReactContextMetadata";
function _T(n, t) {
  return {
    ...t,
    [vT]: n
  };
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const yT = ce.createContext(null), kT = (n) => !!n && typeof n == "object" && "status" in n && ["initializing", "initialized", "error"].includes(n.status), H0 = (n) => (t) => kT(t) && t.status === n, pg = H0("initializing"), CT = (n) => H0("initialized")(n) && n.watchdog.state === "ready";
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const xT = gT(
  "react",
  {
    version: "9.5.0",
    frameworkVersion: ce.version
  }
);
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function TT(n) {
  return D0(n.licenseKey) ? n : cT(n, [
    /**
     * This part of the code is not executed in open-source implementations using a GPL key.
     * It only runs when a specific license key is provided. If you are uncertain whether
     * this applies to your installation, please contact our support team.
     */
    xT
  ]);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const Eu = "Lock from React integration (@ckeditor/ckeditor5-react)";
class j0 extends ce.Component {
  constructor(t) {
    super(t), fi(this, "domContainer", ce.createRef()), fi(this, "editorSemaphore", null), this._checkVersion();
  }
  /**
   * Checks if the CKEditor version used in the application is compatible with the component.
   */
  _checkVersion() {
    const { CKEDITOR_VERSION: t } = window;
    if (!t)
      return console.warn('Cannot find the "CKEDITOR_VERSION" in the "window" scope.');
    const [e] = t.split(".").map(Number);
    e >= 42 || t.startsWith("0.0.0") || console.warn("The <CKEditor> component requires using CKEditor 5 in version 42+ or nightly build.");
  }
  get _semaphoreValue() {
    const { editorSemaphore: t } = this;
    return t ? t.value : null;
  }
  /**
   * An watchdog instance.
   */
  get watchdog() {
    const { _semaphoreValue: t } = this;
    return t ? t.watchdog : null;
  }
  /**
   * An editor instance.
   */
  get editor() {
    const { _semaphoreValue: t } = this;
    return t ? t.instance : null;
  }
  /**
   * The CKEditor component should not be updated by React itself.
   * However, if the component identifier changes, the whole structure should be created once again.
   */
  shouldComponentUpdate(t) {
    const { props: e, editorSemaphore: i } = this;
    return t.id !== e.id || t.disableWatchdog !== e.disableWatchdog ? !0 : (i && (i.runAfterMount(({ instance: s }) => {
      this._shouldUpdateEditorData(e, t, s) && s.data.set(t.data);
    }), "disabled" in t && i.runAfterMount(({ instance: s }) => {
      t.disabled ? s.enableReadOnlyMode(Eu) : s.disableReadOnlyMode(Eu);
    })), !1);
  }
  /**
   * Initialize the editor when the component is mounted.
   */
  componentDidMount() {
    pg(this.context) || this._initLifeCycleSemaphore();
  }
  /**
   * Re-render the entire component once again. The old editor will be destroyed and the new one will be created.
   */
  componentDidUpdate() {
    pg(this.context) || this._initLifeCycleSemaphore();
  }
  /**
   * Destroy the editor before unmounting the component.
   */
  componentWillUnmount() {
    this._unlockLifeCycleSemaphore();
  }
  /**
   * Async destroy attached editor and unlock element semaphore.
   */
  _unlockLifeCycleSemaphore() {
    this.editorSemaphore && (this.editorSemaphore.release(), this.editorSemaphore = null);
  }
  /**
   * Unlocks previous editor semaphore and creates new one..
   */
  _initLifeCycleSemaphore() {
    this._unlockLifeCycleSemaphore(), this.editorSemaphore = new wT(this.domContainer.current, {
      mount: async () => this._initializeEditor(),
      afterMount: ({ mountResult: t }) => {
        const { onReady: e } = this.props;
        e && this.domContainer.current !== null && e(t.instance);
      },
      unmount: async ({ element: t, mountResult: e }) => {
        const { onAfterDestroy: i } = this.props;
        try {
          await this._destroyEditor(e), t.innerHTML = "";
        } finally {
          i && i(e.instance);
        }
      }
    });
  }
  /**
   * Render a <div> element which will be replaced by CKEditor.
   */
  render() {
    return /* @__PURE__ */ ce.createElement("div", { ref: this.domContainer });
  }
  /**
   * Initializes the editor by creating a proper watchdog and initializing it with the editor's configuration.
   */
  async _initializeEditor() {
    if (this.props.disableWatchdog)
      return {
        instance: await this._createEditor(this.domContainer.current, this._getConfig()),
        watchdog: null
      };
    const t = CT(this.context) ? new ET(this.context.watchdog) : new this.props.editor.EditorWatchdog(this.props.editor, this.props.watchdogConfig), e = {
      current: 0
    };
    return t.setCreator(async (i, s) => {
      var r;
      const { editorSemaphore: o } = this, { onAfterDestroy: a } = this.props;
      e.current > 0 && a && ((r = o == null ? void 0 : o.value) != null && r.instance) && a(o.value.instance);
      const l = await this._createEditor(i, s);
      return o && e.current > 0 && (o.unsafeSetValue({
        instance: l,
        watchdog: t
      }), setTimeout(() => {
        this.props.onReady && this.props.onReady(t.editor);
      })), e.current++, l;
    }), t.on("error", (i, { error: s, causesRestart: r }) => {
      (this.props.onError || console.error)(s, { phase: "runtime", willEditorRestart: r });
    }), await t.create(this.domContainer.current, this._getConfig()).catch((i) => {
      (this.props.onError || console.error)(i, { phase: "initialization", willEditorRestart: !1 });
    }), {
      watchdog: t,
      instance: t.editor
    };
  }
  /**
   * Creates an editor from the element and configuration.
   *
   * @param element The source element.
   * @param config CKEditor 5 editor configuration.
   */
  _createEditor(t, e) {
    const { contextItemMetadata: i } = this.props;
    return i && (e = _T(i, e)), this.props.editor.create(
      t,
      TT(e)
    ).then((s) => {
      if ("disabled" in this.props) {
        /* istanbul ignore else -- @preserve */
        this.props.disabled && s.enableReadOnlyMode(Eu);
      }
      const r = s.model.document, o = s.editing.view.document;
      return r.on("change:data", (a) => {
        /* istanbul ignore else -- @preserve */
        this.props.onChange && this.props.onChange(a, s);
      }), o.on("focus", (a) => {
        /* istanbul ignore else -- @preserve */
        this.props.onFocus && this.props.onFocus(a, s);
      }), o.on("blur", (a) => {
        /* istanbul ignore else -- @preserve */
        this.props.onBlur && this.props.onBlur(a, s);
      }), s;
    });
  }
  /**
   * Destroys the editor by destroying the watchdog.
   */
  async _destroyEditor(t) {
    const { watchdog: e, instance: i } = t;
    return new Promise((s, r) => {
      /* istanbul ignore next -- @preserve */
      setTimeout(async () => {
        try {
          if (e)
            return await e.destroy(), s();
          if (i)
            return await i.destroy(), s();
          s();
        } catch (o) {
          console.error(o), r(o);
        }
      });
    });
  }
  /**
   * Returns true when the editor should be updated.
   *
   * @param prevProps Previous react's properties.
   * @param nextProps React's properties.
   * @param editor Current editor instance.
   */
  _shouldUpdateEditorData(t, e, i) {
    return !(t.data === e.data || i.data.get() === e.data);
  }
  /**
   * Returns the editor configuration.
   */
  _getConfig() {
    const t = this.props.config || {};
    return this.props.data && t.initialData && console.warn(
      "Editor data should be provided either using `config.initialData` or `content` property. The config value takes precedence over `content` property and will be used when both are specified."
    ), {
      ...t,
      initialData: t.initialData || this.props.data || ""
    };
  }
}
fi(j0, "contextType", yT);
class ET {
  /**
   * @param contextWatchdog The context watchdog instance that will be wrapped into editor watchdog API.
   */
  constructor(t) {
    fi(this, "_contextWatchdog"), fi(this, "_id"), fi(this, "_creator"), this._contextWatchdog = t, this._id = lT();
  }
  /**
   *  @param creator A watchdog's editor creator function.
   */
  setCreator(t) {
    this._creator = t;
  }
  /**
   * Adds an editor configuration to the context watchdog registry. Creates an instance of it.
   *
   * @param sourceElementOrData A source element or data for the new editor.
   * @param config CKEditor 5 editor config.
   */
  create(t, e) {
    return this._contextWatchdog.add({
      sourceElementOrData: t,
      config: e,
      creator: this._creator,
      id: this._id,
      type: "editor"
    });
  }
  /**
   * Creates a listener that is attached to context watchdog's item and run when the context watchdog fires.
   * Currently works only for the `error` event.
   */
  on(t, e) {
    this._contextWatchdog.on("itemError", (i, { itemId: s, error: r }) => {
      s === this._id && e(null, { error: r, causesRestart: void 0 });
    });
  }
  destroy() {
    return this._contextWatchdog.state === "ready" ? this._contextWatchdog.remove(this._id) : Promise.resolve();
  }
  /**
   * An editor instance.
   */
  get editor() {
    return this._contextWatchdog.getItem(this._id);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function U0(...n) {
  return (t) => {
    n.forEach((e) => {
      typeof e == "function" ? e(t) : e != null && (e.current = t);
    });
  };
}
const ST = F2(Ic(({ id: n, semaphore: t, rootName: e }, i) => {
  const s = yt(null);
  return ht(() => {
    let r, o;
    return t.runAfterMount(({ instance: a }) => {
      if (!s.current)
        return;
      o = a;
      const { ui: l, model: c } = o, u = c.document.getRoot(e);
      u && o.ui.getEditableElement(e) && o.detachEditable(u), r = l.view.createEditable(e, s.current), l.addEditable(r), a.editing.view.forceRender();
    }), () => {
      if (o && o.state !== "destroyed" && s.current) {
        const a = o.model.document.getRoot(e);
        /* istanbul ignore else -- @preserve */
        a && o.detachEditable(a);
      }
    };
  }, [t.revision]), /* @__PURE__ */ ce.createElement(
    "div",
    {
      key: t.revision,
      id: n,
      ref: U0(i, s)
    }
  );
}));
ST.displayName = "EditorEditable";
const AT = Ic(({ editor: n }, t) => {
  const e = yt(null);
  return ht(() => {
    const i = e.current;
    if (!n || !i)
      return;
    const s = n.ui.view.toolbar.element;
    return i.appendChild(s), () => {
      i.contains(s) && i.removeChild(s);
    };
  }, [n && n.id]), /* @__PURE__ */ ce.createElement("div", { ref: U0(e, t) });
});
AT.displayName = "EditorToolbarWrapper";
var W0 = typeof global == "object" && global && global.Object === Object && global, IT = typeof self == "object" && self && self.Object === Object && self, nn = W0 || IT || Function("return this")(), _n = nn.Symbol, q0 = Object.prototype, PT = q0.hasOwnProperty, RT = q0.toString, ho = _n ? _n.toStringTag : void 0;
function OT(n) {
  var t = PT.call(n, ho), e = n[ho];
  try {
    n[ho] = void 0;
    var i = !0;
  } catch {
  }
  var s = RT.call(n);
  return i && (t ? n[ho] = e : delete n[ho]), s;
}
var VT = Object.prototype, LT = VT.toString;
function BT(n) {
  return LT.call(n);
}
var MT = "[object Null]", NT = "[object Undefined]", bg = _n ? _n.toStringTag : void 0;
function ps(n) {
  return n == null ? n === void 0 ? NT : MT : bg && bg in Object(n) ? OT(n) : BT(n);
}
function Ni(n) {
  return n != null && typeof n == "object";
}
var FT = "[object Symbol]";
function Nc(n) {
  return typeof n == "symbol" || Ni(n) && ps(n) == FT;
}
function G0(n, t) {
  for (var e = -1, i = n == null ? 0 : n.length, s = Array(i); ++e < i; )
    s[e] = t(n[e], e, n);
  return s;
}
var wi = Array.isArray, wg = _n ? _n.prototype : void 0, vg = wg ? wg.toString : void 0;
function K0(n) {
  if (typeof n == "string")
    return n;
  if (wi(n))
    return G0(n, K0) + "";
  if (Nc(n))
    return vg ? vg.call(n) : "";
  var t = n + "";
  return t == "0" && 1 / n == -1 / 0 ? "-0" : t;
}
var DT = /\s/;
function $T(n) {
  for (var t = n.length; t-- && DT.test(n.charAt(t)); )
    ;
  return t;
}
var zT = /^\s+/;
function HT(n) {
  return n && n.slice(0, $T(n) + 1).replace(zT, "");
}
function nt(n) {
  var t = typeof n;
  return n != null && (t == "object" || t == "function");
}
var _g = NaN, jT = /^[-+]0x[0-9a-f]+$/i, UT = /^0b[01]+$/i, WT = /^0o[0-7]+$/i, qT = parseInt;
function yg(n) {
  if (typeof n == "number")
    return n;
  if (Nc(n))
    return _g;
  if (nt(n)) {
    var t = typeof n.valueOf == "function" ? n.valueOf() : n;
    n = nt(t) ? t + "" : t;
  }
  if (typeof n != "string")
    return n === 0 ? n : +n;
  n = HT(n);
  var e = UT.test(n);
  return e || WT.test(n) ? qT(n.slice(2), e ? 2 : 8) : jT.test(n) ? _g : +n;
}
function cs(n) {
  return n;
}
var GT = "[object AsyncFunction]", KT = "[object Function]", ZT = "[object GeneratorFunction]", YT = "[object Proxy]";
function Fs(n) {
  if (!nt(n))
    return !1;
  var t = ps(n);
  return t == KT || t == ZT || t == GT || t == YT;
}
var Su = nn["__core-js_shared__"], kg = function() {
  var n = /[^.]+$/.exec(Su && Su.keys && Su.keys.IE_PROTO || "");
  return n ? "Symbol(src)_1." + n : "";
}();
function JT(n) {
  return !!kg && kg in n;
}
var XT = Function.prototype, QT = XT.toString;
function Ws(n) {
  if (n != null) {
    try {
      return QT.call(n);
    } catch {
    }
    try {
      return n + "";
    } catch {
    }
  }
  return "";
}
var eE = /[\\^$.*+?()[\]{}|]/g, tE = /^\[object .+?Constructor\]$/, iE = Function.prototype, nE = Object.prototype, sE = iE.toString, rE = nE.hasOwnProperty, oE = RegExp(
  "^" + sE.call(rE).replace(eE, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
);
function aE(n) {
  if (!nt(n) || JT(n))
    return !1;
  var t = Fs(n) ? oE : tE;
  return t.test(Ws(n));
}
function lE(n, t) {
  return n == null ? void 0 : n[t];
}
function qs(n, t) {
  var e = lE(n, t);
  return aE(e) ? e : void 0;
}
var Ud = qs(nn, "WeakMap"), Cg = Object.create, cE = /* @__PURE__ */ function() {
  function n() {
  }
  return function(t) {
    if (!nt(t))
      return {};
    if (Cg)
      return Cg(t);
    n.prototype = t;
    var e = new n();
    return n.prototype = void 0, e;
  };
}();
function uE(n, t, e) {
  switch (e.length) {
    case 0:
      return n.call(t);
    case 1:
      return n.call(t, e[0]);
    case 2:
      return n.call(t, e[0], e[1]);
    case 3:
      return n.call(t, e[0], e[1], e[2]);
  }
  return n.apply(t, e);
}
function Z0(n, t) {
  var e = -1, i = n.length;
  for (t || (t = Array(i)); ++e < i; )
    t[e] = n[e];
  return t;
}
var dE = 800, hE = 16, fE = Date.now;
function mE(n) {
  var t = 0, e = 0;
  return function() {
    var i = fE(), s = hE - (i - e);
    if (e = i, s > 0) {
      if (++t >= dE)
        return arguments[0];
    } else
      t = 0;
    return n.apply(void 0, arguments);
  };
}
function gE(n) {
  return function() {
    return n;
  };
}
var Wl = function() {
  try {
    var n = qs(Object, "defineProperty");
    return n({}, "", {}), n;
  } catch {
  }
}(), pE = Wl ? function(n, t) {
  return Wl(n, "toString", {
    configurable: !0,
    enumerable: !1,
    value: gE(t),
    writable: !0
  });
} : cs, bE = mE(pE);
function wE(n, t) {
  for (var e = -1, i = n == null ? 0 : n.length; ++e < i && t(n[e], e, n) !== !1; )
    ;
  return n;
}
var vE = 9007199254740991, _E = /^(?:0|[1-9]\d*)$/;
function Fc(n, t) {
  var e = typeof n;
  return t = t ?? vE, !!t && (e == "number" || e != "symbol" && _E.test(n)) && n > -1 && n % 1 == 0 && n < t;
}
function Dc(n, t, e) {
  t == "__proto__" && Wl ? Wl(n, t, {
    configurable: !0,
    enumerable: !0,
    value: e,
    writable: !0
  }) : n[t] = e;
}
function Sa(n, t) {
  return n === t || n !== n && t !== t;
}
var yE = Object.prototype, kE = yE.hasOwnProperty;
function nf(n, t, e) {
  var i = n[t];
  (!(kE.call(n, t) && Sa(i, e)) || e === void 0 && !(t in n)) && Dc(n, t, e);
}
function Jr(n, t, e, i) {
  var s = !e;
  e || (e = {});
  for (var r = -1, o = t.length; ++r < o; ) {
    var a = t[r], l = void 0;
    l === void 0 && (l = n[a]), s ? Dc(e, a, l) : nf(e, a, l);
  }
  return e;
}
var xg = Math.max;
function CE(n, t, e) {
  return t = xg(t === void 0 ? n.length - 1 : t, 0), function() {
    for (var i = arguments, s = -1, r = xg(i.length - t, 0), o = Array(r); ++s < r; )
      o[s] = i[t + s];
    s = -1;
    for (var a = Array(t + 1); ++s < t; )
      a[s] = i[s];
    return a[t] = e(o), uE(n, this, a);
  };
}
function xE(n, t) {
  return bE(CE(n, t, cs), n + "");
}
var TE = 9007199254740991;
function sf(n) {
  return typeof n == "number" && n > -1 && n % 1 == 0 && n <= TE;
}
function Xr(n) {
  return n != null && sf(n.length) && !Fs(n);
}
function EE(n, t, e) {
  if (!nt(e))
    return !1;
  var i = typeof t;
  return (i == "number" ? Xr(e) && Fc(t, e.length) : i == "string" && t in e) ? Sa(e[t], n) : !1;
}
function rf(n) {
  return xE(function(t, e) {
    var i = -1, s = e.length, r = s > 1 ? e[s - 1] : void 0, o = s > 2 ? e[2] : void 0;
    for (r = n.length > 3 && typeof r == "function" ? (s--, r) : void 0, o && EE(e[0], e[1], o) && (r = s < 3 ? void 0 : r, s = 1), t = Object(t); ++i < s; ) {
      var a = e[i];
      a && n(t, a, i, r);
    }
    return t;
  });
}
var SE = Object.prototype;
function of(n) {
  var t = n && n.constructor, e = typeof t == "function" && t.prototype || SE;
  return n === e;
}
function AE(n, t) {
  for (var e = -1, i = Array(n); ++e < n; )
    i[e] = t(e);
  return i;
}
var IE = "[object Arguments]";
function Tg(n) {
  return Ni(n) && ps(n) == IE;
}
var Y0 = Object.prototype, PE = Y0.hasOwnProperty, RE = Y0.propertyIsEnumerable, ql = Tg(/* @__PURE__ */ function() {
  return arguments;
}()) ? Tg : function(n) {
  return Ni(n) && PE.call(n, "callee") && !RE.call(n, "callee");
};
function OE() {
  return !1;
}
var J0 = typeof exports == "object" && exports && !exports.nodeType && exports, Eg = J0 && typeof module == "object" && module && !module.nodeType && module, VE = Eg && Eg.exports === J0, Sg = VE ? nn.Buffer : void 0, LE = Sg ? Sg.isBuffer : void 0, ea = LE || OE, BE = "[object Arguments]", ME = "[object Array]", NE = "[object Boolean]", FE = "[object Date]", DE = "[object Error]", $E = "[object Function]", zE = "[object Map]", HE = "[object Number]", jE = "[object Object]", UE = "[object RegExp]", WE = "[object Set]", qE = "[object String]", GE = "[object WeakMap]", KE = "[object ArrayBuffer]", ZE = "[object DataView]", YE = "[object Float32Array]", JE = "[object Float64Array]", XE = "[object Int8Array]", QE = "[object Int16Array]", eS = "[object Int32Array]", tS = "[object Uint8Array]", iS = "[object Uint8ClampedArray]", nS = "[object Uint16Array]", sS = "[object Uint32Array]", dt = {};
dt[YE] = dt[JE] = dt[XE] = dt[QE] = dt[eS] = dt[tS] = dt[iS] = dt[nS] = dt[sS] = !0;
dt[BE] = dt[ME] = dt[KE] = dt[NE] = dt[ZE] = dt[FE] = dt[DE] = dt[$E] = dt[zE] = dt[HE] = dt[jE] = dt[UE] = dt[WE] = dt[qE] = dt[GE] = !1;
function rS(n) {
  return Ni(n) && sf(n.length) && !!dt[ps(n)];
}
function af(n) {
  return function(t) {
    return n(t);
  };
}
var X0 = typeof exports == "object" && exports && !exports.nodeType && exports, No = X0 && typeof module == "object" && module && !module.nodeType && module, oS = No && No.exports === X0, Au = oS && W0.process, Nr = function() {
  try {
    var n = No && No.require && No.require("util").types;
    return n || Au && Au.binding && Au.binding("util");
  } catch {
  }
}(), Ag = Nr && Nr.isTypedArray, lf = Ag ? af(Ag) : rS, aS = Object.prototype, lS = aS.hasOwnProperty;
function Q0(n, t) {
  var e = wi(n), i = !e && ql(n), s = !e && !i && ea(n), r = !e && !i && !s && lf(n), o = e || i || s || r, a = o ? AE(n.length, String) : [], l = a.length;
  for (var c in n)
    (t || lS.call(n, c)) && !(o && // Safari 9 has enumerable `arguments.length` in strict mode.
    (c == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
    s && (c == "offset" || c == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
    r && (c == "buffer" || c == "byteLength" || c == "byteOffset") || // Skip index properties.
    Fc(c, l))) && a.push(c);
  return a;
}
function ev(n, t) {
  return function(e) {
    return n(t(e));
  };
}
var cS = ev(Object.keys, Object), uS = Object.prototype, dS = uS.hasOwnProperty;
function hS(n) {
  if (!of(n))
    return cS(n);
  var t = [];
  for (var e in Object(n))
    dS.call(n, e) && e != "constructor" && t.push(e);
  return t;
}
function Aa(n) {
  return Xr(n) ? Q0(n) : hS(n);
}
function fS(n) {
  var t = [];
  if (n != null)
    for (var e in Object(n))
      t.push(e);
  return t;
}
var mS = Object.prototype, gS = mS.hasOwnProperty;
function pS(n) {
  if (!nt(n))
    return fS(n);
  var t = of(n), e = [];
  for (var i in n)
    i == "constructor" && (t || !gS.call(n, i)) || e.push(i);
  return e;
}
function Qr(n) {
  return Xr(n) ? Q0(n, !0) : pS(n);
}
var tv = rf(function(n, t) {
  Jr(t, Qr(t), n);
}), bS = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/, wS = /^\w*$/;
function cf(n, t) {
  if (wi(n))
    return !1;
  var e = typeof n;
  return e == "number" || e == "symbol" || e == "boolean" || n == null || Nc(n) ? !0 : wS.test(n) || !bS.test(n) || t != null && n in Object(t);
}
var ta = qs(Object, "create");
function vS() {
  this.__data__ = ta ? ta(null) : {}, this.size = 0;
}
function _S(n) {
  var t = this.has(n) && delete this.__data__[n];
  return this.size -= t ? 1 : 0, t;
}
var yS = "__lodash_hash_undefined__", kS = Object.prototype, CS = kS.hasOwnProperty;
function xS(n) {
  var t = this.__data__;
  if (ta) {
    var e = t[n];
    return e === yS ? void 0 : e;
  }
  return CS.call(t, n) ? t[n] : void 0;
}
var TS = Object.prototype, ES = TS.hasOwnProperty;
function SS(n) {
  var t = this.__data__;
  return ta ? t[n] !== void 0 : ES.call(t, n);
}
var AS = "__lodash_hash_undefined__";
function IS(n, t) {
  var e = this.__data__;
  return this.size += this.has(n) ? 0 : 1, e[n] = ta && t === void 0 ? AS : t, this;
}
function Ds(n) {
  var t = -1, e = n == null ? 0 : n.length;
  for (this.clear(); ++t < e; ) {
    var i = n[t];
    this.set(i[0], i[1]);
  }
}
Ds.prototype.clear = vS;
Ds.prototype.delete = _S;
Ds.prototype.get = xS;
Ds.prototype.has = SS;
Ds.prototype.set = IS;
function PS() {
  this.__data__ = [], this.size = 0;
}
function $c(n, t) {
  for (var e = n.length; e--; )
    if (Sa(n[e][0], t))
      return e;
  return -1;
}
var RS = Array.prototype, OS = RS.splice;
function VS(n) {
  var t = this.__data__, e = $c(t, n);
  if (e < 0)
    return !1;
  var i = t.length - 1;
  return e == i ? t.pop() : OS.call(t, e, 1), --this.size, !0;
}
function LS(n) {
  var t = this.__data__, e = $c(t, n);
  return e < 0 ? void 0 : t[e][1];
}
function BS(n) {
  return $c(this.__data__, n) > -1;
}
function MS(n, t) {
  var e = this.__data__, i = $c(e, n);
  return i < 0 ? (++this.size, e.push([n, t])) : e[i][1] = t, this;
}
function Yn(n) {
  var t = -1, e = n == null ? 0 : n.length;
  for (this.clear(); ++t < e; ) {
    var i = n[t];
    this.set(i[0], i[1]);
  }
}
Yn.prototype.clear = PS;
Yn.prototype.delete = VS;
Yn.prototype.get = LS;
Yn.prototype.has = BS;
Yn.prototype.set = MS;
var ia = qs(nn, "Map");
function NS() {
  this.size = 0, this.__data__ = {
    hash: new Ds(),
    map: new (ia || Yn)(),
    string: new Ds()
  };
}
function FS(n) {
  var t = typeof n;
  return t == "string" || t == "number" || t == "symbol" || t == "boolean" ? n !== "__proto__" : n === null;
}
function zc(n, t) {
  var e = n.__data__;
  return FS(t) ? e[typeof t == "string" ? "string" : "hash"] : e.map;
}
function DS(n) {
  var t = zc(this, n).delete(n);
  return this.size -= t ? 1 : 0, t;
}
function $S(n) {
  return zc(this, n).get(n);
}
function zS(n) {
  return zc(this, n).has(n);
}
function HS(n, t) {
  var e = zc(this, n), i = e.size;
  return e.set(n, t), this.size += e.size == i ? 0 : 1, this;
}
function Jn(n) {
  var t = -1, e = n == null ? 0 : n.length;
  for (this.clear(); ++t < e; ) {
    var i = n[t];
    this.set(i[0], i[1]);
  }
}
Jn.prototype.clear = NS;
Jn.prototype.delete = DS;
Jn.prototype.get = $S;
Jn.prototype.has = zS;
Jn.prototype.set = HS;
var jS = "Expected a function";
function uf(n, t) {
  if (typeof n != "function" || t != null && typeof t != "function")
    throw new TypeError(jS);
  var e = function() {
    var i = arguments, s = t ? t.apply(this, i) : i[0], r = e.cache;
    if (r.has(s))
      return r.get(s);
    var o = n.apply(this, i);
    return e.cache = r.set(s, o) || r, o;
  };
  return e.cache = new (uf.Cache || Jn)(), e;
}
uf.Cache = Jn;
var US = 500;
function WS(n) {
  var t = uf(n, function(i) {
    return e.size === US && e.clear(), i;
  }), e = t.cache;
  return t;
}
var qS = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g, GS = /\\(\\)?/g, KS = WS(function(n) {
  var t = [];
  return n.charCodeAt(0) === 46 && t.push(""), n.replace(qS, function(e, i, s, r) {
    t.push(s ? r.replace(GS, "$1") : i || e);
  }), t;
});
function Ia(n) {
  return n == null ? "" : K0(n);
}
function Hc(n, t) {
  return wi(n) ? n : cf(n, t) ? [n] : KS(Ia(n));
}
function eo(n) {
  if (typeof n == "string" || Nc(n))
    return n;
  var t = n + "";
  return t == "0" && 1 / n == -1 / 0 ? "-0" : t;
}
function df(n, t) {
  t = Hc(t, n);
  for (var e = 0, i = t.length; n != null && e < i; )
    n = n[eo(t[e++])];
  return e && e == i ? n : void 0;
}
function Fr(n, t, e) {
  var i = n == null ? void 0 : df(n, t);
  return i === void 0 ? e : i;
}
function iv(n, t) {
  for (var e = -1, i = t.length, s = n.length; ++e < i; )
    n[s + e] = t[e];
  return n;
}
var hf = ev(Object.getPrototypeOf, Object), ZS = "[object Object]", YS = Function.prototype, JS = Object.prototype, nv = YS.toString, XS = JS.hasOwnProperty, QS = nv.call(Object);
function qi(n) {
  if (!Ni(n) || ps(n) != ZS)
    return !1;
  var t = hf(n);
  if (t === null)
    return !0;
  var e = XS.call(t, "constructor") && t.constructor;
  return typeof e == "function" && e instanceof e && nv.call(e) == QS;
}
function sv(n, t, e) {
  var i = -1, s = n.length;
  t < 0 && (t = -t > s ? 0 : s + t), e = e > s ? s : e, e < 0 && (e += s), s = t > e ? 0 : e - t >>> 0, t >>>= 0;
  for (var r = Array(s); ++i < s; )
    r[i] = n[i + t];
  return r;
}
function eA(n, t, e) {
  var i = n.length;
  return e = e === void 0 ? i : e, sv(n, t, e);
}
var tA = "\\ud800-\\udfff", iA = "\\u0300-\\u036f", nA = "\\ufe20-\\ufe2f", sA = "\\u20d0-\\u20ff", rA = iA + nA + sA, oA = "\\ufe0e\\ufe0f", aA = "\\u200d", lA = RegExp("[" + aA + tA + rA + oA + "]");
function rv(n) {
  return lA.test(n);
}
function cA(n) {
  return n.split("");
}
var ov = "\\ud800-\\udfff", uA = "\\u0300-\\u036f", dA = "\\ufe20-\\ufe2f", hA = "\\u20d0-\\u20ff", fA = uA + dA + hA, mA = "\\ufe0e\\ufe0f", gA = "[" + ov + "]", Wd = "[" + fA + "]", qd = "\\ud83c[\\udffb-\\udfff]", pA = "(?:" + Wd + "|" + qd + ")", av = "[^" + ov + "]", lv = "(?:\\ud83c[\\udde6-\\uddff]){2}", cv = "[\\ud800-\\udbff][\\udc00-\\udfff]", bA = "\\u200d", uv = pA + "?", dv = "[" + mA + "]?", wA = "(?:" + bA + "(?:" + [av, lv, cv].join("|") + ")" + dv + uv + ")*", vA = dv + uv + wA, _A = "(?:" + [av + Wd + "?", Wd, lv, cv, gA].join("|") + ")", yA = RegExp(qd + "(?=" + qd + ")|" + _A + vA, "g");
function kA(n) {
  return n.match(yA) || [];
}
function CA(n) {
  return rv(n) ? kA(n) : cA(n);
}
function xA(n) {
  return function(t) {
    t = Ia(t);
    var e = rv(t) ? CA(t) : void 0, i = e ? e[0] : t.charAt(0), s = e ? eA(e, 1).join("") : t.slice(1);
    return i[n]() + s;
  };
}
var hv = xA("toUpperCase");
function TA(n, t, e, i) {
  for (var s = -1, r = n == null ? 0 : n.length; ++s < r; )
    e = t(e, n[s], s, n);
  return e;
}
function EA(n) {
  return function(t) {
    return n == null ? void 0 : n[t];
  };
}
var SA = {
  // Latin-1 Supplement block.
  À: "A",
  Á: "A",
  Â: "A",
  Ã: "A",
  Ä: "A",
  Å: "A",
  à: "a",
  á: "a",
  â: "a",
  ã: "a",
  ä: "a",
  å: "a",
  Ç: "C",
  ç: "c",
  Ð: "D",
  ð: "d",
  È: "E",
  É: "E",
  Ê: "E",
  Ë: "E",
  è: "e",
  é: "e",
  ê: "e",
  ë: "e",
  Ì: "I",
  Í: "I",
  Î: "I",
  Ï: "I",
  ì: "i",
  í: "i",
  î: "i",
  ï: "i",
  Ñ: "N",
  ñ: "n",
  Ò: "O",
  Ó: "O",
  Ô: "O",
  Õ: "O",
  Ö: "O",
  Ø: "O",
  ò: "o",
  ó: "o",
  ô: "o",
  õ: "o",
  ö: "o",
  ø: "o",
  Ù: "U",
  Ú: "U",
  Û: "U",
  Ü: "U",
  ù: "u",
  ú: "u",
  û: "u",
  ü: "u",
  Ý: "Y",
  ý: "y",
  ÿ: "y",
  Æ: "Ae",
  æ: "ae",
  Þ: "Th",
  þ: "th",
  ß: "ss",
  // Latin Extended-A block.
  Ā: "A",
  Ă: "A",
  Ą: "A",
  ā: "a",
  ă: "a",
  ą: "a",
  Ć: "C",
  Ĉ: "C",
  Ċ: "C",
  Č: "C",
  ć: "c",
  ĉ: "c",
  ċ: "c",
  č: "c",
  Ď: "D",
  Đ: "D",
  ď: "d",
  đ: "d",
  Ē: "E",
  Ĕ: "E",
  Ė: "E",
  Ę: "E",
  Ě: "E",
  ē: "e",
  ĕ: "e",
  ė: "e",
  ę: "e",
  ě: "e",
  Ĝ: "G",
  Ğ: "G",
  Ġ: "G",
  Ģ: "G",
  ĝ: "g",
  ğ: "g",
  ġ: "g",
  ģ: "g",
  Ĥ: "H",
  Ħ: "H",
  ĥ: "h",
  ħ: "h",
  Ĩ: "I",
  Ī: "I",
  Ĭ: "I",
  Į: "I",
  İ: "I",
  ĩ: "i",
  ī: "i",
  ĭ: "i",
  į: "i",
  ı: "i",
  Ĵ: "J",
  ĵ: "j",
  Ķ: "K",
  ķ: "k",
  ĸ: "k",
  Ĺ: "L",
  Ļ: "L",
  Ľ: "L",
  Ŀ: "L",
  Ł: "L",
  ĺ: "l",
  ļ: "l",
  ľ: "l",
  ŀ: "l",
  ł: "l",
  Ń: "N",
  Ņ: "N",
  Ň: "N",
  Ŋ: "N",
  ń: "n",
  ņ: "n",
  ň: "n",
  ŋ: "n",
  Ō: "O",
  Ŏ: "O",
  Ő: "O",
  ō: "o",
  ŏ: "o",
  ő: "o",
  Ŕ: "R",
  Ŗ: "R",
  Ř: "R",
  ŕ: "r",
  ŗ: "r",
  ř: "r",
  Ś: "S",
  Ŝ: "S",
  Ş: "S",
  Š: "S",
  ś: "s",
  ŝ: "s",
  ş: "s",
  š: "s",
  Ţ: "T",
  Ť: "T",
  Ŧ: "T",
  ţ: "t",
  ť: "t",
  ŧ: "t",
  Ũ: "U",
  Ū: "U",
  Ŭ: "U",
  Ů: "U",
  Ű: "U",
  Ų: "U",
  ũ: "u",
  ū: "u",
  ŭ: "u",
  ů: "u",
  ű: "u",
  ų: "u",
  Ŵ: "W",
  ŵ: "w",
  Ŷ: "Y",
  ŷ: "y",
  Ÿ: "Y",
  Ź: "Z",
  Ż: "Z",
  Ž: "Z",
  ź: "z",
  ż: "z",
  ž: "z",
  Ĳ: "IJ",
  ĳ: "ij",
  Œ: "Oe",
  œ: "oe",
  ŉ: "'n",
  ſ: "s"
}, AA = EA(SA), IA = /[\xc0-\xd6\xd8-\xf6\xf8-\xff\u0100-\u017f]/g, PA = "\\u0300-\\u036f", RA = "\\ufe20-\\ufe2f", OA = "\\u20d0-\\u20ff", VA = PA + RA + OA, LA = "[" + VA + "]", BA = RegExp(LA, "g");
function MA(n) {
  return n = Ia(n), n && n.replace(IA, AA).replace(BA, "");
}
var NA = /[^\x00-\x2f\x3a-\x40\x5b-\x60\x7b-\x7f]+/g;
function FA(n) {
  return n.match(NA) || [];
}
var DA = /[a-z][A-Z]|[A-Z]{2}[a-z]|[0-9][a-zA-Z]|[a-zA-Z][0-9]|[^a-zA-Z0-9 ]/;
function $A(n) {
  return DA.test(n);
}
var fv = "\\ud800-\\udfff", zA = "\\u0300-\\u036f", HA = "\\ufe20-\\ufe2f", jA = "\\u20d0-\\u20ff", UA = zA + HA + jA, mv = "\\u2700-\\u27bf", gv = "a-z\\xdf-\\xf6\\xf8-\\xff", WA = "\\xac\\xb1\\xd7\\xf7", qA = "\\x00-\\x2f\\x3a-\\x40\\x5b-\\x60\\x7b-\\xbf", GA = "\\u2000-\\u206f", KA = " \\t\\x0b\\f\\xa0\\ufeff\\n\\r\\u2028\\u2029\\u1680\\u180e\\u2000\\u2001\\u2002\\u2003\\u2004\\u2005\\u2006\\u2007\\u2008\\u2009\\u200a\\u202f\\u205f\\u3000", pv = "A-Z\\xc0-\\xd6\\xd8-\\xde", ZA = "\\ufe0e\\ufe0f", bv = WA + qA + GA + KA, wv = "['’]", Ig = "[" + bv + "]", YA = "[" + UA + "]", vv = "\\d+", JA = "[" + mv + "]", _v = "[" + gv + "]", yv = "[^" + fv + bv + vv + mv + gv + pv + "]", XA = "\\ud83c[\\udffb-\\udfff]", QA = "(?:" + YA + "|" + XA + ")", e3 = "[^" + fv + "]", kv = "(?:\\ud83c[\\udde6-\\uddff]){2}", Cv = "[\\ud800-\\udbff][\\udc00-\\udfff]", yr = "[" + pv + "]", t3 = "\\u200d", Pg = "(?:" + _v + "|" + yv + ")", i3 = "(?:" + yr + "|" + yv + ")", Rg = "(?:" + wv + "(?:d|ll|m|re|s|t|ve))?", Og = "(?:" + wv + "(?:D|LL|M|RE|S|T|VE))?", xv = QA + "?", Tv = "[" + ZA + "]?", n3 = "(?:" + t3 + "(?:" + [e3, kv, Cv].join("|") + ")" + Tv + xv + ")*", s3 = "\\d*(?:1st|2nd|3rd|(?![123])\\dth)(?=\\b|[A-Z_])", r3 = "\\d*(?:1ST|2ND|3RD|(?![123])\\dTH)(?=\\b|[a-z_])", o3 = Tv + xv + n3, a3 = "(?:" + [JA, kv, Cv].join("|") + ")" + o3, l3 = RegExp([
  yr + "?" + _v + "+" + Rg + "(?=" + [Ig, yr, "$"].join("|") + ")",
  i3 + "+" + Og + "(?=" + [Ig, yr + Pg, "$"].join("|") + ")",
  yr + "?" + Pg + "+" + Rg,
  yr + "+" + Og,
  r3,
  s3,
  vv,
  a3
].join("|"), "g");
function c3(n) {
  return n.match(l3) || [];
}
function u3(n, t, e) {
  return n = Ia(n), t = t, t === void 0 ? $A(n) ? c3(n) : FA(n) : n.match(t) || [];
}
var d3 = "['’]", h3 = RegExp(d3, "g");
function f3(n) {
  return function(t) {
    return TA(u3(MA(t).replace(h3, "")), n, "");
  };
}
function m3() {
  this.__data__ = new Yn(), this.size = 0;
}
function g3(n) {
  var t = this.__data__, e = t.delete(n);
  return this.size = t.size, e;
}
function p3(n) {
  return this.__data__.get(n);
}
function b3(n) {
  return this.__data__.has(n);
}
var w3 = 200;
function v3(n, t) {
  var e = this.__data__;
  if (e instanceof Yn) {
    var i = e.__data__;
    if (!ia || i.length < w3 - 1)
      return i.push([n, t]), this.size = ++e.size, this;
    e = this.__data__ = new Jn(i);
  }
  return e.set(n, t), this.size = e.size, this;
}
function Yi(n) {
  var t = this.__data__ = new Yn(n);
  this.size = t.size;
}
Yi.prototype.clear = m3;
Yi.prototype.delete = g3;
Yi.prototype.get = p3;
Yi.prototype.has = b3;
Yi.prototype.set = v3;
function _3(n, t) {
  return n && Jr(t, Aa(t), n);
}
function y3(n, t) {
  return n && Jr(t, Qr(t), n);
}
var Ev = typeof exports == "object" && exports && !exports.nodeType && exports, Vg = Ev && typeof module == "object" && module && !module.nodeType && module, k3 = Vg && Vg.exports === Ev, Lg = k3 ? nn.Buffer : void 0, Bg = Lg ? Lg.allocUnsafe : void 0;
function Sv(n, t) {
  if (t)
    return n.slice();
  var e = n.length, i = Bg ? Bg(e) : new n.constructor(e);
  return n.copy(i), i;
}
function C3(n, t) {
  for (var e = -1, i = n == null ? 0 : n.length, s = 0, r = []; ++e < i; ) {
    var o = n[e];
    t(o, e, n) && (r[s++] = o);
  }
  return r;
}
function Av() {
  return [];
}
var x3 = Object.prototype, T3 = x3.propertyIsEnumerable, Mg = Object.getOwnPropertySymbols, ff = Mg ? function(n) {
  return n == null ? [] : (n = Object(n), C3(Mg(n), function(t) {
    return T3.call(n, t);
  }));
} : Av;
function E3(n, t) {
  return Jr(n, ff(n), t);
}
var S3 = Object.getOwnPropertySymbols, Iv = S3 ? function(n) {
  for (var t = []; n; )
    iv(t, ff(n)), n = hf(n);
  return t;
} : Av;
function A3(n, t) {
  return Jr(n, Iv(n), t);
}
function Pv(n, t, e) {
  var i = t(n);
  return wi(n) ? i : iv(i, e(n));
}
function Gd(n) {
  return Pv(n, Aa, ff);
}
function I3(n) {
  return Pv(n, Qr, Iv);
}
var Kd = qs(nn, "DataView"), Zd = qs(nn, "Promise"), Yd = qs(nn, "Set"), Ng = "[object Map]", P3 = "[object Object]", Fg = "[object Promise]", Dg = "[object Set]", $g = "[object WeakMap]", zg = "[object DataView]", R3 = Ws(Kd), O3 = Ws(ia), V3 = Ws(Zd), L3 = Ws(Yd), B3 = Ws(Ud), ji = ps;
(Kd && ji(new Kd(new ArrayBuffer(1))) != zg || ia && ji(new ia()) != Ng || Zd && ji(Zd.resolve()) != Fg || Yd && ji(new Yd()) != Dg || Ud && ji(new Ud()) != $g) && (ji = function(n) {
  var t = ps(n), e = t == P3 ? n.constructor : void 0, i = e ? Ws(e) : "";
  if (i)
    switch (i) {
      case R3:
        return zg;
      case O3:
        return Ng;
      case V3:
        return Fg;
      case L3:
        return Dg;
      case B3:
        return $g;
    }
  return t;
});
var M3 = Object.prototype, N3 = M3.hasOwnProperty;
function F3(n) {
  var t = n.length, e = new n.constructor(t);
  return t && typeof n[0] == "string" && N3.call(n, "index") && (e.index = n.index, e.input = n.input), e;
}
var Gl = nn.Uint8Array;
function mf(n) {
  var t = new n.constructor(n.byteLength);
  return new Gl(t).set(new Gl(n)), t;
}
function D3(n, t) {
  var e = t ? mf(n.buffer) : n.buffer;
  return new n.constructor(e, n.byteOffset, n.byteLength);
}
var $3 = /\w*$/;
function z3(n) {
  var t = new n.constructor(n.source, $3.exec(n));
  return t.lastIndex = n.lastIndex, t;
}
var Hg = _n ? _n.prototype : void 0, jg = Hg ? Hg.valueOf : void 0;
function H3(n) {
  return jg ? Object(jg.call(n)) : {};
}
function Rv(n, t) {
  var e = t ? mf(n.buffer) : n.buffer;
  return new n.constructor(e, n.byteOffset, n.length);
}
var j3 = "[object Boolean]", U3 = "[object Date]", W3 = "[object Map]", q3 = "[object Number]", G3 = "[object RegExp]", K3 = "[object Set]", Z3 = "[object String]", Y3 = "[object Symbol]", J3 = "[object ArrayBuffer]", X3 = "[object DataView]", Q3 = "[object Float32Array]", eI = "[object Float64Array]", tI = "[object Int8Array]", iI = "[object Int16Array]", nI = "[object Int32Array]", sI = "[object Uint8Array]", rI = "[object Uint8ClampedArray]", oI = "[object Uint16Array]", aI = "[object Uint32Array]";
function lI(n, t, e) {
  var i = n.constructor;
  switch (t) {
    case J3:
      return mf(n);
    case j3:
    case U3:
      return new i(+n);
    case X3:
      return D3(n, e);
    case Q3:
    case eI:
    case tI:
    case iI:
    case nI:
    case sI:
    case rI:
    case oI:
    case aI:
      return Rv(n, e);
    case W3:
      return new i();
    case q3:
    case Z3:
      return new i(n);
    case G3:
      return z3(n);
    case K3:
      return new i();
    case Y3:
      return H3(n);
  }
}
function Ov(n) {
  return typeof n.constructor == "function" && !of(n) ? cE(hf(n)) : {};
}
var cI = "[object Map]";
function uI(n) {
  return Ni(n) && ji(n) == cI;
}
var Ug = Nr && Nr.isMap, dI = Ug ? af(Ug) : uI, hI = "[object Set]";
function fI(n) {
  return Ni(n) && ji(n) == hI;
}
var Wg = Nr && Nr.isSet, mI = Wg ? af(Wg) : fI, gI = 1, pI = 2, bI = 4, Vv = "[object Arguments]", wI = "[object Array]", vI = "[object Boolean]", _I = "[object Date]", yI = "[object Error]", Lv = "[object Function]", kI = "[object GeneratorFunction]", CI = "[object Map]", xI = "[object Number]", Bv = "[object Object]", TI = "[object RegExp]", EI = "[object Set]", SI = "[object String]", AI = "[object Symbol]", II = "[object WeakMap]", PI = "[object ArrayBuffer]", RI = "[object DataView]", OI = "[object Float32Array]", VI = "[object Float64Array]", LI = "[object Int8Array]", BI = "[object Int16Array]", MI = "[object Int32Array]", NI = "[object Uint8Array]", FI = "[object Uint8ClampedArray]", DI = "[object Uint16Array]", $I = "[object Uint32Array]", at = {};
at[Vv] = at[wI] = at[PI] = at[RI] = at[vI] = at[_I] = at[OI] = at[VI] = at[LI] = at[BI] = at[MI] = at[CI] = at[xI] = at[Bv] = at[TI] = at[EI] = at[SI] = at[AI] = at[NI] = at[FI] = at[DI] = at[$I] = !0;
at[yI] = at[Lv] = at[II] = !1;
function Lr(n, t, e, i, s, r) {
  var o, a = t & gI, l = t & pI, c = t & bI;
  if (e && (o = s ? e(n, i, s, r) : e(n)), o !== void 0)
    return o;
  if (!nt(n))
    return n;
  var u = wi(n);
  if (u) {
    if (o = F3(n), !a)
      return Z0(n, o);
  } else {
    var d = ji(n), h = d == Lv || d == kI;
    if (ea(n))
      return Sv(n, a);
    if (d == Bv || d == Vv || h && !s) {
      if (o = l || h ? {} : Ov(n), !a)
        return l ? A3(n, y3(o, n)) : E3(n, _3(o, n));
    } else {
      if (!at[d])
        return s ? n : {};
      o = lI(n, d, a);
    }
  }
  r || (r = new Yi());
  var m = r.get(n);
  if (m)
    return m;
  r.set(n, o), mI(n) ? n.forEach(function(w) {
    o.add(Lr(w, t, e, w, n, r));
  }) : dI(n) && n.forEach(function(w, y) {
    o.set(y, Lr(w, t, e, y, n, r));
  });
  var g = c ? l ? I3 : Gd : l ? Qr : Aa, p = u ? void 0 : g(n);
  return wE(p || n, function(w, y) {
    p && (y = w, w = n[y]), nf(o, y, Lr(w, t, e, y, n, r));
  }), o;
}
var zI = 4;
function Mv(n) {
  return Lr(n, zI);
}
var HI = 1, jI = 4;
function Di(n) {
  return Lr(n, HI | jI);
}
var UI = 1, WI = 4;
function gf(n, t) {
  return t = typeof t == "function" ? t : void 0, Lr(n, UI | WI, t);
}
var qI = "__lodash_hash_undefined__";
function GI(n) {
  return this.__data__.set(n, qI), this;
}
function KI(n) {
  return this.__data__.has(n);
}
function Kl(n) {
  var t = -1, e = n == null ? 0 : n.length;
  for (this.__data__ = new Jn(); ++t < e; )
    this.add(n[t]);
}
Kl.prototype.add = Kl.prototype.push = GI;
Kl.prototype.has = KI;
function ZI(n, t) {
  for (var e = -1, i = n == null ? 0 : n.length; ++e < i; )
    if (t(n[e], e, n))
      return !0;
  return !1;
}
function YI(n, t) {
  return n.has(t);
}
var JI = 1, XI = 2;
function Nv(n, t, e, i, s, r) {
  var o = e & JI, a = n.length, l = t.length;
  if (a != l && !(o && l > a))
    return !1;
  var c = r.get(n), u = r.get(t);
  if (c && u)
    return c == t && u == n;
  var d = -1, h = !0, m = e & XI ? new Kl() : void 0;
  for (r.set(n, t), r.set(t, n); ++d < a; ) {
    var g = n[d], p = t[d];
    if (i)
      var w = o ? i(p, g, d, t, n, r) : i(g, p, d, n, t, r);
    if (w !== void 0) {
      if (w)
        continue;
      h = !1;
      break;
    }
    if (m) {
      if (!ZI(t, function(y, k) {
        if (!YI(m, k) && (g === y || s(g, y, e, i, r)))
          return m.push(k);
      })) {
        h = !1;
        break;
      }
    } else if (!(g === p || s(g, p, e, i, r))) {
      h = !1;
      break;
    }
  }
  return r.delete(n), r.delete(t), h;
}
function QI(n) {
  var t = -1, e = Array(n.size);
  return n.forEach(function(i, s) {
    e[++t] = [s, i];
  }), e;
}
function eP(n) {
  var t = -1, e = Array(n.size);
  return n.forEach(function(i) {
    e[++t] = i;
  }), e;
}
var tP = 1, iP = 2, nP = "[object Boolean]", sP = "[object Date]", rP = "[object Error]", oP = "[object Map]", aP = "[object Number]", lP = "[object RegExp]", cP = "[object Set]", uP = "[object String]", dP = "[object Symbol]", hP = "[object ArrayBuffer]", fP = "[object DataView]", qg = _n ? _n.prototype : void 0, Iu = qg ? qg.valueOf : void 0;
function mP(n, t, e, i, s, r, o) {
  switch (e) {
    case fP:
      if (n.byteLength != t.byteLength || n.byteOffset != t.byteOffset)
        return !1;
      n = n.buffer, t = t.buffer;
    case hP:
      return !(n.byteLength != t.byteLength || !r(new Gl(n), new Gl(t)));
    case nP:
    case sP:
    case aP:
      return Sa(+n, +t);
    case rP:
      return n.name == t.name && n.message == t.message;
    case lP:
    case uP:
      return n == t + "";
    case oP:
      var a = QI;
    case cP:
      var l = i & tP;
      if (a || (a = eP), n.size != t.size && !l)
        return !1;
      var c = o.get(n);
      if (c)
        return c == t;
      i |= iP, o.set(n, t);
      var u = Nv(a(n), a(t), i, s, r, o);
      return o.delete(n), u;
    case dP:
      if (Iu)
        return Iu.call(n) == Iu.call(t);
  }
  return !1;
}
var gP = 1, pP = Object.prototype, bP = pP.hasOwnProperty;
function wP(n, t, e, i, s, r) {
  var o = e & gP, a = Gd(n), l = a.length, c = Gd(t), u = c.length;
  if (l != u && !o)
    return !1;
  for (var d = l; d--; ) {
    var h = a[d];
    if (!(o ? h in t : bP.call(t, h)))
      return !1;
  }
  var m = r.get(n), g = r.get(t);
  if (m && g)
    return m == t && g == n;
  var p = !0;
  r.set(n, t), r.set(t, n);
  for (var w = o; ++d < l; ) {
    h = a[d];
    var y = n[h], k = t[h];
    if (i)
      var C = o ? i(k, y, h, t, n, r) : i(y, k, h, n, t, r);
    if (!(C === void 0 ? y === k || s(y, k, e, i, r) : C)) {
      p = !1;
      break;
    }
    w || (w = h == "constructor");
  }
  if (p && !w) {
    var E = n.constructor, T = t.constructor;
    E != T && "constructor" in n && "constructor" in t && !(typeof E == "function" && E instanceof E && typeof T == "function" && T instanceof T) && (p = !1);
  }
  return r.delete(n), r.delete(t), p;
}
var vP = 1, Gg = "[object Arguments]", Kg = "[object Array]", el = "[object Object]", _P = Object.prototype, Zg = _P.hasOwnProperty;
function yP(n, t, e, i, s, r) {
  var o = wi(n), a = wi(t), l = o ? Kg : ji(n), c = a ? Kg : ji(t);
  l = l == Gg ? el : l, c = c == Gg ? el : c;
  var u = l == el, d = c == el, h = l == c;
  if (h && ea(n)) {
    if (!ea(t))
      return !1;
    o = !0, u = !1;
  }
  if (h && !u)
    return r || (r = new Yi()), o || lf(n) ? Nv(n, t, e, i, s, r) : mP(n, t, l, e, i, s, r);
  if (!(e & vP)) {
    var m = u && Zg.call(n, "__wrapped__"), g = d && Zg.call(t, "__wrapped__");
    if (m || g) {
      var p = m ? n.value() : n, w = g ? t.value() : t;
      return r || (r = new Yi()), s(p, w, e, i, r);
    }
  }
  return h ? (r || (r = new Yi()), wP(n, t, e, i, s, r)) : !1;
}
function Pa(n, t, e, i, s) {
  return n === t ? !0 : n == null || t == null || !Ni(n) && !Ni(t) ? n !== n && t !== t : yP(n, t, e, i, Pa, s);
}
var kP = 1, CP = 2;
function xP(n, t, e, i) {
  var s = e.length, r = s;
  if (n == null)
    return !r;
  for (n = Object(n); s--; ) {
    var o = e[s];
    if (o[2] ? o[1] !== n[o[0]] : !(o[0] in n))
      return !1;
  }
  for (; ++s < r; ) {
    o = e[s];
    var a = o[0], l = n[a], c = o[1];
    if (o[2]) {
      if (l === void 0 && !(a in n))
        return !1;
    } else {
      var u = new Yi(), d;
      if (!(d === void 0 ? Pa(c, l, kP | CP, i, u) : d))
        return !1;
    }
  }
  return !0;
}
function Fv(n) {
  return n === n && !nt(n);
}
function TP(n) {
  for (var t = Aa(n), e = t.length; e--; ) {
    var i = t[e], s = n[i];
    t[e] = [i, s, Fv(s)];
  }
  return t;
}
function Dv(n, t) {
  return function(e) {
    return e == null ? !1 : e[n] === t && (t !== void 0 || n in Object(e));
  };
}
function EP(n) {
  var t = TP(n);
  return t.length == 1 && t[0][2] ? Dv(t[0][0], t[0][1]) : function(e) {
    return e === n || xP(e, n, t);
  };
}
function SP(n, t) {
  return n != null && t in Object(n);
}
function AP(n, t, e) {
  t = Hc(t, n);
  for (var i = -1, s = t.length, r = !1; ++i < s; ) {
    var o = eo(t[i]);
    if (!(r = n != null && e(n, o)))
      break;
    n = n[o];
  }
  return r || ++i != s ? r : (s = n == null ? 0 : n.length, !!s && sf(s) && Fc(o, s) && (wi(n) || ql(n)));
}
function IP(n, t) {
  return n != null && AP(n, t, SP);
}
var PP = 1, RP = 2;
function OP(n, t) {
  return cf(n) && Fv(t) ? Dv(eo(n), t) : function(e) {
    var i = Fr(e, n);
    return i === void 0 && i === t ? IP(e, n) : Pa(t, i, PP | RP);
  };
}
function VP(n) {
  return function(t) {
    return t == null ? void 0 : t[n];
  };
}
function LP(n) {
  return function(t) {
    return df(t, n);
  };
}
function BP(n) {
  return cf(n) ? VP(eo(n)) : LP(n);
}
function $v(n) {
  return typeof n == "function" ? n : n == null ? cs : typeof n == "object" ? wi(n) ? OP(n[0], n[1]) : EP(n) : BP(n);
}
function MP(n) {
  return function(t, e, i) {
    for (var s = -1, r = Object(t), o = i(t), a = o.length; a--; ) {
      var l = o[++s];
      if (e(r[l], l, r) === !1)
        break;
    }
    return t;
  };
}
var zv = MP();
function Hv(n, t) {
  return n && zv(n, t, Aa);
}
function NP(n, t) {
  return function(e, i) {
    if (e == null)
      return e;
    if (!Xr(e))
      return n(e, i);
    for (var s = e.length, r = -1, o = Object(e); ++r < s && i(o[r], r, o) !== !1; )
      ;
    return e;
  };
}
var FP = NP(Hv), Pu = function() {
  return nn.Date.now();
}, DP = "Expected a function", $P = Math.max, zP = Math.min;
function vi(n, t, e) {
  var i, s, r, o, a, l, c = 0, u = !1, d = !1, h = !0;
  if (typeof n != "function")
    throw new TypeError(DP);
  t = yg(t) || 0, nt(e) && (u = !!e.leading, d = "maxWait" in e, r = d ? $P(yg(e.maxWait) || 0, t) : r, h = "trailing" in e ? !!e.trailing : h);
  function m(L) {
    var O = i, N = s;
    return i = s = void 0, c = L, o = n.apply(N, O), o;
  }
  function g(L) {
    return c = L, a = setTimeout(y, t), u ? m(L) : o;
  }
  function p(L) {
    var O = L - l, N = L - c, M = t - O;
    return d ? zP(M, r - N) : M;
  }
  function w(L) {
    var O = L - l, N = L - c;
    return l === void 0 || O >= t || O < 0 || d && N >= r;
  }
  function y() {
    var L = Pu();
    if (w(L))
      return k(L);
    a = setTimeout(y, p(L));
  }
  function k(L) {
    return a = void 0, h && i ? m(L) : (i = s = void 0, o);
  }
  function C() {
    a !== void 0 && clearTimeout(a), c = 0, i = l = s = a = void 0;
  }
  function E() {
    return a === void 0 ? o : k(Pu());
  }
  function T() {
    var L = Pu(), O = w(L);
    if (i = arguments, s = this, l = L, O) {
      if (a === void 0)
        return g(l);
      if (d)
        return clearTimeout(a), a = setTimeout(y, t), m(l);
    }
    return a === void 0 && (a = setTimeout(y, t)), o;
  }
  return T.cancel = C, T.flush = E, T;
}
function Jd(n, t, e) {
  (e !== void 0 && !Sa(n[t], e) || e === void 0 && !(t in n)) && Dc(n, t, e);
}
function HP(n) {
  return Ni(n) && Xr(n);
}
function Xd(n, t) {
  if (!(t === "constructor" && typeof n[t] == "function") && t != "__proto__")
    return n[t];
}
function jP(n) {
  return Jr(n, Qr(n));
}
function UP(n, t, e, i, s, r, o) {
  var a = Xd(n, e), l = Xd(t, e), c = o.get(l);
  if (c) {
    Jd(n, e, c);
    return;
  }
  var u = r ? r(a, l, e + "", n, t, o) : void 0, d = u === void 0;
  if (d) {
    var h = wi(l), m = !h && ea(l), g = !h && !m && lf(l);
    u = l, h || m || g ? wi(a) ? u = a : HP(a) ? u = Z0(a) : m ? (d = !1, u = Sv(l, !0)) : g ? (d = !1, u = Rv(l, !0)) : u = [] : qi(l) || ql(l) ? (u = a, ql(a) ? u = jP(a) : (!nt(a) || Fs(a)) && (u = Ov(l))) : d = !1;
  }
  d && (o.set(l, u), s(u, l, i, r, o), o.delete(l)), Jd(n, e, u);
}
function pf(n, t, e, i, s) {
  n !== t && zv(t, function(r, o) {
    if (s || (s = new Yi()), nt(r))
      UP(n, t, o, e, pf, i, s);
    else {
      var a = i ? i(Xd(n, o), r, o + "", n, t, s) : void 0;
      a === void 0 && (a = r), Jd(n, o, a);
    }
  }, Qr);
}
var WP = rf(function(n, t, e, i) {
  pf(n, t, e, i);
});
function qP(n) {
  var t = n == null ? 0 : n.length;
  return t ? n[t - 1] : void 0;
}
var jv = /[\\^$.*+?()[\]{}|]/g, GP = RegExp(jv.source);
function Uv(n) {
  return n = Ia(n), n && GP.test(n) ? n.replace(jv, "\\$&") : n;
}
function KP(n, t) {
  var e = -1, i = Xr(n) ? Array(n.length) : [];
  return FP(n, function(s, r, o) {
    i[++e] = t(s, r, o);
  }), i;
}
function ZP(n, t) {
  var e = wi(n) ? G0 : KP;
  return e(n, $v(t));
}
var YP = "[object String]";
function Yg(n) {
  return typeof n == "string" || !wi(n) && Ni(n) && ps(n) == YP;
}
function JP(n, t) {
  return t.length < 2 ? n : df(n, sv(t, 0, -1));
}
function Hn(n) {
  return Ni(n) && n.nodeType === 1 && !qi(n);
}
function Dr(n, t) {
  return Pa(n, t);
}
function XP(n, t, e) {
  e = typeof e == "function" ? e : void 0;
  var i = e ? e(n, t) : void 0;
  return i === void 0 ? Pa(n, t, void 0, e) : !!i;
}
function QP(n, t) {
  var e = {};
  return t = $v(t), Hv(n, function(i, s, r) {
    Dc(e, s, t(i, s, r));
  }), e;
}
var bf = rf(function(n, t, e) {
  pf(n, t, e);
});
function e4(n, t) {
  return t = Hc(t, n), n = JP(n, t), n == null || delete n[eo(qP(t))];
}
function t4(n, t, e, i) {
  if (!nt(n))
    return n;
  t = Hc(t, n);
  for (var s = -1, r = t.length, o = r - 1, a = n; a != null && ++s < r; ) {
    var l = eo(t[s]), c = e;
    if (l === "__proto__" || l === "constructor" || l === "prototype")
      return n;
    if (s != o) {
      var u = a[l];
      c = void 0, c === void 0 && (c = nt(u) ? u : Fc(t[s + 1]) ? [] : {});
    }
    nf(a, l, c), a = a[l];
  }
  return n;
}
function Wv(n, t, e) {
  return n == null ? n : t4(n, t, e);
}
var i4 = f3(function(n, t, e) {
  return n + (e ? " " : "") + hv(t);
}), n4 = "Expected a function";
function $s(n, t, e) {
  var i = !0, s = !0;
  if (typeof n != "function")
    throw new TypeError(n4);
  return nt(e) && (i = "leading" in e ? !!e.leading : i, s = "trailing" in e ? !!e.trailing : s), vi(n, t, {
    leading: i,
    maxWait: t,
    trailing: s
  });
}
function s4(n, t) {
  return n == null ? !0 : e4(n, t);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
let Qd;
try {
  Qd = {
    window,
    document
  };
} catch {
  /* istanbul ignore next -- @preserve */
  Qd = {
    window: {},
    document: {}
  };
}
var ae = Qd;
function r4() {
  try {
    return navigator.userAgent.toLowerCase();
  } catch {
    return "";
  }
}
const ys = /* @__PURE__ */ r4(), be = {
  isMac: /* @__PURE__ */ qv(ys),
  isWindows: /* @__PURE__ */ o4(ys),
  isGecko: /* @__PURE__ */ a4(ys),
  isSafari: /* @__PURE__ */ l4(ys),
  isiOS: /* @__PURE__ */ c4(ys),
  isAndroid: /* @__PURE__ */ u4(ys),
  isBlink: /* @__PURE__ */ d4(ys),
  get isMediaForcedColors() {
    return f4();
  },
  features: {
    isRegExpUnicodePropertySupported: /* @__PURE__ */ h4()
  }
};
function qv(n) {
  return n.indexOf("macintosh") > -1;
}
function o4(n) {
  return n.indexOf("windows") > -1;
}
function a4(n) {
  return !!n.match(/gecko\/\d+/);
}
function l4(n) {
  return n.indexOf(" applewebkit/") > -1 && n.indexOf("chrome") === -1;
}
function c4(n) {
  return !!n.match(/iphone|ipad/i) || qv(n) && navigator.maxTouchPoints > 0;
}
function u4(n) {
  return n.indexOf("android") > -1;
}
function d4(n) {
  return n.indexOf("chrome/") > -1 && n.indexOf("edge/") < 0;
}
function h4() {
  let n = !1;
  try {
    n = "ć".search(new RegExp("[\\p{L}]", "u")) === 0;
  } catch {
  }
  return n;
}
function f4() {
  return ae.window.matchMedia ? ae.window.matchMedia("(forced-colors: active)").matches : !1;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Gv(n, t, e, i) {
  e = e || function(l, c) {
    return l === c;
  };
  const s = Array.isArray(n) ? n : Array.prototype.slice.call(n), r = Array.isArray(t) ? t : Array.prototype.slice.call(t), o = m4(s, r, e);
  return i ? p4(o, r.length) : g4(r, o);
}
function m4(n, t, e) {
  const i = Jg(n, t, e);
  if (i === -1)
    return {
      firstIndex: -1,
      lastIndexOld: -1,
      lastIndexNew: -1
    };
  const s = Xg(n, i), r = Xg(t, i), o = Jg(s, r, e), a = n.length - o, l = t.length - o;
  return {
    firstIndex: i,
    lastIndexOld: a,
    lastIndexNew: l
  };
}
function Jg(n, t, e) {
  for (let i = 0; i < Math.max(n.length, t.length); i++)
    if (n[i] === void 0 || t[i] === void 0 || !e(n[i], t[i]))
      return i;
  return -1;
}
function Xg(n, t) {
  return n.slice(t).reverse();
}
function g4(n, t) {
  const e = [], { firstIndex: i, lastIndexOld: s, lastIndexNew: r } = t;
  return r - i > 0 && e.push({
    index: i,
    type: "insert",
    values: n.slice(i, r)
  }), s - i > 0 && e.push({
    index: i + (r - i),
    type: "delete",
    howMany: s - i
  }), e;
}
function p4(n, t) {
  const { firstIndex: e, lastIndexOld: i, lastIndexNew: s } = n;
  if (e === -1)
    return Array(t).fill("equal");
  let r = [];
  return e > 0 && (r = r.concat(Array(e).fill("equal"))), s - e > 0 && (r = r.concat(Array(s - e).fill("insert"))), i - e > 0 && (r = r.concat(Array(i - e).fill("delete"))), s < t && (r = r.concat(Array(t - s).fill("equal"))), r;
}
function Fo(n, t, e) {
  e = e || function(p, w) {
    return p === w;
  };
  const i = n.length, s = t.length;
  if (i > 200 || s > 200 || i + s > 300)
    return Fo.fastDiff(n, t, e, !0);
  let r, o;
  if (s < i) {
    const p = n;
    n = t, t = p, r = "delete", o = "insert";
  } else
    r = "insert", o = "delete";
  const a = n.length, l = t.length, c = l - a, u = {}, d = {};
  function h(p) {
    const w = (d[p - 1] !== void 0 ? d[p - 1] : -1) + 1, y = d[p + 1] !== void 0 ? d[p + 1] : -1, k = w > y ? -1 : 1;
    u[p + k] && (u[p] = u[p + k].slice(0)), u[p] || (u[p] = []), u[p].push(w > y ? r : o);
    let C = Math.max(w, y), E = C - p;
    for (; E < a && C < l && e(n[E], t[C]); )
      E++, C++, u[p].push("equal");
    return C;
  }
  let m = 0, g;
  do {
    for (g = -m; g < c; g++)
      d[g] = h(g);
    for (g = c + m; g > c; g--)
      d[g] = h(g);
    d[c] = h(c), m++;
  } while (d[c] !== l);
  return u[c].slice(1);
}
Fo.fastDiff = Gv;
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Qg() {
  return function n() {
    n.called = !0;
  };
}
class jn {
  /**
  * @param source The emitter.
  * @param name The event name.
  */
  constructor(t, e) {
    /**
    * The object that fired the event.
    */
    f(this, "source");
    /**
    * The event name.
    */
    f(this, "name");
    /**
    * Path this event has followed. See {@link module:utils/emittermixin~Emitter#delegate}.
    */
    f(this, "path");
    /**
    * Stops the event emitter to call further callbacks for this event interaction.
    */
    f(this, "stop");
    /**
    * Removes the current callback from future interactions of this event.
    */
    f(this, "off");
    /**
    * The value which will be returned by {@link module:utils/emittermixin~Emitter#fire}.
    *
    * It's `undefined` by default and can be changed by an event listener:
    *
    * ```ts
    * dataController.fire( 'getSelectedContent', ( evt ) => {
    * 	// This listener will make `dataController.fire( 'getSelectedContent' )`
    * 	// always return an empty DocumentFragment.
    * 	evt.return = new DocumentFragment();
    *
    * 	// Make sure no other listeners are executed.
    * 	evt.stop();
    * } );
    * ```
    */
    f(this, "return");
    this.source = t, this.name = e, this.path = [], this.stop = Qg(), this.off = Qg();
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const Kt = new Array(256).fill("").map((n, t) => ("0" + t.toString(16)).slice(-2));
function Bt() {
  const [n, t, e, i] = crypto.getRandomValues(new Uint32Array(4));
  return "e" + Kt[n >> 0 & 255] + Kt[n >> 8 & 255] + Kt[n >> 16 & 255] + Kt[n >> 24 & 255] + Kt[t >> 0 & 255] + Kt[t >> 8 & 255] + Kt[t >> 16 & 255] + Kt[t >> 24 & 255] + Kt[e >> 0 & 255] + Kt[e >> 8 & 255] + Kt[e >> 16 & 255] + Kt[e >> 24 & 255] + Kt[i >> 0 & 255] + Kt[i >> 8 & 255] + Kt[i >> 16 & 255] + Kt[i >> 24 & 255];
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const Si = {
  get(n = "normal") {
    return typeof n != "number" ? this[n] || this.normal : n;
  },
  highest: 1e5,
  high: 1e3,
  normal: 0,
  low: -1e3,
  lowest: -1e5
};
function Kv(n, t) {
  const e = Si.get(t.priority);
  let i = 0, s = n.length;
  for (; i < s; ) {
    const r = i + s >> 1;
    Si.get(n[r].priority) < e ? s = r : i = r + 1;
  }
  n.splice(i, 0, t);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const b4 = "https://ckeditor.com/docs/ckeditor5/latest/support/error-codes.html";
class S extends Error {
  /**
  * Creates an instance of the CKEditorError class.
  *
  * @param errorName The error id in an `error-name` format. A link to this error documentation page will be added
  * to the thrown error's `message`.
  * @param context A context of the error by which the {@link module:watchdog/watchdog~Watchdog watchdog}
  * is able to determine which editor crashed. It should be an editor instance or a property connected to it. It can be also
  * a `null` value if the editor should not be restarted in case of the error (e.g. during the editor initialization).
  * The error context should be checked using the `areConnectedThroughProperties( editor, context )` utility
  * to check if the object works as the context.
  * @param data Additional data describing the error. A stringified version of this object
  * will be appended to the error message, so the data are quickly visible in the console. The original
  * data object will also be later available under the {@link #data} property.
  */
  constructor(e, i, s) {
    super(v4(e, s));
    /**
    * A context of the error by which the Watchdog is able to determine which editor crashed.
    */
    f(this, "context");
    /**
    * The additional error data passed to the constructor. Undefined if none was passed.
    */
    f(this, "data");
    this.name = "CKEditorError", this.context = i, this.data = s;
  }
  /**
  * Checks if the error is of the `CKEditorError` type.
  */
  is(e) {
    return e === "CKEditorError";
  }
  /**
  * A utility that ensures that the thrown error is a {@link module:utils/ckeditorerror~CKEditorError} one.
  * It is useful when combined with the {@link module:watchdog/watchdog~Watchdog} feature, which can restart the editor in case
  * of a {@link module:utils/ckeditorerror~CKEditorError} error.
  *
  * @param err The error to rethrow.
  * @param context An object connected through properties with the editor instance. This context will be used
  * by the watchdog to verify which editor should be restarted.
  */
  static rethrowUnexpectedError(e, i) {
    if (e.is && e.is("CKEditorError"))
      throw e;
    const s = new S(e.message, i);
    throw s.stack = e.stack, s;
  }
}
function Ve(n, t) {
  console.warn(...Yv(n, t));
}
function w4(n, t) {
  console.error(...Yv(n, t));
}
function Zv(n) {
  return `
Read more: ${b4}#error-${n}`;
}
function v4(n, t) {
  const e = /* @__PURE__ */ new WeakSet(), s = t ? ` ${JSON.stringify(t, (o, a) => {
    if (typeof a == "object" && a !== null) {
      if (e.has(a))
        return `[object ${a.constructor.name}]`;
      e.add(a);
    }
    return a;
  })}` : "", r = Zv(n);
  return n + s + r;
}
function Yv(n, t) {
  const e = Zv(n);
  return t ? [
    n,
    t,
    e
  ] : [
    n,
    e
  ];
}
const _4 = "44.3.0", y4 = new Date(2025, 2, 5);
/* istanbul ignore next -- @preserve */
if (globalThis.CKEDITOR_VERSION)
  throw new S("ckeditor-duplicated-modules", null);
globalThis.CKEDITOR_VERSION = _4;
const kr = Symbol("listeningTo"), eh = Symbol("emitterId"), Rn = Symbol("delegations"), Jv = /* @__PURE__ */ Je(Object);
function Je(n) {
  if (!n)
    return Jv;
  class t extends n {
    on(i, s, r) {
      this.listenTo(this, i, s, r);
    }
    once(i, s, r) {
      let o = !1;
      const a = (l, ...c) => {
        o || (o = !0, l.off(), s.call(this, l, ...c));
      };
      this.listenTo(this, i, a, r);
    }
    off(i, s) {
      this.stopListening(this, i, s);
    }
    listenTo(i, s, r, o = {}) {
      let a, l;
      this[kr] || (this[kr] = {});
      const c = this[kr];
      Ru(i) || Xv(i);
      const u = Ru(i);
      (a = c[u]) || (a = c[u] = {
        emitter: i,
        callbacks: {}
      }), (l = a.callbacks[s]) || (l = a.callbacks[s] = []), l.push(r), E4(this, i, s, r, o);
    }
    stopListening(i, s, r) {
      const o = this[kr];
      let a = i && Ru(i);
      const l = o && a ? o[a] : void 0, c = l && s ? l.callbacks[s] : void 0;
      if (!(!o || i && !l || s && !c))
        if (r)
          Ou(this, i, s, r), c.indexOf(r) !== -1 && (c.length === 1 ? delete l.callbacks[s] : Ou(this, i, s, r));
        else if (c) {
          for (; r = c.pop(); )
            Ou(this, i, s, r);
          delete l.callbacks[s];
        } else if (l) {
          for (s in l.callbacks)
            this.stopListening(i, s);
          delete o[a];
        } else {
          for (a in o)
            this.stopListening(o[a].emitter);
          delete this[kr];
        }
    }
    fire(i, ...s) {
      try {
        const r = i instanceof jn ? i : new jn(this, i), o = r.name;
        let a = T4(this, o);
        if (r.path.push(this), a) {
          a = a.slice();
          for (let c = 0; c < a.length; c++) {
            const u = a[c].callback;
            if (u.call(this, r, ...s), r.off.called && (delete r.off.called, this._removeEventListener(o, u)), r.stop.called)
              break;
          }
        }
        const l = this[Rn];
        if (l) {
          const c = l.get(o), u = l.get("*");
          c && ep(c, r, s), u && ep(u, r, s);
        }
        return r.return;
      } catch (r) {
        /* istanbul ignore next -- @preserve */
        S.rethrowUnexpectedError(r, this);
      }
    }
    delegate(...i) {
      return {
        to: (s, r) => {
          this[Rn] || (this[Rn] = /* @__PURE__ */ new Map()), i.forEach((o) => {
            const a = this[Rn].get(o);
            a ? a.set(s, r) : this[Rn].set(o, /* @__PURE__ */ new Map([
              [
                s,
                r
              ]
            ]));
          });
        }
      };
    }
    stopDelegating(i, s) {
      if (this[Rn])
        if (!i)
          this[Rn].clear();
        else if (!s)
          this[Rn].delete(i);
        else {
          const r = this[Rn].get(i);
          r && r.delete(s);
        }
    }
    _addEventListener(i, s, r) {
      x4(this, i);
      const o = th(this, i), a = Si.get(r.priority), l = {
        callback: s,
        priority: a
      };
      for (const c of o)
        Kv(c, l);
    }
    _removeEventListener(i, s) {
      const r = th(this, i);
      for (const o of r)
        for (let a = 0; a < o.length; a++)
          o[a].callback == s && (o.splice(a, 1), a--);
    }
  }
  return t;
}
[
  "on",
  "once",
  "off",
  "listenTo",
  "stopListening",
  "fire",
  "delegate",
  "stopDelegating",
  "_addEventListener",
  "_removeEventListener"
].forEach((n) => {
  Je[n] = Jv.prototype[n];
});
function k4(n, t) {
  const e = n[kr];
  return e && e[t] ? e[t].emitter : null;
}
function Xv(n, t) {
  n[eh] || (n[eh] = t || Bt());
}
function Ru(n) {
  return n[eh];
}
function Qv(n) {
  return n._events || Object.defineProperty(n, "_events", {
    value: {}
  }), n._events;
}
function C4() {
  return {
    callbacks: [],
    childEvents: []
  };
}
function x4(n, t) {
  const e = Qv(n);
  if (e[t])
    return;
  let i = t, s = null;
  const r = [];
  for (; i !== "" && !e[i]; )
    e[i] = C4(), r.push(e[i]), s && e[i].childEvents.push(s), s = i, i = i.substr(0, i.lastIndexOf(":"));
  if (i !== "") {
    for (const o of r)
      o.callbacks = e[i].callbacks.slice();
    e[i].childEvents.push(s);
  }
}
function th(n, t) {
  const e = Qv(n)[t];
  if (!e)
    return [];
  let i = [
    e.callbacks
  ];
  for (let s = 0; s < e.childEvents.length; s++) {
    const r = th(n, e.childEvents[s]);
    i = i.concat(r);
  }
  return i;
}
function T4(n, t) {
  if (!n._events)
    return null;
  let e = t;
  do {
    const i = n._events[e];
    if (i && i.callbacks && i.callbacks.length)
      return i.callbacks;
    const s = e.lastIndexOf(":");
    e = s > -1 ? e.substring(0, s) : "";
  } while (e);
  return null;
}
function ep(n, t, e) {
  for (let [i, s] of n) {
    s ? typeof s == "function" && (s = s(t.name)) : s = t.name;
    const r = new jn(t.source, s);
    r.path = [
      ...t.path
    ], i.fire(r, ...e);
  }
}
function E4(n, t, e, i, s) {
  t._addEventListener ? t._addEventListener(e, i, s) : n._addEventListener.call(t, e, i, s);
}
function Ou(n, t, e, i) {
  t._removeEventListener ? t._removeEventListener(e, i) : n._removeEventListener.call(t, e, i);
}
const Do = Symbol("observableProperties"), na = Symbol("boundObservables"), $o = Symbol("boundProperties"), ks = Symbol("decoratedMethods"), tp = Symbol("decoratedOriginal"), e_ = /* @__PURE__ */ je(/* @__PURE__ */ Je());
function je(n) {
  var e, i, s, r, o;
  if (!n)
    return e_;
  class t extends (o = n, r = Do, s = ks, i = $o, e = na, o) {
    constructor() {
      super(...arguments);
      f(this, r);
      f(this, s);
      f(this, i);
      f(this, e);
    }
    set(c, u) {
      if (nt(c)) {
        Object.keys(c).forEach((h) => {
          this.set(h, c[h]);
        }, this);
        return;
      }
      Vu(this);
      const d = this[Do];
      if (c in this && !d.has(c))
        throw new S("observable-set-cannot-override", this);
      Object.defineProperty(this, c, {
        enumerable: !0,
        configurable: !0,
        get() {
          return d.get(c);
        },
        set(h) {
          const m = d.get(c);
          let g = this.fire(`set:${c}`, c, h, m);
          g === void 0 && (g = h), (m !== g || !d.has(c)) && (d.set(c, g), this.fire(`change:${c}`, c, g, m));
        }
      }), this[c] = u;
    }
    bind(...c) {
      if (!c.length || !ip(c))
        throw new S("observable-bind-wrong-properties", this);
      if (new Set(c).size !== c.length)
        throw new S("observable-bind-duplicate-properties", this);
      Vu(this);
      const u = this[$o];
      c.forEach((h) => {
        if (u.has(h))
          throw new S("observable-bind-rebind", this);
      });
      const d = /* @__PURE__ */ new Map();
      return c.forEach((h) => {
        const m = {
          property: h,
          to: []
        };
        u.set(h, m), d.set(h, m);
      }), {
        to: S4,
        toMany: A4,
        _observable: this,
        _bindProperties: c,
        _to: [],
        _bindings: d
      };
    }
    unbind(...c) {
      if (!this[Do])
        return;
      const u = this[$o], d = this[na];
      if (c.length) {
        if (!ip(c))
          throw new S("observable-unbind-wrong-properties", this);
        c.forEach((h) => {
          const m = u.get(h);
          m && (m.to.forEach(([g, p]) => {
            const w = d.get(g), y = w[p];
            y.delete(m), y.size || delete w[p], Object.keys(w).length || (d.delete(g), this.stopListening(g, "change"));
          }), u.delete(h));
        });
      } else
        d.forEach((h, m) => {
          this.stopListening(m, "change");
        }), d.clear(), u.clear();
    }
    decorate(c) {
      Vu(this);
      const u = this[c];
      if (!u)
        throw new S("observablemixin-cannot-decorate-undefined", this, {
          object: this,
          methodName: c
        });
      this.on(c, (d, h) => {
        d.return = u.apply(this, h);
      }), this[c] = function(...d) {
        return this.fire(c, d);
      }, this[c][tp] = u, this[ks] || (this[ks] = []), this[ks].push(c);
    }
    // Override the EmitterMixin stopListening method to be able to clean (and restore) decorated methods.
    // This is needed in case of:
    //  1. Have x.foo() decorated.
    //  2. Call x.stopListening()
    //  3. Call x.foo(). Problem: nothing happens (the original foo() method is not executed)
    stopListening(c, u, d) {
      if (!c && this[ks]) {
        for (const h of this[ks])
          this[h] = this[h][tp];
        delete this[ks];
      }
      super.stopListening(c, u, d);
    }
  }
  return t;
}
[
  "set",
  "bind",
  "unbind",
  "decorate",
  "on",
  "once",
  "off",
  "listenTo",
  "stopListening",
  "fire",
  "delegate",
  "stopDelegating",
  "_addEventListener",
  "_removeEventListener"
].forEach((n) => {
  je[n] = e_.prototype[n];
});
function Vu(n) {
  n[Do] || (Object.defineProperty(n, Do, {
    value: /* @__PURE__ */ new Map()
  }), Object.defineProperty(n, na, {
    value: /* @__PURE__ */ new Map()
  }), Object.defineProperty(n, $o, {
    value: /* @__PURE__ */ new Map()
  }));
}
function S4(...n) {
  const t = P4(...n), e = Array.from(this._bindings.keys()), i = e.length;
  if (!t.callback && t.to.length > 1)
    throw new S("observable-bind-to-no-callback", this);
  if (i > 1 && t.callback)
    throw new S("observable-bind-to-extra-callback", this);
  t.to.forEach((s) => {
    if (s.properties.length && s.properties.length !== i)
      throw new S("observable-bind-to-properties-length", this);
    s.properties.length || (s.properties = this._bindProperties);
  }), this._to = t.to, t.callback && (this._bindings.get(e[0]).callback = t.callback), V4(this._observable, this._to), O4(this), this._bindProperties.forEach((s) => {
    t_(this._observable, s);
  });
}
function A4(n, t, e) {
  if (this._bindings.size > 1)
    throw new S("observable-bind-to-many-not-one-binding", this);
  this.to(
    ...I4(n, t),
    // ...using given callback to parse attribute values.
    e
  );
}
function I4(n, t) {
  const e = n.map((i) => [
    i,
    t
  ]);
  return Array.prototype.concat.apply([], e);
}
function ip(n) {
  return n.every((t) => typeof t == "string");
}
function P4(...n) {
  if (!n.length)
    throw new S("observable-bind-to-parse-error", null);
  const t = {
    to: []
  };
  let e;
  return typeof n[n.length - 1] == "function" && (t.callback = n.pop()), n.forEach((i) => {
    if (typeof i == "string")
      e.properties.push(i);
    else if (typeof i == "object")
      e = {
        observable: i,
        properties: []
      }, t.to.push(e);
    else
      throw new S("observable-bind-to-parse-error", null);
  }), t;
}
function R4(n, t, e, i) {
  const s = n[na], r = s.get(e), o = r || {};
  o[i] || (o[i] = /* @__PURE__ */ new Set()), o[i].add(t), r || s.set(e, o);
}
function O4(n) {
  let t;
  n._bindings.forEach((e, i) => {
    n._to.forEach((s) => {
      t = s.properties[e.callback ? 0 : n._bindProperties.indexOf(i)], e.to.push([
        s.observable,
        t
      ]), R4(n._observable, e, s.observable, t);
    });
  });
}
function t_(n, t) {
  const i = n[$o].get(t);
  let s;
  i.callback ? s = i.callback.apply(n, i.to.map((r) => r[0][r[1]])) : (s = i.to[0], s = s[0][s[1]]), Object.prototype.hasOwnProperty.call(n, t) ? n[t] = s : n.set(t, s);
}
function V4(n, t) {
  t.forEach((e) => {
    const i = n[na];
    let s;
    i.get(e.observable) || n.listenTo(e.observable, "change", (r, o) => {
      s = i.get(e.observable)[o], s && s.forEach((a) => {
        t_(n, a.property);
      });
    });
  });
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class i_ {
  constructor() {
    /**
    * The elements replaced by {@link #replace} and their replacements.
    */
    f(this, "_replacedElements");
    this._replacedElements = [];
  }
  /**
  * Hides the `element` and, if specified, inserts the the given element next to it.
  *
  * The effect of this method can be reverted by {@link #restore}.
  *
  * @param element The element to replace.
  * @param newElement The replacement element. If not passed, then the `element` will just be hidden.
  */
  replace(t, e) {
    this._replacedElements.push({
      element: t,
      newElement: e
    }), t.style.display = "none", e && t.parentNode.insertBefore(e, t.nextSibling);
  }
  /**
  * Restores what {@link #replace} did.
  */
  restore() {
    this._replacedElements.forEach(({ element: t, newElement: e }) => {
      t.style.display = "", e && e.remove();
    }), this._replacedElements = [];
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function ih(n) {
  let t = 0;
  for (const e of n)
    t++;
  return t;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Zt(n, t) {
  const e = Math.min(n.length, t.length);
  for (let i = 0; i < e; i++)
    if (n[i] != t[i])
      return i;
  return n.length == t.length ? "same" : n.length < t.length ? "prefix" : "extension";
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Ai(n) {
  return !!(n && n[Symbol.iterator]);
}
function Ft(n, t, e = {}, i = []) {
  const s = e && e.xmlns, r = s ? n.createElementNS(s, t) : n.createElement(t);
  for (const o in e)
    r.setAttribute(o, e[o]);
  (Yg(i) || !Ai(i)) && (i = [
    i
  ]);
  for (let o of i)
    Yg(o) && (o = n.createTextNode(o)), r.appendChild(o);
  return r;
}
class n_ {
  /**
  * Creates an instance of the {@link ~Config} class.
  *
  * @param configurations The initial configurations to be set. Usually, provided by the user.
  * @param defaultConfigurations The default configurations. Usually, provided by the system.
  */
  constructor(t, e) {
    /**
    * Store for the whole configuration.
    */
    f(this, "_config");
    this._config = /* @__PURE__ */ Object.create(null), e && this.define(np(e)), t && this._setObjectToTarget(this._config, t);
  }
  set(t, e) {
    this._setToTarget(this._config, t, e);
  }
  define(t, e) {
    this._setToTarget(this._config, t, e, !0);
  }
  /**
  * Gets the value for a configuration entry.
  *
  * ```ts
  * config.get( 'name' );
  * ```
  *
  * Deep configurations can be retrieved by separating each part with a dot.
  *
  * ```ts
  * config.get( 'toolbar.collapsed' );
  * ```
  *
  * @param name The configuration name. Configuration names are case-sensitive.
  * @returns The configuration value or `undefined` if the configuration entry was not found.
  */
  get(t) {
    return this._getFromSource(this._config, t);
  }
  /**
  * Iterates over all top level configuration names.
  */
  *names() {
    for (const t of Object.keys(this._config))
      yield t;
  }
  /**
  * Saves passed configuration to the specified target (nested object).
  *
  * @param target Nested config object.
  * @param name The configuration name or an object from which take properties as
  * configuration entries. Configuration names are case-sensitive.
  * @param value The configuration value. Used if a name is passed.
  * @param isDefine Define if passed configuration should overwrite existing one.
  */
  _setToTarget(t, e, i, s = !1) {
    if (qi(e)) {
      this._setObjectToTarget(t, e, s);
      return;
    }
    const r = e.split(".");
    e = r.pop();
    for (const o of r)
      qi(t[o]) || (t[o] = /* @__PURE__ */ Object.create(null)), t = t[o];
    if (qi(i)) {
      qi(t[e]) || (t[e] = /* @__PURE__ */ Object.create(null)), t = t[e], this._setObjectToTarget(t, i, s);
      return;
    }
    s && typeof t[e] < "u" || (t[e] = i);
  }
  /**
  * Get specified configuration from specified source (nested object).
  *
  * @param source level of nested object.
  * @param name The configuration name. Configuration names are case-sensitive.
  * @returns The configuration value or `undefined` if the configuration entry was not found.
  */
  _getFromSource(t, e) {
    const i = e.split(".");
    e = i.pop();
    for (const s of i) {
      if (!qi(t[s])) {
        t = null;
        break;
      }
      t = t[s];
    }
    return t ? np(t[e]) : void 0;
  }
  /**
  * Iterates through passed object and calls {@link #_setToTarget} method with object key and value for each property.
  *
  * @param target Nested config object.
  * @param configuration Configuration data set
  * @param isDefine Defines if passed configuration is default configuration or not.
  */
  _setObjectToTarget(t, e, i) {
    Object.keys(e).forEach((s) => {
      this._setToTarget(t, s, e[s], i);
    });
  }
}
function np(n) {
  return gf(n, L4);
}
function L4(n) {
  return Hn(n) || typeof n == "function" ? n : void 0;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function fs(n) {
  if (n) {
    if (n.defaultView)
      return n instanceof n.defaultView.Document;
    if (n.ownerDocument && n.ownerDocument.defaultView)
      return n instanceof n.ownerDocument.defaultView.Node;
  }
  return !1;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Zl(n) {
  const t = Object.prototype.toString.apply(n);
  return t == "[object Window]" || t == "[object global]";
}
const s_ = /* @__PURE__ */ li(/* @__PURE__ */ Je());
function li(n) {
  if (!n)
    return s_;
  class t extends n {
    listenTo(i, s, r, o = {}) {
      if (fs(i) || Zl(i)) {
        const a = {
          capture: !!o.useCapture,
          passive: !!o.usePassive
        }, l = this._getProxyEmitter(i, a) || new B4(i, a);
        this.listenTo(l, s, r, o);
      } else
        super.listenTo(i, s, r, o);
    }
    stopListening(i, s, r) {
      if (fs(i) || Zl(i)) {
        const o = this._getAllProxyEmitters(i);
        for (const a of o)
          this.stopListening(a, s, r);
      } else
        super.stopListening(i, s, r);
    }
    /**
    * Retrieves ProxyEmitter instance for given DOM Node residing in this Host and given options.
    *
    * @param node DOM Node of the ProxyEmitter.
    * @param options Additional options.
    * @param options.useCapture Indicates that events of this type will be dispatched to the registered
    * listener before being dispatched to any EventTarget beneath it in the DOM tree.
    * @param options.usePassive Indicates that the function specified by listener will never call preventDefault()
    * and prevents blocking browser's main thread by this event handler.
    * @returns ProxyEmitter instance bound to the DOM Node.
    */
    _getProxyEmitter(i, s) {
      return k4(this, r_(i, s));
    }
    /**
    * Retrieves all the ProxyEmitter instances for given DOM Node residing in this Host.
    *
    * @param node DOM Node of the ProxyEmitter.
    */
    _getAllProxyEmitters(i) {
      return [
        {
          capture: !1,
          passive: !1
        },
        {
          capture: !1,
          passive: !0
        },
        {
          capture: !0,
          passive: !1
        },
        {
          capture: !0,
          passive: !0
        }
      ].map((s) => this._getProxyEmitter(i, s)).filter((s) => !!s);
    }
  }
  return t;
}
[
  "_getProxyEmitter",
  "_getAllProxyEmitters",
  "on",
  "once",
  "off",
  "listenTo",
  "stopListening",
  "fire",
  "delegate",
  "stopDelegating",
  "_addEventListener",
  "_removeEventListener"
].forEach((n) => {
  li[n] = s_.prototype[n];
});
class B4 extends (/* @__PURE__ */ Je()) {
  /**
  * @param node DOM Node that fires events.
  * @param options Additional options.
  * @param options.useCapture Indicates that events of this type will be dispatched to the registered
  * listener before being dispatched to any EventTarget beneath it in the DOM tree.
  * @param options.usePassive Indicates that the function specified by listener will never call preventDefault()
  * and prevents blocking browser's main thread by this event handler.
  */
  constructor(e, i) {
    super();
    f(this, "_domNode");
    f(this, "_options");
    /**
    * Collection of native DOM listeners.
    */
    f(this, "_domListeners");
    Xv(this, r_(e, i)), this._domNode = e, this._options = i;
  }
  /**
  * Registers a callback function to be executed when an event is fired.
  *
  * It attaches a native DOM listener to the DOM Node. When fired,
  * a corresponding Emitter event will also fire with DOM Event object as an argument.
  *
  * **Note**: This is automatically called by the
  * {@link module:utils/emittermixin~Emitter#listenTo `Emitter#listenTo()`}.
  *
  * @param event The name of the event.
  */
  attach(e) {
    if (this._domListeners && this._domListeners[e])
      return;
    const i = this._createDomListener(e);
    this._domNode.addEventListener(e, i, this._options), this._domListeners || (this._domListeners = {}), this._domListeners[e] = i;
  }
  /**
  * Stops executing the callback on the given event.
  *
  * **Note**: This is automatically called by the
  * {@link module:utils/emittermixin~Emitter#stopListening `Emitter#stopListening()`}.
  *
  * @param event The name of the event.
  */
  detach(e) {
    let i;
    this._domListeners[e] && (!(i = this._events[e]) || !i.callbacks.length) && this._domListeners[e].removeListener();
  }
  /**
  * Adds callback to emitter for given event.
  *
  * @internal
  * @param event The name of the event.
  * @param callback The function to be called on event.
  * @param options Additional options.
  */
  _addEventListener(e, i, s) {
    this.attach(e), Je().prototype._addEventListener.call(this, e, i, s);
  }
  /**
  * Removes callback from emitter for given event.
  *
  * @internal
  * @param event The name of the event.
  * @param callback The function to stop being called.
  */
  _removeEventListener(e, i) {
    Je().prototype._removeEventListener.call(this, e, i), this.detach(e);
  }
  /**
  * Creates a native DOM listener callback. When the native DOM event
  * is fired it will fire corresponding event on this ProxyEmitter.
  * Note: A native DOM Event is passed as an argument.
  *
  * @param event The name of the event.
  * @returns The DOM listener callback.
  */
  _createDomListener(e) {
    const i = (s) => {
      this.fire(e, s);
    };
    return i.removeListener = () => {
      this._domNode.removeEventListener(e, i, this._options), delete this._domListeners[e];
    }, i;
  }
}
function M4(n) {
  return n["data-ck-expando"] || (n["data-ck-expando"] = Bt());
}
function r_(n, t) {
  let e = M4(n);
  for (const i of Object.keys(t).sort())
    t[i] && (e += "-" + i);
  return e;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function N4(n) {
  const t = [];
  let e = n;
  for (; e && e.nodeType != Node.DOCUMENT_NODE; )
    t.unshift(e), e = e.parentNode;
  return t;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function F4(n) {
  return n instanceof HTMLTextAreaElement ? n.value : n.innerHTML;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function o_(n) {
  const t = n.ownerDocument.defaultView.getComputedStyle(n);
  return {
    top: parseInt(t.borderTopWidth, 10),
    right: parseInt(t.borderRightWidth, 10),
    bottom: parseInt(t.borderBottomWidth, 10),
    left: parseInt(t.borderLeftWidth, 10)
  };
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function a_(n) {
  if (!n.target)
    return null;
  const t = n.target.ownerDocument, e = n.clientX, i = n.clientY;
  let s = null;
  return t.caretRangeFromPoint && t.caretRangeFromPoint(e, i) ? s = t.caretRangeFromPoint(e, i) : n.rangeParent && (s = t.createRange(), s.setStart(n.rangeParent, n.rangeOffset), s.collapse(!0)), s;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function mt(n) {
  return Object.prototype.toString.call(n) == "[object Text]";
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function jc(n) {
  return Object.prototype.toString.apply(n) == "[object Range]";
}
function l_(n) {
  return !n || !n.parentNode || n.offsetParent === ae.document.body ? null : n.offsetParent;
}
const c_ = [
  "top",
  "right",
  "bottom",
  "left",
  "width",
  "height"
];
class ke {
  /**
  * Creates an instance of rect.
  *
  * ```ts
  * // Rect of an HTMLElement.
  * const rectA = new Rect( document.body );
  *
  * // Rect of a DOM Range.
  * const rectB = new Rect( document.getSelection().getRangeAt( 0 ) );
  *
  * // Rect of a window (web browser viewport).
  * const rectC = new Rect( window );
  *
  * // Rect out of an object.
  * const rectD = new Rect( { top: 0, right: 10, bottom: 10, left: 0, width: 10, height: 10 } );
  *
  * // Rect out of another Rect instance.
  * const rectE = new Rect( rectD );
  *
  * // Rect out of a ClientRect.
  * const rectF = new Rect( document.body.getClientRects().item( 0 ) );
  * ```
  *
  * **Note**: By default a rect of an HTML element includes its CSS borders and scrollbars (if any)
  * ant the rect of a `window` includes scrollbars too. Use {@link #excludeScrollbarsAndBorders}
  * to get the inner part of the rect.
  *
  * @param source A source object to create the rect.
  */
  constructor(t) {
    /**
    * The "top" value of the rect.
    *
    * @readonly
    */
    f(this, "top");
    /**
    * The "right" value of the rect.
    *
    * @readonly
    */
    f(this, "right");
    /**
    * The "bottom" value of the rect.
    *
    * @readonly
    */
    f(this, "bottom");
    /**
    * The "left" value of the rect.
    *
    * @readonly
    */
    f(this, "left");
    /**
    * The "width" value of the rect.
    *
    * @readonly
    */
    f(this, "width");
    /**
    * The "height" value of the rect.
    *
    * @readonly
    */
    f(this, "height");
    /**
    * The object this rect is for.
    *
    * @readonly
    */
    f(this, "_source");
    const e = jc(t);
    if (Object.defineProperty(this, "_source", {
      // If the source is a Rect instance, copy it's #_source.
      value: t._source || t,
      writable: !0,
      enumerable: !1
    }), nh(t) || e)
      if (e) {
        const i = ke.getDomRangeRects(t);
        tl(this, ke.getBoundingRect(i));
      } else
        tl(this, t.getBoundingClientRect());
    else if (Zl(t)) {
      const { innerWidth: i, innerHeight: s } = t;
      tl(this, {
        top: 0,
        right: i,
        bottom: s,
        left: 0,
        width: i,
        height: s
      });
    } else
      tl(this, t);
  }
  /**
  * Returns a clone of the rect.
  *
  * @returns A cloned rect.
  */
  clone() {
    return new ke(this);
  }
  /**
  * Moves the rect so that its upper–left corner lands in desired `[ x, y ]` location.
  *
  * @param x Desired horizontal location.
  * @param y Desired vertical location.
  * @returns A rect which has been moved.
  */
  moveTo(t, e) {
    return this.top = e, this.right = t + this.width, this.bottom = e + this.height, this.left = t, this;
  }
  /**
  * Moves the rect in–place by a dedicated offset.
  *
  * @param x A horizontal offset.
  * @param y A vertical offset
  * @returns A rect which has been moved.
  */
  moveBy(t, e) {
    return this.top += e, this.right += t, this.left += t, this.bottom += e, this;
  }
  /**
  * Returns a new rect a a result of intersection with another rect.
  */
  getIntersection(t) {
    const e = {
      top: Math.max(this.top, t.top),
      right: Math.min(this.right, t.right),
      bottom: Math.min(this.bottom, t.bottom),
      left: Math.max(this.left, t.left),
      width: 0,
      height: 0
    };
    if (e.width = e.right - e.left, e.height = e.bottom - e.top, e.width < 0 || e.height < 0)
      return null;
    {
      const i = new ke(e);
      return i._source = this._source, i;
    }
  }
  /**
  * Returns the area of intersection with another rect.
  *
  * @returns Area of intersection.
  */
  getIntersectionArea(t) {
    const e = this.getIntersection(t);
    return e ? e.getArea() : 0;
  }
  /**
  * Returns the area of the rect.
  */
  getArea() {
    return this.width * this.height;
  }
  /**
  * Returns a new rect, a part of the original rect, which is actually visible to the user and is relative to the,`body`,
  * e.g. an original rect cropped by parent element rects which have `overflow` set in CSS
  * other than `"visible"`.
  *
  * If there's no such visible rect, which is when the rect is limited by one or many of
  * the ancestors, `null` is returned.
  *
  * **Note**: This method does not consider the boundaries of the viewport (window).
  * To get a rect cropped by all ancestors and the viewport, use an intersection such as:
  *
  * ```ts
  * const visibleInViewportRect = new Rect( window ).getIntersection( new Rect( source ).getVisible() );
  * ```
  *
  * @returns A visible rect instance or `null`, if there's none.
  */
  getVisible() {
    const t = this._source;
    let e = this.clone();
    if (sp(t))
      return e;
    let i = t, s = t.parentNode || t.commonAncestorContainer, r;
    for (; s && !sp(s); ) {
      const o = D4(s) === "visible";
      i instanceof HTMLElement && rp(i) === "absolute" && (r = i);
      const a = rp(s);
      if (o || r && (a === "relative" && o || a !== "relative")) {
        i = s, s = s.parentNode;
        continue;
      }
      const l = new ke(s), c = e.getIntersection(l);
      if (c)
        c.getArea() < e.getArea() && (e = c);
      else
        return null;
      i = s, s = s.parentNode;
    }
    return e;
  }
  /**
  * Checks if all property values ({@link #top}, {@link #left}, {@link #right},
  * {@link #bottom}, {@link #width} and {@link #height}) are the equal in both rect
  * instances.
  *
  * @param anotherRect A rect instance to compare with.
  * @returns `true` when Rects are equal. `false` otherwise.
  */
  isEqual(t) {
    for (const e of c_)
      if (this[e] !== t[e])
        return !1;
    return !0;
  }
  /**
  * Checks whether a rect fully contains another rect instance.
  *
  * @param anotherRect
  * @returns `true` if contains, `false` otherwise.
  */
  contains(t) {
    const e = this.getIntersection(t);
    return !!(e && e.isEqual(t));
  }
  /**
  * Recalculates screen coordinates to coordinates relative to the positioned ancestor offset.
  */
  toAbsoluteRect() {
    const { scrollX: t, scrollY: e } = ae.window, i = this.clone().moveBy(t, e);
    if (nh(i._source)) {
      const s = l_(i._source);
      s && $4(i, s);
    }
    return i;
  }
  /**
  * Excludes scrollbars and CSS borders from the rect.
  *
  * * Borders are removed when {@link #_source} is an HTML element.
  * * Scrollbars are excluded from HTML elements and the `window`.
  *
  * @returns A rect which has been updated.
  */
  excludeScrollbarsAndBorders() {
    const t = this._source;
    let e, i, s;
    if (Zl(t))
      e = t.innerWidth - t.document.documentElement.clientWidth, i = t.innerHeight - t.document.documentElement.clientHeight, s = t.getComputedStyle(t.document.documentElement).direction;
    else {
      const r = o_(t);
      e = t.offsetWidth - t.clientWidth - r.left - r.right, i = t.offsetHeight - t.clientHeight - r.top - r.bottom, s = t.ownerDocument.defaultView.getComputedStyle(t).direction, this.left += r.left, this.top += r.top, this.right -= r.right, this.bottom -= r.bottom, this.width = this.right - this.left, this.height = this.bottom - this.top;
    }
    return this.width -= e, s === "ltr" ? this.right -= e : this.left += e, this.height -= i, this.bottom -= i, this;
  }
  /**
  * Returns an array of rects of the given native DOM Range.
  *
  * @param range A native DOM range.
  * @returns DOM Range rects.
  */
  static getDomRangeRects(t) {
    const e = [], i = Array.from(t.getClientRects());
    if (i.length)
      for (const s of i)
        e.push(new ke(s));
    else {
      let s = t.startContainer;
      mt(s) && (s = s.parentNode);
      const r = new ke(s.getBoundingClientRect());
      r.right = r.left, r.width = 0, e.push(r);
    }
    return e;
  }
  /**
  * Returns a bounding rectangle that contains all the given `rects`.
  *
  * @param rects A list of rectangles that should be contained in the result rectangle.
  * @returns Bounding rectangle or `null` if no `rects` were given.
  */
  static getBoundingRect(t) {
    const e = {
      left: Number.POSITIVE_INFINITY,
      top: Number.POSITIVE_INFINITY,
      right: Number.NEGATIVE_INFINITY,
      bottom: Number.NEGATIVE_INFINITY,
      width: 0,
      height: 0
    };
    let i = 0;
    for (const s of t)
      i++, e.left = Math.min(e.left, s.left), e.top = Math.min(e.top, s.top), e.right = Math.max(e.right, s.right), e.bottom = Math.max(e.bottom, s.bottom);
    return i == 0 ? null : (e.width = e.right - e.left, e.height = e.bottom - e.top, new ke(e));
  }
}
function tl(n, t) {
  for (const e of c_)
    n[e] = t[e];
}
function sp(n) {
  return nh(n) ? n === n.ownerDocument.body : !1;
}
function nh(n) {
  return n !== null && typeof n == "object" && n.nodeType === 1 && typeof n.getBoundingClientRect == "function";
}
function rp(n) {
  return n instanceof HTMLElement ? n.ownerDocument.defaultView.getComputedStyle(n).position : "static";
}
function D4(n) {
  return n instanceof HTMLElement ? n.ownerDocument.defaultView.getComputedStyle(n).overflow : "visible";
}
function $4(n, t) {
  const e = new ke(t), i = o_(t);
  let s = 0, r = 0;
  s -= e.left, r -= e.top, s += t.scrollLeft, r += t.scrollTop, s -= i.left, r -= i.top, n.moveBy(s, r);
}
const ft = class ft {
  /**
  * Creates an instance of the `ResizeObserver` class.
  *
  * @param element A DOM element that is to be observed for resizing. Note that
  * the element must be visible (i.e. not detached from DOM) for the observer to work.
  * @param callback A function called when the observed element was resized. It passes
  * the [`ResizeObserverEntry`](https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserverEntry)
  * object with information about the resize event.
  */
  constructor(t, e) {
    /**
    * The element observed by this observer.
    */
    f(this, "_element");
    /**
    * The callback executed each time {@link #_element} is resized.
    */
    f(this, "_callback");
    ft._observerInstance || ft._createObserver(), this._element = t, this._callback = e, ft._addElementCallback(t, e), ft._observerInstance.observe(t);
  }
  /**
  * The element observed by this observer.
  */
  get element() {
    return this._element;
  }
  /**
  * Destroys the observer which disables the `callback` passed to the {@link #constructor}.
  */
  destroy() {
    ft._deleteElementCallback(this._element, this._callback);
  }
  /**
  * Registers a new resize callback for the DOM element.
  */
  static _addElementCallback(t, e) {
    ft._elementCallbacks || (ft._elementCallbacks = /* @__PURE__ */ new Map());
    let i = ft._elementCallbacks.get(t);
    i || (i = /* @__PURE__ */ new Set(), ft._elementCallbacks.set(t, i)), i.add(e);
  }
  /**
  * Removes a resize callback from the DOM element. If no callbacks are left
  * for the element, it removes the element from the native observer.
  */
  static _deleteElementCallback(t, e) {
    const i = ft._getElementCallbacks(t);
    i && (i.delete(e), i.size || (ft._elementCallbacks.delete(t), ft._observerInstance.unobserve(t))), ft._elementCallbacks && !ft._elementCallbacks.size && (ft._observerInstance = null, ft._elementCallbacks = null);
  }
  /**
  * Returns are registered resize callbacks for the DOM element.
  */
  static _getElementCallbacks(t) {
    return ft._elementCallbacks ? ft._elementCallbacks.get(t) : null;
  }
  /**
  * Creates the single native observer shared across all `ResizeObserver` instances.
  */
  static _createObserver() {
    ft._observerInstance = new ae.window.ResizeObserver((t) => {
      for (const e of t) {
        const i = ft._getElementCallbacks(e.target);
        if (i)
          for (const s of i)
            s(e);
      }
    });
  }
};
/**
* The single native observer instance shared across all {@link module:utils/dom/resizeobserver~ResizeObserver} instances.
*/
f(ft, "_observerInstance", null), /**
* A mapping of native DOM elements and their callbacks shared across all
* {@link module:utils/dom/resizeobserver~ResizeObserver} instances.
*/
f(ft, "_elementCallbacks", null);
let $r = ft;
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function op(n, t) {
  n instanceof HTMLTextAreaElement && (n.value = t), n.innerHTML = t;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function to(n) {
  return (t) => t + n;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Po(n) {
  let t = 0;
  for (; n.previousSibling; )
    n = n.previousSibling, t++;
  return t;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function u_(n, t, e) {
  n.insertBefore(e, n.childNodes[t] || null);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function zo(n) {
  return n && n.nodeType === Node.COMMENT_NODE;
}
function d_(n) {
  try {
    ae.document.createAttribute(n);
  } catch {
    return !1;
  }
  return !0;
}
function yn(n) {
  return n ? mt(n) ? yn(n.parentElement) : n.getClientRects ? !!n.getClientRects().length : !1 : !1;
}
function wf({ element: n, target: t, positions: e, limiter: i, fitInViewport: s, viewportOffsetConfig: r }) {
  Fs(t) && (t = t()), Fs(i) && (i = i());
  const o = l_(n), a = z4(r), l = new ke(n), c = ap(t, a);
  let u;
  if (!c || !a.getIntersection(c))
    return null;
  const d = {
    targetRect: c,
    elementRect: l,
    positionedElementAncestor: o,
    viewportRect: a
  };
  if (!i && !s)
    u = new h_(e[0], d);
  else {
    if (i) {
      const h = ap(i, a);
      h && (d.limiterRect = h);
    }
    u = H4(e, d);
  }
  return u;
}
function ap(n, t) {
  const e = new ke(n).getVisible();
  return e ? e.getIntersection(t) : null;
}
function z4(n) {
  n = Object.assign({
    top: 0,
    bottom: 0,
    left: 0,
    right: 0
  }, n);
  const t = new ke(ae.window);
  return t.top += n.top, t.height -= n.top, t.bottom -= n.bottom, t.height -= n.bottom, t;
}
function H4(n, t) {
  const { elementRect: e } = t, i = e.getArea(), s = n.map((a) => new h_(a, t)).filter((a) => !!a.name);
  let r = 0, o = null;
  for (const a of s) {
    const { limiterIntersectionArea: l, viewportIntersectionArea: c } = a;
    if (l === i)
      return a;
    const u = c ** 2 + l ** 2;
    u > r && (r = u, o = a);
  }
  return o;
}
class h_ {
  /**
  * Creates an instance of the {@link module:utils/dom/position~PositionObject} class.
  *
  * @param positioningFunction function The function that defines the expected
  * coordinates the positioned element should move to.
  * @param options options object.
  * @param options.elementRect The positioned element rect.
  * @param options.targetRect The target element rect.
  * @param options.viewportRect The viewport rect.
  * @param options.limiterRect The limiter rect.
  * @param options.positionedElementAncestor Nearest element ancestor element which CSS position is not "static".
  */
  constructor(t, e) {
    f(this, "name");
    f(this, "config");
    f(this, "_positioningFunctionCoordinates");
    f(this, "_options");
    f(this, "_cachedRect");
    f(this, "_cachedAbsoluteRect");
    const i = t(e.targetRect, e.elementRect, e.viewportRect, e.limiterRect);
    if (!i)
      return;
    const { left: s, top: r, name: o, config: a } = i;
    this.name = o, this.config = a, this._positioningFunctionCoordinates = {
      left: s,
      top: r
    }, this._options = e;
  }
  /**
  * The left value in pixels in the CSS `position: absolute` coordinate system.
  * Set it on the positioned element in DOM to move it to the position.
  */
  get left() {
    return this._absoluteRect.left;
  }
  /**
  * The top value in pixels in the CSS `position: absolute` coordinate system.
  * Set it on the positioned element in DOM to move it to the position.
  */
  get top() {
    return this._absoluteRect.top;
  }
  /**
  * An intersection area between positioned element and limiter within viewport constraints.
  */
  get limiterIntersectionArea() {
    const t = this._options.limiterRect;
    return t ? t.getIntersectionArea(this._rect) : 0;
  }
  /**
  * An intersection area between positioned element and viewport.
  */
  get viewportIntersectionArea() {
    return this._options.viewportRect.getIntersectionArea(this._rect);
  }
  /**
  * An already positioned element rect. A clone of the element rect passed to the constructor
  * but placed in the viewport according to the positioning function.
  */
  get _rect() {
    return this._cachedRect ? this._cachedRect : (this._cachedRect = this._options.elementRect.clone().moveTo(this._positioningFunctionCoordinates.left, this._positioningFunctionCoordinates.top), this._cachedRect);
  }
  /**
  * An already absolutely positioned element rect. See ({@link #_rect}).
  */
  get _absoluteRect() {
    return this._cachedAbsoluteRect ? this._cachedAbsoluteRect : (this._cachedAbsoluteRect = this._rect.toAbsoluteRect(), this._cachedAbsoluteRect);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function lp(n) {
  const t = n.parentNode;
  t && t.removeChild(n);
}
function f_({ target: n, viewportOffset: t = 0, ancestorOffset: e = 0, alignToTop: i, forceScroll: s }) {
  const r = vf(n);
  let o = r, a = null;
  for (t = W4(t); o; ) {
    let l;
    o == r ? l = cp(n) : l = cp(a), U4({
      parent: l,
      getRect: () => Lu(n, o),
      alignToTop: i,
      ancestorOffset: e,
      forceScroll: s
    });
    let c = Lu(n, o);
    const u = Lu(l, o);
    if (c.height > u.height) {
      const d = c.getIntersection(u);
      d && (c = d);
    }
    if (j4({
      window: o,
      rect: c,
      viewportOffset: t,
      alignToTop: i,
      forceScroll: s
    }), o.parent != o) {
      if (a = o.frameElement, o = o.parent, !a)
        return;
    } else
      o = null;
  }
}
function j4({ window: n, rect: t, alignToTop: e, forceScroll: i, viewportOffset: s }) {
  const r = t.clone().moveBy(0, s.bottom), o = t.clone().moveBy(0, -s.top), a = new ke(n).excludeScrollbarsAndBorders(), l = [
    o,
    r
  ], c = e && i, u = l.every((p) => a.contains(p));
  let { scrollX: d, scrollY: h } = n;
  const m = d, g = h;
  c ? h -= a.top - t.top + s.top : u || (g_(o, a) ? h -= a.top - t.top + s.top : m_(r, a) && (e ? h += t.top - a.top - s.top : h += t.bottom - a.bottom + s.bottom)), u || (p_(t, a) ? d -= a.left - t.left + s.left : b_(t, a) && (d += t.right - a.right + s.right)), (d != m || h !== g) && n.scrollTo(d, h);
}
function U4({ parent: n, getRect: t, alignToTop: e, forceScroll: i, ancestorOffset: s = 0, limiterElement: r }) {
  const o = vf(n), a = e && i;
  let l, c, u;
  const d = r || o.document.body;
  for (; n != d; )
    c = t(), l = new ke(n).excludeScrollbarsAndBorders(), u = l.contains(c), a ? n.scrollTop -= l.top - c.top + s : u || (g_(c, l) ? n.scrollTop -= l.top - c.top + s : m_(c, l) && (e ? n.scrollTop += c.top - l.top - s : n.scrollTop += c.bottom - l.bottom + s)), u || (p_(c, l) ? n.scrollLeft -= l.left - c.left + s : b_(c, l) && (n.scrollLeft += c.right - l.right + s)), n = n.parentNode;
}
function m_(n, t) {
  return n.bottom > t.bottom;
}
function g_(n, t) {
  return n.top < t.top;
}
function p_(n, t) {
  return n.left < t.left;
}
function b_(n, t) {
  return n.right > t.right;
}
function vf(n) {
  return jc(n) ? n.startContainer.ownerDocument.defaultView : n.ownerDocument.defaultView;
}
function cp(n) {
  if (jc(n)) {
    let t = n.commonAncestorContainer;
    return mt(t) && (t = t.parentNode), t;
  } else
    return n.parentNode;
}
function Lu(n, t) {
  const e = vf(n), i = new ke(n);
  if (e === t)
    return i;
  {
    let s = e;
    for (; s != t; ) {
      const r = s.frameElement, o = new ke(r).excludeScrollbarsAndBorders();
      i.moveBy(o.left, o.top), s = s.parent;
    }
  }
  return i;
}
function W4(n) {
  return typeof n == "number" ? {
    top: n,
    bottom: n,
    left: n,
    right: n
  } : n;
}
const q4 = {
  ctrl: "⌃",
  cmd: "⌘",
  alt: "⌥",
  shift: "⇧"
}, G4 = {
  ctrl: "Ctrl+",
  alt: "Alt+",
  shift: "Shift+"
}, up = {
  37: "←",
  38: "↑",
  39: "→",
  40: "↓",
  9: "⇥",
  33: "Page Up",
  34: "Page Down"
}, Ie = /* @__PURE__ */ X4(), K4 = /* @__PURE__ */ Object.fromEntries(/* @__PURE__ */ Object.entries(Ie).map(([n, t]) => {
  let e;
  return t in up ? e = up[t] : e = n.charAt(0).toUpperCase() + n.slice(1), [
    t,
    e
  ];
}));
function zs(n) {
  let t;
  if (typeof n == "string") {
    if (t = Ie[n.toLowerCase()], !t)
      throw new S("keyboard-unknown-key", null, {
        key: n
      });
  } else
    t = n.keyCode + (n.altKey ? Ie.alt : 0) + (n.ctrlKey ? Ie.ctrl : 0) + (n.shiftKey ? Ie.shift : 0) + (n.metaKey ? Ie.cmd : 0);
  return t;
}
function Uc(n) {
  return typeof n == "string" && (n = Q4(n)), n.map((t) => typeof t == "string" ? Y4(t) : t).reduce((t, e) => e + t, 0);
}
function Yl(n, t) {
  let e = Uc(n);
  const i = be.isMac || be.isiOS;
  return Object.entries(i ? q4 : G4).reduce((o, [a, l]) => ((e & Ie[a]) != 0 && (e &= ~Ie[a], o += l), o), "") + (e ? K4[e] : "");
}
function Z4(n) {
  return n == Ie.arrowright || n == Ie.arrowleft || n == Ie.arrowup || n == Ie.arrowdown;
}
function Wc(n, t) {
  const e = t === "ltr";
  switch (n) {
    case Ie.arrowleft:
      return e ? "left" : "right";
    case Ie.arrowright:
      return e ? "right" : "left";
    case Ie.arrowup:
      return "up";
    case Ie.arrowdown:
      return "down";
  }
}
function Y4(n) {
  if (n.endsWith("!"))
    return zs(n.slice(0, -1));
  const t = zs(n);
  return (be.isMac || be.isiOS) && t == Ie.ctrl ? Ie.cmd : t;
}
function J4(n, t) {
  const e = Wc(n, t);
  return e === "down" || e === "right";
}
function X4() {
  const n = {
    pageup: 33,
    pagedown: 34,
    end: 35,
    home: 36,
    arrowleft: 37,
    arrowup: 38,
    arrowright: 39,
    arrowdown: 40,
    backspace: 8,
    delete: 46,
    enter: 13,
    space: 32,
    esc: 27,
    tab: 9,
    // The idea about these numbers is that they do not collide with any real key codes, so we can use them
    // like bit masks.
    ctrl: 1114112,
    shift: 2228224,
    alt: 4456448,
    cmd: 8912896
  };
  for (let t = 65; t <= 90; t++) {
    const e = String.fromCharCode(t);
    n[e.toLowerCase()] = t;
  }
  for (let t = 48; t <= 57; t++)
    n[t - 48] = t;
  for (let t = 112; t <= 123; t++)
    n["f" + (t - 111)] = t;
  return Object.assign(n, {
    "'": 222,
    ",": 108,
    "-": 109,
    ".": 110,
    "/": 111,
    ";": 186,
    "=": 187,
    "[": 219,
    "\\": 220,
    "]": 221,
    "`": 223
  }), n;
}
function Q4(n) {
  return n.split("+").map((t) => t.trim());
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const e6 = [
  "ar",
  "ara",
  "dv",
  "div",
  "fa",
  "per",
  "fas",
  "he",
  "heb",
  "ku",
  "kur",
  "ug",
  "uig"
  // Uighur, Uyghur
];
function sh(n) {
  return e6.includes(n) ? "rtl" : "ltr";
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Ue(n) {
  return Array.isArray(n) ? n : [
    n
  ];
}
/* istanbul ignore else -- @preserve */
ae.window.CKEDITOR_TRANSLATIONS || (ae.window.CKEDITOR_TRANSLATIONS = {});
function t6(n, t, e = 1, i) {
  if (typeof e != "number")
    throw new S("translation-service-quantity-not-a-number", null, {
      quantity: e
    });
  const s = i || ae.window.CKEDITOR_TRANSLATIONS, r = s6(s);
  r === 1 && (n = Object.keys(s)[0]);
  const o = t.id || t.string;
  if (r === 0 || !n6(n, o, s))
    return e !== 1 ? t.plural : t.string;
  const a = s[n].dictionary, l = s[n].getPluralForm || ((d) => d === 1 ? 0 : 1), c = a[o];
  if (typeof c == "string")
    return c;
  const u = Number(l(e));
  return c[u];
}
function i6(n) {
  return Array.isArray(n) ? n.reduce((t, e) => bf(t, e)) : n;
}
function n6(n, t, e) {
  return !!e[n] && !!e[n].dictionary[t];
}
function s6(n) {
  return Object.keys(n).length;
}
class r6 {
  /**
  * Creates a new instance of the locale class. Learn more about
  * {@glink getting-started/setup/ui-language configuring the language of the editor}.
  *
  * @param options Locale configuration.
  * @param options.uiLanguage The editor UI language code in the
  * [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format. See {@link #uiLanguage}.
  * @param options.contentLanguage The editor content language code in the
  * [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format. If not specified, the same as `options.language`.
  * See {@link #contentLanguage}.
  * @param translations Translations passed as a editor config parameter.
  */
  constructor({ uiLanguage: t = "en", contentLanguage: e, translations: i } = {}) {
    /**
    * The editor UI language code in the [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format.
    *
    * If the {@link #contentLanguage content language} was not specified in the `Locale` constructor,
    * it also defines the language of the content.
    */
    f(this, "uiLanguage");
    /**
    * Text direction of the {@link #uiLanguage editor UI language}. Either `'ltr'` or `'rtl'`.
    */
    f(this, "uiLanguageDirection");
    /**
    * The editor content language code in the [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format.
    *
    * Usually the same as the {@link #uiLanguage editor language}, it can be customized by passing an optional
    * argument to the `Locale` constructor.
    */
    f(this, "contentLanguage");
    /**
    * Text direction of the {@link #contentLanguage editor content language}.
    *
    * If the content language was passed directly to the `Locale` constructor, this property represents the
    * direction of that language.
    *
    * If the {@link #contentLanguage editor content language} was derived from the {@link #uiLanguage editor language},
    * the content language direction is the same as the {@link #uiLanguageDirection UI language direction}.
    *
    * The value is either `'ltr'` or `'rtl'`.
    */
    f(this, "contentLanguageDirection");
    /**
    * Translates the given message to the {@link #uiLanguage}. This method is also available in
    * {@link module:core/editor/editor~Editor#t `Editor`} and {@link module:ui/view~View#t `View`}.
    *
    * This method's context is statically bound to the `Locale` instance and **should always be called as a function**:
    *
    * ```ts
    * const t = locale.t;
    * t( 'Label' );
    * ```
    *
    * The message can be either a string or an object implementing the {@link module:utils/translation-service~Message} interface.
    *
    * The message may contain placeholders (`%<index>`) for value(s) that are passed as a `values` parameter.
    * For an array of values, the `%<index>` will be changed to an element of that array at the given index.
    * For a single value passed as the second argument, only the `%0` placeholders will be changed to the provided value.
    *
    * ```ts
    * t( 'Created file "%0" in %1ms.', [ fileName, timeTaken ] );
    * t( 'Created file "%0", fileName );
    * ```
    *
    * The message supports plural forms. To specify the plural form, use the `plural` property. Singular or plural form
    * will be chosen depending on the first value from the passed `values`. The value of the `plural` property is used
    * as a default plural translation when the translation for the target language is missing.
    *
    * ```ts
    * t( { string: 'Add a space', plural: 'Add %0 spaces' }, 1 ); // 'Add a space' for the English language.
    * t( { string: 'Add a space', plural: 'Add %0 spaces' }, 5 ); // 'Add 5 spaces' for the English language.
    * t( { string: '%1 a space', plural: '%1 %0 spaces' }, [ 2, 'Add' ] ); // 'Add 2 spaces' for the English language.
    *
    * t( { string: 'Add a space', plural: 'Add %0 spaces' }, 1 ); // 'Dodaj spację' for the Polish language.
    * t( { string: 'Add a space', plural: 'Add %0 spaces' }, 5 ); // 'Dodaj 5 spacji' for the Polish language.
    * t( { string: '%1 a space', plural: '%1 %0 spaces' }, [ 2, 'Add' ] ); // 'Dodaj 2 spacje' for the Polish language.
    * ```
    *
    *  * The message should provide an ID using the `id` property when the message strings are not unique and their
    * translations should be different.
    *
    * ```ts
    * translate( 'en', { string: 'image', id: 'ADD_IMAGE' } );
    * translate( 'en', { string: 'image', id: 'AN_IMAGE' } );
    * ```
    */
    f(this, "t");
    /**
    * Object that contains translations.
    */
    f(this, "translations");
    this.uiLanguage = t, this.contentLanguage = e || this.uiLanguage, this.uiLanguageDirection = sh(this.uiLanguage), this.contentLanguageDirection = sh(this.contentLanguage), this.translations = i6(i), this.t = (s, r) => this._t(s, r);
  }
  /**
  * The editor UI language code in the [ISO 639-1](https://en.wikipedia.org/wiki/ISO_639-1) format.
  *
  * **Note**: This property was deprecated. Please use {@link #uiLanguage} and {@link #contentLanguage}
  * properties instead.
  *
  * @deprecated
  */
  get language() {
    return console.warn("locale-deprecated-language-property: The Locale#language property has been deprecated and will be removed in the near future. Please use #uiLanguage and #contentLanguage properties instead."), this.uiLanguage;
  }
  /**
  * An unbound version of the {@link #t} method.
  */
  _t(t, e = []) {
    e = Ue(e), typeof t == "string" && (t = {
      string: t
    });
    const s = !!t.plural ? e[0] : 1, r = t6(this.uiLanguage, t, s, this.translations);
    return o6(r, e);
  }
}
function o6(n, t) {
  return n.replace(/%(\d+)/g, (e, i) => i < t.length ? t[i] : e);
}
class bt extends (/* @__PURE__ */ Je()) {
  constructor(e = {}, i = {}) {
    super();
    /**
    * The internal list of items in the collection.
    */
    f(this, "_items");
    /**
    * The internal map of items in the collection.
    */
    f(this, "_itemMap");
    /**
    * The name of the property which is considered to identify an item.
    */
    f(this, "_idProperty");
    /**
    * A collection instance this collection is bound to as a result
    * of calling {@link #bindTo} method.
    */
    f(this, "_bindToCollection");
    /**
    * A helper mapping external items of a bound collection ({@link #bindTo})
    * and actual items of this collection. It provides information
    * necessary to properly remove items bound to another collection.
    *
    * See {@link #_bindToInternalToExternalMap}.
    */
    f(this, "_bindToExternalToInternalMap");
    /**
    * A helper mapping items of this collection to external items of a bound collection
    * ({@link #bindTo}). It provides information necessary to manage the bindings, e.g.
    * to avoid loops in two–way bindings.
    *
    * See {@link #_bindToExternalToInternalMap}.
    */
    f(this, "_bindToInternalToExternalMap");
    /**
    * Stores indexes of skipped items from bound external collection.
    */
    f(this, "_skippedIndexesFromExternal");
    const s = Ai(e);
    if (s || (i = e), this._items = [], this._itemMap = /* @__PURE__ */ new Map(), this._idProperty = i.idProperty || "id", this._bindToExternalToInternalMap = /* @__PURE__ */ new WeakMap(), this._bindToInternalToExternalMap = /* @__PURE__ */ new WeakMap(), this._skippedIndexesFromExternal = [], s)
      for (const r of e)
        this._items.push(r), this._itemMap.set(this._getItemIdBeforeAdding(r), r);
  }
  /**
  * The number of items available in the collection.
  */
  get length() {
    return this._items.length;
  }
  /**
  * Returns the first item from the collection or null when collection is empty.
  */
  get first() {
    return this._items[0] || null;
  }
  /**
  * Returns the last item from the collection or null when collection is empty.
  */
  get last() {
    return this._items[this.length - 1] || null;
  }
  /**
  * Adds an item into the collection.
  *
  * If the item does not have an id, then it will be automatically generated and set on the item.
  *
  * @param item
  * @param index The position of the item in the collection. The item
  * is pushed to the collection when `index` not specified.
  * @fires add
  * @fires change
  */
  add(e, i) {
    return this.addMany([
      e
    ], i);
  }
  /**
  * Adds multiple items into the collection.
  *
  * Any item not containing an id will get an automatically generated one.
  *
  * @param items
  * @param index The position of the insertion. Items will be appended if no `index` is specified.
  * @fires add
  * @fires change
  */
  addMany(e, i) {
    if (i === void 0)
      i = this._items.length;
    else if (i > this._items.length || i < 0)
      throw new S("collection-add-item-invalid-index", this);
    let s = 0;
    for (const r of e) {
      const o = this._getItemIdBeforeAdding(r), a = i + s;
      this._items.splice(a, 0, r), this._itemMap.set(o, r), this.fire("add", r, a), s++;
    }
    return this.fire("change", {
      added: e,
      removed: [],
      index: i
    }), this;
  }
  /**
  * Gets an item by its ID or index.
  *
  * @param idOrIndex The item ID or index in the collection.
  * @returns The requested item or `null` if such item does not exist.
  */
  get(e) {
    let i;
    if (typeof e == "string")
      i = this._itemMap.get(e);
    else if (typeof e == "number")
      i = this._items[e];
    else
      throw new S("collection-get-invalid-arg", this);
    return i || null;
  }
  /**
  * Returns a Boolean indicating whether the collection contains an item.
  *
  * @param itemOrId The item or its ID in the collection.
  * @returns `true` if the collection contains the item, `false` otherwise.
  */
  has(e) {
    if (typeof e == "string")
      return this._itemMap.has(e);
    {
      const i = this._idProperty, s = e[i];
      return s && this._itemMap.has(s);
    }
  }
  /**
  * Gets an index of an item in the collection.
  * When an item is not defined in the collection, the index will equal -1.
  *
  * @param itemOrId The item or its ID in the collection.
  * @returns The index of a given item.
  */
  getIndex(e) {
    let i;
    return typeof e == "string" ? i = this._itemMap.get(e) : i = e, i ? this._items.indexOf(i) : -1;
  }
  /**
  * Removes an item from the collection.
  *
  * @param subject The item to remove, its ID or index in the collection.
  * @returns The removed item.
  * @fires remove
  * @fires change
  */
  remove(e) {
    const [i, s] = this._remove(e);
    return this.fire("change", {
      added: [],
      removed: [
        i
      ],
      index: s
    }), i;
  }
  /**
  * Executes the callback for each item in the collection and composes an array or values returned by this callback.
  *
  * @typeParam U The result type of the callback.
  * @param callback
  * @param ctx Context in which the `callback` will be called.
  * @returns The result of mapping.
  */
  map(e, i) {
    return this._items.map(e, i);
  }
  /**
  * Performs the specified action for each item in the collection.
  *
  * @param ctx Context in which the `callback` will be called.
  */
  forEach(e, i) {
    this._items.forEach(e, i);
  }
  /**
  * Finds the first item in the collection for which the `callback` returns a true value.
  *
  * @param callback
  * @param ctx Context in which the `callback` will be called.
  * @returns The item for which `callback` returned a true value.
  */
  find(e, i) {
    return this._items.find(e, i);
  }
  /**
  * Returns an array with items for which the `callback` returned a true value.
  *
  * @param callback
  * @param ctx Context in which the `callback` will be called.
  * @returns The array with matching items.
  */
  filter(e, i) {
    return this._items.filter(e, i);
  }
  /**
  * Removes all items from the collection and destroys the binding created using
  * {@link #bindTo}.
  *
  * @fires remove
  * @fires change
  */
  clear() {
    this._bindToCollection && (this.stopListening(this._bindToCollection), this._bindToCollection = null);
    const e = Array.from(this._items);
    for (; this.length; )
      this._remove(0);
    this.fire("change", {
      added: [],
      removed: e,
      index: 0
    });
  }
  /**
  * Binds and synchronizes the collection with another one.
  *
  * The binding can be a simple factory:
  *
  * ```ts
  * class FactoryClass {
  * 	public label: string;
  *
  * 	constructor( data: { label: string } ) {
  * 		this.label = data.label;
  * 	}
  * }
  *
  * const source = new Collection<{ label: string }>( { idProperty: 'label' } );
  * const target = new Collection<FactoryClass>();
  *
  * target.bindTo( source ).as( FactoryClass );
  *
  * source.add( { label: 'foo' } );
  * source.add( { label: 'bar' } );
  *
  * console.log( target.length ); // 2
  * console.log( target.get( 1 ).label ); // 'bar'
  *
  * source.remove( 0 );
  * console.log( target.length ); // 1
  * console.log( target.get( 0 ).label ); // 'bar'
  * ```
  *
  * or the factory driven by a custom callback:
  *
  * ```ts
  * class FooClass {
  * 	public label: string;
  *
  * 	constructor( data: { label: string } ) {
  * 		this.label = data.label;
  * 	}
  * }
  *
  * class BarClass {
  * 	public label: string;
  *
  * 	constructor( data: { label: string } ) {
  * 		this.label = data.label;
  * 	}
  * }
  *
  * const source = new Collection<{ label: string }>( { idProperty: 'label' } );
  * const target = new Collection<FooClass | BarClass>();
  *
  * target.bindTo( source ).using( ( item ) => {
  * 	if ( item.label == 'foo' ) {
  * 		return new FooClass( item );
  * 	} else {
  * 		return new BarClass( item );
  * 	}
  * } );
  *
  * source.add( { label: 'foo' } );
  * source.add( { label: 'bar' } );
  *
  * console.log( target.length ); // 2
  * console.log( target.get( 0 ) instanceof FooClass ); // true
  * console.log( target.get( 1 ) instanceof BarClass ); // true
  * ```
  *
  * or the factory out of property name:
  *
  * ```ts
  * const source = new Collection<{ nested: { value: string } }>();
  * const target = new Collection<{ value: string }>();
  *
  * target.bindTo( source ).using( 'nested' );
  *
  * source.add( { nested: { value: 'foo' } } );
  * source.add( { nested: { value: 'bar' } } );
  *
  * console.log( target.length ); // 2
  * console.log( target.get( 0 ).value ); // 'foo'
  * console.log( target.get( 1 ).value ); // 'bar'
  * ```
  *
  * It's possible to skip specified items by returning null value:
  *
  * ```ts
  * const source = new Collection<{ hidden: boolean }>();
  * const target = new Collection<{ hidden: boolean }>();
  *
  * target.bindTo( source ).using( item => {
  * 	if ( item.hidden ) {
  * 		return null;
  * 	}
  *
  * 	return item;
  * } );
  *
  * source.add( { hidden: true } );
  * source.add( { hidden: false } );
  *
  * console.log( source.length ); // 2
  * console.log( target.length ); // 1
  * ```
  *
  * **Note**: {@link #clear} can be used to break the binding.
  *
  * @typeParam S The type of `externalCollection` element.
  * @param externalCollection A collection to be bound.
  * @returns The binding chain object.
  */
  bindTo(e) {
    if (this._bindToCollection)
      throw new S("collection-bind-to-rebind", this);
    return this._bindToCollection = e, {
      as: (i) => {
        this._setUpBindToBinding((s) => new i(s));
      },
      using: (i) => {
        typeof i == "function" ? this._setUpBindToBinding(i) : this._setUpBindToBinding((s) => s[i]);
      }
    };
  }
  /**
  * Finalizes and activates a binding initiated by {@link #bindTo}.
  *
  * @param factory A function which produces collection items.
  */
  _setUpBindToBinding(e) {
    const i = this._bindToCollection, s = (r, o, a) => {
      const l = i._bindToCollection == this, c = i._bindToInternalToExternalMap.get(o);
      if (l && c)
        this._bindToExternalToInternalMap.set(o, c), this._bindToInternalToExternalMap.set(c, o);
      else {
        const u = e(o);
        if (!u) {
          this._skippedIndexesFromExternal.push(a);
          return;
        }
        let d = a;
        for (const h of this._skippedIndexesFromExternal)
          a > h && d--;
        for (const h of i._skippedIndexesFromExternal)
          d >= h && d++;
        this._bindToExternalToInternalMap.set(o, u), this._bindToInternalToExternalMap.set(u, o), this.add(u, d);
        for (let h = 0; h < i._skippedIndexesFromExternal.length; h++)
          d <= i._skippedIndexesFromExternal[h] && i._skippedIndexesFromExternal[h]++;
      }
    };
    for (const r of i)
      s(null, r, i.getIndex(r));
    this.listenTo(i, "add", s), this.listenTo(i, "remove", (r, o, a) => {
      const l = this._bindToExternalToInternalMap.get(o);
      l && this.remove(l), this._skippedIndexesFromExternal = this._skippedIndexesFromExternal.reduce((c, u) => (a < u && c.push(u - 1), a > u && c.push(u), c), []);
    });
  }
  /**
  * Returns an unique id property for a given `item`.
  *
  * The method will generate new id and assign it to the `item` if it doesn't have any.
  *
  * @param item Item to be added.
  */
  _getItemIdBeforeAdding(e) {
    const i = this._idProperty;
    let s;
    if (i in e) {
      if (s = e[i], typeof s != "string")
        throw new S("collection-add-invalid-id", this);
      if (this.get(s))
        throw new S("collection-add-item-already-exists", this);
    } else
      e[i] = s = Bt();
    return s;
  }
  /**
  * Core {@link #remove} method implementation shared in other functions.
  *
  * In contrast this method **does not** fire the {@link #event:change} event.
  *
  * @param subject The item to remove, its id or index in the collection.
  * @returns Returns an array with the removed item and its index.
  * @fires remove
  */
  _remove(e) {
    let i, s, r, o = !1;
    const a = this._idProperty;
    if (typeof e == "string" ? (s = e, r = this._itemMap.get(s), o = !r, r && (i = this._items.indexOf(r))) : typeof e == "number" ? (i = e, r = this._items[i], o = !r, r && (s = r[a])) : (r = e, s = r[a], i = this._items.indexOf(r), o = i == -1 || !this._itemMap.get(s)), o)
      throw new S("collection-remove-404", this);
    this._items.splice(i, 1), this._itemMap.delete(s);
    const l = this._bindToInternalToExternalMap.get(r);
    return this._bindToInternalToExternalMap.delete(r), this._bindToExternalToInternalMap.delete(l), this.fire("remove", r, i), [
      r,
      i
    ];
  }
  /**
  * Iterable interface.
  */
  [Symbol.iterator]() {
    return this._items[Symbol.iterator]();
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Xe(n) {
  const t = n.next();
  return t.done ? null : t.value;
}
class ze extends (/* @__PURE__ */ li(/* @__PURE__ */ je())) {
  // @if CK_DEBUG_FOCUSTRACKER // public _label?: string;
  constructor() {
    super();
    /**
    * List of registered DOM elements.
    *
    * @internal
    */
    f(this, "_elements", /* @__PURE__ */ new Set());
    /**
    * List of views with external focus trackers that contribute to the state of this focus tracker.
    *
    * @internal
    */
    f(this, "_externalViews", /* @__PURE__ */ new Set());
    /**
    * Asynchronous blur event timeout.
    */
    f(this, "_blurTimeout", null);
    this.set("isFocused", !1), this.set("focusedElement", null);
  }
  /**
  * List of registered DOM elements.
  *
  * **Note**: The list does do not include elements from {@link #externalViews}.
  */
  get elements() {
    return Array.from(this._elements.values());
  }
  /**
  * List of external focusable views that contribute to the state of this focus tracker. See {@link #add} to learn more.
  */
  get externalViews() {
    return Array.from(this._externalViews.values());
  }
  /**
  * Starts tracking a specified DOM element or a {@link module:ui/view~View} instance.
  *
  * * If a DOM element is passed, the focus tracker listens to the `focus` and `blur` events on this element.
  * Tracked elements are listed in {@link #elements}.
  * * If a {@link module:ui/view~View} instance is passed that has a `FocusTracker` instance ({@link ~ViewWithFocusTracker}),
  * the external focus tracker's state ({@link #isFocused}, {@link #focusedElement}) starts contributing to the current tracker instance.
  * This allows for increasing the "reach" of a focus tracker instance, by connecting two or more focus trackers together when DOM
  * elements they track are located in different subtrees in DOM. External focus trackers are listed in {@link #externalViews}.
  * * If a {@link module:ui/view~View} instance is passed that has no `FocusTracker` (**not** a {@link ~ViewWithFocusTracker}),
  * its {@link module:ui/view~View#element} is used to track focus like any other DOM element.
  */
  add(e) {
    if (hp(e))
      this._addElement(e);
    else if (dp(e))
      this._addView(e);
    else {
      if (!e.element)
        throw new S("focustracker-add-view-missing-element", {
          focusTracker: this,
          view: e
        });
      this._addElement(e.element);
    }
  }
  /**
  * Stops tracking focus in the specified DOM element or a {@link module:ui/view~View view instance}. See {@link #add} to learn more.
  */
  remove(e) {
    hp(e) ? this._removeElement(e) : dp(e) ? this._removeView(e) : this._removeElement(e.element);
  }
  /**
  * Adds a DOM element to the focus tracker and starts listening to the `focus` and `blur` events on it.
  */
  _addElement(e) {
    if (this._elements.has(e))
      throw new S("focustracker-add-element-already-exist", this);
    this.listenTo(e, "focus", () => {
      const i = this.externalViews.find((s) => a6(e, s));
      i ? this._focus(i.element) : this._focus(e);
    }, {
      useCapture: !0
    }), this.listenTo(e, "blur", () => {
      this._blur();
    }, {
      useCapture: !0
    }), this._elements.add(e);
  }
  /**
  * Removes a DOM element from the focus tracker.
  */
  _removeElement(e) {
    this._elements.has(e) && (this.stopListening(e), this._elements.delete(e)), e === this.focusedElement && this._blur();
  }
  /**
  * Adds an external {@link module:ui/view~View view instance} to this focus tracker and makes it contribute to this focus tracker's
  * state either by its `View#element` or by its `View#focusTracker` instance.
  */
  _addView(e) {
    e.element && this._addElement(e.element), this.listenTo(e.focusTracker, "change:focusedElement", () => {
      e.focusTracker.focusedElement ? e.element && this._focus(e.element) : this._blur();
    }), this._externalViews.add(e);
  }
  /**
  * Removes an external {@link module:ui/view~View view instance} from this focus tracker.
  */
  _removeView(e) {
    e.element && this._removeElement(e.element), this.stopListening(e.focusTracker), this._externalViews.delete(e);
  }
  /**
  * Destroys the focus tracker by:
  * - Disabling all event listeners attached to tracked elements or external views.
  * - Removing all tracked elements and views that were previously added.
  */
  destroy() {
    this.stopListening(), this._elements.clear(), this._externalViews.clear(), this.isFocused = !1, this.focusedElement = null;
  }
  /**
  * Stores currently focused element as {@link #focusedElement} and sets {@link #isFocused} `true`.
  */
  _focus(e) {
    this._clearBlurTimeout(), this.focusedElement = e, this.isFocused = !0;
  }
  /**
  * Clears currently {@link #focusedElement} and sets {@link #isFocused} `false`.
  *
  * This method uses `setTimeout()` to change order of `blur` and `focus` events calls, ensuring that moving focus between
  * two elements within a single focus tracker's scope, will not cause `[ blurA, focusB ]` sequence but just `[ focusB ]`.
  * The former would cause a momentary change of `#isFocused` to `false` which is not desired because any logic listening to
  * a focus tracker state would experience UI flashes and glitches as the user focus travels across the UI.
  */
  _blur() {
    this.elements.find((s) => s.contains(document.activeElement)) || this.externalViews.find((s) => s.focusTracker.isFocused && !s.focusTracker._blurTimeout) || (this._clearBlurTimeout(), this._blurTimeout = setTimeout(() => {
      this.focusedElement = null, this.isFocused = !1;
    }, 0));
  }
  /**
  * Clears the asynchronous blur event timeout on demand. See {@link #_blur} to learn more.
  */
  _clearBlurTimeout() {
    clearTimeout(this._blurTimeout), this._blurTimeout = null;
  }
}
function dp(n) {
  return "focusTracker" in n && n.focusTracker instanceof ze;
}
function hp(n) {
  return Hn(n);
}
function a6(n, t) {
  return fp(n, t) ? !0 : !!t.focusTracker.externalViews.find((e) => fp(n, e));
}
function fp(n, t) {
  return !!t.element && t.element.contains(document.activeElement) && n.contains(t.element);
}
class Ze {
  /**
  * Creates an instance of the keystroke handler.
  */
  constructor() {
    /**
    * Listener used to listen to events for easier keystroke handler destruction.
    */
    f(this, "_listener");
    this._listener = new (li())();
  }
  /**
  * Starts listening for `keydown` events from a given emitter.
  */
  listenTo(t) {
    this._listener.listenTo(t, "keydown", (e, i) => {
      this._listener.fire("_keydown:" + zs(i), i);
    });
  }
  /**
  * Registers a handler for the specified keystroke.
  *
  * @param keystroke Keystroke defined in a format accepted by
  * the {@link module:utils/keyboard~parseKeystroke} function.
  * @param callback A function called with the
  * {@link module:engine/view/observer/keyobserver~KeyEventData key event data} object and
  * a helper function to call both `preventDefault()` and `stopPropagation()` on the underlying event.
  * @param options Additional options.
  */
  set(t, e, i = {}) {
    const s = Uc(t), r = i.priority;
    this._listener.listenTo(this._listener, "_keydown:" + s, (o, a) => {
      i.filter && !i.filter(a) || (e(a, () => {
        a.preventDefault(), a.stopPropagation(), o.stop();
      }), o.return = !0);
    }, {
      priority: r
    });
  }
  /**
  * Triggers a keystroke handler for a specified key combination, if such a keystroke was {@link #set defined}.
  *
  * @param keyEvtData Key event data.
  * @returns Whether the keystroke was handled.
  */
  press(t) {
    return !!this._listener.fire("_keydown:" + zs(t), t);
  }
  /**
  * Stops listening to `keydown` events from the given emitter.
  */
  stopListening(t) {
    this._listener.stopListening(t);
  }
  /**
  * Destroys the keystroke handler.
  */
  destroy() {
    this.stopListening();
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function l6(n) {
  const t = /* @__PURE__ */ new Map();
  for (const e in n)
    t.set(e, n[e]);
  return t;
}
function Ji(n) {
  return Ai(n) ? new Map(n) : l6(n);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function mp(n, t, e) {
  const i = n.length, s = t.length;
  for (let r = i - 1; r >= e; r--)
    n[r + s] = n[r];
  for (let r = 0; r < s; r++)
    n[e + r] = t[r];
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function _f(n, t) {
  let e;
  function i(...s) {
    i.cancel(), e = setTimeout(() => n(...s), t);
  }
  return i.cancel = () => {
    clearTimeout(e);
  }, i;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function yf(n) {
  try {
    if (!n.startsWith("ey"))
      return null;
    const t = atob(n.replace(/-/g, "+").replace(/_/g, "/"));
    return JSON.parse(t);
  } catch {
    return null;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function c6() {
  const n = [];
  for (let t = 0; t < 256; t++) {
    let e = t;
    for (let i = 0; i < 8; i++)
      e & 1 ? e = 3988292384 ^ e >>> 1 : e = e >>> 1;
    n[t] = e;
  }
  return n;
}
function u6(n) {
  const t = Array.isArray(n) ? n : [
    n
  ], e = c6();
  let i = -1;
  const s = t.map((r) => Array.isArray(r) ? r.join("") : String(r)).join("");
  for (let r = 0; r < s.length; r++) {
    const o = s.charCodeAt(r);
    i = i >>> 8 ^ e[(i ^ o) & 255];
  }
  return i = (i ^ -1) >>> 0, i.toString(16).padStart(8, "0");
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function d6(n) {
  return !!n && n.length == 1 && /[\u0300-\u036f\u1ab0-\u1aff\u1dc0-\u1dff\u20d0-\u20ff\ufe20-\ufe2f]/.test(n);
}
function h6(n) {
  return !!n && n.length == 1 && /[\ud800-\udbff]/.test(n);
}
function f6(n) {
  return !!n && n.length == 1 && /[\udc00-\udfff]/.test(n);
}
function kf(n, t) {
  return h6(n.charAt(t - 1)) && f6(n.charAt(t));
}
function Cf(n, t) {
  return d6(n.charAt(t));
}
const m6 = /* @__PURE__ */ g6();
function w_(n, t) {
  const e = String(n).matchAll(m6);
  return Array.from(e).some((i) => i.index < t && t < i.index + i[0].length);
}
function g6() {
  const n = [
    // Emoji Tag Sequence (ETS)
    new RegExp("\\p{Emoji}[\\u{E0020}-\\u{E007E}]+\\u{E007F}", "u"),
    // Emoji Keycap Sequence
    new RegExp("\\p{Emoji}\\u{FE0F}?\\u{20E3}", "u"),
    // Emoji Presentation Sequence
    new RegExp("\\p{Emoji}\\u{FE0F}", "u"),
    // Single-Character Emoji / Emoji Modifier Sequence
    new RegExp("(?=\\p{General_Category=Other_Symbol})\\p{Emoji}\\p{Emoji_Modifier}*", "u")
  ], t = new RegExp("\\p{Regional_Indicator}{2}", "u").source, e = "(?:" + n.map((s) => s.source).join("|") + ")", i = `${t}|${e}(?:‍${e})*`;
  return new RegExp(i, "ug");
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const fo = /* @__PURE__ */ new WeakMap();
let gp = !1;
function xf({ view: n, element: t, text: e, isDirectHost: i = !0, keepOnFocus: s = !1 }) {
  const r = n.document;
  fo.has(r) || (fo.set(r, /* @__PURE__ */ new Map()), r.registerPostFixer((a) => Bu(fo.get(r), a)), r.on("change:isComposing", () => {
    n.change((a) => Bu(fo.get(r), a));
  }, {
    priority: "high"
  })), t.is("editableElement") && t.on("change:placeholder", (a, l, c) => o(c)), t.placeholder ? o(t.placeholder) : e && o(e), e && y6();
  function o(a) {
    const l = {
      text: a,
      isDirectHost: i,
      keepOnFocus: s,
      hostElement: i ? t : null
    };
    fo.get(r).set(t, l), n.change((c) => Bu([
      [
        t,
        l
      ]
    ], c));
  }
}
function p6(n, t) {
  return t.hasClass("ck-placeholder") ? !1 : (n.addClass("ck-placeholder", t), !0);
}
function b6(n, t) {
  return t.hasClass("ck-placeholder") ? (n.removeClass("ck-placeholder", t), !0) : !1;
}
function w6(n, t) {
  if (!n.isAttached() || v6(n))
    return !1;
  const e = n.document, s = e.selection.anchor;
  return e.isComposing && s && s.parent === n ? !1 : t || !e.isFocused ? !0 : !!s && s.parent !== n;
}
function v6(n) {
  for (const t of n.getChildren())
    if (!t.is("uiElement"))
      return !0;
  return !1;
}
function Bu(n, t) {
  const e = [];
  let i = !1;
  for (const [s, r] of n)
    r.isDirectHost && (e.push(s), pp(t, s, r) && (i = !0));
  for (const [s, r] of n) {
    if (r.isDirectHost)
      continue;
    const o = _6(s);
    o && (e.includes(o) || (r.hostElement = o, pp(t, s, r) && (i = !0)));
  }
  return i;
}
function pp(n, t, e) {
  const { text: i, isDirectHost: s, hostElement: r } = e;
  let o = !1;
  return r.getAttribute("data-placeholder") !== i && (n.setAttribute("data-placeholder", i, r), o = !0), (s || t.childCount == 1) && w6(r, e.keepOnFocus) ? p6(n, r) && (o = !0) : b6(n, r) && (o = !0), o;
}
function _6(n) {
  if (n.childCount) {
    const t = n.getChild(0);
    if (t.is("element") && !t.is("uiElement") && !t.is("attributeElement"))
      return t;
  }
  return null;
}
function y6() {
  gp || Ve("enableplaceholder-deprecated-text-option"), gp = !0;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
let Gs = class {
  /* istanbul ignore next -- @preserve */
  is() {
    throw new Error("is() method is abstract");
  }
}, Ks = class extends (/* @__PURE__ */ Je(Gs)) {
  /**
  * Creates a tree view node.
  *
  * @param document The document instance to which this node belongs.
  */
  constructor(e) {
    super();
    /**
    * The document instance to which this node belongs.
    */
    f(this, "document");
    /**
    * Parent element. Null by default. Set by {@link module:engine/view/element~Element#_insertChild}.
    */
    f(this, "parent");
    this.document = e, this.parent = null;
  }
  /**
  * Index of the node in the parent element or null if the node has no parent.
  *
  * Accessing this property throws an error if this node's parent element does not contain it.
  * This means that view tree got broken.
  */
  get index() {
    let e;
    if (!this.parent)
      return null;
    if ((e = this.parent.getChildIndex(this)) == -1)
      throw new S("view-node-not-found-in-parent", this);
    return e;
  }
  /**
  * Node's next sibling, or `null` if it is the last child.
  */
  get nextSibling() {
    const e = this.index;
    return e !== null && this.parent.getChild(e + 1) || null;
  }
  /**
  * Node's previous sibling, or `null` if it is the first child.
  */
  get previousSibling() {
    const e = this.index;
    return e !== null && this.parent.getChild(e - 1) || null;
  }
  /**
  * Top-most ancestor of the node. If the node has no parent it is the root itself.
  */
  get root() {
    let e = this;
    for (; e.parent; )
      e = e.parent;
    return e;
  }
  /**
  * Returns true if the node is in a tree rooted in the document (is a descendant of one of its roots).
  */
  isAttached() {
    return this.root.is("rootElement");
  }
  /**
  * Gets a path to the node. The path is an array containing indices of consecutive ancestors of this node,
  * beginning from {@link module:engine/view/node~Node#root root}, down to this node's index.
  *
  * ```ts
  * const abc = downcastWriter.createText( 'abc' );
  * const foo = downcastWriter.createText( 'foo' );
  * const h1 = downcastWriter.createElement( 'h1', null, downcastWriter.createText( 'header' ) );
  * const p = downcastWriter.createElement( 'p', null, [ abc, foo ] );
  * const div = downcastWriter.createElement( 'div', null, [ h1, p ] );
  * foo.getPath(); // Returns [ 1, 3 ]. `foo` is in `p` which is in `div`. `p` starts at offset 1, while `foo` at 3.
  * h1.getPath(); // Returns [ 0 ].
  * div.getPath(); // Returns [].
  * ```
  *
  * @returns The path.
  */
  getPath() {
    const e = [];
    let i = this;
    for (; i.parent; )
      e.unshift(i.index), i = i.parent;
    return e;
  }
  /**
  * Returns ancestors array of this node.
  *
  * @param options Options object.
  * @param options.includeSelf When set to `true` this node will be also included in parent's array.
  * @param options.parentFirst When set to `true`, array will be sorted from node's parent to root element,
  * otherwise root element will be the first item in the array.
  * @returns Array with ancestors.
  */
  getAncestors(e = {}) {
    const i = [];
    let s = e.includeSelf ? this : this.parent;
    for (; s; )
      i[e.parentFirst ? "push" : "unshift"](s), s = s.parent;
    return i;
  }
  /**
  * Returns a {@link module:engine/view/element~Element} or {@link module:engine/view/documentfragment~DocumentFragment}
  * which is a common ancestor of both nodes.
  *
  * @param node The second node.
  * @param options Options object.
  * @param options.includeSelf When set to `true` both nodes will be considered "ancestors" too.
  * Which means that if e.g. node A is inside B, then their common ancestor will be B.
  */
  getCommonAncestor(e, i = {}) {
    const s = this.getAncestors(i), r = e.getAncestors(i);
    let o = 0;
    for (; s[o] == r[o] && s[o]; )
      o++;
    return o === 0 ? null : s[o - 1];
  }
  /**
  * Returns whether this node is before given node. `false` is returned if nodes are in different trees (for example,
  * in different {@link module:engine/view/documentfragment~DocumentFragment}s).
  *
  * @param node Node to compare with.
  */
  isBefore(e) {
    if (this == e || this.root !== e.root)
      return !1;
    const i = this.getPath(), s = e.getPath(), r = Zt(i, s);
    switch (r) {
      case "prefix":
        return !0;
      case "extension":
        return !1;
      default:
        return i[r] < s[r];
    }
  }
  /**
  * Returns whether this node is after given node. `false` is returned if nodes are in different trees (for example,
  * in different {@link module:engine/view/documentfragment~DocumentFragment}s).
  *
  * @param node Node to compare with.
  */
  isAfter(e) {
    return this == e || this.root !== e.root ? !1 : !this.isBefore(e);
  }
  /**
  * Removes node from parent.
  *
  * @internal
  */
  _remove() {
    this.parent._removeChildren(this.index);
  }
  /**
  * @internal
  * @param type Type of the change.
  * @param node Changed node.
  * @param data Additional data.
  * @fires change
  */
  _fireChange(e, i, s) {
    this.fire(`change:${e}`, i, s), this.parent && this.parent._fireChange(e, i, s);
  }
  /**
  * Custom toJSON method to solve child-parent circular dependencies.
  *
  * @returns Clone of this object with the parent property removed.
  */
  toJSON() {
    const e = Mv(this);
    return delete e.parent, e;
  }
};
Ks.prototype.is = function(n) {
  return n === "node" || n === "view:node";
};
let qt = class rh extends Ks {
  /**
  * Creates a tree view text node.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createText
  * @internal
  * @param document The document instance to which this text node belongs.
  * @param data The text's data.
  */
  constructor(e, i) {
    super(e);
    /**
    * The text content.
    *
    * Setting the data fires the {@link module:engine/view/node~Node#event:change:text change event}.
    */
    f(this, "_textData");
    this._textData = i;
  }
  /**
  * The text content.
  */
  get data() {
    return this._textData;
  }
  /**
  * The `_data` property is controlled by a getter and a setter.
  *
  * The getter is required when using the addition assignment operator on protected property:
  *
  * ```ts
  * const foo = downcastWriter.createText( 'foo' );
  * const bar = downcastWriter.createText( 'bar' );
  *
  * foo._data += bar.data;   // executes: `foo._data = foo._data + bar.data`
  * console.log( foo.data ); // prints: 'foobar'
  * ```
  *
  * If the protected getter didn't exist, `foo._data` will return `undefined` and result of the merge will be invalid.
  *
  * The setter sets data and fires the {@link module:engine/view/node~Node#event:change:text change event}.
  *
  * @internal
  */
  get _data() {
    return this.data;
  }
  set _data(e) {
    this._fireChange("text", this), this._textData = e;
  }
  /**
  * Checks if this text node is similar to other text node.
  * Both nodes should have the same data to be considered as similar.
  *
  * @param otherNode Node to check if it is same as this node.
  */
  isSimilar(e) {
    return e instanceof rh ? this === e || this.data === e.data : !1;
  }
  /**
  * Clones this node.
  *
  * @internal
  * @returns Text node that is a clone of this node.
  */
  _clone() {
    return new rh(this.document, this.data);
  }
};
qt.prototype.is = function(n) {
  return n === "$text" || n === "view:$text" || // This are legacy values kept for backward compatibility.
  n === "text" || n === "view:text" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "node" || n === "view:node";
};
let Mn = class extends Gs {
  /**
  * Creates a text proxy.
  *
  * @internal
  * @param textNode Text node which part is represented by this text proxy.
  * @param offsetInText Offset in {@link module:engine/view/textproxy~TextProxy#textNode text node}
  * from which the text proxy starts.
  * @param length Text proxy length, that is how many text node's characters, starting from `offsetInText` it represents.
  * @constructor
  */
  constructor(e, i, s) {
    super();
    /**
    * Reference to the {@link module:engine/view/text~Text} element which TextProxy is a substring.
    */
    f(this, "textNode");
    /**
    * Text data represented by this text proxy.
    */
    f(this, "data");
    /**
    * Offset in the `textNode` where this `TextProxy` instance starts.
    */
    f(this, "offsetInText");
    if (this.textNode = e, i < 0 || i > e.data.length)
      throw new S("view-textproxy-wrong-offsetintext", this);
    if (s < 0 || i + s > e.data.length)
      throw new S("view-textproxy-wrong-length", this);
    this.data = e.data.substring(i, i + s), this.offsetInText = i;
  }
  /**
  * Offset size of this node.
  */
  get offsetSize() {
    return this.data.length;
  }
  /**
  * Flag indicating whether `TextProxy` instance covers only part of the original {@link module:engine/view/text~Text text node}
  * (`true`) or the whole text node (`false`).
  *
  * This is `false` when text proxy starts at the very beginning of {@link module:engine/view/textproxy~TextProxy#textNode textNode}
  * ({@link module:engine/view/textproxy~TextProxy#offsetInText offsetInText} equals `0`) and text proxy sizes is equal to
  * text node size.
  */
  get isPartial() {
    return this.data.length !== this.textNode.data.length;
  }
  /**
  * Parent of this text proxy, which is same as parent of text node represented by this text proxy.
  */
  get parent() {
    return this.textNode.parent;
  }
  /**
  * Root of this text proxy, which is same as root of text node represented by this text proxy.
  */
  get root() {
    return this.textNode.root;
  }
  /**
  * {@link module:engine/view/document~Document View document} that owns this text proxy, or `null` if the text proxy is inside
  * {@link module:engine/view/documentfragment~DocumentFragment document fragment}.
  */
  get document() {
    return this.textNode.document;
  }
  /**
  * Returns ancestors array of this text proxy.
  *
  * @param options Options object.
  * @param options.includeSelf When set to `true`, textNode will be also included in parent's array.
  * @param options.parentFirst When set to `true`, array will be sorted from text proxy parent to
  * root element, otherwise root element will be the first item in the array.
  * @returns Array with ancestors.
  */
  getAncestors(e = {}) {
    const i = [];
    let s = e.includeSelf ? this.textNode : this.parent;
    for (; s !== null; )
      i[e.parentFirst ? "push" : "unshift"](s), s = s.parent;
    return i;
  }
};
Mn.prototype.is = function(n) {
  return n === "$textProxy" || n === "view:$textProxy" || // This are legacy values kept for backward compatibility.
  n === "textProxy" || n === "view:textProxy";
};
class Jl {
  constructor() {
    /**
    * Map of consumable elements. If {@link module:engine/view/element~Element element} is used as a key,
    * {@link module:engine/conversion/viewconsumable~ViewElementConsumables ViewElementConsumables} instance is stored as value.
    * For {@link module:engine/view/text~Text text nodes} and
    * {@link module:engine/view/documentfragment~DocumentFragment document fragments} boolean value is stored as value.
    */
    f(this, "_consumables", /* @__PURE__ */ new Map());
  }
  /**
  * Adds view {@link module:engine/view/element~Element element}, {@link module:engine/view/text~Text text node} or
  * {@link module:engine/view/documentfragment~DocumentFragment document fragment} as ready to be consumed.
  *
  * ```ts
  * viewConsumable.add( p, { name: true } ); // Adds element's name to consume.
  * viewConsumable.add( p, { attributes: 'name' } ); // Adds element's attribute.
  * viewConsumable.add( p, { classes: 'foobar' } ); // Adds element's class.
  * viewConsumable.add( p, { styles: 'color' } ); // Adds element's style
  * viewConsumable.add( p, { attributes: 'name', styles: 'color' } ); // Adds attribute and style.
  * viewConsumable.add( p, { classes: [ 'baz', 'bar' ] } ); // Multiple consumables can be provided.
  * viewConsumable.add( textNode ); // Adds text node to consume.
  * viewConsumable.add( docFragment ); // Adds document fragment to consume.
  * ```
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `viewconsumable-invalid-attribute` when `class` or `style`
  * attribute is provided - it should be handled separately by providing actual style/class.
  *
  * ```ts
  * viewConsumable.add( p, { attributes: 'style' } ); // This call will throw an exception.
  * viewConsumable.add( p, { styles: 'color' } ); // This is properly handled style.
  * ```
  *
  * @param consumables Used only if first parameter is {@link module:engine/view/element~Element view element} instance.
  * @param consumables.name If set to true element's name will be included.
  * @param consumables.attributes Attribute name or array of attribute names.
  * @param consumables.classes Class name or array of class names.
  * @param consumables.styles Style name or array of style names.
  */
  add(t, e) {
    let i;
    if (t.is("$text") || t.is("documentFragment")) {
      this._consumables.set(t, !0);
      return;
    }
    this._consumables.has(t) ? i = this._consumables.get(t) : (i = new k6(t), this._consumables.set(t, i)), i.add(e ? Ro(e) : t._getConsumables());
  }
  /**
  * Tests if {@link module:engine/view/element~Element view element}, {@link module:engine/view/text~Text text node} or
  * {@link module:engine/view/documentfragment~DocumentFragment document fragment} can be consumed.
  * It returns `true` when all items included in method's call can be consumed. Returns `false` when
  * first already consumed item is found and `null` when first non-consumable item is found.
  *
  * ```ts
  * viewConsumable.test( p, { name: true } ); // Tests element's name.
  * viewConsumable.test( p, { attributes: 'name' } ); // Tests attribute.
  * viewConsumable.test( p, { classes: 'foobar' } ); // Tests class.
  * viewConsumable.test( p, { styles: 'color' } ); // Tests style.
  * viewConsumable.test( p, { attributes: 'name', styles: 'color' } ); // Tests attribute and style.
  * viewConsumable.test( p, { classes: [ 'baz', 'bar' ] } ); // Multiple consumables can be tested.
  * viewConsumable.test( textNode ); // Tests text node.
  * viewConsumable.test( docFragment ); // Tests document fragment.
  * ```
  *
  * Testing classes and styles as attribute will test if all added classes/styles can be consumed.
  *
  * ```ts
  * viewConsumable.test( p, { attributes: 'class' } ); // Tests if all added classes can be consumed.
  * viewConsumable.test( p, { attributes: 'style' } ); // Tests if all added styles can be consumed.
  * ```
  *
  * @param consumables Used only if first parameter is {@link module:engine/view/element~Element view element} instance.
  * @param consumables.name If set to true element's name will be included.
  * @param consumables.attributes Attribute name or array of attribute names.
  * @param consumables.classes Class name or array of class names.
  * @param consumables.styles Style name or array of style names.
  * @returns Returns `true` when all items included in method's call can be consumed. Returns `false`
  * when first already consumed item is found and `null` when first non-consumable item is found.
  */
  test(t, e) {
    const i = this._consumables.get(t);
    return i === void 0 ? null : t.is("$text") || t.is("documentFragment") ? i : i.test(Ro(e));
  }
  /**
  * Consumes {@link module:engine/view/element~Element view element}, {@link module:engine/view/text~Text text node} or
  * {@link module:engine/view/documentfragment~DocumentFragment document fragment}.
  * It returns `true` when all items included in method's call can be consumed, otherwise returns `false`.
  *
  * ```ts
  * viewConsumable.consume( p, { name: true } ); // Consumes element's name.
  * viewConsumable.consume( p, { attributes: 'name' } ); // Consumes element's attribute.
  * viewConsumable.consume( p, { classes: 'foobar' } ); // Consumes element's class.
  * viewConsumable.consume( p, { styles: 'color' } ); // Consumes element's style.
  * viewConsumable.consume( p, { attributes: 'name', styles: 'color' } ); // Consumes attribute and style.
  * viewConsumable.consume( p, { classes: [ 'baz', 'bar' ] } ); // Multiple consumables can be consumed.
  * viewConsumable.consume( textNode ); // Consumes text node.
  * viewConsumable.consume( docFragment ); // Consumes document fragment.
  * ```
  *
  * Consuming classes and styles as attribute will test if all added classes/styles can be consumed.
  *
  * ```ts
  * viewConsumable.consume( p, { attributes: 'class' } ); // Consume only if all added classes can be consumed.
  * viewConsumable.consume( p, { attributes: 'style' } ); // Consume only if all added styles can be consumed.
  * ```
  *
  * @param consumables Used only if first parameter is {@link module:engine/view/element~Element view element} instance.
  * @param consumables.name If set to true element's name will be included.
  * @param consumables.attributes Attribute name or array of attribute names.
  * @param consumables.classes Class name or array of class names.
  * @param consumables.styles Style name or array of style names.
  * @returns Returns `true` when all items included in method's call can be consumed,
  * otherwise returns `false`.
  */
  consume(t, e) {
    if (t.is("$text") || t.is("documentFragment"))
      return this.test(t, e) ? (this._consumables.set(t, !1), !0) : !1;
    const i = this._consumables.get(t);
    return i === void 0 ? !1 : i.consume(Ro(e));
  }
  /**
  * Reverts {@link module:engine/view/element~Element view element}, {@link module:engine/view/text~Text text node} or
  * {@link module:engine/view/documentfragment~DocumentFragment document fragment} so they can be consumed once again.
  * Method does not revert items that were never previously added for consumption, even if they are included in
  * method's call.
  *
  * ```ts
  * viewConsumable.revert( p, { name: true } ); // Reverts element's name.
  * viewConsumable.revert( p, { attributes: 'name' } ); // Reverts element's attribute.
  * viewConsumable.revert( p, { classes: 'foobar' } ); // Reverts element's class.
  * viewConsumable.revert( p, { styles: 'color' } ); // Reverts element's style.
  * viewConsumable.revert( p, { attributes: 'name', styles: 'color' } ); // Reverts attribute and style.
  * viewConsumable.revert( p, { classes: [ 'baz', 'bar' ] } ); // Multiple names can be reverted.
  * viewConsumable.revert( textNode ); // Reverts text node.
  * viewConsumable.revert( docFragment ); // Reverts document fragment.
  * ```
  *
  * Reverting classes and styles as attribute will revert all classes/styles that were previously added for
  * consumption.
  *
  * ```ts
  * viewConsumable.revert( p, { attributes: 'class' } ); // Reverts all classes added for consumption.
  * viewConsumable.revert( p, { attributes: 'style' } ); // Reverts all styles added for consumption.
  * ```
  *
  * @param consumables Used only if first parameter is {@link module:engine/view/element~Element view element} instance.
  * @param consumables.name If set to true element's name will be included.
  * @param consumables.attributes Attribute name or array of attribute names.
  * @param consumables.classes Class name or array of class names.
  * @param consumables.styles Style name or array of style names.
  */
  revert(t, e) {
    const i = this._consumables.get(t);
    i !== void 0 && (t.is("$text") || t.is("documentFragment") ? this._consumables.set(t, !0) : i.revert(Ro(e)));
  }
  /**
  * Creates {@link module:engine/conversion/viewconsumable~ViewConsumable ViewConsumable} instance from
  * {@link module:engine/view/node~Node node} or {@link module:engine/view/documentfragment~DocumentFragment document fragment}.
  * Instance will contain all elements, child nodes, attributes, styles and classes added for consumption.
  *
  * @param from View node or document fragment from which `ViewConsumable` will be created.
  * @param instance If provided, given `ViewConsumable` instance will be used
  * to add all consumables. It will be returned instead of a new instance.
  */
  static createFrom(t, e) {
    if (e || (e = new Jl()), t.is("$text"))
      e.add(t);
    else if (t.is("element") || t.is("documentFragment")) {
      e.add(t);
      for (const i of t.getChildren())
        Jl.createFrom(i, e);
    }
    return e;
  }
}
class k6 {
  /**
  * Creates ViewElementConsumables instance.
  *
  * @param from View element from which `ViewElementConsumables` is being created.
  */
  constructor(t) {
    f(this, "element");
    /**
    * Flag indicating if name of the element can be consumed.
    */
    f(this, "_canConsumeName", null);
    /**
    * A map of element's consumables.
    * * For plain attributes the value is a boolean indicating whether the attribute is available to consume.
    * * For token based attributes (like class list and style) the value is a map of tokens to booleans
    * indicating whether the token is available to consume on the given attribute.
    */
    f(this, "_attributes", /* @__PURE__ */ new Map());
    this.element = t;
  }
  /**
  * Adds consumable parts of the {@link module:engine/view/element~Element view element}.
  * Element's name itself can be marked to be consumed (when element's name is consumed its attributes, classes and
  * styles still could be consumed):
  *
  * ```ts
  * consumables.add( { name: true } );
  * ```
  *
  * Attributes classes and styles:
  *
  * ```ts
  * consumables.add( { attributes: [ [ 'title' ], [ 'class', 'foo' ], [ 'style', 'color'] ] } );
  * consumables.add( { attributes: [ [ 'title' ], [ 'name' ], [ 'class', 'foo' ], [ 'class', 'bar' ] ] } );
  * ```
  *
  * Note: This method accepts only {@link module:engine/view/element~NormalizedConsumables}.
  * You can use {@link module:engine/conversion/viewconsumable~normalizeConsumables} helper to convert from
  * {@link module:engine/conversion/viewconsumable~Consumables} to `NormalizedConsumables`.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `viewconsumable-invalid-attribute` when `class` or `style`
  * attribute is provided - it should be handled separately by providing `style` and `class` in consumables object.
  *
  * @param consumables Object describing which parts of the element can be consumed.
  */
  add(t) {
    t.name && (this._canConsumeName = !0);
    for (const [e, i] of t.attributes)
      if (i) {
        let s = this._attributes.get(e);
        (!s || typeof s == "boolean") && (s = /* @__PURE__ */ new Map(), this._attributes.set(e, s)), s.set(i, !0);
      } else {
        if (e == "style" || e == "class")
          throw new S("viewconsumable-invalid-attribute", this);
        this._attributes.set(e, !0);
      }
  }
  /**
  * Tests if parts of the {@link module:engine/view/element~Element view element} can be consumed.
  *
  * Element's name can be tested:
  *
  * ```ts
  * consumables.test( { name: true } );
  * ```
  *
  * Attributes classes and styles:
  *
  * ```ts
  * consumables.test( { attributes: [ [ 'title' ], [ 'class', 'foo' ], [ 'style', 'color' ] ] } );
  * consumables.test( { attributes: [ [ 'title' ], [ 'name' ], [ 'class', 'foo' ], [ 'class', 'bar' ] ] } );
  * ```
  *
  * @param consumables Object describing which parts of the element should be tested.
  * @returns `true` when all tested items can be consumed, `null` when even one of the items
  * was never marked for consumption and `false` when even one of the items was already consumed.
  */
  test(t) {
    if (t.name && !this._canConsumeName)
      return this._canConsumeName;
    for (const [e, i] of t.attributes) {
      const s = this._attributes.get(e);
      if (s === void 0)
        return null;
      if (s === !1)
        return !1;
      if (s !== !0) {
        if (i) {
          const r = s.get(i);
          if (r === void 0)
            return null;
          if (!r)
            return !1;
        } else
          for (const r of s.values())
            if (!r)
              return !1;
      }
    }
    return !0;
  }
  /**
  * Tests if parts of the {@link module:engine/view/element~Element view element} can be consumed and consumes them if available.
  * It returns `true` when all items included in method's call can be consumed, otherwise returns `false`.
  *
  * Element's name can be consumed:
  *
  * ```ts
  * consumables.consume( { name: true } );
  * ```
  *
  * Attributes classes and styles:
  *
  * ```ts
  * consumables.consume( { attributes: [ [ 'title' ], [ 'class', 'foo' ], [ 'style', 'color' ] ] } );
  * consumables.consume( { attributes: [ [ 'title' ], [ 'name' ], [ 'class', 'foo' ], [ 'class', 'bar' ] ] } );
  * ```
  *
  * @param consumables Object describing which parts of the element should be consumed.
  * @returns `true` when all tested items can be consumed and `false` when even one of the items could not be consumed.
  */
  consume(t) {
    if (!this.test(t))
      return !1;
    t.name && (this._canConsumeName = !1);
    for (const [e, i] of t.attributes) {
      const s = this._attributes.get(e);
      if (typeof s == "boolean")
        for (const [r] of this.element._getConsumables(e, i).attributes)
          this._attributes.set(r, !1);
      else if (i)
        for (const [, r] of this.element._getConsumables(e, i).attributes)
          s.set(r, !1);
      else
        for (const r of s.keys())
          s.set(r, !1);
    }
    return !0;
  }
  /**
  * Revert already consumed parts of {@link module:engine/view/element~Element view Element}, so they can be consumed once again.
  * Element's name can be reverted:
  *
  * ```ts
  * consumables.revert( { name: true } );
  * ```
  *
  * Attributes classes and styles:
  *
  * ```ts
  * consumables.revert( { attributes: [ [ 'title' ], [ 'class', 'foo' ], [ 'style', 'color' ] ] } );
  * consumables.revert( { attributes: [ [ 'title' ], [ 'name' ], [ 'class', 'foo' ], [ 'class', 'bar' ] ] } );
  * ```
  *
  * @param consumables Object describing which parts of the element should be reverted.
  */
  revert(t) {
    t.name && (this._canConsumeName = !0);
    for (const [e, i] of t.attributes) {
      const s = this._attributes.get(e);
      if (s === !1) {
        this._attributes.set(e, !0);
        continue;
      }
      if (!(s === void 0 || s === !0))
        if (i)
          s.get(i) === !1 && s.set(i, !0);
        else
          for (const r of s.keys())
            s.set(r, !0);
    }
  }
}
function Ro(n) {
  const t = [];
  return "attributes" in n && n.attributes && Mu(t, n.attributes), "classes" in n && n.classes && Mu(t, n.classes, "class"), "styles" in n && n.styles && Mu(t, n.styles, "style"), {
    name: n.name || !1,
    attributes: t
  };
}
function Mu(n, t, e) {
  if (typeof t == "string") {
    n.push(e ? [
      e,
      t
    ] : [
      t
    ]);
    return;
  }
  for (const i of t)
    Array.isArray(i) ? n.push(i) : n.push(e ? [
      e,
      i
    ] : [
      i
    ]);
}
class oi {
  /**
  * Creates new instance of Matcher.
  *
  * @param pattern Match patterns. See {@link module:engine/view/matcher~Matcher#add add method} for more information.
  */
  constructor(...t) {
    f(this, "_patterns", []);
    this.add(...t);
  }
  /**
  * Adds pattern or patterns to matcher instance.
  *
  * ```ts
  * // String.
  * matcher.add( 'div' );
  *
  * // Regular expression.
  * matcher.add( /^\w/ );
  *
  * // Single class.
  * matcher.add( {
  * 	classes: 'foobar'
  * } );
  * ```
  *
  * See {@link module:engine/view/matcher~MatcherPattern} for more examples.
  *
  * Multiple patterns can be added in one call:
  *
  * ```ts
  * matcher.add( 'div', { classes: 'foobar' } );
  * ```
  *
  * @param pattern Object describing pattern details. If string or regular expression
  * is provided it will be used to match element's name. Pattern can be also provided in a form
  * of a function - then this function will be called with each {@link module:engine/view/element~Element element} as a parameter.
  * Function's return value will be stored under `match` key of the object returned from
  * {@link module:engine/view/matcher~Matcher#match match} or {@link module:engine/view/matcher~Matcher#matchAll matchAll} methods.
  */
  add(...t) {
    for (let e of t)
      (typeof e == "string" || e instanceof RegExp) && (e = {
        name: e
      }), this._patterns.push(e);
  }
  /**
  * Matches elements for currently stored patterns. Returns match information about first found
  * {@link module:engine/view/element~Element element}, otherwise returns `null`.
  *
  * Example of returned object:
  *
  * ```ts
  * {
  * 	element: <instance of found element>,
  * 	pattern: <pattern used to match found element>,
  * 	match: {
  * 		name: true,
  * 		attributes: [
  * 			[ 'title' ],
  * 			[ 'href' ],
  * 			[ 'class', 'foo' ],
  * 			[ 'style', 'color' ],
  * 			[ 'style', 'position' ]
  * 		]
  * 	}
  * }
  * ```
  *
  * You could use the `match` field from the above returned object as an input for the
  * {@link module:engine/conversion/viewconsumable~ViewConsumable#test `ViewConsumable#test()`} and
  * {@link module:engine/conversion/viewconsumable~ViewConsumable#consume `ViewConsumable#consume()`} methods.
  *
  * @see module:engine/view/matcher~Matcher#add
  * @see module:engine/view/matcher~Matcher#matchAll
  * @param element View element to match against stored patterns.
  * @returns The match information about found element or `null`.
  */
  match(...t) {
    for (const e of t)
      for (const i of this._patterns) {
        const s = this._isElementMatching(e, i);
        if (s)
          return {
            element: e,
            pattern: i,
            match: s
          };
      }
    return null;
  }
  /**
  * Matches elements for currently stored patterns. Returns array of match information with all found
  * {@link module:engine/view/element~Element elements}. If no element is found - returns `null`.
  *
  * @see module:engine/view/matcher~Matcher#add
  * @see module:engine/view/matcher~Matcher#match
  * @param element View element to match against stored patterns.
  * @returns Array with match information about found elements or `null`. For more information
  * see {@link module:engine/view/matcher~Matcher#match match method} description.
  */
  matchAll(...t) {
    const e = [];
    for (const i of t)
      for (const s of this._patterns) {
        const r = this._isElementMatching(i, s);
        r && e.push({
          element: i,
          pattern: s,
          match: r
        });
      }
    return e.length > 0 ? e : null;
  }
  /**
  * Returns the name of the element to match if there is exactly one pattern added to the matcher instance
  * and it matches element name defined by `string` (not `RegExp`). Otherwise, returns `null`.
  *
  * @returns Element name trying to match.
  */
  getElementName() {
    if (this._patterns.length !== 1)
      return null;
    const t = this._patterns[0], e = t.name;
    return typeof t != "function" && e && !(e instanceof RegExp) ? e : null;
  }
  /**
  * Returns match information if {@link module:engine/view/element~Element element} is matching provided pattern.
  * If element cannot be matched to provided pattern - returns `null`.
  *
  * @returns Returns object with match information or null if element is not matching.
  */
  _isElementMatching(t, e) {
    if (typeof e == "function") {
      const r = e(t);
      return !r || typeof r != "object" ? r : Ro(r);
    }
    const i = {};
    if (e.name && (i.name = C6(e.name, t.name), !i.name))
      return null;
    const s = [];
    return e.attributes && !x6(e.attributes, t, s) || e.classes && !T6(e.classes, t, s) || e.styles && !E6(e.styles, t, s) ? null : (s.length && (i.attributes = s), i);
  }
}
function Xl(n, t) {
  return n === !0 || n === t || n instanceof RegExp && !!String(t).match(n);
}
function C6(n, t) {
  return n instanceof RegExp ? !!t.match(n) : n === t;
}
function Tf(n, t) {
  if (Array.isArray(n))
    return n.map((i) => typeof i != "object" || i instanceof RegExp ? t ? [
      t,
      i,
      !0
    ] : [
      i,
      !0
    ] : ((i.key === void 0 || i.value === void 0) && Ve("matcher-pattern-missing-key-or-value", i), t ? [
      t,
      i.key,
      i.value
    ] : [
      i.key,
      i.value
    ]));
  if (typeof n != "object" || n instanceof RegExp)
    return [
      t ? [
        t,
        n,
        !0
      ] : [
        n,
        !0
      ]
    ];
  const e = [];
  for (const i in n)
    Object.prototype.hasOwnProperty.call(n, i) && e.push(t ? [
      t,
      i,
      n[i]
    ] : [
      i,
      n[i]
    ]);
  return e;
}
function x6(n, t, e) {
  let i;
  return typeof n == "object" && !(n instanceof RegExp) && !Array.isArray(n) ? (n.style !== void 0 && Ve("matcher-pattern-deprecated-attributes-style-key", n), n.class !== void 0 && Ve("matcher-pattern-deprecated-attributes-class-key", n)) : i = [
    "class",
    "style"
  ], t._collectAttributesMatch(Tf(n), e, i);
}
function T6(n, t, e) {
  return t._collectAttributesMatch(Tf(n, "class"), e);
}
function E6(n, t, e) {
  return t._collectAttributesMatch(Tf(n, "style"), e);
}
class Br {
  /**
  * Creates Styles instance.
  */
  constructor(t) {
    /**
    * Keeps an internal representation of styles map. Normalized styles are kept as object tree to allow unified modification and
    * value access model using lodash's get, set, unset, etc methods.
    *
    * When no style processor rules are defined it acts as simple key-value storage.
    */
    f(this, "_styles");
    /**
    * Cached list of style names for faster access.
    */
    f(this, "_cachedStyleNames", null);
    /**
    * Cached list of expanded style names for faster access.
    */
    f(this, "_cachedExpandedStyleNames", null);
    /**
    * An instance of the {@link module:engine/view/stylesmap~StylesProcessor}.
    */
    f(this, "_styleProcessor");
    this._styles = {}, this._styleProcessor = t;
  }
  /**
  * Returns true if style map has no styles set.
  */
  get isEmpty() {
    return !Object.entries(this._styles).length;
  }
  /**
  * Number of styles defined.
  */
  get size() {
    return this.isEmpty ? 0 : this.getStyleNames().length;
  }
  /**
  * Set styles map to a new value.
  *
  * ```ts
  * styles.setTo( 'border:1px solid blue;margin-top:1px;' );
  * ```
  */
  setTo(t) {
    this.clear();
    const e = A6(t);
    for (const [i, s] of e)
      this._styleProcessor.toNormalizedForm(i, s, this._styles);
    return this;
  }
  /**
  * Checks if a given style is set.
  *
  * ```ts
  * styles.setTo( 'margin-left:1px;' );
  *
  * styles.has( 'margin-left' );    // -> true
  * styles.has( 'padding' );        // -> false
  * ```
  *
  * **Note**: This check supports normalized style names.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * styles.setTo( 'margin:2px;' );
  *
  * styles.has( 'margin' );         // -> true
  * styles.has( 'margin-top' );     // -> true
  * styles.has( 'margin-left' );    // -> true
  *
  * styles.remove( 'margin-top' );
  *
  * styles.has( 'margin' );         // -> false
  * styles.has( 'margin-top' );     // -> false
  * styles.has( 'margin-left' );    // -> true
  * ```
  *
  * @param name Style name.
  */
  has(t) {
    if (this.isEmpty)
      return !1;
    const i = this._styleProcessor.getReducedForm(t, this._styles).find(([s]) => s === t);
    return Array.isArray(i);
  }
  set(t, e) {
    if (this._cachedStyleNames = null, this._cachedExpandedStyleNames = null, nt(t))
      for (const [i, s] of Object.entries(t))
        this._styleProcessor.toNormalizedForm(i, s, this._styles);
    else
      this._styleProcessor.toNormalizedForm(t, e, this._styles);
  }
  /**
  * Removes given style.
  *
  * ```ts
  * styles.setTo( 'background:#f00;margin-right:2px;' );
  *
  * styles.remove( 'background' );
  *
  * styles.toString();   // -> 'margin-right:2px;'
  * ```
  *
  * ***Note**:* This method uses {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules
  * enabled style processor rules} to normalize passed values.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * styles.setTo( 'margin:1px' );
  *
  * styles.remove( 'margin-top' );
  * styles.remove( 'margin-right' );
  *
  * styles.toString(); // -> 'margin-bottom:1px;margin-left:1px;'
  * ```
  *
  * @param names Style name or an array of names.
  */
  remove(t) {
    for (const e of Ue(t)) {
      this._cachedStyleNames = null, this._cachedExpandedStyleNames = null;
      const i = oh(e);
      s4(this._styles, i), delete this._styles[e], this._cleanEmptyObjectsOnPath(i);
    }
  }
  /**
  * Returns a normalized style object or a single value.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * const styles = new Styles();
  * styles.setTo( 'margin:1px 2px 3em;' );
  *
  * styles.getNormalized( 'margin' );
  * // will log:
  * // {
  * //     top: '1px',
  * //     right: '2px',
  * //     bottom: '3em',
  * //     left: '2px'     // normalized value from margin shorthand
  * // }
  *
  * styles.getNormalized( 'margin-left' ); // -> '2px'
  * ```
  *
  * **Note**: This method will only return normalized styles if a style processor was defined.
  *
  * @param name Style name.
  */
  getNormalized(t) {
    return this._styleProcessor.getNormalized(t, this._styles);
  }
  /**
  * Returns a normalized style string. Styles are sorted by name.
  *
  * ```ts
  * styles.set( 'margin' , '1px' );
  * styles.set( 'background', '#f00' );
  *
  * styles.toString(); // -> 'background:#f00;margin:1px;'
  * ```
  *
  * **Note**: This method supports normalized styles if defined.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * styles.set( 'margin' , '1px' );
  * styles.set( 'background', '#f00' );
  * styles.remove( 'margin-top' );
  * styles.remove( 'margin-right' );
  *
  * styles.toString(); // -> 'background:#f00;margin-bottom:1px;margin-left:1px;'
  * ```
  */
  toString() {
    return this.isEmpty ? "" : this.getStylesEntries().map((t) => t.join(":")).sort().join(";") + ";";
  }
  /**
  * Returns property as a value string or undefined if property is not set.
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * const styles = new Styles();
  * styles.setTo( 'margin:1px;' );
  * styles.set( 'margin-bottom', '3em' );
  *
  * styles.getAsString( 'margin' ); // -> 'margin: 1px 1px 3em;'
  * ```
  *
  * Note, however, that all sub-values must be set for the longhand property name to return a value:
  *
  * ```ts
  * const styles = new Styles();
  * styles.setTo( 'margin:1px;' );
  * styles.remove( 'margin-bottom' );
  *
  * styles.getAsString( 'margin' ); // -> undefined
  * ```
  *
  * In the above scenario, it is not possible to return a `margin` value, so `undefined` is returned.
  * Instead, you should use:
  *
  * ```ts
  * const styles = new Styles();
  * styles.setTo( 'margin:1px;' );
  * styles.remove( 'margin-bottom' );
  *
  * for ( const styleName of styles.getStyleNames() ) {
  * 	console.log( styleName, styles.getAsString( styleName ) );
  * }
  * // 'margin-top', '1px'
  * // 'margin-right', '1px'
  * // 'margin-left', '1px'
  * ```
  *
  * In general, it is recommend to iterate over style names like in the example above. This way, you will always get all
  * the currently set style values. So, if all the 4 margin values would be set
  * the for-of loop above would yield only `'margin'`, `'1px'`:
  *
  * ```ts
  * const styles = new Styles();
  * styles.setTo( 'margin:1px;' );
  *
  * for ( const styleName of styles.getStyleNames() ) {
  * 	console.log( styleName, styles.getAsString( styleName ) );
  * }
  * // 'margin', '1px'
  * ```
  *
  * **Note**: To get a normalized version of a longhand property use the {@link #getNormalized `#getNormalized()`} method.
  */
  getAsString(t) {
    if (this.isEmpty)
      return;
    if (this._styles[t] && !nt(this._styles[t]))
      return this._styles[t];
    const i = this._styleProcessor.getReducedForm(t, this._styles).find(([s]) => s === t);
    if (Array.isArray(i))
      return i[1];
  }
  /**
  * Returns all style properties names as they would appear when using {@link #toString `#toString()`}.
  *
  * When `expand` is set to true and there's a shorthand style property set, it will also return all equivalent styles:
  *
  * ```ts
  * stylesMap.setTo( 'margin: 1em' )
  * ```
  *
  * will be expanded to:
  *
  * ```ts
  * [ 'margin', 'margin-top', 'margin-right', 'margin-bottom', 'margin-left' ]
  * ```
  *
  * @param expand Expand shorthand style properties and all return equivalent style representations.
  */
  getStyleNames(t = !1) {
    return this.isEmpty ? [] : t ? (this._cachedExpandedStyleNames = this._cachedExpandedStyleNames || this._styleProcessor.getStyleNames(this._styles), this._cachedExpandedStyleNames) : (this._cachedStyleNames = this._cachedStyleNames || this.getStylesEntries().map(([e]) => e), this._cachedStyleNames);
  }
  /**
  * Alias for {@link #getStyleNames}.
  */
  keys() {
    return this.getStyleNames();
  }
  /**
  * Removes all styles.
  */
  clear() {
    this._styles = {}, this._cachedStyleNames = null, this._cachedExpandedStyleNames = null;
  }
  /**
  * Returns `true` if both attributes have the same styles.
  */
  isSimilar(t) {
    if (this.size !== t.size)
      return !1;
    for (const e of this.getStyleNames())
      if (!t.has(e) || t.getAsString(e) !== this.getAsString(e))
        return !1;
    return !0;
  }
  /**
  * Returns normalized styles entries for further processing.
  */
  getStylesEntries() {
    const t = [], e = Object.keys(this._styles);
    for (const i of e)
      t.push(...this._styleProcessor.getReducedForm(i, this._styles));
    return t;
  }
  /**
  * Clones the attribute value.
  *
  * @internal
  */
  _clone() {
    const t = new this.constructor(this._styleProcessor);
    return t.set(this.getNormalized()), t;
  }
  /**
  * Used by the {@link module:engine/view/matcher~Matcher Matcher} to collect matching styles.
  *
  * @internal
  * @param tokenPattern The matched style name pattern.
  * @param valuePattern The matched style value pattern.
  * @returns An array of matching tokens (style names).
  */
  _getTokensMatch(t, e) {
    const i = [];
    for (const s of this.getStyleNames(!0))
      if (Xl(t, s)) {
        if (e === !0) {
          i.push(s);
          continue;
        }
        const r = this.getAsString(s);
        Xl(e, r) && i.push(s);
      }
    return i.length ? i : void 0;
  }
  /**
  * Returns a list of consumables for the attribute. This includes related styles.
  *
  * Could be filtered by the given style name.
  *
  * @internal
  */
  _getConsumables(t) {
    const e = [];
    if (t) {
      e.push(t);
      for (const i of this._styleProcessor.getRelatedStyles(t))
        e.push(i);
    } else
      for (const i of this.getStyleNames()) {
        for (const s of this._styleProcessor.getRelatedStyles(i))
          e.push(s);
        e.push(i);
      }
    return e;
  }
  /**
  * Used by {@link module:engine/view/element~Element#_canMergeAttributesFrom} to verify if the given attribute can be merged without
  * conflicts into the attribute.
  *
  * This method is indirectly used by the {@link module:engine/view/downcastwriter~DowncastWriter} while down-casting
  * an {@link module:engine/view/attributeelement~AttributeElement} to merge it with other AttributeElement.
  *
  * @internal
  */
  _canMergeFrom(t) {
    for (const e of t.getStyleNames())
      if (this.has(e) && this.getAsString(e) !== t.getAsString(e))
        return !1;
    return !0;
  }
  /**
  * Used by {@link module:engine/view/element~Element#_mergeAttributesFrom} to merge a given attribute into the attribute.
  *
  * This method is indirectly used by the {@link module:engine/view/downcastwriter~DowncastWriter} while down-casting
  * an {@link module:engine/view/attributeelement~AttributeElement} to merge it with other AttributeElement.
  *
  * @internal
  */
  _mergeFrom(t) {
    for (const e of t.getStyleNames())
      this.has(e) || this.set(e, t.getAsString(e));
  }
  /**
  * Used by {@link module:engine/view/element~Element#_canSubtractAttributesOf} to verify if the given attribute can be fully
  * subtracted from the attribute.
  *
  * This method is indirectly used by the {@link module:engine/view/downcastwriter~DowncastWriter} while down-casting
  * an {@link module:engine/view/attributeelement~AttributeElement} to unwrap the AttributeElement.
  *
  * @internal
  */
  _isMatching(t) {
    for (const e of t.getStyleNames())
      if (!this.has(e) || this.getAsString(e) !== t.getAsString(e))
        return !1;
    return !0;
  }
  /**
  * Removes empty objects upon removing an entry from internal object.
  */
  _cleanEmptyObjectsOnPath(t) {
    const e = t.split(".");
    if (!(e.length > 1))
      return;
    const s = e.splice(0, e.length - 1).join("."), r = Fr(this._styles, s);
    if (!r)
      return;
    !Object.keys(r).length && this.remove(s);
  }
}
class S6 {
  /**
  * Creates StylesProcessor instance.
  *
  * @internal
  */
  constructor() {
    f(this, "_normalizers");
    f(this, "_extractors");
    f(this, "_reducers");
    f(this, "_consumables");
    this._normalizers = /* @__PURE__ */ new Map(), this._extractors = /* @__PURE__ */ new Map(), this._reducers = /* @__PURE__ */ new Map(), this._consumables = /* @__PURE__ */ new Map();
  }
  /**
  * Parse style string value to a normalized object and appends it to styles object.
  *
  * ```ts
  * const styles = {};
  *
  * stylesProcessor.toNormalizedForm( 'margin', '1px', styles );
  *
  * // styles will consist: { margin: { top: '1px', right: '1px', bottom: '1px', left: '1px; } }
  * ```
  *
  * **Note**: To define normalizer callbacks use {@link #setNormalizer}.
  *
  * @param name Name of style property.
  * @param propertyValue Value of style property.
  * @param styles Object holding normalized styles.
  */
  toNormalizedForm(t, e, i) {
    if (nt(e)) {
      Nu(i, oh(t), e);
      return;
    }
    if (this._normalizers.has(t)) {
      const s = this._normalizers.get(t), { path: r, value: o } = s(e);
      Nu(i, r, o);
    } else
      Nu(i, t, e);
  }
  /**
  * Returns a normalized version of a style property.
  *
  * ```ts
  * const styles = {
  * 	margin: { top: '1px', right: '1px', bottom: '1px', left: '1px; },
  * 	background: { color: '#f00' }
  * };
  *
  * stylesProcessor.getNormalized( 'background' );
  * // will return: { color: '#f00' }
  *
  * stylesProcessor.getNormalized( 'margin-top' );
  * // will return: '1px'
  * ```
  *
  * **Note**: In some cases extracting single value requires defining an extractor callback {@link #setExtractor}.
  *
  * @param name Name of style property.
  * @param styles Object holding normalized styles.
  */
  getNormalized(t, e) {
    if (!t)
      return bf({}, e);
    if (e[t] !== void 0)
      return e[t];
    if (this._extractors.has(t)) {
      const i = this._extractors.get(t);
      if (typeof i == "string")
        return Fr(e, i);
      const s = i(t, e);
      if (s)
        return s;
    }
    return Fr(e, oh(t));
  }
  /**
  * Returns a reduced form of style property form normalized object.
  *
  * For default margin reducer, the below code:
  *
  * ```ts
  * stylesProcessor.getReducedForm( 'margin', {
  * 	margin: { top: '1px', right: '1px', bottom: '2px', left: '1px; }
  * } );
  * ```
  *
  * will return:
  *
  * ```ts
  * [
  * 	[ 'margin', '1px 1px 2px' ]
  * ]
  * ```
  *
  * because it might be represented as a shorthand 'margin' value. However if one of margin long hand values is missing it should return:
  *
  * ```ts
  * [
  * 	[ 'margin-top', '1px' ],
  * 	[ 'margin-right', '1px' ],
  * 	[ 'margin-bottom', '2px' ]
  * 	// the 'left' value is missing - cannot use 'margin' shorthand.
  * ]
  * ```
  *
  * **Note**: To define reducer callbacks use {@link #setReducer}.
  *
  * @param name Name of style property.
  */
  getReducedForm(t, e) {
    const i = this.getNormalized(t, e);
    return i === void 0 ? [] : this._reducers.has(t) ? this._reducers.get(t)(i) : [
      [
        t,
        i
      ]
    ];
  }
  /**
  * Return all style properties. Also expand shorthand properties (e.g. `margin`, `background`) if respective extractor is available.
  *
  * @param styles Object holding normalized styles.
  */
  getStyleNames(t) {
    const e = /* @__PURE__ */ new Set();
    for (const i of this._consumables.keys()) {
      const s = this.getNormalized(i, t);
      s && (typeof s != "object" || Object.keys(s).length) && e.add(i);
    }
    for (const i of Object.keys(t))
      e.add(i);
    return Array.from(e);
  }
  /**
  * Returns related style names.
  *
  * ```ts
  * stylesProcessor.getRelatedStyles( 'margin' );
  * // will return: [ 'margin-top', 'margin-right', 'margin-bottom', 'margin-left' ];
  *
  * stylesProcessor.getRelatedStyles( 'margin-top' );
  * // will return: [ 'margin' ];
  * ```
  *
  * **Note**: To define new style relations load an existing style processor or use
  * {@link module:engine/view/stylesmap~StylesProcessor#setStyleRelation `StylesProcessor.setStyleRelation()`}.
  */
  getRelatedStyles(t) {
    return this._consumables.get(t) || [];
  }
  /**
  * Adds a normalizer method for a style property.
  *
  * A normalizer returns describing how the value should be normalized.
  *
  * For instance 'margin' style is a shorthand for four margin values:
  *
  * - 'margin-top'
  * - 'margin-right'
  * - 'margin-bottom'
  * - 'margin-left'
  *
  * and can be written in various ways if some values are equal to others. For instance `'margin: 1px 2em;'` is a shorthand for
  * `'margin-top: 1px;margin-right: 2em;margin-bottom: 1px;margin-left: 2em'`.
  *
  * A normalizer should parse various margin notations as a single object:
  *
  * ```ts
  * const styles = {
  * 	margin: {
  * 		top: '1px',
  * 		right: '2em',
  * 		bottom: '1px',
  * 		left: '2em'
  * 	}
  * };
  * ```
  *
  * Thus a normalizer for 'margin' style should return an object defining style path and value to store:
  *
  * ```ts
  * const returnValue = {
  * 	path: 'margin',
  * 	value: {
  * 		top: '1px',
  * 		right: '2em',
  * 		bottom: '1px',
  * 		left: '2em'
  * 	}
  * };
  * ```
  *
  * Additionally to fully support all margin notations there should be also defined 4 normalizers for longhand margin notations. Below
  * is an example for 'margin-top' style property normalizer:
  *
  * ```ts
  * stylesProcessor.setNormalizer( 'margin-top', valueString => {
  * 	return {
  * 		path: 'margin.top',
  * 		value: valueString
  * 	}
  * } );
  * ```
  */
  setNormalizer(t, e) {
    this._normalizers.set(t, e);
  }
  /**
  * Adds a extractor callback for a style property.
  *
  * Most normalized style values are stored as one level objects. It is assumed that `'margin-top'` style will be stored as:
  *
  * ```ts
  * const styles = {
  * 	margin: {
  * 		top: 'value'
  * 	}
  * }
  * ```
  *
  * However, some styles can have conflicting notations and thus it might be harder to extract a style value from shorthand. For instance
  * the 'border-top-style' can be defined using `'border-top:solid'`, `'border-style:solid none none none'` or by `'border:solid'`
  * shorthands. The default border styles processors stores styles as:
  *
  * ```ts
  * const styles = {
  * 	border: {
  * 		style: {
  * 			top: 'solid'
  * 		}
  * 	}
  * }
  * ```
  *
  * as it is better to modify border style independently from other values. On the other part the output of the border might be
  * desired as `border-top`, `border-left`, etc notation.
  *
  * In the above example an extractor should return a side border value that combines style, color and width:
  *
  * ```ts
  * styleProcessor.setExtractor( 'border-top', styles => {
  * 	return {
  * 		color: styles.border.color.top,
  * 		style: styles.border.style.top,
  * 		width: styles.border.width.top
  * 	}
  * } );
  * ```
  *
  * @param callbackOrPath Callback that return a requested value or path string for single values.
  */
  setExtractor(t, e) {
    this._extractors.set(t, e);
  }
  /**
  * Adds a reducer callback for a style property.
  *
  * Reducer returns a minimal notation for given style name. For longhand properties it is not required to write a reducer as
  * by default the direct value from style path is taken.
  *
  * For shorthand styles a reducer should return minimal style notation either by returning single name-value tuple or multiple tuples
  * if a shorthand cannot be used. For instance for a margin shorthand a reducer might return:
  *
  * ```ts
  * const marginShortHandTuple = [
  * 	[ 'margin', '1px 1px 2px' ]
  * ];
  * ```
  *
  * or a longhand tuples for defined values:
  *
  * ```ts
  * // Considering margin.bottom and margin.left are undefined.
  * const marginLonghandsTuples = [
  * 	[ 'margin-top', '1px' ],
  * 	[ 'margin-right', '1px' ]
  * ];
  * ```
  *
  * A reducer obtains a normalized style value:
  *
  * ```ts
  * // Simplified reducer that always outputs 4 values which are always present:
  * stylesProcessor.setReducer( 'margin', margin => {
  * 	return [
  * 		[ 'margin', `${ margin.top } ${ margin.right } ${ margin.bottom } ${ margin.left }` ]
  * 	]
  * } );
  * ```
  */
  setReducer(t, e) {
    this._reducers.set(t, e);
  }
  /**
  * Defines a style shorthand relation to other style notations.
  *
  * ```ts
  * stylesProcessor.setStyleRelation( 'margin', [
  * 	'margin-top',
  * 	'margin-right',
  * 	'margin-bottom',
  * 	'margin-left'
  * ] );
  * ```
  *
  * This enables expanding of style names for shorthands. For instance, if defined,
  * {@link module:engine/conversion/viewconsumable~ViewConsumable view consumable} items are automatically created
  * for long-hand margin style notation alongside the `'margin'` item.
  *
  * This means that when an element being converted has a style `margin`, a converter for `margin-left` will work just
  * fine since the view consumable will contain a consumable `margin-left` item (thanks to the relation) and
  * `element.getStyle( 'margin-left' )` will work as well assuming that the style processor was correctly configured.
  * However, once `margin-left` is consumed, `margin` will not be consumable anymore.
  */
  setStyleRelation(t, e) {
    this._mapStyleNames(t, e);
    for (const i of e)
      this._mapStyleNames(i, [
        t
      ]);
  }
  /**
  * Set two-way binding of style names.
  */
  _mapStyleNames(t, e) {
    this._consumables.has(t) || this._consumables.set(t, []), this._consumables.get(t).push(...e);
  }
}
function A6(n) {
  let t = null, e = 0, i = 0, s = null;
  const r = /* @__PURE__ */ new Map();
  if (n === "")
    return r;
  n.charAt(n.length - 1) != ";" && (n = n + ";");
  for (let o = 0; o < n.length; o++) {
    const a = n.charAt(o);
    if (t === null)
      switch (a) {
        case ":":
          s || (s = n.substr(e, o - e), i = o + 1);
          break;
        case '"':
        case "'":
          t = a;
          break;
        case ";": {
          const l = n.substr(i, o - i);
          s && r.set(s.trim(), l.trim()), s = null, e = o + 1;
          break;
        }
      }
    else a === t && (t = null);
  }
  return r;
}
function oh(n) {
  return n.replace("-", ".");
}
function Nu(n, t, e) {
  let i = e;
  nt(e) && (i = bf({}, Fr(n, t), e)), Wv(n, t, i);
}
class Fu {
  constructor() {
    /**
    * The set of tokens.
    */
    f(this, "_set", /* @__PURE__ */ new Set());
  }
  /**
  * Returns true if token list has no tokens set.
  */
  get isEmpty() {
    return this._set.size == 0;
  }
  /**
  * Number of tokens.
  */
  get size() {
    return this._set.size;
  }
  /**
  * Checks if a given token is set.
  */
  has(t) {
    return this._set.has(t);
  }
  /**
  * Returns all tokens.
  */
  keys() {
    return Array.from(this._set.keys());
  }
  /**
  * Resets the value to the given one.
  */
  setTo(t) {
    this.clear();
    for (const e of t.split(/\s+/))
      e && this._set.add(e);
    return this;
  }
  /**
  * Sets a given token without affecting other tokens.
  */
  set(t) {
    for (const e of Ue(t))
      e && this._set.add(e);
  }
  /**
  * Removes given token.
  */
  remove(t) {
    for (const e of Ue(t))
      this._set.delete(e);
  }
  /**
  * Removes all tokens.
  */
  clear() {
    this._set.clear();
  }
  /**
  * Returns a normalized tokens string.
  */
  toString() {
    return Array.from(this._set).join(" ");
  }
  /**
  * Returns `true` if both attributes have the same tokens.
  */
  isSimilar(t) {
    if (this.size !== t.size)
      return !1;
    for (const e of this.keys())
      if (!t.has(e))
        return !1;
    return !0;
  }
  /**
  * Clones the attribute value.
  *
  * @internal
  */
  _clone() {
    const t = new this.constructor();
    return t._set = new Set(this._set), t;
  }
  /**
  * Used by the {@link module:engine/view/matcher~Matcher Matcher} to collect matching attribute tokens.
  *
  * @internal
  * @param tokenPattern The matched token name pattern.
  * @returns An array of matching tokens.
  */
  _getTokensMatch(t) {
    const e = [];
    if (t === !0) {
      for (const i of this._set.keys())
        e.push(i);
      return e;
    }
    if (typeof t == "string") {
      for (const i of t.split(/\s+/))
        if (this._set.has(i))
          e.push(i);
        else
          return;
      return e;
    }
    for (const i of this._set.keys())
      i.match(t) && e.push(i);
    return e.length ? e : void 0;
  }
  /**
  * Returns a list of consumables for the attribute.
  *
  * Could be filtered by the given token name.
  *
  * @internal
  */
  _getConsumables(t) {
    return t ? [
      t
    ] : this.keys();
  }
  /**
  * Used by {@link module:engine/view/element~Element#_canMergeAttributesFrom} to verify if the given attribute
  * can be merged without conflicts into the attribute.
  *
  * This method is indirectly used by the {@link module:engine/view/downcastwriter~DowncastWriter} while downcasting
  * an {@link module:engine/view/attributeelement~AttributeElement} to merge it with other `AttributeElement`.
  *
  * @internal
  */
  _canMergeFrom() {
    return !0;
  }
  /**
  * Used by {@link module:engine/view/element~Element#_mergeAttributesFrom} to merge a given attribute into the attribute.
  *
  * This method is indirectly used by the {@link module:engine/view/downcastwriter~DowncastWriter} while down-casting
  * an {@link module:engine/view/attributeelement~AttributeElement} to merge it with other AttributeElement.
  *
  * @internal
  */
  _mergeFrom(t) {
    for (const e of t._set.keys())
      this._set.has(e) || this._set.add(e);
  }
  /**
  * Used by {@link module:engine/view/element~Element#_canSubtractAttributesOf} to verify if the given attribute
  * can be fully subtracted from the attribute.
  *
  * This method is indirectly used by the {@link module:engine/view/downcastwriter~DowncastWriter} while down-casting
  * an {@link module:engine/view/attributeelement~AttributeElement} to unwrap the AttributeElement.
  *
  * @internal
  */
  _isMatching(t) {
    for (const e of t._set.keys())
      if (!this._set.has(e))
        return !1;
    return !0;
  }
}
let Un = class v_ extends Ks {
  /**
  * Creates a view element.
  *
  * Attributes can be passed in various formats:
  *
  * ```ts
  * new Element( viewDocument, 'div', { class: 'editor', contentEditable: 'true' } ); // object
  * new Element( viewDocument, 'div', [ [ 'class', 'editor' ], [ 'contentEditable', 'true' ] ] ); // map-like iterator
  * new Element( viewDocument, 'div', mapOfAttributes ); // map
  * ```
  *
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(e, i, s, r) {
    super(e);
    /**
    * Name of the element.
    */
    f(this, "name");
    /**
    * A list of attribute names that should be rendered in the editing pipeline even though filtering mechanisms
    * implemented in the {@link module:engine/view/domconverter~DomConverter} (for instance,
    * {@link module:engine/view/domconverter~DomConverter#shouldRenderAttribute}) would filter them out.
    *
    * These attributes can be specified as an option when the element is created by
    * the {@link module:engine/view/downcastwriter~DowncastWriter}. To check whether an unsafe an attribute should
    * be permitted, use the {@link #shouldRenderUnsafeAttribute} method.
    *
    * @internal
    */
    f(this, "_unsafeAttributesToRender", []);
    /**
    * Map of attributes, where attributes names are keys and attributes values are values.
    */
    f(this, "_attrs");
    /**
    * Array of child nodes.
    */
    f(this, "_children");
    /**
    * Map of custom properties.
    * Custom properties can be added to element instance, will be cloned but not rendered into DOM.
    */
    f(this, "_customProperties", /* @__PURE__ */ new Map());
    this.name = i, this._attrs = this._parseAttributes(s), this._children = [], r && this._insertChild(0, r);
  }
  /**
  * Set of classes associated with element instance.
  *
  * Note that this is just an alias for `this._attrs.get( 'class' );`
  */
  get _classes() {
    return this._attrs.get("class");
  }
  /**
  * Normalized styles.
  *
  * Note that this is just an alias for `this._attrs.get( 'style' );`
  */
  get _styles() {
    return this._attrs.get("style");
  }
  /**
  * Number of element's children.
  */
  get childCount() {
    return this._children.length;
  }
  /**
  * Is `true` if there are no nodes inside this element, `false` otherwise.
  */
  get isEmpty() {
    return this._children.length === 0;
  }
  /**
  * Gets child at the given index.
  *
  * @param index Index of child.
  * @returns Child node.
  */
  getChild(e) {
    return this._children[e];
  }
  /**
  * Gets index of the given child node. Returns `-1` if child node is not found.
  *
  * @param node Child node.
  * @returns Index of the child node.
  */
  getChildIndex(e) {
    return this._children.indexOf(e);
  }
  /**
  * Gets child nodes iterator.
  *
  * @returns Child nodes iterator.
  */
  getChildren() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Returns an iterator that contains the keys for attributes. Order of inserting attributes is not preserved.
  *
  * @returns Keys for attributes.
  */
  *getAttributeKeys() {
    this._classes && (yield "class"), this._styles && (yield "style");
    for (const e of this._attrs.keys())
      e != "class" && e != "style" && (yield e);
  }
  /**
  * Returns iterator that iterates over this element's attributes.
  *
  * Attributes are returned as arrays containing two items. First one is attribute key and second is attribute value.
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  *getAttributes() {
    for (const [e, i] of this._attrs.entries())
      yield [
        e,
        String(i)
      ];
  }
  /**
  * Gets attribute by key. If attribute is not present - returns undefined.
  *
  * @param key Attribute key.
  * @returns Attribute value.
  */
  getAttribute(e) {
    return this._attrs.has(e) ? String(this._attrs.get(e)) : void 0;
  }
  /**
  * Returns a boolean indicating whether an attribute with the specified key exists in the element.
  *
  * @param key Attribute key.
  * @returns `true` if attribute with the specified key exists in the element, `false` otherwise.
  */
  hasAttribute(e, i) {
    return this._attrs.has(e) ? i !== void 0 ? lr(this.name, e) || mo(this.name, e) ? this._attrs.get(e).has(i) : this._attrs.get(e) === i : !0 : !1;
  }
  /**
  * Checks if this element is similar to other element.
  * Both elements should have the same name and attributes to be considered as similar. Two similar elements
  * can contain different set of children nodes.
  */
  isSimilar(e) {
    if (!(e instanceof v_))
      return !1;
    if (this === e)
      return !0;
    if (this.name != e.name || this._attrs.size !== e._attrs.size)
      return !1;
    for (const [i, s] of this._attrs) {
      const r = e._attrs.get(i);
      if (r === void 0)
        return !1;
      if (typeof s == "string" || typeof r == "string") {
        if (r !== s)
          return !1;
      } else if (!s.isSimilar(r))
        return !1;
    }
    return !0;
  }
  /**
  * Returns true if class is present.
  * If more then one class is provided - returns true only when all classes are present.
  *
  * ```ts
  * element.hasClass( 'foo' ); // Returns true if 'foo' class is present.
  * element.hasClass( 'foo', 'bar' ); // Returns true if 'foo' and 'bar' classes are both present.
  * ```
  */
  hasClass(...e) {
    for (const i of e)
      if (!this._classes || !this._classes.has(i))
        return !1;
    return !0;
  }
  /**
  * Returns iterator that contains all class names.
  */
  getClassNames() {
    const e = this._classes ? this._classes.keys() : [], i = e[Symbol.iterator]();
    return Object.assign(e, {
      next: i.next.bind(i)
    });
  }
  /**
  * Returns style value for the given property name.
  * If the style does not exist `undefined` is returned.
  *
  * **Note**: This method can work with normalized style names if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#getAsString `StylesMap#getAsString()`} for details.
  *
  * For an element with style set to `'margin:1px'`:
  *
  * ```ts
  * // Enable 'margin' shorthand processing:
  * editor.data.addStyleProcessorRules( addMarginRules );
  *
  * const element = view.change( writer => {
  * 	const element = writer.createElement();
  * 	writer.setStyle( 'margin', '1px' );
  * 	writer.setStyle( 'margin-bottom', '3em' );
  *
  * 	return element;
  * } );
  *
  * element.getStyle( 'margin' ); // -> 'margin: 1px 1px 3em;'
  * ```
  */
  getStyle(e) {
    return this._styles && this._styles.getAsString(e);
  }
  /**
  * Returns a normalized style object or single style value.
  *
  * For an element with style set to: margin:1px 2px 3em;
  *
  * ```ts
  * element.getNormalizedStyle( 'margin' ) );
  * ```
  *
  * will return:
  *
  * ```ts
  * {
  * 	top: '1px',
  * 	right: '2px',
  * 	bottom: '3em',
  * 	left: '2px'    // a normalized value from margin shorthand
  * }
  * ```
  *
  * and reading for single style value:
  *
  * ```ts
  * styles.getNormalizedStyle( 'margin-left' );
  * ```
  *
  * Will return a `2px` string.
  *
  * **Note**: This method will return normalized values only if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#getNormalized `StylesMap#getNormalized()`} for details.
  *
  * @param property Name of CSS property
  */
  getNormalizedStyle(e) {
    return this._styles && this._styles.getNormalized(e);
  }
  /**
  * Returns an array that contains all style names.
  *
  * @param expand Expand shorthand style properties and return all equivalent style representations.
  */
  getStyleNames(e) {
    return this._styles ? this._styles.getStyleNames(e) : [];
  }
  /**
  * Returns true if style keys are present.
  * If more then one style property is provided - returns true only when all properties are present.
  *
  * ```ts
  * element.hasStyle( 'color' ); // Returns true if 'border-top' style is present.
  * element.hasStyle( 'color', 'border-top' ); // Returns true if 'color' and 'border-top' styles are both present.
  * ```
  */
  hasStyle(...e) {
    for (const i of e)
      if (!this._styles || !this._styles.has(i))
        return !1;
    return !0;
  }
  /**
  * Returns ancestor element that match specified pattern.
  * Provided patterns should be compatible with {@link module:engine/view/matcher~Matcher Matcher} as it is used internally.
  *
  * @see module:engine/view/matcher~Matcher
  * @param patterns Patterns used to match correct ancestor. See {@link module:engine/view/matcher~Matcher}.
  * @returns Found element or `null` if no matching ancestor was found.
  */
  findAncestor(...e) {
    const i = new oi(...e);
    let s = this.parent;
    for (; s && !s.is("documentFragment"); ) {
      if (i.match(s))
        return s;
      s = s.parent;
    }
    return null;
  }
  /**
  * Returns the custom property value for the given key.
  */
  getCustomProperty(e) {
    return this._customProperties.get(e);
  }
  /**
  * Returns an iterator which iterates over this element's custom properties.
  * Iterator provides `[ key, value ]` pairs for each stored property.
  */
  *getCustomProperties() {
    yield* this._customProperties.entries();
  }
  /**
  * Returns identity string based on element's name, styles, classes and other attributes.
  * Two elements that {@link #isSimilar are similar} will have same identity string.
  * It has the following format:
  *
  * ```ts
  * 'name class="class1,class2" style="style1:value1;style2:value2" attr1="val1" attr2="val2"'
  * ```
   *
  * For example:
  *
  * ```ts
  * const element = writer.createContainerElement( 'foo', {
  * 	banana: '10',
  * 	apple: '20',
  * 	style: 'color: red; border-color: white;',
  * 	class: 'baz'
  * } );
  *
  * // returns 'foo class="baz" style="border-color:white;color:red" apple="20" banana="10"'
  * element.getIdentity();
  * ```
  *
  * **Note**: Classes, styles and other attributes are sorted alphabetically.
  */
  getIdentity() {
    const e = this._classes ? this._classes.keys().sort().join(",") : "", i = this._styles && String(this._styles), s = Array.from(this._attrs).filter(([r]) => r != "style" && r != "class").map((r) => `${r[0]}="${r[1]}"`).sort().join(" ");
    return this.name + (e == "" ? "" : ` class="${e}"`) + (i ? ` style="${i}"` : "") + (s == "" ? "" : ` ${s}`);
  }
  /**
  * Decides whether an unsafe attribute is whitelisted and should be rendered in the editing pipeline even though filtering mechanisms
  * like {@link module:engine/view/domconverter~DomConverter#shouldRenderAttribute} say it should not.
  *
  * Unsafe attribute names can be specified when creating an element via {@link module:engine/view/downcastwriter~DowncastWriter}.
  *
  * @param attributeName The name of the attribute to be checked.
  */
  shouldRenderUnsafeAttribute(e) {
    return this._unsafeAttributesToRender.includes(e);
  }
  /**
  * Clones provided element.
  *
  * @internal
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any children.
  * @returns Clone of this element.
  */
  _clone(e = !1) {
    const i = [];
    if (e)
      for (const r of this.getChildren())
        i.push(r._clone(e));
    const s = new this.constructor(this.document, this.name, this._attrs, i);
    return s._customProperties = new Map(this._customProperties), s.getFillerOffset = this.getFillerOffset, s._unsafeAttributesToRender = this._unsafeAttributesToRender, s;
  }
  /**
  * {@link module:engine/view/element~Element#_insertChild Insert} a child node or a list of child nodes at the end of this node
  * and sets the parent of these nodes to this element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#insert
  * @internal
  * @param items Items to be inserted.
  * @fires change
  * @returns Number of appended nodes.
  */
  _appendChild(e) {
    return this._insertChild(this.childCount, e);
  }
  /**
  * Inserts a child node or a list of child nodes on the given index and sets the parent of these nodes to
  * this element.
  *
  * @internal
  * @see module:engine/view/downcastwriter~DowncastWriter#insert
  * @param index Position where nodes should be inserted.
  * @param items Items to be inserted.
  * @fires change
  * @returns Number of inserted nodes.
  */
  _insertChild(e, i) {
    this._fireChange("children", this, {
      index: e
    });
    let s = 0;
    const r = I6(this.document, i);
    for (const o of r)
      o.parent !== null && o._remove(), o.parent = this, o.document = this.document, this._children.splice(e, 0, o), e++, s++;
    return s;
  }
  /**
  * Removes number of child nodes starting at the given index and set the parent of these nodes to `null`.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#remove
  * @internal
  * @param index Number of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @fires change
  * @returns The array of removed nodes.
  */
  _removeChildren(e, i = 1) {
    this._fireChange("children", this, {
      index: e
    });
    for (let s = e; s < e + i; s++)
      this._children[s].parent = null;
    return this._children.splice(e, i);
  }
  /**
  * Adds or overwrite attribute with a specified key and value.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#setAttribute
  * @internal
  * @param key Attribute key.
  * @param value Attribute value.
  * @param overwrite Whether tokenized attribute should override the attribute value or just add a token.
  * @fires change
  */
  _setAttribute(e, i, s = !0) {
    if (this._fireChange("attributes", this), lr(this.name, e) || mo(this.name, e)) {
      let r = this._attrs.get(e);
      r || (r = lr(this.name, e) ? new Br(this.document.stylesProcessor) : new Fu(), this._attrs.set(e, r)), s ? r.setTo(String(i)) : lr(this.name, e) ? Array.isArray(i) ? r.set(i[0], i[1]) : r.set(i) : r.set(typeof i == "string" ? i.split(/\s+/) : i);
    } else
      this._attrs.set(e, String(i));
  }
  /**
  * Removes attribute from the element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeAttribute
  * @internal
  * @param key Attribute key.
  * @param tokens Attribute value tokens to remove. The whole attribute is removed if not specified.
  * @returns Returns true if an attribute existed and has been removed.
  * @fires change
  */
  _removeAttribute(e, i) {
    if (this._fireChange("attributes", this), i !== void 0 && (lr(this.name, e) || mo(this.name, e))) {
      const s = this._attrs.get(e);
      return s ? (mo(this.name, e) && typeof i == "string" && (i = i.split(/\s+/)), s.remove(i), s.isEmpty ? this._attrs.delete(e) : !1) : !1;
    }
    return this._attrs.delete(e);
  }
  /**
  * Adds specified class.
  *
  * ```ts
  * element._addClass( 'foo' ); // Adds 'foo' class.
  * element._addClass( [ 'foo', 'bar' ] ); // Adds 'foo' and 'bar' classes.
  * ```
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#addClass
  * @internal
  * @fires change
  */
  _addClass(e) {
    this._setAttribute("class", e, !1);
  }
  /**
  * Removes specified class.
  *
  * ```ts
  * element._removeClass( 'foo' );  // Removes 'foo' class.
  * element._removeClass( [ 'foo', 'bar' ] ); // Removes both 'foo' and 'bar' classes.
  * ```
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeClass
  * @internal
  * @fires change
  */
  _removeClass(e) {
    this._removeAttribute("class", e);
  }
  _setStyle(e, i) {
    typeof e != "string" ? this._setAttribute("style", e, !1) : this._setAttribute("style", [
      e,
      i
    ], !1);
  }
  /**
  * Removes specified style.
  *
  * ```ts
  * element._removeStyle( 'color' );  // Removes 'color' style.
  * element._removeStyle( [ 'color', 'border-top' ] ); // Removes both 'color' and 'border-top' styles.
  * ```
  *
  * **Note**: This method can work with normalized style names if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#remove `StylesMap#remove()`} for details.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeStyle
  * @internal
  * @fires change
  */
  _removeStyle(e) {
    this._removeAttribute("style", e);
  }
  /**
  * Used by the {@link module:engine/view/matcher~Matcher Matcher} to collect matching attribute tuples
  * (attribute name and optional token).
  *
  * Normalized patterns can be used in following ways:
  * - to match any attribute name with any or no value:
  *
  * ```ts
  * patterns: [
  * 	[ true, true ]
  * ]
  * ```
  *
  * - to match a specific attribute with any value:
  *
  * ```ts
  * patterns: [
  * 	[ 'required', true ]
  * ]
  * ```
  *
  * - to match an attribute name with a RegExp with any value:
  *
  * ```ts
  * patterns: [
  * 	[ /h[1-6]/, true ]
  * ]
  * ```
  *
  * 	- to match a specific attribute with the exact value:
  *
  * ```ts
  * patterns: [
  * 	[ 'rel', 'nofollow' ]
  * ]
  * ```
  *
  * 	- to match a specific attribute with a value matching a RegExp:
  *
  * ```ts
  * patterns: [
  * 	[ 'src', /^https/ ]
  * ]
  * ```
  *
  * 	- to match an attribute name with a RegExp and the exact value:
  *
  * ```ts
  * patterns: [
  * 	[ /^data-property-/, 'foobar' ],
  * ]
  * ```
  *
  * 	- to match an attribute name with a RegExp and match a value with another RegExp:
  *
  * ```ts
  * patterns: [
  * 	[ /^data-property-/, /^foo/ ]
  * ]
  * ```
  *
  * 	- to match a specific style property with the value matching a RegExp:
  *
  * ```ts
  * patterns: [
  * 	[ 'style', 'font-size', /px$/ ]
  * ]
  * ```
  *
  * 	- to match a specific class (class attribute is tokenized so it matches tokens individually):
  *
  * ```ts
  * patterns: [
  * 	[ 'class', 'foo' ]
  * ]
  * ```
  *
  * @internal
  * @param patterns An array of normalized patterns (tuples of 2 or 3 items depending on if tokenized attribute value match is needed).
  * @param match An array to populate with matching tuples.
  * @param exclude Array of attribute names to exclude from match.
  * @returns `true` if element matches all patterns. The matching tuples are pushed to the `match` array.
  */
  _collectAttributesMatch(e, i, s) {
    for (const [r, o, a] of e) {
      let l = !1, c = !1;
      for (const [u, d] of this._attrs)
        if (!(s && s.includes(u) || !Xl(r, u)))
          if (l = !0, typeof d == "string") {
            if (Xl(o, d))
              i.push([
                u
              ]), c = !0;
            else if (!(r instanceof RegExp))
              return !1;
          } else {
            const h = d._getTokensMatch(o, a || !0);
            if (h) {
              c = !0;
              for (const m of h)
                i.push([
                  u,
                  m
                ]);
            } else if (!(r instanceof RegExp))
              return !1;
          }
      if (!l || !c)
        return !1;
    }
    return !0;
  }
  /**
  * Used by the {@link module:engine/conversion/viewconsumable~ViewConsumable} to collect the
  * {@link module:engine/view/element~NormalizedConsumables} for the element.
  *
  * When `key` and `token` parameters are provided the output is filtered for the specified attribute and it's tokens and related tokens.
  *
  * @internal
  * @param key Attribute name.
  * @param token Reference token to collect all related tokens.
  */
  _getConsumables(e, i) {
    const s = [];
    if (e) {
      const r = this._attrs.get(e);
      if (r !== void 0)
        if (typeof r == "string")
          s.push([
            e
          ]);
        else
          for (const o of r._getConsumables(i))
            s.push([
              e,
              o
            ]);
    } else
      for (const [r, o] of this._attrs)
        if (typeof o == "string")
          s.push([
            r
          ]);
        else
          for (const a of o._getConsumables())
            s.push([
              r,
              a
            ]);
    return {
      name: !e,
      attributes: s
    };
  }
  /**
  * Verify if the given element can be merged without conflicts into the element.
  *
  * Note that this method is extended by the {@link module:engine/view/attributeelement~AttributeElement} implementation.
  *
  * This method is used by the {@link module:engine/view/downcastwriter~DowncastWriter} while down-casting
  * an {@link module:engine/view/attributeelement~AttributeElement} to merge it with other AttributeElement.
  *
  * @internal
  * @returns Returns `true` if elements can be merged.
  */
  _canMergeAttributesFrom(e) {
    if (this.name != e.name)
      return !1;
    for (const [i, s] of e._attrs) {
      const r = this._attrs.get(i);
      if (r !== void 0) {
        if (typeof r == "string" || typeof s == "string") {
          if (r !== s)
            return !1;
        } else if (!r._canMergeFrom(s))
          return !1;
      }
    }
    return !0;
  }
  /**
  * Merges attributes of a given element into the element.
  * This includes also tokenized attributes like style and class.
  *
  * Note that you should make sure there are no conflicts before merging (see {@link #_canMergeAttributesFrom}).
  *
  * This method is used by the {@link module:engine/view/downcastwriter~DowncastWriter} while down-casting
  * an {@link module:engine/view/attributeelement~AttributeElement} to merge it with other AttributeElement.
  *
  * @internal
  */
  _mergeAttributesFrom(e) {
    this._fireChange("attributes", this);
    for (const [i, s] of e._attrs) {
      const r = this._attrs.get(i);
      r === void 0 || typeof r == "string" || typeof s == "string" ? this._setAttribute(i, s) : r._mergeFrom(s);
    }
  }
  /**
  * Verify if the given element attributes can be fully subtracted from the element.
  *
  * Note that this method is extended by the {@link module:engine/view/attributeelement~AttributeElement} implementation.
  *
  * This method is used by the {@link module:engine/view/downcastwriter~DowncastWriter} while down-casting
  * an {@link module:engine/view/attributeelement~AttributeElement} to unwrap the AttributeElement.
  *
  * @internal
  * @returns Returns `true` if elements attributes can be fully subtracted.
  */
  _canSubtractAttributesOf(e) {
    if (this.name != e.name)
      return !1;
    for (const [i, s] of e._attrs) {
      const r = this._attrs.get(i);
      if (r === void 0)
        return !1;
      if (typeof r == "string" || typeof s == "string") {
        if (r !== s)
          return !1;
      } else if (!r._isMatching(s))
        return !1;
    }
    return !0;
  }
  /**
  * Removes (subtracts) corresponding attributes of the given element from the element.
  * This includes also tokenized attributes like style and class.
  * All attributes, classes and styles from given element should be present inside the element being unwrapped.
  *
  * Note that you should make sure all attributes could be subtracted before subtracting them (see {@link #_canSubtractAttributesOf}).
  *
  * This method is used by the {@link module:engine/view/downcastwriter~DowncastWriter} while down-casting
  * an {@link module:engine/view/attributeelement~AttributeElement} to unwrap the AttributeElement.
  *
  * @internal
  */
  _subtractAttributesOf(e) {
    this._fireChange("attributes", this);
    for (const [i, s] of e._attrs) {
      const r = this._attrs.get(i);
      typeof r == "string" || typeof s == "string" ? this._attrs.delete(i) : (r.remove(s.keys()), r.isEmpty && this._attrs.delete(i));
    }
  }
  /**
  * Sets a custom property. Unlike attributes, custom properties are not rendered to the DOM,
  * so they can be used to add special data to elements.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#setCustomProperty
  * @internal
  */
  _setCustomProperty(e, i) {
    this._customProperties.set(e, i);
  }
  /**
  * Removes the custom property stored under the given key.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeCustomProperty
  * @internal
  * @returns Returns true if property was removed.
  */
  _removeCustomProperty(e) {
    return this._customProperties.delete(e);
  }
  /**
  * Parses attributes provided to the element constructor before they are applied to an element. If attributes are passed
  * as an object (instead of `Iterable`), the object is transformed to the map. Attributes with `null` value are removed.
  * Attributes with non-`String` value are converted to `String`.
  *
  * @param attrs Attributes to parse.
  * @returns Parsed attributes.
  */
  _parseAttributes(e) {
    const i = Ji(e);
    for (const [s, r] of i)
      if (r === null)
        i.delete(s);
      else if (lr(this.name, s)) {
        const o = r instanceof Br ? r._clone() : new Br(this.document.stylesProcessor).setTo(String(r));
        i.set(s, o);
      } else if (mo(this.name, s)) {
        const o = r instanceof Fu ? r._clone() : new Fu().setTo(String(r));
        i.set(s, o);
      } else typeof r != "string" && i.set(s, String(r));
    return i;
  }
};
Un.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "element" || n === "view:element") : n === "element" || n === "view:element" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "node" || n === "view:node";
};
function I6(n, t) {
  if (typeof t == "string")
    return [
      new qt(n, t)
    ];
  Ai(t) || (t = [
    t
  ]);
  const e = [];
  for (const i of t)
    typeof i == "string" ? e.push(new qt(n, i)) : i instanceof Mn ? e.push(new qt(n, i.data)) : e.push(i);
  return e;
}
function mo(n, t) {
  return t == "class" || n == "a" && t == "rel";
}
function lr(n, t) {
  return t == "style";
}
class sa extends Un {
  /**
  * Creates a container element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createContainerElement
  * @see module:engine/view/element~Element
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, i, s) {
    super(t, e, i, s), this.getFillerOffset = P6;
  }
}
sa.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "containerElement" || n === "view:containerElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "element" || n === "view:element") : n === "containerElement" || n === "view:containerElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "element" || n === "view:element" || n === "node" || n === "view:node";
};
function P6() {
  const n = [
    ...this.getChildren()
  ], t = n[this.childCount - 1];
  if (t && t.is("element", "br"))
    return this.childCount;
  for (const e of n)
    if (!e.is("uiElement"))
      return null;
  return this.childCount;
}
class qc extends (/* @__PURE__ */ je(sa)) {
  /**
  * Creates an editable element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createEditableElement
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, i, s) {
    super(t, e, i, s), this.set("isReadOnly", !1), this.set("isFocused", !1), this.set("placeholder", void 0), this.bind("isReadOnly").to(t), this.bind("isFocused").to(t, "isFocused", (r) => r && t.selection.editableElement == this), this.listenTo(t.selection, "change", () => {
      this.isFocused = t.isFocused && t.selection.editableElement == this;
    });
  }
  destroy() {
    this.stopListening();
  }
}
qc.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "editableElement" || n === "view:editableElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "containerElement" || n === "view:containerElement" || n === "element" || n === "view:element") : n === "editableElement" || n === "view:editableElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "containerElement" || n === "view:containerElement" || n === "element" || n === "view:element" || n === "node" || n === "view:node";
};
const bp = Symbol("rootName");
class __ extends qc {
  /**
  * Creates root editable element.
  *
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  */
  constructor(t, e) {
    super(t, e), this.rootName = "main";
  }
  /**
  * Name of this root inside {@link module:engine/view/document~Document} that is an owner of this root. If no
  * other name is set, `main` name is used.
  *
  * @readonly
  */
  get rootName() {
    return this.getCustomProperty(bp);
  }
  set rootName(t) {
    this._setCustomProperty(bp, t);
  }
  /**
  * Overrides old element name and sets new one.
  * This is needed because view roots are created before they are attached to the DOM.
  * The name of the root element is temporary at this stage. It has to be changed when the
  * view root element is attached to the DOM element.
  *
  * @internal
  * @param name The new name of element.
  */
  set _name(t) {
    this.name = t;
  }
}
__.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "rootElement" || n === "view:rootElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "editableElement" || n === "view:editableElement" || n === "containerElement" || n === "view:containerElement" || n === "element" || n === "view:element") : n === "rootElement" || n === "view:rootElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "editableElement" || n === "view:editableElement" || n === "containerElement" || n === "view:containerElement" || n === "element" || n === "view:element" || n === "node" || n === "view:node";
};
let Rs = class {
  /**
  * Creates a range iterator. All parameters are optional, but you have to specify either `boundaries` or `startPosition`.
  *
  * @param options Object with configuration.
  */
  constructor(t = {}) {
    /**
    * Walking direction. Defaults `'forward'`.
    */
    f(this, "direction");
    /**
    * Iterator boundaries.
    *
    * When the iterator is walking `'forward'` on the end of boundary or is walking `'backward'`
    * on the start of boundary, then `{ done: true }` is returned.
    *
    * If boundaries are not defined they are set before first and after last child of the root node.
    */
    f(this, "boundaries");
    /**
    * Flag indicating whether all characters from {@link module:engine/view/text~Text} should be returned as one
    * {@link module:engine/view/text~Text} or one by one as {@link module:engine/view/textproxy~TextProxy}.
    */
    f(this, "singleCharacters");
    /**
    * Flag indicating whether iterator should enter elements or not. If the iterator is shallow child nodes of any
    * iterated node will not be returned along with `elementEnd` tag.
    */
    f(this, "shallow");
    /**
    * Flag indicating whether iterator should ignore `elementEnd` tags. If set to `true`, walker will not
    * return a parent node of the start position. Each {@link module:engine/view/element~Element} will be returned once.
    * When set to `false` each element might be returned twice: for `'elementStart'` and `'elementEnd'`.
    */
    f(this, "ignoreElementEnd");
    /**
    * Iterator position. If start position is not defined then position depends on {@link #direction}. If direction is
    * `'forward'` position starts form the beginning, when direction is `'backward'` position starts from the end.
    */
    f(this, "_position");
    /**
    * Start boundary parent.
    */
    f(this, "_boundaryStartParent");
    /**
    * End boundary parent.
    */
    f(this, "_boundaryEndParent");
    if (!t.boundaries && !t.startPosition)
      throw new S("view-tree-walker-no-start-position", null);
    if (t.direction && t.direction != "forward" && t.direction != "backward")
      throw new S("view-tree-walker-unknown-direction", t.startPosition, {
        direction: t.direction
      });
    this.boundaries = t.boundaries || null, t.startPosition ? this._position = re._createAt(t.startPosition) : this._position = re._createAt(t.boundaries[t.direction == "backward" ? "end" : "start"]), this.direction = t.direction || "forward", this.singleCharacters = !!t.singleCharacters, this.shallow = !!t.shallow, this.ignoreElementEnd = !!t.ignoreElementEnd, this._boundaryStartParent = this.boundaries ? this.boundaries.start.parent : null, this._boundaryEndParent = this.boundaries ? this.boundaries.end.parent : null;
  }
  /**
  * Iterable interface.
  */
  [Symbol.iterator]() {
    return this;
  }
  /**
  * Iterator position. If start position is not defined then position depends on {@link #direction}. If direction is
  * `'forward'` position starts form the beginning, when direction is `'backward'` position starts from the end.
  */
  get position() {
    return this._position;
  }
  /**
  * Moves {@link #position} in the {@link #direction} skipping values as long as the callback function returns `true`.
  *
  * For example:
  *
  * ```ts
  * walker.skip( value => value.type == 'text' ); // <p>{}foo</p> -> <p>foo[]</p>
  * walker.skip( value => true ); // Move the position to the end: <p>{}foo</p> -> <p>foo</p>[]
  * walker.skip( value => false ); // Do not move the position.
  * ```
  *
  * @param skip Callback function. Gets {@link module:engine/view/treewalker~TreeWalkerValue} and should
  * return `true` if the value should be skipped or `false` if not.
  */
  skip(t) {
    let e, i;
    do
      i = this.position, e = this.next();
    while (!e.done && t(e.value));
    e.done || (this._position = i);
  }
  /**
  * Moves tree walker {@link #position} to provided `position`. Tree walker will
  * continue traversing from that position.
  *
  * Note: in contrary to {@link ~TreeWalker#skip}, this method does not iterate over the nodes along the way.
  * It simply sets the current tree walker position to a new one.
  * From the performance standpoint, it is better to use {@link ~TreeWalker#jumpTo} rather than {@link ~TreeWalker#skip}.
  *
  * If the provided position is before the start boundary, the position will be
  * set to the start boundary. If the provided position is after the end boundary,
  * the position will be set to the end boundary.
  * This is done to prevent the treewalker from traversing outside the boundaries.
  *
  * @param position Position to jump to.
  */
  jumpTo(t) {
    this._boundaryStartParent && t.isBefore(this.boundaries.start) ? t = this.boundaries.start : this._boundaryEndParent && t.isAfter(this.boundaries.end) && (t = this.boundaries.end), this._position = t.clone();
  }
  /**
  * Gets the next tree walker's value.
  *
  * @returns Object implementing iterator interface, returning
  * information about taken step.
  */
  next() {
    return this.direction == "forward" ? this._next() : this._previous();
  }
  /**
  * Makes a step forward in view. Moves the {@link #position} to the next position and returns the encountered value.
  */
  _next() {
    let t = this.position.clone();
    const e = this.position, i = t.parent;
    if (i.parent === null && t.offset === i.childCount)
      return {
        done: !0,
        value: void 0
      };
    if (i === this._boundaryEndParent && t.offset == this.boundaries.end.offset)
      return {
        done: !0,
        value: void 0
      };
    let s;
    if (i && i.is("view:$text")) {
      if (t.isAtEnd)
        return this._position = re._createAfter(i), this._next();
      s = i.data[t.offset];
    } else
      s = i.getChild(t.offset);
    if (typeof s == "string") {
      let r;
      this.singleCharacters ? r = 1 : r = (i === this._boundaryEndParent ? this.boundaries.end.offset : i.data.length) - t.offset;
      const o = new Mn(i, t.offset, r);
      return t.offset += r, this._position = t, this._formatReturnValue("text", o, e, t, r);
    }
    if (s && s.is("view:element")) {
      if (!this.shallow)
        t = new re(s, 0);
      else {
        if (this.boundaries && this.boundaries.end.isBefore(t))
          return {
            done: !0,
            value: void 0
          };
        t.offset++;
      }
      return this._position = t, this._formatReturnValue("elementStart", s, e, t, 1);
    }
    if (s && s.is("view:$text")) {
      if (this.singleCharacters)
        return t = new re(s, 0), this._position = t, this._next();
      let r = s.data.length, o;
      return s == this._boundaryEndParent ? (r = this.boundaries.end.offset, o = new Mn(s, 0, r), t = re._createAfter(o)) : (o = new Mn(s, 0, s.data.length), t.offset++), this._position = t, this._formatReturnValue("text", o, e, t, r);
    }
    return t = re._createAfter(i), this._position = t, this.ignoreElementEnd ? this._next() : this._formatReturnValue("elementEnd", i, e, t);
  }
  /**
  * Makes a step backward in view. Moves the {@link #position} to the previous position and returns the encountered value.
  */
  _previous() {
    let t = this.position.clone();
    const e = this.position, i = t.parent;
    if (i.parent === null && t.offset === 0)
      return {
        done: !0,
        value: void 0
      };
    if (i == this._boundaryStartParent && t.offset == this.boundaries.start.offset)
      return {
        done: !0,
        value: void 0
      };
    let s;
    if (i.is("view:$text")) {
      if (t.isAtStart)
        return this._position = re._createBefore(i), this._previous();
      s = i.data[t.offset - 1];
    } else
      s = i.getChild(t.offset - 1);
    if (typeof s == "string") {
      let r;
      if (this.singleCharacters)
        r = 1;
      else {
        const a = i === this._boundaryStartParent ? this.boundaries.start.offset : 0;
        r = t.offset - a;
      }
      t.offset -= r;
      const o = new Mn(i, t.offset, r);
      return this._position = t, this._formatReturnValue("text", o, e, t, r);
    }
    if (s && s.is("view:element"))
      return this.shallow ? (t.offset--, this._position = t, this._formatReturnValue("elementStart", s, e, t, 1)) : (t = new re(s, s.childCount), this._position = t, this.ignoreElementEnd ? this._previous() : this._formatReturnValue("elementEnd", s, e, t));
    if (s && s.is("view:$text")) {
      if (this.singleCharacters)
        return t = new re(s, s.data.length), this._position = t, this._previous();
      let r = s.data.length, o;
      if (s == this._boundaryStartParent) {
        const a = this.boundaries.start.offset;
        o = new Mn(s, a, s.data.length - a), r = o.data.length, t = re._createBefore(o);
      } else
        o = new Mn(s, 0, s.data.length), t.offset--;
      return this._position = t, this._formatReturnValue("text", o, e, t, r);
    }
    return t = re._createBefore(i), this._position = t, this._formatReturnValue("elementStart", i, e, t, 1);
  }
  /**
  * Format returned data and adjust `previousPosition` and `nextPosition` if reach the bound of the {@link module:engine/view/text~Text}.
  *
  * @param type Type of step.
  * @param item Item between old and new position.
  * @param previousPosition Previous position of iterator.
  * @param nextPosition Next position of iterator.
  * @param length Length of the item.
  */
  _formatReturnValue(t, e, i, s, r) {
    return e.is("view:$textProxy") && (e.offsetInText + e.data.length == e.textNode.data.length && (this.direction == "forward" && !(this.boundaries && this.boundaries.end.isEqual(this.position)) ? (s = re._createAfter(e.textNode), this._position = s) : i = re._createAfter(e.textNode)), e.offsetInText === 0 && (this.direction == "backward" && !(this.boundaries && this.boundaries.start.isEqual(this.position)) ? (s = re._createBefore(e.textNode), this._position = s) : i = re._createBefore(e.textNode))), {
      done: !1,
      value: {
        type: t,
        item: e,
        previousPosition: i,
        nextPosition: s,
        length: r
      }
    };
  }
}, re = class Ln extends Gs {
  /**
  * Creates a position.
  *
  * @param parent Position parent.
  * @param offset Position offset.
  */
  constructor(e, i) {
    super();
    /**
    * Position parent.
    */
    f(this, "parent");
    /**
    * Position offset.
    */
    f(this, "offset");
    this.parent = e, this.offset = i;
  }
  /**
  * Node directly after the position. Equals `null` when there is no node after position or position is located
  * inside text node.
  */
  get nodeAfter() {
    return this.parent.is("$text") ? null : this.parent.getChild(this.offset) || null;
  }
  /**
  * Node directly before the position. Equals `null` when there is no node before position or position is located
  * inside text node.
  */
  get nodeBefore() {
    return this.parent.is("$text") ? null : this.parent.getChild(this.offset - 1) || null;
  }
  /**
  * Is `true` if position is at the beginning of its {@link module:engine/view/position~Position#parent parent}, `false` otherwise.
  */
  get isAtStart() {
    return this.offset === 0;
  }
  /**
  * Is `true` if position is at the end of its {@link module:engine/view/position~Position#parent parent}, `false` otherwise.
  */
  get isAtEnd() {
    const e = this.parent.is("$text") ? this.parent.data.length : this.parent.childCount;
    return this.offset === e;
  }
  /**
  * Position's root, that is the root of the position's parent element.
  */
  get root() {
    return this.parent.root;
  }
  /**
  * {@link module:engine/view/editableelement~EditableElement EditableElement} instance that contains this position, or `null` if
  * position is not inside an editable element.
  */
  get editableElement() {
    let e = this.parent;
    for (; !(e instanceof qc); )
      if (e.parent)
        e = e.parent;
      else
        return null;
    return e;
  }
  /**
  * Returns a new instance of Position with offset incremented by `shift` value.
  *
  * @param shift How position offset should get changed. Accepts negative values.
  * @returns Shifted position.
  */
  getShiftedBy(e) {
    const i = Ln._createAt(this), s = i.offset + e;
    return i.offset = s < 0 ? 0 : s, i;
  }
  /**
  * Gets the farthest position which matches the callback using
  * {@link module:engine/view/treewalker~TreeWalker TreeWalker}.
  *
  * For example:
  *
  * ```ts
  * getLastMatchingPosition( value => value.type == 'text' ); // <p>{}foo</p> -> <p>foo[]</p>
  * getLastMatchingPosition( value => value.type == 'text', { direction: 'backward' } ); // <p>foo[]</p> -> <p>{}foo</p>
  * getLastMatchingPosition( value => false ); // Do not move the position.
  * ```
  *
  * @param skip Callback function. Gets {@link module:engine/view/treewalker~TreeWalkerValue} and should
  * return `true` if the value should be skipped or `false` if not.
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}.
  * @returns The position after the last item which matches the `skip` callback test.
  */
  getLastMatchingPosition(e, i = {}) {
    i.startPosition = this;
    const s = new Rs(i);
    return s.skip(e), s.position;
  }
  /**
  * Returns ancestors array of this position, that is this position's parent and it's ancestors.
  *
  * @returns Array with ancestors.
  */
  getAncestors() {
    return this.parent.is("documentFragment") ? [
      this.parent
    ] : this.parent.getAncestors({
      includeSelf: !0
    });
  }
  /**
  * Returns a {@link module:engine/view/node~Node} or {@link module:engine/view/documentfragment~DocumentFragment}
  * which is a common ancestor of both positions.
  */
  getCommonAncestor(e) {
    const i = this.getAncestors(), s = e.getAncestors();
    let r = 0;
    for (; i[r] == s[r] && i[r]; )
      r++;
    return r === 0 ? null : i[r - 1];
  }
  /**
  * Checks whether this position equals given position.
  *
  * @param otherPosition Position to compare with.
  * @returns True if positions are same.
  */
  isEqual(e) {
    return this.parent == e.parent && this.offset == e.offset;
  }
  /**
  * Checks whether this position is located before given position. When method returns `false` it does not mean that
  * this position is after give one. Two positions may be located inside separate roots and in that situation this
  * method will still return `false`.
  *
  * @see module:engine/view/position~Position#isAfter
  * @see module:engine/view/position~Position#compareWith
  * @param otherPosition Position to compare with.
  * @returns Returns `true` if this position is before given position.
  */
  isBefore(e) {
    return this.compareWith(e) == "before";
  }
  /**
  * Checks whether this position is located after given position. When method returns `false` it does not mean that
  * this position is before give one. Two positions may be located inside separate roots and in that situation this
  * method will still return `false`.
  *
  * @see module:engine/view/position~Position#isBefore
  * @see module:engine/view/position~Position#compareWith
  * @param otherPosition Position to compare with.
  * @returns Returns `true` if this position is after given position.
  */
  isAfter(e) {
    return this.compareWith(e) == "after";
  }
  /**
  * Checks whether this position is before, after or in same position that other position. Two positions may be also
  * different when they are located in separate roots.
  *
  * @param otherPosition Position to compare with.
  */
  compareWith(e) {
    if (this.root !== e.root)
      return "different";
    if (this.isEqual(e))
      return "same";
    const i = this.parent.is("node") ? this.parent.getPath() : [], s = e.parent.is("node") ? e.parent.getPath() : [];
    i.push(this.offset), s.push(e.offset);
    const r = Zt(i, s);
    switch (r) {
      case "prefix":
        return "before";
      case "extension":
        return "after";
      default:
        return i[r] < s[r] ? "before" : "after";
    }
  }
  /**
  * Creates a {@link module:engine/view/treewalker~TreeWalker TreeWalker} instance with this positions as a start position.
  *
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}
  */
  getWalker(e = {}) {
    return e.startPosition = this, new Rs(e);
  }
  /**
  * Clones this position.
  */
  clone() {
    return new Ln(this.parent, this.offset);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/view/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/view/item~Item view item} and `'before'` or `'after'` (sets position before or after given view item).
  *
  * This method is a shortcut to other constructors such as:
  *
  * * {@link module:engine/view/position~Position._createBefore},
  * * {@link module:engine/view/position~Position._createAfter}.
  *
  * @internal
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  static _createAt(e, i) {
    if (e instanceof Ln)
      return new this(e.parent, e.offset);
    {
      const s = e;
      if (i == "end")
        i = s.is("$text") ? s.data.length : s.childCount;
      else {
        if (i == "before")
          return this._createBefore(s);
        if (i == "after")
          return this._createAfter(s);
        if (i !== 0 && !i)
          throw new S("view-createpositionat-offset-required", s);
      }
      return new Ln(s, i);
    }
  }
  /**
  * Creates a new position after given view item.
  *
  * @internal
  * @param item View item after which the position should be located.
  */
  static _createAfter(e) {
    if (e.is("$textProxy"))
      return new Ln(e.textNode, e.offsetInText + e.data.length);
    if (!e.parent)
      throw new S("view-position-after-root", e, {
        root: e
      });
    return new Ln(e.parent, e.index + 1);
  }
  /**
  * Creates a new position before given view item.
  *
  * @internal
  * @param item View item before which the position should be located.
  */
  static _createBefore(e) {
    if (e.is("$textProxy"))
      return new Ln(e.textNode, e.offsetInText);
    if (!e.parent)
      throw new S("view-position-before-root", e, {
        root: e
      });
    return new Ln(e.parent, e.index);
  }
};
re.prototype.is = function(n) {
  return n === "position" || n === "view:position";
};
let Re = class rs extends Gs {
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * **Note:** Constructor creates it's own {@link module:engine/view/position~Position} instances basing on passed values.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at the `start` position.
  */
  constructor(e, i = null) {
    super();
    /**
    * Start position.
    */
    f(this, "start");
    /**
    * End position.
    */
    f(this, "end");
    this.start = e.clone(), this.end = i ? i.clone() : e.clone();
  }
  /**
  * Iterable interface.
  *
  * Iterates over all {@link module:engine/view/item~Item view items} that are in this range and returns
  * them together with additional information like length or {@link module:engine/view/position~Position positions},
  * grouped as {@link module:engine/view/treewalker~TreeWalkerValue}.
  *
  * This iterator uses {@link module:engine/view/treewalker~TreeWalker TreeWalker} with `boundaries` set to this range and
  * `ignoreElementEnd` option
  * set to `true`.
  */
  *[Symbol.iterator]() {
    yield* new Rs({
      boundaries: this,
      ignoreElementEnd: !0
    });
  }
  /**
  * Returns whether the range is collapsed, that is it start and end positions are equal.
  */
  get isCollapsed() {
    return this.start.isEqual(this.end);
  }
  /**
  * Returns whether this range is flat, that is if {@link module:engine/view/range~Range#start start} position and
  * {@link module:engine/view/range~Range#end end} position are in the same {@link module:engine/view/position~Position#parent parent}.
  */
  get isFlat() {
    return this.start.parent === this.end.parent;
  }
  /**
  * Range root element.
  */
  get root() {
    return this.start.root;
  }
  /**
  * Creates a maximal range that has the same content as this range but is expanded in both ways (at the beginning
  * and at the end).
  *
  * For example:
  *
  * ```html
  * <p>Foo</p><p><b>{Bar}</b></p> -> <p>Foo</p>[<p><b>Bar</b>]</p>
  * <p><b>foo</b>{bar}<span></span></p> -> <p><b>foo[</b>bar<span></span>]</p>
  * ```
  *
  * Note that in the sample above:
  *
  * - `<p>` have type of {@link module:engine/view/containerelement~ContainerElement},
  * - `<b>` have type of {@link module:engine/view/attributeelement~AttributeElement},
  * - `<span>` have type of {@link module:engine/view/uielement~UIElement}.
  *
  * @returns Enlarged range.
  */
  getEnlarged() {
    let e = this.start.getLastMatchingPosition(il, {
      direction: "backward"
    }), i = this.end.getLastMatchingPosition(il);
    return e.parent.is("$text") && e.isAtStart && (e = re._createBefore(e.parent)), i.parent.is("$text") && i.isAtEnd && (i = re._createAfter(i.parent)), new rs(e, i);
  }
  /**
  * Creates a minimum range that has the same content as this range but is trimmed in both ways (at the beginning
  * and at the end).
  *
  * For example:
  *
  * ```html
  * <p>Foo</p>[<p><b>Bar</b>]</p> -> <p>Foo</p><p><b>{Bar}</b></p>
  * <p><b>foo[</b>bar<span></span>]</p> -> <p><b>foo</b>{bar}<span></span></p>
  * ```
  *
  * Note that in the sample above:
  *
  * - `<p>` have type of {@link module:engine/view/containerelement~ContainerElement},
  * - `<b>` have type of {@link module:engine/view/attributeelement~AttributeElement},
  * - `<span>` have type of {@link module:engine/view/uielement~UIElement}.
  *
  * @returns Shrunk range.
  */
  getTrimmed() {
    let e = this.start.getLastMatchingPosition(il);
    if (e.isAfter(this.end) || e.isEqual(this.end))
      return new rs(e, e);
    let i = this.end.getLastMatchingPosition(il, {
      direction: "backward"
    });
    const s = e.nodeAfter, r = i.nodeBefore;
    return s && s.is("$text") && (e = new re(s, 0)), r && r.is("$text") && (i = new re(r, r.data.length)), new rs(e, i);
  }
  /**
  * Two ranges are equal if their start and end positions are equal.
  *
  * @param otherRange Range to compare with.
  * @returns `true` if ranges are equal, `false` otherwise
  */
  isEqual(e) {
    return this == e || this.start.isEqual(e.start) && this.end.isEqual(e.end);
  }
  /**
  * Checks whether this range contains given {@link module:engine/view/position~Position position}.
  *
  * @param position Position to check.
  * @returns `true` if given {@link module:engine/view/position~Position position} is contained in this range, `false` otherwise.
  */
  containsPosition(e) {
    return e.isAfter(this.start) && e.isBefore(this.end);
  }
  /**
  * Checks whether this range contains given {@link module:engine/view/range~Range range}.
  *
  * @param otherRange Range to check.
  * @param loose Whether the check is loose or strict. If the check is strict (`false`), compared range cannot
  * start or end at the same position as this range boundaries. If the check is loose (`true`), compared range can start, end or
  * even be equal to this range. Note that collapsed ranges are always compared in strict mode.
  * @returns `true` if given {@link module:engine/view/range~Range range} boundaries are contained by this range, `false`
  * otherwise.
  */
  containsRange(e, i = !1) {
    e.isCollapsed && (i = !1);
    const s = this.containsPosition(e.start) || i && this.start.isEqual(e.start), r = this.containsPosition(e.end) || i && this.end.isEqual(e.end);
    return s && r;
  }
  /**
  * Computes which part(s) of this {@link module:engine/view/range~Range range} is not a part of given
  * {@link module:engine/view/range~Range range}.
  * Returned array contains zero, one or two {@link module:engine/view/range~Range ranges}.
  *
  * Examples:
  *
  * ```ts
  * let foo = downcastWriter.createText( 'foo' );
  * let img = downcastWriter.createContainerElement( 'img' );
  * let bar = downcastWriter.createText( 'bar' );
  * let p = downcastWriter.createContainerElement( 'p', null, [ foo, img, bar ] );
  *
  * let range = view.createRange( view.createPositionAt( foo, 2 ), view.createPositionAt( bar, 1 ); // "o", img, "b" are in range.
  * let otherRange = view.createRange( // "oo", img, "ba" are in range.
  * 	view.createPositionAt( foo, 1 ),
  * 	view.createPositionAt( bar, 2 )
  * );
  * let transformed = range.getDifference( otherRange );
  * // transformed array has no ranges because `otherRange` contains `range`
  *
  * otherRange = view.createRange( view.createPositionAt( foo, 1 ), view.createPositionAt( p, 2 ); // "oo", img are in range.
  * transformed = range.getDifference( otherRange );
  * // transformed array has one range: from ( p, 2 ) to ( bar, 1 )
  *
  * otherRange = view.createRange( view.createPositionAt( p, 1 ), view.createPositionAt( p, 2 ) ); // img is in range.
  * transformed = range.getDifference( otherRange );
  * // transformed array has two ranges: from ( foo, 1 ) to ( p, 1 ) and from ( p, 2 ) to ( bar, 1 )
  * ```
  *
  * @param otherRange Range to differentiate against.
  * @returns The difference between ranges.
  */
  getDifference(e) {
    const i = [];
    return this.isIntersecting(e) ? (this.containsPosition(e.start) && i.push(new rs(this.start, e.start)), this.containsPosition(e.end) && i.push(new rs(e.end, this.end))) : i.push(this.clone()), i;
  }
  /**
  * Returns an intersection of this {@link module:engine/view/range~Range range} and given {@link module:engine/view/range~Range range}.
  * Intersection is a common part of both of those ranges. If ranges has no common part, returns `null`.
  *
  * Examples:
  *
  * ```ts
  * let foo = downcastWriter.createText( 'foo' );
  * let img = downcastWriter.createContainerElement( 'img' );
  * let bar = downcastWriter.createText( 'bar' );
  * let p = downcastWriter.createContainerElement( 'p', null, [ foo, img, bar ] );
  *
  * let range = view.createRange( view.createPositionAt( foo, 2 ), view.createPositionAt( bar, 1 ); // "o", img, "b" are in range.
  * let otherRange = view.createRange( view.createPositionAt( foo, 1 ), view.createPositionAt( p, 2 ); // "oo", img are in range.
  * let transformed = range.getIntersection( otherRange ); // range from ( foo, 1 ) to ( p, 2 ).
  *
  * otherRange = view.createRange( view.createPositionAt( bar, 1 ), view.createPositionAt( bar, 3 ); "ar" is in range.
  * transformed = range.getIntersection( otherRange ); // null - no common part.
  * ```
  *
  * @param otherRange Range to check for intersection.
  * @returns A common part of given ranges or `null` if ranges have no common part.
  */
  getIntersection(e) {
    if (this.isIntersecting(e)) {
      let i = this.start, s = this.end;
      return this.containsPosition(e.start) && (i = e.start), this.containsPosition(e.end) && (s = e.end), new rs(i, s);
    }
    return null;
  }
  /**
  * Creates a {@link module:engine/view/treewalker~TreeWalker TreeWalker} instance with this range as a boundary.
  *
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}.
  */
  getWalker(e = {}) {
    return e.boundaries = this, new Rs(e);
  }
  /**
  * Returns a {@link module:engine/view/node~Node} or {@link module:engine/view/documentfragment~DocumentFragment}
  * which is a common ancestor of range's both ends (in which the entire range is contained).
  */
  getCommonAncestor() {
    return this.start.getCommonAncestor(this.end);
  }
  /**
  * Returns an {@link module:engine/view/element~Element Element} contained by the range.
  * The element will be returned when it is the **only** node within the range and **fully–contained**
  * at the same time.
  */
  getContainedElement() {
    if (this.isCollapsed)
      return null;
    let e = this.start.nodeAfter, i = this.end.nodeBefore;
    return this.start.parent.is("$text") && this.start.isAtEnd && this.start.parent.nextSibling && (e = this.start.parent.nextSibling), this.end.parent.is("$text") && this.end.isAtStart && this.end.parent.previousSibling && (i = this.end.parent.previousSibling), e && e.is("element") && e === i ? e : null;
  }
  /**
  * Clones this range.
  */
  clone() {
    return new rs(this.start, this.end);
  }
  /**
  * Returns an iterator that iterates over all {@link module:engine/view/item~Item view items} that are in this range and returns
  * them.
  *
  * This method uses {@link module:engine/view/treewalker~TreeWalker} with `boundaries` set to this range and `ignoreElementEnd` option
  * set to `true`. However it returns only {@link module:engine/view/item~Item items},
  * not {@link module:engine/view/treewalker~TreeWalkerValue}.
  *
  * You may specify additional options for the tree walker. See {@link module:engine/view/treewalker~TreeWalker} for
  * a full list of available options.
  *
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}.
  */
  *getItems(e = {}) {
    e.boundaries = this, e.ignoreElementEnd = !0;
    const i = new Rs(e);
    for (const s of i)
      yield s.item;
  }
  /**
  * Returns an iterator that iterates over all {@link module:engine/view/position~Position positions} that are boundaries or
  * contained in this range.
  *
  * This method uses {@link module:engine/view/treewalker~TreeWalker} with `boundaries` set to this range. However it returns only
  * {@link module:engine/view/position~Position positions}, not {@link module:engine/view/treewalker~TreeWalkerValue}.
  *
  * You may specify additional options for the tree walker. See {@link module:engine/view/treewalker~TreeWalker} for
  * a full list of available options.
  *
  * @param options Object with configuration options. See {@link module:engine/view/treewalker~TreeWalker}.
  */
  *getPositions(e = {}) {
    e.boundaries = this;
    const i = new Rs(e);
    yield i.position;
    for (const s of i)
      yield s.nextPosition;
  }
  /**
  * Checks and returns whether this range intersects with the given range.
  *
  * @param otherRange Range to compare with.
  * @returns True if ranges intersect.
  */
  isIntersecting(e) {
    return this.start.isBefore(e.end) && this.end.isAfter(e.start);
  }
  /**
  * Creates a range from the given parents and offsets.
  *
  * @internal
  * @param startElement Start position parent element.
  * @param startOffset Start position offset.
  * @param endElement End position parent element.
  * @param endOffset End position offset.
  * @returns Created range.
  */
  static _createFromParentsAndOffsets(e, i, s, r) {
    return new this(new re(e, i), new re(s, r));
  }
  /**
  * Creates a new range, spreading from specified {@link module:engine/view/position~Position position} to a position moved by
  * given `shift`. If `shift` is a negative value, shifted position is treated as the beginning of the range.
  *
  * @internal
  * @param position Beginning of the range.
  * @param shift How long the range should be.
  */
  static _createFromPositionAndShift(e, i) {
    const s = e, r = e.getShiftedBy(i);
    return i > 0 ? new this(s, r) : new this(r, s);
  }
  /**
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @internal
  * @param element Element which is a parent for the range.
  */
  static _createIn(e) {
    return this._createFromParentsAndOffsets(e, 0, e, e.childCount);
  }
  /**
  * Creates a range that starts before given {@link module:engine/view/item~Item view item} and ends after it.
  *
  * @internal
  */
  static _createOn(e) {
    const i = e.is("$textProxy") ? e.offsetSize : 1;
    return this._createFromPositionAndShift(re._createBefore(e), i);
  }
};
Re.prototype.is = function(n) {
  return n === "range" || n === "view:range";
};
function il(n) {
  return !!(n.item.is("attributeElement") || n.item.is("uiElement"));
}
let Dn = class y_ extends (/* @__PURE__ */ Je(Gs)) {
  /**
  * Creates new selection instance.
  *
  * **Note**: The selection constructor is available as a factory method:
  *
  * * {@link module:engine/view/view~View#createSelection `View#createSelection()`},
  * * {@link module:engine/view/upcastwriter~UpcastWriter#createSelection `UpcastWriter#createSelection()`}.
  *
  * ```ts
  * // Creates empty selection without ranges.
  * const selection = writer.createSelection();
  *
  * // Creates selection at the given range.
  * const range = writer.createRange( start, end );
  * const selection = writer.createSelection( range );
  *
  * // Creates selection at the given ranges
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( star2, end2 ) ];
  * const selection = writer.createSelection( ranges );
  *
  * // Creates selection from the other selection.
  * const otherSelection = writer.createSelection();
  * const selection = writer.createSelection( otherSelection );
  *
  * // Creates selection from the document selection.
  * const selection = writer.createSelection( editor.editing.view.document.selection );
  *
  * // Creates selection at the given position.
  * const position = writer.createPositionFromPath( root, path );
  * const selection = writer.createSelection( position );
  *
  * // Creates collapsed selection at the position of given item and offset.
  * const paragraph = writer.createContainerElement( 'paragraph' );
  * const selection = writer.createSelection( paragraph, offset );
  *
  * // Creates a range inside an {@link module:engine/view/element~Element element} which starts before the
  * // first child of that element and ends after the last child of that element.
  * const selection = writer.createSelection( paragraph, 'in' );
  *
  * // Creates a range on an {@link module:engine/view/item~Item item} which starts before the item and ends
  * // just after the item.
  * const selection = writer.createSelection( paragraph, 'on' );
  * ```
  *
  * `Selection`'s constructor allow passing additional options (`backward`, `fake` and `label`) as the last argument.
  *
  * ```ts
  * // Creates backward selection.
  * const selection = writer.createSelection( range, { backward: true } );
  * ```
  *
  * Fake selection does not render as browser native selection over selected elements and is hidden to the user.
  * This way, no native selection UI artifacts are displayed to the user and selection over elements can be
  * represented in other way, for example by applying proper CSS class.
  *
  * Additionally fake's selection label can be provided. It will be used to describe fake selection in DOM
  * (and be  properly handled by screen readers).
  *
  * ```ts
  * // Creates fake selection with label.
  * const selection = writer.createSelection( range, { fake: true, label: 'foo' } );
  * ```
  *
  * @internal
  */
  constructor(...e) {
    super();
    /**
    * Stores all ranges that are selected.
    */
    f(this, "_ranges");
    /**
    * Specifies whether the last added range was added as a backward or forward range.
    */
    f(this, "_lastRangeBackward");
    /**
    * Specifies whether selection instance is fake.
    */
    f(this, "_isFake");
    /**
    * Fake selection's label.
    */
    f(this, "_fakeSelectionLabel");
    this._ranges = [], this._lastRangeBackward = !1, this._isFake = !1, this._fakeSelectionLabel = "", e.length && this.setTo(...e);
  }
  /**
  * Returns true if selection instance is marked as `fake`.
  *
  * @see #setTo
  */
  get isFake() {
    return this._isFake;
  }
  /**
  * Returns fake selection label.
  *
  * @see #setTo
  */
  get fakeSelectionLabel() {
    return this._fakeSelectionLabel;
  }
  /**
  * Selection anchor. Anchor may be described as a position where the selection starts. Together with
  * {@link #focus focus} they define the direction of selection, which is important
  * when expanding/shrinking selection. Anchor is always the start or end of the most recent added range.
  * It may be a bit unintuitive when there are multiple ranges in selection.
  *
  * @see #focus
  */
  get anchor() {
    if (!this._ranges.length)
      return null;
    const e = this._ranges[this._ranges.length - 1];
    return (this._lastRangeBackward ? e.end : e.start).clone();
  }
  /**
  * Selection focus. Focus is a position where the selection ends.
  *
  * @see #anchor
  */
  get focus() {
    if (!this._ranges.length)
      return null;
    const e = this._ranges[this._ranges.length - 1];
    return (this._lastRangeBackward ? e.start : e.end).clone();
  }
  /**
  * Returns whether the selection is collapsed. Selection is collapsed when there is exactly one range which is
  * collapsed.
  */
  get isCollapsed() {
    return this.rangeCount === 1 && this._ranges[0].isCollapsed;
  }
  /**
  * Returns number of ranges in selection.
  */
  get rangeCount() {
    return this._ranges.length;
  }
  /**
  * Specifies whether the {@link #focus} precedes {@link #anchor}.
  */
  get isBackward() {
    return !this.isCollapsed && this._lastRangeBackward;
  }
  /**
  * {@link module:engine/view/editableelement~EditableElement EditableElement} instance that contains this selection, or `null`
  * if the selection is not inside an editable element.
  */
  get editableElement() {
    return this.anchor ? this.anchor.editableElement : null;
  }
  /**
  * Returns an iterable that contains copies of all ranges added to the selection.
  */
  *getRanges() {
    for (const e of this._ranges)
      yield e.clone();
  }
  /**
  * Returns copy of the first range in the selection. First range is the one which
  * {@link module:engine/view/range~Range#start start} position {@link module:engine/view/position~Position#isBefore is before} start
  * position of all other ranges (not to confuse with the first range added to the selection).
  * Returns `null` if no ranges are added to selection.
  */
  getFirstRange() {
    let e = null;
    for (const i of this._ranges)
      (!e || i.start.isBefore(e.start)) && (e = i);
    return e ? e.clone() : null;
  }
  /**
  * Returns copy of the last range in the selection. Last range is the one which {@link module:engine/view/range~Range#end end}
  * position {@link module:engine/view/position~Position#isAfter is after} end position of all other ranges (not to confuse
  * with the last range added to the selection). Returns `null` if no ranges are added to selection.
  */
  getLastRange() {
    let e = null;
    for (const i of this._ranges)
      (!e || i.end.isAfter(e.end)) && (e = i);
    return e ? e.clone() : null;
  }
  /**
  * Returns copy of the first position in the selection. First position is the position that
  * {@link module:engine/view/position~Position#isBefore is before} any other position in the selection ranges.
  * Returns `null` if no ranges are added to selection.
  */
  getFirstPosition() {
    const e = this.getFirstRange();
    return e ? e.start.clone() : null;
  }
  /**
  * Returns copy of the last position in the selection. Last position is the position that
  * {@link module:engine/view/position~Position#isAfter is after} any other position in the selection ranges.
  * Returns `null` if no ranges are added to selection.
  */
  getLastPosition() {
    const e = this.getLastRange();
    return e ? e.end.clone() : null;
  }
  /**
  * Checks whether, this selection is equal to given selection. Selections are equal if they have same directions,
  * same number of ranges and all ranges from one selection equal to a range from other selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are equal, `false` otherwise.
  */
  isEqual(e) {
    if (this.isFake != e.isFake || this.isFake && this.fakeSelectionLabel != e.fakeSelectionLabel || this.rangeCount != e.rangeCount)
      return !1;
    if (this.rangeCount === 0)
      return !0;
    if (!this.anchor.isEqual(e.anchor) || !this.focus.isEqual(e.focus))
      return !1;
    for (const i of this._ranges) {
      let s = !1;
      for (const r of e._ranges)
        if (i.isEqual(r)) {
          s = !0;
          break;
        }
      if (!s)
        return !1;
    }
    return !0;
  }
  /**
  * Checks whether this selection is similar to given selection. Selections are similar if they have same directions, same
  * number of ranges, and all {@link module:engine/view/range~Range#getTrimmed trimmed} ranges from one selection are
  * equal to any trimmed range from other selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are similar, `false` otherwise.
  */
  isSimilar(e) {
    if (this.isBackward != e.isBackward)
      return !1;
    const i = ih(this.getRanges()), s = ih(e.getRanges());
    if (i != s)
      return !1;
    if (i == 0)
      return !0;
    for (let r of this.getRanges()) {
      r = r.getTrimmed();
      let o = !1;
      for (let a of e.getRanges())
        if (a = a.getTrimmed(), r.start.isEqual(a.start) && r.end.isEqual(a.end)) {
          o = !0;
          break;
        }
      if (!o)
        return !1;
    }
    return !0;
  }
  /**
  * Returns the selected element. {@link module:engine/view/element~Element Element} is considered as selected if there is only
  * one range in the selection, and that range contains exactly one element.
  * Returns `null` if there is no selected element.
  */
  getSelectedElement() {
    return this.rangeCount !== 1 ? null : this.getFirstRange().getContainedElement();
  }
  /**
  * Sets this selection's ranges and direction to the specified location based on the given
  * {@link module:engine/view/selection~Selectable selectable}.
  *
  * ```ts
  * // Sets selection to the given range.
  * const range = writer.createRange( start, end );
  * selection.setTo( range );
  *
  * // Sets selection to given ranges.
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( star2, end2 ) ];
  * selection.setTo( range );
  *
  * // Sets selection to the other selection.
  * const otherSelection = writer.createSelection();
  * selection.setTo( otherSelection );
  *
  * // Sets selection to contents of DocumentSelection.
  * selection.setTo( editor.editing.view.document.selection );
  *
  * // Sets collapsed selection at the given position.
  * const position = writer.createPositionAt( root, path );
  * selection.setTo( position );
  *
  * // Sets collapsed selection at the position of given item and offset.
  * selection.setTo( paragraph, offset );
  * ```
  *
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * ```ts
  * selection.setTo( paragraph, 'in' );
  * ```
  *
  * Creates a range on an {@link module:engine/view/item~Item item} which starts before the item and ends just after the item.
  *
  * ```ts
  * selection.setTo( paragraph, 'on' );
  *
  * // Clears selection. Removes all ranges.
  * selection.setTo( null );
  * ```
  *
  * `Selection#setTo()` method allow passing additional options (`backward`, `fake` and `label`) as the last argument.
  *
  * ```ts
  * // Sets selection as backward.
  * selection.setTo( range, { backward: true } );
  * ```
  *
  * Fake selection does not render as browser native selection over selected elements and is hidden to the user.
  * This way, no native selection UI artifacts are displayed to the user and selection over elements can be
  * represented in other way, for example by applying proper CSS class.
  *
  * Additionally fake's selection label can be provided. It will be used to describe fake selection in DOM
  * (and be  properly handled by screen readers).
  *
  * ```ts
  * // Creates fake selection with label.
  * selection.setTo( range, { fake: true, label: 'foo' } );
  * ```
  *
  * @fires change
  */
  setTo(...e) {
    let [i, s, r] = e;
    if (typeof s == "object" && (r = s, s = void 0), i === null)
      this._setRanges([]), this._setFakeOptions(r);
    else if (i instanceof y_ || i instanceof Ef)
      this._setRanges(i.getRanges(), i.isBackward), this._setFakeOptions({
        fake: i.isFake,
        label: i.fakeSelectionLabel
      });
    else if (i instanceof Re)
      this._setRanges([
        i
      ], r && r.backward), this._setFakeOptions(r);
    else if (i instanceof re)
      this._setRanges([
        new Re(i)
      ]), this._setFakeOptions(r);
    else if (i instanceof Ks) {
      const o = !!r && !!r.backward;
      let a;
      if (s === void 0)
        throw new S("view-selection-setto-required-second-parameter", this);
      s == "in" ? a = Re._createIn(i) : s == "on" ? a = Re._createOn(i) : a = new Re(re._createAt(i, s)), this._setRanges([
        a
      ], o), this._setFakeOptions(r);
    } else if (Ai(i))
      this._setRanges(i, r && r.backward), this._setFakeOptions(r);
    else
      throw new S("view-selection-setto-not-selectable", this);
    this.fire("change");
  }
  /**
  * Moves {@link #focus} to the specified location.
  *
  * The location can be specified in the same form as {@link module:engine/view/view~View#createPositionAt view.createPositionAt()}
  * parameters.
  *
  * @fires change
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  setFocus(e, i) {
    if (this.anchor === null)
      throw new S("view-selection-setfocus-no-ranges", this);
    const s = re._createAt(e, i);
    if (s.compareWith(this.focus) == "same")
      return;
    const r = this.anchor;
    this._ranges.pop(), s.compareWith(r) == "before" ? this._addRange(new Re(s, r), !0) : this._addRange(new Re(r, s)), this.fire("change");
  }
  /**
  * Replaces all ranges that were added to the selection with given array of ranges. Last range of the array
  * is treated like the last added range and is used to set {@link #anchor anchor} and {@link #focus focus}.
  * Accepts a flag describing in which way the selection is made.
  *
  * @param newRanges Iterable object of ranges to set.
  * @param isLastBackward Flag describing if last added range was selected forward - from start to end
  * (`false`) or backward - from end to start (`true`). Defaults to `false`.
  */
  _setRanges(e, i = !1) {
    e = Array.from(e), this._ranges = [];
    for (const s of e)
      this._addRange(s);
    this._lastRangeBackward = !!i;
  }
  /**
  * Sets this selection instance to be marked as `fake`. A fake selection does not render as browser native selection
  * over selected elements and is hidden to the user. This way, no native selection UI artifacts are displayed to
  * the user and selection over elements can be represented in other way, for example by applying proper CSS class.
  *
  * Additionally fake's selection label can be provided. It will be used to describe fake selection in DOM (and be
  * properly handled by screen readers).
  */
  _setFakeOptions(e = {}) {
    this._isFake = !!e.fake, this._fakeSelectionLabel = e.fake && e.label || "";
  }
  /**
  * Adds a range to the selection. Added range is copied. This means that passed range is not saved in the
  * selection instance and you can safely operate on it.
  *
  * Accepts a flag describing in which way the selection is made - passed range might be selected from
  * {@link module:engine/view/range~Range#start start} to {@link module:engine/view/range~Range#end end}
  * or from {@link module:engine/view/range~Range#end end} to {@link module:engine/view/range~Range#start start}.
  * The flag is used to set {@link #anchor anchor} and {@link #focus focus} properties.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-selection-range-intersects` if added range intersects
  * with ranges already stored in Selection instance.
  */
  _addRange(e, i = !1) {
    if (!(e instanceof Re))
      throw new S("view-selection-add-range-not-range", this);
    this._pushRange(e), this._lastRangeBackward = !!i;
  }
  /**
  * Adds range to selection - creates copy of given range so it can be safely used and modified.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-selection-range-intersects` if added range intersects
  * with ranges already stored in selection instance.
  */
  _pushRange(e) {
    for (const i of this._ranges)
      if (e.isIntersecting(i))
        throw new S("view-selection-range-intersects", this, {
          addedRange: e,
          intersectingRange: i
        });
    this._ranges.push(new Re(e.start, e.end));
  }
};
Dn.prototype.is = function(n) {
  return n === "selection" || n === "view:selection";
};
let Ef = class extends (/* @__PURE__ */ Je(Gs)) {
  constructor(...e) {
    super();
    /**
    * Selection is used internally (`DocumentSelection` is a proxy to that selection).
    */
    f(this, "_selection");
    this._selection = new Dn(), this._selection.delegate("change").to(this), e.length && this._selection.setTo(...e);
  }
  /**
  * Returns true if selection instance is marked as `fake`.
  *
  * @see #_setTo
  */
  get isFake() {
    return this._selection.isFake;
  }
  /**
  * Returns fake selection label.
  *
  * @see #_setTo
  */
  get fakeSelectionLabel() {
    return this._selection.fakeSelectionLabel;
  }
  /**
  * Selection anchor. Anchor may be described as a position where the selection starts. Together with
  * {@link #focus focus} they define the direction of selection, which is important
  * when expanding/shrinking selection. Anchor is always the start or end of the most recent added range.
  * It may be a bit unintuitive when there are multiple ranges in selection.
  *
  * @see #focus
  */
  get anchor() {
    return this._selection.anchor;
  }
  /**
  * Selection focus. Focus is a position where the selection ends.
  *
  * @see #anchor
  */
  get focus() {
    return this._selection.focus;
  }
  /**
  * Returns whether the selection is collapsed. Selection is collapsed when there is exactly one range which is
  * collapsed.
  */
  get isCollapsed() {
    return this._selection.isCollapsed;
  }
  /**
  * Returns number of ranges in selection.
  */
  get rangeCount() {
    return this._selection.rangeCount;
  }
  /**
  * Specifies whether the {@link #focus} precedes {@link #anchor}.
  */
  get isBackward() {
    return this._selection.isBackward;
  }
  /**
  * {@link module:engine/view/editableelement~EditableElement EditableElement} instance that contains this selection, or `null`
  * if the selection is not inside an editable element.
  */
  get editableElement() {
    return this._selection.editableElement;
  }
  /**
  * Used for the compatibility with the {@link module:engine/view/selection~Selection#isEqual} method.
  *
  * @internal
  */
  get _ranges() {
    return this._selection._ranges;
  }
  /**
  * Returns an iterable that contains copies of all ranges added to the selection.
  */
  *getRanges() {
    yield* this._selection.getRanges();
  }
  /**
  * Returns copy of the first range in the selection. First range is the one which
  * {@link module:engine/view/range~Range#start start} position {@link module:engine/view/position~Position#isBefore is before} start
  * position of all other ranges (not to confuse with the first range added to the selection).
  * Returns `null` if no ranges are added to selection.
  */
  getFirstRange() {
    return this._selection.getFirstRange();
  }
  /**
  * Returns copy of the last range in the selection. Last range is the one which {@link module:engine/view/range~Range#end end}
  * position {@link module:engine/view/position~Position#isAfter is after} end position of all other ranges (not to confuse
  * with the last range added to the selection). Returns `null` if no ranges are added to selection.
  */
  getLastRange() {
    return this._selection.getLastRange();
  }
  /**
  * Returns copy of the first position in the selection. First position is the position that
  * {@link module:engine/view/position~Position#isBefore is before} any other position in the selection ranges.
  * Returns `null` if no ranges are added to selection.
  */
  getFirstPosition() {
    return this._selection.getFirstPosition();
  }
  /**
  * Returns copy of the last position in the selection. Last position is the position that
  * {@link module:engine/view/position~Position#isAfter is after} any other position in the selection ranges.
  * Returns `null` if no ranges are added to selection.
  */
  getLastPosition() {
    return this._selection.getLastPosition();
  }
  /**
  * Returns the selected element. {@link module:engine/view/element~Element Element} is considered as selected if there is only
  * one range in the selection, and that range contains exactly one element.
  * Returns `null` if there is no selected element.
  */
  getSelectedElement() {
    return this._selection.getSelectedElement();
  }
  /**
  * Checks whether, this selection is equal to given selection. Selections are equal if they have same directions,
  * same number of ranges and all ranges from one selection equal to a range from other selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are equal, `false` otherwise.
  */
  isEqual(e) {
    return this._selection.isEqual(e);
  }
  /**
  * Checks whether this selection is similar to given selection. Selections are similar if they have same directions, same
  * number of ranges, and all {@link module:engine/view/range~Range#getTrimmed trimmed} ranges from one selection are
  * equal to any trimmed range from other selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are similar, `false` otherwise.
  */
  isSimilar(e) {
    return this._selection.isSimilar(e);
  }
  /**
  * Sets this selection's ranges and direction to the specified location based on the given
  * {@link module:engine/view/selection~Selectable selectable}.
  *
  * ```ts
  * // Sets selection to the given range.
  * const range = writer.createRange( start, end );
  * documentSelection._setTo( range );
  *
  * // Sets selection to given ranges.
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( start2, end2 ) ];
  * documentSelection._setTo( range );
  *
  * // Sets selection to the other selection.
  * const otherSelection = writer.createSelection();
  * documentSelection._setTo( otherSelection );
  *
  * // Sets collapsed selection at the given position.
  * const position = writer.createPositionAt( root, offset );
  * documentSelection._setTo( position );
  *
  * // Sets collapsed selection at the position of given item and offset.
  * documentSelection._setTo( paragraph, offset );
  * ```
  *
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * ```ts
  * documentSelection._setTo( paragraph, 'in' );
  * ```
  *
  * Creates a range on an {@link module:engine/view/item~Item item} which starts before the item and ends just after the item.
  *
  * ```ts
  * documentSelection._setTo( paragraph, 'on' );
  *
  * // Clears selection. Removes all ranges.
  * documentSelection._setTo( null );
  * ```
  *
  * `Selection#_setTo()` method allow passing additional options (`backward`, `fake` and `label`) as the last argument.
  *
  * ```ts
  * // Sets selection as backward.
  * documentSelection._setTo( range, { backward: true } );
  * ```
  *
  * Fake selection does not render as browser native selection over selected elements and is hidden to the user.
  * This way, no native selection UI artifacts are displayed to the user and selection over elements can be
  * represented in other way, for example by applying proper CSS class.
  *
  * Additionally fake's selection label can be provided. It will be used to des cribe fake selection in DOM
  * (and be  properly handled by screen readers).
  *
  * ```ts
  * // Creates fake selection with label.
  * documentSelection._setTo( range, { fake: true, label: 'foo' } );
  * ```
  *
  * @internal
  * @fires change
  */
  _setTo(...e) {
    this._selection.setTo(...e);
  }
  /**
  * Moves {@link #focus} to the specified location.
  *
  * The location can be specified in the same form as {@link module:engine/view/view~View#createPositionAt view.createPositionAt()}
  * parameters.
  *
  * @internal
  * @fires change
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  _setFocus(e, i) {
    this._selection.setFocus(e, i);
  }
};
Ef.prototype.is = function(n) {
  return n === "selection" || n == "documentSelection" || n == "view:selection" || n == "view:documentSelection";
};
class io extends jn {
  /**
  * @param source The emitter.
  * @param name The event name.
  * @param startRange The view range that the bubbling should start from.
  */
  constructor(e, i, s) {
    super(e, i);
    /**
    * The view range that the bubbling should start from.
    */
    f(this, "startRange");
    /**
    * The current event phase.
    */
    f(this, "_eventPhase");
    /**
    * The current bubbling target.
    */
    f(this, "_currentTarget");
    this.startRange = s, this._eventPhase = "none", this._currentTarget = null;
  }
  /**
  * The current event phase.
  */
  get eventPhase() {
    return this._eventPhase;
  }
  /**
  * The current bubbling target.
  */
  get currentTarget() {
    return this._currentTarget;
  }
}
const Du = Symbol("bubbling contexts");
function ah(n) {
  class t extends n {
    fire(i, ...s) {
      try {
        const r = i instanceof jn ? i : new jn(this, i), o = $u(this);
        if (!o.size)
          return;
        if (go(r, "capturing", this), cr(o, "$capture", r, ...s))
          return r.return;
        const a = r.startRange || this.selection.getFirstRange(), l = a ? a.getContainedElement() : null, c = l ? !!k_(o, l) : !1;
        let u = l || R6(a);
        if (go(r, "atTarget", u), !c) {
          if (cr(o, "$text", r, ...s))
            return r.return;
          go(r, "bubbling", u);
        }
        for (; u; ) {
          if (u.is("rootElement")) {
            if (cr(o, "$root", r, ...s))
              return r.return;
          } else if (u.is("element") && cr(o, u.name, r, ...s))
            return r.return;
          if (cr(o, u, r, ...s))
            return r.return;
          u = u.parent, go(r, "bubbling", u);
        }
        return go(r, "bubbling", this), cr(o, "$document", r, ...s), r.return;
      } catch (r) {
        /* istanbul ignore next -- @preserve */
        S.rethrowUnexpectedError(r, this);
      }
    }
    _addEventListener(i, s, r) {
      const o = Ue(r.context || "$document"), a = $u(this);
      for (const l of o) {
        let c = a.get(l);
        c || (c = new (Je())(), a.set(l, c)), this.listenTo(c, i, s, r);
      }
    }
    _removeEventListener(i, s) {
      const r = $u(this);
      for (const o of r.values())
        this.stopListening(o, i, s);
    }
  }
  return t;
}
{
  const n = ah(Object);
  [
    "fire",
    "_addEventListener",
    "_removeEventListener"
  ].forEach((t) => {
    ah[t] = n.prototype[t];
  });
}
function go(n, t, e) {
  n instanceof io && (n._eventPhase = t, n._currentTarget = e);
}
function cr(n, t, e, ...i) {
  const s = typeof t == "string" ? n.get(t) : k_(n, t);
  return s ? (s.fire(e, ...i), e.stop.called) : !1;
}
function k_(n, t) {
  for (const [e, i] of n)
    if (typeof e == "function" && e(t))
      return i;
  return null;
}
function $u(n) {
  return n[Du] || (n[Du] = /* @__PURE__ */ new Map()), n[Du];
}
function R6(n) {
  if (!n)
    return null;
  const t = n.start.parent, e = n.end.parent, i = t.getPath(), s = e.getPath();
  return i.length > s.length ? t : e;
}
let Gc = class extends (/* @__PURE__ */ ah(/* @__PURE__ */ je())) {
  /**
  * Creates a Document instance.
  *
  * @param stylesProcessor The styles processor instance.
  */
  constructor(e) {
    super();
    /**
    * Selection done on this document.
    */
    f(this, "selection");
    /**
    * Roots of the view tree. Collection of the {@link module:engine/view/element~Element view elements}.
    *
    * View roots are created as a result of binding between {@link module:engine/view/document~Document#roots} and
    * {@link module:engine/model/document~Document#roots} and this is handled by
    * {@link module:engine/controller/editingcontroller~EditingController}, so to create view root we need to create
    * model root using {@link module:engine/model/document~Document#createRoot}.
    */
    f(this, "roots");
    /**
    * The styles processor instance used by this document when normalizing styles.
    */
    f(this, "stylesProcessor");
    /**
    * Post-fixer callbacks registered to the view document.
    */
    f(this, "_postFixers", /* @__PURE__ */ new Set());
    this.selection = new Ef(), this.roots = new bt({
      idProperty: "rootName"
    }), this.stylesProcessor = e, this.set("isReadOnly", !1), this.set("isFocused", !1), this.set("isSelecting", !1), this.set("isComposing", !1);
  }
  /**
  * Gets a {@link module:engine/view/document~Document#roots view root element} with the specified name. If the name is not
  * specific "main" root is returned.
  *
  * @param name Name of the root.
  * @returns The view root element with the specified name or null when there is no root of given name.
  */
  getRoot(e = "main") {
    return this.roots.get(e);
  }
  /**
  * Allows registering post-fixer callbacks. A post-fixers mechanism allows to update the view tree just before it is rendered
  * to the DOM.
  *
  * Post-fixers are executed right after all changes from the outermost change block were applied but
  * before the {@link module:engine/view/view~View#event:render render event} is fired. If a post-fixer callback made
  * a change, it should return `true`. When this happens, all post-fixers are fired again to check if something else should
  * not be fixed in the new document tree state.
  *
  * View post-fixers are useful when you want to apply some fixes whenever the view structure changes. Keep in mind that
  * changes executed in a view post-fixer should not break model-view mapping.
  *
  * The types of changes which should be safe:
  *
  * * adding or removing attribute from elements,
  * * changes inside of {@link module:engine/view/uielement~UIElement UI elements},
  * * {@link module:engine/controller/editingcontroller~EditingController#reconvertItem marking some of the model elements to be
  * re-converted}.
  *
  * Try to avoid changes which touch view structure:
  *
  * * you should not add or remove nor wrap or unwrap any view elements,
  * * you should not change the editor data model in a view post-fixer.
  *
  * As a parameter, a post-fixer callback receives a {@link module:engine/view/downcastwriter~DowncastWriter downcast writer}.
  *
  * Typically, a post-fixer will look like this:
  *
  * ```ts
  * editor.editing.view.document.registerPostFixer( writer => {
  * 	if ( checkSomeCondition() ) {
  * 		writer.doSomething();
  *
  * 		// Let other post-fixers know that something changed.
  * 		return true;
  * 	}
  *
  * 	return false;
  * } );
  * ```
  *
  * Note that nothing happens right after you register a post-fixer (e.g. execute such a code in the console).
  * That is because adding a post-fixer does not execute it.
  * The post-fixer will be executed as soon as any change in the document needs to cause its rendering.
  * If you want to re-render the editor's view after registering the post-fixer then you should do it manually by calling
  * {@link module:engine/view/view~View#forceRender `view.forceRender()`}.
  *
  * If you need to register a callback which is executed when DOM elements are already updated,
  * use {@link module:engine/view/view~View#event:render render event}.
  */
  registerPostFixer(e) {
    this._postFixers.add(e);
  }
  /**
  * Destroys this instance. Makes sure that all observers are destroyed and listeners removed.
  */
  destroy() {
    this.roots.forEach((e) => e.destroy()), this.stopListening();
  }
  /**
  * Performs post-fixer loops. Executes post-fixer callbacks as long as none of them has done any changes to the model.
  *
  * @internal
  */
  _callPostFixers(e) {
    let i = !1;
    do
      for (const s of this._postFixers)
        if (i = s(e), i)
          break;
    while (i);
  }
};
const wp = 10;
class Vs extends Un {
  /**
  * Creates an attribute element.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createAttributeElement
  * @see module:engine/view/element~Element
  * @protected
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(e, i, s, r) {
    super(e, i, s, r);
    /**
    * Element priority. Decides in what order elements are wrapped by {@link module:engine/view/downcastwriter~DowncastWriter}.
    *
    * @internal
    * @readonly
    */
    f(this, "_priority", wp);
    /**
    * Element identifier. If set, it is used by {@link module:engine/view/element~Element#isSimilar},
    * and then two elements are considered similar if, and only if they have the same `_id`.
    *
    * @internal
    * @readonly
    */
    f(this, "_id", null);
    /**
    * Keeps all the attribute elements that have the same {@link module:engine/view/attributeelement~AttributeElement#id ids}
    * and still exist in the view tree.
    *
    * This property is managed by {@link module:engine/view/downcastwriter~DowncastWriter}.
    */
    f(this, "_clonesGroup", null);
    this.getFillerOffset = O6;
  }
  /**
  * Element priority. Decides in what order elements are wrapped by {@link module:engine/view/downcastwriter~DowncastWriter}.
  */
  get priority() {
    return this._priority;
  }
  /**
  * Element identifier. If set, it is used by {@link module:engine/view/element~Element#isSimilar},
  * and then two elements are considered similar if, and only if they have the same `id`.
  */
  get id() {
    return this._id;
  }
  /**
  * Returns all {@link module:engine/view/attributeelement~AttributeElement attribute elements} that has the
  * same {@link module:engine/view/attributeelement~AttributeElement#id id} and are in the view tree (were not removed).
  *
  * Note: If this element has been removed from the tree, returned set will not include it.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError attribute-element-get-elements-with-same-id-no-id}
  * if this element has no `id`.
  *
  * @returns Set containing all the attribute elements
  * with the same `id` that were added and not removed from the view tree.
  */
  getElementsWithSameId() {
    if (this.id === null)
      throw new S("attribute-element-get-elements-with-same-id-no-id", this);
    return new Set(this._clonesGroup);
  }
  /**
  * Checks if this element is similar to other element.
  *
  * If none of elements has set {@link module:engine/view/attributeelement~AttributeElement#id}, then both elements
  * should have the same name, attributes and priority to be considered as similar. Two similar elements can contain
  * different set of children nodes.
  *
  * If at least one element has {@link module:engine/view/attributeelement~AttributeElement#id} set, then both
  * elements have to have the same {@link module:engine/view/attributeelement~AttributeElement#id} value to be
  * considered similar.
  *
  * Similarity is important for {@link module:engine/view/downcastwriter~DowncastWriter}. For example:
  *
  * * two following similar elements can be merged together into one, longer element,
  * * {@link module:engine/view/downcastwriter~DowncastWriter#unwrap} checks similarity of passed element and processed element to
  * decide whether processed element should be unwrapped,
  * * etc.
  */
  isSimilar(e) {
    return this.id !== null || e.id !== null ? this.id === e.id : super.isSimilar(e) && this.priority == e.priority;
  }
  /**
  * Clones provided element with priority.
  *
  * @internal
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any children.
  * @returns Clone of this element.
  */
  _clone(e = !1) {
    const i = super._clone(e);
    return i._priority = this._priority, i._id = this._id, i;
  }
  /**
  * Used by {@link module:engine/view/element~Element#_mergeAttributesFrom} to verify if the given element can be merged without
  * conflicts into this element.
  *
  * @internal
  */
  _canMergeAttributesFrom(e) {
    return this.id !== null || e.id !== null || this.priority !== e.priority ? !1 : super._canMergeAttributesFrom(e);
  }
  /**
  * Used by {@link module:engine/view/element~Element#_subtractAttributesOf} to verify if the given element attributes
  * can be fully subtracted from this element.
  *
  * @internal
  */
  _canSubtractAttributesOf(e) {
    return this.id !== null || e.id !== null || this.priority !== e.priority ? !1 : super._canSubtractAttributesOf(e);
  }
}
f(Vs, "DEFAULT_PRIORITY", wp);
Vs.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "attributeElement" || n === "view:attributeElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "element" || n === "view:element") : n === "attributeElement" || n === "view:attributeElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "element" || n === "view:element" || n === "node" || n === "view:node";
};
function O6() {
  if (zu(this))
    return null;
  let n = this.parent;
  for (; n && n.is("attributeElement"); ) {
    if (zu(n) > 1)
      return null;
    n = n.parent;
  }
  return !n || zu(n) > 1 ? null : this.childCount;
}
function zu(n) {
  return Array.from(n.getChildren()).filter((t) => !t.is("uiElement")).length;
}
class Sf extends Un {
  /**
  * Creates new instance of EmptyElement.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-emptyelement-cannot-add` when third parameter is passed,
  * to inform that usage of EmptyElement is incorrect (adding child nodes to EmptyElement is forbidden).
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createEmptyElement
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, i, s) {
    super(t, e, i, s), this.getFillerOffset = V6;
  }
  /**
  * Overrides {@link module:engine/view/element~Element#_insertChild} method.
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-emptyelement-cannot-add` to prevent
  * adding any child nodes to EmptyElement.
  *
  * @internal
  */
  _insertChild(t, e) {
    if (e && (e instanceof Ks || Array.from(e).length > 0))
      throw new S("view-emptyelement-cannot-add", [
        this,
        e
      ]);
    return 0;
  }
}
Sf.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "emptyElement" || n === "view:emptyElement" || n === "element" || n === "view:element") : n === "emptyElement" || n === "view:emptyElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "element" || n === "view:element" || n === "node" || n === "view:node";
};
function V6() {
  return null;
}
class Kc extends Un {
  /**
  * Creates new instance of UIElement.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-uielement-cannot-add` when third parameter is passed,
  * to inform that usage of UIElement is incorrect (adding child nodes to UIElement is forbidden).
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createUIElement
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, i, s) {
    super(t, e, i, s), this.getFillerOffset = B6;
  }
  /**
  * Overrides {@link module:engine/view/element~Element#_insertChild} method.
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-uielement-cannot-add` to prevent adding any child nodes
  * to UIElement.
  *
  * @internal
  */
  _insertChild(t, e) {
    if (e && (e instanceof Ks || Array.from(e).length > 0))
      throw new S("view-uielement-cannot-add", [
        this,
        e
      ]);
    return 0;
  }
  /**
  * Renders this {@link module:engine/view/uielement~UIElement} to DOM. This method is called by
  * {@link module:engine/view/domconverter~DomConverter}.
  * Do not use inheritance to create custom rendering method, replace `render()` method instead:
  *
  * ```ts
  * const myUIElement = downcastWriter.createUIElement( 'span' );
  * myUIElement.render = function( domDocument, domConverter ) {
  * 	const domElement = this.toDomElement( domDocument );
  *
  * 	domConverter.setContentOf( domElement, '<b>this is ui element</b>' );
  *
  * 	return domElement;
  * };
  * ```
  *
  * If changes in your UI element should trigger some editor UI update you should call
  * the {@link module:ui/editorui/editorui~EditorUI#update `editor.ui.update()`} method
  * after rendering your UI element.
  *
  * @param domConverter Instance of the DomConverter used to optimize the output.
  */
  render(t, e) {
    return this.toDomElement(t);
  }
  /**
  * Creates DOM element based on this view UIElement.
  * Note that each time this method is called new DOM element is created.
  */
  toDomElement(t) {
    const e = t.createElement(this.name);
    for (const i of this.getAttributeKeys())
      e.setAttribute(i, this.getAttribute(i));
    return e;
  }
}
Kc.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "uiElement" || n === "view:uiElement" || n === "element" || n === "view:element") : n === "uiElement" || n === "view:uiElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "element" || n === "view:element" || n === "node" || n === "view:node";
};
function L6(n) {
  n.document.on("arrowKey", (t, e) => M6(t, e, n.domConverter), {
    priority: "low"
  });
}
function B6() {
  return null;
}
function M6(n, t, e) {
  if (t.keyCode == Ie.arrowright) {
    const i = t.domTarget.ownerDocument.defaultView.getSelection(), s = i.rangeCount == 1 && i.getRangeAt(0).collapsed;
    if (s || t.shiftKey) {
      const r = i.focusNode, o = i.focusOffset, a = e.domPositionToView(r, o);
      if (a === null)
        return;
      let l = !1;
      const c = a.getLastMatchingPosition((u) => (u.item.is("uiElement") && (l = !0), !!(u.item.is("uiElement") || u.item.is("attributeElement"))));
      if (l) {
        const u = e.viewPositionToDom(c);
        s ? i.collapse(u.parent, u.offset) : i.extend(u.parent, u.offset);
      }
    }
  }
}
class Af extends Un {
  /**
  * Creates a new instance of a raw element.
  *
  * Throws the `view-rawelement-cannot-add` {@link module:utils/ckeditorerror~CKEditorError CKEditorError} when the `children`
  * parameter is passed to inform that the usage of `RawElement` is incorrect (adding child nodes to `RawElement` is forbidden).
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#createRawElement
  * @internal
  * @param document The document instance to which this element belongs.
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  */
  constructor(t, e, i, s) {
    super(t, e, i, s), this.getFillerOffset = N6;
  }
  /**
  * Overrides the {@link module:engine/view/element~Element#_insertChild} method.
  * Throws the `view-rawelement-cannot-add` {@link module:utils/ckeditorerror~CKEditorError CKEditorError} to prevent
  * adding any child nodes to a raw element.
  *
  * @internal
  */
  _insertChild(t, e) {
    if (e && (e instanceof Ks || Array.from(e).length > 0))
      throw new S("view-rawelement-cannot-add", [
        this,
        e
      ]);
    return 0;
  }
  /**
  * This allows rendering the children of a {@link module:engine/view/rawelement~RawElement} on the DOM level.
  * This method is called by the {@link module:engine/view/domconverter~DomConverter} with the raw DOM element
  * passed as an argument, leaving the number and shape of the children up to the integrator.
  *
  * This method **must be defined** for the raw element to work:
  *
  * ```ts
  * const myRawElement = downcastWriter.createRawElement( 'div' );
  *
  * myRawElement.render = function( domElement, domConverter ) {
  * 	domConverter.setContentOf( domElement, '<b>This is the raw content of myRawElement.</b>' );
  * };
  * ```
  *
  * @param domElement The native DOM element representing the raw view element.
  * @param domConverter Instance of the DomConverter used to optimize the output.
  */
  render(t, e) {
  }
}
Af.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "rawElement" || n === "view:rawElement" || n === "element" || n === "view:element") : n === "rawElement" || n === "view:rawElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === this.name || n === "view:" + this.name || n === "element" || n === "view:element" || n === "node" || n === "view:node";
};
function N6() {
  return null;
}
let Ls = class extends (/* @__PURE__ */ Je(Gs)) {
  /**
  * Creates new DocumentFragment instance.
  *
  * @internal
  * @param document The document to which this document fragment belongs.
  * @param children A list of nodes to be inserted into the created document fragment.
  */
  constructor(e, i) {
    super();
    /**
    * The document to which this document fragment belongs.
    */
    f(this, "document");
    /**
    * Array of child nodes.
    */
    f(this, "_children", []);
    /**
    * Map of custom properties.
    * Custom properties can be added to document fragment instance.
    */
    f(this, "_customProperties", /* @__PURE__ */ new Map());
    this.document = e, i && this._insertChild(0, i);
  }
  /**
  * Iterable interface.
  *
  * Iterates over nodes added to this document fragment.
  */
  [Symbol.iterator]() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Number of child nodes in this document fragment.
  */
  get childCount() {
    return this._children.length;
  }
  /**
  * Is `true` if there are no nodes inside this document fragment, `false` otherwise.
  */
  get isEmpty() {
    return this.childCount === 0;
  }
  /**
  * Artificial root of `DocumentFragment`. Returns itself. Added for compatibility reasons.
  */
  get root() {
    return this;
  }
  /**
  * Artificial parent of `DocumentFragment`. Returns `null`. Added for compatibility reasons.
  */
  get parent() {
    return null;
  }
  /**
  * Artificial element name. Returns `undefined`. Added for compatibility reasons.
  */
  get name() {
  }
  /**
  * Artificial element getFillerOffset. Returns `undefined`. Added for compatibility reasons.
  */
  get getFillerOffset() {
  }
  /**
  * Returns the custom property value for the given key.
  */
  getCustomProperty(e) {
    return this._customProperties.get(e);
  }
  /**
  * Returns an iterator which iterates over this document fragment's custom properties.
  * Iterator provides `[ key, value ]` pairs for each stored property.
  */
  *getCustomProperties() {
    yield* this._customProperties.entries();
  }
  /**
  * {@link module:engine/view/documentfragment~DocumentFragment#_insertChild Insert} a child node or a list of child nodes at the end
  * and sets the parent of these nodes to this fragment.
  *
  * @internal
  * @param items Items to be inserted.
  * @returns Number of appended nodes.
  */
  _appendChild(e) {
    return this._insertChild(this.childCount, e);
  }
  /**
  * Gets child at the given index.
  *
  * @param index Index of child.
  * @returns Child node.
  */
  getChild(e) {
    return this._children[e];
  }
  /**
  * Gets index of the given child node. Returns `-1` if child node is not found.
  *
  * @param node Child node.
  * @returns Index of the child node.
  */
  getChildIndex(e) {
    return this._children.indexOf(e);
  }
  /**
  * Gets child nodes iterator.
  *
  * @returns Child nodes iterator.
  */
  getChildren() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Inserts a child node or a list of child nodes on the given index and sets the parent of these nodes to
  * this fragment.
  *
  * @internal
  * @param index Position where nodes should be inserted.
  * @param items Items to be inserted.
  * @returns Number of inserted nodes.
  */
  _insertChild(e, i) {
    this._fireChange("children", this, {
      index: e
    });
    let s = 0;
    const r = F6(this.document, i);
    for (const o of r)
      o.parent !== null && o._remove(), o.parent = this, this._children.splice(e, 0, o), e++, s++;
    return s;
  }
  /**
  * Removes number of child nodes starting at the given index and set the parent of these nodes to `null`.
  *
  * @internal
  * @param index Number of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @returns The array of removed nodes.
  */
  _removeChildren(e, i = 1) {
    this._fireChange("children", this, {
      index: e
    });
    for (let s = e; s < e + i; s++)
      this._children[s].parent = null;
    return this._children.splice(e, i);
  }
  /**
  * @internal
  * @param type Type of the change.
  * @param node Changed node.
  * @param data Additional data.
  * @fires module:engine/view/node~Node#event:change
  */
  _fireChange(e, i, s) {
    this.fire(`change:${e}`, i, s);
  }
  /**
  * Sets a custom property. They can be used to add special data to elements.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#setCustomProperty
  * @internal
  */
  _setCustomProperty(e, i) {
    this._customProperties.set(e, i);
  }
  /**
  * Removes the custom property stored under the given key.
  *
  * @see module:engine/view/downcastwriter~DowncastWriter#removeCustomProperty
  * @internal
  * @returns Returns true if property was removed.
  */
  _removeCustomProperty(e) {
    return this._customProperties.delete(e);
  }
};
Ls.prototype.is = function(n) {
  return n === "documentFragment" || n === "view:documentFragment";
};
function F6(n, t) {
  return typeof t == "string" ? [
    new qt(n, t)
  ] : (Ai(t) || (t = [
    t
  ]), Array.from(t).map((e) => typeof e == "string" ? new qt(n, e) : e instanceof Mn ? new qt(n, e.data) : e));
}
class C_ {
  /**
  * @param document The view document instance.
  */
  constructor(t) {
    /**
    * The view document instance in which this writer operates.
    */
    f(this, "document");
    /**
    * Holds references to the attribute groups that share the same {@link module:engine/view/attributeelement~AttributeElement#id id}.
    * The keys are `id`s, the values are `Set`s holding {@link module:engine/view/attributeelement~AttributeElement}s.
    */
    f(this, "_cloneGroups", /* @__PURE__ */ new Map());
    /**
    * The slot factory used by the `elementToStructure` downcast helper.
    */
    f(this, "_slotFactory", null);
    this.document = t;
  }
  setSelection(...t) {
    this.document.selection._setTo(...t);
  }
  /**
  * Moves {@link module:engine/view/documentselection~DocumentSelection#focus selection's focus} to the specified location.
  *
  * The location can be specified in the same form as {@link module:engine/view/view~View#createPositionAt view.createPositionAt()}
  * parameters.
  *
  * @param Offset or one of the flags. Used only when the first parameter is a {@link module:engine/view/item~Item view item}.
  */
  setSelectionFocus(t, e) {
    this.document.selection._setFocus(t, e);
  }
  /**
  * Creates a new {@link module:engine/view/documentfragment~DocumentFragment} instance.
  *
  * @param children A list of nodes to be inserted into the created document fragment.
  * @returns The created document fragment.
  */
  createDocumentFragment(t) {
    return new Ls(this.document, t);
  }
  /**
  * Creates a new {@link module:engine/view/text~Text text node}.
  *
  * ```ts
  * writer.createText( 'foo' );
  * ```
  *
  * @param data The text's data.
  * @returns The created text node.
  */
  createText(t) {
    return new qt(this.document, t);
  }
  /**
  * Creates a new {@link module:engine/view/attributeelement~AttributeElement}.
  *
  * ```ts
  * writer.createAttributeElement( 'strong' );
  * writer.createAttributeElement( 'a', { href: 'foo.bar' } );
  *
  * // Make `<a>` element contain other attributes element so the `<a>` element is not broken.
  * writer.createAttributeElement( 'a', { href: 'foo.bar' }, { priority: 5 } );
  *
  * // Set `id` of a marker element so it is not joined or merged with "normal" elements.
  * writer.createAttributeElement( 'span', { class: 'my-marker' }, { id: 'marker:my' } );
  * ```
  *
  * @param name Name of the element.
  * @param attributes Element's attributes.
  * @param options Element's options.
  * @param options.priority Element's {@link module:engine/view/attributeelement~AttributeElement#priority priority}.
  * @param options.id Element's {@link module:engine/view/attributeelement~AttributeElement#id id}.
  * @param options.renderUnsafeAttributes A list of attribute names that should be rendered in the editing
  * pipeline even though they would normally be filtered out by unsafe attribute detection mechanisms.
  * @returns Created element.
  */
  createAttributeElement(t, e, i = {}) {
    const s = new Vs(this.document, t, e);
    return typeof i.priority == "number" && (s._priority = i.priority), i.id && (s._id = i.id), i.renderUnsafeAttributes && s._unsafeAttributesToRender.push(...i.renderUnsafeAttributes), s;
  }
  createContainerElement(t, e, i = {}, s = {}) {
    let r = null;
    qi(i) ? s = i : r = i;
    const o = new sa(this.document, t, e, r);
    return s.renderUnsafeAttributes && o._unsafeAttributesToRender.push(...s.renderUnsafeAttributes), o;
  }
  /**
  * Creates a new {@link module:engine/view/editableelement~EditableElement}.
  *
  * ```ts
  * writer.createEditableElement( 'div' );
  * writer.createEditableElement( 'div', { id: 'foo-1234' } );
  * ```
  *
  * Note: The editable element is to be used in the editing pipeline. Usually, together with
  * {@link module:widget/utils~toWidgetEditable `toWidgetEditable()`}.
  *
  * @param name Name of the element.
  * @param attributes Elements attributes.
  * @param options Element's options.
  * @param options.renderUnsafeAttributes A list of attribute names that should be rendered in the editing
  * pipeline even though they would normally be filtered out by unsafe attribute detection mechanisms.
  * @returns Created element.
  */
  createEditableElement(t, e, i = {}) {
    const s = new qc(this.document, t, e);
    return i.renderUnsafeAttributes && s._unsafeAttributesToRender.push(...i.renderUnsafeAttributes), s;
  }
  /**
  * Creates a new {@link module:engine/view/emptyelement~EmptyElement}.
  *
  * ```ts
  * writer.createEmptyElement( 'img' );
  * writer.createEmptyElement( 'img', { id: 'foo-1234' } );
  * ```
  *
  * @param name Name of the element.
  * @param attributes Elements attributes.
  * @param options Element's options.
  * @param options.renderUnsafeAttributes A list of attribute names that should be rendered in the editing
  * pipeline even though they would normally be filtered out by unsafe attribute detection mechanisms.
  * @returns Created element.
  */
  createEmptyElement(t, e, i = {}) {
    const s = new Sf(this.document, t, e);
    return i.renderUnsafeAttributes && s._unsafeAttributesToRender.push(...i.renderUnsafeAttributes), s;
  }
  /**
  * Creates a new {@link module:engine/view/uielement~UIElement}.
  *
  * ```ts
  * writer.createUIElement( 'span' );
  * writer.createUIElement( 'span', { id: 'foo-1234' } );
  * ```
  *
  * A custom render function can be provided as the third parameter:
  *
  * ```ts
  * writer.createUIElement( 'span', null, function( domDocument ) {
  * 	const domElement = this.toDomElement( domDocument );
  * 	domElement.innerHTML = '<b>this is ui element</b>';
  *
  * 	return domElement;
  * } );
  * ```
  *
  * Unlike {@link #createRawElement raw elements}, UI elements are by no means editor content, for instance,
  * they are ignored by the editor selection system.
  *
  * You should not use UI elements as data containers. Check out {@link #createRawElement} instead.
  *
  * @param name The name of the element.
  * @param attributes Element attributes.
  * @param renderFunction A custom render function.
  * @returns The created element.
  */
  createUIElement(t, e, i) {
    const s = new Kc(this.document, t, e);
    return i && (s.render = i), s;
  }
  /**
  * Creates a new {@link module:engine/view/rawelement~RawElement}.
  *
  * ```ts
  * writer.createRawElement( 'span', { id: 'foo-1234' }, function( domElement ) {
  * 	domElement.innerHTML = '<b>This is the raw content of the raw element.</b>';
  * } );
  * ```
  *
  * Raw elements work as data containers ("wrappers", "sandboxes") but their children are not managed or
  * even recognized by the editor. This encapsulation allows integrations to maintain custom DOM structures
  * in the editor content without, for instance, worrying about compatibility with other editor features.
  * Raw elements are a perfect tool for integration with external frameworks and data sources.
  *
  * Unlike {@link #createUIElement UI elements}, raw elements act like "real" editor content (similar to
  * {@link module:engine/view/containerelement~ContainerElement} or {@link module:engine/view/emptyelement~EmptyElement}),
  * and they are considered by the editor selection.
  *
  * You should not use raw elements to render the UI in the editor content. Check out {@link #createUIElement `#createUIElement()`}
  * instead.
  *
  * @param name The name of the element.
  * @param attributes Element attributes.
  * @param renderFunction A custom render function.
  * @param options Element's options.
  * @param options.renderUnsafeAttributes A list of attribute names that should be rendered in the editing
  * pipeline even though they would normally be filtered out by unsafe attribute detection mechanisms.
  * @returns The created element.
  */
  createRawElement(t, e, i, s = {}) {
    const r = new Af(this.document, t, e);
    return i && (r.render = i), s.renderUnsafeAttributes && r._unsafeAttributesToRender.push(...s.renderUnsafeAttributes), r;
  }
  setAttribute(t, e, i, s) {
    s !== void 0 ? s._setAttribute(t, e, i) : i._setAttribute(t, e);
  }
  removeAttribute(t, e, i) {
    i !== void 0 ? i._removeAttribute(t, e) : e._removeAttribute(t);
  }
  /**
  * Adds specified class to the element.
  *
  * ```ts
  * writer.addClass( 'foo', linkElement );
  * writer.addClass( [ 'foo', 'bar' ], linkElement );
  * ```
  */
  addClass(t, e) {
    e._addClass(t);
  }
  /**
  * Removes specified class from the element.
  *
  * ```ts
  * writer.removeClass( 'foo', linkElement );
  * writer.removeClass( [ 'foo', 'bar' ], linkElement );
  * ```
  */
  removeClass(t, e) {
    e._removeClass(t);
  }
  setStyle(t, e, i) {
    qi(t) && i === void 0 ? e._setStyle(t) : i._setStyle(t, e);
  }
  /**
  * Removes specified style from the element.
  *
  * ```ts
  * writer.removeStyle( 'color', element ); // Removes 'color' style.
  * writer.removeStyle( [ 'color', 'border-top' ], element ); // Removes both 'color' and 'border-top' styles.
  * ```
  *
  * **Note**: This method can work with normalized style names if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#remove `StylesMap#remove()`} for details.
  */
  removeStyle(t, e) {
    e._removeStyle(t);
  }
  /**
  * Sets a custom property on element. Unlike attributes, custom properties are not rendered to the DOM,
  * so they can be used to add special data to elements.
  */
  setCustomProperty(t, e, i) {
    i._setCustomProperty(t, e);
  }
  /**
  * Removes a custom property stored under the given key.
  *
  * @returns Returns true if property was removed.
  */
  removeCustomProperty(t, e) {
    return e._removeCustomProperty(t);
  }
  /**
  * Breaks attribute elements at the provided position or at the boundaries of a provided range. It breaks attribute elements
  * up to their first ancestor that is a container element.
  *
  * In following examples `<p>` is a container, `<b>` and `<u>` are attribute elements:
  *
  * ```html
  * <p>foo<b><u>bar{}</u></b></p> -> <p>foo<b><u>bar</u></b>[]</p>
  * <p>foo<b><u>{}bar</u></b></p> -> <p>foo{}<b><u>bar</u></b></p>
  * <p>foo<b><u>b{}ar</u></b></p> -> <p>foo<b><u>b</u></b>[]<b><u>ar</u></b></p>
  * <p><b>fo{o</b><u>ba}r</u></p> -> <p><b>fo</b><b>o</b><u>ba</u><u>r</u></b></p>
  * ```
  *
  * **Note:** {@link module:engine/view/documentfragment~DocumentFragment DocumentFragment} is treated like a container.
  *
  * **Note:** The difference between {@link module:engine/view/downcastwriter~DowncastWriter#breakAttributes breakAttributes()} and
  * {@link module:engine/view/downcastwriter~DowncastWriter#breakContainer breakContainer()} is that `breakAttributes()` breaks all
  * {@link module:engine/view/attributeelement~AttributeElement attribute elements} that are ancestors of a given `position`,
  * up to the first encountered {@link module:engine/view/containerelement~ContainerElement container element}.
  * `breakContainer()` assumes that a given `position` is directly in the container element and breaks that container element.
  *
  * Throws the `view-writer-invalid-range-container` {@link module:utils/ckeditorerror~CKEditorError CKEditorError}
  * when the {@link module:engine/view/range~Range#start start}
  * and {@link module:engine/view/range~Range#end end} positions of a passed range are not placed inside same parent container.
  *
  * Throws the `view-writer-cannot-break-empty-element` {@link module:utils/ckeditorerror~CKEditorError CKEditorError}
  * when trying to break attributes inside an {@link module:engine/view/emptyelement~EmptyElement EmptyElement}.
  *
  * Throws the `view-writer-cannot-break-ui-element` {@link module:utils/ckeditorerror~CKEditorError CKEditorError}
  * when trying to break attributes inside a {@link module:engine/view/uielement~UIElement UIElement}.
  *
  * @see module:engine/view/attributeelement~AttributeElement
  * @see module:engine/view/containerelement~ContainerElement
  * @see module:engine/view/downcastwriter~DowncastWriter#breakContainer
  * @param positionOrRange The position where to break attribute elements.
  * @returns The new position or range, after breaking the attribute elements.
  */
  breakAttributes(t) {
    return t instanceof re ? this._breakAttributes(t) : this._breakAttributesRange(t);
  }
  /**
  * Breaks a {@link module:engine/view/containerelement~ContainerElement container view element} into two, at the given position.
  * The position has to be directly inside the container element and cannot be in the root. It does not break the conrainer view element
  * if the position is at the beginning or at the end of its parent element.
  *
  * ```html
  * <p>foo^bar</p> -> <p>foo</p><p>bar</p>
  * <div><p>foo</p>^<p>bar</p></div> -> <div><p>foo</p></div><div><p>bar</p></div>
  * <p>^foobar</p> -> ^<p>foobar</p>
  * <p>foobar^</p> -> <p>foobar</p>^
  * ```
  *
  * **Note:** The difference between {@link module:engine/view/downcastwriter~DowncastWriter#breakAttributes breakAttributes()} and
  * {@link module:engine/view/downcastwriter~DowncastWriter#breakContainer breakContainer()} is that `breakAttributes()` breaks all
  * {@link module:engine/view/attributeelement~AttributeElement attribute elements} that are ancestors of a given `position`,
  * up to the first encountered {@link module:engine/view/containerelement~ContainerElement container element}.
  * `breakContainer()` assumes that the given `position` is directly in the container element and breaks that container element.
  *
  * @see module:engine/view/attributeelement~AttributeElement
  * @see module:engine/view/containerelement~ContainerElement
  * @see module:engine/view/downcastwriter~DowncastWriter#breakAttributes
  * @param position The position where to break the element.
  * @returns The position between broken elements. If an element has not been broken,
  * the returned position is placed either before or after it.
  */
  breakContainer(t) {
    const e = t.parent;
    if (!e.is("containerElement"))
      throw new S("view-writer-break-non-container-element", this.document);
    if (!e.parent)
      throw new S("view-writer-break-root", this.document);
    if (t.isAtStart)
      return re._createBefore(e);
    if (!t.isAtEnd) {
      const i = e._clone(!1);
      this.insert(re._createAfter(e), i);
      const s = new Re(t, re._createAt(e, "end")), r = new re(i, 0);
      this.move(s, r);
    }
    return re._createAfter(e);
  }
  /**
  * Merges {@link module:engine/view/attributeelement~AttributeElement attribute elements}. It also merges text nodes if needed.
  * Only {@link module:engine/view/attributeelement~AttributeElement#isSimilar similar} attribute elements can be merged.
  *
  * In following examples `<p>` is a container and `<b>` is an attribute element:
  *
  * ```html
  * <p>foo[]bar</p> -> <p>foo{}bar</p>
  * <p><b>foo</b>[]<b>bar</b></p> -> <p><b>foo{}bar</b></p>
  * <p><b foo="bar">a</b>[]<b foo="baz">b</b></p> -> <p><b foo="bar">a</b>[]<b foo="baz">b</b></p>
  * ```
  *
  * It will also take care about empty attributes when merging:
  *
  * ```html
  * <p><b>[]</b></p> -> <p>[]</p>
  * <p><b>foo</b><i>[]</i><b>bar</b></p> -> <p><b>foo{}bar</b></p>
  * ```
  *
  * **Note:** Difference between {@link module:engine/view/downcastwriter~DowncastWriter#mergeAttributes mergeAttributes} and
  * {@link module:engine/view/downcastwriter~DowncastWriter#mergeContainers mergeContainers} is that `mergeAttributes` merges two
  * {@link module:engine/view/attributeelement~AttributeElement attribute elements} or {@link module:engine/view/text~Text text nodes}
  * while `mergeContainer` merges two {@link module:engine/view/containerelement~ContainerElement container elements}.
  *
  * @see module:engine/view/attributeelement~AttributeElement
  * @see module:engine/view/containerelement~ContainerElement
  * @see module:engine/view/downcastwriter~DowncastWriter#mergeContainers
  * @param position Merge position.
  * @returns Position after merge.
  */
  mergeAttributes(t) {
    const e = t.offset, i = t.parent;
    if (i.is("$text"))
      return t;
    if (i.is("attributeElement") && i.childCount === 0) {
      const o = i.parent, a = i.index;
      return i._remove(), this._removeFromClonedElementsGroup(i), this.mergeAttributes(new re(o, a));
    }
    const s = i.getChild(e - 1), r = i.getChild(e);
    if (!s || !r)
      return t;
    if (s.is("$text") && r.is("$text"))
      return _p(s, r);
    if (s.is("attributeElement") && r.is("attributeElement") && s.isSimilar(r)) {
      const o = s.childCount;
      return s._appendChild(r.getChildren()), r._remove(), this._removeFromClonedElementsGroup(r), this.mergeAttributes(new re(s, o));
    }
    return t;
  }
  /**
  * Merges two {@link module:engine/view/containerelement~ContainerElement container elements} that are before and after given position.
  * Precisely, the element after the position is removed and it's contents are moved to element before the position.
  *
  * ```html
  * <p>foo</p>^<p>bar</p> -> <p>foo^bar</p>
  * <div>foo</div>^<p>bar</p> -> <div>foo^bar</div>
  * ```
  *
  * **Note:** Difference between {@link module:engine/view/downcastwriter~DowncastWriter#mergeAttributes mergeAttributes} and
  * {@link module:engine/view/downcastwriter~DowncastWriter#mergeContainers mergeContainers} is that `mergeAttributes` merges two
  * {@link module:engine/view/attributeelement~AttributeElement attribute elements} or {@link module:engine/view/text~Text text nodes}
  * while `mergeContainer` merges two {@link module:engine/view/containerelement~ContainerElement container elements}.
  *
  * @see module:engine/view/attributeelement~AttributeElement
  * @see module:engine/view/containerelement~ContainerElement
  * @see module:engine/view/downcastwriter~DowncastWriter#mergeAttributes
  * @param position Merge position.
  * @returns Position after merge.
  */
  mergeContainers(t) {
    const e = t.nodeBefore, i = t.nodeAfter;
    if (!e || !i || !e.is("containerElement") || !i.is("containerElement"))
      throw new S("view-writer-merge-containers-invalid-position", this.document);
    const s = e.getChild(e.childCount - 1), r = s instanceof qt ? re._createAt(s, "end") : re._createAt(e, "end");
    return this.move(Re._createIn(i), re._createAt(e, "end")), this.remove(Re._createOn(i)), r;
  }
  /**
  * Inserts a node or nodes at specified position. Takes care about breaking attributes before insertion
  * and merging them afterwards.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-insert-invalid-node` when nodes to insert
  * contains instances that are not {@link module:engine/view/text~Text Texts},
  * {@link module:engine/view/attributeelement~AttributeElement AttributeElements},
  * {@link module:engine/view/containerelement~ContainerElement ContainerElements},
  * {@link module:engine/view/emptyelement~EmptyElement EmptyElements},
  * {@link module:engine/view/rawelement~RawElement RawElements} or
  * {@link module:engine/view/uielement~UIElement UIElements}.
  *
  * @param position Insertion position.
  * @param nodes Node or nodes to insert.
  * @returns Range around inserted nodes.
  */
  insert(t, e) {
    e = Ai(e) ? [
      ...e
    ] : [
      e
    ], x_(e, this.document);
    const i = e.reduce((o, a) => {
      const l = o[o.length - 1], c = !a.is("uiElement");
      return !l || l.breakAttributes != c ? o.push({
        breakAttributes: c,
        nodes: [
          a
        ]
      }) : l.nodes.push(a), o;
    }, []);
    let s = null, r = t;
    for (const { nodes: o, breakAttributes: a } of i) {
      const l = this._insertNodes(r, o, a);
      s || (s = l.start), r = l.end;
    }
    return s ? new Re(s, r) : new Re(t);
  }
  /**
  * Removes provided range from the container.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-invalid-range-container` when
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions are not placed inside
  * same parent container.
  *
  * @param rangeOrItem Range to remove from container
  * or an {@link module:engine/view/item~Item item} to remove. If range is provided, after removing, it will be updated
  * to a collapsed range showing the new position.
  * @returns Document fragment containing removed nodes.
  */
  remove(t) {
    const e = t instanceof Re ? t : Re._createOn(t);
    if (po(e, this.document), e.isCollapsed)
      return new Ls(this.document);
    const { start: i, end: s } = this._breakAttributesRange(e, !0), r = i.parent, o = s.offset - i.offset, a = r._removeChildren(i.offset, o);
    for (const c of a)
      this._removeFromClonedElementsGroup(c);
    const l = this.mergeAttributes(i);
    return e.start = l, e.end = l.clone(), new Ls(this.document, a);
  }
  /**
  * Removes matching elements from given range.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-invalid-range-container` when
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions are not placed inside
  * same parent container.
  *
  * @param range Range to clear.
  * @param element Element to remove.
  */
  clear(t, e) {
    po(t, this.document);
    const i = t.getWalker({
      direction: "backward",
      ignoreElementEnd: !0
    });
    for (const s of i) {
      const r = s.item;
      let o;
      if (r.is("element") && e.isSimilar(r))
        o = Re._createOn(r);
      else if (!s.nextPosition.isAfter(t.start) && r.is("$textProxy")) {
        const a = r.getAncestors().find((l) => l.is("element") && e.isSimilar(l));
        a && (o = Re._createIn(a));
      }
      o && (o.end.isAfter(t.end) && (o.end = t.end), o.start.isBefore(t.start) && (o.start = t.start), this.remove(o));
    }
  }
  /**
  * Moves nodes from provided range to target position.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-invalid-range-container` when
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions are not placed inside
  * same parent container.
  *
  * @param sourceRange Range containing nodes to move.
  * @param targetPosition Position to insert.
  * @returns Range in target container. Inserted nodes are placed between
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions.
  */
  move(t, e) {
    let i;
    if (e.isAfter(t.end)) {
      e = this._breakAttributes(e, !0);
      const s = e.parent, r = s.childCount;
      t = this._breakAttributesRange(t, !0), i = this.remove(t), e.offset += s.childCount - r;
    } else
      i = this.remove(t);
    return this.insert(e, i);
  }
  /**
  * Wraps elements within range with provided {@link module:engine/view/attributeelement~AttributeElement AttributeElement}.
  * If a collapsed range is provided, it will be wrapped only if it is equal to view selection.
  *
  * If a collapsed range was passed and is same as selection, the selection
  * will be moved to the inside of the wrapped attribute element.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-invalid-range-container`
  * when {@link module:engine/view/range~Range#start}
  * and {@link module:engine/view/range~Range#end} positions are not placed inside same parent container.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-wrap-invalid-attribute` when passed attribute element is not
  * an instance of {@link module:engine/view/attributeelement~AttributeElement AttributeElement}.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-wrap-nonselection-collapsed-range` when passed range
  * is collapsed and different than view selection.
  *
  * @param range Range to wrap.
  * @param attribute Attribute element to use as wrapper.
  * @returns range Range after wrapping, spanning over wrapping attribute element.
  */
  wrap(t, e) {
    if (!(e instanceof Vs))
      throw new S("view-writer-wrap-invalid-attribute", this.document);
    if (po(t, this.document), t.isCollapsed) {
      let i = t.start;
      i.parent.is("element") && !D6(i.parent) && (i = i.getLastMatchingPosition((r) => r.item.is("uiElement"))), i = this._wrapPosition(i, e);
      const s = this.document.selection;
      return s.isCollapsed && s.getFirstPosition().isEqual(t.start) && this.setSelection(i), new Re(i);
    } else
      return this._wrapRange(t, e);
  }
  /**
  * Unwraps nodes within provided range from attribute element.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-invalid-range-container` when
  * {@link module:engine/view/range~Range#start start} and {@link module:engine/view/range~Range#end end} positions are not placed inside
  * same parent container.
  */
  unwrap(t, e) {
    if (!(e instanceof Vs))
      throw new S("view-writer-unwrap-invalid-attribute", this.document);
    if (po(t, this.document), t.isCollapsed)
      return t;
    const { start: i, end: s } = this._breakAttributesRange(t, !0), r = i.parent, o = this._unwrapChildren(r, i.offset, s.offset, e), a = this.mergeAttributes(o.start);
    a.isEqual(o.start) || o.end.offset--;
    const l = this.mergeAttributes(o.end);
    return new Re(a, l);
  }
  /**
  * Renames element by creating a copy of renamed element but with changed name and then moving contents of the
  * old element to the new one. Keep in mind that this will invalidate all {@link module:engine/view/position~Position positions} which
  * has renamed element as {@link module:engine/view/position~Position#parent a parent}.
  *
  * New element has to be created because `Element#tagName` property in DOM is readonly.
  *
  * Since this function creates a new element and removes the given one, the new element is returned to keep reference.
  *
  * @param newName New name for element.
  * @param viewElement Element to be renamed.
  * @returns Element created due to rename.
  */
  rename(t, e) {
    const i = new sa(this.document, t, e.getAttributes());
    return this.insert(re._createAfter(e), i), this.move(Re._createIn(e), re._createAt(i, 0)), this.remove(Re._createOn(e)), i;
  }
  /**
  * Cleans up memory by removing obsolete cloned elements group from the writer.
  *
  * Should be used whenever all {@link module:engine/view/attributeelement~AttributeElement attribute elements}
  * with the same {@link module:engine/view/attributeelement~AttributeElement#id id} are going to be removed from the view and
  * the group will no longer be needed.
  *
  * Cloned elements group are not removed automatically in case if the group is still needed after all its elements
  * were removed from the view.
  *
  * Keep in mind that group names are equal to the `id` property of the attribute element.
  *
  * @param groupName Name of the group to clear.
  */
  clearClonedElementsGroup(t) {
    this._cloneGroups.delete(t);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/view/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/view/item~Item view item} and `'before'` or `'after'` (sets position before or after given view item).
  *
  * This method is a shortcut to other constructors such as:
  *
  * * {@link #createPositionBefore},
  * * {@link #createPositionAfter},
  *
  * @param offset Offset or one of the flags. Used only when the first parameter is a {@link module:engine/view/item~Item view item}.
  */
  createPositionAt(t, e) {
    return re._createAt(t, e);
  }
  /**
  * Creates a new position after given view item.
  *
  * @param item View item after which the position should be located.
  */
  createPositionAfter(t) {
    return re._createAfter(t);
  }
  /**
  * Creates a new position before given view item.
  *
  * @param item View item before which the position should be located.
  */
  createPositionBefore(t) {
    return re._createBefore(t);
  }
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * **Note:** This factory method creates its own {@link module:engine/view/position~Position} instances basing on passed values.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at `start` position.
  */
  createRange(t, e) {
    return new Re(t, e);
  }
  /**
  * Creates a range that starts before given {@link module:engine/view/item~Item view item} and ends after it.
  */
  createRangeOn(t) {
    return Re._createOn(t);
  }
  /**
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(t) {
    return Re._createIn(t);
  }
  createSelection(...t) {
    return new Dn(...t);
  }
  /**
  * Creates placeholders for child elements of the {@link module:engine/conversion/downcasthelpers~DowncastHelpers#elementToStructure
  * `elementToStructure()`} conversion helper.
  *
  * ```ts
  * const viewSlot = conversionApi.writer.createSlot();
  * const viewPosition = conversionApi.writer.createPositionAt( viewElement, 0 );
  *
  * conversionApi.writer.insert( viewPosition, viewSlot );
  * ```
  *
  * It could be filtered down to a specific subset of children (only `<foo>` model elements in this case):
  *
  * ```ts
  * const viewSlot = conversionApi.writer.createSlot( node => node.is( 'element', 'foo' ) );
  * const viewPosition = conversionApi.writer.createPositionAt( viewElement, 0 );
  *
  * conversionApi.writer.insert( viewPosition, viewSlot );
  * ```
  *
  * While providing a filtered slot, make sure to provide slots for all child nodes. A single node can not be downcasted into
  * multiple slots.
  *
  * **Note**: You should not change the order of nodes. View elements should be in the same order as model nodes.
  *
  * @param modeOrFilter The filter for child nodes.
  * @returns The slot element to be placed in to the view structure while processing
  * {@link module:engine/conversion/downcasthelpers~DowncastHelpers#elementToStructure `elementToStructure()`}.
  */
  createSlot(t = "children") {
    if (!this._slotFactory)
      throw new S("view-writer-invalid-create-slot-context", this.document);
    return this._slotFactory(this, t);
  }
  /**
  * Registers a slot factory.
  *
  * @internal
  * @param slotFactory The slot factory.
  */
  _registerSlotFactory(t) {
    this._slotFactory = t;
  }
  /**
  * Clears the registered slot factory.
  *
  * @internal
  */
  _clearSlotFactory() {
    this._slotFactory = null;
  }
  /**
  * Inserts a node or nodes at the specified position. Takes care of breaking attributes before insertion
  * and merging them afterwards if requested by the breakAttributes param.
  *
  * @param position Insertion position.
  * @param nodes Node or nodes to insert.
  * @param breakAttributes Whether attributes should be broken.
  * @returns Range around inserted nodes.
  */
  _insertNodes(t, e, i) {
    let s;
    if (i ? s = lh(t) : s = t.parent.is("$text") ? t.parent.parent : t.parent, !s)
      throw new S("view-writer-invalid-position-container", this.document);
    let r;
    i ? r = this._breakAttributes(t, !0) : r = t.parent.is("$text") ? Hu(t) : t;
    const o = s._insertChild(r.offset, e);
    for (const u of e)
      this._addToClonedElementsGroup(u);
    const a = r.getShiftedBy(o), l = this.mergeAttributes(r);
    l.isEqual(r) || a.offset--;
    const c = this.mergeAttributes(a);
    return new Re(l, c);
  }
  /**
  * Wraps children with provided `wrapElement`. Only children contained in `parent` element between
  * `startOffset` and `endOffset` will be wrapped.
  */
  _wrapChildren(t, e, i, s) {
    let r = e;
    const o = [];
    for (; r < i; ) {
      const l = t.getChild(r), c = l.is("$text"), u = l.is("attributeElement");
      if (u && l._canMergeAttributesFrom(s))
        l._mergeAttributesFrom(s), o.push(new re(t, r));
      else if (c || !u || $6(s, l)) {
        const d = s._clone();
        l._remove(), d._appendChild(l), t._insertChild(r, d), this._addToClonedElementsGroup(d), o.push(new re(t, r));
      } else
        this._wrapChildren(l, 0, l.childCount, s);
      r++;
    }
    let a = 0;
    for (const l of o) {
      if (l.offset -= a, l.offset == e)
        continue;
      this.mergeAttributes(l).isEqual(l) || (a++, i--);
    }
    return Re._createFromParentsAndOffsets(t, e, t, i);
  }
  /**
  * Unwraps children from provided `unwrapElement`. Only children contained in `parent` element between
  * `startOffset` and `endOffset` will be unwrapped.
  */
  _unwrapChildren(t, e, i, s) {
    let r = e;
    const o = [];
    for (; r < i; ) {
      const l = t.getChild(r);
      if (!l.is("attributeElement")) {
        r++;
        continue;
      }
      if (l.isSimilar(s)) {
        const c = l.getChildren(), u = l.childCount;
        l._remove(), t._insertChild(r, c), this._removeFromClonedElementsGroup(l), o.push(new re(t, r), new re(t, r + u)), r += u, i += u - 1;
        continue;
      }
      if (l._canSubtractAttributesOf(s)) {
        l._subtractAttributesOf(s), o.push(new re(t, r), new re(t, r + 1)), r++;
        continue;
      }
      this._unwrapChildren(l, 0, l.childCount, s), r++;
    }
    let a = 0;
    for (const l of o) {
      if (l.offset -= a, l.offset == e || l.offset == i)
        continue;
      this.mergeAttributes(l).isEqual(l) || (a++, i--);
    }
    return Re._createFromParentsAndOffsets(t, e, t, i);
  }
  /**
  * Helper function for `view.writer.wrap`. Wraps range with provided attribute element.
  * This method will also merge newly added attribute element with its siblings whenever possible.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-wrap-invalid-attribute` when passed attribute element is not
  * an instance of {@link module:engine/view/attributeelement~AttributeElement AttributeElement}.
  *
  * @returns New range after wrapping, spanning over wrapping attribute element.
  */
  _wrapRange(t, e) {
    const { start: i, end: s } = this._breakAttributesRange(t, !0), r = i.parent, o = this._wrapChildren(r, i.offset, s.offset, e), a = this.mergeAttributes(o.start);
    a.isEqual(o.start) || o.end.offset--;
    const l = this.mergeAttributes(o.end);
    return new Re(a, l);
  }
  /**
  * Helper function for {@link #wrap}. Wraps position with provided attribute element.
  * This method will also merge newly added attribute element with its siblings whenever possible.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError} `view-writer-wrap-invalid-attribute` when passed attribute element is not
  * an instance of {@link module:engine/view/attributeelement~AttributeElement AttributeElement}.
  *
  * @returns New position after wrapping.
  */
  _wrapPosition(t, e) {
    if (e.isSimilar(t.parent))
      return vp(t.clone());
    t.parent.is("$text") && (t = Hu(t));
    const i = this.createAttributeElement("_wrapPosition-fake-element");
    i._priority = Number.POSITIVE_INFINITY, i.isSimilar = () => !1, t.parent._insertChild(t.offset, i);
    const s = new Re(t, t.getShiftedBy(1));
    this.wrap(s, e);
    const r = new re(i.parent, i.index);
    i._remove();
    const o = r.nodeBefore, a = r.nodeAfter;
    return o && o.is("view:$text") && a && a.is("view:$text") ? _p(o, a) : vp(r);
  }
  /**
  * Helper function used by other `DowncastWriter` methods. Breaks attribute elements at the boundaries of given range.
  *
  * @param range Range which `start` and `end` positions will be used to break attributes.
  * @param forceSplitText If set to `true`, will break text nodes even if they are directly in container element.
  * This behavior will result in incorrect view state, but is needed by other view writing methods which then fixes view state.
  * @returns New range with located at break positions.
  */
  _breakAttributesRange(t, e = !1) {
    const i = t.start, s = t.end;
    if (po(t, this.document), t.isCollapsed) {
      const l = this._breakAttributes(t.start, e);
      return new Re(l, l);
    }
    const r = this._breakAttributes(s, e), o = r.parent.childCount, a = this._breakAttributes(i, e);
    return r.offset += r.parent.childCount - o, new Re(a, r);
  }
  /**
  * Helper function used by other `DowncastWriter` methods. Breaks attribute elements at given position.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-cannot-break-empty-element` when break position
  * is placed inside {@link module:engine/view/emptyelement~EmptyElement EmptyElement}.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `view-writer-cannot-break-ui-element` when break position
  * is placed inside {@link module:engine/view/uielement~UIElement UIElement}.
  *
  * @param position Position where to break attributes.
  * @param forceSplitText If set to `true`, will break text nodes even if they are directly in container element.
  * This behavior will result in incorrect view state, but is needed by other view writing methods which then fixes view state.
  * @returns New position after breaking the attributes.
  */
  _breakAttributes(t, e = !1) {
    const i = t.offset, s = t.parent;
    if (t.parent.is("emptyElement"))
      throw new S("view-writer-cannot-break-empty-element", this.document);
    if (t.parent.is("uiElement"))
      throw new S("view-writer-cannot-break-ui-element", this.document);
    if (t.parent.is("rawElement"))
      throw new S("view-writer-cannot-break-raw-element", this.document);
    if (!e && s.is("$text") && ch(s.parent) || ch(s))
      return t.clone();
    if (s.is("$text"))
      return this._breakAttributes(Hu(t), e);
    const r = s.childCount;
    if (i == r) {
      const o = new re(s.parent, s.index + 1);
      return this._breakAttributes(o, e);
    } else if (i === 0) {
      const o = new re(s.parent, s.index);
      return this._breakAttributes(o, e);
    } else {
      const o = s.index + 1, a = s._clone();
      s.parent._insertChild(o, a), this._addToClonedElementsGroup(a);
      const l = s.childCount - i, c = s._removeChildren(i, l);
      a._appendChild(c);
      const u = new re(s.parent, o);
      return this._breakAttributes(u, e);
    }
  }
  /**
  * Stores the information that an {@link module:engine/view/attributeelement~AttributeElement attribute element} was
  * added to the tree. Saves the reference to the group in the given element and updates the group, so other elements
  * from the group now keep a reference to the given attribute element.
  *
  * The clones group can be obtained using {@link module:engine/view/attributeelement~AttributeElement#getElementsWithSameId}.
  *
  * Does nothing if added element has no {@link module:engine/view/attributeelement~AttributeElement#id id}.
  *
  * @param element Attribute element to save.
  */
  _addToClonedElementsGroup(t) {
    if (!t.root.is("rootElement"))
      return;
    if (t.is("element"))
      for (const s of t.getChildren())
        this._addToClonedElementsGroup(s);
    const e = t.id;
    if (!e)
      return;
    let i = this._cloneGroups.get(e);
    i || (i = /* @__PURE__ */ new Set(), this._cloneGroups.set(e, i)), i.add(t), t._clonesGroup = i;
  }
  /**
  * Removes all the information about the given {@link module:engine/view/attributeelement~AttributeElement attribute element}
  * from its clones group.
  *
  * Keep in mind, that the element will still keep a reference to the group (but the group will not keep a reference to it).
  * This allows to reference the whole group even if the element was already removed from the tree.
  *
  * Does nothing if the element has no {@link module:engine/view/attributeelement~AttributeElement#id id}.
  *
  * @param element Attribute element to remove.
  */
  _removeFromClonedElementsGroup(t) {
    if (t.is("element"))
      for (const s of t.getChildren())
        this._removeFromClonedElementsGroup(s);
    const e = t.id;
    if (!e)
      return;
    const i = this._cloneGroups.get(e);
    i && i.delete(t);
  }
}
function D6(n) {
  return Array.from(n.getChildren()).some((t) => !t.is("uiElement"));
}
function lh(n) {
  let t = n.parent;
  for (; !ch(t); ) {
    if (!t)
      return;
    t = t.parent;
  }
  return t;
}
function $6(n, t) {
  return n.priority < t.priority ? !0 : n.priority > t.priority ? !1 : n.getIdentity() < t.getIdentity();
}
function vp(n) {
  const t = n.nodeBefore;
  if (t && t.is("$text"))
    return new re(t, t.data.length);
  const e = n.nodeAfter;
  return e && e.is("$text") ? new re(e, 0) : n;
}
function Hu(n) {
  if (n.offset == n.parent.data.length)
    return new re(n.parent.parent, n.parent.index + 1);
  if (n.offset === 0)
    return new re(n.parent.parent, n.parent.index);
  const t = n.parent.data.slice(n.offset);
  return n.parent._data = n.parent.data.slice(0, n.offset), n.parent.parent._insertChild(n.parent.index + 1, new qt(n.root.document, t)), new re(n.parent.parent, n.parent.index + 1);
}
function _p(n, t) {
  const e = n.data.length;
  return n._data += t.data, t._remove(), new re(n, e);
}
const z6 = [
  qt,
  Vs,
  sa,
  Sf,
  Af,
  Kc
];
function x_(n, t) {
  for (const e of n) {
    if (!z6.some((i) => e instanceof i))
      throw new S("view-writer-insert-invalid-node-type", t);
    e.is("$text") || x_(e.getChildren(), t);
  }
}
function ch(n) {
  return n && (n.is("containerElement") || n.is("documentFragment"));
}
function po(n, t) {
  const e = lh(n.start), i = lh(n.end);
  if (!e || !i || e !== i)
    throw new S("view-writer-invalid-range-container", t);
}
const T_ = (n) => n.createTextNode(" "), E_ = (n) => {
  const t = n.createElement("span");
  return t.dataset.ckeFiller = "true", t.innerText = " ", t;
}, S_ = (n) => {
  const t = n.createElement("br");
  return t.dataset.ckeFiller = "true", t;
}, Gi = 7, ra = "⁠".repeat(Gi);
function Ti(n) {
  return typeof n == "string" ? n.substr(0, Gi) === ra : mt(n) && n.data.substr(0, Gi) === ra;
}
function Ir(n) {
  return n.data.length == Gi && Ti(n);
}
function yp(n) {
  const t = typeof n == "string" ? n : n.data;
  return Ti(n) ? t.slice(Gi) : t;
}
function H6(n) {
  n.document.on("arrowKey", j6, {
    priority: "low"
  });
}
function j6(n, t) {
  if (t.keyCode == Ie.arrowleft) {
    const e = t.domTarget.ownerDocument.defaultView.getSelection();
    if (e.rangeCount == 1 && e.getRangeAt(0).collapsed) {
      const i = e.getRangeAt(0).startContainer, s = e.getRangeAt(0).startOffset;
      Ti(i) && s <= Gi && e.collapse(i, 0);
    }
  }
}
let U6 = class extends (/* @__PURE__ */ je()) {
  /**
  * Creates a renderer instance.
  *
  * @param domConverter Converter instance.
  * @param selection View selection.
  */
  constructor(e, i) {
    super();
    /**
    * Set of DOM Documents instances.
    */
    f(this, "domDocuments", /* @__PURE__ */ new Set());
    /**
    * Converter instance.
    */
    f(this, "domConverter");
    /**
    * Set of nodes which attributes changed and may need to be rendered.
    */
    f(this, "markedAttributes", /* @__PURE__ */ new Set());
    /**
    * Set of elements which child lists changed and may need to be rendered.
    */
    f(this, "markedChildren", /* @__PURE__ */ new Set());
    /**
    * Set of text nodes which text data changed and may need to be rendered.
    */
    f(this, "markedTexts", /* @__PURE__ */ new Set());
    /**
    * View selection. Renderer updates DOM selection based on the view selection.
    */
    f(this, "selection");
    /**
    * The text node in which the inline filler was rendered.
    */
    f(this, "_inlineFiller", null);
    /**
    * DOM element containing fake selection.
    */
    f(this, "_fakeSelectionContainer", null);
    this.domConverter = e, this.selection = i, this.set("isFocused", !1), this.set("isSelecting", !1), this.set("isComposing", !1), be.isBlink && !be.isAndroid && this.on("change:isSelecting", () => {
      this.isSelecting || this.render();
    });
  }
  /**
  * Marks a view node to be updated in the DOM by {@link #render `render()`}.
  *
  * Note that only view nodes whose parents have corresponding DOM elements need to be marked to be synchronized.
  *
  * @see #markedAttributes
  * @see #markedChildren
  * @see #markedTexts
  *
  * @param type Type of the change.
  * @param node ViewNode to be marked.
  */
  markToSync(e, i) {
    if (e === "text")
      this.domConverter.mapViewToDom(i.parent) && this.markedTexts.add(i);
    else {
      if (!this.domConverter.mapViewToDom(i))
        return;
      if (e === "attributes")
        this.markedAttributes.add(i);
      else if (e === "children")
        this.markedChildren.add(i);
      else
        throw new S("view-renderer-unknown-type", this);
    }
  }
  /**
  * Renders all buffered changes ({@link #markedAttributes}, {@link #markedChildren} and {@link #markedTexts}) and
  * the current view selection (if needed) to the DOM by applying a minimal set of changes to it.
  *
  * Renderer tries not to break the text composition (e.g. IME) and x-index of the selection,
  * so it does as little as it is needed to update the DOM.
  *
  * Renderer also handles {@link module:engine/view/filler fillers}. Especially, it checks if the inline filler is needed
  * at the selection position and adds or removes it. To prevent breaking text composition inline filler will not be
  * removed as long as the selection is in the text node which needed it at first.
  */
  render() {
    if (this.isComposing && !be.isAndroid)
      return;
    let e = null;
    const i = be.isBlink && !be.isAndroid ? !this.isSelecting : !0;
    for (const s of this.markedChildren)
      this._updateChildrenMappings(s);
    i ? (this._inlineFiller && !this._isSelectionInInlineFiller() && this._removeInlineFiller(), this._inlineFiller ? e = this._getInlineFillerPosition() : this._needsInlineFillerAtSelection() && (e = this.selection.getFirstPosition(), this.markedChildren.add(e.parent))) : this._inlineFiller && this._inlineFiller.parentNode && (e = this.domConverter.domPositionToView(this._inlineFiller), e && e.parent.is("$text") && (e = re._createBefore(e.parent)));
    for (const s of this.markedAttributes)
      this._updateAttrs(s);
    for (const s of this.markedChildren)
      this._updateChildren(s, {
        inlineFillerPosition: e
      });
    for (const s of this.markedTexts)
      !this.markedChildren.has(s.parent) && this.domConverter.mapViewToDom(s.parent) && this._updateText(s, {
        inlineFillerPosition: e
      });
    if (i)
      if (e) {
        const s = this.domConverter.viewPositionToDom(e), r = s.parent.ownerDocument;
        Ti(s.parent) ? this._inlineFiller = s.parent : this._inlineFiller = kp(r, s.parent, s.offset);
      } else
        this._inlineFiller = null;
    this._updateFocus(), this._updateSelection(), this.domConverter._clearTemporaryCustomProperties(), this.markedTexts.clear(), this.markedAttributes.clear(), this.markedChildren.clear();
  }
  /**
  * Updates mappings of view element's children.
  *
  * Children that were replaced in the view structure by similar elements (same tag name) are treated as 'replaced'.
  * This means that their mappings can be updated so the new view elements are mapped to the existing DOM elements.
  * Thanks to that these elements do not need to be re-rendered completely.
  *
  * @param viewElement The view element whose children mappings will be updated.
  */
  _updateChildrenMappings(e) {
    const i = this.domConverter.mapViewToDom(e);
    if (!i)
      return;
    const s = Array.from(i.childNodes), r = Array.from(this.domConverter.viewChildrenToDom(e, {
      withChildren: !1
    })), o = this._diffNodeLists(s, r), a = this._findUpdateActions(o, s, r, q6);
    if (a.indexOf("update") !== -1) {
      const l = {
        equal: 0,
        insert: 0,
        delete: 0
      };
      for (const c of a)
        if (c === "update") {
          const u = l.equal + l.insert, d = l.equal + l.delete, h = e.getChild(u);
          h && !h.is("uiElement") && !h.is("rawElement") && this._updateElementMappings(h, s[d]), lp(r[u]), l.equal++;
        } else
          l[c]++;
    }
  }
  /**
  * Updates mappings of a given view element.
  *
  * @param viewElement The view element whose mappings will be updated.
  * @param domElement The DOM element representing the given view element.
  */
  _updateElementMappings(e, i) {
    this.domConverter.unbindDomElement(i), this.domConverter.bindElements(i, e), this.markedChildren.add(e), this.markedAttributes.add(e);
  }
  /**
  * Gets the position of the inline filler based on the current selection.
  * Here, we assume that we know that the filler is needed and
  * {@link #_isSelectionInInlineFiller is at the selection position}, and, since it is needed,
  * it is somewhere at the selection position.
  *
  * Note: The filler position cannot be restored based on the filler's DOM text node, because
  * when this method is called (before rendering), the bindings will often be broken. View-to-DOM
  * bindings are only dependable after rendering.
  */
  _getInlineFillerPosition() {
    const e = this.selection.getFirstPosition();
    return e.parent.is("$text") ? re._createBefore(e.parent) : e;
  }
  /**
  * Returns `true` if the selection has not left the inline filler's text node.
  * If it is `true`, it means that the filler had been added for a reason and the selection did not
  * leave the filler's text node. For example, the user can be in the middle of a composition so it should not be touched.
  *
  * @returns `true` if the inline filler and selection are in the same place.
  */
  _isSelectionInInlineFiller() {
    if (this.selection.rangeCount != 1 || !this.selection.isCollapsed)
      return !1;
    const e = this.selection.getFirstPosition(), i = this.domConverter.viewPositionToDom(e);
    return !!(i && mt(i.parent) && Ti(i.parent));
  }
  /**
  * Removes the inline filler.
  */
  _removeInlineFiller() {
    const e = this._inlineFiller;
    if (!Ti(e))
      throw new S("view-renderer-filler-was-lost", this);
    Ir(e) ? e.remove() : e.data = e.data.substr(Gi), this._inlineFiller = null;
  }
  /**
  * Checks if the inline {@link module:engine/view/filler filler} should be added.
  *
  * @returns `true` if the inline filler should be added.
  */
  _needsInlineFillerAtSelection() {
    if (this.selection.rangeCount != 1 || !this.selection.isCollapsed)
      return !1;
    const e = this.selection.getFirstPosition(), i = e.parent, s = e.offset;
    if (!this.domConverter.mapViewToDom(i.root) || !i.is("element") || !W6(i))
      return !1;
    const r = e.nodeBefore, o = e.nodeAfter;
    return !(r instanceof qt || o instanceof qt || s === i.getFillerOffset() && (!r || !r.is("element", "br")) || be.isAndroid && (r || o));
  }
  /**
  * Checks if text needs to be updated and possibly updates it.
  *
  * @param viewText View text to update.
  * @param options.inlineFillerPosition The position where the inline filler should be rendered.
  */
  _updateText(e, i) {
    const s = this.domConverter.findCorrespondingDomText(e);
    let o = this.domConverter.viewToDom(e).data;
    const a = i.inlineFillerPosition;
    a && a.parent == e.parent && a.offset == e.index && (o = ra + o), this._updateTextNode(s, o);
  }
  /**
  * Checks if attribute list needs to be updated and possibly updates it.
  *
  * @param viewElement The view element to update.
  */
  _updateAttrs(e) {
    const i = this.domConverter.mapViewToDom(e);
    if (i) {
      for (const s of Array.from(i.attributes)) {
        const r = s.name;
        e.hasAttribute(r) || this.domConverter.removeDomElementAttribute(i, r);
      }
      for (const s of e.getAttributeKeys())
        this.domConverter.setDomElementAttribute(i, s, e.getAttribute(s), e);
    }
  }
  /**
  * Checks if elements child list needs to be updated and possibly updates it.
  *
  * Note that on Android, to reduce the risk of composition breaks, it tries to update data of an existing
  * child text nodes instead of replacing them completely.
  *
  * @param viewElement View element to update.
  * @param options.inlineFillerPosition The position where the inline filler should be rendered.
  */
  _updateChildren(e, i) {
    const s = this.domConverter.mapViewToDom(e);
    if (!s)
      return;
    if (be.isAndroid) {
      let h = null;
      for (const m of Array.from(s.childNodes)) {
        if (h && mt(h) && mt(m)) {
          s.normalize();
          break;
        }
        h = m;
      }
    }
    const r = i.inlineFillerPosition, o = s.childNodes, a = Array.from(this.domConverter.viewChildrenToDom(e, {
      bind: !0
    }));
    r && r.parent === e && kp(s.ownerDocument, a, r.offset);
    const l = this._diffNodeLists(o, a), c = this._findUpdateActions(l, o, a, G6);
    let u = 0;
    const d = /* @__PURE__ */ new Set();
    for (const h of c)
      h === "delete" ? (d.add(o[u]), lp(o[u])) : (h === "equal" || h === "update") && u++;
    u = 0;
    for (const h of c)
      h === "insert" ? (u_(s, u, a[u]), u++) : h === "update" ? (this._updateTextNode(o[u], a[u].data), u++) : h === "equal" && (this._markDescendantTextToSync(this.domConverter.domToView(a[u])), u++);
    for (const h of d)
      h.parentNode || this.domConverter.unbindDomElement(h);
  }
  /**
  * Shorthand for diffing two arrays or node lists of DOM nodes.
  *
  * @param actualDomChildren Actual DOM children
  * @param expectedDomChildren Expected DOM children.
  * @returns The list of actions based on the {@link module:utils/diff~diff} function.
  */
  _diffNodeLists(e, i) {
    return e = Y6(e, this._fakeSelectionContainer), Fo(e, i, K6.bind(null, this.domConverter));
  }
  /**
  * Finds DOM nodes that were replaced with the similar nodes (same tag name) in the view. All nodes are compared
  * within one `insert`/`delete` action group, for example:
  *
  * ```
  * Actual DOM:		<p><b>Foo</b>Bar<i>Baz</i><b>Bax</b></p>
  * Expected DOM:	<p>Bar<b>123</b><i>Baz</i><b>456</b></p>
  * Input actions:	[ insert, insert, delete, delete, equal, insert, delete ]
  * Output actions:	[ insert, replace, delete, equal, replace ]
  * ```
  *
  * @param actions Actions array which is a result of the {@link module:utils/diff~diff} function.
  * @param actualDom Actual DOM children
  * @param expectedDom Expected DOM children.
  * @param comparator A comparator function that should return `true` if the given node should be reused
  * (either by the update of a text node data or an element children list for similar elements).
  * @returns Actions array modified with the `update` actions.
  */
  _findUpdateActions(e, i, s, r) {
    if (e.indexOf("insert") === -1 || e.indexOf("delete") === -1)
      return e;
    let o = [], a = [], l = [];
    const c = {
      equal: 0,
      insert: 0,
      delete: 0
    };
    for (const u of e)
      u === "insert" ? l.push(s[c.equal + c.insert]) : u === "delete" ? a.push(i[c.equal + c.delete]) : (o = o.concat(Fo(a, l, r).map((d) => d === "equal" ? "update" : d)), o.push("equal"), a = [], l = []), c[u]++;
    return o.concat(Fo(a, l, r).map((u) => u === "equal" ? "update" : u));
  }
  /**
  * Checks if text needs to be updated and possibly updates it by removing and inserting only parts
  * of the data from the existing text node to reduce impact on the IME composition.
  *
  * @param domText DOM text node to update.
  * @param expectedText The expected data of a text node.
  */
  _updateTextNode(e, i) {
    const s = e.data;
    s != i && (be.isAndroid && this.isComposing && s.replace(/\u00A0/g, " ") == i.replace(/\u00A0/g, " ") || this._updateTextNodeInternal(e, i));
  }
  /**
  * Part of the `_updateTextNode` method extracted for easier testing.
  */
  _updateTextNodeInternal(e, i) {
    const s = Gv(e.data, i);
    for (const r of s)
      r.type === "insert" ? e.insertData(r.index, r.values.join("")) : e.deleteData(r.index, r.howMany);
  }
  /**
  * Marks text nodes to be synchronized.
  *
  * If a text node is passed, it will be marked. If an element is passed, all descendant text nodes inside it will be marked.
  *
  * @param viewNode View node to sync.
  */
  _markDescendantTextToSync(e) {
    if (e) {
      if (e.is("$text"))
        this.markedTexts.add(e);
      else if (e.is("element"))
        for (const i of e.getChildren())
          this._markDescendantTextToSync(i);
    }
  }
  /**
  * Checks if the selection needs to be updated and possibly updates it.
  */
  _updateSelection() {
    if (be.isBlink && !be.isAndroid && this.isSelecting && !this.markedChildren.size)
      return;
    if (this.selection.rangeCount === 0) {
      this._removeDomSelection(), this._removeFakeSelection();
      return;
    }
    const e = this.domConverter.mapViewToDom(this.selection.editableElement);
    !this.isFocused || !e || (this.selection.isFake ? this._updateFakeSelection(e) : this._fakeSelectionContainer && this._fakeSelectionContainer.isConnected ? (this._removeFakeSelection(), this._updateDomSelection(e)) : this.isComposing && be.isAndroid || this._updateDomSelection(e));
  }
  /**
  * Updates the fake selection.
  *
  * @param domRoot A valid DOM root where the fake selection container should be added.
  */
  _updateFakeSelection(e) {
    const i = e.ownerDocument;
    this._fakeSelectionContainer || (this._fakeSelectionContainer = J6(i));
    const s = this._fakeSelectionContainer;
    if (this.domConverter.bindFakeSelection(s, this.selection), !this._fakeSelectionNeedsUpdate(e))
      return;
    (!s.parentElement || s.parentElement != e) && e.appendChild(s), s.textContent = this.selection.fakeSelectionLabel || " ";
    const r = i.getSelection(), o = i.createRange();
    r.removeAllRanges(), o.selectNodeContents(s), r.addRange(o);
  }
  /**
  * Updates the DOM selection.
  *
  * @param domRoot A valid DOM root where the DOM selection should be rendered.
  */
  _updateDomSelection(e) {
    const i = e.ownerDocument.defaultView.getSelection();
    if (!this._domSelectionNeedsUpdate(i))
      return;
    const s = this.domConverter.viewPositionToDom(this.selection.anchor), r = this.domConverter.viewPositionToDom(this.selection.focus);
    i.setBaseAndExtent(s.parent, s.offset, r.parent, r.offset), be.isGecko && Z6(r, i);
  }
  /**
  * Checks whether a given DOM selection needs to be updated.
  *
  * @param domSelection The DOM selection to check.
  */
  _domSelectionNeedsUpdate(e) {
    if (!this.domConverter.isDomSelectionCorrect(e))
      return !0;
    const i = e && this.domConverter.domSelectionToView(e);
    return !(i && this.selection.isEqual(i) || !this.selection.isCollapsed && this.selection.isSimilar(i));
  }
  /**
  * Checks whether the fake selection needs to be updated.
  *
  * @param domRoot A valid DOM root where a new fake selection container should be added.
  */
  _fakeSelectionNeedsUpdate(e) {
    const i = this._fakeSelectionContainer, s = e.ownerDocument.getSelection();
    return !i || i.parentElement !== e || s.anchorNode !== i && !i.contains(s.anchorNode) ? !0 : i.textContent !== this.selection.fakeSelectionLabel;
  }
  /**
  * Removes the DOM selection.
  */
  _removeDomSelection() {
    for (const e of this.domDocuments) {
      const i = e.getSelection();
      if (i.rangeCount) {
        const s = e.activeElement, r = this.domConverter.mapDomToView(s);
        s && r && i.removeAllRanges();
      }
    }
  }
  /**
  * Removes the fake selection.
  */
  _removeFakeSelection() {
    const e = this._fakeSelectionContainer;
    e && e.remove();
  }
  /**
  * Checks if focus needs to be updated and possibly updates it.
  */
  _updateFocus() {
    if (this.isFocused) {
      const e = this.selection.editableElement;
      e && this.domConverter.focus(e);
    }
  }
};
function W6(n) {
  if (n.getAttribute("contenteditable") == "false")
    return !1;
  const t = n.findAncestor((e) => e.hasAttribute("contenteditable"));
  return !t || t.getAttribute("contenteditable") == "true";
}
function kp(n, t, e) {
  const i = t instanceof Array ? t : t.childNodes, s = i[e];
  if (mt(s))
    return s.data = ra + s.data, s;
  {
    const r = n.createTextNode(ra);
    return Array.isArray(t) ? i.splice(e, 0, r) : u_(t, e, r), r;
  }
}
function q6(n, t) {
  return fs(n) && fs(t) && !mt(n) && !mt(t) && !zo(n) && !zo(t) && n.tagName.toLowerCase() === t.tagName.toLowerCase();
}
function G6(n, t) {
  return fs(n) && fs(t) && mt(n) && mt(t);
}
function K6(n, t, e) {
  return t === e ? !0 : mt(t) && mt(e) ? t.data === e.data : !!(n.isBlockFiller(t) && n.isBlockFiller(e));
}
function Z6(n, t) {
  let e = n.parent, i = n.offset;
  if (mt(e) && Ir(e) && (i = Po(e) + 1, e = e.parentNode), e.nodeType != Node.ELEMENT_NODE || i != e.childNodes.length - 1)
    return;
  const s = e.childNodes[i];
  s && s.tagName == "BR" && t.addRange(t.getRangeAt(0));
}
function Y6(n, t) {
  const e = Array.from(n);
  return e.length == 0 || !t || e[e.length - 1] == t && e.pop(), e;
}
function J6(n) {
  const t = n.createElement("div");
  return t.className = "ck-fake-selection-container", Object.assign(t.style, {
    position: "fixed",
    top: 0,
    left: "-9999px",
    // See https://github.com/ckeditor/ckeditor5/issues/752.
    width: "42px"
  }), t.textContent = " ", t;
}
const X6 = S_(ae.document), Q6 = T_(ae.document), e7 = E_(ae.document), nl = "data-ck-unsafe-attribute-", Cp = "data-ck-unsafe-element";
class Zc {
  /**
  * Creates a DOM converter.
  *
  * @param document The view document instance.
  * @param options An object with configuration options.
  * @param options.blockFillerMode The type of the block filler to use.
  * Default value depends on the options.renderingMode:
  *  'nbsp' when options.renderingMode == 'data',
  *  'br' when options.renderingMode == 'editing'.
  * @param options.renderingMode Whether to leave the View-to-DOM conversion result unchanged
  * or improve editing experience by filtering out interactive data.
  */
  constructor(t, { blockFillerMode: e, renderingMode: i = "editing" } = {}) {
    f(this, "document");
    /**
    * Whether to leave the View-to-DOM conversion result unchanged or improve editing experience by filtering out interactive data.
    */
    f(this, "renderingMode");
    /**
    * The mode of a block filler used by the DOM converter.
    */
    f(this, "blockFillerMode");
    /**
    * Elements which are considered pre-formatted elements.
    */
    f(this, "preElements");
    /**
    * Elements which are considered block elements (and hence should be filled with a
    * {@link #isBlockFiller block filler}).
    *
    * Whether an element is considered a block element also affects handling of trailing whitespaces.
    *
    * You can extend this array if you introduce support for block elements which are not yet recognized here.
    */
    f(this, "blockElements");
    /**
    * A list of elements that exist inline (in text) but their inner structure cannot be edited because
    * of the way they are rendered by the browser. They are mostly HTML form elements but there are other
    * elements such as `<img>` or `<iframe>` that also have non-editable children or no children whatsoever.
    *
    * Whether an element is considered an inline object has an impact on white space rendering (trimming)
    * around (and inside of it). In short, white spaces in text nodes next to inline objects are not trimmed.
    *
    * You can extend this array if you introduce support for inline object elements which are not yet recognized here.
    */
    f(this, "inlineObjectElements");
    /**
    * A list of elements which may affect the editing experience. To avoid this, those elements are replaced with
    * `<span data-ck-unsafe-element="[element name]"></span>` while rendering in the editing mode.
    */
    f(this, "unsafeElements");
    /**
    * The DOM Document used to create DOM nodes.
    */
    f(this, "_domDocument");
    /**
    * The DOM-to-view mapping.
    */
    f(this, "_domToViewMapping", /* @__PURE__ */ new WeakMap());
    /**
    * The view-to-DOM mapping.
    */
    f(this, "_viewToDomMapping", /* @__PURE__ */ new WeakMap());
    /**
    * Holds the mapping between fake selection containers and corresponding view selections.
    */
    f(this, "_fakeSelectionMapping", /* @__PURE__ */ new WeakMap());
    /**
    * Matcher for view elements whose content should be treated as raw data
    * and not processed during the conversion from DOM nodes to view elements.
    */
    f(this, "_rawContentElementMatcher", new oi());
    /**
    * Matcher for inline object view elements. This is an extension of a simple {@link #inlineObjectElements} array of element names.
    */
    f(this, "_inlineObjectElementMatcher", new oi());
    /**
    * Set of elements with temporary custom properties that require clearing after render.
    */
    f(this, "_elementsWithTemporaryCustomProperties", /* @__PURE__ */ new Set());
    this.document = t, this.renderingMode = i, this.blockFillerMode = e || (i === "editing" ? "br" : "nbsp"), this.preElements = [
      "pre",
      "textarea"
    ], this.blockElements = [
      "address",
      "article",
      "aside",
      "blockquote",
      "caption",
      "center",
      "dd",
      "details",
      "dir",
      "div",
      "dl",
      "dt",
      "fieldset",
      "figcaption",
      "figure",
      "footer",
      "form",
      "h1",
      "h2",
      "h3",
      "h4",
      "h5",
      "h6",
      "header",
      "hgroup",
      "legend",
      "li",
      "main",
      "menu",
      "nav",
      "ol",
      "p",
      "pre",
      "section",
      "summary",
      "table",
      "tbody",
      "td",
      "tfoot",
      "th",
      "thead",
      "tr",
      "ul"
    ], this.inlineObjectElements = [
      "object",
      "iframe",
      "input",
      "button",
      "textarea",
      "select",
      "option",
      "video",
      "embed",
      "audio",
      "img",
      "canvas"
    ], this.unsafeElements = [
      "script",
      "style"
    ], this._domDocument = this.renderingMode === "editing" ? ae.document : ae.document.implementation.createHTMLDocument("");
  }
  /**
  * Binds a given DOM element that represents fake selection to a **position** of a
  * {@link module:engine/view/documentselection~DocumentSelection document selection}.
  * Document selection copy is stored and can be retrieved by the
  * {@link module:engine/view/domconverter~DomConverter#fakeSelectionToView} method.
  */
  bindFakeSelection(t, e) {
    this._fakeSelectionMapping.set(t, new Dn(e));
  }
  /**
  * Returns a {@link module:engine/view/selection~Selection view selection} instance corresponding to a given
  * DOM element that represents fake selection. Returns `undefined` if binding to the given DOM element does not exist.
  */
  fakeSelectionToView(t) {
    return this._fakeSelectionMapping.get(t);
  }
  /**
  * Binds DOM and view elements, so it will be possible to get corresponding elements using
  * {@link module:engine/view/domconverter~DomConverter#mapDomToView} and
  * {@link module:engine/view/domconverter~DomConverter#mapViewToDom}.
  *
  * @param domElement The DOM element to bind.
  * @param viewElement The view element to bind.
  */
  bindElements(t, e) {
    this._domToViewMapping.set(t, e), this._viewToDomMapping.set(e, t);
  }
  /**
  * Unbinds a given DOM element from the view element it was bound to. Unbinding is deep, meaning that all children of
  * the DOM element will be unbound too.
  *
  * @param domElement The DOM element to unbind.
  */
  unbindDomElement(t) {
    const e = this._domToViewMapping.get(t);
    if (e) {
      this._domToViewMapping.delete(t), this._viewToDomMapping.delete(e);
      for (const i of t.children)
        this.unbindDomElement(i);
    }
  }
  /**
  * Binds DOM and view document fragments, so it will be possible to get corresponding document fragments using
  * {@link module:engine/view/domconverter~DomConverter#mapDomToView} and
  * {@link module:engine/view/domconverter~DomConverter#mapViewToDom}.
  *
  * @param domFragment The DOM document fragment to bind.
  * @param viewFragment The view document fragment to bind.
  */
  bindDocumentFragments(t, e) {
    this._domToViewMapping.set(t, e), this._viewToDomMapping.set(e, t);
  }
  /**
  * Decides whether a given pair of attribute key and value should be passed further down the pipeline.
  *
  * @param elementName Element name in lower case.
  */
  shouldRenderAttribute(t, e, i) {
    return this.renderingMode === "data" ? !0 : (t = t.toLowerCase(), t.startsWith("on") || t === "srcdoc" && e.match(/\bon\S+\s*=|javascript:|<\s*\/*script/i) ? !1 : i === "img" && (t === "src" || t === "srcset") || i === "source" && t === "srcset" ? !0 : !e.match(/^\s*(javascript:|data:(image\/svg|text\/x?html))/i));
  }
  /**
  * Set `domElement`'s content using provided `html` argument. Apply necessary filtering for the editing pipeline.
  *
  * @param domElement DOM element that should have `html` set as its content.
  * @param html Textual representation of the HTML that will be set on `domElement`.
  */
  setContentOf(t, e) {
    if (this.renderingMode === "data") {
      t.innerHTML = e;
      return;
    }
    const i = new DOMParser().parseFromString(e, "text/html"), s = i.createDocumentFragment(), r = i.body.childNodes;
    for (; r.length > 0; )
      s.appendChild(r[0]);
    const o = i.createTreeWalker(s, NodeFilter.SHOW_ELEMENT), a = [];
    let l;
    for (; l = o.nextNode(); )
      a.push(l);
    for (const c of a) {
      for (const d of c.getAttributeNames())
        this.setDomElementAttribute(c, d, c.getAttribute(d));
      const u = c.tagName.toLowerCase();
      this._shouldRenameElement(u) && (Sp(u), c.replaceWith(this._createReplacementDomElement(u, c)));
    }
    for (; t.firstChild; )
      t.firstChild.remove();
    t.append(s);
  }
  /**
  * Converts the view to the DOM. For all text nodes, not bound elements and document fragments new items will
  * be created. For bound elements and document fragments the method will return corresponding items.
  *
  * @param viewNode View node or document fragment to transform.
  * @param options Conversion options.
  * @param options.bind Determines whether new elements will be bound.
  * @param options.withChildren If `false`, node's and document fragment's children will not be converted.
  * @returns Converted node or DocumentFragment.
  */
  viewToDom(t, e = {}) {
    if (t.is("$text")) {
      const i = this._processDataFromViewText(t);
      return this._domDocument.createTextNode(i);
    } else {
      const i = t;
      if (this.mapViewToDom(i))
        if (i.getCustomProperty("editingPipeline:doNotReuseOnce"))
          this._elementsWithTemporaryCustomProperties.add(i);
        else
          return this.mapViewToDom(i);
      let s;
      if (i.is("documentFragment"))
        s = this._domDocument.createDocumentFragment(), e.bind && this.bindDocumentFragments(s, i);
      else {
        if (i.is("uiElement"))
          return i.name === "$comment" ? s = this._domDocument.createComment(i.getCustomProperty("$rawContent")) : s = i.render(this._domDocument, this), e.bind && this.bindElements(s, i), s;
        this._shouldRenameElement(i.name) ? (Sp(i.name), s = this._createReplacementDomElement(i.name)) : i.hasAttribute("xmlns") ? s = this._domDocument.createElementNS(i.getAttribute("xmlns"), i.name) : s = this._domDocument.createElement(i.name), i.is("rawElement") && i.render(s, this), e.bind && this.bindElements(s, i);
        for (const r of i.getAttributeKeys())
          this.setDomElementAttribute(s, r, i.getAttribute(r), i);
      }
      if (e.withChildren !== !1)
        for (const r of this.viewChildrenToDom(i, e))
          s instanceof HTMLTemplateElement ? s.content.appendChild(r) : s.appendChild(r);
      return s;
    }
  }
  /**
  * Sets the attribute on a DOM element.
  *
  * **Note**: To remove the attribute, use {@link #removeDomElementAttribute}.
  *
  * @param domElement The DOM element the attribute should be set on.
  * @param key The name of the attribute.
  * @param value The value of the attribute.
  * @param relatedViewElement The view element related to the `domElement` (if there is any).
  * It helps decide whether the attribute set is unsafe. For instance, view elements created via the
  * {@link module:engine/view/downcastwriter~DowncastWriter} methods can allow certain attributes that would normally be filtered out.
  */
  setDomElementAttribute(t, e, i, s) {
    const r = this.shouldRenderAttribute(e, i, t.tagName.toLowerCase()) || s && s.shouldRenderUnsafeAttribute(e);
    if (r || Ve("domconverter-unsafe-attribute-detected", {
      domElement: t,
      key: e,
      value: i
    }), !d_(e)) {
      Ve("domconverter-invalid-attribute-detected", {
        domElement: t,
        key: e,
        value: i
      });
      return;
    }
    t.hasAttribute(e) && !r ? t.removeAttribute(e) : t.hasAttribute(nl + e) && r && t.removeAttribute(nl + e), t.setAttribute(r ? e : nl + e, i);
  }
  /**
  * Removes an attribute from a DOM element.
  *
  * **Note**: To set the attribute, use {@link #setDomElementAttribute}.
  *
  * @param domElement The DOM element the attribute should be removed from.
  * @param key The name of the attribute.
  */
  removeDomElementAttribute(t, e) {
    e != Cp && (t.removeAttribute(e), t.removeAttribute(nl + e));
  }
  /**
  * Converts children of the view element to DOM using the
  * {@link module:engine/view/domconverter~DomConverter#viewToDom} method.
  * Additionally, this method adds block {@link module:engine/view/filler filler} to the list of children, if needed.
  *
  * @param viewElement Parent view element.
  * @param options See {@link module:engine/view/domconverter~DomConverter#viewToDom} options parameter.
  * @returns DOM nodes.
  */
  *viewChildrenToDom(t, e = {}) {
    const i = t.getFillerOffset && t.getFillerOffset();
    let s = 0;
    for (const r of t.getChildren()) {
      i === s && (yield this._getBlockFiller());
      const o = r.is("element") && !!r.getCustomProperty("dataPipeline:transparentRendering") && !Xe(r.getAttributes());
      if (o && this.renderingMode == "data")
        if (r.is("rawElement")) {
          const a = this._domDocument.createElement(r.name);
          r.render(a, this), yield* [
            ...a.childNodes
          ];
        } else
          yield* this.viewChildrenToDom(r, e);
      else
        o && Ve("domconverter-transparent-rendering-unsupported-in-editing-pipeline", {
          viewElement: r
        }), yield this.viewToDom(r, e);
      s++;
    }
    i === s && (yield this._getBlockFiller());
  }
  /**
  * Converts view {@link module:engine/view/range~Range} to DOM range.
  * Inline and block {@link module:engine/view/filler fillers} are handled during the conversion.
  *
  * @param viewRange View range.
  * @returns DOM range.
  */
  viewRangeToDom(t) {
    const e = this.viewPositionToDom(t.start), i = this.viewPositionToDom(t.end), s = this._domDocument.createRange();
    return s.setStart(e.parent, e.offset), s.setEnd(i.parent, i.offset), s;
  }
  /**
  * Converts view {@link module:engine/view/position~Position} to DOM parent and offset.
  *
  * Inline and block {@link module:engine/view/filler fillers} are handled during the conversion.
  * If the converted position is directly before inline filler it is moved inside the filler.
  *
  * @param viewPosition View position.
  * @returns DOM position or `null` if view position could not be converted to DOM.
  * DOM position has two properties:
  * * `parent` - DOM position parent.
  * * `offset` - DOM position offset.
  */
  viewPositionToDom(t) {
    const e = t.parent;
    if (e.is("$text")) {
      const i = this.findCorrespondingDomText(e);
      if (!i)
        return null;
      let s = t.offset;
      return Ti(i) && (s += Gi), {
        parent: i,
        offset: s
      };
    } else {
      let i, s, r;
      if (t.offset === 0) {
        if (i = this.mapViewToDom(e), !i)
          return null;
        r = i.childNodes[0];
      } else {
        const a = t.nodeBefore;
        if (s = a.is("$text") ? this.findCorrespondingDomText(a) : this.mapViewToDom(a), !s)
          return null;
        i = s.parentNode, r = s.nextSibling;
      }
      if (mt(r) && Ti(r))
        return {
          parent: r,
          offset: Gi
        };
      const o = s ? Po(s) + 1 : 0;
      return {
        parent: i,
        offset: o
      };
    }
  }
  /**
  * Converts DOM to view. For all text nodes, not bound elements and document fragments new items will
  * be created. For bound elements and document fragments function will return corresponding items. For
  * {@link module:engine/view/filler fillers} `null` will be returned.
  * For all DOM elements rendered by {@link module:engine/view/uielement~UIElement} that UIElement will be returned.
  *
  * @param domNode DOM node or document fragment to transform.
  * @param options Conversion options.
  * @param options.bind Determines whether new elements will be bound. False by default.
  * @param options.withChildren If `true`, node's and document fragment's children will be converted too. True by default.
  * @param options.keepOriginalCase If `false`, node's tag name will be converted to lower case. False by default.
  * @param options.skipComments If `false`, comment nodes will be converted to `$comment`
  * {@link module:engine/view/uielement~UIElement view UI elements}. False by default.
  * @returns Converted node or document fragment or `null` if DOM node is a {@link module:engine/view/filler filler}
  * or the given node is an empty text node.
  */
  domToView(t, e = {}) {
    const i = [], s = this._domToView(t, e, i), r = s.next().value;
    return !r || (s.next(), this._processDomInlineNodes(null, i, e), this.blockFillerMode == "br" && Tp(r)) || r.is("$text") && r.data.length == 0 ? null : r;
  }
  /**
  * Converts children of the DOM element to view nodes using
  * the {@link module:engine/view/domconverter~DomConverter#domToView} method.
  * Additionally this method omits block {@link module:engine/view/filler filler}, if it exists in the DOM parent.
  *
  * @param domElement Parent DOM element.
  * @param options See {@link module:engine/view/domconverter~DomConverter#domToView} options parameter.
  * @param inlineNodes An array that will be populated with inline nodes. It's used internally for whitespace processing.
  * @returns View nodes.
  */
  *domChildrenToView(t, e = {}, i = []) {
    let s = [];
    t instanceof HTMLTemplateElement ? s = [
      ...t.content.childNodes
    ] : s = [
      ...t.childNodes
    ];
    for (let r = 0; r < s.length; r++) {
      const o = s[r], a = this._domToView(o, e, i), l = a.next().value;
      l !== null && (this._isBlockViewElement(l) && this._processDomInlineNodes(t, i, e), this.blockFillerMode == "br" && Tp(l) || (yield l), a.next());
    }
    this._processDomInlineNodes(t, i, e);
  }
  /**
  * Converts DOM selection to view {@link module:engine/view/selection~Selection}.
  * Ranges which cannot be converted will be omitted.
  *
  * @param domSelection DOM selection.
  * @returns View selection.
  */
  domSelectionToView(t) {
    if (r7(t))
      return new Dn([]);
    if (t.rangeCount === 1) {
      let s = t.getRangeAt(0).startContainer;
      mt(s) && (s = s.parentNode);
      const r = this.fakeSelectionToView(s);
      if (r)
        return r;
    }
    const e = this.isDomSelectionBackward(t), i = [];
    for (let s = 0; s < t.rangeCount; s++) {
      const r = t.getRangeAt(s), o = this.domRangeToView(r);
      o && i.push(o);
    }
    return new Dn(i, {
      backward: e
    });
  }
  /**
  * Converts DOM Range to view {@link module:engine/view/range~Range}.
  * If the start or end position can not be converted `null` is returned.
  *
  * @param domRange DOM range.
  * @returns View range.
  */
  domRangeToView(t) {
    const e = this.domPositionToView(t.startContainer, t.startOffset), i = this.domPositionToView(t.endContainer, t.endOffset);
    return e && i ? new Re(e, i) : null;
  }
  /**
  * Converts DOM parent and offset to view {@link module:engine/view/position~Position}.
  *
  * If the position is inside a {@link module:engine/view/filler filler} which has no corresponding view node,
  * position of the filler will be converted and returned.
  *
  * If the position is inside DOM element rendered by {@link module:engine/view/uielement~UIElement}
  * that position will be converted to view position before that UIElement.
  *
  * If structures are too different and it is not possible to find corresponding position then `null` will be returned.
  *
  * @param domParent DOM position parent.
  * @param domOffset DOM position offset. You can skip it when converting the inline filler node.
  * @returns View position.
  */
  domPositionToView(t, e = 0) {
    if (this.isBlockFiller(t))
      return this.domPositionToView(t.parentNode, Po(t));
    const i = this.mapDomToView(t);
    if (i && (i.is("uiElement") || i.is("rawElement")))
      return re._createBefore(i);
    if (mt(t)) {
      if (Ir(t))
        return this.domPositionToView(t.parentNode, Po(t));
      const s = this.findCorrespondingViewText(t);
      let r = e;
      return s ? (Ti(t) && (r -= Gi, r = r < 0 ? 0 : r), new re(s, r)) : null;
    } else {
      if (e === 0) {
        const s = this.mapDomToView(t);
        if (s)
          return new re(s, 0);
      } else {
        const s = t.childNodes[e - 1];
        if (mt(s) && Ir(s) || s && this.isBlockFiller(s))
          return this.domPositionToView(s.parentNode, Po(s));
        const r = mt(s) ? this.findCorrespondingViewText(s) : this.mapDomToView(s);
        if (r && r.parent)
          return new re(r.parent, r.index + 1);
      }
      return null;
    }
  }
  /**
  * Returns corresponding view {@link module:engine/view/element~Element Element} or
  * {@link module:engine/view/documentfragment~DocumentFragment} for provided DOM element or
  * document fragment. If there is no view item {@link module:engine/view/domconverter~DomConverter#bindElements bound}
  * to the given DOM - `undefined` is returned.
  *
  * For all DOM elements rendered by a {@link module:engine/view/uielement~UIElement} or
  * a {@link module:engine/view/rawelement~RawElement}, the parent `UIElement` or `RawElement` will be returned.
  *
  * @param domElementOrDocumentFragment DOM element or document fragment.
  * @returns Corresponding view element, document fragment or `undefined` if no element was bound.
  */
  mapDomToView(t) {
    return this.getHostViewElement(t) || this._domToViewMapping.get(t);
  }
  /**
  * Finds corresponding text node. Text nodes are not {@link module:engine/view/domconverter~DomConverter#bindElements bound},
  * corresponding text node is returned based on the sibling or parent.
  *
  * If the directly previous sibling is a {@link module:engine/view/domconverter~DomConverter#bindElements bound} element, it is used
  * to find the corresponding text node.
  *
  * If this is a first child in the parent and the parent is a {@link module:engine/view/domconverter~DomConverter#bindElements bound}
  * element, it is used to find the corresponding text node.
  *
  * For all text nodes rendered by a {@link module:engine/view/uielement~UIElement} or
  * a {@link module:engine/view/rawelement~RawElement}, the parent `UIElement` or `RawElement` will be returned.
  *
  * Otherwise `null` is returned.
  *
  * Note that for the block or inline {@link module:engine/view/filler filler} this method returns `null`.
  *
  * @param domText DOM text node.
  * @returns Corresponding view text node or `null`, if it was not possible to find a corresponding node.
  */
  findCorrespondingViewText(t) {
    if (Ir(t))
      return null;
    const e = this.getHostViewElement(t);
    if (e)
      return e;
    const i = t.previousSibling;
    if (i) {
      if (!this.isElement(i))
        return null;
      const s = this.mapDomToView(i);
      if (s) {
        const r = s.nextSibling;
        return r instanceof qt ? r : null;
      }
    } else {
      const s = this.mapDomToView(t.parentNode);
      if (s) {
        const r = s.getChild(0);
        return r instanceof qt ? r : null;
      }
    }
    return null;
  }
  mapViewToDom(t) {
    return this._viewToDomMapping.get(t);
  }
  /**
  * Finds corresponding text node. Text nodes are not {@link module:engine/view/domconverter~DomConverter#bindElements bound},
  * corresponding text node is returned based on the sibling or parent.
  *
  * If the directly previous sibling is a {@link module:engine/view/domconverter~DomConverter#bindElements bound} element, it is used
  * to find the corresponding text node.
  *
  * If this is a first child in the parent and the parent is a {@link module:engine/view/domconverter~DomConverter#bindElements bound}
  * element, it is used to find the corresponding text node.
  *
  * Otherwise `null` is returned.
  *
  * @param viewText View text node.
  * @returns Corresponding DOM text node or `null`, if it was not possible to find a corresponding node.
  */
  findCorrespondingDomText(t) {
    const e = t.previousSibling;
    return e && this.mapViewToDom(e) ? this.mapViewToDom(e).nextSibling : !e && t.parent && this.mapViewToDom(t.parent) ? this.mapViewToDom(t.parent).childNodes[0] : null;
  }
  /**
  * Focuses DOM editable that is corresponding to provided {@link module:engine/view/editableelement~EditableElement}.
  */
  focus(t) {
    const e = this.mapViewToDom(t);
    if (e && e.ownerDocument.activeElement !== e) {
      const { scrollX: i, scrollY: s } = ae.window, r = [];
      xp(e, (o) => {
        const { scrollLeft: a, scrollTop: l } = o;
        r.push([
          a,
          l
        ]);
      }), e.focus(), xp(e, (o) => {
        const [a, l] = r.shift();
        o.scrollLeft = a, o.scrollTop = l;
      }), ae.window.scrollTo(i, s);
    }
  }
  /**
  * Remove DOM selection from blurred editable, so it won't interfere with clicking on dropdowns (especially on iOS).
  *
  * @internal
  */
  _clearDomSelection() {
    const t = this.mapViewToDom(this.document.selection.editableElement);
    if (!t)
      return;
    const e = t.ownerDocument.defaultView.getSelection(), i = this.domSelectionToView(e);
    i && i.rangeCount > 0 && e.removeAllRanges();
  }
  /**
  * Returns `true` when `node.nodeType` equals `Node.ELEMENT_NODE`.
  *
  * @param node Node to check.
  */
  isElement(t) {
    return t && t.nodeType == Node.ELEMENT_NODE;
  }
  /**
  * Returns `true` when `node.nodeType` equals `Node.DOCUMENT_FRAGMENT_NODE`.
  *
  * @param node Node to check.
  */
  isDocumentFragment(t) {
    return t && t.nodeType == Node.DOCUMENT_FRAGMENT_NODE;
  }
  /**
  * Checks if the node is an instance of the block filler for this DOM converter.
  *
  * ```ts
  * const converter = new DomConverter( viewDocument, { blockFillerMode: 'br' } );
  *
  * converter.isBlockFiller( BR_FILLER( document ) ); // true
  * converter.isBlockFiller( NBSP_FILLER( document ) ); // false
  * ```
  *
  * **Note:**: For the `'nbsp'` mode the method also checks context of a node so it cannot be a detached node.
  *
  * **Note:** A special case in the `'nbsp'` mode exists where the `<br>` in `<p><br></p>` is treated as a block filler.
  *
  * @param domNode DOM node to check.
  * @returns True if a node is considered a block filler for given mode.
  */
  isBlockFiller(t) {
    return this.blockFillerMode == "br" ? t.isEqualNode(X6) : Ep(t, this.blockElements) ? !0 : t.isEqualNode(e7) || i7(t, this.blockElements);
  }
  /**
  * Returns `true` if given selection is a backward selection, that is, if it's `focus` is before `anchor`.
  *
  * @param DOM Selection instance to check.
  */
  isDomSelectionBackward(t) {
    if (t.isCollapsed)
      return !1;
    const e = this._domDocument.createRange();
    try {
      e.setStart(t.anchorNode, t.anchorOffset), e.setEnd(t.focusNode, t.focusOffset);
    } catch {
      return !1;
    }
    const i = e.collapsed;
    return e.detach(), i;
  }
  /**
  * Returns a parent {@link module:engine/view/uielement~UIElement} or {@link module:engine/view/rawelement~RawElement}
  * that hosts the provided DOM node. Returns `null` if there is no such parent.
  */
  getHostViewElement(t) {
    const e = N4(t);
    for (e.pop(); e.length; ) {
      const i = e.pop(), s = this._domToViewMapping.get(i);
      if (s && (s.is("uiElement") || s.is("rawElement")))
        return s;
    }
    return null;
  }
  /**
  * Checks if the given selection's boundaries are at correct places.
  *
  * The following places are considered as incorrect for selection boundaries:
  *
  * * before or in the middle of an inline filler sequence,
  * * inside a DOM element which represents {@link module:engine/view/uielement~UIElement a view UI element},
  * * inside a DOM element which represents {@link module:engine/view/rawelement~RawElement a view raw element}.
  *
  * @param domSelection The DOM selection object to be checked.
  * @returns `true` if the given selection is at a correct place, `false` otherwise.
  */
  isDomSelectionCorrect(t) {
    return this._isDomSelectionPositionCorrect(t.anchorNode, t.anchorOffset) && this._isDomSelectionPositionCorrect(t.focusNode, t.focusOffset);
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} for view elements whose content should be treated as raw data
  * and not processed during the conversion from DOM nodes to view elements.
  *
  * This is affecting how {@link module:engine/view/domconverter~DomConverter#domToView} and
  * {@link module:engine/view/domconverter~DomConverter#domChildrenToView} process DOM nodes.
  *
  * The raw data can be later accessed by a
  * {@link module:engine/view/element~Element#getCustomProperty custom property of a view element} called `"$rawContent"`.
  *
  * @param pattern Pattern matching a view element whose content should
  * be treated as raw data.
  */
  registerRawContentMatcher(t) {
    this._rawContentElementMatcher.add(t);
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} for inline object view elements.
  *
  * This is affecting how {@link module:engine/view/domconverter~DomConverter#domToView} and
  * {@link module:engine/view/domconverter~DomConverter#domChildrenToView} process DOM nodes.
  *
  * This is an extension of a simple {@link #inlineObjectElements} array of element names.
  *
  * @param pattern Pattern matching a view element which should be treated as an inline object.
  */
  registerInlineObjectMatcher(t) {
    this._inlineObjectElementMatcher.add(t);
  }
  /**
  * Clear temporary custom properties.
  *
  * @internal
  */
  _clearTemporaryCustomProperties() {
    for (const t of this._elementsWithTemporaryCustomProperties)
      t._removeCustomProperty("editingPipeline:doNotReuseOnce");
    this._elementsWithTemporaryCustomProperties.clear();
  }
  /**
  * Returns the block {@link module:engine/view/filler filler} node based on the current {@link #blockFillerMode} setting.
  */
  _getBlockFiller() {
    switch (this.blockFillerMode) {
      case "nbsp":
        return T_(this._domDocument);
      // eslint-disable-line new-cap
      case "markedNbsp":
        return E_(this._domDocument);
      // eslint-disable-line new-cap
      case "br":
        return S_(this._domDocument);
    }
  }
  /**
  * Checks if the given DOM position is a correct place for selection boundary. See {@link #isDomSelectionCorrect}.
  *
  * @param domParent Position parent.
  * @param offset Position offset.
  * @returns `true` if given position is at a correct place for selection boundary, `false` otherwise.
  */
  _isDomSelectionPositionCorrect(t, e) {
    if (mt(t) && Ti(t) && e < Gi || this.isElement(t) && Ti(t.childNodes[e]))
      return !1;
    const i = this.mapDomToView(t);
    return !(i && (i.is("uiElement") || i.is("rawElement")));
  }
  /**
  * Internal generator for {@link #domToView}. Also used by {@link #domChildrenToView}.
  * Separates DOM nodes conversion from whitespaces processing.
  *
  * @param domNode DOM node or document fragment to transform.
  * @param inlineNodes An array of recently encountered inline nodes truncated to the block element boundaries.
  * Used later to process whitespaces.
  */
  *_domToView(t, e, i) {
    if (this.blockFillerMode != "br" && Ep(t, this.blockElements))
      return null;
    const s = this.getHostViewElement(t);
    if (s)
      return s;
    if (zo(t) && e.skipComments)
      return null;
    if (mt(t)) {
      if (Ir(t))
        return null;
      {
        const r = t.data;
        if (r === "")
          return null;
        const o = new qt(this.document, r);
        return i.push(o), o;
      }
    } else {
      let r = this.mapDomToView(t);
      if (r)
        return this._isInlineObjectElement(r) && i.push(r), r;
      if (this.isDocumentFragment(t))
        r = new Ls(this.document), e.bind && this.bindDocumentFragments(t, r);
      else {
        r = this._createViewElement(t, e), e.bind && this.bindElements(t, r);
        const a = t.attributes;
        if (a)
          for (let l = a.length, c = 0; c < l; c++)
            r._setAttribute(a[c].name, a[c].value);
        if (this._isViewElementWithRawContent(r, e))
          return r._setCustomProperty("$rawContent", t.innerHTML), this._isBlockViewElement(r) || i.push(r), r;
        if (zo(t))
          return r._setCustomProperty("$rawContent", t.data), r;
      }
      yield r;
      const o = [];
      if (e.withChildren !== !1)
        for (const a of this.domChildrenToView(t, e, o))
          r._appendChild(a);
      if (this._isInlineObjectElement(r))
        i.push(r), this._processDomInlineNodes(null, o, e);
      else
        for (const a of o)
          i.push(a);
    }
  }
  /**
  * Internal helper that walks the list of inline view nodes already generated from DOM nodes
  * and handles whitespaces and NBSPs.
  *
  * @param domParent The DOM parent of the given inline nodes. This should be a document fragment or
  * a block element to whitespace processing start cleaning.
  * @param inlineNodes An array of recently encountered inline nodes truncated to the block element boundaries.
  */
  _processDomInlineNodes(t, e, i) {
    if (!e.length || t && !this.isDocumentFragment(t) && !this._isBlockDomElement(t))
      return;
    let s = !1;
    for (let r = 0; r < e.length; r++) {
      const o = e[r];
      if (!o.is("$text")) {
        s = !1;
        continue;
      }
      let a, l = !1;
      if (this._isPreFormatted(o))
        a = yp(o.data);
      else {
        a = o.data.replace(/[ \n\t\r]{1,}/g, " "), l = /[^\S\u00A0]/.test(a.charAt(a.length - 1));
        const c = r > 0 ? e[r - 1] : null, u = r + 1 < e.length ? e[r + 1] : null, d = !c || c.is("element") && c.name == "br" || s, h = u ? !1 : !Ti(o.data);
        i.withChildren !== !1 && (d && (a = a.replace(/^ /, "")), h && (a = a.replace(/ $/, ""))), a = yp(a), this.blockFillerMode != "br" && o.parent && (s7(o.parent, a) ? (a = "", o.parent.parent && (o.parent.parent._setCustomProperty("$hasBlockFiller", !0), o.parent._remove())) : n7(o.parent, a, this.blockElements) && (a = "", o.parent._setCustomProperty("$hasBlockFiller", !0))), a = a.replace(/ \u00A0/g, "  ");
        const m = u && u.is("element") && u.name != "br", g = u && u.is("$text") && u.data.charAt(0) == " ";
        (/[ \u00A0]\u00A0$/.test(a) || !u || m || g) && (a = a.replace(/\u00A0$/, " ")), (d || c && c.is("element") && c.name != "br") && (a = a.replace(/^\u00A0/, " "));
      }
      a.length == 0 && o.parent ? (o._remove(), e.splice(r, 1), r--) : (o._data = a, s = l);
    }
    e.length = 0;
  }
  /**
  * Takes text data from a given {@link module:engine/view/text~Text#data} and processes it so
  * it is correctly displayed in the DOM.
  *
  * Following changes are done:
  *
  * * a space at the beginning is changed to `&nbsp;` if this is the first text node in its container
  * element or if a previous text node ends with a space character,
  * * space at the end of the text node is changed to `&nbsp;` if there are two spaces at the end of a node or if next node
  * starts with a space or if it is the last text node in its container,
  * * remaining spaces are replaced to a chain of spaces and `&nbsp;` (e.g. `'x   x'` becomes `'x &nbsp; x'`).
  *
  * Content of {@link #preElements} is not processed.
  *
  * @param node View text node to process.
  * @returns Processed text data.
  */
  _processDataFromViewText(t) {
    let e = t.data;
    if (this._isPreFormatted(t))
      return e;
    if (e.charAt(0) == " ") {
      const i = this._getTouchingInlineViewNode(t, !1);
      (i && i.is("$textProxy") && this._nodeEndsWithSpace(i) || !i) && (e = " " + e.substr(1));
    }
    if (e.charAt(e.length - 1) == " ") {
      const i = this._getTouchingInlineViewNode(t, !0), s = i && i.is("$textProxy") && i.data.charAt(0) == " ";
      (e.charAt(e.length - 2) == " " || !i || s) && (e = e.substr(0, e.length - 1) + " ");
    }
    return e.replace(/ {2}/g, "  ");
  }
  /**
  * Checks whether given node ends with a space character after changing appropriate space characters to `&nbsp;`s.
  *
  * @param  node Node to check.
  * @returns `true` if given `node` ends with space, `false` otherwise.
  */
  _nodeEndsWithSpace(t) {
    if (this._isPreFormatted(t))
      return !1;
    const e = this._processDataFromViewText(t);
    return e.charAt(e.length - 1) == " ";
  }
  /**
  * Checks whether given text contains preformatted white space. This is the case if
  * * any of node ancestors has a name which is in `preElements` array, or
  * * the closest ancestor that has the `white-space` CSS property sets it to a value that preserves spaces
  *
  * @param node Node to check
  * @returns `true` if given node contains preformatted white space, `false` otherwise.
  */
  _isPreFormatted(t) {
    if (t7(t, this.preElements))
      return !0;
    for (const e of t.getAncestors({
      parentFirst: !0
    }))
      if (!(!e.is("element") || !e.hasStyle("white-space") || e.getStyle("white-space") === "inherit"))
        return [
          "pre",
          "pre-wrap",
          "break-spaces"
        ].includes(e.getStyle("white-space"));
    return !1;
  }
  /**
  * Helper function. For given {@link module:engine/view/text~Text view text node}, it finds previous or next sibling
  * that is contained in the same container element. If there is no such sibling, `null` is returned.
  *
  * @param node Reference node.
  * @returns Touching text node, an inline object
  * or `null` if there is no next or previous touching text node.
  */
  _getTouchingInlineViewNode(t, e) {
    const i = new Rs({
      startPosition: e ? re._createAfter(t) : re._createBefore(t),
      direction: e ? "forward" : "backward"
    });
    for (const { item: s } of i) {
      if (s.is("$textProxy"))
        return s;
      if (s.is("element") && s.getCustomProperty("dataPipeline:transparentRendering"))
        continue;
      if (s.is("element", "br"))
        return null;
      if (this._isInlineObjectElement(s))
        return s;
      if (s.is("containerElement"))
        return null;
    }
    return null;
  }
  /**
  * Returns `true` if a DOM node belongs to {@link #blockElements}. `false` otherwise.
  */
  _isBlockDomElement(t) {
    return this.isElement(t) && this.blockElements.includes(t.tagName.toLowerCase());
  }
  /**
  * Returns `true` if a view node belongs to {@link #blockElements}. `false` otherwise.
  */
  _isBlockViewElement(t) {
    return t.is("element") && this.blockElements.includes(t.name);
  }
  /**
  * Returns `true` if a DOM node belongs to {@link #inlineObjectElements}. `false` otherwise.
  */
  _isInlineObjectElement(t) {
    return t.is("element") ? t.name == "br" || this.inlineObjectElements.includes(t.name) || !!this._inlineObjectElementMatcher.match(t) : !1;
  }
  /**
  * Creates view element basing on the node type.
  *
  * @param node DOM node to check.
  * @param options Conversion options. See {@link module:engine/view/domconverter~DomConverter#domToView} options parameter.
  */
  _createViewElement(t, e) {
    if (zo(t))
      return new Kc(this.document, "$comment");
    const i = e.keepOriginalCase ? t.tagName : t.tagName.toLowerCase();
    return new Un(this.document, i);
  }
  /**
  * Checks if view element's content should be treated as a raw data.
  *
  * @param viewElement View element to check.
  * @param options Conversion options. See {@link module:engine/view/domconverter~DomConverter#domToView} options parameter.
  */
  _isViewElementWithRawContent(t, e) {
    return e.withChildren !== !1 && t.is("element") && !!this._rawContentElementMatcher.match(t);
  }
  /**
  * Checks whether a given element name should be renamed in a current rendering mode.
  *
  * @param elementName The name of view element.
  */
  _shouldRenameElement(t) {
    const e = t.toLowerCase();
    return this.renderingMode === "editing" && this.unsafeElements.includes(e);
  }
  /**
  * Return a <span> element with a special attribute holding the name of the original element.
  * Optionally, copy all the attributes of the original element if that element is provided.
  *
  * @param elementName The name of view element.
  * @param originalDomElement The original DOM element to copy attributes and content from.
  */
  _createReplacementDomElement(t, e) {
    const i = this._domDocument.createElement("span");
    if (i.setAttribute(Cp, t), e) {
      for (; e.firstChild; )
        i.appendChild(e.firstChild);
      for (const s of e.getAttributeNames())
        i.setAttribute(s, e.getAttribute(s));
    }
    return i;
  }
}
function t7(n, t) {
  return n.getAncestors().some((e) => e.is("element") && t.includes(e.name));
}
function xp(n, t) {
  let e = n;
  for (; e; )
    t(e), e = e.parentElement;
}
function i7(n, t) {
  return n.isEqualNode(Q6) && A_(n, t) && n.parentNode.childNodes.length === 1;
}
function A_(n, t) {
  const e = n.parentNode;
  return !!e && !!e.tagName && t.includes(e.tagName.toLowerCase());
}
function n7(n, t, e) {
  return t == " " && n && n.is("element") && n.childCount == 1 && e.includes(n.name);
}
function s7(n, t) {
  return t == " " && n && n.is("element", "span") && n.childCount == 1 && n.hasAttribute("data-cke-filler");
}
function Tp(n) {
  return n.is("element", "br") && n.hasAttribute("data-cke-filler");
}
function Ep(n, t) {
  return n.tagName === "BR" && A_(n, t) && n.parentNode.childNodes.length === 1;
}
function Sp(n) {
  n === "script" && Ve("domconverter-unsafe-script-element-detected"), n === "style" && Ve("domconverter-unsafe-style-element-detected");
}
function r7(n) {
  if (!be.isGecko || !n.rangeCount)
    return !1;
  const t = n.getRangeAt(0).startContainer;
  try {
    Object.prototype.toString.call(t);
  } catch {
    return !0;
  }
  return !1;
}
class sn extends (/* @__PURE__ */ li()) {
  /**
  * Creates an instance of the observer.
  */
  constructor(e) {
    super();
    /**
    * An instance of the view controller.
    */
    f(this, "view");
    /**
    * A reference to the {@link module:engine/view/document~Document} object.
    */
    f(this, "document");
    /**
    * The state of the observer. If it is disabled, no events will be fired.
    */
    f(this, "_isEnabled", !1);
    this.view = e, this.document = e.document;
  }
  /**
  * The state of the observer. If it is disabled, no events will be fired.
  */
  get isEnabled() {
    return this._isEnabled;
  }
  /**
  * Enables the observer. This method is called when the observer is registered to the
  * {@link module:engine/view/view~View} and after {@link module:engine/view/view~View#forceRender rendering}
  * (all observers are {@link #disable disabled} before rendering).
  *
  * A typical use case for disabling observers is that mutation observers need to be disabled for the rendering.
  * However, a child class may not need to be disabled, so it can implement an empty method.
  *
  * @see module:engine/view/observer/observer~Observer#disable
  */
  enable() {
    this._isEnabled = !0;
  }
  /**
  * Disables the observer. This method is called before
  * {@link module:engine/view/view~View#forceRender rendering} to prevent firing events during rendering.
  *
  * @see module:engine/view/observer/observer~Observer#enable
  */
  disable() {
    this._isEnabled = !1;
  }
  /**
  * Disables and destroys the observer, among others removes event listeners created by the observer.
  */
  destroy() {
    this.disable(), this.stopListening();
  }
  /**
  * Checks whether a given DOM event should be ignored (should not be turned into a synthetic view document event).
  *
  * Currently, an event will be ignored only if its target or any of its ancestors has the `data-cke-ignore-events` attribute.
  * This attribute can be used inside the structures generated by
  * {@link module:engine/view/downcastwriter~DowncastWriter#createUIElement `DowncastWriter#createUIElement()`} to ignore events
  * fired within a UI that should be excluded from CKEditor 5's realms.
  *
  * @param domTarget The DOM event target to check (usually an element, sometimes a text node and
  * potentially sometimes a document, too).
  * @returns Whether this event should be ignored by the observer.
  */
  checkShouldIgnoreEventFromTarget(e) {
    return e && e.nodeType === 3 && (e = e.parentNode), !e || e.nodeType !== 1 ? !1 : e.matches("[data-cke-ignore-events], [data-cke-ignore-events] *");
  }
}
class zr {
  /**
  * @param view The instance of the view controller.
  * @param domEvent The DOM event.
  * @param additionalData Additional properties that the instance should contain.
  */
  constructor(t, e, i) {
    /**
    * Instance of the view controller.
    */
    f(this, "view");
    /**
    * The instance of the document.
    */
    f(this, "document");
    /**
    * The DOM event.
    */
    f(this, "domEvent");
    /**
    * The DOM target.
    */
    f(this, "domTarget");
    this.view = t, this.document = t.document, this.domEvent = e, this.domTarget = e.target, tv(this, i);
  }
  /**
  * The tree view element representing the target.
  */
  get target() {
    return this.view.domConverter.mapDomToView(this.domTarget);
  }
  /**
  * Prevents the native's event default action.
  */
  preventDefault() {
    this.domEvent.preventDefault();
  }
  /**
  * Stops native event propagation.
  */
  stopPropagation() {
    this.domEvent.stopPropagation();
  }
}
class xn extends sn {
  constructor() {
    super(...arguments);
    /**
    * If set to `true` DOM events will be listened on the capturing phase.
    * Default value is `false`.
    */
    f(this, "useCapture", !1);
    /**
    * If set to `true`, indicates that the function specified by listener will never call `preventDefault()`.
    * Default value is `false`.
    */
    f(this, "usePassive", !1);
  }
  /**
  * @inheritDoc
  */
  observe(e) {
    (typeof this.domEventType == "string" ? [
      this.domEventType
    ] : this.domEventType).forEach((s) => {
      this.listenTo(e, s, (r, o) => {
        this.isEnabled && !this.checkShouldIgnoreEventFromTarget(o.target) && this.onDomEvent(o);
      }, {
        useCapture: this.useCapture,
        usePassive: this.usePassive
      });
    });
  }
  /**
  * @inheritDoc
  */
  stopObserving(e) {
    this.stopListening(e);
  }
  /**
  * Calls `Document#fire()` if observer {@link #isEnabled is enabled}.
  *
  * @see module:utils/emittermixin~Emitter#fire
  * @param eventType The event type (name).
  * @param domEvent The DOM event.
  * @param additionalData The additional data which should extend the
  * {@link module:engine/view/observer/domeventdata~DomEventData event data} object.
  */
  fire(e, i, s) {
    this.isEnabled && this.document.fire(e, new zr(this.view, i, s));
  }
}
class o7 extends xn {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    f(this, "domEventType", [
      "keydown",
      "keyup"
    ]);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    const i = {
      keyCode: e.keyCode,
      altKey: e.altKey,
      ctrlKey: e.ctrlKey,
      shiftKey: e.shiftKey,
      metaKey: e.metaKey,
      get keystroke() {
        return zs(this);
      }
    };
    this.fire(e.type, e, i);
  }
}
class a7 extends sn {
  /**
  * Creates new FakeSelectionObserver instance.
  */
  constructor(e) {
    super(e);
    /**
    * Fires debounced event `selectionChangeDone`. It uses `lodash#debounce` method to delay function call.
    */
    f(this, "_fireSelectionChangeDoneDebounced");
    this._fireSelectionChangeDoneDebounced = vi((i) => {
      this.document.fire("selectionChangeDone", i);
    }, 200);
  }
  /**
  * @inheritDoc
  */
  observe() {
    const e = this.document;
    e.on("arrowKey", (i, s) => {
      e.selection.isFake && this.isEnabled && s.preventDefault();
    }, {
      context: "$capture"
    }), e.on("arrowKey", (i, s) => {
      e.selection.isFake && this.isEnabled && this._handleSelectionMove(s.keyCode);
    }, {
      priority: "lowest"
    });
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._fireSelectionChangeDoneDebounced.cancel();
  }
  /**
  * Handles collapsing view selection according to given key code. If left or up key is provided - new selection will be
  * collapsed to left. If right or down key is pressed - new selection will be collapsed to right.
  *
  * This method fires {@link module:engine/view/document~Document#event:selectionChange} and
  * {@link module:engine/view/document~Document#event:selectionChangeDone} events imitating behaviour of
  * {@link module:engine/view/observer/selectionobserver~SelectionObserver}.
  */
  _handleSelectionMove(e) {
    const i = this.document.selection, s = new Dn(i.getRanges(), {
      backward: i.isBackward,
      fake: !1
    });
    (e == Ie.arrowleft || e == Ie.arrowup) && s.setTo(s.getFirstPosition()), (e == Ie.arrowright || e == Ie.arrowdown) && s.setTo(s.getLastPosition());
    const r = {
      oldSelection: i,
      newSelection: s,
      domSelection: null
    };
    this.document.fire("selectionChange", r), this._fireSelectionChangeDoneDebounced(r);
  }
}
let I_ = class extends sn {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Reference to the {@link module:engine/view/view~View#domConverter}.
    */
    f(this, "domConverter");
    /**
    * Native mutation observer config.
    */
    f(this, "_config");
    /**
    * Observed DOM elements.
    */
    f(this, "_domElements");
    /**
    * Native mutation observer.
    */
    f(this, "_mutationObserver");
    this._config = {
      childList: !0,
      characterData: !0,
      subtree: !0
    }, this.domConverter = e.domConverter, this._domElements = /* @__PURE__ */ new Set(), this._mutationObserver = new window.MutationObserver(this._onMutations.bind(this));
  }
  /**
  * Synchronously handles mutations and empties the queue.
  */
  flush() {
    this._onMutations(this._mutationObserver.takeRecords());
  }
  /**
  * @inheritDoc
  */
  observe(e) {
    this._domElements.add(e), this.isEnabled && this._mutationObserver.observe(e, this._config);
  }
  /**
  * @inheritDoc
  */
  stopObserving(e) {
    if (this._domElements.delete(e), this.isEnabled) {
      this._mutationObserver.disconnect();
      for (const i of this._domElements)
        this._mutationObserver.observe(i, this._config);
    }
  }
  /**
  * @inheritDoc
  */
  enable() {
    super.enable();
    for (const e of this._domElements)
      this._mutationObserver.observe(e, this._config);
  }
  /**
  * @inheritDoc
  */
  disable() {
    super.disable(), this._mutationObserver.disconnect();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._mutationObserver.disconnect();
  }
  /**
  * Handles mutations. Mark view elements to sync and call render.
  *
  * @param domMutations Array of native mutations.
  */
  _onMutations(e) {
    if (e.length === 0)
      return;
    const i = this.domConverter, s = /* @__PURE__ */ new Set(), r = /* @__PURE__ */ new Set();
    for (const a of e) {
      const l = i.mapDomToView(a.target);
      l && (l.is("uiElement") || l.is("rawElement") || a.type === "childList" && !this._isBogusBrMutation(a) && r.add(l));
    }
    for (const a of e) {
      const l = i.mapDomToView(a.target);
      if (!(l && (l.is("uiElement") || l.is("rawElement"))) && a.type === "characterData") {
        const c = i.findCorrespondingViewText(a.target);
        c && !r.has(c.parent) ? s.add(c) : !c && Ti(a.target) && r.add(i.mapDomToView(a.target.parentNode));
      }
    }
    const o = [];
    for (const a of s)
      o.push({
        type: "text",
        node: a
      });
    for (const a of r) {
      const l = i.mapViewToDom(a), c = Array.from(a.getChildren()), u = Array.from(i.domChildrenToView(l, {
        withChildren: !1
      }));
      XP(c, u, l7) || o.push({
        type: "children",
        node: a
      });
    }
    o.length && this.document.fire("mutations", {
      mutations: o
    });
  }
  /**
  * Checks if mutation was generated by the browser inserting bogus br on the end of the block element.
  * Such mutations are generated while pressing space or performing native spellchecker correction
  * on the end of the block element in Firefox browser.
  *
  * @param mutation Native mutation object.
  */
  _isBogusBrMutation(e) {
    let i = null;
    return e.nextSibling === null && e.removedNodes.length === 0 && e.addedNodes.length == 1 && (i = this.domConverter.domToView(e.addedNodes[0], {
      withChildren: !1
    })), i && i.is("element", "br");
  }
};
function l7(n, t) {
  if (!Array.isArray(n))
    return n === t ? !0 : n.is("$text") && t.is("$text") ? n.data === t.data : !1;
}
class Ql extends xn {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Identifier of the timeout currently used by focus listener to delay rendering execution.
    */
    f(this, "_renderTimeoutId", null);
    /**
    * Set to `true` if the document is in the process of setting the focus.
    *
    * The flag is used to indicate that setting the focus is in progress.
    */
    f(this, "_isFocusChanging", !1);
    /**
    * @inheritDoc
    */
    f(this, "domEventType", [
      "focus",
      "blur"
    ]);
    this.useCapture = !0;
    const i = this.document;
    i.on("focus", () => this._handleFocus()), i.on("blur", (s, r) => this._handleBlur(r)), i.on("beforeinput", () => {
      i.isFocused || this._handleFocus();
    }, {
      priority: "highest"
    });
  }
  /**
  * Finishes setting the document focus state.
  */
  flush() {
    this._isFocusChanging && (this._isFocusChanging = !1, this.document.isFocused = !0);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._clearTimeout(), super.destroy();
  }
  /**
  * The `focus` event handler.
  */
  _handleFocus() {
    this._clearTimeout(), this._isFocusChanging = !0, this._renderTimeoutId = setTimeout(() => {
      this._renderTimeoutId = null, this.flush(), this.view.change(() => {
      });
    }, 50);
  }
  /**
  * The `blur` event handler.
  */
  _handleBlur(e) {
    const i = this.document.selection.editableElement;
    (i === null || i === e.target) && (this.document.isFocused = !1, this._isFocusChanging = !1, this.view.change(() => {
    }));
  }
  /**
  * Clears timeout.
  */
  _clearTimeout() {
    this._renderTimeoutId && (clearTimeout(this._renderTimeoutId), this._renderTimeoutId = null);
  }
}
class c7 extends sn {
  constructor(e) {
    super(e);
    /**
    * Instance of the mutation observer. Selection observer calls
    * {@link module:engine/view/observer/mutationobserver~MutationObserver#flush} to ensure that the mutations will be handled
    * before the {@link module:engine/view/document~Document#event:selectionChange} event is fired.
    */
    f(this, "mutationObserver");
    /**
    * Instance of the focus observer. Focus observer calls
    * {@link module:engine/view/observer/focusobserver~FocusObserver#flush} to mark the latest focus change as complete.
    */
    f(this, "focusObserver");
    /**
    * Reference to the view {@link module:engine/view/documentselection~DocumentSelection} object used to compare
    * new selection with it.
    */
    f(this, "selection");
    /**
    * Reference to the {@link module:engine/view/view~View#domConverter}.
    */
    f(this, "domConverter");
    /**
    * A set of documents which have added `selectionchange` listener to avoid adding a listener twice to the same
    * document.
    */
    f(this, "_documents");
    /**
    * Fires debounced event `selectionChangeDone`. It uses `lodash#debounce` method to delay function call.
    */
    f(this, "_fireSelectionChangeDoneDebounced");
    /**
    * When called, starts clearing the {@link #_loopbackCounter} counter in time intervals. When the number of selection
    * changes exceeds a certain limit within the interval of time, the observer will not fire `selectionChange` but warn about
    * possible infinite selection loop.
    */
    f(this, "_clearInfiniteLoopInterval");
    /**
    * Unlocks the `isSelecting` state of the view document in case the selection observer did not record this fact
    * correctly (for whatever reason). It is a safeguard (paranoid check), that returns document to the normal state
    * after a certain period of time (debounced, postponed by each selectionchange event).
    */
    f(this, "_documentIsSelectingInactivityTimeoutDebounced");
    /**
    * Private property to check if the code does not enter infinite loop.
    */
    f(this, "_loopbackCounter");
    this.mutationObserver = e.getObserver(I_), this.focusObserver = e.getObserver(Ql), this.selection = this.document.selection, this.domConverter = e.domConverter, this._documents = /* @__PURE__ */ new WeakSet(), this._fireSelectionChangeDoneDebounced = vi((i) => {
      this.document.fire("selectionChangeDone", i);
    }, 200), this._clearInfiniteLoopInterval = setInterval(() => this._clearInfiniteLoop(), 1e3), this._documentIsSelectingInactivityTimeoutDebounced = vi(() => this.document.isSelecting = !1, 5e3), this._loopbackCounter = 0;
  }
  /**
  * @inheritDoc
  */
  observe(e) {
    const i = e.ownerDocument, s = () => {
      this.document.isSelecting = !0, this._documentIsSelectingInactivityTimeoutDebounced();
    }, r = () => {
      this.document.isSelecting && (this._handleSelectionChange(i), this.document.isSelecting = !1, this._documentIsSelectingInactivityTimeoutDebounced.cancel());
    };
    this.listenTo(e, "selectstart", s, {
      priority: "highest"
    }), this.listenTo(e, "keydown", r, {
      priority: "highest",
      useCapture: !0
    }), this.listenTo(e, "keyup", r, {
      priority: "highest",
      useCapture: !0
    }), !this._documents.has(i) && (this.listenTo(i, "mouseup", r, {
      priority: "highest",
      useCapture: !0
    }), this.listenTo(i, "selectionchange", () => {
      this.document.isComposing && !be.isAndroid || (this._handleSelectionChange(i), this._documentIsSelectingInactivityTimeoutDebounced());
    }), this.listenTo(this.view.document, "compositionstart", () => {
      this._handleSelectionChange(i);
    }, {
      priority: "lowest"
    }), this._documents.add(i));
  }
  /**
  * @inheritDoc
  */
  stopObserving(e) {
    this.stopListening(e);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), clearInterval(this._clearInfiniteLoopInterval), this._fireSelectionChangeDoneDebounced.cancel(), this._documentIsSelectingInactivityTimeoutDebounced.cancel();
  }
  /* istanbul ignore next -- @preserve */
  _reportInfiniteLoop() {
  }
  /**
  * Selection change listener. {@link module:engine/view/observer/mutationobserver~MutationObserver#flush Flush} mutations, check if
  * a selection changes and fires {@link module:engine/view/document~Document#event:selectionChange} event on every change
  * and {@link module:engine/view/document~Document#event:selectionChangeDone} when a selection stop changing.
  *
  * @param domDocument DOM document.
  */
  _handleSelectionChange(e) {
    if (!this.isEnabled)
      return;
    const i = e.defaultView.getSelection();
    if (this.checkShouldIgnoreEventFromTarget(i.anchorNode))
      return;
    this.mutationObserver.flush();
    const s = this.domConverter.domSelectionToView(i);
    if (s.rangeCount == 0) {
      this.view.hasDomSelection = !1;
      return;
    }
    if (this.view.hasDomSelection = !0, this.focusObserver.flush(), !(this.selection.isEqual(s) && this.domConverter.isDomSelectionCorrect(i))) {
      if (++this._loopbackCounter > 60) {
        this._reportInfiniteLoop();
        return;
      }
      if (this.selection.isSimilar(s))
        this.view.forceRender();
      else {
        const r = {
          oldSelection: this.selection,
          newSelection: s,
          domSelection: i
        };
        this.document.fire("selectionChange", r), this._fireSelectionChangeDoneDebounced(r);
      }
    }
  }
  /**
  * Clears `SelectionObserver` internal properties connected with preventing infinite loop.
  */
  _clearInfiniteLoop() {
    this._loopbackCounter = 0;
  }
}
class u7 extends xn {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * @inheritDoc
    */
    f(this, "domEventType", [
      "compositionstart",
      "compositionupdate",
      "compositionend"
    ]);
    const i = this.document;
    i.on("compositionstart", () => {
      i.isComposing = !0;
    }, {
      priority: "low"
    }), i.on("compositionend", () => {
      i.isComposing = !1;
    }, {
      priority: "low"
    });
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e, {
      data: e.data
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class P_ {
  /**
  * @param nativeDataTransfer The native [`DataTransfer`](https://developer.mozilla.org/en-US/docs/Web/API/DataTransfer) object.
  * @param options.cacheFiles Whether `files` list should be initialized in the constructor.
  */
  constructor(t, e = {}) {
    /**
    * The array of files created from the native `DataTransfer#files` or `DataTransfer#items`.
    */
    f(this, "_files");
    /**
    * The native DataTransfer object.
    */
    f(this, "_native");
    this._files = e.cacheFiles ? Ap(t) : null, this._native = t;
  }
  /**
  * The array of files created from the native `DataTransfer#files` or `DataTransfer#items`.
  */
  get files() {
    return this._files || (this._files = Ap(this._native)), this._files;
  }
  /**
  * Returns an array of available native content types.
  */
  get types() {
    return this._native.types;
  }
  /**
  * Gets the data from the data transfer by its MIME type.
  *
  * ```ts
  * dataTransfer.getData( 'text/plain' );
  * ```
  *
  * @param type The MIME type. E.g. `text/html` or `text/plain`.
  */
  getData(t) {
    return this._native.getData(t);
  }
  /**
  * Sets the data in the data transfer.
  *
  * @param type The MIME type. E.g. `text/html` or `text/plain`.
  */
  setData(t, e) {
    this._native.setData(t, e);
  }
  /**
  * The effect that is allowed for a drag operation.
  */
  set effectAllowed(t) {
    this._native.effectAllowed = t;
  }
  get effectAllowed() {
    return this._native.effectAllowed;
  }
  /**
  * The actual drop effect.
  */
  set dropEffect(t) {
    this._native.dropEffect = t;
  }
  get dropEffect() {
    return this._native.dropEffect;
  }
  /**
  * Set a preview image of the dragged content.
  */
  setDragImage(t, e, i) {
    this._native.setDragImage(t, e, i);
  }
  /**
  * Whether the dragging operation was canceled.
  */
  get isCanceled() {
    return this._native.dropEffect == "none" || !!this._native.mozUserCancelled;
  }
}
function Ap(n) {
  const t = Array.from(n.files || []), e = Array.from(n.items || []);
  return t.length ? t : e.filter((i) => i.kind === "file").map((i) => i.getAsFile());
}
class d7 extends xn {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    f(this, "domEventType", "beforeinput");
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    const i = e.getTargetRanges(), s = this.view, r = s.document;
    let o = null, a = null, l = [];
    if (e.dataTransfer && (o = new P_(e.dataTransfer)), e.data !== null ? a = e.data : o && (a = o.getData("text/plain")), r.selection.isFake)
      l = Array.from(r.selection.getRanges());
    else if (i.length)
      l = i.map((c) => {
        const u = s.domConverter.domPositionToView(c.startContainer, c.startOffset), d = s.domConverter.domPositionToView(c.endContainer, c.endOffset);
        if (u)
          return s.createRange(u, d);
        if (d)
          return s.createRange(d);
      }).filter((c) => !!c);
    else if (be.isAndroid) {
      const c = e.target.ownerDocument.defaultView.getSelection();
      l = Array.from(s.domConverter.domSelectionToView(c).getRanges());
    }
    if (be.isAndroid && e.inputType == "insertCompositionText" && a && a.endsWith(`
`)) {
      this.fire(e.type, e, {
        inputType: "insertParagraph",
        targetRanges: [
          s.createRange(l[0].end)
        ]
      });
      return;
    }
    if (e.inputType == "insertText" && a && a.includes(`
`)) {
      const c = a.split(/\n{1,2}/g);
      let u = l;
      for (let d = 0; d < c.length; d++) {
        const h = c[d];
        h != "" && (this.fire(e.type, e, {
          data: h,
          dataTransfer: o,
          targetRanges: u,
          inputType: e.inputType,
          isComposing: e.isComposing
        }), u = [
          r.selection.getFirstRange()
        ]), d + 1 < c.length && (this.fire(e.type, e, {
          inputType: "insertParagraph",
          targetRanges: u
        }), u = [
          r.selection.getFirstRange()
        ]);
      }
      return;
    }
    this.fire(e.type, e, {
      data: a,
      dataTransfer: o,
      targetRanges: l,
      inputType: e.inputType,
      isComposing: e.isComposing
    });
  }
}
class h7 extends sn {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.document.on("keydown", (e, i) => {
      if (this.isEnabled && Z4(i.keyCode)) {
        const s = new io(this.document, "arrowKey", this.document.selection.getFirstRange());
        this.document.fire(s, i), s.stop.called && e.stop();
      }
    });
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
class f7 extends sn {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t);
    const e = this.document;
    e.on("keydown", (i, s) => {
      if (!this.isEnabled || s.keyCode != Ie.tab || s.ctrlKey)
        return;
      const r = new io(e, "tab", e.selection.getFirstRange());
      e.fire(r, s), r.stop.called && i.stop();
    });
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
let m7 = class extends (/* @__PURE__ */ je()) {
  /**
  * @param stylesProcessor The styles processor instance.
  */
  constructor(e) {
    super();
    /**
    * Instance of the {@link module:engine/view/document~Document} associated with this view controller.
    */
    f(this, "document");
    /**
    * Instance of the {@link module:engine/view/domconverter~DomConverter domConverter} used by
    * {@link module:engine/view/view~View#_renderer renderer}
    * and {@link module:engine/view/observer/observer~Observer observers}.
    */
    f(this, "domConverter");
    /**
    * Roots of the DOM tree. Map on the `HTMLElement`s with roots names as keys.
    */
    f(this, "domRoots", /* @__PURE__ */ new Map());
    /**
    * Instance of the {@link module:engine/view/renderer~Renderer renderer}.
    */
    f(this, "_renderer");
    /**
    * A DOM root attributes cache. It saves the initial values of DOM root attributes before the DOM element
    * is {@link module:engine/view/view~View#attachDomRoot attached} to the view so later on, when
    * the view is destroyed ({@link module:engine/view/view~View#detachDomRoot}), they can be easily restored.
    * This way, the DOM element can go back to the (clean) state as if the editing view never used it.
    */
    f(this, "_initialDomRootAttributes", /* @__PURE__ */ new WeakMap());
    /**
    * Map of registered {@link module:engine/view/observer/observer~Observer observers}.
    */
    f(this, "_observers", /* @__PURE__ */ new Map());
    /**
    * DowncastWriter instance used in {@link #change change method} callbacks.
    */
    f(this, "_writer");
    /**
    * Is set to `true` when {@link #change view changes} are currently in progress.
    */
    f(this, "_ongoingChange", !1);
    /**
    * Used to prevent calling {@link #forceRender} and {@link #change} during rendering view to the DOM.
    */
    f(this, "_postFixersInProgress", !1);
    /**
    * Internal flag to temporary disable rendering. See the usage in the {@link #_disableRendering}.
    */
    f(this, "_renderingDisabled", !1);
    /**
    * Internal flag that disables rendering when there are no changes since the last rendering.
    * It stores information about changed selection and changed elements from attached document roots.
    */
    f(this, "_hasChangedSinceTheLastRendering", !1);
    this.document = new Gc(e), this.domConverter = new Zc(this.document), this.set("isRenderingInProgress", !1), this.set("hasDomSelection", !1), this._renderer = new U6(this.domConverter, this.document.selection), this._renderer.bind("isFocused", "isSelecting", "isComposing").to(this.document, "isFocused", "isSelecting", "isComposing"), this._writer = new C_(this.document), this.addObserver(I_), this.addObserver(Ql), this.addObserver(c7), this.addObserver(o7), this.addObserver(a7), this.addObserver(u7), this.addObserver(h7), this.addObserver(d7), this.addObserver(f7), H6(this), L6(this), this.on("render", () => {
      this._render(), this.document.fire("layoutChanged"), this._hasChangedSinceTheLastRendering = !1;
    }), this.listenTo(this.document.selection, "change", () => {
      this._hasChangedSinceTheLastRendering = !0;
    }), this.listenTo(this.document, "change:isFocused", () => {
      this._hasChangedSinceTheLastRendering = !0;
    }), be.isiOS && this.listenTo(this.document, "blur", (i, s) => {
      this.domConverter.mapDomToView(s.domEvent.relatedTarget) || this.domConverter._clearDomSelection();
    }), this.listenTo(this.document, "mutations", (i, { mutations: s }) => {
      s.forEach((r) => this._renderer.markToSync(r.type, r.node));
    }, {
      priority: "low"
    }), this.listenTo(this.document, "mutations", () => {
      this.forceRender();
    }, {
      priority: "lowest"
    });
  }
  /**
  * Attaches a DOM root element to the view element and enable all observers on that element.
  * Also {@link module:engine/view/renderer~Renderer#markToSync mark element} to be synchronized
  * with the view what means that all child nodes will be removed and replaced with content of the view root.
  *
  * This method also will change view element name as the same as tag name of given dom root.
  * Name is always transformed to lower case.
  *
  * **Note:** Use {@link #detachDomRoot `detachDomRoot()`} to revert this action.
  *
  * @param domRoot DOM root element.
  * @param name Name of the root.
  */
  attachDomRoot(e, i = "main") {
    const s = this.document.getRoot(i);
    s._name = e.tagName.toLowerCase();
    const r = {};
    for (const { name: a, value: l } of Array.from(e.attributes))
      r[a] = l, a === "class" ? this._writer.addClass(l.split(" "), s) : s.hasAttribute(a) || this._writer.setAttribute(a, l, s);
    this._initialDomRootAttributes.set(e, r);
    const o = () => {
      this._writer.setAttribute("contenteditable", (!s.isReadOnly).toString(), s), s.isReadOnly ? this._writer.addClass("ck-read-only", s) : this._writer.removeClass("ck-read-only", s);
    };
    o(), this.domRoots.set(i, e), this.domConverter.bindElements(e, s), this._renderer.markToSync("children", s), this._renderer.markToSync("attributes", s), this._renderer.domDocuments.add(e.ownerDocument), s.on("change:children", (a, l) => this._renderer.markToSync("children", l)), s.on("change:attributes", (a, l) => this._renderer.markToSync("attributes", l)), s.on("change:text", (a, l) => this._renderer.markToSync("text", l)), s.on("change:isReadOnly", () => this.change(o)), s.on("change", () => {
      this._hasChangedSinceTheLastRendering = !0;
    });
    for (const a of this._observers.values())
      a.observe(e, i);
  }
  /**
  * Detaches a DOM root element from the view element and restores its attributes to the state before
  * {@link #attachDomRoot `attachDomRoot()`}.
  *
  * @param name Name of the root to detach.
  */
  detachDomRoot(e) {
    const i = this.domRoots.get(e);
    Array.from(i.attributes).forEach(({ name: r }) => i.removeAttribute(r));
    const s = this._initialDomRootAttributes.get(i);
    for (const r in s)
      i.setAttribute(r, s[r]);
    this.domRoots.delete(e), this.domConverter.unbindDomElement(i);
    for (const r of this._observers.values())
      r.stopObserving(i);
  }
  /**
  * Gets DOM root element.
  *
  * @param name  Name of the root.
  * @returns DOM root element instance.
  */
  getDomRoot(e = "main") {
    return this.domRoots.get(e);
  }
  /**
  * Creates observer of the given type if not yet created, {@link module:engine/view/observer/observer~Observer#enable enables} it
  * and {@link module:engine/view/observer/observer~Observer#observe attaches} to all existing and future
  * {@link #domRoots DOM roots}.
  *
  * Note: Observers are recognized by their constructor (classes). A single observer will be instantiated and used only
  * when registered for the first time. This means that features and other components can register a single observer
  * multiple times without caring whether it has been already added or not.
  *
  * @param ObserverConstructor The constructor of an observer to add.
  * Should create an instance inheriting from {@link module:engine/view/observer/observer~Observer}.
  * @returns Added observer instance.
  */
  addObserver(e) {
    let i = this._observers.get(e);
    if (i)
      return i;
    i = new e(this), this._observers.set(e, i);
    for (const [s, r] of this.domRoots)
      i.observe(r, s);
    return i.enable(), i;
  }
  /**
  * Returns observer of the given type or `undefined` if such observer has not been added yet.
  *
  * @param ObserverConstructor The constructor of an observer to get.
  * @returns Observer instance or undefined.
  */
  getObserver(e) {
    return this._observers.get(e);
  }
  /**
  * Disables all added observers.
  */
  disableObservers() {
    for (const e of this._observers.values())
      e.disable();
  }
  /**
  * Enables all added observers.
  */
  enableObservers() {
    for (const e of this._observers.values())
      e.enable();
  }
  /**
  * Scrolls the page viewport and {@link #domRoots} with their ancestors to reveal the
  * caret, **if not already visible to the user**.
  *
  * **Note**: Calling this method fires the {@link module:engine/view/view~ViewScrollToTheSelectionEvent} event that
  * allows custom behaviors.
  *
  * @param options Additional configuration of the scrolling behavior.
  * @param options.viewportOffset A distance between the DOM selection and the viewport boundary to be maintained
  * while scrolling to the selection (default is 20px). Setting this value to `0` will reveal the selection precisely at
  * the viewport boundary.
  * @param options.ancestorOffset A distance between the DOM selection and scrollable DOM root ancestor(s) to be maintained
  * while scrolling to the selection (default is 20px). Setting this value to `0` will reveal the selection precisely at
  * the scrollable ancestor(s) boundary.
  * @param options.alignToTop When set `true`, the DOM selection will be aligned to the top of the viewport if not already visible
  * (see `forceScroll` to learn more).
  * @param options.forceScroll When set `true`, the DOM selection will be aligned to the top of the viewport and scrollable ancestors
  * whether it is already visible or not. This option will only work when `alignToTop` is `true`.
  */
  scrollToTheSelection({ alignToTop: e, forceScroll: i, viewportOffset: s = 20, ancestorOffset: r = 20 } = {}) {
    const o = this.document.selection.getFirstRange();
    if (!o)
      return;
    const a = Di({
      alignToTop: e,
      forceScroll: i,
      viewportOffset: s,
      ancestorOffset: r
    });
    typeof s == "number" && (s = {
      top: s,
      bottom: s,
      left: s,
      right: s
    });
    const l = {
      target: this.domConverter.viewRangeToDom(o),
      viewportOffset: s,
      ancestorOffset: r,
      alignToTop: e,
      forceScroll: i
    };
    this.fire("scrollToTheSelection", l, a), f_(l);
  }
  /**
  * It will focus DOM element representing {@link module:engine/view/editableelement~EditableElement EditableElement}
  * that is currently having selection inside.
  */
  focus() {
    if (!this.document.isFocused) {
      const e = this.document.selection.editableElement;
      e && (this.domConverter.focus(e), this.forceRender());
    }
  }
  /**
  * The `change()` method is the primary way of changing the view. You should use it to modify any node in the view tree.
  * It makes sure that after all changes are made the view is rendered to the DOM (assuming that the view will be changed
  * inside the callback). It prevents situations when the DOM is updated when the view state is not yet correct. It allows
  * to nest calls one inside another and still performs a single rendering after all those changes are made.
  * It also returns the return value of its callback.
  *
  * ```ts
  * const text = view.change( writer => {
  * 	const newText = writer.createText( 'foo' );
  * 	writer.insert( position1, newText );
  *
  * 	view.change( writer => {
  * 		writer.insert( position2, writer.createText( 'bar' ) );
  * 	} );
  *
  * 	writer.remove( range );
  *
  * 	return newText;
  * } );
  * ```
  *
  * When the outermost change block is done and rendering to the DOM is over the
  * {@link module:engine/view/view~View#event:render `View#render`} event is fired.
  *
  * This method throws a `applying-view-changes-on-rendering` error when
  * the change block is used after rendering to the DOM has started.
  *
  * @param callback Callback function which may modify the view.
  * @returns Value returned by the callback.
  */
  change(e) {
    if (this.isRenderingInProgress || this._postFixersInProgress)
      throw new S("cannot-change-view-tree", this);
    try {
      if (this._ongoingChange)
        return e(this._writer);
      this._ongoingChange = !0;
      const i = e(this._writer);
      return this._ongoingChange = !1, !this._renderingDisabled && this._hasChangedSinceTheLastRendering && (this._postFixersInProgress = !0, this.document._callPostFixers(this._writer), this._postFixersInProgress = !1, this.fire("render")), i;
    } catch (i) {
      /* istanbul ignore next -- @preserve */
      S.rethrowUnexpectedError(i, this);
    }
  }
  /**
  * Forces rendering {@link module:engine/view/document~Document view document} to DOM. If any view changes are
  * currently in progress, rendering will start after all {@link #change change blocks} are processed.
  *
  * Note that this method is dedicated for special cases. All view changes should be wrapped in the {@link #change}
  * block and the view will automatically check whether it needs to render DOM or not.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `applying-view-changes-on-rendering` when
  * trying to re-render when rendering to DOM has already started.
  */
  forceRender() {
    this._hasChangedSinceTheLastRendering = !0, this.getObserver(Ql).flush(), this.change(() => {
    });
  }
  /**
  * Destroys this instance. Makes sure that all observers are destroyed and listeners removed.
  */
  destroy() {
    for (const e of this._observers.values())
      e.destroy();
    this.document.destroy(), this.stopListening();
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/view/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/view/item~Item view item} and `'before'` or `'after'` (sets position before or after given view item).
  *
  * This method is a shortcut to other constructors such as:
  *
  * * {@link #createPositionBefore},
  * * {@link #createPositionAfter},
  *
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  createPositionAt(e, i) {
    return re._createAt(e, i);
  }
  /**
  * Creates a new position after given view item.
  *
  * @param item View item after which the position should be located.
  */
  createPositionAfter(e) {
    return re._createAfter(e);
  }
  /**
  * Creates a new position before given view item.
  *
  * @param item View item before which the position should be located.
  */
  createPositionBefore(e) {
    return re._createBefore(e);
  }
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * **Note:** This factory method creates it's own {@link module:engine/view/position~Position} instances basing on passed values.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at `start` position.
  */
  createRange(e, i) {
    return new Re(e, i);
  }
  /**
  * Creates a range that starts before given {@link module:engine/view/item~Item view item} and ends after it.
  */
  createRangeOn(e) {
    return Re._createOn(e);
  }
  /**
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(e) {
    return Re._createIn(e);
  }
  createSelection(...e) {
    return new Dn(...e);
  }
  /**
  * Disables or enables rendering. If the flag is set to `true` then the rendering will be disabled.
  * If the flag is set to `false` and if there was some change in the meantime, then the rendering action will be performed.
  *
  * @internal
  * @param flag A flag indicates whether the rendering should be disabled.
  */
  _disableRendering(e) {
    this._renderingDisabled = e, e == !1 && this.change(() => {
    });
  }
  /**
  * Renders all changes. In order to avoid triggering the observers (e.g. selection) all observers are disabled
  * before rendering and re-enabled after that.
  */
  _render() {
    this.isRenderingInProgress = !0, this.disableObservers(), this._renderer.render(), this.enableObservers(), this.isRenderingInProgress = !1;
  }
};
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class bs {
  /* istanbul ignore next -- @preserve */
  is() {
    throw new Error("is() method is abstract");
  }
}
class Ki extends bs {
  /**
  * Creates a text proxy.
  *
  * @internal
  * @param textNode Text node which part is represented by this text proxy.
  * @param offsetInText Offset in {@link module:engine/model/textproxy~TextProxy#textNode text node} from which the text proxy
  * starts.
  * @param length Text proxy length, that is how many text node's characters, starting from `offsetInText` it represents.
  */
  constructor(e, i, s) {
    super();
    /**
    * Text node which part is represented by this text proxy.
    */
    f(this, "textNode");
    /**
    * Text data represented by this text proxy.
    */
    f(this, "data");
    /**
    * Offset in {@link module:engine/model/textproxy~TextProxy#textNode text node} from which the text proxy starts.
    */
    f(this, "offsetInText");
    if (this.textNode = e, i < 0 || i > e.offsetSize)
      throw new S("model-textproxy-wrong-offsetintext", this);
    if (s < 0 || i + s > e.offsetSize)
      throw new S("model-textproxy-wrong-length", this);
    this.data = e.data.substring(i, i + s), this.offsetInText = i;
  }
  /**
  * Offset at which this text proxy starts in it's parent.
  *
  * @see module:engine/model/node~Node#startOffset
  */
  get startOffset() {
    return this.textNode.startOffset !== null ? this.textNode.startOffset + this.offsetInText : null;
  }
  /**
  * Offset size of this text proxy. Equal to the number of characters represented by the text proxy.
  *
  * @see module:engine/model/node~Node#offsetSize
  */
  get offsetSize() {
    return this.data.length;
  }
  /**
  * Offset at which this text proxy ends in it's parent.
  *
  * @see module:engine/model/node~Node#endOffset
  */
  get endOffset() {
    return this.startOffset !== null ? this.startOffset + this.offsetSize : null;
  }
  /**
  * Flag indicating whether `TextProxy` instance covers only part of the original {@link module:engine/model/text~Text text node}
  * (`true`) or the whole text node (`false`).
  *
  * This is `false` when text proxy starts at the very beginning of {@link module:engine/model/textproxy~TextProxy#textNode textNode}
  * ({@link module:engine/model/textproxy~TextProxy#offsetInText offsetInText} equals `0`) and text proxy sizes is equal to
  * text node size.
  */
  get isPartial() {
    return this.offsetSize !== this.textNode.offsetSize;
  }
  /**
  * Parent of this text proxy, which is same as parent of text node represented by this text proxy.
  */
  get parent() {
    return this.textNode.parent;
  }
  /**
  * Root of this text proxy, which is same as root of text node represented by this text proxy.
  */
  get root() {
    return this.textNode.root;
  }
  /**
  * Gets path to this text proxy.
  *
  * @see module:engine/model/node~Node#getPath
  */
  getPath() {
    const e = this.textNode.getPath();
    return e.length > 0 && (e[e.length - 1] += this.offsetInText), e;
  }
  /**
  * Returns ancestors array of this text proxy.
  *
  * @param options Options object.
  * @param options.includeSelf When set to `true` this text proxy will be also included in parent's array.
  * @param options.parentFirst When set to `true`, array will be sorted from text proxy parent to root element,
  * otherwise root element will be the first item in the array.
  * @returns Array with ancestors.
  */
  getAncestors(e = {}) {
    const i = [];
    let s = e.includeSelf ? this : this.parent;
    for (; s; )
      i[e.parentFirst ? "push" : "unshift"](s), s = s.parent;
    return i;
  }
  /**
  * Checks if this text proxy has an attribute for given key.
  *
  * @param key Key of attribute to check.
  * @returns `true` if attribute with given key is set on text proxy, `false` otherwise.
  */
  hasAttribute(e) {
    return this.textNode.hasAttribute(e);
  }
  /**
  * Gets an attribute value for given key or `undefined` if that attribute is not set on text proxy.
  *
  * @param key Key of attribute to look for.
  * @returns Attribute value or `undefined`.
  */
  getAttribute(e) {
    return this.textNode.getAttribute(e);
  }
  /**
  * Returns iterator that iterates over this node's attributes. Attributes are returned as arrays containing two
  * items. First one is attribute key and second is attribute value.
  *
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  getAttributes() {
    return this.textNode.getAttributes();
  }
  /**
  * Returns iterator that iterates over this node's attribute keys.
  */
  getAttributeKeys() {
    return this.textNode.getAttributeKeys();
  }
}
Ki.prototype.is = function(n) {
  return n === "$textProxy" || n === "model:$textProxy" || // This are legacy values kept for backward compatibility.
  n === "textProxy" || n === "model:textProxy";
};
class Fn {
  /**
  * Creates a range iterator. All parameters are optional, but you have to specify either `boundaries` or `startPosition`.
  *
  * @param options Object with configuration.
  */
  constructor(t) {
    /**
    * Walking direction. Defaults `'forward'`.
    */
    f(this, "direction");
    /**
    * Iterator boundaries.
    *
    * When the iterator is walking `'forward'` on the end of boundary or is walking `'backward'`
    * on the start of boundary, then `{ done: true }` is returned.
    *
    * If boundaries are not defined they are set before first and after last child of the root node.
    */
    f(this, "boundaries");
    /**
    * Flag indicating whether all consecutive characters with the same attributes should be
    * returned as one {@link module:engine/model/textproxy~TextProxy} (`true`) or one by one (`false`).
    */
    f(this, "singleCharacters");
    /**
    * Flag indicating whether iterator should enter elements or not. If the iterator is shallow child nodes of any
    * iterated node will not be returned along with `elementEnd` tag.
    */
    f(this, "shallow");
    /**
    * Flag indicating whether iterator should ignore `elementEnd` tags. If the option is true walker will not
    * return a parent node of the start position. If this option is `true` each {@link module:engine/model/element~Element} will
    * be returned once, while if the option is `false` they might be returned twice:
    * for `'elementStart'` and `'elementEnd'`.
    */
    f(this, "ignoreElementEnd");
    /**
    * Iterator position. This is always static position, even if the initial position was a
    * {@link module:engine/model/liveposition~LivePosition live position}. If start position is not defined then position depends
    * on {@link #direction}. If direction is `'forward'` position starts form the beginning, when direction
    * is `'backward'` position starts from the end.
    */
    f(this, "_position");
    /**
    * Start boundary cached for optimization purposes.
    */
    f(this, "_boundaryStartParent");
    /**
    * End boundary cached for optimization purposes.
    */
    f(this, "_boundaryEndParent");
    /**
    * Parent of the most recently visited node. Cached for optimization purposes.
    */
    f(this, "_visitedParent");
    if (!t || !t.boundaries && !t.startPosition)
      throw new S("model-tree-walker-no-start-position", null);
    const e = t.direction || "forward";
    if (e != "forward" && e != "backward")
      throw new S("model-tree-walker-unknown-direction", t, {
        direction: e
      });
    this.direction = e, this.boundaries = t.boundaries || null, t.startPosition ? this._position = t.startPosition.clone() : this._position = G._createAt(this.boundaries[this.direction == "backward" ? "end" : "start"]), this.position.stickiness = "toNone", this.singleCharacters = !!t.singleCharacters, this.shallow = !!t.shallow, this.ignoreElementEnd = !!t.ignoreElementEnd, this._boundaryStartParent = this.boundaries ? this.boundaries.start.parent : null, this._boundaryEndParent = this.boundaries ? this.boundaries.end.parent : null, this._visitedParent = this.position.parent;
  }
  /**
  * Iterable interface.
  *
  * @returns {Iterable.<module:engine/model/treewalker~TreeWalkerValue>}
  */
  [Symbol.iterator]() {
    return this;
  }
  /**
  * Iterator position. This is always static position, even if the initial position was a
  * {@link module:engine/model/liveposition~LivePosition live position}. If start position is not defined then position depends
  * on {@link #direction}. If direction is `'forward'` position starts form the beginning, when direction
  * is `'backward'` position starts from the end.
  */
  get position() {
    return this._position;
  }
  /**
  * Moves {@link #position} in the {@link #direction} skipping values as long as the callback function returns `true`.
  *
  * For example:
  *
  * ```ts
  * walker.skip( value => value.type == 'text' ); // <paragraph>[]foo</paragraph> -> <paragraph>foo[]</paragraph>
  * walker.skip( () => true ); // Move the position to the end: <paragraph>[]foo</paragraph> -> <paragraph>foo</paragraph>[]
  * walker.skip( () => false ); // Do not move the position.
  * ```
  *
  * @param skip Callback function. Gets {@link module:engine/model/treewalker~TreeWalkerValue} and should
  * return `true` if the value should be skipped or `false` if not.
  */
  skip(t) {
    let e, i, s, r;
    do
      s = this.position, r = this._visitedParent, { done: e, value: i } = this.next();
    while (!e && t(i));
    e || (this._position = s, this._visitedParent = r);
  }
  /**
  * Moves tree walker {@link #position} to provided `position`. Tree walker will
  * continue traversing from that position.
  *
  * Note: in contrary to {@link ~TreeWalker#skip}, this method does not iterate over the nodes along the way.
  * It simply sets the current tree walker position to a new one.
  * From the performance standpoint, it is better to use {@link ~TreeWalker#jumpTo} rather than {@link ~TreeWalker#skip}.
  *
  * If the provided position is before the start boundary, the position will be
  * set to the start boundary. If the provided position is after the end boundary,
  * the position will be set to the end boundary.
  * This is done to prevent the treewalker from traversing outside the boundaries.
  *
  * @param position Position to jump to.
  */
  jumpTo(t) {
    this._boundaryStartParent && t.isBefore(this.boundaries.start) ? t = this.boundaries.start : this._boundaryEndParent && t.isAfter(this.boundaries.end) && (t = this.boundaries.end), this._position = t.clone(), this._visitedParent = t.parent;
  }
  /**
  * Gets the next tree walker's value.
  */
  next() {
    return this.direction == "forward" ? this._next() : this._previous();
  }
  /**
  * Makes a step forward in model. Moves the {@link #position} to the next position and returns the encountered value.
  */
  _next() {
    const t = this.position, e = this.position.clone(), i = this._visitedParent;
    if (i.parent === null && e.offset === i.maxOffset)
      return {
        done: !0,
        value: void 0
      };
    if (i === this._boundaryEndParent && e.offset == this.boundaries.end.offset)
      return {
        done: !0,
        value: void 0
      };
    const s = Ho(e, i), r = s || R_(e, i, s);
    if (r && r.is("model:element")) {
      if (!this.shallow)
        e.path.push(0), this._visitedParent = r;
      else {
        if (this.boundaries && this.boundaries.end.isBefore(e))
          return {
            done: !0,
            value: void 0
          };
        e.offset++;
      }
      return this._position = e, Cs("elementStart", r, t, e, 1);
    }
    if (r && r.is("model:$text")) {
      let o;
      if (this.singleCharacters)
        o = 1;
      else {
        let c = r.endOffset;
        this._boundaryEndParent == i && this.boundaries.end.offset < c && (c = this.boundaries.end.offset), o = c - e.offset;
      }
      const a = e.offset - r.startOffset, l = new Ki(r, a, o);
      return e.offset += o, this._position = e, Cs("text", l, t, e, o);
    }
    return e.path.pop(), e.offset++, this._position = e, this._visitedParent = i.parent, this.ignoreElementEnd ? this._next() : Cs("elementEnd", i, t, e);
  }
  /**
  * Makes a step backward in model. Moves the {@link #position} to the previous position and returns the encountered value.
  */
  _previous() {
    const t = this.position, e = this.position.clone(), i = this._visitedParent;
    if (i.parent === null && e.offset === 0)
      return {
        done: !0,
        value: void 0
      };
    if (i == this._boundaryStartParent && e.offset == this.boundaries.start.offset)
      return {
        done: !0,
        value: void 0
      };
    const s = e.parent, r = Ho(e, s), o = r || O_(e, s, r);
    if (o && o.is("model:element"))
      return e.offset--, this.shallow ? (this._position = e, Cs("elementStart", o, t, e, 1)) : (e.path.push(o.maxOffset), this._position = e, this._visitedParent = o, this.ignoreElementEnd ? this._previous() : Cs("elementEnd", o, t, e));
    if (o && o.is("model:$text")) {
      let a;
      if (this.singleCharacters)
        a = 1;
      else {
        let u = o.startOffset;
        this._boundaryStartParent == i && this.boundaries.start.offset > u && (u = this.boundaries.start.offset), a = e.offset - u;
      }
      const l = e.offset - o.startOffset, c = new Ki(o, l - a, a);
      return e.offset -= a, this._position = e, Cs("text", c, t, e, a);
    }
    return e.path.pop(), this._position = e, this._visitedParent = i.parent, Cs("elementStart", i, t, e, 1);
  }
}
function Cs(n, t, e, i, s) {
  return {
    done: !1,
    value: {
      type: n,
      item: t,
      previousPosition: e,
      nextPosition: i,
      length: s
    }
  };
}
class G extends bs {
  /**
  * Creates a position.
  *
  * @param root Root of the position.
  * @param path Position path. See {@link module:engine/model/position~Position#path}.
  * @param stickiness Position stickiness. See {@link module:engine/model/position~PositionStickiness}.
  */
  constructor(e, i, s = "toNone") {
    super();
    /**
    * Root of the position path.
    */
    f(this, "root");
    /**
    * Position of the node in the tree. **Path contains offsets, not indexes.**
    *
    * Position can be placed before, after or in a {@link module:engine/model/node~Node node} if that node has
    * {@link module:engine/model/node~Node#offsetSize} greater than `1`. Items in position path are
    * {@link module:engine/model/node~Node#startOffset starting offsets} of position ancestors, starting from direct root children,
    * down to the position offset in it's parent.
    *
    * ```
    * ROOT
    *  |- P            before: [ 0 ]         after: [ 1 ]
    *  |- UL           before: [ 1 ]         after: [ 2 ]
    *     |- LI        before: [ 1, 0 ]      after: [ 1, 1 ]
    *     |  |- foo    before: [ 1, 0, 0 ]   after: [ 1, 0, 3 ]
    *     |- LI        before: [ 1, 1 ]      after: [ 1, 2 ]
    *        |- bar    before: [ 1, 1, 0 ]   after: [ 1, 1, 3 ]
    * ```
    *
    * `foo` and `bar` are representing {@link module:engine/model/text~Text text nodes}. Since text nodes has offset size
    * greater than `1` you can place position offset between their start and end:
    *
    * ```
    * ROOT
    *  |- P
    *  |- UL
    *     |- LI
    *     |  |- f^o|o  ^ has path: [ 1, 0, 1 ]   | has path: [ 1, 0, 2 ]
    *     |- LI
    *        |- b^a|r  ^ has path: [ 1, 1, 1 ]   | has path: [ 1, 1, 2 ]
    * ```
    */
    f(this, "path");
    /**
    * Position stickiness. See {@link module:engine/model/position~PositionStickiness}.
    */
    f(this, "stickiness");
    if (!e.is("element") && !e.is("documentFragment"))
      throw new S("model-position-root-invalid", e);
    if (!Array.isArray(i) || i.length === 0)
      throw new S("model-position-path-incorrect-format", e, {
        path: i
      });
    e.is("rootElement") ? i = i.slice() : (i = [
      ...e.getPath(),
      ...i
    ], e = e.root), this.root = e, this.path = i, this.stickiness = s;
  }
  /**
  * Offset at which this position is located in its {@link module:engine/model/position~Position#parent parent}. It is equal
  * to the last item in position {@link module:engine/model/position~Position#path path}.
  *
  * @type {Number}
  */
  get offset() {
    return this.path[this.path.length - 1];
  }
  set offset(e) {
    this.path[this.path.length - 1] = e;
  }
  /**
  * Parent element of this position.
  *
  * Keep in mind that `parent` value is calculated when the property is accessed.
  * If {@link module:engine/model/position~Position#path position path}
  * leads to a non-existing element, `parent` property will throw error.
  *
  * Also it is a good idea to cache `parent` property if it is used frequently in an algorithm (i.e. in a long loop).
  */
  get parent() {
    let e = this.root;
    for (let i = 0; i < this.path.length - 1; i++)
      if (e = e.getChildAtOffset(this.path[i]), !e)
        throw new S("model-position-path-incorrect", this, {
          position: this
        });
    if (e.is("$text"))
      throw new S("model-position-path-incorrect", this, {
        position: this
      });
    return e;
  }
  /**
  * Position {@link module:engine/model/position~Position#offset offset} converted to an index in position's parent node. It is
  * equal to the {@link module:engine/model/node~Node#index index} of a node after this position. If position is placed
  * in text node, position index is equal to the index of that text node.
  */
  get index() {
    return this.parent.offsetToIndex(this.offset);
  }
  /**
  * Returns {@link module:engine/model/text~Text text node} instance in which this position is placed or `null` if this
  * position is not in a text node.
  */
  get textNode() {
    return Ho(this, this.parent);
  }
  /**
  * Node directly after this position. Returns `null` if this position is at the end of its parent, or if it is in a text node.
  */
  get nodeAfter() {
    const e = this.parent;
    return R_(this, e, Ho(this, e));
  }
  /**
  * Node directly before this position. Returns `null` if this position is at the start of its parent, or if it is in a text node.
  */
  get nodeBefore() {
    const e = this.parent;
    return O_(this, e, Ho(this, e));
  }
  /**
  * Is `true` if position is at the beginning of its {@link module:engine/model/position~Position#parent parent}, `false` otherwise.
  */
  get isAtStart() {
    return this.offset === 0;
  }
  /**
  * Is `true` if position is at the end of its {@link module:engine/model/position~Position#parent parent}, `false` otherwise.
  */
  get isAtEnd() {
    return this.offset == this.parent.maxOffset;
  }
  /**
  * Checks whether the position is valid in current model tree, that is whether it points to an existing place in the model.
  */
  isValid() {
    if (this.offset < 0)
      return !1;
    let e = this.root;
    for (let i = 0; i < this.path.length - 1; i++)
      if (e = e.getChildAtOffset(this.path[i]), !e)
        return !1;
    return this.offset <= e.maxOffset;
  }
  /**
  * Checks whether this position is before or after given position.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  */
  compareWith(e) {
    if (this.root != e.root)
      return "different";
    const i = Zt(this.path, e.path);
    switch (i) {
      case "same":
        return "same";
      case "prefix":
        return "before";
      case "extension":
        return "after";
      default:
        return this.path[i] < e.path[i] ? "before" : "after";
    }
  }
  /**
  * Gets the farthest position which matches the callback using
  * {@link module:engine/model/treewalker~TreeWalker TreeWalker}.
  *
  * For example:
  *
  * ```ts
  * getLastMatchingPosition( value => value.type == 'text' );
  * // <paragraph>[]foo</paragraph> -> <paragraph>foo[]</paragraph>
  *
  * getLastMatchingPosition( value => value.type == 'text', { direction: 'backward' } );
  * // <paragraph>foo[]</paragraph> -> <paragraph>[]foo</paragraph>
  *
  * getLastMatchingPosition( value => false );
  * // Do not move the position.
  * ```
  *
  * @param skip Callback function. Gets {@link module:engine/model/treewalker~TreeWalkerValue} and should
  * return `true` if the value should be skipped or `false` if not.
  * @param options Object with configuration options. See {@link module:engine/model/treewalker~TreeWalker}.
  *
  * @returns The position after the last item which matches the `skip` callback test.
  */
  getLastMatchingPosition(e, i = {}) {
    i.startPosition = this;
    const s = new Fn(i);
    return s.skip(e), s.position;
  }
  /**
  * Returns a path to this position's parent. Parent path is equal to position {@link module:engine/model/position~Position#path path}
  * but without the last item.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @returns Path to the parent.
  */
  getParentPath() {
    return this.path.slice(0, -1);
  }
  /**
  * Returns ancestors array of this position, that is this position's parent and its ancestors.
  *
  * @returns Array with ancestors.
  */
  getAncestors() {
    const e = this.parent;
    return e.is("documentFragment") ? [
      e
    ] : e.getAncestors({
      includeSelf: !0
    });
  }
  /**
  * Returns the parent element of the given name. Returns null if the position is not inside the desired parent.
  *
  * @param parentName The name of the parent element to find.
  */
  findAncestor(e) {
    const i = this.parent;
    return i.is("element") ? i.findAncestor(e, {
      includeSelf: !0
    }) : null;
  }
  /**
  * Returns the slice of two position {@link #path paths} which is identical. The {@link #root roots}
  * of these two paths must be identical.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param position The second position.
  * @returns The common path.
  */
  getCommonPath(e) {
    if (this.root != e.root)
      return [];
    const i = Zt(this.path, e.path), s = typeof i == "string" ? Math.min(this.path.length, e.path.length) : i;
    return this.path.slice(0, s);
  }
  /**
  * Returns an {@link module:engine/model/element~Element} or {@link module:engine/model/documentfragment~DocumentFragment}
  * which is a common ancestor of both positions. The {@link #root roots} of these two positions must be identical.
  *
  * @param position The second position.
  */
  getCommonAncestor(e) {
    const i = this.getAncestors(), s = e.getAncestors();
    let r = 0;
    for (; i[r] == s[r] && i[r]; )
      r++;
    return r === 0 ? null : i[r - 1];
  }
  /**
  * Returns a new instance of `Position`, that has same {@link #parent parent} but it's offset
  * is shifted by `shift` value (can be a negative value).
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param shift Offset shift. Can be a negative value.
  * @returns Shifted position.
  */
  getShiftedBy(e) {
    const i = this.clone(), s = i.offset + e;
    return i.offset = s < 0 ? 0 : s, i;
  }
  /**
  * Checks whether this position is after given position.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @see module:engine/model/position~Position#isBefore
  * @param  otherPosition Position to compare with.
  * @returns True if this position is after given position.
  */
  isAfter(e) {
    return this.compareWith(e) == "after";
  }
  /**
  * Checks whether this position is before given position.
  *
  * **Note:** watch out when using negation of the value returned by this method, because the negation will also
  * be `true` if positions are in different roots and you might not expect this. You should probably use
  * `a.isAfter( b ) || a.isEqual( b )` or `!a.isBefore( p ) && a.root == b.root` in most scenarios. If your
  * condition uses multiple `isAfter` and `isBefore` checks, build them so they do not use negated values, i.e.:
  *
  * ```ts
  * if ( a.isBefore( b ) && c.isAfter( d ) ) {
  * 	// do A.
  * } else {
  * 	// do B.
  * }
  * ```
  *
  * or, if you have only one if-branch:
  *
  * ```ts
  * if ( !( a.isBefore( b ) && c.isAfter( d ) ) {
  * 	// do B.
  * }
  * ```
  *
  * rather than:
  *
  * ```ts
  * if ( !a.isBefore( b ) || && !c.isAfter( d ) ) {
  * 	// do B.
  * } else {
  * 	// do A.
  * }
  * ```
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param otherPosition Position to compare with.
  * @returns True if this position is before given position.
  */
  isBefore(e) {
    return this.compareWith(e) == "before";
  }
  /**
  * Checks whether this position is equal to given position.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param otherPosition Position to compare with.
  * @returns True if positions are same.
  */
  isEqual(e) {
    return this.compareWith(e) == "same";
  }
  /**
  * Checks whether this position is touching given position. Positions touch when there are no text nodes
  * or empty nodes in a range between them. Technically, those positions are not equal but in many cases
  * they are very similar or even indistinguishable.
  *
  * @param otherPosition Position to compare with.
  * @returns True if positions touch.
  */
  isTouching(e) {
    if (this.root !== e.root)
      return !1;
    const i = Math.min(this.path.length, e.path.length);
    for (let s = 0; s < i; s++) {
      const r = this.path[s] - e.path[s];
      if (r < -1 || r > 1)
        return !1;
      if (r === 1)
        return Ip(e, this, s);
      if (r === -1)
        return Ip(this, e, s);
    }
    return this.path.length === e.path.length ? !0 : this.path.length > e.path.length ? uh(this.path, i) : uh(e.path, i);
  }
  /**
  * Checks if two positions are in the same parent.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param position Position to compare with.
  * @returns `true` if positions have the same parent, `false` otherwise.
  */
  hasSameParentAs(e) {
    if (this.root !== e.root)
      return !1;
    const i = this.getParentPath(), s = e.getParentPath();
    return Zt(i, s) == "same";
  }
  /**
  * Returns a copy of this position that is transformed by given `operation`.
  *
  * The new position's parameters are updated accordingly to the effect of the `operation`.
  *
  * For example, if `n` nodes are inserted before the position, the returned position {@link ~Position#offset} will be
  * increased by `n`. If the position was in a merged element, it will be accordingly moved to the new element, etc.
  *
  * This method is safe to use it on non-existing positions (for example during operational transformation).
  *
  * @param operation Operation to transform by.
  * @returns Transformed position.
  */
  getTransformedByOperation(e) {
    let i;
    switch (e.type) {
      case "insert":
        i = this._getTransformedByInsertOperation(e);
        break;
      case "move":
      case "remove":
      case "reinsert":
        i = this._getTransformedByMoveOperation(e);
        break;
      case "split":
        i = this._getTransformedBySplitOperation(e);
        break;
      case "merge":
        i = this._getTransformedByMergeOperation(e);
        break;
      default:
        i = G._createAt(this);
        break;
    }
    return i;
  }
  /**
  * Returns a copy of this position transformed by an insert operation.
  *
  * @internal
  */
  _getTransformedByInsertOperation(e) {
    return this._getTransformedByInsertion(e.position, e.howMany);
  }
  /**
  * Returns a copy of this position transformed by a move operation.
  *
  * @internal
  */
  _getTransformedByMoveOperation(e) {
    return this._getTransformedByMove(e.sourcePosition, e.targetPosition, e.howMany);
  }
  /**
  * Returns a copy of this position transformed by a split operation.
  *
  * @internal
  */
  _getTransformedBySplitOperation(e) {
    const i = e.movedRange;
    return i.containsPosition(this) || i.start.isEqual(this) && this.stickiness == "toNext" ? this._getCombined(e.splitPosition, e.moveTargetPosition) : e.graveyardPosition ? this._getTransformedByMove(e.graveyardPosition, e.insertionPosition, 1) : this._getTransformedByInsertion(e.insertionPosition, 1);
  }
  /**
  * Returns a copy of this position transformed by merge operation.
  *
  * @internal
  */
  _getTransformedByMergeOperation(e) {
    const i = e.movedRange, s = i.containsPosition(this) || i.start.isEqual(this);
    let r;
    return s ? (r = this._getCombined(e.sourcePosition, e.targetPosition), e.sourcePosition.isBefore(e.targetPosition) && (r = r._getTransformedByDeletion(e.deletionPosition, 1))) : this.isEqual(e.deletionPosition) ? r = G._createAt(e.deletionPosition) : r = this._getTransformedByMove(e.deletionPosition, e.graveyardPosition, 1), r;
  }
  /**
  * Returns a copy of this position that is updated by removing `howMany` nodes starting from `deletePosition`.
  * It may happen that this position is in a removed node. If that is the case, `null` is returned instead.
  *
  * @internal
  * @param deletePosition Position before the first removed node.
  * @param howMany How many nodes are removed.
  * @returns Transformed position or `null`.
  */
  _getTransformedByDeletion(e, i) {
    const s = G._createAt(this);
    if (this.root != e.root)
      return s;
    if (Zt(e.getParentPath(), this.getParentPath()) == "same") {
      if (e.offset < this.offset) {
        if (e.offset + i > this.offset)
          return null;
        s.offset -= i;
      }
    } else if (Zt(e.getParentPath(), this.getParentPath()) == "prefix") {
      const r = e.path.length - 1;
      if (e.offset <= this.path[r]) {
        if (e.offset + i > this.path[r])
          return null;
        s.path[r] -= i;
      }
    }
    return s;
  }
  /**
  * Returns a copy of this position that is updated by inserting `howMany` nodes at `insertPosition`.
  *
  * @internal
  * @param insertPosition Position where nodes are inserted.
  * @param howMany How many nodes are inserted.
  * @returns Transformed position.
  */
  _getTransformedByInsertion(e, i) {
    const s = G._createAt(this);
    if (this.root != e.root)
      return s;
    if (Zt(e.getParentPath(), this.getParentPath()) == "same")
      (e.offset < this.offset || e.offset == this.offset && this.stickiness != "toPrevious") && (s.offset += i);
    else if (Zt(e.getParentPath(), this.getParentPath()) == "prefix") {
      const r = e.path.length - 1;
      e.offset <= this.path[r] && (s.path[r] += i);
    }
    return s;
  }
  /**
  * Returns a copy of this position that is updated by moving `howMany` nodes from `sourcePosition` to `targetPosition`.
  *
  * @internal
  * @param sourcePosition Position before the first element to move.
  * @param targetPosition Position where moved elements will be inserted.
  * @param howMany How many consecutive nodes to move, starting from `sourcePosition`.
  * @returns Transformed position.
  */
  _getTransformedByMove(e, i, s) {
    if (i = i._getTransformedByDeletion(e, s), e.isEqual(i))
      return G._createAt(this);
    const r = this._getTransformedByDeletion(e, s);
    return r === null || e.isEqual(this) && this.stickiness == "toNext" || e.getShiftedBy(s).isEqual(this) && this.stickiness == "toPrevious" ? this._getCombined(e, i) : r._getTransformedByInsertion(i, s);
  }
  /**
  * Returns a new position that is a combination of this position and given positions.
  *
  * The combined position is a copy of this position transformed by moving a range starting at `source` position
  * to the `target` position. It is expected that this position is inside the moved range.
  *
  * Example:
  *
  * ```ts
  * let original = model.createPositionFromPath( root, [ 2, 3, 1 ] );
  * let source = model.createPositionFromPath( root, [ 2, 2 ] );
  * let target = model.createPositionFromPath( otherRoot, [ 1, 1, 3 ] );
  * original._getCombined( source, target ); // path is [ 1, 1, 4, 1 ], root is `otherRoot`
  * ```
  *
  * Explanation:
  *
  * We have a position `[ 2, 3, 1 ]` and move some nodes from `[ 2, 2 ]` to `[ 1, 1, 3 ]`. The original position
  * was inside moved nodes and now should point to the new place. The moved nodes will be after
  * positions `[ 1, 1, 3 ]`, `[ 1, 1, 4 ]`, `[ 1, 1, 5 ]`. Since our position was in the second moved node,
  * the transformed position will be in a sub-tree of a node at `[ 1, 1, 4 ]`. Looking at original path, we
  * took care of `[ 2, 3 ]` part of it. Now we have to add the rest of the original path to the transformed path.
  * Finally, the transformed position will point to `[ 1, 1, 4, 1 ]`.
  *
  * @internal
  * @param source Beginning of the moved range.
  * @param target Position where the range is moved.
  * @returns Combined position.
  */
  _getCombined(e, i) {
    const s = e.path.length - 1, r = G._createAt(i);
    return r.stickiness = this.stickiness, r.offset = r.offset + this.path[s] - e.offset, r.path = [
      ...r.path,
      ...this.path.slice(s + 1)
    ], r;
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    return {
      root: this.root.toJSON(),
      path: Array.from(this.path),
      stickiness: this.stickiness
    };
  }
  /**
  * Returns a new position that is equal to current position.
  */
  clone() {
    return new this.constructor(this.root, this.path, this.stickiness);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/model/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/model/item~Item model item} and `'before'` or `'after'` (sets position before or after given model item).
  *
  * This method is a shortcut to other factory methods such as:
  *
  * * {@link module:engine/model/position~Position._createBefore},
  * * {@link module:engine/model/position~Position._createAfter}.
  *
  * @internal
  * @param offset Offset or one of the flags. Used only when the first parameter is a {@link module:engine/model/item~Item model item}.
  * @param stickiness Position stickiness. Used only when the first parameter is a {@link module:engine/model/item~Item model item}.
  */
  static _createAt(e, i, s = "toNone") {
    if (e.is("model:position"))
      return new G(e.root, e.path, e.stickiness);
    {
      const r = e;
      if (i == "end")
        i = r.maxOffset;
      else {
        if (i == "before")
          return this._createBefore(r, s);
        if (i == "after")
          return this._createAfter(r, s);
        if (i !== 0 && !i)
          throw new S("model-createpositionat-offset-required", [
            this,
            e
          ]);
      }
      if (!r.is("element") && !r.is("documentFragment"))
        throw new S("model-position-parent-incorrect", [
          this,
          e
        ]);
      const o = r.getPath();
      return o.push(i), new this(r.root, o, s);
    }
  }
  /**
  * Creates a new position, after given {@link module:engine/model/item~Item model item}.
  *
  * @internal
  * @param item Item after which the position should be placed.
  * @param stickiness Position stickiness.
  */
  static _createAfter(e, i) {
    if (!e.parent)
      throw new S("model-position-after-root", [
        this,
        e
      ], {
        root: e
      });
    return this._createAt(e.parent, e.endOffset, i);
  }
  /**
  * Creates a new position, before the given {@link module:engine/model/item~Item model item}.
  *
  * @internal
  * @param item Item before which the position should be placed.
  * @param stickiness Position stickiness.
  */
  static _createBefore(e, i) {
    if (!e.parent)
      throw new S("model-position-before-root", e, {
        root: e
      });
    return this._createAt(e.parent, e.startOffset, i);
  }
  /**
  * Creates a `Position` instance from given plain object (i.e. parsed JSON string).
  *
  * @param json Plain object to be converted to `Position`.
  * @param doc Document object that will be position owner.
  * @returns `Position` instance created using given plain object.
  */
  static fromJSON(e, i) {
    if (e.root === "$graveyard") {
      const s = new G(i.graveyard, e.path);
      return s.stickiness = e.stickiness, s;
    }
    if (!i.getRoot(e.root))
      throw new S("model-position-fromjson-no-root", i, {
        rootName: e.root
      });
    return new G(i.getRoot(e.root), e.path, e.stickiness);
  }
}
G.prototype.is = function(n) {
  return n === "position" || n === "model:position";
};
function Ho(n, t) {
  const e = t.getChildAtOffset(n.offset);
  return e && e.is("$text") && e.startOffset < n.offset ? e : null;
}
function R_(n, t, e) {
  return e !== null ? null : t.getChildAtOffset(n.offset);
}
function O_(n, t, e) {
  return e !== null ? null : t.getChild(t.offsetToIndex(n.offset) - 1);
}
function Ip(n, t, e) {
  return !(e + 1 === n.path.length || !uh(t.path, e + 1) || !g7(n, e + 1));
}
function uh(n, t) {
  for (; t < n.length; ) {
    if (n[t] !== 0)
      return !1;
    t++;
  }
  return !0;
}
function g7(n, t) {
  let e = n.parent, i = n.path.length - 1, s = 0;
  for (; i >= t; ) {
    if (n.path[i] + s !== e.maxOffset)
      return !1;
    s = 1, i--, e = e.parent;
  }
  return !0;
}
class F extends bs {
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * @param start The start position.
  * @param end The end position. If not set, the range will be collapsed at the `start` position.
  */
  constructor(e, i) {
    super();
    /**
    * Start position.
    */
    f(this, "start");
    /**
    * End position.
    */
    f(this, "end");
    this.start = G._createAt(e), this.end = i ? G._createAt(i) : G._createAt(e), this.start.stickiness = this.isCollapsed ? "toNone" : "toNext", this.end.stickiness = this.isCollapsed ? "toNone" : "toPrevious";
  }
  /**
  * Iterable interface.
  *
  * Iterates over all {@link module:engine/model/item~Item items} that are in this range and returns
  * them together with additional information like length or {@link module:engine/model/position~Position positions},
  * grouped as {@link module:engine/model/treewalker~TreeWalkerValue}.
  * It iterates over all {@link module:engine/model/textproxy~TextProxy text contents} that are inside the range
  * and all the {@link module:engine/model/element~Element}s that are entered into when iterating over this range.
  *
  * This iterator uses {@link module:engine/model/treewalker~TreeWalker} with `boundaries` set to this range
  * and `ignoreElementEnd` option set to `true`.
  */
  *[Symbol.iterator]() {
    yield* new Fn({
      boundaries: this,
      ignoreElementEnd: !0
    });
  }
  /**
  * Describes whether the range is collapsed, that is if {@link #start} and
  * {@link #end} positions are equal.
  */
  get isCollapsed() {
    return this.start.isEqual(this.end);
  }
  /**
  * Describes whether this range is flat, that is if {@link #start} position and
  * {@link #end} position are in the same {@link module:engine/model/position~Position#parent}.
  */
  get isFlat() {
    const e = this.start.getParentPath(), i = this.end.getParentPath();
    return Zt(e, i) == "same";
  }
  /**
  * Range root element.
  */
  get root() {
    return this.start.root;
  }
  /**
  * Checks whether this range contains given {@link module:engine/model/position~Position position}.
  *
  * @param position Position to check.
  * @returns `true` if given {@link module:engine/model/position~Position position} is contained
  * in this range,`false` otherwise.
  */
  containsPosition(e) {
    return e.isAfter(this.start) && e.isBefore(this.end);
  }
  /**
  * Checks whether this range contains given {@link ~Range range}.
  *
  * @param otherRange Range to check.
  * @param loose Whether the check is loose or strict. If the check is strict (`false`), compared range cannot
  * start or end at the same position as this range boundaries. If the check is loose (`true`), compared range can start, end or
  * even be equal to this range. Note that collapsed ranges are always compared in strict mode.
  * @returns {Boolean} `true` if given {@link ~Range range} boundaries are contained by this range, `false` otherwise.
  */
  containsRange(e, i = !1) {
    e.isCollapsed && (i = !1);
    const s = this.containsPosition(e.start) || i && this.start.isEqual(e.start), r = this.containsPosition(e.end) || i && this.end.isEqual(e.end);
    return s && r;
  }
  /**
  * Checks whether given {@link module:engine/model/item~Item} is inside this range.
  */
  containsItem(e) {
    const i = G._createBefore(e);
    return this.containsPosition(i) || this.start.isEqual(i);
  }
  /**
  * Two ranges are equal if their {@link #start} and {@link #end} positions are equal.
  *
  * @param otherRange Range to compare with.
  * @returns `true` if ranges are equal, `false` otherwise.
  */
  isEqual(e) {
    return this.start.isEqual(e.start) && this.end.isEqual(e.end);
  }
  /**
  * Checks and returns whether this range intersects with given range.
  *
  * @param otherRange Range to compare with.
  * @returns `true` if ranges intersect, `false` otherwise.
  */
  isIntersecting(e) {
    return this.start.isBefore(e.end) && this.end.isAfter(e.start);
  }
  /**
  * Computes which part(s) of this {@link ~Range range} is not a part of given {@link ~Range range}.
  * Returned array contains zero, one or two {@link ~Range ranges}.
  *
  * Examples:
  *
  * ```ts
  * let range = model.createRange(
  * 	model.createPositionFromPath( root, [ 2, 7 ] ),
  * 	model.createPositionFromPath( root, [ 4, 0, 1 ] )
  * );
  * let otherRange = model.createRange( model.createPositionFromPath( root, [ 1 ] ), model.createPositionFromPath( root, [ 5 ] ) );
  * let transformed = range.getDifference( otherRange );
  * // transformed array has no ranges because `otherRange` contains `range`
  *
  * otherRange = model.createRange( model.createPositionFromPath( root, [ 1 ] ), model.createPositionFromPath( root, [ 3 ] ) );
  * transformed = range.getDifference( otherRange );
  * // transformed array has one range: from [ 3 ] to [ 4, 0, 1 ]
  *
  * otherRange = model.createRange( model.createPositionFromPath( root, [ 3 ] ), model.createPositionFromPath( root, [ 4 ] ) );
  * transformed = range.getDifference( otherRange );
  * // transformed array has two ranges: from [ 2, 7 ] to [ 3 ] and from [ 4 ] to [ 4, 0, 1 ]
  * ```
  *
  * @param otherRange Range to differentiate against.
  * @returns The difference between ranges.
  */
  getDifference(e) {
    const i = [];
    return this.isIntersecting(e) ? (this.containsPosition(e.start) && i.push(new F(this.start, e.start)), this.containsPosition(e.end) && i.push(new F(e.end, this.end))) : i.push(new F(this.start, this.end)), i;
  }
  /**
  * Returns an intersection of this {@link ~Range range} and given {@link ~Range range}.
  * Intersection is a common part of both of those ranges. If ranges has no common part, returns `null`.
  *
  * Examples:
  *
  * ```ts
  * let range = model.createRange(
  * 	model.createPositionFromPath( root, [ 2, 7 ] ),
  * 	model.createPositionFromPath( root, [ 4, 0, 1 ] )
  * );
  * let otherRange = model.createRange( model.createPositionFromPath( root, [ 1 ] ), model.createPositionFromPath( root, [ 2 ] ) );
  * let transformed = range.getIntersection( otherRange ); // null - ranges have no common part
  *
  * otherRange = model.createRange( model.createPositionFromPath( root, [ 3 ] ), model.createPositionFromPath( root, [ 5 ] ) );
  * transformed = range.getIntersection( otherRange ); // range from [ 3 ] to [ 4, 0, 1 ]
  * ```
  *
  * @param otherRange Range to check for intersection.
  * @returns A common part of given ranges or `null` if ranges have no common part.
  */
  getIntersection(e) {
    if (this.isIntersecting(e)) {
      let i = this.start, s = this.end;
      return this.containsPosition(e.start) && (i = e.start), this.containsPosition(e.end) && (s = e.end), new F(i, s);
    }
    return null;
  }
  /**
  * Returns a range created by joining this {@link ~Range range} with the given {@link ~Range range}.
  * If ranges have no common part, returns `null`.
  *
  * Examples:
  *
  * ```ts
  * let range = model.createRange(
  * 	model.createPositionFromPath( root, [ 2, 7 ] ),
  * 	model.createPositionFromPath( root, [ 4, 0, 1 ] )
  * );
  * let otherRange = model.createRange(
  * 	model.createPositionFromPath( root, [ 1 ] ),
  * 	model.createPositionFromPath( root, [ 2 ] )
   * );
  * let transformed = range.getJoined( otherRange ); // null - ranges have no common part
  *
  * otherRange = model.createRange(
  * 	model.createPositionFromPath( root, [ 3 ] ),
  * 	model.createPositionFromPath( root, [ 5 ] )
  * );
  * transformed = range.getJoined( otherRange ); // range from [ 2, 7 ] to [ 5 ]
  * ```
  *
  * @param otherRange Range to be joined.
  * @param loose Whether the intersection check is loose or strict. If the check is strict (`false`),
  * ranges are tested for intersection or whether start/end positions are equal. If the check is loose (`true`),
  * compared range is also checked if it's {@link module:engine/model/position~Position#isTouching touching} current range.
  * @returns A sum of given ranges or `null` if ranges have no common part.
  */
  getJoined(e, i = !1) {
    let s = this.isIntersecting(e);
    if (s || (this.start.isBefore(e.start) ? s = i ? this.end.isTouching(e.start) : this.end.isEqual(e.start) : s = i ? e.end.isTouching(this.start) : e.end.isEqual(this.start)), !s)
      return null;
    let r = this.start, o = this.end;
    return e.start.isBefore(r) && (r = e.start), e.end.isAfter(o) && (o = e.end), new F(r, o);
  }
  /**
  * Computes and returns the smallest set of {@link #isFlat flat} ranges, that covers this range in whole.
  *
  * See an example of a model structure (`[` and `]` are range boundaries):
  *
  * ```
  * root                                                            root
  *  |- element DIV                         DIV             P2              P3             DIV
  *  |   |- element H                   H        P1        f o o           b a r       H         P4
  *  |   |   |- "fir[st"             fir[st     lorem                               se]cond     ipsum
  *  |   |- element P1
  *  |   |   |- "lorem"                                              ||
  *  |- element P2                                                   ||
  *  |   |- "foo"                                                    VV
  *  |- element P3
  *  |   |- "bar"                                                   root
  *  |- element DIV                         DIV             [P2             P3]             DIV
  *  |   |- element H                   H       [P1]       f o o           b a r        H         P4
  *  |   |   |- "se]cond"            fir[st]    lorem                               [se]cond     ipsum
  *  |   |- element P4
  *  |   |   |- "ipsum"
  * ```
  *
  * As it can be seen, letters contained in the range are: `stloremfoobarse`, spread across different parents.
  * We are looking for minimal set of flat ranges that contains the same nodes.
  *
  * Minimal flat ranges for above range `( [ 0, 0, 3 ], [ 3, 0, 2 ] )` will be:
  *
  * ```
  * ( [ 0, 0, 3 ], [ 0, 0, 5 ] ) = "st"
  * ( [ 0, 1 ], [ 0, 2 ] ) = element P1 ("lorem")
  * ( [ 1 ], [ 3 ] ) = element P2, element P3 ("foobar")
  * ( [ 3, 0, 0 ], [ 3, 0, 2 ] ) = "se"
  * ```
  *
  * **Note:** if an {@link module:engine/model/element~Element element} is not wholly contained in this range, it won't be returned
  * in any of the returned flat ranges. See in the example how `H` elements at the beginning and at the end of the range
  * were omitted. Only their parts that were wholly in the range were returned.
  *
  * **Note:** this method is not returning flat ranges that contain no nodes.
  *
  * @returns Array of flat ranges covering this range.
  */
  getMinimalFlatRanges() {
    const e = [], i = this.start.getCommonPath(this.end).length, s = G._createAt(this.start);
    let r = s.parent;
    for (; s.path.length > i + 1; ) {
      const o = r.maxOffset - s.offset;
      o !== 0 && e.push(new F(s, s.getShiftedBy(o))), s.path = s.path.slice(0, -1), s.offset++, r = r.parent;
    }
    for (; s.path.length <= this.end.path.length; ) {
      const o = this.end.path[s.path.length - 1], a = o - s.offset;
      a !== 0 && e.push(new F(s, s.getShiftedBy(a))), s.offset = o, s.path.push(0);
    }
    return e;
  }
  /**
  * Creates a {@link module:engine/model/treewalker~TreeWalker TreeWalker} instance with this range as a boundary.
  *
  * For example, to iterate over all items in the entire document root:
  *
  * ```ts
  * // Create a range spanning over the entire root content:
  * const range = editor.model.createRangeIn( editor.model.document.getRoot() );
  *
  * // Iterate over all items in this range:
  * for ( const value of range.getWalker() ) {
  * 	console.log( value.item );
  * }
  * ```
  *
  * @param options Object with configuration options. See {@link module:engine/model/treewalker~TreeWalker}.
  */
  getWalker(e = {}) {
    return e.boundaries = this, new Fn(e);
  }
  /**
  * Returns an iterator that iterates over all {@link module:engine/model/item~Item items} that are in this range and returns
  * them.
  *
  * This method uses {@link module:engine/model/treewalker~TreeWalker} with `boundaries` set to this range and `ignoreElementEnd` option
  * set to `true`. However it returns only {@link module:engine/model/item~Item model items},
  * not {@link module:engine/model/treewalker~TreeWalkerValue}.
  *
  * You may specify additional options for the tree walker. See {@link module:engine/model/treewalker~TreeWalker} for
  * a full list of available options.
  *
  * @param options Object with configuration options. See {@link module:engine/model/treewalker~TreeWalker}.
  */
  *getItems(e = {}) {
    e.boundaries = this, e.ignoreElementEnd = !0;
    const i = new Fn(e);
    for (const s of i)
      yield s.item;
  }
  /**
  * Returns an iterator that iterates over all {@link module:engine/model/position~Position positions} that are boundaries or
  * contained in this range.
  *
  * This method uses {@link module:engine/model/treewalker~TreeWalker} with `boundaries` set to this range. However it returns only
  * {@link module:engine/model/position~Position positions}, not {@link module:engine/model/treewalker~TreeWalkerValue}.
  *
  * You may specify additional options for the tree walker. See {@link module:engine/model/treewalker~TreeWalker} for
  * a full list of available options.
  *
  * @param options Object with configuration options. See {@link module:engine/model/treewalker~TreeWalker}.
  */
  *getPositions(e = {}) {
    e.boundaries = this;
    const i = new Fn(e);
    yield i.position;
    for (const s of i)
      yield s.nextPosition;
  }
  /**
  * Returns a range that is a result of transforming this range by given `operation`.
  *
  * **Note:** transformation may break one range into multiple ranges (for example, when a part of the range is
  * moved to a different part of document tree). For this reason, an array is returned by this method and it
  * may contain one or more `Range` instances.
  *
  * @param operation Operation to transform range by.
  * @returns Range which is the result of transformation.
  */
  getTransformedByOperation(e) {
    switch (e.type) {
      case "insert":
        return this._getTransformedByInsertOperation(e);
      case "move":
      case "remove":
      case "reinsert":
        return this._getTransformedByMoveOperation(e);
      case "split":
        return [
          this._getTransformedBySplitOperation(e)
        ];
      case "merge":
        return [
          this._getTransformedByMergeOperation(e)
        ];
    }
    return [
      new F(this.start, this.end)
    ];
  }
  /**
  * Returns a range that is a result of transforming this range by multiple `operations`.
  *
  * @see ~Range#getTransformedByOperation
  * @param operations Operations to transform the range by.
  * @returns Range which is the result of transformation.
  */
  getTransformedByOperations(e) {
    const i = [
      new F(this.start, this.end)
    ];
    for (const s of e)
      for (let r = 0; r < i.length; r++) {
        const o = i[r].getTransformedByOperation(s);
        i.splice(r, 1, ...o), r += o.length - 1;
      }
    for (let s = 0; s < i.length; s++) {
      const r = i[s];
      for (let o = s + 1; o < i.length; o++) {
        const a = i[o];
        (r.containsRange(a) || a.containsRange(r) || r.isEqual(a)) && i.splice(o, 1);
      }
    }
    return i;
  }
  /**
  * Returns an {@link module:engine/model/element~Element} or {@link module:engine/model/documentfragment~DocumentFragment}
  * which is a common ancestor of the range's both ends (in which the entire range is contained).
  */
  getCommonAncestor() {
    return this.start.getCommonAncestor(this.end);
  }
  /**
  * Returns an {@link module:engine/model/element~Element Element} contained by the range.
  * The element will be returned when it is the **only** node within the range and **fully–contained**
  * at the same time.
  */
  getContainedElement() {
    if (this.isCollapsed)
      return null;
    const e = this.start.nodeAfter, i = this.end.nodeBefore;
    return e && e.is("element") && e === i ? e : null;
  }
  /**
  * Converts `Range` to plain object and returns it.
  *
  * @returns `Node` converted to plain object.
  */
  toJSON() {
    return {
      start: this.start.toJSON(),
      end: this.end.toJSON()
    };
  }
  /**
  * Returns a new range that is equal to current range.
  */
  clone() {
    return new this.constructor(this.start, this.end);
  }
  /**
  * Returns a result of transforming a copy of this range by insert operation.
  *
  * One or more ranges may be returned as a result of this transformation.
  *
  * @internal
  */
  _getTransformedByInsertOperation(e, i = !1) {
    return this._getTransformedByInsertion(e.position, e.howMany, i);
  }
  /**
  * Returns a result of transforming a copy of this range by move operation.
  *
  * One or more ranges may be returned as a result of this transformation.
  *
  * @internal
  */
  _getTransformedByMoveOperation(e, i = !1) {
    const s = e.sourcePosition, r = e.howMany, o = e.targetPosition;
    return this._getTransformedByMove(s, o, r, i);
  }
  /**
  * Returns a result of transforming a copy of this range by split operation.
  *
  * Always one range is returned. The transformation is done in a way to not break the range.
  *
  * @internal
  */
  _getTransformedBySplitOperation(e) {
    const i = this.start._getTransformedBySplitOperation(e);
    let s = this.end._getTransformedBySplitOperation(e);
    return this.end.isEqual(e.insertionPosition) && (s = this.end.getShiftedBy(1)), i.root != s.root && (s = this.end.getShiftedBy(-1)), new F(i, s);
  }
  /**
  * Returns a result of transforming a copy of this range by merge operation.
  *
  * Always one range is returned. The transformation is done in a way to not break the range.
  *
  * @internal
  */
  _getTransformedByMergeOperation(e) {
    if (this.start.isEqual(e.targetPosition) && this.end.isEqual(e.deletionPosition))
      return new F(this.start);
    let i = this.start._getTransformedByMergeOperation(e), s = this.end._getTransformedByMergeOperation(e);
    return i.root != s.root && (s = this.end.getShiftedBy(-1)), i.isAfter(s) ? (e.sourcePosition.isBefore(e.targetPosition) ? (i = G._createAt(s), i.offset = 0) : (e.deletionPosition.isEqual(i) || (s = e.deletionPosition), i = e.targetPosition), new F(i, s)) : new F(i, s);
  }
  /**
  * Returns an array containing one or two {@link ~Range ranges} that are a result of transforming this
  * {@link ~Range range} by inserting `howMany` nodes at `insertPosition`. Two {@link ~Range ranges} are
  * returned if the insertion was inside this {@link ~Range range} and `spread` is set to `true`.
  *
  * Examples:
  *
  * ```ts
  * let range = model.createRange(
  * 	model.createPositionFromPath( root, [ 2, 7 ] ),
  * 	model.createPositionFromPath( root, [ 4, 0, 1 ] )
  * );
  * let transformed = range._getTransformedByInsertion( model.createPositionFromPath( root, [ 1 ] ), 2 );
  * // transformed array has one range from [ 4, 7 ] to [ 6, 0, 1 ]
  *
  * transformed = range._getTransformedByInsertion( model.createPositionFromPath( root, [ 4, 0, 0 ] ), 4 );
  * // transformed array has one range from [ 2, 7 ] to [ 4, 0, 5 ]
  *
  * transformed = range._getTransformedByInsertion( model.createPositionFromPath( root, [ 3, 2 ] ), 4 );
  * // transformed array has one range, which is equal to original range
  *
  * transformed = range._getTransformedByInsertion( model.createPositionFromPath( root, [ 3, 2 ] ), 4, true );
  * // transformed array has two ranges: from [ 2, 7 ] to [ 3, 2 ] and from [ 3, 6 ] to [ 4, 0, 1 ]
  * ```
  *
  * @internal
  * @param insertPosition Position where nodes are inserted.
  * @param howMany How many nodes are inserted.
  * @param spread Flag indicating whether this range should be spread if insertion
  * was inside the range. Defaults to `false`.
  * @returns Result of the transformation.
  */
  _getTransformedByInsertion(e, i, s = !1) {
    if (s && this.containsPosition(e))
      return [
        new F(this.start, e),
        new F(e.getShiftedBy(i), this.end._getTransformedByInsertion(e, i))
      ];
    {
      const r = new F(this.start, this.end);
      return r.start = r.start._getTransformedByInsertion(e, i), r.end = r.end._getTransformedByInsertion(e, i), [
        r
      ];
    }
  }
  /**
  * Returns an array containing {@link ~Range ranges} that are a result of transforming this
  * {@link ~Range range} by moving `howMany` nodes from `sourcePosition` to `targetPosition`.
  *
  * @internal
  * @param sourcePosition Position from which nodes are moved.
  * @param targetPosition Position to where nodes are moved.
  * @param howMany How many nodes are moved.
  * @param spread Whether the range should be spread if the move points inside the range.
  * @returns  Result of the transformation.
  */
  _getTransformedByMove(e, i, s, r = !1) {
    if (this.isCollapsed) {
      const h = this.start._getTransformedByMove(e, i, s);
      return [
        new F(h)
      ];
    }
    const o = F._createFromPositionAndShift(e, s), a = i._getTransformedByDeletion(e, s);
    if (this.containsPosition(i) && !r && (o.containsPosition(this.start) || o.containsPosition(this.end))) {
      const h = this.start._getTransformedByMove(e, i, s), m = this.end._getTransformedByMove(e, i, s);
      return [
        new F(h, m)
      ];
    }
    let l;
    const c = this.getDifference(o);
    let u = null;
    const d = this.getIntersection(o);
    if (c.length == 1 ? u = new F(c[0].start._getTransformedByDeletion(e, s), c[0].end._getTransformedByDeletion(e, s)) : c.length == 2 && (u = new F(this.start, this.end._getTransformedByDeletion(e, s))), u ? l = u._getTransformedByInsertion(a, s, d !== null || r) : l = [], d) {
      const h = new F(d.start._getCombined(o.start, a), d.end._getCombined(o.start, a));
      l.length == 2 ? l.splice(1, 0, h) : l.push(h);
    }
    return l;
  }
  /**
  * Returns a copy of this range that is transformed by deletion of `howMany` nodes from `deletePosition`.
  *
  * If the deleted range is intersecting with the transformed range, the transformed range will be shrank.
  *
  * If the deleted range contains transformed range, `null` will be returned.
  *
  * @internal
  * @param deletionPosition Position from which nodes are removed.
  * @param howMany How many nodes are removed.
  * @returns Result of the transformation.
  */
  _getTransformedByDeletion(e, i) {
    let s = this.start._getTransformedByDeletion(e, i), r = this.end._getTransformedByDeletion(e, i);
    return s == null && r == null ? null : (s == null && (s = e), r == null && (r = e), new F(s, r));
  }
  /**
  * Creates a new range, spreading from specified {@link module:engine/model/position~Position position} to a position moved by
  * given `shift`. If `shift` is a negative value, shifted position is treated as the beginning of the range.
  *
  * @internal
  * @param position Beginning of the range.
  * @param shift How long the range should be.
  */
  static _createFromPositionAndShift(e, i) {
    const s = e, r = e.getShiftedBy(i);
    return i > 0 ? new this(s, r) : new this(r, s);
  }
  /**
  * Creates a range inside an {@link module:engine/model/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @internal
  * @param element Element which is a parent for the range.
  */
  static _createIn(e) {
    return new this(G._createAt(e, 0), G._createAt(e, e.maxOffset));
  }
  /**
  * Creates a range that starts before given {@link module:engine/model/item~Item model item} and ends after it.
  *
  * @internal
  */
  static _createOn(e) {
    return this._createFromPositionAndShift(G._createBefore(e), e.offsetSize);
  }
  /**
  * Combines all ranges from the passed array into a one range. At least one range has to be passed.
  * Passed ranges must not have common parts.
  *
  * The first range from the array is a reference range. If other ranges start or end on the exactly same position where
  * the reference range, they get combined into one range.
  *
  * ```
  * [  ][]  [    ][ ][             ][ ][]  [  ]  // Passed ranges, shown sorted
  * [    ]                                       // The result of the function if the first range was a reference range.
  *         [                           ]        // The result of the function if the third-to-seventh range was a reference range.
  *                                        [  ]  // The result of the function if the last range was a reference range.
  * ```
  *
  * @internal
  * @param ranges Ranges to combine.
  * @returns Combined range.
  */
  static _createFromRanges(e) {
    if (e.length === 0)
      throw new S("range-create-from-ranges-empty-array", null);
    if (e.length == 1)
      return e[0].clone();
    const i = e[0];
    e.sort((o, a) => o.start.isAfter(a.start) ? 1 : -1);
    const s = e.indexOf(i), r = new this(i.start, i.end);
    for (let o = s - 1; o >= 0 && e[o].end.isEqual(r.start); o--)
      r.start = G._createAt(e[o].start);
    for (let o = s + 1; o < e.length && e[o].start.isEqual(r.end); o++)
      r.end = G._createAt(e[o].end);
    return r;
  }
  /**
  * Creates a `Range` instance from given plain object (i.e. parsed JSON string).
  *
  * @param json Plain object to be converted to `Range`.
  * @param doc Document object that will be range owner.
  * @returns `Range` instance created using given plain object.
  */
  static fromJSON(e, i) {
    return new this(G.fromJSON(e.start, i), G.fromJSON(e.end, i));
  }
}
F.prototype.is = function(n) {
  return n === "range" || n === "model:range";
};
class V_ extends (/* @__PURE__ */ Je()) {
  /**
  * Creates an instance of the mapper.
  */
  constructor() {
    super();
    /**
    * Model element to view element mapping.
    */
    f(this, "_modelToViewMapping", /* @__PURE__ */ new WeakMap());
    /**
    * View element to model element mapping.
    */
    f(this, "_viewToModelMapping", /* @__PURE__ */ new WeakMap());
    /**
    * A map containing callbacks between view element names and functions evaluating length of view elements
    * in model.
    */
    f(this, "_viewToModelLengthCallbacks", /* @__PURE__ */ new Map());
    /**
    * Model marker name to view elements mapping.
    *
    * Keys are `String`s while values are `Set`s with {@link module:engine/view/element~Element view elements}.
    * One marker (name) can be mapped to multiple elements.
    */
    f(this, "_markerNameToElements", /* @__PURE__ */ new Map());
    /**
    * View element to model marker names mapping.
    *
    * This is reverse to {@link ~Mapper#_markerNameToElements} map.
    */
    f(this, "_elementToMarkerNames", /* @__PURE__ */ new Map());
    /**
    * The map of removed view elements with their current root (used for deferred unbinding).
    */
    f(this, "_deferredBindingRemovals", /* @__PURE__ */ new Map());
    /**
    * Stores marker names of markers which have changed due to unbinding a view element (so it is assumed that the view element
    * has been removed, moved or renamed).
    */
    f(this, "_unboundMarkerNames", /* @__PURE__ */ new Set());
    /**
    * Manages dynamic cache for the `Mapper` to improve the performance.
    */
    f(this, "_cache", new p7());
    this.on("modelToViewPosition", (e, i) => {
      if (i.viewPosition)
        return;
      const s = this._modelToViewMapping.get(i.modelPosition.parent);
      if (!s)
        throw new S("mapping-model-position-view-parent-not-found", this, {
          modelPosition: i.modelPosition
        });
      i.viewPosition = this.findPositionIn(s, i.modelPosition.offset);
    }, {
      priority: "low"
    }), this.on("viewToModelPosition", (e, i) => {
      if (i.modelPosition)
        return;
      const s = this.findMappedViewAncestor(i.viewPosition), r = this._viewToModelMapping.get(s), o = this._toModelOffset(i.viewPosition.parent, i.viewPosition.offset, s);
      i.modelPosition = G._createAt(r, o);
    }, {
      priority: "low"
    });
  }
  /**
  * Marks model and view elements as corresponding. Corresponding elements can be retrieved by using
  * the {@link module:engine/conversion/mapper~Mapper#toModelElement toModelElement} and
  * {@link module:engine/conversion/mapper~Mapper#toViewElement toViewElement} methods.
  * The information that elements are bound is also used to translate positions.
  *
  * @param modelElement Model element.
  * @param viewElement View element.
  */
  bindElements(e, i) {
    this._modelToViewMapping.set(e, i), this._viewToModelMapping.set(i, e);
  }
  /**
  * Unbinds the given {@link module:engine/view/element~Element view element} from the map.
  *
  * **Note:** view-to-model binding will be removed, if it existed. However, corresponding model-to-view binding
  * will be removed only if model element is still bound to the passed `viewElement`.
  *
  * This behavior allows for re-binding model element to another view element without fear of losing the new binding
  * when the previously bound view element is unbound.
  *
  * @param viewElement View element to unbind.
  * @param options The options object.
  * @param options.defer Controls whether the binding should be removed immediately or deferred until a
  * {@link #flushDeferredBindings `flushDeferredBindings()`} call.
  */
  unbindViewElement(e, i = {}) {
    const s = this.toModelElement(e);
    if (this._elementToMarkerNames.has(e))
      for (const r of this._elementToMarkerNames.get(e))
        this._unboundMarkerNames.add(r);
    i.defer ? this._deferredBindingRemovals.set(e, e.root) : (this._viewToModelMapping.delete(e) && this._cache.stopTracking(e), this._modelToViewMapping.get(s) == e && this._modelToViewMapping.delete(s));
  }
  /**
  * Unbinds the given {@link module:engine/model/element~Element model element} from the map.
  *
  * **Note:** the model-to-view binding will be removed, if it existed. However, the corresponding view-to-model binding
  * will be removed only if the view element is still bound to the passed `modelElement`.
  *
  * This behavior lets for re-binding view element to another model element without fear of losing the new binding
  * when the previously bound model element is unbound.
  *
  * @param modelElement Model element to unbind.
  */
  unbindModelElement(e) {
    const i = this.toViewElement(e);
    this._modelToViewMapping.delete(e), this._viewToModelMapping.get(i) == e && this._viewToModelMapping.delete(i) && this._cache.stopTracking(i);
  }
  /**
  * Binds the given marker name with the given {@link module:engine/view/element~Element view element}. The element
  * will be added to the current set of elements bound with the given marker name.
  *
  * @param element Element to bind.
  * @param name Marker name.
  */
  bindElementToMarker(e, i) {
    const s = this._markerNameToElements.get(i) || /* @__PURE__ */ new Set();
    s.add(e);
    const r = this._elementToMarkerNames.get(e) || /* @__PURE__ */ new Set();
    r.add(i), this._markerNameToElements.set(i, s), this._elementToMarkerNames.set(e, r);
  }
  /**
  * Unbinds an element from given marker name.
  *
  * @param element Element to unbind.
  * @param name Marker name.
  */
  unbindElementFromMarkerName(e, i) {
    const s = this._markerNameToElements.get(i);
    s && (s.delete(e), s.size == 0 && this._markerNameToElements.delete(i));
    const r = this._elementToMarkerNames.get(e);
    r && (r.delete(i), r.size == 0 && this._elementToMarkerNames.delete(e));
  }
  /**
  * Returns all marker names of markers which have changed due to unbinding a view element (so it is assumed that the view element
  * has been removed, moved or renamed) since the last flush. After returning, the marker names list is cleared.
  */
  flushUnboundMarkerNames() {
    const e = Array.from(this._unboundMarkerNames);
    return this._unboundMarkerNames.clear(), e;
  }
  /**
  * Unbinds all deferred binding removals of view elements that in the meantime were not re-attached to some root or document fragment.
  *
  * See: {@link #unbindViewElement `unbindViewElement()`}.
  */
  flushDeferredBindings() {
    for (const [e, i] of this._deferredBindingRemovals)
      e.root == i && this.unbindViewElement(e);
    this._deferredBindingRemovals = /* @__PURE__ */ new Map();
  }
  /**
  * Removes all model to view and view to model bindings.
  */
  clearBindings() {
    this._modelToViewMapping = /* @__PURE__ */ new WeakMap(), this._viewToModelMapping = /* @__PURE__ */ new WeakMap(), this._markerNameToElements = /* @__PURE__ */ new Map(), this._elementToMarkerNames = /* @__PURE__ */ new Map(), this._unboundMarkerNames = /* @__PURE__ */ new Set(), this._deferredBindingRemovals = /* @__PURE__ */ new Map();
  }
  toModelElement(e) {
    return this._viewToModelMapping.get(e);
  }
  toViewElement(e) {
    return this._modelToViewMapping.get(e);
  }
  /**
  * Gets the corresponding model range.
  *
  * @param viewRange View range.
  * @returns Corresponding model range.
  */
  toModelRange(e) {
    return new F(this.toModelPosition(e.start), this.toModelPosition(e.end));
  }
  /**
  * Gets the corresponding view range.
  *
  * @param modelRange Model range.
  * @returns Corresponding view range.
  */
  toViewRange(e) {
    return new Re(this.toViewPosition(e.start), this.toViewPosition(e.end));
  }
  /**
  * Gets the corresponding model position.
  *
  * @fires viewToModelPosition
  * @param viewPosition View position.
  * @returns Corresponding model position.
  */
  toModelPosition(e) {
    const i = {
      viewPosition: e,
      mapper: this
    };
    return this.fire("viewToModelPosition", i), i.modelPosition;
  }
  /**
  * Gets the corresponding view position.
  *
  * @fires modelToViewPosition
  * @param modelPosition Model position.
  * @param options Additional options for position mapping process.
  * @param options.isPhantom Should be set to `true` if the model position to map is pointing to a place
  * in model tree which no longer exists. For example, it could be an end of a removed model range.
  * @returns Corresponding view position.
  */
  toViewPosition(e, i = {}) {
    const s = {
      modelPosition: e,
      mapper: this,
      isPhantom: i.isPhantom
    };
    return this.fire("modelToViewPosition", s), s.viewPosition;
  }
  /**
  * Gets all view elements bound to the given marker name.
  *
  * @param name Marker name.
  * @returns View elements bound with the given marker name or `null`
  * if no elements are bound to the given marker name.
  */
  markerNameToElements(e) {
    const i = this._markerNameToElements.get(e);
    if (!i)
      return null;
    const s = /* @__PURE__ */ new Set();
    for (const r of i)
      if (r.is("attributeElement"))
        for (const o of r.getElementsWithSameId())
          s.add(o);
      else
        s.add(r);
    return s;
  }
  /**
  * **This method is deprecated and will be removed in one of the future CKEditor 5 releases.**
  *
  * **Using this method will turn off `Mapper` caching system and may degrade performance when operating on bigger documents.**
  *
  * Registers a callback that evaluates the length in the model of a view element with the given name.
  *
  * The callback is fired with one argument, which is a view element instance. The callback is expected to return
  * a number representing the length of the view element in the model.
  *
  * ```ts
  * // List item in view may contain nested list, which have other list items. In model though,
  * // the lists are represented by flat structure. Because of those differences, length of list view element
  * // may be greater than one. In the callback it's checked how many nested list items are in evaluated list item.
  *
  * function getViewListItemLength( element ) {
  * 	let length = 1;
  *
  * 	for ( let child of element.getChildren() ) {
  * 		if ( child.name == 'ul' || child.name == 'ol' ) {
  * 			for ( let item of child.getChildren() ) {
  * 				length += getViewListItemLength( item );
  * 			}
  * 		}
  * 	}
  *
  * 	return length;
  * }
  *
  * mapper.registerViewToModelLength( 'li', getViewListItemLength );
  * ```
  *
  * @param viewElementName Name of view element for which callback is registered.
  * @param lengthCallback Function return a length of view element instance in model.
  * @deprecated
  */
  registerViewToModelLength(e, i) {
    this._viewToModelLengthCallbacks.set(e, i);
  }
  /**
  * For the given `viewPosition`, finds and returns the closest ancestor of this position that has a mapping to
  * the model.
  *
  * @param viewPosition Position for which a mapped ancestor should be found.
  */
  findMappedViewAncestor(e) {
    let i = e.parent;
    for (; !this._viewToModelMapping.has(i); )
      i = i.parent;
    return i;
  }
  /**
  * Calculates model offset based on the view position and the block element.
  *
  * Example:
  *
  * ```html
  * <p>foo<b>ba|r</b></p> // _toModelOffset( b, 2, p ) -> 5
  * ```
  *
  * Is a sum of:
  *
  * ```html
  * <p>foo|<b>bar</b></p> // _toModelOffset( p, 3, p ) -> 3
  * <p>foo<b>ba|r</b></p> // _toModelOffset( b, 2, b ) -> 2
  * ```
  *
  * @param viewParent Position parent.
  * @param viewOffset Position offset.
  * @param viewBlock Block used as a base to calculate offset.
  * @returns Offset in the model.
  */
  _toModelOffset(e, i, s) {
    if (s != e) {
      const o = this._toModelOffset(e.parent, e.index, s), a = this._toModelOffset(e, i, e);
      return o + a;
    }
    if (e.is("$text"))
      return i;
    let r = 0;
    for (let o = 0; o < i; o++)
      r += this.getModelLength(e.getChild(o));
    return r;
  }
  /**
  * Gets the length of the view element in the model.
  *
  * The length is calculated as follows:
  * * if a {@link #registerViewToModelLength length mapping callback} is provided for the given `viewNode`, it is used to
  * evaluate the model length (`viewNode` is used as first and only parameter passed to the callback),
  * * length of a {@link module:engine/view/text~Text text node} is equal to the length of its
  * {@link module:engine/view/text~Text#data data},
  * * length of a {@link module:engine/view/uielement~UIElement ui element} is equal to 0,
  * * length of a mapped {@link module:engine/view/element~Element element} is equal to 1,
  * * length of a non-mapped {@link module:engine/view/element~Element element} is equal to the length of its children.
  *
  * Examples:
  *
  * ```
  * foo                          -> 3 // Text length is equal to its data length.
  * <p>foo</p>                   -> 1 // Length of an element which is mapped is by default equal to 1.
  * <b>foo</b>                   -> 3 // Length of an element which is not mapped is a length of its children.
  * <div><p>x</p><p>y</p></div>  -> 2 // Assuming that <div> is not mapped and <p> are mapped.
  * ```
  *
  * @param viewNode View node.
  * @returns Length of the node in the tree model.
  */
  getModelLength(e) {
    const i = [
      e
    ];
    let s = 0;
    for (; i.length > 0; ) {
      const r = i.pop(), o = r.name && this._viewToModelLengthCallbacks.size > 0 && this._viewToModelLengthCallbacks.get(r.name);
      if (o)
        s += o(r);
      else if (this._viewToModelMapping.has(r))
        s += 1;
      else if (r.is("$text"))
        s += r.data.length;
      else {
        if (r.is("uiElement"))
          continue;
        for (const a of r.getChildren())
          i.push(a);
      }
    }
    return s;
  }
  /**
  * Finds the position in a view element or view document fragment node (or in its children) with the expected model offset.
  *
  * If the passed `viewContainer` is bound to model, `Mapper` will use caching mechanism to improve performance.
  *
  * @param viewContainer Tree view element in which we are looking for the position.
  * @param modelOffset Expected offset.
  * @returns Found position.
  */
  findPositionIn(e, i) {
    if (i === 0)
      return this._moveViewPositionToTextNode(new re(e, 0));
    if (this._viewToModelLengthCallbacks.size == 0 && this._viewToModelMapping.has(e)) {
      const r = this._cache.getClosest(e, i);
      return this._findPositionStartingFrom(r.viewPosition, r.modelOffset, i, e, !0);
    } else
      return this._findPositionStartingFrom(new re(e, 0), 0, i, e, !1);
  }
  /**
  * Performs most of the logic for `Mapper#findPositionIn()`.
  *
  * It allows to start looking for the requested model offset from a given starting position, to enable caching. Using the cache,
  * you can set the starting point and skip all the calculations that were already previously done.
  *
  * This method uses recursion to find positions inside deep structures. Example:
  *
  * ```
  * <p>fo<b>bar</b>bom</p>  -> target offset: 4
  * <p>|fo<b>bar</b>bom</p> -> target offset: 4, traversed offset: 0
  * <p>fo|<b>bar</b>bom</p> -> target offset: 4, traversed offset: 2
  * <p>fo<b>bar</b>|bom</p> -> target offset: 4, traversed offset: 5 -> we are too far, look recursively in <b>.
  *
  * <p>fo<b>|bar</b>bom</p> -> target offset: 4, traversed offset: 2
  * <p>fo<b>bar|</b>bom</p> -> target offset: 4, traversed offset: 5 -> we are too far, look inside "bar".
  *
  * <p>fo<b>ba|r</b>bom</p> -> target offset: 4, traversed offset: 2 -> position is inside text node at offset 4-2 = 2.
  * ```
  *
  * @param startViewPosition View position to start looking from.
  * @param startModelOffset Model offset related to `startViewPosition`.
  * @param targetModelOffset Target model offset to find.
  * @param viewContainer Mapped ancestor of `startViewPosition`. `startModelOffset` is the offset inside a model element or model
  * document fragment mapped to `viewContainer`.
  * @param useCache Whether `Mapper` should cache positions while traversing the view tree looking for `expectedModelOffset`.
  * @returns View position mapped to `targetModelOffset`.
  */
  _findPositionStartingFrom(e, i, s, r, o) {
    let a = e.parent, l = e.offset;
    if (a.is("$text"))
      return new re(a, s - i);
    let c, u = i, d = 0;
    for (; u < s; ) {
      if (c = a.getChild(l), !c) {
        if (a == r)
          throw new S("mapping-model-offset-not-found", this, {
            modelOffset: s,
            viewContainer: r
          });
        l = a.parent.getChildIndex(a) + 1, a = a.parent;
        continue;
      }
      d = this.getModelLength(c), u += d, l++, o && this._cache.save(a, l, r, u);
    }
    return u == s ? this._moveViewPositionToTextNode(new re(a, l)) : this._findPositionStartingFrom(new re(c, 0), u - d, s, r, o);
  }
  /**
  * Because we prefer positions in the text nodes over positions next to text nodes, if the view position was next to a text node,
  * it moves it into the text node instead.
  *
  * ```
  * <p>[]<b>foo</b></p> -> <p>[]<b>foo</b></p> // do not touch if position is not directly next to text
  * <p>foo[]<b>foo</b></p> -> <p>foo{}<b>foo</b></p> // move to text node
  * <p><b>[]foo</b></p> -> <p><b>{}foo</b></p> // move to text node
  * ```
  *
  * @param viewPosition Position potentially next to the text node.
  * @returns Position in the text node if possible.
  */
  _moveViewPositionToTextNode(e) {
    const i = e.nodeBefore, s = e.nodeAfter;
    return i && i.is("view:$text") ? new re(i, i.data.length) : s && s.is("view:$text") ? new re(s, 0) : e;
  }
}
class p7 extends (/* @__PURE__ */ Je()) {
  constructor() {
    super(...arguments);
    /**
    * For every view element or document fragment tracked by `MapperCache`, it holds currently cached data, or more precisely,
    * model offset to view position mappings. See also `MappingCache` and `CacheItem`.
    *
    * If an item is tracked by `MapperCache` it has an entry in this structure, so this structure can be used to check which items
    * are tracked by `MapperCache`. When an item is no longer tracked, it is removed from this structure.
    *
    * Although `MappingCache` and `CacheItem` structures allows for caching any model offsets and view positions, we only cache
    * values for model offsets that are after a view node. So, in essence, positions inside text nodes are not cached. However, it takes
    * from one to at most a few steps, to get from a cached position to a position that is inside a view text node.
    *
    * Additionally, only one item per `modelOffset` is cached. There can be several view nodes that "end" at the same `modelOffset`.
    * In this case, we favour positions that are closer to the mapped item. For example
    *
    * * for model: `<paragraph>Some <$text bold=true italic=true>formatted</$text> text.</paragraph>`,
    * * and view: `<p>Some <em><strong>formatted</strong></em> text.</p>`,
    *
    * for model offset `14` (after "d"), we store view position after `<em>` element (i.e. view position: at `<p>`, offset `2`).
    */
    f(this, "_cachedMapping", /* @__PURE__ */ new WeakMap());
    /**
    * When `MapperCache` {@link ~MapperCache#save saves} view position -> model offset mapping, a `CacheItem` is inserted into certain
    * `MappingCache#cacheList` at some index. Additionally, we store that index with the view node that is before the cached view position.
    *
    * This allows to quickly get a cache list item related to certain view node, and hence, for fast cache invalidation.
    *
    * For example, consider view: `<p>Some <strong>bold</strong> text.</p>`, where `<p>` is a view element tracked by `MapperCache`.
    * If all `<p>` children were visited by `MapperCache`, then `<p>` cache list would have four items, related to following model offsets:
    * `0`, `5`, `9`, `15`. Then, view node `"Some "` would have index `1`, `<strong>` index `2`, and `" text." index `3`.
    *
    * Note that the index related with a node is always greater than `0`. The first item in cache list is always for model offset `0`
    * (and view offset `0`), and it is not related to any node.
    */
    f(this, "_nodeToCacheListIndex", /* @__PURE__ */ new WeakMap());
    /**
    * Callback fired whenever there is a direct or indirect children change in tracked view element or tracked view document fragment.
    *
    * This is specified as a property to make it easier to set as an event callback and to later turn off that event.
    */
    f(this, "_invalidateOnChildrenChangeCallback", (e, i, s) => {
      this._clearCacheInsideParent(i, s.index);
    });
    /**
    * Callback fired whenever a view text node directly or indirectly inside a tracked view element or tracked view document fragment
    * changes its text data.
    *
    * This is specified as a property to make it easier to set as an event callback and to later turn off that event.
    */
    f(this, "_invalidateOnTextChangeCallback", (e, i) => {
      this._clearCacheStartingBefore(i);
    });
  }
  /**
  * Saves cache for given view position mapping <-> model offset mapping. The view position should be after a node (i.e. it cannot
  * be the first position inside its parent, or in other words, `viewOffset` must be greater than `0`).
  *
  * @param viewParent View position parent.
  * @param viewOffset View position offset. Must be greater than `0`.
  * @param viewContainer Tracked view position ascendant (it may be the direct parent of the view position).
  * @param modelOffset Model offset in the model element or document fragment which is mapped to `viewContainer`.
  */
  save(e, i, s, r) {
    const o = this._cachedMapping.get(s), a = o.cacheMap.get(r);
    if (a) {
      const d = e.getChild(i - 1), h = a.viewPosition.nodeBefore ? this._nodeToCacheListIndex.get(a.viewPosition.nodeBefore) : 0;
      this._nodeToCacheListIndex.set(d, h);
      return;
    }
    const c = {
      viewPosition: new re(e, i),
      modelOffset: r
    };
    o.maxModelOffset = r > o.maxModelOffset ? r : o.maxModelOffset, o.cacheMap.set(r, c);
    let u = o.cacheList.length - 1;
    for (; u >= 0 && o.cacheList[u].modelOffset > r; )
      u--;
    if (o.cacheList.splice(u + 1, 0, c), i > 0) {
      const d = e.getChild(i - 1);
      this._nodeToCacheListIndex.set(d, u + 1);
    }
  }
  /**
  * For given `modelOffset` inside a model element mapped to given `viewContainer`, it returns the closest saved cache item
  * (view position and related model offset) to the requested one.
  *
  * It can be exactly the requested mapping, or it can be mapping that is the closest starting point to look for the requested mapping.
  *
  * `viewContainer` must be a view element or document fragment that is mapped by the {@link ~Mapper Mapper}.
  *
  * If `viewContainer` is not yet tracked by the `MapperCache`, it will be automatically tracked after calling this method.
  *
  * Note: this method will automatically "hoist" cached positions, i.e. it will return a position that is closest to the tracked element.
  *
  * For example, if `<p>` is tracked element, and `^` is cached position:
  *
  * ```
  * <p>This is <strong>some <em>heavily <u>formatted</u>^</em></strong> text.</p>
  * ```
  *
  * If this position would be returned, instead, a position directly in `<p>` would be returned:
  *
  * ```
  * <p>This is <strong>some <em>heavily <u>formatted</u></em></strong>^ text.</p>
  * ```
  *
  * Note, that `modelOffset` for both positions is the same.
  *
  * @param viewContainer Tracked view element or document fragment, which cache will be used.
  * @param modelOffset Model offset in a model element or document fragment, which is mapped to `viewContainer`.
  */
  getClosest(e, i) {
    const s = this._cachedMapping.get(e);
    let r;
    if (s)
      if (i > s.maxModelOffset)
        r = s.cacheList[s.cacheList.length - 1];
      else {
        const a = s.cacheMap.get(i);
        a ? r = a : r = this._findInCacheList(s.cacheList, i);
      }
    else
      r = this.startTracking(e);
    const o = this._hoistViewPosition(r.viewPosition);
    return {
      modelOffset: r.modelOffset,
      viewPosition: o
    };
  }
  /**
  * Moves a view position to a preferred location.
  *
  * The view position is moved up from a non-tracked view element as long as it remains at the end of its current parent.
  *
  * See example below to understand when it is important:
  *
  * Starting state:
  *
  * ```
  * <p>This is <strong>some <em>heavily <u>formatted</u>^ piece of</em></strong> text.</p>
  * ```
  *
  * Then we remove " piece of " and invalidate some cache:
  *
  * ```
  * <p>This is <strong>some <em>heavily <u>formatted</u>^</em></strong> text.</p>
  * ```
  *
  * Now, if we ask for model offset after letter "d" in "formatted", we should get a position in " text", but we will get in `<em>`.
  * For this scenario, we need to hoist the position.
  *
  * ```
  * <p>This is <strong>some <em>heavily <u>formatted</u></em></strong>^ text.</p>
  * ```
  */
  _hoistViewPosition(e) {
    for (; e.parent.parent && !this._cachedMapping.has(e.parent) && e.isAtEnd; ) {
      const i = e.parent.parent, s = i.getChildIndex(e.parent) + 1;
      e = new re(i, s);
    }
    return e;
  }
  /**
  * Starts tracking given `viewContainer`, which must be mapped to a model element or model document fragment.
  *
  * Note, that this method is automatically called by
  * {@link module:engine/conversion/mapper~MapperCache#getClosest `MapperCache#getClosest()`} and there is no need to call it manually.
  *
  * This method initializes the cache for `viewContainer` and adds callbacks for
  * {@link module:engine/view/node~ViewNodeChangeEvent `change` event} fired by `viewContainer`. `MapperCache` listens to `change` event
  * on the tracked elements to invalidate the stored cache.
  */
  startTracking(e) {
    const s = {
      viewPosition: new re(e, 0),
      modelOffset: 0
    }, r = {
      maxModelOffset: 0,
      cacheList: [
        s
      ],
      cacheMap: /* @__PURE__ */ new Map([
        [
          0,
          s
        ]
      ])
    };
    return this._cachedMapping.set(e, r), e.on("change:children", this._invalidateOnChildrenChangeCallback), e.on("change:text", this._invalidateOnTextChangeCallback), s;
  }
  /**
  * Stops tracking given `viewContainer`.
  *
  * It removes the cached data and stops listening to {@link module:engine/view/node~ViewNodeChangeEvent `change` event} on the
  * `viewContainer`.
  */
  stopTracking(e) {
    e.off("change:children", this._invalidateOnChildrenChangeCallback), e.off("change:text", this._invalidateOnTextChangeCallback), this._cachedMapping.delete(e);
  }
  /**
  * Invalidates cache inside `viewParent`, starting from given `index` in that parent.
  */
  _clearCacheInsideParent(e, i) {
    if (i == 0)
      this._cachedMapping.has(e) ? this._clearCacheAll(e) : this._clearCacheStartingBefore(e);
    else {
      const s = e.getChild(i - 1);
      this._clearCacheStartingBefore(s);
    }
  }
  /**
  * Clears all the cache for given tracked `viewContainer`.
  */
  _clearCacheAll(e) {
    const i = this._cachedMapping.get(e);
    i.maxModelOffset > 0 && (i.maxModelOffset = 0, i.cacheList.length = 1, i.cacheMap.clear(), i.cacheMap.set(0, i.cacheList[0]));
  }
  /**
  * Clears all the stored cache starting before given `viewNode`. The `viewNode` can be any node that is inside a tracked view element
  * or view document fragment.
  */
  _clearCacheStartingBefore(e) {
    const i = this._nodeToCacheListIndex.get(e);
    if (i === void 0) {
      const r = e.parent;
      this._cachedMapping.has(r) || this._clearCacheStartingBefore(r);
      return;
    }
    let s = e.parent;
    for (; !this._cachedMapping.has(s); )
      s = s.parent;
    this._clearCacheFromIndex(s, i);
  }
  /**
  * Clears all the cache in the cache list related to given `viewContainer`, starting from `index` (inclusive).
  */
  _clearCacheFromIndex(e, i) {
    i === 0 && (i = 1);
    const s = this._cachedMapping.get(e), r = s.cacheList[i - 1];
    if (!r)
      return;
    s.maxModelOffset = r.modelOffset;
    const o = s.cacheList.splice(i);
    for (const a of o) {
      s.cacheMap.delete(a.modelOffset);
      const l = a.viewPosition.nodeBefore;
      this._nodeToCacheListIndex.delete(l);
    }
  }
  /**
  * Finds a cache item in the given cache list, which `modelOffset` is closest (but smaller or equal) to given `offset`.
  *
  * Since `cacheList` is a sorted array, this uses binary search to retrieve the item quickly.
  */
  _findInCacheList(e, i) {
    let s = 0, r = e.length - 1, o = r - s >> 1, a = e[o];
    for (; s < r; )
      a.modelOffset < i ? s = o + 1 : r = o - 1, o = s + (r - s >> 1), a = e[o];
    return a.modelOffset <= i ? a : e[o - 1];
  }
}
class b7 {
  constructor() {
    /**
    * Contains list of consumable values.
    */
    f(this, "_consumable", /* @__PURE__ */ new Map());
    /**
    * For each {@link module:engine/model/textproxy~TextProxy} added to `ModelConsumable`, this registry holds a parent
    * of that `TextProxy` and the start and end indices of that `TextProxy`. This allows identification of the `TextProxy`
    * instances that point to the same part of the model but are different instances. Each distinct `TextProxy`
    * is given a unique `Symbol` which is then registered as consumable. This process is transparent for the `ModelConsumable`
    * API user because whenever `TextProxy` is added, tested, consumed or reverted, the internal mechanisms of
    * `ModelConsumable` translate `TextProxy` to that unique `Symbol`.
    */
    f(this, "_textProxyRegistry", /* @__PURE__ */ new Map());
  }
  /**
  * Adds a consumable value to the consumables list and links it with a given model item.
  *
  * ```ts
  * modelConsumable.add( modelElement, 'insert' ); // Add `modelElement` insertion change to consumable values.
  * modelConsumable.add( modelElement, 'addAttribute:bold' ); // Add `bold` attribute insertion on `modelElement` change.
  * modelConsumable.add( modelElement, 'removeAttribute:bold' ); // Add `bold` attribute removal on `modelElement` change.
  * modelConsumable.add( modelSelection, 'selection' ); // Add `modelSelection` to consumable values.
  * modelConsumable.add( modelRange, 'range' ); // Add `modelRange` to consumable values.
  * ```
  *
  * @param item Model item, range or selection that has the consumable.
  * @param type Consumable type. Will be normalized to a proper form, that is either `<word>` or `<part>:<part>`.
  * Second colon and everything after will be cut. Passing event name is a safe and good practice.
  */
  add(t, e) {
    e = sl(e), t instanceof Ki && (t = this._getSymbolForTextProxy(t)), this._consumable.has(t) || this._consumable.set(t, /* @__PURE__ */ new Map()), this._consumable.get(t).set(e, !0);
  }
  /**
  * Removes a given consumable value from a given model item.
  *
  * ```ts
  * modelConsumable.consume( modelElement, 'insert' ); // Remove `modelElement` insertion change from consumable values.
  * modelConsumable.consume( modelElement, 'addAttribute:bold' ); // Remove `bold` attribute insertion on `modelElement` change.
  * modelConsumable.consume( modelElement, 'removeAttribute:bold' ); // Remove `bold` attribute removal on `modelElement` change.
  * modelConsumable.consume( modelSelection, 'selection' ); // Remove `modelSelection` from consumable values.
  * modelConsumable.consume( modelRange, 'range' ); // Remove 'modelRange' from consumable values.
  * ```
  *
  * @param item Model item, range or selection from which consumable will be consumed.
  * @param type Consumable type. Will be normalized to a proper form, that is either `<word>` or `<part>:<part>`.
  * Second colon and everything after will be cut. Passing event name is a safe and good practice.
  * @returns `true` if consumable value was available and was consumed, `false` otherwise.
  */
  consume(t, e) {
    return e = sl(e), t instanceof Ki && (t = this._getSymbolForTextProxy(t)), this.test(t, e) ? (this._consumable.get(t).set(e, !1), !0) : !1;
  }
  /**
  * Tests whether there is a consumable value of a given type connected with a given model item.
  *
  * ```ts
  * modelConsumable.test( modelElement, 'insert' ); // Check for `modelElement` insertion change.
  * modelConsumable.test( modelElement, 'addAttribute:bold' ); // Check for `bold` attribute insertion on `modelElement` change.
  * modelConsumable.test( modelElement, 'removeAttribute:bold' ); // Check for `bold` attribute removal on `modelElement` change.
  * modelConsumable.test( modelSelection, 'selection' ); // Check if `modelSelection` is consumable.
  * modelConsumable.test( modelRange, 'range' ); // Check if `modelRange` is consumable.
  * ```
  *
  * @param item Model item, range or selection to be tested.
  * @param type Consumable type. Will be normalized to a proper form, that is either `<word>` or `<part>:<part>`.
  * Second colon and everything after will be cut. Passing event name is a safe and good practice.
  * @returns `null` if such consumable was never added, `false` if the consumable values was
  * already consumed or `true` if it was added and not consumed yet.
  */
  test(t, e) {
    e = sl(e), t instanceof Ki && (t = this._getSymbolForTextProxy(t));
    const i = this._consumable.get(t);
    if (i === void 0)
      return null;
    const s = i.get(e);
    return s === void 0 ? null : s;
  }
  /**
  * Reverts consuming of a consumable value.
  *
  * ```ts
  * modelConsumable.revert( modelElement, 'insert' ); // Revert consuming `modelElement` insertion change.
  * modelConsumable.revert( modelElement, 'addAttribute:bold' ); // Revert consuming `bold` attribute insert from `modelElement`.
  * modelConsumable.revert( modelElement, 'removeAttribute:bold' ); // Revert consuming `bold` attribute remove from `modelElement`.
  * modelConsumable.revert( modelSelection, 'selection' ); // Revert consuming `modelSelection`.
  * modelConsumable.revert( modelRange, 'range' ); // Revert consuming `modelRange`.
  * ```
  *
  * @param item Model item, range or selection to be reverted.
  * @param type Consumable type.
  * @returns `true` if consumable has been reversed, `false` otherwise. `null` if the consumable has
  * never been added.
  */
  revert(t, e) {
    e = sl(e), t instanceof Ki && (t = this._getSymbolForTextProxy(t));
    const i = this.test(t, e);
    return i === !1 ? (this._consumable.get(t).set(e, !0), !0) : i === !0 ? !1 : null;
  }
  /**
  * Verifies if all events from the specified group were consumed.
  *
  * @param eventGroup The events group to verify.
  */
  verifyAllConsumed(t) {
    const e = [];
    for (const [i, s] of this._consumable)
      for (const [r, o] of s) {
        const a = r.split(":")[0];
        o && t == a && e.push({
          event: r,
          item: i.name || i.description
        });
      }
    if (e.length)
      throw new S("conversion-model-consumable-not-consumed", null, {
        items: e
      });
  }
  /**
  * Gets a unique symbol for the passed {@link module:engine/model/textproxy~TextProxy} instance. All `TextProxy` instances that
  * have same parent, same start index and same end index will get the same symbol.
  *
  * Used internally to correctly consume `TextProxy` instances.
  *
  * @internal
  * @param textProxy `TextProxy` instance to get a symbol for.
  * @returns Symbol representing all equal instances of `TextProxy`.
  */
  _getSymbolForTextProxy(t) {
    let e = null;
    const i = this._textProxyRegistry.get(t.startOffset);
    if (i) {
      const s = i.get(t.endOffset);
      s && (e = s.get(t.parent));
    }
    return e || (e = this._addSymbolForTextProxy(t)), e;
  }
  /**
  * Adds a symbol for the given {@link module:engine/model/textproxy~TextProxy} instance.
  *
  * Used internally to correctly consume `TextProxy` instances.
  *
  * @param textProxy Text proxy instance.
  * @returns Symbol generated for given `TextProxy`.
  */
  _addSymbolForTextProxy(t) {
    const e = t.startOffset, i = t.endOffset, s = t.parent, r = Symbol("$textProxy:" + t.data);
    let o, a;
    return o = this._textProxyRegistry.get(e), o || (o = /* @__PURE__ */ new Map(), this._textProxyRegistry.set(e, o)), a = o.get(i), a || (a = /* @__PURE__ */ new Map(), o.set(i, a)), a.set(s, r), r;
  }
}
function sl(n) {
  const t = n.split(":");
  return t[0] == "insert" ? t[0] : t[0] == "addMarker" || t[0] == "removeMarker" ? n : t.length > 1 ? t[0] + ":" + t[1] : t[0];
}
class L_ extends (/* @__PURE__ */ Je()) {
  /**
  * Creates a downcast dispatcher instance.
  *
  * @see module:engine/conversion/downcastdispatcher~DowncastConversionApi
  *
  * @param conversionApi Additional properties for an interface that will be passed to events fired
  * by the downcast dispatcher.
  */
  constructor(e) {
    super();
    /**
    * A template for an interface passed by the dispatcher to the event callbacks.
    *
    * @internal
    */
    f(this, "_conversionApi");
    /**
    * A map of already fired events for a given `ModelConsumable`.
    */
    f(this, "_firedEventsMap");
    this._conversionApi = {
      dispatcher: this,
      ...e
    }, this._firedEventsMap = /* @__PURE__ */ new WeakMap();
  }
  /**
  * Converts changes buffered in the given {@link module:engine/model/differ~Differ model differ}
  * and fires conversion events based on it.
  *
  * @fires insert
  * @fires remove
  * @fires attribute
  * @fires addMarker
  * @fires removeMarker
  * @fires reduceChanges
  * @param differ The differ object with buffered changes.
  * @param markers Markers related to the model fragment to convert.
  * @param writer The view writer that should be used to modify the view document.
  */
  convertChanges(e, i, s) {
    const r = this._createConversionApi(s, e.getRefreshedItems());
    for (const a of e.getMarkersToRemove())
      this._convertMarkerRemove(a.name, a.range, r);
    const o = this._reduceChanges(e.getChanges());
    for (const a of o)
      a.type === "insert" ? this._convertInsert(F._createFromPositionAndShift(a.position, a.length), r) : a.type === "reinsert" ? this._convertReinsert(F._createFromPositionAndShift(a.position, a.length), r) : a.type === "remove" ? this._convertRemove(a.position, a.length, a.name, r) : this._convertAttribute(a.range, a.attributeKey, a.attributeOldValue, a.attributeNewValue, r);
    r.mapper.flushDeferredBindings();
    for (const a of r.mapper.flushUnboundMarkerNames()) {
      const l = i.get(a).getRange();
      this._convertMarkerRemove(a, l, r), this._convertMarkerAdd(a, l, r);
    }
    for (const a of e.getMarkersToAdd())
      this._convertMarkerAdd(a.name, a.range, r);
    r.consumable.verifyAllConsumed("insert");
  }
  /**
  * Starts a conversion of a model range and the provided markers.
  *
  * @fires insert
  * @fires attribute
  * @fires addMarker
  * @param range The inserted range.
  * @param markers The map of markers that should be down-casted.
  * @param writer The view writer that should be used to modify the view document.
  * @param options Optional options object passed to `convertionApi.options`.
  */
  convert(e, i, s, r = {}) {
    const o = this._createConversionApi(s, void 0, r);
    this._convertInsert(e, o);
    for (const [a, l] of i)
      this._convertMarkerAdd(a, l, o);
    o.consumable.verifyAllConsumed("insert");
  }
  /**
  * Starts the model selection conversion.
  *
  * Fires events for a given {@link module:engine/model/selection~Selection selection} to start the selection conversion.
  *
  * @fires selection
  * @fires addMarker
  * @fires attribute
  * @param selection The selection to convert.
  * @param markers Markers connected with the converted model.
  * @param writer View writer that should be used to modify the view document.
  */
  convertSelection(e, i, s) {
    const r = this._createConversionApi(s);
    this.fire("cleanSelection", {
      selection: e
    }, r);
    const o = e.getFirstPosition().root;
    if (!r.mapper.toViewElement(o))
      return;
    const a = Array.from(i.getMarkersAtPosition(e.getFirstPosition()));
    if (this._addConsumablesForSelection(r.consumable, e, a), this.fire("selection", {
      selection: e
    }, r), !!e.isCollapsed) {
      for (const l of a)
        if (r.consumable.test(e, "addMarker:" + l.name)) {
          const c = l.getRange();
          if (!w7(e.getFirstPosition(), l, r.mapper))
            continue;
          const u = {
            item: e,
            markerName: l.name,
            markerRange: c
          };
          this.fire(`addMarker:${l.name}`, u, r);
        }
      for (const l of e.getAttributeKeys())
        if (r.consumable.test(e, "attribute:" + l)) {
          const c = {
            item: e,
            range: e.getFirstRange(),
            attributeKey: l,
            attributeOldValue: null,
            attributeNewValue: e.getAttribute(l)
          };
          this.fire(`attribute:${l}:$text`, c, r);
        }
    }
  }
  /**
  * Fires insertion conversion of a range of nodes.
  *
  * For each node in the range, {@link #event:insert `insert` event is fired}. For each attribute on each node,
  * {@link #event:attribute `attribute` event is fired}.
  *
  * @fires insert
  * @fires attribute
  * @param range The inserted range.
  * @param conversionApi The conversion API object.
  * @param options.doNotAddConsumables Whether the ModelConsumable should not get populated
  * for items in the provided range.
  */
  _convertInsert(e, i, s = {}) {
    s.doNotAddConsumables || this._addConsumablesForInsert(i.consumable, e);
    for (const r of e.getWalker({
      shallow: !0
    }))
      this._testAndFire("insert", Pp(r), i);
  }
  /**
  * Fires conversion of a single node removal. Fires {@link #event:remove remove event} with provided data.
  *
  * @param position Position from which node was removed.
  * @param length Offset size of removed node.
  * @param name Name of removed node.
  * @param conversionApi The conversion API object.
  */
  _convertRemove(e, i, s, r) {
    this.fire(`remove:${s}`, {
      position: e,
      length: i
    }, r);
  }
  /**
  * Starts a conversion of an attribute change on a given `range`.
  *
  * For each node in the given `range`, {@link #event:attribute attribute event} is fired with the passed data.
  *
  * @fires attribute
  * @param range Changed range.
  * @param key Key of the attribute that has changed.
  * @param oldValue Attribute value before the change or `null` if the attribute has not been set before.
  * @param newValue New attribute value or `null` if the attribute has been removed.
  * @param conversionApi The conversion API object.
  */
  _convertAttribute(e, i, s, r, o) {
    this._addConsumablesForRange(o.consumable, e, `attribute:${i}`);
    for (const a of e) {
      const l = {
        item: a.item,
        range: F._createFromPositionAndShift(a.previousPosition, a.length),
        attributeKey: i,
        attributeOldValue: s,
        attributeNewValue: r
      };
      this._testAndFire(`attribute:${i}`, l, o);
    }
  }
  /**
  * Fires re-insertion conversion (with a `reconversion` flag passed to `insert` events)
  * of a range of elements (only elements on the range depth, without children).
  *
  * For each node in the range on its depth (without children), {@link #event:insert `insert` event} is fired.
  * For each attribute on each node, {@link #event:attribute `attribute` event} is fired.
  *
  * @fires insert
  * @fires attribute
  * @param range The range to reinsert.
  * @param conversionApi The conversion API object.
  */
  _convertReinsert(e, i) {
    const s = Array.from(e.getWalker({
      shallow: !0
    }));
    this._addConsumablesForInsert(i.consumable, s);
    for (const r of s.map(Pp))
      this._testAndFire("insert", {
        ...r,
        reconversion: !0
      }, i);
  }
  /**
  * Converts the added marker. Fires the {@link #event:addMarker `addMarker`} event for each item
  * in the marker's range. If the range is collapsed, a single event is dispatched. See the event description for more details.
  *
  * @fires addMarker
  * @param markerName Marker name.
  * @param markerRange The marker range.
  * @param conversionApi The conversion API object.
  */
  _convertMarkerAdd(e, i, s) {
    if (i.root.rootName == "$graveyard")
      return;
    const r = `addMarker:${e}`;
    if (s.consumable.add(i, r), this.fire(r, {
      markerName: e,
      markerRange: i
    }, s), !!s.consumable.consume(i, r)) {
      this._addConsumablesForRange(s.consumable, i, r);
      for (const o of i.getItems()) {
        if (!s.consumable.test(o, r))
          continue;
        const a = {
          item: o,
          range: F._createOn(o),
          markerName: e,
          markerRange: i
        };
        this.fire(r, a, s);
      }
    }
  }
  /**
  * Fires the conversion of the marker removal. Fires the {@link #event:removeMarker `removeMarker`} event with the provided data.
  *
  * @fires removeMarker
  * @param markerName Marker name.
  * @param markerRange The marker range.
  * @param conversionApi The conversion API object.
  */
  _convertMarkerRemove(e, i, s) {
    i.root.rootName != "$graveyard" && this.fire(`removeMarker:${e}`, {
      markerName: e,
      markerRange: i
    }, s);
  }
  /**
  * Fires the reduction of changes buffered in the {@link module:engine/model/differ~Differ `Differ`}.
  *
  * Features can replace selected {@link module:engine/model/differ~DiffItem `DiffItem`}s with `reinsert` entries to trigger
  * reconversion. The {@link module:engine/conversion/downcasthelpers~DowncastHelpers#elementToStructure
  * `DowncastHelpers.elementToStructure()`} is using this event to trigger reconversion.
  *
  * @fires reduceChanges
  */
  _reduceChanges(e) {
    const i = {
      changes: e
    };
    return this.fire("reduceChanges", i), i.changes;
  }
  /**
  * Populates provided {@link module:engine/conversion/modelconsumable~ModelConsumable} with values to consume from a given range,
  * assuming that the range has just been inserted to the model.
  *
  * @param consumable The consumable.
  * @param walkerValues The walker values for the inserted range.
  * @returns The values to consume.
  */
  _addConsumablesForInsert(e, i) {
    for (const s of i) {
      const r = s.item;
      if (e.test(r, "insert") === null) {
        e.add(r, "insert");
        for (const o of r.getAttributeKeys())
          e.add(r, "attribute:" + o);
      }
    }
    return e;
  }
  /**
  * Populates provided {@link module:engine/conversion/modelconsumable~ModelConsumable} with values to consume for a given range.
  *
  * @param consumable The consumable.
  * @param range The affected range.
  * @param type Consumable type.
  * @returns The values to consume.
  */
  _addConsumablesForRange(e, i, s) {
    for (const r of i.getItems())
      e.add(r, s);
    return e;
  }
  /**
  * Populates provided {@link module:engine/conversion/modelconsumable~ModelConsumable} with selection consumable values.
  *
  * @param consumable The consumable.
  * @param selection The selection to create the consumable from.
  * @param markers Markers that contain the selection.
  * @returns The values to consume.
  */
  _addConsumablesForSelection(e, i, s) {
    e.add(i, "selection");
    for (const r of s)
      e.add(i, "addMarker:" + r.name);
    for (const r of i.getAttributeKeys())
      e.add(i, "attribute:" + r);
    return e;
  }
  /**
  * Tests whether given event wasn't already fired and if so, fires it.
  *
  * @fires insert
  * @fires attribute
  * @param type Event type.
  * @param data Event data.
  * @param conversionApi The conversion API object.
  */
  _testAndFire(e, i, s) {
    const r = v7(e, i), o = i.item.is("$textProxy") ? s.consumable._getSymbolForTextProxy(i.item) : i.item, a = this._firedEventsMap.get(s), l = a.get(o);
    if (!l)
      a.set(o, /* @__PURE__ */ new Set([
        r
      ]));
    else if (!l.has(r))
      l.add(r);
    else
      return;
    this.fire(r, i, s);
  }
  /**
  * Fires not already fired events for setting attributes on just inserted item.
  *
  * @param item The model item to convert attributes for.
  * @param conversionApi The conversion API object.
  */
  _testAndFireAddAttributes(e, i) {
    const s = {
      item: e,
      range: F._createOn(e)
    };
    for (const r of s.item.getAttributeKeys())
      s.attributeKey = r, s.attributeOldValue = null, s.attributeNewValue = s.item.getAttribute(r), this._testAndFire(`attribute:${r}`, s, i);
  }
  /**
  * Builds an instance of the {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi} from a template and a given
  * {@link module:engine/view/downcastwriter~DowncastWriter `DowncastWriter`} and options object.
  *
  * @param writer View writer that should be used to modify the view document.
  * @param refreshedItems A set of model elements that should not reuse their
  * previous view representations.
  * @param options Optional options passed to `convertionApi.options`.
  * @return The conversion API object.
  */
  _createConversionApi(e, i = /* @__PURE__ */ new Set(), s = {}) {
    const r = {
      ...this._conversionApi,
      consumable: new b7(),
      writer: e,
      options: s,
      convertItem: (o) => this._convertInsert(F._createOn(o), r),
      convertChildren: (o) => this._convertInsert(F._createIn(o), r, {
        doNotAddConsumables: !0
      }),
      convertAttributes: (o) => this._testAndFireAddAttributes(o, r),
      canReuseView: (o) => !i.has(r.mapper.toModelElement(o))
    };
    return this._firedEventsMap.set(r, /* @__PURE__ */ new Map()), r;
  }
}
function w7(n, t, e) {
  const i = t.getRange(), s = Array.from(n.getAncestors());
  return s.shift(), s.reverse(), !s.some((o) => {
    if (i.containsItem(o))
      return !!e.toViewElement(o).getCustomProperty("addHighlight");
  });
}
function v7(n, t) {
  const e = t.item.is("element") ? t.item.name : "$text";
  return `${n}:${e}`;
}
function Pp(n) {
  return {
    item: n.item,
    range: F._createFromPositionAndShift(n.previousPosition, n.length)
  };
}
let Zs = class extends bs {
  /**
  * Creates a model node.
  *
  * This is an abstract class, so this constructor should not be used directly.
  *
  * @param attrs Node's attributes. See {@link module:utils/tomap~toMap} for a list of accepted values.
  */
  constructor(e) {
    super();
    /**
    * Parent of this node. It could be {@link module:engine/model/element~Element}
    * or {@link module:engine/model/documentfragment~DocumentFragment}.
    * Equals to `null` if the node has no parent.
    */
    f(this, "parent", null);
    /**
    * Attributes set on this node.
    */
    f(this, "_attrs");
    /**
    * Index of this node in its parent or `null` if the node has no parent.
    *
    * @internal
    */
    f(this, "_index", null);
    /**
    * Offset at which this node starts in its parent or `null` if the node has no parent.
    *
    * @internal
    */
    f(this, "_startOffset", null);
    this._attrs = Ji(e);
  }
  /**
  * {@link module:engine/model/document~Document Document} that owns this root element.
  */
  get document() {
    return null;
  }
  /**
  * Index of this node in its parent or `null` if the node has no parent.
  */
  get index() {
    return this._index;
  }
  /**
  * Offset at which this node starts in its parent. It is equal to the sum of {@link #offsetSize offsetSize}
  * of all its previous siblings. Equals to `null` if node has no parent.
  */
  get startOffset() {
    return this._startOffset;
  }
  /**
  * Offset size of this node.
  *
  * Represents how much "offset space" is occupied by the node in its parent. It is important for
  * {@link module:engine/model/position~Position position}. When node has `offsetSize` greater than `1`, position can be placed between
  * that node start and end. `offsetSize` greater than `1` is for nodes that represents more than one entity, i.e.
  * a {@link module:engine/model/text~Text text node}.
  */
  get offsetSize() {
    return 1;
  }
  /**
  * Offset at which this node ends in its parent. It is equal to the sum of this node's
  * {@link module:engine/model/node~Node#startOffset start offset} and {@link #offsetSize offset size}.
  * Equals to `null` if the node has no parent.
  */
  get endOffset() {
    return this.startOffset === null ? null : this.startOffset + this.offsetSize;
  }
  /**
  * Node's next sibling or `null` if the node is a last child of it's parent or if the node has no parent.
  */
  get nextSibling() {
    const e = this.index;
    return e !== null && this.parent.getChild(e + 1) || null;
  }
  /**
  * Node's previous sibling or `null` if the node is a first child of it's parent or if the node has no parent.
  */
  get previousSibling() {
    const e = this.index;
    return e !== null && this.parent.getChild(e - 1) || null;
  }
  /**
  * The top-most ancestor of the node. If node has no parent it is the root itself. If the node is a part
  * of {@link module:engine/model/documentfragment~DocumentFragment}, it's `root` is equal to that `DocumentFragment`.
  */
  get root() {
    let e = this;
    for (; e.parent; )
      e = e.parent;
    return e;
  }
  /**
  * Returns `true` if the node is inside a document root that is attached to the document.
  */
  isAttached() {
    return this.parent === null ? !1 : this.root.isAttached();
  }
  /**
  * Gets path to the node. The path is an array containing starting offsets of consecutive ancestors of this node,
  * beginning from {@link module:engine/model/node~Node#root root}, down to this node's starting offset. The path can be used to
  * create {@link module:engine/model/position~Position Position} instance.
  *
  * ```ts
  * const abc = new Text( 'abc' );
  * const foo = new Text( 'foo' );
  * const h1 = new Element( 'h1', null, new Text( 'header' ) );
  * const p = new Element( 'p', null, [ abc, foo ] );
  * const div = new Element( 'div', null, [ h1, p ] );
  * foo.getPath(); // Returns [ 1, 3 ]. `foo` is in `p` which is in `div`. `p` starts at offset 1, while `foo` at 3.
  * h1.getPath(); // Returns [ 0 ].
  * div.getPath(); // Returns [].
  * ```
  */
  getPath() {
    const e = [];
    let i = this;
    for (; i.parent; )
      e.unshift(i.startOffset), i = i.parent;
    return e;
  }
  /**
  * Returns ancestors array of this node.
  *
  * @param options Options object.
  * @param options.includeSelf When set to `true` this node will be also included in parent's array.
  * @param options.parentFirst When set to `true`, array will be sorted from node's parent to root element,
  * otherwise root element will be the first item in the array.
  * @returns Array with ancestors.
  */
  getAncestors(e = {}) {
    const i = [];
    let s = e.includeSelf ? this : this.parent;
    for (; s; )
      i[e.parentFirst ? "push" : "unshift"](s), s = s.parent;
    return i;
  }
  /**
  * Returns a {@link module:engine/model/element~Element} or {@link module:engine/model/documentfragment~DocumentFragment}
  * which is a common ancestor of both nodes.
  *
  * @param node The second node.
  * @param options Options object.
  * @param options.includeSelf When set to `true` both nodes will be considered "ancestors" too.
  * Which means that if e.g. node A is inside B, then their common ancestor will be B.
  */
  getCommonAncestor(e, i = {}) {
    const s = this.getAncestors(i), r = e.getAncestors(i);
    let o = 0;
    for (; s[o] == r[o] && s[o]; )
      o++;
    return o === 0 ? null : s[o - 1];
  }
  /**
  * Returns whether this node is before given node. `false` is returned if nodes are in different trees (for example,
  * in different {@link module:engine/model/documentfragment~DocumentFragment}s).
  *
  * @param node Node to compare with.
  */
  isBefore(e) {
    if (this == e || this.root !== e.root)
      return !1;
    const i = this.getPath(), s = e.getPath(), r = Zt(i, s);
    switch (r) {
      case "prefix":
        return !0;
      case "extension":
        return !1;
      default:
        return i[r] < s[r];
    }
  }
  /**
  * Returns whether this node is after given node. `false` is returned if nodes are in different trees (for example,
  * in different {@link module:engine/model/documentfragment~DocumentFragment}s).
  *
  * @param node Node to compare with.
  */
  isAfter(e) {
    return this == e || this.root !== e.root ? !1 : !this.isBefore(e);
  }
  /**
  * Checks if the node has an attribute with given key.
  *
  * @param key Key of attribute to check.
  * @returns `true` if attribute with given key is set on node, `false` otherwise.
  */
  hasAttribute(e) {
    return this._attrs.has(e);
  }
  /**
  * Gets an attribute value for given key or `undefined` if that attribute is not set on node.
  *
  * @param key Key of attribute to look for.
  * @returns Attribute value or `undefined`.
  */
  getAttribute(e) {
    return this._attrs.get(e);
  }
  /**
  * Returns iterator that iterates over this node's attributes.
  *
  * Attributes are returned as arrays containing two items. First one is attribute key and second is attribute value.
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  getAttributes() {
    return this._attrs.entries();
  }
  /**
  * Returns iterator that iterates over this node's attribute keys.
  */
  getAttributeKeys() {
    return this._attrs.keys();
  }
  /**
  * Converts `Node` to plain object and returns it.
  *
  * @returns `Node` converted to plain object.
  */
  toJSON() {
    const e = {};
    return this._attrs.size && (e.attributes = Array.from(this._attrs).reduce((i, s) => (i[s[0]] = s[1], i), {})), e;
  }
  /**
  * Creates a copy of this node, that is a node with exactly same attributes, and returns it.
  *
  * @internal
  * @returns Node with same attributes as this node.
  */
  _clone(e) {
    return new this.constructor(this._attrs);
  }
  /**
  * Removes this node from its parent.
  *
  * @internal
  * @see module:engine/model/writer~Writer#remove
  */
  _remove() {
    this.parent._removeChildren(this.index);
  }
  /**
  * Sets attribute on the node. If attribute with the same key already is set, it's value is overwritten.
  *
  * @see module:engine/model/writer~Writer#setAttribute
  * @internal
  * @param key Key of attribute to set.
  * @param value Attribute value.
  */
  _setAttribute(e, i) {
    this._attrs.set(e, i);
  }
  /**
  * Removes all attributes from the node and sets given attributes.
  *
  * @see module:engine/model/writer~Writer#setAttributes
  * @internal
  * @param attrs Attributes to set. See {@link module:utils/tomap~toMap} for a list of accepted values.
  */
  _setAttributesTo(e) {
    this._attrs = Ji(e);
  }
  /**
  * Removes an attribute with given key from the node.
  *
  * @see module:engine/model/writer~Writer#removeAttribute
  * @internal
  * @param key Key of attribute to remove.
  * @returns `true` if the attribute was set on the element, `false` otherwise.
  */
  _removeAttribute(e) {
    return this._attrs.delete(e);
  }
  /**
  * Removes all attributes from the node.
  *
  * @see module:engine/model/writer~Writer#clearAttributes
  * @internal
  */
  _clearAttributes() {
    this._attrs.clear();
  }
};
Zs.prototype.is = function(n) {
  return n === "node" || n === "model:node";
};
class Ei extends (/* @__PURE__ */ Je(bs)) {
  /**
  * Creates a new selection instance based on the given {@link module:engine/model/selection~Selectable selectable}
  * or creates an empty selection if no arguments were passed.
  *
  * ```ts
  * // Creates empty selection without ranges.
  * const selection = writer.createSelection();
  *
  * // Creates selection at the given range.
  * const range = writer.createRange( start, end );
  * const selection = writer.createSelection( range );
  *
  * // Creates selection at the given ranges
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( star2, end2 ) ];
  * const selection = writer.createSelection( ranges );
  *
  * // Creates selection from the other selection.
  * // Note: It doesn't copy selection attributes.
  * const otherSelection = writer.createSelection();
  * const selection = writer.createSelection( otherSelection );
  *
  * // Creates selection from the given document selection.
  * // Note: It doesn't copy selection attributes.
  * const documentSelection = model.document.selection;
  * const selection = writer.createSelection( documentSelection );
  *
  * // Creates selection at the given position.
  * const position = writer.createPositionFromPath( root, path );
  * const selection = writer.createSelection( position );
  *
  * // Creates selection at the given offset in the given element.
  * const paragraph = writer.createElement( 'paragraph' );
  * const selection = writer.createSelection( paragraph, offset );
  *
  * // Creates a range inside an {@link module:engine/model/element~Element element} which starts before the
  * // first child of that element and ends after the last child of that element.
  * const selection = writer.createSelection( paragraph, 'in' );
  *
  * // Creates a range on an {@link module:engine/model/item~Item item} which starts before the item and ends
  * // just after the item.
  * const selection = writer.createSelection( paragraph, 'on' );
  * ```
  *
  * Selection's constructor allow passing additional options (`'backward'`) as the last argument.
  *
  * ```ts
  * // Creates backward selection.
  * const selection = writer.createSelection( range, { backward: true } );
  * ```
  *
  * @internal
  */
  constructor(...e) {
    super();
    /**
    * Specifies whether the last added range was added as a backward or forward range.
    */
    f(this, "_lastRangeBackward", !1);
    /**
    * List of attributes set on current selection.
    */
    f(this, "_attrs", /* @__PURE__ */ new Map());
    /** @internal */
    f(this, "_ranges", []);
    e.length && this.setTo(...e);
  }
  /**
  * Selection anchor. Anchor is the position from which the selection was started. If a user is making a selection
  * by dragging the mouse, the anchor is where the user pressed the mouse button (the beginning of the selection).
  *
  * Anchor and {@link #focus} define the direction of the selection, which is important
  * when expanding/shrinking selection. The focus moves, while the anchor should remain in the same place.
  *
  * Anchor is always set to the {@link module:engine/model/range~Range#start start} or
  * {@link module:engine/model/range~Range#end end} position of the last of selection's ranges. Whether it is
  * the `start` or `end` depends on the specified `options.backward`. See the {@link #setTo `setTo()`} method.
  *
  * May be set to `null` if there are no ranges in the selection.
  *
  * @see #focus
  */
  get anchor() {
    if (this._ranges.length > 0) {
      const e = this._ranges[this._ranges.length - 1];
      return this._lastRangeBackward ? e.end : e.start;
    }
    return null;
  }
  /**
  * Selection focus. Focus is the position where the selection ends. If a user is making a selection
  * by dragging the mouse, the focus is where the mouse cursor is.
  *
  * May be set to `null` if there are no ranges in the selection.
  *
  * @see #anchor
  */
  get focus() {
    if (this._ranges.length > 0) {
      const e = this._ranges[this._ranges.length - 1];
      return this._lastRangeBackward ? e.start : e.end;
    }
    return null;
  }
  /**
  * Whether the selection is collapsed. Selection is collapsed when there is exactly one range in it
  * and it is collapsed.
  */
  get isCollapsed() {
    return this._ranges.length === 1 ? this._ranges[0].isCollapsed : !1;
  }
  /**
  * Returns the number of ranges in the selection.
  */
  get rangeCount() {
    return this._ranges.length;
  }
  /**
  * Specifies whether the selection's {@link #focus} precedes the selection's {@link #anchor}.
  */
  get isBackward() {
    return !this.isCollapsed && this._lastRangeBackward;
  }
  /**
  * Checks whether this selection is equal to the given selection. Selections are equal if they have the same directions,
  * the same number of ranges and all ranges from one selection equal to ranges from the another selection.
  *
  * @param otherSelection Selection to compare with.
  * @returns `true` if selections are equal, `false` otherwise.
  */
  isEqual(e) {
    if (this.rangeCount != e.rangeCount)
      return !1;
    if (this.rangeCount === 0)
      return !0;
    if (!this.anchor.isEqual(e.anchor) || !this.focus.isEqual(e.focus))
      return !1;
    for (const i of this._ranges) {
      let s = !1;
      for (const r of e._ranges)
        if (i.isEqual(r)) {
          s = !0;
          break;
        }
      if (!s)
        return !1;
    }
    return !0;
  }
  /**
  * Returns an iterable object that iterates over copies of selection ranges.
  */
  *getRanges() {
    for (const e of this._ranges)
      yield new F(e.start, e.end);
  }
  /**
  * Returns a copy of the first range in the selection.
  * First range is the one which {@link module:engine/model/range~Range#start start} position
  * {@link module:engine/model/position~Position#isBefore is before} start position of all other ranges
  * (not to confuse with the first range added to the selection).
  *
  * Returns `null` if there are no ranges in selection.
  */
  getFirstRange() {
    let e = null;
    for (const i of this._ranges)
      (!e || i.start.isBefore(e.start)) && (e = i);
    return e ? new F(e.start, e.end) : null;
  }
  /**
  * Returns a copy of the last range in the selection.
  * Last range is the one which {@link module:engine/model/range~Range#end end} position
  * {@link module:engine/model/position~Position#isAfter is after} end position of all other ranges (not to confuse with the range most
  * recently added to the selection).
  *
  * Returns `null` if there are no ranges in selection.
  */
  getLastRange() {
    let e = null;
    for (const i of this._ranges)
      (!e || i.end.isAfter(e.end)) && (e = i);
    return e ? new F(e.start, e.end) : null;
  }
  /**
  * Returns the first position in the selection.
  * First position is the position that {@link module:engine/model/position~Position#isBefore is before}
  * any other position in the selection.
  *
  * Returns `null` if there are no ranges in selection.
  */
  getFirstPosition() {
    const e = this.getFirstRange();
    return e ? e.start.clone() : null;
  }
  /**
  * Returns the last position in the selection.
  * Last position is the position that {@link module:engine/model/position~Position#isAfter is after}
  * any other position in the selection.
  *
  * Returns `null` if there are no ranges in selection.
  */
  getLastPosition() {
    const e = this.getLastRange();
    return e ? e.end.clone() : null;
  }
  /**
  * Sets this selection's ranges and direction to the specified location based on the given
  * {@link module:engine/model/selection~Selectable selectable}.
  *
  * ```ts
  * // Removes all selection's ranges.
  * selection.setTo( null );
  *
  * // Sets selection to the given range.
  * const range = writer.createRange( start, end );
  * selection.setTo( range );
  *
  * // Sets selection to given ranges.
  * const ranges = [ writer.createRange( start1, end2 ), writer.createRange( star2, end2 ) ];
  * selection.setTo( ranges );
  *
  * // Sets selection to other selection.
  * // Note: It doesn't copy selection attributes.
  * const otherSelection = writer.createSelection();
  * selection.setTo( otherSelection );
  *
  * // Sets selection to the given document selection.
  * // Note: It doesn't copy selection attributes.
  * const documentSelection = new DocumentSelection( doc );
  * selection.setTo( documentSelection );
  *
  * // Sets collapsed selection at the given position.
  * const position = writer.createPositionFromPath( root, path );
  * selection.setTo( position );
  *
  * // Sets collapsed selection at the position of the given node and an offset.
  * selection.setTo( paragraph, offset );
  * ```
  *
  * Creates a range inside an {@link module:engine/model/element~Element element} which starts before the first child of
   * that element and ends after the last child of that element.
  *
  * ```ts
  * selection.setTo( paragraph, 'in' );
  * ```
  *
  * Creates a range on an {@link module:engine/model/item~Item item} which starts before the item and ends just after the item.
  *
  * ```ts
  * selection.setTo( paragraph, 'on' );
  * ```
  *
  * `Selection#setTo()`' method allow passing additional options (`backward`) as the last argument.
  *
  * ```ts
  * // Sets backward selection.
  * const selection = writer.createSelection( range, { backward: true } );
  * ```
  */
  setTo(...e) {
    let [i, s, r] = e;
    if (typeof s == "object" && (r = s, s = void 0), i === null)
      this._setRanges([]);
    else if (i instanceof Ei)
      this._setRanges(i.getRanges(), i.isBackward);
    else if (i && typeof i.getRanges == "function")
      this._setRanges(i.getRanges(), i.isBackward);
    else if (i instanceof F)
      this._setRanges([
        i
      ], !!r && !!r.backward);
    else if (i instanceof G)
      this._setRanges([
        new F(i)
      ]);
    else if (i instanceof Zs) {
      const o = !!r && !!r.backward;
      let a;
      if (s == "in")
        a = F._createIn(i);
      else if (s == "on")
        a = F._createOn(i);
      else if (s !== void 0)
        a = new F(G._createAt(i, s));
      else
        throw new S("model-selection-setto-required-second-parameter", [
          this,
          i
        ]);
      this._setRanges([
        a
      ], o);
    } else if (Ai(i))
      this._setRanges(i, r && !!r.backward);
    else
      throw new S("model-selection-setto-not-selectable", [
        this,
        i
      ]);
  }
  /**
  * Replaces all ranges that were added to the selection with given array of ranges. Last range of the array
  * is treated like the last added range and is used to set {@link module:engine/model/selection~Selection#anchor} and
  * {@link module:engine/model/selection~Selection#focus}. Accepts a flag describing in which direction the selection is made.
  *
  * @fires change:range
  * @param newRanges Ranges to set.
  * @param isLastBackward Flag describing if last added range was selected forward - from start to end (`false`)
  * or backward - from end to start (`true`).
  */
  _setRanges(e, i = !1) {
    const s = Array.from(e), r = s.some((o) => {
      if (!(o instanceof F))
        throw new S("model-selection-set-ranges-not-range", [
          this,
          e
        ]);
      return this._ranges.every((a) => !a.isEqual(o));
    });
    s.length === this._ranges.length && !r || (this._replaceAllRanges(s), this._lastRangeBackward = !!i, this.fire("change:range", {
      directChange: !0
    }));
  }
  /**
  * Moves {@link module:engine/model/selection~Selection#focus} to the specified location.
  *
  * The location can be specified in the same form as
  * {@link module:engine/model/writer~Writer#createPositionAt writer.createPositionAt()} parameters.
  *
  * @fires change:range
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/model/item~Item model item}.
  */
  setFocus(e, i) {
    if (this.anchor === null)
      throw new S("model-selection-setfocus-no-ranges", [
        this,
        e
      ]);
    const s = G._createAt(e, i);
    if (s.compareWith(this.focus) == "same")
      return;
    const r = this.anchor;
    this._ranges.length && this._popRange(), s.compareWith(r) == "before" ? (this._pushRange(new F(s, r)), this._lastRangeBackward = !0) : (this._pushRange(new F(r, s)), this._lastRangeBackward = !1), this.fire("change:range", {
      directChange: !0
    });
  }
  /**
  * Gets an attribute value for given key or `undefined` if that attribute is not set on the selection.
  *
  * @param key Key of attribute to look for.
  * @returns Attribute value or `undefined`.
  */
  getAttribute(e) {
    return this._attrs.get(e);
  }
  /**
  * Returns iterable that iterates over this selection's attributes.
  *
  * Attributes are returned as arrays containing two items. First one is attribute key and second is attribute value.
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  getAttributes() {
    return this._attrs.entries();
  }
  /**
  * Returns iterable that iterates over this selection's attribute keys.
  */
  getAttributeKeys() {
    return this._attrs.keys();
  }
  /**
  * Checks if the selection has an attribute for given key.
  *
  * @param key Key of attribute to check.
  * @returns `true` if attribute with given key is set on selection, `false` otherwise.
  */
  hasAttribute(e) {
    return this._attrs.has(e);
  }
  /**
  * Removes an attribute with given key from the selection.
  *
  * If given attribute was set on the selection, fires the {@link #event:change:range} event with
  * removed attribute key.
  *
  * @fires change:attribute
  * @param key Key of attribute to remove.
  */
  removeAttribute(e) {
    this.hasAttribute(e) && (this._attrs.delete(e), this.fire("change:attribute", {
      attributeKeys: [
        e
      ],
      directChange: !0
    }));
  }
  /**
  * Sets attribute on the selection. If attribute with the same key already is set, it's value is overwritten.
  *
  * If the attribute value has changed, fires the {@link #event:change:range} event with
  * the attribute key.
  *
  * @fires change:attribute
  * @param key Key of attribute to set.
  * @param value Attribute value.
  */
  setAttribute(e, i) {
    this.getAttribute(e) !== i && (this._attrs.set(e, i), this.fire("change:attribute", {
      attributeKeys: [
        e
      ],
      directChange: !0
    }));
  }
  /**
  * Returns the selected element. {@link module:engine/model/element~Element Element} is considered as selected if there is only
  * one range in the selection, and that range contains exactly one element.
  * Returns `null` if there is no selected element.
  */
  getSelectedElement() {
    return this.rangeCount !== 1 ? null : this.getFirstRange().getContainedElement();
  }
  /**
  * Gets elements of type {@link module:engine/model/schema~Schema#isBlock "block"} touched by the selection.
  *
  * This method's result can be used for example to apply block styling to all blocks covered by this selection.
  *
  * **Note:** `getSelectedBlocks()` returns blocks that are nested in other non-block elements
  * but will not return blocks nested in other blocks.
  *
  * In this case the function will return exactly all 3 paragraphs (note: `<blockQuote>` is not a block itself):
  *
  * ```xml
  * <paragraph>[a</paragraph>
  * <blockQuote>
  * 	<paragraph>b</paragraph>
  * </blockQuote>
  * <paragraph>c]d</paragraph>
  * ```
  *
  * In this case the paragraph will also be returned, despite the collapsed selection:
  *
  * ```xml
  * <paragraph>[]a</paragraph>
  * ```
  *
  * In such a scenario, however, only blocks A, B & E will be returned as blocks C & D are nested in block B:
  *
  * ```xml
  * [<blockA></blockA>
  * <blockB>
  * 	<blockC></blockC>
  * 	<blockD></blockD>
  * </blockB>
  * <blockE></blockE>]
  * ```
  *
  * If the selection is inside a block all the inner blocks (A & B) are returned:
  *
  * ```xml
  * <block>
  * 	<blockA>[a</blockA>
  * 	<blockB>b]</blockB>
  * </block>
  * ```
  *
  * **Special case**: Selection ignores first and/or last blocks if nothing (from user perspective) is selected in them.
  *
  * ```xml
  * // Selection ends and the beginning of the last block.
  * <paragraph>[a</paragraph>
  * <paragraph>b</paragraph>
  * <paragraph>]c</paragraph> // This block will not be returned
  *
  * // Selection begins at the end of the first block.
  * <paragraph>a[</paragraph> // This block will not be returned
  * <paragraph>b</paragraph>
  * <paragraph>c]</paragraph>
  *
  * // Selection begings at the end of the first block and ends at the beginning of the last block.
  * <paragraph>a[</paragraph> // This block will not be returned
  * <paragraph>b</paragraph>
  * <paragraph>]c</paragraph> // This block will not be returned
  * ```
  */
  *getSelectedBlocks() {
    const e = /* @__PURE__ */ new WeakSet();
    for (const i of this.getRanges()) {
      const s = Rp(i.start, e);
      y7(s, i) && (yield s);
      const r = i.getWalker();
      for (const a of r) {
        const l = a.item;
        a.type == "elementEnd" && _7(l, e, i) ? yield l : a.type == "elementStart" && l.is("model:element") && l.root.document.model.schema.isBlock(l) && r.jumpTo(G._createAt(l, "end"));
      }
      const o = Rp(i.end, e);
      k7(o, i) && (yield o);
    }
  }
  /**
  * Checks whether the selection contains the entire content of the given element. This means that selection must start
  * at a position {@link module:engine/model/position~Position#isTouching touching} the element's start and ends at position
  * touching the element's end.
  *
  * By default, this method will check whether the entire content of the selection's current root is selected.
  * Useful to check if e.g. the user has just pressed <kbd>Ctrl</kbd> + <kbd>A</kbd>.
  */
  containsEntireContent(e = this.anchor.root) {
    const i = G._createAt(e, 0), s = G._createAt(e, "end");
    return i.isTouching(this.getFirstPosition()) && s.isTouching(this.getLastPosition());
  }
  /**
  * Adds given range to internal {@link #_ranges ranges array}. Throws an error
  * if given range is intersecting with any range that is already stored in this selection.
  */
  _pushRange(e) {
    this._checkRange(e), this._ranges.push(new F(e.start, e.end));
  }
  /**
  * Checks if given range intersects with ranges that are already in the selection. Throws an error if it does.
  */
  _checkRange(e) {
    for (let i = 0; i < this._ranges.length; i++)
      if (e.isIntersecting(this._ranges[i]))
        throw new S("model-selection-range-intersects", [
          this,
          e
        ], {
          addedRange: e,
          intersectingRange: this._ranges[i]
        });
  }
  /**
  * Replaces all the ranges by the given ones.
  * Uses {@link #_popRange _popRange} and {@link #_pushRange _pushRange} to ensure proper ranges removal and addition.
  */
  _replaceAllRanges(e) {
    this._removeAllRanges();
    for (const i of e)
      this._pushRange(i);
  }
  /**
  * Deletes ranges from internal range array. Uses {@link #_popRange _popRange} to
  * ensure proper ranges removal.
  */
  _removeAllRanges() {
    for (; this._ranges.length > 0; )
      this._popRange();
  }
  /**
  * Removes most recently added range from the selection.
  */
  _popRange() {
    this._ranges.pop();
  }
}
Ei.prototype.is = function(n) {
  return n === "selection" || n === "model:selection";
};
function B_(n, t) {
  return t.has(n) ? !1 : (t.add(n), n.root.document.model.schema.isBlock(n) && !!n.parent);
}
function _7(n, t, e) {
  return B_(n, t) && If(n, e);
}
function Rp(n, t) {
  const i = n.parent.root.document.model.schema, s = n.parent.getAncestors({
    parentFirst: !0,
    includeSelf: !0
  });
  let r = !1;
  const o = s.find((a) => r ? !1 : (r = i.isLimit(a), !r && B_(a, t)));
  return s.forEach((a) => t.add(a)), o;
}
function If(n, t) {
  const e = C7(n);
  return e ? !t.containsRange(F._createOn(e), !0) : !0;
}
function y7(n, t) {
  return n ? t.isCollapsed || n.isEmpty ? !0 : t.start.isTouching(G._createAt(n, n.maxOffset)) ? !1 : If(n, t) : !1;
}
function k7(n, t) {
  return n ? t.isCollapsed || n.isEmpty ? !0 : t.end.isTouching(G._createAt(n, 0)) ? !1 : If(n, t) : !1;
}
function C7(n) {
  const t = n.root.document.model.schema;
  let e = n.parent;
  for (; e; ) {
    if (t.isBlock(e))
      return e;
    e = e.parent;
  }
}
class ni extends (/* @__PURE__ */ Je(F)) {
  /**
  * Creates a live range.
  *
  * @see module:engine/model/range~Range
  */
  constructor(t, e) {
    super(t, e), x7.call(this);
  }
  /**
  * Unbinds all events previously bound by `LiveRange`. Use it whenever you don't need `LiveRange` instance
  * anymore (i.e. when leaving scope in which it was declared or before re-assigning variable that was
  * referring to it).
  */
  detach() {
    this.stopListening();
  }
  /**
  * Creates a {@link module:engine/model/range~Range range instance} that is equal to this live range.
  */
  toRange() {
    return new F(this.start, this.end);
  }
  /**
  * Creates a `LiveRange` instance that is equal to the given range.
  */
  static fromRange(t) {
    return new ni(t.start, t.end);
  }
}
ni.prototype.is = function(n) {
  return n === "liveRange" || n === "model:liveRange" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n == "range" || n === "model:range";
};
function x7() {
  this.listenTo(this.root.document.model, "applyOperation", (n, t) => {
    const e = t[0];
    e.isDocumentOperation && T7.call(this, e);
  }, {
    priority: "low"
  });
}
function T7(n) {
  const t = this.getTransformedByOperation(n), e = F._createFromRanges(t), i = !e.isEqual(this), s = E7(this, n);
  let r = null;
  if (i) {
    e.root.rootName == "$graveyard" && (n.type == "remove" ? r = n.sourcePosition : r = n.deletionPosition);
    const o = this.toRange();
    this.start = e.start, this.end = e.end, this.fire("change:range", o, {
      deletionPosition: r
    });
  } else s && this.fire("change:content", this.toRange(), {
    deletionPosition: r
  });
}
function E7(n, t) {
  switch (t.type) {
    case "insert":
      return n.containsPosition(t.position);
    case "move":
    case "remove":
    case "reinsert":
    case "merge":
      return n.containsPosition(t.sourcePosition) || n.start.isEqual(t.sourcePosition) || n.containsPosition(t.targetPosition);
    case "split":
      return n.containsPosition(t.splitPosition) || n.containsPosition(t.insertionPosition);
  }
  return !1;
}
class lt extends Zs {
  /**
  * Creates a text node.
  *
  * **Note:** Constructor of this class shouldn't be used directly in the code.
  * Use the {@link module:engine/model/writer~Writer#createText} method instead.
  *
  * @internal
  * @param data Node's text.
  * @param attrs Node's attributes. See {@link module:utils/tomap~toMap} for a list of accepted values.
  */
  constructor(e, i) {
    super(i);
    /**
    * Text data contained in this text node.
    *
    * @internal
    */
    f(this, "_data");
    this._data = e || "";
  }
  /**
  * @inheritDoc
  */
  get offsetSize() {
    return this.data.length;
  }
  /**
  * Returns a text data contained in the node.
  */
  get data() {
    return this._data;
  }
  /**
  * Converts `Text` instance to plain object and returns it.
  *
  * @returns`Text` instance converted to plain object.
  */
  toJSON() {
    const e = super.toJSON();
    return e.data = this.data, e;
  }
  /**
  * Creates a copy of this text node and returns it. Created text node has same text data and attributes as original text node.
  *
  * @internal
  * @returns `Text` instance created using given plain object.
  */
  _clone() {
    return new lt(this.data, this.getAttributes());
  }
  /**
  * Creates a `Text` instance from given plain object (i.e. parsed JSON string).
  *
  * @param json Plain object to be converted to `Text`.
  * @returns `Text` instance created using given plain object.
  */
  static fromJSON(e) {
    return new lt(e.data, e.attributes);
  }
}
lt.prototype.is = function(n) {
  return n === "$text" || n === "model:$text" || // This are legacy values kept for backward compatibility.
  n === "text" || n === "model:text" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "node" || n === "model:node";
};
const oa = "selection:";
class Xi extends (/* @__PURE__ */ Je(bs)) {
  /**
  * Creates an empty live selection for given {@link module:engine/model/document~Document}.
  *
  * @param doc Document which owns this selection.
  */
  constructor(e) {
    super();
    /**
    * Selection used internally by that class (`DocumentSelection` is a proxy to that selection).
    */
    f(this, "_selection");
    this._selection = new S7(e), this._selection.delegate("change:range").to(this), this._selection.delegate("change:attribute").to(this), this._selection.delegate("change:marker").to(this);
  }
  /**
  * Describes whether the selection is collapsed. Selection is collapsed when there is exactly one range which is
  * collapsed.
  */
  get isCollapsed() {
    return this._selection.isCollapsed;
  }
  /**
  * Selection anchor. Anchor may be described as a position where the most recent part of the selection starts.
  * Together with {@link #focus} they define the direction of selection, which is important
  * when expanding/shrinking selection. Anchor is always {@link module:engine/model/range~Range#start start} or
  * {@link module:engine/model/range~Range#end end} position of the most recently added range.
  *
  * Is set to `null` if there are no ranges in selection.
  *
  * @see #focus
  */
  get anchor() {
    return this._selection.anchor;
  }
  /**
  * Selection focus. Focus is a position where the selection ends.
  *
  * Is set to `null` if there are no ranges in selection.
  *
  * @see #anchor
  */
  get focus() {
    return this._selection.focus;
  }
  /**
  * Number of ranges in selection.
  */
  get rangeCount() {
    return this._selection.rangeCount;
  }
  /**
  * Describes whether `Documentselection` has own range(s) set, or if it is defaulted to
  * {@link module:engine/model/document~Document#_getDefaultRange document's default range}.
  */
  get hasOwnRange() {
    return this._selection.hasOwnRange;
  }
  /**
  * Specifies whether the {@link #focus}
  * precedes {@link #anchor}.
  *
  * @readonly
  * @type {Boolean}
  */
  get isBackward() {
    return this._selection.isBackward;
  }
  /**
  * Describes whether the gravity is overridden (using {@link module:engine/model/writer~Writer#overrideSelectionGravity}) or not.
  *
  * Note that the gravity remains overridden as long as will not be restored the same number of times as it was overridden.
  */
  get isGravityOverridden() {
    return this._selection.isGravityOverridden;
  }
  /**
  * A collection of selection {@link module:engine/model/markercollection~Marker markers}.
  * Marker is a selection marker when selection range is inside the marker range.
  *
  * **Note**: Only markers from {@link ~DocumentSelection#observeMarkers observed markers groups} are collected.
  */
  get markers() {
    return this._selection.markers;
  }
  /**
  * Used for the compatibility with the {@link module:engine/model/selection~Selection#isEqual} method.
  *
  * @internal
  */
  get _ranges() {
    return this._selection._ranges;
  }
  /**
  * Returns an iterable that iterates over copies of selection ranges.
  */
  getRanges() {
    return this._selection.getRanges();
  }
  /**
  * Returns the first position in the selection.
  * First position is the position that {@link module:engine/model/position~Position#isBefore is before}
  * any other position in the selection.
  *
  * Returns `null` if there are no ranges in selection.
  */
  getFirstPosition() {
    return this._selection.getFirstPosition();
  }
  /**
  * Returns the last position in the selection.
  * Last position is the position that {@link module:engine/model/position~Position#isAfter is after}
  * any other position in the selection.
  *
  * Returns `null` if there are no ranges in selection.
  */
  getLastPosition() {
    return this._selection.getLastPosition();
  }
  /**
  * Returns a copy of the first range in the selection.
  * First range is the one which {@link module:engine/model/range~Range#start start} position
  * {@link module:engine/model/position~Position#isBefore is before} start position of all other ranges
  * (not to confuse with the first range added to the selection).
  *
  * Returns `null` if there are no ranges in selection.
  */
  getFirstRange() {
    return this._selection.getFirstRange();
  }
  /**
  * Returns a copy of the last range in the selection.
  * Last range is the one which {@link module:engine/model/range~Range#end end} position
  * {@link module:engine/model/position~Position#isAfter is after} end position of all other ranges (not to confuse with the range most
  * recently added to the selection).
  *
  * Returns `null` if there are no ranges in selection.
  */
  getLastRange() {
    return this._selection.getLastRange();
  }
  /**
  * Gets elements of type {@link module:engine/model/schema~Schema#isBlock "block"} touched by the selection.
  *
  * This method's result can be used for example to apply block styling to all blocks covered by this selection.
  *
  * **Note:** `getSelectedBlocks()` returns blocks that are nested in other non-block elements
  * but will not return blocks nested in other blocks.
  *
  * In this case the function will return exactly all 3 paragraphs (note: `<blockQuote>` is not a block itself):
  *
  * ```
  * <paragraph>[a</paragraph>
  * <blockQuote>
  * 	<paragraph>b</paragraph>
  * </blockQuote>
  * <paragraph>c]d</paragraph>
  * ```
  *
  * In this case the paragraph will also be returned, despite the collapsed selection:
  *
  * ```
  * <paragraph>[]a</paragraph>
  * ```
  *
  * In such a scenario, however, only blocks A, B & E will be returned as blocks C & D are nested in block B:
  *
  * ```
  * [<blockA></blockA>
  * <blockB>
  * 	<blockC></blockC>
  * 	<blockD></blockD>
  * </blockB>
  * <blockE></blockE>]
  * ```
  *
  * If the selection is inside a block all the inner blocks (A & B) are returned:
  *
  * ```
  * <block>
  * 	<blockA>[a</blockA>
  * 	<blockB>b]</blockB>
  * </block>
  * ```
  *
  * **Special case**: If a selection ends at the beginning of a block, that block is not returned as from user perspective
  * this block wasn't selected. See [#984](https://github.com/ckeditor/ckeditor5-engine/issues/984) for more details.
  *
  * ```
  * <paragraph>[a</paragraph>
  * <paragraph>b</paragraph>
  * <paragraph>]c</paragraph> // this block will not be returned
  * ```
  */
  getSelectedBlocks() {
    return this._selection.getSelectedBlocks();
  }
  /**
  * Returns the selected element. {@link module:engine/model/element~Element Element} is considered as selected if there is only
  * one range in the selection, and that range contains exactly one element.
  * Returns `null` if there is no selected element.
  */
  getSelectedElement() {
    return this._selection.getSelectedElement();
  }
  /**
  * Checks whether the selection contains the entire content of the given element. This means that selection must start
  * at a position {@link module:engine/model/position~Position#isTouching touching} the element's start and ends at position
  * touching the element's end.
  *
  * By default, this method will check whether the entire content of the selection's current root is selected.
  * Useful to check if e.g. the user has just pressed <kbd>Ctrl</kbd> + <kbd>A</kbd>.
  */
  containsEntireContent(e) {
    return this._selection.containsEntireContent(e);
  }
  /**
  * Unbinds all events previously bound by document selection.
  */
  destroy() {
    this._selection.destroy();
  }
  /**
  * Returns iterable that iterates over this selection's attribute keys.
  */
  getAttributeKeys() {
    return this._selection.getAttributeKeys();
  }
  /**
  * Returns iterable that iterates over this selection's attributes.
  *
  * Attributes are returned as arrays containing two items. First one is attribute key and second is attribute value.
  * This format is accepted by native `Map` object and also can be passed in `Node` constructor.
  */
  getAttributes() {
    return this._selection.getAttributes();
  }
  /**
  * Gets an attribute value for given key or `undefined` if that attribute is not set on the selection.
  *
  * @param key Key of attribute to look for.
  * @returns Attribute value or `undefined`.
  */
  getAttribute(e) {
    return this._selection.getAttribute(e);
  }
  /**
  * Checks if the selection has an attribute for given key.
  *
  * @param key Key of attribute to check.
  * @returns `true` if attribute with given key is set on selection, `false` otherwise.
  */
  hasAttribute(e) {
    return this._selection.hasAttribute(e);
  }
  /**
  * Refreshes selection attributes and markers according to the current position in the model.
  */
  refresh() {
    this._selection.updateMarkers(), this._selection._updateAttributes(!1);
  }
  /**
  * Registers a marker group prefix or a marker name to be collected in the
  * {@link ~DocumentSelection#markers selection markers collection}.
  *
  * See also {@link module:engine/model/markercollection~MarkerCollection#getMarkersGroup `MarkerCollection#getMarkersGroup()`}.
  *
  * @param prefixOrName The marker group prefix or marker name.
  */
  observeMarkers(e) {
    this._selection.observeMarkers(e);
  }
  /**
  * Moves {@link module:engine/model/documentselection~DocumentSelection#focus} to the specified location.
  * Should be used only within the {@link module:engine/model/writer~Writer#setSelectionFocus} method.
  *
  * The location can be specified in the same form as
  * {@link module:engine/model/writer~Writer#createPositionAt writer.createPositionAt()} parameters.
  *
  * @see module:engine/model/writer~Writer#setSelectionFocus
  * @internal
  * @param offset Offset or one of the flags. Used only when
  * first parameter is a {@link module:engine/model/item~Item model item}.
  */
  _setFocus(e, i) {
    this._selection.setFocus(e, i);
  }
  /**
  * Sets this selection's ranges and direction to the specified location based on the given
  * {@link module:engine/model/selection~Selectable selectable}.
  * Should be used only within the {@link module:engine/model/writer~Writer#setSelection} method.
  *
  * @see module:engine/model/writer~Writer#setSelection
  * @internal
  */
  _setTo(...e) {
    this._selection.setTo(...e);
  }
  /**
  * Sets attribute on the selection. If attribute with the same key already is set, it's value is overwritten.
  * Should be used only within the {@link module:engine/model/writer~Writer#setSelectionAttribute} method.
  *
  * @see module:engine/model/writer~Writer#setSelectionAttribute
  * @internal
  * @param key Key of the attribute to set.
  * @param value Attribute value.
  */
  _setAttribute(e, i) {
    this._selection.setAttribute(e, i);
  }
  /**
  * Removes an attribute with given key from the selection.
  * If the given attribute was set on the selection, fires the {@link module:engine/model/selection~Selection#event:change:range}
  * event with removed attribute key.
  * Should be used only within the {@link module:engine/model/writer~Writer#removeSelectionAttribute} method.
  *
  * @see module:engine/model/writer~Writer#removeSelectionAttribute
  * @internal
  * @param key Key of the attribute to remove.
  */
  _removeAttribute(e) {
    this._selection.removeAttribute(e);
  }
  /**
  * Returns an iterable that iterates through all selection attributes stored in current selection's parent.
  *
  * @internal
  */
  _getStoredAttributes() {
    return this._selection.getStoredAttributes();
  }
  /**
  * Temporarily changes the gravity of the selection from the left to the right.
  *
  * The gravity defines from which direction the selection inherits its attributes. If it's the default left
  * gravity, the selection (after being moved by the the user) inherits attributes from its left hand side.
  * This method allows to temporarily override this behavior by forcing the gravity to the right.
  *
  * It returns an unique identifier which is required to restore the gravity. It guarantees the symmetry
  * of the process.
  *
  * @see module:engine/model/writer~Writer#overrideSelectionGravity
  * @internal
  * @returns The unique id which allows restoring the gravity.
  */
  _overrideGravity() {
    return this._selection.overrideGravity();
  }
  /**
  * Restores the {@link ~DocumentSelection#_overrideGravity overridden gravity}.
  *
  * Restoring the gravity is only possible using the unique identifier returned by
  * {@link ~DocumentSelection#_overrideGravity}. Note that the gravity remains overridden as long as won't be restored
  * the same number of times it was overridden.
  *
  * @see module:engine/model/writer~Writer#restoreSelectionGravity
  * @internal
  * @param uid The unique id returned by {@link #_overrideGravity}.
  */
  _restoreGravity(e) {
    this._selection.restoreGravity(e);
  }
  /**
  * Generates and returns an attribute key for selection attributes store, basing on original attribute key.
  *
  * @internal
  * @param key Attribute key to convert.
  * @returns Converted attribute key, applicable for selection store.
  */
  static _getStoreAttributeKey(e) {
    return oa + e;
  }
  /**
  * Checks whether the given attribute key is an attribute stored on an element.
  *
  * @internal
  */
  static _isStoreAttributeKey(e) {
    return e.startsWith(oa);
  }
}
Xi.prototype.is = function(n) {
  return n === "selection" || n == "model:selection" || n == "documentSelection" || n == "model:documentSelection";
};
class S7 extends Ei {
  /**
  * Creates an empty live selection for given {@link module:engine/model/document~Document}.
  *
  * @param doc Document which owns this selection.
  */
  constructor(e) {
    super();
    /**
    * List of selection markers.
    * Marker is a selection marker when selection range is inside the marker range.
    */
    f(this, "markers", new bt({
      idProperty: "name"
    }));
    /**
    * Document which owns this selection.
    */
    f(this, "_model");
    /**
    * Document which owns this selection.
    */
    f(this, "_document");
    /**
    * Keeps mapping of attribute name to priority with which the attribute got modified (added/changed/removed)
    * last time. Possible values of priority are: `'low'` and `'normal'`.
    *
    * Priorities are used by internal `LiveSelection` mechanisms. All attributes set using `LiveSelection`
    * attributes API are set with `'normal'` priority.
    */
    f(this, "_attributePriority", /* @__PURE__ */ new Map());
    /**
    * Position to which the selection should be set if the last selection range was moved to the graveyard.
    */
    f(this, "_selectionRestorePosition", null);
    /**
    * Flag that informs whether the selection ranges have changed. It is changed on true when `LiveRange#change:range` event is fired.
    */
    f(this, "_hasChangedRange", !1);
    /**
    * Each overriding gravity adds an UID to the set and each removal removes it.
    * Gravity is overridden when there's at least one UID in the set.
    * Gravity is restored when the set is empty.
    * This is to prevent conflicts when gravity is overridden by more than one feature at the same time.
    */
    f(this, "_overriddenGravityRegister", /* @__PURE__ */ new Set());
    /**
    * Prefixes of marker names that should affect `LiveSelection#markers` collection.
    */
    f(this, "_observedMarkers", /* @__PURE__ */ new Set());
    this._model = e.model, this._document = e, this.listenTo(this._model, "applyOperation", (i, s) => {
      const r = s[0];
      !r.isDocumentOperation || r.type == "marker" || r.type == "rename" || r.type == "noop" || (this._ranges.length == 0 && this._selectionRestorePosition && this._fixGraveyardSelection(this._selectionRestorePosition), this._selectionRestorePosition = null, this._hasChangedRange && (this._hasChangedRange = !1, this.fire("change:range", {
        directChange: !1
      })));
    }, {
      priority: "lowest"
    }), this.on("change:range", () => {
      this._validateSelectionRanges(this.getRanges());
    }), this.listenTo(this._model.markers, "update", (i, s, r, o) => {
      this._updateMarker(s, o);
    }), this.listenTo(this._document, "change", (i, s) => {
      A7(this._model, s);
    });
  }
  get isCollapsed() {
    return this._ranges.length === 0 ? this._document._getDefaultRange().isCollapsed : super.isCollapsed;
  }
  get anchor() {
    return super.anchor || this._document._getDefaultRange().start;
  }
  get focus() {
    return super.focus || this._document._getDefaultRange().end;
  }
  get rangeCount() {
    return this._ranges.length ? this._ranges.length : 1;
  }
  /**
  * Describes whether `LiveSelection` has own range(s) set, or if it is defaulted to
  * {@link module:engine/model/document~Document#_getDefaultRange document's default range}.
  */
  get hasOwnRange() {
    return this._ranges.length > 0;
  }
  /**
  * When set to `true` then selection attributes on node before the caret won't be taken
  * into consideration while updating selection attributes.
  */
  get isGravityOverridden() {
    return !!this._overriddenGravityRegister.size;
  }
  /**
  * Unbinds all events previously bound by live selection.
  */
  destroy() {
    for (let e = 0; e < this._ranges.length; e++)
      this._ranges[e].detach();
    this.stopListening();
  }
  *getRanges() {
    this._ranges.length ? yield* super.getRanges() : yield this._document._getDefaultRange();
  }
  getFirstRange() {
    return super.getFirstRange() || this._document._getDefaultRange();
  }
  getLastRange() {
    return super.getLastRange() || this._document._getDefaultRange();
  }
  setTo(...e) {
    super.setTo(...e), this._updateAttributes(!0), this.updateMarkers();
  }
  setFocus(e, i) {
    super.setFocus(e, i), this._updateAttributes(!0), this.updateMarkers();
  }
  setAttribute(e, i) {
    if (this._setAttribute(e, i)) {
      const s = [
        e
      ];
      this.fire("change:attribute", {
        attributeKeys: s,
        directChange: !0
      });
    }
  }
  removeAttribute(e) {
    if (this._removeAttribute(e)) {
      const i = [
        e
      ];
      this.fire("change:attribute", {
        attributeKeys: i,
        directChange: !0
      });
    }
  }
  overrideGravity() {
    const e = Bt();
    return this._overriddenGravityRegister.add(e), this._overriddenGravityRegister.size === 1 && this._updateAttributes(!0), e;
  }
  restoreGravity(e) {
    if (!this._overriddenGravityRegister.has(e))
      throw new S("document-selection-gravity-wrong-restore", this, {
        uid: e
      });
    this._overriddenGravityRegister.delete(e), this.isGravityOverridden || this._updateAttributes(!0);
  }
  observeMarkers(e) {
    this._observedMarkers.add(e), this.updateMarkers();
  }
  _replaceAllRanges(e) {
    this._validateSelectionRanges(e), super._replaceAllRanges(e);
  }
  _popRange() {
    this._ranges.pop().detach();
  }
  _pushRange(e) {
    const i = this._prepareRange(e);
    i && this._ranges.push(i);
  }
  _validateSelectionRanges(e) {
    for (const i of e)
      if (!this._document._validateSelectionRange(i))
        throw new S("document-selection-wrong-position", this, {
          range: i
        });
  }
  /**
  * Prepares given range to be added to selection. Checks if it is correct,
  * converts it to {@link module:engine/model/liverange~LiveRange LiveRange}
  * and sets listeners listening to the range's change event.
  */
  _prepareRange(e) {
    if (this._checkRange(e), e.root == this._document.graveyard)
      return;
    const i = ni.fromRange(e);
    return i.on("change:range", (s, r, o) => {
      if (this._hasChangedRange = !0, i.root == this._document.graveyard) {
        this._selectionRestorePosition = o.deletionPosition;
        const a = this._ranges.indexOf(i);
        this._ranges.splice(a, 1), i.detach();
      }
    }), i;
  }
  updateMarkers() {
    if (!this._observedMarkers.size)
      return;
    const e = [];
    let i = !1;
    for (const r of this._model.markers) {
      const o = r.name.split(":", 1)[0];
      if (!this._observedMarkers.has(o))
        continue;
      const a = r.getRange();
      for (const l of this.getRanges())
        a.containsRange(l, !l.isCollapsed) && e.push(r);
    }
    const s = Array.from(this.markers);
    for (const r of e)
      this.markers.has(r) || (this.markers.add(r), i = !0);
    for (const r of Array.from(this.markers))
      e.includes(r) || (this.markers.remove(r), i = !0);
    i && this.fire("change:marker", {
      oldMarkers: s,
      directChange: !1
    });
  }
  _updateMarker(e, i) {
    const s = e.name.split(":", 1)[0];
    if (!this._observedMarkers.has(s))
      return;
    let r = !1;
    const o = Array.from(this.markers), a = this.markers.has(e);
    if (!i)
      a && (this.markers.remove(e), r = !0);
    else {
      let l = !1;
      for (const c of this.getRanges())
        if (i.containsRange(c, !c.isCollapsed)) {
          l = !0;
          break;
        }
      l && !a ? (this.markers.add(e), r = !0) : !l && a && (this.markers.remove(e), r = !0);
    }
    r && this.fire("change:marker", {
      oldMarkers: o,
      directChange: !1
    });
  }
  /**
  * Updates this selection attributes according to its ranges and the {@link module:engine/model/document~Document model document}.
  */
  _updateAttributes(e) {
    const i = Ji(this._getSurroundingAttributes()), s = Ji(this.getAttributes());
    if (e)
      this._attributePriority = /* @__PURE__ */ new Map(), this._attrs = /* @__PURE__ */ new Map();
    else
      for (const [o, a] of this._attributePriority)
        a == "low" && (this._attrs.delete(o), this._attributePriority.delete(o));
    this._setAttributesTo(i);
    const r = [];
    for (const [o, a] of this.getAttributes())
      (!s.has(o) || s.get(o) !== a) && r.push(o);
    for (const [o] of s)
      this.hasAttribute(o) || r.push(o);
    r.length > 0 && this.fire("change:attribute", {
      attributeKeys: r,
      directChange: !1
    });
  }
  /**
  * Internal method for setting `LiveSelection` attribute. Supports attribute priorities (through `directChange`
  * parameter).
  */
  _setAttribute(e, i, s = !0) {
    const r = s ? "normal" : "low";
    return r == "low" && this._attributePriority.get(e) == "normal" || super.getAttribute(e) === i ? !1 : (this._attrs.set(e, i), this._attributePriority.set(e, r), !0);
  }
  /**
  * Internal method for removing `LiveSelection` attribute. Supports attribute priorities (through `directChange`
  * parameter).
  *
  * NOTE: Even if attribute is not present in the selection but is provided to this method, it's priority will
  * be changed according to `directChange` parameter.
  */
  _removeAttribute(e, i = !0) {
    const s = i ? "normal" : "low";
    return s == "low" && this._attributePriority.get(e) == "normal" || (this._attributePriority.set(e, s), !super.hasAttribute(e)) ? !1 : (this._attrs.delete(e), !0);
  }
  /**
  * Internal method for setting multiple `LiveSelection` attributes. Supports attribute priorities (through
  * `directChange` parameter).
  */
  _setAttributesTo(e) {
    const i = /* @__PURE__ */ new Set();
    for (const [s, r] of this.getAttributes())
      e.get(s) !== r && this._removeAttribute(s, !1);
    for (const [s, r] of e)
      this._setAttribute(s, r, !1) && i.add(s);
    return i;
  }
  /**
  * Returns an iterable that iterates through all selection attributes stored in current selection's parent.
  */
  *getStoredAttributes() {
    const e = this.getFirstPosition().parent;
    if (this.isCollapsed && e.isEmpty)
      for (const i of e.getAttributeKeys())
        i.startsWith(oa) && (yield [
          i.substr(oa.length),
          e.getAttribute(i)
        ]);
  }
  /**
  * Checks model text nodes that are closest to the selection's first position and returns attributes of first
  * found element. If there are no text nodes in selection's first position parent, it returns selection
  * attributes stored in that parent.
  */
  _getSurroundingAttributes() {
    const e = this.getFirstPosition(), i = this._model.schema;
    if (e.root.rootName == "$graveyard")
      return null;
    let s = null;
    if (this.isCollapsed) {
      const r = e.textNode ? e.textNode : e.nodeBefore, o = e.textNode ? e.textNode : e.nodeAfter;
      if (this.isGravityOverridden || (s = bo(r, i)), s || (s = bo(o, i)), !this.isGravityOverridden && !s) {
        let a = r;
        for (; a && !s; )
          a = a.previousSibling, s = bo(a, i);
      }
      if (!s) {
        let a = o;
        for (; a && !s; )
          a = a.nextSibling, s = bo(a, i);
      }
      s || (s = this.getStoredAttributes());
    } else {
      const r = this.getFirstRange();
      for (const o of r) {
        if (o.item.is("element") && i.isObject(o.item)) {
          s = bo(o.item, i);
          break;
        }
        if (o.type == "text") {
          s = o.item.getAttributes();
          break;
        }
      }
    }
    return s;
  }
  /**
  * Fixes the selection after all its ranges got removed.
  * @param deletionPosition Position where the deletion happened.
  */
  _fixGraveyardSelection(e) {
    const i = this._model.schema.getNearestSelectionRange(e);
    i && this._pushRange(i);
  }
}
function bo(n, t) {
  if (!n)
    return null;
  if (n instanceof Ki || n instanceof lt)
    return n.getAttributes();
  if (!t.isInline(n))
    return null;
  if (!t.isObject(n))
    return [];
  const e = [];
  for (const [i, s] of n.getAttributes())
    t.checkAttribute("$text", i) && t.getAttributeProperties(i).copyFromObject !== !1 && e.push([
      i,
      s
    ]);
  return e;
}
function A7(n, t) {
  const e = n.document.differ;
  for (const i of e.getChanges()) {
    if (i.type != "insert")
      continue;
    const s = i.position.parent;
    i.length === s.maxOffset && n.enqueueChange(t, (o) => {
      const a = Array.from(s.getAttributeKeys()).filter((l) => l.startsWith(oa));
      for (const l of a)
        o.removeAttribute(l, s);
    });
  }
}
class jo {
  /**
  * Creates a node list.
  *
  * @internal
  * @param nodes Nodes contained in this node list.
  */
  constructor(t) {
    /**
    * Nodes contained in this node list.
    */
    f(this, "_nodes", []);
    /**
    * This array maps numbers (offsets) to node that is placed at that offset.
    *
    * This array is similar to `_nodes` with the difference that one node may occupy multiple consecutive items in the array.
    *
    * This array is needed to quickly retrieve a node that is placed at given offset.
    */
    f(this, "_offsetToNode", []);
    t && this._insertNodes(0, t);
  }
  /**
  * Iterable interface.
  *
  * Iterates over all nodes contained inside this node list.
  */
  [Symbol.iterator]() {
    return this._nodes[Symbol.iterator]();
  }
  /**
  * Number of nodes contained inside this node list.
  */
  get length() {
    return this._nodes.length;
  }
  /**
  * Sum of {@link module:engine/model/node~Node#offsetSize offset sizes} of all nodes contained inside this node list.
  */
  get maxOffset() {
    return this._offsetToNode.length;
  }
  /**
  * Gets the node at the given index. Returns `null` if incorrect index was passed.
  */
  getNode(t) {
    return this._nodes[t] || null;
  }
  /**
  * Gets the node at the given offset. Returns `null` if incorrect offset was passed.
  */
  getNodeAtOffset(t) {
    return this._offsetToNode[t] || null;
  }
  /**
  * Returns an index of the given node or `null` if given node does not have a parent.
  *
  * This is an alias to {@link module:engine/model/node~Node#index}.
  */
  getNodeIndex(t) {
    return t.index;
  }
  /**
  * Returns the offset at which given node is placed in its parent or `null` if given node does not have a parent.
  *
  * This is an alias to {@link module:engine/model/node~Node#startOffset}.
  */
  getNodeStartOffset(t) {
    return t.startOffset;
  }
  /**
  * Converts index to offset in node list.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `model-nodelist-index-out-of-bounds` if given index is less
  * than `0` or more than {@link #length}.
  */
  indexToOffset(t) {
    if (t == this._nodes.length)
      return this.maxOffset;
    const e = this._nodes[t];
    if (!e)
      throw new S("model-nodelist-index-out-of-bounds", this);
    return this.getNodeStartOffset(e);
  }
  /**
  * Converts offset in node list to index.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `model-nodelist-offset-out-of-bounds` if given offset is less
  * than `0` or more than {@link #maxOffset}.
  */
  offsetToIndex(t) {
    if (t == this._offsetToNode.length)
      return this._nodes.length;
    const e = this._offsetToNode[t];
    if (!e)
      throw new S("model-nodelist-offset-out-of-bounds", this, {
        offset: t,
        nodeList: this
      });
    return this.getNodeIndex(e);
  }
  /**
  * Inserts given nodes at given index.
  *
  * @internal
  * @param index Index at which nodes should be inserted.
  * @param nodes Nodes to be inserted.
  */
  _insertNodes(t, e) {
    const i = [];
    for (const r of e) {
      if (!(r instanceof Zs))
        throw new S("model-nodelist-insertnodes-not-node", this);
      i.push(r);
    }
    let s = this.indexToOffset(t);
    mp(this._nodes, i, t), mp(this._offsetToNode, I7(i), s);
    for (let r = t; r < this._nodes.length; r++)
      this._nodes[r]._index = r, this._nodes[r]._startOffset = s, s += this._nodes[r].offsetSize;
  }
  /**
  * Removes one or more nodes starting at the given index.
  *
  * @internal
  * @param indexStart Index of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @returns Array containing removed nodes.
  */
  _removeNodes(t, e = 1) {
    if (e == 0)
      return [];
    let i = this.indexToOffset(t);
    const s = this._nodes.splice(t, e), r = s[s.length - 1], o = r.startOffset + r.offsetSize - i;
    this._offsetToNode.splice(i, o);
    for (const a of s)
      a._index = null, a._startOffset = null;
    for (let a = t; a < this._nodes.length; a++)
      this._nodes[a]._index = a, this._nodes[a]._startOffset = i, i += this._nodes[a].offsetSize;
    return s;
  }
  /**
  * Removes children nodes provided as an array. These nodes do not need to be direct siblings.
  *
  * This method is faster than removing nodes one by one, as it recalculates offsets only once.
  *
  * @internal
  * @param nodes Array of nodes.
  */
  _removeNodesArray(t) {
    if (t.length == 0)
      return;
    for (const i of t)
      i._index = null, i._startOffset = null;
    this._nodes = this._nodes.filter((i) => i.index !== null), this._offsetToNode = this._offsetToNode.filter((i) => i.index !== null);
    let e = 0;
    for (let i = 0; i < this._nodes.length; i++)
      this._nodes[i]._index = i, this._nodes[i]._startOffset = e, e += this._nodes[i].offsetSize;
  }
  /**
  * Converts `NodeList` instance to an array containing nodes that were inserted in the node list. Nodes
  * are also converted to their plain object representation.
  *
  * @returns `NodeList` instance converted to `Array`.
  */
  toJSON() {
    return this._nodes.map((t) => t.toJSON());
  }
}
function I7(n) {
  const t = [];
  let e = 0;
  for (const i of n)
    for (let s = 0; s < i.offsetSize; s++)
      t[e++] = i;
  return t;
}
class et extends Zs {
  /**
  * Creates a model element.
  *
  * **Note:** Constructor of this class shouldn't be used directly in the code.
  * Use the {@link module:engine/model/writer~Writer#createElement} method instead.
  *
  * @internal
  * @param name Element's name.
  * @param attrs Element's attributes. See {@link module:utils/tomap~toMap} for a list of accepted values.
  * @param children One or more nodes to be inserted as children of created element.
  */
  constructor(e, i, s) {
    super(i);
    /**
    * Element name.
    */
    f(this, "name");
    /**
    * List of children nodes.
    */
    f(this, "_children", new jo());
    this.name = e, s && this._insertChild(0, s);
  }
  /**
  * Number of this element's children.
  */
  get childCount() {
    return this._children.length;
  }
  /**
  * Sum of {@link module:engine/model/node~Node#offsetSize offset sizes} of all of this element's children.
  */
  get maxOffset() {
    return this._children.maxOffset;
  }
  /**
  * Is `true` if there are no nodes inside this element, `false` otherwise.
  */
  get isEmpty() {
    return this.childCount === 0;
  }
  /**
  * Gets the child at the given index. Returns `null` if incorrect index was passed.
  *
  * @param index Index in this element.
  * @returns Child node.
  */
  getChild(e) {
    return this._children.getNode(e);
  }
  /**
  * Gets the child at the given offset. Returns `null` if incorrect index was passed.
  *
  * @param offset Offset in this element.
  * @returns Child node.
  */
  getChildAtOffset(e) {
    return this._children.getNodeAtOffset(e);
  }
  /**
  * Returns an iterator that iterates over all of this element's children.
  */
  getChildren() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Returns an index of the given child node. Returns `null` if given node is not a child of this element.
  *
  * @param node Child node to look for.
  * @returns Child node's index in this element.
  */
  getChildIndex(e) {
    return this._children.getNodeIndex(e);
  }
  /**
  * Returns the starting offset of given child. Starting offset is equal to the sum of
  * {@link module:engine/model/node~Node#offsetSize offset sizes} of all node's siblings that are before it. Returns `null` if
  * given node is not a child of this element.
  *
  * @param node Child node to look for.
  * @returns Child node's starting offset.
  */
  getChildStartOffset(e) {
    return this._children.getNodeStartOffset(e);
  }
  /**
  * Returns index of a node that occupies given offset. If given offset is too low, returns `0`. If given offset is
  * too high, returns {@link module:engine/model/element~Element#getChildIndex index after last child}.
  *
  * ```ts
  * const textNode = new Text( 'foo' );
  * const pElement = new Element( 'p' );
  * const divElement = new Element( [ textNode, pElement ] );
  * divElement.offsetToIndex( -1 ); // Returns 0, because offset is too low.
  * divElement.offsetToIndex( 0 ); // Returns 0, because offset 0 is taken by `textNode` which is at index 0.
  * divElement.offsetToIndex( 1 ); // Returns 0, because `textNode` has `offsetSize` equal to 3, so it occupies offset 1 too.
  * divElement.offsetToIndex( 2 ); // Returns 0.
  * divElement.offsetToIndex( 3 ); // Returns 1.
  * divElement.offsetToIndex( 4 ); // Returns 2. There are no nodes at offset 4, so last available index is returned.
  * ```
  */
  offsetToIndex(e) {
    return this._children.offsetToIndex(e);
  }
  /**
  * Returns a descendant node by its path relative to this element.
  *
  * ```ts
  * // <this>a<b>c</b></this>
  * this.getNodeByPath( [ 0 ] );     // -> "a"
  * this.getNodeByPath( [ 1 ] );     // -> <b>
  * this.getNodeByPath( [ 1, 0 ] );  // -> "c"
  * ```
  *
  * @param relativePath Path of the node to find, relative to this element.
  */
  getNodeByPath(e) {
    let i = this;
    for (const s of e)
      i = i.getChildAtOffset(s);
    return i;
  }
  /**
  * Returns the parent element of the given name. Returns null if the element is not inside the desired parent.
  *
  * @param parentName The name of the parent element to find.
  * @param options Options object.
  * @param options.includeSelf When set to `true` this node will be also included while searching.
  */
  findAncestor(e, i = {}) {
    let s = i.includeSelf ? this : this.parent;
    for (; s; ) {
      if (s.name === e)
        return s;
      s = s.parent;
    }
    return null;
  }
  /**
  * Converts `Element` instance to plain object and returns it. Takes care of converting all of this element's children.
  *
  * @returns `Element` instance converted to plain object.
  */
  toJSON() {
    const e = super.toJSON();
    if (e.name = this.name, this._children.length > 0) {
      e.children = [];
      for (const i of this._children)
        e.children.push(i.toJSON());
    }
    return e;
  }
  /**
  * Creates a copy of this element and returns it. Created element has the same name and attributes as the original element.
  * If clone is deep, the original element's children are also cloned. If not, then empty element is returned.
  *
  * @internal
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any child.
  */
  _clone(e = !1) {
    const i = e ? R7(this._children) : void 0;
    return new et(this.name, this.getAttributes(), i);
  }
  /**
  * {@link module:engine/model/element~Element#_insertChild Inserts} one or more nodes at the end of this element.
  *
  * @see module:engine/model/writer~Writer#append
  * @internal
  * @param nodes Nodes to be inserted.
  */
  _appendChild(e) {
    this._insertChild(this.childCount, e);
  }
  /**
  * Inserts one or more nodes at the given index and sets {@link module:engine/model/node~Node#parent parent} of these nodes
  * to this element.
  *
  * @see module:engine/model/writer~Writer#insert
  * @internal
  * @param index Index at which nodes should be inserted.
  * @param items Items to be inserted.
  */
  _insertChild(e, i) {
    const s = P7(i);
    for (const r of s)
      r.parent !== null && r._remove(), r.parent = this;
    this._children._insertNodes(e, s);
  }
  /**
  * Removes one or more nodes starting at the given index and sets
  * {@link module:engine/model/node~Node#parent parent} of these nodes to `null`.
  *
  * @see module:engine/model/writer~Writer#remove
  * @internal
  * @param index Index of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @returns Array containing removed nodes.
  */
  _removeChildren(e, i = 1) {
    const s = this._children._removeNodes(e, i);
    for (const r of s)
      r.parent = null;
    return s;
  }
  /**
  * Removes children nodes provided as an array and sets
  * the {@link module:engine/model/node~Node#parent parent} of these nodes to `null`.
  *
  * These nodes do not need to be direct siblings.
  *
  * This method is faster than removing nodes one by one, as it recalculates offsets only once.
  *
  * @internal
  * @param nodes Array of nodes.
  */
  _removeChildrenArray(e) {
    this._children._removeNodesArray(e);
    for (const i of e)
      i.parent = null;
  }
  /**
  * Creates an `Element` instance from given plain object (i.e. parsed JSON string).
  * Converts `Element` children to proper nodes.
  *
  * @param json Plain object to be converted to `Element`.
  * @returns `Element` instance created using given plain object.
  */
  static fromJSON(e) {
    let i;
    if (e.children) {
      i = [];
      for (const s of e.children)
        s.name ? i.push(et.fromJSON(s)) : i.push(lt.fromJSON(s));
    }
    return new et(e.name, e.attributes, i);
  }
}
et.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "element" || n === "model:element") : n === "element" || n === "model:element" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "node" || n === "model:node";
};
function P7(n) {
  if (typeof n == "string")
    return [
      new lt(n)
    ];
  Ai(n) || (n = [
    n
  ]);
  const t = [];
  for (const e of n)
    typeof e == "string" ? t.push(new lt(e)) : e instanceof Ki ? t.push(new lt(e.data, e.getAttributes())) : t.push(e);
  return t;
}
function R7(n) {
  const t = [];
  for (const e of n)
    t.push(e._clone(!0));
  return t;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class M_ {
  /**
  * Creates a conversion helpers instance.
  */
  constructor(t) {
    f(this, "_dispatchers");
    this._dispatchers = t;
  }
  /**
  * Registers a conversion helper.
  *
  * **Note**: See full usage example in the `{@link module:engine/conversion/conversion~Conversion#for conversion.for()}`
  * method description.
  *
  * @param conversionHelper The function to be called on event.
  */
  add(t) {
    for (const e of this._dispatchers)
      t(e);
    return this;
  }
}
class O7 extends M_ {
  /**
  * Model element to view element conversion helper.
  *
  * This conversion results in creating a view element. For example, model `<paragraph>Foo</paragraph>` becomes `<p>Foo</p>` in the view.
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: 'paragraph',
  * 	view: 'p'
  * } );
  *
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: 'paragraph',
  * 	view: 'div',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: 'fancyParagraph',
  * 	view: {
  * 		name: 'p',
  * 		classes: 'fancy'
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: 'heading',
  * 	view: ( modelElement, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createContainerElement( 'h' + modelElement.getAttribute( 'level' ) );
  * 	}
  * } );
  * ```
  *
  * The element-to-element conversion supports the reconversion mechanism. It can be enabled by using either the `attributes` or
  * the `children` props on a model description. You will find a couple examples below.
  *
  * In order to reconvert an element if any of its direct children have been added or removed, use the `children` property on a `model`
  * description. For example, this model:
  *
  * ```xml
  * <box>
  * 	<paragraph>Some text.</paragraph>
  * </box>
  * ```
  *
  * will be converted into this structure in the view:
  *
  * ```html
  * <div class="box" data-type="single">
  * 	<p>Some text.</p>
  * </div>
  * ```
  *
  * But if more items were inserted in the model:
  *
  * ```xml
  * <box>
  * 	<paragraph>Some text.</paragraph>
  * 	<paragraph>Other item.</paragraph>
  * </box>
  * ```
  *
  * it will be converted into this structure in the view (note the element `data-type` change):
  *
  * ```html
  * <div class="box" data-type="multiple">
  * 	<p>Some text.</p>
  * 	<p>Other item.</p>
  * </div>
  * ```
  *
  * Such a converter would look like this (note that the `paragraph` elements are converted separately):
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: {
  * 		name: 'box',
  * 		children: true
  * 	},
  * 	view: ( modelElement, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createContainerElement( 'div', {
  * 			class: 'box',
  * 			'data-type': modelElement.childCount == 1 ? 'single' : 'multiple'
  * 		} );
  * 	}
  * } );
  * ```
  *
  * In order to reconvert element if any of its attributes have been updated, use the `attributes` property on a `model`
  * description. For example, this model:
  *
  * ```xml
  * <heading level="2">Some text.</heading>
  * ```
  *
  * will be converted into this structure in the view:
  *
  * ```html
  * <h2>Some text.</h2>
  * ```
  *
  * But if the `heading` element's `level` attribute has been updated to `3` for example, then
  * it will be converted into this structure in the view:
  *
  * ```html
  * <h3>Some text.</h3>
  * ```
  *
  * Such a converter would look as follows:
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).elementToElement( {
  * 	model: {
  * 		name: 'heading',
  * 		attributes: 'level'
  * 	},
  * 	view: ( modelElement, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createContainerElement( 'h' + modelElement.getAttribute( 'level' ) );
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * You can read more about the element-to-element conversion in the
  * {@glink framework/deep-dive/conversion/downcast downcast conversion} guide.
  *
  * @param config Conversion configuration.
  * @param config.model The description or a name of the model element to convert.
  * @param config.model.attributes The list of attribute names that should be consumed while creating
  * the view element. Note that the view will be reconverted if any of the listed attributes changes.
   * @param config.model.children Specifies whether the view element requires reconversion if the list
  * of the model child nodes changed.
  * @param config.view A view element definition or a function that takes the model element and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API}
  * as parameters and returns a view container element.
  */
  elementToElement(t) {
    return this.add(Y7(t));
  }
  /**
  * The model element to view structure (several elements) conversion helper.
  *
  * This conversion results in creating a view structure with one or more slots defined for the child nodes.
  * For example, a model `<table>` may become this structure in the view:
  *
  * ```html
  * <figure class="table">
  * 	<table>
  * 		<tbody>${ slot for table rows }</tbody>
  * 	</table>
  * </figure>
  * ```
  *
  * The children of the model's `<table>` element will be inserted into the `<tbody>` element.
  * If the `elementToElement()` helper was used, the children would be inserted into the `<figure>`.
  *
  * Imagine a table feature where for this model structure:
  *
  * ```xml
  * <table headingRows="1">
  * 	<tableRow> ... table cells 1 ... </tableRow>
  * 	<tableRow> ... table cells 2 ... </tableRow>
  * 	<tableRow> ... table cells 3 ... </tableRow>
  * 	<caption>Caption text</caption>
  * </table>
  * ```
  *
  * we want to generate this view structure:
  *
  * ```html
  * <figure class="table">
  * 	<table>
  * 		<thead>
  * 			<tr> ... table cells 1 ... </tr>
  * 		</thead>
  * 		<tbody>
  * 			<tr> ... table cells 2 ... </tr>
  * 			<tr> ... table cells 3 ... </tr>
  * 		</tbody>
  * 	</table>
  * 	<figcaption>Caption text</figcaption>
  * </figure>
  * ```
  *
  * The converter has to take the `headingRows` attribute into consideration when allocating the `<tableRow>` elements
  * into the `<tbody>` and `<thead>` elements. Hence, we need two slots and need to define proper filter callbacks for them.
  *
  * Additionally, all elements other than `<tableRow>` should be placed outside the `<table>` tag.
  * In the example above, this will handle the table caption.
  *
  * Such a converter would look like this:
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).elementToStructure( {
  * 	model: {
  * 		name: 'table',
  * 		attributes: [ 'headingRows' ]
  * 	},
  * 	view: ( modelElement, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		const figureElement = writer.createContainerElement( 'figure', { class: 'table' } );
  * 		const tableElement = writer.createContainerElement( 'table' );
  *
  * 		writer.insert( writer.createPositionAt( figureElement, 0 ), tableElement );
  *
  * 		const headingRows = modelElement.getAttribute( 'headingRows' ) || 0;
  *
  * 		if ( headingRows > 0 ) {
  * 			const tableHead = writer.createContainerElement( 'thead' );
  *
  * 			const headSlot = writer.createSlot( node => node.is( 'element', 'tableRow' ) && node.index < headingRows );
  *
  * 			writer.insert( writer.createPositionAt( tableElement, 'end' ), tableHead );
  * 			writer.insert( writer.createPositionAt( tableHead, 0 ), headSlot );
  * 		}
  *
  * 		if ( headingRows < tableUtils.getRows( table ) ) {
  * 			const tableBody = writer.createContainerElement( 'tbody' );
  *
  * 			const bodySlot = writer.createSlot( node => node.is( 'element', 'tableRow' ) && node.index >= headingRows );
  *
  * 			writer.insert( writer.createPositionAt( tableElement, 'end' ), tableBody );
  * 			writer.insert( writer.createPositionAt( tableBody, 0 ), bodySlot );
  * 		}
  *
  * 		const restSlot = writer.createSlot( node => !node.is( 'element', 'tableRow' ) );
  *
  * 		writer.insert( writer.createPositionAt( figureElement, 'end' ), restSlot );
  *
  * 		return figureElement;
  * 	}
  * } );
  * ```
  *
  * Note: The children of a model element that's being converted must be allocated in the same order in the view
  * in which they are placed in the model.
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
   * @param config.model The description or a name of the model element to convert.
  * @param config.model.name The name of the model element to convert.
   * @param config.model.attributes The list of attribute names that should be consumed while creating
  * the view structure. Note that the view will be reconverted if any of the listed attributes will change.
  * @param config.view A function that takes the model element and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as parameters
  * and returns a view container element with slots for model child nodes to be converted into.
  */
  elementToStructure(t) {
    return this.add(J7(t));
  }
  /**
  * Model attribute to view element conversion helper.
  *
  * This conversion results in wrapping view nodes with a view attribute element. For example, a model text node with
  * `"Foo"` as data and the `bold` attribute becomes `<strong>Foo</strong>` in the view.
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: 'bold',
  * 	view: 'strong'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: 'bold',
  * 	view: 'b',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: 'invert',
  * 	view: {
  * 		name: 'span',
  * 		classes: [ 'font-light', 'bg-dark' ]
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: {
  * 		key: 'fontSize',
  * 		values: [ 'big', 'small' ]
  * 	},
  * 	view: {
  * 		big: {
  * 			name: 'span',
  * 			styles: {
  * 				'font-size': '1.2em'
  * 			}
  * 		},
  * 		small: {
  * 			name: 'span',
  * 			styles: {
  * 				'font-size': '0.8em'
  * 			}
  * 		}
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: 'bold',
  * 	view: ( modelAttributeValue, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createAttributeElement( 'span', {
  * 			style: 'font-weight:' + modelAttributeValue
  * 		} );
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToElement( {
  * 	model: {
  * 		key: 'color',
  * 		name: '$text'
  * 	},
  * 	view: ( modelAttributeValue, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createAttributeElement( 'span', {
  * 			style: 'color:' + modelAttributeValue
  * 		} );
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The key of the attribute to convert from or a `{ key, values }` object. `values` is an array
  * of `String`s with possible values if the model attribute is an enumerable.
  * @param config.view A view element definition or a function
  * that takes the model attribute value and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as parameters and returns a view
  * attribute element. If `config.model.values` is given, `config.view` should be an object assigning values from `config.model.values`
  * to view element definitions or functions.
  * @param config.converterPriority Converter priority.
  */
  attributeToElement(t) {
    return this.add(X7(t));
  }
  /**
  * Model attribute to view attribute conversion helper.
  *
  * This conversion results in adding an attribute to a view node, basing on an attribute from a model node. For example,
  * `<imageInline src='foo.jpg'></imageInline>` is converted to `<img src='foo.jpg'></img>`.
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: 'source',
  * 	view: 'src'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: 'source',
  * 	view: 'href',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: {
  * 		name: 'imageInline',
  * 		key: 'source'
  * 	},
  * 	view: 'src'
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: {
  * 		name: 'styled',
  * 		values: [ 'dark', 'light' ]
  * 	},
  * 	view: {
  * 		dark: {
  * 			key: 'class',
  * 			value: [ 'styled', 'styled-dark' ]
  * 		},
  * 		light: {
  * 			key: 'class',
  * 			value: [ 'styled', 'styled-light' ]
  * 		}
  * 	}
  * } );
  *
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: 'styled',
  * 	view: modelAttributeValue => ( {
  * 		key: 'class',
  * 		value: 'styled-' + modelAttributeValue
  * 	} )
  * } );
  * ```
  *
  * **Note**: Downcasting to a style property requires providing `value` as an object:
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).attributeToAttribute( {
  * 	model: 'lineHeight',
  * 	view: modelAttributeValue => ( {
  * 		key: 'style',
  * 		value: {
  * 			'line-height': modelAttributeValue,
  * 			'border-bottom': '1px dotted #ba2'
  * 		}
  * 	} )
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The key of the attribute to convert from or a `{ key, values, [ name ] }` object describing
  * the attribute key, possible values and, optionally, an element name to convert from.
  * @param config.view A view attribute key, or a `{ key, value }` object or a function that takes the model attribute value and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API}
  * as parameters and returns a `{ key, value }` object. If the `key` is `'class'`, the `value` can be a `String` or an
  * array of `String`s. If the `key` is `'style'`, the `value` is an object with key-value pairs. In other cases, `value` is a `String`.
  * If `config.model.values` is set, `config.view` should be an object assigning values from `config.model.values` to
  * `{ key, value }` objects or a functions.
  * @param config.converterPriority Converter priority.
  */
  attributeToAttribute(t) {
    return this.add(Q7(t));
  }
  /**
  * Model marker to view element conversion helper.
  *
  * **Note**: This method should be used mainly for editing the downcast and it is recommended
  * to use the {@link #markerToData `#markerToData()`} helper instead.
  *
  * This helper may produce invalid HTML code (e.g. a span between table cells).
  * It should only be used when you are sure that the produced HTML will be semantically correct.
  *
  * This conversion results in creating a view element on the boundaries of the converted marker. If the converted marker
  * is collapsed, only one element is created. For example, a model marker set like this: `<paragraph>F[oo b]ar</paragraph>`
  * becomes `<p>F<span data-marker="search"></span>oo b<span data-marker="search"></span>ar</p>` in the view.
  *
  * ```ts
  * editor.conversion.for( 'editingDowncast' ).markerToElement( {
  * 	model: 'search',
  * 	view: 'marker-search'
  * } );
  *
  * editor.conversion.for( 'editingDowncast' ).markerToElement( {
  * 	model: 'search',
  * 	view: 'search-result',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'editingDowncast' ).markerToElement( {
  * 	model: 'search',
  * 	view: {
  * 		name: 'span',
  * 		attributes: {
  * 			'data-marker': 'search'
  * 		}
  * 	}
  * } );
  *
  * editor.conversion.for( 'editingDowncast' ).markerToElement( {
  * 	model: 'search',
  * 	view: ( markerData, conversionApi ) => {
  * 		const { writer } = conversionApi;
  *
  * 		return writer.createUIElement( 'span', {
  * 			'data-marker': 'search',
  * 			'data-start': markerData.isOpening
  * 		} );
  * 	}
  * } );
  * ```
  *
  * If a function is passed as the `config.view` parameter, it will be used to generate both boundary elements. The function
  * receives the `data` object and {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API}
  * as a parameters and should return an instance of the
  * {@link module:engine/view/uielement~UIElement view UI element}. The `data` object and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi `conversionApi`} are passed from
  * {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher#event:addMarker}. Additionally,
  * the `data.isOpening` parameter is passed, which is set to `true` for the marker start boundary element, and `false` for
  * the marker end boundary element.
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The name of the model marker (or model marker group) to convert.
  * @param config.view A view element definition or a function that takes the model marker data and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as a parameters
  * and returns a view UI element.
  * @param config.converterPriority Converter priority.
  */
  markerToElement(t) {
    return this.add(eR(t));
  }
  /**
  * Model marker to highlight conversion helper.
  *
  * This conversion results in creating a highlight on view nodes. For this kind of conversion,
  * the {@link module:engine/conversion/downcasthelpers~HighlightDescriptor} should be provided.
  *
  * For text nodes, a `<span>` {@link module:engine/view/attributeelement~AttributeElement} is created and it wraps all text nodes
  * in the converted marker range. For example, a model marker set like this: `<paragraph>F[oo b]ar</paragraph>` becomes
  * `<p>F<span class="comment">oo b</span>ar</p>` in the view.
  *
  * {@link module:engine/view/containerelement~ContainerElement} may provide a custom way of handling highlight. Most often,
  * the element itself is given classes and attributes described in the highlight descriptor (instead of being wrapped in `<span>`).
  * For example, a model marker set like this:
  * `[<imageInline src="foo.jpg"></imageInline>]` becomes `<img src="foo.jpg" class="comment"></img>` in the view.
  *
  * For container elements, the conversion is two-step. While the converter processes the highlight descriptor and passes it
  * to a container element, it is the container element instance itself that applies values from the highlight descriptor.
  * So, in a sense, the converter takes care of stating what should be applied on what, while the element decides how to apply that.
  *
  * ```ts
  * editor.conversion.for( 'downcast' ).markerToHighlight( { model: 'comment', view: { classes: 'comment' } } );
  *
  * editor.conversion.for( 'downcast' ).markerToHighlight( {
  * 	model: 'comment',
  * 	view: { classes: 'comment' },
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'downcast' ).markerToHighlight( {
  * 	model: 'comment',
  * 	view: ( data, conversionApi ) => {
  * 		// Assuming that the marker name is in a form of comment:commentType:commentId.
  * 		const [ , commentType, commentId ] = data.markerName.split( ':' );
  *
  * 		return {
  * 			classes: [ 'comment', 'comment-' + commentType ],
  * 			attributes: { 'data-comment-id': commentId }
  * 		};
  * 	}
  * } );
  * ```
  *
  * If a function is passed as the `config.view` parameter, it will be used to generate the highlight descriptor. The function
  * receives the `data` object and {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API}
  * as the parameters and should return a
  * {@link module:engine/conversion/downcasthelpers~HighlightDescriptor highlight descriptor}.
  * The `data` object properties are passed from {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher#event:addMarker}.
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The name of the model marker (or model marker group) to convert.
  * @param config.view A highlight descriptor that will be used for highlighting or a function that takes the model marker data and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as a parameters
  * and returns a highlight descriptor.
  * @param config.converterPriority Converter priority.
  */
  markerToHighlight(t) {
    return this.add(iR(t));
  }
  /**
  * Model marker converter for data downcast.
  *
  * This conversion creates a representation for model marker boundaries in the view:
  *
  * * If the marker boundary is before or after a model element, a view attribute is set on a corresponding view element.
  * * In other cases, a view element with the specified tag name is inserted at the corresponding view position.
  *
  * Typically, the marker names use the `group:uniqueId:otherData` convention. For example: `comment:e34zfk9k2n459df53sjl34:zx32c`.
  * The default configuration for this conversion is that the first part is the `group` part and the rest of
  * the marker name becomes the `name` part.
  *
  * Tag and attribute names and values are generated from the marker name:
  *
  * * The templates for attributes are `data-[group]-start-before="[name]"`, `data-[group]-start-after="[name]"`,
  * `data-[group]-end-before="[name]"` and `data-[group]-end-after="[name]"`.
  * * The templates for view elements are `<[group]-start name="[name]">` and `<[group]-end name="[name]">`.
  *
  * Attributes mark whether the given marker's start or end boundary is before or after the given element.
  * The `data-[group]-start-before` and `data-[group]-end-after` attributes are favored.
  * The other two are used when the former two cannot be used.
  *
  * The conversion configuration can take a function that will generate different group and name parts.
  * If such a function is set as the `config.view` parameter, it is passed a marker name and it is expected to return an object with two
  * properties: `group` and `name`. If the function returns a falsy value, the conversion will not take place.
  *
  * Basic usage:
  *
  * ```ts
  * // Using the default conversion.
  * // In this case, all markers with names starting with 'comment:' will be converted.
  * // The `group` parameter will be set to `comment`.
  * // The `name` parameter will be the rest of the marker name (without the `:`).
  * editor.conversion.for( 'dataDowncast' ).markerToData( {
  * 	model: 'comment'
  * } );
  * ```
  *
  * An example of a view that may be generated by this conversion (assuming a marker with the name `comment:commentId:uid` marked
  * by `[]`):
  *
  * ```
  * // Model:
  * <paragraph>Foo[bar</paragraph>
  * <imageBlock src="abc.jpg"></imageBlock>]
  *
  * // View:
  * <p>Foo<comment-start name="commentId:uid"></comment-start>bar</p>
  * <figure data-comment-end-after="commentId:uid" class="image"><img src="abc.jpg" /></figure>
  * ```
  *
  * In the example above, the comment starts before "bar" and ends after the image.
  *
  * If the `name` part is empty, the following view may be generated:
  *
  * ```html
  * <p>Foo <myMarker-start></myMarker-start>bar</p>
  * <figure data-myMarker-end-after="" class="image"><img src="abc.jpg" /></figure>
  * ```
  *
  * **Note:** A situation where some markers have the `name` part and some do not, is incorrect and should be avoided.
  *
  * Examples where `data-group-start-after` and `data-group-end-before` are used:
  *
  * ```
  * // Model:
  * <blockQuote>[]<paragraph>Foo</paragraph></blockQuote>
  *
  * // View:
  * <blockquote><p data-group-end-before="name" data-group-start-before="name">Foo</p></blockquote>
  * ```
  *
  * Similarly, when a marker is collapsed after the last element:
  *
  * ```
  * // Model:
  * <blockQuote><paragraph>Foo</paragraph>[]</blockQuote>
  *
  * // View:
  * <blockquote><p data-group-end-after="name" data-group-start-after="name">Foo</p></blockquote>
  * ```
  *
  * When there are multiple markers from the same group stored in the same attribute of the same element, their
  * name parts are put together in the attribute value, for example: `data-group-start-before="name1,name2,name3"`.
  *
  * Other examples of usage:
  *
  * ```ts
  * // Using a custom function which is the same as the default conversion:
  * editor.conversion.for( 'dataDowncast' ).markerToData( {
  * 	model: 'comment',
  * 	view: markerName => ( {
  * 		group: 'comment',
  * 		name: markerName.substr( 8 ) // Removes 'comment:' part.
  * 	} )
  * } );
  *
  * // Using the converter priority:
  * editor.conversion.for( 'dataDowncast' ).markerToData( {
  * 	model: 'comment',
  * 	view: markerName => ( {
  * 		group: 'comment',
  * 		name: markerName.substr( 8 ) // Removes 'comment:' part.
  * 	} ),
  * 	converterPriority: 'high'
  * } );
  * ```
  *
  * This kind of conversion is useful for saving data into the database, so it should be used in the data conversion pipeline.
  *
  * See the {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} API guide to learn how to
  * add a converter to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.model The name of the model marker (or the model marker group) to convert.
  * @param config.view A function that takes the model marker name and
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi downcast conversion API} as the parameters
  * and returns an object with the `group` and `name` properties.
  * @param config.converterPriority Converter priority.
  */
  markerToData(t) {
    return this.add(tR(t));
  }
}
function N_() {
  return (n, t, e) => {
    if (!e.consumable.consume(t.item, n.name))
      return;
    const i = e.writer, s = e.mapper.toViewPosition(t.range.start), r = i.createText(t.item.data);
    i.insert(s, r);
  };
}
function F_() {
  return (n, t, e) => {
    e.convertAttributes(t.item), !t.reconversion && t.item.is("element") && !t.item.isEmpty && e.convertChildren(t.item);
  };
}
function V7() {
  return (n, t, e) => {
    const i = e.mapper.toViewPosition(t.position), s = t.position.getShiftedBy(t.length), r = e.mapper.toViewPosition(s, {
      isPhantom: !0
    }), o = e.writer.createRange(i, r), a = e.writer.remove(o.getTrimmed());
    for (const l of e.writer.createRangeIn(a).getItems())
      e.mapper.unbindViewElement(l, {
        defer: !0
      });
  };
}
function D_(n, t) {
  const e = n.createAttributeElement("span", t.attributes);
  return t.classes && e._addClass(t.classes), typeof t.priority == "number" && (e._priority = t.priority), e._id = t.id, e;
}
function L7() {
  return (n, t, e) => {
    const i = t.selection;
    if (i.isCollapsed || !e.consumable.consume(i, "selection"))
      return;
    const s = [];
    for (const r of i.getRanges())
      s.push(e.mapper.toViewRange(r));
    e.writer.setSelection(s, {
      backward: i.isBackward
    });
  };
}
function B7() {
  return (n, t, e) => {
    const i = t.selection;
    if (!i.isCollapsed || !e.consumable.consume(i, "selection"))
      return;
    const s = e.writer, r = i.getFirstPosition(), o = e.mapper.toViewPosition(r), a = s.breakAttributes(o);
    s.setSelection(a);
  };
}
function M7() {
  return (n, t, e) => {
    const i = e.writer, s = i.document.selection;
    for (const r of s.getRanges())
      r.isCollapsed && r.end.parent.isAttached() && e.writer.mergeAttributes(r.start);
    i.setSelection(null);
  };
}
function N7(n) {
  return (t, e, i) => {
    if (!i.consumable.test(e.item, t.name))
      return;
    const s = n(e.attributeOldValue, i, e), r = n(e.attributeNewValue, i, e);
    if (!s && !r)
      return;
    i.consumable.consume(e.item, t.name);
    const o = i.writer, a = o.document.selection;
    if (e.item instanceof Ei || e.item instanceof Xi)
      o.wrap(a.getFirstRange(), r);
    else {
      let l = i.mapper.toViewRange(e.range);
      e.attributeOldValue !== null && s && (l = o.unwrap(l, s)), e.attributeNewValue !== null && r && o.wrap(l, r);
    }
  };
}
function F7(n, t = cR) {
  return (e, i, s) => {
    if (!t(i.item, s.consumable, {
      preflight: !0
    }))
      return;
    const r = n(i.item, s, i);
    if (!r)
      return;
    t(i.item, s.consumable);
    const o = s.mapper.toViewPosition(i.range.start);
    s.mapper.bindElements(i.item, r), s.writer.insert(o, r), s.convertAttributes(i.item), U_(r, i.item.getChildren(), s, {
      reconversion: i.reconversion
    });
  };
}
function D7(n, t) {
  return (e, i, s) => {
    if (!t(i.item, s.consumable, {
      preflight: !0
    }))
      return;
    const r = /* @__PURE__ */ new Map();
    s.writer._registerSlotFactory(rR(i.item, r, s));
    const o = n(i.item, s, i);
    if (s.writer._clearSlotFactory(), !o)
      return;
    oR(i.item, r, s), t(i.item, s.consumable);
    const a = s.mapper.toViewPosition(i.range.start);
    s.mapper.bindElements(i.item, o), s.writer.insert(a, o), s.convertAttributes(i.item), aR(o, r, s, {
      reconversion: i.reconversion
    });
  };
}
function $7(n) {
  return (t, e, i) => {
    e.isOpening = !0;
    const s = n(e, i);
    e.isOpening = !1;
    const r = n(e, i);
    if (!s || !r)
      return;
    const o = e.markerRange;
    if (o.isCollapsed && !i.consumable.consume(o, t.name))
      return;
    for (const c of o)
      if (!i.consumable.consume(c.item, t.name))
        return;
    const a = i.mapper, l = i.writer;
    l.insert(a.toViewPosition(o.start), s), i.mapper.bindElementToMarker(s, e.markerName), o.isCollapsed || (l.insert(a.toViewPosition(o.end), r), i.mapper.bindElementToMarker(r, e.markerName)), t.stop();
  };
}
function z7() {
  return (n, t, e) => {
    const i = e.mapper.markerNameToElements(t.markerName);
    if (i) {
      for (const s of i)
        e.mapper.unbindElementFromMarkerName(s, t.markerName), e.writer.clear(e.writer.createRangeOn(s), s);
      e.writer.clearClonedElementsGroup(t.markerName), n.stop();
    }
  };
}
function H7(n) {
  return (t, e, i) => {
    const s = n(e.markerName, i);
    if (!s)
      return;
    const r = e.markerRange;
    i.consumable.consume(r, t.name) && (Op(r, !1, i, e, s), Op(r, !0, i, e, s), t.stop());
  };
}
function Op(n, t, e, i, s) {
  const r = t ? n.start : n.end, o = r.nodeAfter && r.nodeAfter.is("element") ? r.nodeAfter : null, a = r.nodeBefore && r.nodeBefore.is("element") ? r.nodeBefore : null;
  if (o || a) {
    let c, u;
    t && o || !t && !a ? (c = o, u = !0) : (c = a, u = !1);
    const d = e.mapper.toViewElement(c);
    if (d) {
      j7(d, t, u, e, i, s);
      return;
    }
  }
  const l = e.mapper.toViewPosition(r);
  U7(l, t, e, i, s);
}
function j7(n, t, e, i, s, r) {
  const o = `data-${r.group}-${t ? "start" : "end"}-${e ? "before" : "after"}`, a = n.hasAttribute(o) ? n.getAttribute(o).split(",") : [];
  a.unshift(r.name), i.writer.setAttribute(o, a.join(","), n), i.mapper.bindElementToMarker(n, s.markerName);
}
function U7(n, t, e, i, s) {
  const r = `${s.group}-${t ? "start" : "end"}`, o = s.name ? {
    name: s.name
  } : null, a = e.writer.createUIElement(r, o);
  e.writer.insert(n, a), e.mapper.bindElementToMarker(a, i.markerName);
}
function W7(n) {
  return (t, e, i) => {
    const s = n(e.markerName, i);
    if (!s)
      return;
    const r = i.mapper.markerNameToElements(e.markerName);
    if (!r)
      return;
    for (const a of r)
      i.mapper.unbindElementFromMarkerName(a, e.markerName), a.is("containerElement") ? (o(`data-${s.group}-start-before`, a), o(`data-${s.group}-start-after`, a), o(`data-${s.group}-end-before`, a), o(`data-${s.group}-end-after`, a)) : i.writer.clear(i.writer.createRangeOn(a), a);
    i.writer.clearClonedElementsGroup(e.markerName), t.stop();
    function o(a, l) {
      if (l.hasAttribute(a)) {
        const c = new Set(l.getAttribute(a).split(","));
        c.delete(s.name), c.size == 0 ? i.writer.removeAttribute(a, l) : i.writer.setAttribute(a, Array.from(c).join(","), l);
      }
    }
  };
}
function q7(n) {
  return (t, e, i) => {
    if (!i.consumable.test(e.item, t.name))
      return;
    const s = n(e.attributeOldValue, i, e), r = n(e.attributeNewValue, i, e);
    if (!s && !r)
      return;
    i.consumable.consume(e.item, t.name);
    const o = i.mapper.toViewElement(e.item), a = i.writer;
    if (!o)
      throw new S("conversion-attribute-to-attribute-on-text", i.dispatcher, e);
    if (e.attributeOldValue !== null && s) {
      let l = s.value;
      s.key == "style" && (typeof s.value == "string" ? l = new Br(a.document.stylesProcessor).setTo(s.value).getStylesEntries().map(([c]) => c) : l = Object.keys(s.value)), a.removeAttribute(s.key, l, o);
    }
    if (e.attributeNewValue !== null && r) {
      let l = r.value;
      r.key == "style" && typeof r.value == "string" && (l = Object.fromEntries(new Br(a.document.stylesProcessor).setTo(r.value).getStylesEntries())), a.setAttribute(r.key, l, !1, o);
    }
  };
}
function G7(n) {
  return (t, e, i) => {
    if (!e.item || !(e.item instanceof Ei || e.item instanceof Xi) && !e.item.is("$textProxy"))
      return;
    const s = Pf(n, e, i);
    if (!s || !i.consumable.consume(e.item, t.name))
      return;
    const r = i.writer, o = D_(r, s), a = r.document.selection;
    if (e.item instanceof Ei || e.item instanceof Xi)
      r.wrap(a.getFirstRange(), o);
    else {
      const l = i.mapper.toViewRange(e.range), c = r.wrap(l, o);
      for (const u of c.getItems())
        if (u.is("attributeElement") && u.isSimilar(o)) {
          i.mapper.bindElementToMarker(u, e.markerName);
          break;
        }
    }
  };
}
function K7(n) {
  return (t, e, i) => {
    if (!e.item || !(e.item instanceof et))
      return;
    const s = Pf(n, e, i);
    if (!s || !i.consumable.test(e.item, t.name))
      return;
    const r = i.mapper.toViewElement(e.item);
    if (r && r.getCustomProperty("addHighlight")) {
      i.consumable.consume(e.item, t.name);
      for (const a of F._createIn(e.item))
        i.consumable.consume(a.item, t.name);
      r.getCustomProperty("addHighlight")(r, s, i.writer), i.mapper.bindElementToMarker(r, e.markerName);
    }
  };
}
function Z7(n) {
  return (t, e, i) => {
    if (e.markerRange.isCollapsed)
      return;
    const s = Pf(n, e, i);
    if (!s)
      return;
    const r = D_(i.writer, s), o = i.mapper.markerNameToElements(e.markerName);
    if (o) {
      for (const a of o)
        i.mapper.unbindElementFromMarkerName(a, e.markerName), a.is("attributeElement") ? i.writer.unwrap(i.writer.createRangeOn(a), r) : a.getCustomProperty("removeHighlight")(a, s.id, i.writer);
      i.writer.clearClonedElementsGroup(e.markerName), t.stop();
    }
  };
}
function Y7(n) {
  const t = $_(n.model), e = aa(n.view, "container");
  return t.attributes.length && (t.children = !0), (i) => {
    i.on(`insert:${t.name}`, F7(e, j_(t)), {
      priority: n.converterPriority || "normal"
    }), (t.children || t.attributes.length) && i.on("reduceChanges", H_(t), {
      priority: "low"
    });
  };
}
function J7(n) {
  const t = $_(n.model), e = aa(n.view, "container");
  return t.children = !0, (i) => {
    if (i._conversionApi.schema.checkChild(t.name, "$text"))
      throw new S("conversion-element-to-structure-disallowed-text", i, {
        elementName: t.name
      });
    i.on(`insert:${t.name}`, D7(e, j_(t)), {
      priority: n.converterPriority || "normal"
    }), i.on("reduceChanges", H_(t), {
      priority: "low"
    });
  };
}
function X7(n) {
  n = Di(n);
  let t = n.model;
  typeof t == "string" && (t = {
    key: t
  });
  let e = `attribute:${t.key}`;
  if (t.name && (e += ":" + t.name), t.values)
    for (const s of t.values)
      n.view[s] = aa(n.view[s], "attribute");
  else
    n.view = aa(n.view, "attribute");
  const i = z_(n);
  return (s) => {
    s.on(e, N7(i), {
      priority: n.converterPriority || "normal"
    });
  };
}
function Q7(n) {
  n = Di(n);
  let t = n.model;
  typeof t == "string" && (t = {
    key: t
  });
  let e = `attribute:${t.key}`;
  if (t.name && (e += ":" + t.name), t.values)
    for (const s of t.values)
      n.view[s] = Vp(n.view[s]);
  else
    n.view = Vp(n.view);
  const i = z_(n);
  return (s) => {
    s.on(e, q7(i), {
      priority: n.converterPriority || "normal"
    });
  };
}
function eR(n) {
  const t = aa(n.view, "ui");
  return (e) => {
    e.on(`addMarker:${n.model}`, $7(t), {
      priority: n.converterPriority || "normal"
    }), e.on(`removeMarker:${n.model}`, z7(), {
      priority: n.converterPriority || "normal"
    });
  };
}
function tR(n) {
  n = Di(n);
  const t = n.model;
  let e = n.view;
  return e || (e = (i) => ({
    group: t,
    name: i.substr(n.model.length + 1)
  })), (i) => {
    i.on(`addMarker:${t}`, H7(e), {
      priority: n.converterPriority || "normal"
    }), i.on(`removeMarker:${t}`, W7(e), {
      priority: n.converterPriority || "normal"
    });
  };
}
function iR(n) {
  return (t) => {
    t.on(`addMarker:${n.model}`, G7(n.view), {
      priority: n.converterPriority || "normal"
    }), t.on(`addMarker:${n.model}`, K7(n.view), {
      priority: n.converterPriority || "normal"
    }), t.on(`removeMarker:${n.model}`, Z7(n.view), {
      priority: n.converterPriority || "normal"
    });
  };
}
function $_(n) {
  return typeof n == "string" && (n = {
    name: n
  }), {
    name: n.name,
    attributes: n.attributes ? Ue(n.attributes) : [],
    children: !!n.children
  };
}
function aa(n, t) {
  return typeof n == "function" ? n : (e, i) => nR(n, i, t);
}
function nR(n, t, e) {
  typeof n == "string" && (n = {
    name: n
  });
  let i;
  const s = t.writer, r = Object.assign({}, n.attributes);
  if (e == "container")
    i = s.createContainerElement(n.name, r);
  else if (e == "attribute") {
    const o = {
      priority: n.priority || Vs.DEFAULT_PRIORITY
    };
    i = s.createAttributeElement(n.name, r, o);
  } else
    i = s.createUIElement(n.name, r);
  if (n.styles) {
    const o = Object.keys(n.styles);
    for (const a of o)
      s.setStyle(a, n.styles[a], i);
  }
  if (n.classes) {
    const o = n.classes;
    if (typeof o == "string")
      s.addClass(o, i);
    else
      for (const a of o)
        s.addClass(a, i);
  }
  return i;
}
function z_(n) {
  return n.model.values ? (t, e, i) => {
    const s = n.view[t];
    return s ? s(t, e, i) : null;
  } : n.view;
}
function Vp(n) {
  return typeof n == "string" ? (t) => ({
    key: n,
    value: t
  }) : typeof n == "object" ? n.value ? () => n : (t) => ({
    key: n.key,
    value: t
  }) : n;
}
function Pf(n, t, e) {
  const i = typeof n == "function" ? n(t, e) : n;
  return i ? (i.priority || (i.priority = 10), i.id || (i.id = t.markerName), i) : null;
}
function sR(n) {
  return (t, e) => {
    if (!t.is("element", n.name))
      return !1;
    if (e.type == "attribute") {
      if (n.attributes.includes(e.attributeKey))
        return !0;
    } else {
      /* istanbul ignore else: This is always true because otherwise it would not register a reducer callback. -- @preserve */
      if (n.children)
        return !0;
    }
    return !1;
  };
}
function H_(n) {
  const t = sR(n);
  return (e, i) => {
    const s = [];
    i.reconvertedElements || (i.reconvertedElements = /* @__PURE__ */ new Set());
    for (const r of i.changes) {
      const o = r.type == "attribute" ? r.range.start.nodeAfter : r.position.parent;
      if (!o || !t(o, r)) {
        s.push(r);
        continue;
      }
      if (!i.reconvertedElements.has(o)) {
        i.reconvertedElements.add(o);
        const a = G._createBefore(o);
        let l = s.length;
        for (let c = s.length - 1; c >= 0; c--) {
          const u = s[c], h = (u.type == "attribute" ? u.range.start : u.position).compareWith(a);
          if (h == "before" || u.type == "remove" && h == "same")
            break;
          l = c;
        }
        s.splice(l, 0, {
          type: "remove",
          name: o.name,
          position: a,
          length: 1
        }, {
          type: "reinsert",
          name: o.name,
          position: a,
          length: 1
        });
      }
    }
    i.changes = s;
  };
}
function j_(n) {
  return (t, e, i = {}) => {
    const s = [
      "insert"
    ];
    for (const r of n.attributes)
      t.hasAttribute(r) && s.push(`attribute:${r}`);
    return s.every((r) => e.test(t, r)) ? (i.preflight || s.forEach((r) => e.consume(t, r)), !0) : !1;
  };
}
function rR(n, t, e) {
  return (i, s) => {
    const r = i.createContainerElement("$slot");
    let o = null;
    if (s === "children")
      o = Array.from(n.getChildren());
    else if (typeof s == "function")
      o = Array.from(n.getChildren()).filter((a) => s(a));
    else
      throw new S("conversion-slot-mode-unknown", e.dispatcher, {
        modeOrFilter: s
      });
    return t.set(r, o), r;
  };
}
function oR(n, t, e) {
  const i = Array.from(t.values()).flat(), s = new Set(i);
  if (s.size != i.length)
    throw new S("conversion-slot-filter-overlap", e.dispatcher, {
      element: n
    });
  if (s.size != n.childCount)
    throw new S("conversion-slot-filter-incomplete", e.dispatcher, {
      element: n
    });
}
function aR(n, t, e, i) {
  e.mapper.on("modelToViewPosition", o, {
    priority: "highest"
  });
  let s = null, r = null;
  for ([s, r] of t)
    U_(n, r, e, i), e.writer.move(e.writer.createRangeIn(s), e.writer.createPositionBefore(s)), e.writer.remove(s);
  e.mapper.off("modelToViewPosition", o);
  function o(a, l) {
    const c = l.modelPosition.nodeAfter, u = r.indexOf(c);
    u < 0 || (l.viewPosition = l.mapper.findPositionIn(s, u));
  }
}
function U_(n, t, e, i) {
  for (const s of t)
    lR(n.root, s, e, i) || e.convertItem(s);
}
function lR(n, t, e, i) {
  const { writer: s, mapper: r } = e;
  if (!i.reconversion)
    return !1;
  const o = r.toViewElement(t);
  return !o || o.root == n || !e.canReuseView(o) ? !1 : (s.move(s.createRangeOn(o), r.toViewPosition(G._createBefore(t))), !0);
}
function cR(n, t, { preflight: e } = {}) {
  return e ? t.test(n, "insert") : t.consume(n, "insert");
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function W_(n) {
  const { schema: t, document: e } = n.model;
  for (const i of e.getRoots())
    if (i.isEmpty && !t.checkChild(i, "$text") && t.checkChild(i, "paragraph"))
      return n.insertElement("paragraph", i), !0;
  return !1;
}
function q_(n, t, e) {
  const i = e.createContext(n);
  return !(!e.checkChild(i, "paragraph") || !e.checkChild(i.push("paragraph"), t));
}
function G_(n, t) {
  const e = t.createElement("paragraph");
  return t.insert(e, n), t.createPositionAt(e, 0);
}
class uR extends M_ {
  /**
  * View element to model element conversion helper.
  *
  * This conversion results in creating a model element. For example,
  * view `<p>Foo</p>` becomes `<paragraph>Foo</paragraph>` in the model.
  *
  * Keep in mind that the element will be inserted only if it is allowed
  * by {@link module:engine/model/schema~Schema schema} configuration.
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).elementToElement( {
  * 	view: 'p',
  * 	model: 'paragraph'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToElement( {
  * 	view: 'p',
  * 	model: 'paragraph',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToElement( {
  * 	view: {
  * 		name: 'p',
  * 		classes: 'fancy'
  * 	},
  * 	model: 'fancyParagraph'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToElement( {
  * 	view: {
  * 		name: 'p',
  * 		classes: 'heading'
  * 	},
  * 	model: ( viewElement, conversionApi ) => {
  * 		const modelWriter = conversionApi.writer;
  *
  * 		return modelWriter.createElement( 'heading', { level: viewElement.getAttribute( 'data-level' ) } );
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view Pattern matching all view elements which should be converted. If not set, the converter
  * will fire for every view element.
  * @param config.model Name of the model element, a model element instance or a function that takes a view element
  * and {@link module:engine/conversion/upcastdispatcher~UpcastConversionApi upcast conversion API}
  * and returns a model element. The model element will be inserted in the model.
  * @param config.converterPriority Converter priority.
  */
  elementToElement(t) {
    return this.add(K_(t));
  }
  /**
  * View element to model attribute conversion helper.
  *
  * This conversion results in setting an attribute on a model node. For example, view `<strong>Foo</strong>` becomes
  * `Foo` {@link module:engine/model/text~Text model text node} with `bold` attribute set to `true`.
  *
  * This helper is meant to set a model attribute on all the elements that are inside the converted element:
  *
  * ```
  * <strong>Foo</strong>   -->   <strong><p>Foo</p></strong>   -->   <paragraph><$text bold="true">Foo</$text></paragraph>
  * ```
  *
  * Above is a sample of HTML code, that goes through autoparagraphing (first step) and then is converted (second step).
  * Even though `<strong>` is over `<p>` element, `bold="true"` was added to the text. See
  * {@link module:engine/conversion/upcasthelpers~UpcastHelpers#attributeToAttribute} for comparison.
  *
  * Keep in mind that the attribute will be set only if it is allowed by {@link module:engine/model/schema~Schema schema} configuration.
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: 'strong',
  * 	model: 'bold'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: 'strong',
  * 	model: 'bold',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: {
  * 		name: 'span',
  * 		classes: 'bold'
  * 	},
  * 	model: 'bold'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: {
  * 		name: 'span',
  * 		classes: [ 'styled', 'styled-dark' ]
  * 	},
  * 	model: {
  * 		key: 'styled',
  * 		value: 'dark'
  * 	}
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToAttribute( {
  * 	view: {
  * 		name: 'span',
  * 		styles: {
  * 			'font-size': /[\s\S]+/
  * 		}
  * 	},
  * 	model: {
  * 		key: 'fontSize',
  * 		value: ( viewElement, conversionApi ) => {
  * 			const fontSize = viewElement.getStyle( 'font-size' );
  * 			const value = fontSize.substr( 0, fontSize.length - 2 );
  *
  * 			if ( value <= 10 ) {
  * 				return 'small';
  * 			} else if ( value > 12 ) {
  * 				return 'big';
  * 			}
  *
  * 			return null;
  * 		}
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view Pattern matching all view elements which should be converted.
  * @param config.model Model attribute key or an object with `key` and `value` properties, describing
  * the model attribute. `value` property may be set as a function that takes a view element and
  * {@link module:engine/conversion/upcastdispatcher~UpcastConversionApi upcast conversion API} and returns the value.
  * If `String` is given, the model attribute value will be set to `true`.
  * @param config.converterPriority Converter priority. Defaults to `low`.
  */
  elementToAttribute(t) {
    return this.add(fR(t));
  }
  /**
  * View attribute to model attribute conversion helper.
  *
  * This conversion results in setting an attribute on a model node. For example, view `<img src="foo.jpg"></img>` becomes
  * `<imageBlock source="foo.jpg"></imageBlock>` in the model.
  *
  * This helper is meant to convert view attributes from view elements which got converted to the model, so the view attribute
  * is set only on the corresponding model node:
  *
  * ```
  * <div class="dark"><div>foo</div></div>    -->    <div dark="true"><div>foo</div></div>
  * ```
  *
  * Above, `class="dark"` attribute is added only to the `<div>` elements that has it. This is in contrast to
  * {@link module:engine/conversion/upcasthelpers~UpcastHelpers#elementToAttribute} which sets attributes for
  * all the children in the model:
  *
  * ```
  * <strong>Foo</strong>   -->   <strong><p>Foo</p></strong>   -->   <paragraph><$text bold="true">Foo</$text></paragraph>
  * ```
  *
  * Above is a sample of HTML code, that goes through autoparagraphing (first step) and then is converted (second step).
  * Even though `<strong>` is over `<p>` element, `bold="true"` was added to the text.
  *
  * Keep in mind that the attribute will be set only if it is allowed by {@link module:engine/model/schema~Schema schema} configuration.
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: 'src',
  * 	model: 'source'
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: { key: 'src' },
  * 	model: 'source'
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: { key: 'src' },
  * 	model: 'source',
  * 	converterPriority: 'normal'
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		key: 'data-style',
  * 		value: /[\s\S]+/
  * 	},
  * 	model: 'styled'
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		name: 'img',
  * 		key: 'class',
  * 		value: 'styled-dark'
  * 	},
  * 	model: {
  * 		key: 'styled',
  * 		value: 'dark'
  * 	}
  * } );
  *
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		key: 'class',
  * 		value: /styled-[\S]+/
  * 	},
  * 	model: {
  * 		key: 'styled'
  * 		value: ( viewElement, conversionApi ) => {
  * 			const regexp = /styled-([\S]+)/;
  * 			const match = viewElement.getAttribute( 'class' ).match( regexp );
  *
  * 			return match[ 1 ];
  * 		}
  * 	}
  * } );
  * ```
  *
  * Converting styles works a bit differently as it requires `view.styles` to be an object and by default
  * a model attribute will be set to `true` by such a converter. You can set the model attribute to any value by providing the `value`
  * callback that returns the desired value.
  *
  * ```ts
  * // Default conversion of font-weight style will result in setting bold attribute to true.
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		styles: {
  * 			'font-weight': 'bold'
  * 		}
  * 	},
  * 	model: 'bold'
  * } );
  *
  * // This converter will pass any style value to the `lineHeight` model attribute.
  * editor.conversion.for( 'upcast' ).attributeToAttribute( {
  * 	view: {
  * 		styles: {
  * 			'line-height': /[\s\S]+/
  * 		}
  * 	},
  * 	model: {
  * 		key: 'lineHeight',
  * 		value: ( viewElement, conversionApi ) => viewElement.getStyle( 'line-height' )
  * 	}
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view Specifies which view attribute will be converted. If a `String` is passed,
  * attributes with given key will be converted. If an `Object` is passed, it must have a required `key` property,
  * specifying view attribute key, and may have an optional `value` property, specifying view attribute value and optional `name`
  * property specifying a view element name from/on which the attribute should be converted. `value` can be given as a `String`,
  * a `RegExp` or a function callback, that takes view attribute value as the only parameter and returns `Boolean`.
  * @param config.model Model attribute key or an object with `key` and `value` properties, describing
  * the model attribute. `value` property may be set as a function that takes a view element and
  * {@link module:engine/conversion/upcastdispatcher~UpcastConversionApi upcast conversion API} and returns the value.
  * If `String` is given, the model attribute value will be same as view attribute value.
  * @param config.converterPriority Converter priority. Defaults to `low`.
  */
  attributeToAttribute(t) {
    return this.add(mR(t));
  }
  /**
  * View element to model marker conversion helper.
  *
  * This conversion results in creating a model marker. For example, if the marker was stored in a view as an element:
  * `<p>Fo<span data-marker="comment" data-comment-id="7"></span>o</p><p>B<span data-marker="comment" data-comment-id="7"></span>ar</p>`,
  * after the conversion is done, the marker will be available in
  * {@link module:engine/model/model~Model#markers model document markers}.
  *
  * **Note**: When this helper is used in the data upcast in combination with
  * {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToData `#markerToData()`} in the data downcast,
  * then invalid HTML code (e.g. a span between table cells) may be produced by the latter converter.
  *
  * In most of the cases, the {@link #dataToMarker} should be used instead.
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).elementToMarker( {
  * 	view: 'marker-search',
  * 	model: 'search'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToMarker( {
  * 	view: 'marker-search',
  * 	model: 'search',
  * 	converterPriority: 'high'
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToMarker( {
  * 	view: 'marker-search',
  * 	model: ( viewElement, conversionApi ) => 'comment:' + viewElement.getAttribute( 'data-comment-id' )
  * } );
  *
  * editor.conversion.for( 'upcast' ).elementToMarker( {
  * 	view: {
  * 		name: 'span',
  * 		attributes: {
  * 			'data-marker': 'search'
  * 		}
  * 	},
  * 	model: 'search'
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view Pattern matching all view elements which should be converted.
  * @param config.model Name of the model marker, or a function that takes a view element and returns
  * a model marker name.
  * @param config.converterPriority Converter priority.
  */
  elementToMarker(t) {
    return this.add(gR(t));
  }
  /**
  * View-to-model marker conversion helper.
  *
  * Converts view data created by {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToData `#markerToData()`}
  * back to a model marker.
  *
  * This converter looks for specific view elements and view attributes that mark marker boundaries. See
  * {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToData `#markerToData()`} to learn what view data
  * is expected by this converter.
  *
  * The `config.view` property is equal to the marker group name to convert.
  *
  * By default, this converter creates markers with the `group:name` name convention (to match the default `markerToData` conversion).
  *
  * The conversion configuration can take a function that will generate a marker name.
  * If such function is set as the `config.model` parameter, it is passed the `name` part from the view element or attribute and it is
  * expected to return a string with the marker name.
  *
  * Basic usage:
  *
  * ```ts
  * // Using the default conversion.
  * // In this case, all markers from the `comment` group will be converted.
  * // The conversion will look for `<comment-start>` and `<comment-end>` tags and
  * // `data-comment-start-before`, `data-comment-start-after`,
  * // `data-comment-end-before` and `data-comment-end-after` attributes.
  * editor.conversion.for( 'upcast' ).dataToMarker( {
  * 	view: 'comment'
  * } );
  * ```
  *
  * An example of a model that may be generated by this conversion:
  *
  * ```
  * // View:
  * <p>Foo<comment-start name="commentId:uid"></comment-start>bar</p>
  * <figure data-comment-end-after="commentId:uid" class="image"><img src="abc.jpg" /></figure>
  *
  * // Model:
  * <paragraph>Foo[bar</paragraph>
  * <imageBlock src="abc.jpg"></imageBlock>]
  * ```
  *
  * Where `[]` are boundaries of a marker that will receive the `comment:commentId:uid` name.
  *
  * Other examples of usage:
  *
  * ```ts
  * // Using a custom function which is the same as the default conversion:
  * editor.conversion.for( 'upcast' ).dataToMarker( {
  * 	view: 'comment',
  * 	model: ( name, conversionApi ) => 'comment:' + name,
  * } );
  *
  * // Using the converter priority:
  * editor.conversion.for( 'upcast' ).dataToMarker( {
  * 	view: 'comment',
  * 	model: ( name, conversionApi ) => 'comment:' + name,
  * 	converterPriority: 'high'
  * } );
  * ```
  *
  * See {@link module:engine/conversion/conversion~Conversion#for `conversion.for()`} to learn how to add a converter
  * to the conversion process.
  *
  * @param config Conversion configuration.
  * @param config.view The marker group name to convert.
  * @param config.model A function that takes the `name` part from the view element or attribute and
  * {@link module:engine/conversion/upcastdispatcher~UpcastConversionApi upcast conversion API} and returns the marker name.
  * @param config.converterPriority Converter priority.
  */
  dataToMarker(t) {
    return this.add(pR(t));
  }
}
function Lp() {
  return (n, t, e) => {
    if (!t.modelRange && e.consumable.consume(t.viewItem, {
      name: !0
    })) {
      const { modelRange: i, modelCursor: s } = e.convertChildren(t.viewItem, t.modelCursor);
      t.modelRange = i, t.modelCursor = s;
    }
  };
}
function dR() {
  return (n, t, { schema: e, consumable: i, writer: s }) => {
    let r = t.modelCursor;
    if (!i.test(t.viewItem))
      return;
    if (!e.checkChild(r, "$text")) {
      if (!q_(r, "$text", e) || t.viewItem.data.trim().length == 0)
        return;
      r = G_(r, s);
    }
    i.consume(t.viewItem);
    const o = s.createText(t.viewItem.data);
    s.insert(o, r), t.modelRange = s.createRange(r, r.getShiftedBy(o.offsetSize)), t.modelCursor = t.modelRange.end;
  };
}
function hR(n, t) {
  return (e, i) => {
    const s = i.newSelection, r = [];
    for (const a of s.getRanges())
      r.push(t.toModelRange(a));
    const o = n.createSelection(r, {
      backward: s.isBackward
    });
    o.isEqual(n.document.selection) || n.change((a) => {
      a.setSelection(o);
    });
  };
}
function K_(n) {
  n = Di(n);
  const t = dh(n), e = Rf(n.view), i = e ? `element:${e}` : "element";
  return (s) => {
    s.on(i, t, {
      priority: n.converterPriority || "normal"
    });
  };
}
function fR(n) {
  n = Di(n), Z_(n);
  const t = Y_(n, !1), e = Rf(n.view), i = e ? `element:${e}` : "element";
  return (s) => {
    s.on(i, t, {
      priority: n.converterPriority || "low"
    });
  };
}
function mR(n) {
  n = Di(n);
  let t = null;
  (typeof n.view == "string" || n.view.key) && (t = vR(n)), Z_(n, t);
  const e = Y_(n, !0);
  return (i) => {
    i.on("element", e, {
      priority: n.converterPriority || "low"
    });
  };
}
function gR(n) {
  const t = kR(n.model);
  return K_({
    ...n,
    model: t
  });
}
function pR(n) {
  n = Di(n), n.model || (n.model = (s) => s ? n.view + ":" + s : n.view);
  const t = {
    view: n.view,
    model: n.model
  }, e = dh(Bp(t, "start")), i = dh(Bp(t, "end"));
  return (s) => {
    s.on(`element:${n.view}-start`, e, {
      priority: n.converterPriority || "normal"
    }), s.on(`element:${n.view}-end`, i, {
      priority: n.converterPriority || "normal"
    });
    const r = Si.low, o = Si.highest, a = Si.get(n.converterPriority) / o;
    s.on("element", bR(t), {
      priority: r + a
    });
  };
}
function bR(n) {
  return (t, e, i) => {
    const s = `data-${n.view}`;
    if (!i.consumable.test(e.viewItem, {
      attributes: s + "-end-after"
    }) && !i.consumable.test(e.viewItem, {
      attributes: s + "-start-after"
    }) && !i.consumable.test(e.viewItem, {
      attributes: s + "-end-before"
    }) && !i.consumable.test(e.viewItem, {
      attributes: s + "-start-before"
    }))
      return;
    e.modelRange || Object.assign(e, i.convertChildren(e.viewItem, e.modelCursor)), i.consumable.consume(e.viewItem, {
      attributes: s + "-end-after"
    }) && r(e.modelRange.end, e.viewItem.getAttribute(s + "-end-after").split(",")), i.consumable.consume(e.viewItem, {
      attributes: s + "-start-after"
    }) && r(e.modelRange.end, e.viewItem.getAttribute(s + "-start-after").split(",")), i.consumable.consume(e.viewItem, {
      attributes: s + "-end-before"
    }) && r(e.modelRange.start, e.viewItem.getAttribute(s + "-end-before").split(",")), i.consumable.consume(e.viewItem, {
      attributes: s + "-start-before"
    }) && r(e.modelRange.start, e.viewItem.getAttribute(s + "-start-before").split(","));
    function r(o, a) {
      for (const l of a) {
        const c = n.model(l, i), u = i.writer.createElement("$marker", {
          "data-name": c
        });
        i.writer.insert(u, o), e.modelCursor.isEqual(o) ? e.modelCursor = e.modelCursor.getShiftedBy(1) : e.modelCursor = e.modelCursor._getTransformedByInsertion(o, 1), e.modelRange = e.modelRange._getTransformedByInsertion(o, 1)[0];
      }
    }
  };
}
function Rf(n) {
  return typeof n == "string" ? n : typeof n == "object" && typeof n.name == "string" ? n.name : null;
}
function dh(n) {
  const t = new oi(n.view);
  return (e, i, s) => {
    const r = t.match(i.viewItem);
    if (!r)
      return;
    const o = r.match;
    if (o.name = !0, !s.consumable.test(i.viewItem, o))
      return;
    const a = wR(n.model, i.viewItem, s);
    a && s.safeInsert(a, i.modelCursor) && (s.consumable.consume(i.viewItem, o), s.convertChildren(i.viewItem, a), s.updateConversionResult(a, i));
  };
}
function wR(n, t, e) {
  return n instanceof Function ? n(t, e) : e.writer.createElement(n);
}
function vR(n) {
  typeof n.view == "string" && (n.view = {
    key: n.view
  });
  const t = n.view.key, e = typeof n.view.value > "u" ? /[\s\S]*/ : n.view.value;
  let i;
  return t == "class" || t == "style" ? i = {
    [t == "class" ? "classes" : "styles"]: e
  } : i = {
    attributes: {
      [t]: e
    }
  }, n.view.name && (i.name = n.view.name), n.view = i, t;
}
function Z_(n, t = null) {
  const e = t === null ? !0 : (r) => r.getAttribute(t), i = typeof n.model != "object" ? n.model : n.model.key, s = typeof n.model != "object" || typeof n.model.value > "u" ? e : n.model.value;
  n.model = {
    key: i,
    value: s
  };
}
function Y_(n, t) {
  const e = new oi(n.view);
  return (i, s, r) => {
    if (!s.modelRange && t)
      return;
    const o = e.match(s.viewItem);
    if (!o || (_R(n.view, s.viewItem) ? o.match.name = !0 : delete o.match.name, !r.consumable.test(s.viewItem, o.match)))
      return;
    const a = n.model.key, l = typeof n.model.value == "function" ? n.model.value(s.viewItem, r) : n.model.value;
    if (l === null)
      return;
    s.modelRange || Object.assign(s, r.convertChildren(s.viewItem, s.modelCursor)), yR(s.modelRange, {
      key: a,
      value: l
    }, t, r) && (r.consumable.test(s.viewItem, {
      name: !0
    }) && (o.match.name = !0), r.consumable.consume(s.viewItem, o.match));
  };
}
function _R(n, t) {
  const e = typeof n == "function" ? n(t) : n;
  return typeof e == "object" && !Rf(e) ? !1 : !e.classes && !e.attributes && !e.styles;
}
function yR(n, t, e, i) {
  let s = !1;
  for (const r of Array.from(n.getItems({
    shallow: e
  })))
    i.schema.checkAttribute(r, t.key) && (s = !0, !r.hasAttribute(t.key) && i.writer.setAttribute(t.key, t.value, r));
  return s;
}
function kR(n) {
  return (t, e) => {
    const i = typeof n == "string" ? n : n(t, e);
    return e.writer.createElement("$marker", {
      "data-name": i
    });
  };
}
function Bp(n, t) {
  const e = (i, s) => {
    const r = i.getAttribute("name"), o = n.model(r, s);
    return s.writer.createElement("$marker", {
      "data-name": o
    });
  };
  return {
    // Upcast <markerGroup-start> and <markerGroup-end> elements.
    view: `${n.view}-${t}`,
    model: e
  };
}
function CR(n) {
  n.document.registerPostFixer((t) => xR(t, n));
}
function xR(n, t) {
  const e = t.document.selection, i = t.schema, s = [];
  let r = !1;
  for (const o of e.getRanges()) {
    const a = J_(o, i);
    a && !a.isEqual(o) ? (s.push(a), r = !0) : s.push(o);
  }
  return r && n.setSelection(AR(s), {
    backward: e.isBackward
  }), !1;
}
function J_(n, t) {
  return n.isCollapsed ? TR(n, t) : ER(n, t);
}
function TR(n, t) {
  const e = n.start, i = t.getNearestSelectionRange(e);
  if (!i) {
    const r = e.getAncestors().reverse().find((o) => t.isObject(o));
    return r ? F._createOn(r) : null;
  }
  if (!i.isCollapsed)
    return i;
  const s = i.start;
  return e.isEqual(s) ? null : new F(s);
}
function ER(n, t) {
  const { start: e, end: i } = n, s = t.checkChild(e, "$text"), r = t.checkChild(i, "$text"), o = t.getLimitElement(e), a = t.getLimitElement(i);
  if (o === a) {
    if (s && r)
      return null;
    if (SR(e, i, t)) {
      const d = e.nodeAfter && t.isSelectable(e.nodeAfter) ? null : t.getNearestSelectionRange(e, "forward"), m = i.nodeBefore && t.isSelectable(i.nodeBefore) ? null : t.getNearestSelectionRange(i, "backward"), g = d ? d.start : e, p = m ? m.end : i;
      return new F(g, p);
    }
  }
  const l = o && !o.is("rootElement"), c = a && !a.is("rootElement");
  if (l || c) {
    const u = e.nodeAfter && i.nodeBefore && e.nodeAfter.parent === i.nodeBefore.parent, d = l && (!u || !Np(e.nodeAfter, t)), h = c && (!u || !Np(i.nodeBefore, t));
    let m = e, g = i;
    return d && (m = G._createBefore(Mp(o, t))), h && (g = G._createAfter(Mp(a, t))), new F(m, g);
  }
  return null;
}
function Mp(n, t) {
  let e = n, i = e;
  for (; t.isLimit(i) && i.parent; )
    e = i, i = i.parent;
  return e;
}
function SR(n, t, e) {
  const i = n.nodeAfter && !e.isLimit(n.nodeAfter) || e.checkChild(n, "$text"), s = t.nodeBefore && !e.isLimit(t.nodeBefore) || e.checkChild(t, "$text");
  return i || s;
}
function AR(n) {
  const t = [
    ...n
  ], e = /* @__PURE__ */ new Set();
  let i = 1;
  for (; i < t.length; ) {
    const r = t[i], o = t.slice(0, i);
    for (const [a, l] of o.entries())
      if (!e.has(a)) {
        if (r.isEqual(l))
          e.add(a);
        else if (r.isIntersecting(l)) {
          e.add(a), e.add(i);
          const c = r.getJoined(l);
          t.push(c);
        }
      }
    i++;
  }
  return t.filter((r, o) => !e.has(o));
}
function Np(n, t) {
  return n && t.isSelectable(n);
}
class IR extends (/* @__PURE__ */ je()) {
  /**
  * Creates an editing controller instance.
  *
  * @param model Editing model.
  * @param stylesProcessor The styles processor instance.
  */
  constructor(e, i) {
    super();
    /**
    * Editor model.
    */
    f(this, "model");
    /**
    * Editing view controller.
    */
    f(this, "view");
    /**
    * A mapper that describes the model-view binding.
    */
    f(this, "mapper");
    /**
    * Downcast dispatcher that converts changes from the model to the {@link #view editing view}.
    */
    f(this, "downcastDispatcher");
    this.model = e, this.view = new m7(i), this.mapper = new V_(), this.downcastDispatcher = new L_({
      mapper: this.mapper,
      schema: e.schema
    });
    const s = this.model.document, r = s.selection, o = this.model.markers;
    this.listenTo(this.model, "_beforeChanges", () => {
      this.view._disableRendering(!0);
    }, {
      priority: "highest"
    }), this.listenTo(this.model, "_afterChanges", () => {
      this.view._disableRendering(!1);
    }, {
      priority: "lowest"
    }), this.listenTo(s, "change", () => {
      this.view.change((a) => {
        this.downcastDispatcher.convertChanges(s.differ, o, a), this.downcastDispatcher.convertSelection(r, o, a);
      });
    }, {
      priority: "low"
    }), this.listenTo(this.view.document, "selectionChange", hR(this.model, this.mapper)), this.listenTo(this.view.document, "beforeinput", PR(this.mapper, this.model.schema, this.view), {
      priority: "high"
    }), this.downcastDispatcher.on("insert:$text", N_(), {
      priority: "lowest"
    }), this.downcastDispatcher.on("insert", F_(), {
      priority: "lowest"
    }), this.downcastDispatcher.on("remove", V7(), {
      priority: "low"
    }), this.downcastDispatcher.on("cleanSelection", M7()), this.downcastDispatcher.on("selection", L7(), {
      priority: "low"
    }), this.downcastDispatcher.on("selection", B7(), {
      priority: "low"
    }), this.view.document.roots.bindTo(this.model.document.roots).using((a) => {
      if (a.rootName == "$graveyard")
        return null;
      const l = new __(this.view.document, a.name);
      return l.rootName = a.rootName, this.mapper.bindElements(a, l), l;
    });
  }
  /**
  * Removes all event listeners attached to the `EditingController`. Destroys all objects created
  * by `EditingController` that need to be destroyed.
  */
  destroy() {
    this.view.destroy(), this.stopListening();
  }
  /**
  * Calling this method will refresh the marker by triggering the downcast conversion for it.
  *
  * Reconverting the marker is useful when you want to change its {@link module:engine/view/element~Element view element}
  * without changing any marker data. For instance:
  *
  * ```ts
  * let isCommentActive = false;
  *
  * model.conversion.markerToHighlight( {
  * 	model: 'comment',
  * 	view: data => {
  * 		const classes = [ 'comment-marker' ];
  *
  * 		if ( isCommentActive ) {
  * 			classes.push( 'comment-marker--active' );
  * 		}
  *
  * 		return { classes };
  * 	}
  * } );
  *
  * // ...
  *
  * // Change the property that indicates if marker is displayed as active or not.
  * isCommentActive = true;
  *
  * // Reconverting will downcast and synchronize the marker with the new isCommentActive state value.
  * editor.editing.reconvertMarker( 'comment' );
  * ```
  *
  * **Note**: If you want to reconvert a model item, use {@link #reconvertItem} instead.
  *
  * @param markerOrName Name of a marker to update, or a marker instance.
  */
  reconvertMarker(e) {
    const i = typeof e == "string" ? e : e.name, s = this.model.markers.get(i);
    if (!s)
      throw new S("editingcontroller-reconvertmarker-marker-not-exist", this, {
        markerName: i
      });
    this.model.change(() => {
      this.model.markers._refresh(s);
    });
  }
  /**
  * Calling this method will downcast a model item on demand (by requesting a refresh in the {@link module:engine/model/differ~Differ}).
  *
  * You can use it if you want the view representation of a specific item updated as a response to external modifications. For instance,
  * when the view structure depends not only on the associated model data but also on some external state.
  *
  * **Note**: If you want to reconvert a model marker, use {@link #reconvertMarker} instead.
  *
  * @param item Item to refresh.
  */
  reconvertItem(e) {
    this.model.change(() => {
      this.model.document.differ._refreshItem(e);
    });
  }
}
function PR(n, t, e) {
  return (i, s) => {
    if (!(e.document.isComposing && !be.isAndroid))
      for (let r = 0; r < s.targetRanges.length; r++) {
        const o = s.targetRanges[r], a = n.toModelRange(o), l = J_(a, t);
        !l || l.isEqual(a) || (s.targetRanges[r] = n.toViewRange(l));
      }
  };
}
class RR extends (/* @__PURE__ */ je()) {
  /**
  * Creates a schema instance.
  */
  constructor() {
    super();
    f(this, "_sourceDefinitions", {});
    /**
    * A dictionary containing attribute properties.
    */
    f(this, "_attributeProperties", /* @__PURE__ */ Object.create(null));
    /**
    * Stores additional callbacks registered for schema items, which are evaluated when {@link ~Schema#checkChild} is called.
    *
    * Keys are schema item names for which the callbacks are registered. Values are arrays with the callbacks.
    *
    * Some checks are added under {@link ~Schema#_genericCheckSymbol} key, these are evaluated for every {@link ~Schema#checkChild} call.
    */
    f(this, "_customChildChecks", /* @__PURE__ */ new Map());
    /**
    * Stores additional callbacks registered for attribute names, which are evaluated when {@link ~Schema#checkAttribute} is called.
    *
    * Keys are schema attribute names for which the callbacks are registered. Values are arrays with the callbacks.
    *
    * Some checks are added under {@link ~Schema#_genericCheckSymbol} key, these are evaluated for every
    * {@link ~Schema#checkAttribute} call.
    */
    f(this, "_customAttributeChecks", /* @__PURE__ */ new Map());
    f(this, "_genericCheckSymbol", Symbol("$generic"));
    f(this, "_compiledDefinitions");
    this.decorate("checkChild"), this.decorate("checkAttribute"), this.on("checkAttribute", (e, i) => {
      i[0] = new hs(i[0]);
    }, {
      priority: "highest"
    }), this.on("checkChild", (e, i) => {
      i[0] = new hs(i[0]), i[1] = this.getDefinition(i[1]);
    }, {
      priority: "highest"
    });
  }
  /**
  * Registers a schema item. Can only be called once for every item name.
  *
  * ```ts
  * schema.register( 'paragraph', {
  * 	inheritAllFrom: '$block'
  * } );
  * ```
  */
  register(e, i) {
    if (this._sourceDefinitions[e])
      throw new S("schema-cannot-register-item-twice", this, {
        itemName: e
      });
    this._sourceDefinitions[e] = [
      Object.assign({}, i)
    ], this._clearCache();
  }
  /**
  * Extends a {@link #register registered} item's definition.
  *
  * Extending properties such as `allowIn` will add more items to the existing properties,
  * while redefining properties such as `isBlock` will override the previously defined ones.
  *
  * ```ts
  * schema.register( 'foo', {
  * 	allowIn: '$root',
  * 	isBlock: true;
  * } );
  * schema.extend( 'foo', {
  * 	allowIn: 'blockQuote',
  * 	isBlock: false
  * } );
  *
  * schema.getDefinition( 'foo' );
  * //	{
  * //		allowIn: [ '$root', 'blockQuote' ],
  * // 		isBlock: false
  * //	}
  * ```
  */
  extend(e, i) {
    if (!this._sourceDefinitions[e])
      throw new S("schema-cannot-extend-missing-item", this, {
        itemName: e
      });
    this._sourceDefinitions[e].push(Object.assign({}, i)), this._clearCache();
  }
  /**
  * Returns data of all registered items.
  *
  * This method should normally be used for reflection purposes (e.g. defining a clone of a certain element,
  * checking a list of all block elements, etc).
  * Use specific methods (such as {@link #checkChild `checkChild()`} or {@link #isLimit `isLimit()`})
  * in other cases.
  */
  getDefinitions() {
    return this._compiledDefinitions || this._compile(), this._compiledDefinitions;
  }
  /**
  * Returns a definition of the given item or `undefined` if an item is not registered.
  *
  * This method should normally be used for reflection purposes (e.g. defining a clone of a certain element,
  * checking a list of all block elements, etc).
  * Use specific methods (such as {@link #checkChild `checkChild()`} or {@link #isLimit `isLimit()`})
  * in other cases.
  */
  getDefinition(e) {
    let i;
    return typeof e == "string" ? i = e : "is" in e && (e.is("$text") || e.is("$textProxy")) ? i = "$text" : i = e.name, this.getDefinitions()[i];
  }
  /**
  * Returns `true` if the given item is registered in the schema.
  *
  * ```ts
  * schema.isRegistered( 'paragraph' ); // -> true
  * schema.isRegistered( editor.model.document.getRoot() ); // -> true
  * schema.isRegistered( 'foo' ); // -> false
  * ```
  */
  isRegistered(e) {
    return !!this.getDefinition(e);
  }
  /**
  * Returns `true` if the given item is defined to be
  * a block by the {@link module:engine/model/schema~SchemaItemDefinition}'s `isBlock` property.
  *
  * ```ts
  * schema.isBlock( 'paragraph' ); // -> true
  * schema.isBlock( '$root' ); // -> false
  *
  * const paragraphElement = writer.createElement( 'paragraph' );
  * schema.isBlock( paragraphElement ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#block-elements Block elements} section of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isBlock(e) {
    const i = this.getDefinition(e);
    return !!(i && i.isBlock);
  }
  /**
  * Returns `true` if the given item should be treated as a limit element.
  *
  * It considers an item to be a limit element if its
  * {@link module:engine/model/schema~SchemaItemDefinition}'s
  * {@link module:engine/model/schema~SchemaItemDefinition#isLimit `isLimit`} or
  * {@link module:engine/model/schema~SchemaItemDefinition#isObject `isObject`} property
  * was set to `true`.
  *
  * ```ts
  * schema.isLimit( 'paragraph' ); // -> false
  * schema.isLimit( '$root' ); // -> true
  * schema.isLimit( editor.model.document.getRoot() ); // -> true
  * schema.isLimit( 'imageBlock' ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#limit-elements Limit elements} section of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isLimit(e) {
    const i = this.getDefinition(e);
    return i ? !!(i.isLimit || i.isObject) : !1;
  }
  /**
  * Returns `true` if the given item should be treated as an object element.
  *
  * It considers an item to be an object element if its
  * {@link module:engine/model/schema~SchemaItemDefinition}'s
  * {@link module:engine/model/schema~SchemaItemDefinition#isObject `isObject`} property
  * was set to `true`.
  *
  * ```ts
  * schema.isObject( 'paragraph' ); // -> false
  * schema.isObject( 'imageBlock' ); // -> true
  *
  * const imageElement = writer.createElement( 'imageBlock' );
  * schema.isObject( imageElement ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#object-elements Object elements} section of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isObject(e) {
    const i = this.getDefinition(e);
    return i ? !!(i.isObject || i.isLimit && i.isSelectable && i.isContent) : !1;
  }
  /**
  * Returns `true` if the given item is defined to be
  * an inline element by the {@link module:engine/model/schema~SchemaItemDefinition}'s `isInline` property.
  *
  * ```ts
  * schema.isInline( 'paragraph' ); // -> false
  * schema.isInline( 'softBreak' ); // -> true
  *
  * const text = writer.createText( 'foo' );
  * schema.isInline( text ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#inline-elements Inline elements} section of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isInline(e) {
    const i = this.getDefinition(e);
    return !!(i && i.isInline);
  }
  /**
  * Returns `true` if the given item is defined to be
  * a selectable element by the {@link module:engine/model/schema~SchemaItemDefinition}'s `isSelectable` property.
  *
  * ```ts
  * schema.isSelectable( 'paragraph' ); // -> false
  * schema.isSelectable( 'heading1' ); // -> false
  * schema.isSelectable( 'imageBlock' ); // -> true
  * schema.isSelectable( 'tableCell' ); // -> true
  *
  * const text = writer.createText( 'foo' );
  * schema.isSelectable( text ); // -> false
  * ```
  *
  * See the {@glink framework/deep-dive/schema#selectable-elements Selectable elements section} of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isSelectable(e) {
    const i = this.getDefinition(e);
    return i ? !!(i.isSelectable || i.isObject) : !1;
  }
  /**
  * Returns `true` if the given item is defined to be
  * a content by the {@link module:engine/model/schema~SchemaItemDefinition}'s `isContent` property.
  *
  * ```ts
  * schema.isContent( 'paragraph' ); // -> false
  * schema.isContent( 'heading1' ); // -> false
  * schema.isContent( 'imageBlock' ); // -> true
  * schema.isContent( 'horizontalLine' ); // -> true
  *
  * const text = writer.createText( 'foo' );
  * schema.isContent( text ); // -> true
  * ```
  *
  * See the {@glink framework/deep-dive/schema#content-elements Content elements section} of
  * the {@glink framework/deep-dive/schema Schema deep-dive} guide for more details.
  */
  isContent(e) {
    const i = this.getDefinition(e);
    return i ? !!(i.isContent || i.isObject) : !1;
  }
  /**
  * Checks whether the given node can be a child of the given context.
  *
  * ```ts
  * schema.checkChild( model.document.getRoot(), paragraph ); // -> false
  *
  * schema.register( 'paragraph', {
  * 	allowIn: '$root'
  * } );
  *
  * schema.checkChild( model.document.getRoot(), paragraph ); // -> true
  * ```
  *
  * Both {@link module:engine/model/schema~Schema#addChildCheck callback checks} and declarative rules (added when
  * {@link module:engine/model/schema~Schema#register registering} and {@link module:engine/model/schema~Schema#extend extending} items)
  * are evaluated when this method is called.
  *
  * Note that callback checks have bigger priority than declarative rules checks and may overwrite them.
  *
  * Note that when verifying whether the given node can be a child of the given context, the schema also verifies the entire
  * context &ndash; from its root to its last element. Therefore, it is possible for `checkChild()` to return `false` even though
  * the `context` last element can contain the checked child. It happens if one of the `context` elements does not allow its child.
  * When `context` is verified, {@link module:engine/model/schema~Schema#addChildCheck custom checks} are considered as well.
  *
  * @fires checkChild
  * @param context The context in which the child will be checked.
  * @param def The child to check.
  */
  checkChild(e, i) {
    return i ? this._checkContextMatch(e, i) : !1;
  }
  /**
  * Checks whether the given attribute can be applied in the given context (on the last item of the context).
  *
  * ```ts
  * schema.checkAttribute( textNode, 'bold' ); // -> false
  *
  * schema.extend( '$text', {
  * 	allowAttributes: 'bold'
  * } );
  *
  * schema.checkAttribute( textNode, 'bold' ); // -> true
  * ```
  *
  * Both {@link module:engine/model/schema~Schema#addAttributeCheck callback checks} and declarative rules (added when
  * {@link module:engine/model/schema~Schema#register registering} and {@link module:engine/model/schema~Schema#extend extending} items)
  * are evaluated when this method is called.
  *
  * Note that callback checks have bigger priority than declarative rules checks and may overwrite them.
  *
  * @fires checkAttribute
  * @param context The context in which the attribute will be checked.
  * @param attributeName Name of attribute to check in the given context.
  */
  checkAttribute(e, i) {
    const s = this.getDefinition(e.last);
    if (!s)
      return !1;
    const r = this._evaluateAttributeChecks(e, i);
    return r !== void 0 ? r : s.allowAttributes.includes(i);
  }
  /**
  * Checks whether the given element (`elementToMerge`) can be merged with the specified base element (`positionOrBaseElement`).
  *
  * In other words &ndash; both elements are not a limit elements and whether `elementToMerge`'s children
  * {@link #checkChild are allowed} in the `positionOrBaseElement`.
  *
  * This check ensures that elements merged with {@link module:engine/model/writer~Writer#merge `Writer#merge()`}
  * will be valid.
  *
  * Instead of elements, you can pass the instance of the {@link module:engine/model/position~Position} class as the
  * `positionOrBaseElement`. It means that the elements before and after the position will be checked whether they can be merged.
  *
  * @param positionOrBaseElement The position or base element to which the `elementToMerge` will be merged.
  * @param elementToMerge The element to merge. Required if `positionOrBaseElement` is an element.
  */
  checkMerge(e, i) {
    if (e instanceof G) {
      const s = e.nodeBefore, r = e.nodeAfter;
      if (!(s instanceof et))
        throw new S("schema-check-merge-no-element-before", this);
      if (!(r instanceof et))
        throw new S("schema-check-merge-no-element-after", this);
      return this.checkMerge(s, r);
    }
    if (this.isLimit(e) || this.isLimit(i))
      return !1;
    for (const s of i.getChildren())
      if (!this.checkChild(e, s))
        return !1;
    return !0;
  }
  /**
  * Allows registering a callback to the {@link #checkChild} method calls.
  *
  * Callbacks allow you to implement rules which are not otherwise possible to achieve
  * by using the declarative API of {@link module:engine/model/schema~SchemaItemDefinition}.
  *
  * Note that callback checks have bigger priority than declarative rules checks and may overwrite them.
  *
  * For example, by using this method you can disallow elements in specific contexts:
  *
  * ```ts
  * // Disallow `heading1` inside a `blockQuote` that is inside a table.
  * schema.addChildCheck( ( context, childDefinition ) => {
  * 	if ( context.endsWith( 'tableCell blockQuote' ) ) {
  * 		return false;
  * 	}
  * }, 'heading1' );
  * ```
  *
  * You can skip the optional `itemName` parameter to evaluate the callback for every `checkChild()` call.
  *
  * ```ts
  * // Inside specific custom element, allow only children, which allows for a specific attribute.
  * schema.addChildCheck( ( context, childDefinition ) => {
  * 	if ( context.endsWith( 'myElement' ) ) {
  * 		return childDefinition.allowAttributes.includes( 'myAttribute' );
  * 	}
  * } );
  * ```
  *
  * Please note that the generic callbacks may affect the editor performance and should be avoided if possible.
  *
  * When one of the callbacks makes a decision (returns `true` or `false`) the processing is finished and other callbacks are not fired.
  * Callbacks are fired in the order they were added, however generic callbacks are fired before callbacks added for a specified item.
  *
  * You can also use `checkChild` event, if you need even better control. The result from the example above could also be
  * achieved with following event callback:
  *
  * ```ts
  * schema.on( 'checkChild', ( evt, args ) => {
  * 	const context = args[ 0 ];
  * 	const childDefinition = args[ 1 ];
  *
  * 	if ( context.endsWith( 'myElement' ) ) {
  * 		// Prevent next listeners from being called.
  * 		evt.stop();
  * 		// Set the `checkChild()` return value.
  * 		evt.return = childDefinition.allowAttributes.includes( 'myAttribute' );
  * 	}
  * }, { priority: 'high' } );
  * ```
  *
  * Note that the callback checks and declarative rules checks are processed on `normal` priority.
  *
  * Adding callbacks this way can also negatively impact editor performance.
  *
  * @param callback The callback to be called. It is called with two parameters:
  * {@link module:engine/model/schema~SchemaContext} (context) instance and
  * {@link module:engine/model/schema~SchemaCompiledItemDefinition} (definition). The callback may return `true/false` to override
  * `checkChild()`'s return value. If it does not return a boolean value, the default algorithm (or other callbacks) will define
  * `checkChild()`'s return value.
  * @param itemName Name of the schema item for which the callback is registered. If specified, the callback will be run only for
  * `checkChild()` calls which `def` parameter matches the `itemName`. Otherwise, the callback will run for every `checkChild` call.
  */
  addChildCheck(e, i) {
    const s = i !== void 0 ? i : this._genericCheckSymbol, r = this._customChildChecks.get(s) || [];
    r.push(e), this._customChildChecks.set(s, r);
  }
  /**
  * Allows registering a callback to the {@link #checkAttribute} method calls.
  *
  * Callbacks allow you to implement rules which are not otherwise possible to achieve
  * by using the declarative API of {@link module:engine/model/schema~SchemaItemDefinition}.
  *
  * Note that callback checks have bigger priority than declarative rules checks and may overwrite them.
  *
  * For example, by using this method you can disallow setting attributes on nodes in specific contexts:
  *
  * ```ts
  * // Disallow setting `bold` on text inside `heading1` element:
  * schema.addAttributeCheck( context => {
  * 	if ( context.endsWith( 'heading1 $text' ) ) {
  * 		return false;
  * 	}
  * }, 'bold' );
  * ```
  *
  * You can skip the optional `attributeName` parameter to evaluate the callback for every `checkAttribute()` call.
  *
  * ```ts
  * // Disallow formatting attributes on text inside custom `myTitle` element:
  * schema.addAttributeCheck( ( context, attributeName ) => {
  * 	if ( context.endsWith( 'myTitle $text' ) && schema.getAttributeProperties( attributeName ).isFormatting ) {
  * 		return false;
  * 	}
  * } );
  * ```
  *
  * Please note that the generic callbacks may affect the editor performance and should be avoided if possible.
  *
  * When one of the callbacks makes a decision (returns `true` or `false`) the processing is finished and other callbacks are not fired.
  * Callbacks are fired in the order they were added, however generic callbacks are fired before callbacks added for a specified item.
  *
  * You can also use {@link #event:checkAttribute} event, if you need even better control. The result from the example above could also
  * be achieved with following event callback:
  *
  * ```ts
  * schema.on( 'checkAttribute', ( evt, args ) => {
  * 	const context = args[ 0 ];
  * 	const attributeName = args[ 1 ];
  *
  * 	if ( context.endsWith( 'myTitle $text' ) && schema.getAttributeProperties( attributeName ).isFormatting ) {
  * 		// Prevent next listeners from being called.
  * 		evt.stop();
  * 		// Set the `checkAttribute()` return value.
  * 		evt.return = false;
  * 	}
  * }, { priority: 'high' } );
  * ```
  *
  * Note that the callback checks and declarative rules checks are processed on `normal` priority.
  *
  * Adding callbacks this way can also negatively impact editor performance.
  *
  * @param callback The callback to be called. It is called with two parameters:
  * {@link module:engine/model/schema~SchemaContext `context`} and attribute name. The callback may return `true` or `false`, to
  * override `checkAttribute()`'s return value. If it does not return a boolean value, the default algorithm (or other callbacks)
  * will define `checkAttribute()`'s return value.
  * @param attributeName Name of the attribute for which the callback is registered. If specified, the callback will be run only for
  * `checkAttribute()` calls with matching `attributeName`. Otherwise, the callback will run for every `checkAttribute()` call.
  */
  addAttributeCheck(e, i) {
    const s = i !== void 0 ? i : this._genericCheckSymbol, r = this._customAttributeChecks.get(s) || [];
    r.push(e), this._customAttributeChecks.set(s, r);
  }
  /**
  * This method allows assigning additional metadata to the model attributes. For example,
  * {@link module:engine/model/schema~AttributeProperties `AttributeProperties#isFormatting` property} is
  * used to mark formatting attributes (like `bold` or `italic`).
  *
  * ```ts
  * // Mark bold as a formatting attribute.
  * schema.setAttributeProperties( 'bold', {
  * 	isFormatting: true
  * } );
  *
  * // Override code not to be considered a formatting markup.
  * schema.setAttributeProperties( 'code', {
  * 	isFormatting: false
  * } );
  * ```
  *
  * Properties are not limited to members defined in the
  * {@link module:engine/model/schema~AttributeProperties `AttributeProperties` type} and you can also use custom properties:
  *
  * ```ts
  * schema.setAttributeProperties( 'blockQuote', {
  * 	customProperty: 'value'
  * } );
  * ```
  *
  * Subsequent calls with the same attribute will extend its custom properties:
  *
  * ```ts
  * schema.setAttributeProperties( 'blockQuote', {
  * 	one: 1
  * } );
  *
  * schema.setAttributeProperties( 'blockQuote', {
  * 	two: 2
  * } );
  *
  * console.log( schema.getAttributeProperties( 'blockQuote' ) );
  * // Logs: { one: 1, two: 2 }
  * ```
  *
  * @param attributeName A name of the attribute to receive the properties.
  * @param properties A dictionary of properties.
  */
  setAttributeProperties(e, i) {
    this._attributeProperties[e] = Object.assign(this.getAttributeProperties(e), i);
  }
  /**
  * Returns properties associated with a given model attribute. See {@link #setAttributeProperties `setAttributeProperties()`}.
  *
  * @param attributeName A name of the attribute.
  */
  getAttributeProperties(e) {
    return this._attributeProperties[e] || /* @__PURE__ */ Object.create(null);
  }
  /**
  * Returns the lowest {@link module:engine/model/schema~Schema#isLimit limit element} containing the entire
  * selection/range/position or the root otherwise.
  *
  * @param selectionOrRangeOrPosition The selection/range/position to check.
  * @returns The lowest limit element containing the entire `selectionOrRangeOrPosition`.
  */
  getLimitElement(e) {
    let i;
    for (e instanceof G ? i = e.parent : i = (e instanceof F ? [
      e
    ] : Array.from(e.getRanges())).reduce((r, o) => {
      const a = o.getCommonAncestor();
      return r ? r.getCommonAncestor(a, {
        includeSelf: !0
      }) : a;
    }, null); !this.isLimit(i) && i.parent; )
      i = i.parent;
    return i;
  }
  /**
  * Checks whether the attribute is allowed in selection:
  *
  * * if the selection is not collapsed, then checks if the attribute is allowed on any of nodes in that range,
  * * if the selection is collapsed, then checks if on the selection position there's a text with the
  * specified attribute allowed.
  *
  * @param selection Selection which will be checked.
  * @param attribute The name of the attribute to check.
  */
  checkAttributeInSelection(e, i) {
    if (e.isCollapsed) {
      const r = [
        ...e.getFirstPosition().getAncestors(),
        new lt("", e.getAttributes())
      ];
      return this.checkAttribute(r, i);
    } else {
      const s = e.getRanges();
      for (const r of s)
        for (const o of r)
          if (this.checkAttribute(o.item, i))
            return !0;
    }
    return !1;
  }
  /**
  * Transforms the given set of ranges into a set of ranges where the given attribute is allowed (and can be applied).
  *
  * @param ranges Ranges to be validated.
  * @param attribute The name of the attribute to check.
  * @returns Ranges in which the attribute is allowed.
  */
  *getValidRanges(e, i) {
    e = GR(e);
    for (const s of e)
      yield* this._getValidRangesForRange(s, i);
  }
  /**
  * Basing on given `position`, finds and returns a {@link module:engine/model/range~Range range} which is
  * nearest to that `position` and is a correct range for selection.
  *
  * The correct selection range might be collapsed when it is located in a position where the text node can be placed.
  * Non-collapsed range is returned when selection can be placed around element marked as an "object" in
  * the {@link module:engine/model/schema~Schema schema}.
  *
  * Direction of searching for the nearest correct selection range can be specified as:
  *
  * * `both` - searching will be performed in both ways,
  * * `forward` - searching will be performed only forward,
  * * `backward` - searching will be performed only backward.
  *
  * When valid selection range cannot be found, `null` is returned.
  *
  * @param position Reference position where new selection range should be looked for.
  * @param direction Search direction.
  * @returns Nearest selection range or `null` if one cannot be found.
  */
  getNearestSelectionRange(e, i = "both") {
    if (e.root.rootName == "$graveyard")
      return null;
    if (this.checkChild(e, "$text"))
      return new F(e);
    let s, r;
    const o = e.getAncestors().reverse().find((a) => this.isLimit(a)) || e.root;
    (i == "both" || i == "backward") && (s = new Fn({
      boundaries: F._createIn(o),
      startPosition: e,
      direction: "backward"
    })), (i == "both" || i == "forward") && (r = new Fn({
      boundaries: F._createIn(o),
      startPosition: e
    }));
    for (const a of qR(s, r)) {
      const l = a.walker == s ? "elementEnd" : "elementStart", c = a.value;
      if (c.type == l && this.isObject(c.item))
        return F._createOn(c.item);
      if (this.checkChild(c.nextPosition, "$text"))
        return new F(c.nextPosition);
    }
    return null;
  }
  /**
  * Tries to find position ancestors that allow to insert a given node.
  * It starts searching from the given position and goes node by node to the top of the model tree
  * as long as a {@link module:engine/model/schema~Schema#isLimit limit element}, an
  * {@link module:engine/model/schema~Schema#isObject object element} or a topmost ancestor is not reached.
  *
  * @param position The position that the search will start from.
  * @param node The node for which an allowed parent should be found or its name.
  * @returns Allowed parent or null if nothing was found.
  */
  findAllowedParent(e, i) {
    let s = e.parent;
    for (; s; ) {
      if (this.checkChild(s, i))
        return s;
      if (this.isLimit(s))
        return null;
      s = s.parent;
    }
    return null;
  }
  /**
  * Sets attributes allowed by the schema on a given node.
  *
  * @param node A node to set attributes on.
  * @param attributes Attributes keys and values.
  * @param writer An instance of the model writer.
  */
  setAllowedAttributes(e, i, s) {
    const r = s.model;
    for (const [o, a] of Object.entries(i))
      r.schema.checkAttribute(e, o) && s.setAttribute(o, a, e);
  }
  /**
  * Removes attributes disallowed by the schema.
  *
  * @param nodes Nodes that will be filtered.
  */
  removeDisallowedAttributes(e, i) {
    for (const s of e)
      if (s.is("$text"))
        Fp(this, s, i);
      else {
        const o = F._createIn(s).getPositions();
        for (const a of o) {
          const l = a.nodeBefore || a.parent;
          Fp(this, l, i);
        }
      }
  }
  /**
  * Gets attributes of a node that have a given property.
  *
  * @param node Node to get attributes from.
  * @param propertyName Name of the property that attribute must have to return it.
  * @param propertyValue Desired value of the property that we want to check.
  * When `undefined` attributes will be returned if they have set a given property no matter what the value is. If specified it will
  * return attributes which given property's value is equal to this parameter.
  * @returns Object with attributes' names as key and attributes' values as value.
  */
  getAttributesWithProperty(e, i, s) {
    const r = {};
    for (const [o, a] of e.getAttributes()) {
      const l = this.getAttributeProperties(o);
      l[i] !== void 0 && (s === void 0 || s === l[i]) && (r[o] = a);
    }
    return r;
  }
  /**
  * Creates an instance of the schema context.
  */
  createContext(e) {
    return new hs(e);
  }
  _clearCache() {
    this._compiledDefinitions = null;
  }
  _compile() {
    const e = {}, i = this._sourceDefinitions, s = Object.keys(i);
    for (const o of s)
      e[o] = OR(i[o], o);
    const r = Object.values(e);
    for (const o of r)
      VR(e, o), LR(e, o), BR(e, o), MR(e, o);
    for (const o of r)
      NR(e, o);
    for (const o of r)
      FR(e, o);
    for (const o of r)
      DR(e, o);
    for (const o of r)
      zR(e, o);
    for (const o of r)
      HR(e, o);
    this._compiledDefinitions = $R(e);
  }
  _checkContextMatch(e, i) {
    const s = e.last;
    let r = this._evaluateChildChecks(e, i);
    if (r = r !== void 0 ? r : i.allowIn.includes(s.name), !r)
      return !1;
    const o = this.getDefinition(s), a = e.trimLast();
    return o ? a.length == 0 ? !0 : this._checkContextMatch(a, o) : !1;
  }
  /**
  * Calls child check callbacks to decide whether `def` is allowed in `context`. It uses both generic and specific (defined for `def`
  * item) callbacks. If neither callback makes a decision, `undefined` is returned.
  *
  * Note that the first callback that makes a decision "wins", i.e., if any callback returns `true` or `false`, then the processing
  * is over and that result is returned.
  */
  _evaluateChildChecks(e, i) {
    const s = this._customChildChecks.get(this._genericCheckSymbol) || [], r = this._customChildChecks.get(i.name) || [];
    for (const o of [
      ...s,
      ...r
    ]) {
      const a = o(e, i);
      if (a !== void 0)
        return a;
    }
  }
  /**
  * Calls attribute check callbacks to decide whether `attributeName` can be set on the last element of `context`. It uses both
  * generic and specific (defined for `attributeName`) callbacks. If neither callback makes a decision, `undefined` is returned.
  *
  * Note that the first callback that makes a decision "wins", i.e., if any callback returns `true` or `false`, then the processing
  * is over and that result is returned.
  */
  _evaluateAttributeChecks(e, i) {
    const s = this._customAttributeChecks.get(this._genericCheckSymbol) || [], r = this._customAttributeChecks.get(i) || [];
    for (const o of [
      ...s,
      ...r
    ]) {
      const a = o(e, i);
      if (a !== void 0)
        return a;
    }
  }
  /**
  * Takes a flat range and an attribute name. Traverses the range recursively and deeply to find and return all ranges
  * inside the given range on which the attribute can be applied.
  *
  * This is a helper function for {@link ~Schema#getValidRanges}.
  *
  * @param range The range to process.
  * @param attribute The name of the attribute to check.
  * @returns Ranges in which the attribute is allowed.
  */
  *_getValidRangesForRange(e, i) {
    let s = e.start, r = e.start;
    for (const o of e.getItems({
      shallow: !0
    }))
      o.is("element") && (yield* this._getValidRangesForRange(F._createIn(o), i)), this.checkAttribute(o, i) || (s.isEqual(r) || (yield new F(s, r)), s = G._createAfter(o)), r = G._createAfter(o);
    s.isEqual(r) || (yield new F(s, r));
  }
  /**
  * Returns a model range which is optimal (in terms of UX) for inserting a widget block.
  *
  * For instance, if a selection is in the middle of a paragraph, the collapsed range before this paragraph
  * will be returned so that it is not split. If the selection is at the end of a paragraph,
  * the collapsed range after this paragraph will be returned.
  *
  * Note: If the selection is placed in an empty block, the range in that block will be returned. If that range
  * is then passed to {@link module:engine/model/model~Model#insertContent}, the block will be fully replaced
  * by the inserted widget block.
  *
  * @internal
  * @param selection The selection based on which the insertion position should be calculated.
  * @param place The place where to look for optimal insertion range.
  * The `auto` value will determine itself the best position for insertion.
  * The `before` value will try to find a position before selection.
  * The `after` value will try to find a position after selection.
  * @returns The optimal range.
  */
  findOptimalInsertionRange(e, i) {
    const s = e.getSelectedElement();
    if (s && this.isObject(s) && !this.isInline(s))
      return i == "before" || i == "after" ? new F(G._createAt(s, i)) : F._createOn(s);
    const r = Xe(e.getSelectedBlocks());
    if (!r)
      return new F(e.focus);
    if (r.isEmpty)
      return new F(G._createAt(r, 0));
    const o = G._createAfter(r);
    return e.focus.isTouching(o) ? new F(o) : new F(G._createBefore(r));
  }
}
class hs {
  /**
  * Creates an instance of the context.
  */
  constructor(t) {
    f(this, "_items");
    if (t instanceof hs)
      return t;
    let e;
    typeof t == "string" ? e = [
      t
    ] : Array.isArray(t) ? e = t : e = t.getAncestors({
      includeSelf: !0
    }), this._items = e.map(WR);
  }
  /**
  * The number of items.
  */
  get length() {
    return this._items.length;
  }
  /**
  * The last item (the lowest node).
  */
  get last() {
    return this._items[this._items.length - 1];
  }
  /**
  * Iterable interface.
  *
  * Iterates over all context items.
  */
  [Symbol.iterator]() {
    return this._items[Symbol.iterator]();
  }
  /**
  * Returns a new schema context instance with an additional item.
  *
  * Item can be added as:
  *
  * ```ts
  * const context = new SchemaContext( [ '$root' ] );
  *
  * // An element.
  * const fooElement = writer.createElement( 'fooElement' );
  * const newContext = context.push( fooElement ); // [ '$root', 'fooElement' ]
  *
  * // A text node.
  * const text = writer.createText( 'foobar' );
  * const newContext = context.push( text ); // [ '$root', '$text' ]
  *
  * // A string (element name).
  * const newContext = context.push( 'barElement' ); // [ '$root', 'barElement' ]
  * ```
  *
  * **Note** {@link module:engine/model/node~Node} that is already in the model tree will be added as the only item
  * (without ancestors).
  *
  * @param item An item that will be added to the current context.
  * @returns A new schema context instance with an additional item.
  */
  push(t) {
    const e = new hs([
      t
    ]);
    return e._items = [
      ...this._items,
      ...e._items
    ], e;
  }
  /**
  * Returns a new schema context that is based on this context but has the last item removed.
  *
  * ```ts
  * const ctxParagraph = new SchemaContext( [ '$root', 'blockQuote', 'paragraph' ] );
  * const ctxBlockQuote = ctxParagraph.trimLast(); // Items in `ctxBlockQuote` are: `$root` an `blockQuote`.
  * const ctxRoot = ctxBlockQuote.trimLast(); // Items in `ctxRoot` are: `$root`.
  * ```
  *
  * @returns A new reduced schema context instance.
  */
  trimLast() {
    const t = new hs([]);
    return t._items = this._items.slice(0, -1), t;
  }
  /**
  * Gets an item on the given index.
  */
  getItem(t) {
    return this._items[t];
  }
  /**
  * Returns the names of items.
  */
  *getNames() {
    yield* this._items.map((t) => t.name);
  }
  /**
  * Checks whether the context ends with the given nodes.
  *
  * ```ts
  * const ctx = new SchemaContext( [ rootElement, paragraphElement, textNode ] );
  *
  * ctx.endsWith( '$text' ); // -> true
  * ctx.endsWith( 'paragraph $text' ); // -> true
  * ctx.endsWith( '$root' ); // -> false
  * ctx.endsWith( 'paragraph' ); // -> false
  * ```
  */
  endsWith(t) {
    return Array.from(this.getNames()).join(" ").endsWith(t);
  }
  /**
  * Checks whether the context starts with the given nodes.
  *
  * ```ts
  * const ctx = new SchemaContext( [ rootElement, paragraphElement, textNode ] );
  *
  * ctx.endsWith( '$root' ); // -> true
  * ctx.endsWith( '$root paragraph' ); // -> true
  * ctx.endsWith( '$text' ); // -> false
  * ctx.endsWith( 'paragraph' ); // -> false
  * ```
  */
  startsWith(t) {
    return Array.from(this.getNames()).join(" ").startsWith(t);
  }
}
function OR(n, t) {
  const e = {
    name: t,
    allowIn: /* @__PURE__ */ new Set(),
    allowChildren: /* @__PURE__ */ new Set(),
    disallowIn: /* @__PURE__ */ new Set(),
    disallowChildren: /* @__PURE__ */ new Set(),
    allowContentOf: /* @__PURE__ */ new Set(),
    allowWhere: /* @__PURE__ */ new Set(),
    allowAttributes: /* @__PURE__ */ new Set(),
    disallowAttributes: /* @__PURE__ */ new Set(),
    allowAttributesOf: /* @__PURE__ */ new Set(),
    inheritTypesFrom: /* @__PURE__ */ new Set()
  };
  return jR(n, e), on(n, e, "allowIn"), on(n, e, "allowChildren"), on(n, e, "disallowIn"), on(n, e, "disallowChildren"), on(n, e, "allowContentOf"), on(n, e, "allowWhere"), on(n, e, "allowAttributes"), on(n, e, "disallowAttributes"), on(n, e, "allowAttributesOf"), on(n, e, "inheritTypesFrom"), UR(n, e), e;
}
function VR(n, t) {
  for (const e of t.allowIn) {
    const i = n[e];
    i ? i.allowChildren.add(t.name) : t.allowIn.delete(e);
  }
}
function LR(n, t) {
  for (const e of t.allowChildren) {
    const i = n[e];
    i ? i.allowIn.add(t.name) : t.allowChildren.delete(e);
  }
}
function BR(n, t) {
  for (const e of t.disallowIn) {
    const i = n[e];
    i ? i.disallowChildren.add(t.name) : t.disallowIn.delete(e);
  }
}
function MR(n, t) {
  for (const e of t.disallowChildren) {
    const i = n[e];
    i ? i.disallowIn.add(t.name) : t.disallowChildren.delete(e);
  }
}
function NR(n, t) {
  for (const e of t.disallowChildren)
    t.allowChildren.delete(e);
  for (const e of t.disallowIn)
    t.allowIn.delete(e);
  for (const e of t.disallowAttributes)
    t.allowAttributes.delete(e);
}
function FR(n, t) {
  for (const e of t.allowContentOf) {
    const i = n[e];
    i && (i.disallowChildren.forEach((s) => {
      t.allowChildren.has(s) || (t.disallowChildren.add(s), n[s].disallowIn.add(t.name));
    }), i.allowChildren.forEach((s) => {
      t.disallowChildren.has(s) || (t.allowChildren.add(s), n[s].allowIn.add(t.name));
    }));
  }
}
function DR(n, t) {
  for (const e of t.allowWhere) {
    const i = n[e];
    i && (i.disallowIn.forEach((s) => {
      t.allowIn.has(s) || (t.disallowIn.add(s), n[s].disallowChildren.add(t.name));
    }), i.allowIn.forEach((s) => {
      t.disallowIn.has(s) || (t.allowIn.add(s), n[s].allowChildren.add(t.name));
    }));
  }
}
function $R(n) {
  const t = {};
  for (const e of Object.values(n))
    t[e.name] = {
      name: e.name,
      // `is*` properties may not be set - convert `undefined` to `false`.
      isBlock: !!e.isBlock,
      isContent: !!e.isContent,
      isInline: !!e.isInline,
      isLimit: !!e.isLimit,
      isObject: !!e.isObject,
      isSelectable: !!e.isSelectable,
      // Filter out non-existing items.
      allowIn: Array.from(e.allowIn).filter((i) => !!n[i]),
      allowChildren: Array.from(e.allowChildren).filter((i) => !!n[i]),
      allowAttributes: Array.from(e.allowAttributes)
    };
  return t;
}
function zR(n, t) {
  for (const e of t.allowAttributesOf) {
    const i = n[e];
    if (!i)
      return;
    i.allowAttributes.forEach((s) => {
      t.disallowAttributes.has(s) || t.allowAttributes.add(s);
    });
  }
}
function HR(n, t) {
  for (const e of t.inheritTypesFrom) {
    const i = n[e];
    if (i) {
      const s = Object.keys(i).filter((r) => r.startsWith("is"));
      for (const r of s)
        r in t || (t[r] = i[r]);
    }
  }
}
function jR(n, t) {
  for (const e of n) {
    const i = Object.keys(e).filter((s) => s.startsWith("is"));
    for (const s of i)
      t[s] = !!e[s];
  }
}
function on(n, t, e) {
  for (const i of n) {
    let s = i[e];
    typeof s == "string" && (s = [
      s
    ]), Array.isArray(s) && s.forEach((r) => t[e].add(r));
  }
}
function UR(n, t) {
  for (const e of n) {
    const i = e.inheritAllFrom;
    i && (t.allowContentOf.add(i), t.allowWhere.add(i), t.allowAttributesOf.add(i), t.inheritTypesFrom.add(i));
  }
}
function WR(n) {
  return typeof n == "string" || n.is("documentFragment") ? {
    name: typeof n == "string" ? n : "$documentFragment",
    *getAttributeKeys() {
    },
    getAttribute() {
    }
  } : {
    // '$text' means text nodes and text proxies.
    name: n.is("element") ? n.name : "$text",
    *getAttributeKeys() {
      yield* n.getAttributeKeys();
    },
    getAttribute(t) {
      return n.getAttribute(t);
    }
  };
}
function* qR(n, t) {
  let e = !1;
  for (; !e; ) {
    if (e = !0, n) {
      const i = n.next();
      i.done || (e = !1, yield {
        walker: n,
        value: i.value
      });
    }
    if (t) {
      const i = t.next();
      i.done || (e = !1, yield {
        walker: t,
        value: i.value
      });
    }
  }
}
function* GR(n) {
  for (const t of n)
    yield* t.getMinimalFlatRanges();
}
function Fp(n, t, e) {
  for (const i of t.getAttributeKeys())
    n.checkAttribute(t, i) || e.removeAttribute(i, t);
}
class KR extends (/* @__PURE__ */ Je()) {
  /**
  * Creates an upcast dispatcher that operates using the passed API.
  *
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi
  * @param conversionApi Additional properties for an interface that will be passed to events fired
  * by the upcast dispatcher.
  */
  constructor(e) {
    super();
    /**
    * An interface passed by the dispatcher to the event callbacks.
    */
    f(this, "conversionApi");
    /**
    * The list of elements that were created during splitting.
    *
    * After the conversion process, the list is cleared.
    */
    f(this, "_splitParts", /* @__PURE__ */ new Map());
    /**
    * The list of cursor parent elements that were created during splitting.
    *
    * After the conversion process the list is cleared.
    */
    f(this, "_cursorParents", /* @__PURE__ */ new Map());
    /**
    * The position in the temporary structure where the converted content is inserted. The structure reflects the context of
    * the target position where the content will be inserted. This property is built based on the context parameter of the
    * convert method.
    */
    f(this, "_modelCursor", null);
    /**
    * The list of elements that were created during the splitting but should not get removed on conversion end even if they are empty.
    *
    * The list is cleared after the conversion process.
    */
    f(this, "_emptyElementsToKeep", /* @__PURE__ */ new Set());
    this.conversionApi = {
      ...e,
      consumable: null,
      writer: null,
      store: null,
      convertItem: (i, s) => this._convertItem(i, s),
      convertChildren: (i, s) => this._convertChildren(i, s),
      safeInsert: (i, s) => this._safeInsert(i, s),
      updateConversionResult: (i, s) => this._updateConversionResult(i, s),
      // Advanced API - use only if custom position handling is needed.
      splitToAllowedParent: (i, s) => this._splitToAllowedParent(i, s),
      getSplitParts: (i) => this._getSplitParts(i),
      keepEmptyElement: (i) => this._keepEmptyElement(i)
    };
  }
  /**
  * Starts the conversion process. The entry point for the conversion.
  *
  * @fires element
  * @fires text
  * @fires documentFragment
  * @param viewElement The part of the view to be converted.
  * @param writer An instance of the model writer.
  * @param context Elements will be converted according to this context.
  * @returns Model data that is the result of the conversion process
  * wrapped in `DocumentFragment`. Converted marker elements will be set as the document fragment's
  * {@link module:engine/model/documentfragment~DocumentFragment#markers static markers map}.
  */
  convert(e, i, s = [
    "$root"
  ]) {
    this.fire("viewCleanup", e), this._modelCursor = YR(s, i), this.conversionApi.writer = i, this.conversionApi.consumable = Jl.createFrom(e), this.conversionApi.store = {};
    const { modelRange: r } = this._convertItem(e, this._modelCursor), o = i.createDocumentFragment();
    if (r) {
      this._removeEmptyElements();
      const a = this._modelCursor.parent, l = a._removeChildren(0, a.childCount);
      o._insertChild(0, l), o.markers = ZR(o, i);
    }
    return this._modelCursor = null, this._splitParts.clear(), this._cursorParents.clear(), this._emptyElementsToKeep.clear(), this.conversionApi.writer = null, this.conversionApi.store = null, o;
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#convertItem
  */
  _convertItem(e, i) {
    const s = {
      viewItem: e,
      modelCursor: i,
      modelRange: null
    };
    if (e.is("element") ? this.fire(`element:${e.name}`, s, this.conversionApi) : e.is("$text") ? this.fire("text", s, this.conversionApi) : this.fire("documentFragment", s, this.conversionApi), s.modelRange && !(s.modelRange instanceof F))
      throw new S("view-conversion-dispatcher-incorrect-result", this);
    return {
      modelRange: s.modelRange,
      modelCursor: s.modelCursor
    };
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#convertChildren
  */
  _convertChildren(e, i) {
    let s = i.is("position") ? i : G._createAt(i, 0);
    const r = new F(s);
    for (const o of Array.from(e.getChildren())) {
      const a = this._convertItem(o, s);
      a.modelRange instanceof F && (r.end = a.modelRange.end, s = a.modelCursor);
    }
    return {
      modelRange: r,
      modelCursor: s
    };
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#safeInsert
  */
  _safeInsert(e, i) {
    const s = this._splitToAllowedParent(e, i);
    return s ? (this.conversionApi.writer.insert(e, s.position), !0) : !1;
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#updateConversionResult
  */
  _updateConversionResult(e, i) {
    const s = this._getSplitParts(e), r = this.conversionApi.writer;
    i.modelRange || (i.modelRange = r.createRange(r.createPositionBefore(e), r.createPositionAfter(s[s.length - 1])));
    const o = this._cursorParents.get(e);
    o ? i.modelCursor = r.createPositionAt(o, 0) : i.modelCursor = i.modelRange.end;
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#splitToAllowedParent
  */
  _splitToAllowedParent(e, i) {
    const { schema: s, writer: r } = this.conversionApi;
    let o = s.findAllowedParent(i, e);
    if (o) {
      if (o === i.parent)
        return {
          position: i
        };
      this._modelCursor.parent.getAncestors().includes(o) && (o = null);
    }
    if (!o)
      return q_(i, e, s) ? {
        position: G_(i, r)
      } : null;
    const a = this.conversionApi.writer.split(i, o), l = [];
    for (const u of a.range.getWalker())
      if (u.type == "elementEnd")
        l.push(u.item);
      else {
        const d = l.pop(), h = u.item;
        this._registerSplitPair(d, h);
      }
    const c = a.range.end.parent;
    return this._cursorParents.set(e, c), {
      position: a.position,
      cursorParent: c
    };
  }
  /**
  * Registers that a `splitPart` element is a split part of the `originalPart` element.
  *
  * The data set by this method is used by {@link #_getSplitParts} and {@link #_removeEmptyElements}.
  */
  _registerSplitPair(e, i) {
    this._splitParts.has(e) || this._splitParts.set(e, [
      e
    ]);
    const s = this._splitParts.get(e);
    this._splitParts.set(i, s), s.push(i);
  }
  /**
  * @see module:engine/conversion/upcastdispatcher~UpcastConversionApi#getSplitParts
  */
  _getSplitParts(e) {
    let i;
    return this._splitParts.has(e) ? i = this._splitParts.get(e) : i = [
      e
    ], i;
  }
  /**
  * Mark an element that were created during the splitting to not get removed on conversion end even if it is empty.
  */
  _keepEmptyElement(e) {
    this._emptyElementsToKeep.add(e);
  }
  /**
  * Checks if there are any empty elements created while splitting and removes them.
  *
  * This method works recursively to re-check empty elements again after at least one element was removed in the initial call,
  * as some elements might have become empty after other empty elements were removed from them.
  */
  _removeEmptyElements() {
    const e = /* @__PURE__ */ new Map();
    for (const i of this._splitParts.keys())
      if (i.isEmpty && !this._emptyElementsToKeep.has(i)) {
        const s = e.get(i.parent) || [];
        s.push(i), this._splitParts.delete(i), e.set(i.parent, s);
      }
    for (const [i, s] of e)
      i._removeChildrenArray(s);
    e.size && this._removeEmptyElements();
  }
}
function ZR(n, t) {
  const e = /* @__PURE__ */ new Set(), i = /* @__PURE__ */ new Map(), s = F._createIn(n).getItems();
  for (const r of s)
    r.is("element", "$marker") && e.add(r);
  for (const r of e) {
    const o = r.getAttribute("data-name"), a = t.createPositionBefore(r);
    i.has(o) ? i.get(o).end = a.clone() : i.set(o, new F(a.clone())), t.remove(r);
  }
  return i;
}
function YR(n, t) {
  let e;
  for (const i of new hs(n)) {
    const s = {};
    for (const o of i.getAttributeKeys())
      s[o] = i.getAttribute(o);
    const r = t.createElement(i.name, s);
    e && t.insert(r, e), e = G._createAt(r, 0);
  }
  return e;
}
class JR {
  /**
  * Returns an HTML string created from the document fragment.
  */
  getHtml(t) {
    const i = ae.document.implementation.createHTMLDocument("").createElement("div");
    return i.appendChild(t), i.innerHTML;
  }
}
class Of {
  /**
  * Creates a new instance of the HTML data processor class.
  *
  * @param document The view document instance.
  */
  constructor(t) {
    /**
    * A DOM parser instance used to parse an HTML string to an HTML document.
    */
    f(this, "domParser");
    /**
    * A DOM converter used to convert DOM elements to view elements.
    */
    f(this, "domConverter");
    /**
    * A basic HTML writer instance used to convert DOM elements to an HTML string.
    */
    f(this, "htmlWriter");
    f(this, "skipComments", !0);
    this.domParser = new DOMParser(), this.domConverter = new Zc(t, {
      renderingMode: "data"
    }), this.htmlWriter = new JR();
  }
  /**
  * Converts a provided {@link module:engine/view/documentfragment~DocumentFragment document fragment}
  * to data format &ndash; in this case to an HTML string.
  *
  * @returns HTML string.
  */
  toData(t) {
    const e = this.domConverter.viewToDom(t);
    return this.htmlWriter.getHtml(e);
  }
  /**
  * Converts the provided HTML string to a view tree.
  *
  * @param data An HTML string.
  * @returns A converted view element.
  */
  toView(t) {
    const e = this._toDom(t);
    return this.domConverter.domToView(e, {
      skipComments: this.skipComments
    });
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} for view elements whose content should be treated as raw data
  * and not processed during the conversion from the DOM to the view elements.
  *
  * The raw data can be later accessed by a
  * {@link module:engine/view/element~Element#getCustomProperty custom property of a view element} called `"$rawContent"`.
  *
  * @param pattern Pattern matching all view elements whose content should be treated as raw data.
  */
  registerRawContentMatcher(t) {
    this.domConverter.registerRawContentMatcher(t);
  }
  /**
  * If the processor is set to use marked fillers, it will insert `&nbsp;` fillers wrapped in `<span>` elements
  * (`<span data-cke-filler="true">&nbsp;</span>`) instead of regular `&nbsp;` characters.
  *
  * This mode allows for a more precise handling of the block fillers (so they do not leak into the editor content) but
  * bloats the editor data with additional markup.
  *
  * This mode may be required by some features and will be turned on by them automatically.
  *
  * @param type Whether to use the default or the marked `&nbsp;` block fillers.
  */
  useFillerType(t) {
    this.domConverter.blockFillerMode = t == "marked" ? "markedNbsp" : "nbsp";
  }
  /**
  * Converts an HTML string to its DOM representation. Returns a document fragment containing nodes parsed from
  * the provided data.
  */
  _toDom(t) {
    /<(?:html|body|head|meta)(?:\s[^>]*)?>/i.test(t.trim().slice(0, 1e4)) || (t = `<body>${t}</body>`);
    const e = this.domParser.parseFromString(t, "text/html"), i = e.createDocumentFragment(), s = e.body.childNodes;
    for (; s.length > 0; )
      i.appendChild(s[0]);
    return i;
  }
}
class XR extends (/* @__PURE__ */ Je()) {
  /**
  * Creates a data controller instance.
  *
  * @param model Data model.
  * @param stylesProcessor The styles processor instance.
  */
  constructor(e, i) {
    super();
    /**
    * Data model.
    */
    f(this, "model");
    /**
    * Mapper used for the conversion. It has no permanent bindings, because these are created while getting data and
    * are cleared directly after the data are converted. However, the mapper is defined as a class property, because
    * it needs to be passed to the `DowncastDispatcher` as a conversion API.
    */
    f(this, "mapper");
    /**
    * Downcast dispatcher used by the {@link #get get method}. Downcast converters should be attached to it.
    */
    f(this, "downcastDispatcher");
    /**
    * Upcast dispatcher used by the {@link #set set method}. Upcast converters should be attached to it.
    */
    f(this, "upcastDispatcher");
    /**
    * The view document used by the data controller.
    */
    f(this, "viewDocument");
    /**
    * Styles processor used during the conversion.
    */
    f(this, "stylesProcessor");
    /**
    * Data processor used specifically for HTML conversion.
    */
    f(this, "htmlProcessor");
    /**
    * Data processor used during the conversion.
    * Same instance as {@link #htmlProcessor} by default. Can be replaced at run time to handle different format, e.g. XML or Markdown.
    */
    f(this, "processor");
    /**
    * The view downcast writer just for data conversion purposes, i.e. to modify
    * the {@link #viewDocument}.
    */
    f(this, "_viewWriter");
    this.model = e, this.mapper = new V_(), this.downcastDispatcher = new L_({
      mapper: this.mapper,
      schema: e.schema
    }), this.downcastDispatcher.on("insert:$text", N_(), {
      priority: "lowest"
    }), this.downcastDispatcher.on("insert", F_(), {
      priority: "lowest"
    }), this.upcastDispatcher = new KR({
      schema: e.schema
    }), this.viewDocument = new Gc(i), this.stylesProcessor = i, this.htmlProcessor = new Of(this.viewDocument), this.processor = this.htmlProcessor, this._viewWriter = new C_(this.viewDocument), this.upcastDispatcher.on("text", dR(), {
      priority: "lowest"
    }), this.upcastDispatcher.on("element", Lp(), {
      priority: "lowest"
    }), this.upcastDispatcher.on("documentFragment", Lp(), {
      priority: "lowest"
    }), je().prototype.decorate.call(this, "init"), je().prototype.decorate.call(this, "set"), je().prototype.decorate.call(this, "get"), je().prototype.decorate.call(this, "toView"), je().prototype.decorate.call(this, "toModel"), this.on("init", () => {
      this.fire("ready");
    }, {
      priority: "lowest"
    }), this.on("ready", () => {
      this.model.enqueueChange({
        isUndoable: !1
      }, W_);
    }, {
      priority: "lowest"
    });
  }
  /**
  * Returns the model's data converted by downcast dispatchers attached to {@link #downcastDispatcher} and
  * formatted by the {@link #processor data processor}.
  *
  * A warning is logged when you try to retrieve data for a detached root, as most probably this is a mistake. A detached root should
  * be treated like it is removed, and you should not save its data. Note, that the detached root data is always an empty string.
  *
  * @fires get
  * @param options Additional configuration for the retrieved data. `DataController` provides two optional
  * properties: `rootName` and `trim`. Other properties of this object are specified by various editor features.
  * @param options.rootName Root name. Default 'main'.
  * @param options.trim Whether returned data should be trimmed. This option is set to `empty` by default,
  * which means whenever editor content is considered empty, an empty string will be returned. To turn off trimming completely
  * use `'none'`. In such cases the exact content will be returned (for example a `<p>&nbsp;</p>` for an empty editor).
  * @returns Output data.
  */
  get(e = {}) {
    const { rootName: i = "main", trim: s = "empty" } = e;
    if (!this._checkIfRootsExists([
      i
    ]))
      throw new S("datacontroller-get-non-existent-root", this);
    const r = this.model.document.getRoot(i);
    return r.isAttached() || Ve("datacontroller-get-detached-root", this), s === "empty" && !this.model.hasContent(r, {
      ignoreWhitespaces: !0
    }) ? "" : this.stringify(r, e);
  }
  /**
  * Returns the content of the given {@link module:engine/model/element~Element model's element} or
  * {@link module:engine/model/documentfragment~DocumentFragment model document fragment} converted by the downcast converters
  * attached to the {@link #downcastDispatcher} and formatted by the {@link #processor data processor}.
  *
  * @param modelElementOrFragment The element whose content will be stringified.
  * @param options Additional configuration passed to the conversion process.
  * @returns Output data.
  */
  stringify(e, i = {}) {
    const s = this.toView(e, i);
    return this.processor.toData(s);
  }
  /**
  * Returns the content of the given {@link module:engine/model/element~Element model element} or
  * {@link module:engine/model/documentfragment~DocumentFragment model document fragment} converted by the downcast
  * converters attached to {@link #downcastDispatcher} into a
  * {@link module:engine/view/documentfragment~DocumentFragment view document fragment}.
  *
  * @fires toView
  * @param modelElementOrFragment Element or document fragment whose content will be converted.
  * @param options Additional configuration that will be available through the
  * {@link module:engine/conversion/downcastdispatcher~DowncastConversionApi#options} during the conversion process.
  * @returns Output view DocumentFragment.
  */
  toView(e, i = {}) {
    const s = this.viewDocument, r = this._viewWriter;
    this.mapper.clearBindings();
    const o = F._createIn(e), a = new Ls(s);
    this.mapper.bindElements(e, a);
    const l = e.is("documentFragment") ? e.markers : QR(e);
    return this.downcastDispatcher.convert(o, l, r, i), a;
  }
  /**
  * Sets the initial input data parsed by the {@link #processor data processor} and
  * converted by the {@link #upcastDispatcher view-to-model converters}.
  * Initial data can be only set to a document whose {@link module:engine/model/document~Document#version} is equal 0.
  *
  * **Note** This method is {@link module:utils/observablemixin~Observable#decorate decorated} which is
  * used by e.g. collaborative editing plugin that syncs remote data on init.
  *
  * When data is passed as a string, it is initialized on the default `main` root:
  *
  * ```ts
  * dataController.init( '<p>Foo</p>' ); // Initializes data on the `main` root only, as no other is specified.
  * ```
  *
  * To initialize data on a different root or multiple roots at once, an object containing `rootName` - `data` pairs should be passed:
  *
  * ```ts
  * dataController.init( { main: '<p>Foo</p>', title: '<h1>Bar</h1>' } ); // Initializes data on both the `main` and `title` roots.
  * ```
  *
  * @fires init
  * @param data Input data as a string or an object containing the `rootName` - `data`
  * pairs to initialize data on multiple roots at once.
  * @returns Promise that is resolved after the data is set on the editor.
  */
  init(e) {
    if (this.model.document.version)
      throw new S("datacontroller-init-document-not-empty", this);
    let i = {};
    if (typeof e == "string" ? i.main = e : i = e, !this._checkIfRootsExists(Object.keys(i)))
      throw new S("datacontroller-init-non-existent-root", this);
    return this.model.enqueueChange({
      isUndoable: !1
    }, (s) => {
      for (const r of Object.keys(i)) {
        const o = this.model.document.getRoot(r);
        s.insert(this.parse(i[r], o), o, 0);
      }
    }), Promise.resolve();
  }
  /**
  * Sets the input data parsed by the {@link #processor data processor} and
  * converted by the {@link #upcastDispatcher view-to-model converters}.
  * This method can be used any time to replace existing editor data with the new one without clearing the
  * {@link module:engine/model/document~Document#history document history}.
  *
  * This method also creates a batch with all the changes applied. If all you need is to parse data, use
  * the {@link #parse} method.
  *
  * When data is passed as a string it is set on the default `main` root:
  *
  * ```ts
  * dataController.set( '<p>Foo</p>' ); // Sets data on the `main` root, as no other is specified.
  * ```
  *
  * To set data on a different root or multiple roots at once, an object containing `rootName` - `data` pairs should be passed:
  *
  * ```ts
  * dataController.set( { main: '<p>Foo</p>', title: '<h1>Bar</h1>' } ); // Sets data on the `main` and `title` roots as specified.
  * ```
  *
  * To set the data with a preserved undo stack and add the change to the undo stack, set `{ isUndoable: true }` as a `batchType` option.
  *
  * ```ts
  * dataController.set( '<p>Foo</p>', { batchType: { isUndoable: true } } );
  * ```
  *
  * @fires set
  * @param data Input data as a string or an object containing the `rootName` - `data`
  * pairs to set data on multiple roots at once.
  * @param options Options for setting data.
  * @param options.batchType The batch type that will be used to create a batch for the changes applied by this method.
  * By default, the batch will be set as {@link module:engine/model/batch~Batch#isUndoable not undoable} and the undo stack will be
  * cleared after the new data is applied (all undo steps will be removed). If the batch type `isUndoable` flag is be set to `true`,
  * the undo stack will be preserved instead and not cleared when new data is applied.
  */
  set(e, i = {}) {
    let s = {};
    if (typeof e == "string" ? s.main = e : s = e, !this._checkIfRootsExists(Object.keys(s)))
      throw new S("datacontroller-set-non-existent-root", this);
    this.model.enqueueChange(i.batchType || {}, (r) => {
      r.setSelection(null), r.removeSelectionAttribute(this.model.document.selection.getAttributeKeys());
      for (const o of Object.keys(s)) {
        const a = this.model.document.getRoot(o);
        r.remove(r.createRangeIn(a)), r.insert(this.parse(s[o], a), a, 0);
      }
    });
  }
  /**
  * Returns the data parsed by the {@link #processor data processor} and then converted by upcast converters
  * attached to the {@link #upcastDispatcher}.
  *
  * @see #set
  * @param data Data to parse.
  * @param context Base context in which the view will be converted to the model.
  * See: {@link module:engine/conversion/upcastdispatcher~UpcastDispatcher#convert}.
  * @returns Parsed data.
  */
  parse(e, i = "$root") {
    const s = this.processor.toView(e);
    return this.toModel(s, i);
  }
  /**
  * Returns the result of the given {@link module:engine/view/element~Element view element} or
  * {@link module:engine/view/documentfragment~DocumentFragment view document fragment} converted by the
  * {@link #upcastDispatcher view-to-model converters}, wrapped by {@link module:engine/model/documentfragment~DocumentFragment}.
  *
  * When marker elements were converted during the conversion process, it will be set as a document fragment's
  * {@link module:engine/model/documentfragment~DocumentFragment#markers static markers map}.
  *
  * @fires toModel
  * @param viewElementOrFragment The element or document fragment whose content will be converted.
  * @param context Base context in which the view will be converted to the model.
  * See: {@link module:engine/conversion/upcastdispatcher~UpcastDispatcher#convert}.
  * @returns Output document fragment.
  */
  toModel(e, i = "$root") {
    return this.model.change((s) => this.upcastDispatcher.convert(e, s, i));
  }
  /**
  * Adds the style processor normalization rules.
  *
  * You can implement your own rules as well as use one of the available processor rules:
  *
  * * background: {@link module:engine/view/styles/background~addBackgroundRules}
  * * border: {@link module:engine/view/styles/border~addBorderRules}
  * * margin: {@link module:engine/view/styles/margin~addMarginRules}
  * * padding: {@link module:engine/view/styles/padding~addPaddingRules}
  */
  addStyleProcessorRules(e) {
    e(this.stylesProcessor);
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} on an {@link #htmlProcessor htmlProcessor}
  * and a {@link #processor processor} for view elements whose content should be treated as raw data
  * and not processed during the conversion from DOM to view elements.
  *
  * The raw data can be later accessed by the {@link module:engine/view/element~Element#getCustomProperty view element custom property}
  * `"$rawContent"`.
  *
  * @param pattern Pattern matching all view elements whose content should be treated as a raw data.
  */
  registerRawContentMatcher(e) {
    this.processor && this.processor !== this.htmlProcessor && this.processor.registerRawContentMatcher(e), this.htmlProcessor.registerRawContentMatcher(e);
  }
  /**
  * Removes all event listeners set by the DataController.
  */
  destroy() {
    this.stopListening();
  }
  /**
  * Checks whether all provided root names are actually existing editor roots.
  *
  * @param rootNames Root names to check.
  * @returns Whether all provided root names are existing editor roots.
  */
  _checkIfRootsExists(e) {
    for (const i of e)
      if (!this.model.document.getRoot(i))
        return !1;
    return !0;
  }
}
function QR(n) {
  const t = [], e = n.root.document;
  if (!e)
    return /* @__PURE__ */ new Map();
  const i = F._createIn(n);
  for (const s of e.model.markers) {
    const r = s.getRange(), o = r.isCollapsed, a = r.start.isEqual(i.start) || r.end.isEqual(i.end);
    if (o && a)
      t.push([
        s.name,
        r
      ]);
    else {
      const l = i.getIntersection(r);
      l && t.push([
        s.name,
        l
      ]);
    }
  }
  return t.sort(([s, r], [o, a]) => {
    if (r.end.compareWith(a.start) !== "after")
      return 1;
    if (r.start.compareWith(a.end) !== "before")
      return -1;
    switch (r.start.compareWith(a.start)) {
      case "before":
        return 1;
      case "after":
        return -1;
      default:
        switch (r.end.compareWith(a.end)) {
          case "before":
            return 1;
          case "after":
            return -1;
          default:
            return o.localeCompare(s);
        }
    }
  }), new Map(t);
}
class eO {
  /**
  * Creates a new conversion instance.
  */
  constructor(t, e) {
    /**
    * Maps dispatchers group name to ConversionHelpers instances.
    */
    f(this, "_helpers", /* @__PURE__ */ new Map());
    f(this, "_downcast");
    f(this, "_upcast");
    this._downcast = Ue(t), this._createConversionHelpers({
      name: "downcast",
      dispatchers: this._downcast,
      isDowncast: !0
    }), this._upcast = Ue(e), this._createConversionHelpers({
      name: "upcast",
      dispatchers: this._upcast,
      isDowncast: !1
    });
  }
  /**
  * Define an alias for registered dispatcher.
  *
  * ```ts
  * const conversion = new Conversion(
  * 	[ dataDowncastDispatcher, editingDowncastDispatcher ],
  * 	upcastDispatcher
  * );
  *
  * conversion.addAlias( 'dataDowncast', dataDowncastDispatcher );
  * ```
  *
  * @param alias An alias of a dispatcher.
  * @param dispatcher Dispatcher which should have an alias.
  */
  addAlias(t, e) {
    const i = this._downcast.includes(e);
    if (!this._upcast.includes(e) && !i)
      throw new S("conversion-add-alias-dispatcher-not-registered", this);
    this._createConversionHelpers({
      name: t,
      dispatchers: [
        e
      ],
      isDowncast: i
    });
  }
  /**
  * Provides a chainable API to assign converters to a conversion dispatchers group.
  *
  * If the given group name has not been registered, the
  * {@link module:utils/ckeditorerror~CKEditorError `conversion-for-unknown-group` error} is thrown.
  *
  * You can use conversion helpers available directly in the `for()` chain or your custom ones via
  * the {@link module:engine/conversion/conversionhelpers~ConversionHelpers#add `add()`} method.
  *
  * # Using built-in conversion helpers
  *
  * The `for()` chain comes with a set of conversion helpers which you can use like this:
  *
  * ```ts
  * editor.conversion.for( 'downcast' )
  * 	.elementToElement( config1 )        // Adds an element-to-element downcast converter.
  * 	.attributeToElement( config2 );     // Adds an attribute-to-element downcast converter.
  *
  * editor.conversion.for( 'upcast' )
  * 	.elementToAttribute( config3 );     // Adds an element-to-attribute upcast converter.
  * ```
  *
  * Refer to the documentation of built-in conversion helpers to learn about their configuration options.
  *
  * * downcast (model-to-view) conversion helpers:
  *
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#elementToElement `elementToElement()`},
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#attributeToElement `attributeToElement()`},
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#attributeToAttribute `attributeToAttribute()`}.
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToElement `markerToElement()`}.
  *	* {@link module:engine/conversion/downcasthelpers~DowncastHelpers#markerToHighlight `markerToHighlight()`}.
  *
  * * upcast (view-to-model) conversion helpers:
  *
  *	* {@link module:engine/conversion/upcasthelpers~UpcastHelpers#elementToElement `elementToElement()`},
  *	* {@link module:engine/conversion/upcasthelpers~UpcastHelpers#elementToAttribute `elementToAttribute()`},
  *	* {@link module:engine/conversion/upcasthelpers~UpcastHelpers#attributeToAttribute `attributeToAttribute()`}.
  *	* {@link module:engine/conversion/upcasthelpers~UpcastHelpers#elementToMarker `elementToMarker()`}.
  *
  * # Using custom conversion helpers
  *
  * If you need to implement an atypical converter, you can do so by calling:
  *
  * ```ts
  * editor.conversion.for( direction ).add( customHelper );
  * ```
  *
  * The `.add()` method takes exactly one parameter, which is a function. This function should accept one parameter that
  * is a dispatcher instance. The function should add an actual converter to the passed dispatcher instance.
  *
  * Example:
  *
  * ```ts
  * editor.conversion.for( 'upcast' ).add( dispatcher => {
  * 	dispatcher.on( 'element:a',  ( evt, data, conversionApi ) => {
  * 		// Do something with a view <a> element.
  * 	} );
  * } );
  * ```
  *
  * Refer to the documentation of {@link module:engine/conversion/upcastdispatcher~UpcastDispatcher}
  * and {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher} to learn how to write
  * custom converters.
  *
  * @param groupName The name of dispatchers group to add the converters to.
  */
  for(t) {
    if (!this._helpers.has(t))
      throw new S("conversion-for-unknown-group", this);
    return this._helpers.get(t);
  }
  /**
  * Sets up converters between the model and the view that convert a model element to a view element (and vice versa).
  * For example, the model `<paragraph>Foo</paragraph>` is turned into `<p>Foo</p>` in the view.
  *
  * ```ts
  * // A simple conversion from the `paragraph` model element to the `<p>` view element (and vice versa).
  * editor.conversion.elementToElement( { model: 'paragraph', view: 'p' } );
  *
  * // Override other converters by specifying a converter definition with a higher priority.
  * editor.conversion.elementToElement( { model: 'paragraph', view: 'div', converterPriority: 'high' } );
  *
  * // View specified as an object instead of a string.
  * editor.conversion.elementToElement( {
  * 	model: 'fancyParagraph',
  * 	view: {
  * 		name: 'p',
  * 		classes: 'fancy'
  * 	}
  * } );
  *
  * // Use `upcastAlso` to define other view elements that should also be converted to a `paragraph` element.
  * editor.conversion.elementToElement( {
  * 	model: 'paragraph',
  * 	view: 'p',
  * 	upcastAlso: [
  * 		'div',
  * 		{
  * 			// Any element with the `display: block` style.
  * 			styles: {
  * 				display: 'block'
  * 			}
  * 		}
  * 	]
  * } );
  *
  * // `upcastAlso` set as callback enables a conversion of a wide range of different view elements.
  * editor.conversion.elementToElement( {
  * 	model: 'heading',
  * 	view: 'h2',
  * 	// Convert "heading-like" paragraphs to headings.
  * 	upcastAlso: viewElement => {
  * 		const fontSize = viewElement.getStyle( 'font-size' );
  *
  * 		if ( !fontSize ) {
  * 			return null;
  * 		}
  *
  * 		const match = fontSize.match( /(\d+)\s*px/ );
  *
  * 		if ( !match ) {
  * 			return null;
  * 		}
  *
  * 		const size = Number( match[ 1 ] );
  *
  * 		if ( size > 26 ) {
  * 			// Returned value can be an object with the matched properties.
  * 			// These properties will be "consumed" during the conversion.
  * 			// See `engine.view.Matcher~MatcherPattern` and `engine.view.Matcher#match` for more details.
  *
  * 			return { name: true, styles: [ 'font-size' ] };
  * 		}
  *
  * 		return null;
  * 	}
  * } );
  * ```
  *
  * `definition.model` is a `String` with a model element name to convert from or to.
  *
  * @param definition The converter definition.
  */
  elementToElement(t) {
    this.for("downcast").elementToElement(t);
    for (const { model: e, view: i } of ju(t))
      this.for("upcast").elementToElement({
        model: e,
        view: i,
        converterPriority: t.converterPriority
      });
  }
  /**
  * Sets up converters between the model and the view that convert a model attribute to a view element (and vice versa).
  * For example, a model text node with `"Foo"` as data and the `bold` attribute will be turned to `<strong>Foo</strong>` in the view.
  *
  * ```ts
  * // A simple conversion from the `bold=true` attribute to the `<strong>` view element (and vice versa).
  * editor.conversion.attributeToElement( { model: 'bold', view: 'strong' } );
  *
  * // Override other converters by specifying a converter definition with a higher priority.
  * editor.conversion.attributeToElement( { model: 'bold', view: 'b', converterPriority: 'high' } );
  *
  * // View specified as an object instead of a string.
  * editor.conversion.attributeToElement( {
  * 	model: 'bold',
  * 	view: {
  * 		name: 'span',
  * 		classes: 'bold'
  * 	}
  * } );
  *
  * // Use `config.model.name` to define the conversion only from a given node type, `$text` in this case.
  * // The same attribute on different elements may then be handled by a different converter.
  * editor.conversion.attributeToElement( {
  * 	model: {
  * 		key: 'textDecoration',
  * 		values: [ 'underline', 'lineThrough' ],
  * 		name: '$text'
  * 	},
  * 	view: {
  * 		underline: {
  * 			name: 'span',
  * 			styles: {
  * 				'text-decoration': 'underline'
  * 			}
  * 		},
  * 		lineThrough: {
  * 			name: 'span',
  * 			styles: {
  * 				'text-decoration': 'line-through'
  * 			}
  * 		}
  * 	}
  * } );
  *
  * // Use `upcastAlso` to define other view elements that should also be converted to the `bold` attribute.
  * editor.conversion.attributeToElement( {
  * 	model: 'bold',
  * 	view: 'strong',
  * 	upcastAlso: [
  * 		'b',
  * 		{
  * 			name: 'span',
  * 			classes: 'bold'
  * 		},
  * 		{
  * 			name: 'span',
  * 			styles: {
  * 				'font-weight': 'bold'
  * 			}
  * 		},
  * 		viewElement => {
  * 			const fontWeight = viewElement.getStyle( 'font-weight' );
  *
  * 			if ( viewElement.is( 'element', 'span' ) && fontWeight && /\d+/.test() && Number( fontWeight ) > 500 ) {
  * 				// Returned value can be an object with the matched properties.
  * 				// These properties will be "consumed" during the conversion.
  * 				// See `engine.view.Matcher~MatcherPattern` and `engine.view.Matcher#match` for more details.
  *
  * 				return {
  * 					name: true,
  * 					styles: [ 'font-weight' ]
  * 				};
  * 			}
  * 		}
  * 	]
  * } );
  *
  * // Conversion from and to a model attribute key whose value is an enum (`fontSize=big|small`).
  * // `upcastAlso` set as callback enables a conversion of a wide range of different view elements.
  * editor.conversion.attributeToElement( {
  * 	model: {
  * 		key: 'fontSize',
  * 		values: [ 'big', 'small' ]
  * 	},
  * 	view: {
  * 		big: {
  * 			name: 'span',
  * 			styles: {
  * 				'font-size': '1.2em'
  * 			}
  * 		},
  * 		small: {
  * 			name: 'span',
  * 			styles: {
  * 				'font-size': '0.8em'
  * 			}
  * 		}
  * 	},
  * 	upcastAlso: {
  * 		big: viewElement => {
  * 			const fontSize = viewElement.getStyle( 'font-size' );
  *
  * 			if ( !fontSize ) {
  * 				return null;
  * 			}
  *
  * 			const match = fontSize.match( /(\d+)\s*px/ );
  *
  * 			if ( !match ) {
  * 				return null;
  * 			}
  *
  * 			const size = Number( match[ 1 ] );
  *
  * 			if ( viewElement.is( 'element', 'span' ) && size > 10 ) {
  * 				// Returned value can be an object with the matched properties.
  * 				// These properties will be "consumed" during the conversion.
  * 				// See `engine.view.Matcher~MatcherPattern` and `engine.view.Matcher#match` for more details.
  *
  * 				return { name: true, styles: [ 'font-size' ] };
  * 			}
  *
  * 			return null;
  * 		},
  * 		small: viewElement => {
  * 			const fontSize = viewElement.getStyle( 'font-size' );
  *
  * 			if ( !fontSize ) {
  * 				return null;
  * 			}
  *
  * 			const match = fontSize.match( /(\d+)\s*px/ );
  *
  * 			if ( !match ) {
  * 				return null;
  * 			}
  *
  * 			const size = Number( match[ 1 ] );
  *
  * 			if ( viewElement.is( 'element', 'span' ) && size < 10 ) {
  * 				// Returned value can be an object with the matched properties.
  * 				// These properties will be "consumed" during the conversion.
  * 				// See `engine.view.Matcher~MatcherPattern` and `engine.view.Matcher#match` for more details.
  *
  * 				return { name: true, styles: [ 'font-size' ] };
  * 			}
  *
  * 			return null;
  * 		}
  * 	}
  * } );
  * ```
  *
  * The `definition.model` parameter specifies which model attribute should be converted from or to. It can be a `{ key, value }` object
  * describing the attribute key and value to convert or a `String` specifying just the attribute key (in such a case
  * `value` is set to `true`).
  *
  * @param definition The converter definition.
  */
  attributeToElement(t) {
    this.for("downcast").attributeToElement(t);
    for (const { model: e, view: i } of ju(t))
      this.for("upcast").elementToAttribute({
        view: i,
        model: e,
        converterPriority: t.converterPriority
      });
  }
  /**
  * Sets up converters between the model and the view that convert a model attribute to a view attribute (and vice versa). For example,
  * `<imageBlock src='foo.jpg'></imageBlock>` is converted to `<img src='foo.jpg'></img>` (the same attribute key and value).
  * This type of converters is intended to be used with {@link module:engine/model/element~Element model element} nodes.
  * To convert the text attributes,
  * the {@link module:engine/conversion/conversion~Conversion#attributeToElement `attributeToElement converter`}should be set up.
  *
  * ```ts
  * // A simple conversion from the `source` model attribute to the `src` view attribute (and vice versa).
  * editor.conversion.attributeToAttribute( { model: 'source', view: 'src' } );
  *
  * // Attribute values are strictly specified.
  * editor.conversion.attributeToAttribute( {
  * 	model: {
  * 		name: 'imageInline',
  * 		key: 'aside',
  * 		values: [ 'aside' ]
  * 	},
  * 	view: {
  * 		aside: {
  * 			name: 'img',
  * 			key: 'class',
  * 			value: [ 'aside', 'half-size' ]
  * 		}
  * 	}
  * } );
  *
  * // Set the style attribute.
  * editor.conversion.attributeToAttribute( {
  * 	model: {
  * 		name: 'imageInline',
  * 		key: 'aside',
  * 		values: [ 'aside' ]
  * 	},
  * 	view: {
  * 		aside: {
  * 			name: 'img',
  * 			key: 'style',
  * 			value: {
  * 				float: 'right',
  * 				width: '50%',
  * 				margin: '5px'
  * 			}
  * 		}
  * 	}
  * } );
  *
  * // Conversion from and to a model attribute key whose value is an enum (`align=right|center`).
  * // Use `upcastAlso` to define other view elements that should also be converted to the `align=right` attribute.
  * editor.conversion.attributeToAttribute( {
  * 	model: {
  * 		key: 'align',
  * 		values: [ 'right', 'center' ]
  * 	},
  * 	view: {
  * 		right: {
  * 			key: 'class',
  * 			value: 'align-right'
  * 		},
  * 		center: {
  * 			key: 'class',
  * 			value: 'align-center'
  * 		}
  * 	},
  * 	upcastAlso: {
  * 		right: {
  * 			styles: {
  * 				'text-align': 'right'
  * 			}
  * 		},
  * 		center: {
  * 			styles: {
  * 				'text-align': 'center'
  * 			}
  * 		}
  * 	}
  * } );
  * ```
  *
  * The `definition.model` parameter specifies which model attribute should be converted from and to.
  * It can be a `{ key, [ values ], [ name ] }` object or a `String`, which will be treated like `{ key: definition.model }`.
  * The `key` property is the model attribute key to convert from and to.
  * The `values` are the possible model attribute values. If the `values` parameter is not set, the model attribute value
  * will be the same as the view attribute value.
  * If `name` is set, the conversion will be set up only for model elements with the given name.
  *
  * The `definition.view` parameter specifies which view attribute should be converted from and to.
  * It can be a `{ key, value, [ name ] }` object or a `String`, which will be treated like `{ key: definition.view }`.
  * The `key` property is the view attribute key to convert from and to.
  * The `value` is the view attribute value to convert from and to. If `definition.value` is not set, the view attribute value will be
  * the same as the model attribute value.
  * If `key` is `'class'`, `value` can be a `String` or an array of `String`s.
  * If `key` is `'style'`, `value` is an object with key-value pairs.
  * In other cases, `value` is a `String`.
  * If `name` is set, the conversion will be set up only for model elements with the given name.
  * If `definition.model.values` is set, `definition.view` is an object that assigns values from `definition.model.values`
  * to `{ key, value, [ name ] }` objects.
  *
  * `definition.upcastAlso` specifies which other matching view elements should also be upcast to the given model configuration.
  * If `definition.model.values` is set, `definition.upcastAlso` should be an object assigning values from `definition.model.values`
  * to {@link module:engine/view/matcher~MatcherPattern}s or arrays of {@link module:engine/view/matcher~MatcherPattern}s.
  *
  * **Note:** `definition.model` and `definition.view` form should be mirrored, so the same types of parameters should
  * be given in both parameters.
  *
  * @param definition The converter definition.
  * @param definition.model The model attribute to convert from and to.
  * @param definition.view The view attribute to convert from and to.
  * @param definition.upcastAlso Any view element matching `definition.upcastAlso` will also be converted to the given model attribute.
  * `definition.upcastAlso` is used only if `config.model.values` is specified.
  */
  attributeToAttribute(t) {
    this.for("downcast").attributeToAttribute(t);
    for (const { model: e, view: i } of ju(t))
      this.for("upcast").attributeToAttribute({
        view: i,
        model: e
      });
  }
  /**
  * Creates and caches conversion helpers for given dispatchers group.
  *
  * @param options.name Group name.
  */
  _createConversionHelpers({ name: t, dispatchers: e, isDowncast: i }) {
    if (this._helpers.has(t))
      throw new S("conversion-group-exists", this);
    const s = i ? new O7(e) : new uR(e);
    this._helpers.set(t, s);
  }
}
function* ju(n) {
  if (n.model.values)
    for (const t of n.model.values) {
      const e = {
        key: n.model.key,
        value: t
      }, i = n.view[t], s = n.upcastAlso ? n.upcastAlso[t] : void 0;
      yield* Dp(e, i, s);
    }
  else
    yield* Dp(n.model, n.view, n.upcastAlso);
}
function* Dp(n, t, e) {
  if (yield {
    model: n,
    view: t
  }, e)
    for (const i of Ue(e))
      yield {
        model: n,
        view: i
      };
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class Ii {
  /**
  * Base operation constructor.
  *
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(t) {
    /**
    * {@link module:engine/model/document~Document#version} on which operation can be applied. If you try to
    * {@link module:engine/model/model~Model#applyOperation apply} operation with different base version than the
    * {@link module:engine/model/document~Document#version document version} the
    * {@link module:utils/ckeditorerror~CKEditorError model-document-applyOperation-wrong-version} error is thrown.
    */
    f(this, "baseVersion");
    /**
    * Defines whether operation is executed on attached or detached {@link module:engine/model/item~Item items}.
    */
    f(this, "isDocumentOperation");
    /**
    * {@link module:engine/model/batch~Batch Batch} to which the operation is added or `null` if the operation is not
    * added to any batch yet.
    *
    * Note, that a {@link #isDocumentOperation non-document operation} has this property always set to `null`, and is never added
    * to any batch.
    */
    f(this, "batch");
    this.baseVersion = t, this.isDocumentOperation = this.baseVersion !== null, this.batch = null;
  }
  /**
  * Checks whether the operation's parameters are correct and the operation can be correctly executed. Throws
  * an error if operation is not valid.
  *
  * @internal
  */
  _validate() {
  }
  /**
  * Custom toJSON method to solve child-parent circular dependencies.
  *
  * @returns Clone of this object with the operation property replaced with string.
  */
  toJSON() {
    const t = Object.assign({}, this);
    return t.__className = this.constructor.className, delete t.batch, delete t.isDocumentOperation, t;
  }
  /**
  * Name of the operation class used for serialization.
  */
  static get className() {
    return "Operation";
  }
  /**
  * Creates `Operation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param doc Document on which this operation will be applied.
  */
  static fromJSON(t, e) {
    return new this(t.baseVersion);
  }
}
function Vf(n, t) {
  const e = Q_(t), i = e.reduce((o, a) => o + a.offsetSize, 0), s = n.parent;
  ua(n);
  const r = n.index;
  return s._insertChild(r, e), ca(s, r + e.length), ca(s, r), new F(n, n.getShiftedBy(i));
}
function X_(n) {
  if (!n.isFlat)
    throw new S("operation-utils-remove-range-not-flat", this);
  const t = n.start.parent;
  ua(n.start), ua(n.end);
  const e = t._removeChildren(n.start.index, n.end.index - n.start.index);
  return ca(t, n.start.index), e;
}
function la(n, t) {
  if (!n.isFlat)
    throw new S("operation-utils-move-range-not-flat", this);
  const e = X_(n);
  return t = t._getTransformedByDeletion(n.start, n.end.offset - n.start.offset), Vf(t, e);
}
function tO(n, t, e) {
  ua(n.start), ua(n.end);
  for (const i of n.getItems({
    shallow: !0
  })) {
    const s = i.is("$textProxy") ? i.textNode : i;
    e !== null ? s._setAttribute(t, e) : s._removeAttribute(t), ca(s.parent, s.index);
  }
  ca(n.end.parent, n.end.index);
}
function Q_(n) {
  const t = [];
  function e(i) {
    if (typeof i == "string")
      t.push(new lt(i));
    else if (i instanceof Ki)
      t.push(new lt(i.data, i.getAttributes()));
    else if (i instanceof Zs)
      t.push(i);
    else if (Ai(i))
      for (const s of i)
        e(s);
  }
  e(n);
  for (let i = 1; i < t.length; i++) {
    const s = t[i], r = t[i - 1];
    s instanceof lt && r instanceof lt && ey(s, r) && (t.splice(i - 1, 2, new lt(r.data + s.data, r.getAttributes())), i--);
  }
  return t;
}
function ca(n, t) {
  const e = n.getChild(t - 1), i = n.getChild(t);
  if (e && i && e.is("$text") && i.is("$text") && ey(e, i)) {
    const s = new lt(e.data + i.data, e.getAttributes());
    n._removeChildren(t - 1, 2), n._insertChild(t - 1, s);
  }
}
function ua(n) {
  const t = n.textNode, e = n.parent;
  if (t) {
    const i = n.offset - t.startOffset, s = t.index;
    e._removeChildren(s, 1);
    const r = new lt(t.data.substr(0, i), t.getAttributes()), o = new lt(t.data.substr(i), t.getAttributes());
    e._insertChild(s, [
      r,
      o
    ]);
  }
}
function ey(n, t) {
  const e = n.getAttributes(), i = t.getAttributes();
  for (const s of e) {
    if (s[1] !== t.getAttribute(s[0]))
      return !1;
    i.next();
  }
  return i.next().done;
}
class He extends Ii {
  /**
  * Creates a move operation.
  *
  * @param sourcePosition Position before the first {@link module:engine/model/item~Item model item} to move.
  * @param howMany Offset size of moved range. Moved range will start from `sourcePosition` and end at
  * `sourcePosition` with offset shifted by `howMany`.
  * @param targetPosition Position at which moved nodes will be inserted.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, i, s, r) {
    super(r);
    /**
    * Position before the first {@link module:engine/model/item~Item model item} to move.
    */
    f(this, "sourcePosition");
    /**
    * Offset size of moved range.
    */
    f(this, "howMany");
    /**
    * Position at which moved nodes will be inserted.
    */
    f(this, "targetPosition");
    this.sourcePosition = e.clone(), this.sourcePosition.stickiness = "toNext", this.howMany = i, this.targetPosition = s.clone(), this.targetPosition.stickiness = "toNone";
  }
  /**
  * @inheritDoc
  */
  get type() {
    return this.targetPosition.root.rootName == "$graveyard" ? "remove" : this.sourcePosition.root.rootName == "$graveyard" ? "reinsert" : "move";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return [
      F._createFromPositionAndShift(this.sourcePosition, this.howMany),
      F._createFromPositionAndShift(this.targetPosition, 0)
    ];
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new He(this.sourcePosition, this.howMany, this.targetPosition, this.baseVersion);
  }
  /**
  * Returns the start position of the moved range after it got moved. This may be different than
  * {@link module:engine/model/operation/moveoperation~MoveOperation#targetPosition} in some cases, i.e. when a range is moved
  * inside the same parent but {@link module:engine/model/operation/moveoperation~MoveOperation#targetPosition targetPosition}
  * is after {@link module:engine/model/operation/moveoperation~MoveOperation#sourcePosition sourcePosition}.
  *
  * ```
  *  vv              vv
  * abcdefg ===> adefbcg
  *      ^          ^
  *      targetPos  movedRangeStart
  *      offset 6   offset 4
  *```
  */
  getMovedRangeStart() {
    return this.targetPosition._getTransformedByDeletion(this.sourcePosition, this.howMany);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    const e = this.sourcePosition._getTransformedByInsertion(this.targetPosition, this.howMany);
    return new He(this.getMovedRangeStart(), this.howMany, e, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.sourcePosition.parent, i = this.targetPosition.parent, s = this.sourcePosition.offset, r = this.targetPosition.offset;
    if (s + this.howMany > e.maxOffset)
      throw new S("move-operation-nodes-do-not-exist", this);
    if (e === i && s < r && r < s + this.howMany)
      throw new S("move-operation-range-into-itself", this);
    if (this.sourcePosition.root == this.targetPosition.root && Zt(this.sourcePosition.getParentPath(), this.targetPosition.getParentPath()) == "prefix") {
      const o = this.sourcePosition.path.length - 1;
      if (this.targetPosition.path[o] >= s && this.targetPosition.path[o] < s + this.howMany)
        throw new S("move-operation-node-into-itself", this);
    }
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    la(F._createFromPositionAndShift(this.sourcePosition, this.howMany), this.targetPosition);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.sourcePosition = this.sourcePosition.toJSON(), e.targetPosition = this.targetPosition.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "MoveOperation";
  }
  /**
  * Creates `MoveOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, i) {
    const s = G.fromJSON(e.sourcePosition, i), r = G.fromJSON(e.targetPosition, i);
    return new this(s, e.howMany, r, e.baseVersion);
  }
}
class Ht extends Ii {
  /**
  * Creates an insert operation.
  *
  * @param position Position of insertion.
  * @param nodes The list of nodes to be inserted.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, i, s) {
    super(s);
    /**
    * Position of insertion.
    *
    * @readonly
    */
    f(this, "position");
    /**
    * List of nodes to insert.
    *
    * @readonly
    */
    f(this, "nodes");
    /**
    * Flag deciding how the operation should be transformed. If set to `true`, nodes might get additional attributes
    * during operational transformation. This happens when the operation insertion position is inside of a range
    * where attributes have changed.
    */
    f(this, "shouldReceiveAttributes");
    this.position = e.clone(), this.position.stickiness = "toNone", this.nodes = new jo(Q_(i)), this.shouldReceiveAttributes = !1;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "insert";
  }
  /**
  * Total offset size of inserted nodes.
  */
  get howMany() {
    return this.nodes.maxOffset;
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this.position.clone();
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    const e = new jo([
      ...this.nodes
    ].map((s) => s._clone(!0))), i = new Ht(this.position, e, this.baseVersion);
    return i.shouldReceiveAttributes = this.shouldReceiveAttributes, i;
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    const e = this.position.root.document.graveyard, i = new G(e, [
      0
    ]);
    return new He(this.position, this.nodes.maxOffset, i, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.position.parent;
    if (!e || e.maxOffset < this.position.offset)
      throw new S("insert-operation-position-invalid", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    const e = this.nodes;
    this.nodes = new jo([
      ...e
    ].map((i) => i._clone(!0))), Vf(this.position, e);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.position = this.position.toJSON(), e.nodes = this.nodes.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "InsertOperation";
  }
  /**
  * Creates `InsertOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, i) {
    const s = [];
    for (const o of e.nodes)
      o.name ? s.push(et.fromJSON(o)) : s.push(lt.fromJSON(o));
    const r = new Ht(G.fromJSON(e.position, i), s, e.baseVersion);
    return r.shouldReceiveAttributes = e.shouldReceiveAttributes, r;
  }
}
class tt extends Ii {
  /**
  * Creates a split operation.
  *
  * @param splitPosition Position at which an element should be split.
  * @param howMany Total offset size of elements that are in the split element after `position`.
  * @param insertionPosition Position at which the clone of split element (or element from graveyard) will be inserted.
  * @param graveyardPosition Position in the graveyard root before the element which
  * should be used as a parent of the nodes after `position`. If it is not set, a copy of the the `position` parent will be used.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, i, s, r, o) {
    super(o);
    /**
    * Position at which an element should be split.
    */
    f(this, "splitPosition");
    /**
    * Total offset size of elements that are in the split element after `position`.
    */
    f(this, "howMany");
    /**
    * Position at which the clone of split element (or element from graveyard) will be inserted.
    */
    f(this, "insertionPosition");
    /**
    * Position in the graveyard root before the element which should be used as a parent of the nodes after `position`.
    * If it is not set, a copy of the the `position` parent will be used.
    *
    * The default behavior is to clone the split element. Element from graveyard is used during undo.
    */
    f(this, "graveyardPosition");
    this.splitPosition = e.clone(), this.splitPosition.stickiness = "toNext", this.howMany = i, this.insertionPosition = s, this.graveyardPosition = r ? r.clone() : null, this.graveyardPosition && (this.graveyardPosition.stickiness = "toNext");
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "split";
  }
  /**
  * Position inside the new clone of a split element.
  *
  * This is a position where nodes that are after the split position will be moved to.
  */
  get moveTargetPosition() {
    const e = this.insertionPosition.path.slice();
    return e.push(0), new G(this.insertionPosition.root, e);
  }
  /**
  * Artificial range that contains all the nodes from the split element that will be moved to the new element.
  * The range starts at {@link #splitPosition} and ends in the same parent, at `POSITIVE_INFINITY` offset.
  */
  get movedRange() {
    const e = this.splitPosition.getShiftedBy(Number.POSITIVE_INFINITY);
    return new F(this.splitPosition, e);
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    const e = [
      F._createFromPositionAndShift(this.splitPosition, 0),
      F._createFromPositionAndShift(this.insertionPosition, 0)
    ];
    return this.graveyardPosition && e.push(F._createFromPositionAndShift(this.graveyardPosition, 0)), e;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  *
  * @returns Clone of this operation.
  */
  clone() {
    return new tt(this.splitPosition, this.howMany, this.insertionPosition, this.graveyardPosition, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    const e = this.splitPosition.root.document.graveyard, i = new G(e, [
      0
    ]);
    return new _t(this.moveTargetPosition, this.howMany, this.splitPosition, i, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.splitPosition.parent, i = this.splitPosition.offset;
    if (!e || e.maxOffset < i)
      throw new S("split-operation-position-invalid", this);
    if (e.parent) {
      if (this.howMany != e.maxOffset - this.splitPosition.offset)
        throw new S("split-operation-how-many-invalid", this);
      if (this.graveyardPosition && !this.graveyardPosition.nodeAfter)
        throw new S("split-operation-graveyard-position-invalid", this);
    } else throw new S("split-operation-split-in-root", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    const e = this.splitPosition.parent;
    if (this.graveyardPosition)
      la(F._createFromPositionAndShift(this.graveyardPosition, 1), this.insertionPosition);
    else {
      const s = e._clone();
      Vf(this.insertionPosition, s);
    }
    const i = new F(G._createAt(e, this.splitPosition.offset), G._createAt(e, e.maxOffset));
    la(i, this.moveTargetPosition);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.splitPosition = this.splitPosition.toJSON(), e.insertionPosition = this.insertionPosition.toJSON(), this.graveyardPosition && (e.graveyardPosition = this.graveyardPosition.toJSON()), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "SplitOperation";
  }
  /**
  * Helper function that returns a default insertion position basing on given `splitPosition`. The default insertion
  * position is after the split element.
  */
  static getInsertionPosition(e) {
    const i = e.path.slice(0, -1);
    return i[i.length - 1]++, new G(e.root, i, "toPrevious");
  }
  /**
  * Creates `SplitOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, i) {
    const s = G.fromJSON(e.splitPosition, i), r = G.fromJSON(e.insertionPosition, i), o = e.graveyardPosition ? G.fromJSON(e.graveyardPosition, i) : null;
    return new this(s, e.howMany, r, o, e.baseVersion);
  }
}
class _t extends Ii {
  /**
  * Creates a merge operation.
  *
  * @param sourcePosition Position inside the merged element. All nodes from that
  * element after that position will be moved to {@link #targetPosition}.
  * @param howMany Summary offset size of nodes which will be moved from the merged element to the new parent.
  * @param targetPosition Position which the nodes from the merged elements will be moved to.
  * @param graveyardPosition Position in graveyard to which the merged element will be moved.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, i, s, r, o) {
    super(o);
    /**
    * Position inside the merged element. All nodes from that element after that position will be moved to {@link #targetPosition}.
    */
    f(this, "sourcePosition");
    /**
    * Summary offset size of nodes which will be moved from the merged element to the new parent.
    */
    f(this, "howMany");
    /**
    * Position which the nodes from the merged elements will be moved to.
    */
    f(this, "targetPosition");
    /**
    * Position in graveyard to which the merged element will be moved.
    */
    f(this, "graveyardPosition");
    this.sourcePosition = e.clone(), this.sourcePosition.stickiness = "toPrevious", this.howMany = i, this.targetPosition = s.clone(), this.targetPosition.stickiness = "toNext", this.graveyardPosition = r.clone();
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "merge";
  }
  /**
  * Position before the merged element (which will be deleted).
  */
  get deletionPosition() {
    return new G(this.sourcePosition.root, this.sourcePosition.path.slice(0, -1));
  }
  /**
  * Artificial range that contains all the nodes from the merged element that will be moved to {@link ~MergeOperation#sourcePosition}.
  * The range starts at {@link ~MergeOperation#sourcePosition} and ends in the same parent, at `POSITIVE_INFINITY` offset.
  */
  get movedRange() {
    const e = this.sourcePosition.getShiftedBy(Number.POSITIVE_INFINITY);
    return new F(this.sourcePosition, e);
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    const e = this.sourcePosition.parent;
    return [
      F._createOn(e),
      // These could be positions but `Selectable` type only supports `Iterable<Range>`.
      F._createFromPositionAndShift(this.targetPosition, 0),
      F._createFromPositionAndShift(this.graveyardPosition, 0)
    ];
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new _t(this.sourcePosition, this.howMany, this.targetPosition, this.graveyardPosition, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    const e = this.targetPosition._getTransformedByMergeOperation(this), i = this.sourcePosition.path.slice(0, -1), s = new G(this.sourcePosition.root, i)._getTransformedByMergeOperation(this);
    return new tt(e, this.howMany, s, this.graveyardPosition, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.sourcePosition.parent, i = this.targetPosition.parent;
    if (e.parent)
      if (i.parent) {
        if (this.howMany != e.maxOffset)
          throw new S("merge-operation-how-many-invalid", this);
      } else throw new S("merge-operation-target-position-invalid", this);
    else throw new S("merge-operation-source-position-invalid", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    const e = this.sourcePosition.parent, i = F._createIn(e);
    la(i, this.targetPosition), la(F._createOn(e), this.graveyardPosition);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.sourcePosition = e.sourcePosition.toJSON(), e.targetPosition = e.targetPosition.toJSON(), e.graveyardPosition = e.graveyardPosition.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "MergeOperation";
  }
  /**
  * Creates `MergeOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, i) {
    const s = G.fromJSON(e.sourcePosition, i), r = G.fromJSON(e.targetPosition, i), o = G.fromJSON(e.graveyardPosition, i);
    return new this(s, e.howMany, r, o, e.baseVersion);
  }
}
class si extends Ii {
  /**
  * @param name Marker name.
  * @param oldRange Marker range before the change.
  * @param newRange Marker range after the change.
  * @param markers Marker collection on which change should be executed.
  * @param affectsData Specifies whether the marker operation affects the data produced by the data pipeline
  * (is persisted in the editor's data).
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, i, s, r, o, a) {
    super(a);
    /**
    * Marker name.
    *
    * @readonly
    */
    f(this, "name");
    /**
    * Marker range before the change.
    *
    * @readonly
    */
    f(this, "oldRange");
    /**
    * Marker range after the change.
    *
    * @readonly
    */
    f(this, "newRange");
    /**
    * Specifies whether the marker operation affects the data produced by the data pipeline
    * (is persisted in the editor's data).
    *
    * @readonly
    */
    f(this, "affectsData");
    /**
    * Marker collection on which change should be executed.
    */
    f(this, "_markers");
    this.name = e, this.oldRange = i ? i.clone() : null, this.newRange = s ? s.clone() : null, this.affectsData = o, this._markers = r;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "marker";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    const e = [];
    return this.oldRange && e.push(this.oldRange.clone()), this.newRange && (this.oldRange ? e.push(...this.newRange.getDifference(this.oldRange)) : e.push(this.newRange.clone())), e;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new si(this.name, this.oldRange, this.newRange, this._markers, this.affectsData, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new si(this.name, this.newRange, this.oldRange, this._markers, this.affectsData, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    this.newRange ? this._markers._set(this.name, this.newRange, !0, this.affectsData) : this._markers._remove(this.name);
  }
  /**
  * @inheritDoc
  * @internal
  */
  toJSON() {
    const e = super.toJSON();
    return this.oldRange && (e.oldRange = this.oldRange.toJSON()), this.newRange && (e.newRange = this.newRange.toJSON()), delete e._markers, e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "MarkerOperation";
  }
  /**
  * Creates `MarkerOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, i) {
    return new si(e.name, e.oldRange ? F.fromJSON(e.oldRange, i) : null, e.newRange ? F.fromJSON(e.newRange, i) : null, i.model.markers, e.affectsData, e.baseVersion);
  }
}
class Ct extends Ii {
  /**
  * Creates an operation that changes, removes or adds attributes.
  *
  * If only `newValue` is set, attribute will be added on a node. Note that all nodes in operation's range must not
  * have an attribute with the same key as the added attribute.
  *
  * If only `oldValue` is set, then attribute with given key will be removed. Note that all nodes in operation's range
  * must have an attribute with that key added.
  *
  * If both `newValue` and `oldValue` are set, then the operation will change the attribute value. Note that all nodes in
  * operation's ranges must already have an attribute with given key and `oldValue` as value
  *
  * @param range Range on which the operation should be applied. Must be a flat range.
  * @param key Key of an attribute to change or remove.
  * @param oldValue Old value of the attribute with given key or `null`, if attribute was not set before.
  * @param newValue New value of the attribute with given key or `null`, if operation should remove attribute.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, i, s, r, o) {
    super(o);
    /**
    * Range on which operation should be applied.
    *
    * @readonly
    */
    f(this, "range");
    /**
    * Key of an attribute to change or remove.
    *
    * @readonly
    */
    f(this, "key");
    /**
    * Old value of the attribute with given key or `null`, if attribute was not set before.
    *
    * @readonly
    */
    f(this, "oldValue");
    /**
    * New value of the attribute with given key or `null`, if operation should remove attribute.
    *
    * @readonly
    */
    f(this, "newValue");
    this.range = e.clone(), this.key = i, this.oldValue = s === void 0 ? null : s, this.newValue = r === void 0 ? null : r;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return this.oldValue === null ? "addAttribute" : this.newValue === null ? "removeAttribute" : "changeAttribute";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this.range.clone();
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new Ct(this.range, this.key, this.oldValue, this.newValue, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new Ct(this.range, this.key, this.newValue, this.oldValue, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.range = this.range.toJSON(), e;
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    if (!this.range.isFlat)
      throw new S("attribute-operation-range-not-flat", this);
    for (const e of this.range.getItems({
      shallow: !0
    })) {
      if (this.oldValue !== null && !Dr(e.getAttribute(this.key), this.oldValue))
        throw new S("attribute-operation-wrong-old-value", this, {
          item: e,
          key: this.key,
          value: this.oldValue
        });
      if (this.oldValue === null && this.newValue !== null && e.hasAttribute(this.key))
        throw new S("attribute-operation-attribute-exists", this, {
          node: e,
          key: this.key
        });
    }
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    Dr(this.oldValue, this.newValue) || tO(this.range, this.key, this.newValue);
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "AttributeOperation";
  }
  /**
  * Creates `AttributeOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, i) {
    return new Ct(F.fromJSON(e.range, i), e.key, e.oldValue, e.newValue, e.baseVersion);
  }
}
class Lt extends Ii {
  get type() {
    return "noop";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return null;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  */
  clone() {
    return new Lt(this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new Lt(this.baseVersion + 1);
  }
  /** @internal */
  _execute() {
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "NoOperation";
  }
}
class bi extends Ii {
  /**
  * Creates an operation that changes element's name.
  *
  * @param position Position before an element to change.
  * @param oldName Current name of the element.
  * @param newName New name for the element.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, i, s, r) {
    super(r);
    /**
    * Position before an element to change.
    */
    f(this, "position");
    /**
    * Current name of the element.
    */
    f(this, "oldName");
    /**
    * New name for the element.
    */
    f(this, "newName");
    this.position = e, this.position.stickiness = "toNext", this.oldName = i, this.newName = s;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "rename";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this.position.nodeAfter;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  *
  * @returns Clone of this operation.
  */
  clone() {
    return new bi(this.position.clone(), this.oldName, this.newName, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new bi(this.position.clone(), this.newName, this.oldName, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    const e = this.position.nodeAfter;
    if (e instanceof et) {
      if (e.name !== this.oldName)
        throw new S("rename-operation-wrong-name", this);
    } else throw new S("rename-operation-wrong-position", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    const e = this.position.nodeAfter;
    e.name = this.newName;
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.position = this.position.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "RenameOperation";
  }
  /**
  * Creates `RenameOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, i) {
    return new bi(G.fromJSON(e.position, i), e.oldName, e.newName, e.baseVersion);
  }
}
class $n extends Ii {
  /**
  * Creates an operation that changes, removes or adds attributes on root element.
  *
  * @see module:engine/model/operation/attributeoperation~AttributeOperation
  * @param root Root element to change.
  * @param key Key of an attribute to change or remove.
  * @param oldValue Old value of the attribute with given key or `null`, if attribute was not set before.
  * @param newValue New value of the attribute with given key or `null`, if operation should remove attribute.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation
  * can be applied or `null` if the operation operates on detached (non-document) tree.
  */
  constructor(e, i, s, r, o) {
    super(o);
    /**
    * Root element to change.
    */
    f(this, "root");
    /**
    * Key of an attribute to change or remove.
    */
    f(this, "key");
    /**
    * Old value of the attribute with given key or `null`, if attribute was not set before.
    *
    * @readonly
    */
    f(this, "oldValue");
    /**
    * New value of the attribute with given key or `null`, if operation should remove attribute.
    *
    * @readonly
    */
    f(this, "newValue");
    this.root = e, this.key = i, this.oldValue = s === void 0 ? null : s, this.newValue = r === void 0 ? null : r;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return this.oldValue === null ? "addRootAttribute" : this.newValue === null ? "removeRootAttribute" : "changeRootAttribute";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this.root;
  }
  /**
  * Creates and returns an operation that has the same parameters as this operation.
  *
  * @returns Clone of this operation.
  */
  clone() {
    return new $n(this.root, this.key, this.oldValue, this.newValue, this.baseVersion);
  }
  /**
  * See {@link module:engine/model/operation/operation~Operation#getReversed `Operation#getReversed()`}.
  */
  getReversed() {
    return new $n(this.root, this.key, this.newValue, this.oldValue, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    if (this.root != this.root.root || this.root.is("documentFragment"))
      throw new S("rootattribute-operation-not-a-root", this, {
        root: this.root,
        key: this.key
      });
    if (this.oldValue !== null && this.root.getAttribute(this.key) !== this.oldValue)
      throw new S("rootattribute-operation-wrong-old-value", this, {
        root: this.root,
        key: this.key
      });
    if (this.oldValue === null && this.newValue !== null && this.root.hasAttribute(this.key))
      throw new S("rootattribute-operation-attribute-exists", this, {
        root: this.root,
        key: this.key
      });
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    this.newValue !== null ? this.root._setAttribute(this.key, this.newValue) : this.root._removeAttribute(this.key);
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.root = this.root.toJSON(), e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "RootAttributeOperation";
  }
  /**
  * Creates `RootAttributeOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, i) {
    if (!i.getRoot(e.root))
      throw new S("rootattribute-operation-fromjson-no-root", this, {
        rootName: e.root
      });
    return new $n(i.getRoot(e.root), e.key, e.oldValue, e.newValue, e.baseVersion);
  }
}
class bn extends Ii {
  /**
  * Creates an operation that creates or removes a root element.
  *
  * @param rootName Root name to create or detach.
  * @param elementName Root element name.
  * @param isAdd Specifies whether the operation adds (`true`) or detaches the root (`false`).
  * @param document Document which owns the root.
  * @param baseVersion Document {@link module:engine/model/document~Document#version} on which operation can be applied.
  */
  constructor(e, i, s, r, o) {
    super(o);
    /**
    * Root name to create or detach.
    */
    f(this, "rootName");
    /**
    * Root element name.
    */
    f(this, "elementName");
    /**
    * Specifies whether the operation adds (`true`) or detaches the root (`false`).
    */
    f(this, "isAdd");
    /**
    * Document which owns the root.
    */
    f(this, "_document");
    if (this.rootName = e, this.elementName = i, this.isAdd = s, this._document = r, !this._document.getRoot(this.rootName)) {
      const a = this._document.createRoot(this.elementName, this.rootName);
      a._isAttached = !1;
    }
  }
  /**
  * @inheritDoc
  */
  get type() {
    return this.isAdd ? "addRoot" : "detachRoot";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return this._document.getRoot(this.rootName);
  }
  /**
  * @inheritDoc
  */
  clone() {
    return new bn(this.rootName, this.elementName, this.isAdd, this._document, this.baseVersion);
  }
  /**
  * @inheritDoc
  */
  getReversed() {
    return new bn(this.rootName, this.elementName, !this.isAdd, this._document, this.baseVersion + 1);
  }
  /**
  * @inheritDoc
  */
  _execute() {
    this._document.getRoot(this.rootName)._isAttached = this.isAdd;
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return delete e._document, e;
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "RootOperation";
  }
  /**
  * Creates `RootOperation` object from deserialized object, i.e. from parsed JSON string.
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(e, i) {
    return new bn(e.rootName, e.elementName, e.isAdd, i, e.baseVersion);
  }
}
const $i = {};
$i[Ct.className] = Ct;
$i[Ht.className] = Ht;
$i[si.className] = si;
$i[He.className] = He;
$i[Lt.className] = Lt;
$i[Ii.className] = Ii;
$i[bi.className] = bi;
$i[$n.className] = $n;
$i[bn.className] = bn;
$i[tt.className] = tt;
$i[_t.className] = _t;
class iO {
  /**
  * Creates an operation instance from a JSON object (parsed JSON string).
  *
  * @param json Deserialized JSON object.
  * @param document Document on which this operation will be applied.
  */
  static fromJSON(t, e) {
    return $i[t.__className].fromJSON(t, e);
  }
}
const hh = /* @__PURE__ */ new Map();
function $e(n, t, e) {
  let i = hh.get(n);
  i || (i = /* @__PURE__ */ new Map(), hh.set(n, i)), i.set(t, e);
}
function nO(n, t) {
  const e = hh.get(n);
  return e && e.has(t) ? e.get(t) : sO;
}
function sO(n) {
  return [
    n
  ];
}
function $p(n, t, e = {}) {
  const i = nO(n.constructor, t.constructor);
  try {
    return n = n.clone(), i(n, t, e);
  } catch (s) {
    throw s;
  }
}
function rO(n, t, e) {
  n = n.slice(), t = t.slice();
  const i = new oO(e.document, e.useRelations, e.forceWeakRemove);
  i.setOriginalOperations(n), i.setOriginalOperations(t);
  const s = i.originalOperations;
  if (n.length == 0 || t.length == 0)
    return {
      operationsA: n,
      operationsB: t,
      originalOperations: s
    };
  const r = /* @__PURE__ */ new WeakMap();
  for (const l of n)
    r.set(l, 0);
  const o = {
    nextBaseVersionA: n[n.length - 1].baseVersion + 1,
    nextBaseVersionB: t[t.length - 1].baseVersion + 1,
    originalOperationsACount: n.length,
    originalOperationsBCount: t.length
  };
  let a = 0;
  for (; a < n.length; ) {
    const l = n[a], c = r.get(l);
    if (c == t.length) {
      a++;
      continue;
    }
    const u = t[c], d = $p(l, u, i.getContext(l, u, !0)), h = $p(u, l, i.getContext(u, l, !1));
    i.updateRelation(l, u), i.setOriginalOperations(d, l), i.setOriginalOperations(h, u);
    for (const m of d)
      r.set(m, c + h.length);
    n.splice(a, 1, ...d), t.splice(c, 1, ...h);
  }
  return Hp(n), Hp(t), zp(n, o.nextBaseVersionB), zp(t, o.nextBaseVersionA), {
    operationsA: n,
    operationsB: t,
    originalOperations: s
  };
}
class oO {
  /**
  * Creates `ContextFactory` instance.
  *
  * @param document Document which the operations change.
  * @param useRelations Whether during transformation relations should be used (used during undo for
  * better conflict resolution).
  * @param forceWeakRemove If set to `false`, remove operation will be always stronger than move operation,
  * so the removed nodes won't end up back in the document root. When set to `true`, context data will be used.
  */
  constructor(t, e, i = !1) {
    f(this, "originalOperations");
    f(this, "_history");
    f(this, "_useRelations");
    f(this, "_forceWeakRemove");
    f(this, "_relations");
    this.originalOperations = /* @__PURE__ */ new Map(), this._history = t.history, this._useRelations = e, this._forceWeakRemove = !!i, this._relations = /* @__PURE__ */ new Map();
  }
  /**
  * Sets "original operation" for given operations.
  *
  * During transformation process, operations are cloned, then changed, then processed again, sometimes broken into two
  * or multiple operations. When gathering additional data it is important that all operations can be somehow linked
  * so a cloned and transformed "version" still kept track of the data assigned earlier to it.
  *
  * The original operation object will be used as such an universal linking id. Throughout the transformation process
  * all cloned operations will refer to "the original operation" when storing and reading additional data.
  *
  * If `takeFrom` is not set, each operation from `operations` array will be assigned itself as "the original operation".
  * This should be used as an initialization step.
  *
  * If `takeFrom` is set, each operation from `operations` will be assigned the same original operation as assigned
  * for `takeFrom` operation. This should be used to update original operations. It should be used in a way that
  * `operations` are the result of `takeFrom` transformation to ensure proper "original operation propagation".
  */
  setOriginalOperations(t, e = null) {
    const i = e ? this.originalOperations.get(e) : null;
    for (const s of t)
      this.originalOperations.set(s, i || s);
  }
  /**
  * Saves a relation between operations `opA` and `opB`.
  *
  * Relations are then later used to help solve conflicts when operations are transformed.
  */
  updateRelation(t, e) {
    if (t instanceof He)
      e instanceof _t ? t.targetPosition.isEqual(e.sourcePosition) || e.movedRange.containsPosition(t.targetPosition) ? this._setRelation(t, e, "insertAtSource") : t.targetPosition.isEqual(e.deletionPosition) ? this._setRelation(t, e, "insertBetween") : t.targetPosition.isAfter(e.sourcePosition) && this._setRelation(t, e, "moveTargetAfter") : e instanceof He && (t.targetPosition.isEqual(e.sourcePosition) || t.targetPosition.isBefore(e.sourcePosition) ? this._setRelation(t, e, "insertBefore") : this._setRelation(t, e, "insertAfter"));
    else if (t instanceof tt) {
      if (e instanceof _t)
        t.splitPosition.isBefore(e.sourcePosition) && this._setRelation(t, e, "splitBefore");
      else if (e instanceof He)
        if (t.splitPosition.isEqual(e.sourcePosition) || t.splitPosition.isBefore(e.sourcePosition))
          this._setRelation(t, e, "splitBefore");
        else {
          const i = F._createFromPositionAndShift(e.sourcePosition, e.howMany);
          if (t.splitPosition.hasSameParentAs(e.sourcePosition) && i.containsPosition(t.splitPosition)) {
            const s = i.end.offset - t.splitPosition.offset, r = t.splitPosition.offset - i.start.offset;
            this._setRelation(t, e, {
              howMany: s,
              offset: r
            });
          }
        }
    } else if (t instanceof _t)
      e instanceof _t ? (t.targetPosition.isEqual(e.sourcePosition) || this._setRelation(t, e, "mergeTargetNotMoved"), t.sourcePosition.isEqual(e.targetPosition) && this._setRelation(t, e, "mergeSourceNotMoved"), t.sourcePosition.isEqual(e.sourcePosition) && this._setRelation(t, e, "mergeSameElement")) : e instanceof tt ? t.sourcePosition.isEqual(e.splitPosition) && this._setRelation(t, e, "splitAtSource") : e instanceof He && e.howMany > 0 && (t.sourcePosition.isEqual(e.sourcePosition.getShiftedBy(e.howMany)) && this._setRelation(t, e, "mergeSourceAffected"), t.targetPosition.isEqual(e.sourcePosition) && this._setRelation(t, e, "mergeTargetWasBefore"));
    else if (t instanceof si) {
      const i = t.newRange;
      if (!i)
        return;
      if (e instanceof _t) {
        const s = i.start.isEqual(e.targetPosition), r = i.start.isEqual(e.deletionPosition), o = i.end.isEqual(e.deletionPosition), a = i.end.isEqual(e.sourcePosition);
        (s || r || o || a) && this._setRelation(t, e, {
          wasInLeftElement: s,
          wasStartBeforeMergedElement: r,
          wasEndBeforeMergedElement: o,
          wasInRightElement: a
        });
      }
    }
  }
  /**
  * Evaluates and returns contextual information about two given operations `opA` and `opB` which are about to be transformed.
  */
  getContext(t, e, i) {
    return {
      aIsStrong: i,
      aWasUndone: this._wasUndone(t),
      bWasUndone: this._wasUndone(e),
      abRelation: this._useRelations ? this._getRelation(t, e) : null,
      baRelation: this._useRelations ? this._getRelation(e, t) : null,
      forceWeakRemove: this._forceWeakRemove
    };
  }
  /**
  * Returns whether given operation `op` has already been undone.
  *
  * Information whether an operation was undone gives more context when making a decision when two operations are in conflict.
  */
  _wasUndone(t) {
    const e = this.originalOperations.get(t);
    return e.wasUndone || this._history.isUndoneOperation(e);
  }
  /**
  * Returns a relation between `opA` and an operation which is undone by `opB`. This can be `String` value if a relation
  * was set earlier or `null` if there was no relation between those operations.
  *
  * This is a little tricky to understand, so let's compare it to `ContextFactory#_wasUndone`.
  *
  * When `wasUndone( opB )` is used, we check if the `opB` has already been undone. It is obvious, that the
  * undoing operation must happen after the undone operation. So, essentially, we have `opB`, we take document history,
  * we look forward in the future and ask if in that future `opB` was undone.
  *
  * Relations is a backward process to `wasUndone()`.
  *
  * Long story short - using relations is asking what happened in the past. Looking back. This time we have an undoing
  * operation `opB` which has undone some other operation. When there is a transformation `opA` x `opB` and there is
  * a conflict to solve and `opB` is an undoing operation, we can look back in the history and see what was a relation
  * between `opA` and the operation which `opB` undone. Basing on that relation from the past, we can now make
  * a better decision when resolving a conflict between two operations, because we know more about the context of
  * those two operations.
  *
  * This is why this function does not return a relation directly between `opA` and `opB` because we need to look
  * back to search for a meaningful contextual information.
  */
  _getRelation(t, e) {
    const i = this.originalOperations.get(e), s = this._history.getUndoneOperation(i);
    if (!s)
      return null;
    const r = this.originalOperations.get(t), o = this._relations.get(r);
    return o && o.get(s) || null;
  }
  /**
  * Helper function for `ContextFactory#updateRelations`.
  */
  _setRelation(t, e, i) {
    const s = this.originalOperations.get(t), r = this.originalOperations.get(e);
    let o = this._relations.get(s);
    o || (o = /* @__PURE__ */ new Map(), this._relations.set(s, o)), o.set(r, i);
  }
}
function zp(n, t) {
  for (const e of n)
    e.baseVersion = t++;
}
function Hp(n) {
  const t = /* @__PURE__ */ new Map();
  for (let e = 0; e < n.length; e++) {
    const i = n[e];
    i instanceof si && (i.baseVersion !== -1 ? t.set(i.name, {
      op: i,
      ranges: i.newRange ? [
        i.newRange
      ] : []
    }) : (i.newRange && t.get(i.name).ranges.push(i.newRange), n.splice(e, 1), e--));
  }
  for (const { op: e, ranges: i } of t.values())
    i.length ? e.newRange = F._createFromRanges(i) : e.newRange = null;
}
$e(Ct, Ct, (n, t, e) => {
  if (n.key === t.key && n.range.start.hasSameParentAs(t.range.start)) {
    const i = n.range.getDifference(t.range).map((r) => new Ct(r, n.key, n.oldValue, n.newValue, 0)), s = n.range.getIntersection(t.range);
    return s && e.aIsStrong && i.push(new Ct(s, t.key, t.newValue, n.newValue, 0)), i.length == 0 ? [
      new Lt(0)
    ] : i;
  } else
    return [
      n
    ];
});
$e(Ct, Ht, (n, t) => {
  if (n.range.start.hasSameParentAs(t.position) && n.range.containsPosition(t.position)) {
    const i = n.range._getTransformedByInsertion(t.position, t.howMany, !t.shouldReceiveAttributes).map((s) => new Ct(s, n.key, n.oldValue, n.newValue, n.baseVersion));
    if (t.shouldReceiveAttributes) {
      const s = ty(t, n.key, n.oldValue);
      s && i.unshift(s);
    }
    return i;
  }
  return n.range = n.range._getTransformedByInsertion(t.position, t.howMany, !1)[0], [
    n
  ];
});
function ty(n, t, e) {
  const s = n.nodes.getNode(0).getAttribute(t);
  if (s == e)
    return null;
  const r = new F(n.position, n.position.getShiftedBy(n.howMany));
  return new Ct(r, t, s, e, 0);
}
$e(Ct, _t, (n, t) => {
  const e = [];
  n.range.start.hasSameParentAs(t.deletionPosition) && (n.range.containsPosition(t.deletionPosition) || n.range.start.isEqual(t.deletionPosition)) && e.push(F._createFromPositionAndShift(t.graveyardPosition, 1));
  const i = n.range._getTransformedByMergeOperation(t);
  return i.isCollapsed || e.push(i), e.map((s) => new Ct(s, n.key, n.oldValue, n.newValue, n.baseVersion));
});
$e(Ct, He, (n, t) => aO(n.range, t).map((i) => new Ct(i, n.key, n.oldValue, n.newValue, n.baseVersion)));
function aO(n, t) {
  const e = F._createFromPositionAndShift(t.sourcePosition, t.howMany);
  let i = null, s = [];
  e.containsRange(n, !0) ? i = n : n.start.hasSameParentAs(e.start) ? (s = n.getDifference(e), i = n.getIntersection(e)) : s = [
    n
  ];
  const r = [];
  for (let o of s) {
    o = o._getTransformedByDeletion(t.sourcePosition, t.howMany);
    const a = t.getMovedRangeStart(), l = o.start.hasSameParentAs(a), c = o._getTransformedByInsertion(a, t.howMany, l);
    r.push(...c);
  }
  return i && r.push(i._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany, !1)[0]), r;
}
$e(Ct, tt, (n, t) => {
  if (n.range.end.isEqual(t.insertionPosition))
    return t.graveyardPosition || n.range.end.offset++, [
      n
    ];
  if (n.range.start.hasSameParentAs(t.splitPosition) && n.range.containsPosition(t.splitPosition)) {
    const e = n.clone();
    return e.range = new F(t.moveTargetPosition.clone(), n.range.end._getCombined(t.splitPosition, t.moveTargetPosition)), n.range.end = t.splitPosition.clone(), n.range.end.stickiness = "toPrevious", [
      n,
      e
    ];
  }
  return n.range = n.range._getTransformedBySplitOperation(t), [
    n
  ];
});
$e(Ht, Ct, (n, t) => {
  const e = [
    n
  ];
  if (n.shouldReceiveAttributes && n.position.hasSameParentAs(t.range.start) && t.range.containsPosition(n.position)) {
    const i = ty(n, t.key, t.newValue);
    i && e.push(i);
  }
  return e;
});
$e(Ht, Ht, (n, t, e) => n.position.isEqual(t.position) && e.aIsStrong ? [
  n
] : (n.position = n.position._getTransformedByInsertOperation(t), [
  n
]));
$e(Ht, He, (n, t) => (n.position = n.position._getTransformedByMoveOperation(t), [
  n
]));
$e(Ht, tt, (n, t) => (n.position = n.position._getTransformedBySplitOperation(t), [
  n
]));
$e(Ht, _t, (n, t) => (n.position = n.position._getTransformedByMergeOperation(t), [
  n
]));
$e(si, Ht, (n, t) => (n.oldRange && (n.oldRange = n.oldRange._getTransformedByInsertOperation(t)[0]), n.newRange && (n.newRange = n.newRange._getTransformedByInsertOperation(t)[0]), [
  n
]));
$e(si, si, (n, t, e) => {
  if (n.name == t.name)
    if (e.aIsStrong)
      n.oldRange = t.newRange ? t.newRange.clone() : null;
    else
      return [
        new Lt(0)
      ];
  return [
    n
  ];
});
$e(si, _t, (n, t) => (n.oldRange && (n.oldRange = n.oldRange._getTransformedByMergeOperation(t)), n.newRange && (n.newRange = n.newRange._getTransformedByMergeOperation(t)), [
  n
]));
$e(si, He, (n, t) => {
  const e = [
    n
  ];
  if (n.oldRange && (n.oldRange = F._createFromRanges(n.oldRange._getTransformedByMoveOperation(t))), n.newRange) {
    const i = n.newRange._getTransformedByMoveOperation(t);
    n.newRange = i[0];
    for (let s = 1; s < i.length; s++) {
      const r = n.clone();
      r.oldRange = null, r.newRange = i[s], r.baseVersion = -1, e.push(r);
    }
  }
  return e;
});
$e(si, tt, (n, t, e) => {
  if (n.oldRange && (n.oldRange = n.oldRange._getTransformedBySplitOperation(t)), n.newRange) {
    if (e.abRelation) {
      const i = n.newRange._getTransformedBySplitOperation(t);
      return n.newRange.start.isEqual(t.splitPosition) && e.abRelation.wasStartBeforeMergedElement ? n.newRange.start = G._createAt(t.insertionPosition) : n.newRange.start.isEqual(t.splitPosition) && !e.abRelation.wasInLeftElement ? n.newRange.start = G._createAt(t.moveTargetPosition) : n.newRange.start = i.start, n.newRange.end.isEqual(t.splitPosition) && e.abRelation.wasInRightElement ? n.newRange.end = G._createAt(t.moveTargetPosition) : n.newRange.end.isEqual(t.splitPosition) && e.abRelation.wasEndBeforeMergedElement ? n.newRange.end = G._createAt(t.insertionPosition) : n.newRange.end = i.end, [
        n
      ];
    }
    n.newRange = n.newRange._getTransformedBySplitOperation(t);
  }
  return [
    n
  ];
});
$e(_t, Ht, (n, t) => (n.sourcePosition.hasSameParentAs(t.position) && (n.howMany += t.howMany), n.sourcePosition = n.sourcePosition._getTransformedByInsertOperation(t), n.targetPosition = n.targetPosition._getTransformedByInsertOperation(t), [
  n
]));
$e(_t, _t, (n, t, e) => {
  if (n.sourcePosition.isEqual(t.sourcePosition) && n.targetPosition.isEqual(t.targetPosition))
    if (e.bWasUndone) {
      const i = t.graveyardPosition.path.slice();
      return i.push(0), n.sourcePosition = new G(t.graveyardPosition.root, i), n.howMany = 0, [
        n
      ];
    } else
      return [
        new Lt(0)
      ];
  if (n.sourcePosition.isEqual(t.sourcePosition) && !n.targetPosition.isEqual(t.targetPosition) && !e.bWasUndone && e.abRelation != "splitAtSource") {
    const i = n.targetPosition.root.rootName == "$graveyard", s = t.targetPosition.root.rootName == "$graveyard";
    if (s && !i || !(i && !s) && e.aIsStrong) {
      const l = t.targetPosition._getTransformedByMergeOperation(t), c = n.targetPosition._getTransformedByMergeOperation(t);
      return [
        new He(l, n.howMany, c, 0)
      ];
    } else
      return [
        new Lt(0)
      ];
  }
  return n.sourcePosition.hasSameParentAs(t.targetPosition) && (n.howMany += t.howMany), n.sourcePosition = n.sourcePosition._getTransformedByMergeOperation(t), n.targetPosition = n.targetPosition._getTransformedByMergeOperation(t), (!n.graveyardPosition.isEqual(t.graveyardPosition) || !e.aIsStrong) && (n.graveyardPosition = n.graveyardPosition._getTransformedByMergeOperation(t)), [
    n
  ];
});
$e(_t, He, (n, t, e) => {
  const i = F._createFromPositionAndShift(t.sourcePosition, t.howMany);
  return t.type == "remove" && !e.bWasUndone && n.deletionPosition.hasSameParentAs(t.sourcePosition) && i.containsPosition(n.sourcePosition) ? [
    new Lt(0)
  ] : (t.sourcePosition.getShiftedBy(t.howMany).isEqual(n.sourcePosition) ? n.sourcePosition.stickiness = "toNone" : t.targetPosition.isEqual(n.sourcePosition) && e.abRelation == "mergeSourceAffected" ? n.sourcePosition.stickiness = "toNext" : t.sourcePosition.isEqual(n.targetPosition) ? (n.targetPosition.stickiness = "toNone", n.howMany -= t.howMany) : t.targetPosition.isEqual(n.targetPosition) && e.abRelation == "mergeTargetWasBefore" ? (n.targetPosition.stickiness = "toPrevious", n.howMany += t.howMany) : (n.sourcePosition.hasSameParentAs(t.targetPosition) && (n.howMany += t.howMany), n.sourcePosition.hasSameParentAs(t.sourcePosition) && (n.howMany -= t.howMany)), n.sourcePosition = n.sourcePosition._getTransformedByMoveOperation(t), n.targetPosition = n.targetPosition._getTransformedByMoveOperation(t), n.sourcePosition.stickiness = "toPrevious", n.targetPosition.stickiness = "toNext", n.graveyardPosition.isEqual(t.targetPosition) || (n.graveyardPosition = n.graveyardPosition._getTransformedByMoveOperation(t)), [
    n
  ]);
});
$e(_t, tt, (n, t, e) => {
  if (t.graveyardPosition && (n.graveyardPosition = n.graveyardPosition._getTransformedByDeletion(t.graveyardPosition, 1), n.deletionPosition.isEqual(t.graveyardPosition) && (n.howMany = t.howMany)), n.targetPosition.isEqual(t.splitPosition) && (t.graveyardPosition && n.deletionPosition.isEqual(t.graveyardPosition) || e.abRelation == "mergeTargetNotMoved"))
    return n.sourcePosition = n.sourcePosition._getTransformedBySplitOperation(t), [
      n
    ];
  if (n.sourcePosition.isEqual(t.splitPosition)) {
    if (e.abRelation == "mergeSourceNotMoved")
      return n.howMany = 0, n.targetPosition = n.targetPosition._getTransformedBySplitOperation(t), [
        n
      ];
    if (e.abRelation == "mergeSameElement" || n.sourcePosition.offset > 0)
      return n.sourcePosition = t.moveTargetPosition.clone(), n.targetPosition = n.targetPosition._getTransformedBySplitOperation(t), [
        n
      ];
  }
  return n.sourcePosition.hasSameParentAs(t.splitPosition) && (n.howMany = t.splitPosition.offset), n.sourcePosition = n.sourcePosition._getTransformedBySplitOperation(t), n.targetPosition = n.targetPosition._getTransformedBySplitOperation(t), [
    n
  ];
});
$e(He, Ht, (n, t) => {
  const i = F._createFromPositionAndShift(n.sourcePosition, n.howMany)._getTransformedByInsertOperation(t, !1)[0];
  return n.sourcePosition = i.start, n.howMany = i.end.offset - i.start.offset, n.targetPosition.isEqual(t.position) || (n.targetPosition = n.targetPosition._getTransformedByInsertOperation(t)), [
    n
  ];
});
$e(He, He, (n, t, e) => {
  const i = F._createFromPositionAndShift(n.sourcePosition, n.howMany), s = F._createFromPositionAndShift(t.sourcePosition, t.howMany);
  let r = e.aIsStrong, o = !e.aIsStrong;
  e.abRelation == "insertBefore" || e.baRelation == "insertAfter" ? o = !0 : (e.abRelation == "insertAfter" || e.baRelation == "insertBefore") && (o = !1);
  let a;
  if (n.targetPosition.isEqual(t.targetPosition) && o ? a = n.targetPosition._getTransformedByDeletion(t.sourcePosition, t.howMany) : a = n.targetPosition._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), jp(n, t) && jp(t, n))
    return [
      t.getReversed()
    ];
  if (i.containsPosition(t.targetPosition) && i.containsRange(s, !0))
    return i.start = i.start._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), i.end = i.end._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), Pr([
      i
    ], a);
  if (s.containsPosition(n.targetPosition) && s.containsRange(i, !0))
    return i.start = i.start._getCombined(t.sourcePosition, t.getMovedRangeStart()), i.end = i.end._getCombined(t.sourcePosition, t.getMovedRangeStart()), Pr([
      i
    ], a);
  const u = Zt(n.sourcePosition.getParentPath(), t.sourcePosition.getParentPath());
  if (u == "prefix" || u == "extension")
    return i.start = i.start._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), i.end = i.end._getTransformedByMove(t.sourcePosition, t.targetPosition, t.howMany), Pr([
      i
    ], a);
  n.type == "remove" && t.type != "remove" && !e.aWasUndone && !e.forceWeakRemove ? r = !0 : n.type != "remove" && t.type == "remove" && !e.bWasUndone && !e.forceWeakRemove && (r = !1);
  const d = [], h = i.getDifference(s);
  for (const g of h) {
    g.start = g.start._getTransformedByDeletion(t.sourcePosition, t.howMany), g.end = g.end._getTransformedByDeletion(t.sourcePosition, t.howMany);
    const p = Zt(g.start.getParentPath(), t.getMovedRangeStart().getParentPath()) == "same", w = g._getTransformedByInsertion(t.getMovedRangeStart(), t.howMany, p);
    d.push(...w);
  }
  const m = i.getIntersection(s);
  return m !== null && r && (m.start = m.start._getCombined(t.sourcePosition, t.getMovedRangeStart()), m.end = m.end._getCombined(t.sourcePosition, t.getMovedRangeStart()), d.length === 0 ? d.push(m) : d.length == 1 ? s.start.isBefore(i.start) || s.start.isEqual(i.start) ? d.unshift(m) : d.push(m) : d.splice(1, 0, m)), d.length === 0 ? [
    new Lt(n.baseVersion)
  ] : Pr(d, a);
});
$e(He, tt, (n, t, e) => {
  let i = n.targetPosition.clone();
  (!n.targetPosition.isEqual(t.insertionPosition) || !t.graveyardPosition || e.abRelation == "moveTargetAfter") && (i = n.targetPosition._getTransformedBySplitOperation(t));
  const s = F._createFromPositionAndShift(n.sourcePosition, n.howMany);
  if (s.end.isEqual(t.insertionPosition))
    return t.graveyardPosition || n.howMany++, n.targetPosition = i, [
      n
    ];
  if (s.start.hasSameParentAs(t.splitPosition) && s.containsPosition(t.splitPosition)) {
    let a = new F(t.splitPosition, s.end);
    a = a._getTransformedBySplitOperation(t);
    const l = [
      new F(s.start, t.splitPosition),
      a
    ];
    return Pr(l, i);
  }
  n.targetPosition.isEqual(t.splitPosition) && e.abRelation == "insertAtSource" && (i = t.moveTargetPosition), n.targetPosition.isEqual(t.insertionPosition) && e.abRelation == "insertBetween" && (i = n.targetPosition);
  const o = [
    s._getTransformedBySplitOperation(t)
  ];
  if (t.graveyardPosition) {
    const a = s.start.isEqual(t.graveyardPosition) || s.containsPosition(t.graveyardPosition);
    n.howMany > 1 && a && !e.aWasUndone && o.push(F._createFromPositionAndShift(t.insertionPosition, 1));
  }
  return Pr(o, i);
});
$e(He, _t, (n, t, e) => {
  const i = F._createFromPositionAndShift(n.sourcePosition, n.howMany);
  if (t.deletionPosition.hasSameParentAs(n.sourcePosition) && i.containsPosition(t.sourcePosition)) {
    if (n.type == "remove" && !e.forceWeakRemove) {
      if (!e.aWasUndone) {
        const o = [];
        let a = t.graveyardPosition.clone(), l = t.targetPosition._getTransformedByMergeOperation(t);
        const c = n.targetPosition.getTransformedByOperation(t);
        n.howMany > 1 && (o.push(new He(n.sourcePosition, n.howMany - 1, c, 0)), a = a._getTransformedByMove(n.sourcePosition, c, n.howMany - 1), l = l._getTransformedByMove(n.sourcePosition, c, n.howMany - 1));
        const u = t.deletionPosition._getCombined(n.sourcePosition, c), d = new He(a, 1, u, 0), h = d.getMovedRangeStart().path.slice();
        h.push(0);
        const m = new G(d.targetPosition.root, h);
        l = l._getTransformedByMove(a, u, 1);
        const g = new He(l, t.howMany, m, 0);
        return o.push(d), o.push(g), o;
      }
    } else if (n.howMany == 1)
      return e.bWasUndone ? (n.sourcePosition = t.graveyardPosition.clone(), n.targetPosition = n.targetPosition._getTransformedByMergeOperation(t), [
        n
      ]) : [
        new Lt(0)
      ];
  }
  const r = F._createFromPositionAndShift(n.sourcePosition, n.howMany)._getTransformedByMergeOperation(t);
  return n.sourcePosition = r.start, n.howMany = r.end.offset - r.start.offset, n.targetPosition = n.targetPosition._getTransformedByMergeOperation(t), [
    n
  ];
});
$e(bi, Ht, (n, t) => (n.position = n.position._getTransformedByInsertOperation(t), [
  n
]));
$e(bi, _t, (n, t) => n.position.isEqual(t.deletionPosition) ? (n.position = t.graveyardPosition.clone(), n.position.stickiness = "toNext", [
  n
]) : (n.position = n.position._getTransformedByMergeOperation(t), [
  n
]));
$e(bi, He, (n, t) => (n.position = n.position._getTransformedByMoveOperation(t), [
  n
]));
$e(bi, bi, (n, t, e) => {
  if (n.position.isEqual(t.position))
    if (e.aIsStrong)
      n.oldName = t.newName;
    else
      return [
        new Lt(0)
      ];
  return [
    n
  ];
});
$e(bi, tt, (n, t) => {
  const e = n.position.path, i = t.splitPosition.getParentPath();
  if (Zt(e, i) == "same" && !t.graveyardPosition) {
    const s = new bi(n.position.getShiftedBy(1), n.oldName, n.newName, 0);
    return [
      n,
      s
    ];
  }
  return n.position = n.position._getTransformedBySplitOperation(t), [
    n
  ];
});
$e($n, $n, (n, t, e) => {
  if (n.root === t.root && n.key === t.key) {
    if (!e.aIsStrong || n.newValue === t.newValue)
      return [
        new Lt(0)
      ];
    n.oldValue = t.newValue;
  }
  return [
    n
  ];
});
$e(bn, bn, (n, t) => n.rootName === t.rootName && n.isAdd === t.isAdd ? [
  new Lt(0)
] : [
  n
]);
$e(tt, Ht, (n, t) => (n.splitPosition.hasSameParentAs(t.position) && n.splitPosition.offset < t.position.offset && (n.howMany += t.howMany), n.splitPosition = n.splitPosition._getTransformedByInsertOperation(t), n.insertionPosition = n.insertionPosition._getTransformedByInsertOperation(t), [
  n
]));
$e(tt, _t, (n, t, e) => {
  if (!n.graveyardPosition && !e.bWasUndone && n.splitPosition.hasSameParentAs(t.sourcePosition)) {
    const i = t.graveyardPosition.path.slice();
    i.push(0);
    const s = new G(t.graveyardPosition.root, i), r = tt.getInsertionPosition(new G(t.graveyardPosition.root, i)), o = new tt(s, 0, r, null, 0);
    return n.splitPosition = n.splitPosition._getTransformedByMergeOperation(t), n.insertionPosition = tt.getInsertionPosition(n.splitPosition), n.graveyardPosition = o.insertionPosition.clone(), n.graveyardPosition.stickiness = "toNext", [
      o,
      n
    ];
  }
  return n.splitPosition.hasSameParentAs(t.deletionPosition) && !n.splitPosition.isAfter(t.deletionPosition) && n.howMany--, n.splitPosition.hasSameParentAs(t.targetPosition) && (n.howMany += t.howMany), n.splitPosition = n.splitPosition._getTransformedByMergeOperation(t), n.insertionPosition = tt.getInsertionPosition(n.splitPosition), n.graveyardPosition && (n.graveyardPosition = n.graveyardPosition._getTransformedByMergeOperation(t)), [
    n
  ];
});
$e(tt, He, (n, t, e) => {
  const i = F._createFromPositionAndShift(t.sourcePosition, t.howMany);
  if (n.graveyardPosition) {
    const r = i.start.isEqual(n.graveyardPosition) || i.containsPosition(n.graveyardPosition);
    if (!e.bWasUndone && r) {
      const o = n.splitPosition._getTransformedByMoveOperation(t), a = n.graveyardPosition._getTransformedByMoveOperation(t), l = a.path.slice();
      l.push(0);
      const c = new G(a.root, l);
      return [
        new He(o, n.howMany, c, 0)
      ];
    }
    n.graveyardPosition = n.graveyardPosition._getTransformedByMoveOperation(t);
  }
  const s = n.splitPosition.isEqual(t.targetPosition);
  if (s && (e.baRelation == "insertAtSource" || e.abRelation == "splitBefore"))
    return n.howMany += t.howMany, n.splitPosition = n.splitPosition._getTransformedByDeletion(t.sourcePosition, t.howMany), n.insertionPosition = tt.getInsertionPosition(n.splitPosition), [
      n
    ];
  if (s && e.abRelation && e.abRelation.howMany) {
    const { howMany: r, offset: o } = e.abRelation;
    return n.howMany += r, n.splitPosition = n.splitPosition.getShiftedBy(o), [
      n
    ];
  }
  if (n.splitPosition.hasSameParentAs(t.sourcePosition) && i.containsPosition(n.splitPosition)) {
    const r = t.howMany - (n.splitPosition.offset - t.sourcePosition.offset);
    return n.howMany -= r, n.splitPosition.hasSameParentAs(t.targetPosition) && n.splitPosition.offset < t.targetPosition.offset && (n.howMany += t.howMany), n.splitPosition = t.sourcePosition.clone(), n.insertionPosition = tt.getInsertionPosition(n.splitPosition), [
      n
    ];
  }
  return t.sourcePosition.isEqual(t.targetPosition) || (n.splitPosition.hasSameParentAs(t.sourcePosition) && n.splitPosition.offset <= t.sourcePosition.offset && (n.howMany -= t.howMany), n.splitPosition.hasSameParentAs(t.targetPosition) && n.splitPosition.offset < t.targetPosition.offset && (n.howMany += t.howMany)), n.splitPosition.stickiness = "toNone", n.splitPosition = n.splitPosition._getTransformedByMoveOperation(t), n.splitPosition.stickiness = "toNext", n.graveyardPosition ? n.insertionPosition = n.insertionPosition._getTransformedByMoveOperation(t) : n.insertionPosition = tt.getInsertionPosition(n.splitPosition), [
    n
  ];
});
$e(tt, tt, (n, t, e) => {
  if (n.splitPosition.isEqual(t.splitPosition)) {
    if (!n.graveyardPosition && !t.graveyardPosition)
      return [
        new Lt(0)
      ];
    if (n.graveyardPosition && t.graveyardPosition && n.graveyardPosition.isEqual(t.graveyardPosition))
      return [
        new Lt(0)
      ];
    if (e.abRelation == "splitBefore")
      return n.howMany = 0, n.graveyardPosition = n.graveyardPosition._getTransformedBySplitOperation(t), [
        n
      ];
  }
  if (n.graveyardPosition && t.graveyardPosition && n.graveyardPosition.isEqual(t.graveyardPosition)) {
    const i = n.splitPosition.root.rootName == "$graveyard", s = t.splitPosition.root.rootName == "$graveyard";
    if (s && !i || !(i && !s) && e.aIsStrong) {
      const l = [];
      return t.howMany && l.push(new He(t.moveTargetPosition, t.howMany, t.splitPosition, 0)), n.howMany && l.push(new He(n.splitPosition, n.howMany, n.moveTargetPosition, 0)), l;
    } else
      return [
        new Lt(0)
      ];
  }
  if (n.graveyardPosition && (n.graveyardPosition = n.graveyardPosition._getTransformedBySplitOperation(t)), n.splitPosition.isEqual(t.insertionPosition) && e.abRelation == "splitBefore")
    return n.howMany++, [
      n
    ];
  if (t.splitPosition.isEqual(n.insertionPosition) && e.baRelation == "splitBefore") {
    const i = t.insertionPosition.path.slice();
    i.push(0);
    const s = new G(t.insertionPosition.root, i), r = new He(n.insertionPosition, 1, s, 0);
    return [
      n,
      r
    ];
  }
  return n.splitPosition.hasSameParentAs(t.splitPosition) && n.splitPosition.offset < t.splitPosition.offset && (n.howMany -= t.howMany), n.splitPosition = n.splitPosition._getTransformedBySplitOperation(t), n.insertionPosition = tt.getInsertionPosition(n.splitPosition), [
    n
  ];
});
function jp(n, t) {
  return n.targetPosition._getTransformedByDeletion(t.sourcePosition, t.howMany) === null;
}
function Pr(n, t) {
  const e = [];
  for (let i = 0; i < n.length; i++) {
    const s = n[i], r = new He(s.start, s.end.offset - s.start.offset, t, 0);
    e.push(r);
    for (let o = i + 1; o < n.length; o++)
      n[o] = n[o]._getTransformedByMove(r.sourcePosition, r.targetPosition, r.howMany)[0];
    t = t._getTransformedByMove(r.sourcePosition, r.targetPosition, r.howMany);
  }
  return e;
}
class At extends (/* @__PURE__ */ Je(G)) {
  /**
  * Creates a live position.
  *
  * @see module:engine/model/position~Position
  */
  constructor(t, e, i = "toNone") {
    if (super(t, e, i), !this.root.is("rootElement"))
      throw new S("model-liveposition-root-not-rootelement", t);
    lO.call(this);
  }
  /**
  * Unbinds all events previously bound by `LivePosition`. Use it whenever you don't need `LivePosition` instance
  * anymore (i.e. when leaving scope in which it was declared or before re-assigning variable that was
  * referring to it).
  */
  detach() {
    this.stopListening();
  }
  /**
  * Creates a {@link module:engine/model/position~Position position instance}, which is equal to this live position.
  */
  toPosition() {
    return new G(this.root, this.path.slice(), this.stickiness);
  }
  /**
  * Creates a `LivePosition` instance that is equal to position.
  */
  static fromPosition(t, e) {
    return new this(t.root, t.path.slice(), e || t.stickiness);
  }
}
At.prototype.is = function(n) {
  return n === "livePosition" || n === "model:livePosition" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n == "position" || n === "model:position";
};
function lO() {
  this.listenTo(this.root.document.model, "applyOperation", (n, t) => {
    const e = t[0];
    e.isDocumentOperation && cO.call(this, e);
  }, {
    priority: "low"
  });
}
function cO(n) {
  const t = this.getTransformedByOperation(n);
  if (!this.isEqual(t)) {
    const e = this.toPosition();
    this.path = t.path, this.root = t.root, this.fire("change", e);
  }
}
class ur {
  /**
  * Creates a batch instance.
  *
  * @see module:engine/model/model~Model#enqueueChange
  * @see module:engine/model/model~Model#change
  * @param type A set of flags that specify the type of the batch. Batch type can alter how some of the features work
  * when encountering a given `Batch` instance (for example, when a feature listens to applied operations).
  */
  constructor(t = {}) {
    /**
    * An array of operations that compose this batch.
    */
    f(this, "operations");
    /**
    * Whether the batch can be undone through the undo feature.
    */
    f(this, "isUndoable");
    /**
    * Whether the batch includes operations created locally (`true`) or operations created on other, remote editors (`false`).
    */
    f(this, "isLocal");
    /**
    * Whether the batch was created by the undo feature and undoes other operations.
    */
    f(this, "isUndo");
    /**
    * Whether the batch includes operations connected with typing.
    */
    f(this, "isTyping");
    typeof t == "string" && (t = t === "transparent" ? {
      isUndoable: !1
    } : {}, Ve("batch-constructor-deprecated-string-type"));
    const { isUndoable: e = !0, isLocal: i = !0, isUndo: s = !1, isTyping: r = !1 } = t;
    this.operations = [], this.isUndoable = e, this.isLocal = i, this.isUndo = s, this.isTyping = r;
  }
  /**
  * The type of the batch.
  *
  * **This property has been deprecated and is always set to the `'default'` value.**
  *
  * It can be one of the following values:
  * * `'default'` &ndash; All "normal" batches. This is the most commonly used type.
  * * `'transparent'` &ndash; A batch that should be ignored by other features, i.e. an initial batch or collaborative editing
  * changes.
  *
  * @deprecated
  */
  get type() {
    return Ve("batch-type-deprecated"), "default";
  }
  /**
  * Returns the base version of this batch, which is equal to the base version of the first operation in the batch.
  * If there are no operations in the batch or neither operation has the base version set, it returns `null`.
  */
  get baseVersion() {
    for (const t of this.operations)
      if (t.baseVersion !== null)
        return t.baseVersion;
    return null;
  }
  /**
  * Adds an operation to the batch instance.
  *
  * @param operation An operation to add.
  * @returns The added operation.
  */
  addOperation(t) {
    return t.isDocumentOperation && (t.batch = this, this.operations.push(t)), t;
  }
}
const Jo = class Jo {
  /**
  * Creates a `Differ` instance.
  *
  * @param markerCollection Model's marker collection.
  */
  constructor(t) {
    /**
    * Reference to the model's marker collection.
    */
    f(this, "_markerCollection");
    /**
    * A map that stores changes that happened in a given element.
    *
    * The keys of the map are references to the model elements.
    * The values of the map are arrays with changes that were done on this element.
    */
    f(this, "_changesInElement", /* @__PURE__ */ new Map());
    /**
    * Stores a snapshot for these model nodes that might have changed.
    *
    * This complements {@link ~Differ#_elementChildrenSnapshots `_elementChildrenSnapshots`}.
    *
    * See also {@link ~DifferSnapshot}.
    */
    f(this, "_elementsSnapshots", /* @__PURE__ */ new Map());
    /**
    * For each element or document fragment inside which there was a change, it stores a snapshot of the child nodes list (an array
    * of children snapshots that represent the state in the element / fragment before any change has happened).
    *
    * This complements {@link ~Differ#_elementsSnapshots `_elementsSnapshots`}.
    *
    * See also {@link ~DifferSnapshot}.
    */
    f(this, "_elementChildrenSnapshots", /* @__PURE__ */ new Map());
    /**
    * Keeps the state for a given element, describing how the element was changed so far. It is used to evaluate the `action` property
    * of diff items returned by {@link ~Differ#getChanges}.
    *
    * Possible values, in the order from the lowest priority to the highest priority:
    *
    * * `'refresh'` - element was refreshed,
    * * `'rename'` - element was renamed,
    * * `'move'` - element was moved (or, usually, removed, that is moved to the graveyard).
    *
    * Element that was refreshed, may change its state to `'rename'` if it was later renamed, or to `'move'` if it was removed.
    * But the element cannot change its state from `'move'` to `'rename'`, or from `'rename'` to `'refresh'`.
    *
    * Only already existing elements are registered in `_elementState`. If a new element was inserted as a result of a buffered operation,
    * it is not be registered in `_elementState`.
    */
    f(this, "_elementState", /* @__PURE__ */ new Map());
    /**
    * A map that stores all changed markers.
    *
    * The keys of the map are marker names.
    *
    * The values of the map are objects with the following properties:
    *
    * * `oldMarkerData`,
    * * `newMarkerData`.
    */
    f(this, "_changedMarkers", /* @__PURE__ */ new Map());
    /**
    * A map that stores all roots that have been changed.
    *
    * The keys are the names of the roots while value represents the changes.
    */
    f(this, "_changedRoots", /* @__PURE__ */ new Map());
    /**
    * Stores the number of changes that were processed. Used to order the changes chronologically. It is important
    * when changes are sorted.
    */
    f(this, "_changeCount", 0);
    /**
    * For efficiency purposes, `Differ` stores the change set returned by the differ after {@link #getChanges} call.
    * Cache is reset each time a new operation is buffered. If the cache has not been reset, {@link #getChanges} will
    * return the cached value instead of calculating it again.
    *
    * This property stores those changes that did not take place in graveyard root.
    */
    f(this, "_cachedChanges", null);
    /**
    * For efficiency purposes, `Differ` stores the change set returned by the differ after the {@link #getChanges} call.
    * The cache is reset each time a new operation is buffered. If the cache has not been reset, {@link #getChanges} will
    * return the cached value instead of calculating it again.
    *
    * This property stores all changes evaluated by `Differ`, including those that took place in the graveyard.
    */
    f(this, "_cachedChangesWithGraveyard", null);
    /**
    * Set of model items that were marked to get refreshed in {@link #_refreshItem}.
    */
    f(this, "_refreshedItems", /* @__PURE__ */ new Set());
    this._markerCollection = t;
  }
  /**
  * Informs whether there are any changes buffered in `Differ`.
  */
  get isEmpty() {
    return this._changesInElement.size == 0 && this._changedMarkers.size == 0 && this._changedRoots.size == 0;
  }
  /**
  * Buffers the given operation. **An operation has to be buffered before it is executed.**
  *
  * @param operationToBuffer An operation to buffer.
  */
  bufferOperation(t) {
    const e = t;
    switch (e.type) {
      case "insert": {
        if (this._isInInsertedElement(e.position.parent))
          return;
        this._markInsert(e.position.parent, e.position.offset, e.nodes.maxOffset);
        break;
      }
      case "addAttribute":
      case "removeAttribute":
      case "changeAttribute": {
        for (const i of e.range.getItems({
          shallow: !0
        }))
          this._isInInsertedElement(i.parent) || this._markAttribute(i);
        break;
      }
      case "remove":
      case "move":
      case "reinsert": {
        if (e.sourcePosition.isEqual(e.targetPosition) || e.sourcePosition.getShiftedBy(e.howMany).isEqual(e.targetPosition))
          return;
        const i = this._isInInsertedElement(e.sourcePosition.parent), s = this._isInInsertedElement(e.targetPosition.parent);
        i || this._markRemove(e.sourcePosition.parent, e.sourcePosition.offset, e.howMany), s || this._markInsert(e.targetPosition.parent, e.getMovedRangeStart().offset, e.howMany);
        const r = F._createFromPositionAndShift(e.sourcePosition, e.howMany);
        for (const o of r.getItems({
          shallow: !0
        }))
          this._setElementState(o, "move");
        break;
      }
      case "rename": {
        if (this._isInInsertedElement(e.position.parent))
          return;
        this._markRemove(e.position.parent, e.position.offset, 1), this._markInsert(e.position.parent, e.position.offset, 1);
        const i = F._createFromPositionAndShift(e.position, 1);
        for (const s of this._markerCollection.getMarkersIntersectingRange(i)) {
          const r = s.getData();
          this.bufferMarkerChange(s.name, r, r);
        }
        this._setElementState(e.position.nodeAfter, "rename");
        break;
      }
      case "split": {
        const i = e.splitPosition.parent;
        if (!this._isInInsertedElement(i)) {
          this._markRemove(i, e.splitPosition.offset, e.howMany);
          const s = F._createFromPositionAndShift(e.splitPosition, e.howMany);
          for (const r of s.getItems({
            shallow: !0
          }))
            this._setElementState(r, "move");
        }
        this._isInInsertedElement(e.insertionPosition.parent) || this._markInsert(e.insertionPosition.parent, e.insertionPosition.offset, 1), e.graveyardPosition && (this._markRemove(e.graveyardPosition.parent, e.graveyardPosition.offset, 1), this._setElementState(e.graveyardPosition.nodeAfter, "move"));
        break;
      }
      case "merge": {
        const i = e.sourcePosition.parent;
        this._isInInsertedElement(i.parent) || this._markRemove(i.parent, i.startOffset, 1);
        const s = e.graveyardPosition.parent;
        this._markInsert(s, e.graveyardPosition.offset, 1), this._setElementState(i, "move");
        const r = e.targetPosition.parent;
        if (!this._isInInsertedElement(r)) {
          this._markInsert(r, e.targetPosition.offset, i.maxOffset);
          const o = F._createFromPositionAndShift(e.sourcePosition, e.howMany);
          for (const a of o.getItems({
            shallow: !0
          }))
            this._setElementState(a, "move");
        }
        break;
      }
      case "detachRoot":
      case "addRoot": {
        const i = e.affectedSelectable;
        if (!i._isLoaded || i.isAttached() == e.isAdd)
          return;
        this._bufferRootStateChange(e.rootName, e.isAdd);
        break;
      }
      case "addRootAttribute":
      case "removeRootAttribute":
      case "changeRootAttribute": {
        if (!e.root._isLoaded)
          return;
        const i = e.root.rootName;
        this._bufferRootAttributeChange(i, e.key, e.oldValue, e.newValue);
        break;
      }
    }
    this._cachedChanges = null;
  }
  /**
  * Buffers a marker change.
  *
  * @param markerName The name of the marker that changed.
  * @param oldMarkerData Marker data before the change.
  * @param newMarkerData Marker data after the change.
  */
  bufferMarkerChange(t, e, i) {
    e.range && e.range.root.is("rootElement") && !e.range.root._isLoaded && (e.range = null), i.range && i.range.root.is("rootElement") && !i.range.root._isLoaded && (i.range = null);
    let s = this._changedMarkers.get(t);
    s ? s.newMarkerData = i : (s = {
      newMarkerData: i,
      oldMarkerData: e
    }, this._changedMarkers.set(t, s)), s.oldMarkerData.range == null && i.range == null && this._changedMarkers.delete(t);
  }
  /**
  * Returns all markers that should be removed as a result of buffered changes.
  *
  * @returns Markers to remove. Each array item is an object containing the `name` and `range` properties.
  */
  getMarkersToRemove() {
    const t = [];
    for (const [e, i] of this._changedMarkers)
      i.oldMarkerData.range != null && t.push({
        name: e,
        range: i.oldMarkerData.range
      });
    return t;
  }
  /**
  * Returns all markers which should be added as a result of buffered changes.
  *
  * @returns Markers to add. Each array item is an object containing the `name` and `range` properties.
  */
  getMarkersToAdd() {
    const t = [];
    for (const [e, i] of this._changedMarkers)
      i.newMarkerData.range != null && t.push({
        name: e,
        range: i.newMarkerData.range
      });
    return t;
  }
  /**
  * Returns all markers which changed.
  */
  getChangedMarkers() {
    return Array.from(this._changedMarkers).map(([t, e]) => ({
      name: t,
      data: {
        oldRange: e.oldMarkerData.range,
        newRange: e.newMarkerData.range
      }
    }));
  }
  /**
  * Checks whether some of the buffered changes affect the editor data.
  *
  * Types of changes which affect the editor data:
  *
  * * model structure changes,
  * * attribute changes,
  * * a root is added or detached,
  * * changes of markers which were defined as `affectsData`,
  * * changes of markers' `affectsData` property.
  */
  hasDataChanges() {
    if (this.getChanges().length || this._changedRoots.size > 0)
      return !0;
    for (const { newMarkerData: t, oldMarkerData: e } of this._changedMarkers.values()) {
      if (t.affectsData !== e.affectsData)
        return !0;
      if (t.affectsData) {
        const i = t.range && !e.range, s = !t.range && e.range, r = t.range && e.range && !t.range.isEqual(e.range);
        if (i || s || r)
          return !0;
      }
    }
    return !1;
  }
  /**
  * Calculates the diff between the old model tree state (the state before the first buffered operations since the last {@link #reset}
  * call) and the new model tree state (actual one). It should be called after all buffered operations are executed.
  *
  * The diff set is returned as an array of {@link module:engine/model/differ~DiffItem diff items}, each describing a change done
  * on the model. The items are sorted by the position on which the change happened. If a position
  * {@link module:engine/model/position~Position#isBefore is before} another one, it will be on an earlier index in the diff set.
  *
  * **Note**: Elements inside inserted element will not have a separate diff item, only the top most element change will be reported.
  *
  * Because calculating the diff is a costly operation, the result is cached. If no new operation was buffered since the
  * previous {@link #getChanges} call, the next call will return the cached value.
  *
  * @param options Additional options.
  * @param options.includeChangesInGraveyard If set to `true`, also changes that happened
  * in the graveyard root will be returned. By default, changes in the graveyard root are not returned.
  * @returns Diff between the old and the new model tree state.
  */
  getChanges(t = {}) {
    if (this._cachedChanges)
      return t.includeChangesInGraveyard ? this._cachedChangesWithGraveyard.slice() : this._cachedChanges.slice();
    let e = [];
    for (const i of this._changesInElement.keys()) {
      const s = this._changesInElement.get(i).sort((u, d) => u.offset === d.offset ? u.type != d.type ? u.type == "remove" ? -1 : 1 : 0 : u.offset < d.offset ? -1 : 1), r = this._elementChildrenSnapshots.get(i), o = Wp(i.getChildren()), a = uO(r.length, s);
      let l = 0, c = 0;
      for (const u of a)
        if (u === "i") {
          const d = this._getDiffActionForNode(o[l].node, "insert"), h = this._elementsSnapshots.get(o[l].node), m = this._getInsertDiff(i, l, d, o[l], h);
          e.push(m), l++;
        } else if (u === "r") {
          const d = this._getDiffActionForNode(r[c].node, "remove"), h = this._getRemoveDiff(i, l, d, r[c]);
          e.push(h), c++;
        } else if (u === "a") {
          const d = r[c].attributes, h = o[l].attributes;
          let m;
          if (o[l].name == "$text")
            m = new F(G._createAt(i, l), G._createAt(i, l + 1));
          else {
            const p = i.offsetToIndex(l);
            m = new F(G._createAt(i, l), G._createAt(i.getChild(p), 0));
          }
          const g = this._getAttributesDiff(m, d, h);
          e.push(...g), l++, c++;
        } else
          l++, c++;
    }
    e.sort((i, s) => i.position.root != s.position.root ? i.position.root.rootName < s.position.root.rootName ? -1 : 1 : i.position.isEqual(s.position) ? i.changeCount - s.changeCount : i.position.isBefore(s.position) ? -1 : 1);
    for (let i = 1, s = 0; i < e.length; i++) {
      const r = e[s], o = e[i], a = r.type == "remove" && o.type == "remove" && r.name == "$text" && o.name == "$text" && r.position.isEqual(o.position), l = r.type == "insert" && o.type == "insert" && r.name == "$text" && o.name == "$text" && r.position.parent == o.position.parent && r.position.offset + r.length == o.position.offset, c = r.type == "attribute" && o.type == "attribute" && r.position.parent == o.position.parent && r.range.isFlat && o.range.isFlat && r.position.offset + r.length == o.position.offset && r.attributeKey == o.attributeKey && r.attributeOldValue == o.attributeOldValue && r.attributeNewValue == o.attributeNewValue;
      a || l || c ? (r.length++, c && (r.range.end = r.range.end.getShiftedBy(1)), e[i] = null) : s = i;
    }
    e = e.filter((i) => i);
    for (const i of e)
      delete i.changeCount, i.type == "attribute" && (delete i.position, delete i.length);
    return this._changeCount = 0, this._cachedChangesWithGraveyard = e, this._cachedChanges = e.filter(dO), t.includeChangesInGraveyard ? this._cachedChangesWithGraveyard.slice() : this._cachedChanges.slice();
  }
  /**
  * Returns all roots that have changed (either were attached, or detached, or their attributes changed).
  *
  * @returns Diff between the old and the new roots state.
  */
  getChangedRoots() {
    return Array.from(this._changedRoots.values()).map((t) => {
      const e = {
        ...t
      };
      return e.state !== void 0 && delete e.attributes, e;
    });
  }
  /**
  * Returns a set of model items that were marked to get refreshed.
  */
  getRefreshedItems() {
    return new Set(this._refreshedItems);
  }
  /**
  * Resets `Differ`. Removes all buffered changes.
  */
  reset() {
    this._changesInElement.clear(), this._elementChildrenSnapshots.clear(), this._elementsSnapshots.clear(), this._elementState.clear(), this._changedMarkers.clear(), this._changedRoots.clear(), this._refreshedItems.clear(), this._cachedChanges = null;
  }
  /**
  * Marks the given `item` in differ to be "refreshed". It means that the item will be marked as removed and inserted
  * in the differ changes set, so it will be effectively re-converted when the differ changes are handled by a dispatcher.
  *
  * @internal
  * @param item Item to refresh.
  */
  _refreshItem(t) {
    if (this._isInInsertedElement(t.parent))
      return;
    this._markRemove(t.parent, t.startOffset, t.offsetSize), this._markInsert(t.parent, t.startOffset, t.offsetSize), this._refreshedItems.add(t), this._setElementState(t, "refresh");
    const e = F._createOn(t);
    for (const i of this._markerCollection.getMarkersIntersectingRange(e)) {
      const s = i.getData();
      this.bufferMarkerChange(i.name, s, s);
    }
    this._cachedChanges = null;
  }
  /**
  * Buffers all the data related to given root like it was all just added to the editor.
  *
  * Following changes are buffered:
  *
  * * root is attached,
  * * all root content is inserted,
  * * all root attributes are added,
  * * all markers inside the root are added.
  *
  * @internal
  */
  _bufferRootLoad(t) {
    if (t.isAttached()) {
      this._bufferRootStateChange(t.rootName, !0), this._markInsert(t, 0, t.maxOffset);
      for (const e of t.getAttributeKeys())
        this._bufferRootAttributeChange(t.rootName, e, null, t.getAttribute(e));
      for (const e of this._markerCollection)
        if (e.getRange().root == t) {
          const i = e.getData();
          this.bufferMarkerChange(e.name, {
            ...i,
            range: null
          }, i);
        }
    }
  }
  /**
  * Buffers the root state change after the root was attached or detached
  */
  _bufferRootStateChange(t, e) {
    if (!this._changedRoots.has(t)) {
      this._changedRoots.set(t, {
        name: t,
        state: e ? "attached" : "detached"
      });
      return;
    }
    const i = this._changedRoots.get(t);
    i.state !== void 0 ? (delete i.state, i.attributes === void 0 && this._changedRoots.delete(t)) : i.state = e ? "attached" : "detached";
  }
  /**
  * Buffers a root attribute change.
  */
  _bufferRootAttributeChange(t, e, i, s) {
    const r = this._changedRoots.get(t) || {
      name: t
    }, o = r.attributes || {};
    if (o[e]) {
      const a = o[e];
      s === a.oldValue ? delete o[e] : a.newValue = s;
    } else
      o[e] = {
        oldValue: i,
        newValue: s
      };
    Object.entries(o).length === 0 ? (delete r.attributes, r.state === void 0 && this._changedRoots.delete(t)) : (r.attributes = o, this._changedRoots.set(t, r));
  }
  /**
  * Saves and handles an insert change.
  */
  _markInsert(t, e, i) {
    if (t.root.is("rootElement") && !t.root._isLoaded)
      return;
    const s = {
      type: "insert",
      offset: e,
      howMany: i,
      count: this._changeCount++
    };
    this._markChange(t, s);
  }
  /**
  * Saves and handles a remove change.
  */
  _markRemove(t, e, i) {
    if (t.root.is("rootElement") && !t.root._isLoaded)
      return;
    const s = {
      type: "remove",
      offset: e,
      howMany: i,
      count: this._changeCount++
    };
    this._markChange(t, s), this._removeAllNestedChanges(t, e, i);
  }
  /**
  * Saves and handles an attribute change.
  */
  _markAttribute(t) {
    if (t.root.is("rootElement") && !t.root._isLoaded)
      return;
    const e = {
      type: "attribute",
      offset: t.startOffset,
      howMany: t.offsetSize,
      count: this._changeCount++
    };
    this._markChange(t.parent, e);
  }
  /**
  * Saves and handles a model change.
  */
  _markChange(t, e) {
    this._makeSnapshots(t);
    const i = this._getChangesForElement(t);
    this._handleChange(e, i), i.push(e);
    for (let s = 0; s < i.length; s++)
      i[s].howMany < 1 && (i.splice(s, 1), s--);
  }
  /**
  * Tries to set given state for given item.
  *
  * This method does simple validation (it sets the state only for model elements, not for text proxy nodes). It also follows state
  * setting rules, that is, `'refresh'` cannot overwrite `'rename'`, and `'rename'` cannot overwrite `'move'`.
  */
  _setElementState(t, e) {
    if (!t.is("element"))
      return;
    const i = Jo._statesPriority.indexOf(this._elementState.get(t));
    Jo._statesPriority.indexOf(e) > i && this._elementState.set(t, e);
  }
  /**
  * Returns a value for {@link ~DifferItemAction `action`} property for diff items returned by {@link ~Differ#getChanges}.
  * This method aims to return `'rename'` or `'refresh'` when it should, and `diffItemType` ("default action") in all other cases.
  *
  * It bases on a few factors:
  *
  * * for text nodes, the method always returns `diffItemType`,
  * * for newly inserted element, the method returns `diffItemType`,
  * * if {@link ~Differ#_elementState element state} was not recorded, the method returns `diffItemType`,
  * * if state was recorded, and it was `'move'` (default action), the method returns `diffItemType`,
  * * finally, if state was `'refresh'` or `'rename'`, the method returns the state value.
  */
  _getDiffActionForNode(t, e) {
    if (!t.is("element") || !this._elementsSnapshots.has(t))
      return e;
    const i = this._elementState.get(t);
    return !i || i == "move" ? e : i;
  }
  /**
  * Gets an array of changes that have already been saved for a given element.
  */
  _getChangesForElement(t) {
    let e;
    return this._changesInElement.has(t) ? e = this._changesInElement.get(t) : (e = [], this._changesInElement.set(t, e)), e;
  }
  /**
  * Creates and saves a snapshot for all children of the given element.
  */
  _makeSnapshots(t) {
    if (this._elementChildrenSnapshots.has(t))
      return;
    const e = Wp(t.getChildren());
    this._elementChildrenSnapshots.set(t, e);
    for (const i of e)
      this._elementsSnapshots.set(i.node, i);
  }
  /**
  * For a given newly saved change, compares it with a change already done on the element and modifies the incoming
  * change and/or the old change.
  *
  * @param inc Incoming (new) change.
  * @param changes An array containing all the changes done on that element.
  */
  _handleChange(t, e) {
    t.nodesToHandle = t.howMany;
    for (const i of e) {
      const s = t.offset + t.howMany, r = i.offset + i.howMany;
      if (t.type == "insert" && (i.type == "insert" && (t.offset <= i.offset ? i.offset += t.howMany : t.offset < r && (i.howMany += t.nodesToHandle, t.nodesToHandle = 0)), i.type == "remove" && t.offset < i.offset && (i.offset += t.howMany), i.type == "attribute")) {
        if (t.offset <= i.offset)
          i.offset += t.howMany;
        else if (t.offset < r) {
          const o = i.howMany;
          i.howMany = t.offset - i.offset, e.unshift({
            type: "attribute",
            offset: s,
            howMany: o - i.howMany,
            count: this._changeCount++
          });
        }
      }
      if (t.type == "remove") {
        if (i.type == "insert") {
          if (s <= i.offset)
            i.offset -= t.howMany;
          else if (s <= r)
            if (t.offset < i.offset) {
              const o = s - i.offset;
              i.offset = t.offset, i.howMany -= o, t.nodesToHandle -= o;
            } else
              i.howMany -= t.nodesToHandle, t.nodesToHandle = 0;
          else if (t.offset <= i.offset)
            t.nodesToHandle -= i.howMany, i.howMany = 0;
          else if (t.offset < r) {
            const o = r - t.offset;
            i.howMany -= o, t.nodesToHandle -= o;
          }
        }
        if (i.type == "remove" && (s <= i.offset ? i.offset -= t.howMany : t.offset < i.offset && (t.nodesToHandle += i.howMany, i.howMany = 0)), i.type == "attribute") {
          if (s <= i.offset)
            i.offset -= t.howMany;
          else if (t.offset < i.offset) {
            const o = s - i.offset;
            i.offset = t.offset, i.howMany -= o;
          } else if (t.offset < r)
            if (s <= r) {
              const o = i.howMany;
              i.howMany = t.offset - i.offset;
              const a = o - i.howMany - t.nodesToHandle;
              e.unshift({
                type: "attribute",
                offset: t.offset,
                howMany: a,
                count: this._changeCount++
              });
            } else
              i.howMany -= r - t.offset;
        }
      }
      if (t.type == "attribute") {
        if (i.type == "insert")
          if (t.offset < i.offset && s > i.offset) {
            if (s > r) {
              const o = {
                type: "attribute",
                offset: r,
                howMany: s - r,
                count: this._changeCount++
              };
              this._handleChange(o, e), e.push(o);
            }
            t.nodesToHandle = i.offset - t.offset, t.howMany = t.nodesToHandle;
          } else t.offset >= i.offset && t.offset < r && (s > r ? (t.nodesToHandle = s - r, t.offset = r) : t.nodesToHandle = 0);
        if (i.type == "remove" && t.offset < i.offset && s > i.offset) {
          const o = {
            type: "attribute",
            offset: i.offset,
            howMany: s - i.offset,
            count: this._changeCount++
          };
          this._handleChange(o, e), e.push(o), t.nodesToHandle = i.offset - t.offset, t.howMany = t.nodesToHandle;
        }
        i.type == "attribute" && (t.offset >= i.offset && s <= r ? (t.nodesToHandle = 0, t.howMany = 0, t.offset = 0) : t.offset <= i.offset && s >= r && (i.howMany = 0));
      }
    }
    t.howMany = t.nodesToHandle, delete t.nodesToHandle;
  }
  /**
  * Returns an object with a single insert change description.
  *
  * @param parent The element in which the change happened.
  * @param offset The offset at which change happened.
  * @param action Further specifies what kind of action led to generating this change.
  * @param elementSnapshot Snapshot of the inserted node after changes.
  * @param elementSnapshotBefore Snapshot of the inserted node before changes.
  * @returns The diff item.
  */
  _getInsertDiff(t, e, i, s, r) {
    const o = {
      type: "insert",
      position: G._createAt(t, e),
      name: s.name,
      attributes: new Map(s.attributes),
      length: 1,
      changeCount: this._changeCount++,
      action: i
    };
    return i != "insert" && r && (o.before = {
      name: r.name,
      attributes: new Map(r.attributes)
    }), o;
  }
  /**
  * Returns an object with a single remove change description.
  *
  * @param parent The element in which change happened.
  * @param offset The offset at which change happened.
  * @param action Further specifies what kind of action led to generating this change.
  * @param elementSnapshot The snapshot of the removed node before changes.
  * @returns The diff item.
  */
  _getRemoveDiff(t, e, i, s) {
    return {
      type: "remove",
      action: i,
      position: G._createAt(t, e),
      name: s.name,
      attributes: new Map(s.attributes),
      length: 1,
      changeCount: this._changeCount++
    };
  }
  /**
  * Returns an array of objects where each one is a single attribute change description.
  *
  * @param range The range where the change happened.
  * @param oldAttributes A map, map iterator or compatible object that contains attributes before the change.
  * @param newAttributes A map, map iterator or compatible object that contains attributes after the change.
  * @returns An array containing one or more diff items.
  */
  _getAttributesDiff(t, e, i) {
    const s = [];
    i = new Map(i);
    for (const [r, o] of e) {
      const a = i.has(r) ? i.get(r) : null;
      a !== o && s.push({
        type: "attribute",
        position: t.start,
        range: t.clone(),
        length: 1,
        attributeKey: r,
        attributeOldValue: o,
        attributeNewValue: a,
        changeCount: this._changeCount++
      }), i.delete(r);
    }
    for (const [r, o] of i)
      s.push({
        type: "attribute",
        position: t.start,
        range: t.clone(),
        length: 1,
        attributeKey: r,
        attributeOldValue: null,
        attributeNewValue: o,
        changeCount: this._changeCount++
      });
    return s;
  }
  /**
  * Checks whether given element or any of its parents is an element that is buffered as an inserted element.
  */
  _isInInsertedElement(t) {
    const e = t.parent;
    if (!e)
      return !1;
    const i = this._changesInElement.get(e), s = t.startOffset;
    if (i) {
      for (const r of i)
        if (r.type == "insert" && s >= r.offset && s < r.offset + r.howMany)
          return !0;
    }
    return this._isInInsertedElement(e);
  }
  /**
  * Removes deeply all buffered changes that are registered in elements from range specified by `parent`, `offset`
  * and `howMany`.
  */
  _removeAllNestedChanges(t, e, i) {
    const s = new F(G._createAt(t, e), G._createAt(t, e + i));
    for (const r of s.getItems({
      shallow: !0
    }))
      r.is("element") && (this._changesInElement.delete(r), this._removeAllNestedChanges(r, 0, r.maxOffset));
  }
};
/**
* Priority of the {@link ~Differ#_elementState element states}. States on higher indexes of the array can overwrite states on the lower
* indexes.
*/
f(Jo, "_statesPriority", [
  void 0,
  "refresh",
  "rename",
  "move"
]);
let fh = Jo;
function Up(n) {
  return {
    node: n,
    name: n.is("$text") ? "$text" : n.name,
    attributes: new Map(n.getAttributes())
  };
}
function Wp(n) {
  const t = [];
  for (const e of n)
    if (e.is("$text"))
      for (let i = 0; i < e.data.length; ++i)
        t.push(Up(e));
    else
      t.push(Up(e));
  return t;
}
function uO(n, t) {
  const e = [];
  let i = 0, s = 0;
  for (const r of t) {
    if (r.offset > i) {
      for (let o = 0; o < r.offset - i; o++)
        e.push("e");
      s += r.offset - i;
    }
    if (r.type == "insert") {
      for (let o = 0; o < r.howMany; o++)
        e.push("i");
      i = r.offset + r.howMany;
    } else if (r.type == "remove") {
      for (let o = 0; o < r.howMany; o++)
        e.push("r");
      i = r.offset, s += r.howMany;
    } else {
      if (r.howMany > 1500)
        for (let o = 0; o < r.howMany; o++)
          e.push("a");
      else
        e.push(..."a".repeat(r.howMany).split(""));
      i = r.offset + r.howMany, s += r.howMany;
    }
  }
  if (s < n)
    for (let r = 0; r < n - s - i; r++)
      e.push("e");
  return e;
}
function dO(n) {
  const t = "position" in n && n.position.root.rootName == "$graveyard", e = "range" in n && n.range.root.rootName == "$graveyard";
  return !t && !e;
}
class hO {
  constructor() {
    /**
    * Operations added to the history.
    */
    f(this, "_operations", []);
    /**
    * Holds an information which {@link module:engine/model/operation/operation~Operation operation} undoes which
    * {@link module:engine/model/operation/operation~Operation operation}.
    *
    * Keys of the map are "undoing operations", that is operations that undone some other operations. For each key, the
    * value is an operation that has been undone by the "undoing operation".
    */
    f(this, "_undoPairs", /* @__PURE__ */ new Map());
    /**
    * Holds all undone operations.
    */
    f(this, "_undoneOperations", /* @__PURE__ */ new Set());
    /**
    * A map that allows retrieving the operations fast based on the given base version.
    */
    f(this, "_baseVersionToOperationIndex", /* @__PURE__ */ new Map());
    /**
    * The history version.
    */
    f(this, "_version", 0);
    /**
    * The gap pairs kept in the <from,to> format.
    *
    * Anytime the `history.version` is set to a version larger than `history.version + 1`,
    * a new <lastHistoryVersion, newHistoryVersion> entry is added to the map.
    */
    f(this, "_gaps", /* @__PURE__ */ new Map());
  }
  /**
  * The version of the last operation in the history.
  *
  * The history version is incremented automatically when a new operation is added to the history.
  * Setting the version manually should be done only in rare circumstances when a gap is planned
  * between history versions. When doing so, a gap will be created and the history will accept adding
  * an operation with base version equal to the new history version.
  */
  get version() {
    return this._version;
  }
  set version(t) {
    this._operations.length && t > this._version + 1 && this._gaps.set(this._version, t), this._version = t;
  }
  /**
  * The last history operation.
  */
  get lastOperation() {
    return this._operations[this._operations.length - 1];
  }
  /**
  * Adds an operation to the history and increments the history version.
  *
  * The operation's base version should be equal to the history version. Otherwise an error is thrown.
  */
  addOperation(t) {
    if (t.baseVersion !== this.version)
      throw new S("model-document-history-addoperation-incorrect-version", this, {
        operation: t,
        historyVersion: this.version
      });
    this._operations.push(t), this._version++, this._baseVersionToOperationIndex.set(t.baseVersion, this._operations.length - 1);
  }
  /**
  * Returns operations from the given range of operation base versions that were added to the history.
  *
  * Note that there may be gaps in operations base versions.
  *
  * @param fromBaseVersion Base version from which operations should be returned (inclusive).
  * @param toBaseVersion Base version up to which operations should be returned (exclusive).
     * @returns History operations for the given range, in chronological order.
  */
  getOperations(t, e = this.version) {
    if (!this._operations.length)
      return [];
    const i = this._operations[0];
    t === void 0 && (t = i.baseVersion);
    let s = e - 1;
    for (const [a, l] of this._gaps)
      t > a && t < l && (t = l), s > a && s < l && (s = a - 1);
    if (s < i.baseVersion || t > this.lastOperation.baseVersion)
      return [];
    let r = this._baseVersionToOperationIndex.get(t);
    r === void 0 && (r = 0);
    let o = this._baseVersionToOperationIndex.get(s);
    return o === void 0 && (o = this._operations.length - 1), this._operations.slice(
      r,
      // The `toIndex` should be included in the returned operations, so add `1`.
      o + 1
    );
  }
  /**
  * Returns operation from the history that bases on given `baseVersion`.
  *
  * @param baseVersion Base version of the operation to get.
  * @returns Operation with given base version or `undefined` if there is no such operation in history.
  */
  getOperation(t) {
    const e = this._baseVersionToOperationIndex.get(t);
    if (e !== void 0)
      return this._operations[e];
  }
  /**
  * Marks in history that one operation is an operation that is undoing the other operation. By marking operation this way,
  * history is keeping more context information about operations, which helps in operational transformation.
  *
  * @param undoneOperation Operation which is undone by `undoingOperation`.
  * @param undoingOperation Operation which undoes `undoneOperation`.
  */
  setOperationAsUndone(t, e) {
    this._undoPairs.set(e, t), this._undoneOperations.add(t);
  }
  /**
  * Checks whether given `operation` is undoing any other operation.
  *
  * @param operation Operation to check.
  * @returns `true` if given `operation` is undoing any other operation, `false` otherwise.
  */
  isUndoingOperation(t) {
    return this._undoPairs.has(t);
  }
  /**
  * Checks whether given `operation` has been undone by any other operation.
  *
  * @param operation Operation to check.
  * @returns `true` if given `operation` has been undone any other operation, `false` otherwise.
  */
  isUndoneOperation(t) {
    return this._undoneOperations.has(t);
  }
  /**
  * For given `undoingOperation`, returns the operation which has been undone by it.
  *
  * @returns Operation that has been undone by given `undoingOperation` or `undefined`
  * if given `undoingOperation` is not undoing any other operation.
  */
  getUndoneOperation(t) {
    return this._undoPairs.get(t);
  }
  /**
  * Resets the history of operations.
  */
  reset() {
    this._version = 0, this._undoPairs = /* @__PURE__ */ new Map(), this._operations = [], this._undoneOperations = /* @__PURE__ */ new Set(), this._gaps = /* @__PURE__ */ new Map(), this._baseVersionToOperationIndex = /* @__PURE__ */ new Map();
  }
}
class ec extends et {
  /**
  * Creates root element.
  *
  * @param document Document that is an owner of this root.
  * @param name Node name.
  * @param rootName Unique root name used to identify this root element by {@link module:engine/model/document~Document}.
  */
  constructor(e, i, s = "main") {
    super(i);
    /**
    * Unique root name used to identify this root element by {@link module:engine/model/document~Document}.
    */
    f(this, "rootName");
    /**
    * Document that is an owner of this root.
    */
    f(this, "_document");
    /**
    * @internal
    */
    f(this, "_isAttached", !0);
    /**
    * Informs if the root element is loaded (default).
    *
    * @internal
    */
    f(this, "_isLoaded", !0);
    this._document = e, this.rootName = s;
  }
  /**
  * {@link module:engine/model/document~Document Document} that owns this root element.
  */
  get document() {
    return this._document;
  }
  /**
  * Informs if the root element is currently attached to the document, or not.
  *
  * A detached root is equivalent to being removed and cannot contain any children or markers.
  *
  * By default, a newly added root is attached. It can be detached using
  * {@link module:engine/model/writer~Writer#detachRoot `Writer#detachRoot`}. A detached root can be re-attached again using
  * {@link module:engine/model/writer~Writer#addRoot `Writer#addRoot`}.
  */
  isAttached() {
    return this._isAttached;
  }
  /**
  * Converts `RootElement` instance to `string` containing its name.
  *
  * @returns `RootElement` instance converted to `string`.
  */
  toJSON() {
    return this.rootName;
  }
}
ec.prototype.is = function(n, t) {
  return t ? t === this.name && (n === "rootElement" || n === "model:rootElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "element" || n === "model:element") : n === "rootElement" || n === "model:rootElement" || // From super.is(). This is highly utilised method and cannot call super. See ckeditor/ckeditor5#6529.
  n === "element" || n === "model:element" || n === "node" || n === "model:node";
};
const qp = "$graveyard";
class fO extends (/* @__PURE__ */ Je()) {
  /**
  * Creates an empty document instance with no {@link #roots} (other than
  * the {@link #graveyard graveyard root}).
  */
  constructor(e) {
    super();
    /**
    * The {@link module:engine/model/model~Model model} that the document is a part of.
    */
    f(this, "model");
    /**
    * The document's history.
    */
    f(this, "history");
    /**
    * The selection in this document.
    */
    f(this, "selection");
    /**
    * A list of roots that are owned and managed by this document. Use {@link #createRoot}, {@link #getRoot} and
    * {@link #getRootNames} to manipulate it.
    */
    f(this, "roots");
    /**
    * The model differ object. Its role is to buffer changes done on the model document and then calculate a diff of those changes.
    */
    f(this, "differ");
    /**
    * Defines whether the document is in a read-only mode.
    *
    * The user should not be able to change the data of a document that is read-only.
    *
    * @readonly
    */
    f(this, "isReadOnly");
    /**
    * Post-fixer callbacks registered to the model document.
    */
    f(this, "_postFixers");
    /**
    * A flag that indicates whether the selection has changed since last change block.
    */
    f(this, "_hasSelectionChangedFromTheLastChangeBlock");
    this.model = e, this.history = new hO(), this.selection = new Xi(this), this.roots = new bt({
      idProperty: "rootName"
    }), this.differ = new fh(e.markers), this.isReadOnly = !1, this._postFixers = /* @__PURE__ */ new Set(), this._hasSelectionChangedFromTheLastChangeBlock = !1, this.createRoot("$root", qp), this.listenTo(e, "applyOperation", (i, s) => {
      const r = s[0];
      r.isDocumentOperation && this.differ.bufferOperation(r);
    }, {
      priority: "high"
    }), this.listenTo(e, "applyOperation", (i, s) => {
      const r = s[0];
      r.isDocumentOperation && this.history.addOperation(r);
    }, {
      priority: "low"
    }), this.listenTo(this.selection, "change", () => {
      this._hasSelectionChangedFromTheLastChangeBlock = !0;
    }), this.listenTo(e.markers, "update", (i, s, r, o, a) => {
      const l = {
        ...s.getData(),
        range: o
      };
      this.differ.bufferMarkerChange(s.name, a, l), r === null && s.on("change", (c, u) => {
        const d = s.getData();
        this.differ.bufferMarkerChange(s.name, {
          ...d,
          range: u
        }, d);
      });
    }), this.registerPostFixer((i) => {
      let s = !1;
      for (const r of this.roots)
        !r.isAttached() && !r.isEmpty && (i.remove(i.createRangeIn(r)), s = !0);
      for (const r of this.model.markers)
        r.getRange().root.isAttached() || (i.removeMarker(r), s = !0);
      return s;
    });
  }
  /**
  * The document version. Every applied operation increases the version number. It is used to
  * ensure that operations are applied on a proper document version.
  *
  * This property is equal to {@link module:engine/model/history~History#version `model.Document#history#version`}.
  *
  * If the {@link module:engine/model/operation/operation~Operation#baseVersion base version} does not match the document version,
  * a {@link module:utils/ckeditorerror~CKEditorError model-document-applyoperation-wrong-version} error is thrown.
  */
  get version() {
    return this.history.version;
  }
  set version(e) {
    this.history.version = e;
  }
  /**
  * The graveyard tree root. A document always has a graveyard root that stores removed nodes.
  */
  get graveyard() {
    return this.getRoot(qp);
  }
  /**
  * Creates a new root.
  *
  * **Note:** do not use this method after the editor has been initialized! If you want to dynamically add a root, use
  * {@link module:engine/model/writer~Writer#addRoot `model.Writer#addRoot`} instead.
  *
  * @param elementName The element name. Defaults to `'$root'` which also has some basic schema defined
  * (e.g. `$block` elements are allowed inside the `$root`). Make sure to define a proper schema if you use a different name.
  * @param rootName A unique root name.
  * @returns The created root.
  */
  createRoot(e = "$root", i = "main") {
    if (this.roots.get(i))
      throw new S("model-document-createroot-name-exists", this, {
        name: i
      });
    const s = new ec(this, e, i);
    return this.roots.add(s), s;
  }
  /**
  * Removes all event listeners set by the document instance.
  */
  destroy() {
    this.selection.destroy(), this.stopListening();
  }
  /**
  * Returns a root by its name.
  *
  * Detached roots are returned by this method. This is to be able to operate on the detached root (for example, to be able to create
  * a position inside such a root for undo feature purposes).
  *
  * @param name The root name of the root to return.
  * @returns The root registered under a given name or `null` when there is no root with the given name.
  */
  getRoot(e = "main") {
    return this.roots.get(e);
  }
  /**
  * Returns an array with names of all roots added to the document (except the {@link #graveyard graveyard root}).
  *
  * Detached roots **are not** returned by this method by default. This is to make sure that all features or algorithms that operate
  * on the document data know which roots are still a part of the document and should be processed.
  *
  * @param includeDetached Specified whether detached roots should be returned as well.
  */
  getRootNames(e = !1) {
    return this.getRoots(e).map((i) => i.rootName);
  }
  /**
  * Returns an array with all roots added to the document (except the {@link #graveyard graveyard root}).
  *
  * Detached roots **are not** returned by this method by default. This is to make sure that all features or algorithms that operate
  * on the document data know which roots are still a part of the document and should be processed.
  *
  * @param includeDetached Specified whether detached roots should be returned as well.
  */
  getRoots(e = !1) {
    return this.roots.filter((i) => i != this.graveyard && (e || i.isAttached()) && i._isLoaded);
  }
  /**
  * Used to register a post-fixer callback. A post-fixer mechanism guarantees that the features
  * will operate on a correct model state.
  *
  * An execution of a feature may lead to an incorrect document tree state. The callbacks are used to fix the document tree after
  * it has changed. Post-fixers are fired just after all changes from the outermost change block were applied but
  * before the {@link module:engine/model/document~Document#event:change change event} is fired. If a post-fixer callback made
  * a change, it should return `true`. When this happens, all post-fixers are fired again to check if something else should
  * not be fixed in the new document tree state.
  *
  * As a parameter, a post-fixer callback receives a {@link module:engine/model/writer~Writer writer} instance connected with the
  * executed changes block. Thanks to that, all changes done by the callback will be added to the same
  * {@link module:engine/model/batch~Batch batch} (and undo step) as the original changes. This makes post-fixer changes transparent
  * for the user.
  *
  * An example of a post-fixer is a callback that checks if all the data were removed from the editor. If so, the
  * callback should add an empty paragraph so that the editor is never empty:
  *
  * ```ts
  * document.registerPostFixer( writer => {
  * 	const changes = document.differ.getChanges();
  *
  * 	// Check if the changes lead to an empty root in the editor.
  * 	for ( const entry of changes ) {
  * 		if ( entry.type == 'remove' && entry.position.root.isEmpty ) {
  * 			writer.insertElement( 'paragraph', entry.position.root, 0 );
  *
  * 			// It is fine to return early, even if multiple roots would need to be fixed.
  * 			// All post-fixers will be fired again, so if there are more empty roots, those will be fixed, too.
  * 			return true;
  * 		}
  * 	}
  *
  * 	return false;
  * } );
  * ```
  */
  registerPostFixer(e) {
    this._postFixers.add(e);
  }
  /**
  * A custom `toJSON()` method to solve child-parent circular dependencies.
  *
  * @returns A clone of this object with the document property changed to a string.
  */
  toJSON() {
    const e = Mv(this);
    return e.selection = "[engine.model.DocumentSelection]", e.model = "[engine.model.Model]", e;
  }
  /**
  * Check if there were any changes done on document, and if so, call post-fixers,
  * fire `change` event for features and conversion and then reset the differ.
  * Fire `change:data` event when at least one operation or buffered marker changes the data.
  *
  * @internal
  * @fires change
  * @fires change:data
  * @param writer The writer on which post-fixers will be called.
  */
  _handleChangeBlock(e) {
    this._hasDocumentChangedFromTheLastChangeBlock() && (this._callPostFixers(e), this.selection.refresh(), this.differ.hasDataChanges() ? this.fire("change:data", e.batch) : this.fire("change", e.batch), this.selection.refresh(), this.differ.reset()), this._hasSelectionChangedFromTheLastChangeBlock = !1;
  }
  /**
  * Returns whether there is a buffered change or if the selection has changed from the last
  * {@link module:engine/model/model~Model#enqueueChange `enqueueChange()` block}
  * or {@link module:engine/model/model~Model#change `change()` block}.
  *
  * @returns Returns `true` if document has changed from the last `change()` or `enqueueChange()` block.
  */
  _hasDocumentChangedFromTheLastChangeBlock() {
    return !this.differ.isEmpty || this._hasSelectionChangedFromTheLastChangeBlock;
  }
  /**
  * Returns the default root for this document which is either the first root that was added to the document using
  * {@link #createRoot} or the {@link #graveyard graveyard root} if no other roots were created.
  *
  * @returns The default root for this document.
  */
  _getDefaultRoot() {
    const e = this.getRoots();
    return e.length ? e[0] : this.graveyard;
  }
  /**
  * Returns the default range for this selection. The default range is a collapsed range that starts and ends
  * at the beginning of this selection's document {@link #_getDefaultRoot default root}.
  *
  * @internal
  */
  _getDefaultRange() {
    const e = this._getDefaultRoot(), i = this.model, s = i.schema, r = i.createPositionFromPath(e, [
      0
    ]);
    return s.getNearestSelectionRange(r) || i.createRange(r);
  }
  /**
  * Checks whether a given {@link module:engine/model/range~Range range} is a valid range for
  * the {@link #selection document's selection}.
  *
  * @internal
  * @param range A range to check.
  * @returns `true` if `range` is valid, `false` otherwise.
  */
  _validateSelectionRange(e) {
    return e.start.isValid() && e.end.isValid() && Gp(e.start) && Gp(e.end);
  }
  /**
  * Performs post-fixer loops. Executes post-fixer callbacks as long as none of them has done any changes to the model.
  *
  * @param writer The writer on which post-fixer callbacks will be called.
  */
  _callPostFixers(e) {
    let i = !1;
    do
      for (const s of this._postFixers)
        if (this.selection.refresh(), i = s(e), i)
          break;
    while (i);
  }
}
function Gp(n) {
  const t = n.textNode;
  if (t) {
    const e = t.data, i = n.offset - t.startOffset;
    return !kf(e, i) && !Cf(e, i);
  }
  return !0;
}
class mO extends (/* @__PURE__ */ Je()) {
  constructor() {
    super(...arguments);
    /**
    * Stores {@link ~Marker markers} added to the collection.
    */
    f(this, "_markers", /* @__PURE__ */ new Map());
  }
  /**
  * Iterable interface.
  *
  * Iterates over all {@link ~Marker markers} added to the collection.
  */
  [Symbol.iterator]() {
    return this._markers.values();
  }
  /**
  * Checks if given {@link ~Marker marker} or marker name is in the collection.
  *
  * @param markerOrName Name of marker or marker instance to check.
  * @returns `true` if marker is in the collection, `false` otherwise.
  */
  has(e) {
    const i = e instanceof Cr ? e.name : e;
    return this._markers.has(i);
  }
  /**
  * Returns {@link ~Marker marker} with given `markerName`.
  *
  * @param markerName Name of marker to get.
  * @returns Marker with given name or `null` if such marker was
  * not added to the collection.
  */
  get(e) {
    return this._markers.get(e) || null;
  }
  /**
  * Creates and adds a {@link ~Marker marker} to the `MarkerCollection` with given name on given
  * {@link module:engine/model/range~Range range}.
  *
  * If `MarkerCollection` already had a marker with given name (or {@link ~Marker marker} was passed), the marker in
  * collection is updated and {@link module:engine/model/markercollection~MarkerCollection#event:update} event is fired
  * but only if there was a change (marker range or {@link module:engine/model/markercollection~Marker#managedUsingOperations}
  * flag has changed.
  *
  * @internal
  * @fires update
  * @param markerOrName Name of marker to set or marker instance to update.
  * @param range Marker range.
  * @param managedUsingOperations Specifies whether the marker is managed using operations.
  * @param affectsData Specifies whether the marker affects the data produced by the data pipeline
  * (is persisted in the editor's data).
  * @returns `Marker` instance which was added or updated.
  */
  _set(e, i, s = !1, r = !1) {
    const o = e instanceof Cr ? e.name : e;
    if (o.includes(","))
      throw new S("markercollection-incorrect-marker-name", this);
    const a = this._markers.get(o);
    if (a) {
      const u = a.getData(), d = a.getRange();
      let h = !1;
      return d.isEqual(i) || (a._attachLiveRange(ni.fromRange(i)), h = !0), s != a.managedUsingOperations && (a._managedUsingOperations = s, h = !0), typeof r == "boolean" && r != a.affectsData && (a._affectsData = r, h = !0), h && this.fire(`update:${o}`, a, d, i, u), a;
    }
    const l = ni.fromRange(i), c = new Cr(o, l, s, r);
    return this._markers.set(o, c), this.fire(`update:${o}`, c, null, i, {
      ...c.getData(),
      range: null
    }), c;
  }
  /**
  * Removes given {@link ~Marker marker} or a marker with given name from the `MarkerCollection`.
  *
  * @internal
  * @fires update
  * @param markerOrName Marker or name of a marker to remove.
  * @returns `true` if marker was found and removed, `false` otherwise.
  */
  _remove(e) {
    const i = e instanceof Cr ? e.name : e, s = this._markers.get(i);
    return s ? (this._markers.delete(i), this.fire(`update:${i}`, s, s.getRange(), null, s.getData()), this._destroyMarker(s), !0) : !1;
  }
  /**
  * Fires an {@link module:engine/model/markercollection~MarkerCollection#event:update} event for the given {@link ~Marker marker}
  * but does not change the marker. Useful to force {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher downcast
  * conversion} for the marker.
  *
  * @internal
  * @fires update
  * @param markerOrName Marker or name of a marker to refresh.
  */
  _refresh(e) {
    const i = e instanceof Cr ? e.name : e, s = this._markers.get(i);
    if (!s)
      throw new S("markercollection-refresh-marker-not-exists", this);
    const r = s.getRange();
    this.fire(`update:${i}`, s, r, r, s.getData());
  }
  /**
  * Returns iterator that iterates over all markers, which ranges contain given {@link module:engine/model/position~Position position}.
  */
  *getMarkersAtPosition(e) {
    for (const i of this)
      i.getRange().containsPosition(e) && (yield i);
  }
  /**
  * Returns iterator that iterates over all markers, which intersects with given {@link module:engine/model/range~Range range}.
  */
  *getMarkersIntersectingRange(e) {
    for (const i of this)
      i.getRange().getIntersection(e) !== null && (yield i);
  }
  /**
  * Destroys marker collection and all markers inside it.
  */
  destroy() {
    for (const e of this._markers.values())
      this._destroyMarker(e);
    this._markers = null, this.stopListening();
  }
  /**
  * Iterates over all markers that starts with given `prefix`.
  *
  * ```ts
  * const markerFooA = markersCollection._set( 'foo:a', rangeFooA );
  * const markerFooB = markersCollection._set( 'foo:b', rangeFooB );
  * const markerBarA = markersCollection._set( 'bar:a', rangeBarA );
  * const markerFooBarA = markersCollection._set( 'foobar:a', rangeFooBarA );
  * Array.from( markersCollection.getMarkersGroup( 'foo' ) ); // [ markerFooA, markerFooB ]
  * Array.from( markersCollection.getMarkersGroup( 'a' ) ); // []
  * ```
  */
  *getMarkersGroup(e) {
    for (const i of this._markers.values())
      i.name.startsWith(e + ":") && (yield i);
  }
  /**
  * Destroys the marker.
  */
  _destroyMarker(e) {
    e.stopListening(), e._detachLiveRange();
  }
}
class Cr extends (/* @__PURE__ */ Je(bs)) {
  /**
  * Creates a marker instance.
  *
  * @param name Marker name.
  * @param liveRange Range marked by the marker.
  * @param managedUsingOperations Specifies whether the marker is managed using operations.
  * @param affectsData Specifies whether the marker affects the data produced by the data pipeline (is persisted in the editor's data).
  */
  constructor(e, i, s, r) {
    super();
    /**
    * Marker's name.
    */
    f(this, "name");
    /**
    * Flag indicates if the marker is managed using operations or not.
    *
    * @internal
    */
    f(this, "_managedUsingOperations");
    /**
    * Specifies whether the marker affects the data produced by the data pipeline
    * (is persisted in the editor's data).
    *
    * @internal
    */
    f(this, "_affectsData");
    /**
    * Range marked by the marker.
    */
    f(this, "_liveRange");
    this.name = e, this._liveRange = this._attachLiveRange(i), this._managedUsingOperations = s, this._affectsData = r;
  }
  /**
  * A value indicating if the marker is managed using operations.
  * See {@link ~Marker marker class description} to learn more about marker types.
  * See {@link module:engine/model/writer~Writer#addMarker}.
  */
  get managedUsingOperations() {
    if (!this._liveRange)
      throw new S("marker-destroyed", this);
    return this._managedUsingOperations;
  }
  /**
  * A value indicating if the marker changes the data.
  */
  get affectsData() {
    if (!this._liveRange)
      throw new S("marker-destroyed", this);
    return this._affectsData;
  }
  /**
  * Returns the marker data (properties defining the marker).
  */
  getData() {
    return {
      range: this.getRange(),
      affectsData: this.affectsData,
      managedUsingOperations: this.managedUsingOperations
    };
  }
  /**
  * Returns current marker start position.
  */
  getStart() {
    if (!this._liveRange)
      throw new S("marker-destroyed", this);
    return this._liveRange.start.clone();
  }
  /**
  * Returns current marker end position.
  */
  getEnd() {
    if (!this._liveRange)
      throw new S("marker-destroyed", this);
    return this._liveRange.end.clone();
  }
  /**
  * Returns a range that represents the current state of the marker.
  *
  * Keep in mind that returned value is a {@link module:engine/model/range~Range Range}, not a
  * {@link module:engine/model/liverange~LiveRange LiveRange}. This means that it is up-to-date and relevant only
  * until next model document change. Do not store values returned by this method. Instead, store {@link ~Marker#name}
  * and get `Marker` instance from {@link module:engine/model/markercollection~MarkerCollection MarkerCollection} every
  * time there is a need to read marker properties. This will guarantee that the marker has not been removed and
  * that it's data is up-to-date.
  */
  getRange() {
    if (!this._liveRange)
      throw new S("marker-destroyed", this);
    return this._liveRange.toRange();
  }
  /**
  * Binds new live range to the marker and detach the old one if is attached.
  *
  * @internal
  * @param liveRange Live range to attach
  * @returns Attached live range.
  */
  _attachLiveRange(e) {
    return this._liveRange && this._detachLiveRange(), e.delegate("change:range").to(this), e.delegate("change:content").to(this), this._liveRange = e, e;
  }
  /**
  * Unbinds and destroys currently attached live range.
  *
  * @internal
  */
  _detachLiveRange() {
    this._liveRange.stopDelegating("change:range", this), this._liveRange.stopDelegating("change:content", this), this._liveRange.detach(), this._liveRange = null;
  }
}
Cr.prototype.is = function(n) {
  return n === "marker" || n === "model:marker";
};
class gO extends Ii {
  /**
  * Creates an insert operation.
  *
  * @param sourcePosition Position before the first {@link module:engine/model/item~Item model item} to move.
  * @param howMany Offset size of moved range. Moved range will start from `sourcePosition` and end at
  * `sourcePosition` with offset shifted by `howMany`.
  */
  constructor(e, i) {
    super(null);
    /**
    * Position before the first {@link module:engine/model/item~Item model item} to detach.
    */
    f(this, "sourcePosition");
    /**
    * Offset size of moved range.
    */
    f(this, "howMany");
    this.sourcePosition = e.clone(), this.howMany = i;
  }
  /**
  * @inheritDoc
  */
  get type() {
    return "detach";
  }
  /**
  * @inheritDoc
  */
  get affectedSelectable() {
    return null;
  }
  /**
  * @inheritDoc
  */
  toJSON() {
    const e = super.toJSON();
    return e.sourcePosition = this.sourcePosition.toJSON(), e;
  }
  /**
  * @inheritDoc
  * @internal
  */
  _validate() {
    if (this.sourcePosition.root.document)
      throw new S("detach-operation-on-document-node", this);
  }
  /**
  * @inheritDoc
  * @internal
  */
  _execute() {
    X_(F._createFromPositionAndShift(this.sourcePosition, this.howMany));
  }
  /**
  * @inheritDoc
  */
  static get className() {
    return "DetachOperation";
  }
}
class dn extends bs {
  /**
  * Creates an empty `DocumentFragment`.
  *
  * **Note:** Constructor of this class shouldn't be used directly in the code.
  * Use the {@link module:engine/model/writer~Writer#createDocumentFragment} method instead.
  *
  * @internal
  * @param children Nodes to be contained inside the `DocumentFragment`.
  */
  constructor(e) {
    super();
    /**
    * DocumentFragment static markers map. This is a list of names and {@link module:engine/model/range~Range ranges}
    * which will be set as Markers to {@link module:engine/model/model~Model#markers model markers collection}
    * when DocumentFragment will be inserted to the document.
    */
    f(this, "markers", /* @__PURE__ */ new Map());
    /**
    * List of nodes contained inside the document fragment.
    */
    f(this, "_children", new jo());
    e && this._insertChild(0, e);
  }
  /**
  * Returns an iterator that iterates over all nodes contained inside this document fragment.
  */
  [Symbol.iterator]() {
    return this.getChildren();
  }
  /**
  * Number of this document fragment's children.
  */
  get childCount() {
    return this._children.length;
  }
  /**
  * Sum of {@link module:engine/model/node~Node#offsetSize offset sizes} of all of this document fragment's children.
  */
  get maxOffset() {
    return this._children.maxOffset;
  }
  /**
  * Is `true` if there are no nodes inside this document fragment, `false` otherwise.
  */
  get isEmpty() {
    return this.childCount === 0;
  }
  /**
  * Artificial next sibling. Returns `null`. Added for compatibility reasons.
  */
  get nextSibling() {
    return null;
  }
  /**
  * Artificial previous sibling. Returns `null`. Added for compatibility reasons.
  */
  get previousSibling() {
    return null;
  }
  /**
  * Artificial root of `DocumentFragment`. Returns itself. Added for compatibility reasons.
  */
  get root() {
    return this;
  }
  /**
  * Artificial parent of `DocumentFragment`. Returns `null`. Added for compatibility reasons.
  */
  get parent() {
    return null;
  }
  /**
  * Artificial owner of `DocumentFragment`. Returns `null`. Added for compatibility reasons.
  */
  get document() {
    return null;
  }
  /**
  * Returns `false` as `DocumentFragment` by definition is not attached to a document. Added for compatibility reasons.
  */
  isAttached() {
    return !1;
  }
  /**
  * Returns empty array. Added for compatibility reasons.
  */
  getAncestors() {
    return [];
  }
  /**
  * Gets the child at the given index. Returns `null` if incorrect index was passed.
  *
  * @param index Index in this document fragment.
  * @returns Child node.
  */
  getChild(e) {
    return this._children.getNode(e);
  }
  /**
  * Gets the child at the given offset. Returns `null` if incorrect index was passed.
  *
  * @param offset Offset in this document fragment.
  * @returns Child node.
  */
  getChildAtOffset(e) {
    return this._children.getNodeAtOffset(e);
  }
  /**
  * Returns an iterator that iterates over all of this document fragment's children.
  */
  getChildren() {
    return this._children[Symbol.iterator]();
  }
  /**
  * Returns an index of the given child node. Returns `null` if given node is not a child of this document fragment.
  *
  * @param node Child node to look for.
  * @returns Child node's index.
  */
  getChildIndex(e) {
    return this._children.getNodeIndex(e);
  }
  /**
  * Returns the starting offset of given child. Starting offset is equal to the sum of
  * {@link module:engine/model/node~Node#offsetSize offset sizes} of all node's siblings that are before it. Returns `null` if
  * given node is not a child of this document fragment.
  *
  * @param node Child node to look for.
  * @returns Child node's starting offset.
  */
  getChildStartOffset(e) {
    return this._children.getNodeStartOffset(e);
  }
  /**
  * Returns path to a `DocumentFragment`, which is an empty array. Added for compatibility reasons.
  */
  getPath() {
    return [];
  }
  /**
  * Returns a descendant node by its path relative to this element.
  *
  * ```ts
  * // <this>a<b>c</b></this>
  * this.getNodeByPath( [ 0 ] );     // -> "a"
  * this.getNodeByPath( [ 1 ] );     // -> <b>
  * this.getNodeByPath( [ 1, 0 ] );  // -> "c"
  * ```
  *
  * @param relativePath Path of the node to find, relative to this element.
  */
  getNodeByPath(e) {
    let i = this;
    for (const s of e)
      i = i.getChildAtOffset(s);
    return i;
  }
  /**
  * Converts offset "position" to index "position".
  *
  * Returns index of a node that occupies given offset. If given offset is too low, returns `0`. If given offset is
  * too high, returns index after last child.
  *
  * ```ts
  * const textNode = new Text( 'foo' );
  * const pElement = new Element( 'p' );
  * const docFrag = new DocumentFragment( [ textNode, pElement ] );
  * docFrag.offsetToIndex( -1 ); // Returns 0, because offset is too low.
  * docFrag.offsetToIndex( 0 ); // Returns 0, because offset 0 is taken by `textNode` which is at index 0.
  * docFrag.offsetToIndex( 1 ); // Returns 0, because `textNode` has `offsetSize` equal to 3, so it occupies offset 1 too.
  * docFrag.offsetToIndex( 2 ); // Returns 0.
  * docFrag.offsetToIndex( 3 ); // Returns 1.
  * docFrag.offsetToIndex( 4 ); // Returns 2. There are no nodes at offset 4, so last available index is returned.
  * ```
  *
  * @param offset Offset to look for.
  * @returns Index of a node that occupies given offset.
  */
  offsetToIndex(e) {
    return this._children.offsetToIndex(e);
  }
  /**
  * Converts `DocumentFragment` instance to plain object and returns it.
  * Takes care of converting all of this document fragment's children.
  *
  * @returns `DocumentFragment` instance converted to plain object.
  */
  toJSON() {
    const e = [];
    for (const i of this._children)
      e.push(i.toJSON());
    return e;
  }
  /**
  * Creates a `DocumentFragment` instance from given plain object (i.e. parsed JSON string).
  * Converts `DocumentFragment` children to proper nodes.
  *
  * @param json Plain object to be converted to `DocumentFragment`.
  * @returns `DocumentFragment` instance created using given plain object.
  */
  static fromJSON(e) {
    const i = [];
    for (const s of e)
      s.name ? i.push(et.fromJSON(s)) : i.push(lt.fromJSON(s));
    return new dn(i);
  }
  /**
  * {@link #_insertChild Inserts} one or more nodes at the end of this document fragment.
  *
  * @internal
  * @param items Items to be inserted.
  */
  _appendChild(e) {
    this._insertChild(this.childCount, e);
  }
  /**
  * Inserts one or more nodes at the given index and sets {@link module:engine/model/node~Node#parent parent} of these nodes
  * to this document fragment.
  *
  * @internal
  * @param index Index at which nodes should be inserted.
  * @param items Items to be inserted.
  */
  _insertChild(e, i) {
    const s = pO(i);
    for (const r of s)
      r.parent !== null && r._remove(), r.parent = this;
    this._children._insertNodes(e, s);
  }
  /**
  * Removes one or more nodes starting at the given index
  * and sets {@link module:engine/model/node~Node#parent parent} of these nodes to `null`.
  *
  * @internal
  * @param index Index of the first node to remove.
  * @param howMany Number of nodes to remove.
  * @returns Array containing removed nodes.
  */
  _removeChildren(e, i = 1) {
    const s = this._children._removeNodes(e, i);
    for (const r of s)
      r.parent = null;
    return s;
  }
  /**
  * Removes children nodes provided as an array and sets
  * the {@link module:engine/model/node~Node#parent parent} of these nodes to `null`.
  *
  * These nodes do not need to be direct siblings.
  *
  * This method is faster than removing nodes one by one, as it recalculates offsets only once.
  *
  * @internal
  * @param nodes Array of nodes.
  */
  _removeChildrenArray(e) {
    this._children._removeNodesArray(e);
    for (const i of e)
      i.parent = null;
  }
}
dn.prototype.is = function(n) {
  return n === "documentFragment" || n === "model:documentFragment";
};
function pO(n) {
  return typeof n == "string" ? [
    new lt(n)
  ] : (Ai(n) || (n = [
    n
  ]), Array.from(n).map((t) => typeof t == "string" ? new lt(t) : t instanceof Ki ? new lt(t.data, t.getAttributes()) : t));
}
class bO {
  /**
  * Creates a writer instance.
  *
  * **Note:** It is not recommended to use it directly. Use {@link module:engine/model/model~Model#change `Model#change()`} or
  * {@link module:engine/model/model~Model#enqueueChange `Model#enqueueChange()`} instead.
  *
  * @internal
  */
  constructor(t, e) {
    /**
    * Instance of the model on which this writer operates.
    */
    f(this, "model");
    /**
    * The batch to which this writer will add changes.
    */
    f(this, "batch");
    this.model = t, this.batch = e;
  }
  /**
  * Creates a new {@link module:engine/model/text~Text text node}.
  *
  * ```ts
  * writer.createText( 'foo' );
  * writer.createText( 'foo', { bold: true } );
  * ```
  *
  * @param data Text data.
  * @param attributes Text attributes.
  * @returns {module:engine/model/text~Text} Created text node.
  */
  createText(t, e) {
    return new lt(t, e);
  }
  /**
  * Creates a new {@link module:engine/model/element~Element element}.
  *
  * ```ts
  * writer.createElement( 'paragraph' );
  * writer.createElement( 'paragraph', { alignment: 'center' } );
  * ```
  *
  * @param name Name of the element.
  * @param attributes Elements attributes.
  * @returns Created element.
  */
  createElement(t, e) {
    return new et(t, e);
  }
  /**
  * Creates a new {@link module:engine/model/documentfragment~DocumentFragment document fragment}.
  *
  * @returns Created document fragment.
  */
  createDocumentFragment() {
    return new dn();
  }
  /**
  * Creates a copy of the element and returns it. Created element has the same name and attributes as the original element.
  * If clone is deep, the original element's children are also cloned. If not, then empty element is returned.
  *
  * @param element The element to clone.
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any child.
  */
  cloneElement(t, e = !0) {
    return t._clone(e);
  }
  /**
  * Inserts item on given position.
  *
  * ```ts
  * const paragraph = writer.createElement( 'paragraph' );
  * writer.insert( paragraph, position );
  * ```
  *
  * Instead of using position you can use parent and offset:
  *
  * ```ts
  * const text = writer.createText( 'foo' );
  * writer.insert( text, paragraph, 5 );
  * ```
  *
  * You can also use `end` instead of the offset to insert at the end:
  *
  * ```ts
  * const text = writer.createText( 'foo' );
  * writer.insert( text, paragraph, 'end' );
  * ```
  *
  * Or insert before or after another element:
  *
  * ```ts
  * const paragraph = writer.createElement( 'paragraph' );
  * writer.insert( paragraph, anotherParagraph, 'after' );
  * ```
  *
  * These parameters works the same way as {@link #createPositionAt `writer.createPositionAt()`}.
  *
  * Note that if the item already has parent it will be removed from the previous parent.
  *
  * Note that you cannot re-insert a node from a document to a different document or a document fragment. In this case,
  * `model-writer-insert-forbidden-move` is thrown.
  *
  * If you want to move {@link module:engine/model/range~Range range} instead of an
  * {@link module:engine/model/item~Item item} use {@link module:engine/model/writer~Writer#move `Writer#move()`}.
  *
  * **Note:** For a paste-like content insertion mechanism see
  * {@link module:engine/model/model~Model#insertContent `model.insertContent()`}.
  *
  * @param item Item or document fragment to insert.
  * @param offset Offset or one of the flags. Used only when second parameter is a {@link module:engine/model/item~Item model item}.
  */
  insert(t, e, i = 0) {
    if (this._assertWriterUsedCorrectly(), t instanceof lt && t.data == "")
      return;
    const s = G._createAt(e, i);
    if (t.parent)
      if (Yp(t.root, s.root)) {
        this.move(F._createOn(t), s);
        return;
      } else {
        if (t.root.document)
          throw new S("model-writer-insert-forbidden-move", this);
        this.remove(t);
      }
    const r = s.root.document ? s.root.document.version : null, o = t instanceof dn ? t._removeChildren(0, t.childCount) : t, a = new Ht(s, o, r);
    if (t instanceof lt && (a.shouldReceiveAttributes = !0), this.batch.addOperation(a), this.model.applyOperation(a), t instanceof dn)
      for (const [l, c] of t.markers) {
        const u = G._createAt(c.root, 0), h = {
          range: new F(c.start._getCombined(u, s), c.end._getCombined(u, s)),
          usingOperation: !0,
          affectsData: !0
        };
        this.model.markers.has(l) ? this.updateMarker(l, h) : this.addMarker(l, h);
      }
  }
  insertText(t, e, i, s) {
    e instanceof dn || e instanceof et || e instanceof G ? this.insert(this.createText(t), e, i) : this.insert(this.createText(t, e), i, s);
  }
  insertElement(t, e, i, s) {
    e instanceof dn || e instanceof et || e instanceof G ? this.insert(this.createElement(t), e, i) : this.insert(this.createElement(t, e), i, s);
  }
  /**
  * Inserts item at the end of the given parent.
  *
  * ```ts
  * const paragraph = writer.createElement( 'paragraph' );
  * writer.append( paragraph, root );
  * ```
  *
  * Note that if the item already has parent it will be removed from the previous parent.
  *
  * If you want to move {@link module:engine/model/range~Range range} instead of an
  * {@link module:engine/model/item~Item item} use {@link module:engine/model/writer~Writer#move `Writer#move()`}.
  *
  * @param item Item or document fragment to insert.
  */
  append(t, e) {
    this.insert(t, e, "end");
  }
  appendText(t, e, i) {
    e instanceof dn || e instanceof et ? this.insert(this.createText(t), e, "end") : this.insert(this.createText(t, e), i, "end");
  }
  appendElement(t, e, i) {
    e instanceof dn || e instanceof et ? this.insert(this.createElement(t), e, "end") : this.insert(this.createElement(t, e), i, "end");
  }
  /**
  * Sets value of the attribute with given key on a {@link module:engine/model/item~Item model item}
  * or on a {@link module:engine/model/range~Range range}.
  *
  * @param key Attribute key.
  * @param value Attribute new value.
  * @param itemOrRange Model item or range on which the attribute will be set.
  */
  setAttribute(t, e, i) {
    if (this._assertWriterUsedCorrectly(), i instanceof F) {
      const s = i.getMinimalFlatRanges();
      for (const r of s)
        Kp(this, t, e, r);
    } else
      Zp(this, t, e, i);
  }
  /**
  * Sets values of attributes on a {@link module:engine/model/item~Item model item}
  * or on a {@link module:engine/model/range~Range range}.
  *
  * ```ts
  * writer.setAttributes( {
  * 	bold: true,
  * 	italic: true
  * }, range );
  * ```
  *
  * @param attributes Attributes keys and values.
  * @param itemOrRange Model item or range on which the attributes will be set.
  */
  setAttributes(t, e) {
    for (const [i, s] of Ji(t))
      this.setAttribute(i, s, e);
  }
  /**
  * Removes an attribute with given key from a {@link module:engine/model/item~Item model item}
  * or from a {@link module:engine/model/range~Range range}.
  *
  * @param key Attribute key.
  * @param itemOrRange Model item or range from which the attribute will be removed.
  */
  removeAttribute(t, e) {
    if (this._assertWriterUsedCorrectly(), e instanceof F) {
      const i = e.getMinimalFlatRanges();
      for (const s of i)
        Kp(this, t, null, s);
    } else
      Zp(this, t, null, e);
  }
  /**
  * Removes all attributes from all elements in the range or from the given item.
  *
  * @param itemOrRange Model item or range from which all attributes will be removed.
  */
  clearAttributes(t) {
    this._assertWriterUsedCorrectly();
    const e = (i) => {
      for (const s of i.getAttributeKeys())
        this.removeAttribute(s, i);
    };
    if (!(t instanceof F))
      e(t);
    else
      for (const i of t.getItems())
        e(i);
  }
  /**
  * Moves all items in the source range to the target position.
  *
  * ```ts
  * writer.move( sourceRange, targetPosition );
  * ```
  *
  * Instead of the target position you can use parent and offset or define that range should be moved to the end
  * or before or after chosen item:
  *
  * ```ts
  * // Moves all items in the range to the paragraph at offset 5:
  * writer.move( sourceRange, paragraph, 5 );
  * // Moves all items in the range to the end of a blockquote:
  * writer.move( sourceRange, blockquote, 'end' );
  * // Moves all items in the range to a position after an image:
  * writer.move( sourceRange, image, 'after' );
  * ```
  *
  * These parameters work the same way as {@link #createPositionAt `writer.createPositionAt()`}.
  *
  * Note that items can be moved only within the same tree. It means that you can move items within the same root
  * (element or document fragment) or between {@link module:engine/model/document~Document#roots documents roots},
  * but you can not move items from document fragment to the document or from one detached element to another. Use
  * {@link module:engine/model/writer~Writer#insert} in such cases.
  *
  * @param range Source range.
  * @param offset Offset or one of the flags. Used only when second parameter is a {@link module:engine/model/item~Item model item}.
  */
  move(t, e, i) {
    if (this._assertWriterUsedCorrectly(), !(t instanceof F))
      throw new S("writer-move-invalid-range", this);
    if (!t.isFlat)
      throw new S("writer-move-range-not-flat", this);
    const s = G._createAt(e, i);
    if (s.isEqual(t.start))
      return;
    if (this._addOperationForAffectedMarkers("move", t), !Yp(t.root, s.root))
      throw new S("writer-move-different-document", this);
    const r = t.root.document ? t.root.document.version : null, o = new He(t.start, t.end.offset - t.start.offset, s, r);
    this.batch.addOperation(o), this.model.applyOperation(o);
  }
  /**
  * Removes given model {@link module:engine/model/item~Item item} or {@link module:engine/model/range~Range range}.
  *
  * @param itemOrRange Model item or range to remove.
  */
  remove(t) {
    this._assertWriterUsedCorrectly();
    const i = (t instanceof F ? t : F._createOn(t)).getMinimalFlatRanges().reverse();
    for (const s of i)
      this._addOperationForAffectedMarkers("move", s), wO(s.start, s.end.offset - s.start.offset, this.batch, this.model);
  }
  /**
  * Merges two siblings at the given position.
  *
  * Node before and after the position have to be an element. Otherwise `writer-merge-no-element-before` or
  * `writer-merge-no-element-after` error will be thrown.
  *
  * @param position Position between merged elements.
  */
  merge(t) {
    this._assertWriterUsedCorrectly();
    const e = t.nodeBefore, i = t.nodeAfter;
    if (this._addOperationForAffectedMarkers("merge", t), !(e instanceof et))
      throw new S("writer-merge-no-element-before", this);
    if (!(i instanceof et))
      throw new S("writer-merge-no-element-after", this);
    t.root.document ? this._merge(t) : this._mergeDetached(t);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createPositionFromPath `Model#createPositionFromPath()`}.
  *
  * @param root Root of the position.
  * @param path Position path. See {@link module:engine/model/position~Position#path}.
  * @param stickiness Position stickiness. See {@link module:engine/model/position~PositionStickiness}.
  */
  createPositionFromPath(t, e, i) {
    return this.model.createPositionFromPath(t, e, i);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createPositionAt `Model#createPositionAt()`}.
  *
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/model/item~Item model item}.
  */
  createPositionAt(t, e) {
    return this.model.createPositionAt(t, e);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createPositionAfter `Model#createPositionAfter()`}.
  *
  * @param item Item after which the position should be placed.
  */
  createPositionAfter(t) {
    return this.model.createPositionAfter(t);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createPositionBefore `Model#createPositionBefore()`}.
  *
  * @param item Item after which the position should be placed.
  */
  createPositionBefore(t) {
    return this.model.createPositionBefore(t);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createRange `Model#createRange()`}.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at `start` position.
  */
  createRange(t, e) {
    return this.model.createRange(t, e);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createRangeIn `Model#createRangeIn()`}.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(t) {
    return this.model.createRangeIn(t);
  }
  /**
  * Shortcut for {@link module:engine/model/model~Model#createRangeOn `Model#createRangeOn()`}.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeOn(t) {
    return this.model.createRangeOn(t);
  }
  createSelection(...t) {
    return this.model.createSelection(...t);
  }
  /**
  * Performs merge action in a detached tree.
  *
  * @param position Position between merged elements.
  */
  _mergeDetached(t) {
    const e = t.nodeBefore, i = t.nodeAfter;
    this.move(F._createIn(i), G._createAt(e, "end")), this.remove(i);
  }
  /**
  * Performs merge action in a non-detached tree.
  *
  * @param position Position between merged elements.
  */
  _merge(t) {
    const e = G._createAt(t.nodeBefore, "end"), i = G._createAt(t.nodeAfter, 0), s = t.root.document.graveyard, r = new G(s, [
      0
    ]), o = t.root.document.version, a = new _t(i, t.nodeAfter.maxOffset, e, r, o);
    this.batch.addOperation(a), this.model.applyOperation(a);
  }
  /**
  * Renames the given element.
  *
  * @param element The element to rename.
  * @param newName New element name.
  */
  rename(t, e) {
    if (this._assertWriterUsedCorrectly(), !(t instanceof et))
      throw new S("writer-rename-not-element-instance", this);
    const i = t.root.document ? t.root.document.version : null, s = new bi(G._createBefore(t), t.name, e, i);
    this.batch.addOperation(s), this.model.applyOperation(s);
  }
  /**
  * Splits elements starting from the given position and going to the top of the model tree as long as given
  * `limitElement` is reached. When `limitElement` is not defined then only the parent of the given position will be split.
  *
  * The element needs to have a parent. It cannot be a root element nor a document fragment.
  * The `writer-split-element-no-parent` error will be thrown if you try to split an element with no parent.
  *
  * @param position Position of split.
  * @param limitElement Stop splitting when this element will be reached.
  * @returns Split result with properties:
  * * `position` - Position between split elements.
  * * `range` - Range that stars from the end of the first split element and ends at the beginning of the first copy element.
  */
  split(t, e) {
    this._assertWriterUsedCorrectly();
    let i = t.parent;
    if (!i.parent)
      throw new S("writer-split-element-no-parent", this);
    if (e || (e = i.parent), !t.parent.getAncestors({
      includeSelf: !0
    }).includes(e))
      throw new S("writer-split-invalid-limit-element", this);
    let s, r;
    do {
      const o = i.root.document ? i.root.document.version : null, a = i.maxOffset - t.offset, l = tt.getInsertionPosition(t), c = new tt(t, a, l, null, o);
      this.batch.addOperation(c), this.model.applyOperation(c), !s && !r && (s = i, r = t.parent.nextSibling), t = this.createPositionAfter(t.parent), i = t.parent;
    } while (i !== e);
    return {
      position: t,
      range: new F(G._createAt(s, "end"), G._createAt(r, 0))
    };
  }
  /**
  * Wraps the given range with the given element or with a new element (if a string was passed).
  *
  * **Note:** range to wrap should be a "flat range" (see {@link module:engine/model/range~Range#isFlat `Range#isFlat`}).
  * If not, an error will be thrown.
  *
  * @param range Range to wrap.
  * @param elementOrString Element or name of element to wrap the range with.
  */
  wrap(t, e) {
    if (this._assertWriterUsedCorrectly(), !t.isFlat)
      throw new S("writer-wrap-range-not-flat", this);
    const i = e instanceof et ? e : new et(e);
    if (i.childCount > 0)
      throw new S("writer-wrap-element-not-empty", this);
    if (i.parent !== null)
      throw new S("writer-wrap-element-attached", this);
    this.insert(i, t.start);
    const s = new F(t.start.getShiftedBy(1), t.end.getShiftedBy(1));
    this.move(s, G._createAt(i, 0));
  }
  /**
  * Unwraps children of the given element – all its children are moved before it and then the element is removed.
  * Throws error if you try to unwrap an element which does not have a parent.
  *
  * @param element Element to unwrap.
  */
  unwrap(t) {
    if (this._assertWriterUsedCorrectly(), t.parent === null)
      throw new S("writer-unwrap-element-no-parent", this);
    this.move(F._createIn(t), this.createPositionAfter(t)), this.remove(t);
  }
  /**
  * Adds a {@link module:engine/model/markercollection~Marker marker}. Marker is a named range, which tracks
  * changes in the document and updates its range automatically, when model tree changes.
  *
  * As the first parameter you can set marker name.
  *
  * The required `options.usingOperation` parameter lets you decide if the marker should be managed by operations or not. See
  * {@link module:engine/model/markercollection~Marker marker class description} to learn about the difference between
  * markers managed by operations and not-managed by operations.
  *
  * The `options.affectsData` parameter, which defaults to `false`, allows you to define if a marker affects the data. It should be
  * `true` when the marker change changes the data returned by the
  * {@link module:core/editor/editor~Editor#getData `editor.getData()`} method.
  * When set to `true` it fires the {@link module:engine/model/document~Document#event:change:data `change:data`} event.
  * When set to `false` it fires the {@link module:engine/model/document~Document#event:change `change`} event.
  *
  * Create marker directly base on marker's name:
  *
  * ```ts
  * addMarker( markerName, { range, usingOperation: false } );
  * ```
  *
  * Create marker using operation:
  *
  * ```ts
  * addMarker( markerName, { range, usingOperation: true } );
  * ```
  *
  * Create marker that affects the editor data:
  *
  * ```ts
  * addMarker( markerName, { range, usingOperation: false, affectsData: true } );
  * ```
  *
  * Note: For efficiency reasons, it's best to create and keep as little markers as possible.
  *
  * @see module:engine/model/markercollection~Marker
  * @param name Name of a marker to create - must be unique.
  * @param options.usingOperation Flag indicating that the marker should be added by MarkerOperation.
  * See {@link module:engine/model/markercollection~Marker#managedUsingOperations}.
  * @param options.range Marker range.
  * @param options.affectsData Flag indicating that the marker changes the editor data.
  * @returns Marker that was set.
  */
  addMarker(t, e) {
    if (this._assertWriterUsedCorrectly(), !e || typeof e.usingOperation != "boolean")
      throw new S("writer-addmarker-no-usingoperation", this);
    const i = e.usingOperation, s = e.range, r = e.affectsData === void 0 ? !1 : e.affectsData;
    if (this.model.markers.has(t))
      throw new S("writer-addmarker-marker-exists", this);
    if (!s)
      throw new S("writer-addmarker-no-range", this);
    return i ? (wo(this, t, null, s, r), this.model.markers.get(t)) : this.model.markers._set(t, s, i, r);
  }
  /**
  * Adds, updates or refreshes a {@link module:engine/model/markercollection~Marker marker}. Marker is a named range, which tracks
  * changes in the document and updates its range automatically, when model tree changes. Still, it is possible to change the
  * marker's range directly using this method.
  *
  * As the first parameter you can set marker name or instance. If none of them is provided, new marker, with a unique
  * name is created and returned.
  *
  * **Note**: If you want to change the {@link module:engine/view/element~Element view element} of the marker while its data in the model
  * remains the same, use the dedicated {@link module:engine/controller/editingcontroller~EditingController#reconvertMarker} method.
  *
  * The `options.usingOperation` parameter lets you change if the marker should be managed by operations or not. See
  * {@link module:engine/model/markercollection~Marker marker class description} to learn about the difference between
  * markers managed by operations and not-managed by operations. It is possible to change this option for an existing marker.
  *
  * The `options.affectsData` parameter, which defaults to `false`, allows you to define if a marker affects the data. It should be
  * `true` when the marker change changes the data returned by
  * the {@link module:core/editor/editor~Editor#getData `editor.getData()`} method.
  * When set to `true` it fires the {@link module:engine/model/document~Document#event:change:data `change:data`} event.
  * When set to `false` it fires the {@link module:engine/model/document~Document#event:change `change`} event.
  *
  * Update marker directly base on marker's name:
  *
  * ```ts
  * updateMarker( markerName, { range } );
  * ```
  *
  * Update marker using operation:
  *
  * ```ts
  * updateMarker( marker, { range, usingOperation: true } );
  * updateMarker( markerName, { range, usingOperation: true } );
  * ```
  *
  * Change marker's option (start using operations to manage it):
  *
  * ```ts
  * updateMarker( marker, { usingOperation: true } );
  * ```
  *
  * Change marker's option (inform the engine, that the marker does not affect the data anymore):
  *
  * ```ts
  * updateMarker( markerName, { affectsData: false } );
  * ```
  *
  * @see module:engine/model/markercollection~Marker
  * @param markerOrName Name of a marker to update, or a marker instance.
  * @param options If options object is not defined then marker will be refreshed by triggering
  * downcast conversion for this marker with the same data.
  * @param options.range Marker range to update.
  * @param options.usingOperation Flag indicated whether the marker should be added by MarkerOperation.
  * See {@link module:engine/model/markercollection~Marker#managedUsingOperations}.
  * @param options.affectsData Flag indicating that the marker changes the editor data.
  */
  updateMarker(t, e) {
    this._assertWriterUsedCorrectly();
    const i = typeof t == "string" ? t : t.name, s = this.model.markers.get(i);
    if (!s)
      throw new S("writer-updatemarker-marker-not-exists", this);
    if (!e) {
      Ve("writer-updatemarker-reconvert-using-editingcontroller", {
        markerName: i
      }), this.model.markers._refresh(s);
      return;
    }
    const r = typeof e.usingOperation == "boolean", o = typeof e.affectsData == "boolean", a = o ? e.affectsData : s.affectsData;
    if (!r && !e.range && !o)
      throw new S("writer-updatemarker-wrong-options", this);
    const l = s.getRange(), c = e.range ? e.range : l;
    if (r && e.usingOperation !== s.managedUsingOperations) {
      e.usingOperation ? wo(this, i, null, c, a) : (wo(this, i, l, null, a), this.model.markers._set(i, c, void 0, a));
      return;
    }
    s.managedUsingOperations ? wo(this, i, l, c, a) : this.model.markers._set(i, c, void 0, a);
  }
  /**
  * Removes given {@link module:engine/model/markercollection~Marker marker} or marker with given name.
  * The marker is removed accordingly to how it has been created, so if the marker was created using operation,
  * it will be destroyed using operation.
  *
  * @param markerOrName Marker or marker name to remove.
  */
  removeMarker(t) {
    this._assertWriterUsedCorrectly();
    const e = typeof t == "string" ? t : t.name;
    if (!this.model.markers.has(e))
      throw new S("writer-removemarker-no-marker", this);
    const i = this.model.markers.get(e);
    if (!i.managedUsingOperations) {
      this.model.markers._remove(e);
      return;
    }
    const s = i.getRange();
    wo(this, e, s, null, i.affectsData);
  }
  /**
  * Adds a new root to the document (or re-attaches a {@link #detachRoot detached root}).
  *
  * Throws an error, if trying to add a root that is already added and attached.
  *
  * @param rootName Name of the added root.
  * @param elementName The element name. Defaults to `'$root'` which also has some basic schema defined
  * (e.g. `$block` elements are allowed inside the `$root`). Make sure to define a proper schema if you use a different name.
  * @returns The added root element.
  */
  addRoot(t, e = "$root") {
    this._assertWriterUsedCorrectly();
    const i = this.model.document.getRoot(t);
    if (i && i.isAttached())
      throw new S("writer-addroot-root-exists", this);
    const s = this.model.document, r = new bn(t, e, !0, s, s.version);
    return this.batch.addOperation(r), this.model.applyOperation(r), this.model.document.getRoot(t);
  }
  /**
  * Detaches the root from the document.
  *
  * All content and markers are removed from the root upon detaching. New content and new markers cannot be added to the root, as long
  * as it is detached.
  *
  * A root cannot be fully removed from the document, it can be only detached. A root is permanently removed only after you
  * re-initialize the editor and do not specify the root in the initial data.
  *
  * A detached root can be re-attached using {@link #addRoot}.
  *
  * Throws an error if the root does not exist or the root is already detached.
  *
  * @param rootOrName Name of the detached root.
  */
  detachRoot(t) {
    this._assertWriterUsedCorrectly();
    const e = typeof t == "string" ? this.model.document.getRoot(t) : t;
    if (!e || !e.isAttached())
      throw new S("writer-detachroot-no-root", this);
    for (const r of this.model.markers)
      r.getRange().root === e && this.removeMarker(r);
    for (const r of e.getAttributeKeys())
      this.removeAttribute(r, e);
    this.remove(this.createRangeIn(e));
    const i = this.model.document, s = new bn(e.rootName, e.name, !1, i, i.version);
    this.batch.addOperation(s), this.model.applyOperation(s);
  }
  setSelection(...t) {
    this._assertWriterUsedCorrectly(), this.model.document.selection._setTo(...t);
  }
  /**
  * Moves {@link module:engine/model/documentselection~DocumentSelection#focus} to the specified location.
  *
  * The location can be specified in the same form as
  * {@link #createPositionAt `writer.createPositionAt()`} parameters.
  *
  * @param itemOrPosition
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/model/item~Item model item}.
  */
  setSelectionFocus(t, e) {
    this._assertWriterUsedCorrectly(), this.model.document.selection._setFocus(t, e);
  }
  setSelectionAttribute(t, e) {
    if (this._assertWriterUsedCorrectly(), typeof t == "string")
      this._setSelectionAttribute(t, e);
    else
      for (const [i, s] of Ji(t))
        this._setSelectionAttribute(i, s);
  }
  /**
  * Removes attribute(s) with given key(s) from the selection.
  *
  * Remove one attribute:
  *
  * ```ts
  * writer.removeSelectionAttribute( 'italic' );
  * ```
  *
  * Remove multiple attributes:
  *
  * ```ts
  * writer.removeSelectionAttribute( [ 'italic', 'bold' ] );
  * ```
  *
  * @param keyOrIterableOfKeys Key of the attribute to remove or an iterable of attribute keys to remove.
  */
  removeSelectionAttribute(t) {
    if (this._assertWriterUsedCorrectly(), typeof t == "string")
      this._removeSelectionAttribute(t);
    else
      for (const e of t)
        this._removeSelectionAttribute(e);
  }
  /**
  * Temporarily changes the {@link module:engine/model/documentselection~DocumentSelection#isGravityOverridden gravity}
  * of the selection from left to right.
  *
  * The gravity defines from which direction the selection inherits its attributes. If it's the default left gravity,
  * then the selection (after being moved by the user) inherits attributes from its left-hand side.
  * This method allows to temporarily override this behavior by forcing the gravity to the right.
  *
  * For the following model fragment:
  *
  * ```xml
  * <$text bold="true" linkHref="url">bar[]</$text><$text bold="true">biz</$text>
  * ```
  *
  * * Default gravity: selection will have the `bold` and `linkHref` attributes.
  * * Overridden gravity: selection will have `bold` attribute.
  *
  * **Note**: It returns an unique identifier which is required to restore the gravity. It guarantees the symmetry
  * of the process.
  *
  * @returns The unique id which allows restoring the gravity.
  */
  overrideSelectionGravity() {
    return this.model.document.selection._overrideGravity();
  }
  /**
  * Restores {@link ~Writer#overrideSelectionGravity} gravity to default.
  *
  * Restoring the gravity is only possible using the unique identifier returned by
  * {@link ~Writer#overrideSelectionGravity}. Note that the gravity remains overridden as long as won't be restored
  * the same number of times it was overridden.
  *
  * @param uid The unique id returned by {@link ~Writer#overrideSelectionGravity}.
  */
  restoreSelectionGravity(t) {
    this.model.document.selection._restoreGravity(t);
  }
  /**
  * @param key Key of the attribute to remove.
  * @param value Attribute value.
  */
  _setSelectionAttribute(t, e) {
    const i = this.model.document.selection;
    if (i.isCollapsed && i.anchor.parent.isEmpty) {
      const s = Xi._getStoreAttributeKey(t);
      this.setAttribute(s, e, i.anchor.parent);
    }
    i._setAttribute(t, e);
  }
  /**
  * @param key Key of the attribute to remove.
  */
  _removeSelectionAttribute(t) {
    const e = this.model.document.selection;
    if (e.isCollapsed && e.anchor.parent.isEmpty) {
      const i = Xi._getStoreAttributeKey(t);
      this.removeAttribute(i, e.anchor.parent);
    }
    e._removeAttribute(t);
  }
  /**
  * Throws `writer-detached-writer-tries-to-modify-model` error when the writer is used outside of the `change()` block.
  */
  _assertWriterUsedCorrectly() {
    if (this.model._currentWriter !== this)
      throw new S("writer-incorrect-use", this);
  }
  /**
  * For given action `type` and `positionOrRange` where the action happens, this function finds all affected markers
  * and applies a marker operation with the new marker range equal to the current range. Thanks to this, the marker range
  * can be later correctly processed during undo.
  *
  * @param type Writer action type.
  * @param positionOrRange Position or range where the writer action happens.
  */
  _addOperationForAffectedMarkers(t, e) {
    for (const i of this.model.markers) {
      if (!i.managedUsingOperations)
        continue;
      const s = i.getRange();
      let r = !1;
      if (t === "move") {
        const o = e;
        r = o.containsPosition(s.start) || o.start.isEqual(s.start) || o.containsPosition(s.end) || o.end.isEqual(s.end);
      } else {
        const o = e, a = o.nodeBefore, l = o.nodeAfter, c = s.start.parent == a && s.start.isAtEnd, u = s.end.parent == l && s.end.offset == 0, d = s.end.nodeAfter == l, h = s.start.nodeAfter == l;
        r = c || u || d || h;
      }
      r && this.updateMarker(i.name, {
        range: s
      });
    }
  }
}
function Kp(n, t, e, i) {
  const s = n.model, r = s.document;
  let o = i.start, a, l, c;
  for (const d of i.getWalker({
    shallow: !0
  }))
    c = d.item.getAttribute(t), a && l != c && (l != e && u(), o = a), a = d.nextPosition, l = c;
  a instanceof G && a != o && l != e && u();
  function u() {
    const d = new F(o, a), h = d.root.document ? r.version : null, m = new Ct(d, t, l, e, h);
    n.batch.addOperation(m), s.applyOperation(m);
  }
}
function Zp(n, t, e, i) {
  const s = n.model, r = s.document, o = i.getAttribute(t);
  let a, l;
  if (o != e) {
    if (i.root === i) {
      const u = i.document ? r.version : null;
      l = new $n(i, t, o, e, u);
    } else {
      a = new F(G._createBefore(i), n.createPositionAfter(i));
      const u = a.root.document ? r.version : null;
      l = new Ct(a, t, o, e, u);
    }
    n.batch.addOperation(l), s.applyOperation(l);
  }
}
function wo(n, t, e, i, s) {
  const r = n.model, o = r.document, a = new si(t, e, i, r.markers, !!s, o.version);
  n.batch.addOperation(a), r.applyOperation(a);
}
function wO(n, t, e, i) {
  let s;
  if (n.root.document) {
    const r = i.document, o = new G(r.graveyard, [
      0
    ]);
    s = new He(n, t, o, r.version);
  } else
    s = new gO(n, t);
  e.addOperation(s), i.applyOperation(s);
}
function Yp(n, t) {
  return n === t || n instanceof ec && t instanceof ec;
}
function vO(n, t, e = {}) {
  if (t.isCollapsed)
    return;
  const i = t.getFirstRange();
  if (i.root.rootName == "$graveyard")
    return;
  const s = n.schema;
  n.change((r) => {
    if (!e.doNotResetEntireContent && AO(s, t)) {
      SO(r, t);
      return;
    }
    const o = {};
    if (!e.doNotAutoparagraph) {
      const c = t.getSelectedElement();
      c && Object.assign(o, s.getAttributesWithProperty(c, "copyOnReplace", !0));
    }
    const [a, l] = _O(i);
    a.isTouching(l) || r.remove(r.createRange(a, l)), e.leaveUnmerged || (kO(r, a, l), s.removeDisallowedAttributes(a.parent.getChildren(), r)), ry(r, t, a), !e.doNotAutoparagraph && TO(s, a) && sy(r, a, t, o), a.detach(), l.detach();
  });
}
function _O(n) {
  const t = n.root.document.model, e = n.start;
  let i = n.end;
  if (t.hasContent(n, {
    ignoreMarkers: !0
  })) {
    const s = yO(i);
    if (s && i.isTouching(t.createPositionAt(s, 0))) {
      const r = t.createSelection(n);
      t.modifySelection(r, {
        direction: "backward"
      });
      const o = r.getLastPosition(), a = t.createRange(o, i);
      t.hasContent(a, {
        ignoreMarkers: !0
      }) || (i = o);
    }
  }
  return [
    At.fromPosition(e, "toPrevious"),
    At.fromPosition(i, "toNext")
  ];
}
function yO(n) {
  const t = n.parent, e = t.root.document.model.schema, i = t.getAncestors({
    parentFirst: !0,
    includeSelf: !0
  });
  for (const s of i) {
    if (e.isLimit(s))
      return null;
    if (e.isBlock(s))
      return s;
  }
}
function kO(n, t, e) {
  const i = n.model;
  if (!Lf(n.model.schema, t, e))
    return;
  const [s, r] = xO(t, e);
  !s || !r || (!i.hasContent(s, {
    ignoreMarkers: !0
  }) && i.hasContent(r, {
    ignoreMarkers: !0
  }) ? ny(n, t, e, s.parent) : iy(n, t, e, s.parent));
}
function iy(n, t, e, i) {
  const s = t.parent, r = e.parent;
  if (!(s == i || r == i)) {
    for (t = n.createPositionAfter(s), e = n.createPositionBefore(r), e.isEqual(t) || n.insert(r, t), n.merge(t); e.parent.isEmpty; ) {
      const o = e.parent;
      e = n.createPositionBefore(o), n.remove(o);
    }
    Lf(n.model.schema, t, e) && iy(n, t, e, i);
  }
}
function ny(n, t, e, i) {
  const s = t.parent, r = e.parent;
  if (!(s == i || r == i)) {
    for (t = n.createPositionAfter(s), e = n.createPositionBefore(r), e.isEqual(t) || n.insert(s, e); t.parent.isEmpty; ) {
      const o = t.parent;
      t = n.createPositionBefore(o), n.remove(o);
    }
    e = n.createPositionBefore(r), CO(n, e), Lf(n.model.schema, t, e) && ny(n, t, e, i);
  }
}
function CO(n, t) {
  const e = t.nodeBefore, i = t.nodeAfter;
  e.name != i.name && n.rename(e, i.name), n.clearAttributes(e), n.setAttributes(Object.fromEntries(i.getAttributes()), e), n.merge(t);
}
function Lf(n, t, e) {
  const i = t.parent, s = e.parent;
  return i == s || n.isLimit(i) || n.isLimit(s) ? !1 : EO(t, e, n);
}
function xO(n, t) {
  const e = n.getAncestors(), i = t.getAncestors();
  let s = 0;
  for (; e[s] && e[s] == i[s]; )
    s++;
  return [
    e[s],
    i[s]
  ];
}
function TO(n, t) {
  const e = n.checkChild(t, "$text"), i = n.checkChild(t, "paragraph");
  return !e && i;
}
function EO(n, t, e) {
  const i = new F(n, t);
  for (const s of i.getWalker())
    if (e.isLimit(s.item))
      return !1;
  return !0;
}
function sy(n, t, e, i = {}) {
  const s = n.createElement("paragraph");
  n.model.schema.setAllowedAttributes(s, i, n), n.insert(s, t), ry(n, e, n.createPositionAt(s, 0));
}
function SO(n, t) {
  const e = n.model.schema.getLimitElement(t);
  n.remove(n.createRangeIn(e)), sy(n, n.createPositionAt(e, 0), t);
}
function AO(n, t) {
  const e = n.getLimitElement(t);
  if (!t.containsEntireContent(e))
    return !1;
  const i = t.getFirstRange();
  return i.start.parent == i.end.parent ? !1 : n.checkChild(e, "paragraph");
}
function ry(n, t, e) {
  t instanceof Xi ? n.setSelection(e) : t.setTo(e);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function IO(n, t) {
  return n.change((e) => {
    const i = e.createDocumentFragment(), s = t.getFirstRange();
    if (!s || s.isCollapsed)
      return i;
    const r = s.start.root, o = s.start.getCommonPath(s.end), a = r.getNodeByPath(o);
    let l;
    s.start.parent == s.end.parent ? l = s : l = e.createRange(e.createPositionAt(a, s.start.path[o.length]), e.createPositionAt(a, s.end.path[o.length] + 1));
    const c = l.end.offset - l.start.offset;
    for (const u of l.getItems({
      shallow: !0
    }))
      u.is("$textProxy") ? e.appendText(u.data, u.getAttributes(), i) : e.append(e.cloneElement(u, !0), i);
    if (l != s) {
      const u = s._getTransformedByMove(l.start, e.createPositionAt(i, 0), c)[0], d = e.createRange(e.createPositionAt(i, 0), u.start), h = e.createRange(u.end, e.createPositionAt(i, "end"));
      Jp(h, e), Jp(d, e);
    }
    return i;
  });
}
function Jp(n, t) {
  const e = [];
  Array.from(n.getItems({
    direction: "backward"
  })).map((i) => t.createRangeOn(i)).filter((i) => (i.start.isAfter(n.start) || i.start.isEqual(n.start)) && (i.end.isBefore(n.end) || i.end.isEqual(n.end))).forEach((i) => {
    e.push(i.start.parent), t.remove(i);
  }), e.forEach((i) => {
    let s = i;
    for (; s.parent && s.isEmpty; ) {
      const r = t.createRangeOn(s);
      s = s.parent, t.remove(r);
    }
  });
}
function PO(n, t, e) {
  return n.change((i) => {
    const s = e || n.document.selection;
    s.isCollapsed || n.deleteContent(s, {
      doNotAutoparagraph: !0
    });
    const r = new RO(n, i, s.anchor), o = [];
    let a;
    if (t.is("documentFragment")) {
      if (t.markers.size) {
        const u = [];
        for (const [d, h] of t.markers) {
          const { start: m, end: g } = h, p = m.isEqual(g);
          u.push({
            position: m,
            name: d,
            isCollapsed: p
          }, {
            position: g,
            name: d,
            isCollapsed: p
          });
        }
        u.sort(({ position: d }, { position: h }) => d.isBefore(h) ? 1 : -1);
        for (const { position: d, name: h, isCollapsed: m } of u) {
          let g = null, p = null;
          const w = d.parent === t && d.isAtStart, y = d.parent === t && d.isAtEnd;
          !w && !y ? (g = i.createElement("$marker"), i.insert(g, d)) : m && (p = w ? "start" : "end"), o.push({
            name: h,
            element: g,
            collapsed: p
          });
        }
      }
      a = t.getChildren();
    } else
      a = [
        t
      ];
    r.handleNodes(a);
    let l = r.getSelectionRange();
    if (t.is("documentFragment") && o.length) {
      const u = l ? ni.fromRange(l) : null, d = {};
      for (let h = o.length - 1; h >= 0; h--) {
        const { name: m, element: g, collapsed: p } = o[h], w = !d[m];
        if (w && (d[m] = []), g) {
          const y = i.createPositionAt(g, "before");
          d[m].push(y), i.remove(g);
        } else {
          const y = r.getAffectedRange();
          if (!y) {
            p && d[m].push(r.position);
            continue;
          }
          p ? d[m].push(y[p]) : d[m].push(w ? y.start : y.end);
        }
      }
      for (const [h, [m, g]] of Object.entries(d))
        m && g && m.root === g.root && m.root.document && !i.model.markers.has(h) && i.addMarker(h, {
          usingOperation: !0,
          affectsData: !0,
          range: new F(m, g)
        });
      u && (l = u.toRange(), u.detach());
    }
    /* istanbul ignore else -- @preserve */
    l && (s instanceof Xi ? i.setSelection(l) : s.setTo(l));
    const c = r.getAffectedRange() || n.createRange(s.anchor);
    return r.destroy(), c;
  });
}
class RO {
  constructor(t, e, i) {
    /**
    * The model in context of which the insertion should be performed.
    */
    f(this, "model");
    /**
    * Batch to which operations will be added.
    */
    f(this, "writer");
    /**
    * The position at which (or near which) the next node will be inserted.
    */
    f(this, "position");
    /**
    * Elements with which the inserted elements can be merged.
    *
    * ```html
    * <p>x^</p><p>y</p> + <p>z</p> (can merge to <p>x</p>)
    * <p>x</p><p>^y</p> + <p>z</p> (can merge to <p>y</p>)
    * <p>x^y</p> + <p>z</p> (can merge to <p>xy</p> which will be split during the action,
    * 						so both its pieces will be added to this set)
    * ```
    */
    f(this, "canMergeWith");
    /**
    * Schema of the model.
    */
    f(this, "schema");
    /**
    * The temporary DocumentFragment used for grouping multiple nodes for single insert operation.
    */
    f(this, "_documentFragment");
    /**
    * The current position in the temporary DocumentFragment.
    */
    f(this, "_documentFragmentPosition");
    /**
    * The reference to the first inserted node.
    */
    f(this, "_firstNode", null);
    /**
    * The reference to the last inserted node.
    */
    f(this, "_lastNode", null);
    /**
    * The reference to the last auto paragraph node.
    */
    f(this, "_lastAutoParagraph", null);
    /**
    * The array of nodes that should be cleaned of not allowed attributes.
    */
    f(this, "_filterAttributesOf", []);
    /**
    * Beginning of the affected range. See {@link module:engine/model/utils/insertcontent~Insertion#getAffectedRange}.
    */
    f(this, "_affectedStart", null);
    /**
    * End of the affected range. See {@link module:engine/model/utils/insertcontent~Insertion#getAffectedRange}.
    */
    f(this, "_affectedEnd", null);
    f(this, "_nodeToSelect", null);
    this.model = t, this.writer = e, this.position = i, this.canMergeWith = /* @__PURE__ */ new Set([
      this.position.parent
    ]), this.schema = t.schema, this._documentFragment = e.createDocumentFragment(), this._documentFragmentPosition = e.createPositionAt(this._documentFragment, 0);
  }
  /**
  * Handles insertion of a set of nodes.
  *
  * @param nodes Nodes to insert.
  */
  handleNodes(t) {
    for (const e of Array.from(t))
      this._handleNode(e);
    this._insertPartialFragment(), this._lastAutoParagraph && this._updateLastNodeFromAutoParagraph(this._lastAutoParagraph), this._mergeOnRight(), this.schema.removeDisallowedAttributes(this._filterAttributesOf, this.writer), this._filterAttributesOf = [];
  }
  /**
  * Updates the last node after the auto paragraphing.
  *
  * @param node The last auto paragraphing node.
  */
  _updateLastNodeFromAutoParagraph(t) {
    const e = this.writer.createPositionAfter(this._lastNode), i = this.writer.createPositionAfter(t);
    if (i.isAfter(e)) {
      this._lastNode = t;
      /* istanbul ignore if -- @preserve */
      if (this.position.parent != t || !this.position.isAtEnd)
        throw new S("insertcontent-invalid-insertion-position", this);
      this.position = i, this._setAffectedBoundaries(this.position);
    }
  }
  /**
  * Returns range to be selected after insertion.
  * Returns `null` if there is no valid range to select after insertion.
  */
  getSelectionRange() {
    return this._nodeToSelect ? F._createOn(this._nodeToSelect) : this.model.schema.getNearestSelectionRange(this.position);
  }
  /**
  * Returns a range which contains all the performed changes. This is a range that, if removed, would return the model to the state
  * before the insertion. Returns `null` if no changes were done.
  */
  getAffectedRange() {
    return this._affectedStart ? new F(this._affectedStart, this._affectedEnd) : null;
  }
  /**
  * Destroys `Insertion` instance.
  */
  destroy() {
    this._affectedStart && this._affectedStart.detach(), this._affectedEnd && this._affectedEnd.detach();
  }
  /**
  * Handles insertion of a single node.
  */
  _handleNode(t) {
    if (!this._checkAndSplitToAllowedPosition(t)) {
      this.schema.isObject(t) || this._handleDisallowedNode(t);
      return;
    }
    this._appendToFragment(t), this._firstNode || (this._firstNode = t), this._lastNode = t;
  }
  /**
  * Inserts the temporary DocumentFragment into the model.
  */
  _insertPartialFragment() {
    if (this._documentFragment.isEmpty)
      return;
    const t = At.fromPosition(this.position, "toNext");
    this._setAffectedBoundaries(this.position), this._documentFragment.getChild(0) == this._firstNode && (this.writer.insert(this._firstNode, this.position), this._mergeOnLeft(), this.position = t.toPosition()), this._documentFragment.isEmpty || this.writer.insert(this._documentFragment, this.position), this._documentFragmentPosition = this.writer.createPositionAt(this._documentFragment, 0), this.position = t.toPosition(), t.detach();
  }
  /**
  * @param node The disallowed node which needs to be handled.
  */
  _handleDisallowedNode(t) {
    t.is("element") && this.handleNodes(t.getChildren());
  }
  /**
  * Append a node to the temporary DocumentFragment.
  *
  * @param node The node to insert.
  */
  _appendToFragment(t) {
    /* istanbul ignore if -- @preserve */
    if (!this.schema.checkChild(this.position, t))
      throw new S("insertcontent-wrong-position", this, {
        node: t,
        position: this.position
      });
    this.writer.insert(t, this._documentFragmentPosition), this._documentFragmentPosition = this._documentFragmentPosition.getShiftedBy(t.offsetSize), this.schema.isObject(t) && !this.schema.checkChild(this.position, "$text") ? this._nodeToSelect = t : this._nodeToSelect = null, this._filterAttributesOf.push(t);
  }
  /**
  * Sets `_affectedStart` and `_affectedEnd` to the given `position`. Should be used before a change is done during insertion process to
  * mark the affected range.
  *
  * This method is used before inserting a node or splitting a parent node. `_affectedStart` and `_affectedEnd` are also changed
  * during merging, but the logic there is more complicated so it is left out of this function.
  */
  _setAffectedBoundaries(t) {
    this._affectedStart || (this._affectedStart = At.fromPosition(t, "toPrevious")), (!this._affectedEnd || this._affectedEnd.isBefore(t)) && (this._affectedEnd && this._affectedEnd.detach(), this._affectedEnd = At.fromPosition(t, "toNext"));
  }
  /**
  * Merges the previous sibling of the first node if it should be merged.
  *
  * After the content was inserted we may try to merge it with its siblings.
  * This should happen only if the selection was in those elements initially.
  */
  _mergeOnLeft() {
    const t = this._firstNode;
    if (!(t instanceof et) || !this._canMergeLeft(t))
      return;
    const e = At._createBefore(t);
    e.stickiness = "toNext";
    const i = At.fromPosition(this.position, "toNext");
    this._affectedStart.isEqual(e) && (this._affectedStart.detach(), this._affectedStart = At._createAt(e.nodeBefore, "end", "toPrevious")), this._firstNode === this._lastNode && (this._firstNode = e.nodeBefore, this._lastNode = e.nodeBefore), this.writer.merge(e), e.isEqual(this._affectedEnd) && this._firstNode === this._lastNode && (this._affectedEnd.detach(), this._affectedEnd = At._createAt(e.nodeBefore, "end", "toNext")), this.position = i.toPosition(), i.detach(), this._filterAttributesOf.push(this.position.parent), e.detach();
  }
  /**
  * Merges the next sibling of the last node if it should be merged.
  *
  * After the content was inserted we may try to merge it with its siblings.
  * This should happen only if the selection was in those elements initially.
  */
  _mergeOnRight() {
    const t = this._lastNode;
    if (!(t instanceof et) || !this._canMergeRight(t))
      return;
    const e = At._createAfter(t);
    e.stickiness = "toNext";
    /* istanbul ignore if -- @preserve */
    if (!this.position.isEqual(e))
      throw new S("insertcontent-invalid-insertion-position", this);
    this.position = G._createAt(e.nodeBefore, "end");
    const i = At.fromPosition(this.position, "toPrevious");
    this._affectedEnd.isEqual(e) && (this._affectedEnd.detach(), this._affectedEnd = At._createAt(e.nodeBefore, "end", "toNext")), this._firstNode === this._lastNode && (this._firstNode = e.nodeBefore, this._lastNode = e.nodeBefore), this.writer.merge(e), e.getShiftedBy(-1).isEqual(this._affectedStart) && this._firstNode === this._lastNode && (this._affectedStart.detach(), this._affectedStart = At._createAt(e.nodeBefore, 0, "toPrevious")), this.position = i.toPosition(), i.detach(), this._filterAttributesOf.push(this.position.parent), e.detach();
  }
  /**
  * Checks whether specified node can be merged with previous sibling element.
  *
  * @param node The node which could potentially be merged.
  */
  _canMergeLeft(t) {
    const e = t.previousSibling;
    return e instanceof et && this.canMergeWith.has(e) && this.model.schema.checkMerge(e, t);
  }
  /**
  * Checks whether specified node can be merged with next sibling element.
  *
  * @param node The node which could potentially be merged.
  */
  _canMergeRight(t) {
    const e = t.nextSibling;
    return e instanceof et && this.canMergeWith.has(e) && this.model.schema.checkMerge(t, e);
  }
  /**
  * Inserts a paragraph and moves the insertion position into it.
  */
  _insertAutoParagraph() {
    this._insertPartialFragment();
    const t = this.writer.createElement("paragraph");
    this.writer.insert(t, this.position), this._setAffectedBoundaries(this.position), this._lastAutoParagraph = t, this.position = this.writer.createPositionAt(t, 0);
  }
  /**
  * @returns Whether an allowed position was found.
  * `false` is returned if the node isn't allowed at any position up in the tree, `true` if was.
  */
  _checkAndSplitToAllowedPosition(t) {
    const e = this._getAllowedIn(this.position.parent, t);
    if (!e)
      return !1;
    for (e != this.position.parent && this._insertPartialFragment(); e != this.position.parent; )
      if (this.position.isAtStart) {
        const i = this.position.parent;
        this.position = this.writer.createPositionBefore(i), i.isEmpty && i.parent === e && this.writer.remove(i);
      } else if (this.position.isAtEnd)
        this.position = this.writer.createPositionAfter(this.position.parent);
      else {
        const i = this.writer.createPositionAfter(this.position.parent);
        this._setAffectedBoundaries(this.position), this.writer.split(this.position), this.position = i, this.canMergeWith.add(this.position.nodeAfter);
      }
    return this.schema.checkChild(this.position.parent, t) || this._insertAutoParagraph(), !0;
  }
  /**
  * Gets the element in which the given node is allowed. It checks the passed element and all its ancestors.
  *
  * It also verifies if auto-paragraphing could help.
  *
  * @param contextElement The element in which context the node should be checked.
  * @param childNode The node to check.
  */
  _getAllowedIn(t, e) {
    return this.schema.checkChild(t, e) || this.schema.checkChild(t, "paragraph") && this.schema.checkChild("paragraph", e) ? t : this.schema.isLimit(t) ? null : this._getAllowedIn(t.parent, e);
  }
}
function OO(n, t, e, i = {}) {
  if (!n.schema.isObject(t))
    throw new S("insertobject-element-not-an-object", n, {
      object: t
    });
  const s = e || n.document.selection;
  let r = s;
  i.findOptimalPosition && n.schema.isBlock(t) && (r = n.createSelection(n.schema.findOptimalInsertionRange(s, i.findOptimalPosition)));
  const o = Xe(s.getSelectedBlocks()), a = {};
  return o && Object.assign(a, n.schema.getAttributesWithProperty(o, "copyOnReplace", !0)), n.change((l) => {
    r.isCollapsed || n.deleteContent(r, {
      doNotAutoparagraph: !0
    });
    let c = t;
    const u = r.anchor.parent;
    !n.schema.checkChild(u, t) && n.schema.checkChild(u, "paragraph") && n.schema.checkChild("paragraph", t) && (c = l.createElement("paragraph"), l.insert(t, c)), n.schema.setAllowedAttributes(c, a, l);
    const d = n.insertContent(c, r);
    return d.isCollapsed || i.setSelection && VO(l, t, i.setSelection, a), d;
  });
}
function VO(n, t, e, i) {
  const s = n.model;
  if (e == "on") {
    n.setSelection(t, "on");
    return;
  }
  if (e != "after")
    throw new S("insertobject-invalid-place-parameter-value", s);
  let r = t.nextSibling;
  if (s.schema.isInline(t)) {
    n.setSelection(t, "after");
    return;
  }
  !(r && s.schema.checkChild(r, "$text")) && s.schema.checkChild(t.parent, "paragraph") && (r = n.createElement("paragraph"), s.schema.setAllowedAttributes(r, i, n), s.insertContent(r, n.createPositionAfter(t))), r && n.setSelection(r, 0);
}
const LO = ' ,.?!:;"-()';
function BO(n, t, e = {}) {
  const i = n.schema, s = e.direction != "backward", r = e.unit ? e.unit : "character", o = !!e.treatEmojiAsSingleUnit, a = t.focus, l = new Fn({
    boundaries: DO(a, s),
    singleCharacters: !0,
    direction: s ? "forward" : "backward"
  }), c = {
    walker: l,
    schema: i,
    isForward: s,
    unit: r,
    treatEmojiAsSingleUnit: o
  };
  let u;
  for (; u = l.next(); ) {
    if (u.done)
      return;
    const d = MO(c, u.value);
    if (d) {
      t instanceof Xi ? n.change((h) => {
        h.setSelectionFocus(d);
      }) : t.setFocus(d);
      return;
    }
  }
}
function MO(n, t) {
  const { isForward: e, walker: i, unit: s, schema: r, treatEmojiAsSingleUnit: o } = n, { type: a, item: l, nextPosition: c } = t;
  if (a == "text")
    return n.unit === "word" ? FO(i, e) : NO(i, s, o);
  if (a == (e ? "elementStart" : "elementEnd")) {
    if (r.isSelectable(l))
      return G._createAt(l, e ? "after" : "before");
    if (r.checkChild(c, "$text"))
      return c;
  } else {
    if (r.isLimit(l)) {
      i.skip(() => !0);
      return;
    }
    if (r.checkChild(c, "$text"))
      return c;
  }
}
function NO(n, t, e) {
  const i = n.position.textNode;
  if (i) {
    const s = i.data;
    let r = n.position.offset - i.startOffset;
    for (; kf(s, r) || t == "character" && Cf(s, r) || e && w_(s, r); )
      n.next(), r = n.position.offset - i.startOffset;
  }
  return n.position;
}
function FO(n, t) {
  let e = n.position.textNode;
  for (e || (e = t ? n.position.nodeAfter : n.position.nodeBefore); e && e.is("$text"); ) {
    const i = n.position.offset - e.startOffset;
    if (zO(e, i, t))
      e = t ? n.position.nodeAfter : n.position.nodeBefore;
    else {
      if ($O(e.data, i, t))
        break;
      n.next();
    }
  }
  return n.position;
}
function DO(n, t) {
  const e = n.root, i = G._createAt(e, t ? "end" : 0);
  return t ? new F(n, i) : new F(i, n);
}
function $O(n, t, e) {
  const i = t + (e ? 0 : -1);
  return LO.includes(n.charAt(i));
}
function zO(n, t, e) {
  return t === (e ? n.offsetSize : 0);
}
let HO = class extends (/* @__PURE__ */ je()) {
  // @if CK_DEBUG_ENGINE // private _operationLogs: Array<string>;
  // @if CK_DEBUG_ENGINE // private _appliedOperations: Array<Operation>;
  constructor() {
    super();
    /**
    * Model's marker collection.
    */
    f(this, "markers");
    /**
    * Model's document.
    */
    f(this, "document");
    /**
    * Model's schema.
    */
    f(this, "schema");
    /**
    * All callbacks added by {@link module:engine/model/model~Model#change} or
    * {@link module:engine/model/model~Model#enqueueChange} methods waiting to be executed.
    */
    f(this, "_pendingChanges");
    /**
    * The last created and currently used writer instance.
    */
    f(this, "_currentWriter");
    this.markers = new mO(), this.document = new fO(this), this.schema = new RR(), this._pendingChanges = [], this._currentWriter = null, [
      "deleteContent",
      "modifySelection",
      "getSelectedContent",
      "applyOperation"
    ].forEach((e) => this.decorate(e)), this.on("applyOperation", (e, i) => {
      i[0]._validate();
    }, {
      priority: "highest"
    }), this.schema.register("$root", {
      isLimit: !0
    }), this.schema.register("$container", {
      allowIn: [
        "$root",
        "$container"
      ]
    }), this.schema.register("$block", {
      allowIn: [
        "$root",
        "$container"
      ],
      isBlock: !0
    }), this.schema.register("$blockObject", {
      allowWhere: "$block",
      isBlock: !0,
      isObject: !0
    }), this.schema.register("$inlineObject", {
      allowWhere: "$text",
      allowAttributesOf: "$text",
      isInline: !0,
      isObject: !0
    }), this.schema.register("$text", {
      allowIn: "$block",
      isInline: !0,
      isContent: !0
    }), this.schema.register("$clipboardHolder", {
      allowContentOf: "$root",
      allowChildren: "$text",
      isLimit: !0
    }), this.schema.register("$documentFragment", {
      allowContentOf: "$root",
      allowChildren: "$text",
      isLimit: !0
    }), this.schema.register("$marker"), this.schema.addChildCheck(() => !0, "$marker"), CR(this), this.document.registerPostFixer(W_), this.on("insertContent", (e, [i, s]) => {
      e.return = PO(this, i, s);
    }), this.on("insertObject", (e, [i, s, r]) => {
      e.return = OO(this, i, s, r);
    }), this.on("canEditAt", (e) => {
      const i = !this.document.isReadOnly;
      e.return = i, i || e.stop();
    });
  }
  /**
  * The `change()` method is the primary way of changing the model. You should use it to modify all document nodes
  * (including detached nodes – i.e. nodes not added to the {@link module:engine/model/model~Model#document model document}),
  * the {@link module:engine/model/document~Document#selection document's selection}, and
  * {@link module:engine/model/model~Model#markers model markers}.
  *
  * ```ts
  * model.change( writer => {
  * 	writer.insertText( 'foo', paragraph, 'end' );
  * } );
  * ```
  *
  * All changes inside the change block use the same {@link module:engine/model/batch~Batch} so they are combined
  * into a single undo step.
  *
  * ```ts
  * model.change( writer => {
  * 	writer.insertText( 'foo', paragraph, 'end' ); // foo.
  *
  * 	model.change( writer => {
  * 		writer.insertText( 'bar', paragraph, 'end' ); // foobar.
  * 	} );
  *
  * 	writer.insertText( 'bom', paragraph, 'end' ); // foobarbom.
  * } );
  * ```
  *
  * The callback of the `change()` block is executed synchronously.
  *
  * You can also return a value from the change block.
  *
  * ```ts
  * const img = model.change( writer => {
  * 	return writer.createElement( 'img' );
  * } );
  * ```
  *
  * @see #enqueueChange
  * @typeParam TReturn The return type of the provided callback.
  * @param callback Callback function which may modify the model.
  */
  change(e) {
    try {
      return this._pendingChanges.length === 0 ? (this._pendingChanges.push({
        batch: new ur(),
        callback: e
      }), this._runPendingChanges()[0]) : e(this._currentWriter);
    } catch (i) {
      /* istanbul ignore next -- @preserve */
      S.rethrowUnexpectedError(i, this);
    }
  }
  enqueueChange(e, i) {
    try {
      e ? typeof e == "function" ? (i = e, e = new ur()) : e instanceof ur || (e = new ur(e)) : e = new ur(), this._pendingChanges.push({
        batch: e,
        callback: i
      }), this._pendingChanges.length == 1 && this._runPendingChanges();
    } catch (s) {
      /* istanbul ignore next -- @preserve */
      S.rethrowUnexpectedError(s, this);
    }
  }
  /**
  * {@link module:utils/observablemixin~Observable#decorate Decorated} function for applying
  * {@link module:engine/model/operation/operation~Operation operations} to the model.
  *
  * This is a low-level way of changing the model. It is exposed for very specific use cases (like the undo feature).
  * Normally, to modify the model, you will want to use {@link module:engine/model/writer~Writer `Writer`}.
  * See also {@glink framework/architecture/editing-engine#changing-the-model Changing the model} section
  * of the {@glink framework/architecture/editing-engine Editing architecture} guide.
  *
  * @param operation The operation to apply.
  */
  applyOperation(e) {
    e._execute();
  }
  // @if CK_DEBUG_ENGINE // public getAppliedOperation(): string {
  // @if CK_DEBUG_ENGINE // 	if ( !this._appliedOperations ) {
  // @if CK_DEBUG_ENGINE // 		return '';
  // @if CK_DEBUG_ENGINE // 	}
  // @if CK_DEBUG_ENGINE // 	return this._appliedOperations.map( operation => JSON.stringify( operation ) ).join( '-------' );
  // @if CK_DEBUG_ENGINE // }
  // @if CK_DEBUG_ENGINE // public createReplayer( stringifiedOperations: string ): typeof OperationReplayer {
  // @if CK_DEBUG_ENGINE // 	return new OperationReplayer( this, '-------', stringifiedOperations );
  // @if CK_DEBUG_ENGINE // }
  /**
  * Inserts content at the position in the editor specified by the selection, as one would expect the paste
  * functionality to work.
  *
  * **Note**: If you want to insert an {@glink framework/deep-dive/schema#object-elements object element}
  * (e.g. a {@link module:widget/utils~toWidget widget}), see {@link #insertObject} instead.
  *
  * This is a high-level method. It takes the {@link #schema schema} into consideration when inserting
  * the content, clears the given selection's content before inserting nodes and moves the selection
  * to its target position at the end of the process.
  * It can split elements, merge them, wrap bare text nodes with paragraphs, etc. &ndash; just like the
  * pasting feature should do.
  *
  * For lower-level methods see {@link module:engine/model/writer~Writer `Writer`}.
  *
  * This method, unlike {@link module:engine/model/writer~Writer `Writer`}'s methods, does not have to be used
  * inside a {@link #change `change()` block}.
  *
  * # Conversion and schema
  *
  * Inserting elements and text nodes into the model is not enough to make CKEditor 5 render that content
  * to the user. CKEditor 5 implements a model-view-controller architecture and what `model.insertContent()` does
  * is only adding nodes to the model. Additionally, you need to define
  * {@glink framework/architecture/editing-engine#conversion converters} between the model and view
  * and define those nodes in the {@glink framework/architecture/editing-engine#schema schema}.
  *
  * So, while this method may seem similar to CKEditor 4 `editor.insertHtml()` (in fact, both methods
  * are used for paste-like content insertion), the CKEditor 5 method cannot be use to insert arbitrary HTML
  * unless converters are defined for all elements and attributes in that HTML.
  *
  * # Examples
  *
  * Using `insertContent()` with a manually created model structure:
  *
  * ```ts
  * // Let's create a document fragment containing such content as:
  * //
  * // <paragraph>foo</paragraph>
  * // <blockQuote>
  * //    <paragraph>bar</paragraph>
  * // </blockQuote>
  * const docFrag = editor.model.change( writer => {
  * 	const p1 = writer.createElement( 'paragraph' );
  * 	const p2 = writer.createElement( 'paragraph' );
  * 	const blockQuote = writer.createElement( 'blockQuote' );
  * 	const docFrag = writer.createDocumentFragment();
  *
  * 	writer.append( p1, docFrag );
  * 	writer.append( blockQuote, docFrag );
  * 	writer.append( p2, blockQuote );
  * 	writer.insertText( 'foo', p1 );
  * 	writer.insertText( 'bar', p2 );
  *
  * 	return docFrag;
  * } );
  *
  * // insertContent() does not have to be used in a change() block. It can, though,
  * // so this code could be moved to the callback defined above.
  * editor.model.insertContent( docFrag );
  * ```
  *
  * Using `insertContent()` with an HTML string converted to a model document fragment (similar to the pasting mechanism):
  *
  * ```ts
  * // You can create your own HtmlDataProcessor instance or use editor.data.processor
  * // if you have not overridden the default one (which is the HtmlDataProcessor instance).
  * const htmlDP = new HtmlDataProcessor( viewDocument );
  *
  * // Convert an HTML string to a view document fragment:
  * const viewFragment = htmlDP.toView( htmlString );
  *
  * // Convert the view document fragment to a model document fragment
  * // in the context of $root. This conversion takes the schema into
  * // account so if, for example, the view document fragment contained a bare text node,
  * // this text node cannot be a child of $root, so it will be automatically
  * // wrapped with a <paragraph>. You can define the context yourself (in the second parameter),
  * // and e.g. convert the content like it would happen in a <paragraph>.
  * // Note: The clipboard feature uses a custom context called $clipboardHolder
  * // which has a loosened schema.
  * const modelFragment = editor.data.toModel( viewFragment );
  *
  * editor.model.insertContent( modelFragment );
  * ```
  *
  * By default this method will use the document selection but it can also be used with a position, range or selection instance.
  *
  * ```ts
  * // Insert text at the current document selection position.
  * editor.model.change( writer => {
  * 	editor.model.insertContent( writer.createText( 'x' ) );
  * } );
  *
  * // Insert text at a given position - the document selection will not be modified.
  * editor.model.change( writer => {
  * 	editor.model.insertContent( writer.createText( 'x' ), doc.getRoot(), 2 );
  *
  * 	// Which is a shorthand for:
  * 	editor.model.insertContent( writer.createText( 'x' ), writer.createPositionAt( doc.getRoot(), 2 ) );
  * } );
  * ```
  *
  * If you want the document selection to be moved to the inserted content, use the
  * {@link module:engine/model/writer~Writer#setSelection `setSelection()`} method of the writer after inserting
  * the content:
  *
  * ```ts
  * editor.model.change( writer => {
  * 	const paragraph = writer.createElement( 'paragraph' );
  *
  * 	// Insert an empty paragraph at the beginning of the root.
  * 	editor.model.insertContent( paragraph, writer.createPositionAt( editor.model.document.getRoot(), 0 ) );
  *
  * 	// Move the document selection to the inserted paragraph.
  * 	writer.setSelection( paragraph, 'in' );
  * } );
  * ```
  *
  * If an instance of the {@link module:engine/model/selection~Selection model selection} is passed as `selectable`,
  * the new content will be inserted at the passed selection (instead of document selection):
  *
  * ```ts
  * editor.model.change( writer => {
  * 	// Create a selection in a paragraph that will be used as a place of insertion.
  * 	const selection = writer.createSelection( paragraph, 'in' );
  *
  * 	// Insert the new text at the created selection.
  * 	editor.model.insertContent( writer.createText( 'x' ), selection );
  *
  * 	// insertContent() modifies the passed selection instance so it can be used to set the document selection.
  * 	// Note: This is not necessary when you passed the document selection to insertContent().
  * 	writer.setSelection( selection );
  * } );
  * ```
  *
  * @fires insertContent
  * @param content The content to insert.
  * @param selectable The selection into which the content should be inserted.
  * If not provided the current model document selection will be used.
  * @param placeOrOffset To be used when a model item was passed as `selectable`.
  * This param defines a position in relation to that item.
  * at the insertion position.
  */
  insertContent(e, i, s, ...r) {
    const o = Uu(i, s);
    return this.fire("insertContent", [
      e,
      o,
      s,
      ...r
    ]);
  }
  /**
  * Inserts an {@glink framework/deep-dive/schema#object-elements object element} at a specific position in the editor content.
  *
  * This is a high-level API:
  * * It takes the {@link #schema schema} into consideration,
  * * It clears the content of passed `selectable` before inserting,
  * * It can move the selection at the end of the process,
  * * It will copy the selected block's attributes to preserve them upon insertion,
  * * It can split elements or wrap inline objects with paragraphs if they are not allowed in target position,
  * * etc.
  *
  * # Notes
  *
  * * If you want to insert a non-object content, see {@link #insertContent} instead.
  * * For lower-level API, see {@link module:engine/model/writer~Writer `Writer`}.
  * * Unlike {@link module:engine/model/writer~Writer `Writer`}, this method does not have to be used inside
  * a {@link #change `change()` block}.
  * * Inserting object into the model is not enough to make CKEditor 5 render that content to the user.
  * CKEditor 5 implements a model-view-controller architecture and what `model.insertObject()` does
  * is only adding nodes to the model. Additionally, you need to define
  * {@glink framework/architecture/editing-engine#conversion converters} between the model and view
  * and define those nodes in the {@glink framework/architecture/editing-engine#schema schema}.
  *
  * # Examples
  *
  * Use the following code to insert an object at the current selection and keep the selection on the inserted element:
  *
  * ```ts
  * const rawHtmlEmbedElement = writer.createElement( 'rawHtml' );
  *
  * model.insertObject( rawHtmlEmbedElement, null, null, {
  * 	setSelection: 'on'
  * } );
  * ```
  *
  * Use the following code to insert an object at the current selection and nudge the selection after the inserted object:
  *
  * ```ts
  * const pageBreakElement = writer.createElement( 'pageBreak' );
   *
  * model.insertObject( pageBreakElement, null, null, {
  * 	setSelection: 'after'
  * } );
  * ```
  *
  * Use the following code to insert an object at the current selection and avoid splitting the content (non-destructive insertion):
  *
  * ```ts
  * const tableElement = writer.createElement( 'table' );
   *
  * model.insertObject( tableElement, null, null, {
  * 	findOptimalPosition: 'auto'
  * } );
  * ```
  *
  * Use the following code to insert an object at the specific range (also: replace the content of the range):
  *
  * ```ts
  * const tableElement = writer.createElement( 'table' );
  * const range = model.createRangeOn( model.document.getRoot().getChild( 1 ) );
   *
  * model.insertObject( tableElement, range );
  * ```
  *
  * @param element An object to be inserted into the model document.
  * @param selectable A selectable where the content should be inserted. If not specified, the current
  * {@link module:engine/model/document~Document#selection document selection} will be used instead.
  * @param placeOrOffset Specifies the exact place or offset for the insertion to take place, relative to `selectable`.
  * @param options Additional options.
  * @param options.findOptimalPosition An option that, when set, adjusts the insertion position (relative to
  * `selectable` and `placeOrOffset`) so that the content of `selectable` is not split upon insertion (a.k.a. non-destructive insertion).
  * * When `'auto'`, the algorithm will decide whether to insert the object before or after `selectable` to avoid content splitting.
  * * When `'before'`, the closest position before `selectable` will be used that will not result in content splitting.
  * * When `'after'`, the closest position after `selectable` will be used that will not result in content splitting.
  *
  * Note that this option only works for block objects. Inline objects are inserted into text and do not split blocks.
  * @param options.setSelection An option that, when set, moves the
  * {@link module:engine/model/document~Document#selection document selection} after inserting the object.
  * * When `'on'`, the document selection will be set on the inserted object.
  * * When `'after'`, the document selection will move to the closest text node after the inserted object. If there is no
  * such text node, a paragraph will be created and the document selection will be moved inside it.
  * at the insertion position.
  */
  insertObject(e, i, s, r, ...o) {
    const a = Uu(i, s);
    return this.fire("insertObject", [
      e,
      a,
      r,
      r,
      ...o
    ]);
  }
  /**
  * Deletes content of the selection and merge siblings. The resulting selection is always collapsed.
  *
  * **Note:** For the sake of predictability, the resulting selection should always be collapsed.
  * In cases where a feature wants to modify deleting behavior so selection isn't collapsed
  * (e.g. a table feature may want to keep row selection after pressing <kbd>Backspace</kbd>),
  * then that behavior should be implemented in the view's listener. At the same time, the table feature
  * will need to modify this method's behavior too, e.g. to "delete contents and then collapse
  * the selection inside the last selected cell" or "delete the row and collapse selection somewhere near".
  * That needs to be done in order to ensure that other features which use `deleteContent()` will work well with tables.
  *
  * @fires deleteContent
  * @param selection Selection of which the content should be deleted.
  * @param options.leaveUnmerged Whether to merge elements after removing the content of the selection.
  *
  * For example `<heading1>x[x</heading1><paragraph>y]y</paragraph>` will become:
  *
  * * `<heading1>x^y</heading1>` with the option disabled (`leaveUnmerged == false`)
  * * `<heading1>x^</heading1><paragraph>y</paragraph>` with enabled (`leaveUnmerged == true`).
  *
  * Note: {@link module:engine/model/schema~Schema#isObject object} and {@link module:engine/model/schema~Schema#isLimit limit}
  * elements will not be merged.
  *
  * @param options.doNotResetEntireContent Whether to skip replacing the entire content with a
  * paragraph when the entire content was selected.
  *
  * For example `<heading1>[x</heading1><paragraph>y]</paragraph>` will become:
  *
  * * `<paragraph>^</paragraph>` with the option disabled (`doNotResetEntireContent == false`)
  * * `<heading1>^</heading1>` with enabled (`doNotResetEntireContent == true`)
  *
  * @param options.doNotAutoparagraph Whether to create a paragraph if after content deletion selection is moved
  * to a place where text cannot be inserted.
  *
  * For example `<paragraph>x</paragraph>[<imageBlock src="foo.jpg"></imageBlock>]` will become:
  *
  * * `<paragraph>x</paragraph><paragraph>[]</paragraph>` with the option disabled (`doNotAutoparagraph == false`)
  * * `<paragraph>x[]</paragraph>` with the option enabled (`doNotAutoparagraph == true`).
  *
  * **Note:** if there is no valid position for the selection, the paragraph will always be created:
  *
  * `[<imageBlock src="foo.jpg"></imageBlock>]` -> `<paragraph>[]</paragraph>`.
  *
  * @param options.direction The direction in which the content is being consumed.
  * Deleting backward corresponds to using the <kbd>Backspace</kbd> key, while deleting content forward corresponds to
  * the <kbd>Shift</kbd>+<kbd>Backspace</kbd> keystroke.
  */
  deleteContent(e, i) {
    vO(this, e, i);
  }
  /**
  * Modifies the selection. Currently, the supported modifications are:
  *
  * * Extending. The selection focus is moved in the specified `options.direction` with a step specified in `options.unit`.
  * Possible values for `unit` are:
  *  * `'character'` (default) - moves selection by one user-perceived character. In most cases this means moving by one
  *  character in `String` sense. However, unicode also defines "combing marks". These are special symbols, that combines
  *  with a symbol before it ("base character") to create one user-perceived character. For example, `q̣̇` is a normal
  *  letter `q` with two "combining marks": upper dot (`Ux0307`) and lower dot (`Ux0323`). For most actions, i.e. extending
  *  selection by one position, it is correct to include both "base character" and all of it's "combining marks". That is
  *  why `'character'` value is most natural and common method of modifying selection.
  *  * `'codePoint'` - moves selection by one unicode code point. In contrary to, `'character'` unit, this will insert
  *  selection between "base character" and "combining mark", because "combining marks" have their own unicode code points.
  *  However, for technical reasons, unicode code points with values above `UxFFFF` are represented in native `String` by
  *  two characters, called "surrogate pairs". Halves of "surrogate pairs" have a meaning only when placed next to each other.
  *  For example `𨭎` is represented in `String` by `\uD862\uDF4E`. Both `\uD862` and `\uDF4E` do not have any meaning
  *  outside the pair (are rendered as ? when alone). Position between them would be incorrect. In this case, selection
  *  extension will include whole "surrogate pair".
  *  * `'word'` - moves selection by a whole word.
  *
  * **Note:** if you extend a forward selection in a backward direction you will in fact shrink it.
  *
  * @fires modifySelection
  * @param selection The selection to modify.
  * @param options.direction The direction in which the selection should be modified.
  * @param options.unit The unit by which selection should be modified.
  * @param options.treatEmojiAsSingleUnit Whether multi-characer emoji sequences should be handled as single unit.
  */
  modifySelection(e, i) {
    BO(this, e, i);
  }
  /**
  * Gets a clone of the selected content.
  *
  * For example, for the following selection:
  *
  * ```html
  * <paragraph>x</paragraph>
  * <blockQuote>
  * 	<paragraph>y</paragraph>
  * 	<heading1>fir[st</heading1>
  * </blockQuote>
  * <paragraph>se]cond</paragraph>
  * <paragraph>z</paragraph>
  * ```
  *
  * It will return a document fragment with such a content:
  *
  * ```html
  * <blockQuote>
  * 	<heading1>st</heading1>
  * </blockQuote>
  * <paragraph>se</paragraph>
  * ```
  *
  * @fires getSelectedContent
  * @param selection The selection of which content will be returned.
  */
  getSelectedContent(e) {
    return IO(this, e);
  }
  /**
  * Checks whether the given {@link module:engine/model/range~Range range} or
  * {@link module:engine/model/element~Element element} has any meaningful content.
  *
  * Meaningful content is:
  *
  * * any text node (`options.ignoreWhitespaces` allows controlling whether this text node must also contain
  * any non-whitespace characters),
  * * or any {@link module:engine/model/schema~Schema#isContent content element},
  * * or any {@link module:engine/model/markercollection~Marker marker} which
  * {@link module:engine/model/markercollection~Marker#_affectsData affects data}.
  *
  * This means that a range containing an empty `<paragraph></paragraph>` is not considered to have a meaningful content.
  * However, a range containing an `<imageBlock></imageBlock>` (which would normally be marked in the schema as an object element)
  * is considered non-empty.
  *
  * @param rangeOrElement Range or element to check.
  * @param options.ignoreWhitespaces Whether text node with whitespaces only should be considered empty.
  * @param options.ignoreMarkers Whether markers should be ignored.
  */
  hasContent(e, i = {}) {
    const s = e instanceof F ? e : F._createIn(e);
    if (s.isCollapsed)
      return !1;
    const { ignoreWhitespaces: r = !1, ignoreMarkers: o = !1 } = i;
    if (!o) {
      for (const a of this.markers.getMarkersIntersectingRange(s))
        if (a.affectsData)
          return !0;
    }
    for (const a of s.getItems())
      if (this.schema.isContent(a))
        if (a.is("$textProxy"))
          if (r) {
            if (a.data.search(/\S/) !== -1)
              return !0;
          } else return !0;
        else
          return !0;
    return !1;
  }
  /**
  * Check whether given selectable is at a place in the model where it can be edited (returns `true`) or not (returns `false`).
  *
  * Should be used instead of {@link module:core/editor/editor~Editor#isReadOnly} to check whether a user action can happen at
  * given selectable. It may be decorated and used differently in different environment (e.g. multi-root editor can disable
  * a particular root).
  *
  * This method is decorated. Although this method accepts any parameter of `Selectable` type, the
  * {@link ~Model#event:canEditAt `canEditAt` event} is fired with `selectable` normalized to an instance of
  * {@link module:engine/model/selection~Selection} or {@link module:engine/model/documentselection~DocumentSelection}
  *
  * @fires canEditAt
  */
  canEditAt(e) {
    const i = Uu(e);
    return this.fire("canEditAt", [
      i
    ]);
  }
  /**
  * Creates a position from the given root and path in that root.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createPositionFromPath `Writer#createPositionFromPath()`}.
  *
  * @param root Root of the position.
  * @param path Position path. See {@link module:engine/model/position~Position#path}.
  * @param stickiness Position stickiness. See {@link module:engine/model/position~PositionStickiness}.
  */
  createPositionFromPath(e, i, s) {
    return new G(e, i, s);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/model/position~Position position},
  * * a parent element and offset in that element,
  * * a parent element and `'end'` (the position will be set at the end of that element),
  * * a {@link module:engine/model/item~Item model item} and `'before'` or `'after'`
  * (the position will be set before or after the given model item).
  *
  * This method is a shortcut to other factory methods such as:
  *
  * * {@link module:engine/model/model~Model#createPositionBefore `createPositionBefore()`},
  * * {@link module:engine/model/model~Model#createPositionAfter `createPositionAfter()`}.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createPositionAt `Writer#createPositionAt()`},
  *
  * @param itemOrPosition
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/model/item~Item model item}.
  */
  createPositionAt(e, i) {
    return G._createAt(e, i);
  }
  /**
  * Creates a new position after the given {@link module:engine/model/item~Item model item}.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createPositionAfter `Writer#createPositionAfter()`}.
  *
  * @param item Item after which the position should be placed.
  */
  createPositionAfter(e) {
    return G._createAfter(e);
  }
  /**
  * Creates a new position before the given {@link module:engine/model/item~Item model item}.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createPositionBefore `Writer#createPositionBefore()`}.
  *
  * @param item Item before which the position should be placed.
  */
  createPositionBefore(e) {
    return G._createBefore(e);
  }
  /**
  * Creates a range spanning from the `start` position to the `end` position.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createRange `Writer#createRange()`}:
  *
  * ```ts
  * model.change( writer => {
  * 	const range = writer.createRange( start, end );
  * } );
  * ```
  *
  * @param start Start position.
  * @param end End position. If not set, the range will be collapsed to the `start` position.
  */
  createRange(e, i) {
    return new F(e, i);
  }
  /**
  * Creates a range inside the given element which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * Note: This method is also available as
  * {@link module:engine/model/writer~Writer#createRangeIn `Writer#createRangeIn()`}:
  *
  * ```ts
  * model.change( writer => {
  * 	const range = writer.createRangeIn( paragraph );
  * } );
  * ```
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(e) {
    return F._createIn(e);
  }
  /**
  * Creates a range that starts before the given {@link module:engine/model/item~Item model item} and ends after it.
  *
  * Note: This method is also available on `writer` instance as
  * {@link module:engine/model/writer~Writer#createRangeOn `Writer.createRangeOn()`}:
  *
  * ```ts
  * model.change( writer => {
  * 	const range = writer.createRangeOn( paragraph );
  * } );
  * ```
  *
  * @param item
  */
  createRangeOn(e) {
    return F._createOn(e);
  }
  createSelection(...e) {
    return new Ei(...e);
  }
  /**
  * Creates a {@link module:engine/model/batch~Batch} instance.
  *
  * **Note:** In most cases creating a batch instance is not necessary as they are created when using:
  *
  * * {@link #change `change()`},
  * * {@link #enqueueChange `enqueueChange()`}.
  *
  * @param type {@link module:engine/model/batch~Batch#constructor The type} of the batch.
  */
  createBatch(e) {
    return new ur(e);
  }
  /**
  * Creates an operation instance from a JSON object (parsed JSON string).
  *
  * This is an alias for {@link module:engine/model/operation/operationfactory~OperationFactory.fromJSON `OperationFactory.fromJSON()`}.
  *
  * @param json Deserialized JSON object.
  */
  createOperationFromJSON(e) {
    return iO.fromJSON(e, this.document);
  }
  /**
  * Removes all events listeners set by model instance and destroys {@link module:engine/model/document~Document}.
  */
  destroy() {
    this.document.destroy(), this.stopListening();
  }
  /**
  * Common part of {@link module:engine/model/model~Model#change} and {@link module:engine/model/model~Model#enqueueChange}
  * which calls callbacks and returns array of values returned by these callbacks.
  */
  _runPendingChanges() {
    const e = [];
    this.fire("_beforeChanges");
    try {
      for (; this._pendingChanges.length; ) {
        const i = this._pendingChanges[0].batch;
        this._currentWriter = new bO(this, i);
        const s = this._pendingChanges[0].callback(this._currentWriter);
        e.push(s), this.document._handleChangeBlock(this._currentWriter), this._pendingChanges.shift(), this._currentWriter = null;
      }
    } finally {
      this._pendingChanges.length = 0, this._currentWriter = null, this.fire("_afterChanges");
    }
    return e;
  }
};
function Uu(n, t) {
  if (n)
    return n instanceof Ei || n instanceof Xi ? n : n instanceof Zs ? t || t === 0 ? new Ei(n, t) : n.is("rootElement") ? new Ei(n, "in") : new Ei(n, "on") : new Ei(n);
}
class oy extends xn {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    f(this, "domEventType", "click");
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e);
  }
}
class Yc extends xn {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    f(this, "domEventType", [
      "mousedown",
      "mouseup",
      "mouseover",
      "mouseout"
    ]);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e);
  }
}
class jO extends xn {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    f(this, "domEventType", [
      "touchstart",
      "touchend",
      "touchmove"
    ]);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e);
  }
}
class Pi {
  /**
  * @param document The view document instance in which this upcast writer operates.
  */
  constructor(t) {
    /**
    * The view document instance in which this upcast writer operates.
    */
    f(this, "document");
    this.document = t;
  }
  /**
  * Creates a new {@link module:engine/view/documentfragment~DocumentFragment} instance.
  *
  * @param children A list of nodes to be inserted into the created document fragment.
  * @returns The created document fragment.
  */
  createDocumentFragment(t) {
    return new Ls(this.document, t);
  }
  /**
  * Creates a new {@link module:engine/view/element~Element} instance.
  *
  * Attributes can be passed in various formats:
  *
  * ```ts
  * upcastWriter.createElement( 'div', { class: 'editor', contentEditable: 'true' } ); // object
  * upcastWriter.createElement( 'div', [ [ 'class', 'editor' ], [ 'contentEditable', 'true' ] ] ); // map-like iterator
  * upcastWriter.createElement( 'div', mapOfAttributes ); // map
  * ```
  *
  * @param name Node name.
  * @param attrs Collection of attributes.
  * @param children A list of nodes to be inserted into created element.
  * @returns Created element.
  */
  createElement(t, e, i) {
    return new Un(this.document, t, e, i);
  }
  /**
  * Creates a new {@link module:engine/view/text~Text} instance.
  *
  * @param data The text's data.
  * @returns The created text node.
  */
  createText(t) {
    return new qt(this.document, t);
  }
  /**
  * Clones the provided element.
  *
  * @see module:engine/view/element~Element#_clone
  * @param element Element to be cloned.
  * @param deep If set to `true` clones element and all its children recursively. When set to `false`,
  * element will be cloned without any children.
  * @returns Clone of this element.
  */
  clone(t, e = !1) {
    return t._clone(e);
  }
  /**
  * Appends a child node or a list of child nodes at the end of this node
  * and sets the parent of these nodes to this element.
  *
  * @see module:engine/view/element~Element#_appendChild
  * @param items Items to be inserted.
  * @param element Element to which items will be appended.
  * @returns Number of appended nodes.
  */
  appendChild(t, e) {
    return e._appendChild(t);
  }
  /**
  * Inserts a child node or a list of child nodes on the given index and sets the parent of these nodes to
  * this element.
  *
  * @see module:engine/view/element~Element#_insertChild
  * @param index Offset at which nodes should be inserted.
  * @param items Items to be inserted.
  * @param element Element to which items will be inserted.
  * @returns Number of inserted nodes.
  */
  insertChild(t, e, i) {
    return i._insertChild(t, e);
  }
  /**
  * Removes the given number of child nodes starting at the given index and set the parent of these nodes to `null`.
  *
  * @see module:engine/view/element~Element#_removeChildren
  * @param index Offset from which nodes will be removed.
  * @param howMany Number of nodes to remove.
  * @param element Element which children will be removed.
  * @returns The array containing removed nodes.
  */
  removeChildren(t, e, i) {
    return i._removeChildren(t, e);
  }
  /**
  * Removes given element from the view structure. Will not have effect on detached elements.
  *
  * @param element Element which will be removed.
  * @returns The array containing removed nodes.
  */
  remove(t) {
    const e = t.parent;
    return e ? this.removeChildren(e.getChildIndex(t), 1, e) : [];
  }
  /**
  * Replaces given element with the new one in the view structure. Will not have effect on detached elements.
  *
  * @param oldElement Element which will be replaced.
  * @param newElement Element which will be inserted in the place of the old element.
  * @returns Whether old element was successfully replaced.
  */
  replace(t, e) {
    const i = t.parent;
    if (i) {
      const s = i.getChildIndex(t);
      return this.removeChildren(s, 1, i), this.insertChild(s, e, i), !0;
    }
    return !1;
  }
  /**
  * Removes given element from view structure and places its children in its position.
  * It does nothing if element has no parent.
  *
  * @param element Element to unwrap.
  */
  unwrapElement(t) {
    const e = t.parent;
    if (e) {
      const i = e.getChildIndex(t);
      this.remove(t), this.insertChild(i, t.getChildren(), e);
    }
  }
  /**
  * Renames element by creating a copy of a given element but with its name changed and then moving contents of the
  * old element to the new one.
  *
  * Since this function creates a new element and removes the given one, the new element is returned to keep reference.
  *
  * @param newName New element name.
  * @param  element Element to be renamed.
  * @returns New element or null if the old element was not replaced (happens for detached elements).
  */
  rename(t, e) {
    const i = new Un(this.document, t, e.getAttributes(), e.getChildren());
    return this.replace(e, i) ? i : null;
  }
  /**
  * Adds or overwrites element's attribute with a specified key and value.
  *
  * ```ts
  * writer.setAttribute( 'href', 'http://ckeditor.com', linkElement );
  * ```
  *
  * @see module:engine/view/element~Element#_setAttribute
  * @param key Attribute key.
  * @param value Attribute value.
  * @param element Element for which attribute will be set.
  */
  setAttribute(t, e, i) {
    i._setAttribute(t, e);
  }
  /**
  * Removes attribute from the element.
  *
  * ```ts
  * writer.removeAttribute( 'href', linkElement );
  * ```
  *
  * @see module:engine/view/element~Element#_removeAttribute
  * @param key Attribute key.
  * @param element Element from which attribute will be removed.
  */
  removeAttribute(t, e) {
    e._removeAttribute(t);
  }
  /**
  * Adds specified class to the element.
  *
  * ```ts
  * writer.addClass( 'foo', linkElement );
  * writer.addClass( [ 'foo', 'bar' ], linkElement );
  * ```
  *
  * @see module:engine/view/element~Element#_addClass
  * @param className Single class name or array of class names which will be added.
  * @param element Element for which class will be added.
  */
  addClass(t, e) {
    e._addClass(t);
  }
  /**
  * Removes specified class from the element.
  *
  * ```ts
  * writer.removeClass( 'foo', linkElement );
  * writer.removeClass( [ 'foo', 'bar' ], linkElement );
  * ```
  *
  * @see module:engine/view/element~Element#_removeClass
  * @param className Single class name or array of class names which will be removed.
  * @param element Element from which class will be removed.
  */
  removeClass(t, e) {
    e._removeClass(t);
  }
  setStyle(t, e, i) {
    qi(t) && i === void 0 ? e._setStyle(t) : i._setStyle(t, e);
  }
  /**
  * Removes specified style from the element.
  *
  * ```ts
  * writer.removeStyle( 'color', element );  // Removes 'color' style.
  * writer.removeStyle( [ 'color', 'border-top' ], element ); // Removes both 'color' and 'border-top' styles.
  * ```
  *
  * **Note**: This method can work with normalized style names if
  * {@link module:engine/controller/datacontroller~DataController#addStyleProcessorRules a particular style processor rule is enabled}.
  * See {@link module:engine/view/stylesmap~StylesMap#remove `StylesMap#remove()`} for details.
  *
  * @see module:engine/view/element~Element#_removeStyle
  * @param property Style property name or names to be removed.
  * @param element Element from which style will be removed.
  */
  removeStyle(t, e) {
    e._removeStyle(t);
  }
  /**
  * Sets a custom property on element. Unlike attributes, custom properties are not rendered to the DOM,
  * so they can be used to add special data to elements.
  *
  * @see module:engine/view/element~Element#_setCustomProperty
  * @param key Custom property name/key.
  * @param value Custom property value to be stored.
  * @param element Element for which custom property will be set.
  */
  setCustomProperty(t, e, i) {
    i._setCustomProperty(t, e);
  }
  /**
  * Removes a custom property stored under the given key.
  *
  * @see module:engine/view/element~Element#_removeCustomProperty
  * @param key Name/key of the custom property to be removed.
  * @param element Element from which the custom property will be removed.
  * @returns Returns true if property was removed.
  */
  removeCustomProperty(t, e) {
    return e._removeCustomProperty(t);
  }
  /**
  * Creates position at the given location. The location can be specified as:
  *
  * * a {@link module:engine/view/position~Position position},
  * * parent element and offset (offset defaults to `0`),
  * * parent element and `'end'` (sets position at the end of that element),
  * * {@link module:engine/view/item~Item view item} and `'before'` or `'after'` (sets position before or after given view item).
  *
  * This method is a shortcut to other constructors such as:
  *
  * * {@link #createPositionBefore},
  * * {@link #createPositionAfter},
  *
  * @param offset Offset or one of the flags. Used only when first parameter is a {@link module:engine/view/item~Item view item}.
  */
  createPositionAt(t, e) {
    return re._createAt(t, e);
  }
  /**
  * Creates a new position after given view item.
  *
  * @param item View item after which the position should be located.
  */
  createPositionAfter(t) {
    return re._createAfter(t);
  }
  /**
  * Creates a new position before given view item.
  *
  * @param item View item before which the position should be located.
  */
  createPositionBefore(t) {
    return re._createBefore(t);
  }
  /**
  * Creates a range spanning from `start` position to `end` position.
  *
  * **Note:** This factory method creates it's own {@link module:engine/view/position~Position} instances basing on passed values.
  *
  * @param start Start position.
  * @param end End position. If not set, range will be collapsed at `start` position.
  */
  createRange(t, e) {
    return new Re(t, e);
  }
  /**
  * Creates a range that starts before given {@link module:engine/view/item~Item view item} and ends after it.
  */
  createRangeOn(t) {
    return Re._createOn(t);
  }
  /**
  * Creates a range inside an {@link module:engine/view/element~Element element} which starts before the first child of
  * that element and ends after the last child of that element.
  *
  * @param element Element which is a parent for the range.
  */
  createRangeIn(t) {
    return Re._createIn(t);
  }
  createSelection(...t) {
    return new Dn(...t);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const UO = /^#([0-9a-f]{3,4}|[0-9a-f]{6}|[0-9a-f]{8})$/i, WO = /^rgb\([ ]?([0-9]{1,3}[ %]?,[ ]?){2,3}[0-9]{1,3}[ %]?\)$/i, qO = /^rgba\([ ]?([0-9]{1,3}[ %]?,[ ]?){3}(1|[0-9]+%|[0]?\.?[0-9]+)\)$/i, GO = /^hsl\([ ]?([0-9]{1,3}[ %]?[,]?[ ]*){3}(1|[0-9]+%|[0]?\.?[0-9]+)?\)$/i, KO = /^hsla\([ ]?([0-9]{1,3}[ %]?,[ ]?){2,3}(1|[0-9]+%|[0]?\.?[0-9]+)\)$/i, ZO = /\w+\((?:[^()]|\([^()]*\))*\)|\S+/gi, YO = /* @__PURE__ */ new Set([
  // CSS Level 1
  "black",
  "silver",
  "gray",
  "white",
  "maroon",
  "red",
  "purple",
  "fuchsia",
  "green",
  "lime",
  "olive",
  "yellow",
  "navy",
  "blue",
  "teal",
  "aqua",
  // CSS Level 2 (Revision 1)
  "orange",
  // CSS Color Module Level 3
  "aliceblue",
  "antiquewhite",
  "aquamarine",
  "azure",
  "beige",
  "bisque",
  "blanchedalmond",
  "blueviolet",
  "brown",
  "burlywood",
  "cadetblue",
  "chartreuse",
  "chocolate",
  "coral",
  "cornflowerblue",
  "cornsilk",
  "crimson",
  "cyan",
  "darkblue",
  "darkcyan",
  "darkgoldenrod",
  "darkgray",
  "darkgreen",
  "darkgrey",
  "darkkhaki",
  "darkmagenta",
  "darkolivegreen",
  "darkorange",
  "darkorchid",
  "darkred",
  "darksalmon",
  "darkseagreen",
  "darkslateblue",
  "darkslategray",
  "darkslategrey",
  "darkturquoise",
  "darkviolet",
  "deeppink",
  "deepskyblue",
  "dimgray",
  "dimgrey",
  "dodgerblue",
  "firebrick",
  "floralwhite",
  "forestgreen",
  "gainsboro",
  "ghostwhite",
  "gold",
  "goldenrod",
  "greenyellow",
  "grey",
  "honeydew",
  "hotpink",
  "indianred",
  "indigo",
  "ivory",
  "khaki",
  "lavender",
  "lavenderblush",
  "lawngreen",
  "lemonchiffon",
  "lightblue",
  "lightcoral",
  "lightcyan",
  "lightgoldenrodyellow",
  "lightgray",
  "lightgreen",
  "lightgrey",
  "lightpink",
  "lightsalmon",
  "lightseagreen",
  "lightskyblue",
  "lightslategray",
  "lightslategrey",
  "lightsteelblue",
  "lightyellow",
  "limegreen",
  "linen",
  "magenta",
  "mediumaquamarine",
  "mediumblue",
  "mediumorchid",
  "mediumpurple",
  "mediumseagreen",
  "mediumslateblue",
  "mediumspringgreen",
  "mediumturquoise",
  "mediumvioletred",
  "midnightblue",
  "mintcream",
  "mistyrose",
  "moccasin",
  "navajowhite",
  "oldlace",
  "olivedrab",
  "orangered",
  "orchid",
  "palegoldenrod",
  "palegreen",
  "paleturquoise",
  "palevioletred",
  "papayawhip",
  "peachpuff",
  "peru",
  "pink",
  "plum",
  "powderblue",
  "rosybrown",
  "royalblue",
  "saddlebrown",
  "salmon",
  "sandybrown",
  "seagreen",
  "seashell",
  "sienna",
  "skyblue",
  "slateblue",
  "slategray",
  "slategrey",
  "snow",
  "springgreen",
  "steelblue",
  "tan",
  "thistle",
  "tomato",
  "turquoise",
  "violet",
  "wheat",
  "whitesmoke",
  "yellowgreen",
  // CSS Color Module Level 3 (System Colors)
  "activeborder",
  "activecaption",
  "appworkspace",
  "background",
  "buttonface",
  "buttonhighlight",
  "buttonshadow",
  "buttontext",
  "captiontext",
  "graytext",
  "highlight",
  "highlighttext",
  "inactiveborder",
  "inactivecaption",
  "inactivecaptiontext",
  "infobackground",
  "infotext",
  "menu",
  "menutext",
  "scrollbar",
  "threeddarkshadow",
  "threedface",
  "threedhighlight",
  "threedlightshadow",
  "threedshadow",
  "window",
  "windowframe",
  "windowtext",
  // CSS Color Module Level 4
  "rebeccapurple",
  // Keywords
  "currentcolor",
  "transparent"
]);
function ay(n) {
  return n.startsWith("#") ? UO.test(n) : n.startsWith("rgb") ? WO.test(n) || qO.test(n) : n.startsWith("hsl") ? GO.test(n) || KO.test(n) : YO.has(n.toLowerCase());
}
const JO = [
  "none",
  "hidden",
  "dotted",
  "dashed",
  "solid",
  "double",
  "groove",
  "ridge",
  "inset",
  "outset"
];
function XO(n) {
  return JO.includes(n);
}
const QO = /^([+-]?[0-9]*([.][0-9]+)?(px|cm|mm|in|pc|pt|ch|em|ex|rem|vh|vw|vmin|vmax)|0)$/;
function Jc(n) {
  return QO.test(n);
}
const e8 = /^[+-]?[0-9]*([.][0-9]+)?%$/;
function ly(n) {
  return e8.test(n);
}
const t8 = [
  "repeat-x",
  "repeat-y",
  "repeat",
  "space",
  "round",
  "no-repeat"
];
function i8(n) {
  return t8.includes(n);
}
const n8 = [
  "center",
  "top",
  "bottom",
  "left",
  "right"
];
function s8(n) {
  return n8.includes(n);
}
const r8 = [
  "fixed",
  "scroll",
  "local"
];
function o8(n) {
  return r8.includes(n);
}
const a8 = /^url\(/;
function l8(n) {
  return a8.test(n);
}
function Uo(n = "") {
  if (n === "")
    return {
      top: void 0,
      right: void 0,
      bottom: void 0,
      left: void 0
    };
  const t = Bf(n), e = t[0], i = t[2] || e, s = t[1] || e, r = t[3] || s;
  return {
    top: e,
    bottom: i,
    right: s,
    left: r
  };
}
function Wo(n) {
  return (t) => {
    const { top: e, right: i, bottom: s, left: r } = t, o = [];
    return [
      e,
      i,
      r,
      s
    ].every((a) => !!a) ? o.push([
      n,
      c8(t)
    ]) : (e && o.push([
      n + "-top",
      e
    ]), i && o.push([
      n + "-right",
      i
    ]), s && o.push([
      n + "-bottom",
      s
    ]), r && o.push([
      n + "-left",
      r
    ])), o;
  };
}
function c8({ top: n, right: t, bottom: e, left: i }) {
  const s = [];
  return i !== t ? s.push(n, t, e, i) : e !== n ? s.push(n, t, e) : t !== n ? s.push(n, t) : s.push(n), s.join(" ");
}
function cy(n) {
  return (t) => ({
    path: n,
    value: Uo(t)
  });
}
function Bf(n) {
  const t = n.trim().slice(0, 1500).matchAll(ZO);
  return Array.from(t).map((e) => e[0]);
}
function Mf(n) {
  n.setNormalizer("background", u8()), n.setNormalizer("background-color", d8()), n.setReducer("background", h8()), n.setStyleRelation("background", [
    "background-color"
  ]);
}
function u8() {
  return (n) => {
    const t = {}, e = Bf(n);
    for (const i of e)
      i8(i) ? (t.repeat = t.repeat || [], t.repeat.push(i)) : s8(i) ? (t.position = t.position || [], t.position.push(i)) : o8(i) ? t.attachment = i : ay(i) ? t.color = i : l8(i) && (t.image = i);
    return {
      path: "background",
      value: t
    };
  };
}
function d8() {
  return (n) => ({
    path: "background.color",
    value: n
  });
}
function h8() {
  return (n) => {
    const t = [];
    return t.push([
      "background-color",
      n.color
    ]), t;
  };
}
function uy(n) {
  n.setNormalizer("border", f8()), n.setNormalizer("border-top", rl("top")), n.setNormalizer("border-right", rl("right")), n.setNormalizer("border-bottom", rl("bottom")), n.setNormalizer("border-left", rl("left")), n.setNormalizer("border-color", Wu("color")), n.setNormalizer("border-width", Wu("width")), n.setNormalizer("border-style", Wu("style")), n.setNormalizer("border-top-color", Oi("color", "top")), n.setNormalizer("border-top-style", Oi("style", "top")), n.setNormalizer("border-top-width", Oi("width", "top")), n.setNormalizer("border-right-color", Oi("color", "right")), n.setNormalizer("border-right-style", Oi("style", "right")), n.setNormalizer("border-right-width", Oi("width", "right")), n.setNormalizer("border-bottom-color", Oi("color", "bottom")), n.setNormalizer("border-bottom-style", Oi("style", "bottom")), n.setNormalizer("border-bottom-width", Oi("width", "bottom")), n.setNormalizer("border-left-color", Oi("color", "left")), n.setNormalizer("border-left-style", Oi("style", "left")), n.setNormalizer("border-left-width", Oi("width", "left")), n.setExtractor("border-top", ol("top")), n.setExtractor("border-right", ol("right")), n.setExtractor("border-bottom", ol("bottom")), n.setExtractor("border-left", ol("left")), n.setExtractor("border-top-color", "border.color.top"), n.setExtractor("border-right-color", "border.color.right"), n.setExtractor("border-bottom-color", "border.color.bottom"), n.setExtractor("border-left-color", "border.color.left"), n.setExtractor("border-top-width", "border.width.top"), n.setExtractor("border-right-width", "border.width.right"), n.setExtractor("border-bottom-width", "border.width.bottom"), n.setExtractor("border-left-width", "border.width.left"), n.setExtractor("border-top-style", "border.style.top"), n.setExtractor("border-right-style", "border.style.right"), n.setExtractor("border-bottom-style", "border.style.bottom"), n.setExtractor("border-left-style", "border.style.left"), n.setReducer("border-color", Wo("border-color")), n.setReducer("border-style", Wo("border-style")), n.setReducer("border-width", Wo("border-width")), n.setReducer("border-top", al("top")), n.setReducer("border-right", al("right")), n.setReducer("border-bottom", al("bottom")), n.setReducer("border-left", al("left")), n.setReducer("border", g8()), n.setStyleRelation("border", [
    "border-color",
    "border-style",
    "border-width",
    "border-top",
    "border-right",
    "border-bottom",
    "border-left",
    "border-top-color",
    "border-right-color",
    "border-bottom-color",
    "border-left-color",
    "border-top-style",
    "border-right-style",
    "border-bottom-style",
    "border-left-style",
    "border-top-width",
    "border-right-width",
    "border-bottom-width",
    "border-left-width"
  ]), n.setStyleRelation("border-color", [
    "border-top-color",
    "border-right-color",
    "border-bottom-color",
    "border-left-color"
  ]), n.setStyleRelation("border-style", [
    "border-top-style",
    "border-right-style",
    "border-bottom-style",
    "border-left-style"
  ]), n.setStyleRelation("border-width", [
    "border-top-width",
    "border-right-width",
    "border-bottom-width",
    "border-left-width"
  ]), n.setStyleRelation("border-top", [
    "border-top-color",
    "border-top-style",
    "border-top-width"
  ]), n.setStyleRelation("border-right", [
    "border-right-color",
    "border-right-style",
    "border-right-width"
  ]), n.setStyleRelation("border-bottom", [
    "border-bottom-color",
    "border-bottom-style",
    "border-bottom-width"
  ]), n.setStyleRelation("border-left", [
    "border-left-color",
    "border-left-style",
    "border-left-width"
  ]);
}
function f8() {
  return (n) => {
    const { color: t, style: e, width: i } = dy(n);
    return {
      path: "border",
      value: {
        color: Uo(t),
        style: Uo(e),
        width: Uo(i)
      }
    };
  };
}
function rl(n) {
  return (t) => {
    const { color: e, style: i, width: s } = dy(t), r = {};
    return e !== void 0 && (r.color = {
      [n]: e
    }), i !== void 0 && (r.style = {
      [n]: i
    }), s !== void 0 && (r.width = {
      [n]: s
    }), {
      path: "border",
      value: r
    };
  };
}
function Wu(n) {
  return (t) => ({
    path: "border",
    value: m8(t, n)
  });
}
function m8(n, t) {
  return {
    [t]: Uo(n)
  };
}
function Oi(n, t) {
  return (e) => ({
    path: "border",
    value: {
      [n]: {
        [t]: e
      }
    }
  });
}
function ol(n) {
  return (t, e) => {
    if (e.border)
      return Oo(e.border, n);
  };
}
function Oo(n, t) {
  const e = {};
  return n.width && n.width[t] && (e.width = n.width[t]), n.style && n.style[t] && (e.style = n.style[t]), n.color && n.color[t] && (e.color = n.color[t]), e;
}
function dy(n) {
  const t = {}, e = Bf(n);
  for (const i of e)
    Jc(i) || /thin|medium|thick/.test(i) ? t.width = i : XO(i) ? t.style = i : t.color = i;
  return t;
}
function g8() {
  return (t) => {
    const e = Oo(t, "top"), i = Oo(t, "right"), s = Oo(t, "bottom"), r = Oo(t, "left"), o = [
      e,
      i,
      s,
      r
    ], a = {
      width: n(o, "width"),
      style: n(o, "style"),
      color: n(o, "color")
    }, l = xr(a, "all");
    return l.length ? l : [
      ...Object.entries(a).reduce((u, [d, h]) => (h && (u.push([
        `border-${d}`,
        h
      ]), o.forEach((m) => delete m[d])), u), []),
      ...xr(e, "top"),
      ...xr(i, "right"),
      ...xr(s, "bottom"),
      ...xr(r, "left")
    ];
  };
  function n(t, e) {
    return t.map((i) => i[e]).reduce((i, s) => i == s ? i : null);
  }
}
function al(n) {
  return (t) => xr(t, n);
}
function xr(n, t) {
  const e = [];
  if (n && n.width && e.push("width"), n && n.style && e.push("style"), n && n.color && e.push("color"), e.length == 3) {
    const i = e.map((s) => n[s]).join(" ");
    return [
      t == "all" ? [
        "border",
        i
      ] : [
        `border-${t}`,
        i
      ]
    ];
  }
  return t == "all" ? [] : e.map((i) => [
    `border-${t}-${i}`,
    n[i]
  ]);
}
function p8(n) {
  n.setNormalizer("margin", cy("margin")), n.setNormalizer("margin-top", (t) => ({
    path: "margin.top",
    value: t
  })), n.setNormalizer("margin-right", (t) => ({
    path: "margin.right",
    value: t
  })), n.setNormalizer("margin-bottom", (t) => ({
    path: "margin.bottom",
    value: t
  })), n.setNormalizer("margin-left", (t) => ({
    path: "margin.left",
    value: t
  })), n.setReducer("margin", Wo("margin")), n.setStyleRelation("margin", [
    "margin-top",
    "margin-right",
    "margin-bottom",
    "margin-left"
  ]);
}
function b8(n) {
  n.setNormalizer("padding", cy("padding")), n.setNormalizer("padding-top", (t) => ({
    path: "padding.top",
    value: t
  })), n.setNormalizer("padding-right", (t) => ({
    path: "padding.right",
    value: t
  })), n.setNormalizer("padding-bottom", (t) => ({
    path: "padding.bottom",
    value: t
  })), n.setNormalizer("padding-left", (t) => ({
    path: "padding.left",
    value: t
  })), n.setReducer("padding", Wo("padding")), n.setStyleRelation("padding", [
    "padding-top",
    "padding-right",
    "padding-bottom",
    "padding-left"
  ]);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class hy {
  /**
  * @param {module:watchdog/watchdog~WatchdogConfig} config The watchdog plugin configuration.
  */
  constructor(t) {
    /**
    * An array of crashes saved as an object with the following properties:
    *
    * * `message`: `String`,
    * * `stack`: `String`,
    * * `date`: `Number`,
    * * `filename`: `String | undefined`,
    * * `lineno`: `Number | undefined`,
    * * `colno`: `Number | undefined`,
    */
    f(this, "crashes", []);
    /**
    * Specifies the state of the item watched by the watchdog. The state can be one of the following values:
    *
    * * `initializing` &ndash; Before the first initialization, and after crashes, before the item is ready.
    * * `ready` &ndash; A state when the user can interact with the item.
    * * `crashed` &ndash; A state when an error occurs. It quickly changes to `initializing` or `crashedPermanently`
    * depending on how many and how frequent errors have been caught recently.
    * * `crashedPermanently` &ndash; A state when the watchdog stops reacting to errors and keeps the item it is watching crashed,
    * * `destroyed` &ndash; A state when the item is manually destroyed by the user after calling `watchdog.destroy()`.
    */
    f(this, "state", "initializing");
    /**
    * @see module:watchdog/watchdog~WatchdogConfig
    */
    f(this, "_crashNumberLimit");
    /**
    * Returns the result of the `Date.now()` call. It can be overridden in tests to mock time as some popular
    * approaches like `sinon.useFakeTimers()` do not work well with error handling.
    */
    f(this, "_now", Date.now);
    /**
    * @see module:watchdog/watchdog~WatchdogConfig
    */
    f(this, "_minimumNonErrorTimePeriod");
    /**
    * Checks if the event error comes from the underlying item and restarts the item.
    */
    f(this, "_boundErrorHandler");
    /**
    * A dictionary of event emitter listeners.
    */
    f(this, "_listeners");
    if (this.crashes = [], this._crashNumberLimit = typeof t.crashNumberLimit == "number" ? t.crashNumberLimit : 3, this._minimumNonErrorTimePeriod = typeof t.minimumNonErrorTimePeriod == "number" ? t.minimumNonErrorTimePeriod : 5e3, this._boundErrorHandler = (e) => {
      const i = "error" in e ? e.error : e.reason;
      i instanceof Error && this._handleError(i, e);
    }, this._listeners = {}, !this._restart)
      throw new Error("The Watchdog class was split into the abstract `Watchdog` class and the `EditorWatchdog` class. Please, use `EditorWatchdog` if you have used the `Watchdog` class previously.");
  }
  /**
  * Destroys the watchdog and releases the resources.
  */
  destroy() {
    this._stopErrorHandling(), this._listeners = {};
  }
  /**
  * Starts listening to a specific event name by registering a callback that will be executed
  * whenever an event with a given name fires.
  *
  * Note that this method differs from the CKEditor 5's default `EventEmitterMixin` implementation.
  *
  * @param eventName The event name.
  * @param callback A callback which will be added to event listeners.
  */
  on(t, e) {
    this._listeners[t] || (this._listeners[t] = []), this._listeners[t].push(e);
  }
  /**
  * Stops listening to the specified event name by removing the callback from event listeners.
  *
  * Note that this method differs from the CKEditor 5's default `EventEmitterMixin` implementation.
  *
  * @param eventName The event name.
  * @param callback A callback which will be removed from event listeners.
  */
  off(t, e) {
    this._listeners[t] = this._listeners[t].filter((i) => i !== e);
  }
  /**
  * Fires an event with a given event name and arguments.
  *
  * Note that this method differs from the CKEditor 5's default `EventEmitterMixin` implementation.
  */
  _fire(t, ...e) {
    const i = this._listeners[t] || [];
    for (const s of i)
      s.apply(this, [
        null,
        ...e
      ]);
  }
  /**
  * Starts error handling by attaching global error handlers.
  */
  _startErrorHandling() {
    window.addEventListener("error", this._boundErrorHandler), window.addEventListener("unhandledrejection", this._boundErrorHandler);
  }
  /**
  * Stops error handling by detaching global error handlers.
  */
  _stopErrorHandling() {
    window.removeEventListener("error", this._boundErrorHandler), window.removeEventListener("unhandledrejection", this._boundErrorHandler);
  }
  /**
  * Checks if an error comes from the watched item and restarts it.
  * It reacts to {@link module:utils/ckeditorerror~CKEditorError `CKEditorError` errors} only.
  *
  * @fires error
  * @param error Error.
  * @param evt An error event.
  */
  _handleError(t, e) {
    if (this._shouldReactToError(t)) {
      this.crashes.push({
        message: t.message,
        stack: t.stack,
        // `evt.filename`, `evt.lineno` and `evt.colno` are available only in ErrorEvent events
        filename: e instanceof ErrorEvent ? e.filename : void 0,
        lineno: e instanceof ErrorEvent ? e.lineno : void 0,
        colno: e instanceof ErrorEvent ? e.colno : void 0,
        date: this._now()
      });
      const i = this._shouldRestart();
      this.state = "crashed", this._fire("stateChange"), this._fire("error", {
        error: t,
        causesRestart: i
      }), i ? this._restart() : (this.state = "crashedPermanently", this._fire("stateChange"));
    }
  }
  /**
  * Checks whether an error should be handled by the watchdog.
  *
  * @param error An error that was caught by the error handling process.
  */
  _shouldReactToError(t) {
    return t.is && t.is("CKEditorError") && t.context !== void 0 && // In some cases the watched item should not be restarted - e.g. during the item initialization.
    // That's why the `null` was introduced as a correct error context which does cause restarting.
    t.context !== null && // Do not react to errors if the watchdog is in states other than `ready`.
    this.state === "ready" && this._isErrorComingFromThisItem(t);
  }
  /**
  * Checks if the watchdog should restart the underlying item.
  */
  _shouldRestart() {
    if (this.crashes.length <= this._crashNumberLimit)
      return !0;
    const t = this.crashes[this.crashes.length - 1].date, e = this.crashes[this.crashes.length - 1 - this._crashNumberLimit].date;
    return (t - e) / this._crashNumberLimit > this._minimumNonErrorTimePeriod;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function mh(n, t = /* @__PURE__ */ new Set()) {
  const e = [
    n
  ], i = /* @__PURE__ */ new Set();
  let s = 0;
  for (; e.length > s; ) {
    const r = e[s++];
    if (!(i.has(r) || !w8(r) || t.has(r)))
      if (i.add(r), Symbol.iterator in r)
        try {
          for (const o of r)
            e.push(o);
        } catch {
        }
      else
        for (const o in r)
          o !== "defaultValue" && e.push(r[o]);
  }
  return i;
}
function w8(n) {
  const t = Object.prototype.toString.call(n), e = typeof n;
  return !(e === "number" || e === "boolean" || e === "string" || e === "symbol" || e === "function" || t === "[object Date]" || t === "[object RegExp]" || t === "[object Module]" || n === void 0 || n === null || // This flag is meant to exclude singletons shared across editor instances. So when an error is thrown in one editor,
  // the other editors connected through the reference to the same singleton are not restarted. This is a temporary workaround
  // until a better solution is found.
  // More in https://github.com/ckeditor/ckeditor5/issues/12292.
  n._watchdogExcluded || // Skip native DOM objects, e.g. Window, nodes, events, etc.
  n instanceof EventTarget || n instanceof Event);
}
function fy(n, t, e = /* @__PURE__ */ new Set()) {
  if (n === t && v8(n))
    return !0;
  const i = mh(n, e), s = mh(t, e);
  for (const r of i)
    if (s.has(r))
      return !0;
  return !1;
}
function v8(n) {
  return typeof n == "object" && n !== null;
}
class my extends hy {
  /**
  * @param Editor The editor class.
  * @param watchdogConfig The watchdog plugin configuration.
  */
  constructor(e, i = {}) {
    super(i);
    /**
    * The current editor instance.
    */
    f(this, "_editor", null);
    /**
    * A promise associated with the life cycle of the editor (creation or destruction processes).
    *
    * It is used to prevent the initialization of the editor if the previous instance has not been destroyed yet,
    * and conversely, to prevent the destruction of the editor if it has not been initialized.
    */
    f(this, "_lifecyclePromise", null);
    /**
    * Throttled save method. The `save()` method is called the specified `saveInterval` after `throttledSave()` is called,
    * unless a new action happens in the meantime.
    */
    f(this, "_throttledSave");
    /**
    * The latest saved editor data represented as a root name -> root data object.
    */
    f(this, "_data");
    /**
    * The last document version.
    */
    f(this, "_lastDocumentVersion");
    /**
    * The editor source element or data.
    */
    f(this, "_elementOrData");
    /**
    * Specifies whether the editor was initialized using document data (`true`) or HTML elements (`false`).
    */
    f(this, "_initUsingData", !0);
    /**
    * The latest record of the editor editable elements. Used to restart the editor.
    */
    f(this, "_editables", {});
    /**
    * The editor configuration.
    */
    f(this, "_config");
    f(this, "_excludedProps");
    this._throttledSave = $s(this._save.bind(this), typeof i.saveInterval == "number" ? i.saveInterval : 5e3), e && (this._creator = (s, r) => e.create(s, r)), this._destructor = (s) => s.destroy();
  }
  /**
  * The current editor instance.
  */
  get editor() {
    return this._editor;
  }
  /**
  * @internal
  */
  get _item() {
    return this._editor;
  }
  /**
  * Sets the function that is responsible for the editor creation.
  * It expects a function that should return a promise.
  *
  * ```ts
  * watchdog.setCreator( ( element, config ) => ClassicEditor.create( element, config ) );
  * ```
  */
  setCreator(e) {
    this._creator = e;
  }
  /**
  * Sets the function that is responsible for the editor destruction.
  * Overrides the default destruction function, which destroys only the editor instance.
  * It expects a function that should return a promise or `undefined`.
  *
  * ```ts
  * watchdog.setDestructor( editor => {
  * 	// Do something before the editor is destroyed.
  *
  * 	return editor
  * 		.destroy()
  * 		.then( () => {
  * 			// Do something after the editor is destroyed.
  * 		} );
  * } );
  * ```
  */
  setDestructor(e) {
    this._destructor = e;
  }
  /**
  * Restarts the editor instance. This method is called whenever an editor error occurs. It fires the `restart` event and changes
  * the state to `initializing`.
  *
  * @fires restart
  */
  _restart() {
    return Promise.resolve().then(() => (this.state = "initializing", this._fire("stateChange"), this._destroy())).catch((e) => {
      console.error("An error happened during the editor destroying.", e);
    }).then(() => {
      const e = {}, i = [], s = this._config.rootsAttributes || {}, r = {};
      for (const [a, l] of Object.entries(this._data.roots))
        l.isLoaded ? (e[a] = "", r[a] = s[a] || {}) : i.push(a);
      const o = {
        ...this._config,
        extraPlugins: this._config.extraPlugins || [],
        lazyRoots: i,
        rootsAttributes: r,
        _watchdogInitialData: this._data
      };
      return delete o.initialData, o.extraPlugins.push(_8), this._initUsingData ? this.create(e, o, o.context) : Hn(this._elementOrData) ? this.create(this._elementOrData, o, o.context) : this.create(this._editables, o, o.context);
    }).then(() => {
      this._fire("restart");
    });
  }
  /**
  * Creates the editor instance and keeps it running, using the defined creator and destructor.
  *
  * @param elementOrData The editor source element or the editor data.
  * @param config The editor configuration.
  * @param context A context for the editor.
  */
  create(e = this._elementOrData, i = this._config, s) {
    return this._lifecyclePromise = Promise.resolve(this._lifecyclePromise).then(() => (super._startErrorHandling(), this._elementOrData = e, this._initUsingData = typeof e == "string" || Object.keys(e).length > 0 && typeof Object.values(e)[0] == "string", this._config = this._cloneEditorConfiguration(i) || {}, this._config.context = s, this._creator(e, this._config))).then((r) => {
      this._editor = r, r.model.document.on("change:data", this._throttledSave), this._lastDocumentVersion = r.model.document.version, this._data = this._getData(), this._initUsingData || (this._editables = this._getEditables()), this.state = "ready", this._fire("stateChange");
    }).finally(() => {
      this._lifecyclePromise = null;
    }), this._lifecyclePromise;
  }
  /**
  * Destroys the watchdog and the current editor instance. It fires the callback
  * registered in {@link #setDestructor `setDestructor()`} and uses it to destroy the editor instance.
  * It also sets the state to `destroyed`.
  */
  destroy() {
    return this._lifecyclePromise = Promise.resolve(this._lifecyclePromise).then(() => (this.state = "destroyed", this._fire("stateChange"), super.destroy(), this._destroy())).finally(() => {
      this._lifecyclePromise = null;
    }), this._lifecyclePromise;
  }
  _destroy() {
    return Promise.resolve().then(() => {
      this._stopErrorHandling(), this._throttledSave.cancel();
      const e = this._editor;
      return this._editor = null, e.model.document.off("change:data", this._throttledSave), this._destructor(e);
    });
  }
  /**
  * Saves the editor data, so it can be restored after the crash even if the data cannot be fetched at
  * the moment of the crash.
  */
  _save() {
    const e = this._editor.model.document.version;
    try {
      this._data = this._getData(), this._initUsingData || (this._editables = this._getEditables()), this._lastDocumentVersion = e;
    } catch (i) {
      console.error(i, "An error happened during restoring editor data. Editor will be restored from the previously saved data.");
    }
  }
  /**
  * @internal
  */
  _setExcludedProperties(e) {
    this._excludedProps = e;
  }
  /**
  * Gets all data that is required to reinitialize editor instance.
  */
  _getData() {
    const e = this._editor, i = e.model.document.roots.filter((l) => l.isAttached() && l.rootName != "$graveyard"), { plugins: s } = e, r = s.has("CommentsRepository") && s.get("CommentsRepository"), o = s.has("TrackChanges") && s.get("TrackChanges"), a = {
      roots: {},
      markers: {},
      commentThreads: JSON.stringify([]),
      suggestions: JSON.stringify([])
    };
    i.forEach((l) => {
      a.roots[l.rootName] = {
        content: JSON.stringify(Array.from(l.getChildren())),
        attributes: JSON.stringify(Array.from(l.getAttributes())),
        isLoaded: l._isLoaded
      };
    });
    for (const l of e.model.markers)
      l._affectsData && (a.markers[l.name] = {
        rangeJSON: l.getRange().toJSON(),
        usingOperation: l._managedUsingOperations,
        affectsData: l._affectsData
      });
    return r && (a.commentThreads = JSON.stringify(r.getCommentThreads({
      toJSON: !0,
      skipNotAttached: !0
    }))), o && (a.suggestions = JSON.stringify(o.getSuggestions({
      toJSON: !0,
      skipNotAttached: !0
    }))), a;
  }
  /**
  * For each attached model root, returns its HTML editable element (if available).
  */
  _getEditables() {
    const e = {};
    for (const i of this.editor.model.document.getRootNames()) {
      const s = this.editor.ui.getEditableElement(i);
      s && (e[i] = s);
    }
    return e;
  }
  /**
  * Traverses the error context and the current editor to find out whether these structures are connected
  * to each other via properties.
  *
  * @internal
  */
  _isErrorComingFromThisItem(e) {
    return fy(this._editor, e.context, this._excludedProps);
  }
  /**
  * Clones the editor configuration.
  */
  _cloneEditorConfiguration(e) {
    return gf(e, (i, s) => {
      if (Hn(i) || s === "context")
        return i;
    });
  }
}
class _8 {
  constructor(t) {
    f(this, "editor");
    f(this, "_data");
    this.editor = t, this._data = t.config.get("_watchdogInitialData");
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.data.on("init", (t) => {
      t.stop(), this.editor.model.enqueueChange({
        isUndoable: !1
      }, (e) => {
        this._restoreCollaborationData(), this._restoreEditorData(e);
      }), this.editor.data.fire("ready");
    }, {
      priority: 999
    });
  }
  /**
  * Creates a model node (element or text) based on provided JSON.
  */
  _createNode(t, e) {
    if ("name" in e) {
      const i = t.createElement(e.name, e.attributes);
      if (e.children)
        for (const s of e.children)
          i._appendChild(this._createNode(t, s));
      return i;
    } else
      return t.createText(e.data, e.attributes);
  }
  /**
  * Restores the editor by setting the document data, roots attributes and markers.
  */
  _restoreEditorData(t) {
    const e = this.editor;
    Object.entries(this._data.roots).forEach(([i, { content: s, attributes: r }]) => {
      const o = JSON.parse(s), a = JSON.parse(r), l = e.model.document.getRoot(i);
      for (const [c, u] of a)
        t.setAttribute(c, u, l);
      for (const c of o) {
        const u = this._createNode(t, c);
        t.insert(u, l, "end");
      }
    }), Object.entries(this._data.markers).forEach(([i, s]) => {
      const { document: r } = e.model, { rangeJSON: { start: o, end: a }, ...l } = s, c = r.getRoot(o.root), u = t.createPositionFromPath(c, o.path, o.stickiness), d = t.createPositionFromPath(c, a.path, a.stickiness), h = t.createRange(u, d);
      t.addMarker(i, {
        range: h,
        ...l
      });
    });
  }
  /**
  * Restores the editor collaboration data - comment threads and suggestions.
  */
  _restoreCollaborationData() {
    const t = JSON.parse(this._data.commentThreads), e = JSON.parse(this._data.suggestions);
    t.forEach((i) => {
      const s = this.editor.config.get("collaboration.channelId"), r = this.editor.plugins.get("CommentsRepository");
      r.hasCommentThread(i.threadId) && r.getCommentThread(i.threadId).remove(), r.addCommentThread({
        channelId: s,
        ...i
      });
    }), e.forEach((i) => {
      const s = this.editor.plugins.get("TrackChangesEditing");
      if (s.hasSuggestion(i.id)) {
        const r = s.getSuggestion(i.id);
        r.attributes = i.attributes;
      } else
        s.addSuggestionData(i);
    });
  }
}
const qo = Symbol("MainQueueId");
class y8 extends hy {
  /**
  * The context watchdog class constructor.
  *
  * ```ts
  * const watchdog = new ContextWatchdog( Context );
  *
  * await watchdog.create( contextConfiguration );
  *
  * await watchdog.add( item );
  * ```
  *
  * See the {@glink features/watchdog Watchdog feature guide} to learn more how to use this feature.
  *
  * @param Context The {@link module:core/context~Context} class.
  * @param watchdogConfig The watchdog configuration.
  */
  constructor(e, i = {}) {
    super(i);
    /**
    * A map of internal watchdogs for added items.
    */
    f(this, "_watchdogs", /* @__PURE__ */ new Map());
    /**
    * The watchdog configuration.
    */
    f(this, "_watchdogConfig");
    /**
    * The current context instance.
    */
    f(this, "_context", null);
    /**
    * Context properties (nodes/references) that are gathered during the initial context creation
    * and are used to distinguish the origin of an error.
    */
    f(this, "_contextProps", /* @__PURE__ */ new Set());
    /**
    * An action queue, which is used to handle async functions queuing.
    */
    f(this, "_actionQueues", new k8());
    /**
    * The configuration for the {@link module:core/context~Context}.
    */
    f(this, "_contextConfig");
    /**
    * The watched item.
    */
    f(this, "_item");
    this._watchdogConfig = i, this._creator = (s) => e.create(s), this._destructor = (s) => s.destroy(), this._actionQueues.onEmpty(() => {
      this.state === "initializing" && (this.state = "ready", this._fire("stateChange"));
    });
  }
  /**
  * Sets the function that is responsible for the context creation.
  * It expects a function that should return a promise (or `undefined`).
  *
  * ```ts
  * watchdog.setCreator( config => Context.create( config ) );
  * ```
  */
  setCreator(e) {
    this._creator = e;
  }
  /**
  * Sets the function that is responsible for the context destruction.
  * Overrides the default destruction function, which destroys only the context instance.
  * It expects a function that should return a promise (or `undefined`).
  *
  * ```ts
  * watchdog.setDestructor( context => {
  * 	// Do something before the context is destroyed.
  *
  * 	return context
  * 		.destroy()
  * 		.then( () => {
  * 			// Do something after the context is destroyed.
  * 		} );
  * } );
  * ```
  */
  setDestructor(e) {
    this._destructor = e;
  }
  /**
  * The context instance. Keep in mind that this property might be changed when the context watchdog restarts,
  * so do not keep this instance internally. Always operate on the `ContextWatchdog#context` property.
  */
  get context() {
    return this._context;
  }
  /**
  * Initializes the context watchdog. Once it is created, the watchdog takes care about
  * recreating the context and the provided items, and starts the error handling mechanism.
  *
  * ```ts
  * await watchdog.create( {
  * 	plugins: []
  * } );
  * ```
  *
  * @param contextConfig The context configuration. See {@link module:core/context~Context}.
  */
  create(e = {}) {
    return this._actionQueues.enqueue(qo, () => (this._contextConfig = e, this._create()));
  }
  /**
  * Returns an item instance with the given `itemId`.
  *
  * ```ts
  * const editor1 = watchdog.getItem( 'editor1' );
  * ```
  *
  * @param itemId The item ID.
  * @returns The item instance or `undefined` if an item with a given ID has not been found.
  */
  getItem(e) {
    return this._getWatchdog(e)._item;
  }
  /**
  * Gets the state of the given item. See {@link #state} for a list of available states.
  *
  * ```ts
  * const editor1State = watchdog.getItemState( 'editor1' );
  * ```
  *
  * @param itemId Item ID.
  * @returns The state of the item.
  */
  getItemState(e) {
    return this._getWatchdog(e).state;
  }
  /**
  * Adds items to the watchdog. Once created, instances of these items will be available using the {@link #getItem} method.
  *
  * Items can be passed together as an array of objects:
  *
  * ```ts
  * await watchdog.add( [ {
  * 	id: 'editor1',
  * 	type: 'editor',
  * 	sourceElementOrData: document.querySelector( '#editor' ),
  * 	config: {
  * 		plugins: [ Essentials, Paragraph, Bold, Italic ],
  * 		toolbar: [ 'bold', 'italic', 'alignment' ]
  * 	},
  * 	creator: ( element, config ) => ClassicEditor.create( element, config )
  * } ] );
  * ```
  *
  * Or one by one as objects:
  *
  * ```ts
  * await watchdog.add( {
  * 	id: 'editor1',
  * 	type: 'editor',
  * 	sourceElementOrData: document.querySelector( '#editor' ),
  * 	config: {
  * 		plugins: [ Essentials, Paragraph, Bold, Italic ],
  * 		toolbar: [ 'bold', 'italic', 'alignment' ]
  * 	},
  * 	creator: ( element, config ) => ClassicEditor.create( element, config )
  * ] );
  * ```
  *
  * Then an instance can be retrieved using the {@link #getItem} method:
  *
  * ```ts
  * const editor1 = watchdog.getItem( 'editor1' );
  * ```
  *
  * Note that this method can be called multiple times, but for performance reasons it is better
  * to pass all items together.
  *
  * @param itemConfigurationOrItemConfigurations An item configuration object or an array of item configurations.
  */
  add(e) {
    const i = Xp(e);
    return Promise.all(i.map((s) => this._actionQueues.enqueue(s.id, () => {
      if (this.state === "destroyed")
        throw new Error("Cannot add items to destroyed watchdog.");
      if (!this._context)
        throw new Error("Context was not created yet. You should call the `ContextWatchdog#create()` method first.");
      let r;
      if (this._watchdogs.has(s.id))
        throw new Error(`Item with the given id is already added: '${s.id}'.`);
      if (s.type === "editor")
        return r = new my(null, this._watchdogConfig), r.setCreator(s.creator), r._setExcludedProperties(this._contextProps), s.destructor && r.setDestructor(s.destructor), this._watchdogs.set(s.id, r), r.on("error", (o, { error: a, causesRestart: l }) => {
          this._fire("itemError", {
            itemId: s.id,
            error: a
          }), l && this._actionQueues.enqueue(s.id, () => new Promise((c) => {
            const u = () => {
              r.off("restart", u), this._fire("itemRestart", {
                itemId: s.id
              }), c();
            };
            r.on("restart", u);
          }));
        }), r.create(s.sourceElementOrData, s.config, this._context);
      throw new Error(`Not supported item type: '${s.type}'.`);
    })));
  }
  /**
  * Removes and destroys item(s) with given ID(s).
  *
  * ```ts
  * await watchdog.remove( 'editor1' );
  * ```
  *
  * Or
  *
  * ```ts
  * await watchdog.remove( [ 'editor1', 'editor2' ] );
  * ```
  *
  * @param itemIdOrItemIds Item ID or an array of item IDs.
  */
  remove(e) {
    const i = Xp(e);
    return Promise.all(i.map((s) => this._actionQueues.enqueue(s, () => {
      const r = this._getWatchdog(s);
      return this._watchdogs.delete(s), r.destroy();
    })));
  }
  /**
  * Destroys the context watchdog and all added items.
  * Once the context watchdog is destroyed, new items cannot be added.
  *
  * ```ts
  * await watchdog.destroy();
  * ```
  */
  destroy() {
    return this._actionQueues.enqueue(qo, () => (this.state = "destroyed", this._fire("stateChange"), super.destroy(), this._destroy()));
  }
  /**
  * Restarts the context watchdog.
  */
  _restart() {
    return this._actionQueues.enqueue(qo, () => (this.state = "initializing", this._fire("stateChange"), this._destroy().catch((e) => {
      console.error("An error happened during destroying the context or items.", e);
    }).then(() => this._create()).then(() => this._fire("restart"))));
  }
  /**
  * Initializes the context watchdog.
  */
  _create() {
    return Promise.resolve().then(() => (this._startErrorHandling(), this._creator(this._contextConfig))).then((e) => (this._context = e, this._contextProps = mh(this._context), Promise.all(Array.from(this._watchdogs.values()).map((i) => (i._setExcludedProperties(this._contextProps), i.create(void 0, void 0, this._context))))));
  }
  /**
  * Destroys the context instance and all added items.
  */
  _destroy() {
    return Promise.resolve().then(() => {
      this._stopErrorHandling();
      const e = this._context;
      return this._context = null, this._contextProps = /* @__PURE__ */ new Set(), Promise.all(Array.from(this._watchdogs.values()).map((i) => i.destroy())).then(() => this._destructor(e));
    });
  }
  /**
  * Returns the watchdog for a given item ID.
  *
  * @param itemId Item ID.
  */
  _getWatchdog(e) {
    const i = this._watchdogs.get(e);
    if (!i)
      throw new Error(`Item with the given id was not registered: ${e}.`);
    return i;
  }
  /**
  * Checks whether an error comes from the context instance and not from the item instances.
  *
  * @internal
  */
  _isErrorComingFromThisItem(e) {
    for (const i of this._watchdogs.values())
      if (i._isErrorComingFromThisItem(e))
        return !1;
    return fy(this._context, e.context);
  }
}
class k8 {
  constructor() {
    f(this, "_onEmptyCallbacks", []);
    f(this, "_queues", /* @__PURE__ */ new Map());
    f(this, "_activeActions", 0);
  }
  /**
  * Used to register callbacks that will be run when the queue becomes empty.
  *
  * @param onEmptyCallback A callback that will be run whenever the queue becomes empty.
  */
  onEmpty(t) {
    this._onEmptyCallbacks.push(t);
  }
  /**
  * It adds asynchronous actions (functions) to the proper queue and runs them one by one.
  *
  * @param queueId The action queue ID.
  * @param action A function that should be enqueued.
  */
  enqueue(t, e) {
    const i = t === qo;
    this._activeActions++, this._queues.get(t) || this._queues.set(t, Promise.resolve());
    const r = (i ? Promise.all(this._queues.values()) : Promise.all([
      this._queues.get(qo),
      this._queues.get(t)
    ])).then(e), o = r.catch(() => {
    });
    return this._queues.set(t, o), r.finally(() => {
      this._activeActions--, this._queues.get(t) === o && this._activeActions === 0 && this._onEmptyCallbacks.forEach((a) => a());
    });
  }
}
function Xp(n) {
  return Array.isArray(n) ? n : [
    n
  ];
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class I extends (/* @__PURE__ */ je()) {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super();
    /**
    * The editor instance.
    *
    * Note that most editors implement the {@link module:core/editor/editor~Editor#ui} property.
    * However, editors with an external UI (i.e. Bootstrap-based) or a headless editor may not have this property or
    * throw an error when accessing it.
    *
    * Because of above, to make plugins more universal, it is recommended to split features into:
    *  - The "editing" part that uses the {@link module:core/editor/editor~Editor} class without `ui` property.
    *  - The "UI" part that uses the {@link module:core/editor/editor~Editor} class and accesses `ui` property.
    */
    f(this, "editor");
    /**
    * Holds identifiers for {@link #forceDisabled} mechanism.
    */
    f(this, "_disableStack", /* @__PURE__ */ new Set());
    this.editor = e, this.set("isEnabled", !0);
  }
  /**
  * Disables the plugin.
  *
  * Plugin may be disabled by multiple features or algorithms (at once). When disabling a plugin, unique id should be passed
  * (e.g. feature name). The same identifier should be used when {@link #clearForceDisabled enabling back} the plugin.
  * The plugin becomes enabled only after all features {@link #clearForceDisabled enabled it back}.
  *
  * Disabling and enabling a plugin:
  *
  * ```ts
  * plugin.isEnabled; // -> true
  * plugin.forceDisabled( 'MyFeature' );
  * plugin.isEnabled; // -> false
  * plugin.clearForceDisabled( 'MyFeature' );
  * plugin.isEnabled; // -> true
  * ```
  *
  * Plugin disabled by multiple features:
  *
  * ```ts
  * plugin.forceDisabled( 'MyFeature' );
  * plugin.forceDisabled( 'OtherFeature' );
  * plugin.clearForceDisabled( 'MyFeature' );
  * plugin.isEnabled; // -> false
  * plugin.clearForceDisabled( 'OtherFeature' );
  * plugin.isEnabled; // -> true
  * ```
  *
  * Multiple disabling with the same identifier is redundant:
  *
  * ```ts
  * plugin.forceDisabled( 'MyFeature' );
  * plugin.forceDisabled( 'MyFeature' );
  * plugin.clearForceDisabled( 'MyFeature' );
  * plugin.isEnabled; // -> true
  * ```
  *
  * **Note:** some plugins or algorithms may have more complex logic when it comes to enabling or disabling certain plugins,
  * so the plugin might be still disabled after {@link #clearForceDisabled} was used.
  *
  * @param id Unique identifier for disabling. Use the same id when {@link #clearForceDisabled enabling back} the plugin.
  */
  forceDisabled(e) {
    this._disableStack.add(e), this._disableStack.size == 1 && (this.on("set:isEnabled", Qp, {
      priority: "highest"
    }), this.isEnabled = !1);
  }
  /**
  * Clears forced disable previously set through {@link #forceDisabled}. See {@link #forceDisabled}.
  *
  * @param id Unique identifier, equal to the one passed in {@link #forceDisabled} call.
  */
  clearForceDisabled(e) {
    this._disableStack.delete(e), this._disableStack.size == 0 && (this.off("set:isEnabled", Qp), this.isEnabled = !0);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this.stopListening();
  }
  /**
  * @inheritDoc
  */
  static get isContextPlugin() {
    return !1;
  }
  /**
  * @inheritDoc
  * @internal
  */
  static get isOfficialPlugin() {
    return !1;
  }
  /**
  * @inheritDoc
  * @internal
  */
  static get isPremiumPlugin() {
    return !1;
  }
}
function Qp(n) {
  n.return = !1, n.stop();
}
class he extends (/* @__PURE__ */ je()) {
  /**
  * Creates a new `Command` instance.
  *
  * @param editor The editor on which this command will be used.
  */
  constructor(e) {
    super();
    /**
    * The editor on which this command will be used.
    */
    f(this, "editor");
    /**
    * A flag indicating whether a command's `isEnabled` state should be changed depending on where the document
    * selection is placed.
    *
    * By default, it is set to `true`. If the document selection is placed in a
    * {@link module:engine/model/model~Model#canEditAt non-editable} place (such as non-editable root), the command becomes disabled.
    *
    * The flag should be changed to `false` in a concrete command's constructor if the command should not change its `isEnabled`
    * accordingly to the document selection.
    */
    f(this, "_isEnabledBasedOnSelection");
    /**
    * A flag indicating whether a command execution changes the editor data or not.
    *
    * @see #affectsData
    */
    f(this, "_affectsData");
    /**
    * Holds identifiers for {@link #forceDisabled} mechanism.
    */
    f(this, "_disableStack");
    this.editor = e, this.set("value", void 0), this.set("isEnabled", !1), this._affectsData = !0, this._isEnabledBasedOnSelection = !0, this._disableStack = /* @__PURE__ */ new Set(), this.decorate("execute"), this.listenTo(this.editor.model.document, "change", () => {
      this.refresh();
    }), this.listenTo(e, "change:isReadOnly", () => {
      this.refresh();
    }), this.on("set:isEnabled", (i) => {
      if (!this.affectsData)
        return;
      const s = e.model.document.selection, o = !(s.getFirstPosition().root.rootName == "$graveyard") && e.model.canEditAt(s);
      (e.isReadOnly || this._isEnabledBasedOnSelection && !o) && (i.return = !1, i.stop());
    }, {
      priority: "highest"
    }), this.on("execute", (i) => {
      this.isEnabled || i.stop();
    }, {
      priority: "high"
    });
  }
  /**
  * A flag indicating whether a command execution changes the editor data or not.
  *
  * Commands with `affectsData` set to `false` will not be automatically disabled in
  * the {@link module:core/editor/editor~Editor#isReadOnly read-only mode} and
  * {@glink features/read-only#related-features other editor modes} with restricted user write permissions.
  *
  * **Note:** You do not have to set it for your every command. It is `true` by default.
  *
  * @default true
  */
  get affectsData() {
    return this._affectsData;
  }
  set affectsData(e) {
    this._affectsData = e;
  }
  /**
  * Refreshes the command. The command should update its {@link #isEnabled} and {@link #value} properties
  * in this method.
  *
  * This method is automatically called when
  * {@link module:engine/model/document~Document#event:change any changes are applied to the document}.
  */
  refresh() {
    this.isEnabled = !0;
  }
  /**
  * Disables the command.
  *
  * Command may be disabled by multiple features or algorithms (at once). When disabling a command, unique id should be passed
  * (e.g. the feature name). The same identifier should be used when {@link #clearForceDisabled enabling back} the command.
  * The command becomes enabled only after all features {@link #clearForceDisabled enabled it back}.
  *
  * Disabling and enabling a command:
  *
  * ```ts
  * command.isEnabled; // -> true
  * command.forceDisabled( 'MyFeature' );
  * command.isEnabled; // -> false
  * command.clearForceDisabled( 'MyFeature' );
  * command.isEnabled; // -> true
  * ```
  *
  * Command disabled by multiple features:
  *
  * ```ts
  * command.forceDisabled( 'MyFeature' );
  * command.forceDisabled( 'OtherFeature' );
  * command.clearForceDisabled( 'MyFeature' );
  * command.isEnabled; // -> false
  * command.clearForceDisabled( 'OtherFeature' );
  * command.isEnabled; // -> true
  * ```
  *
  * Multiple disabling with the same identifier is redundant:
  *
  * ```ts
  * command.forceDisabled( 'MyFeature' );
  * command.forceDisabled( 'MyFeature' );
  * command.clearForceDisabled( 'MyFeature' );
  * command.isEnabled; // -> true
  * ```
  *
  * **Note:** some commands or algorithms may have more complex logic when it comes to enabling or disabling certain commands,
  * so the command might be still disabled after {@link #clearForceDisabled} was used.
  *
  * @param id Unique identifier for disabling. Use the same id when {@link #clearForceDisabled enabling back} the command.
  */
  forceDisabled(e) {
    this._disableStack.add(e), this._disableStack.size == 1 && (this.on("set:isEnabled", eb, {
      priority: "highest"
    }), this.isEnabled = !1);
  }
  /**
  * Clears forced disable previously set through {@link #forceDisabled}. See {@link #forceDisabled}.
  *
  * @param id Unique identifier, equal to the one passed in {@link #forceDisabled} call.
  */
  clearForceDisabled(e) {
    this._disableStack.delete(e), this._disableStack.size == 0 && (this.off("set:isEnabled", eb), this.refresh());
  }
  /**
  * Executes the command.
  *
  * A command may accept parameters. They will be passed from {@link module:core/editor/editor~Editor#execute `editor.execute()`}
  * to the command.
  *
  * The `execute()` method will automatically abort when the command is disabled ({@link #isEnabled} is `false`).
  * This behavior is implemented by a high priority listener to the {@link #event:execute} event.
  *
  * In order to see how to disable a command from "outside" see the {@link #isEnabled} documentation.
  *
  * This method may return a value, which would be forwarded all the way down to the
  * {@link module:core/editor/editor~Editor#execute `editor.execute()`}.
  *
  * @fires execute
  */
  execute(...e) {
  }
  /**
  * Destroys the command.
  */
  destroy() {
    this.stopListening();
  }
}
function eb(n) {
  n.return = !1, n.stop();
}
class tb extends he {
  constructor() {
    super(...arguments);
    /**
    * Registered child commands definitions.
    */
    f(this, "_childCommandsDefinitions", []);
  }
  /**
  * @inheritDoc
  */
  refresh() {
  }
  /**
  * Executes the first enabled command which has the highest priority of all registered child commands.
  *
  * @returns The value returned by the {@link module:core/command~Command#execute `command.execute()`}.
  */
  execute(...e) {
    const i = this._getFirstEnabledCommand();
    return !!i && i.execute(e);
  }
  /**
  * Registers a child command.
  *
  * @param options An object with configuration options.
  * @param options.priority Priority of a command to register.
  */
  registerChildCommand(e, i = {}) {
    Kv(this._childCommandsDefinitions, {
      command: e,
      priority: i.priority || "normal"
    }), e.on("change:isEnabled", () => this._checkEnabled()), this._checkEnabled();
  }
  /**
  * Checks if any of child commands is enabled.
  */
  _checkEnabled() {
    this.isEnabled = !!this._getFirstEnabledCommand();
  }
  /**
  * Returns a first enabled command with the highest priority or `undefined` if none of them is enabled.
  */
  _getFirstEnabledCommand() {
    const e = this._childCommandsDefinitions.find(({ command: i }) => i.isEnabled);
    return e && e.command;
  }
}
class gy extends (/* @__PURE__ */ Je()) {
  /**
  * Creates an instance of the plugin collection class.
  * Allows loading and initializing plugins and their dependencies.
  * Allows providing a list of already loaded plugins. These plugins will not be destroyed along with this collection.
  *
  * @param availablePlugins Plugins (constructors) which the collection will be able to use
  * when {@link module:core/plugincollection~PluginCollection#init} is used with the plugin names (strings, instead of constructors).
  * Usually, the editor will pass its built-in plugins to the collection so they can later be
  * used in `config.plugins` or `config.removePlugins` by names.
  * @param contextPlugins A list of already initialized plugins represented by a `[ PluginConstructor, pluginInstance ]` pair.
  */
  constructor(e, i = [], s = []) {
    super();
    f(this, "_context");
    f(this, "_plugins", /* @__PURE__ */ new Map());
    /**
    * A map of plugin constructors that can be retrieved by their names.
    */
    f(this, "_availablePlugins");
    /**
    * Map of {@link module:core/contextplugin~ContextPlugin context plugins} which can be retrieved by their constructors or instances.
    */
    f(this, "_contextPlugins");
    this._context = e, this._availablePlugins = /* @__PURE__ */ new Map();
    for (const r of i)
      r.pluginName && this._availablePlugins.set(r.pluginName, r);
    this._contextPlugins = /* @__PURE__ */ new Map();
    for (const [r, o] of s)
      this._contextPlugins.set(r, o), this._contextPlugins.set(o, r), r.pluginName && this._availablePlugins.set(r.pluginName, r);
  }
  /**
  * Iterable interface.
  *
  * Returns `[ PluginConstructor, pluginInstance ]` pairs.
  */
  *[Symbol.iterator]() {
    for (const e of this._plugins)
      typeof e[0] == "function" && (yield e);
  }
  /**
  * Gets the plugin instance by its constructor or name.
  *
  * ```ts
  * // Check if 'Clipboard' plugin was loaded.
  * if ( editor.plugins.has( 'ClipboardPipeline' ) ) {
  * 	// Get clipboard plugin instance
  * 	const clipboard = editor.plugins.get( 'ClipboardPipeline' );
  *
  * 	this.listenTo( clipboard, 'inputTransformation', ( evt, data ) => {
  * 		// Do something on clipboard input.
  * 	} );
  * }
  * ```
  *
  * **Note**: This method will throw an error if a plugin is not loaded. Use `{@link #has editor.plugins.has()}`
  * to check if a plugin is available.
  *
  * @param key The plugin constructor or {@link module:core/plugin~PluginStaticMembers#pluginName name}.
  */
  get(e) {
    const i = this._plugins.get(e);
    if (!i) {
      let s = e;
      throw typeof e == "function" && (s = e.pluginName || e.name), new S("plugincollection-plugin-not-loaded", this._context, {
        plugin: s
      });
    }
    return i;
  }
  /**
  * Checks if a plugin is loaded.
  *
  * ```ts
  * // Check if the 'Clipboard' plugin was loaded.
  * if ( editor.plugins.has( 'ClipboardPipeline' ) ) {
  * 	// Now use the clipboard plugin instance:
  * 	const clipboard = editor.plugins.get( 'ClipboardPipeline' );
  *
  * 	// ...
  * }
  * ```
  *
  * @param key The plugin constructor or {@link module:core/plugin~PluginStaticMembers#pluginName name}.
  */
  has(e) {
    return this._plugins.has(e);
  }
  /**
  * Initializes a set of plugins and adds them to the collection.
  *
  * @param plugins An array of {@link module:core/plugin~PluginInterface plugin constructors}
  * or {@link module:core/plugin~PluginStaticMembers#pluginName plugin names}.
  * @param pluginsToRemove Names of the plugins or plugin constructors
  * that should not be loaded (despite being specified in the `plugins` array).
  * @param pluginsSubstitutions An array of {@link module:core/plugin~PluginInterface plugin constructors}
  * that will be used to replace plugins of the same names that were passed in `plugins` or that are in their dependency tree.
  * A useful option for replacing built-in plugins while creating tests (for mocking their APIs). Plugins that will be replaced
  * must follow these rules:
  *   * The new plugin must be a class.
  *   * The new plugin must be named.
  *   * Both plugins must not depend on other plugins.
  * @returns A promise which gets resolved once all plugins are loaded and available in the collection.
  */
  init(e, i = [], s = []) {
    const r = this, o = this._context;
    g(e), w(e);
    const a = e.filter((O) => !h(O, i)), l = [
      ...p(a)
    ];
    L(l, s);
    const c = E(l);
    return T(c, "init").then(() => T(c, "afterInit")).then(() => c);
    function u(O) {
      return typeof O == "function";
    }
    function d(O) {
      return u(O) && !!O.isContextPlugin;
    }
    function h(O, N) {
      return N.some((M) => M === O || m(O) === M || m(M) === O);
    }
    function m(O) {
      return u(O) ? O.pluginName || O.name : O;
    }
    function g(O, N = /* @__PURE__ */ new Set()) {
      O.forEach((M) => {
        u(M) && (N.has(M) || (N.add(M), M.pluginName && !r._availablePlugins.has(M.pluginName) && r._availablePlugins.set(M.pluginName, M), M.requires && g(M.requires, N)));
      });
    }
    function p(O, N = /* @__PURE__ */ new Set()) {
      return O.map((M) => u(M) ? M : r._availablePlugins.get(M)).reduce((M, J) => N.has(J) ? M : (N.add(J), J.requires && (w(J.requires, J), p(J.requires, N).forEach((D) => M.add(D))), M.add(J)), /* @__PURE__ */ new Set());
    }
    function w(O, N = null) {
      O.map((M) => u(M) ? M : r._availablePlugins.get(M) || M).forEach((M) => {
        y(M, N), k(M, N), C(M, N);
      });
    }
    function y(O, N) {
      if (!u(O))
        throw N ? new S("plugincollection-soft-required", o, {
          missingPlugin: O,
          requiredBy: m(N)
        }) : new S("plugincollection-plugin-not-found", o, {
          plugin: O
        });
    }
    function k(O, N) {
      if (d(N) && !d(O))
        throw new S("plugincollection-context-required", o, {
          plugin: m(O),
          requiredBy: m(N)
        });
    }
    function C(O, N) {
      if (N && h(O, i))
        throw new S("plugincollection-required", o, {
          plugin: m(O),
          requiredBy: m(N)
        });
    }
    function E(O) {
      return O.map((N) => {
        let M = r._contextPlugins.get(N);
        return M = M || new N(o), r._add(N, M), M;
      });
    }
    function T(O, N) {
      return O.reduce((M, J) => !J[N] || r._contextPlugins.has(J) ? M : M.then(J[N].bind(J)), Promise.resolve());
    }
    function L(O, N) {
      for (const M of N) {
        if (typeof M != "function")
          throw new S("plugincollection-replace-plugin-invalid-type", null, {
            pluginItem: M
          });
        const J = M.pluginName;
        if (!J)
          throw new S("plugincollection-replace-plugin-missing-name", null, {
            pluginItem: M
          });
        if (M.requires && M.requires.length)
          throw new S("plugincollection-plugin-for-replacing-cannot-have-dependencies", null, {
            pluginName: J
          });
        const D = r._availablePlugins.get(J);
        if (!D)
          throw new S("plugincollection-plugin-for-replacing-not-exist", null, {
            pluginName: J
          });
        const B = O.indexOf(D);
        if (B === -1) {
          if (r._contextPlugins.has(D))
            return;
          throw new S("plugincollection-plugin-for-replacing-not-loaded", null, {
            pluginName: J
          });
        }
        if (D.requires && D.requires.length)
          throw new S("plugincollection-replaced-plugin-cannot-have-dependencies", null, {
            pluginName: J
          });
        O.splice(B, 1, M), r._availablePlugins.set(J, M);
      }
    }
  }
  /**
  * Destroys all loaded plugins.
  */
  destroy() {
    const e = [];
    for (const [, i] of this)
      typeof i.destroy == "function" && !this._contextPlugins.has(i) && e.push(i.destroy());
    return Promise.all(e);
  }
  /**
  * Adds the plugin to the collection. Exposed mainly for testing purposes.
  *
  * @param PluginConstructor The plugin constructor.
  * @param plugin The instance of the plugin.
  */
  _add(e, i) {
    this._plugins.set(e, i);
    const s = e.pluginName;
    if (s) {
      if (this._plugins.has(s))
        throw new S("plugincollection-plugin-name-conflict", null, {
          pluginName: s,
          plugin1: this._plugins.get(s).constructor,
          plugin2: e
        });
      this._plugins.set(s, i);
    }
  }
}
class tc {
  /**
  * Creates a context instance with a given configuration.
  *
  * Usually not to be used directly. See the static {@link module:core/context~Context.create `create()`} method.
  *
  * @param config The context configuration.
  */
  constructor(t) {
    /**
    * Stores all the configurations specific to this context instance.
    */
    f(this, "config");
    /**
    * The plugins loaded and in use by this context instance.
    */
    f(this, "plugins");
    f(this, "locale");
    /**
    * Shorthand for {@link module:utils/locale~Locale#t}.
    */
    f(this, "t");
    /**
    * A list of editors that this context instance is injected to.
    */
    f(this, "editors");
    /**
    * Reference to the editor which created the context.
    * Null when the context was created outside of the editor.
    *
    * It is used to destroy the context when removing the editor that has created the context.
    */
    f(this, "_contextOwner", null);
    const { translations: e, ...i } = t || {};
    this.config = new n_(i, this.constructor.defaultConfig);
    const s = this.constructor.builtinPlugins;
    this.config.define("plugins", s), this.plugins = new gy(this, s);
    const r = this.config.get("language") || {};
    this.locale = new r6({
      uiLanguage: typeof r == "string" ? r : r.ui,
      contentLanguage: this.config.get("language.content"),
      translations: e
    }), this.t = this.locale.t, this.editors = new bt();
  }
  /**
  * Loads and initializes plugins specified in the configuration.
  *
  * @returns A promise which resolves once the initialization is completed, providing an array of loaded plugins.
  */
  initPlugins() {
    const t = this.config.get("plugins") || [], e = this.config.get("substitutePlugins") || [];
    for (const i of t.concat(e)) {
      if (typeof i != "function")
        throw new S("context-initplugins-constructor-only", null, {
          Plugin: i
        });
      if (i.isContextPlugin !== !0)
        throw new S("context-initplugins-invalid-plugin", null, {
          Plugin: i
        });
    }
    return this.plugins.init(t, [], e);
  }
  /**
  * Destroys the context instance and all editors used with the context,
  * releasing all resources used by the context.
  *
  * @returns A promise that resolves once the context instance is fully destroyed.
  */
  destroy() {
    return Promise.all(Array.from(this.editors, (t) => t.destroy())).then(() => this.plugins.destroy());
  }
  /**
  * Adds a reference to the editor which is used with this context.
  *
  * When the given editor has created the context, the reference to this editor will be stored
  * as a {@link ~Context#_contextOwner}.
  *
  * This method should only be used by the editor.
  *
  * @internal
  * @param isContextOwner Stores the given editor as a context owner.
  */
  _addEditor(t, e) {
    if (this._contextOwner)
      throw new S("context-addeditor-private-context");
    this.editors.add(t), e && (this._contextOwner = t);
  }
  /**
  * Removes a reference to the editor which was used with this context.
  * When the context was created by the given editor, the context will be destroyed.
  *
  * This method should only be used by the editor.
  *
  * @internal
  * @return A promise that resolves once the editor is removed from the context or when the context was destroyed.
  */
  _removeEditor(t) {
    return this.editors.has(t) && this.editors.remove(t), this._contextOwner === t ? this.destroy() : Promise.resolve();
  }
  /**
  * Returns the context configuration which will be copied to the editors created using this context.
  *
  * The configuration returned by this method has the plugins configuration removed &ndash; plugins are shared with all editors
  * through another mechanism.
  *
  * This method should only be used by the editor.
  *
  * @internal
  * @returns Configuration as a plain object.
  */
  _getEditorConfig() {
    const t = {};
    for (const e of this.config.names())
      [
        "plugins",
        "removePlugins",
        "extraPlugins"
      ].includes(e) || (t[e] = this.config.get(e));
    return t;
  }
  /**
  * Creates and initializes a new context instance.
  *
  * ```ts
  * const commonConfig = { ... }; // Configuration for all the plugins and editors.
  * const editorPlugins = [ ... ]; // Regular plugins here.
  *
  * Context
  * 	.create( {
  * 		// Only context plugins here.
  * 		plugins: [ ... ],
  *
  * 		// Configure the language for all the editors (it cannot be overwritten).
  * 		language: { ... },
  *
  * 		// Configuration for context plugins.
  * 		comments: { ... },
  * 		...
  *
  * 		// Default configuration for editor plugins.
  * 		toolbar: { ... },
  * 		image: { ... },
  * 		...
  * 	} )
  * 	.then( context => {
  * 		const promises = [];
  *
  * 		promises.push( ClassicEditor.create(
  * 			document.getElementById( 'editor1' ),
  * 			{
  * 				editorPlugins,
  * 				context
  * 			}
  * 		) );
  *
  * 		promises.push( ClassicEditor.create(
  * 			document.getElementById( 'editor2' ),
  * 			{
  * 				editorPlugins,
  * 				context,
  * 				toolbar: { ... } // You can overwrite the configuration of the context.
  * 			}
  * 		) );
  *
  * 		return Promise.all( promises );
  * 	} );
  * ```
  *
  * @param config The context configuration.
  * @returns A promise resolved once the context is ready. The promise resolves with the created context instance.
  */
  static create(t) {
    return new Promise((e) => {
      const i = new this(t);
      e(i.initPlugins().then(() => i));
    });
  }
}
/**
* The default configuration which is built into the `Context` class.
*
* It is used in CKEditor 5 builds featuring `Context` to provide the default configuration options which are later used during the
* context initialization.
*
* ```ts
* Context.defaultConfig = {
* 	foo: 1,
* 	bar: 2
* };
*
* Context
* 	.create()
* 	.then( context => {
* 		context.config.get( 'foo' ); // -> 1
* 		context.config.get( 'bar' ); // -> 2
* 	} );
*
* // The default options can be overridden by the configuration passed to create().
* Context
* 	.create( { bar: 3 } )
* 	.then( context => {
* 		context.config.get( 'foo' ); // -> 1
* 		context.config.get( 'bar' ); // -> 3
* 	} );
* ```
*
* See also {@link module:core/context~Context.builtinPlugins `Context.builtinPlugins`}
* and {@link module:core/editor/editor~Editor.defaultConfig `Editor.defaultConfig`}.
*/
f(tc, "defaultConfig"), /**
* An array of plugins built into the `Context` class.
*
* It is used in CKEditor 5 builds featuring `Context` to provide a list of context plugins which are later automatically initialized
* during the context initialization.
*
* They will be automatically initialized by `Context` unless `config.plugins` is passed.
*
* ```ts
* // Build some context plugins into the Context class first.
* Context.builtinPlugins = [ FooPlugin, BarPlugin ];
*
* // Normally, you need to define config.plugins, but since Context.builtinPlugins was
* // defined, now you can call create() without any configuration.
* Context
* 	.create()
* 	.then( context => {
* 		context.plugins.get( FooPlugin ); // -> An instance of the Foo plugin.
* 		context.plugins.get( BarPlugin ); // -> An instance of the Bar plugin.
* 	} );
* ```
*
* See also {@link module:core/context~Context.defaultConfig `Context.defaultConfig`}
* and {@link module:core/editor/editor~Editor.builtinPlugins `Editor.builtinPlugins`}.
*/
f(tc, "builtinPlugins");
class py extends (/* @__PURE__ */ je()) {
  /**
  * Creates a new plugin instance.
  */
  constructor(e) {
    super();
    /**
    * The context or editor instance.
    */
    f(this, "context");
    this.context = e;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this.stopListening();
  }
  /**
  * @inheritDoc
  */
  static get isContextPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !1;
  }
  /**
  * @inheritDoc
  */
  static get isPremiumPlugin() {
    return !1;
  }
}
class C8 {
  /**
  * Creates collection instance.
  */
  constructor() {
    /**
    * Command map.
    */
    f(this, "_commands");
    this._commands = /* @__PURE__ */ new Map();
  }
  /**
  * Registers a new command.
  *
  * @param commandName The name of the command.
  */
  add(t, e) {
    this._commands.set(t, e);
  }
  /**
  * Retrieves a command from the collection.
  *
  * @param commandName The name of the command.
  */
  get(t) {
    return this._commands.get(t);
  }
  /**
  * Executes a command.
  *
  * @param commandName The name of the command.
  * @param commandParams Command parameters.
  * @returns The value returned by the {@link module:core/command~Command#execute `command.execute()`}.
  */
  execute(t, ...e) {
    const i = this.get(t);
    if (!i)
      throw new S("commandcollection-command-not-found", this, {
        commandName: t
      });
    return i.execute(...e);
  }
  /**
  * Returns iterator of command names.
  */
  *names() {
    yield* this._commands.keys();
  }
  /**
  * Returns iterator of command instances.
  */
  *commands() {
    yield* this._commands.values();
  }
  /**
  * Iterable interface.
  *
  * Returns `[ commandName, commandInstance ]` pairs.
  */
  [Symbol.iterator]() {
    return this._commands[Symbol.iterator]();
  }
  /**
  * Destroys all collection commands.
  */
  destroy() {
    for (const t of this.commands())
      t.destroy();
  }
}
class x8 extends Ze {
  /**
  * Creates an instance of the keystroke handler.
  */
  constructor(e) {
    super();
    /**
    * The editor instance.
    */
    f(this, "editor");
    this.editor = e;
  }
  /**
  * Registers a handler for the specified keystroke.
  *
  * The handler can be specified as a command name or a callback.
  *
  * @param keystroke Keystroke defined in a format accepted by
  * the {@link module:utils/keyboard~parseKeystroke} function.
  * @param callback If a string is passed, then the keystroke will
  * {@link module:core/editor/editor~Editor#execute execute a command}.
  * If a function, then it will be called with the
  * {@link module:engine/view/observer/keyobserver~KeyEventData key event data} object and
  * a `cancel()` helper to both `preventDefault()` and `stopPropagation()` of the event.
  * @param options Additional options.
  * @param options.priority The priority of the keystroke callback. The higher the priority value
  * the sooner the callback will be executed. Keystrokes having the same priority
  * are called in the order they were added.
  */
  set(e, i, s = {}) {
    if (typeof i == "string") {
      const r = i;
      i = (o, a) => {
        this.editor.execute(r), a();
      };
    }
    super.set(e, i, s);
  }
}
const qu = "contentEditing", ib = "common";
class T8 {
  /**
  * @inheritDoc
  */
  constructor(t) {
    /**
    * Stores information about keystrokes brought by editor features for the users to interact with the editor, mainly
    * keystroke combinations and their accessible labels.
    *
    * This information is particularly useful for screen reader and other assistive technology users. It gets displayed
    * by the {@link module:ui/editorui/accessibilityhelp/accessibilityhelp~AccessibilityHelp Accessibility help} dialog.
    *
    * Keystrokes are organized in categories and groups. They can be added using ({@link #addKeystrokeInfoCategory},
    * {@link #addKeystrokeInfoGroup}, and {@link #addKeystrokeInfos}) methods.
    *
    * Please note that:
    * * two categories are always available:
    *   * `'contentEditing'` for keystrokes related to content creation,
    *   * `'navigation'` for keystrokes related to navigation in the UI and the content.
    * * unless specified otherwise, new keystrokes are added into the `'contentEditing'` category and the `'common'`
    * keystroke group within that category while using the {@link #addKeystrokeInfos} method.
    */
    f(this, "keystrokeInfos", /* @__PURE__ */ new Map());
    /**
    * The editor instance.
    */
    f(this, "_editor");
    this._editor = t;
    const e = t.config.get("menuBar.isVisible"), i = t.locale.t;
    this.addKeystrokeInfoCategory({
      id: qu,
      label: i("Content editing keystrokes"),
      description: i("These keyboard shortcuts allow for quick access to content editing features.")
    });
    const s = [
      {
        label: i("Close contextual balloons, dropdowns, and dialogs"),
        keystroke: "Esc"
      },
      {
        label: i("Open the accessibility help dialog"),
        keystroke: "Alt+0"
      },
      {
        label: i("Move focus between form fields (inputs, buttons, etc.)"),
        keystroke: [
          [
            "Tab"
          ],
          [
            "Shift+Tab"
          ]
        ]
      },
      {
        label: i("Move focus to the toolbar, navigate between toolbars"),
        keystroke: "Alt+F10",
        mayRequireFn: !0
      },
      {
        label: i("Navigate through the toolbar or menu bar"),
        keystroke: [
          [
            "arrowup"
          ],
          [
            "arrowright"
          ],
          [
            "arrowdown"
          ],
          [
            "arrowleft"
          ]
        ]
      },
      {
        // eslint-disable-next-line max-len
        label: i("Execute the currently focused button. Executing buttons that interact with the editor content moves the focus back to the content."),
        keystroke: [
          [
            "Enter"
          ],
          [
            "Space"
          ]
        ]
      }
    ];
    e && s.push({
      label: i("Move focus to the menu bar, navigate between menu bars"),
      keystroke: "Alt+F9",
      mayRequireFn: !0
    }), this.addKeystrokeInfoCategory({
      id: "navigation",
      label: i("User interface and content navigation keystrokes"),
      description: i("Use the following keystrokes for more efficient navigation in the CKEditor 5 user interface."),
      groups: [
        {
          id: "common",
          keystrokes: s
        }
      ]
    });
  }
  /**
  * Adds a top-level category in the {@link #keystrokeInfos keystroke information database} with a label and optional description.
  *
  * Categories organize keystrokes and help users to find the right keystroke. Each category can have multiple groups
  * of keystrokes that narrow down the context in which the keystrokes are available. Every keystroke category comes
  * with a `'common'` group by default.
  *
  * By default, two categories are available:
  * * `'contentEditing'` for keystrokes related to content creation,
  * * `'navigation'` for keystrokes related to navigation in the UI and the content.
  *
  * To create a new keystroke category with new groups, use the following code:
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfoCategory( {
  * 			id: 'myCategory',
  * 			label: t( 'My category' ),
  * 			description: t( 'My category description.' ),
  * 			groups: [
  * 				{
  * 					id: 'myGroup',
  * 					label: t( 'My keystroke group' ),
  * 					keystrokes: [
  * 						{
  * 							label: t( 'Keystroke label 1' ),
  * 							keystroke: 'Ctrl+Shift+N'
  * 						},
  * 						{
  * 							label: t( 'Keystroke label 2' ),
  * 							keystroke: 'Ctrl+Shift+M'
  * 						}
  * 					]
  * 				}
  * 			]
  * 		};
  * 	}
  * }
  * ```
  *
  * See {@link #keystrokeInfos}, {@link #addKeystrokeInfoGroup}, and {@link #addKeystrokeInfos}.
  */
  addKeystrokeInfoCategory({ id: t, label: e, description: i, groups: s }) {
    this.keystrokeInfos.set(t, {
      id: t,
      label: e,
      description: i,
      groups: /* @__PURE__ */ new Map()
    }), this.addKeystrokeInfoGroup({
      categoryId: t,
      id: ib
    }), s && s.forEach((r) => {
      this.addKeystrokeInfoGroup({
        categoryId: t,
        ...r
      });
    });
  }
  /**
  * Adds a group of keystrokes in a specific category to the {@link #keystrokeInfos keystroke information database}.
  *
  * Groups narrow down the context in which the keystrokes are available. When `categoryId` is not specified,
  * the group goes to the `'contentEditing'` category (default).
  *
  * To create a new group within an existing category, use the following code:
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfoGroup( {
  * 			id: 'myGroup',
  * 			categoryId: 'navigation',
  * 			label: t( 'My keystroke group' ),
  * 			keystrokes: [
  * 				{
  * 					label: t( 'Keystroke label 1' ),
  * 					keystroke: 'Ctrl+Shift+N'
  * 				},
  * 				{
  * 					label: t( 'Keystroke label 2' ),
  * 					keystroke: 'Ctrl+Shift+M'
  * 				}
  * 			]
  * 		} );
  * 	}
  * }
  * ```
  *
  * See {@link #keystrokeInfos}, {@link #addKeystrokeInfoCategory}, and {@link #addKeystrokeInfos}.
  */
  addKeystrokeInfoGroup({ categoryId: t = qu, id: e, label: i, keystrokes: s }) {
    const r = this.keystrokeInfos.get(t);
    if (!r)
      throw new S("accessibility-unknown-keystroke-info-category", this._editor, {
        groupId: e,
        categoryId: t
      });
    r.groups.set(e, {
      id: e,
      label: i,
      keystrokes: s || []
    });
  }
  /**
  * Adds information about keystrokes to the {@link #keystrokeInfos keystroke information database}.
  *
  * Keystrokes without specified `groupId` or `categoryId` go to the `'common'` group in the `'contentEditing'` category (default).
  *
  * To add a keystroke brought by your plugin (using default group and category), use the following code:
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfos( {
  * 			keystrokes: [
  * 				{
  * 					label: t( 'Keystroke label' ),
  * 					keystroke: 'CTRL+B'
  * 				}
  * 			]
  * 		} );
  * 	}
  * }
  * ```
  * To add a keystroke in a specific existing `'widget'` group in the default `'contentEditing'` category:
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfos( {
  * 			// Add a keystroke to the existing "widget" group.
  * 			groupId: 'widget',
  * 			keystrokes: [
  * 				{
  * 					label: t( 'A an action on a selected widget' ),
  * 					keystroke: 'Ctrl+D',
  * 				}
  * 			]
  * 		} );
  * 	}
  * }
  * ```
  *
  * To add a keystroke to another existing category (using default group):
  *
  * ```js
  * class MyPlugin extends Plugin {
  * 	// ...
  * 	init() {
  * 		const editor = this.editor;
  * 		const t = editor.t;
  *
  * 		// ...
  *
  * 		editor.accessibility.addKeystrokeInfos( {
  * 			// Add keystrokes to the "navigation" category (one of defaults).
  * 			categoryId: 'navigation',
  * 			keystrokes: [
  * 				{
  * 					label: t( 'Keystroke label' ),
  * 					keystroke: 'CTRL+B'
  * 				}
  * 			]
  * 		} );
  * 	}
  * }
  * ```
  *
  * See {@link #keystrokeInfos}, {@link #addKeystrokeInfoGroup}, and {@link #addKeystrokeInfoCategory}.
  */
  addKeystrokeInfos({ categoryId: t = qu, groupId: e = ib, keystrokes: i }) {
    if (!this.keystrokeInfos.has(t))
      throw new S("accessibility-unknown-keystroke-info-category", this._editor, {
        categoryId: t,
        keystrokes: i
      });
    const s = this.keystrokeInfos.get(t);
    if (!s.groups.has(e))
      throw new S("accessibility-unknown-keystroke-info-group", this._editor, {
        groupId: e,
        categoryId: t,
        keystrokes: i
      });
    s.groups.get(e).keystrokes.push(...i);
  }
}
function E8(n) {
  return {
    sessionId: R8(),
    pageSessionId: O8(),
    hostname: window.location.hostname,
    version: globalThis.CKEDITOR_VERSION,
    type: S8(n),
    plugins: A8(n.plugins),
    distribution: I8(),
    env: P8(),
    integration: /* @__PURE__ */ Object.create(null),
    menuBar: {
      isVisible: !!n.config.get("menuBar.isVisible")
    },
    language: {
      ui: n.locale.uiLanguage,
      content: n.locale.contentLanguage
    },
    toolbar: {
      main: Gu(n.config.get("toolbar")),
      block: Gu(n.config.get("blockToolbar")),
      balloon: Gu(n.config.get("balloonToolbar"))
    }
  };
}
function S8(n) {
  return Object.getPrototypeOf(n).constructor.editorName;
}
function A8(n) {
  return Array.from(n).filter(([t]) => !!t.pluginName).map(([t]) => {
    const { pluginName: e, isContextPlugin: i, isOfficialPlugin: s, isPremiumPlugin: r } = t;
    return {
      isContext: !!i,
      isOfficial: !!s,
      isPremium: !!r,
      name: e
    };
  });
}
function Gu(n) {
  if (!n)
    return;
  const t = Array.isArray(n) ? {
    items: n
  } : n, e = r(t.items || []);
  return {
    isMultiline: e.includes("-"),
    shouldNotGroupWhenFull: !!t.shouldNotGroupWhenFull,
    items: s(e)
  };
  function s(o) {
    return o.filter((a) => a !== "|" && a !== "-");
  }
  function r(o) {
    return o.flatMap((a) => typeof a == "string" ? [
      a
    ] : r(a.items));
  }
}
function I8() {
  return {
    channel: window[Symbol.for("cke distribution")] || "sh"
  };
}
function P8() {
  let n = "unknown", t = "unknown";
  return be.isMac ? n = "mac" : be.isWindows ? n = "windows" : be.isiOS ? n = "ios" : be.isAndroid && (n = "android"), be.isGecko ? t = "gecko" : be.isBlink ? t = "blink" : be.isSafari && (t = "safari"), {
    os: n,
    browser: t
  };
}
function R8() {
  return localStorage.getItem("__ckeditor-session-id") || localStorage.setItem("__ckeditor-session-id", Bt()), localStorage.getItem("__ckeditor-session-id");
}
function O8() {
  return ae.window.CKEDITOR_PAGE_SESSION_ID = ae.window.CKEDITOR_PAGE_SESSION_ID || Bt(), ae.window.CKEDITOR_PAGE_SESSION_ID;
}
class Tr extends (/* @__PURE__ */ je()) {
  /**
  * Creates a new instance of the editor class.
  *
  * Usually, not to be used directly. See the static {@link module:core/editor/editor~Editor.create `create()`} method.
  *
  * @param config The editor configuration.
  */
  constructor(e = {}) {
    super();
    /**
    * A namespace for the accessibility features of the editor.
    */
    f(this, "accessibility");
    /**
    * Commands registered to the editor.
    *
    * Use the shorthand {@link #execute `editor.execute()`} method to execute commands:
    *
    * ```ts
    * // Execute the bold command:
    * editor.execute( 'bold' );
    *
    * // Check the state of the bold command:
    * editor.commands.get( 'bold' ).value;
    * ```
    */
    f(this, "commands");
    /**
    * Stores all configurations specific to this editor instance.
    *
    * ```ts
    * editor.config.get( 'image.toolbar' );
    * // -> [ 'imageStyle:block', 'imageStyle:side', '|', 'toggleImageCaption', 'imageTextAlternative' ]
    * ```
    */
    f(this, "config");
    /**
    * Conversion manager through which you can register model-to-view and view-to-model converters.
    *
    * See the {@link module:engine/conversion/conversion~Conversion} documentation to learn how to add converters.
    */
    f(this, "conversion");
    /**
    * The {@link module:engine/controller/datacontroller~DataController data controller}.
    * Used e.g. for setting and retrieving the editor data.
    */
    f(this, "data");
    /**
    * The {@link module:engine/controller/editingcontroller~EditingController editing controller}.
    * Controls user input and rendering the content for editing.
    */
    f(this, "editing");
    /**
    * The locale instance.
    */
    f(this, "locale");
    /**
    * The editor's model.
    *
    * The central point of the editor's abstract data model.
    */
    f(this, "model");
    /**
    * The plugins loaded and in use by this editor instance.
    *
    * ```ts
    * editor.plugins.get( 'ClipboardPipeline' ); // -> An instance of the clipboard pipeline plugin.
    * ```
    */
    f(this, "plugins");
    /**
    * An instance of the {@link module:core/editingkeystrokehandler~EditingKeystrokeHandler}.
    *
    * It allows setting simple keystrokes:
    *
    * ```ts
    * // Execute the bold command on Ctrl+E:
    * editor.keystrokes.set( 'Ctrl+E', 'bold' );
    *
    * // Execute your own callback:
    * editor.keystrokes.set( 'Ctrl+E', ( data, cancel ) => {
    * 	console.log( data.keyCode );
    *
    * 	// Prevent the default (native) action and stop the underlying keydown event
    * 	// so no other editor feature will interfere.
    * 	cancel();
    * } );
    * ```
    *
    * Note: Certain typing-oriented keystrokes (like <kbd>Backspace</kbd> or <kbd>Enter</kbd>) are handled
    * by a low-level mechanism and trying to listen to them via the keystroke handler will not work reliably.
    * To handle these specific keystrokes, see the events fired by the
    * {@link module:engine/view/document~Document editing view document} (`editor.editing.view.document`).
    */
    f(this, "keystrokes");
    /**
    * Shorthand for {@link module:utils/locale~Locale#t}.
    *
    * @see module:utils/locale~Locale#t
    */
    f(this, "t");
    /**
    * The editor context.
    * When it is not provided through the configuration, the editor creates it.
    */
    f(this, "_context");
    /**
    * A set of lock IDs for the {@link #isReadOnly} getter.
    */
    f(this, "_readOnlyLocks");
    if ("sanitizeHtml" in e)
      throw new S("editor-config-sanitizehtml-not-supported");
    const i = this.constructor, { translations: s, ...r } = i.defaultConfig || {}, { translations: o = s, ...a } = e, l = e.language || r.language;
    this._context = e.context || new tc({
      language: l,
      translations: o
    }), this._context._addEditor(this, !e.context);
    const c = Array.from(i.builtinPlugins || []);
    this.config = new n_(a, r), this.config.define("plugins", c), this.config.define(this._context._getEditorConfig()), d(this.config), this.plugins = new gy(this, c, this._context.plugins), this.locale = this._context.locale, this.t = this.locale.t, this._readOnlyLocks = /* @__PURE__ */ new Set(), this.commands = new C8(), this.set("state", "initializing"), this.once("ready", () => this.state = "ready", {
      priority: "high"
    }), this.once("destroy", () => this.state = "destroyed", {
      priority: "high"
    }), this.model = new HO(), this.on("change:isReadOnly", () => {
      this.model.document.isReadOnly = this.isReadOnly;
    });
    const u = new S6();
    this.data = new XR(this.model, u), this.editing = new IR(this.model, u), this.editing.view.document.bind("isReadOnly").to(this), this.conversion = new eO([
      this.editing.downcastDispatcher,
      this.data.downcastDispatcher
    ], this.data.upcastDispatcher), this.conversion.addAlias("dataDowncast", this.data.downcastDispatcher), this.conversion.addAlias("editingDowncast", this.editing.downcastDispatcher), this.keystrokes = new x8(this), this.keystrokes.listenTo(this.editing.view.document), this.accessibility = new T8(this), h(this);
    function d(m) {
      let g = m.get("licenseKey");
      if (!g && window.CKEDITOR_GLOBAL_LICENSE_KEY && (g = window.CKEDITOR_GLOBAL_LICENSE_KEY, m.set("licenseKey", g)), !g)
        throw new S("license-key-missing");
    }
    function h(m) {
      const g = m.config.get("licenseKey"), p = window[Symbol.for("cke distribution")] || "sh";
      function w(M) {
        m.enableReadOnlyMode(Symbol("invalidLicense")), m._showLicenseError(M);
      }
      function y(M) {
        const J = M.split(".");
        return J.length != 3 ? null : J[1];
      }
      function k(M) {
        return [
          "exp",
          "jti",
          "vc"
        ].every((D) => D in M);
      }
      function C(M) {
        return Object.getOwnPropertyNames(M).sort().filter((B) => B != "vc" && M[B] != null).map((B) => M[B]);
      }
      function E(M) {
        const { hostname: J } = new URL(window.location.href);
        if (M.includes(J))
          return !0;
        const D = J.split(".");
        return M.filter((B) => B.includes("*")).map((B) => B.split(".")).filter((B) => B.length <= D.length).map((B) => Array(D.length - B.length).fill(B[0] === "*" ? "*" : "").concat(B)).some((B) => D.every(($, W) => B[W] === $ || B[W] === "*"));
      }
      if (g == "GPL") {
        p == "cloud" && w("distributionChannel");
        return;
      }
      const T = y(g);
      if (!T) {
        w("invalid");
        return;
      }
      const L = yf(T);
      if (!L) {
        w("invalid");
        return;
      }
      if (!k(L)) {
        w("invalid");
        return;
      }
      if (L.distributionChannel && !Ue(L.distributionChannel).includes(p)) {
        w("distributionChannel");
        return;
      }
      if (u6(C(L)) != L.vc.toLowerCase()) {
        w("invalid");
        return;
      }
      if (new Date(L.exp * 1e3) < y4) {
        w("expired");
        return;
      }
      const N = L.licensedHosts;
      if (N && N.length > 0 && !E(N)) {
        w("domainLimit");
        return;
      }
      if ([
        "evaluation",
        "trial"
      ].includes(L.licenseType) && L.exp * 1e3 < Date.now()) {
        w("expired");
        return;
      }
      if ([
        "development",
        "evaluation",
        "trial"
      ].includes(L.licenseType)) {
        const { licenseType: M } = L, J = M[0].toUpperCase() + M.slice(1), D = M === "evaluation" ? "an" : "a";
        console.info(`%cCKEditor 5 ${J} License`, "color: #ffffff; background: #743CCD; font-size: 14px; padding: 4px 8px; border-radius: 4px;"), console.warn(`⚠️ You are using ${D} ${M} license of CKEditor 5${M === "trial" ? " which is for evaluation purposes only" : ""}. For production usage, please obtain a production license at https://portal.ckeditor.com/`);
      }
      if ([
        "evaluation",
        "trial"
      ].includes(L.licenseType)) {
        const M = L.licenseType, J = setTimeout(() => {
          w(`${M}Limit`);
        }, 6e5);
        m.on("destroy", () => {
          clearTimeout(J);
        });
      }
      L.usageEndpoint && m.once("ready", () => {
        const M = {
          requestId: Bt(),
          requestTime: Math.round(Date.now() / 1e3),
          license: g,
          editor: V8(m)
        };
        m._sendUsageRequest(L.usageEndpoint, M).then((J) => {
          const { status: D, message: B } = J;
          B && console.warn(B), D != "ok" && w("usageLimit");
        }, () => {
          w4("license-key-validation-endpoint-not-reachable", {
            url: L.usageEndpoint
          });
        });
      }, {
        priority: "high"
      });
    }
  }
  /**
  * A required name of the editor class. The name should reflect the constructor name.
  */
  static get editorName() {
    return "Editor";
  }
  /**
  * Defines whether the editor is in the read-only mode.
  *
  * In read-only mode the editor {@link #commands commands} are disabled so it is not possible
  * to modify the document by using them. Also, the editable element(s) become non-editable.
  *
  * In order to make the editor read-only, you need to call the {@link #enableReadOnlyMode} method:
  *
  * ```ts
  * editor.enableReadOnlyMode( 'feature-id' );
  * ```
  *
     * Later, to turn off the read-only mode, call {@link #disableReadOnlyMode}:
  *
  * ```ts
  * editor.disableReadOnlyMode( 'feature-id' );
  * ```
  *
  * @readonly
  * @observable
  */
  get isReadOnly() {
    return this._readOnlyLocks.size > 0;
  }
  set isReadOnly(e) {
    throw new S("editor-isreadonly-has-no-setter");
  }
  /**
  * Turns on the read-only mode in the editor.
  *
  * Editor can be switched to or out of the read-only mode by many features, under various circumstances. The editor supports locking
  * mechanism for the read-only mode. It enables easy control over the read-only mode when many features wants to turn it on or off at
  * the same time, without conflicting with each other. It guarantees that you will not make the editor editable accidentally (which
  * could lead to errors).
  *
  * Each read-only mode request is identified by a unique id (also called "lock"). If multiple plugins requested to turn on the
  * read-only mode, then, the editor will become editable only after all these plugins turn the read-only mode off (using the same ids).
  *
  * Note, that you cannot force the editor to disable the read-only mode if other plugins set it.
  *
  * After the first `enableReadOnlyMode()` call, the {@link #isReadOnly `isReadOnly` property} will be set to `true`:
  *
  * ```ts
  * editor.isReadOnly; // `false`.
  * editor.enableReadOnlyMode( 'my-feature-id' );
  * editor.isReadOnly; // `true`.
  * ```
  *
  * You can turn off the read-only mode ("clear the lock") using the {@link #disableReadOnlyMode `disableReadOnlyMode()`} method:
  *
  * ```ts
  * editor.enableReadOnlyMode( 'my-feature-id' );
  * // ...
  * editor.disableReadOnlyMode( 'my-feature-id' );
  * editor.isReadOnly; // `false`.
  * ```
  *
  * All "locks" need to be removed to enable editing:
  *
  * ```ts
  * editor.enableReadOnlyMode( 'my-feature-id' );
  * editor.enableReadOnlyMode( 'my-other-feature-id' );
  * // ...
  * editor.disableReadOnlyMode( 'my-feature-id' );
  * editor.isReadOnly; // `true`.
  * editor.disableReadOnlyMode( 'my-other-feature-id' );
  * editor.isReadOnly; // `false`.
  * ```
  *
  * @param lockId A unique ID for setting the editor to the read-only state.
  */
  enableReadOnlyMode(e) {
    if (typeof e != "string" && typeof e != "symbol")
      throw new S("editor-read-only-lock-id-invalid", null, {
        lockId: e
      });
    this._readOnlyLocks.has(e) || (this._readOnlyLocks.add(e), this._readOnlyLocks.size === 1 && this.fire("change:isReadOnly", "isReadOnly", !0, !1));
  }
  /**
  * Removes the read-only lock from the editor with given lock ID.
  *
  * When no lock is present on the editor anymore, then the {@link #isReadOnly `isReadOnly` property} will be set to `false`.
  *
  * @param lockId The lock ID for setting the editor to the read-only state.
  */
  disableReadOnlyMode(e) {
    if (typeof e != "string" && typeof e != "symbol")
      throw new S("editor-read-only-lock-id-invalid", null, {
        lockId: e
      });
    this._readOnlyLocks.has(e) && (this._readOnlyLocks.delete(e), this._readOnlyLocks.size === 0 && this.fire("change:isReadOnly", "isReadOnly", !1, !0));
  }
  /**
  * Sets the data in the editor.
  *
  * ```ts
  * editor.setData( '<p>This is editor!</p>' );
  * ```
  *
  * If your editor implementation uses multiple roots, you should pass an object with keys corresponding
  * to the editor root names and values equal to the data that should be set in each root:
  *
  * ```ts
  * editor.setData( {
  *     header: '<p>Content for header part.</p>',
  *     content: '<p>Content for main part.</p>',
  *     footer: '<p>Content for footer part.</p>'
  * } );
  * ```
  *
  * By default the editor accepts HTML. This can be controlled by injecting a different data processor.
  * See the {@glink features/markdown Markdown output} guide for more details.
  *
  * @param data Input data.
  */
  setData(e) {
    this.data.set(e);
  }
  /**
  * Gets the data from the editor.
  *
  * ```ts
  * editor.getData(); // -> '<p>This is editor!</p>'
  * ```
  *
  * If your editor implementation uses multiple roots, you should pass root name as one of the options:
  *
  * ```ts
  * editor.getData( { rootName: 'header' } ); // -> '<p>Content for header part.</p>'
  * ```
  *
  * By default, the editor outputs HTML. This can be controlled by injecting a different data processor.
  * See the {@glink features/markdown Markdown output} guide for more details.
  *
  * A warning is logged when you try to retrieve data for a detached root, as most probably this is a mistake. A detached root should
  * be treated like it is removed, and you should not save its data. Note, that the detached root data is always an empty string.
  *
  * @param options Additional configuration for the retrieved data.
  * Editor features may introduce more configuration options that can be set through this parameter.
  * @param options.rootName Root name. Defaults to `'main'`.
  * @param options.trim Whether returned data should be trimmed. This option is set to `'empty'` by default,
  * which means that whenever editor content is considered empty, an empty string is returned. To turn off trimming
  * use `'none'`. In such cases exact content will be returned (for example `'<p>&nbsp;</p>'` for an empty editor).
  * @returns Output data.
  */
  getData(e) {
    return this.data.get(e);
  }
  /**
  * Loads and initializes plugins specified in the configuration.
  *
  * @returns A promise which resolves once the initialization is completed, providing an array of loaded plugins.
  */
  initPlugins() {
    const e = this.config, i = e.get("plugins"), s = e.get("removePlugins") || [], r = e.get("extraPlugins") || [], o = e.get("substitutePlugins") || [];
    return this.plugins.init(i.concat(r), s, o);
  }
  /**
  * Destroys the editor instance, releasing all resources used by it.
  *
  * **Note** The editor cannot be destroyed during the initialization phase so if it is called
  * while the editor {@link #state is being initialized}, it will wait for the editor initialization before destroying it.
  *
  * @fires destroy
  * @returns A promise that resolves once the editor instance is fully destroyed.
  */
  destroy() {
    let e = Promise.resolve();
    return this.state == "initializing" && (e = new Promise((i) => this.once("ready", i))), e.then(() => {
      this.fire("destroy"), this.stopListening(), this.commands.destroy();
    }).then(() => this.plugins.destroy()).then(() => {
      this.model.destroy(), this.data.destroy(), this.editing.destroy(), this.keystrokes.destroy();
    }).then(() => this._context._removeEditor(this));
  }
  /**
  * Executes the specified command with given parameters.
  *
  * Shorthand for:
  *
  * ```ts
  * editor.commands.get( commandName ).execute( ... );
  * ```
  *
  * @param commandName The name of the command to execute.
  * @param commandParams Command parameters.
  * @returns The value returned by the {@link module:core/commandcollection~CommandCollection#execute `commands.execute()`}.
  */
  execute(e, ...i) {
    try {
      return this.commands.execute(e, ...i);
    } catch (s) {
      /* istanbul ignore next -- @preserve */
      S.rethrowUnexpectedError(s, this);
    }
  }
  /**
  * Focuses the editor.
  *
  * **Note** To explicitly focus the editing area of the editor, use the
  * {@link module:engine/view/view~View#focus `editor.editing.view.focus()`} method of the editing view.
  *
  * Check out the {@glink framework/deep-dive/ui/focus-tracking#focus-in-the-editor-ui Focus in the editor UI} section
  * of the {@glink framework/deep-dive/ui/focus-tracking Deep dive into focus tracking} guide to learn more.
  */
  focus() {
    this.editing.view.focus();
  }
  /* istanbul ignore next -- @preserve */
  /**
  * Creates and initializes a new editor instance.
  *
  * This is an abstract method. Every editor type needs to implement its own initialization logic.
  *
  * See the `create()` methods of the existing editor types to learn how to use them:
  *
  * * {@link module:editor-classic/classiceditor~ClassicEditor.create `ClassicEditor.create()`}
  * * {@link module:editor-balloon/ballooneditor~BalloonEditor.create `BalloonEditor.create()`}
  * * {@link module:editor-decoupled/decouplededitor~DecoupledEditor.create `DecoupledEditor.create()`}
  * * {@link module:editor-inline/inlineeditor~InlineEditor.create `InlineEditor.create()`}
  */
  static create(...e) {
    throw new Error("This is an abstract method.");
  }
  _showLicenseError(e, i) {
    setTimeout(() => {
      if (e == "invalid")
        throw new S("invalid-license-key");
      if (e == "expired")
        throw new S("license-key-expired");
      if (e == "domainLimit")
        throw new S("license-key-domain-limit");
      if (e == "featureNotAllowed")
        throw new S("license-key-plugin-not-allowed", null, {
          pluginName: i
        });
      if (e == "evaluationLimit")
        throw new S("license-key-evaluation-limit");
      if (e == "trialLimit")
        throw new S("license-key-trial-limit");
      if (e == "developmentLimit")
        throw new S("license-key-development-limit");
      if (e == "usageLimit")
        throw new S("license-key-usage-limit");
      if (e == "distributionChannel")
        throw new S("license-key-invalid-distribution-channel");
    }, 0), this._showLicenseError = () => {
    };
  }
  /**
  * This part of the code is _not_ executed in installations under the GPL license (with `config.licenseKey = 'GPL'`).
     *
  * It is only executed when a specific license key is provided. If you are uncertain whether
  * this applies to your installation, please contact our support team.
  */
  async _sendUsageRequest(e, i) {
    const s = new Headers({
      "Content-Type": "application/json"
    }), r = await fetch(new URL(e), {
      method: "POST",
      headers: s,
      body: JSON.stringify(i)
    });
    if (!r.ok)
      throw new Error(`HTTP Response: ${r.status}`);
    return r.json();
  }
}
/**
* The default configuration which is built into the editor class.
*
* It is used in CKEditor 5 builds to provide the default configuration options which are later used during the editor initialization.
*
* ```ts
* ClassicEditor.defaultConfig = {
* 	foo: 1,
* 	bar: 2
* };
*
* ClassicEditor
* 	.create( sourceElement )
* 	.then( editor => {
* 		editor.config.get( 'foo' ); // -> 1
* 		editor.config.get( 'bar' ); // -> 2
* 	} );
*
* // The default options can be overridden by the configuration passed to create().
* ClassicEditor
* 	.create( sourceElement, { bar: 3 } )
* 	.then( editor => {
* 		editor.config.get( 'foo' ); // -> 1
* 		editor.config.get( 'bar' ); // -> 3
* 	} );
* ```
*
* See also {@link module:core/editor/editor~Editor.builtinPlugins}.
*/
f(Tr, "defaultConfig"), /**
* An array of plugins built into this editor class.
*
* It is used in CKEditor 5 builds to provide a list of plugins which are later automatically initialized
* during the editor initialization.
*
* They will be automatically initialized by the editor, unless listed in `config.removePlugins` and
* unless `config.plugins` is passed.
*
* ```ts
* // Build some plugins into the editor class first.
* ClassicEditor.builtinPlugins = [ FooPlugin, BarPlugin ];
*
* // Normally, you need to define config.plugins, but since ClassicEditor.builtinPlugins was
* // defined, now you can call create() without any configuration.
* ClassicEditor
* 	.create( sourceElement )
* 	.then( editor => {
* 		editor.plugins.get( FooPlugin ); // -> An instance of the Foo plugin.
* 		editor.plugins.get( BarPlugin ); // -> An instance of the Bar plugin.
* 	} );
*
* ClassicEditor
* 	.create( sourceElement, {
* 		// Do not initialize these plugins (note: it is defined by a string):
* 		removePlugins: [ 'Foo' ]
* 	} )
* 	.then( editor => {
* 		editor.plugins.get( FooPlugin ); // -> Undefined.
* 		editor.config.get( BarPlugin ); // -> An instance of the Bar plugin.
* 	} );
*
* ClassicEditor
* 	.create( sourceElement, {
* 		// Load only this plugin. It can also be defined by a string if
* 		// this plugin was built into the editor class.
* 		plugins: [ FooPlugin ]
* 	} )
* 	.then( editor => {
* 		editor.plugins.get( FooPlugin ); // -> An instance of the Foo plugin.
* 		editor.config.get( BarPlugin ); // -> Undefined.
* 	} );
* ```
*
* See also {@link module:core/editor/editor~Editor.defaultConfig}.
*/
f(Tr, "builtinPlugins"), /**
* The {@link module:core/context~Context} class.
*
* Exposed as static editor field for easier access in editor builds.
*/
f(Tr, "Context", tc), /**
* The {@link module:watchdog/editorwatchdog~EditorWatchdog} class.
*
* Exposed as static editor field for easier access in editor builds.
*/
f(Tr, "EditorWatchdog", my), /**
* The {@link module:watchdog/contextwatchdog~ContextWatchdog} class.
*
* Exposed as static editor field for easier access in editor builds.
*/
f(Tr, "ContextWatchdog", y8);
function V8(n) {
  const t = E8(n);
  function e(i, s) {
    if (Fr(t, i) !== void 0)
      throw new S("editor-usage-data-path-already-set", {
        path: i
      });
    Wv(t, i, s);
  }
  return n.fire("collectUsageData", {
    setUsageData: e
  }), t;
}
function L8(n) {
  if (!Fs(n.updateSourceElement))
    throw new S("attachtoform-missing-elementapi-interface", n);
  const t = n.sourceElement;
  if (B8(t) && t.form) {
    let e;
    const i = t.form, s = () => n.updateSourceElement();
    Fs(i.submit) && (e = i.submit, i.submit = () => {
      s(), e.apply(i);
    }), i.addEventListener("submit", s), n.on("destroy", () => {
      i.removeEventListener("submit", s), e && (i.submit = e);
    });
  }
}
function B8(n) {
  return !!n && n.tagName.toLowerCase() === "textarea";
}
function gh(n) {
  class t extends n {
    constructor() {
      super(...arguments);
      f(this, "sourceElement");
    }
    updateSourceElement(s) {
      if (!this.sourceElement)
        throw new S("editor-missing-sourceelement", this);
      const r = this.config.get("updateSourceElementOnDestroy"), o = this.sourceElement instanceof HTMLTextAreaElement;
      if (!r && !o) {
        op(this.sourceElement, "");
        return;
      }
      const a = typeof s == "string" ? s : this.data.get();
      op(this.sourceElement, a);
    }
  }
  return t;
}
gh.updateSourceElement = gh(Object).prototype.updateSourceElement;
class Hr extends py {
  constructor() {
    super(...arguments);
    /**
    * A list of pending actions.
    */
    f(this, "_actions");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PendingActions";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.set("hasAny", !1), this._actions = new bt({
      idProperty: "_id"
    }), this._actions.delegate("add", "remove").to(this);
  }
  /**
  * Adds an action to the list of pending actions.
  *
  * This method returns an action object with an observable message property.
  * The action object can be later used in the {@link #remove} method. It also allows you to change the message.
  *
  * @param message The action message.
  * @returns An observable object that represents a pending action.
  */
  add(e) {
    if (typeof e != "string")
      throw new S("pendingactions-add-invalid-message", this);
    const i = new (je())();
    return i.set("message", e), this._actions.add(i), this.hasAny = !0, i;
  }
  /**
  * Removes an action from the list of pending actions.
  *
  * @param action An action object.
  */
  remove(e) {
    this._actions.remove(e), this.hasAny = !!this._actions.length;
  }
  /**
  * Returns the first action from the list or null if the list is empty
  *
  * @returns The pending action object.
  */
  get first() {
    return this._actions.get(0);
  }
  /**
  * Iterable interface.
  */
  [Symbol.iterator]() {
    return this._actions[Symbol.iterator]();
  }
}
var M8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.591 10.177 4.243 4.242a1 1 0 0 1-1.415 1.415l-4.242-4.243-4.243 4.243a1 1 0 0 1-1.414-1.415l4.243-4.242L4.52 5.934A1 1 0 0 1 5.934 4.52l4.243 4.243 4.242-4.243a1 1 0 1 1 1.415 1.414l-4.243 4.243z"/></svg>', N8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 16h9a1 1 0 0 1 0 2H2a1 1 0 0 1 0-2z"/><path d="M17 1a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2V3a2 2 0 0 1 2-2h14zm0 1.5H3a.5.5 0 0 0-.492.41L2.5 3v9a.5.5 0 0 0 .41.492L3 12.5h14a.5.5 0 0 0 .492-.41L17.5 12V3a.5.5 0 0 0-.41-.492L17 2.5z" fill-opacity=".6"/></svg>', F8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M6.972 16.615a.997.997 0 0 1-.744-.292l-4.596-4.596a1 1 0 1 1 1.414-1.414l3.926 3.926 9.937-9.937a1 1 0 0 1 1.414 1.415L7.717 16.323a.997.997 0 0 1-.745.292z"/></svg>', D8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.333 2 .19 2.263a5.899 5.899 0 0 1 1.458.604L14.714 3.4 16.6 5.286l-1.467 1.733c.263.452.468.942.605 1.46L18 8.666v2.666l-2.263.19a5.899 5.899 0 0 1-.604 1.458l1.467 1.733-1.886 1.886-1.733-1.467a5.899 5.899 0 0 1-1.46.605L11.334 18H8.667l-.19-2.263a5.899 5.899 0 0 1-1.458-.604L5.286 16.6 3.4 14.714l1.467-1.733a5.899 5.899 0 0 1-.604-1.458L2 11.333V8.667l2.262-.189a5.899 5.899 0 0 1 .605-1.459L3.4 5.286 5.286 3.4l1.733 1.467a5.899 5.899 0 0 1 1.46-.605L8.666 2h2.666zM10 6.267a3.733 3.733 0 1 0 0 7.466 3.733 3.733 0 0 0 0-7.466z"/></svg>', $8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10.209 18.717A8.5 8.5 0 1 1 18.686 9.6h-.008l.002.12a3 3 0 0 1-2.866 2.997h-.268l-.046-.002v.002h-4.791a2 2 0 1 0 0 4 1 1 0 1 1-.128 1.992 8.665 8.665 0 0 1-.372.008Zm-3.918-7.01a1.25 1.25 0 1 0-2.415-.648 1.25 1.25 0 0 0 2.415.647ZM5.723 8.18a1.25 1.25 0 1 0 .647-2.414 1.25 1.25 0 0 0-.647 2.414ZM9.76 6.155a1.25 1.25 0 1 0 .647-2.415 1.25 1.25 0 0 0-.647 2.415Zm4.028 1.759a1.25 1.25 0 1 0 .647-2.415 1.25 1.25 0 0 0-.647 2.415Z"/></svg>', z8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m8.636 9.531-2.758 3.94a.5.5 0 0 0 .122.696l3.224 2.284h1.314l2.636-3.736L8.636 9.53zm.288 8.451L5.14 15.396a2 2 0 0 1-.491-2.786l6.673-9.53a2 2 0 0 1 2.785-.49l3.742 2.62a2 2 0 0 1 .491 2.785l-7.269 10.053-2.147-.066z"/><path d="M4 18h5.523v-1H4zm-2 0h1v-1H2z"/></svg>', H8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M11 1a9 9 0 1 1-8.027 13.075l1.128-1.129A7.502 7.502 0 0 0 18.5 10a7.5 7.5 0 1 0-14.962.759l-.745-.746-.76.76A9 9 0 0 1 11 1z"/><path d="M.475 8.17a.75.75 0 0 1 .978.047l.075.082 1.284 1.643 1.681-1.284a.75.75 0 0 1 .978.057l.073.083a.75.75 0 0 1-.057.978l-.083.073-2.27 1.737a.75.75 0 0 1-.973-.052l-.074-.082-1.741-2.23a.75.75 0 0 1 .13-1.052z"/><path d="M11.5 5v4.999l3.196 3.196-1.06 1.06L10.1 10.72l-.1-.113V5z"/></svg>', j8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M5.085 6.22 2.943 4.078a.75.75 0 1 1 1.06-1.06l2.592 2.59A11.094 11.094 0 0 1 10 5.068c4.738 0 8.578 3.101 8.578 5.083 0 1.197-1.401 2.803-3.555 3.887l1.714 1.713a.75.75 0 0 1-.09 1.138.488.488 0 0 1-.15.084.75.75 0 0 1-.821-.16L6.17 7.304c-.258.11-.51.233-.757.365l6.239 6.24-.006.005.78.78c-.388.094-.78.166-1.174.215l-1.11-1.11h.011L4.55 8.197a7.2 7.2 0 0 0-.665.514l-.112.098 4.897 4.897-.005.006 1.276 1.276a10.164 10.164 0 0 1-1.477-.117l-.479-.479-.009.009-4.863-4.863-.022.031a2.563 2.563 0 0 0-.124.2c-.043.077-.08.158-.108.241a.534.534 0 0 0-.028.133.29.29 0 0 0 .008.072.927.927 0 0 0 .082.226c.067.133.145.26.234.379l3.242 3.365.025.01.59.623c-3.265-.918-5.59-3.155-5.59-4.668 0-1.194 1.448-2.838 3.663-3.93zm7.07.531a4.632 4.632 0 0 1 1.108 5.992l.345.344.046-.018a9.313 9.313 0 0 0 2-1.112c.256-.187.5-.392.727-.613.137-.134.27-.277.392-.431.072-.091.141-.185.203-.286.057-.093.107-.19.148-.292a.72.72 0 0 0 .036-.12.29.29 0 0 0 .008-.072.492.492 0 0 0-.028-.133.999.999 0 0 0-.036-.096 2.165 2.165 0 0 0-.071-.145 2.917 2.917 0 0 0-.125-.2 3.592 3.592 0 0 0-.263-.335 5.444 5.444 0 0 0-.53-.523 7.955 7.955 0 0 0-1.054-.768 9.766 9.766 0 0 0-1.879-.891c-.337-.118-.68-.219-1.027-.301zm-2.85.21-.069.002a.508.508 0 0 0-.254.097.496.496 0 0 0-.104.679.498.498 0 0 0 .326.199l.045.005c.091.003.181.003.272.012a2.45 2.45 0 0 1 2.017 1.513c.024.061.043.125.069.185a.494.494 0 0 0 .45.287h.008a.496.496 0 0 0 .35-.158.482.482 0 0 0 .13-.335.638.638 0 0 0-.048-.219 3.379 3.379 0 0 0-.36-.723 3.438 3.438 0 0 0-2.791-1.543l-.028-.001h-.013z"/></svg>', U8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3.035 1C2.446 1 2 1.54 2 2.098V10.5h1.5v-8h13v8H18V2.098C18 1.539 17.48 1 16.9 1H3.035Zm10.453 2.61a1.885 1.885 0 0 0-1.442.736 1.89 1.89 0 0 0 1.011 2.976 1.903 1.903 0 0 0 2.253-1.114 1.887 1.887 0 0 0-1.822-2.598ZM7.463 8.163a.611.611 0 0 0-.432.154L5.071 10.5h5.119L7.88 8.348a.628.628 0 0 0-.417-.185Zm6.236 1.059a.62.62 0 0 0-.42.164L12.07 10.5h2.969l-.92-1.113a.618.618 0 0 0-.42-.165ZM.91 11.5a.91.91 0 0 0-.91.912v6.877c0 .505.405.91.91.91h18.178a.91.91 0 0 0 .912-.91v-6.877a.908.908 0 0 0-.912-.912H.91ZM3.668 13h1.947l2.135 5.7H5.898l-.28-.946H3.601l-.278.945H1.516L3.668 13Zm4.947 0h1.801v4.3h2.7v1.4h-4.5V13h-.001Zm4.5 0h5.4v1.4h-1.798v4.3h-1.701v-4.3h-1.9V13h-.001Zm-8.517 1.457-.614 2.059h1.262l-.648-2.059Z"/></svg>', W8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M12.68 13.74h-.001l4.209 4.208a1 1 0 1 0 1.414-1.414l-4.267-4.268a6 6 0 1 0-1.355 1.474ZM13 9a4 4 0 1 1-8 0 4 4 0 0 1 8 0Z"/></svg>', q8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M11.463 5.187a.888.888 0 1 1 1.254 1.255L9.16 10l3.557 3.557a.888.888 0 1 1-1.254 1.255L7.26 10.61a.888.888 0 0 1 .16-1.382l4.043-4.042z"/></svg>', G8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M8.537 14.813a.888.888 0 1 1-1.254-1.255L10.84 10 7.283 6.442a.888.888 0 1 1 1.254-1.255L12.74 9.39a.888.888 0 0 1-.16 1.382l-4.043 4.042z"/></svg>', K8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M6.66 9.118a.693.693 0 0 1 .956.032l3.65 3.411 2.422-2.238a.695.695 0 0 1 .945 0L17.5 13.6V2.5h-15v11.1l4.16-4.482ZM17.8 1c.652 0 1.2.47 1.2 1.1v14.362c0 .64-.532 1.038-1.184 1.038H2.184C1.532 17.5 1 17.103 1 16.462V2.1C1 1.47 1.537 1 2.2 1h15.6Zm-5.655 6a2.128 2.128 0 0 1 .157-2.364A2.133 2.133 0 1 1 12.145 7Z"/></svg>', Z8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M1.201 1C.538 1 0 1.47 0 2.1v14.363c0 .64.534 1.037 1.186 1.037h9.494a2.97 2.97 0 0 1-.414-.287 2.998 2.998 0 0 1-1.055-2.03 3.003 3.003 0 0 1 .693-2.185l.383-.455-.02.018-3.65-3.41a.695.695 0 0 0-.957-.034L1.5 13.6V2.5h15v5.535a2.97 2.97 0 0 1 1.412.932l.088.105V2.1c0-.63-.547-1.1-1.2-1.1H1.202Zm11.713 2.803a2.146 2.146 0 0 0-2.049 1.992 2.14 2.14 0 0 0 1.28 2.096 2.13 2.13 0 0 0 2.644-3.11 2.134 2.134 0 0 0-1.875-.978Z"/><path d="M15.522 19.1a.79.79 0 0 0 .79-.79v-5.373l2.059 2.455a.79.79 0 1 0 1.211-1.015l-3.352-3.995a.79.79 0 0 0-.995-.179.784.784 0 0 0-.299.221l-3.35 3.99a.79.79 0 1 0 1.21 1.017l1.936-2.306v5.185c0 .436.353.79.79.79Z"/><path d="M15.522 19.1a.79.79 0 0 0 .79-.79v-5.373l2.059 2.455a.79.79 0 1 0 1.211-1.015l-3.352-3.995a.79.79 0 0 0-.995-.179.784.784 0 0 0-.299.221l-3.35 3.99a.79.79 0 1 0 1.21 1.017l1.936-2.306v5.185c0 .436.353.79.79.79Z"/></svg>', Y8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M1.201 1c-.662 0-1.2.47-1.2 1.1v14.248c0 .64.533 1.152 1.185 1.152h6.623v-7.236L6.617 9.15a.694.694 0 0 0-.957-.033L1.602 13.55V2.553l14.798.003V9.7H18V2.1c0-.63-.547-1.1-1.2-1.1H1.202Zm11.723 2.805a2.094 2.094 0 0 0-1.621.832 2.127 2.127 0 0 0 1.136 3.357 2.13 2.13 0 0 0 2.611-1.506 2.133 2.133 0 0 0-.76-2.244 2.13 2.13 0 0 0-1.366-.44Z"/><path clip-rule="evenodd" d="M19.898 12.369v6.187a.844.844 0 0 1-.844.844h-8.719a.844.844 0 0 1-.843-.844v-7.312a.844.844 0 0 1 .843-.844h2.531a.843.843 0 0 1 .597.248l.838.852h4.75c.223 0 .441.114.6.272a.844.844 0 0 1 .247.597Zm-1.52.654-4.377.02-1.1-1.143H11v6h7.4l-.023-4.877Z"/></svg>', J8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M1.201 1C.538 1 0 1.47 0 2.1v14.363c0 .64.534 1.037 1.186 1.037h7.029a5.401 5.401 0 0 1 .615-4.338l.762-1.232-2.975-2.78a.696.696 0 0 0-.957-.033L1.5 13.6V2.5h15v6.023c.449.131.887.32 1.307.573l.058.033c.046.028.09.057.135.086V2.1c0-.63-.547-1.1-1.2-1.1H1.202Zm11.713 2.803a2.15 2.15 0 0 0-1.611.834 2.118 2.118 0 0 0-.438 1.158 2.14 2.14 0 0 0 1.277 2.096 2.132 2.132 0 0 0 2.645-3.11 2.13 2.13 0 0 0-1.873-.978Z"/><path d="M16.63 10.294a3.003 3.003 0 0 0-4.142.887l-.117.177a.647.647 0 0 0-.096.492.664.664 0 0 0 .278.418.7.7 0 0 0 .944-.234 1.741 1.741 0 0 1 2.478-.463 1.869 1.869 0 0 1 .476 2.55.637.637 0 0 0-.071.5.646.646 0 0 0 .309.396.627.627 0 0 0 .869-.19l.027-.041a3.226 3.226 0 0 0-.956-4.492Zm-6.061 3.78-.044.066a3.228 3.228 0 0 0 .82 4.403 3.005 3.005 0 0 0 4.275-.798l.13-.197a.626.626 0 0 0 .092-.475.638.638 0 0 0-.268-.402.713.713 0 0 0-.99.26l-.018.029a1.741 1.741 0 0 1-2.477.461 1.87 1.87 0 0 1-.475-2.55l.029-.047a.647.647 0 0 0 .086-.485.66.66 0 0 0-.275-.408l-.04-.027a.609.609 0 0 0-.845.17Z"/><path d="M15.312 13.925c.24-.36.154-.838-.19-1.067-.346-.23-.82-.124-1.059.236l-1.268 1.907c-.239.36-.153.838.192 1.067.345.23.818.123 1.057-.236l1.268-1.907Z"/></svg>', X8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m9.239 13.938-2.88-1.663a.75.75 0 0 1 .75-1.3L9 12.067V4.75a.75.75 0 1 1 1.5 0v7.318l1.89-1.093a.75.75 0 0 1 .75 1.3l-2.879 1.663a.752.752 0 0 1-.511.187.752.752 0 0 1-.511-.187zM4.25 17a.75.75 0 1 1 0-1.5h10.5a.75.75 0 0 1 0 1.5H4.25z"/></svg>', Q8 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M9.75 11.875a.752.752 0 0 1 .508.184l2.883 1.666a.75.75 0 0 1-.659 1.344l-.091-.044-1.892-1.093.001 4.318a.75.75 0 1 1-1.5 0v-4.317l-1.89 1.092a.75.75 0 0 1-.75-1.3l2.879-1.663a.752.752 0 0 1 .51-.187zM15.25 9a.75.75 0 1 1 0 1.5H4.75a.75.75 0 1 1 0-1.5h10.5zM9.75.375a.75.75 0 0 1 .75.75v4.318l1.89-1.093.092-.045a.75.75 0 0 1 .659 1.344l-2.883 1.667a.752.752 0 0 1-.508.184.752.752 0 0 1-.511-.187L6.359 5.65a.75.75 0 0 1 .75-1.299L9 5.442V1.125a.75.75 0 0 1 .75-.75z"/></svg>', eV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m10.261 7.062 2.88 1.663a.75.75 0 0 1-.75 1.3L10.5 8.933v7.317a.75.75 0 1 1-1.5 0V8.932l-1.89 1.093a.75.75 0 0 1-.75-1.3l2.879-1.663a.752.752 0 0 1 .511-.187.752.752 0 0 1 .511.187zM15.25 4a.75.75 0 1 1 0 1.5H4.75a.75.75 0 0 1 0-1.5h10.5z"/></svg>', tV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 8c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 4c0 .414.336.75.75.75h9.929a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0-8c0 .414.336.75.75.75h9.929a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75z"/></svg>', iV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 8c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm2.286 4c0 .414.336.75.75.75h9.928a.75.75 0 1 0 0-1.5H5.036a.75.75 0 0 0-.75.75zm0-8c0 .414.336.75.75.75h9.928a.75.75 0 1 0 0-1.5H5.036a.75.75 0 0 0-.75.75z"/></svg>', nV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M18 3.75a.75.75 0 0 1-.75.75H2.75a.75.75 0 1 1 0-1.5h14.5a.75.75 0 0 1 .75.75zm0 8a.75.75 0 0 1-.75.75H2.75a.75.75 0 1 1 0-1.5h14.5a.75.75 0 0 1 .75.75zm0 4a.75.75 0 0 1-.75.75H7.321a.75.75 0 1 1 0-1.5h9.929a.75.75 0 0 1 .75.75zm0-8a.75.75 0 0 1-.75.75H7.321a.75.75 0 1 1 0-1.5h9.929a.75.75 0 0 1 .75.75z"/></svg>', sV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 8c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0 4c0 .414.336.75.75.75h9.929a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm0-8c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75z"/></svg>', rV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2z"/><path d="M12.003 7v5.5a1 1 0 0 1-1 1H2.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H3.5V12h6.997V7.5z"/></svg>', oV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2z"/><path d="M15.003 7v5.5a1 1 0 0 1-1 1H5.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H6.5V12h6.997V7.5z"/></svg>', aV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2z"/><path d="M18.003 7v5.5a1 1 0 0 1-1 1H8.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H9.5V12h6.997V7.5z"/></svg>', lV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2z"/><path d="M18 7v5.5a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h14a1 1 0 0 1 1 1zm-1.505.5H3.504V12h12.991V7.5z"/></svg>', cV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm11.5 9H18v1.5h-4.5zM2 15h16v1.5H2z"/><path d="M12.003 7v5.5a1 1 0 0 1-1 1H2.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H3.5V12h6.997V7.5z"/></svg>', uV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm11.5 9H18v1.5h-4.5zm0-3H18v1.5h-4.5zm0-3H18v1.5h-4.5zM2 15h16v1.5H2z"/><path d="M12.003 7v5.5a1 1 0 0 1-1 1H2.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H3.5V12h6.997V7.5z"/></svg>', dV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path opacity=".5" d="M2 3h16v1.5H2zm0 12h16v1.5H2zm0-9h5v1.5H2zm0 3h5v1.5H2zm0 3h5v1.5H2z"/><path d="M18.003 7v5.5a1 1 0 0 1-1 1H8.996a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h8.007a1 1 0 0 1 1 1zm-1.506.5H9.5V12h6.997V7.5z"/></svg>', hV = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20"><path d="M2.5 17v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zm2 0v1h-1v-1h1zM1 15.5v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm-19-2v1H0v-1h1zm19 0v1h-1v-1h1zm0-2v1h-1v-1h1zm-19 0v1H0v-1h1zM14.5 2v1h-1V2h1zm2 0v1h-1V2h1zm2 0v1h-1V2h1zm-8 0v1h-1V2h1zm-2 0v1h-1V2h1zm-2 0v1h-1V2h1zm-2 0v1h-1V2h1zm8 0v1h-1V2h1zm-10 0v1h-1V2h1z"/><path d="M18.095 2H1.905C.853 2 0 2.895 0 4v12c0 1.105.853 2 1.905 2h16.19C19.147 18 20 17.105 20 16V4c0-1.105-.853-2-1.905-2zm0 1.5c.263 0 .476.224.476.5v12c0 .276-.213.5-.476.5H1.905a.489.489 0 0 1-.476-.5V4c0-.276.213-.5.476-.5h16.19z"/></svg>', fV = '<svg xmlns="http://www.w3.org/2000/svg" xmlns:v="https://vecta.io/nano" viewBox="0 0 20 20"><path d="M.95 1.43a.95.95 0 0 0-.95.95v3.1a.95.95 0 0 0 .95.95h.75v6.3H.95a.95.95 0 0 0-.95.95v3.1a.95.95 0 0 0 .95.95h3.1a.95.95 0 0 0 .95-.95v-.65h1.932l1.539-1.5H5v-.95a.95.95 0 0 0-.95-.95H3.2v-6.3h.85A.95.95 0 0 0 5 5.48v-.55h10v.55a.95.95 0 0 0 .95.95h3.1a.95.95 0 0 0 .95-.95v-3.1a.95.95 0 0 0-.95-.95h-3.1a.95.95 0 0 0-.95.95v1.05H5V2.38a.95.95 0 0 0-.95-.95H.95zm.55 3.5v-2h2v2h-2zm0 9.3v2h2v-2h-2zm15-11.3v2h2v-2h-2z"/><path d="M8.139 20.004v-2.388l7.045-7.048 2.391 2.391-7.046 7.046h-2.39zm11.421-9.101a.64.64 0 0 1-.138.206l-1.165 1.168-2.391-2.391 1.167-1.163a.63.63 0 0 1 .206-.138.635.635 0 0 1 .243-.049.63.63 0 0 1 .449.187l1.491 1.488c.059.059.108.129.138.206s.049.16.049.243a.6.6 0 0 1-.049.243z"/></svg>', mV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 16.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1ZM1 15v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 13v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 11v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 9v1H0V9h1Zm19 0v1h-1V9h1ZM1 7v1H0V7h1Zm19 0v1h-1V7h1ZM1 5v1H0V5h1Zm19 0v1h-1V5h1Zm0-2v1h-1V3h1ZM1 3v1H0V3h1Zm13.5-1.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm-8 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm8 0v1h-1v-1h1Zm-10 0v1h-1v-1h1Z"/><path d="M13 5.5H2a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h11a2 2 0 0 0 2-2v-8a2 2 0 0 0-2-2ZM13 7a.5.5 0 0 1 .5.5v8a.5.5 0 0 1-.5.5H2a.5.5 0 0 1-.5-.5v-8A.5.5 0 0 1 2 7h11Z"/></svg>', gV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 16.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1ZM1 15v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 13v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 11v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 9v1H0V9h1Zm19 0v1h-1V9h1ZM1 7v1H0V7h1Zm19 0v1h-1V7h1ZM1 5v1H0V5h1Zm19 0v1h-1V5h1Zm0-2v1h-1V3h1ZM1 3v1H0V3h1Zm13.5-1.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm-8 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm8 0v1h-1v-1h1Zm-10 0v1h-1v-1h1Z"/><path d="M7 9.5H2a2 2 0 0 0-2 2v4a2 2 0 0 0 2 2h5a2 2 0 0 0 2-2v-4a2 2 0 0 0-2-2ZM7 11a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5H2a.5.5 0 0 1-.5-.5v-4A.5.5 0 0 1 2 11h5Z"/></svg>', pV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 16.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1ZM1 15v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 13v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 11v1H0v-1h1Zm19 0v1h-1v-1h1ZM1 9v1H0V9h1Zm19 0v1h-1V9h1ZM1 7v1H0V7h1Zm19 0v1h-1V7h1ZM1 5v1H0V5h1Zm19 0v1h-1V5h1Zm0-2v1h-1V3h1ZM1 3v1H0V3h1Zm13.5-1.5v1h-1v-1h1Zm2 0v1h-1v-1h1Zm2 0v1h-1v-1h1Zm-8 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm-2 0v1h-1v-1h1Zm8 0v1h-1v-1h1Zm-10 0v1h-1v-1h1Z"/><path d="M10 7.5H2a2 2 0 0 0-2 2v6a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2v-6a2 2 0 0 0-2-2ZM10 9a.5.5 0 0 1 .5.5v6a.5.5 0 0 1-.5.5H2a.5.5 0 0 1-.5-.5v-6A.5.5 0 0 1 2 9h8Z"/></svg>', bV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m7.3 17.37-.061.088a1.518 1.518 0 0 1-.934.535l-4.178.663-.806-4.153a1.495 1.495 0 0 1 .187-1.058l.056-.086L8.77 2.639c.958-1.351 2.803-1.076 4.296-.03 1.497 1.047 2.387 2.693 1.433 4.055L7.3 17.37zM9.14 4.728l-5.545 8.346 3.277 2.294 5.544-8.346L9.14 4.728zM6.07 16.512l-3.276-2.295.53 2.73 2.746-.435zM9.994 3.506 13.271 5.8c.316-.452-.16-1.333-1.065-1.966-.905-.634-1.895-.78-2.212-.328zM8 18.5 9.375 17H19v1.5H8z"/></svg>', wV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M6.999 2H15a1 1 0 0 1 0 2h-1.004v13a1 1 0 1 1-2 0V4H8.999v13a1 1 0 1 1-2 0v-7A4 4 0 0 1 3 6a4 4 0 0 1 3.999-4z"/></svg>', vV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 10.423a6.5 6.5 0 0 1 6.056-6.408l.038.67C6.448 5.423 5.354 7.663 5.22 10H9c.552 0 .5.432.5.986v4.511c0 .554-.448.503-1 .503h-5c-.552 0-.5-.449-.5-1.003v-4.574zm8 0a6.5 6.5 0 0 1 6.056-6.408l.038.67c-2.646.739-3.74 2.979-3.873 5.315H17c.552 0 .5.432.5.986v4.511c0 .554-.448.503-1 .503h-5c-.552 0-.5-.449-.5-1.003v-4.574z"/></svg>', _V = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><circle cx="9.5" cy="4.5" r="1.5"/><circle cx="9.5" cy="10.5" r="1.5"/><circle cx="9.5" cy="16.5" r="1.5"/></svg>', yV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M5 3.25a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M12 3.25a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M5 10a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M12 10a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M5 16.75a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/><path d="M12 16.75a1.5 1.5 0 1 0 3 0 1.5 1.5 0 1 0-3 0"/></svg>', kV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10.187 17H5.773c-.637 0-1.092-.138-1.364-.415-.273-.277-.409-.718-.409-1.323V4.738c0-.617.14-1.062.419-1.332.279-.27.73-.406 1.354-.406h4.68c.69 0 1.288.041 1.793.124.506.083.96.242 1.36.478.341.197.644.447.906.75a3.262 3.262 0 0 1 .808 2.162c0 1.401-.722 2.426-2.167 3.075C15.05 10.175 16 11.315 16 13.01a3.756 3.756 0 0 1-2.296 3.504 6.1 6.1 0 0 1-1.517.377c-.571.073-1.238.11-2 .11zm-.217-6.217H7v4.087h3.069c1.977 0 2.965-.69 2.965-2.072 0-.707-.256-1.22-.768-1.537-.512-.319-1.277-.478-2.296-.478zM7 5.13v3.619h2.606c.729 0 1.292-.067 1.69-.2a1.6 1.6 0 0 0 .91-.765c.165-.267.247-.566.247-.897 0-.707-.26-1.176-.778-1.409-.519-.232-1.31-.348-2.375-.348H7z"/></svg>', CV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10.5 5.5H7v5h3.5a2.5 2.5 0 1 0 0-5zM5 3h6.5v.025a5 5 0 0 1 0 9.95V13H7v4a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1z"/></svg>', xV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10 2a1 1 0 0 0-1 1v6H3a1 1 0 1 0 0 2h6v6a1 1 0 1 0 2 0v-6h6a1 1 0 1 0 0-2h-6V3a1 1 0 0 0-1-1Z"/></svg>', TV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M9.816 11.5 7.038 4.785 4.261 11.5h5.555Zm.62 1.5H3.641l-1.666 4.028H.312l5.789-14h1.875l5.789 14h-1.663L10.436 13Z"/><path d="m12.09 17-.534-1.292.848-1.971.545 1.319L12.113 17h-.023Zm1.142-5.187.545 1.319L15.5 9.13l1.858 4.316h-3.45l.398.965h3.467L18.887 17H20l-3.873-9h-1.254l-1.641 3.813Z"/></svg>', EV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M19 4.5 14 0H3v12.673l.868-1.041c.185-.222.4-.402.632-.54V1.5h8v5h5v7.626a2.24 2.24 0 0 1 1.5.822V4.5ZM14 5V2l3.3 3H14Zm-3.692 12.5c.062.105.133.206.213.303L11.52 19H8v-.876a2.243 2.243 0 0 0 1.82-.624h.488Zm7.518-.657a.75.75 0 0 0-1.152-.96L15.5 17.29V12H14v5.29l-1.174-1.408a.75.75 0 0 0-1.152.96l2.346 2.816a.95.95 0 0 0 1.46 0l2.346-2.815Zm-15.056-.38a.75.75 0 0 1-.096-1.056l2.346-2.815a.95.95 0 0 1 1.46 0l2.346 2.815a.75.75 0 1 1-1.152.96L6.5 14.96V20H5v-5.04l-1.174 1.408a.75.75 0 0 1-1.056.096Z"/></svg>', SV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m14.958 9.367-2.189 1.837a.75.75 0 0 0 .965 1.149l3.788-3.18a.747.747 0 0 0 .21-.284.75.75 0 0 0-.17-.945L13.77 4.762a.75.75 0 1 0-.964 1.15l2.331 1.955H6.22A.75.75 0 0 0 6 7.9a4 4 0 1 0 1.477 7.718l-.344-1.489A2.5 2.5 0 1 1 6.039 9.4l-.008-.032h8.927z"/></svg>', AV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m5.042 9.367 2.189 1.837a.75.75 0 0 1-.965 1.149l-3.788-3.18a.747.747 0 0 1-.21-.284.75.75 0 0 1 .17-.945L6.23 4.762a.75.75 0 1 1 .964 1.15L4.863 7.866h8.917A.75.75 0 0 1 14 7.9a4 4 0 1 1-1.477 7.718l.344-1.489a2.5 2.5 0 1 0 1.094-4.73l.008-.032H5.042z"/></svg>', IV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M7 5.75c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zm-6 0C1 4.784 1.777 4 2.75 4c.966 0 1.75.777 1.75 1.75 0 .966-.777 1.75-1.75 1.75C1.784 7.5 1 6.723 1 5.75zm6 9c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zm-6 0c0-.966.777-1.75 1.75-1.75.966 0 1.75.777 1.75 1.75 0 .966-.777 1.75-1.75 1.75-.966 0-1.75-.777-1.75-1.75z"/></svg>', PV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M7 5.75c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zM3.5 3v5H2V3.7H1v-1h2.5V3zM.343 17.857l2.59-3.257H2.92a.6.6 0 1 0-1.04 0H.302a2 2 0 1 1 3.995 0h-.001c-.048.405-.16.734-.333.988-.175.254-.59.692-1.244 1.312H4.3v1h-4l.043-.043zM7 14.75a.75.75 0 0 1 .75-.75h9.5a.75.75 0 1 1 0 1.5h-9.5a.75.75 0 0 1-.75-.75z"/></svg>', RV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m2.315 14.705 2.224-2.24a.689.689 0 0 1 .963 0 .664.664 0 0 1 0 .949L2.865 16.07a.682.682 0 0 1-.112.089.647.647 0 0 1-.852-.051L.688 14.886a.635.635 0 0 1 0-.903.647.647 0 0 1 .91 0l.717.722zm5.185.045a.75.75 0 0 1 .75-.75h9.5a.75.75 0 1 1 0 1.5h-9.5a.75.75 0 0 1-.75-.75zM2.329 5.745l2.21-2.226a.689.689 0 0 1 .963 0 .664.664 0 0 1 0 .95L2.865 7.125a.685.685 0 0 1-.496.196.644.644 0 0 1-.468-.187L.688 5.912a.635.635 0 0 1 0-.903.647.647 0 0 1 .91 0l.73.736zM7.5 5.75A.75.75 0 0 1 8.25 5h9.5a.75.75 0 1 1 0 1.5h-9.5a.75.75 0 0 1-.75-.75z"/></svg>', OV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M12.87 12.61a.75.75 0 0 1-.089.976l-.085.07-3.154 2.254 3.412 2.414a.75.75 0 0 1 .237.95l-.057.095a.75.75 0 0 1-.95.237l-.096-.058-4.272-3.022-.003-1.223 4.01-2.867a.75.75 0 0 1 1.047.174zm2.795-.231.095.057 4.011 2.867-.003 1.223-4.272 3.022-.095.058a.75.75 0 0 1-.88-.151l-.07-.086-.058-.095a.75.75 0 0 1 .15-.88l.087-.07 3.412-2.414-3.154-2.253-.085-.071a.75.75 0 0 1 .862-1.207zM16 0a2 2 0 0 1 2 2v9.354l-.663-.492-.837-.001V2a.5.5 0 0 0-.5-.5H2a.5.5 0 0 0-.5.5v15a.5.5 0 0 0 .5.5h3.118L7.156 19H2a2 2 0 0 1-2-2V2a2 2 0 0 1 2-2h14zM5.009 15l.003 1H3v-1h2.009zm2.188-2-1.471 1H5v-1h2.197zM10 11v.095L8.668 12H7v-1h3zm4-2v1H7V9h7zm0-2v1H7V7h7zm-4-2v1H5V5h5zM6 3v1H3V3h3z"/></svg>', VV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M11.627 16.5zm5.873-.196zm0-7.001V8h-13v8.5h4.341c.191.54.457 1.044.785 1.5H2a1.5 1.5 0 0 1-1.5-1.5v-13A1.5 1.5 0 0 1 2 2h4.5a1.5 1.5 0 0 1 1.06.44L9.122 4H16a1.5 1.5 0 0 1 1.5 1.5v1A1.5 1.5 0 0 1 19 8v2.531a6.027 6.027 0 0 0-1.5-1.228zM16 6.5v-1H8.5l-2-2H2v13h1V8a1.5 1.5 0 0 1 1.5-1.5H16z"/><path d="M14.5 19.5a5 5 0 1 1 0-10 5 5 0 0 1 0 10zM15 14v-2h-1v2h-2v1h2v2h1v-2h2v-1h-2z"/></svg>', LV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M19 9v10h-2v-8h-2V9h4zM4 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1H10a1 1 0 0 1-1-1V11H4v4.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1H3a1 1 0 0 1 1 1v4.5z"/></svg>', BV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1H9a1 1 0 0 1-1-1V11H3v4.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1H2a1 1 0 0 1 1 1v4.5zm16.076 8.343V18.5h-6.252c.067-.626.27-1.22.61-1.78.338-.561 1.006-1.305 2.005-2.232.804-.749 1.297-1.257 1.479-1.523.245-.368.368-.732.368-1.092 0-.398-.107-.703-.32-.917-.214-.214-.51-.32-.886-.32-.372 0-.669.111-.889.336-.22.224-.347.596-.38 1.117l-1.778-.178c.106-.982.438-1.686.997-2.114.558-.427 1.257-.64 2.095-.64.918 0 1.64.247 2.164.742.525.495.787 1.11.787 1.847 0 .419-.075.818-.225 1.197-.15.378-.388.775-.714 1.19-.216.275-.605.67-1.168 1.187-.563.516-.92.859-1.07 1.028a3.11 3.11 0 0 0-.365.495h3.542z"/></svg>', MV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1H9a1 1 0 0 1-1-1V11H3v4.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1H2a1 1 0 0 1 1 1v4.5zm9.989 7.53 1.726-.209c.055.44.203.777.445 1.01.24.232.533.349.876.349.368 0 .678-.14.93-.42.251-.279.377-.655.377-1.13 0-.448-.12-.803-.362-1.066a1.153 1.153 0 0 0-.882-.393c-.228 0-.501.044-.819.133l.197-1.453c.482.012.85-.092 1.105-.315.253-.222.38-.517.38-.885 0-.313-.093-.563-.279-.75-.186-.185-.434-.278-.743-.278a1.07 1.07 0 0 0-.78.317c-.216.212-.347.52-.394.927l-1.644-.28c.114-.562.287-1.012.517-1.348.231-.337.553-.601.965-.794a3.24 3.24 0 0 1 1.387-.289c.876 0 1.579.28 2.108.838.436.457.653.973.653 1.549 0 .817-.446 1.468-1.339 1.955.533.114.96.37 1.28.768.319.398.478.878.478 1.441 0 .817-.298 1.513-.895 2.088-.596.576-1.339.864-2.228.864-.842 0-1.54-.243-2.094-.727-.555-.485-.876-1.118-.965-1.901z"/></svg>', NV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3.5 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V11h-5v4.5a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v4.5zm13.55 10v-1.873h-3.81v-1.561l4.037-5.91h1.498v5.904h1.156v1.567h-1.156V18.5H17.05zm0-3.44v-3.18l-2.14 3.18h2.14z"/></svg>', FV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3.5 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V11h-5v4.5a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v4.5zm9.578 7.607 1.777-.184c.05.402.201.72.45.955a1.223 1.223 0 0 0 1.81-.101c.258-.303.387-.759.387-1.368 0-.572-.128-1-.384-1.286-.256-.285-.59-.428-1-.428-.512 0-.971.226-1.377.679l-1.448-.21.915-4.843h4.716v1.67H15.56l-.28 1.58a2.697 2.697 0 0 1 1.219-.298 2.68 2.68 0 0 1 2.012.863c.55.576.825 1.323.825 2.241a3.36 3.36 0 0 1-.666 2.05c-.605.821-1.445 1.232-2.52 1.232-.86 0-1.56-.23-2.101-.692-.542-.461-.866-1.081-.971-1.86z"/></svg>', DV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3.5 8.5h5V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v11.5a1 1 0 0 1-1 1h-.5a1 1 0 0 1-1-1V11h-5v4.5a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1h.5a1 1 0 0 1 1 1v4.5zm15.595 2.973-1.726.19c-.043-.355-.153-.617-.33-.787-.178-.169-.409-.253-.692-.253-.377 0-.695.169-.956.507-.26.339-.424 1.043-.492 2.114.445-.525.997-.787 1.657-.787.745 0 1.383.284 1.914.85.531.568.797 1.3.797 2.197 0 .952-.28 1.716-.838 2.291-.559.576-1.276.864-2.152.864-.94 0-1.712-.365-2.317-1.095-.605-.73-.908-1.927-.908-3.59 0-1.705.316-2.935.946-3.688.63-.753 1.45-1.13 2.457-1.13.706 0 1.291.198 1.755.594.463.395.758.97.885 1.723zm-4.043 3.891c0 .58.133 1.028.4 1.343.266.315.57.473.914.473.33 0 .605-.13.825-.388.22-.258.33-.68.33-1.27 0-.604-.118-1.047-.355-1.329a1.115 1.115 0 0 0-.89-.422c-.342 0-.632.134-.869.403s-.355.666-.355 1.19z"/></svg>', $V = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 9h16v2H2z"/></svg>', zV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M17 0a2 2 0 0 1 2 2v7a1 1 0 0 1 1 1v5a1 1 0 0 1-.883.993l-.118.006L19 17a2 2 0 0 1-2 2H3a2 2 0 0 1-2-2l-.001-1.001-.116-.006A1 1 0 0 1 0 15v-5a1 1 0 0 1 .999-1L1 2a2 2 0 0 1 2-2h14zm.499 15.999h-15L2.5 17a.5.5 0 0 0 .5.5h14a.5.5 0 0 0 .5-.5l-.001-1.001zm-3.478-6.013-.014.014H14v.007l-1.525 1.525-1.46-1.46-.015.013V10h-1v5h1v-3.53l1.428 1.43.048.043.131-.129L14 11.421V15h1v-5h-.965l-.014-.014zM2 10H1v5h1v-2h2v2h1v-5H4v2H2v-2zm7 0H6v1h1v4h1v-4h1v-1zm8 0h-1v5h3v-1h-2v-4zm0-8.5H3a.5.5 0 0 0-.5.5l-.001 6.999h15L17.5 2a.5.5 0 0 0-.5-.5zM10 7v1H4V7h6zm3-2v1H4V5h9zm-3-2v1H4V3h6z"/></svg>', HV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm5 6c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zM2.75 16.5h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 1 0 0 1.5zM1.632 6.95 5.02 9.358a.4.4 0 0 1-.013.661l-3.39 2.207A.4.4 0 0 1 1 11.892V7.275a.4.4 0 0 1 .632-.326z"/></svg>', jV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2 3.75c0 .414.336.75.75.75h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 0 0-.75.75zm5 6c0 .414.336.75.75.75h9.5a.75.75 0 1 0 0-1.5h-9.5a.75.75 0 0 0-.75.75zM2.75 16.5h14.5a.75.75 0 1 0 0-1.5H2.75a.75.75 0 1 0 0 1.5zm1.618-9.55L.98 9.358a.4.4 0 0 0 .013.661l3.39 2.207A.4.4 0 0 0 5 11.892V7.275a.4.4 0 0 0-.632-.326z"/></svg>', UV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 5.5v3h4v-3H3Zm0 4v3h4v-3H3Zm0 4v3h4v-3H3Zm5 3h4v-3H8v3Zm5 0h4v-3h-4v3Zm4-4v-3h-4v3h4Zm0-4v-3h-4v3h4Zm1.5 8A1.5 1.5 0 0 1 17 18H3a1.5 1.5 0 0 1-1.5-1.5V3c.222-.863 1.068-1.5 2-1.5h13c.932 0 1.778.637 2 1.5v13.5Zm-6.5-4v-3H8v3h4Zm0-4v-3H8v3h4Z"/></svg>', WV = `<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m12.5 0 5 4.5v15.003h-16V0h11zM3 1.5v3.25l-1.497 1-.003 8 1.5 1v3.254L7.685 18l-.001 1.504H17.5V8.002L16 9.428l-.004-4.22-4.222-3.692L3 1.5z"/><path d="M4.06 6.64a.75.75 0 0 1 .958 1.15l-.085.07L2.29 9.75l2.646 1.89c.302.216.4.62.232.951l-.058.095a.75.75 0 0 1-.951.232l-.095-.058-3.5-2.5V9.14l3.496-2.5zm4.194 6.22a.75.75 0 0 1-.958-1.149l.085-.07 2.643-1.89-2.646-1.89a.75.75 0 0 1-.232-.952l.058-.095a.75.75 0 0 1 .95-.232l.096.058 3.5 2.5v1.22l-3.496 2.5zm7.644-.836 2.122 2.122-5.825 5.809-2.125-.005.003-2.116zm2.539-1.847 1.414 1.414a.5.5 0 0 1 0 .707l-1.06 1.06-2.122-2.12 1.061-1.061a.5.5 0 0 1 .707 0z"/></svg>
`, qV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M5.2 7h9.2c.6 0 1 .4 1 1v9.9c0 .5-.4 1-1 1H5.2a1 1 0 0 1-1-1V8c0-.6.4-1 1-1Zm1 1.5c-.3 0-.5.2-.5.5v8c0 .3.2.5.5.5h.5c.2 0 .5-.2.5-.5V9c0-.3-.3-.5-.5-.5h-.5Zm3.2 0c-.2 0-.5.2-.5.5v8c0 .3.3.5.5.5h.5c.3 0 .5-.2.5-.5V9c0-.3-.2-.5-.5-.5h-.5Zm3.5 0c-.2 0-.5.2-.5.5v8c0 .3.3.5.5.5h.5c.3 0 .5-.2.5-.5V9c0-.3-.2-.5-.5-.5h-.5Zm-1.4-7.1H8.3L6.5 3.6H3.8c-.5 0-.7.3-.7.8s.2.7.7.7h12c.6 0 .9-.2.9-.7 0-.5-.3-.8-1-.8h-2.4l-1.8-2.2Z"/></svg>', GV = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path clip-rule="evenodd" d="M5.68 3.417a.238.238 0 0 0-.24.236v12.66l3.793-3.102a1.215 1.215 0 0 1 1.534 0l3.793 3.103V3.654a.238.238 0 0 0-.24-.237H5.68ZM4 3.653C4 2.74 4.752 2 5.68 2h8.64c.928 0 1.68.74 1.68 1.653v13.164c0 1-1.185 1.547-1.967.908L10 14.426l-4.033 3.299c-.782.64-1.967.092-1.967-.908V3.653Z"/></svg>', KV = '<svg viewBox="0 0 14 16" xmlns="http://www.w3.org/2000/svg"><path class="ck-icon__fill" d="M2 14.436V2a1 1 0 0 1 1-1h8a1 1 0 0 1 1 1v12.436a.5.5 0 0 1-.819.385l-3.862-3.2a.5.5 0 0 0-.638 0l-3.862 3.2A.5.5 0 0 1 2 14.436Z"/></svg>', ZV = `<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path clip-rule="evenodd" d="M8.5 8.25a1.25 1.25 0 1 1-2.499.001A1.25 1.25 0 0 1 8.5 8.25Z"/><path clip-rule="evenodd" d="M14 8.25a1.25 1.25 0 1 1-2.499.001A1.25 1.25 0 0 1 14 8.25Z"/><path clip-rule="evenodd" d="M7.127 12.088a.75.75 0 1 0-1.254.824C6.88 14.444 8.423 15.25 10 15.25c1.578 0 3.12-.805 4.127-2.338a.75.75 0 0 0-1.254-.824C12.13 13.221 11.048 13.75 10 13.75c-1.047 0-2.13-.529-2.873-1.662Z"/><path d="M10 19a9 9 0 1 0 0-18 9 9 0 0 0 0 18Zm0-1.5a7.5 7.5 0 1 1 0-15 7.5 7.5 0 0 1 0 15Z"/></svg>
`;
const Z = {
  bold: kV,
  cancel: M8,
  caption: N8,
  check: F8,
  cog: D8,
  colorPalette: $8,
  eraser: z8,
  history: H8,
  image: K8,
  imageUpload: Z8,
  imageAssetManager: Y8,
  imageUrl: J8,
  lowVision: j8,
  textAlternative: U8,
  loupe: W8,
  previousArrow: q8,
  nextArrow: G8,
  importExport: EV,
  paragraph: CV,
  plus: xV,
  text: TV,
  alignBottom: X8,
  alignMiddle: Q8,
  alignTop: eV,
  alignLeft: tV,
  alignCenter: iV,
  alignRight: nV,
  alignJustify: sV,
  objectLeft: uV,
  objectCenter: oV,
  objectRight: dV,
  objectFullWidth: lV,
  objectInline: cV,
  objectBlockLeft: rV,
  objectBlockRight: aV,
  objectSizeCustom: fV,
  objectSizeFull: hV,
  objectSizeLarge: mV,
  objectSizeSmall: gV,
  objectSizeMedium: pV,
  pencil: bV,
  pilcrow: wV,
  quote: vV,
  threeVerticalDots: _V,
  dragIndicator: yV,
  redo: SV,
  undo: AV,
  bulletedList: IV,
  numberedList: PV,
  todoList: RV,
  codeBlock: OV,
  browseFiles: VV,
  heading1: LV,
  heading2: BV,
  heading3: MV,
  heading4: NV,
  heading5: FV,
  heading6: DV,
  horizontalLine: $V,
  html: zV,
  indent: HV,
  outdent: jV,
  table: UV,
  source: WV,
  remove: qV,
  bookmark: GV,
  bookmarkInline: KV,
  emoji: ZV
};
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class YV extends (/* @__PURE__ */ je()) {
  /**
  * Creates an instance of the FileReader.
  */
  constructor() {
    super();
    f(this, "total");
    /**
    * Instance of native FileReader.
    */
    f(this, "_reader");
    /**
    * Holds the data of an already loaded file. The file must be first loaded
    * by using {@link module:upload/filereader~FileReader#read `read()`}.
    */
    f(this, "_data");
    const e = new window.FileReader();
    this._reader = e, this._data = void 0, this.set("loaded", 0), e.onprogress = (i) => {
      this.loaded = i.loaded;
    };
  }
  /**
  * Returns error that occurred during file reading.
  */
  get error() {
    return this._reader.error;
  }
  /**
  * Holds the data of an already loaded file. The file must be first loaded
  * by using {@link module:upload/filereader~FileReader#read `read()`}.
  */
  get data() {
    return this._data;
  }
  /**
  * Reads the provided file.
  *
  * @param file Native File object.
  * @returns Returns a promise that will be resolved with file's content.
  * The promise will be rejected in case of an error or when the reading process is aborted.
  */
  read(e) {
    const i = this._reader;
    return this.total = e.size, new Promise((s, r) => {
      i.onload = () => {
        const o = i.result;
        this._data = o, s(o);
      }, i.onerror = () => {
        r("error");
      }, i.onabort = () => {
        r("aborted");
      }, this._reader.readAsDataURL(e);
    });
  }
  /**
  * Aborts file reader.
  */
  abort() {
    this._reader.abort();
  }
}
class Rr extends I {
  constructor() {
    super(...arguments);
    /**
    * Collection of loaders associated with this repository.
    */
    f(this, "loaders", new bt());
    /**
    * Loaders mappings used to retrieve loaders references.
    */
    f(this, "_loadersMap", /* @__PURE__ */ new Map());
    /**
    * Reference to a pending action registered in a {@link module:core/pendingactions~PendingActions} plugin
    * while upload is in progress. When there is no upload then value is `null`.
    */
    f(this, "_pendingAction", null);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FileRepository";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hr
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    this.loaders.on("change", () => this._updatePendingAction()), this.set("uploaded", 0), this.set("uploadTotal", null), this.bind("uploadedPercent").to(this, "uploaded", this, "uploadTotal", (e, i) => i ? e / i * 100 : 0);
  }
  /**
  * Returns the loader associated with specified file or promise.
  *
  * To get loader by id use `fileRepository.loaders.get( id )`.
  *
  * @param fileOrPromise Native file or promise handle.
  */
  getLoader(e) {
    return this._loadersMap.get(e) || null;
  }
  /**
  * Creates a loader instance for the given file.
  *
  * Requires {@link #createUploadAdapter} factory to be defined.
  *
  * @param fileOrPromise Native File object or native Promise object which resolves to a File.
  */
  createLoader(e) {
    if (!this.createUploadAdapter)
      return Ve("filerepository-no-upload-adapter"), null;
    const i = new nb(Promise.resolve(e), this.createUploadAdapter);
    return this.loaders.add(i), this._loadersMap.set(e, i), e instanceof Promise && i.file.then((s) => {
      this._loadersMap.set(s, i);
    }).catch(() => {
    }), i.on("change:uploaded", () => {
      let s = 0;
      for (const r of this.loaders)
        s += r.uploaded;
      this.uploaded = s;
    }), i.on("change:uploadTotal", () => {
      let s = 0;
      for (const r of this.loaders)
        r.uploadTotal && (s += r.uploadTotal);
      this.uploadTotal = s;
    }), i;
  }
  /**
  * Destroys the given loader.
  *
  * @param fileOrPromiseOrLoader File or Promise associated with that loader or loader itself.
  */
  destroyLoader(e) {
    const i = e instanceof nb ? e : this.getLoader(e);
    i._destroy(), this.loaders.remove(i), this._loadersMap.forEach((s, r) => {
      s === i && this._loadersMap.delete(r);
    });
  }
  /**
  * Registers or deregisters pending action bound with upload progress.
  */
  _updatePendingAction() {
    const e = this.editor.plugins.get(Hr);
    if (this.loaders.length) {
      if (!this._pendingAction) {
        const i = this.editor.t, s = (r) => `${i("Upload in progress")} ${parseInt(r)}%.`;
        this._pendingAction = e.add(s(this.uploadedPercent)), this._pendingAction.bind("message").to(this, "uploadedPercent", s);
      }
    } else
      e.remove(this._pendingAction), this._pendingAction = null;
  }
}
class nb extends (/* @__PURE__ */ je()) {
  /**
  * Creates a new instance of `FileLoader`.
  *
  * @param filePromise A promise which resolves to a file instance.
  * @param uploadAdapterCreator The function which returns {@link module:upload/filerepository~UploadAdapter} instance.
  */
  constructor(e, i) {
    super();
    /**
    * Unique id of FileLoader instance.
    *
    * @readonly
    */
    f(this, "id");
    /**
    * Additional wrapper over the initial file promise passed to this loader.
    */
    f(this, "_filePromiseWrapper");
    /**
    * Adapter instance associated with this file loader.
    */
    f(this, "_adapter");
    /**
    * FileReader used by FileLoader.
    */
    f(this, "_reader");
    this.id = Bt(), this._filePromiseWrapper = this._createFilePromiseWrapper(e), this._adapter = i(this), this._reader = new YV(), this.set("status", "idle"), this.set("uploaded", 0), this.set("uploadTotal", null), this.bind("uploadedPercent").to(this, "uploaded", this, "uploadTotal", (s, r) => r ? s / r * 100 : 0), this.set("uploadResponse", null);
  }
  /**
  * A `Promise` which resolves to a `File` instance associated with this file loader.
  */
  get file() {
    return this._filePromiseWrapper ? this._filePromiseWrapper.promise.then((e) => this._filePromiseWrapper ? e : null) : Promise.resolve(null);
  }
  /**
  * Returns the file data. To read its data, you need for first load the file
  * by using the {@link module:upload/filerepository~FileLoader#read `read()`} method.
  */
  get data() {
    return this._reader.data;
  }
  /**
  * Reads file using {@link module:upload/filereader~FileReader}.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `filerepository-read-wrong-status` when status
  * is different than `idle`.
  *
  * Example usage:
  *
  * ```ts
  * fileLoader.read()
  * 	.then( data => { ... } )
  * 	.catch( err => {
  * 		if ( err === 'aborted' ) {
  * 			console.log( 'Reading aborted.' );
  * 		} else {
  * 			console.log( 'Reading error.', err );
  * 		}
  * 	} );
  * ```
  *
  * @returns Returns promise that will be resolved with read data. Promise will be rejected if error
  * occurs or if read process is aborted.
  */
  read() {
    if (this.status != "idle")
      throw new S("filerepository-read-wrong-status", this);
    return this.status = "reading", this.file.then((e) => this._reader.read(e)).then((e) => {
      if (this.status !== "reading")
        throw this.status;
      return this.status = "idle", e;
    }).catch((e) => {
      throw e === "aborted" ? (this.status = "aborted", "aborted") : (this.status = "error", this._reader.error ? this._reader.error : e);
    });
  }
  /**
  * Reads file using the provided {@link module:upload/filerepository~UploadAdapter}.
  *
  * Throws {@link module:utils/ckeditorerror~CKEditorError CKEditorError} `filerepository-upload-wrong-status` when status
  * is different than `idle`.
  * Example usage:
  *
  * ```ts
  * fileLoader.upload()
  * 	.then( data => { ... } )
  * 	.catch( e => {
  * 		if ( e === 'aborted' ) {
  * 			console.log( 'Uploading aborted.' );
  * 		} else {
  * 			console.log( 'Uploading error.', e );
  * 		}
  * 	} );
  * ```
  *
  * @returns Returns promise that will be resolved with response data. Promise will be rejected if error
  * occurs or if read process is aborted.
  */
  upload() {
    if (this.status != "idle")
      throw new S("filerepository-upload-wrong-status", this);
    return this.status = "uploading", this.file.then(() => this._adapter.upload()).then((e) => (this.uploadResponse = e, this.status = "idle", e)).catch((e) => {
      throw this.status === "aborted" ? "aborted" : (this.status = "error", e);
    });
  }
  /**
  * Aborts loading process.
  */
  abort() {
    const e = this.status;
    this.status = "aborted", this._filePromiseWrapper.isFulfilled ? e == "reading" ? this._reader.abort() : e == "uploading" && this._adapter.abort && this._adapter.abort() : (this._filePromiseWrapper.promise.catch(() => {
    }), this._filePromiseWrapper.rejecter("aborted")), this._destroy();
  }
  /**
  * Performs cleanup.
  *
  * @internal
  */
  _destroy() {
    this._filePromiseWrapper = void 0, this._reader = void 0, this._adapter = void 0, this.uploadResponse = void 0;
  }
  /**
  * Wraps a given file promise into another promise giving additional
  * control (resolving, rejecting, checking if fulfilled) over it.
  *
  * @param filePromise The initial file promise to be wrapped.
  */
  _createFilePromiseWrapper(e) {
    const i = {};
    return i.promise = new Promise((s, r) => {
      i.rejecter = r, i.isFulfilled = !1, e.then((o) => {
        i.isFulfilled = !0, s(o);
      }).catch((o) => {
        i.isFulfilled = !0, r(o);
      });
    }), i;
  }
}
var Ku, sb;
function by() {
  return sb || (sb = 1, Ku = {
    aliceblue: [240, 248, 255],
    antiquewhite: [250, 235, 215],
    aqua: [0, 255, 255],
    aquamarine: [127, 255, 212],
    azure: [240, 255, 255],
    beige: [245, 245, 220],
    bisque: [255, 228, 196],
    black: [0, 0, 0],
    blanchedalmond: [255, 235, 205],
    blue: [0, 0, 255],
    blueviolet: [138, 43, 226],
    brown: [165, 42, 42],
    burlywood: [222, 184, 135],
    cadetblue: [95, 158, 160],
    chartreuse: [127, 255, 0],
    chocolate: [210, 105, 30],
    coral: [255, 127, 80],
    cornflowerblue: [100, 149, 237],
    cornsilk: [255, 248, 220],
    crimson: [220, 20, 60],
    cyan: [0, 255, 255],
    darkblue: [0, 0, 139],
    darkcyan: [0, 139, 139],
    darkgoldenrod: [184, 134, 11],
    darkgray: [169, 169, 169],
    darkgreen: [0, 100, 0],
    darkgrey: [169, 169, 169],
    darkkhaki: [189, 183, 107],
    darkmagenta: [139, 0, 139],
    darkolivegreen: [85, 107, 47],
    darkorange: [255, 140, 0],
    darkorchid: [153, 50, 204],
    darkred: [139, 0, 0],
    darksalmon: [233, 150, 122],
    darkseagreen: [143, 188, 143],
    darkslateblue: [72, 61, 139],
    darkslategray: [47, 79, 79],
    darkslategrey: [47, 79, 79],
    darkturquoise: [0, 206, 209],
    darkviolet: [148, 0, 211],
    deeppink: [255, 20, 147],
    deepskyblue: [0, 191, 255],
    dimgray: [105, 105, 105],
    dimgrey: [105, 105, 105],
    dodgerblue: [30, 144, 255],
    firebrick: [178, 34, 34],
    floralwhite: [255, 250, 240],
    forestgreen: [34, 139, 34],
    fuchsia: [255, 0, 255],
    gainsboro: [220, 220, 220],
    ghostwhite: [248, 248, 255],
    gold: [255, 215, 0],
    goldenrod: [218, 165, 32],
    gray: [128, 128, 128],
    green: [0, 128, 0],
    greenyellow: [173, 255, 47],
    grey: [128, 128, 128],
    honeydew: [240, 255, 240],
    hotpink: [255, 105, 180],
    indianred: [205, 92, 92],
    indigo: [75, 0, 130],
    ivory: [255, 255, 240],
    khaki: [240, 230, 140],
    lavender: [230, 230, 250],
    lavenderblush: [255, 240, 245],
    lawngreen: [124, 252, 0],
    lemonchiffon: [255, 250, 205],
    lightblue: [173, 216, 230],
    lightcoral: [240, 128, 128],
    lightcyan: [224, 255, 255],
    lightgoldenrodyellow: [250, 250, 210],
    lightgray: [211, 211, 211],
    lightgreen: [144, 238, 144],
    lightgrey: [211, 211, 211],
    lightpink: [255, 182, 193],
    lightsalmon: [255, 160, 122],
    lightseagreen: [32, 178, 170],
    lightskyblue: [135, 206, 250],
    lightslategray: [119, 136, 153],
    lightslategrey: [119, 136, 153],
    lightsteelblue: [176, 196, 222],
    lightyellow: [255, 255, 224],
    lime: [0, 255, 0],
    limegreen: [50, 205, 50],
    linen: [250, 240, 230],
    magenta: [255, 0, 255],
    maroon: [128, 0, 0],
    mediumaquamarine: [102, 205, 170],
    mediumblue: [0, 0, 205],
    mediumorchid: [186, 85, 211],
    mediumpurple: [147, 112, 219],
    mediumseagreen: [60, 179, 113],
    mediumslateblue: [123, 104, 238],
    mediumspringgreen: [0, 250, 154],
    mediumturquoise: [72, 209, 204],
    mediumvioletred: [199, 21, 133],
    midnightblue: [25, 25, 112],
    mintcream: [245, 255, 250],
    mistyrose: [255, 228, 225],
    moccasin: [255, 228, 181],
    navajowhite: [255, 222, 173],
    navy: [0, 0, 128],
    oldlace: [253, 245, 230],
    olive: [128, 128, 0],
    olivedrab: [107, 142, 35],
    orange: [255, 165, 0],
    orangered: [255, 69, 0],
    orchid: [218, 112, 214],
    palegoldenrod: [238, 232, 170],
    palegreen: [152, 251, 152],
    paleturquoise: [175, 238, 238],
    palevioletred: [219, 112, 147],
    papayawhip: [255, 239, 213],
    peachpuff: [255, 218, 185],
    peru: [205, 133, 63],
    pink: [255, 192, 203],
    plum: [221, 160, 221],
    powderblue: [176, 224, 230],
    purple: [128, 0, 128],
    rebeccapurple: [102, 51, 153],
    red: [255, 0, 0],
    rosybrown: [188, 143, 143],
    royalblue: [65, 105, 225],
    saddlebrown: [139, 69, 19],
    salmon: [250, 128, 114],
    sandybrown: [244, 164, 96],
    seagreen: [46, 139, 87],
    seashell: [255, 245, 238],
    sienna: [160, 82, 45],
    silver: [192, 192, 192],
    skyblue: [135, 206, 235],
    slateblue: [106, 90, 205],
    slategray: [112, 128, 144],
    slategrey: [112, 128, 144],
    snow: [255, 250, 250],
    springgreen: [0, 255, 127],
    steelblue: [70, 130, 180],
    tan: [210, 180, 140],
    teal: [0, 128, 128],
    thistle: [216, 191, 216],
    tomato: [255, 99, 71],
    turquoise: [64, 224, 208],
    violet: [238, 130, 238],
    wheat: [245, 222, 179],
    white: [255, 255, 255],
    whitesmoke: [245, 245, 245],
    yellow: [255, 255, 0],
    yellowgreen: [154, 205, 50]
  }), Ku;
}
var JV = by();
const rb = /* @__PURE__ */ Rc(JV);
var ob = {
  red: 0,
  orange: 60,
  yellow: 120,
  green: 180,
  blue: 240,
  purple: 300
};
function ab(n) {
  var t, e = [], i = 1, s;
  if (typeof n == "string")
    if (rb[n])
      e = rb[n].slice(), s = "rgb";
    else if (n === "transparent")
      i = 0, s = "rgb", e = [0, 0, 0];
    else if (/^#[A-Fa-f0-9]+$/.test(n)) {
      var r = n.slice(1), o = r.length, a = o <= 4;
      i = 1, a ? (e = [
        parseInt(r[0] + r[0], 16),
        parseInt(r[1] + r[1], 16),
        parseInt(r[2] + r[2], 16)
      ], o === 4 && (i = parseInt(r[3] + r[3], 16) / 255)) : (e = [
        parseInt(r[0] + r[1], 16),
        parseInt(r[2] + r[3], 16),
        parseInt(r[4] + r[5], 16)
      ], o === 8 && (i = parseInt(r[6] + r[7], 16) / 255)), e[0] || (e[0] = 0), e[1] || (e[1] = 0), e[2] || (e[2] = 0), s = "rgb";
    } else if (t = /^((?:rgb|hs[lvb]|hwb|cmyk?|xy[zy]|gray|lab|lchu?v?|[ly]uv|lms)a?)\s*\(([^\)]*)\)/.exec(n)) {
      var l = t[1], c = l === "rgb", r = l.replace(/a$/, "");
      s = r;
      var o = r === "cmyk" ? 4 : r === "gray" ? 1 : 3;
      e = t[2].trim().split(/\s*[,\/]\s*|\s+/).map(function(h, m) {
        if (/%$/.test(h))
          return m === o ? parseFloat(h) / 100 : r === "rgb" ? parseFloat(h) * 255 / 100 : parseFloat(h);
        if (r[m] === "h") {
          if (/deg$/.test(h))
            return parseFloat(h);
          if (ob[h] !== void 0)
            return ob[h];
        }
        return parseFloat(h);
      }), l === r && e.push(1), i = c || e[o] === void 0 ? 1 : e[o], e = e.slice(0, o);
    } else n.length > 10 && /[0-9](?:\s|\/)/.test(n) && (e = n.match(/([0-9]+)/g).map(function(u) {
      return parseFloat(u);
    }), s = n.match(/([a-z])/ig).join("").toLowerCase());
  else isNaN(n) ? Array.isArray(n) || n.length ? (e = [n[0], n[1], n[2]], s = "rgb", i = n.length === 4 ? n[3] : 1) : n instanceof Object && (n.r != null || n.red != null || n.R != null ? (s = "rgb", e = [
    n.r || n.red || n.R || 0,
    n.g || n.green || n.G || 0,
    n.b || n.blue || n.B || 0
  ]) : (s = "hsl", e = [
    n.h || n.hue || n.H || 0,
    n.s || n.saturation || n.S || 0,
    n.l || n.lightness || n.L || n.b || n.brightness
  ]), i = n.a || n.alpha || n.opacity || 1, n.opacity != null && (i /= 100)) : (s = "rgb", e = [n >>> 16, (n & 65280) >>> 8, n & 255]);
  return {
    space: s,
    values: e,
    alpha: i
  };
}
var Zu, lb;
function wy() {
  if (lb) return Zu;
  lb = 1;
  const n = by(), t = {};
  for (const s of Object.keys(n))
    t[n[s]] = s;
  const e = {
    rgb: { channels: 3, labels: "rgb" },
    hsl: { channels: 3, labels: "hsl" },
    hsv: { channels: 3, labels: "hsv" },
    hwb: { channels: 3, labels: "hwb" },
    cmyk: { channels: 4, labels: "cmyk" },
    xyz: { channels: 3, labels: "xyz" },
    lab: { channels: 3, labels: "lab" },
    lch: { channels: 3, labels: "lch" },
    hex: { channels: 1, labels: ["hex"] },
    keyword: { channels: 1, labels: ["keyword"] },
    ansi16: { channels: 1, labels: ["ansi16"] },
    ansi256: { channels: 1, labels: ["ansi256"] },
    hcg: { channels: 3, labels: ["h", "c", "g"] },
    apple: { channels: 3, labels: ["r16", "g16", "b16"] },
    gray: { channels: 1, labels: ["gray"] }
  };
  Zu = e;
  for (const s of Object.keys(e)) {
    if (!("channels" in e[s]))
      throw new Error("missing channels property: " + s);
    if (!("labels" in e[s]))
      throw new Error("missing channel labels property: " + s);
    if (e[s].labels.length !== e[s].channels)
      throw new Error("channel and label counts mismatch: " + s);
    const { channels: r, labels: o } = e[s];
    delete e[s].channels, delete e[s].labels, Object.defineProperty(e[s], "channels", { value: r }), Object.defineProperty(e[s], "labels", { value: o });
  }
  e.rgb.hsl = function(s) {
    const r = s[0] / 255, o = s[1] / 255, a = s[2] / 255, l = Math.min(r, o, a), c = Math.max(r, o, a), u = c - l;
    let d, h;
    c === l ? d = 0 : r === c ? d = (o - a) / u : o === c ? d = 2 + (a - r) / u : a === c && (d = 4 + (r - o) / u), d = Math.min(d * 60, 360), d < 0 && (d += 360);
    const m = (l + c) / 2;
    return c === l ? h = 0 : m <= 0.5 ? h = u / (c + l) : h = u / (2 - c - l), [d, h * 100, m * 100];
  }, e.rgb.hsv = function(s) {
    let r, o, a, l, c;
    const u = s[0] / 255, d = s[1] / 255, h = s[2] / 255, m = Math.max(u, d, h), g = m - Math.min(u, d, h), p = function(w) {
      return (m - w) / 6 / g + 1 / 2;
    };
    return g === 0 ? (l = 0, c = 0) : (c = g / m, r = p(u), o = p(d), a = p(h), u === m ? l = a - o : d === m ? l = 1 / 3 + r - a : h === m && (l = 2 / 3 + o - r), l < 0 ? l += 1 : l > 1 && (l -= 1)), [
      l * 360,
      c * 100,
      m * 100
    ];
  }, e.rgb.hwb = function(s) {
    const r = s[0], o = s[1];
    let a = s[2];
    const l = e.rgb.hsl(s)[0], c = 1 / 255 * Math.min(r, Math.min(o, a));
    return a = 1 - 1 / 255 * Math.max(r, Math.max(o, a)), [l, c * 100, a * 100];
  }, e.rgb.cmyk = function(s) {
    const r = s[0] / 255, o = s[1] / 255, a = s[2] / 255, l = Math.min(1 - r, 1 - o, 1 - a), c = (1 - r - l) / (1 - l) || 0, u = (1 - o - l) / (1 - l) || 0, d = (1 - a - l) / (1 - l) || 0;
    return [c * 100, u * 100, d * 100, l * 100];
  };
  function i(s, r) {
    return (s[0] - r[0]) ** 2 + (s[1] - r[1]) ** 2 + (s[2] - r[2]) ** 2;
  }
  return e.rgb.keyword = function(s) {
    const r = t[s];
    if (r)
      return r;
    let o = 1 / 0, a;
    for (const l of Object.keys(n)) {
      const c = n[l], u = i(s, c);
      u < o && (o = u, a = l);
    }
    return a;
  }, e.keyword.rgb = function(s) {
    return n[s];
  }, e.rgb.xyz = function(s) {
    let r = s[0] / 255, o = s[1] / 255, a = s[2] / 255;
    r = r > 0.04045 ? ((r + 0.055) / 1.055) ** 2.4 : r / 12.92, o = o > 0.04045 ? ((o + 0.055) / 1.055) ** 2.4 : o / 12.92, a = a > 0.04045 ? ((a + 0.055) / 1.055) ** 2.4 : a / 12.92;
    const l = r * 0.4124 + o * 0.3576 + a * 0.1805, c = r * 0.2126 + o * 0.7152 + a * 0.0722, u = r * 0.0193 + o * 0.1192 + a * 0.9505;
    return [l * 100, c * 100, u * 100];
  }, e.rgb.lab = function(s) {
    const r = e.rgb.xyz(s);
    let o = r[0], a = r[1], l = r[2];
    o /= 95.047, a /= 100, l /= 108.883, o = o > 8856e-6 ? o ** (1 / 3) : 7.787 * o + 16 / 116, a = a > 8856e-6 ? a ** (1 / 3) : 7.787 * a + 16 / 116, l = l > 8856e-6 ? l ** (1 / 3) : 7.787 * l + 16 / 116;
    const c = 116 * a - 16, u = 500 * (o - a), d = 200 * (a - l);
    return [c, u, d];
  }, e.hsl.rgb = function(s) {
    const r = s[0] / 360, o = s[1] / 100, a = s[2] / 100;
    let l, c, u;
    if (o === 0)
      return u = a * 255, [u, u, u];
    a < 0.5 ? l = a * (1 + o) : l = a + o - a * o;
    const d = 2 * a - l, h = [0, 0, 0];
    for (let m = 0; m < 3; m++)
      c = r + 1 / 3 * -(m - 1), c < 0 && c++, c > 1 && c--, 6 * c < 1 ? u = d + (l - d) * 6 * c : 2 * c < 1 ? u = l : 3 * c < 2 ? u = d + (l - d) * (2 / 3 - c) * 6 : u = d, h[m] = u * 255;
    return h;
  }, e.hsl.hsv = function(s) {
    const r = s[0];
    let o = s[1] / 100, a = s[2] / 100, l = o;
    const c = Math.max(a, 0.01);
    a *= 2, o *= a <= 1 ? a : 2 - a, l *= c <= 1 ? c : 2 - c;
    const u = (a + o) / 2, d = a === 0 ? 2 * l / (c + l) : 2 * o / (a + o);
    return [r, d * 100, u * 100];
  }, e.hsv.rgb = function(s) {
    const r = s[0] / 60, o = s[1] / 100;
    let a = s[2] / 100;
    const l = Math.floor(r) % 6, c = r - Math.floor(r), u = 255 * a * (1 - o), d = 255 * a * (1 - o * c), h = 255 * a * (1 - o * (1 - c));
    switch (a *= 255, l) {
      case 0:
        return [a, h, u];
      case 1:
        return [d, a, u];
      case 2:
        return [u, a, h];
      case 3:
        return [u, d, a];
      case 4:
        return [h, u, a];
      case 5:
        return [a, u, d];
    }
  }, e.hsv.hsl = function(s) {
    const r = s[0], o = s[1] / 100, a = s[2] / 100, l = Math.max(a, 0.01);
    let c, u;
    u = (2 - o) * a;
    const d = (2 - o) * l;
    return c = o * l, c /= d <= 1 ? d : 2 - d, c = c || 0, u /= 2, [r, c * 100, u * 100];
  }, e.hwb.rgb = function(s) {
    const r = s[0] / 360;
    let o = s[1] / 100, a = s[2] / 100;
    const l = o + a;
    let c;
    l > 1 && (o /= l, a /= l);
    const u = Math.floor(6 * r), d = 1 - a;
    c = 6 * r - u, (u & 1) !== 0 && (c = 1 - c);
    const h = o + c * (d - o);
    let m, g, p;
    switch (u) {
      default:
      case 6:
      case 0:
        m = d, g = h, p = o;
        break;
      case 1:
        m = h, g = d, p = o;
        break;
      case 2:
        m = o, g = d, p = h;
        break;
      case 3:
        m = o, g = h, p = d;
        break;
      case 4:
        m = h, g = o, p = d;
        break;
      case 5:
        m = d, g = o, p = h;
        break;
    }
    return [m * 255, g * 255, p * 255];
  }, e.cmyk.rgb = function(s) {
    const r = s[0] / 100, o = s[1] / 100, a = s[2] / 100, l = s[3] / 100, c = 1 - Math.min(1, r * (1 - l) + l), u = 1 - Math.min(1, o * (1 - l) + l), d = 1 - Math.min(1, a * (1 - l) + l);
    return [c * 255, u * 255, d * 255];
  }, e.xyz.rgb = function(s) {
    const r = s[0] / 100, o = s[1] / 100, a = s[2] / 100;
    let l, c, u;
    return l = r * 3.2406 + o * -1.5372 + a * -0.4986, c = r * -0.9689 + o * 1.8758 + a * 0.0415, u = r * 0.0557 + o * -0.204 + a * 1.057, l = l > 31308e-7 ? 1.055 * l ** (1 / 2.4) - 0.055 : l * 12.92, c = c > 31308e-7 ? 1.055 * c ** (1 / 2.4) - 0.055 : c * 12.92, u = u > 31308e-7 ? 1.055 * u ** (1 / 2.4) - 0.055 : u * 12.92, l = Math.min(Math.max(0, l), 1), c = Math.min(Math.max(0, c), 1), u = Math.min(Math.max(0, u), 1), [l * 255, c * 255, u * 255];
  }, e.xyz.lab = function(s) {
    let r = s[0], o = s[1], a = s[2];
    r /= 95.047, o /= 100, a /= 108.883, r = r > 8856e-6 ? r ** (1 / 3) : 7.787 * r + 16 / 116, o = o > 8856e-6 ? o ** (1 / 3) : 7.787 * o + 16 / 116, a = a > 8856e-6 ? a ** (1 / 3) : 7.787 * a + 16 / 116;
    const l = 116 * o - 16, c = 500 * (r - o), u = 200 * (o - a);
    return [l, c, u];
  }, e.lab.xyz = function(s) {
    const r = s[0], o = s[1], a = s[2];
    let l, c, u;
    c = (r + 16) / 116, l = o / 500 + c, u = c - a / 200;
    const d = c ** 3, h = l ** 3, m = u ** 3;
    return c = d > 8856e-6 ? d : (c - 16 / 116) / 7.787, l = h > 8856e-6 ? h : (l - 16 / 116) / 7.787, u = m > 8856e-6 ? m : (u - 16 / 116) / 7.787, l *= 95.047, c *= 100, u *= 108.883, [l, c, u];
  }, e.lab.lch = function(s) {
    const r = s[0], o = s[1], a = s[2];
    let l;
    l = Math.atan2(a, o) * 360 / 2 / Math.PI, l < 0 && (l += 360);
    const u = Math.sqrt(o * o + a * a);
    return [r, u, l];
  }, e.lch.lab = function(s) {
    const r = s[0], o = s[1], l = s[2] / 360 * 2 * Math.PI, c = o * Math.cos(l), u = o * Math.sin(l);
    return [r, c, u];
  }, e.rgb.ansi16 = function(s, r = null) {
    const [o, a, l] = s;
    let c = r === null ? e.rgb.hsv(s)[2] : r;
    if (c = Math.round(c / 50), c === 0)
      return 30;
    let u = 30 + (Math.round(l / 255) << 2 | Math.round(a / 255) << 1 | Math.round(o / 255));
    return c === 2 && (u += 60), u;
  }, e.hsv.ansi16 = function(s) {
    return e.rgb.ansi16(e.hsv.rgb(s), s[2]);
  }, e.rgb.ansi256 = function(s) {
    const r = s[0], o = s[1], a = s[2];
    return r === o && o === a ? r < 8 ? 16 : r > 248 ? 231 : Math.round((r - 8) / 247 * 24) + 232 : 16 + 36 * Math.round(r / 255 * 5) + 6 * Math.round(o / 255 * 5) + Math.round(a / 255 * 5);
  }, e.ansi16.rgb = function(s) {
    let r = s % 10;
    if (r === 0 || r === 7)
      return s > 50 && (r += 3.5), r = r / 10.5 * 255, [r, r, r];
    const o = (~~(s > 50) + 1) * 0.5, a = (r & 1) * o * 255, l = (r >> 1 & 1) * o * 255, c = (r >> 2 & 1) * o * 255;
    return [a, l, c];
  }, e.ansi256.rgb = function(s) {
    if (s >= 232) {
      const c = (s - 232) * 10 + 8;
      return [c, c, c];
    }
    s -= 16;
    let r;
    const o = Math.floor(s / 36) / 5 * 255, a = Math.floor((r = s % 36) / 6) / 5 * 255, l = r % 6 / 5 * 255;
    return [o, a, l];
  }, e.rgb.hex = function(s) {
    const o = (((Math.round(s[0]) & 255) << 16) + ((Math.round(s[1]) & 255) << 8) + (Math.round(s[2]) & 255)).toString(16).toUpperCase();
    return "000000".substring(o.length) + o;
  }, e.hex.rgb = function(s) {
    const r = s.toString(16).match(/[a-f0-9]{6}|[a-f0-9]{3}/i);
    if (!r)
      return [0, 0, 0];
    let o = r[0];
    r[0].length === 3 && (o = o.split("").map((d) => d + d).join(""));
    const a = parseInt(o, 16), l = a >> 16 & 255, c = a >> 8 & 255, u = a & 255;
    return [l, c, u];
  }, e.rgb.hcg = function(s) {
    const r = s[0] / 255, o = s[1] / 255, a = s[2] / 255, l = Math.max(Math.max(r, o), a), c = Math.min(Math.min(r, o), a), u = l - c;
    let d, h;
    return u < 1 ? d = c / (1 - u) : d = 0, u <= 0 ? h = 0 : l === r ? h = (o - a) / u % 6 : l === o ? h = 2 + (a - r) / u : h = 4 + (r - o) / u, h /= 6, h %= 1, [h * 360, u * 100, d * 100];
  }, e.hsl.hcg = function(s) {
    const r = s[1] / 100, o = s[2] / 100, a = o < 0.5 ? 2 * r * o : 2 * r * (1 - o);
    let l = 0;
    return a < 1 && (l = (o - 0.5 * a) / (1 - a)), [s[0], a * 100, l * 100];
  }, e.hsv.hcg = function(s) {
    const r = s[1] / 100, o = s[2] / 100, a = r * o;
    let l = 0;
    return a < 1 && (l = (o - a) / (1 - a)), [s[0], a * 100, l * 100];
  }, e.hcg.rgb = function(s) {
    const r = s[0] / 360, o = s[1] / 100, a = s[2] / 100;
    if (o === 0)
      return [a * 255, a * 255, a * 255];
    const l = [0, 0, 0], c = r % 1 * 6, u = c % 1, d = 1 - u;
    let h = 0;
    switch (Math.floor(c)) {
      case 0:
        l[0] = 1, l[1] = u, l[2] = 0;
        break;
      case 1:
        l[0] = d, l[1] = 1, l[2] = 0;
        break;
      case 2:
        l[0] = 0, l[1] = 1, l[2] = u;
        break;
      case 3:
        l[0] = 0, l[1] = d, l[2] = 1;
        break;
      case 4:
        l[0] = u, l[1] = 0, l[2] = 1;
        break;
      default:
        l[0] = 1, l[1] = 0, l[2] = d;
    }
    return h = (1 - o) * a, [
      (o * l[0] + h) * 255,
      (o * l[1] + h) * 255,
      (o * l[2] + h) * 255
    ];
  }, e.hcg.hsv = function(s) {
    const r = s[1] / 100, o = s[2] / 100, a = r + o * (1 - r);
    let l = 0;
    return a > 0 && (l = r / a), [s[0], l * 100, a * 100];
  }, e.hcg.hsl = function(s) {
    const r = s[1] / 100, a = s[2] / 100 * (1 - r) + 0.5 * r;
    let l = 0;
    return a > 0 && a < 0.5 ? l = r / (2 * a) : a >= 0.5 && a < 1 && (l = r / (2 * (1 - a))), [s[0], l * 100, a * 100];
  }, e.hcg.hwb = function(s) {
    const r = s[1] / 100, o = s[2] / 100, a = r + o * (1 - r);
    return [s[0], (a - r) * 100, (1 - a) * 100];
  }, e.hwb.hcg = function(s) {
    const r = s[1] / 100, a = 1 - s[2] / 100, l = a - r;
    let c = 0;
    return l < 1 && (c = (a - l) / (1 - l)), [s[0], l * 100, c * 100];
  }, e.apple.rgb = function(s) {
    return [s[0] / 65535 * 255, s[1] / 65535 * 255, s[2] / 65535 * 255];
  }, e.rgb.apple = function(s) {
    return [s[0] / 255 * 65535, s[1] / 255 * 65535, s[2] / 255 * 65535];
  }, e.gray.rgb = function(s) {
    return [s[0] / 100 * 255, s[0] / 100 * 255, s[0] / 100 * 255];
  }, e.gray.hsl = function(s) {
    return [0, 0, s[0]];
  }, e.gray.hsv = e.gray.hsl, e.gray.hwb = function(s) {
    return [0, 100, s[0]];
  }, e.gray.cmyk = function(s) {
    return [0, 0, 0, s[0]];
  }, e.gray.lab = function(s) {
    return [s[0], 0, 0];
  }, e.gray.hex = function(s) {
    const r = Math.round(s[0] / 100 * 255) & 255, a = ((r << 16) + (r << 8) + r).toString(16).toUpperCase();
    return "000000".substring(a.length) + a;
  }, e.rgb.gray = function(s) {
    return [(s[0] + s[1] + s[2]) / 3 / 255 * 100];
  }, Zu;
}
var Yu, cb;
function XV() {
  if (cb) return Yu;
  cb = 1;
  const n = wy();
  function t() {
    const r = {}, o = Object.keys(n);
    for (let a = o.length, l = 0; l < a; l++)
      r[o[l]] = {
        // http://jsperf.com/1-vs-infinity
        // micro-opt, but this is simple.
        distance: -1,
        parent: null
      };
    return r;
  }
  function e(r) {
    const o = t(), a = [r];
    for (o[r].distance = 0; a.length; ) {
      const l = a.pop(), c = Object.keys(n[l]);
      for (let u = c.length, d = 0; d < u; d++) {
        const h = c[d], m = o[h];
        m.distance === -1 && (m.distance = o[l].distance + 1, m.parent = l, a.unshift(h));
      }
    }
    return o;
  }
  function i(r, o) {
    return function(a) {
      return o(r(a));
    };
  }
  function s(r, o) {
    const a = [o[r].parent, r];
    let l = n[o[r].parent][r], c = o[r].parent;
    for (; o[c].parent; )
      a.unshift(o[c].parent), l = i(n[o[c].parent][c], l), c = o[c].parent;
    return l.conversion = a, l;
  }
  return Yu = function(r) {
    const o = e(r), a = {}, l = Object.keys(o);
    for (let c = l.length, u = 0; u < c; u++) {
      const d = l[u];
      o[d].parent !== null && (a[d] = s(d, o));
    }
    return a;
  }, Yu;
}
var Ju, ub;
function QV() {
  if (ub) return Ju;
  ub = 1;
  const n = wy(), t = XV(), e = {}, i = Object.keys(n);
  function s(o) {
    const a = function(...l) {
      const c = l[0];
      return c == null ? c : (c.length > 1 && (l = c), o(l));
    };
    return "conversion" in o && (a.conversion = o.conversion), a;
  }
  function r(o) {
    const a = function(...l) {
      const c = l[0];
      if (c == null)
        return c;
      c.length > 1 && (l = c);
      const u = o(l);
      if (typeof u == "object")
        for (let d = u.length, h = 0; h < d; h++)
          u[h] = Math.round(u[h]);
      return u;
    };
    return "conversion" in o && (a.conversion = o.conversion), a;
  }
  return i.forEach((o) => {
    e[o] = {}, Object.defineProperty(e[o], "channels", { value: n[o].channels }), Object.defineProperty(e[o], "labels", { value: n[o].labels });
    const a = t(o);
    Object.keys(a).forEach((c) => {
      const u = a[c];
      e[o][c] = r(u), e[o][c].raw = s(u);
    });
  }), Ju = e, Ju;
}
var vy = QV();
const eL = /* @__PURE__ */ Rc(vy), _y = /* @__PURE__ */ $2({
  __proto__: null,
  default: eL
}, [vy]), da = (n, t = 0, e = 1) => n > e ? e : n < t ? t : n, Wt = (n, t = 0, e = Math.pow(10, t)) => Math.round(e * n) / e, tL = (n) => oL(ph(n)), ph = (n) => (n[0] === "#" && (n = n.substring(1)), n.length < 6 ? {
  r: parseInt(n[0] + n[0], 16),
  g: parseInt(n[1] + n[1], 16),
  b: parseInt(n[2] + n[2], 16),
  a: n.length === 4 ? Wt(parseInt(n[3] + n[3], 16) / 255, 2) : 1
} : {
  r: parseInt(n.substring(0, 2), 16),
  g: parseInt(n.substring(2, 4), 16),
  b: parseInt(n.substring(4, 6), 16),
  a: n.length === 8 ? Wt(parseInt(n.substring(6, 8), 16) / 255, 2) : 1
}), iL = (n) => rL(sL(n)), nL = ({ h: n, s: t, v: e, a: i }) => {
  const s = (200 - t) * e / 100;
  return {
    h: Wt(n),
    s: Wt(s > 0 && s < 200 ? t * e / 100 / (s <= 100 ? s : 200 - s) * 100 : 0),
    l: Wt(s / 2),
    a: Wt(i, 2)
  };
}, bh = (n) => {
  const { h: t, s: e, l: i } = nL(n);
  return `hsl(${t}, ${e}%, ${i}%)`;
}, sL = ({ h: n, s: t, v: e, a: i }) => {
  n = n / 360 * 6, t = t / 100, e = e / 100;
  const s = Math.floor(n), r = e * (1 - t), o = e * (1 - (n - s) * t), a = e * (1 - (1 - n + s) * t), l = s % 6;
  return {
    r: Wt([e, o, r, r, a, e][l] * 255),
    g: Wt([a, e, e, o, r, r][l] * 255),
    b: Wt([r, r, a, e, e, o][l] * 255),
    a: Wt(i, 2)
  };
}, ll = (n) => {
  const t = n.toString(16);
  return t.length < 2 ? "0" + t : t;
}, rL = ({ r: n, g: t, b: e, a: i }) => {
  const s = i < 1 ? ll(Wt(i * 255)) : "";
  return "#" + ll(n) + ll(t) + ll(e) + s;
}, oL = ({ r: n, g: t, b: e, a: i }) => {
  const s = Math.max(n, t, e), r = s - Math.min(n, t, e), o = r ? s === n ? (t - e) / r : s === t ? 2 + (e - n) / r : 4 + (n - t) / r : 0;
  return {
    h: Wt(60 * (o < 0 ? o + 6 : o)),
    s: Wt(s ? r / s * 100 : 0),
    v: Wt(s / 255 * 100),
    a: i
  };
}, yy = (n, t) => {
  if (n === t)
    return !0;
  for (const e in n)
    if (n[e] !== t[e])
      return !1;
  return !0;
}, aL = (n, t) => n.toLowerCase() === t.toLowerCase() ? !0 : yy(ph(n), ph(t)), db = {}, ky = (n) => {
  let t = db[n];
  return t || (t = document.createElement("template"), t.innerHTML = n, db[n] = t), t;
}, Nf = (n, t, e) => {
  n.dispatchEvent(new CustomEvent(t, {
    bubbles: !0,
    detail: e
  }));
};
let Mr = !1;
const wh = (n) => "touches" in n, lL = (n) => Mr && !wh(n) ? !1 : (Mr || (Mr = wh(n)), !0), hb = (n, t) => {
  const e = wh(t) ? t.touches[0] : t, i = n.el.getBoundingClientRect();
  Nf(n.el, "move", n.getMove({
    x: da((e.pageX - (i.left + window.pageXOffset)) / i.width),
    y: da((e.pageY - (i.top + window.pageYOffset)) / i.height)
  }));
}, cL = (n, t) => {
  const e = t.keyCode;
  e > 40 || n.xy && e < 37 || e < 33 || (t.preventDefault(), Nf(n.el, "move", n.getMove({
    x: e === 39 ? 0.01 : e === 37 ? -0.01 : e === 34 ? 0.05 : e === 33 ? -0.05 : e === 35 ? 1 : e === 36 ? -1 : 0,
    y: e === 40 ? 0.01 : e === 38 ? -0.01 : 0
  }, !0)));
};
let Cy = class {
  constructor(t, e, i, s) {
    const r = ky(`<div role="slider" tabindex="0" part="${e}" ${i}><div part="${e}-pointer"></div></div>`);
    t.appendChild(r.content.cloneNode(!0));
    const o = t.querySelector(`[part=${e}]`);
    o.addEventListener("mousedown", this), o.addEventListener("touchstart", this), o.addEventListener("keydown", this), this.el = o, this.xy = s, this.nodes = [o.firstChild, o];
  }
  set dragging(t) {
    const e = t ? document.addEventListener : document.removeEventListener;
    e(Mr ? "touchmove" : "mousemove", this), e(Mr ? "touchend" : "mouseup", this);
  }
  handleEvent(t) {
    switch (t.type) {
      case "mousedown":
      case "touchstart":
        if (t.preventDefault(), !lL(t) || !Mr && t.button != 0)
          return;
        this.el.focus(), hb(this, t), this.dragging = !0;
        break;
      case "mousemove":
      case "touchmove":
        t.preventDefault(), hb(this, t);
        break;
      case "mouseup":
      case "touchend":
        this.dragging = !1;
        break;
      case "keydown":
        cL(this, t);
        break;
    }
  }
  style(t) {
    t.forEach((e, i) => {
      for (const s in e)
        this.nodes[i].style.setProperty(s, e[s]);
    });
  }
};
class uL extends Cy {
  constructor(t) {
    super(t, "hue", 'aria-label="Hue" aria-valuemin="0" aria-valuemax="360"', !1);
  }
  update({ h: t }) {
    this.h = t, this.style([
      {
        left: `${t / 360 * 100}%`,
        color: bh({ h: t, s: 100, v: 100, a: 1 })
      }
    ]), this.el.setAttribute("aria-valuenow", `${Wt(t)}`);
  }
  getMove(t, e) {
    return { h: e ? da(this.h + t.x * 360, 0, 360) : 360 * t.x };
  }
}
class dL extends Cy {
  constructor(t) {
    super(t, "saturation", 'aria-label="Color"', !0);
  }
  update(t) {
    this.hsva = t, this.style([
      {
        top: `${100 - t.v}%`,
        left: `${t.s}%`,
        color: bh(t)
      },
      {
        "background-color": bh({ h: t.h, s: 100, v: 100, a: 1 })
      }
    ]), this.el.setAttribute("aria-valuetext", `Saturation ${Wt(t.s)}%, Brightness ${Wt(t.v)}%`);
  }
  getMove(t, e) {
    return {
      s: e ? da(this.hsva.s + t.x * 100, 0, 100) : t.x * 100,
      v: e ? da(this.hsva.v - t.y * 100, 0, 100) : Math.round(100 - t.y * 100)
    };
  }
}
const hL = ':host{display:flex;flex-direction:column;position:relative;width:200px;height:200px;user-select:none;-webkit-user-select:none;cursor:default}:host([hidden]){display:none!important}[role=slider]{position:relative;touch-action:none;user-select:none;-webkit-user-select:none;outline:0}[role=slider]:last-child{border-radius:0 0 8px 8px}[part$=pointer]{position:absolute;z-index:1;box-sizing:border-box;width:28px;height:28px;display:flex;place-content:center center;transform:translate(-50%,-50%);background-color:#fff;border:2px solid #fff;border-radius:50%;box-shadow:0 2px 4px rgba(0,0,0,.2)}[part$=pointer]::after{content:"";width:100%;height:100%;border-radius:inherit;background-color:currentColor}[role=slider]:focus [part$=pointer]{transform:translate(-50%,-50%) scale(1.1)}', fL = "[part=hue]{flex:0 0 24px;background:linear-gradient(to right,red 0,#ff0 17%,#0f0 33%,#0ff 50%,#00f 67%,#f0f 83%,red 100%)}[part=hue-pointer]{top:50%;z-index:2}", mL = "[part=saturation]{flex-grow:1;border-color:transparent;border-bottom:12px solid #000;border-radius:8px 8px 0 0;background-image:linear-gradient(to top,#000,transparent),linear-gradient(to right,#fff,rgba(255,255,255,0));box-shadow:inset 0 0 0 1px rgba(0,0,0,.05)}[part=saturation-pointer]{z-index:3}", cl = Symbol("same"), Xu = Symbol("color"), fb = Symbol("hsva"), Qu = Symbol("update"), mb = Symbol("parts"), gb = Symbol("css"), pb = Symbol("sliders");
class gL extends HTMLElement {
  static get observedAttributes() {
    return ["color"];
  }
  get [gb]() {
    return [hL, fL, mL];
  }
  get [pb]() {
    return [dL, uL];
  }
  get color() {
    return this[Xu];
  }
  set color(t) {
    if (!this[cl](t)) {
      const e = this.colorModel.toHsva(t);
      this[Qu](e), this[Xu] = t;
    }
  }
  constructor() {
    super();
    const t = ky(`<style>${this[gb].join("")}</style>`), e = this.attachShadow({ mode: "open" });
    e.appendChild(t.content.cloneNode(!0)), e.addEventListener("move", this), this[mb] = this[pb].map((i) => new i(e));
  }
  connectedCallback() {
    if (this.hasOwnProperty("color")) {
      const t = this.color;
      delete this.color, this.color = t;
    } else this.color || (this.color = this.colorModel.defaultColor);
  }
  attributeChangedCallback(t, e, i) {
    const s = this.colorModel.fromAttr(i);
    this[cl](s) || (this.color = s);
  }
  handleEvent(t) {
    const e = this[fb], i = { ...e, ...t.detail };
    this[Qu](i);
    let s;
    !yy(i, e) && !this[cl](s = this.colorModel.fromHsva(i)) && (this[Xu] = s, Nf(this, "color-changed", { value: s }));
  }
  [cl](t) {
    return this.color && this.colorModel.equal(t, this.color);
  }
  [Qu](t) {
    this[fb] = t, this[mb].forEach((e) => e.update(t));
  }
}
const pL = {
  defaultColor: "#000",
  toHsva: tL,
  fromHsva: ({ h: n, s: t, v: e }) => iL({ h: n, s: t, v: e, a: 1 }),
  equal: aL,
  fromAttr: (n) => n
};
class bL extends gL {
  get colorModel() {
    return pL;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class Et extends bt {
  /**
  * Creates a new instance of the {@link module:ui/viewcollection~ViewCollection}.
  *
  * @param initialItems The initial items of the collection.
  */
  constructor(e = []) {
    super(e, {
      // An #id Number attribute should be legal and not break the `ViewCollection` instance.
      // https://github.com/ckeditor/ckeditor5-ui/issues/93
      idProperty: "viewUid"
    });
    /**
    * A parent element within which child views are rendered and managed in DOM.
    */
    f(this, "_parentElement");
    this.on("add", (i, s, r) => {
      this._renderViewIntoCollectionParent(s, r);
    }), this.on("remove", (i, s) => {
      s.element && this._parentElement && s.element.remove();
    }), this._parentElement = null;
  }
  /**
  * Destroys the view collection along with child views.
  * See the view {@link module:ui/view~View#destroy} method.
  */
  destroy() {
    this.map((e) => e.destroy());
  }
  /**
  * Sets the parent HTML element of this collection. When parent is set, {@link #add adding} and
  * {@link #remove removing} views in the collection synchronizes their
  * {@link module:ui/view~View#element elements} in the parent element.
  *
  * @param element A new parent element.
  */
  setParent(e) {
    this._parentElement = e;
    for (const i of this)
      this._renderViewIntoCollectionParent(i);
  }
  /**
  * Delegates selected events coming from within views in the collection to any
  * {@link module:utils/emittermixin~Emitter}.
  *
  * For the following views and collection:
  *
  * ```ts
  * const viewA = new View();
  * const viewB = new View();
  * const viewC = new View();
  *
  * const views = parentView.createCollection();
  *
  * views.delegate( 'eventX' ).to( viewB );
  * views.delegate( 'eventX', 'eventY' ).to( viewC );
  *
  * views.add( viewA );
  * ```
  *
  * the `eventX` is delegated (fired by) `viewB` and `viewC` along with `customData`:
  *
  * ```ts
  * viewA.fire( 'eventX', customData );
  * ```
  *
  * and `eventY` is delegated (fired by) `viewC` along with `customData`:
  *
  * ```ts
  * viewA.fire( 'eventY', customData );
  * ```
  *
  * See {@link module:utils/emittermixin~Emitter#delegate}.
  *
  * @param events {@link module:ui/view~View} event names to be delegated to another
  * {@link module:utils/emittermixin~Emitter}.
  * @returns Object with `to` property, a function which accepts the destination
  * of {@link module:utils/emittermixin~Emitter#delegate delegated} events.
  */
  delegate(...e) {
    if (!e.length || !wL(e))
      throw new S("ui-viewcollection-delegate-wrong-events", this);
    return {
      to: (i) => {
        for (const s of this)
          for (const r of e)
            s.delegate(r).to(i);
        this.on("add", (s, r) => {
          for (const o of e)
            r.delegate(o).to(i);
        }), this.on("remove", (s, r) => {
          for (const o of e)
            r.stopDelegating(o, i);
        });
      }
    };
  }
  /**
  * This method {@link module:ui/view~View#render renders} a new view added to the collection.
  *
  * If the {@link #_parentElement parent element} of the collection is set, this method also adds
  * the view's {@link module:ui/view~View#element} as a child of the parent in DOM at a specified index.
  *
  * **Note**: If index is not specified, the view's element is pushed as the last child
  * of the parent element.
  *
  * @param view A new view added to the collection.
  * @param index An index the view holds in the collection. When not specified,
  * the view is added at the end.
  */
  _renderViewIntoCollectionParent(e, i) {
    e.isRendered || e.render(), e.element && this._parentElement && this._parentElement.insertBefore(e.element, this._parentElement.children[i]);
  }
  /**
  * Removes a child view from the collection. If the {@link #setParent parent element} of the
  * collection has been set, the {@link module:ui/view~View#element element} of the view is also removed
  * in DOM, reflecting the order of the collection.
  *
  * See the {@link #add} method.
  *
  * @param subject The view to remove, its id or index in the collection.
  * @returns The removed view.
  */
  remove(e) {
    return super.remove(e);
  }
}
function wL(n) {
  return n.every((t) => typeof t == "string");
}
const vL = "http://www.w3.org/1999/xhtml";
class ri extends (/* @__PURE__ */ Je()) {
  /**
  * Creates an instance of the {@link ~Template} class.
  *
  * @param def The definition of the template.
  */
  constructor(e) {
    super();
    f(this, "ns");
    /**
    * The tag (`tagName`) of this template, e.g. `div`. It also indicates that the template
    * renders to an HTML element.
    */
    f(this, "tag");
    /**
    * The text of the template. It also indicates that the template renders to a DOM text node.
    */
    f(this, "text");
    /**
    * The attributes of the template, e.g. `{ id: [ 'ck-id' ] }`, corresponding with
    * the attributes of an HTML element.
    *
    * **Note**: This property only makes sense when {@link #tag} is defined.
    */
    f(this, "attributes");
    /**
    * The children of the template. They can be either:
    * * independent instances of {@link ~Template} (sub–templates),
    * * native DOM Nodes.
    *
    * **Note**: This property only makes sense when {@link #tag} is defined.
    */
    f(this, "children");
    /**
    * The DOM event listeners of the template.
    */
    f(this, "eventListeners");
    /**
    * Indicates whether this particular Template instance has been
    * {@link #render rendered}.
    */
    f(this, "_isRendered");
    /**
    * The data used by the {@link #revert} method to restore a node to its original state.
    *
    * See: {@link #apply}.
    */
    f(this, "_revertData");
    Object.assign(this, wb(bb(e))), this._isRendered = !1, this._revertData = null;
  }
  /**
  * Renders a DOM Node (an HTML element or text) out of the template.
  *
  * ```ts
  * const domNode = new Template( { ... } ).render();
  * ```
  *
  * See: {@link #apply}.
  */
  render() {
    const e = this._renderNode({
      intoFragment: !0
    });
    return this._isRendered = !0, e;
  }
  /**
  * Applies the template to an existing DOM Node, either HTML element or text.
  *
  * **Note:** No new DOM nodes will be created. Applying extends:
  *
  * {@link module:ui/template~TemplateDefinition attributes},
  * {@link module:ui/template~TemplateDefinition event listeners}, and
  * `textContent` of {@link module:ui/template~TemplateDefinition children} only.
  *
  * **Note:** Existing `class` and `style` attributes are extended when a template
  * is applied to an HTML element, while other attributes and `textContent` are overridden.
  *
  * **Note:** The process of applying a template can be easily reverted using the
  * {@link module:ui/template~Template#revert} method.
  *
  * ```ts
  * const element = document.createElement( 'div' );
  * const observable = new Model( { divClass: 'my-div' } );
  * const emitter = Object.create( EmitterMixin );
  * const bind = Template.bind( observable, emitter );
  *
  * new Template( {
  * 	attributes: {
  * 		id: 'first-div',
  * 		class: bind.to( 'divClass' )
  * 	},
  * 	on: {
  * 		click: bind( 'elementClicked' ) // Will be fired by the observable.
  * 	},
  * 	children: [
  * 		'Div text.'
  * 	]
  * } ).apply( element );
  *
  * console.log( element.outerHTML ); // -> '<div id="first-div" class="my-div"></div>'
  * ```
  *
  * @see module:ui/template~Template#render
  * @see module:ui/template~Template#revert
  * @param node Root node for the template to apply.
  */
  apply(e) {
    return this._revertData = yb(), this._renderNode({
      node: e,
      intoFragment: !1,
      isApplying: !0,
      revertData: this._revertData
    }), e;
  }
  /**
  * Reverts a template {@link module:ui/template~Template#apply applied} to a DOM node.
  *
  * @param node The root node for the template to revert. In most of the cases, it is the
  * same node used by {@link module:ui/template~Template#apply}.
  */
  revert(e) {
    if (!this._revertData)
      throw new S("ui-template-revert-not-applied", [
        this,
        e
      ]);
    this._revertTemplateFromNode(e, this._revertData);
  }
  /**
  * Returns an iterator which traverses the template in search of {@link module:ui/view~View}
  * instances and returns them one by one.
  *
  * ```ts
  * const viewFoo = new View();
  * const viewBar = new View();
  * const viewBaz = new View();
  * const template = new Template( {
  * 	tag: 'div',
  * 	children: [
  * 		viewFoo,
  * 		{
  * 			tag: 'div',
  * 			children: [
  * 				viewBar
  * 			]
  * 		},
  * 		viewBaz
  * 	]
  * } );
  *
  * // Logs: viewFoo, viewBar, viewBaz
  * for ( const view of template.getViews() ) {
  * 	console.log( view );
  * }
  * ```
  */
  *getViews() {
    function* e(i) {
      if (i.children)
        for (const s of i.children)
          ic(s) ? yield s : Ff(s) && (yield* e(s));
    }
    yield* e(this);
  }
  /**
  * An entry point to the interface which binds DOM nodes to
  * {@link module:utils/observablemixin~Observable observables}.
  * There are two types of bindings:
  *
  * * HTML element attributes or text `textContent` synchronized with attributes of an
  * {@link module:utils/observablemixin~Observable}. Learn more about {@link module:ui/template~BindChain#to}
  * and {@link module:ui/template~BindChain#if}.
  *
  * ```ts
  * const bind = Template.bind( observable, emitter );
  *
  * new Template( {
  * 	attributes: {
  * 		// Binds the element "class" attribute to observable#classAttribute.
  * 		class: bind.to( 'classAttribute' )
  * 	}
  * } ).render();
  * ```
  *
  * * DOM events fired on HTML element propagated through
  * {@link module:utils/observablemixin~Observable}. Learn more about {@link module:ui/template~BindChain#to}.
  *
  * ```ts
  * const bind = Template.bind( observable, emitter );
  *
  * new Template( {
  * 	on: {
  * 		// Will be fired by the observable.
  * 		click: bind( 'elementClicked' )
  * 	}
  * } ).render();
  * ```
  *
  * Also see {@link module:ui/view~View#bindTemplate}.
  *
  * @param observable An observable which provides boundable attributes.
  * @param emitter An emitter that listens to observable attribute
  * changes or DOM Events (depending on the kind of the binding). Usually, a {@link module:ui/view~View} instance.
  */
  static bind(e, i) {
    return {
      to(s, r) {
        return new _L({
          eventNameOrFunction: s,
          attribute: s,
          observable: e,
          emitter: i,
          callback: r
        });
      },
      if(s, r, o) {
        return new xy({
          observable: e,
          emitter: i,
          attribute: s,
          valueIfTrue: r,
          callback: o
        });
      }
    };
  }
  /**
  * Extends an existing {@link module:ui/template~Template} instance with some additional content
  * from another {@link module:ui/template~TemplateDefinition}.
  *
  * ```ts
  * const bind = Template.bind( observable, emitter );
  *
  * const template = new Template( {
  * 	tag: 'p',
  * 	attributes: {
  * 		class: 'a',
  * 		data-x: bind.to( 'foo' )
  * 	},
  * 	children: [
  * 		{
  * 			tag: 'span',
  * 			attributes: {
  * 				class: 'b'
  * 			},
  * 			children: [
  * 				'Span'
  * 			]
  * 		}
  * 	]
  *  } );
  *
  * // Instance-level extension.
  * Template.extend( template, {
  * 	attributes: {
  * 		class: 'b',
  * 		data-x: bind.to( 'bar' )
  * 	},
  * 	children: [
  * 		{
  * 			attributes: {
  * 				class: 'c'
  * 			}
  * 		}
  * 	]
  * } );
  *
  * // Child extension.
  * Template.extend( template.children[ 0 ], {
  * 	attributes: {
  * 		class: 'd'
  * 	}
  * } );
  * ```
  *
  * the `outerHTML` of `template.render()` is:
  *
  * ```html
  * <p class="a b" data-x="{ observable.foo } { observable.bar }">
  * 	<span class="b c d">Span</span>
  * </p>
  * ```
  *
  * @param template An existing template instance to be extended.
  * @param def Additional definition to be applied to a template.
  */
  static extend(e, i) {
    if (e._isRendered)
      throw new S("template-extend-render", [
        this,
        e
      ]);
    Ay(e, wb(bb(i)));
  }
  /**
  * Renders a DOM Node (either an HTML element or text) out of the template.
  *
  * @param data Rendering data.
  */
  _renderNode(e) {
    let i;
    if (e.node ? i = this.tag && this.text : i = this.tag ? this.text : !this.text, i)
      throw new S("ui-template-wrong-syntax", this);
    return this.text ? this._renderText(e) : this._renderElement(e);
  }
  /**
  * Renders an HTML element out of the template.
  *
  * @param data Rendering data.
  */
  _renderElement(e) {
    let i = e.node;
    return i || (i = e.node = document.createElementNS(this.ns || vL, this.tag)), this._renderAttributes(e), this._renderElementChildren(e), this._setUpListeners(e), i;
  }
  /**
  * Renders a text node out of {@link module:ui/template~Template#text}.
  *
  * @param data Rendering data.
  */
  _renderText(e) {
    let i = e.node;
    return i ? e.revertData.text = i.textContent : i = e.node = document.createTextNode(""), Ol(this.text) ? this._bindToObservable({
      schema: this.text,
      updater: kL(i),
      data: e
    }) : i.textContent = this.text.join(""), i;
  }
  /**
  * Renders HTML element attributes out of {@link module:ui/template~Template#attributes}.
  *
  * @param data Rendering data.
  */
  _renderAttributes(e) {
    if (!this.attributes)
      return;
    const i = e.node, s = e.revertData;
    for (const r in this.attributes) {
      const o = i.getAttribute(r), a = this.attributes[r];
      s && (s.attributes[r] = o);
      const l = _b(a) ? a[0].ns : null;
      if (Ol(a)) {
        const c = _b(a) ? a[0].value : a;
        s && kb(r) && c.unshift(o), this._bindToObservable({
          schema: c,
          updater: CL(i, r, l),
          data: e
        });
      } else if (r == "style" && typeof a[0] != "string")
        this._renderStyleAttribute(a[0], e);
      else {
        s && o && kb(r) && a.unshift(o);
        const c = a.map((u) => u && (u.value || u)).reduce((u, d) => u.concat(d), []).reduce(Sy, "");
        jr(c) || i.setAttributeNS(l, r, c);
      }
    }
  }
  /**
  * Renders the `style` attribute of an HTML element based on
  * {@link module:ui/template~Template#attributes}.
  *
  * A style attribute is an object with static values:
  *
  * ```ts
  * attributes: {
  * 	style: {
  * 		color: 'red'
  * 	}
  * }
  * ```
  *
  * or values bound to {@link module:ui/model~Model} properties:
  *
  * ```ts
  * attributes: {
  * 	style: {
  * 		color: bind.to( ... )
  * 	}
  * }
  * ```
  *
  * Note: The `style` attribute is rendered without setting the namespace. It does not seem to be
  * needed.
  *
  * @param styles Styles located in `attributes.style` of {@link module:ui/template~TemplateDefinition}.
  * @param data Rendering data.
  */
  _renderStyleAttribute(e, i) {
    const s = i.node;
    for (const r in e) {
      const o = e[r];
      Ol(o) ? this._bindToObservable({
        schema: [
          o
        ],
        updater: xL(s, r),
        data: i
      }) : s.style[r] = o;
    }
  }
  /**
  * Recursively renders HTML element's children from {@link module:ui/template~Template#children}.
  *
  * @param data Rendering data.
  */
  _renderElementChildren(e) {
    const i = e.node, s = e.intoFragment ? document.createDocumentFragment() : i, r = e.isApplying;
    let o = 0;
    for (const a of this.children)
      if (Df(a)) {
        if (!r) {
          a.setParent(i);
          for (const l of a)
            s.appendChild(l.element);
        }
      } else if (ic(a))
        r || (a.isRendered || a.render(), s.appendChild(a.element));
      else if (fs(a))
        s.appendChild(a);
      else if (r) {
        const l = e.revertData, c = yb();
        l.children.push(c), a._renderNode({
          intoFragment: !1,
          node: s.childNodes[o++],
          isApplying: !0,
          revertData: c
        });
      } else
        s.appendChild(a.render());
    e.intoFragment && i.appendChild(s);
  }
  /**
  * Activates `on` event listeners from the {@link module:ui/template~TemplateDefinition}
  * on an HTML element.
  *
  * @param data Rendering data.
  */
  _setUpListeners(e) {
    if (this.eventListeners)
      for (const i in this.eventListeners) {
        const s = this.eventListeners[i].map((r) => {
          const [o, a] = i.split("@");
          return r.activateDomEventListener(o, a, e);
        });
        e.revertData && e.revertData.bindings.push(s);
      }
  }
  /**
  * For a given {@link module:ui/template~TemplateValueSchema} containing {@link module:ui/template~TemplateBinding}
  * activates the binding and sets its initial value.
  *
  * Note: {@link module:ui/template~TemplateValueSchema} can be for HTML element attributes or
  * text node `textContent`.
  *
  * @param options Binding options.
  * @param options.updater A function which updates the DOM (like attribute or text).
  * @param options.data Rendering data.
  */
  _bindToObservable({ schema: e, updater: i, data: s }) {
    const r = s.revertData;
    Ty(e, i, s);
    const o = e.filter((a) => !jr(a)).filter((a) => a.observable).map((a) => a.activateAttributeListener(e, i, s));
    r && r.bindings.push(o);
  }
  /**
  * Reverts {@link module:ui/template~RenderData#revertData template data} from a node to
  * return it to the original state.
  *
  * @param node A node to be reverted.
  * @param revertData An object that stores information about what changes have been made by
  * {@link #apply} to the node. See {@link module:ui/template~RenderData#revertData} for more information.
  */
  _revertTemplateFromNode(e, i) {
    for (const r of i.bindings)
      for (const o of r)
        o();
    if (i.text) {
      e.textContent = i.text;
      return;
    }
    const s = e;
    for (const r in i.attributes) {
      const o = i.attributes[r];
      o === null ? s.removeAttribute(r) : s.setAttribute(r, o);
    }
    for (let r = 0; r < i.children.length; ++r)
      this._revertTemplateFromNode(s.childNodes[r], i.children[r]);
  }
}
class Ra {
  /**
  * Creates an instance of the {@link module:ui/template~TemplateBinding} class.
  *
  * @param def The definition of the binding.
  */
  constructor(t) {
    /**
    * The name of the {@link module:ui/template~TemplateBinding#observable observed attribute}.
    */
    f(this, "attribute");
    /**
    * An observable instance of the binding. It either:
    *
    * * provides the attribute with the value,
    * * or passes the event when a corresponding DOM event is fired.
    */
    f(this, "observable");
    /**
    * An {@link module:utils/emittermixin~Emitter} used by the binding to:
    *
    * * listen to the attribute change in the {@link module:ui/template~TemplateBinding#observable},
    * * or listen to the event in the DOM.
    */
    f(this, "emitter");
    /**
    * A custom function to process the value of the {@link module:ui/template~TemplateBinding#attribute}.
    */
    f(this, "callback");
    this.attribute = t.attribute, this.observable = t.observable, this.emitter = t.emitter, this.callback = t.callback;
  }
  /**
  * Returns the value of the binding. It is the value of the {@link module:ui/template~TemplateBinding#attribute} in
  * {@link module:ui/template~TemplateBinding#observable}. The value may be processed by the
  * {@link module:ui/template~TemplateBinding#callback}, if such has been passed to the binding.
  *
  * @param node A native DOM node, passed to the custom {@link module:ui/template~TemplateBinding#callback}.
  * @returns The value of {@link module:ui/template~TemplateBinding#attribute} in
  * {@link module:ui/template~TemplateBinding#observable}.
  */
  getValue(t) {
    const e = this.observable[this.attribute];
    return this.callback ? this.callback(e, t) : e;
  }
  /**
  * Activates the listener which waits for changes of the {@link module:ui/template~TemplateBinding#attribute} in
  * {@link module:ui/template~TemplateBinding#observable}, then updates the DOM with the aggregated
  * value of {@link module:ui/template~TemplateValueSchema}.
  *
  * @param schema A full schema to generate an attribute or text in the DOM.
  * @param updater A DOM updater function used to update the native DOM attribute or text.
  * @param data Rendering data.
  * @returns A function to sever the listener binding.
  */
  activateAttributeListener(t, e, i) {
    const s = () => Ty(t, e, i);
    return this.emitter.listenTo(this.observable, `change:${this.attribute}`, s), () => {
      this.emitter.stopListening(this.observable, `change:${this.attribute}`, s);
    };
  }
}
class _L extends Ra {
  constructor(e) {
    super(e);
    f(this, "eventNameOrFunction");
    this.eventNameOrFunction = e.eventNameOrFunction;
  }
  /**
  * Activates the listener for the native DOM event, which when fired, is propagated by
  * the {@link module:ui/template~TemplateBinding#emitter}.
  *
  * @param domEvtName The name of the native DOM event.
  * @param domSelector The selector in the DOM to filter delegated events.
  * @param data Rendering data.
  * @returns A function to sever the listener binding.
  */
  activateDomEventListener(e, i, s) {
    const r = (o, a) => {
      (!i || a.target.matches(i)) && (typeof this.eventNameOrFunction == "function" ? this.eventNameOrFunction(a) : this.observable.fire(this.eventNameOrFunction, a));
    };
    return this.emitter.listenTo(s.node, e, r), () => {
      this.emitter.stopListening(s.node, e, r);
    };
  }
}
class xy extends Ra {
  constructor(e) {
    super(e);
    /**
    * The value of the DOM attribute or text to be set if the {@link module:ui/template~TemplateBinding#attribute} in
    * {@link module:ui/template~TemplateBinding#observable} is `true`.
    */
    f(this, "valueIfTrue");
    this.valueIfTrue = e.valueIfTrue;
  }
  /**
  * @inheritDoc
  */
  getValue(e) {
    const i = super.getValue(e);
    return jr(i) ? !1 : this.valueIfTrue || !0;
  }
}
function Ol(n) {
  return n ? (n.value && (n = n.value), Array.isArray(n) ? n.some(Ol) : n instanceof Ra) : !1;
}
function yL(n, t) {
  return n.map((e) => e instanceof Ra ? e.getValue(t) : e);
}
function Ty(n, t, { node: e }) {
  const i = yL(n, e);
  let s;
  n.length == 1 && n[0] instanceof xy ? s = i[0] : s = i.reduce(Sy, ""), jr(s) ? t.remove() : t.set(s);
}
function kL(n) {
  return {
    set(t) {
      n.textContent = t;
    },
    remove() {
      n.textContent = "";
    }
  };
}
function CL(n, t, e) {
  return {
    set(i) {
      n.setAttributeNS(e, t, i);
    },
    remove() {
      n.removeAttributeNS(e, t);
    }
  };
}
function xL(n, t) {
  return {
    set(e) {
      n.style[t] = e;
    },
    remove() {
      n.style[t] = null;
    }
  };
}
function bb(n) {
  return gf(n, (e) => {
    if (e && (e instanceof Ra || Ff(e) || ic(e) || Df(e)))
      return e;
  });
}
function wb(n) {
  if (typeof n == "string" ? n = SL(n) : n.text && AL(n), n.on && (n.eventListeners = EL(n.on), delete n.on), !n.text) {
    n.attributes && TL(n.attributes);
    const t = [];
    if (n.children)
      if (Df(n.children))
        t.push(n.children);
      else
        for (const e of n.children)
          Ff(e) || ic(e) || fs(e) ? t.push(e) : t.push(new ri(e));
    n.children = t;
  }
  return n;
}
function TL(n) {
  for (const t in n)
    n[t].value && (n[t].value = Ue(n[t].value)), Ey(n, t);
}
function EL(n) {
  for (const t in n)
    Ey(n, t);
  return n;
}
function SL(n) {
  return {
    text: [
      n
    ]
  };
}
function AL(n) {
  n.text = Ue(n.text);
}
function Ey(n, t) {
  n[t] = Ue(n[t]);
}
function Sy(n, t) {
  return jr(t) ? n : jr(n) ? t : `${n} ${t}`;
}
function vb(n, t) {
  for (const e in t)
    n[e] ? n[e].push(...t[e]) : n[e] = t[e];
}
function Ay(n, t) {
  if (t.attributes && (n.attributes || (n.attributes = {}), vb(n.attributes, t.attributes)), t.eventListeners && (n.eventListeners || (n.eventListeners = {}), vb(n.eventListeners, t.eventListeners)), t.text && n.text.push(...t.text), t.children && t.children.length) {
    if (n.children.length != t.children.length)
      throw new S("ui-template-extend-children-mismatch", n);
    let e = 0;
    for (const i of t.children)
      Ay(n.children[e++], i);
  }
}
function jr(n) {
  return !n && n !== 0;
}
function ic(n) {
  return n instanceof ee;
}
function Ff(n) {
  return n instanceof ri;
}
function Df(n) {
  return n instanceof Et;
}
function _b(n) {
  return nt(n[0]) && n[0].ns;
}
function yb() {
  return {
    children: [],
    bindings: [],
    attributes: {}
  };
}
function kb(n) {
  return n == "class" || n == "style";
}
class ee extends (/* @__PURE__ */ li(/* @__PURE__ */ je())) {
  /**
  * Creates an instance of the {@link module:ui/view~View} class.
  *
  * Also see {@link #render}.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super();
    /**
    * An HTML element of the view. `null` until {@link #render rendered}
    * from the {@link #template}.
    *
    * ```ts
    * class SampleView extends View {
    * 	constructor() {
    * 		super();
    *
    * 		// A template instance the #element will be created from.
    * 		this.setTemplate( {
    * 			tag: 'p'
    *
    * 			// ...
    * 		} );
    * 	}
    * }
    *
    * const view = new SampleView();
    *
    * // Renders the #template.
    * view.render();
    *
    * // Append the HTML element of the view to <body>.
    * document.body.appendChild( view.element );
    * ```
    *
    * **Note**: The element of the view can also be assigned directly:
    *
    * ```ts
    * view.element = document.querySelector( '#my-container' );
    * ```
    */
    f(this, "element");
    /**
    * Set `true` when the view has already been {@link module:ui/view~View#render rendered}.
    *
    * @readonly
    */
    f(this, "isRendered");
    /**
    * A set of tools to localize the user interface.
    *
    * Also see {@link module:core/editor/editor~Editor#locale}.
    *
    * @readonly
    */
    f(this, "locale");
    /**
    * Shorthand for {@link module:utils/locale~Locale#t}.
    *
    * Note: If {@link #locale} instance hasn't been passed to the view this method may not
    * be available.
    *
    * @see module:utils/locale~Locale#t
    */
    f(this, "t");
    /**
    * Template of this view. It provides the {@link #element} representing
    * the view in DOM, which is {@link #render rendered}.
    */
    f(this, "template");
    /**
    * Collections registered with {@link #createCollection}.
    */
    f(this, "_viewCollections");
    /**
    * A collection of view instances, which have been added directly
    * into the {@link module:ui/template~Template#children}.
    */
    f(this, "_unboundChildren");
    /**
    * Cached {@link module:ui/template~BindChain bind chain} object created by the
    * {@link #template}. See {@link #bindTemplate}.
    */
    f(this, "_bindTemplate");
    this.element = null, this.isRendered = !1, this.locale = e, this.t = e && e.t, this._viewCollections = new bt(), this._unboundChildren = this.createCollection(), this._viewCollections.on("add", (i, s) => {
      s.locale = e, s.t = e && e.t;
    }), this.decorate("render");
  }
  /**
  * Shorthand for {@link module:ui/template~Template.bind}, a binding
  * {@link module:ui/template~BindChain interface} pre–configured for the view instance.
  *
  * It provides {@link module:ui/template~BindChain#to `to()`} and
  * {@link module:ui/template~BindChain#if `if()`} methods that initialize bindings with
  * observable attributes and attach DOM listeners.
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor( locale ) {
  * 		super( locale );
  *
  * 		const bind = this.bindTemplate;
  *
  * 		// These {@link module:utils/observablemixin~Observable observable} attributes will control
  * 		// the state of the view in DOM.
  * 		this.set( {
  * 			elementClass: 'foo',
  * 		 	isEnabled: true
  * 		 } );
  *
  * 		this.setTemplate( {
  * 			tag: 'p',
  *
  * 			attributes: {
  * 				// The class HTML attribute will follow elementClass
  * 				// and isEnabled view attributes.
  * 				class: [
  * 					bind.to( 'elementClass' )
  * 					bind.if( 'isEnabled', 'present-when-enabled' )
  * 				]
  * 			},
  *
  * 			on: {
  * 				// The view will fire the "clicked" event upon clicking <p> in DOM.
  * 				click: bind.to( 'clicked' )
  * 			}
  * 		} );
  * 	}
  * }
  * ```
  */
  get bindTemplate() {
    return this._bindTemplate ? this._bindTemplate : this._bindTemplate = ri.bind(this, this);
  }
  /**
  * Creates a new collection of views, which can be used as
  * {@link module:ui/template~Template#children} of this view.
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor( locale ) {
  * 		super( locale );
  *
  * 		const child = new ChildView( locale );
  * 		this.items = this.createCollection( [ child ] );
   *
  * 		this.setTemplate( {
  * 			tag: 'p',
  *
  * 			// `items` collection will render here.
  * 			children: this.items
  * 		} );
  * 	}
  * }
  *
  * const view = new SampleView( locale );
  * view.render();
  *
  * // It will append <p><child#element></p> to the <body>.
  * document.body.appendChild( view.element );
  * ```
  *
  * @param views Initial views of the collection.
  * @returns A new collection of view instances.
  */
  createCollection(e) {
    const i = new Et(e);
    return this._viewCollections.add(i), i;
  }
  /**
  * Registers a new child view under the view instance. Once registered, a child
  * view is managed by its parent, including {@link #render rendering}
  * and {@link #destroy destruction}.
  *
  * To revert this, use {@link #deregisterChild}.
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor( locale ) {
  * 		super( locale );
  *
  * 		this.childA = new SomeChildView( locale );
  * 		this.childB = new SomeChildView( locale );
  *
  * 		this.setTemplate( { tag: 'p' } );
  *
  * 		// Register the children.
  * 		this.registerChild( [ this.childA, this.childB ] );
  * 	}
  *
  * 	render() {
  * 		super.render();
  *
  * 		this.element.appendChild( this.childA.element );
  * 		this.element.appendChild( this.childB.element );
  * 	}
  * }
  *
  * const view = new SampleView( locale );
  *
  * view.render();
  *
  * // Will append <p><childA#element><b></b><childB#element></p>.
  * document.body.appendChild( view.element );
  * ```
  *
  * **Note**: There's no need to add child views if they're already referenced in the
  * {@link #template}:
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor( locale ) {
  * 		super( locale );
  *
  * 		this.childA = new SomeChildView( locale );
  * 		this.childB = new SomeChildView( locale );
  *
  * 		this.setTemplate( {
  * 			tag: 'p',
  *
   * 			// These children will be added automatically. There's no
   * 			// need to call {@link #registerChild} for any of them.
  * 			children: [ this.childA, this.childB ]
  * 		} );
  * 	}
  *
  * 	// ...
  * }
  * ```
  *
  * @param children Children views to be registered.
  */
  registerChild(e) {
    Ai(e) || (e = [
      e
    ]);
    for (const i of e)
      this._unboundChildren.add(i);
  }
  /**
  * The opposite of {@link #registerChild}. Removes a child view from this view instance.
  * Once removed, the child is no longer managed by its parent, e.g. it can safely
  * become a child of another parent view.
  *
  * @see #registerChild
  * @param children Child views to be removed.
  */
  deregisterChild(e) {
    Ai(e) || (e = [
      e
    ]);
    for (const i of e)
      this._unboundChildren.remove(i);
  }
  /**
  * Sets the {@link #template} of the view with with given definition.
  *
  * A shorthand for:
  *
  * ```ts
  * view.setTemplate( definition );
  * ```
  *
  * @param definition Definition of view's template.
  */
  setTemplate(e) {
    this.template = new ri(e);
  }
  /**
  * {@link module:ui/template~Template.extend Extends} the {@link #template} of the view with
  * with given definition.
  *
  * A shorthand for:
  *
  * ```ts
  * Template.extend( view.template, definition );
  * ```
  *
  * **Note**: Is requires the {@link #template} to be already set. See {@link #setTemplate}.
  *
  * @param definition Definition which extends the {@link #template}.
  */
  extendTemplate(e) {
    ri.extend(this.template, e);
  }
  /**
  * Recursively renders the view.
  *
  * Once the view is rendered:
  * * the {@link #element} becomes an HTML element out of {@link #template},
  * * the {@link #isRendered} flag is set `true`.
  *
  * **Note**: The children of the view:
  * * defined directly in the {@link #template}
  * * residing in collections created by the {@link #createCollection} method,
  * * and added by {@link #registerChild}
  * are also rendered in the process.
  *
  * In general, `render()` method is the right place to keep the code which refers to the
  * {@link #element} and should be executed at the very beginning of the view's life cycle.
  *
  * It is possible to {@link module:ui/template~Template.extend} the {@link #template} before
  * the view is rendered. To allow an early customization of the view (e.g. by its parent),
  * such references should be done in `render()`.
  *
  * ```ts
  * class SampleView extends View {
  * 	constructor() {
  * 		this.setTemplate( {
  * 			// ...
  * 		} );
  * 	},
  *
  * 	render() {
  * 		// View#element becomes available.
  * 		super.render();
  *
  * 		// The "scroll" listener depends on #element.
  * 		this.listenTo( window, 'scroll', () => {
  * 			// A reference to #element would render the #template and make it non-extendable.
  * 			if ( window.scrollY > 0 ) {
  * 				this.element.scrollLeft = 100;
  * 			} else {
  * 				this.element.scrollLeft = 0;
  * 			}
  * 		} );
  * 	}
  * }
  *
  * const view = new SampleView();
  *
  * // Let's customize the view before it gets rendered.
  * view.extendTemplate( {
  * 	attributes: {
  * 		class: [
  * 			'additional-class'
  * 		]
  * 	}
  * } );
  *
  * // Late rendering allows customization of the view.
  * view.render();
  * ```
  */
  render() {
    if (this.isRendered)
      throw new S("ui-view-render-already-rendered", this);
    this.template && (this.element = this.template.render(), this.registerChild(this.template.getViews())), this.isRendered = !0;
  }
  /**
  * Recursively destroys the view instance and child views added by {@link #registerChild} and
  * residing in collections created by the {@link #createCollection}.
  *
  * Destruction disables all event listeners:
  * * created on the view, e.g. `view.on( 'event', () => {} )`,
  * * defined in the {@link #template} for DOM events.
  */
  destroy() {
    this.stopListening(), this._viewCollections.map((e) => e.destroy()), this.template && this.template._revertData && this.template.revert(this.element);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Xn({ emitter: n, activator: t, callback: e, contextElements: i, listenerOptions: s }) {
  n.listenTo(document, "mousedown", (r, o) => {
    if (!t())
      return;
    const a = typeof o.composedPath == "function" ? o.composedPath() : [], l = typeof i == "function" ? i() : i;
    for (const c of l)
      if (c.contains(o.target) || a.includes(c))
        return;
    e();
  }, s);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function no(n) {
  class t extends n {
    disableCssTransitions() {
      this._isCssTransitionsDisabled = !0;
    }
    enableCssTransitions() {
      this._isCssTransitionsDisabled = !1;
    }
    constructor(...i) {
      super(...i), this.set("_isCssTransitionsDisabled", !1), this.initializeCssTransitionDisablerMixin();
    }
    initializeCssTransitionDisablerMixin() {
      this.extendTemplate({
        attributes: {
          class: [
            this.bindTemplate.if("_isCssTransitionsDisabled", "ck-transitions-disabled")
          ]
        }
      });
    }
  }
  return t;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Tn({ view: n }) {
  n.listenTo(n.element, "submit", (t, e) => {
    e.preventDefault(), n.fire("submit");
  }, {
    useCapture: !0
  });
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Oa({ keystrokeHandler: n, focusTracker: t, gridItems: e, numberOfColumns: i, uiLanguageDirection: s }) {
  const r = typeof i == "number" ? () => i : i;
  n.set("arrowright", o((c, u) => s === "rtl" ? l(c, u.length) : a(c, u.length))), n.set("arrowleft", o((c, u) => s === "rtl" ? a(c, u.length) : l(c, u.length))), n.set("arrowup", o((c, u) => {
    let d = c - r();
    return d < 0 && (d = c + r() * Math.floor(u.length / r()), d > u.length - 1 && (d -= r())), d;
  })), n.set("arrowdown", o((c, u) => {
    let d = c + r();
    return d > u.length - 1 && (d = c % r()), d;
  }));
  function o(c) {
    return (u) => {
      const d = e.find((g) => g.element === t.focusedElement), h = e.getIndex(d), m = c(h, e);
      e.get(m).focus(), u.stopPropagation(), u.preventDefault();
    };
  }
  function a(c, u) {
    return c === u - 1 ? 0 : c + 1;
  }
  function l(c, u) {
    return c === 0 ? u - 1 : c - 1;
  }
}
const Tc = class Tc extends ee {
  /**
  * @inheritDoc
  */
  constructor() {
    super();
    const t = this.bindTemplate;
    this.set("content", ""), this.set("viewBox", "0 0 20 20"), this.set("fillColor", ""), this.set("isColorInherited", !0), this.set("isVisible", !0), this.setTemplate({
      tag: "svg",
      ns: "http://www.w3.org/2000/svg",
      attributes: {
        class: [
          "ck",
          "ck-icon",
          t.if("isVisible", "ck-hidden", (e) => !e),
          // Exclude icon internals from the CSS reset to allow rich (non-monochromatic) icons
          // (https://github.com/ckeditor/ckeditor5/issues/12599).
          "ck-reset_all-excluded",
          // The class to remove the dynamic color inheritance is toggleable
          // (https://github.com/ckeditor/ckeditor5/issues/12599).
          t.if("isColorInherited", "ck-icon_inherit-color")
        ],
        viewBox: t.to("viewBox"),
        "aria-hidden": !0
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this._updateXMLContent(), this._colorFillPaths(), this.on("change:content", () => {
      this._updateXMLContent(), this._colorFillPaths();
    }), this.on("change:fillColor", () => {
      this._colorFillPaths();
    });
  }
  /**
  * Updates the {@link #element} with the value of {@link #content}.
  */
  _updateXMLContent() {
    if (this.content) {
      const e = new DOMParser().parseFromString(this.content.trim(), "image/svg+xml").querySelector("svg"), i = e.getAttribute("viewBox");
      i && (this.viewBox = i);
      for (const { name: s, value: r } of Array.from(e.attributes))
        Tc.presentationalAttributeNames.includes(s) && this.element.setAttribute(s, r);
      for (; this.element.firstChild; )
        this.element.removeChild(this.element.firstChild);
      for (; e.childNodes.length > 0; )
        this.element.appendChild(e.childNodes[0]);
    }
  }
  /**
  * Fills all child `path.ck-icon__fill` with the `#fillColor`.
  */
  _colorFillPaths() {
    this.fillColor && this.element.querySelectorAll(".ck-icon__fill").forEach((t) => {
      t.style.fill = this.fillColor;
    });
  }
};
/**
* A list of presentational attributes that can be set on the `<svg>` element and should be preserved
* when the icon {@link module:ui/icon/iconview~IconView#content content} is loaded.
*
* See the [specification](https://www.w3.org/TR/SVG/styling.html#TermPresentationAttribute) to learn more.
*/
f(Tc, "presentationalAttributeNames", [
  "alignment-baseline",
  "baseline-shift",
  "clip-path",
  "clip-rule",
  "color",
  "color-interpolation",
  "color-interpolation-filters",
  "color-rendering",
  "cursor",
  "direction",
  "display",
  "dominant-baseline",
  "fill",
  "fill-opacity",
  "fill-rule",
  "filter",
  "flood-color",
  "flood-opacity",
  "font-family",
  "font-size",
  "font-size-adjust",
  "font-stretch",
  "font-style",
  "font-variant",
  "font-weight",
  "image-rendering",
  "letter-spacing",
  "lighting-color",
  "marker-end",
  "marker-mid",
  "marker-start",
  "mask",
  "opacity",
  "overflow",
  "paint-order",
  "pointer-events",
  "shape-rendering",
  "stop-color",
  "stop-opacity",
  "stroke",
  "stroke-dasharray",
  "stroke-dashoffset",
  "stroke-linecap",
  "stroke-linejoin",
  "stroke-miterlimit",
  "stroke-opacity",
  "stroke-width",
  "text-anchor",
  "text-decoration",
  "text-overflow",
  "text-rendering",
  "transform",
  "unicode-bidi",
  "vector-effect",
  "visibility",
  "white-space",
  "word-spacing",
  "writing-mode"
]);
let Qi = Tc;
class Iy extends ee {
  /**
  * @inheritDoc
  */
  constructor() {
    super(), this.set({
      style: void 0,
      text: void 0,
      id: void 0
    });
    const t = this.bindTemplate;
    this.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-button__label"
        ],
        style: t.to("style"),
        id: t.to("id")
      },
      children: [
        {
          text: t.to("text")
        }
      ]
    });
  }
}
class le extends ee {
  /**
  * Creates an instance of the button view class.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param labelView The instance of the button's label. If not provided, an instance of
  * {@link module:ui/button/buttonlabelview~ButtonLabelView} is used.
  */
  constructor(e, i = new Iy()) {
    super(e);
    /**
    * Collection of the child views inside of the button {@link #element}.
    */
    f(this, "children");
    /**
    * Label of the button view. Its text is configurable using the {@link #label label attribute}.
    *
    * If not configured otherwise in the `constructor()`, by default the label is an instance
    * of {@link module:ui/button/buttonlabelview~ButtonLabelView}.
    */
    f(this, "labelView");
    /**
    * The icon view of the button. Will be added to {@link #children} when the
    * {@link #icon icon attribute} is defined.
    */
    f(this, "iconView");
    /**
    * A view displaying the keystroke of the button next to the {@link #labelView label}.
    * Added to {@link #children} when the {@link #withKeystroke `withKeystroke` attribute}
    * is defined.
    */
    f(this, "keystrokeView");
    /**
    * Delayed focus function for focus handling in Safari.
    */
    f(this, "_focusDelayed", null);
    const s = this.bindTemplate, r = Bt();
    this.set("_ariaPressed", !1), this.set("_ariaChecked", !1), this.set("ariaLabel", void 0), this.set("ariaLabelledBy", `ck-editor__aria-label_${r}`), this.set("class", void 0), this.set("labelStyle", void 0), this.set("icon", void 0), this.set("isEnabled", !0), this.set("isOn", !1), this.set("isVisible", !0), this.set("isToggleable", !1), this.set("keystroke", void 0), this.set("label", void 0), this.set("role", void 0), this.set("tabindex", -1), this.set("tooltip", !1), this.set("tooltipPosition", "s"), this.set("type", "button"), this.set("withText", !1), this.set("withKeystroke", !1), this.children = this.createCollection(), this.labelView = this._setupLabelView(i), this.iconView = new Qi(), this.iconView.extendTemplate({
      attributes: {
        class: "ck-button__icon"
      }
    }), this.keystrokeView = this._createKeystrokeView(), this.bind("_tooltipString").to(this, "tooltip", this, "label", this, "keystroke", this._getTooltipString.bind(this));
    const o = {
      tag: "button",
      attributes: {
        class: [
          "ck",
          "ck-button",
          s.to("class"),
          s.if("isEnabled", "ck-disabled", (a) => !a),
          s.if("isVisible", "ck-hidden", (a) => !a),
          s.to("isOn", (a) => a ? "ck-on" : "ck-off"),
          s.if("withText", "ck-button_with-text"),
          s.if("withKeystroke", "ck-button_with-keystroke")
        ],
        role: s.to("role"),
        type: s.to("type", (a) => a || "button"),
        tabindex: s.to("tabindex"),
        "aria-checked": s.to("_ariaChecked"),
        "aria-pressed": s.to("_ariaPressed"),
        "aria-label": s.to("ariaLabel"),
        "aria-labelledby": s.to("ariaLabelledBy"),
        "aria-disabled": s.if("isEnabled", !0, (a) => !a),
        "data-cke-tooltip-text": s.to("_tooltipString"),
        "data-cke-tooltip-position": s.to("tooltipPosition")
      },
      children: this.children,
      on: {
        click: s.to((a) => {
          this.isEnabled ? this.fire("execute") : a.preventDefault();
        })
      }
    };
    this.bind("_ariaPressed").to(this, "isOn", this, "isToggleable", this, "role", (a, l, c) => !l || Cb(c) ? !1 : String(!!a)), this.bind("_ariaChecked").to(this, "isOn", this, "isToggleable", this, "role", (a, l, c) => !l || !Cb(c) ? !1 : String(!!a)), be.isSafari && (this._focusDelayed || (this._focusDelayed = _f(() => this.focus(), 0)), o.on.mousedown = s.to(() => {
      this._focusDelayed();
    }), o.on.mouseup = s.to(() => {
      this._focusDelayed.cancel();
    })), this.setTemplate(o);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.icon && (this.iconView.bind("content").to(this, "icon"), this.children.add(this.iconView)), this.children.add(this.labelView), this.withKeystroke && this.keystroke && this.children.add(this.keystrokeView);
  }
  /**
  * Focuses the {@link #element} of the button.
  */
  focus() {
    this.element.focus();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._focusDelayed && this._focusDelayed.cancel(), super.destroy();
  }
  /**
  * Binds the label view instance it with button attributes.
  */
  _setupLabelView(e) {
    return e.bind("text", "style", "id").to(this, "label", "labelStyle", "ariaLabelledBy"), e;
  }
  /**
  * Creates a view that displays a keystroke next to a {@link #labelView label }
  * and binds it with button attributes.
  */
  _createKeystrokeView() {
    const e = new ee();
    return e.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-button__keystroke"
        ]
      },
      children: [
        {
          text: this.bindTemplate.to("keystroke", (i) => Yl(i))
        }
      ]
    }), e;
  }
  /**
  * Gets the text for the tooltip from the combination of
  * {@link #tooltip}, {@link #label} and {@link #keystroke} attributes.
  *
  * @see #tooltip
  * @see #_tooltipString
  * @param tooltip Button tooltip.
  * @param label Button label.
  * @param keystroke Button keystroke.
  */
  _getTooltipString(e, i, s) {
    return e ? typeof e == "string" ? e : (s && (s = Yl(s)), e instanceof Function ? e(i, s) : `${i}${s ? ` (${s})` : ""}`) : "";
  }
}
function Cb(n) {
  switch (n) {
    case "radio":
    case "checkbox":
    case "option":
    case "switch":
    case "menuitemcheckbox":
    case "menuitemradio":
      return !0;
    default:
      return !1;
  }
}
class Va extends ee {
  /**
  * Creates an instance of the form header class.
  *
  * @param locale The locale instance.
  * @param options.label A label.
  * @param options.class An additional class.
  */
  constructor(e, i = {}) {
    super(e);
    /**
    * A collection of header items.
    */
    f(this, "children");
    /**
    * The icon view instance. Defined only if icon was passed in the constructor's options.
    */
    f(this, "iconView");
    const s = this.bindTemplate;
    this.set("label", i.label || ""), this.set("class", i.class || null), this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-form__header",
          s.to("class")
        ]
      },
      children: this.children
    }), i.icon && (this.iconView = new Qi(), this.iconView.content = i.icon, this.children.add(this.iconView));
    const r = new ee(e);
    r.setTemplate({
      tag: "h2",
      attributes: {
        class: [
          "ck",
          "ck-form__header__label"
        ],
        role: "presentation"
      },
      children: [
        {
          text: s.to("label")
        }
      ]
    }), this.children.add(r);
  }
}
class Pt extends (/* @__PURE__ */ Je()) {
  /**
  * Creates an instance of the focus cycler utility.
  *
  * @param options Configuration options.
  */
  constructor(e) {
    super();
    /**
    * A {@link module:ui/focuscycler~FocusableView focusable views} collection that the cycler operates on.
    */
    f(this, "focusables");
    /**
    * A focus tracker instance that the cycler uses to determine the current focus
    * state in {@link #focusables}.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}
    * which can respond to certain keystrokes and cycle the focus.
    */
    f(this, "keystrokeHandler");
    /**
    * Actions that the cycler can take when a keystroke is pressed. Requires
    * `options.keystrokeHandler` to be passed and working. When an action is
    * performed, `preventDefault` and `stopPropagation` will be called on the event
    * the keystroke fired in the DOM.
    *
    * ```ts
    * actions: {
    * 	// Will call #focusPrevious() when arrowleft or arrowup is pressed.
    * 	focusPrevious: [ 'arrowleft', 'arrowup' ],
    *
    * 	// Will call #focusNext() when arrowdown is pressed.
    * 	focusNext: 'arrowdown'
    * }
    * ```
    */
    f(this, "actions");
    if (this.focusables = e.focusables, this.focusTracker = e.focusTracker, this.keystrokeHandler = e.keystrokeHandler, this.actions = e.actions, e.actions && e.keystrokeHandler)
      for (const i in e.actions) {
        let s = e.actions[i];
        typeof s == "string" && (s = [
          s
        ]);
        for (const r of s)
          e.keystrokeHandler.set(r, (o, a) => {
            this[i](), a();
          }, e.keystrokeHandlerOptions);
      }
    this.on("forwardCycle", () => this.focusFirst(), {
      priority: "low"
    }), this.on("backwardCycle", () => this.focusLast(), {
      priority: "low"
    });
  }
  /**
  * Returns the first focusable view in {@link #focusables}.
  * Returns `null` if there is none.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  get first() {
    return this.focusables.find(ed) || null;
  }
  /**
  * Returns the last focusable view in {@link #focusables}.
  * Returns `null` if there is none.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  get last() {
    return this.focusables.filter(ed).slice(-1)[0] || null;
  }
  /**
  * Returns the next focusable view in {@link #focusables} based on {@link #current}.
  * Returns `null` if there is none.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  get next() {
    return this._getDomFocusableItem(1);
  }
  /**
  * Returns the previous focusable view in {@link #focusables} based on {@link #current}.
  * Returns `null` if there is none.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  get previous() {
    return this._getDomFocusableItem(-1);
  }
  /**
  * An index of the view in the {@link #focusables} which is focused according
  * to {@link #focusTracker}. Returns `null` when there is no such view.
  */
  get current() {
    let e = null;
    return this.focusTracker.focusedElement === null ? null : (this.focusables.find((i, s) => {
      const r = i.element === this.focusTracker.focusedElement;
      return r && (e = s), r;
    }), e);
  }
  /**
  * Focuses the {@link #first} item in {@link #focusables}.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  focusFirst() {
    this._focus(this.first, 1);
  }
  /**
  * Focuses the {@link #last} item in {@link #focusables}.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  focusLast() {
    this._focus(this.last, -1);
  }
  /**
  * Focuses the {@link #next} item in {@link #focusables}.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  focusNext() {
    const e = this.next;
    if (e && this.focusables.getIndex(e) === this.current) {
      this.fire("forwardCycle");
      return;
    }
    e === this.first ? this.fire("forwardCycle") : this._focus(e, 1);
  }
  /**
  * Focuses the {@link #previous} item in {@link #focusables}.
  *
  * **Note**: Hidden views (e.g. with `display: none`) are ignored.
  */
  focusPrevious() {
    const e = this.previous;
    if (e && this.focusables.getIndex(e) === this.current) {
      this.fire("backwardCycle");
      return;
    }
    e === this.last ? this.fire("backwardCycle") : this._focus(e, -1);
  }
  /**
  * Allows for creating continuous focus cycling across multiple focus cyclers and their collections of {@link #focusables}.
  *
  * It starts listening to the {@link module:ui/focuscycler~FocusCyclerForwardCycleEvent} and
  * {@link module:ui/focuscycler~FocusCyclerBackwardCycleEvent} events of the chained focus cycler and engages,
  * whenever the user reaches the last (forwards navigation) or first (backwards navigation) focusable view
  * and would normally start over. Instead, the navigation continues on the higher level (flattens).
  *
  * For instance, for the following nested focus navigation structure, the focus would get stuck the moment
  * the AB gets focused and its focus cycler starts managing it:
  *
  *	   ┌────────────┐   ┌──────────────────────────────────┐   ┌────────────┐
  *	   │ AA         │   │ AB                               │   │ AC         │
  *	   │            │   │                                  │   │            │
  *	   │            │   │    ┌─────┐  ┌─────┐  ┌─────┐     │   │            │
  *	   │            │   │ ┌──► ABA ├──► ABB ├──► ABC ├───┐ │   │            │
  *	   │            ├───► │  └─────┘  └─────┘  └─────┘   │ │   │            │
  *	   │            │   │ │                              │ │   │            │
  *	   │            │   │ │                              │ │   │            │
  *	   │            │   │ └──────────────────────────────┘ │   │            │
  *	   │            │   │                                  │   │            │
  *	   └────────────┘   └──────────────────────────────────┘   └────────────┘
  *
  * Chaining a focus tracker that manages AA, AB, and AC with the focus tracker that manages ABA, ABB, and ABC
  * creates a seamless navigation experience instead:
  *
  *	   ┌────────────┐   ┌──────────────────────────────────┐   ┌────────────┐
  *	   │ AA         │   │ AB                               │   │ AC         │
  *	   │            │   │                                  │   │            │
  *	   │            │   │    ┌─────┐  ┌─────┐  ┌─────┐     │   │            │
  *	   │            │   │ ┌──► ABA ├──► ABB ├──► ABC ├──┐  │   │            │
  *	┌──►            ├───┼─┘  └─────┘  └─────┘  └─────┘  └──┼───►            ├──┐
  *	│  │            │   │                                  │   │            │  │
  *	│  │            │   │                                  │   │            │  │
  *	│  │            │   │                                  │   │            │  │
  *	│  │            │   │                                  │   │            │  │
  *	│  └────────────┘   └──────────────────────────────────┘   └────────────┘  │
  *	│                                                                          │
  *	│                                                                          │
  *	└──────────────────────────────────────────────────────────────────────────┘
  *
  * See {@link #unchain} to reverse the chaining.
  */
  chain(e) {
    const i = () => this.current === null ? null : this.focusables.get(this.current);
    this.listenTo(e, "forwardCycle", (s) => {
      const r = i();
      this.focusNext(), r !== i() && s.stop();
    }, {
      priority: "low"
    }), this.listenTo(e, "backwardCycle", (s) => {
      const r = i();
      this.focusPrevious(), r !== i() && s.stop();
    }, {
      priority: "low"
    });
  }
  /**
  * Reverses a chaining made by {@link #chain}.
  */
  unchain(e) {
    this.stopListening(e);
  }
  /**
  * Focuses the given view if it exists.
  *
  * @param view The view to be focused
  * @param direction The direction of the focus if the view has focusable children.
  * @returns
  */
  _focus(e, i) {
    e && this.focusTracker.focusedElement !== e.element && e.focus(i);
  }
  /**
  * Returns the next or previous focusable view in {@link #focusables} with respect
  * to {@link #current}.
  *
  * @param step Either `1` for checking forward from {@link #current} or `-1` for checking backwards.
  */
  _getDomFocusableItem(e) {
    const i = this.focusables.length;
    if (!i)
      return null;
    const s = this.current;
    if (s === null)
      return this[e === 1 ? "first" : "last"];
    let r = this.focusables.get(s), o = (s + i + e) % i;
    do {
      const a = this.focusables.get(o);
      if (ed(a)) {
        r = a;
        break;
      }
      o = (o + i + e) % i;
    } while (o !== s);
    return r;
  }
}
function ed(n) {
  return La(n) && yn(n.element);
}
function La(n) {
  return "focus" in n && typeof n.focus == "function";
}
function IL(n) {
  return La(n) && "focusCycler" in n && n.focusCycler instanceof Pt;
}
function PL(n) {
  class t extends n {
    /**
    * @inheritDoc
    */
    constructor(...s) {
      super(...s);
      /**
      * A bound version of {@link #_onDrag}.
      */
      f(this, "_onDragBound", this._onDrag.bind(this));
      /**
      * A bound version of {@link #_onDragEnd}.
      */
      f(this, "_onDragEndBound", this._onDragEnd.bind(this));
      /**
      * The last coordinates of the view. It is updated on every mouse move.
      */
      f(this, "_lastDraggingCoordinates", {
        x: 0,
        y: 0
      });
      this.on("render", () => {
        this._attachListeners();
      }), this.set("isDragging", !1);
    }
    /**
    * Attaches the listeners for the drag start.
    */
    _attachListeners() {
      this.listenTo(this.element, "mousedown", this._onDragStart.bind(this)), this.listenTo(this.element, "touchstart", this._onDragStart.bind(this));
    }
    /**
    * Attaches the listeners for the dragging and drag end.
    */
    _attachDragListeners() {
      this.listenTo(ae.document, "mouseup", this._onDragEndBound), this.listenTo(ae.document, "touchend", this._onDragEndBound), this.listenTo(ae.document, "mousemove", this._onDragBound), this.listenTo(ae.document, "touchmove", this._onDragBound);
    }
    /**
    * Detaches the listeners after the drag end.
    */
    _detachDragListeners() {
      this.stopListening(ae.document, "mouseup", this._onDragEndBound), this.stopListening(ae.document, "touchend", this._onDragEndBound), this.stopListening(ae.document, "mousemove", this._onDragBound), this.stopListening(ae.document, "touchmove", this._onDragBound);
    }
    /**
    * Starts the dragging listeners and sets the initial view coordinates.
    */
    _onDragStart(s, r) {
      if (!this._isHandleElementPressed(r))
        return;
      this._attachDragListeners();
      let o = 0, a = 0;
      r instanceof MouseEvent ? (o = r.clientX, a = r.clientY) : (o = r.touches[0].clientX, a = r.touches[0].clientY), this._lastDraggingCoordinates = {
        x: o,
        y: a
      }, this.isDragging = !0;
    }
    /**
    * Updates the view coordinates and fires the `drag` event.
    */
    _onDrag(s, r) {
      if (!this.isDragging) {
        this._detachDragListeners();
        return;
      }
      let o = 0, a = 0;
      r instanceof MouseEvent ? (o = r.clientX, a = r.clientY) : (o = r.touches[0].clientX, a = r.touches[0].clientY), r.preventDefault(), this.fire("drag", {
        deltaX: Math.round(o - this._lastDraggingCoordinates.x),
        deltaY: Math.round(a - this._lastDraggingCoordinates.y)
      }), this._lastDraggingCoordinates = {
        x: o,
        y: a
      };
    }
    /**
    * Stops the dragging and detaches the listeners.
    */
    _onDragEnd() {
      this._detachDragListeners(), this.isDragging = !1;
    }
    /**
    * Checks if the drag handle element was pressed.
    */
    _isHandleElementPressed(s) {
      return this.dragHandleElement ? this.dragHandleElement === s.target || s.target instanceof HTMLElement && this.dragHandleElement.contains(s.target) : !1;
    }
  }
  return t;
}
class RL extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of button views.
    */
    f(this, "children");
    /**
    * A keystroke handler instance.
    */
    f(this, "keystrokes");
    /**
    * A focus cycler instance.
    */
    f(this, "focusCycler");
    /**
    * A focus tracker instance.
    */
    f(this, "_focusTracker");
    /**
    * A collection of focusable views.
    */
    f(this, "_focusables");
    this.children = this.createCollection(), this.keystrokes = new Ze(), this._focusTracker = new ze(), this._focusables = new Et(), this.focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this._focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-dialog__actions"
        ]
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.listenTo(this.element);
  }
  /**
  * Creates the button views based on the given definitions.
  * Then adds them to the {@link #children} collection and to the focus cycler.
  */
  setButtons(e) {
    for (const i of e) {
      const s = new le(this.locale);
      let r;
      s.on("execute", () => i.onExecute()), i.onCreate && i.onCreate(s);
      for (r in i)
        r != "onExecute" && r != "onCreate" && s.set(r, i[r]);
      this.children.add(s);
    }
    this._updateFocusCyclableItems();
  }
  /**
  * @inheritDoc
  */
  focus(e) {
    e === -1 ? this.focusCycler.focusLast() : this.focusCycler.focusFirst();
  }
  /**
  * Adds all elements from the {@link #children} collection to the {@link #_focusables} collection
  * and to the {@link #_focusTracker} instance.
  */
  _updateFocusCyclableItems() {
    Array.from(this.children).forEach((e) => {
      this._focusables.add(e), this._focusTracker.add(e.element);
    });
  }
}
class OL extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of content items.
    */
    f(this, "children");
    this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-dialog__content"
        ]
      },
      children: this.children
    });
  }
  /**
  * Removes all the child views.
  */
  reset() {
    for (; this.children.length; )
      this.children.remove(0);
  }
}
const Ci = {
  SCREEN_CENTER: "screen-center",
  EDITOR_CENTER: "editor-center",
  EDITOR_TOP_SIDE: "editor-top-side",
  EDITOR_TOP_CENTER: "editor-top-center",
  EDITOR_BOTTOM_CENTER: "editor-bottom-center",
  EDITOR_ABOVE_CENTER: "editor-above-center",
  EDITOR_BELOW_CENTER: "editor-below-center"
}, xb = /* @__PURE__ */ to("px"), Ec = class Ec extends (/* @__PURE__ */ PL(ee)) {
  /**
  * @inheritDoc
  */
  constructor(e, { getCurrentDomRoot: i, getViewportOffset: s, keystrokeHandlerOptions: r }) {
    super(e);
    /**
    * A collection of the child views inside of the dialog.
    * A dialog can have 3 optional parts: header, content, and actions.
    */
    f(this, "parts");
    /**
    * A header view of the dialog. It is also a drag handle of the dialog.
    */
    f(this, "headerView");
    /**
    * A close button view. It is automatically added to the header view if present.
    */
    f(this, "closeButtonView");
    /**
    * A view with the action buttons available to the user.
    */
    f(this, "actionsView");
    /**
    * A view with the dialog content.
    */
    f(this, "contentView");
    /**
    * A keystroke handler instance.
    */
    f(this, "keystrokes");
    /**
    * A focus tracker instance.
    */
    f(this, "focusTracker");
    /**
    * A flag indicating if the dialog was moved manually. If so, its position
    * will not be updated automatically upon window resize or document scroll.
    */
    f(this, "wasMoved", !1);
    /**
    * A callback returning the DOM root that requested the dialog.
    */
    f(this, "_getCurrentDomRoot");
    /**
    * A callback returning the configured editor viewport offset.
    */
    f(this, "_getViewportOffset");
    /**
    * The list of the focusable elements inside the dialog view.
    */
    f(this, "_focusables");
    /**
    * The focus cycler instance.
    */
    f(this, "_focusCycler");
    const o = this.bindTemplate, a = e.t;
    this.set("className", ""), this.set("ariaLabel", a("Editor dialog")), this.set("isModal", !1), this.set("position", Ci.SCREEN_CENTER), this.set("_isVisible", !1), this.set("_isTransparent", !1), this.set("_top", 0), this.set("_left", 0), this._getCurrentDomRoot = i, this._getViewportOffset = s, this.decorate("moveTo"), this.parts = this.createCollection(), this.keystrokes = new Ze(), this.focusTracker = new ze(), this._focusables = new Et(), this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      },
      keystrokeHandlerOptions: r
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-dialog-overlay",
          o.if("isModal", "ck-dialog-overlay__transparent", (l) => !l),
          o.if("_isVisible", "ck-hidden", (l) => !l)
        ],
        // Prevent from editor losing focus when clicking on the modal overlay.
        tabindex: "-1"
      },
      children: [
        {
          tag: "div",
          attributes: {
            tabindex: "-1",
            class: [
              "ck",
              "ck-dialog",
              o.if("isModal", "ck-dialog_modal"),
              o.to("className")
            ],
            role: "dialog",
            "aria-label": o.to("ariaLabel"),
            style: {
              top: o.to("_top", (l) => xb(l)),
              left: o.to("_left", (l) => xb(l)),
              visibility: o.if("_isTransparent", "hidden")
            }
          },
          children: this.parts
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.set("Esc", (e, i) => {
      e.defaultPrevented || (this.fire("close", {
        source: "escKeyPress"
      }), i());
    }), this.on("drag", (e, { deltaX: i, deltaY: s }) => {
      this.wasMoved = !0, this.moveBy(i, s);
    }), this.listenTo(ae.window, "resize", () => {
      this._isVisible && !this.wasMoved && this.updatePosition();
    }), this.listenTo(ae.document, "scroll", () => {
      this._isVisible && !this.wasMoved && this.updatePosition();
    }), this.on("change:_isVisible", (e, i, s) => {
      s && (this._isTransparent = !0, setTimeout(() => {
        this.updatePosition(), this._isTransparent = !1, this.focus();
      }, 10));
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * Returns the element that should be used as a drag handle.
  */
  get dragHandleElement() {
    return this.headerView && !this.isModal ? this.headerView.element : null;
  }
  /**
  * Creates the dialog parts. Which of them are created depends on the arguments passed to the method.
  * There are no rules regarding the dialog construction, that is, no part is mandatory.
  * Each part can only be created once.
  *
  * @internal
  */
  setupParts({ icon: e, title: i, hasCloseButton: s = !0, content: r, actionButtons: o }) {
    i && (this.headerView = new Va(this.locale, {
      icon: e
    }), s && (this.closeButtonView = this._createCloseButton(), this.headerView.children.add(this.closeButtonView)), this.headerView.label = i, this.ariaLabel = i, this.parts.add(this.headerView, 0)), r && (r instanceof ee && (r = [
      r
    ]), this.contentView = new OL(this.locale), this.contentView.children.addMany(r), this.parts.add(this.contentView)), o && (this.actionsView = new RL(this.locale), this.actionsView.setButtons(o), this.parts.add(this.actionsView)), this._updateFocusCyclableItems();
  }
  /**
  * Focuses the first focusable element inside the dialog.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Normalizes the passed coordinates to make sure the dialog view
  * is displayed within the visible viewport and moves it there.
  *
  * @internal
  */
  moveTo(e, i) {
    const s = this._getViewportRect(), r = this._getDialogRect();
    e + r.width > s.right && (e = s.right - r.width), e < s.left && (e = s.left), i < s.top && (i = s.top), this._moveTo(e, i);
  }
  /**
  * Moves the dialog to the specified coordinates.
  */
  _moveTo(e, i) {
    this._left = e, this._top = i;
  }
  /**
  * Moves the dialog by the specified offset.
  *
  * @internal
  */
  moveBy(e, i) {
    this.moveTo(this._left + e, this._top + i);
  }
  /**
  * Moves the dialog view to the off-screen position.
  * Used when there is no space to display the dialog.
  */
  _moveOffScreen() {
    this._moveTo(-9999, -9999);
  }
  /**
  * Recalculates the dialog according to the set position and viewport,
  * and moves it to the new position.
  */
  updatePosition() {
    if (!this.element || !this.element.parentNode)
      return;
    const e = this._getViewportRect();
    let i = this.position, s;
    this._getCurrentDomRoot() ? s = this._getVisibleDomRootRect(e) : i = Ci.SCREEN_CENTER;
    const r = Ec.defaultOffset, o = this._getDialogRect();
    switch (i) {
      case Ci.EDITOR_TOP_SIDE: {
        if (s) {
          const a = this.locale.contentLanguageDirection === "ltr" ? s.right - o.width - r : s.left + r;
          this.moveTo(a, s.top + r);
        } else
          this._moveOffScreen();
        break;
      }
      case Ci.EDITOR_CENTER: {
        s ? this.moveTo(Math.round(s.left + s.width / 2 - o.width / 2), Math.round(s.top + s.height / 2 - o.height / 2)) : this._moveOffScreen();
        break;
      }
      case Ci.SCREEN_CENTER: {
        this.moveTo(Math.round((e.width - o.width) / 2), Math.round((e.height - o.height) / 2));
        break;
      }
      case Ci.EDITOR_TOP_CENTER: {
        s ? this.moveTo(Math.round(s.left + s.width / 2 - o.width / 2), s.top + r) : this._moveOffScreen();
        break;
      }
      case Ci.EDITOR_BOTTOM_CENTER: {
        s ? this.moveTo(Math.round(s.left + s.width / 2 - o.width / 2), s.bottom - o.height - r) : this._moveOffScreen();
        break;
      }
      case Ci.EDITOR_ABOVE_CENTER: {
        s ? this.moveTo(Math.round(s.left + s.width / 2 - o.width / 2), s.top - o.height - r) : this._moveOffScreen();
        break;
      }
      case Ci.EDITOR_BELOW_CENTER: {
        s ? this.moveTo(Math.round(s.left + s.width / 2 - o.width / 2), s.bottom + r) : this._moveOffScreen();
        break;
      }
    }
  }
  /**
  * Calculates the visible DOM root part.
  */
  _getVisibleDomRootRect(e) {
    let i = new ke(this._getCurrentDomRoot()).getVisible();
    if (i) {
      if (i = e.getIntersection(i), !i)
        return null;
    } else
      return null;
    return i;
  }
  /**
  * Calculates the dialog element rect.
  */
  _getDialogRect() {
    return new ke(this.element.firstElementChild);
  }
  /**
  * Returns a viewport `Rect` shrunk by the viewport offset config from all sides.
  *
  * TODO: This is a duplicate from position.ts module. It should either be exported there or land somewhere in utils.
  */
  _getViewportRect() {
    const e = new ke(ae.window);
    if (this.isModal)
      return e;
    const i = {
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      ...this._getViewportOffset()
    };
    return e.top += i.top, e.height -= i.top, e.bottom -= i.bottom, e.height -= i.bottom, e.left += i.left, e.right -= i.right, e.width -= i.left + i.right, e;
  }
  /**
  * Collects all focusable elements inside the dialog parts
  * and adds them to the focus tracker and focus cycler.
  */
  _updateFocusCyclableItems() {
    const e = [];
    if (this.contentView)
      for (const i of this.contentView.children)
        La(i) && e.push(i);
    this.actionsView && e.push(this.actionsView), this.closeButtonView && e.push(this.closeButtonView), e.forEach((i) => {
      this._focusables.add(i), this.focusTracker.add(i.element), IL(i) && this._focusCycler.chain(i.focusCycler);
    });
  }
  /**
  * Creates the close button view that is displayed in the header view corner.
  */
  _createCloseButton() {
    const e = new le(this.locale), i = this.locale.t;
    return e.set({
      label: i("Close"),
      tooltip: !0,
      icon: Z.cancel
    }), e.on("execute", () => this.fire("close", {
      source: "closeButton"
    })), e;
  }
};
/**
* A default dialog element offset from the reference element (e.g. editor editable area).
*/
f(Ec, "defaultOffset", 15);
let ha = Ec;
const Hi = class Hi extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The currently visible dialog view instance.
    */
    f(this, "view");
    /**
    * A configurable callback called when the dialog is hidden.
    */
    f(this, "_onHide");
    const i = e.t;
    this._initShowHideListeners(), this._initFocusToggler(), this._initMultiRootIntegration(), this.set({
      id: null,
      isOpen: !1
    }), e.accessibility.addKeystrokeInfos({
      categoryId: "navigation",
      keystrokes: [
        {
          label: i("Move focus in and out of an active dialog window"),
          keystroke: "Ctrl+F6",
          mayRequireFn: !0
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Dialog";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), Hi._visibleDialogPlugin === this && this._unlockBodyScroll();
  }
  /**
  * Initiates listeners for the `show` and `hide` events emitted by this plugin.
  *
  * We could not simply decorate the {@link #show} and {@link #hide} methods to fire events,
  * because they would be fired in the wrong order &ndash; first would be `show` and then `hide`
  * (because showing the dialog actually starts with hiding the previously visible one).
  * Hence, we added private methods {@link #_show} and {@link #_hide} which are called on events
  * in the desired sequence.
  */
  _initShowHideListeners() {
    this.on("show", (e, i) => {
      this._show(i);
    }), this.on("show", (e, i) => {
      i.onShow && i.onShow(this);
    }, {
      priority: "low"
    }), this.on("hide", () => {
      Hi._visibleDialogPlugin && Hi._visibleDialogPlugin._hide();
    }), this.on("hide", () => {
      this._onHide && (this._onHide(this), this._onHide = void 0);
    }, {
      priority: "low"
    });
  }
  /**
  * Initiates keystroke handler for toggling the focus between the editor and the dialog view.
  */
  _initFocusToggler() {
    const e = this.editor;
    e.keystrokes.set("Ctrl+F6", (i, s) => {
      !this.isOpen || this.view.isModal || (this.view.focusTracker.isFocused ? e.editing.view.focus() : this.view.focus(), s());
    });
  }
  /**
  * Provides an integration between the root attaching and detaching and positioning of the view.
  */
  _initMultiRootIntegration() {
    const e = this.editor.model;
    e.document.on("change:data", () => {
      if (!this.view)
        return;
      const i = e.document.differ.getChangedRoots();
      for (const s of i)
        s.state && this.view.updatePosition();
    });
  }
  /**
  * Displays a dialog window.
  *
  * This method requires a {@link ~DialogDefinition} that defines the dialog's content, title, icon, action buttons, etc.
  *
  * For example, the following definition will create a dialog with:
  * * A header consisting of an icon, a title, and a "Close" button (it is added by default).
  * * A content consisting of a view with a single paragraph.
  * * A footer consisting of two buttons: "Yes" and "No".
  *
  * ```js
  * // Create the view that will be used as the dialog's content.
  * const textView = new View( locale );
  *
  * textView.setTemplate( {
  * 	tag: 'div',
  * 	attributes: {
  * 		style: {
  * 			padding: 'var(--ck-spacing-large)',
  * 			whiteSpace: 'initial',
  * 			width: '100%',
  * 			maxWidth: '500px'
  * 		},
  * 		tabindex: -1
  * 	},
  * 	children: [
  * 		'Lorem ipsum dolor sit amet...'
  * 	]
  * } );
  *
  * // Show the dialog.
  * editor.plugins.get( 'Dialog' ).show( {
  *	id: 'myDialog',
  * 	icon: 'myIcon', // This should be an SVG string.
  * 	title: 'My dialog',
  * 	content: textView,
  * 	actionButtons: [
  *		{
  *			label: t( 'Yes' ),
  *			class: 'ck-button-action',
  *			withText: true,
  *			onExecute: () => dialog.hide()
  *		},
  *		{
  *			label: t( 'No' ),
  *			withText: true,
  *			onExecute: () => dialog.hide()
  *		}
  *	]
  * } );
  * ```
  *
  * By specifying the {@link ~DialogDefinition#onShow} and {@link ~DialogDefinition#onHide} callbacks
  * it is also possible to add callbacks that will be called when the dialog is shown or hidden.
  *
  * For example, the callbacks in the following definition:
  * * Disable the default behavior of the <kbd>Esc</kbd> key.
  * * Fire a custom event when the dialog gets hidden.
  *
  * ```js
  * editor.plugins.get( 'Dialog' ).show( {
  * 	// ...
  * 	onShow: dialog => {
  * 		dialog.view.on( 'close', ( evt, data ) => {
  * 			// Only prevent the event from the "Esc" key - do not affect the other ways of closing the dialog.
  * 			if ( data.source === 'escKeyPress' ) {
  * 				evt.stop();
  * 			}
  * 		} );
  * 	},
  * 	onHide: dialog => {
  * 		dialog.fire( 'dialogDestroyed' );
  * 	}
  * } );
  * ```
  *
  * Internally, calling this method:
  * 1. Hides the currently visible dialog (if any) calling the {@link #hide} method
  * (fires the {@link ~DialogHideEvent hide event}).
  * 2. Fires the {@link ~DialogShowEvent show event} which allows for adding callbacks that customize the
  * behavior of the dialog.
  * 3. Shows the dialog.
  */
  show(e) {
    this.hide(), this.fire(`show:${e.id}`, e);
  }
  /**
  * Handles creating the {@link module:ui/dialog/dialogview~DialogView} instance and making it visible.
  */
  _show({ id: e, icon: i, title: s, hasCloseButton: r = !0, content: o, actionButtons: a, className: l, isModal: c, position: u, onHide: d, keystrokeHandlerOptions: h }) {
    const m = this.editor;
    this.view = new ha(m.locale, {
      getCurrentDomRoot: () => m.editing.view.getDomRoot(m.model.document.selection.anchor.root.rootName),
      getViewportOffset: () => m.ui.viewportOffset,
      keystrokeHandlerOptions: h
    });
    const g = this.view;
    g.on("close", () => {
      this.hide();
    }), m.ui.view.body.add(g), m.keystrokes.listenTo(g.element), u || (u = c ? Ci.SCREEN_CENTER : Ci.EDITOR_CENTER), c && this._lockBodyScroll(), g.set({
      position: u,
      _isVisible: !0,
      className: l,
      isModal: c
    }), g.setupParts({
      icon: i,
      title: s,
      hasCloseButton: r,
      content: o,
      actionButtons: a
    }), this.id = e, d && (this._onHide = d), this.isOpen = !0, Hi._visibleDialogPlugin = this;
  }
  /**
  * Hides the dialog. This method is decorated to enable interacting on the {@link ~DialogHideEvent hide event}.
  *
  * See {@link #show}.
  */
  hide() {
    Hi._visibleDialogPlugin && Hi._visibleDialogPlugin.fire(`hide:${Hi._visibleDialogPlugin.id}`);
  }
  /**
  * Destroys the {@link module:ui/dialog/dialogview~DialogView} and cleans up the stored dialog state.
  */
  _hide() {
    if (!this.view)
      return;
    const e = this.editor, i = this.view;
    i.isModal && this._unlockBodyScroll(), i.contentView && i.contentView.reset(), e.ui.view.body.remove(i), e.ui.focusTracker.remove(i.element), e.keystrokes.stopListening(i.element), i.destroy(), e.editing.view.focus(), this.id = null, this.isOpen = !1, Hi._visibleDialogPlugin = null;
  }
  /**
  * Makes the <body> unscrollable (e.g. when the modal shows up).
  */
  _lockBodyScroll() {
    document.documentElement.classList.add("ck-dialog-scroll-locked");
  }
  /**
  * Makes the <body> scrollable again (e.g. once the modal hides).
  */
  _unlockBodyScroll() {
    document.documentElement.classList.remove("ck-dialog-scroll-locked");
  }
};
/**
* The `Dialog` plugin instance which most recently showed the dialog.
*
* Only one dialog can be visible at once, even if there are many editor instances on the page.
* If an editor wants to show a dialog, it should first hide the dialog that is already opened.
* But only the `Dialog` instance that showed the dialog is able able to properly hide it.
* This is why we need to store it in a globally available space (static property).
* This way every `Dialog` plugin in every editor is able to correctly close any open dialog window.
*/
f(Hi, "_visibleDialogPlugin");
let Hs = Hi;
class Ur extends le {
  /**
  * @inheritDoc
  */
  constructor(e, i = new Iy()) {
    super(e, i);
    /**
    * Holds the view for the check icon of a button list item.
    */
    f(this, "_checkIconHolderView", new VL());
    this.set({
      hasCheckSpace: !1,
      _hasCheck: this.isToggleable
    });
    const s = this.bindTemplate;
    this.extendTemplate({
      attributes: {
        class: [
          "ck-list-item-button",
          s.if("isToggleable", "ck-list-item-button_toggleable")
        ]
      }
    }), this.bind("_hasCheck").to(this, "hasCheckSpace", this, "isToggleable", (r, o) => r || o);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this._hasCheck && this.children.add(this._checkIconHolderView, 0), this._watchCheckIconHolderMount();
  }
  /**
  * Renders the check icon if the button is toggleable.
  */
  _watchCheckIconHolderMount() {
    this._checkIconHolderView.bind("isOn").to(this, "isOn", (e) => this.isToggleable && e), this.on("change:_hasCheck", (e, i, s) => {
      const { children: r, _checkIconHolderView: o } = this;
      s ? r.add(o, 0) : r.remove(o);
    });
  }
}
class VL extends ee {
  /**
  * @inheritDoc
  */
  constructor() {
    super();
    /**
    * Collection of child views.
    */
    f(this, "children");
    /**
    * The view for the check icon of the button list item.
    */
    f(this, "_checkIconView", this._createCheckIconView());
    const e = this.bindTemplate;
    this.children = this.createCollection(), this.set("isOn", !1), this.setTemplate({
      tag: "span",
      children: this.children,
      attributes: {
        class: [
          "ck",
          "ck-list-item-button__check-holder",
          e.to("isOn", (i) => i ? "ck-on" : "ck-off")
        ]
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.isOn && this.children.add(this._checkIconView, 0), this._watchCheckIconMount();
  }
  /**
  * Renders the check icon if the button is toggleable.
  */
  _watchCheckIconMount() {
    this.on("change:isOn", (e, i, s) => {
      const { children: r, _checkIconView: o } = this;
      s && !r.has(o) ? r.add(o) : !s && r.has(o) && r.remove(o);
    });
  }
  /**
  * Creates a check icon view.
  */
  _createCheckIconView() {
    const e = new Qi();
    return e.content = Z.check, e.extendTemplate({
      attributes: {
        class: "ck-list-item-button__check-icon"
      }
    }), e;
  }
}
class Fe extends Ur {
  /**
  * Creates an instance of the menu bar list button view.
  *
  * @param locale The localization services instance.
  */
  constructor(t) {
    super(t), this.set({
      withText: !0,
      withKeystroke: !0,
      tooltip: !1,
      role: "menuitem"
    }), this.extendTemplate({
      attributes: {
        class: [
          "ck-menu-bar__menu__item__button"
        ]
      }
    });
  }
}
class gi extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * An unique id of the label. It can be used by other UI components to reference
    * the label, for instance, using the `aria-describedby` DOM attribute.
    */
    f(this, "id");
    this.set("text", void 0), this.set("for", void 0), this.id = `ck-editor__label_${Bt()}`;
    const i = this.bindTemplate;
    this.setTemplate({
      tag: "label",
      attributes: {
        class: [
          "ck",
          "ck-label"
        ],
        id: this.id,
        for: i.to("for")
      },
      children: [
        {
          text: i.to("text")
        }
      ]
    });
  }
}
class LL extends ee {
  /**
  * @inheritDoc
  */
  constructor(t, e) {
    super(t);
    const i = t.t, s = new gi();
    s.text = i("Help Contents. To close this dialog press ESC."), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-accessibility-help-dialog__content"
        ],
        "aria-labelledby": s.id,
        role: "document",
        tabindex: -1
      },
      children: [
        Ft(document, "p", {}, i("Below, you can find a list of keyboard shortcuts that can be used in the editor.")),
        ...this._createCategories(Array.from(e.values())),
        s
      ]
    });
  }
  /**
  * @inheritDoc
  */
  focus() {
    this.element.focus();
  }
  /**
  * Creates `<section><h3>Category label</h3>...</section>` elements for each category of keystrokes.
  */
  _createCategories(t) {
    return t.map((e) => {
      const i = [
        // Category header.
        Ft(document, "h3", {}, e.label),
        // Category definitions (<dl>) and their optional headers (<h4>).
        ...Array.from(e.groups.values()).map((s) => this._createGroup(s)).flat()
      ];
      return e.description && i.splice(1, 0, Ft(document, "p", {}, e.description)), Ft(document, "section", {}, i);
    });
  }
  /**
  * Creates `[<h4>Optional label</h4>]<dl>...</dl>` elements for each group of keystrokes in a category.
  */
  _createGroup(t) {
    const e = t.keystrokes.sort((s, r) => s.label.localeCompare(r.label)).map((s) => this._createGroupRow(s)).flat(), i = [
      Ft(document, "dl", {}, e)
    ];
    return t.label && i.unshift(Ft(document, "h4", {}, t.label)), i;
  }
  /**
  * Creates `<dt>Keystroke label</dt><dd>Keystroke definition</dd>` elements for each keystroke in a group.
  */
  _createGroupRow(t) {
    const e = this.locale.t, i = Ft(document, "dt"), s = Ft(document, "dd"), r = ML(t.keystroke), o = [];
    for (const a of r)
      o.push(a.map(BL).join(""));
    return i.innerHTML = t.label, s.innerHTML = o.join(", ") + (t.mayRequireFn && be.isMac ? ` ${e("(may require <kbd>Fn</kbd>)")}` : ""), [
      i,
      s
    ];
  }
}
function BL(n) {
  return Yl(n).split("+").map((t) => `<kbd>${t}</kbd>`).join("+");
}
function ML(n) {
  return typeof n == "string" ? [
    [
      n
    ]
  ] : typeof n[0] == "string" ? [
    n
  ] : n;
}
var Tb = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10 6.628a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3Z"/><path d="M8.5 9.125a.3.3 0 0 0-.253-.296L5.11 8.327a.75.75 0 1 1 .388-1.449l4.04.716c.267.072.624.08.893.009l4.066-.724a.75.75 0 1 1 .388 1.45l-3.132.5a.3.3 0 0 0-.253.296v1.357a.3.3 0 0 0 .018.102l1.615 4.438a.75.75 0 0 1-1.41.513l-1.35-3.71a.3.3 0 0 0-.281-.197h-.209a.3.3 0 0 0-.282.198l-1.35 3.711a.75.75 0 0 1-1.41-.513l1.64-4.509a.3.3 0 0 0 .019-.103V9.125Z"/><path clip-rule="evenodd" d="M10 18.5a8.5 8.5 0 1 1 0-17 8.5 8.5 0 0 1 0 17Zm0 1.5c5.523 0 10-4.477 10-10S15.523 0 10 0 0 4.477 0 10s4.477 10 10 10Z"/></svg>';
class NL extends I {
  constructor() {
    super(...arguments);
    /**
    * The view that displays the dialog content (list of keystrokes).
    * Created when the dialog is opened for the first time.
    */
    f(this, "contentView", null);
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hs
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AccessibilityHelp";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.locale.t;
    e.ui.componentFactory.add("accessibilityHelp", () => {
      const s = this._createButton(le);
      return s.set({
        tooltip: !0,
        withText: !1,
        label: i("Accessibility help")
      }), s;
    }), e.ui.componentFactory.add("menuBar:accessibilityHelp", () => {
      const s = this._createButton(Fe);
      return s.label = i("Accessibility"), s;
    }), e.keystrokes.set("Alt+0", (s, r) => {
      this._toggleDialog(), r();
    }), this._setupRootLabels();
  }
  /**
  * Creates a button to show accessibility help dialog, for use either in toolbar or in menu bar.
  */
  _createButton(e) {
    const i = this.editor, s = i.plugins.get("Dialog"), r = i.locale, o = new e(r);
    return o.set({
      keystroke: "Alt+0",
      icon: Tb,
      isToggleable: !0
    }), o.on("execute", () => this._toggleDialog()), o.bind("isOn").to(s, "id", (a) => a === "accessibilityHelp"), o;
  }
  /**
  * Injects a help text into each editing root's `aria-label` attribute allowing assistive technology users
  * to discover the availability of the Accessibility help dialog.
  */
  _setupRootLabels() {
    const e = this.editor, i = e.editing.view, s = e.t;
    e.ui.on("ready", () => {
      i.change((o) => {
        for (const a of i.document.roots)
          r(o, a);
      }), e.on("addRoot", (o, a) => {
        const l = e.editing.view.document.getRoot(a.rootName);
        i.change((c) => r(c, l));
      }, {
        priority: "low"
      });
    });
    function r(o, a) {
      const c = [
        a.getAttribute("aria-label"),
        s("Press %0 for help.", [
          Yl("Alt+0")
        ])
      ].filter((u) => u).join(". ");
      o.setAttribute("aria-label", c, a);
    }
  }
  /**
  * Shows the accessibility help dialog. Also, creates {@link #contentView} on demand.
  */
  _toggleDialog() {
    const e = this.editor, i = e.plugins.get("Dialog"), s = e.locale.t;
    this.contentView || (this.contentView = new LL(e.locale, e.accessibility.keystrokeInfos)), i.id === "accessibilityHelp" ? i.hide() : i.show({
      id: "accessibilityHelp",
      className: "ck-accessibility-help-dialog",
      title: s("Accessibility help"),
      icon: Tb,
      hasCloseButton: !0,
      content: this.contentView
    });
  }
}
const Bi = class Bi extends Et {
  /**
  * Creates a new instance of the {@link module:ui/editorui/bodycollection~BodyCollection}.
  *
  * @param locale The {@link module:core/editor/editor~Editor editor's locale} instance.
  * @param initialItems The initial items of the collection.
  */
  constructor(e, i = []) {
    super(i);
    /**
    * The {@link module:core/editor/editor~Editor#locale editor's locale} instance.
    * See the view {@link module:ui/view~View#locale locale} property.
    */
    f(this, "locale");
    /**
    * The element holding elements of the body collection.
    */
    f(this, "_bodyCollectionContainer");
    this.locale = e;
  }
  /**
  * The element holding elements of the body collection.
  */
  get bodyCollectionContainer() {
    return this._bodyCollectionContainer;
  }
  /**
  * Attaches the body collection to the DOM body element. You need to execute this method to render the content of
  * the body collection.
  */
  attachToDom() {
    this._bodyCollectionContainer = new ri({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-reset_all",
          "ck-body",
          "ck-rounded-corners"
        ],
        dir: this.locale.uiLanguageDirection,
        role: "application"
      },
      children: this
    }).render(), (!Bi._bodyWrapper || !Bi._bodyWrapper.isConnected) && (Bi._bodyWrapper = Ft(document, "div", {
      class: "ck-body-wrapper"
    }), document.body.appendChild(Bi._bodyWrapper)), Bi._bodyWrapper.appendChild(this._bodyCollectionContainer);
  }
  /**
  * Detaches the collection from the DOM structure. Use this method when you do not need to use the body collection
  * anymore to clean-up the DOM structure.
  */
  detachFromDom() {
    super.destroy(), this._bodyCollectionContainer && this._bodyCollectionContainer.remove(), Bi._bodyWrapper && !Bi._bodyWrapper.childElementCount && (Bi._bodyWrapper.remove(), delete Bi._bodyWrapper);
  }
};
/**
* The wrapper element that holds all of the {@link #_bodyCollectionContainer} elements.
*/
f(Bi, "_bodyWrapper");
let vh = Bi;
class js extends le {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The toggle switch of the button.
    */
    f(this, "toggleSwitchView");
    this.isToggleable = !0, this.toggleSwitchView = this._createToggleView(), this.extendTemplate({
      attributes: {
        class: "ck-switchbutton"
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.children.add(this.toggleSwitchView);
  }
  /**
  * Creates a toggle child view.
  */
  _createToggleView() {
    const e = new ee();
    return e.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-button__toggle"
        ]
      },
      children: [
        {
          tag: "span",
          attributes: {
            class: [
              "ck",
              "ck-button__toggle__inner"
            ]
          }
        }
      ]
    }), e;
  }
}
class Eb extends (/* @__PURE__ */ Py(le)) {
}
class FL extends (/* @__PURE__ */ Py(Ur)) {
}
function Py(n) {
  class t extends n {
    /**
    * @inheritDoc
    */
    constructor(...s) {
      super(...s);
      /**
      * The button view of the component.
      *
      * @deprecated
      */
      f(this, "buttonView");
      /**
      * A hidden `<input>` view used to execute file dialog.
      */
      f(this, "_fileInputView");
      this.buttonView = this, this._fileInputView = new DL(this.locale), this._fileInputView.bind("acceptedType").to(this), this._fileInputView.bind("allowMultipleFiles").to(this), this._fileInputView.delegate("done").to(this), this.on("execute", () => {
        this._fileInputView.open();
      }), this.extendTemplate({
        attributes: {
          class: "ck-file-dialog-button"
        }
      });
    }
    /**
    * @inheritDoc
    */
    render() {
      super.render(), this.children.add(this._fileInputView);
    }
  }
  return t;
}
class DL extends ee {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.set("acceptedType", void 0), this.set("allowMultipleFiles", !1);
    const e = this.bindTemplate;
    this.setTemplate({
      tag: "input",
      attributes: {
        class: [
          "ck-hidden"
        ],
        type: "file",
        tabindex: "-1",
        accept: e.to("acceptedType"),
        multiple: e.to("allowMultipleFiles")
      },
      on: {
        // Removing from code coverage since we cannot programmatically set input element files.
        change: e.to(
          /* istanbul ignore next -- @preserve */
          () => {
            this.element && this.element.files && this.element.files.length && this.fire("done", this.element.files), this.element.value = "";
          }
        )
      }
    });
  }
  /**
  * Opens file dialog.
  */
  open() {
    this.element.click();
  }
}
var Xc = '<svg viewBox="0 0 10 10" xmlns="http://www.w3.org/2000/svg"><path d="M.941 4.523a.75.75 0 1 1 1.06-1.06l3.006 3.005 3.005-3.005a.75.75 0 1 1 1.06 1.06l-3.549 3.55a.75.75 0 0 1-1.168-.136L.941 4.523z"/></svg>';
class $f extends ee {
  /**
  * Creates an instance of the collapsible view.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param childViews An optional array of initial child views to be inserted into the collapsible.
  */
  constructor(e, i) {
    super(e);
    /**
    * The main button that, when clicked, collapses or expands the container with {@link #children}.
    */
    f(this, "buttonView");
    /**
    * A collection of the child views that can be collapsed by clicking the {@link #buttonView}.
    */
    f(this, "children");
    const s = this.bindTemplate;
    this.set("isCollapsed", !1), this.set("label", ""), this.buttonView = this._createButtonView(), this.children = this.createCollection(), this.set("_collapsibleAriaLabelUid", void 0), i && this.children.addMany(i), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-collapsible",
          s.if("isCollapsed", "ck-collapsible_collapsed")
        ]
      },
      children: [
        this.buttonView,
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-collapsible__children"
            ],
            role: "region",
            hidden: s.if("isCollapsed", "hidden"),
            "aria-labelledby": s.to("_collapsibleAriaLabelUid")
          },
          children: this.children
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this._collapsibleAriaLabelUid = this.buttonView.labelView.element.id;
  }
  /**
  * Focuses the first focusable.
  */
  focus() {
    this.buttonView.focus();
  }
  /**
  * Creates the main {@link #buttonView} of the collapsible.
  */
  _createButtonView() {
    const e = new le(this.locale), i = e.bindTemplate;
    return e.set({
      withText: !0,
      icon: Xc
    }), e.extendTemplate({
      attributes: {
        "aria-expanded": i.to("isOn", (s) => String(s))
      }
    }), e.bind("label").to(this), e.bind("isOn").to(this, "isCollapsed", (s) => !s), e.on("execute", () => {
      this.isCollapsed = !this.isCollapsed;
    }), e;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function fa(n, t) {
  const e = n.t, i = {
    Black: e("Black"),
    "Dim grey": e("Dim grey"),
    Grey: e("Grey"),
    "Light grey": e("Light grey"),
    White: e("White"),
    Red: e("Red"),
    Orange: e("Orange"),
    Yellow: e("Yellow"),
    "Light green": e("Light green"),
    Green: e("Green"),
    Aquamarine: e("Aquamarine"),
    Turquoise: e("Turquoise"),
    "Light blue": e("Light blue"),
    Blue: e("Blue"),
    Purple: e("Purple")
  };
  return t.map((s) => {
    const r = i[s.label];
    return r && r != s.label && (s.label = r), s;
  });
}
function ma(n) {
  return n.map($L).filter((t) => !!t);
}
function $L(n) {
  return typeof n == "string" ? {
    model: n,
    label: n,
    hasBorder: !1,
    view: {
      name: "span",
      styles: {
        color: n
      }
    }
  } : {
    model: n.color,
    label: n.label || n.color,
    hasBorder: n.hasBorder === void 0 ? !1 : n.hasBorder,
    view: {
      name: "span",
      styles: {
        color: `${n.color}`
      }
    }
  };
}
var zL = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path class="ck-icon__fill" d="M16.935 5.328a2 2 0 0 1 0 2.829l-7.778 7.778a2 2 0 0 1-2.829 0L3.5 13.107a1.999 1.999 0 1 1 2.828-2.829l.707.707a1 1 0 0 0 1.414 0l5.658-5.657a2 2 0 0 1 2.828 0z"/><path d="M14.814 6.035 8.448 12.4a1 1 0 0 1-1.414 0l-1.413-1.415A1 1 0 1 0 4.207 12.4l2.829 2.829a1 1 0 0 0 1.414 0l7.778-7.778a1 1 0 1 0-1.414-1.415z"/></svg>';
class Ry extends le {
  constructor(t) {
    super(t);
    const e = this.bindTemplate;
    this.set("color", void 0), this.set("hasBorder", !1), this.icon = zL, this.extendTemplate({
      attributes: {
        style: {
          // https://github.com/ckeditor/ckeditor5/issues/14907
          backgroundColor: e.to("color", (i) => be.isMediaForcedColors ? null : i)
        },
        class: [
          "ck",
          "ck-color-grid__tile",
          e.if("hasBorder", "ck-color-selector__color-tile_bordered")
        ]
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.iconView.fillColor = "hsl(0, 0%, 100%)";
  }
}
class Sb extends ee {
  /**
  * Creates an instance of a color grid containing {@link module:ui/colorgrid/colortileview~ColorTileView tiles}.
  *
  * @fires execute
  * @param locale The localization services instance.
  * @param options Component configuration
  * @param options.colorDefinitions Array with definitions
  * required to create the {@link module:ui/colorgrid/colortileview~ColorTileView tiles}.
  * @param options.columns A number of columns to display the tiles.
  */
  constructor(e, i) {
    super(e);
    /**
    * A number of columns for the tiles grid.
    */
    f(this, "columns");
    /**
    * Collection of the child tile views.
    */
    f(this, "items");
    /**
    * Tracks information about DOM focus in the grid.
    */
    f(this, "focusTracker");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    const s = i && i.colorDefinitions ? i.colorDefinitions : [];
    this.columns = i && i.columns ? i.columns : 5;
    const r = {
      gridTemplateColumns: `repeat( ${this.columns}, 1fr)`
    };
    this.set("selectedColor", void 0), this.items = this.createCollection(), this.focusTracker = new ze(), this.keystrokes = new Ze(), this.items.on("add", (o, a) => {
      a.isOn = a.color === this.selectedColor;
    }), s.forEach((o) => {
      const a = new Ry();
      a.set({
        color: o.color,
        label: o.label,
        tooltip: !0,
        hasBorder: o.options.hasBorder
      }), a.on("execute", () => {
        this.fire("execute", {
          value: o.color,
          hasBorder: o.options.hasBorder,
          label: o.label
        });
      }), this.items.add(a);
    }), this.setTemplate({
      tag: "div",
      children: this.items,
      attributes: {
        class: [
          "ck",
          "ck-color-grid"
        ],
        style: r
      }
    }), this.on("change:selectedColor", (o, a, l) => {
      for (const c of this.items)
        c.isOn = c.color === l;
    });
  }
  /**
  * Focuses the first focusable in {@link #items}.
  */
  focus() {
    this.items.length && this.items.first.focus();
  }
  /**
  * Focuses the last focusable in {@link #items}.
  */
  focusLast() {
    this.items.length && this.items.last.focus();
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    for (const e of this.items)
      this.focusTracker.add(e.element);
    this.items.on("add", (e, i) => {
      this.focusTracker.add(i.element);
    }), this.items.on("remove", (e, i) => {
      this.focusTracker.remove(i.element);
    }), this.keystrokes.listenTo(this.element), Oa({
      keystrokeHandler: this.keystrokes,
      focusTracker: this.focusTracker,
      gridItems: this.items,
      numberOfColumns: this.columns,
      uiLanguageDirection: this.locale && this.locale.uiLanguageDirection
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Oy(n, t) {
  if (!n)
    return "";
  const e = Vy(n);
  if (!e)
    return "";
  if (e.space === t)
    return n;
  if (!WL(e))
    return "";
  const s = _y[e.space][t];
  if (!s)
    return "";
  const r = s(e.space === "hex" ? e.hexValue : e.values);
  return UL(r, t);
}
function HL(n) {
  if (!n)
    return "";
  const t = Vy(n);
  return t ? t.space === "hex" ? t.hexValue : Oy(n, "hex") : "#000";
}
function jL(n, t) {
  customElements.get(n) === void 0 && customElements.define(n, t);
}
function UL(n, t) {
  switch (t) {
    case "hex":
      return `#${n}`;
    case "rgb":
      return `rgb(${n[0]}, ${n[1]}, ${n[2]})`;
    case "hsl":
      return `hsl(${n[0]}, ${n[1]}%, ${n[2]}%)`;
    case "hwb":
      return `hwb(${n[0]}, ${n[1]}, ${n[2]})`;
    case "lab":
      return `lab(${n[0]}% ${n[1]} ${n[2]})`;
    case "lch":
      return `lch(${n[0]}% ${n[1]} ${n[2]})`;
    default:
      return "";
  }
}
function Vy(n) {
  if (n.startsWith("#")) {
    const e = ab(n);
    return {
      space: "hex",
      values: e.values,
      hexValue: n,
      alpha: e.alpha
    };
  }
  const t = ab(n);
  return t.space ? t : null;
}
function WL(n) {
  return Object.keys(_y).includes(n.space);
}
class gt extends ee {
  /**
  * Creates an instance of the labeled field view class using a provided creator function
  * that provides the view to be labeled.
  *
  * @param locale The locale instance.
  * @param viewCreator A function that returns a {@link module:ui/view~View}
  * that will be labeled. The following arguments are passed to the creator function:
  *
  * * an instance of the `LabeledFieldView` to allow binding observable properties,
  * * an UID string that connects the {@link #labelView label} and the labeled field view in DOM,
  * * an UID string that connects the {@link #statusView status} and the labeled field view in DOM.
  */
  constructor(e, i) {
    super(e);
    /**
    * The field view that gets labeled.
    */
    f(this, "fieldView");
    /**
    * The label view instance that describes the entire view.
    */
    f(this, "labelView");
    /**
    * The status view for the {@link #fieldView}. It displays {@link #errorText} and
    * {@link #infoText}.
    */
    f(this, "statusView");
    /**
    * A collection of children of the internal wrapper element. Allows inserting additional DOM elements (views) next to
    * the {@link #fieldView} for easy styling (e.g. positioning).
    *
    * By default, the collection contains {@link #fieldView} and {@link #labelView}.
    */
    f(this, "fieldWrapperChildren");
    const s = `ck-labeled-field-view-${Bt()}`, r = `ck-labeled-field-view-status-${Bt()}`;
    this.fieldView = i(this, s, r), this.set("label", void 0), this.set("isEnabled", !0), this.set("isEmpty", !0), this.set("isFocused", !1), this.set("errorText", null), this.set("infoText", null), this.set("class", void 0), this.set("placeholder", void 0), this.labelView = this._createLabelView(s), this.statusView = this._createStatusView(r), this.fieldWrapperChildren = this.createCollection([
      this.fieldView,
      this.labelView
    ]), this.bind("_statusText").to(this, "errorText", this, "infoText", (a, l) => a || l);
    const o = this.bindTemplate;
    this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-labeled-field-view",
          o.to("class"),
          o.if("isEnabled", "ck-disabled", (a) => !a),
          o.if("isEmpty", "ck-labeled-field-view_empty"),
          o.if("isFocused", "ck-labeled-field-view_focused"),
          o.if("placeholder", "ck-labeled-field-view_placeholder"),
          o.if("errorText", "ck-error")
        ]
      },
      children: [
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-labeled-field-view__input-wrapper"
            ]
          },
          children: this.fieldWrapperChildren
        },
        this.statusView
      ]
    });
  }
  /**
  * Creates label view class instance and bind with view.
  *
  * @param id Unique id to set as labelView#for attribute.
  */
  _createLabelView(e) {
    const i = new gi(this.locale);
    return i.for = e, i.bind("text").to(this, "label"), i;
  }
  /**
  * Creates the status view instance. It displays {@link #errorText} and {@link #infoText}
  * next to the {@link #fieldView}. See {@link #_statusText}.
  *
  * @param statusUid Unique id of the status, shared with the {@link #fieldView view's}
  * `aria-describedby` attribute.
  */
  _createStatusView(e) {
    const i = new ee(this.locale), s = this.bindTemplate;
    return i.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-labeled-field-view__status",
          s.if("errorText", "ck-labeled-field-view__status_error"),
          s.if("_statusText", "ck-hidden", (r) => !r)
        ],
        id: e,
        role: s.if("errorText", "alert")
      },
      children: [
        {
          text: s.to("_statusText")
        }
      ]
    }), i;
  }
  /**
  * Focuses the {@link #fieldView}.
  */
  focus(e) {
    this.fieldView.focus(e);
  }
}
class qL extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Stores information about the editor UI focus and propagates it so various plugins and components
    * are unified as a focus group.
    */
    f(this, "focusTracker");
    this.set("value", void 0), this.set("id", void 0), this.set("placeholder", void 0), this.set("tabIndex", void 0), this.set("isReadOnly", !1), this.set("hasError", !1), this.set("ariaDescribedById", void 0), this.set("ariaLabel", void 0), this.focusTracker = new ze(), this.bind("isFocused").to(this.focusTracker), this.set("isEmpty", !0);
    const i = this.bindTemplate;
    this.setTemplate({
      tag: "input",
      attributes: {
        class: [
          "ck",
          "ck-input",
          i.if("isFocused", "ck-input_focused"),
          i.if("isEmpty", "ck-input-text_empty"),
          i.if("hasError", "ck-error")
        ],
        id: i.to("id"),
        placeholder: i.to("placeholder"),
        tabindex: i.to("tabIndex"),
        readonly: i.to("isReadOnly"),
        "aria-invalid": i.if("hasError", !0),
        "aria-describedby": i.to("ariaDescribedById"),
        "aria-label": i.to("ariaLabel")
      },
      on: {
        input: i.to((...s) => {
          this.fire("input", ...s), this._updateIsEmpty();
        }),
        change: i.to(this._updateIsEmpty.bind(this))
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.element), this._setDomElementValue(this.value), this._updateIsEmpty(), this.on("change:value", (e, i, s) => {
      this._setDomElementValue(s), this._updateIsEmpty();
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy();
  }
  /**
  * Moves the focus to the input and selects the value.
  */
  select() {
    this.element.select();
  }
  /**
  * Focuses the input.
  */
  focus() {
    this.element.focus();
  }
  /**
  * Resets the value of the input
  */
  reset() {
    this.value = this.element.value = "", this._updateIsEmpty();
  }
  /**
  * Updates the {@link #isEmpty} property value on demand.
  */
  _updateIsEmpty() {
    this.isEmpty = GL(this.element);
  }
  /**
  * Sets the `value` property of the {@link #element DOM element} on demand.
  */
  _setDomElementValue(e) {
    this.element.value = !e && e !== 0 ? "" : e;
  }
}
function GL(n) {
  return !n.value;
}
class Ly extends qL {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.set("inputMode", "text");
    const e = this.bindTemplate;
    this.extendTemplate({
      attributes: {
        inputmode: e.to("inputMode")
      }
    });
  }
}
class By extends Ly {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.extendTemplate({
      attributes: {
        type: "text",
        class: [
          "ck-input-text"
        ]
      }
    });
  }
}
class KL extends Ly {
  /**
  * Creates an instance of the input number view.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param options The options of the input.
  * @param options.min The value of the `min` DOM attribute (the lowest accepted value).
  * @param options.max The value of the `max` DOM attribute (the highest accepted value).
  * @param options.step The value of the `step` DOM attribute.
  */
  constructor(t, { min: e, max: i, step: s } = {}) {
    super(t);
    const r = this.bindTemplate;
    this.set("min", e), this.set("max", i), this.set("step", s), this.extendTemplate({
      attributes: {
        type: "number",
        class: [
          "ck-input-number"
        ],
        min: r.to("min"),
        max: r.to("max"),
        step: r.to("step")
      }
    });
  }
}
class ZL extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views in this panel.
    *
    * A common child type is the {@link module:ui/list/listview~ListView} and {@link module:ui/toolbar/toolbarview~ToolbarView}.
    * See {@link module:ui/dropdown/utils~addListToDropdown} and
    * {@link module:ui/dropdown/utils~addToolbarToDropdown} to learn more about child views of dropdowns.
    */
    f(this, "children");
    const i = this.bindTemplate;
    this.set("isVisible", !1), this.set("position", "se"), this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-reset",
          "ck-dropdown__panel",
          i.to("position", (s) => `ck-dropdown__panel_${s}`),
          i.if("isVisible", "ck-dropdown__panel-visible")
        ],
        tabindex: "-1"
      },
      children: this.children,
      on: {
        // Drag and drop in the panel should not break the selection in the editor.
        // https://github.com/ckeditor/ckeditor5-ui/issues/228
        selectstart: i.to((s) => {
          s.target.tagName.toLocaleLowerCase() !== "input" && s.preventDefault();
        })
      }
    });
  }
  /**
  * Focuses the first view in the {@link #children} collection.
  *
  * See also {@link module:ui/dropdown/dropdownpanelfocusable~DropdownPanelFocusable}.
  */
  focus() {
    if (this.children.length) {
      const e = this.children.first;
      typeof e.focus == "function" ? e.focus() : Ve("ui-dropdown-panel-focus-child-missing-focus", {
        childView: this.children.first,
        dropdownPanel: this
      });
    }
  }
  /**
  * Focuses the view element or last item in view collection on opening dropdown's panel.
  *
  * See also {@link module:ui/dropdown/dropdownpanelfocusable~DropdownPanelFocusable}.
  */
  focusLast() {
    if (this.children.length) {
      const e = this.children.last;
      typeof e.focusLast == "function" ? e.focusLast() : e.focus();
    }
  }
}
const Or = class Or extends ee {
  /**
  * Creates an instance of the dropdown.
  *
  * Also see {@link #render}.
  *
  * @param locale The localization services instance.
  */
  constructor(e, i, s) {
    super(e);
    /**
    * Button of the dropdown view. Clicking the button opens the {@link #panelView}.
    */
    f(this, "buttonView");
    /**
    * Panel of the dropdown. It opens when the {@link #buttonView} is
    * {@link module:ui/button/button~Button#event:execute executed} (i.e. clicked).
    *
    * Child views can be added to the panel's `children` collection:
    *
    * ```ts
    * dropdown.panelView.children.add( childView );
    * ```
    *
    * See {@link module:ui/dropdown/dropdownpanelview~DropdownPanelView#children} and
    * {@link module:ui/viewcollection~ViewCollection#add}.
    */
    f(this, "panelView");
    /**
    * Tracks information about the DOM focus in the dropdown.
    */
    f(this, "focusTracker");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}. It manages
    * keystrokes of the dropdown:
    *
    * * <kbd>▼</kbd> opens the dropdown,
    * * <kbd>◀</kbd> and <kbd>Esc</kbd> closes the dropdown.
    */
    f(this, "keystrokes");
    /**
    * A child {@link module:ui/list/listview~ListView list view} of the dropdown located
    * in its {@link module:ui/dropdown/dropdownview~DropdownView#panelView panel}.
    *
    * **Note**: Only supported when dropdown has list view added using {@link module:ui/dropdown/utils~addListToDropdown}.
    */
    f(this, "listView");
    /**
    * A child toolbar of the dropdown located in the
    * {@link module:ui/dropdown/dropdownview~DropdownView#panelView panel}.
    *
    * **Note**: Only supported when dropdown has a toolbar added using {@link module:ui/dropdown/utils~addToolbarToDropdown}.
    */
    f(this, "toolbarView");
    /**
    * A child menu component of the dropdown located
    * in its {@link module:ui/dropdown/dropdownview~DropdownView#panelView panel}.
    *
    * **Note**: Only supported when dropdown has a menu added using {@link module:ui/dropdown/utils~addMenuToDropdown}.
    */
    f(this, "menuView");
    const r = this.bindTemplate;
    this.buttonView = i, this.panelView = s, this.set("isOpen", !1), this.set("isEnabled", !0), this.set("class", void 0), this.set("id", void 0), this.set("panelPosition", "auto"), this.panelView.bind("isVisible").to(this, "isOpen"), this.keystrokes = new Ze(), this.focusTracker = new ze(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-dropdown",
          r.to("class"),
          r.if("isEnabled", "ck-disabled", (o) => !o)
        ],
        id: r.to("id"),
        "aria-describedby": r.to("ariaDescribedById")
      },
      children: [
        i,
        s
      ]
    }), i.extendTemplate({
      attributes: {
        class: [
          "ck-dropdown__button"
        ],
        "data-cke-tooltip-disabled": r.to("isOpen")
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.buttonView.element), this.focusTracker.add(this.panelView.element), this.listenTo(this.buttonView, "open", () => {
      this.isOpen = !this.isOpen;
    }), this.on("change:isOpen", (i, s, r) => {
      if (r)
        if (this.panelPosition === "auto") {
          const o = Or._getOptimalPosition({
            element: this.panelView.element,
            target: this.buttonView.element,
            fitInViewport: !0,
            positions: this._panelPositions
          });
          this.panelView.position = o ? o.name : this._defaultPanelPositionName;
        } else
          this.panelView.position = this.panelPosition;
    }), this.keystrokes.listenTo(this.element);
    const e = (i, s) => {
      this.isOpen && (this.isOpen = !1, s());
    };
    this.keystrokes.set("arrowdown", (i, s) => {
      this.buttonView.isEnabled && !this.isOpen && (this.isOpen = !0, s());
    }), this.keystrokes.set("arrowright", (i, s) => {
      this.isOpen && s();
    }), this.keystrokes.set("arrowleft", e), this.keystrokes.set("esc", e);
  }
  /**
  * Focuses the {@link #buttonView}.
  */
  focus() {
    this.buttonView.focus();
  }
  /**
  * Returns {@link #panelView panel} positions to be used by the
  * {@link module:utils/dom/position~getOptimalPosition `getOptimalPosition()`}
  * utility considering the direction of the language the UI of the editor is displayed in.
  */
  get _panelPositions() {
    const { south: e, north: i, southEast: s, southWest: r, northEast: o, northWest: a, southMiddleEast: l, southMiddleWest: c, northMiddleEast: u, northMiddleWest: d } = Or.defaultPanelPositions;
    return this.locale.uiLanguageDirection !== "rtl" ? [
      s,
      r,
      l,
      c,
      e,
      o,
      a,
      u,
      d,
      i
    ] : [
      r,
      s,
      c,
      l,
      e,
      a,
      o,
      d,
      u,
      i
    ];
  }
  /**
  * Returns the default position of the dropdown panel based on the direction of the UI language.
  * It is used when the {@link #panelPosition} is set to `'auto'` and the panel has not found a
  * suitable position to fit into the viewport.
  */
  get _defaultPanelPositionName() {
    return this.locale.uiLanguageDirection === "rtl" ? "sw" : "se";
  }
};
/**
* A set of positioning functions used by the dropdown view to determine
* the optimal position (i.e. fitting into the browser viewport) of its
* {@link module:ui/dropdown/dropdownview~DropdownView#panelView panel} when
* {@link module:ui/dropdown/dropdownview~DropdownView#panelPosition} is set to 'auto'`.
*
* The available positioning functions are as follow:
*
* **South**
*
* * `south`
*
* ```
*			[ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* * `southEast`
*
* ```
*		[ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* * `southWest`
*
* ```
*		         [ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* * `southMiddleEast`
*
* ```
*		  [ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* * `southMiddleWest`
*
* ```
*		       [ Button ]
*		+-----------------+
*		|      Panel      |
*		+-----------------+
* ```
*
* **North**
*
* * `north`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		    [ Button ]
* ```
*
* * `northEast`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		[ Button ]
* ```
*
* * `northWest`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		         [ Button ]
* ```
*
* * `northMiddleEast`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		  [ Button ]
* ```
*
* * `northMiddleWest`
*
* ```
*		+-----------------+
*		|      Panel      |
*		+-----------------+
*		       [ Button ]
* ```
*
* Positioning functions are compatible with {@link module:utils/dom/position~DomPoint}.
*
* The name that position function returns will be reflected in dropdown panel's class that
* controls its placement. See {@link module:ui/dropdown/dropdownview~DropdownView#panelPosition}
* to learn more.
*/
f(Or, "defaultPanelPositions", {
  south: (e, i) => ({
    top: e.bottom,
    left: e.left - (i.width - e.width) / 2,
    name: "s"
  }),
  southEast: (e) => ({
    top: e.bottom,
    left: e.left,
    name: "se"
  }),
  southWest: (e, i) => ({
    top: e.bottom,
    left: e.left - i.width + e.width,
    name: "sw"
  }),
  southMiddleEast: (e, i) => ({
    top: e.bottom,
    left: e.left - (i.width - e.width) / 4,
    name: "sme"
  }),
  southMiddleWest: (e, i) => ({
    top: e.bottom,
    left: e.left - (i.width - e.width) * 3 / 4,
    name: "smw"
  }),
  north: (e, i) => ({
    top: e.top - i.height,
    left: e.left - (i.width - e.width) / 2,
    name: "n"
  }),
  northEast: (e, i) => ({
    top: e.top - i.height,
    left: e.left,
    name: "ne"
  }),
  northWest: (e, i) => ({
    top: e.top - i.height,
    left: e.left - i.width + e.width,
    name: "nw"
  }),
  northMiddleEast: (e, i) => ({
    top: e.top - i.height,
    left: e.left - (i.width - e.width) / 4,
    name: "nme"
  }),
  northMiddleWest: (e, i) => ({
    top: e.top - i.height,
    left: e.left - (i.width - e.width) * 3 / 4,
    name: "nmw"
  })
}), /**
* A function used to calculate the optimal position for the dropdown panel.
*/
f(Or, "_getOptimalPosition", wf);
let nc = Or;
class My extends le {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * An icon that displays arrow to indicate a dropdown button.
    */
    f(this, "arrowView");
    this.arrowView = this._createArrowView(), this.extendTemplate({
      attributes: {
        "aria-haspopup": !0,
        "aria-expanded": this.bindTemplate.to("isOn", (i) => String(i))
      }
    }), this.delegate("execute").to(this, "open");
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.children.add(this.arrowView);
  }
  /**
  * Creates a {@link module:ui/icon/iconview~IconView} instance as {@link #arrowView}.
  */
  _createArrowView() {
    const e = new Qi();
    return e.content = Xc, e.extendTemplate({
      attributes: {
        class: "ck-dropdown__arrow"
      }
    }), e;
  }
}
class Wn extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views inside of the list item {@link #element}.
    */
    f(this, "children");
    const i = this.bindTemplate;
    this.set("isVisible", !0), this.children = this.createCollection(), this.setTemplate({
      tag: "li",
      attributes: {
        class: [
          "ck",
          "ck-list__item",
          i.if("isVisible", "ck-hidden", (s) => !s)
        ],
        role: "presentation"
      },
      children: this.children
    });
  }
  /**
  * Focuses the list item.
  */
  focus() {
    this.children.first && this.children.first.focus();
  }
}
class Ba extends ee {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.setTemplate({
      tag: "li",
      attributes: {
        class: [
          "ck",
          "ck-list__separator"
        ]
      }
    });
  }
}
class Vl extends ee {
  /**
  * Creates an instance of the list item group view class.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param labelView The instance of the group's label. If not provided, an instance of
  * {@link module:ui/label/labelview~LabelView} is used.
  */
  constructor(e, i = new gi()) {
    super(e);
    /**
    * Label of the group view. Its text is configurable using the {@link #label label attribute}.
    *
    * If a custom label view is not passed in `ListItemGroupView` constructor, the label is an instance
    * of {@link module:ui/label/labelview~LabelView}.
    */
    f(this, "labelView");
    /**
    * Collection of the child list items inside this group.
    */
    f(this, "items");
    /**
    * Collection of the child elements of the group.
    */
    f(this, "children");
    const s = this.bindTemplate, r = new Qc(e);
    this.set({
      label: "",
      isVisible: !0
    }), this.labelView = i, this.labelView.bind("text").to(this, "label"), this.children = this.createCollection(), this.children.addMany([
      this.labelView,
      r
    ]), r.set({
      role: "group",
      ariaLabelledBy: i.id
    }), r.focusTracker.destroy(), r.keystrokes.destroy(), this.items = r.items, this.setTemplate({
      tag: "li",
      attributes: {
        role: "presentation",
        class: [
          "ck",
          "ck-list__group",
          s.if("isVisible", "ck-hidden", (o) => !o)
        ]
      },
      children: this.children
    });
  }
  /**
  * Focuses the list item (which is not a separator).
  */
  focus() {
    if (this.items) {
      const e = this.items.find((i) => !(i instanceof Ba));
      e && e.focus();
    }
  }
}
class Qc extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The collection of focusable views in the list. It is used to determine accessible navigation
    * between the {@link module:ui/list/listitemview~ListItemView list items} and
    * {@link module:ui/list/listitemgroupview~ListItemGroupView list groups}.
    */
    f(this, "focusables");
    /**
    * Collection of the child list views.
    */
    f(this, "items");
    /**
    * Tracks information about DOM focus in the list.
    */
    f(this, "focusTracker");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * Helps cycling over focusable {@link #items} in the list.
    */
    f(this, "_focusCycler");
    /**
    * A cached map of {@link module:ui/list/listitemgroupview~ListItemGroupView} to `change` event listeners for their `items`.
    * Used for accessibility and keyboard navigation purposes.
    */
    f(this, "_listItemGroupToChangeListeners", /* @__PURE__ */ new WeakMap());
    const i = this.bindTemplate;
    this.focusables = new Et(), this.items = this.createCollection(), this.focusTracker = new ze(), this.keystrokes = new Ze(), this._focusCycler = new Pt({
      focusables: this.focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate list items backwards using the arrowup key.
        focusPrevious: "arrowup",
        // Navigate toolbar items forwards using the arrowdown key.
        focusNext: "arrowdown"
      }
    }), this.set("ariaLabel", void 0), this.set("ariaLabelledBy", void 0), this.set("role", void 0), this.setTemplate({
      tag: "ul",
      attributes: {
        class: [
          "ck",
          "ck-reset",
          "ck-list"
        ],
        role: i.to("role"),
        "aria-label": i.to("ariaLabel"),
        "aria-labelledby": i.to("ariaLabelledBy")
      },
      children: this.items
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    for (const e of this.items)
      e instanceof Vl ? this._registerFocusableItemsGroup(e) : e instanceof Wn && this._registerFocusableListItem(e);
    this.items.on("change", (e, i) => {
      for (const s of i.removed)
        s instanceof Vl ? this._deregisterFocusableItemsGroup(s) : s instanceof Wn && this._deregisterFocusableListItem(s);
      for (const s of Array.from(i.added).reverse())
        s instanceof Vl ? this._registerFocusableItemsGroup(s, i.index) : this._registerFocusableListItem(s, i.index);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the first focusable in {@link #items}.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Focuses the first focusable in {@link #items}.
  */
  focusFirst() {
    this._focusCycler.focusFirst();
  }
  /**
  * Focuses the last focusable in {@link #items}.
  */
  focusLast() {
    this._focusCycler.focusLast();
  }
  /**
  * Registers a list item view in the focus tracker.
  *
  * @param item The list item view to be registered.
  * @param index Index of the list item view in the {@link #items} collection. If not specified, the item will be added at the end.
  */
  _registerFocusableListItem(e, i) {
    this.focusTracker.add(e.element), this.focusables.add(e, i);
  }
  /**
  * Removes a list item view from the focus tracker.
  *
  * @param item The list item view to be removed.
  */
  _deregisterFocusableListItem(e) {
    this.focusTracker.remove(e.element), this.focusables.remove(e);
  }
  /**
  * Gets a callback that will be called when the `items` collection of a {@link module:ui/list/listitemgroupview~ListItemGroupView}
  * change.
  *
  * @param groupView The group view for which the callback will be created.
  * @returns The callback function to be used for the items `change` event listener in a group.
  */
  _getOnGroupItemsChangeCallback(e) {
    return (i, s) => {
      for (const r of s.removed)
        this._deregisterFocusableListItem(r);
      for (const r of Array.from(s.added).reverse())
        this._registerFocusableListItem(r, this.items.getIndex(e) + s.index);
    };
  }
  /**
  * Registers a list item group view (and its children) in the focus tracker.
  *
  * @param groupView A group view to be registered.
  * @param groupIndex Index of the group view in the {@link #items} collection. If not specified, the group will be added at the end.
  */
  _registerFocusableItemsGroup(e, i) {
    Array.from(e.items).forEach((r, o) => {
      const a = typeof i < "u" ? i + o : void 0;
      this._registerFocusableListItem(r, a);
    });
    const s = this._getOnGroupItemsChangeCallback(e);
    this._listItemGroupToChangeListeners.set(e, s), e.items.on("change", s);
  }
  /**
  * Removes a list item group view (and its children) from the focus tracker.
  *
  * @param groupView The group view to be removed.
  */
  _deregisterFocusableItemsGroup(e) {
    for (const i of e.items)
      this._deregisterFocusableListItem(i);
    e.items.off("change", this._listItemGroupToChangeListeners.get(e)), this._listItemGroupToChangeListeners.delete(e);
  }
}
const Ab = /* @__PURE__ */ to("px"), YL = {
  top: -99999,
  left: -99999,
  name: "arrowless",
  config: {
    withArrow: !1
  }
}, yi = class yi extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of the child views that creates the balloon panel contents.
    */
    f(this, "content");
    /**
    * A callback that starts pinning the panel when {@link #isVisible} gets
    * `true`. Used by {@link #pin}.
    *
    * @private
    */
    f(this, "_pinWhenIsVisibleCallback");
    /**
    * An instance of resize observer used to detect if target element is still visible.
    */
    f(this, "_resizeObserver");
    const i = this.bindTemplate;
    this.set("top", 0), this.set("left", 0), this.set("position", "arrow_nw"), this.set("isVisible", !1), this.set("withArrow", !0), this.set("class", void 0), this._pinWhenIsVisibleCallback = null, this._resizeObserver = null, this.content = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-balloon-panel",
          i.to("position", (s) => `ck-balloon-panel_${s}`),
          i.if("isVisible", "ck-balloon-panel_visible"),
          i.if("withArrow", "ck-balloon-panel_with-arrow"),
          i.to("class")
        ],
        style: {
          top: i.to("top", Ab),
          left: i.to("left", Ab)
        }
      },
      children: this.content
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this.hide(), super.destroy();
  }
  /**
  * Shows the panel.
  *
  * See {@link #isVisible}.
  */
  show() {
    this.isVisible = !0;
  }
  /**
  * Hides the panel.
  *
  * See {@link #isVisible}.
  */
  hide() {
    this.isVisible = !1;
  }
  /**
  * Attaches the panel to a specified {@link module:utils/dom/position~Options#target} with a
  * smart positioning heuristics that chooses from available positions to make sure the panel
  * is visible to the user i.e. within the limits of the viewport.
  *
  * This method accepts configuration {@link module:utils/dom/position~Options options}
  * to set the `target`, optional `limiter` and `positions` the balloon should choose from.
  *
  * ```ts
  * const panel = new BalloonPanelView( locale );
  * const positions = BalloonPanelView.defaultPositions;
  *
  * panel.render();
  *
  * // Attach the panel to an element with the "target" id DOM.
  * panel.attachTo( {
  * 	target: document.querySelector( '#target' ),
  * 	positions: [
  * 		positions.northArrowSouth,
  * 		positions.southArrowNorth
  * 	]
  * } );
  * ```
  *
  * **Note**: Attaching the panel will also automatically {@link #show} it.
  *
  * **Note**: An attached panel will not follow its target when the window is scrolled or resized.
  * See the {@link #pin} method for a more permanent positioning strategy.
  *
  * @param options Positioning options compatible with {@link module:utils/dom/position~getOptimalPosition}.
  * Default `positions` array is {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.defaultPositions}.
  * @returns Whether the balloon was shown and successfully attached or not. Attaching can fail if the target
  * provided in the options is invisible (e.g. element detached from DOM).
  */
  attachTo(e) {
    const i = Ll(e.target);
    if (i && !yn(i))
      return !1;
    this.show();
    const s = yi.defaultPositions, r = Object.assign({}, {
      element: this.element,
      positions: [
        s.southArrowNorth,
        s.southArrowNorthMiddleWest,
        s.southArrowNorthMiddleEast,
        s.southArrowNorthWest,
        s.southArrowNorthEast,
        s.northArrowSouth,
        s.northArrowSouthMiddleWest,
        s.northArrowSouthMiddleEast,
        s.northArrowSouthWest,
        s.northArrowSouthEast,
        s.viewportStickyNorth
      ],
      limiter: ae.document.body,
      fitInViewport: !0
    }, e), o = yi._getOptimalPosition(r) || YL, a = parseInt(o.left), l = parseInt(o.top), c = o.name, u = o.config || {}, { withArrow: d = !0 } = u;
    return this.top = l, this.left = a, this.position = c, this.withArrow = d, !0;
  }
  /**
  * Works the same way as the {@link #attachTo} method except that the position of the panel is
  * continuously updated when:
  *
  * * any ancestor of the {@link module:utils/dom/position~Options#target}
  * or {@link module:utils/dom/position~Options#limiter} is scrolled,
  * * the browser window gets resized or scrolled.
  *
  * Thanks to that, the panel always sticks to the {@link module:utils/dom/position~Options#target}
  * and is immune to the changing environment.
  *
  * ```ts
  * const panel = new BalloonPanelView( locale );
  * const positions = BalloonPanelView.defaultPositions;
  *
  * panel.render();
  *
  * // Pin the panel to an element with the "target" id DOM.
  * panel.pin( {
  * 	target: document.querySelector( '#target' ),
  * 	positions: [
  * 		positions.northArrowSouth,
  * 		positions.southArrowNorth
  * 	]
  * } );
  * ```
  *
  * To leave the pinned state, use the {@link #unpin} method.
  *
  * **Note**: Pinning the panel will also automatically {@link #show} it.
  *
  * @param options Positioning options compatible with {@link module:utils/dom/position~getOptimalPosition}.
  * Default `positions` array is {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.defaultPositions}.
  */
  pin(e) {
    this.unpin(), this._startPinning(e) && (this._pinWhenIsVisibleCallback = () => {
      this.isVisible ? this._startPinning(e) : this._stopPinning();
    }, this.listenTo(this, "change:isVisible", this._pinWhenIsVisibleCallback));
  }
  /**
  * Stops pinning the panel, as set up by {@link #pin}.
  */
  unpin() {
    this._pinWhenIsVisibleCallback && (this._stopPinning(), this.stopListening(this, "change:isVisible", this._pinWhenIsVisibleCallback), this._pinWhenIsVisibleCallback = null, this.hide());
  }
  /**
  * Starts managing the pinned state of the panel. See {@link #pin}.
  *
  * @param options Positioning options compatible with {@link module:utils/dom/position~getOptimalPosition}.
  * @returns Whether the balloon was shown and successfully attached or not. Attaching can fail if the target
  * provided in the options is invisible (e.g. element detached from DOM).
  */
  _startPinning(e) {
    if (!this.attachTo(e))
      return !1;
    let i = Ll(e.target);
    const s = e.limiter ? Ll(e.limiter) : ae.document.body;
    if (this.listenTo(ae.document, "scroll", (r, o) => {
      const a = o.target, l = i && a.contains(i), c = s && a.contains(s);
      (l || c || !i || !s) && this.attachTo(e);
    }, {
      useCapture: !0
    }), this.listenTo(ae.window, "resize", () => {
      this.attachTo(e);
    }), !this._resizeObserver && (i && mt(i) && (i = i.parentElement), i)) {
      const r = () => {
        yn(i) || this.unpin();
      };
      this._resizeObserver = new $r(i, r);
    }
    return !0;
  }
  /**
  * Stops managing the pinned state of the panel. See {@link #pin}.
  */
  _stopPinning() {
    this.stopListening(ae.document, "scroll"), this.stopListening(ae.window, "resize"), this._resizeObserver && (this._resizeObserver.destroy(), this._resizeObserver = null);
  }
  /**
  * Returns available {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView}
  * {@link module:utils/dom/position~PositioningFunction positioning functions} adjusted by the specific offsets.
  *
  * @internal
  * @param options Options to generate positions. If not specified, this helper will simply return
  * {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.defaultPositions}.
  * @param options.sideOffset A custom side offset (in pixels) of each position. If
  * not specified, {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.arrowSideOffset the default value}
  * will be used.
  * @param options.heightOffset A custom height offset (in pixels) of each position. If
  * not specified, {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.arrowHeightOffset the default value}
  * will be used.
  * @param options.stickyVerticalOffset A custom offset (in pixels) of the `viewportStickyNorth` positioning function.
  * If not specified, {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.stickyVerticalOffset the default value}
  * will be used.
  * @param options.config Additional configuration of the balloon balloon panel view.
  * Currently only {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView#withArrow} is supported. Learn more
  * about {@link module:utils/dom/position~PositioningFunction positioning functions}.
  */
  static generatePositions(e = {}) {
    const { sideOffset: i = yi.arrowSideOffset, heightOffset: s = yi.arrowHeightOffset, stickyVerticalOffset: r = yi.stickyVerticalOffset, config: o } = e;
    return {
      // ------- North west
      northWestArrowSouthWest: (c, u) => ({
        top: a(c, u),
        left: c.left - i,
        name: "arrow_sw",
        ...o && {
          config: o
        }
      }),
      northWestArrowSouthMiddleWest: (c, u) => ({
        top: a(c, u),
        left: c.left - u.width * 0.25 - i,
        name: "arrow_smw",
        ...o && {
          config: o
        }
      }),
      northWestArrowSouth: (c, u) => ({
        top: a(c, u),
        left: c.left - u.width / 2,
        name: "arrow_s",
        ...o && {
          config: o
        }
      }),
      northWestArrowSouthMiddleEast: (c, u) => ({
        top: a(c, u),
        left: c.left - u.width * 0.75 + i,
        name: "arrow_sme",
        ...o && {
          config: o
        }
      }),
      northWestArrowSouthEast: (c, u) => ({
        top: a(c, u),
        left: c.left - u.width + i,
        name: "arrow_se",
        ...o && {
          config: o
        }
      }),
      // ------- North
      northArrowSouthWest: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - i,
        name: "arrow_sw",
        ...o && {
          config: o
        }
      }),
      northArrowSouthMiddleWest: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - u.width * 0.25 - i,
        name: "arrow_smw",
        ...o && {
          config: o
        }
      }),
      northArrowSouth: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - u.width / 2,
        name: "arrow_s",
        ...o && {
          config: o
        }
      }),
      northArrowSouthMiddleEast: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - u.width * 0.75 + i,
        name: "arrow_sme",
        ...o && {
          config: o
        }
      }),
      northArrowSouthEast: (c, u) => ({
        top: a(c, u),
        left: c.left + c.width / 2 - u.width + i,
        name: "arrow_se",
        ...o && {
          config: o
        }
      }),
      // ------- North east
      northEastArrowSouthWest: (c, u) => ({
        top: a(c, u),
        left: c.right - i,
        name: "arrow_sw",
        ...o && {
          config: o
        }
      }),
      northEastArrowSouthMiddleWest: (c, u) => ({
        top: a(c, u),
        left: c.right - u.width * 0.25 - i,
        name: "arrow_smw",
        ...o && {
          config: o
        }
      }),
      northEastArrowSouth: (c, u) => ({
        top: a(c, u),
        left: c.right - u.width / 2,
        name: "arrow_s",
        ...o && {
          config: o
        }
      }),
      northEastArrowSouthMiddleEast: (c, u) => ({
        top: a(c, u),
        left: c.right - u.width * 0.75 + i,
        name: "arrow_sme",
        ...o && {
          config: o
        }
      }),
      northEastArrowSouthEast: (c, u) => ({
        top: a(c, u),
        left: c.right - u.width + i,
        name: "arrow_se",
        ...o && {
          config: o
        }
      }),
      // ------- South west
      southWestArrowNorthWest: (c) => ({
        top: l(c),
        left: c.left - i,
        name: "arrow_nw",
        ...o && {
          config: o
        }
      }),
      southWestArrowNorthMiddleWest: (c, u) => ({
        top: l(c),
        left: c.left - u.width * 0.25 - i,
        name: "arrow_nmw",
        ...o && {
          config: o
        }
      }),
      southWestArrowNorth: (c, u) => ({
        top: l(c),
        left: c.left - u.width / 2,
        name: "arrow_n",
        ...o && {
          config: o
        }
      }),
      southWestArrowNorthMiddleEast: (c, u) => ({
        top: l(c),
        left: c.left - u.width * 0.75 + i,
        name: "arrow_nme",
        ...o && {
          config: o
        }
      }),
      southWestArrowNorthEast: (c, u) => ({
        top: l(c),
        left: c.left - u.width + i,
        name: "arrow_ne",
        ...o && {
          config: o
        }
      }),
      // ------- South
      southArrowNorthWest: (c) => ({
        top: l(c),
        left: c.left + c.width / 2 - i,
        name: "arrow_nw",
        ...o && {
          config: o
        }
      }),
      southArrowNorthMiddleWest: (c, u) => ({
        top: l(c),
        left: c.left + c.width / 2 - u.width * 0.25 - i,
        name: "arrow_nmw",
        ...o && {
          config: o
        }
      }),
      southArrowNorth: (c, u) => ({
        top: l(c),
        left: c.left + c.width / 2 - u.width / 2,
        name: "arrow_n",
        ...o && {
          config: o
        }
      }),
      southArrowNorthMiddleEast: (c, u) => ({
        top: l(c),
        left: c.left + c.width / 2 - u.width * 0.75 + i,
        name: "arrow_nme",
        ...o && {
          config: o
        }
      }),
      southArrowNorthEast: (c, u) => ({
        top: l(c),
        left: c.left + c.width / 2 - u.width + i,
        name: "arrow_ne",
        ...o && {
          config: o
        }
      }),
      // ------- South east
      southEastArrowNorthWest: (c) => ({
        top: l(c),
        left: c.right - i,
        name: "arrow_nw",
        ...o && {
          config: o
        }
      }),
      southEastArrowNorthMiddleWest: (c, u) => ({
        top: l(c),
        left: c.right - u.width * 0.25 - i,
        name: "arrow_nmw",
        ...o && {
          config: o
        }
      }),
      southEastArrowNorth: (c, u) => ({
        top: l(c),
        left: c.right - u.width / 2,
        name: "arrow_n",
        ...o && {
          config: o
        }
      }),
      southEastArrowNorthMiddleEast: (c, u) => ({
        top: l(c),
        left: c.right - u.width * 0.75 + i,
        name: "arrow_nme",
        ...o && {
          config: o
        }
      }),
      southEastArrowNorthEast: (c, u) => ({
        top: l(c),
        left: c.right - u.width + i,
        name: "arrow_ne",
        ...o && {
          config: o
        }
      }),
      // ------- West
      westArrowEast: (c, u) => ({
        top: c.top + c.height / 2 - u.height / 2,
        left: c.left - u.width - s,
        name: "arrow_e",
        ...o && {
          config: o
        }
      }),
      // ------- East
      eastArrowWest: (c, u) => ({
        top: c.top + c.height / 2 - u.height / 2,
        left: c.right + s,
        name: "arrow_w",
        ...o && {
          config: o
        }
      }),
      // ------- Sticky
      viewportStickyNorth: (c, u, d, h) => {
        const m = h || d;
        return !c.getIntersection(m) || m.height - c.height > r ? null : {
          top: m.top + r,
          left: c.left + c.width / 2 - u.width / 2,
          name: "arrowless",
          config: {
            withArrow: !1,
            ...o
          }
        };
      }
    };
    function a(c, u) {
      return c.top - u.height - s;
    }
    function l(c) {
      return c.bottom + s;
    }
  }
};
/**
* A side offset of the arrow tip from the edge of the balloon. Controlled by CSS.
*
* ```
*		 ┌───────────────────────┐
*		 │                       │
*		 │         Balloon       │
*		 │         Content       │
*		 │                       │
*		 └──+    +───────────────┘
*		 |   \  /
*		 |    \/
*		>┼─────┼< ─────────────────────── side offset
*
* ```
*
* @default 25
*/
f(yi, "arrowSideOffset", 25), /**
 * A height offset of the arrow from the edge of the balloon. Controlled by CSS.
 *
 * ```
 *		 ┌───────────────────────┐
 *		 │                       │
 *		 │         Balloon       │
 *		 │         Content       │      ╱-- arrow height offset
 *		 │                       │      V
 *		 └──+    +───────────────┘ --- ─┼───────
 *		     \  /                       │
 *		      \/                        │
 *		────────────────────────────────┼───────
 *		                                ^
 *
 *
 *		>┼────┼<  arrow height offset
 *		 │    │
 *		 │    ┌────────────────────────┐
 *		 │    │                        │
 *		 │   ╱                         │
 *		 │ ╱            Balloon        │
 *		 │ ╲            Content        │
 *		 │   ╲                         │
 *		 │    │                        │
 *		 │    └────────────────────────┘
 * ```
 *
 * @default 10
*/
f(yi, "arrowHeightOffset", 10), /**
* A vertical offset of the balloon panel from the edge of the viewport if sticky.
* It helps in accessing toolbar buttons underneath the balloon panel.
*
* ```
*		  ┌───────────────────────────────────────────────────┐
*		  │                      Target                       │
*		  │                                                   │
*		  │                            /── vertical offset    │
*		┌─────────────────────────────V─────────────────────────┐
*		│ Toolbar            ┌─────────────┐                    │
*		├────────────────────│   Balloon   │────────────────────┤
*		│ │                  └─────────────┘                  │ │
*		│ │                                                   │ │
*		│ │                                                   │ │
*		│ │                                                   │ │
*		│ └───────────────────────────────────────────────────┘ │
*		│                        Viewport                       │
*		└───────────────────────────────────────────────────────┘
* ```
*
* @default 20
*/
f(yi, "stickyVerticalOffset", 20), /**
* Function used to calculate the optimal position for the balloon.
*/
f(yi, "_getOptimalPosition", wf), /**
* A default set of positioning functions used by the balloon panel view
* when attaching using the {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView#attachTo} method.
*
* The available positioning functions are as follows:
*
* **North west**
*
* * `northWestArrowSouthWest`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		 V
*		 [ Target ]
* ```
*
* * `northWestArrowSouthMiddleWest`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		    V
*		    [ Target ]
* ```
*
* * `northWestArrowSouth`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		         V
*		         [ Target ]
* ```
*
* * `northWestArrowSouthMiddleEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		             V
*		             [ Target ]
* ```
*
* * `northWestArrowSouthEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		                 V
*		                 [ Target ]
* ```
*
* **North**
*
* * `northArrowSouthWest`
*
* ```
*		    +-----------------+
*		    |     Balloon     |
*		    +-----------------+
*		     V
*		[ Target ]
* ```
*
* * `northArrowSouthMiddleWest`
*
* ```
*		 +-----------------+
*		 |     Balloon     |
*		 +-----------------+
*		     V
*		[ Target ]
* ```
* * `northArrowSouth`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		         V
*		    [ Target ]
* ```
*
* * `northArrowSouthMiddleEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		             V
*		        [ Target ]
* ```
*
* * `northArrowSouthEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		                V
*		           [ Target ]
* ```
*
* **North east**
*
* * `northEastArrowSouthWest`
*
* ```
*		        +-----------------+
*		        |     Balloon     |
*		        +-----------------+
*		         V
*		[ Target ]
* ```
*
* * `northEastArrowSouthMiddleWest`
*
* ```
*		     +-----------------+
*		     |     Balloon     |
*		     +-----------------+
*		         V
*		[ Target ]
* ```
*
* * `northEastArrowSouth`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		         V
*		[ Target ]
* ```
*
* * `northEastArrowSouthMiddleEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		             V
*		    [ Target ]
* ```
*
* * `northEastArrowSouthEast`
*
* ```
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
*		                 V
*		        [ Target ]
* ```
*
* **South**
*
* * `southArrowNorthWest`
*
* ```
*		[ Target ]
*		     ^
*		    +-----------------+
*		    |     Balloon     |
*		    +-----------------+
* ```
*
* * `southArrowNorthMiddleWest`
*
* ```
*		   [ Target ]
*		        ^
*		    +-----------------+
*		    |     Balloon     |
*		    +-----------------+
* ```
*
* * `southArrowNorth`
*
* ```
*		    [ Target ]
*		         ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southArrowNorthMiddleEast`
*
* ```
*		            [ Target ]
*		                 ^
*		   +-----------------+
*		   |     Balloon     |
*		   +-----------------+
* ```
*
* * `southArrowNorthEast`
*
* ```
*		            [ Target ]
*		                 ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* **South west**
*
* * `southWestArrowNorthWest`
*
*
* ```
*		 [ Target ]
*		 ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southWestArrowNorthMiddleWest`
*
* ```
*		     [ Target ]
*		     ^
*		 +-----------------+
*		 |     Balloon     |
*		 +-----------------+
* ```
*
* * `southWestArrowNorth`
*
* ```
*		         [ Target ]
*		         ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southWestArrowNorthMiddleEast`
*
* ```
*		              [ Target ]
*		              ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southWestArrowNorthEast`
*
* ```
*		                 [ Target ]
*		                 ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* **South east**
*
* * `southEastArrowNorthWest`
*
* ```
*		[ Target ]
*		         ^
*		        +-----------------+
*		        |     Balloon     |
*		        +-----------------+
* ```
*
* * `southEastArrowNorthMiddleWest`
*
* ```
*		   [ Target ]
*		            ^
*		        +-----------------+
*		        |     Balloon     |
*		        +-----------------+
* ```
*
* * `southEastArrowNorth`
*
* ```
*		[ Target ]
*		         ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southEastArrowNorthMiddleEast`
*
* ```
*		     [ Target ]
*		              ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* * `southEastArrowNorthEast`
*
* ```
*		        [ Target ]
*		                 ^
*		+-----------------+
*		|     Balloon     |
*		+-----------------+
* ```
*
* **West**
*
* * `westArrowEast`
*
* ```
*		+-----------------+
*		|     Balloon     |>[ Target ]
*		+-----------------+
* ```
*
* **East**
*
* * `eastArrowWest`
*
* ```
*		           +-----------------+
*		[ Target ]<|     Balloon     |
*		           +-----------------+
* ```
*
* **Sticky**
*
* * `viewportStickyNorth`
*
* ```
*		    +---------------------------+
*		    |        [ Target ]         |
*		    |                           |
*		+-----------------------------------+
*		|   |    +-----------------+    |   |
*		|   |    |     Balloon     |    |   |
*		|   |    +-----------------+    |   |
*		|   |                           |   |
*		|   |                           |   |
*		|   |                           |   |
*		|   |                           |   |
*		|   +---------------------------+   |
*		|             Viewport              |
*		+-----------------------------------+
* ```
*
* See {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView#attachTo}.
*
* Positioning functions must be compatible with {@link module:utils/dom/position~DomPoint}.
*
* Default positioning functions with customized offsets can be generated using
* {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView.generatePositions}.
*
* The name that the position function returns will be reflected in the balloon panel's class that
* controls the placement of the "arrow". See {@link #position} to learn more.
*/
f(yi, "defaultPositions", /* @__PURE__ */ yi.generatePositions());
let Dt = yi;
function Ll(n) {
  return Hn(n) ? n : jc(n) ? n.commonAncestorContainer : typeof n == "function" ? Ll(n()) : null;
}
class zf extends ee {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-toolbar__separator"
        ]
      }
    });
  }
}
class JL extends ee {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-toolbar__line-break"
        ]
      }
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function XL(n) {
  return n.bindTemplate.to((t) => {
    t.target === n.element && t.preventDefault();
  });
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Hf(n) {
  if (Array.isArray(n))
    return {
      items: n,
      removeItems: []
    };
  const t = {
    items: [],
    removeItems: []
  };
  return n ? {
    ...t,
    ...n
  } : t;
}
const QL = {
  alignLeft: Z.alignLeft,
  bold: Z.bold,
  importExport: Z.importExport,
  paragraph: Z.paragraph,
  plus: Z.plus,
  text: Z.text,
  threeVerticalDots: Z.threeVerticalDots,
  pilcrow: Z.pilcrow,
  dragIndicator: Z.dragIndicator
};
class Us extends ee {
  /**
  * Creates an instance of the {@link module:ui/toolbar/toolbarview~ToolbarView} class.
  *
  * Also see {@link #render}.
  *
  * @param locale The localization services instance.
  * @param options Configuration options of the toolbar.
  */
  constructor(e, i) {
    super(e);
    /**
    * A reference to the options object passed to the constructor.
    */
    f(this, "options");
    /**
    * A collection of toolbar items (buttons, dropdowns, etc.).
    */
    f(this, "items");
    /**
    * Tracks information about the DOM focus in the toolbar.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}
    * to handle keyboard navigation in the toolbar.
    */
    f(this, "keystrokes");
    /**
    * A (child) view containing {@link #items toolbar items}.
    */
    f(this, "itemsView");
    /**
    * A top–level collection aggregating building blocks of the toolbar.
    *
    *	┌───────────────── ToolbarView ─────────────────┐
    *	| ┌──────────────── #children ────────────────┐ |
    *	| |   ┌──────────── #itemsView ───────────┐   | |
    *	| |   | [ item1 ] [ item2 ] ... [ itemN ] |   | |
    *	| |   └──────────────────────────────────-┘   | |
    *	| └───────────────────────────────────────────┘ |
    *	└───────────────────────────────────────────────┘
    *
    * By default, it contains the {@link #itemsView} but it can be extended with additional
    * UI elements when necessary.
    */
    f(this, "children");
    /**
    * A collection of {@link #items} that take part in the focus cycling
    * (i.e. navigation using the keyboard). Usually, it contains a subset of {@link #items} with
    * some optional UI elements that also belong to the toolbar and should be focusable
    * by the user.
    */
    f(this, "focusables");
    /**
    * Helps cycling over {@link #focusables focusable items} in the toolbar.
    */
    f(this, "_focusCycler");
    /**
    * An instance of the active toolbar behavior that shapes its look and functionality.
    *
    * See {@link module:ui/toolbar/toolbarview~ToolbarBehavior} to learn more.
    */
    f(this, "_behavior");
    const s = this.bindTemplate, r = this.t;
    this.options = i || {}, this.set("ariaLabel", r("Editor toolbar")), this.set("maxWidth", "auto"), this.set("role", "toolbar"), this.items = this.createCollection(), this.focusTracker = new ze(), this.keystrokes = new Ze(), this.set("class", void 0), this.set("isCompact", !1), this.itemsView = new e9(e), this.children = this.createCollection(), this.children.add(this.itemsView), this.focusables = this.createCollection();
    const o = e.uiLanguageDirection === "rtl";
    this._focusCycler = new Pt({
      focusables: this.focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate toolbar items backwards using the arrow[left,up] keys.
        focusPrevious: [
          o ? "arrowright" : "arrowleft",
          "arrowup"
        ],
        // Navigate toolbar items forwards using the arrow[right,down] keys.
        focusNext: [
          o ? "arrowleft" : "arrowright",
          "arrowdown"
        ]
      }
    });
    const a = [
      "ck",
      "ck-toolbar",
      s.to("class"),
      s.if("isCompact", "ck-toolbar_compact")
    ];
    this.options.shouldGroupWhenFull && this.options.isFloating && a.push("ck-toolbar_floating"), this.setTemplate({
      tag: "div",
      attributes: {
        class: a,
        role: s.to("role"),
        "aria-label": s.to("ariaLabel"),
        style: {
          maxWidth: s.to("maxWidth")
        },
        tabindex: -1
      },
      children: this.children,
      on: {
        // https://github.com/ckeditor/ckeditor5-ui/issues/206
        mousedown: XL(this)
      }
    }), this._behavior = this.options.shouldGroupWhenFull ? new i9(this) : new t9(this);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.element);
    for (const e of this.items)
      this.focusTracker.add(e);
    this.items.on("add", (e, i) => {
      this.focusTracker.add(i);
    }), this.items.on("remove", (e, i) => {
      this.focusTracker.remove(i);
    }), this.keystrokes.listenTo(this.element), this._behavior.render(this);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this._behavior.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy(), super.destroy();
  }
  /**
  * Focuses the first focusable in {@link #focusables}.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Focuses the last focusable in {@link #focusables}.
  */
  focusLast() {
    this._focusCycler.focusLast();
  }
  /**
  * A utility that expands the plain toolbar configuration into
  * {@link module:ui/toolbar/toolbarview~ToolbarView#items} using a given component factory.
  *
  * @param itemsOrConfig The toolbar items or the entire toolbar configuration object.
  * @param factory A factory producing toolbar items.
  * @param removeItems An array of items names to be removed from the configuration. When present, applies
  * to this toolbar and all nested ones as well.
  */
  fillFromConfig(e, i, s) {
    this.items.addMany(this._buildItemsFromConfig(e, i, s));
  }
  /**
  * A utility that expands the plain toolbar configuration into a list of view items using a given component factory.
  *
  * @param itemsOrConfig The toolbar items or the entire toolbar configuration object.
  * @param factory A factory producing toolbar items.
  * @param removeItems An array of items names to be removed from the configuration. When present, applies
  * to this toolbar and all nested ones as well.
  */
  _buildItemsFromConfig(e, i, s) {
    const r = Hf(e), o = s || r.removeItems;
    return this._cleanItemsConfiguration(r.items, i, o).map((l) => nt(l) ? this._createNestedToolbarDropdown(l, i, o) : l === "|" ? new zf() : l === "-" ? new JL() : i.create(l)).filter((l) => !!l);
  }
  /**
  * Cleans up the {@link module:ui/toolbar/toolbarview~ToolbarView#items} of the toolbar by removing unwanted items and
  * duplicated (obsolete) separators or line breaks.
  *
  * @param items The toolbar items configuration.
  * @param factory A factory producing toolbar items.
  * @param removeItems An array of items names to be removed from the configuration.
  * @returns Items after the clean-up.
  */
  _cleanItemsConfiguration(e, i, s) {
    const r = e.filter((o, a, l) => o === "|" ? !0 : s.indexOf(o) !== -1 ? !1 : o === "-" ? this.options.shouldGroupWhenFull ? (Ve("toolbarview-line-break-ignored-when-grouping-items", l), !1) : !0 : !nt(o) && !i.has(o) ? (Ve("toolbarview-item-unavailable", {
      item: o
    }), !1) : !0);
    return this._cleanSeparatorsAndLineBreaks(r);
  }
  /**
  * Remove leading, trailing, and duplicated separators (`-` and `|`).
  *
  * @returns Toolbar items after the separator and line break clean-up.
  */
  _cleanSeparatorsAndLineBreaks(e) {
    const i = (a) => a !== "-" && a !== "|", s = e.length, r = e.findIndex(i);
    if (r === -1)
      return [];
    const o = s - e.slice().reverse().findIndex(i);
    return e.slice(r, o).filter((a, l, c) => i(a) ? !0 : !(l > 0 && c[l - 1] === a));
  }
  /**
  * Creates a user-defined dropdown containing a toolbar with items.
  *
  * @param definition A definition of the nested toolbar dropdown.
  * @param definition.label A label of the dropdown.
  * @param definition.icon An icon of the drop-down. One of 'bold', 'plus', 'text', 'importExport', 'alignLeft',
  * 'paragraph' or an SVG string. When `false` is passed, no icon will be used.
  * @param definition.withText When set `true`, the label of the dropdown will be visible. See
  * {@link module:ui/button/buttonview~ButtonView#withText} to learn more.
  * @param definition.tooltip A tooltip of the dropdown button. See
  * {@link module:ui/button/buttonview~ButtonView#tooltip} to learn more. Defaults to `true`.
  * @param componentFactory Component factory used to create items
  * of the nested toolbar.
  */
  _createNestedToolbarDropdown(e, i, s) {
    let { label: r, icon: o, items: a, tooltip: l = !0, withText: c = !1 } = e;
    if (a = this._cleanItemsConfiguration(a, i, s), !a.length)
      return null;
    const u = this.locale, d = Tt(u);
    return r || Ve("toolbarview-nested-toolbar-dropdown-missing-label", e), d.class = "ck-toolbar__nested-toolbar-dropdown", d.buttonView.set({
      label: r,
      tooltip: l,
      withText: !!c
    }), o !== !1 ? d.buttonView.icon = QL[o] || o || Z.threeVerticalDots : d.buttonView.withText = !0, Ma(d, () => d.toolbarView._buildItemsFromConfig(a, i, s)), d;
  }
}
class e9 extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of items (buttons, dropdowns, etc.).
    */
    f(this, "children");
    this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-toolbar__items"
        ]
      },
      children: this.children
    });
  }
}
class t9 {
  /**
  * Creates an instance of the {@link module:ui/toolbar/toolbarview~StaticLayout} toolbar
  * behavior.
  *
  * @param view An instance of the toolbar that this behavior is added to.
  */
  constructor(t) {
    const e = t.bindTemplate;
    t.set("isVertical", !1), t.itemsView.children.bindTo(t.items).using((i) => i), t.focusables.bindTo(t.items).using((i) => La(i) ? i : null), t.extendTemplate({
      attributes: {
        class: [
          // When vertical, the toolbar has an additional CSS class.
          e.if("isVertical", "ck-toolbar_vertical")
        ]
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
  }
  /**
  * @inheritDoc
  */
  destroy() {
  }
}
class i9 {
  /**
  * Creates an instance of the {@link module:ui/toolbar/toolbarview~DynamicGrouping} toolbar
  * behavior.
  *
  * @param view An instance of the toolbar that this behavior is added to.
  */
  constructor(t) {
    /**
    * A toolbar view this behavior belongs to.
    */
    f(this, "view");
    /**
    * A collection of toolbar children.
    */
    f(this, "viewChildren");
    /**
    * A collection of focusable toolbar elements.
    */
    f(this, "viewFocusables");
    /**
    * A view containing toolbar items.
    */
    f(this, "viewItemsView");
    /**
    * Toolbar focus tracker.
    */
    f(this, "viewFocusTracker");
    /**
    * Toolbar locale.
    */
    f(this, "viewLocale");
    /**
    * A subset of toolbar {@link module:ui/toolbar/toolbarview~ToolbarView#items}.
    * Aggregates items that fit into a single row of the toolbar and were not {@link #groupedItems grouped}
    * into a {@link #groupedItemsDropdown dropdown}. Items of this collection are displayed in the
    * {@link module:ui/toolbar/toolbarview~ToolbarView#itemsView}.
    *
    * When none of the {@link module:ui/toolbar/toolbarview~ToolbarView#items} were grouped, it
    * matches the {@link module:ui/toolbar/toolbarview~ToolbarView#items} collection in size and order.
    */
    f(this, "ungroupedItems");
    /**
    * A subset of toolbar {@link module:ui/toolbar/toolbarview~ToolbarView#items}.
    * A collection of the toolbar items that do not fit into a single row of the toolbar.
    * Grouped items are displayed in a dedicated {@link #groupedItemsDropdown dropdown}.
    *
    * When none of the {@link module:ui/toolbar/toolbarview~ToolbarView#items} were grouped,
    * this collection is empty.
    */
    f(this, "groupedItems");
    /**
    * The dropdown that aggregates {@link #groupedItems grouped items} that do not fit into a single
    * row of the toolbar. It is displayed on demand as the last of
    * {@link module:ui/toolbar/toolbarview~ToolbarView#children toolbar children} and offers another
    * (nested) toolbar which displays items that would normally overflow.
    */
    f(this, "groupedItemsDropdown");
    /**
    * An instance of the resize observer that helps dynamically determine the geometry of the toolbar
    * and manage items that do not fit into a single row.
    *
    * **Note:** Created in {@link #_enableGroupingOnResize}.
    *
    * @readonly
    */
    f(this, "resizeObserver", null);
    /**
    * A cached value of the horizontal padding style used by {@link #_updateGrouping}
    * to manage the {@link module:ui/toolbar/toolbarview~ToolbarView#items} that do not fit into
    * a single toolbar line. This value can be reused between updates because it is unlikely that
    * the padding will change and re–using `Window.getComputedStyle()` is expensive.
    *
    * @readonly
    */
    f(this, "cachedPadding", null);
    /**
    * A flag indicating that an items grouping update has been queued (e.g. due to the toolbar being visible)
    * and should be executed immediately the next time the toolbar shows up.
    *
    * @readonly
    */
    f(this, "shouldUpdateGroupingOnNextResize", !1);
    /**
    * Toolbar element.
    *
    * @readonly
    */
    f(this, "viewElement");
    this.view = t, this.viewChildren = t.children, this.viewFocusables = t.focusables, this.viewItemsView = t.itemsView, this.viewFocusTracker = t.focusTracker, this.viewLocale = t.locale, this.ungroupedItems = t.createCollection(), this.groupedItems = t.createCollection(), this.groupedItemsDropdown = this._createGroupedItemsDropdown(), t.itemsView.children.bindTo(this.ungroupedItems).using((e) => e), this.ungroupedItems.on("change", this._updateFocusCyclableItems.bind(this)), t.children.on("change", this._updateFocusCyclableItems.bind(this)), t.items.on("change", (e, i) => {
      const s = i.index, r = Array.from(i.added);
      for (const o of i.removed)
        s >= this.ungroupedItems.length ? this.groupedItems.remove(o) : this.ungroupedItems.remove(o);
      for (let o = s; o < s + r.length; o++) {
        const a = r[o - s];
        o > this.ungroupedItems.length ? this.groupedItems.add(a, o - this.ungroupedItems.length) : this.ungroupedItems.add(a, o);
      }
      this._updateGrouping();
    }), t.extendTemplate({
      attributes: {
        class: [
          // To group items dynamically, the toolbar needs a dedicated CSS class.
          "ck-toolbar_grouping"
        ]
      }
    });
  }
  /**
  * Enables dynamic items grouping based on the dimensions of the toolbar.
  *
  * @param view An instance of the toolbar that this behavior is added to.
  */
  render(t) {
    this.viewElement = t.element, this._enableGroupingOnResize(), this._enableGroupingOnMaxWidthChange(t);
  }
  /**
  * Cleans up the internals used by this behavior.
  */
  destroy() {
    this.groupedItemsDropdown.destroy(), this.resizeObserver.destroy();
  }
  /**
  * When called, it will check if any of the {@link #ungroupedItems} do not fit into a single row of the toolbar,
  * and it will move them to the {@link #groupedItems} when it happens.
  *
  * At the same time, it will also check if there is enough space in the toolbar for the first of the
  * {@link #groupedItems} to be returned back to {@link #ungroupedItems} and still fit into a single row
  * without the toolbar wrapping.
  */
  _updateGrouping() {
    if (!this.viewElement.ownerDocument.body.contains(this.viewElement))
      return;
    if (!yn(this.viewElement)) {
      this.shouldUpdateGroupingOnNextResize = !0;
      return;
    }
    const t = this.groupedItems.length;
    let e;
    for (; this._areItemsOverflowing; )
      this._groupLastItem(), e = !0;
    if (!e && this.groupedItems.length) {
      for (; this.groupedItems.length && !this._areItemsOverflowing; )
        this._ungroupFirstItem();
      this._areItemsOverflowing && this._groupLastItem();
    }
    this.groupedItems.length !== t && this.view.fire("groupedItemsUpdate");
  }
  /**
  * Returns `true` when {@link module:ui/toolbar/toolbarview~ToolbarView#element} children visually overflow,
  * for instance if the toolbar is narrower than its members. Returns `false` otherwise.
  */
  get _areItemsOverflowing() {
    if (!this.ungroupedItems.length)
      return !1;
    const t = this.viewElement, e = this.viewLocale.uiLanguageDirection, i = new ke(t.lastChild), s = new ke(t);
    if (!this.cachedPadding) {
      const r = ae.window.getComputedStyle(t), o = e === "ltr" ? "paddingRight" : "paddingLeft";
      this.cachedPadding = Number.parseInt(r[o]);
    }
    return e === "ltr" ? i.right > s.right - this.cachedPadding : i.left < s.left + this.cachedPadding;
  }
  /**
  * Enables the functionality that prevents {@link #ungroupedItems} from overflowing (wrapping to the next row)
  * upon resize when there is little space available. Instead, the toolbar items are moved to the
  * {@link #groupedItems} collection and displayed in a dropdown at the end of the row (which has its own nested toolbar).
  *
  * When called, the toolbar will automatically analyze the location of its {@link #ungroupedItems} and "group"
  * them in the dropdown if necessary. It will also observe the browser window for size changes in
  * the future and respond to them by grouping more items or reverting already grouped back, depending
  * on the visual space available.
  */
  _enableGroupingOnResize() {
    let t;
    this.resizeObserver = new $r(this.viewElement, (e) => {
      (!t || t !== e.contentRect.width || this.shouldUpdateGroupingOnNextResize) && (this.shouldUpdateGroupingOnNextResize = !1, this._updateGrouping(), t = e.contentRect.width);
    }), this._updateGrouping();
  }
  /**
  * Enables the grouping functionality, just like {@link #_enableGroupingOnResize} but the difference is that
  * it listens to the changes of {@link module:ui/toolbar/toolbarview~ToolbarView#maxWidth} instead.
  */
  _enableGroupingOnMaxWidthChange(t) {
    t.on("change:maxWidth", () => {
      this._updateGrouping();
    });
  }
  /**
  * When called, it will remove the last item from {@link #ungroupedItems} and move it back
  * to the {@link #groupedItems} collection.
  *
  * The opposite of {@link #_ungroupFirstItem}.
  */
  _groupLastItem() {
    this.groupedItems.length || (this.viewChildren.add(new zf()), this.viewChildren.add(this.groupedItemsDropdown), this.viewFocusTracker.add(this.groupedItemsDropdown.element)), this.groupedItems.add(this.ungroupedItems.remove(this.ungroupedItems.last), 0);
  }
  /**
  * Moves the very first item belonging to {@link #groupedItems} back
  * to the {@link #ungroupedItems} collection.
  *
  * The opposite of {@link #_groupLastItem}.
  */
  _ungroupFirstItem() {
    this.ungroupedItems.add(this.groupedItems.remove(this.groupedItems.first)), this.groupedItems.length || (this.viewChildren.remove(this.groupedItemsDropdown), this.viewChildren.remove(this.viewChildren.last), this.viewFocusTracker.remove(this.groupedItemsDropdown.element));
  }
  /**
  * Creates the {@link #groupedItemsDropdown} that hosts the members of the {@link #groupedItems}
  * collection when there is not enough space in the toolbar to display all items in a single row.
  */
  _createGroupedItemsDropdown() {
    const t = this.viewLocale, e = t.t, i = Tt(t);
    return i.class = "ck-toolbar__grouped-dropdown", i.panelPosition = t.uiLanguageDirection === "ltr" ? "sw" : "se", Ma(i, this.groupedItems), i.buttonView.set({
      label: e("Show more items"),
      tooltip: !0,
      tooltipPosition: t.uiLanguageDirection === "rtl" ? "se" : "sw",
      icon: Z.threeVerticalDots
    }), i;
  }
  /**
  * Updates the {@link module:ui/toolbar/toolbarview~ToolbarView#focusables focus–cyclable items}
  * collection so it represents the up–to–date state of the UI from the perspective of the user.
  *
  * For instance, the {@link #groupedItemsDropdown} can show up and hide but when it is visible,
  * it must be subject to focus cycling in the toolbar.
  *
  * See the {@link module:ui/toolbar/toolbarview~ToolbarView#focusables collection} documentation
  * to learn more about the purpose of this method.
  */
  _updateFocusCyclableItems() {
    this.viewFocusables.clear(), this.ungroupedItems.map((t) => {
      La(t) && this.viewFocusables.add(t);
    }), this.groupedItems.length && this.viewFocusables.add(this.groupedItemsDropdown);
  }
}
class Ys extends ee {
  /**
  * @inheritDoc
  */
  constructor(e, i) {
    super(e);
    /**
    * Collection of the child views inside of the split button {@link #element}.
    */
    f(this, "children");
    /**
    * A main button of split button.
    */
    f(this, "actionView");
    /**
    * A secondary button of split button that opens dropdown.
    */
    f(this, "arrowView");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}. It manages
    * keystrokes of the split button:
    *
    * * <kbd>▶</kbd> moves focus to arrow view when action view is focused,
    * * <kbd>◀</kbd> moves focus to action view when arrow view is focused.
    */
    f(this, "keystrokes");
    /**
    * Tracks information about DOM focus in the dropdown.
    */
    f(this, "focusTracker");
    const s = this.bindTemplate;
    this.set("class", void 0), this.set("labelStyle", void 0), this.set("icon", void 0), this.set("isEnabled", !0), this.set("isOn", !1), this.set("isToggleable", !1), this.set("isVisible", !0), this.set("keystroke", void 0), this.set("withKeystroke", !1), this.set("label", void 0), this.set("tabindex", -1), this.set("tooltip", !1), this.set("tooltipPosition", "s"), this.set("type", "button"), this.set("withText", !1), this.children = this.createCollection(), this.actionView = this._createActionView(i), this.arrowView = this._createArrowView(), this.keystrokes = new Ze(), this.focusTracker = new ze(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-splitbutton",
          s.to("class"),
          s.if("isVisible", "ck-hidden", (r) => !r),
          this.arrowView.bindTemplate.if("isOn", "ck-splitbutton_open")
        ]
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.children.add(this.actionView), this.children.add(this.arrowView), this.focusTracker.add(this.actionView.element), this.focusTracker.add(this.arrowView.element), this.keystrokes.listenTo(this.element), this.keystrokes.set("arrowright", (e, i) => {
      this.focusTracker.focusedElement === this.actionView.element && (this.arrowView.focus(), i());
    }), this.keystrokes.set("arrowleft", (e, i) => {
      this.focusTracker.focusedElement === this.arrowView.element && (this.actionView.focus(), i());
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the {@link module:ui/button/buttonview~ButtonView#element} of the action part of split button.
  */
  focus() {
    this.actionView.focus();
  }
  /**
  * Creates a {@link module:ui/button/buttonview~ButtonView} instance as {@link #actionView} and binds it with main split button
  * attributes.
  */
  _createActionView(e) {
    const i = e || new le();
    return e || i.bind("icon", "isEnabled", "isOn", "isToggleable", "keystroke", "label", "tabindex", "tooltip", "tooltipPosition", "type", "withText").to(this), i.extendTemplate({
      attributes: {
        class: "ck-splitbutton__action"
      }
    }), i.delegate("execute").to(this), i;
  }
  /**
  * Creates a {@link module:ui/button/buttonview~ButtonView} instance as {@link #arrowView} and binds it with main split button
  * attributes.
  */
  _createArrowView() {
    const e = new le(), i = e.bindTemplate;
    return e.icon = Xc, e.extendTemplate({
      attributes: {
        class: [
          "ck-splitbutton__arrow"
        ],
        "data-cke-tooltip-disabled": i.to("isOn"),
        "aria-haspopup": !0,
        "aria-expanded": i.to("isOn", (s) => String(s))
      }
    }), e.bind("isEnabled").to(this), e.bind("label").to(this), e.bind("tooltip").to(this), e.delegate("execute").to(this, "open"), e;
  }
}
function Tt(n, t = My) {
  const e = typeof t == "function" ? new t(n) : t, i = new ZL(n), s = new nc(n, e, i);
  return e.bind("isEnabled").to(s), e instanceof Ys ? e.arrowView.bind("isOn").to(s, "isOpen") : e.bind("isOn").to(s, "isOpen"), n9(s), s;
}
function Ma(n, t, e = {}) {
  n.extendTemplate({
    attributes: {
      class: [
        "ck-toolbar-dropdown"
      ]
    }
  }), n.isOpen ? Ib(n, t, e) : n.once("change:isOpen", () => Ib(n, t, e), {
    priority: "highest"
  }), e.enableActiveItemFocusOnDropdownOpen && eu(n, () => n.toolbarView.items.find((i) => i.isOn));
}
function Ib(n, t, e) {
  const i = n.locale, s = i.t, r = n.toolbarView = new Us(i), o = typeof t == "function" ? t() : t;
  r.ariaLabel = e.ariaLabel || s("Dropdown toolbar"), e.maxWidth && (r.maxWidth = e.maxWidth), e.class && (r.class = e.class), e.isCompact && (r.isCompact = e.isCompact), e.isVertical && (r.isVertical = !0), o instanceof Et ? r.items.bindTo(o).using((a) => a) : r.items.addMany(o), n.panelView.children.add(r), n.focusTracker.add(r), r.items.delegate("execute").to(n);
}
function En(n, t, e = {}) {
  n.isOpen ? Pb(n, t, e) : n.once("change:isOpen", () => Pb(n, t, e), {
    priority: "highest"
  }), eu(n, () => n.listView.items.find((i) => i instanceof Wn ? i.children.first.isOn : !1));
}
function Pb(n, t, e) {
  const i = n.locale, s = n.listView = new Qc(i), r = typeof t == "function" ? t() : t;
  s.ariaLabel = e.ariaLabel, s.role = e.role, Fy(n, s.items, r, i), n.panelView.children.add(s), s.items.delegate("execute").to(n);
}
function eu(n, t) {
  n.on("change:isOpen", () => {
    if (!n.isOpen)
      return;
    const e = t();
    e && (typeof e.focus == "function" ? e.focus() : Ve("ui-dropdown-focus-child-on-open-child-missing-focus", {
      view: e
    }));
  }, {
    priority: Si.low - 10
  });
}
function n9(n) {
  s9(n), r9(n), o9(n), a9(n), l9(n), c9(n);
}
function s9(n) {
  Xn({
    emitter: n,
    activator: () => n.isRendered && n.isOpen,
    callback: () => {
      n.isOpen = !1;
    },
    contextElements: () => [
      n.element,
      // Include all elements connected to the dropdown's focus tracker, but exclude those that are direct children
      // of DropdownView#element. They would be identified as descendants of #element anyway upon clicking and would
      // not contribute to the logic.
      ...Ny(n.focusTracker).filter((t) => !n.element.contains(t))
    ]
  });
}
function Ny(n) {
  return [
    ...n.elements,
    ...n.externalViews.flatMap((t) => Ny(t.focusTracker))
  ];
}
function r9(n) {
  n.on("execute", (t) => {
    t.source instanceof js || (n.isOpen = !1);
  });
}
function o9(n) {
  n.focusTracker.on("change:isFocused", (t, e, i) => {
    i || !n.isOpen || (n.isOpen = !1);
  });
}
function a9(n) {
  n.keystrokes.set("arrowdown", (t, e) => {
    n.isOpen && (n.panelView.focus(), e());
  }), n.keystrokes.set("arrowup", (t, e) => {
    n.isOpen && (n.panelView.focusLast(), e());
  });
}
function l9(n) {
  n.on("change:isOpen", (t, e, i) => {
    if (i)
      return;
    n.focusTracker.elements.some((r) => r.contains(ae.document.activeElement)) && n.buttonView.focus();
  });
}
function c9(n) {
  n.on("change:isOpen", (t, e, i) => {
    i && n.panelView.focus();
  }, {
    priority: "low"
  });
}
function Fy(n, t, e, i) {
  t.on("change", () => {
    const s = [
      ...t
    ].reduce((o, a) => (a instanceof Wn && a.children.first instanceof Ur && o.push(a.children.first), o), []), r = s.some((o) => o.isToggleable);
    s.forEach((o) => {
      o.hasCheckSpace = r;
    });
  }), t.bindTo(e).using((s) => {
    if (s.type === "separator")
      return new Ba(i);
    if (s.type === "group") {
      const r = new Vl(i);
      return r.set({
        label: s.label
      }), Fy(n, r.items, s.items, i), r.items.delegate("execute").to(n), r;
    } else if (s.type === "button" || s.type === "switchbutton") {
      const r = s.model.role === "menuitemcheckbox" || s.model.role === "menuitemradio", o = new Wn(i);
      let a;
      return s.type === "button" ? (a = new Ur(i), a.set({
        isToggleable: r
      })) : a = new js(i), a.bind(...Object.keys(s.model)).to(s.model), a.delegate("execute").to(o), o.children.add(a), o;
    }
    return null;
  });
}
const pi = (n, t, e) => {
  const i = new By(n.locale);
  return i.set({
    id: t,
    ariaDescribedById: e
  }), i.bind("isReadOnly").to(n, "isEnabled", (s) => !s), i.bind("hasError").to(n, "errorText", (s) => !!s), i.on("input", () => {
    n.errorText = null;
  }), n.bind("isEmpty", "isFocused", "placeholder").to(i), i;
}, Dy = (n, t, e) => {
  const i = new KL(n.locale);
  return i.set({
    id: t,
    ariaDescribedById: e,
    inputMode: "numeric"
  }), i.bind("isReadOnly").to(n, "isEnabled", (s) => !s), i.bind("hasError").to(n, "errorText", (s) => !!s), i.on("input", () => {
    n.errorText = null;
  }), n.bind("isEmpty", "isFocused", "placeholder").to(i), i;
}, jf = (n, t, e) => {
  const i = Tt(n.locale);
  return i.set({
    id: t,
    ariaDescribedById: e
  }), i.bind("isEnabled").to(n), i;
}, u9 = 150;
class d9 extends ee {
  /**
  * Creates a view of color picker.
  *
  * @param locale
  * @param config
  */
  constructor(e, i = {}) {
    super(e);
    /**
    * Container for a `#` sign prefix and an input for displaying and defining custom colors
    * in HEX format.
    */
    f(this, "hexInputRow");
    /**
    * Debounced function updating the `color` property in the component
    * and firing the `ColorPickerColorSelectedEvent`. Executed whenever color in component
    * is changed by the user interaction (through the palette or input).
    *
    * @private
    */
    f(this, "_debounceColorPickerEvent");
    /**
    * A reference to the configuration of the color picker specified in the constructor.
    *
    * @private
    */
    f(this, "_config");
    this.set({
      color: "",
      _hexColor: ""
    }), this.hexInputRow = this._createInputRow();
    const s = this.createCollection();
    i.hideInput || s.add(this.hexInputRow), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-picker"
        ],
        tabindex: -1
      },
      children: s
    }), this._config = i, this._debounceColorPickerEvent = vi((r) => {
      this.set("color", r), this.fire("colorSelected", {
        color: this.color
      });
    }, u9, {
      leading: !0
    }), this.on("set:color", (r, o, a) => {
      r.return = Oy(a, this._config.format || "hsl");
    }), this.on("change:color", () => {
      this._hexColor = td(this.color);
    }), this.on("change:_hexColor", () => {
      document.activeElement !== this.picker && this.picker.setAttribute("color", this._hexColor), td(this.color) != td(this._hexColor) && (this.color = this._hexColor);
    });
  }
  /**
  * Renders color picker in the view.
  */
  render() {
    if (super.render(), jL("hex-color-picker", bL), this.picker = ae.document.createElement("hex-color-picker"), this.picker.setAttribute("class", "hex-color-picker"), this.picker.setAttribute("tabindex", "-1"), this._createSlidersView(), this.element) {
      this.hexInputRow.element ? this.element.insertBefore(this.picker, this.hexInputRow.element) : this.element.appendChild(this.picker);
      const e = document.createElement("style");
      e.textContent = '[role="slider"]:focus [part$="pointer"] {border: 1px solid #fff;outline: 1px solid var(--ck-color-focus-border);box-shadow: 0 0 0 2px #fff;}', this.picker.shadowRoot.appendChild(e);
    }
    this.picker.addEventListener("color-changed", (e) => {
      const i = e.detail.value;
      this._debounceColorPickerEvent(i);
    });
  }
  /**
  * Focuses the first pointer in color picker.
  *
  */
  focus() {
    /* istanbul ignore next -- @preserve */
    !this._config.hideInput && (be.isGecko || be.isiOS || be.isSafari || be.isBlink) && this.hexInputRow.children.get(1).focus(), this.slidersView.first.focus();
  }
  /**
  * Creates collection of sliders in color picker.
  *
  * @private
  */
  _createSlidersView() {
    const s = [
      ...this.picker.shadowRoot.children
    ].filter((r) => r.getAttribute("role") === "slider").map((r) => new h9(r));
    this.slidersView = this.createCollection(), s.forEach((r) => {
      this.slidersView.add(r);
    });
  }
  /**
  * Creates input row for defining custom colors in color picker.
  *
  * @private
  */
  _createInputRow() {
    const e = this._createColorInput();
    return new m9(this.locale, e);
  }
  /**
  * Creates the input where user can type or paste the color in hex format.
  *
  * @private
  */
  _createColorInput() {
    const e = new gt(this.locale, pi), { t: i } = this.locale;
    return e.set({
      label: i("HEX"),
      class: "color-picker-hex-input"
    }), e.fieldView.bind("value").to(this, "_hexColor", (s) => e.isFocused ? e.fieldView.value : s.startsWith("#") ? s.substring(1) : s), e.fieldView.on("input", () => {
      const s = e.fieldView.element.value;
      if (s) {
        const r = $y(s);
        r && this._debounceColorPickerEvent(r);
      }
    }), e;
  }
  /**
  * Validates the view and returns `false` when some fields are invalid.
  */
  isValid() {
    const { t: e } = this.locale;
    return this._config.hideInput ? !0 : (this.resetValidationStatus(), this.hexInputRow.getParsedColor() ? !0 : (this.hexInputRow.inputView.errorText = e('Please enter a valid color (e.g. "ff0000").'), !1));
  }
  /**
  * Cleans up the supplementary error and information text of input inside the {@link #hexInputRow}
  * bringing them back to the state when the form has been displayed for the first time.
  *
  * See {@link #isValid}.
  */
  resetValidationStatus() {
    this.hexInputRow.inputView.errorText = null;
  }
}
function td(n) {
  let t = HL(n);
  return t || (t = "#000"), t.length === 4 && (t = "#" + [
    t[1],
    t[1],
    t[2],
    t[2],
    t[3],
    t[3]
  ].join("")), t.toLowerCase();
}
class h9 extends ee {
  /**
  * @param element HTML element of slider in color picker.
  */
  constructor(t) {
    super(), this.element = t;
  }
  /**
  * Focuses element.
  */
  focus() {
    this.element.focus();
  }
}
class f9 extends ee {
  constructor(t) {
    super(t), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-picker__hash-view"
        ]
      },
      children: "#"
    });
  }
}
class m9 extends ee {
  /**
  * Creates an instance of the form row class.
  *
  * @param locale The locale instance.
  * @param inputView Hex color input element.
  */
  constructor(e, i) {
    super(e);
    /**
    * A collection of row items (buttons, dropdowns, etc.).
    */
    f(this, "children");
    /**
    * Hex input view element.
    */
    f(this, "inputView");
    this.inputView = i, this.children = this.createCollection([
      new f9(),
      this.inputView
    ]), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-picker__row"
        ]
      },
      children: this.children
    });
  }
  /**
  * Returns false if color input value is not in hex format.
  */
  getParsedColor() {
    return $y(this.inputView.fieldView.element.value);
  }
}
function $y(n) {
  if (!n)
    return null;
  const t = n.trim().replace(/^#/, "");
  return ![
    3,
    4,
    6,
    8
  ].includes(t.length) || !/^(([0-9a-fA-F]{2}){3,4}|([0-9a-fA-F]){3,4})$/.test(t) ? null : `#${t}`;
}
class g9 extends (/* @__PURE__ */ je(bt)) {
  constructor(t) {
    super(t), this.set("isEmpty", !0), this.on("change", () => {
      this.set("isEmpty", this.length === 0);
    });
  }
  /**
  * Adds a color to the document color collection.
  *
  * This method ensures that no color duplicates are inserted (compared using
  * the color value of the {@link module:ui/colorgrid/colorgridview~ColorDefinition}).
  *
  * If the item does not have an ID, it will be automatically generated and set on the item.
  *
  * @param index The position of the item in the collection. The item is pushed to the collection when `index` is not specified.
  * @fires add
  * @fires change
  */
  add(t, e) {
    return this.find((i) => i.color === t.color) ? this : super.add(t, e);
  }
  /**
  * Checks if an object with given colors is present in the document color collection.
  */
  hasColor(t) {
    return !!this.find((e) => e.color === t);
  }
}
class p9 extends ee {
  /**
  * Creates an instance of the view.
  *
  * @param locale The localization services instance.
  * @param colors An array with definitions of colors to be displayed in the table.
  * @param columns The number of columns in the color grid.
  * @param removeButtonLabel The label of the button responsible for removing the color.
  * @param colorPickerLabel The label of the button responsible for color picker appearing.
  * @param documentColorsLabel The label for the section with the document colors.
  * @param documentColorsCount The number of colors in the document colors section inside the color dropdown.
  * @param focusTracker Tracks information about the DOM focus in the list.
  * @param focusables A collection of views that can be focused in the view.
  */
  constructor(e, { colors: i, columns: s, removeButtonLabel: r, documentColorsLabel: o, documentColorsCount: a, colorPickerLabel: l, focusTracker: c, focusables: u }) {
    super(e);
    /**
    * A collection of the children of the table.
    */
    f(this, "items");
    /**
    * An array with objects representing colors to be displayed in the grid.
    */
    f(this, "colorDefinitions");
    /**
    * Tracks information about the DOM focus in the list.
    */
    f(this, "focusTracker");
    /**
    * The number of columns in the color grid.
    */
    f(this, "columns");
    /**
    * Preserves the reference to {@link module:ui/colorselector/documentcolorcollection~DocumentColorCollection} used to collect
    * definitions that store the document colors.
    *
    * @readonly
    */
    f(this, "documentColors");
    /**
    * The maximum number of colors in the document colors section.
    * If it equals 0, the document colors section is not added.
    *
    * @readonly
    */
    f(this, "documentColorsCount");
    /**
    * Preserves the reference to {@link module:ui/colorgrid/colorgridview~ColorGridView} used to create
    * the default (static) color set.
    *
    * The property is loaded once the the parent dropdown is opened the first time.
    *
    * @readonly
    */
    f(this, "staticColorsGrid");
    /**
    * Preserves the reference to {@link module:ui/colorgrid/colorgridview~ColorGridView} used to create
    * the document colors. It remains undefined if the document colors feature is disabled.
    *
    * The property is loaded once the the parent dropdown is opened the first time.
    *
    * @readonly
    */
    f(this, "documentColorsGrid");
    /**
    * The "Color picker" button view.
    */
    f(this, "colorPickerButtonView");
    /**
    * The "Remove color" button view.
    */
    f(this, "removeColorButtonView");
    /**
    * A collection of views that can be focused in the view.
    *
    * @readonly
    */
    f(this, "_focusables");
    /**
    * Document color section's label.
    *
    * @readonly
    */
    f(this, "_documentColorsLabel");
    /**
    * The label of the button responsible for removing color attributes.
    */
    f(this, "_removeButtonLabel");
    /**
    * The label of the button responsible for switching to the color picker component.
    */
    f(this, "_colorPickerLabel");
    const d = this.bindTemplate;
    this.set("isVisible", !0), this.focusTracker = c, this.items = this.createCollection(), this.colorDefinitions = i, this.columns = s, this.documentColors = new g9(), this.documentColorsCount = a, this._focusables = u, this._removeButtonLabel = r, this._colorPickerLabel = l, this._documentColorsLabel = o, this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck-color-grids-fragment",
          d.if("isVisible", "ck-hidden", (h) => !h)
        ]
      },
      children: this.items
    }), this.removeColorButtonView = this._createRemoveColorButton(), this.items.add(this.removeColorButtonView);
  }
  /**
  * Scans through the editor model and searches for text node attributes with the given attribute name.
  * Found entries are set as document colors.
  *
  * All the previously stored document colors will be lost in the process.
  *
  * @param model The model used as a source to obtain the document colors.
  * @param attributeName Determines the name of the related model's attribute for a given dropdown.
  */
  updateDocumentColors(e, i) {
    const s = e.document, r = this.documentColorsCount;
    this.documentColors.clear();
    for (const o of s.getRoots()) {
      const a = e.createRangeIn(o);
      for (const l of a.getItems())
        if (l.is("$textProxy") && l.hasAttribute(i) && (this._addColorToDocumentColors(l.getAttribute(i)), this.documentColors.length >= r))
          return;
    }
  }
  /**
  * Refreshes the state of the selected color in one or both {@link module:ui/colorgrid/colorgridview~ColorGridView}s
  * available in the {@link module:ui/colorselector/colorselectorview~ColorSelectorView}. It guarantees that the selection will
  * occur only in one of them.
  */
  updateSelectedColors() {
    const e = this.documentColorsGrid, i = this.staticColorsGrid, s = this.selectedColor;
    i.selectedColor = s, e && (e.selectedColor = s);
  }
  /**
  * @inheritDoc
  */
  render() {
    if (super.render(), this.staticColorsGrid = this._createStaticColorsGrid(), this.items.add(this.staticColorsGrid), this.documentColorsCount) {
      const e = ri.bind(this.documentColors, this.documentColors), i = new ee(this.locale);
      i.setTemplate({
        tag: "span",
        attributes: {
          class: [
            "ck",
            "ck-color-grid__label",
            e.if("isEmpty", "ck-hidden")
          ]
        },
        children: [
          {
            text: this._documentColorsLabel
          }
        ]
      }), this.items.add(i), this.documentColorsGrid = this._createDocumentColorsGrid(), this.items.add(this.documentColorsGrid);
    }
    this._createColorPickerButton(), this._addColorSelectorElementsToFocusTracker();
  }
  /**
  * Focuses the component.
  */
  focus() {
    this.removeColorButtonView.focus();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy();
  }
  /**
  * Handles displaying the color picker button (if it was previously created) and making it focusable.
  */
  addColorPickerButton() {
    this.colorPickerButtonView && (this.items.add(this.colorPickerButtonView), this.focusTracker.add(this.colorPickerButtonView.element), this._focusables.add(this.colorPickerButtonView));
  }
  /**
  * Adds color selector elements to focus tracker.
  */
  _addColorSelectorElementsToFocusTracker() {
    this.focusTracker.add(this.removeColorButtonView.element), this._focusables.add(this.removeColorButtonView), this.staticColorsGrid && (this.focusTracker.add(this.staticColorsGrid.element), this._focusables.add(this.staticColorsGrid)), this.documentColorsGrid && (this.focusTracker.add(this.documentColorsGrid.element), this._focusables.add(this.documentColorsGrid));
  }
  /**
  * Creates the button responsible for displaying the color picker component.
  */
  _createColorPickerButton() {
    this.colorPickerButtonView = new le(), this.colorPickerButtonView.set({
      label: this._colorPickerLabel,
      withText: !0,
      icon: Z.colorPalette,
      class: "ck-color-selector__color-picker"
    }), this.colorPickerButtonView.on("execute", () => {
      this.fire("colorPicker:show");
    });
  }
  /**
  * Adds the remove color button as a child of the current view.
  */
  _createRemoveColorButton() {
    const e = new le();
    return e.set({
      withText: !0,
      icon: Z.eraser,
      label: this._removeButtonLabel
    }), e.class = "ck-color-selector__remove-color", e.on("execute", () => {
      this.fire("execute", {
        value: null,
        source: "removeColorButton"
      });
    }), e.render(), e;
  }
  /**
  * Creates a static color grid based on the editor configuration.
  */
  _createStaticColorsGrid() {
    const e = new Sb(this.locale, {
      colorDefinitions: this.colorDefinitions,
      columns: this.columns
    });
    return e.on("execute", (i, s) => {
      this.fire("execute", {
        value: s.value,
        source: "staticColorsGrid"
      });
    }), e;
  }
  /**
  * Creates the document colors section view and binds it to {@link #documentColors}.
  */
  _createDocumentColorsGrid() {
    const e = ri.bind(this.documentColors, this.documentColors), i = new Sb(this.locale, {
      columns: this.columns
    });
    return i.extendTemplate({
      attributes: {
        class: e.if("isEmpty", "ck-hidden")
      }
    }), i.items.bindTo(this.documentColors).using((s) => {
      const r = new Ry();
      return r.set({
        color: s.color,
        hasBorder: s.options && s.options.hasBorder
      }), s.label && r.set({
        label: s.label,
        tooltip: !0
      }), r.on("execute", () => {
        this.fire("execute", {
          value: s.color,
          source: "documentColorsGrid"
        });
      }), r;
    }), this.documentColors.on("change:isEmpty", (s, r, o) => {
      o && (i.selectedColor = null);
    }), i;
  }
  /**
  * Adds a given color to the document colors list. If possible, the method will attempt to use
  * data from the {@link #colorDefinitions} (label, color options).
  *
  * @param color A string that stores the value of the recently applied color.
  */
  _addColorToDocumentColors(e) {
    const i = this.colorDefinitions.find((s) => s.color === e);
    i ? this.documentColors.add(Object.assign({}, i)) : this.documentColors.add({
      color: e,
      label: e,
      options: {
        hasBorder: !1
      }
    });
  }
}
class b9 extends ee {
  /**
  * Creates an instance of the view.
  *
  * @param locale The localization services instance.
  * @param focusTracker Tracks information about the DOM focus in the list.
  * @param focusables A collection of views that can be focused in the view..
  * @param keystrokes An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
  * @param colorPickerViewConfig The configuration of color picker feature. If set to `false`, the color picker
  * will not be rendered.
  */
  constructor(e, { focusTracker: i, focusables: s, keystrokes: r, colorPickerViewConfig: o }) {
    super(e);
    /**
    * A collection of component's children.
    */
    f(this, "items");
    /**
    * A view with saturation and hue sliders and color input.
    */
    f(this, "colorPickerView");
    /**
    * The "Save" button view.
    */
    f(this, "saveButtonView");
    /**
    * The "Cancel" button view.
    */
    f(this, "cancelButtonView");
    /**
    * The action bar where are "Save" button and "Cancel" button.
    */
    f(this, "actionBarView");
    /**
    * Tracks information about the DOM focus in the list.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * A collection of views that can be focused in the view.
    *
    * @readonly
    */
    f(this, "_focusables");
    /**
    * A reference to the configuration of {@link #colorPickerView}. `false` when the view was
    * configured without a color picker.
    *
    * @readonly
    */
    f(this, "_colorPickerViewConfig");
    this.items = this.createCollection(), this.focusTracker = i, this.keystrokes = r, this.set("isVisible", !1), this.set("selectedColor", void 0), this._focusables = s, this._colorPickerViewConfig = o;
    const a = this.bindTemplate, { saveButtonView: l, cancelButtonView: c } = this._createActionButtons();
    this.saveButtonView = l, this.cancelButtonView = c, this.actionBarView = this._createActionBarView({
      saveButtonView: l,
      cancelButtonView: c
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck-color-picker-fragment",
          a.if("isVisible", "ck-hidden", (u) => !u)
        ]
      },
      children: this.items
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    const e = new d9(this.locale, {
      ...this._colorPickerViewConfig
    });
    this.colorPickerView = e, this.colorPickerView.render(), this.selectedColor && (e.color = this.selectedColor), this.listenTo(this, "change:selectedColor", (i, s, r) => {
      e.color = r;
    }), this.items.add(this.colorPickerView), this.items.add(this.actionBarView), this._addColorPickersElementsToFocusTracker(), this._stopPropagationOnArrowsKeys(), this._executeOnEnterPress(), this._executeUponColorChange();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy();
  }
  /**
  * Focuses the color picker.
  */
  focus() {
    this.colorPickerView.focus();
  }
  /**
  * Reset validation messages.
  */
  resetValidationStatus() {
    this.colorPickerView.resetValidationStatus();
  }
  /**
  * When color picker is focused and "enter" is pressed it executes command.
  */
  _executeOnEnterPress() {
    this.keystrokes.set("enter", (e) => {
      this.isVisible && this.focusTracker.focusedElement !== this.cancelButtonView.element && this.colorPickerView.isValid() && (this.fire("execute", {
        value: this.selectedColor
      }), e.stopPropagation(), e.preventDefault());
    });
  }
  /**
  * Removes default behavior of arrow keys in dropdown.
  */
  _stopPropagationOnArrowsKeys() {
    const e = (i) => i.stopPropagation();
    this.keystrokes.set("arrowright", e), this.keystrokes.set("arrowleft", e), this.keystrokes.set("arrowup", e), this.keystrokes.set("arrowdown", e);
  }
  /**
  * Adds color picker elements to focus tracker.
  */
  _addColorPickersElementsToFocusTracker() {
    for (const i of this.colorPickerView.slidersView)
      this.focusTracker.add(i.element), this._focusables.add(i);
    const e = this.colorPickerView.hexInputRow.children.get(1);
    e.element && (this.focusTracker.add(e.element), this._focusables.add(e)), this.focusTracker.add(this.saveButtonView.element), this._focusables.add(this.saveButtonView), this.focusTracker.add(this.cancelButtonView.element), this._focusables.add(this.cancelButtonView);
  }
  /**
  * Creates bar containing "Save" and "Cancel" buttons.
  */
  _createActionBarView({ saveButtonView: e, cancelButtonView: i }) {
    const s = new ee(), r = this.createCollection();
    return r.add(e), r.add(i), s.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-selector_action-bar"
        ]
      },
      children: r
    }), s;
  }
  /**
  * Creates "Save" and "Cancel" buttons.
  */
  _createActionButtons() {
    const e = this.locale, i = e.t, s = new le(e), r = new le(e);
    return s.set({
      icon: Z.check,
      class: "ck-button-save",
      type: "button",
      withText: !1,
      label: i("Accept")
    }), r.set({
      icon: Z.cancel,
      class: "ck-button-cancel",
      type: "button",
      withText: !1,
      label: i("Cancel")
    }), s.on("execute", () => {
      this.colorPickerView.isValid() && this.fire("execute", {
        source: "colorPickerSaveButton",
        value: this.selectedColor
      });
    }), r.on("execute", () => {
      this.fire("colorPicker:cancel");
    }), {
      saveButtonView: s,
      cancelButtonView: r
    };
  }
  /**
  * Fires the `execute` event if color in color picker has been changed
  * by the user.
  */
  _executeUponColorChange() {
    this.colorPickerView.on("colorSelected", (e, i) => {
      this.fire("execute", {
        value: i.color,
        source: "colorPicker"
      }), this.set("selectedColor", i.color);
    });
  }
}
class Uf extends ee {
  /**
  * Creates a view to be inserted as a child of {@link module:ui/dropdown/dropdownview~DropdownView}.
  *
  * @param locale The localization services instance.
  * @param colors An array with definitions of colors to be displayed in the table.
  * @param columns The number of columns in the color grid.
  * @param removeButtonLabel The label of the button responsible for removing the color.
  * @param colorPickerLabel The label of the button responsible for color picker appearing.
  * @param documentColorsLabel The label for the section with the document colors.
  * @param documentColorsCount The number of colors in the document colors section inside the color dropdown.
  * @param colorPickerViewConfig The configuration of color picker feature. If set to `false`, the color picker will be hidden.
  */
  constructor(e, { colors: i, columns: s, removeButtonLabel: r, documentColorsLabel: o, documentColorsCount: a, colorPickerLabel: l, colorPickerViewConfig: c }) {
    super(e);
    /**
    * Tracks information about the DOM focus in the list.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * A collection of components.
    */
    f(this, "items");
    /**
    * A fragment that allows users to select colors from the a predefined set and from existing document colors.
    */
    f(this, "colorGridsFragmentView");
    /**
    * A fragment that allows users to select a color from a color picker.
    */
    f(this, "colorPickerFragmentView");
    /**
    * Helps cycling over focusable {@link #items} in the list.
    *
    * @readonly
    */
    f(this, "_focusCycler");
    /**
    * A collection of views that can be focused in the view.
    *
    * @readonly
    */
    f(this, "_focusables");
    /**
    * The configuration of color picker sub-component.
    */
    f(this, "_colorPickerViewConfig");
    this.items = this.createCollection(), this.focusTracker = new ze(), this.keystrokes = new Ze(), this._focusables = new Et(), this._colorPickerViewConfig = c, this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate list items backwards using the <kbd>Shift</kbd> + <kbd>Tab</kbd> keystroke.
        focusPrevious: "shift + tab",
        // Navigate list items forwards using the <kbd>Tab</kbd> key.
        focusNext: "tab"
      }
    }), this.colorGridsFragmentView = new p9(e, {
      colors: i,
      columns: s,
      removeButtonLabel: r,
      documentColorsLabel: o,
      documentColorsCount: a,
      colorPickerLabel: l,
      focusTracker: this.focusTracker,
      focusables: this._focusables
    }), this.colorPickerFragmentView = new b9(e, {
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokes: this.keystrokes,
      colorPickerViewConfig: c
    }), this.set("_isColorGridsFragmentVisible", !0), this.set("_isColorPickerFragmentVisible", !1), this.set("selectedColor", void 0), this.colorGridsFragmentView.bind("isVisible").to(this, "_isColorGridsFragmentVisible"), this.colorPickerFragmentView.bind("isVisible").to(this, "_isColorPickerFragmentVisible"), this.on("change:selectedColor", (u, d, h) => {
      this.colorGridsFragmentView.set("selectedColor", h), this.colorPickerFragmentView.set("selectedColor", h);
    }), this.colorGridsFragmentView.on("change:selectedColor", (u, d, h) => {
      this.set("selectedColor", h);
    }), this.colorPickerFragmentView.on("change:selectedColor", (u, d, h) => {
      this.set("selectedColor", h);
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-color-selector"
        ]
      },
      children: this.items
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Renders the internals of the component on demand:
  * * {@link #colorPickerFragmentView},
  * * {@link #colorGridsFragmentView}.
  *
  * It allows for deferring component initialization to improve the performance.
  *
  * See {@link #showColorPickerFragment}, {@link #showColorGridsFragment}.
  */
  appendUI() {
    this._appendColorGridsFragment(), this._colorPickerViewConfig && this._appendColorPickerFragment();
  }
  /**
  * Shows the {@link #colorPickerFragmentView} and hides the {@link #colorGridsFragmentView}.
  *
  * **Note**: It requires {@link #appendUI} to be called first.
  *
  * See {@link #showColorGridsFragment}, {@link ~ColorSelectorView#event:colorPicker:show}.
  */
  showColorPickerFragment() {
    !this.colorPickerFragmentView.colorPickerView || this._isColorPickerFragmentVisible || (this._isColorPickerFragmentVisible = !0, this.colorPickerFragmentView.focus(), this.colorPickerFragmentView.resetValidationStatus(), this._isColorGridsFragmentVisible = !1);
  }
  /**
  * Shows the {@link #colorGridsFragmentView} and hides the {@link #colorPickerFragmentView}.
  *
  * See {@link #showColorPickerFragment}.
  *
  * **Note**: It requires {@link #appendUI} to be called first.
  */
  showColorGridsFragment() {
    this._isColorGridsFragmentVisible || (this._isColorGridsFragmentVisible = !0, this.colorGridsFragmentView.focus(), this._isColorPickerFragmentVisible = !1);
  }
  /**
  * Focuses the first focusable element in {@link #items}.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Focuses the last focusable element in {@link #items}.
  */
  focusLast() {
    this._focusCycler.focusLast();
  }
  /**
  * Scans through the editor model and searches for text node attributes with the given `attributeName`.
  * Found entries are set as document colors in {@link #colorGridsFragmentView}.
  *
  * All the previously stored document colors will be lost in the process.
  *
  * @param model The model used as a source to obtain the document colors.
  * @param attributeName Determines the name of the related model's attribute for a given dropdown.
  */
  updateDocumentColors(e, i) {
    this.colorGridsFragmentView.updateDocumentColors(e, i);
  }
  /**
  * Refreshes the state of the selected color in one or both grids located in {@link #colorGridsFragmentView}.
  *
  * It guarantees that the selection will occur only in one of them.
  */
  updateSelectedColors() {
    this.colorGridsFragmentView.updateSelectedColors();
  }
  /**
  * Appends the view containing static and document color grid views.
  */
  _appendColorGridsFragment() {
    this.items.length || (this.items.add(this.colorGridsFragmentView), this.colorGridsFragmentView.delegate("execute").to(this), this.colorGridsFragmentView.delegate("colorPicker:show").to(this));
  }
  /**
  * Appends the view with the color picker.
  */
  _appendColorPickerFragment() {
    this.items.length !== 2 && (this.items.add(this.colorPickerFragmentView), this.colorGridsFragmentView.colorPickerButtonView && this.colorGridsFragmentView.colorPickerButtonView.on("execute", () => {
      this.showColorPickerFragment();
    }), this.colorGridsFragmentView.addColorPickerButton(), this.colorPickerFragmentView.delegate("execute").to(this), this.colorPickerFragmentView.delegate("colorPicker:cancel").to(this));
  }
}
class w9 {
  /**
  * Creates an instance of the factory.
  *
  * @param editor The editor instance.
  */
  constructor(t) {
    /**
    * The editor instance that the factory belongs to.
    */
    f(this, "editor");
    /**
    * Registered component factories.
    */
    f(this, "_components", /* @__PURE__ */ new Map());
    this.editor = t;
  }
  /**
  * Returns an iterator of registered component names. Names are returned in lower case.
  */
  *names() {
    for (const t of this._components.values())
      yield t.originalName;
  }
  /**
  * Registers a component factory function that will be used by the
  * {@link #create create} method and called with the
  * {@link module:core/editor/editor~Editor#locale editor locale} as an argument,
  * allowing localization of the {@link module:ui/view~View view}.
  *
  * @param name The name of the component.
  * @param callback The callback that returns the component.
  */
  add(t, e) {
    this._components.set(id(t), {
      callback: e,
      originalName: t
    });
  }
  /**
  * Creates an instance of a component registered in the factory under a specific name.
  *
  * When called, the {@link module:core/editor/editor~Editor#locale editor locale} is passed to
  * the previously {@link #add added} factory function, allowing localization of the
  * {@link module:ui/view~View view}.
  *
  * @param name The name of the component.
  * @returns The instantiated component view.
  */
  create(t) {
    if (!this.has(t))
      throw new S("componentfactory-item-missing", this, {
        name: t
      });
    return this._components.get(id(t)).callback(this.editor.locale);
  }
  /**
  * Checks if a component of a given name is registered in the factory.
  *
  * @param name The name of the component.
  */
  has(t) {
    return this._components.has(id(t));
  }
}
function id(n) {
  return String(n).toLowerCase();
}
const Rb = "ck-tooltip", Mt = class Mt extends (/* @__PURE__ */ li()) {
  /**
  * Creates an instance of the tooltip manager.
  */
  constructor(e) {
    super();
    /**
    * The view rendering text of the tooltip.
    */
    f(this, "tooltipTextView");
    /**
    * The instance of the balloon panel that renders and positions the tooltip.
    */
    f(this, "balloonPanelView");
    /**
    * Stores the reference to the DOM element the tooltip is attached to. `null` when there's no tooltip
    * in the UI.
    */
    f(this, "_currentElementWithTooltip", null);
    /**
    * Stores the current tooltip position. `null` when there's no tooltip in the UI.
    */
    f(this, "_currentTooltipPosition", null);
    /**
    * An instance of the mutation observer that keeps track on target element attributes changes.
    */
    f(this, "_mutationObserver", null);
    /**
    * A debounced version of {@link #_pinTooltip}. Tooltips show with a delay to avoid flashing and
    * to improve the UX.
    */
    f(this, "_pinTooltipDebounced");
    /**
    * A debounced version of {@link #_unpinTooltip}. Tooltips hide with a delay to allow hovering of their titles.
    */
    f(this, "_unpinTooltipDebounced");
    f(this, "_watchdogExcluded");
    if (Mt._editors.add(e), Mt._instance)
      return Mt._instance;
    Mt._instance = this, this.tooltipTextView = new ee(e.locale), this.tooltipTextView.set("text", ""), this.tooltipTextView.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-tooltip__text"
        ]
      },
      children: [
        {
          text: this.tooltipTextView.bindTemplate.to("text")
        }
      ]
    }), this.balloonPanelView = new Dt(e.locale), this.balloonPanelView.class = Rb, this.balloonPanelView.content.add(this.tooltipTextView), this._mutationObserver = v9(() => {
      this._updateTooltipPosition();
    }), this._pinTooltipDebounced = vi(this._pinTooltip, 600), this._unpinTooltipDebounced = vi(this._unpinTooltip, 400), this.listenTo(ae.document, "keydown", this._onKeyDown.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "mouseenter", this._onEnterOrFocus.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "mouseleave", this._onLeaveOrBlur.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "focus", this._onEnterOrFocus.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "blur", this._onLeaveOrBlur.bind(this), {
      useCapture: !0
    }), this.listenTo(ae.document, "scroll", this._onScroll.bind(this), {
      useCapture: !0
    }), this._watchdogExcluded = !0;
  }
  /**
  * Destroys the tooltip manager.
  *
  * **Note**: The manager singleton cannot be destroyed until all editors that use it are destroyed.
  *
  * @param editor The editor the manager was created for.
  */
  destroy(e) {
    const i = e.ui.view && e.ui.view.body;
    Mt._editors.delete(e), this.stopListening(e.ui), i && i.has(this.balloonPanelView) && i.remove(this.balloonPanelView), Mt._editors.size || (this._unpinTooltip(), this.balloonPanelView.destroy(), this.stopListening(), Mt._instance = null);
  }
  /**
  * Returns {@link #balloonPanelView} {@link module:utils/dom/position~PositioningFunction positioning functions} for a given position
  * name.
  *
  * @param position Name of the position (`s`, `se`, `sw`, `n`, `e`, or `w`).
  * @returns Positioning functions to be used by the {@link #balloonPanelView}.
  */
  static getPositioningFunctions(e) {
    const i = Mt.defaultBalloonPositions;
    return {
      // South is most popular. We can use positioning heuristics to avoid clipping by the viewport with the sane fallback.
      s: [
        i.southArrowNorth,
        i.southArrowNorthEast,
        i.southArrowNorthWest
      ],
      n: [
        i.northArrowSouth
      ],
      e: [
        i.eastArrowWest
      ],
      w: [
        i.westArrowEast
      ],
      sw: [
        i.southArrowNorthEast
      ],
      se: [
        i.southArrowNorthWest
      ]
    }[e];
  }
  /**
  * Handles hiding tooltips on `keydown` in DOM.
  *
  * @param evt An object containing information about the fired event.
  * @param domEvent The DOM event.
  */
  _onKeyDown(e, i) {
    i.key === "Escape" && this._currentElementWithTooltip && (this._unpinTooltip(), i.stopPropagation());
  }
  /**
  * Handles displaying tooltips on `mouseenter` and `focus` in DOM.
  *
  * @param evt An object containing information about the fired event.
  * @param domEvent The DOM event.
  */
  _onEnterOrFocus(e, { target: i }) {
    const s = nd(i);
    if (!s) {
      e.name === "focus" && this._unpinTooltip();
      return;
    }
    if (s === this._currentElementWithTooltip) {
      this._unpinTooltipDebounced.cancel();
      return;
    }
    this._unpinTooltip(), e.name === "focus" && !s.matches(":hover") || s.matches("[data-cke-tooltip-instant]") ? this._pinTooltip(s, sd(s)) : this._pinTooltipDebounced(s, sd(s));
  }
  /**
  * Handles hiding tooltips on `mouseleave` and `blur` in DOM.
  *
  * @param evt An object containing information about the fired event.
  * @param domEvent The DOM event.
  */
  _onLeaveOrBlur(e, { target: i, relatedTarget: s }) {
    if (e.name === "mouseleave") {
      if (!Hn(i))
        return;
      const r = this.balloonPanelView.element, o = r && (r === s || r.contains(s)), a = !o && i === r;
      if (o) {
        this._unpinTooltipDebounced.cancel();
        return;
      }
      if (!a && this._currentElementWithTooltip && i !== this._currentElementWithTooltip)
        return;
      const l = nd(i), c = nd(s);
      (a || l && l !== c) && (this._pinTooltipDebounced.cancel(), this._currentElementWithTooltip && this._currentElementWithTooltip.matches("[data-cke-tooltip-instant]") || l && l.matches("[data-cke-tooltip-instant]") ? this._unpinTooltip() : this._unpinTooltipDebounced());
    } else {
      if (this._currentElementWithTooltip && i !== this._currentElementWithTooltip)
        return;
      this._pinTooltipDebounced.cancel(), this._unpinTooltipDebounced();
    }
  }
  /**
  * Handles hiding tooltips on `scroll` in DOM.
  *
  * @param evt An object containing information about the fired event.
  * @param domEvent The DOM event.
  */
  _onScroll(e, { target: i }) {
    this._currentElementWithTooltip && (i.contains(this.balloonPanelView.element) && i.contains(this._currentElementWithTooltip) || this._unpinTooltip());
  }
  /**
  * Pins the tooltip to a specific DOM element.
  *
  * @param options.text Text of the tooltip to display.
  * @param options.position The position of the tooltip.
  * @param options.cssClass Additional CSS class of the balloon with the tooltip.
  */
  _pinTooltip(e, { text: i, position: s, cssClass: r }) {
    this._unpinTooltip();
    const o = Xe(Mt._editors.values()).ui.view.body;
    o.has(this.balloonPanelView) || o.add(this.balloonPanelView), this.tooltipTextView.text = i, this.balloonPanelView.class = [
      Rb,
      r
    ].filter((a) => a).join(" "), this.balloonPanelView.pin({
      target: e,
      positions: Mt.getPositioningFunctions(s)
    }), this._mutationObserver.attach(e);
    for (const a of Mt._editors)
      this.listenTo(a.ui, "update", this._updateTooltipPosition.bind(this), {
        priority: "low"
      });
    this._currentElementWithTooltip = e, this._currentTooltipPosition = s;
  }
  /**
  * Unpins the tooltip and cancels all queued pinning.
  */
  _unpinTooltip() {
    this._unpinTooltipDebounced.cancel(), this._pinTooltipDebounced.cancel(), this.balloonPanelView.unpin();
    for (const e of Mt._editors)
      this.stopListening(e.ui, "update");
    this._currentElementWithTooltip = null, this._currentTooltipPosition = null, this.tooltipTextView.text = "", this._mutationObserver.detach();
  }
  /**
  * Updates the position of the tooltip so it stays in sync with the element it is pinned to.
  *
  * Hides the tooltip when the element is no longer visible in DOM or the tooltip text was removed.
  */
  _updateTooltipPosition() {
    if (!this._currentElementWithTooltip)
      return;
    const e = sd(this._currentElementWithTooltip);
    if (!yn(this._currentElementWithTooltip) || !e.text) {
      this._unpinTooltip();
      return;
    }
    this.balloonPanelView.pin({
      target: this._currentElementWithTooltip,
      positions: Mt.getPositioningFunctions(e.position)
    });
  }
};
/**
* A set of default {@link module:utils/dom/position~PositioningFunction positioning functions} used by the `TooltipManager`
* to pin tooltips in different positions.
*/
f(Mt, "defaultBalloonPositions", /* @__PURE__ */ Dt.generatePositions({
  heightOffset: 5,
  sideOffset: 13
})), /**
* A set of editors the single tooltip manager instance must listen to.
* This is mostly to handle `EditorUI#update` listeners from individual editors.
*/
f(Mt, "_editors", /* @__PURE__ */ new Set()), /**
* A reference to the `TooltipManager` instance. The class is a singleton and as such,
* successive attempts at creating instances should return this instance.
*/
f(Mt, "_instance", null);
let _h = Mt;
function nd(n) {
  return Hn(n) ? n.closest("[data-cke-tooltip-text]:not([data-cke-tooltip-disabled])") : null;
}
function sd(n) {
  return {
    text: n.dataset.ckeTooltipText,
    position: n.dataset.ckeTooltipPosition || "s",
    cssClass: n.dataset.ckeTooltipClass || ""
  };
}
function v9(n) {
  const t = new MutationObserver(() => {
    n();
  });
  return {
    attach(e) {
      t.observe(e, {
        attributes: !0,
        attributeFilter: [
          "data-cke-tooltip-text",
          "data-cke-tooltip-position"
        ]
      });
    },
    detach() {
      t.disconnect();
    }
  };
}
const _9 = 50, y9 = 350;
class zy extends (/* @__PURE__ */ li()) {
  /**
  * Creates a badge for a given editor. The feature is initialized on Editor#ready
  * event.
  */
  constructor(e, i = {}) {
    super();
    /**
    * Editor instance the helper was created for.
    */
    f(this, "editor");
    /**
    * A reference to the balloon panel hosting and positioning the badge content.
    */
    f(this, "_balloonView", null);
    /**
    * A throttled version of the {@link #_showBalloon} method meant for frequent use to avoid performance loss.
    */
    f(this, "_showBalloonThrottled", $s(() => this._showBalloon(), 50, {
      leading: !0
    }));
    /**
    * A reference to the last editable element (root, source editing area, etc.) focused by the user.
    * Since the focus can move to other focusable elements in the UI, this reference allows positioning the balloon over the
    * right element whether the user is typing or using the UI.
    */
    f(this, "_lastFocusedEditableElement", null);
    /**
    * An additional CSS class added to the `BalloonView`.
    */
    f(this, "_balloonClass");
    this.editor = e, this._balloonClass = i.balloonClass, e.on("ready", () => this._handleEditorReady());
  }
  /**
  * Destroys the badge along with its view.
  */
  destroy() {
    const e = this._balloonView;
    e && (e.unpin(), this._balloonView = null), this._showBalloonThrottled.cancel(), this.stopListening();
  }
  /**
  * Enables badge label once the editor (ui) is ready.
  */
  _handleEditorReady() {
    const e = this.editor;
    this._isEnabled() && e.ui.view && (e.ui.focusTracker.on("change:isFocused", (i, s, r) => {
      this._updateLastFocusedEditableElement(), r ? this._showBalloon() : this._hideBalloon();
    }), e.ui.focusTracker.on("change:focusedElement", (i, s, r) => {
      this._updateLastFocusedEditableElement(), r && this._showBalloon();
    }), e.ui.on("update", () => {
      this._showBalloonThrottled();
    }));
  }
  /**
  * Returns normalized configuration for the badge.
  */
  _getNormalizedConfig() {
    return {
      side: this.editor.locale.contentLanguageDirection === "ltr" ? "right" : "left",
      position: "border",
      verticalOffset: 0,
      horizontalOffset: 5
    };
  }
  /**
  * Attempts to display the balloon with the badge view.
  */
  _showBalloon() {
    const e = this._getBalloonAttachOptions();
    e && (this._balloonView || (this._balloonView = this._createBalloonView()), this._balloonView.pin(e));
  }
  /**
  * Hides the badge balloon if already visible.
  */
  _hideBalloon() {
    this._balloonView && this._balloonView.unpin();
  }
  /**
  * Creates an instance of the {@link module:ui/panel/balloon/balloonpanelview~BalloonPanelView balloon panel}
  * with the badge view inside ready for positioning.
  */
  _createBalloonView() {
    const e = this.editor, i = new Dt(), s = this._createBadgeContent();
    return i.content.add(s), this._balloonClass && (i.class = this._balloonClass), e.ui.view.body.add(i), i;
  }
  /**
  * Returns the options for attaching the balloon to the focused editable element.
  */
  _getBalloonAttachOptions() {
    if (!this._lastFocusedEditableElement)
      return null;
    const e = this._getNormalizedConfig(), i = e.side === "right" ? k9(this._lastFocusedEditableElement, e) : C9(this._lastFocusedEditableElement, e);
    return {
      target: this._lastFocusedEditableElement,
      positions: [
        i
      ]
    };
  }
  /**
  * Updates the {@link #_lastFocusedEditableElement} based on the state of the global focus tracker.
  */
  _updateLastFocusedEditableElement() {
    const e = this.editor, i = e.ui.focusTracker.isFocused, s = e.ui.focusTracker.focusedElement;
    if (!i || !s) {
      this._lastFocusedEditableElement = null;
      return;
    }
    const r = Array.from(e.ui.getEditableElementsNames()).map((o) => e.ui.getEditableElement(o));
    r.includes(s) ? this._lastFocusedEditableElement = s : this._lastFocusedEditableElement = r[0];
  }
}
function k9(n, t) {
  return Hy(n, t, (e, i) => e.left + e.width - i.width - t.horizontalOffset);
}
function C9(n, t) {
  return Hy(n, t, (e) => e.left + t.horizontalOffset);
}
function Hy(n, t, e) {
  return (i, s) => {
    const r = new ke(n);
    if (r.width < y9 || r.height < _9)
      return null;
    let o;
    t.position === "inside" ? o = r.bottom - s.height : o = r.bottom - s.height / 2, o -= t.verticalOffset;
    const a = e(r, s), c = i.clone().moveTo(a, o).getIntersection(s.clone().moveTo(a, o)).getVisible();
    return !c || c.getArea() < s.getArea() ? null : {
      top: o,
      left: a,
      name: `position_${t.position}-side_${t.side}`,
      config: {
        withArrow: !1
      }
    };
  };
}
var x9 = `<svg xmlns="http://www.w3.org/2000/svg" width="53" height="10" viewBox="0 0 53 10"><path fill="#1C2331" d="M31.724 1.492a15.139 15.139 0 0 0 .045 1.16 2.434 2.434 0 0 0-.687-.34 3.68 3.68 0 0 0-1.103-.166 2.332 2.332 0 0 0-1.14.255 1.549 1.549 0 0 0-.686.87c-.15.41-.225.98-.225 1.712 0 .939.148 1.659.444 2.161.297.503.792.754 1.487.754.452.015.9-.094 1.294-.316.296-.174.557-.4.771-.669l.14.852h1.282V.007h-1.623v1.485ZM31 6.496a1.77 1.77 0 0 1-.494.061.964.964 0 0 1-.521-.127.758.758 0 0 1-.296-.466 3.984 3.984 0 0 1-.093-.992 4.208 4.208 0 0 1 .098-1.052.753.753 0 0 1 .307-.477 1.08 1.08 0 0 1 .55-.122c.233-.004.466.026.69.089l.483.144v2.553c-.11.076-.213.143-.307.2a1.73 1.73 0 0 1-.417.189ZM35.68 0l-.702.004c-.322.002-.482.168-.48.497l.004.581c.002.33.164.493.486.49l.702-.004c.322-.002.481-.167.48-.496L36.165.49c-.002-.33-.164-.493-.486-.491ZM36.145 2.313l-1.612.01.034 5.482 1.613-.01-.035-5.482ZM39.623.79 37.989.8 38 2.306l-.946.056.006 1.009.949-.006.024 2.983c.003.476.143.844.419 1.106.275.26.658.39 1.148.387.132 0 .293-.01.483-.03.19-.02.38-.046.57-.08.163-.028.324-.068.482-.119l-.183-1.095-.702.004a.664.664 0 0 1-.456-.123.553.553 0 0 1-.14-.422l-.016-2.621 1.513-.01-.006-1.064-1.514.01-.01-1.503ZM46.226 2.388c-.41-.184-.956-.274-1.636-.27-.673.004-1.215.101-1.627.29-.402.179-.72.505-.888.91-.18.419-.268.979-.264 1.68.004.688.1 1.24.285 1.655.172.404.495.724.9.894.414.18.957.268 1.63.264.68-.004 1.224-.099 1.632-.284.4-.176.714-.501.878-.905.176-.418.263-.971.258-1.658-.004-.702-.097-1.261-.28-1.677a1.696 1.696 0 0 0-.888-.9Zm-.613 3.607a.77.77 0 0 1-.337.501 1.649 1.649 0 0 1-1.317.009.776.776 0 0 1-.343-.497 4.066 4.066 0 0 1-.105-1.02 4.136 4.136 0 0 1 .092-1.03.786.786 0 0 1 .337-.507 1.59 1.59 0 0 1 1.316-.008.79.79 0 0 1 .344.502c.078.337.113.683.105 1.03.012.343-.019.685-.092 1.02ZM52.114 2.07a2.67 2.67 0 0 0-1.128.278c-.39.191-.752.437-1.072.73l-.157-.846-1.273.008.036 5.572 1.623-.01-.024-3.78c.35-.124.646-.22.887-.286.26-.075.53-.114.8-.118l.45-.003.144-1.546-.286.001ZM22.083 7.426l-1.576-2.532a2.137 2.137 0 0 0-.172-.253 1.95 1.95 0 0 0-.304-.29.138.138 0 0 1 .042-.04 1.7 1.7 0 0 0 .328-.374l1.75-2.71c.01-.015.025-.028.024-.048-.01-.01-.021-.007-.031-.007L20.49 1.17a.078.078 0 0 0-.075.045l-.868 1.384c-.23.366-.46.732-.688 1.099a.108.108 0 0 1-.112.06c-.098-.005-.196-.001-.294-.002-.018 0-.038.006-.055-.007.002-.02.002-.039.005-.058a4.6 4.6 0 0 0 .046-.701V1.203c0-.02-.009-.032-.03-.03h-.033L16.93 1.17c-.084 0-.073-.01-.073.076v6.491c-.001.018.006.028.025.027h1.494c.083 0 .072.007.072-.071v-2.19c0-.055-.003-.11-.004-.166a3.366 3.366 0 0 0-.05-.417h.06c.104 0 .209.002.313-.002a.082.082 0 0 1 .084.05c.535.913 1.07 1.824 1.607 2.736a.104.104 0 0 0 .103.062c.554-.003 1.107-.002 1.66-.002l.069-.003-.019-.032-.188-.304ZM27.112 6.555c-.005-.08-.004-.08-.082-.08h-2.414c-.053 0-.106-.003-.159-.011a.279.279 0 0 1-.246-.209.558.558 0 0 1-.022-.15c0-.382 0-.762-.002-1.143 0-.032.007-.049.042-.044h2.504c.029.003.037-.012.034-.038V3.814c0-.089.013-.078-.076-.078h-2.44c-.07 0-.062.003-.062-.06v-.837c0-.047.004-.093.013-.14a.283.283 0 0 1 .241-.246.717.717 0 0 1 .146-.011h2.484c.024.002.035-.009.036-.033l.003-.038.03-.496c.01-.183.024-.365.034-.548.005-.085.003-.087-.082-.094-.218-.018-.437-.038-.655-.05a17.845 17.845 0 0 0-.657-.026 72.994 72.994 0 0 0-1.756-.016 1.7 1.7 0 0 0-.471.064 1.286 1.286 0 0 0-.817.655c-.099.196-.149.413-.145.633v3.875c0 .072.003.144.011.216a1.27 1.27 0 0 0 .711 1.029c.228.113.48.167.734.158.757-.005 1.515.002 2.272-.042.274-.016.548-.034.82-.053.03-.002.043-.008.04-.041-.008-.104-.012-.208-.019-.312a69.964 69.964 0 0 1-.05-.768ZM16.14 7.415l-.127-1.075c-.004-.03-.014-.04-.044-.037a13.125 13.125 0 0 1-.998.073c-.336.01-.672.02-1.008.016-.116-.001-.233-.014-.347-.039a.746.746 0 0 1-.45-.262c-.075-.1-.132-.211-.167-.33a3.324 3.324 0 0 1-.126-.773 9.113 9.113 0 0 1-.015-.749c0-.285.022-.57.065-.852.023-.158.066-.312.127-.46a.728.728 0 0 1 .518-.443 1.64 1.64 0 0 1 .397-.048c.628-.001 1.255.003 1.882.05.022.001.033-.006.036-.026l.003-.031.06-.55c.019-.177.036-.355.057-.532.004-.034-.005-.046-.04-.056a5.595 5.595 0 0 0-1.213-.21 10.783 10.783 0 0 0-.708-.02c-.24-.003-.48.01-.719.041a3.477 3.477 0 0 0-.625.14 1.912 1.912 0 0 0-.807.497c-.185.2-.33.433-.424.688a4.311 4.311 0 0 0-.24 1.096c-.031.286-.045.572-.042.86-.006.43.024.86.091 1.286.04.25.104.497.193.734.098.279.26.53.473.734.214.205.473.358.756.446.344.11.702.17 1.063.177a8.505 8.505 0 0 0 1.578-.083 6.11 6.11 0 0 0 .766-.18c.03-.008.047-.023.037-.057a.157.157 0 0 1-.003-.025Z"/><path fill="#AFE229" d="M6.016 6.69a1.592 1.592 0 0 0-.614.21c-.23.132-.422.32-.56.546-.044.072-.287.539-.287.539l-.836 1.528.009.006c.038.025.08.046.123.063.127.046.26.07.395.073.505.023 1.011-.007 1.517-.003.29.009.58.002.869-.022a.886.886 0 0 0 .395-.116.962.962 0 0 0 .312-.286c.056-.083.114-.163.164-.249.24-.408.48-.816.718-1.226.075-.128.148-.257.222-.386l.112-.192a1.07 1.07 0 0 0 .153-.518l-1.304.023s-1.258-.005-1.388.01Z"/><path fill="#771BFF" d="m2.848 9.044.76-1.39.184-.352c-.124-.067-.245-.14-.367-.21-.346-.204-.706-.384-1.045-.6a.984.984 0 0 1-.244-.207c-.108-.134-.136-.294-.144-.46-.021-.409-.002-.818-.009-1.227-.003-.195 0-.39.003-.585.004-.322.153-.553.427-.713l.833-.488c.22-.13.44-.257.662-.385.05-.029.105-.052.158-.077.272-.128.519-.047.76.085l.044.028c.123.06.242.125.358.196.318.178.635.357.952.537.095.056.187.117.275.184.194.144.254.35.266.578.016.284.007.569.006.853-.001.28.004.558 0 .838.592-.003 1.259 0 1.259 0l.723-.013c-.003-.292-.007-.584-.007-.876 0-.524.015-1.048-.016-1.571-.024-.42-.135-.8-.492-1.067a5.02 5.02 0 0 0-.506-.339A400.52 400.52 0 0 0 5.94.787C5.722.664 5.513.524 5.282.423 5.255.406 5.228.388 5.2.373 4.758.126 4.305-.026 3.807.21c-.097.046-.197.087-.29.14A699.896 699.896 0 0 0 .783 1.948c-.501.294-.773.717-.778 1.31-.004.36-.009.718-.001 1.077.016.754-.017 1.508.024 2.261.016.304.07.6.269.848.127.15.279.28.448.382.622.4 1.283.734 1.92 1.11l.183.109Z"/></svg>
`;
const T9 = "Powered by";
class E9 extends zy {
  constructor(t) {
    super(t, {
      balloonClass: "ck-powered-by-balloon"
    });
  }
  /**
  * Enables "powered by" label.
  */
  _isEnabled() {
    const t = this.editor;
    if (t.config.get("ui.poweredBy.forceVisible"))
      return !0;
    const i = t.config.get("licenseKey");
    if (i == "GPL")
      return !0;
    const s = yf(i.split(".")[1]);
    return s ? !s.whiteLabel : !0;
  }
  /**
  * Creates a "powered by" badge content.
  */
  _createBadgeContent() {
    return new S9(this.editor.locale, this._getNormalizedConfig().label);
  }
  /**
  * Returns the normalized configuration for the "powered by" badge.
  * It takes the user configuration into account and falls back to the default one.
  */
  _getNormalizedConfig() {
    const t = super._getNormalizedConfig(), e = this.editor.config.get("ui.poweredBy") || {}, i = e.position || t.position, s = i === "inside" ? 5 : t.verticalOffset;
    return {
      position: i,
      side: e.side || t.side,
      label: e.label === void 0 ? T9 : e.label,
      verticalOffset: e.verticalOffset !== void 0 ? e.verticalOffset : s,
      horizontalOffset: e.horizontalOffset !== void 0 ? e.horizontalOffset : t.horizontalOffset,
      forceVisible: !!e.forceVisible
    };
  }
}
class S9 extends ee {
  /**
  * Creates an instance of the "powered by" view.
  *
  * @param locale The localization services instance.
  * @param label The label text.
  */
  constructor(t, e) {
    super(t);
    const i = new Qi(), s = this.bindTemplate;
    i.set({
      content: x9,
      isColorInherited: !1
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-powered-by"
        ],
        "aria-hidden": !0
      },
      children: [
        {
          tag: "a",
          attributes: {
            href: "https://ckeditor.com/powered-by-ckeditor/?utm_source=ckeditor&utm_medium=referral&utm_campaign=701Dn000000hVgmIAE_powered_by_ckeditor_logo",
            target: "_blank",
            tabindex: "-1"
          },
          children: [
            ...e ? [
              {
                tag: "span",
                attributes: {
                  class: [
                    "ck",
                    "ck-powered-by__label"
                  ]
                },
                children: [
                  e
                ]
              }
            ] : [],
            i
          ],
          on: {
            dragstart: s.to((r) => r.preventDefault())
          }
        }
      ]
    });
  }
}
class A9 extends zy {
  constructor(e) {
    super(e, {
      balloonClass: "ck-evaluation-badge-balloon"
    });
    f(this, "licenseTypeMessage", {
      evaluation: "For evaluation purposes only",
      trial: "For evaluation purposes only",
      development: "For development purposes only"
    });
  }
  /**
  * Enables "evaluation badge" label.
  */
  _isEnabled() {
    const i = this.editor.config.get("licenseKey"), s = Ob(i);
    return !!(s && this.licenseTypeMessage[s]);
  }
  /**
  * Creates the content of the "evaluation badge".
  */
  _createBadgeContent() {
    const e = this.editor.config.get("licenseKey"), i = Ob(e);
    return new I9(this.editor.locale, this.licenseTypeMessage[i]);
  }
  /**
  * Returns the normalized configuration for the "evaluation badge".
  * It takes 'ui.poweredBy' configuration into account to determine the badge position and side.
  */
  _getNormalizedConfig() {
    const e = super._getNormalizedConfig(), i = this.editor.config.get("ui.poweredBy") || {}, s = i.position || e.position, r = i.side || e.side;
    return {
      position: s,
      side: r === "left" ? "right" : "left",
      verticalOffset: e.verticalOffset,
      horizontalOffset: e.horizontalOffset
    };
  }
}
class I9 extends ee {
  /**
  * Creates an instance of the "evaluation badge" view.
  *
  * @param locale The localization services instance.
  * @param label The label text.
  */
  constructor(t, e) {
    super(t), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-evaluation-badge"
        ],
        "aria-hidden": !0
      },
      children: [
        {
          tag: "span",
          attributes: {
            class: [
              "ck",
              "ck-evaluation-badge__label"
            ]
          },
          children: [
            e
          ]
        }
      ]
    });
  }
}
function Ob(n) {
  if (n == "GPL")
    return "GPL";
  const t = yf(n.split(".")[1]);
  return t ? t.licenseType || "production" : null;
}
const Vb = {
  POLITE: "polite",
  ASSERTIVE: "assertive"
};
class P9 {
  /**
  * @inheritDoc
  */
  constructor(t) {
    /**
    * The editor instance.
    */
    f(this, "editor");
    /**
    * The view that aggregates all `aria-live` regions.
    */
    f(this, "view");
    this.editor = t, t.once("ready", () => {
      for (const e of Object.values(Vb))
        this.announce("", e);
    });
  }
  /**
  * Sets an announcement text to an aria region that is then announced by a screen reader to the user.
  *
  * If the aria region of a specified politeness does not exist, it will be created and can be re-used later.
  *
  * The default announcement politeness level is `'polite'`.
  *
  * ```ts
  * // Most screen readers will queue announcements from multiple aria-live regions and read them out in the order they were emitted.
   * editor.ui.ariaLiveAnnouncer.announce( 'Image uploaded.' );
   * editor.ui.ariaLiveAnnouncer.announce( 'Connection lost. Reconnecting.' );
   * ```
  */
  announce(t, e = Vb.POLITE) {
    const i = this.editor;
    if (!i.ui.view)
      return;
    this.view || (this.view = new R9(i.locale), i.ui.view.body.add(this.view));
    const { politeness: s, isUnsafeHTML: r } = typeof e == "string" ? {
      politeness: e
    } : e;
    let o = this.view.regionViews.find((a) => a.politeness === s);
    o || (o = new O9(i, s), this.view.regionViews.add(o)), o.announce({
      announcement: t,
      isUnsafeHTML: r
    });
  }
}
class R9 extends ee {
  constructor(e) {
    super(e);
    /**
    * A collection of all views that represent individual `aria-live` regions.
    */
    f(this, "regionViews");
    this.regionViews = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-aria-live-announcer"
        ]
      },
      children: this.regionViews
    });
  }
}
class O9 extends ee {
  constructor(e, i) {
    super(e.locale);
    /**
    * Current politeness level of the region.
    */
    f(this, "politeness");
    /**
    * DOM converter used to sanitize unsafe HTML passed to {@link #announce} method.
    */
    f(this, "_domConverter");
    /**
    * Interval used to remove additions. It prevents accumulation of added nodes in region.
    */
    f(this, "_pruneAnnouncementsInterval");
    this.setTemplate({
      tag: "div",
      attributes: {
        "aria-live": i,
        "aria-relevant": "additions"
      },
      children: [
        {
          tag: "ul",
          attributes: {
            class: [
              "ck",
              "ck-aria-live-region-list"
            ]
          }
        }
      ]
    }), e.on("destroy", () => {
      this._pruneAnnouncementsInterval !== null && (clearInterval(this._pruneAnnouncementsInterval), this._pruneAnnouncementsInterval = null);
    }), this.politeness = i, this._domConverter = e.data.htmlProcessor.domConverter, this._pruneAnnouncementsInterval = setInterval(() => {
      this.element && this._listElement.firstChild && this._listElement.firstChild.remove();
    }, 5e3);
  }
  /**
  * Appends new announcement to region.
  */
  announce({ announcement: e, isUnsafeHTML: i }) {
    if (!e.trim().length)
      return;
    const s = document.createElement("li");
    i ? this._domConverter.setContentOf(s, e) : s.innerText = e, this._listElement.appendChild(s);
  }
  /**
  * Return current announcements list HTML element.
  */
  get _listElement() {
    return this.element.querySelector("ul");
  }
}
class en extends Wn {
  /**
  * Creates an instance of the list item view.
  *
  * @param locale The localization services instance.
  */
  constructor(t, e) {
    super(t);
    const i = this.bindTemplate;
    this.extendTemplate({
      attributes: {
        class: [
          "ck-menu-bar__menu__item"
        ]
      },
      on: {
        mouseenter: i.to("mouseenter")
      }
    }), this.delegate("mouseenter").to(e);
  }
}
const ul = 5, dr = {
  /**
  * When the bar is already open:
  * * Opens the menu when the user hovers over its button.
  * * Closes open menu when another menu's button gets hovered.
  */
  toggleMenusAndFocusItemsOnHover(n) {
    n.on("menu:mouseenter", (t) => {
      if (!(!n.isFocusBorderEnabled && !n.isOpen)) {
        if (n.isOpen)
          for (const e of n.menus) {
            const i = t.path[0], s = i instanceof en && i.children.first === e;
            e.isOpen = (t.path.includes(e) || s) && e.isEnabled;
          }
        t.source.focus();
      }
    });
  },
  /**
  * Moves between top-level menus using the arrow left and right keys.
  *
  * If the menubar has already been open, the arrow keys move focus between top-level menu buttons and open them.
  * If the menubar is closed, the arrow keys only move focus between top-level menu buttons.
  */
  focusCycleMenusOnArrows(n) {
    const t = n.locale.uiLanguageDirection === "rtl";
    n.on("menu:arrowright", (i) => {
      e(i.source, t ? -1 : 1);
    }), n.on("menu:arrowleft", (i) => {
      e(i.source, t ? 1 : -1);
    });
    function e(i, s) {
      const r = n.children.getIndex(i), o = i.isOpen, a = n.children.length, l = n.children.get((r + a + s) % a);
      i.isOpen = !1, o && (l.isOpen = !0), l.buttonView.focus();
    }
  },
  /**
  * Closes the entire sub-menu structure when the bar is closed. This prevents sub-menus from being open if the user
  * closes the entire bar, and then re-opens some top-level menu.
  */
  closeMenusWhenTheBarCloses(n) {
    n.on("change:isOpen", () => {
      n.isOpen || n.menus.forEach((t) => {
        t.isOpen = !1;
      });
    });
  },
  /**
  * Handles the following case:
  * 1. Hover to open a sub-menu (A). The button has focus.
  * 2. Press arrow up/down to move focus to another sub-menu (B) button.
  * 3. Press arrow right to open the sub-menu (B).
  * 4. The sub-menu (A) should close as it would with `toggleMenusAndFocusItemsOnHover()`.
  */
  closeMenuWhenAnotherOnTheSameLevelOpens(n) {
    n.on("menu:change:isOpen", (t, e, i) => {
      i && n.menus.filter((s) => t.source.parentMenuView === s.parentMenuView && t.source !== s && s.isOpen).forEach((s) => {
        s.isOpen = !1;
      });
    });
  },
  /**
  * Closes the bar when the user clicked outside of it (page body, editor root, etc.).
  */
  closeOnClickOutside(n) {
    Xn({
      emitter: n,
      activator: () => n.isOpen,
      callback: () => n.close(),
      contextElements: () => n.children.map((t) => t.element)
    });
  },
  /**
  * Tracks the keyboard focus interaction on the menu bar view. It is used to determine if the nested items
  * of the menu bar should render focus rings after first interaction with the keyboard.
  */
  enableFocusHighlightOnInteraction(n) {
    let t = !1;
    n.on("change:isOpen", (e, i, s) => {
      s || (t || (n.isFocusBorderEnabled = !1), t = !1);
    }), n.listenTo(n.element, "keydown", () => {
      t = !0;
    }, {
      useCapture: !0
    }), n.listenTo(n.element, "keyup", () => {
      t = !1;
    }, {
      useCapture: !0
    }), n.listenTo(n.element, "focus", () => {
      t && (n.isFocusBorderEnabled = !0);
    }, {
      useCapture: !0
    });
  }
}, ns = {
  /**
  * If the button of the menu is focused, pressing the arrow down key should open the panel and focus it.
  * This is analogous to the {@link module:ui/dropdown/dropdownview~DropdownView}.
  */
  openAndFocusPanelOnArrowDownKey(n) {
    n.keystrokes.set("arrowdown", (t, e) => {
      n.focusTracker.focusedElement === n.buttonView.element && (n.isOpen || (n.isOpen = !0), n.panelView.focus(), e());
    });
  },
  /**
  * Open the menu on the right arrow key press. This allows for navigating to sub-menus using the keyboard.
  */
  openOnArrowRightKey(n) {
    const t = n.locale.uiLanguageDirection === "rtl" ? "arrowleft" : "arrowright";
    n.keystrokes.set(t, (e, i) => {
      n.focusTracker.focusedElement !== n.buttonView.element || !n.isEnabled || (n.isOpen || (n.isOpen = !0), n.panelView.focus(), i());
    });
  },
  /**
  * Opens the menu on its button click. Note that this behavior only opens but never closes the menu (unlike
  * {@link module:ui/dropdown/dropdownview~DropdownView}).
  */
  openOnButtonClick(n) {
    n.buttonView.on("execute", () => {
      n.isOpen = !0;
    });
  },
  /**
  * Toggles the menu on its button click. This behavior is analogous to {@link module:ui/dropdown/dropdownview~DropdownView}.
  */
  toggleOnButtonClick(n) {
    n.buttonView.on("execute", () => {
      n.isOpen = !n.isOpen;
    });
  },
  /**
  * Opens the menu and focuses the panel content upon pressing the Enter key.
  */
  openAndFocusOnEnterKeyPress(n) {
    n.keystrokes.set("enter", (t, e) => {
      n.focusTracker.focusedElement === n.buttonView.element && (n.isOpen = !0, n.panelView.focus(), e());
    });
  },
  /**
  * Closes the menu on the right left key press. This allows for navigating to sub-menus using the keyboard.
  */
  closeOnArrowLeftKey(n) {
    const t = n.locale.uiLanguageDirection === "rtl" ? "arrowright" : "arrowleft";
    n.keystrokes.set(t, (e, i) => {
      n.isOpen && (n.isOpen = !1, n.focus(), i());
    });
  },
  /**
  * Closes the menu on the esc key press. This allows for navigating to sub-menus using the keyboard.
  */
  closeOnEscKey(n) {
    n.keystrokes.set("esc", (t, e) => {
      n.isOpen && (n.isOpen = !1, n.focus(), e());
    });
  },
  /**
  * Closes the menu when its parent menu also closed. This prevents from orphaned open menus when the parent menu re-opens.
  */
  closeOnParentClose(n) {
    n.parentMenuView.on("change:isOpen", (t, e, i) => {
      !i && t.source === n.parentMenuView && (n.isOpen = !1);
    });
  }
}, V9 = {
  southEast: (n) => ({
    top: n.bottom,
    left: n.left,
    name: "se"
  }),
  southWest: (n, t) => ({
    top: n.bottom,
    left: n.left - t.width + n.width,
    name: "sw"
  }),
  northEast: (n, t) => ({
    top: n.top - t.height,
    left: n.left,
    name: "ne"
  }),
  northWest: (n, t) => ({
    top: n.top - t.height,
    left: n.left - t.width + n.width,
    name: "nw"
  }),
  eastSouth: (n) => ({
    top: n.top,
    left: n.right - ul,
    name: "es"
  }),
  eastNorth: (n, t) => ({
    top: n.top - t.height,
    left: n.right - ul,
    name: "en"
  }),
  westSouth: (n, t) => ({
    top: n.top,
    left: n.left - t.width + ul,
    name: "ws"
  }),
  westNorth: (n, t) => ({
    top: n.top - t.height,
    left: n.left - t.width + ul,
    name: "wn"
  })
}, L9 = [
  {
    menuId: "file",
    label: "File",
    groups: [
      {
        groupId: "export",
        items: [
          "menuBar:exportPdf",
          "menuBar:exportWord"
        ]
      },
      {
        groupId: "import",
        items: [
          "menuBar:importWord"
        ]
      },
      {
        groupId: "revisionHistory",
        items: [
          "menuBar:revisionHistory"
        ]
      }
    ]
  },
  {
    menuId: "edit",
    label: "Edit",
    groups: [
      {
        groupId: "undo",
        items: [
          "menuBar:undo",
          "menuBar:redo"
        ]
      },
      {
        groupId: "selectAll",
        items: [
          "menuBar:selectAll"
        ]
      },
      {
        groupId: "findAndReplace",
        items: [
          "menuBar:findAndReplace"
        ]
      }
    ]
  },
  {
    menuId: "view",
    label: "View",
    groups: [
      {
        groupId: "sourceEditingEnhanced",
        items: [
          "menuBar:sourceEditingEnhanced"
        ]
      },
      {
        groupId: "sourceEditing",
        items: [
          "menuBar:sourceEditing"
        ]
      },
      {
        groupId: "showBlocks",
        items: [
          "menuBar:showBlocks"
        ]
      },
      {
        groupId: "previewMergeFields",
        items: [
          "menuBar:previewMergeFields"
        ]
      },
      {
        groupId: "restrictedEditing",
        items: [
          "menuBar:restrictedEditing"
        ]
      }
    ]
  },
  {
    menuId: "insert",
    label: "Insert",
    groups: [
      {
        groupId: "insertMainWidgets",
        items: [
          "menuBar:insertImage",
          "menuBar:ckbox",
          "menuBar:ckfinder",
          "menuBar:insertTable"
        ]
      },
      {
        groupId: "insertInline",
        items: [
          "menuBar:link",
          "menuBar:bookmark",
          "menuBar:comment",
          "menuBar:insertMergeField",
          "menuBar:emoji"
        ]
      },
      {
        groupId: "insertMinorWidgets",
        items: [
          "menuBar:mediaEmbed",
          "menuBar:insertTemplate",
          "menuBar:specialCharacters",
          "menuBar:blockQuote",
          "menuBar:codeBlock",
          "menuBar:htmlEmbed"
        ]
      },
      {
        groupId: "insertStructureWidgets",
        items: [
          "menuBar:horizontalLine",
          "menuBar:pageBreak",
          "menuBar:tableOfContents"
        ]
      },
      {
        groupId: "restrictedEditingException",
        items: [
          "menuBar:restrictedEditingException"
        ]
      }
    ]
  },
  {
    menuId: "format",
    label: "Format",
    groups: [
      {
        groupId: "textAndFont",
        items: [
          {
            menuId: "text",
            label: "Text",
            groups: [
              {
                groupId: "basicStyles",
                items: [
                  "menuBar:bold",
                  "menuBar:italic",
                  "menuBar:underline",
                  "menuBar:strikethrough",
                  "menuBar:superscript",
                  "menuBar:subscript",
                  "menuBar:code"
                ]
              },
              {
                groupId: "textPartLanguage",
                items: [
                  "menuBar:textPartLanguage"
                ]
              }
            ]
          },
          {
            menuId: "font",
            label: "Font",
            groups: [
              {
                groupId: "fontProperties",
                items: [
                  "menuBar:fontSize",
                  "menuBar:fontFamily"
                ]
              },
              {
                groupId: "fontColors",
                items: [
                  "menuBar:fontColor",
                  "menuBar:fontBackgroundColor"
                ]
              },
              {
                groupId: "highlight",
                items: [
                  "menuBar:highlight"
                ]
              }
            ]
          },
          "menuBar:heading"
        ]
      },
      {
        groupId: "list",
        items: [
          "menuBar:bulletedList",
          "menuBar:numberedList",
          "menuBar:multiLevelList",
          "menuBar:todoList"
        ]
      },
      {
        groupId: "indent",
        items: [
          "menuBar:alignment",
          "menuBar:indent",
          "menuBar:outdent"
        ]
      },
      {
        groupId: "caseChange",
        items: [
          "menuBar:caseChange"
        ]
      },
      {
        groupId: "removeFormat",
        items: [
          "menuBar:removeFormat"
        ]
      }
    ]
  },
  {
    menuId: "tools",
    label: "Tools",
    groups: [
      {
        groupId: "aiTools",
        items: [
          "menuBar:aiAssistant",
          "menuBar:aiCommands"
        ]
      },
      {
        groupId: "tools",
        items: [
          "menuBar:trackChanges",
          "menuBar:commentsArchive"
        ]
      }
    ]
  },
  {
    menuId: "help",
    label: "Help",
    groups: [
      {
        groupId: "help",
        items: [
          "menuBar:accessibilityHelp"
        ]
      }
    ]
  }
];
function B9(n) {
  let t;
  return !("items" in n) || !n.items ? t = {
    items: Di(L9),
    addItems: [],
    removeItems: [],
    isVisible: !0,
    isUsingDefaultConfig: !0,
    ...n
  } : t = {
    items: n.items,
    removeItems: [],
    addItems: [],
    isVisible: !0,
    isUsingDefaultConfig: !1,
    ...n
  }, t;
}
function M9({ normalizedConfig: n, locale: t, componentFactory: e, extraItems: i }) {
  const s = Di(n);
  return Lb(n, s, i), N9(n, s), Lb(n, s, s.addItems), F9(n, s, e), jy(n, s), D9(s, t), s;
}
function N9(n, t) {
  const e = t.removeItems, i = [];
  t.items = t.items.filter(({ menuId: s }) => e.includes(s) ? (i.push(s), !1) : !0), so(t.items, (s) => {
    s.groups = s.groups.filter(({ groupId: r }) => e.includes(r) ? (i.push(r), !1) : !0);
    for (const r of s.groups)
      r.items = r.items.filter((o) => {
        const a = Uy(o);
        return e.includes(a) ? (i.push(a), !1) : !0;
      });
  });
  for (const s of e)
    i.includes(s) || Ve("menu-bar-item-could-not-be-removed", {
      menuBarConfig: n,
      itemName: s
    });
}
function Lb(n, t, e) {
  const i = [];
  if (e.length != 0) {
    for (const s of e) {
      const r = H9(s.position), o = j9(s.position);
      if ($9(s))
        if (!o)
          r === "start" ? (t.items.unshift(s.menu), i.push(s)) : r === "end" && (t.items.push(s.menu), i.push(s));
        else {
          const a = t.items.findIndex((l) => l.menuId === o);
          a != -1 ? r === "before" ? (t.items.splice(a, 0, s.menu), i.push(s)) : r === "after" && (t.items.splice(a + 1, 0, s.menu), i.push(s)) : Bb(t, s.menu, o, r) && i.push(s);
        }
      else z9(s) ? so(t.items, (a) => {
        if (a.menuId === o)
          r === "start" ? (a.groups.unshift(s.group), i.push(s)) : r === "end" && (a.groups.push(s.group), i.push(s));
        else {
          const l = a.groups.findIndex((c) => c.groupId === o);
          l !== -1 && (r === "before" ? (a.groups.splice(l, 0, s.group), i.push(s)) : r === "after" && (a.groups.splice(l + 1, 0, s.group), i.push(s)));
        }
      }) : Bb(t, s.item, o, r) && i.push(s);
    }
    for (const s of e)
      i.includes(s) || Ve("menu-bar-item-could-not-be-added", {
        menuBarConfig: n,
        addedItemConfig: s
      });
  }
}
function Bb(n, t, e, i) {
  let s = !1;
  return so(n.items, (r) => {
    for (const { groupId: o, items: a } of r.groups) {
      if (s)
        return;
      if (o === e)
        i === "start" ? (a.unshift(t), s = !0) : i === "end" && (a.push(t), s = !0);
      else {
        const l = a.findIndex((c) => Uy(c) === e);
        l !== -1 && (i === "before" ? (a.splice(l, 0, t), s = !0) : i === "after" && (a.splice(l + 1, 0, t), s = !0));
      }
    }
  }), s;
}
function F9(n, t, e) {
  so(t.items, (i) => {
    for (const s of i.groups)
      s.items = s.items.filter((r) => {
        const o = typeof r == "string" && !e.has(r);
        return o && !t.isUsingDefaultConfig && Ve("menu-bar-item-unavailable", {
          menuBarConfig: n,
          parentMenuConfig: Di(i),
          componentName: r
        }), !o;
      });
  });
}
function jy(n, t) {
  const e = t.isUsingDefaultConfig;
  let i = !1;
  if (t.items = t.items.filter((s) => s.groups.length ? !0 : (rd(n, s, e), !1)), !t.items.length) {
    rd(n, n, e);
    return;
  }
  so(t.items, (s) => {
    s.groups = s.groups.filter((r) => r.items.length ? !0 : (i = !0, !1));
    for (const r of s.groups)
      r.items = r.items.filter((o) => Wy(o) && !o.groups.length ? (rd(n, o, e), i = !0, !1) : !0);
  }), i && jy(n, t);
}
function rd(n, t, e) {
  e || Ve("menu-bar-menu-empty", {
    menuBarConfig: n,
    emptyMenuConfig: t
  });
}
function D9(n, t) {
  const e = t.t, i = {
    // Top-level categories.
    File: e({
      string: "File",
      id: "MENU_BAR_MENU_FILE"
    }),
    Edit: e({
      string: "Edit",
      id: "MENU_BAR_MENU_EDIT"
    }),
    View: e({
      string: "View",
      id: "MENU_BAR_MENU_VIEW"
    }),
    Insert: e({
      string: "Insert",
      id: "MENU_BAR_MENU_INSERT"
    }),
    Format: e({
      string: "Format",
      id: "MENU_BAR_MENU_FORMAT"
    }),
    Tools: e({
      string: "Tools",
      id: "MENU_BAR_MENU_TOOLS"
    }),
    Help: e({
      string: "Help",
      id: "MENU_BAR_MENU_HELP"
    }),
    // Sub-menus.
    Text: e({
      string: "Text",
      id: "MENU_BAR_MENU_TEXT"
    }),
    Font: e({
      string: "Font",
      id: "MENU_BAR_MENU_FONT"
    })
  };
  so(n.items, (s) => {
    s.label in i && (s.label = i[s.label]);
  });
}
function so(n, t) {
  if (Array.isArray(n))
    for (const i of n)
      e(i);
  function e(i) {
    t(i);
    for (const s of i.groups)
      for (const r of s.items)
        Wy(r) && e(r);
  }
}
function $9(n) {
  return typeof n == "object" && "menu" in n;
}
function z9(n) {
  return typeof n == "object" && "group" in n;
}
function H9(n) {
  return n.startsWith("start") ? "start" : n.startsWith("end") ? "end" : n.startsWith("after") ? "after" : "before";
}
function j9(n) {
  const t = n.match(/^[^:]+:(.+)/);
  return t ? t[1] : null;
}
function Uy(n) {
  return typeof n == "string" ? n : n.menuId;
}
function Wy(n) {
  return typeof n == "object" && "menuId" in n;
}
class U9 extends (/* @__PURE__ */ je()) {
  /**
  * Creates an instance of the editor UI class.
  *
  * @param editor The editor instance.
  */
  constructor(e) {
    super();
    /**
    * The editor that the UI belongs to.
    */
    f(this, "editor");
    /**
    * An instance of the {@link module:ui/componentfactory~ComponentFactory}, a registry used by plugins
    * to register factories of specific UI components.
    */
    f(this, "componentFactory");
    /**
    * Stores the information about the editor UI focus and propagates it so various plugins and components
    * are unified as a focus group.
    */
    f(this, "focusTracker");
    /**
    * Manages the tooltips displayed on mouseover and focus across the UI.
    */
    f(this, "tooltipManager");
    /**
    * A helper that enables the "powered by" feature in the editor and renders a link to the project's webpage.
    */
    f(this, "poweredBy");
    /**
    * A helper that enables the "evaluation badge" feature in the editor.
    */
    f(this, "evaluationBadge");
    /**
    * A helper that manages the content of an `aria-live` regions used by editor features to announce status changes
    * to screen readers.
    */
    f(this, "ariaLiveAnnouncer");
    /**
    * Indicates the UI is ready. Set `true` after {@link #event:ready} event is fired.
    *
    * @readonly
    * @default false
    */
    f(this, "isReady", !1);
    /**
    * Stores all editable elements used by the editor instance.
    */
    f(this, "_editableElementsMap", /* @__PURE__ */ new Map());
    /**
    * All available & focusable toolbars.
    */
    f(this, "_focusableToolbarDefinitions", []);
    /**
    * All additional menu bar items, groups or menus that have their default location defined.
    */
    f(this, "_extraMenuBarElements", []);
    /**
    * The last focused element to which focus should return on `Esc` press.
    */
    f(this, "_lastFocusedForeignElement", null);
    const i = e.editing.view;
    this.editor = e, this.componentFactory = new w9(e), this.focusTracker = new ze(), this.tooltipManager = new _h(e), this.poweredBy = new E9(e), this.evaluationBadge = new A9(e), this.ariaLiveAnnouncer = new P9(e), this.set("viewportOffset", this._readViewportOffsetFromConfig()), this.once("ready", () => {
      this._bindBodyCollectionWithFocusTracker(), this.isReady = !0;
    }), this.listenTo(i.document, "layoutChanged", this.update.bind(this)), this.listenTo(i, "scrollToTheSelection", this._handleScrollToTheSelection.bind(this)), this._initFocusTracking();
  }
  /**
  * The main (outermost) DOM element of the editor UI.
  *
  * For example, in {@link module:editor-classic/classiceditor~ClassicEditor} it is a `<div>` which
  * wraps the editable element and the toolbar. In {@link module:editor-inline/inlineeditor~InlineEditor}
  * it is the editable element itself (as there is no other wrapper). However, in
  * {@link module:editor-decoupled/decouplededitor~DecoupledEditor} it is set to `null` because this editor does not
  * come with a single "main" HTML element (its editable element and toolbar are separate).
  *
  * This property can be understood as a shorthand for retrieving the element that a specific editor integration
  * considers to be its main DOM element.
  */
  get element() {
    return null;
  }
  /**
  * Fires the {@link module:ui/editorui/editorui~EditorUI#event:update `update`} event.
  *
  * This method should be called when the editor UI (e.g. positions of its balloons) needs to be updated due to
  * some environmental change which CKEditor 5 is not aware of (e.g. resize of a container in which it is used).
  */
  update() {
    this.fire("update");
  }
  /**
  * Destroys the UI.
  */
  destroy() {
    this.stopListening(), this.focusTracker.destroy(), this.tooltipManager.destroy(this.editor), this.poweredBy.destroy(), this.evaluationBadge.destroy();
    for (const e of this._editableElementsMap.values())
      e.ckeditorInstance = null, this.editor.keystrokes.stopListening(e);
    this._editableElementsMap = /* @__PURE__ */ new Map(), this._focusableToolbarDefinitions = [];
  }
  /**
  * Stores the native DOM editable element used by the editor under a unique name.
  *
  * Also, registers the element in the editor to maintain the accessibility of the UI. When the user is editing text in a focusable
  * editable area, they can use the <kbd>Alt</kbd> + <kbd>F10</kbd> keystroke to navigate over editor toolbars. See {@link #addToolbar}.
  *
  * @param rootName The unique name of the editable element.
  * @param domElement The native DOM editable element.
  */
  setEditableElement(e, i) {
    this._editableElementsMap.set(e, i), i.ckeditorInstance || (i.ckeditorInstance = this.editor), this.focusTracker.add(i);
    const s = () => {
      this.editor.editing.view.getDomRoot(e) || this.editor.keystrokes.listenTo(i);
    };
    this.isReady ? s() : this.once("ready", s);
  }
  /**
  * Removes the editable from the editor UI. Removes all handlers added by {@link #setEditableElement}.
  *
  * @param rootName The name of the editable element to remove.
  */
  removeEditableElement(e) {
    const i = this._editableElementsMap.get(e);
    i && (this._editableElementsMap.delete(e), this.editor.keystrokes.stopListening(i), this.focusTracker.remove(i), i.ckeditorInstance = null);
  }
  /**
  * Returns the editable editor element with the given name or null if editable does not exist.
  *
  * @param rootName The editable name.
  */
  getEditableElement(e = "main") {
    return this._editableElementsMap.get(e);
  }
  /**
  * Returns array of names of all editor editable elements.
  */
  getEditableElementsNames() {
    return this._editableElementsMap.keys();
  }
  /**
  * Adds a toolbar to the editor UI. Used primarily to maintain the accessibility of the UI.
  *
  * Focusable toolbars can be accessed (focused) by users by pressing the <kbd>Alt</kbd> + <kbd>F10</kbd> keystroke.
  * Successive keystroke presses navigate over available toolbars.
  *
  * @param toolbarView A instance of the toolbar to be registered.
  */
  addToolbar(e, i = {}) {
    e.isRendered ? (this.focusTracker.add(e), this.editor.keystrokes.listenTo(e.element)) : e.once("render", () => {
      this.focusTracker.add(e), this.editor.keystrokes.listenTo(e.element);
    }), this._focusableToolbarDefinitions.push({
      toolbarView: e,
      options: i
    });
  }
  /**
  * Registers an extra menu bar element, which could be a single item, a group of items, or a menu containing groups.
  *
  * ```ts
  * // Register a new menu bar item.
  * editor.ui.extendMenuBar( {
  *   item: 'menuBar:customFunctionButton',
  *   position: 'after:menuBar:bold'
  * } );
  *
  * // Register a new menu bar group.
  * editor.ui.extendMenuBar( {
  *   group: {
  *     groupId: 'customGroup',
  *     items: [
  *       'menuBar:customFunctionButton'
  *     ]
  *   },
  *   position: 'start:help'
  * } );
  *
  * // Register a new menu bar menu.
  * editor.ui.extendMenuBar( {
  *   menu: {
  *     menuId: 'customMenu',
  *     label: 'customMenu',
  *     groups: [
  *       {
  *         groupId: 'customGroup',
  *         items: [
  *           'menuBar:customFunctionButton'
  *         ]
  *       }
  *     ]
  *   },
  *   position: 'after:help'
  * } );
  * ```
  */
  extendMenuBar(e) {
    this._extraMenuBarElements.push(e);
  }
  /**
  * Stores all editable elements used by the editor instance.
  *
  * @deprecated
  */
  get _editableElements() {
    return console.warn("editor-ui-deprecated-editable-elements: The EditorUI#_editableElements property has been deprecated and will be removed in the near future.", {
      editorUI: this
    }), this._editableElementsMap;
  }
  /**
  * Initializes menu bar.
  */
  _initMenuBar(e) {
    const i = e.element;
    this.focusTracker.add(i), this.editor.keystrokes.listenTo(i);
    const s = B9(this.editor.config.get("menuBar") || {});
    e.fillFromConfig(s, this.componentFactory, this._extraMenuBarElements), this.editor.keystrokes.set("Esc", (r, o) => {
      i.contains(this.editor.ui.focusTracker.focusedElement) && (this._lastFocusedForeignElement ? (this._lastFocusedForeignElement.focus(), this._lastFocusedForeignElement = null) : this.editor.editing.view.focus(), o());
    }), this.editor.keystrokes.set("Alt+F9", (r, o) => {
      i.contains(this.editor.ui.focusTracker.focusedElement) || (this._saveLastFocusedForeignElement(), e.isFocusBorderEnabled = !0, e.focus(), o());
    });
  }
  /**
  * Returns viewport offsets object:
  *
  * ```js
  * {
  * 	top: Number,
  * 	right: Number,
  * 	bottom: Number,
  * 	left: Number
  * }
  * ```
  *
  * Only top property is currently supported.
  */
  _readViewportOffsetFromConfig() {
    const e = this.editor, i = e.config.get("ui.viewportOffset");
    if (i)
      return i;
    const s = e.config.get("toolbar.viewportTopOffset");
    return s ? (console.warn("editor-ui-deprecated-viewport-offset-config: The `toolbar.vieportTopOffset` configuration option is deprecated. It will be removed from future CKEditor versions. Use `ui.viewportOffset.top` instead."), {
      top: s
    }) : {
      top: 0
    };
  }
  /**
  * Starts listening for <kbd>Alt</kbd> + <kbd>F10</kbd> and <kbd>Esc</kbd> keystrokes in the context of focusable
  * {@link #setEditableElement editable elements} and {@link #addToolbar toolbars}
  * to allow users navigate across the UI.
  */
  _initFocusTracking() {
    const e = this.editor;
    let i;
    e.keystrokes.set("Alt+F10", (s, r) => {
      this._saveLastFocusedForeignElement();
      const o = this._getCurrentFocusedToolbarDefinition();
      (!o || !i) && (i = this._getFocusableCandidateToolbarDefinitions());
      for (let a = 0; a < i.length; a++) {
        const l = i.shift();
        if (i.push(l), l !== o && this._focusFocusableCandidateToolbar(l)) {
          o && o.options.afterBlur && o.options.afterBlur();
          break;
        }
      }
      r();
    }), e.keystrokes.set("Esc", (s, r) => {
      const o = this._getCurrentFocusedToolbarDefinition();
      o && (this._lastFocusedForeignElement ? (this._lastFocusedForeignElement.focus(), this._lastFocusedForeignElement = null) : e.editing.view.focus(), o.options.afterBlur && o.options.afterBlur(), r());
    });
  }
  /**
  * Saves last focused element that doen not belong to editing view to restore focus on `Esc`.
  */
  _saveLastFocusedForeignElement() {
    const e = this.focusTracker.focusedElement;
    Array.from(this._editableElementsMap.values()).includes(e) && !Array.from(this.editor.editing.view.domRoots.values()).includes(e) && (this._lastFocusedForeignElement = e);
  }
  /**
  * Returns definitions of toolbars that could potentially be focused, sorted by their importance for the user.
  *
  * Focusable toolbars candidates are either:
  * * already visible,
  * * have `beforeFocus()` set in their {@link module:ui/editorui/editorui~FocusableToolbarDefinition definition} that suggests that
  * they might show up when called. Keep in mind that determining whether a toolbar will show up (and become focusable) is impossible
  * at this stage because it depends on its implementation, that in turn depends on the editing context (selection).
  *
  * **Note**: Contextual toolbars take precedence over regular toolbars.
  */
  _getFocusableCandidateToolbarDefinitions() {
    const e = [];
    for (const i of this._focusableToolbarDefinitions) {
      const { toolbarView: s, options: r } = i;
      (yn(s.element) || r.beforeFocus) && e.push(i);
    }
    return e.sort((i, s) => Mb(i) - Mb(s)), e;
  }
  /**
  * Returns a definition of the toolbar that is currently visible and focused (one of its children has focus).
  *
  * `null` is returned when no toolbar is currently focused.
  */
  _getCurrentFocusedToolbarDefinition() {
    for (const e of this._focusableToolbarDefinitions)
      if (e.toolbarView.element && e.toolbarView.element.contains(this.focusTracker.focusedElement))
        return e;
    return null;
  }
  /**
  * Focuses a focusable toolbar candidate using its definition.
  *
  * @param candidateToolbarDefinition A definition of the toolbar to focus.
  * @returns `true` when the toolbar candidate was focused. `false` otherwise.
  */
  _focusFocusableCandidateToolbar(e) {
    const { toolbarView: i, options: { beforeFocus: s } } = e;
    return s && s(), yn(i.element) ? (i.focus(), !0) : !1;
  }
  /**
  * Provides an integration between {@link #viewportOffset} and {@link module:utils/dom/scroll~scrollViewportToShowTarget}.
  * It allows the UI-agnostic engine method to consider user-configured viewport offsets specific for the integration.
  *
  * @param evt The `scrollToTheSelection` event info.
  * @param data The payload carried by the `scrollToTheSelection` event.
  */
  _handleScrollToTheSelection(e, i) {
    const s = {
      top: 0,
      bottom: 0,
      left: 0,
      right: 0,
      ...this.viewportOffset
    };
    i.viewportOffset.top += s.top, i.viewportOffset.bottom += s.bottom, i.viewportOffset.left += s.left, i.viewportOffset.right += s.right;
  }
  /**
  * Ensures that the focus tracker is aware of all views' DOM elements in the body collection.
  */
  _bindBodyCollectionWithFocusTracker() {
    const e = this.view.body;
    for (const i of e)
      this.focusTracker.add(i.element);
    e.on("add", (i, s) => {
      this.focusTracker.add(s.element);
    }), e.on("remove", (i, s) => {
      this.focusTracker.remove(s.element);
    });
  }
}
function Mb(n) {
  const { toolbarView: t, options: e } = n;
  let i = 10;
  return yn(t.element) && i--, e.isContextual && i--, i;
}
class W9 extends ee {
  /**
  * Creates an instance of the editor UI view class.
  *
  * @param locale The locale instance.
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views, detached from the DOM
    * structure of the editor, like panels, icons etc.
    */
    f(this, "body");
    /**
    * Menu bar view instance. May not be initialized in some editor types.
    */
    f(this, "menuBarView");
    this.body = new vh(e);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.body.attachToDom();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this.body.detachFromDom(), super.destroy();
  }
}
class q9 extends W9 {
  /**
  * Creates an instance of the boxed editor UI view class.
  *
  * @param locale The locale instance..
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views located in the top (`.ck-editor__top`)
    * area of the UI.
    */
    f(this, "top");
    /**
    * Collection of the child views located in the main (`.ck-editor__main`)
    * area of the UI.
    */
    f(this, "main");
    /**
    * Voice label of the UI.
    */
    f(this, "_voiceLabelView");
    this.top = this.createCollection(), this.main = this.createCollection(), this._voiceLabelView = this._createVoiceLabel(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-reset",
          "ck-editor",
          "ck-rounded-corners"
        ],
        role: "application",
        dir: e.uiLanguageDirection,
        lang: e.uiLanguage,
        "aria-labelledby": this._voiceLabelView.id
      },
      children: [
        this._voiceLabelView,
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-editor__top",
              "ck-reset_all"
            ],
            role: "presentation"
          },
          children: this.top
        },
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-editor__main"
            ],
            role: "presentation"
          },
          children: this.main
        }
      ]
    });
  }
  /**
  * Creates a voice label view instance.
  */
  _createVoiceLabel() {
    const e = this.t, i = new gi();
    return i.text = e("Rich Text Editor"), i.extendTemplate({
      attributes: {
        class: "ck-voice-label"
      }
    }), i;
  }
}
class G9 extends ee {
  /**
  * Creates an instance of EditableUIView class.
  *
  * @param locale The locale instance.
  * @param editingView The editing view instance the editable is related to.
  * @param editableElement The editable element. If not specified, this view
  * should create it. Otherwise, the existing element should be used.
  */
  constructor(e, i, s) {
    super(e);
    /**
    * The name of the editable UI view.
    */
    f(this, "name", null);
    /**
    * The editing view instance the editable is related to. Editable uses the editing
    * view to dynamically modify its certain DOM attributes after {@link #render rendering}.
    *
    * **Note**: The DOM attributes are performed by the editing view and not UI
    * {@link module:ui/view~View#bindTemplate template bindings} because once rendered,
    * the editable DOM element must remain under the full control of the engine to work properly.
    */
    f(this, "_editingView");
    /**
    * The element which is the main editable element (usually the one with `contentEditable="true"`).
    */
    f(this, "_editableElement");
    /**
    * Whether an external {@link #_editableElement} was passed into the constructor, which also means
    * the view will not render its {@link #template}.
    */
    f(this, "_hasExternalElement");
    this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-content",
          "ck-editor__editable",
          "ck-rounded-corners"
        ],
        lang: e.contentLanguage,
        dir: e.contentLanguageDirection
      }
    }), this.set("isFocused", !1), this._editableElement = s, this._hasExternalElement = !!this._editableElement, this._editingView = i;
  }
  /**
  * Renders the view by either applying the {@link #template} to the existing
  * {@link module:ui/editableui/editableuiview~EditableUIView#_editableElement} or assigning {@link #element}
  * as {@link module:ui/editableui/editableuiview~EditableUIView#_editableElement}.
  */
  render() {
    super.render(), this._hasExternalElement ? this.template.apply(this.element = this._editableElement) : this._editableElement = this.element, this.on("change:isFocused", () => this._updateIsFocusedClasses()), this._updateIsFocusedClasses();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._hasExternalElement && this.template.revert(this._editableElement), super.destroy();
  }
  /**
  * Whether an external {@link #_editableElement} was passed into the constructor, which also means
  * the view will not render its {@link #template}.
  */
  get hasExternalElement() {
    return this._hasExternalElement;
  }
  /**
  * Updates the `ck-focused` and `ck-blurred` CSS classes on the {@link #element} according to
  * the {@link #isFocused} property value using the {@link #_editingView editing view} API.
  */
  _updateIsFocusedClasses() {
    const e = this._editingView;
    e.isRenderingInProgress ? s(this) : i(this);
    function i(r) {
      e.change((o) => {
        const a = e.document.getRoot(r.name);
        o.addClass(r.isFocused ? "ck-focused" : "ck-blurred", a), o.removeClass(r.isFocused ? "ck-blurred" : "ck-focused", a);
      });
    }
    function s(r) {
      e.once("change:isRenderingInProgress", (o, a, l) => {
        l ? s(r) : i(r);
      });
    }
  }
}
class K9 extends G9 {
  /**
  * Creates an instance of the InlineEditableUIView class.
  *
  * @param locale The locale instance.
  * @param editingView The editing view instance the editable is related to.
  * @param editableElement The editable element. If not specified, the
  * {@link module:ui/editableui/editableuiview~EditableUIView}
  * will create it. Otherwise, the existing element will be used.
  * @param options Additional configuration of the view.
  * @param options.label The label of the editable for assistive technologies. If not provided, a default label is used or,
  * the existing `aria-label` attribute value from the specified `editableElement` is preserved.
  */
  constructor(e, i, s, r = {}) {
    super(e, i, s);
    /**
    * The cached options object passed to the constructor.
    */
    f(this, "_options");
    this._options = r, this.extendTemplate({
      attributes: {
        role: "textbox",
        class: "ck-editor__editable_inline"
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    const e = this._editingView;
    e.change((i) => {
      const s = e.document.getRoot(this.name);
      i.setAttribute("aria-label", this.getEditableAriaLabel(), s);
    });
  }
  /**
  * Returns a normalized label for the editable view based on the environment.
  */
  getEditableAriaLabel() {
    const e = this.locale.t, i = this._options.label, s = this._editableElement, r = this.name;
    if (typeof i == "string")
      return i;
    if (typeof i == "object")
      return i[r];
    if (typeof i == "function")
      return i(this);
    if (s) {
      const o = s.getAttribute("aria-label");
      if (o)
        return o;
    }
    return e("Rich Text Editor. Editing area: %0", r);
  }
}
class Nb extends py {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Notification";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.on("show:warning", (t, e) => {
      window.alert(e.message);
    }, {
      priority: "lowest"
    });
  }
  /**
  * Shows a success notification.
  *
  * By default, it fires the {@link #event:show:success `show:success` event} with the given `data`. The event namespace can be extended
  * using the `data.namespace` option. For example:
  *
  * ```ts
  * showSuccess( 'Image is uploaded.', {
  * 	namespace: 'upload:image'
  * } );
  * ```
  *
  * will fire the `show:success:upload:image` event.
  *
  * You can provide the title of the notification:
  *
  * ```ts
  * showSuccess( 'Image is uploaded.', {
  * 	title: 'Image upload success'
  * } );
  * ```
  *
  * @param message The content of the notification.
  * @param data Additional data.
  * @param data.namespace Additional event namespace.
  * @param data.title The title of the notification.
  */
  showSuccess(t, e = {}) {
    this._showNotification({
      message: t,
      type: "success",
      namespace: e.namespace,
      title: e.title
    });
  }
  /**
  * Shows an information notification.
  *
  * By default, it fires the {@link #event:show:info `show:info` event} with the given `data`. The event namespace can be extended
  * using the `data.namespace` option. For example:
  *
  * ```ts
  * showInfo( 'Editor is offline.', {
  * 	namespace: 'editor:status'
  * } );
  * ```
  *
  * will fire the `show:info:editor:status` event.
  *
  * You can provide the title of the notification:
  *
  * ```ts
  * showInfo( 'Editor is offline.', {
  * 	title: 'Network information'
  * } );
  * ```
  *
  * @param message The content of the notification.
  * @param data Additional data.
  * @param data.namespace Additional event namespace.
  * @param data.title The title of the notification.
  */
  showInfo(t, e = {}) {
    this._showNotification({
      message: t,
      type: "info",
      namespace: e.namespace,
      title: e.title
    });
  }
  /**
  * Shows a warning notification.
  *
  * By default, it fires the {@link #event:show:warning `show:warning` event}
  * with the given `data`. The event namespace can be extended using the `data.namespace` option. For example:
  *
  * ```ts
  * showWarning( 'Image upload error.', {
  * 	namespace: 'upload:image'
  * } );
  * ```
  *
  * will fire the `show:warning:upload:image` event.
  *
  * You can provide the title of the notification:
  *
  * ```ts
  * showWarning( 'Image upload error.', {
  * 	title: 'Upload failed'
  * } );
  * ```
  *
  * Note that each unhandled and not stopped `warning` notification will be displayed as a system alert.
  * The plugin responsible for displaying warnings should `stop()` the event to prevent displaying it as an alert:
  *
  * ```ts
  * notifications.on( 'show:warning', ( evt, data ) => {
  * 	// Do something with the data.
  *
  * 	// Stop this event to prevent displaying it as an alert.
  * 	evt.stop();
  * } );
  * ```
  *
  * You can attach many listeners to the same event and `stop()` this event in a listener with a low priority:
  *
  * ```ts
  * notifications.on( 'show:warning', ( evt, data ) => {
  * 	// Show the warning in the UI, but do not stop it.
  * } );
  *
  * notifications.on( 'show:warning', ( evt, data ) => {
  * 	// Log the warning to some error tracker.
  *
  * 	// Stop this event to prevent displaying it as an alert.
  * 	evt.stop();
  * }, { priority: 'low' } );
  * ```
  *
  * @param message The content of the notification.
  * @param data Additional data.
  * @param data.namespace Additional event namespace.
  * @param data.title The title of the notification.
  */
  showWarning(t, e = {}) {
    this._showNotification({
      message: t,
      type: "warning",
      namespace: e.namespace,
      title: e.title
    });
  }
  /**
  * Fires the `show` event with the specified type, namespace and message.
  *
  * @param data The message data.
  * @param data.message The content of the notification.
  * @param data.type The type of the message.
  * @param data.namespace Additional event namespace.
  * @param data.title The title of the notification.
  */
  _showNotification(t) {
    const e = t.namespace ? `show:${t.type}:${t.namespace}` : `show:${t.type}`;
    this.fire(e, {
      message: t.message,
      type: t.type,
      title: t.title || ""
    });
  }
}
class tn extends (/* @__PURE__ */ je()) {
  /**
  * Creates a new Model instance.
  *
  * @param attributes The model state attributes to be defined during the instance creation.
  * @param properties The (out of state) properties to be appended to the instance during creation.
  */
  constructor(t, e) {
    super(), e && tv(this, e), t && this.set(t);
  }
}
const dl = /* @__PURE__ */ to("px");
class ai extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The {@link module:utils/dom/position~Options#limiter position limiter}
    * for the {@link #view balloon}, used when no `limiter` has been passed into {@link #add}
    * or {@link #updatePosition}.
    *
    * By default, a function that obtains the farthest DOM
    * {@link module:engine/view/rooteditableelement~RootEditableElement}
    * of the {@link module:engine/view/document~Document#selection}.
    */
    f(this, "positionLimiter");
    f(this, "visibleStack");
    /**
    * The map of views and their stacks.
    */
    f(this, "_viewToStack", /* @__PURE__ */ new Map());
    /**
    * The map of IDs and stacks.
    */
    f(this, "_idToStack", /* @__PURE__ */ new Map());
    /**
    * The common balloon panel view.
    */
    f(this, "_view", null);
    /**
    * Rotator view embedded in the contextual balloon.
    * Displays the currently visible view in the balloon and provides navigation for switching stacks.
    */
    f(this, "_rotatorView", null);
    /**
    * Displays fake panels under the balloon panel view when multiple stacks are added to the balloon.
    */
    f(this, "_fakePanelsView", null);
    this.positionLimiter = () => {
      const i = this.editor.editing.view, r = i.document.selection.editableElement;
      return r ? i.domConverter.mapViewToDom(r.root) : null;
    }, this.decorate("getPositionOptions"), this.set("visibleView", null), this.set("_numberOfStacks", 0), this.set("_singleViewMode", !1);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ContextualBalloon";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._view && this._view.destroy(), this._rotatorView && this._rotatorView.destroy(), this._fakePanelsView && this._fakePanelsView.destroy();
  }
  /**
  * The common balloon panel view.
  */
  get view() {
    return this._view || this._createPanelView(), this._view;
  }
  /**
  * Returns `true` when the given view is in one of the stacks. Otherwise returns `false`.
  */
  hasView(e) {
    return Array.from(this._viewToStack.keys()).includes(e);
  }
  /**
  * Adds a new view to the stack and makes it visible if the current stack is visible
  * or it is the first view in the balloon.
  *
  * @param data The configuration of the view.
  * @param data.stackId The ID of the stack that the view is added to. Defaults to `'main'`.
  * @param data.view The content of the balloon.
  * @param data.position Positioning options.
  * @param data.balloonClassName An additional CSS class added to the {@link #view balloon} when visible.
  * @param data.withArrow Whether the {@link #view balloon} should be rendered with an arrow. Defaults to `true`.
  * @param data.singleViewMode Whether the view should be the only visible view even if other stacks were added. Defaults to `false`.
  */
  add(e) {
    if (this._view || this._createPanelView(), this.hasView(e.view))
      throw new S("contextualballoon-add-view-exist", [
        this,
        e
      ]);
    const i = e.stackId || "main";
    if (!this._idToStack.has(i)) {
      this._idToStack.set(i, /* @__PURE__ */ new Map([
        [
          e.view,
          e
        ]
      ])), this._viewToStack.set(e.view, this._idToStack.get(i)), this._numberOfStacks = this._idToStack.size, (!this._visibleStack || e.singleViewMode) && this.showStack(i);
      return;
    }
    const s = this._idToStack.get(i);
    e.singleViewMode && this.showStack(i), s.set(e.view, e), this._viewToStack.set(e.view, s), s === this._visibleStack && this._showView(e);
  }
  /**
  * Removes the given view from the stack. If the removed view was visible,
  * the view preceding it in the stack will become visible instead.
  * When there is no view in the stack, the next stack will be displayed.
  * When there are no more stacks, the balloon will hide.
  *
  * @param view A view to be removed from the balloon.
  */
  remove(e) {
    if (!this.hasView(e))
      throw new S("contextualballoon-remove-view-not-exist", [
        this,
        e
      ]);
    const i = this._viewToStack.get(e);
    this._singleViewMode && this.visibleView === e && (this._singleViewMode = !1), this.visibleView === e && (i.size === 1 ? this._idToStack.size > 1 ? this._showNextStack() : (this.view.hide(), this.visibleView = null, this._rotatorView.hideView()) : this._showView(Array.from(i.values())[i.size - 2])), i.size === 1 ? (this._idToStack.delete(this._getStackId(i)), this._numberOfStacks = this._idToStack.size) : i.delete(e), this._viewToStack.delete(e);
  }
  /**
  * Updates the position of the balloon using the position data of the first visible view in the stack.
  * When new position data is given, the position data of the currently visible view will be updated.
  *
  * @param position Position options.
  */
  updatePosition(e) {
    e && (this._visibleStack.get(this.visibleView).position = e), this.view.pin(this.getPositionOptions()), this._fakePanelsView.updatePosition();
  }
  /**
  * Returns position options of the last view in the stack.
  * This keeps the balloon in the same position when the view is changed.
  */
  getPositionOptions() {
    let e = Array.from(this._visibleStack.values()).pop().position;
    return e && (e.limiter || (e = Object.assign({}, e, {
      limiter: this.positionLimiter
    })), e = Object.assign({}, e, {
      viewportOffsetConfig: this.editor.ui.viewportOffset
    })), e;
  }
  /**
  * Shows the last view from the stack of a given ID.
  */
  showStack(e) {
    this.visibleStack = e;
    const i = this._idToStack.get(e);
    if (!i)
      throw new S("contextualballoon-showstack-stack-not-exist", this);
    this._visibleStack !== i && this._showView(Array.from(i.values()).pop());
  }
  /**
  * Initializes view instances.
  */
  _createPanelView() {
    this._view = new Dt(this.editor.locale), this.editor.ui.view.body.add(this._view), this._rotatorView = this._createRotatorView(), this._fakePanelsView = this._createFakePanelsView();
  }
  /**
  * Returns the stack of the currently visible view.
  */
  get _visibleStack() {
    return this._viewToStack.get(this.visibleView);
  }
  /**
  * Returns the ID of the given stack.
  */
  _getStackId(e) {
    return Array.from(this._idToStack.entries()).find((s) => s[1] === e)[0];
  }
  /**
  * Shows the last view from the next stack.
  */
  _showNextStack() {
    const e = Array.from(this._idToStack.values());
    let i = e.indexOf(this._visibleStack) + 1;
    e[i] || (i = 0), this.showStack(this._getStackId(e[i]));
  }
  /**
  * Shows the last view from the previous stack.
  */
  _showPrevStack() {
    const e = Array.from(this._idToStack.values());
    let i = e.indexOf(this._visibleStack) - 1;
    e[i] || (i = e.length - 1), this.showStack(this._getStackId(e[i]));
  }
  /**
  * Creates a rotator view.
  */
  _createRotatorView() {
    const e = new Z9(this.editor.locale), i = this.editor.locale.t;
    return this.view.content.add(e), e.bind("isNavigationVisible").to(this, "_numberOfStacks", this, "_singleViewMode", (s, r) => !r && s > 1), e.on("change:isNavigationVisible", () => this.updatePosition(), {
      priority: "low"
    }), e.bind("counter").to(this, "visibleView", this, "_numberOfStacks", (s, r) => {
      if (r < 2)
        return "";
      const o = Array.from(this._idToStack.values()).indexOf(this._visibleStack) + 1;
      return i("%0 of %1", [
        o,
        r
      ]);
    }), e.buttonNextView.on("execute", () => {
      e.focusTracker.isFocused && this.editor.editing.view.focus(), this._showNextStack();
    }), e.buttonPrevView.on("execute", () => {
      e.focusTracker.isFocused && this.editor.editing.view.focus(), this._showPrevStack();
    }), e;
  }
  /**
  * Creates a fake panels view.
  */
  _createFakePanelsView() {
    const e = new Y9(this.editor.locale, this.view);
    return e.bind("numberOfPanels").to(this, "_numberOfStacks", this, "_singleViewMode", (i, s) => !s && i >= 2 ? Math.min(i - 1, 2) : 0), e.listenTo(this.view, "change:top", () => e.updatePosition()), e.listenTo(this.view, "change:left", () => e.updatePosition()), this.editor.ui.view.body.add(e), e;
  }
  /**
  * Sets the view as the content of the balloon and attaches the balloon using position
  * options of the first view.
  *
  * @param data Configuration.
  * @param data.view The view to show in the balloon.
  * @param data.balloonClassName Additional class name which will be added to the {@link #view balloon}.
  * @param data.withArrow Whether the {@link #view balloon} should be rendered with an arrow.
  */
  _showView({ view: e, balloonClassName: i = "", withArrow: s = !0, singleViewMode: r = !1 }) {
    this.view.class = i, this.view.withArrow = s, this._rotatorView.showView(e), this.visibleView = e, this.view.pin(this.getPositionOptions()), this._fakePanelsView.updatePosition(), r && (this._singleViewMode = !0);
  }
}
class Z9 extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Used for checking if a view is focused or not.
    */
    f(this, "focusTracker");
    /**
    * Navigation button for switching the stack to the previous one.
    */
    f(this, "buttonPrevView");
    /**
    * Navigation button for switching the stack to the next one.
    */
    f(this, "buttonNextView");
    /**
    * A collection of the child views that creates the rotator content.
    */
    f(this, "content");
    const i = e.t, s = this.bindTemplate;
    this.set("isNavigationVisible", !0), this.focusTracker = new ze(), this.buttonPrevView = this._createButtonView(i("Previous"), Z.previousArrow), this.buttonNextView = this._createButtonView(i("Next"), Z.nextArrow), this.content = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-balloon-rotator"
        ],
        "z-index": "-1"
      },
      children: [
        {
          tag: "div",
          attributes: {
            class: [
              "ck-balloon-rotator__navigation",
              s.to("isNavigationVisible", (r) => r ? "" : "ck-hidden")
            ]
          },
          children: [
            this.buttonPrevView,
            {
              tag: "span",
              attributes: {
                class: [
                  "ck-balloon-rotator__counter"
                ]
              },
              children: [
                {
                  text: s.to("counter")
                }
              ]
            },
            this.buttonNextView
          ]
        },
        {
          tag: "div",
          attributes: {
            class: "ck-balloon-rotator__content"
          },
          children: this.content
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy();
  }
  /**
  * Shows a given view.
  *
  * @param view The view to show.
  */
  showView(e) {
    this.hideView(), this.content.add(e);
  }
  /**
  * Hides the currently displayed view.
  */
  hideView() {
    this.content.clear();
  }
  /**
  * Creates a navigation button view.
  *
  * @param label The button label.
  * @param icon The button icon.
  */
  _createButtonView(e, i) {
    const s = new le(this.locale);
    return s.set({
      label: e,
      icon: i,
      tooltip: !0
    }), s;
  }
}
class Y9 extends ee {
  /**
  * @inheritDoc
  */
  constructor(e, i) {
    super(e);
    /**
    * Collection of the child views which creates fake panel content.
    */
    f(this, "content");
    /**
    * Context.
    */
    f(this, "_balloonPanelView");
    const s = this.bindTemplate;
    this.set("top", 0), this.set("left", 0), this.set("height", 0), this.set("width", 0), this.set("numberOfPanels", 0), this.content = this.createCollection(), this._balloonPanelView = i, this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck-fake-panel",
          s.to("numberOfPanels", (r) => r ? "" : "ck-hidden")
        ],
        style: {
          top: s.to("top", dl),
          left: s.to("left", dl),
          width: s.to("width", dl),
          height: s.to("height", dl)
        }
      },
      children: this.content
    }), this.on("change:numberOfPanels", (r, o, a, l) => {
      a > l ? this._addPanels(a - l) : this._removePanels(l - a), this.updatePosition();
    });
  }
  _addPanels(e) {
    for (; e--; ) {
      const i = new ee();
      i.setTemplate({
        tag: "div"
      }), this.content.add(i), this.registerChild(i);
    }
  }
  _removePanels(e) {
    for (; e--; ) {
      const i = this.content.last;
      this.content.remove(i), this.deregisterChild(i), i.destroy();
    }
  }
  /**
  * Updates coordinates of fake panels.
  */
  updatePosition() {
    if (this.numberOfPanels) {
      const { top: e, left: i } = this._balloonPanelView, { width: s, height: r } = new ke(this._balloonPanelView.element);
      Object.assign(this, {
        top: e,
        left: i,
        width: s,
        height: r
      });
    }
  }
}
const hr = /* @__PURE__ */ to("px");
class J9 extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views which creates balloon panel contents.
    */
    f(this, "content");
    /**
    * The panel which accepts children into {@link #content} collection.
    * Also an element which is positioned when {@link #isSticky}.
    */
    f(this, "contentPanelElement");
    /**
    * A dummy element which visually fills the space as long as the
    * actual panel is sticky. It prevents flickering of the UI.
    */
    f(this, "_contentPanelPlaceholder");
    const i = this.bindTemplate;
    this.set("isActive", !1), this.set("isSticky", !1), this.set("limiterElement", null), this.set("limiterBottomOffset", 50), this.set("viewportTopOffset", 0), this.set("_marginLeft", null), this.set("_isStickyToTheBottomOfLimiter", !1), this.set("_stickyTopOffset", null), this.set("_stickyBottomOffset", null), this.content = this.createCollection(), this._contentPanelPlaceholder = new ri({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-sticky-panel__placeholder"
        ],
        style: {
          display: i.to("isSticky", (s) => s ? "block" : "none"),
          height: i.to("isSticky", (s) => s ? hr(this._contentPanelRect.height) : null)
        }
      }
    }).render(), this.contentPanelElement = new ri({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-sticky-panel__content",
          // Toggle class of the panel when "sticky" state changes in the view.
          i.if("isSticky", "ck-sticky-panel__content_sticky"),
          i.if("_isStickyToTheBottomOfLimiter", "ck-sticky-panel__content_sticky_bottom-limit")
        ],
        style: {
          width: i.to("isSticky", (s) => s ? hr(this._contentPanelPlaceholder.getBoundingClientRect().width) : null),
          top: i.to("_stickyTopOffset", (s) => s && hr(s)),
          bottom: i.to("_stickyBottomOffset", (s) => s && hr(s)),
          marginLeft: i.to("_marginLeft")
        }
      },
      children: this.content
    }).render(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-sticky-panel"
        ]
      },
      children: [
        this._contentPanelPlaceholder,
        this.contentPanelElement
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.checkIfShouldBeSticky(), this.listenTo(ae.document, "scroll", () => {
      this.checkIfShouldBeSticky();
    }, {
      useCapture: !0
    }), this.listenTo(this, "change:isActive", () => {
      this.checkIfShouldBeSticky();
    });
  }
  /**
  * Analyzes the environment to decide whether the panel should be sticky or not.
  * Then handles the positioning of the panel.
  */
  checkIfShouldBeSticky() {
    if (!this.limiterElement || !this.isActive) {
      this._unstick();
      return;
    }
    const e = new ke(this.limiterElement);
    let i = e.getVisible();
    if (i) {
      const s = new ke(ae.window);
      s.top += this.viewportTopOffset, s.height -= this.viewportTopOffset, i = i.getIntersection(s);
    }
    if (i && e.top < i.top) {
      const s = i.top;
      if (s + this._contentPanelRect.height + this.limiterBottomOffset > i.bottom) {
        const r = Math.max(e.bottom - i.bottom, 0) + this.limiterBottomOffset;
        e.bottom - r > e.top + this._contentPanelRect.height ? this._stickToBottomOfLimiter(r) : this._unstick();
      } else
        this._contentPanelRect.height + this.limiterBottomOffset < e.height ? this._stickToTopOfAncestors(s) : this._unstick();
    } else
      this._unstick();
  }
  /**
  * Sticks the panel at the given CSS `top` offset.
  *
  * @private
  * @param topOffset
  */
  _stickToTopOfAncestors(e) {
    this.isSticky = !0, this._isStickyToTheBottomOfLimiter = !1, this._stickyTopOffset = e, this._stickyBottomOffset = null, this._marginLeft = hr(-ae.window.scrollX);
  }
  /**
  * Sticks the panel at the bottom of the limiter with a given CSS `bottom` offset.
  *
  * @private
  * @param stickyBottomOffset
  */
  _stickToBottomOfLimiter(e) {
    this.isSticky = !0, this._isStickyToTheBottomOfLimiter = !0, this._stickyTopOffset = null, this._stickyBottomOffset = e, this._marginLeft = hr(-ae.window.scrollX);
  }
  /**
  * Unsticks the panel putting it back to its original position.
  *
  * @private
  */
  _unstick() {
    this.isSticky = !1, this._isStickyToTheBottomOfLimiter = !1, this._stickyTopOffset = null, this._stickyBottomOffset = null, this._marginLeft = null;
  }
  /**
  * Returns the bounding rect of the {@link #contentPanelElement}.
  *
  * @private
  */
  get _contentPanelRect() {
    return new ke(this.contentPanelElement);
  }
}
const X9 = /* @__PURE__ */ to("px");
class Q9 extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The toolbar view displayed in the balloon.
    */
    f(this, "toolbarView");
    /**
    * Tracks the focus of the {@link module:ui/editorui/editorui~EditorUI#getEditableElement editable element}
    * and the {@link #toolbarView}. When both are blurred then the toolbar should hide.
    */
    f(this, "focusTracker");
    /**
    * A cached and normalized `config.balloonToolbar` object.
    */
    f(this, "_balloonConfig");
    /**
    * An instance of the resize observer that allows to respond to changes in editable's geometry
    * so the toolbar can stay within its boundaries (and group toolbar items that do not fit).
    *
    * **Note**: Used only when `shouldNotGroupWhenFull` was **not** set in the
    * {@link module:core/editor/editorconfig~EditorConfig#balloonToolbar configuration}.
    *
    * **Note:** Created in {@link #init}.
    */
    f(this, "_resizeObserver", null);
    /**
    * The contextual balloon plugin instance.
    */
    f(this, "_balloon");
    /**
    * Fires `_selectionChangeDebounced` event using `lodash#debounce`.
    *
    * This event is an internal plugin event which is fired 200 ms after model selection last change.
    * This is to makes easy test debounced action without need to use `setTimeout`.
    *
    * This function is stored as a plugin property to make possible to cancel
    * trailing debounced invocation on destroy.
    */
    f(this, "_fireSelectionChangeDebounced");
    this._balloonConfig = Hf(e.config.get("balloonToolbar")), this.toolbarView = this._createToolbarView(), this.focusTracker = new ze(), this._trackFocusableEditableElements(), this.focusTracker.add(this.toolbarView), e.ui.addToolbar(this.toolbarView, {
      beforeFocus: () => this.show(!0),
      afterBlur: () => this.hide(),
      isContextual: !0
    }), this._balloon = e.plugins.get(ai), this._fireSelectionChangeDebounced = vi(() => this.fire("_selectionChangeDebounced"), 200), this.decorate("show");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BalloonToolbar";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ai
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model.document.selection;
    this.listenTo(this.focusTracker, "change:isFocused", (s, r, o) => {
      const a = this._balloon.visibleView === this.toolbarView;
      !o && a ? this.hide() : o && this.show();
    }), this.listenTo(i, "change:range", (s, r) => {
      (r.directChange || i.isCollapsed) && this.hide(), this._fireSelectionChangeDebounced();
    }), this.listenTo(this, "_selectionChangeDebounced", () => {
      this.editor.editing.view.document.isFocused && this.show();
    }), this._balloonConfig.shouldNotGroupWhenFull || this.listenTo(e, "ready", () => {
      const s = e.ui.view.editable.element;
      this._resizeObserver = new $r(s, (r) => {
        this.toolbarView.maxWidth = X9(r.contentRect.width * 0.9);
      });
    }), this.listenTo(this.toolbarView, "groupedItemsUpdate", () => {
      this._updatePosition();
    }), e.ui.once("ready", () => {
      this.toolbarView.fillFromConfig(this._balloonConfig, this.editor.ui.componentFactory);
    });
  }
  /**
  * Creates the toolbar view instance.
  */
  _createToolbarView() {
    const e = this.editor.locale.t, i = !this._balloonConfig.shouldNotGroupWhenFull, s = new Us(this.editor.locale, {
      shouldGroupWhenFull: i,
      isFloating: !0
    });
    return s.ariaLabel = e("Editor contextual toolbar"), s.render(), s;
  }
  /**
  * Shows the toolbar and attaches it to the selection.
  *
  * Fires {@link #event:show} event which can be stopped to prevent the toolbar from showing up.
  *
  * @param showForCollapsedSelection When set `true`, the toolbar will show despite collapsed selection in the
  * editing view.
  */
  show(e = !1) {
    const i = this.editor, s = i.model.document.selection, r = i.model.schema;
    this._balloon.hasView(this.toolbarView) || s.isCollapsed && !e || eB(s, r) || Array.from(this.toolbarView.items).every((o) => o.isEnabled !== void 0 && !o.isEnabled) || (this.listenTo(this.editor.ui, "update", () => {
      this._updatePosition();
    }), this._balloon.add({
      view: this.toolbarView,
      position: this._getBalloonPositionData(),
      balloonClassName: "ck-toolbar-container"
    }));
  }
  /**
  * Hides the toolbar.
  */
  hide() {
    this._balloon.hasView(this.toolbarView) && (this.stopListening(this.editor.ui, "update"), this._balloon.remove(this.toolbarView));
  }
  /**
  * Add or remove editable elements to the focus tracker. It watches added and removed roots
  * and adds or removes their editable elements to the focus tracker.
  */
  _trackFocusableEditableElements() {
    const { editor: e, focusTracker: i } = this, { editing: s } = e;
    s.view.addObserver(class extends sn {
      /**
      * @inheritDoc
      */
      observe(o) {
        i.add(o);
      }
      /**
      * @inheritDoc
      */
      stopObserving(o) {
        i.remove(o);
      }
    });
  }
  /**
  * Returns positioning options for the {@link #_balloon}. They control the way balloon is attached
  * to the selection.
  */
  _getBalloonPositionData() {
    const i = this.editor.editing.view, s = i.document, r = s.selection, o = s.selection.isBackward;
    return {
      // Because the target for BalloonPanelView is a Rect (not DOMRange), it's geometry will stay fixed
      // as the window scrolls. To let the BalloonPanelView follow such Rect, is must be continuously
      // computed and hence, the target is defined as a function instead of a static value.
      // https://github.com/ckeditor/ckeditor5-ui/issues/195
      target: () => {
        const a = o ? r.getFirstRange() : r.getLastRange(), l = ke.getDomRangeRects(i.domConverter.viewRangeToDom(a));
        return o ? l[0] : (l.length > 1 && l[l.length - 1].width === 0 && l.pop(), l[l.length - 1]);
      },
      positions: this._getBalloonPositions(o)
    };
  }
  /**
  * Updates the position of the {@link #_balloon} to make up for changes:
  *
  * * in the geometry of the selection it is attached to (e.g. the selection moved in the viewport or expanded or shrunk),
  * * or the geometry of the balloon toolbar itself (e.g. the toolbar has grouped or ungrouped some items and it is shorter or longer).
  */
  _updatePosition() {
    this._balloon.updatePosition(this._getBalloonPositionData());
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.stopListening(), this._fireSelectionChangeDebounced.cancel(), this.toolbarView.destroy(), this.focusTracker.destroy(), this._resizeObserver && this._resizeObserver.destroy();
  }
  /**
  * Returns toolbar positions for the given direction of the selection.
  */
  _getBalloonPositions(e) {
    const s = be.isSafari && be.isiOS ? Dt.generatePositions({
      // 20px when zoomed out. Less then 20px when zoomed in; the "radius" of the native selection handle gets
      // smaller as the user zooms in. No less than the default v-offset, though.
      heightOffset: Math.max(Dt.arrowHeightOffset, Math.round(20 / ae.window.visualViewport.scale))
    }) : Dt.defaultPositions;
    return e ? [
      s.northWestArrowSouth,
      s.northWestArrowSouthWest,
      s.northWestArrowSouthEast,
      s.northWestArrowSouthMiddleEast,
      s.northWestArrowSouthMiddleWest,
      s.southWestArrowNorth,
      s.southWestArrowNorthWest,
      s.southWestArrowNorthEast,
      s.southWestArrowNorthMiddleWest,
      s.southWestArrowNorthMiddleEast
    ] : [
      s.southEastArrowNorth,
      s.southEastArrowNorthEast,
      s.southEastArrowNorthWest,
      s.southEastArrowNorthMiddleEast,
      s.southEastArrowNorthMiddleWest,
      s.northEastArrowSouth,
      s.northEastArrowSouthEast,
      s.northEastArrowSouthWest,
      s.northEastArrowSouthMiddleEast,
      s.northEastArrowSouthMiddleWest
    ];
  }
}
function eB(n, t) {
  return n.rangeCount === 1 ? !1 : [
    ...n.getRanges()
  ].every((e) => {
    const i = e.getContainedElement();
    return i && t.isSelectable(i);
  });
}
class tB extends Ur {
  /**
  * Creates an instance of the menu bar button view.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * An icon that displays an arrow to indicate a direction of the menu.
    */
    f(this, "arrowView");
    const i = this.bindTemplate;
    this.set({
      withText: !0,
      role: "menuitem"
    }), this.arrowView = this._createArrowView(), this.extendTemplate({
      attributes: {
        class: [
          "ck-menu-bar__menu__button"
        ],
        "aria-haspopup": !0,
        "aria-expanded": this.bindTemplate.to("isOn", (s) => String(s)),
        "data-cke-tooltip-disabled": i.to("isOn")
      },
      on: {
        mouseenter: i.to("mouseenter")
      }
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.children.add(this.arrowView);
  }
  /**
  * Creates the {@link #arrowView} instance.
  */
  _createArrowView() {
    const e = new Qi();
    return e.content = Xc, e.extendTemplate({
      attributes: {
        class: "ck-menu-bar__menu__button__arrow"
      }
    }), e;
  }
}
class iB extends ee {
  /**
  * Creates an instance of the menu panel view.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views in this panel.
    */
    f(this, "children");
    const i = this.bindTemplate;
    this.set("isVisible", !1), this.set("position", "se"), this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-reset",
          "ck-menu-bar__menu__panel",
          i.to("position", (s) => `ck-menu-bar__menu__panel_position_${s}`),
          i.if("isVisible", "ck-hidden", (s) => !s)
        ],
        tabindex: "-1"
      },
      children: this.children,
      on: {
        // Drag and drop in the panel should not break the selection in the editor.
        // https://github.com/ckeditor/ckeditor5-ui/issues/228
        selectstart: i.to((s) => {
          s.target.tagName.toLocaleLowerCase() !== "input" && s.preventDefault();
        })
      }
    });
  }
  /**
  * Focuses the first child of the panel (default) or the last one if the `direction` is `-1`.
  */
  focus(e = 1) {
    this.children.length && (e === 1 ? this.children.first.focus() : this.children.last.focus());
  }
}
const Sc = class Sc extends ee {
  /**
  * Creates an instance of the menu view.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * Button of the menu view.
    */
    f(this, "buttonView");
    /**
    * Panel of the menu. It hosts children of the menu.
    */
    f(this, "panelView");
    /**
    * Tracks information about the DOM focus in the menu.
    */
    f(this, "focusTracker");
    /**
    * Instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}. It manages
    * keystrokes of the menu.
    */
    f(this, "keystrokes");
    const i = this.bindTemplate;
    this.buttonView = new tB(e), this.buttonView.delegate("mouseenter").to(this), this.buttonView.bind("isOn", "isEnabled").to(this, "isOpen", "isEnabled"), this.panelView = new iB(e), this.panelView.bind("isVisible").to(this, "isOpen"), this.keystrokes = new Ze(), this.focusTracker = new ze(), this.set("isOpen", !1), this.set("isEnabled", !0), this.set("panelPosition", "w"), this.set("class", void 0), this.set("parentMenuView", null), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-menu-bar__menu",
          i.to("class"),
          i.if("isEnabled", "ck-disabled", (s) => !s),
          i.if("parentMenuView", "ck-menu-bar__menu_top-level", (s) => !s)
        ]
      },
      children: [
        this.buttonView,
        this.panelView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.buttonView.element), this.focusTracker.add(this.panelView.element), this.keystrokes.listenTo(this.element), ns.closeOnEscKey(this), this._repositionPanelOnOpen();
  }
  // For now, this method cannot be called in the render process because the `parentMenuView` may be assigned
  // after the rendering process.
  //
  // TODO: We should reconsider the way we handle this logic.
  /**
  * Attach all keyboard behaviors for the menu bar view.
  *
  * @internal
  */
  _attachBehaviors() {
    this.parentMenuView ? (ns.openOnButtonClick(this), ns.openOnArrowRightKey(this), ns.closeOnArrowLeftKey(this), ns.openAndFocusOnEnterKeyPress(this), ns.closeOnParentClose(this)) : (this._propagateArrowKeystrokeEvents(), ns.openAndFocusPanelOnArrowDownKey(this), ns.toggleOnButtonClick(this));
  }
  /**
  * Fires `arrowright` and `arrowleft` events when the user pressed corresponding arrow keys.
  */
  _propagateArrowKeystrokeEvents() {
    this.keystrokes.set("arrowright", (e, i) => {
      this.fire("arrowright"), i();
    }), this.keystrokes.set("arrowleft", (e, i) => {
      this.fire("arrowleft"), i();
    });
  }
  /**
  * Sets the position of the panel when the menu opens. The panel is positioned
  * so that it optimally uses the available space in the viewport.
  */
  _repositionPanelOnOpen() {
    this.on("change:isOpen", (e, i, s) => {
      if (!s)
        return;
      const r = Sc._getOptimalPosition({
        element: this.panelView.element,
        target: this.buttonView.element,
        fitInViewport: !0,
        positions: this._panelPositions
      });
      this.panelView.position = r ? r.name : this._defaultMenuPositionName;
    });
  }
  /**
  * @inheritDoc
  */
  focus() {
    this.buttonView.focus();
  }
  /**
  * Positioning functions for the {@link #panelView} . They change depending on the role of the menu (top-level vs sub-menu) in
  * the {@link module:ui/menubar/menubarview~MenuBarView menu bar} and the UI language direction.
  */
  get _panelPositions() {
    const { southEast: e, southWest: i, northEast: s, northWest: r, westSouth: o, eastSouth: a, westNorth: l, eastNorth: c } = V9;
    return this.locale.uiLanguageDirection === "ltr" ? this.parentMenuView ? [
      a,
      c,
      o,
      l
    ] : [
      e,
      i,
      s,
      r
    ] : this.parentMenuView ? [
      o,
      l,
      a,
      c
    ] : [
      i,
      e,
      r,
      s
    ];
  }
  /**
  * The default position of the panel when the menu is opened.
  * It is used when the optimal position cannot be calculated.
  */
  get _defaultMenuPositionName() {
    return this.locale.uiLanguageDirection === "ltr" ? this.parentMenuView ? "es" : "se" : this.parentMenuView ? "ws" : "sw";
  }
};
/**
* A function used to calculate the optimal position for the dropdown panel.
*
* Referenced for unit testing purposes.
*/
f(Sc, "_getOptimalPosition", wf);
let Yt = Sc;
class kn extends Qc {
  /**
  * Creates an instance of the list view.
  *
  * @param locale The localization services instance.
  */
  constructor(t) {
    super(t), this.role = "menu", this.items.on("change", this._setItemsCheckSpace.bind(this));
  }
  /**
  * This method adds empty space if there is any toggleable item in the list.
  * It makes the list properly aligned.
  */
  _setItemsCheckSpace() {
    const t = Array.from(this.items).some((e) => {
      const i = Fb(e);
      return i && i.isToggleable;
    });
    this.items.forEach((e) => {
      const i = Fb(e);
      i && (i.hasCheckSpace = t);
    });
  }
}
function Fb(n) {
  return n instanceof Wn ? n.children.map((t) => nB(t) ? t.buttonView : t).find((t) => t instanceof Ur) : null;
}
function nB(n) {
  return typeof n == "object" && "buttonView" in n && n.buttonView instanceof le;
}
class qy extends FL {
  /**
  * Creates an instance of the menu bar list button view.
  *
  * @param locale The localization services instance.
  */
  constructor(t) {
    super(t), this.set({
      withText: !0,
      withKeystroke: !0,
      tooltip: !1,
      role: "menuitem"
    }), this.extendTemplate({
      attributes: {
        class: [
          "ck-menu-bar__menu__item__button"
        ]
      }
    });
  }
}
const Db = [
  "mouseenter",
  "arrowleft",
  "arrowright",
  "change:isOpen"
];
class sB extends ee {
  /**
  * Creates an instance of the menu bar view.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * Collection of the child views inside the {@link #element}.
    */
    f(this, "children");
    /**
    * A list of {@link module:ui/menubar/menubarmenuview~MenuBarMenuView} instances registered in the menu bar.
    *
    * @observable
    */
    f(this, "menus", []);
    const i = e.t, s = this.bindTemplate;
    this.set({
      isOpen: !1,
      isFocusBorderEnabled: !1
    }), this._setupIsOpenUpdater(), this.children = this.createCollection(), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-menu-bar",
          s.if("isFocusBorderEnabled", "ck-menu-bar_focus-border-enabled")
        ],
        "aria-label": i("Editor menu bar"),
        role: "menubar"
      },
      children: this.children
    });
  }
  /**
  * A utility that expands a plain menu bar configuration into a structure of menus (also: sub-menus)
  * and items using a given {@link module:ui/componentfactory~ComponentFactory component factory}.
  *
  * See the {@link module:core/editor/editorconfig~EditorConfig#menuBar menu bar} in the editor
  * configuration reference to learn how to configure the menu bar.
  */
  fillFromConfig(e, i, s = []) {
    const r = this.locale, a = M9({
      normalizedConfig: e,
      locale: r,
      componentFactory: i,
      extraItems: s
    }).items.map((l) => this._createMenu({
      componentFactory: i,
      menuDefinition: l
    }));
    this.children.addMany(a);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), dr.toggleMenusAndFocusItemsOnHover(this), dr.closeMenusWhenTheBarCloses(this), dr.closeMenuWhenAnotherOnTheSameLevelOpens(this), dr.focusCycleMenusOnArrows(this), dr.closeOnClickOutside(this), dr.enableFocusHighlightOnInteraction(this);
  }
  /**
  * Focuses the menu bar.
  */
  focus() {
    this.children.first && this.children.first.focus();
  }
  /**
  * Closes all menus in the bar.
  */
  close() {
    for (const e of this.children)
      e.isOpen = !1;
  }
  /**
  * Registers a menu view in the menu bar. Every {@link module:ui/menubar/menubarmenuview~MenuBarMenuView} instance must be registered
  * in the menu bar to be properly managed.
  */
  registerMenu(e, i = null) {
    i ? (e.delegate(...Db).to(i), e.parentMenuView = i) : e.delegate(...Db).to(this, (s) => "menu:" + s), e._attachBehaviors(), this.menus.push(e);
  }
  /**
  * Creates a {@link module:ui/menubar/menubarmenuview~MenuBarMenuView} based on the given definition.
  */
  _createMenu({ componentFactory: e, menuDefinition: i, parentMenuView: s }) {
    const r = this.locale, o = new Yt(r);
    return this.registerMenu(o, s), o.buttonView.set({
      label: i.label
    }), o.once("change:isOpen", () => {
      const a = new kn(r);
      a.ariaLabel = i.label, o.panelView.children.add(a), a.items.addMany(this._createMenuItems({
        menuDefinition: i,
        parentMenuView: o,
        componentFactory: e
      }));
    }), o;
  }
  /**
  * Creates a {@link module:ui/menubar/menubarmenuview~MenuBarMenuView} items based on the given definition.
  */
  _createMenuItems({ menuDefinition: e, parentMenuView: i, componentFactory: s }) {
    const r = this.locale, o = [];
    for (const a of e.groups) {
      for (const l of a.items) {
        const c = new en(r, i);
        if (nt(l))
          c.children.add(this._createMenu({
            componentFactory: s,
            menuDefinition: l,
            parentMenuView: i
          }));
        else {
          const u = this._createMenuItemContentFromFactory({
            componentName: l,
            componentFactory: s,
            parentMenuView: i
          });
          if (!u)
            continue;
          c.children.add(u);
        }
        o.push(c);
      }
      a !== e.groups[e.groups.length - 1] && o.push(new Ba(r));
    }
    return o;
  }
  /**
  * Uses the component factory to create a content of the menu item (a button or a sub-menu).
  */
  _createMenuItemContentFromFactory({ componentName: e, parentMenuView: i, componentFactory: s }) {
    const r = s.create(e);
    return r instanceof Yt || r instanceof Fe || r instanceof qy ? (this._registerMenuTree(r, i), r.on("execute", () => {
      this.close();
    }), r) : (Ve("menu-bar-component-unsupported", {
      componentName: e,
      componentView: r
    }), null);
  }
  /**
  * Checks component and its children recursively and calls {@link #registerMenu}
  * for each item that is {@link module:ui/menubar/menubarmenuview~MenuBarMenuView}.
  *
  * @internal
  */
  _registerMenuTree(e, i) {
    if (!(e instanceof Yt)) {
      e.delegate("mouseenter").to(i);
      return;
    }
    this.registerMenu(e, i);
    const s = e.panelView.children.filter((o) => o instanceof kn)[0];
    if (!s) {
      e.delegate("mouseenter").to(i);
      return;
    }
    const r = s.items.filter((o) => o instanceof Wn);
    for (const o of r)
      this._registerMenuTree(o.children.get(0), e);
  }
  /**
  * Manages the state of the {@link #isOpen} property of the menu bar. Because the state is a sum of individual
  * top-level menus' states, it's necessary to listen to their changes and update the state accordingly.
  *
  * Additionally, it prevents from unnecessary changes of `isOpen` when one top-level menu opens and another closes
  * (regardless of in which order), maintaining a stable `isOpen === true` in that situation.
  */
  _setupIsOpenUpdater() {
    let e;
    this.on("menu:change:isOpen", (i, s, r) => {
      clearTimeout(e), r ? this.isOpen = !0 : e = setTimeout(() => {
        this.isOpen = Array.from(this.children).some((o) => o.isOpen);
      }, 0);
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const Wf = [
  "left",
  "right",
  "center",
  "justify"
];
function Gy(n) {
  return Wf.includes(n);
}
function Ky(n, t) {
  return t.contentLanguageDirection == "rtl" ? n === "right" : n === "left";
}
function Zy(n) {
  const t = n.map((i) => {
    let s;
    return typeof i == "string" ? s = {
      name: i
    } : s = i, s;
  }).filter((i) => {
    const s = Wf.includes(i.name);
    return s || Ve("alignment-config-name-not-recognized", {
      option: i
    }), s;
  }), e = t.filter((i) => !!i.className).length;
  if (e && e < t.length)
    throw new S("alignment-config-classnames-are-missing", {
      configuredOptions: n
    });
  return t.forEach((i, s, r) => {
    const o = r.slice(s + 1);
    if (o.some((l) => l.name == i.name))
      throw new S("alignment-config-name-already-defined", {
        option: i,
        configuredOptions: n
      });
    if (i.className && o.some((c) => c.className == i.className))
      throw new S("alignment-config-classname-already-defined", {
        option: i,
        configuredOptions: n
      });
  }), t;
}
const qf = "alignment";
class rB extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.locale, i = Xe(this.editor.model.document.selection.getSelectedBlocks());
    this.isEnabled = !!i && this._canBeAligned(i), this.isEnabled && i.hasAttribute("alignment") ? this.value = i.getAttribute("alignment") : this.value = e.contentLanguageDirection === "rtl" ? "right" : "left";
  }
  /**
  * Executes the command. Applies the alignment `value` to the selected blocks.
  * If no `value` is passed, the `value` is the default one or it is equal to the currently selected block's alignment attribute,
  * the command will remove the attribute from the selected blocks.
  *
  * @param options Options for the executed command.
  * @param options.value The value to apply.
  * @fires execute
  */
  execute(t = {}) {
    const e = this.editor, i = e.locale, s = e.model, r = s.document, o = t.value;
    s.change((a) => {
      const l = Array.from(r.selection.getSelectedBlocks()).filter((d) => this._canBeAligned(d)), c = l[0].getAttribute("alignment");
      Ky(o, i) || c === o || !o ? oB(l, a) : aB(l, a, o);
    });
  }
  /**
  * Checks whether a block can have alignment set.
  *
  * @param block The block to be checked.
  */
  _canBeAligned(t) {
    return this.editor.model.schema.checkAttribute(t, qf);
  }
}
function oB(n, t) {
  for (const e of n)
    t.removeAttribute(qf, e);
}
function aB(n, t, e) {
  for (const i of n)
    t.setAttribute(qf, e, i);
}
class lB extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AlignmentEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("alignment", {
      options: Wf.map((e) => ({
        name: e
      }))
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale, i = t.model.schema, r = Zy(t.config.get("alignment.options")).filter((c) => Gy(c.name) && !Ky(c.name, e)), o = r.some((c) => !!c.className);
    i.extend("$block", {
      allowAttributes: "alignment"
    }), t.model.schema.setAttributeProperties("alignment", {
      isFormatting: !0
    }), o ? t.conversion.attributeToAttribute(hB(r)) : t.conversion.for("downcast").attributeToAttribute(cB(r));
    const a = uB(r);
    for (const c of a)
      t.conversion.for("upcast").attributeToAttribute(c);
    const l = dB(r);
    for (const c of l)
      t.conversion.for("upcast").attributeToAttribute(c);
    t.commands.add("alignment", new rB(t));
  }
}
function cB(n) {
  const t = {};
  for (const { name: i } of n)
    t[i] = {
      key: "style",
      value: {
        "text-align": i
      }
    };
  return {
    model: {
      key: "alignment",
      values: n.map((i) => i.name)
    },
    view: t
  };
}
function uB(n) {
  const t = [];
  for (const { name: e } of n)
    t.push({
      view: {
        key: "style",
        value: {
          "text-align": e
        }
      },
      model: {
        key: "alignment",
        value: e
      }
    });
  return t;
}
function dB(n) {
  const t = [];
  for (const { name: e } of n)
    t.push({
      view: {
        key: "align",
        value: e
      },
      model: {
        key: "alignment",
        value: e
      }
    });
  return t;
}
function hB(n) {
  const t = {};
  for (const i of n)
    t[i.name] = {
      key: "class",
      value: i.className
    };
  return {
    model: {
      key: "alignment",
      values: n.map((i) => i.name)
    },
    view: t
  };
}
const vo = /* @__PURE__ */ new Map([
  [
    "left",
    Z.alignLeft
  ],
  [
    "right",
    Z.alignRight
  ],
  [
    "center",
    Z.alignCenter
  ],
  [
    "justify",
    Z.alignJustify
  ]
]);
class fB extends I {
  /**
  * Returns the localized option titles provided by the plugin.
  *
  * The following localized titles corresponding with
  * {@link module:alignment/alignmentconfig~AlignmentConfig#options} are available:
  *
  * * `'left'`,
  * * `'right'`,
  * * `'center'`,
  * * `'justify'`.
  *
  * @readonly
  */
  get localizedOptionTitles() {
    const t = this.editor.t;
    return {
      left: t("Align left"),
      right: t("Align right"),
      center: t("Align center"),
      justify: t("Justify")
    };
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AlignmentUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = Zy(t.config.get("alignment.options"));
    e.map((i) => i.name).filter(Gy).forEach((i) => this._addButton(i)), this._addToolbarDropdown(e), this._addMenuBarMenu(e);
  }
  /**
  * Helper method for initializing the button and linking it with an appropriate command.
  *
  * @param option The name of the alignment option for which the button is added.
  */
  _addButton(t) {
    this.editor.ui.componentFactory.add(`alignment:${t}`, (i) => this._createButton(i, t));
  }
  /**
  * Helper method for creating the button view element.
  *
  * @param locale Editor locale.
  * @param option The name of the alignment option for which the button is added.
  * @param buttonAttrs Optional parameters passed to button view instance.
  */
  _createButton(t, e, i = {}) {
    const s = this.editor, r = s.commands.get("alignment"), o = new le(t);
    return o.set({
      label: this.localizedOptionTitles[e],
      icon: vo.get(e),
      tooltip: !0,
      isToggleable: !0,
      ...i
    }), o.bind("isEnabled").to(r), o.bind("isOn").to(r, "value", (a) => a === e), this.listenTo(o, "execute", () => {
      s.execute("alignment", {
        value: e
      }), s.editing.view.focus();
    }), o;
  }
  /**
  * Helper method for initializing the toolnar dropdown and linking it with an appropriate command.
  *
  * @param option The name of the alignment option for which the button is added.
  */
  _addToolbarDropdown(t) {
    const e = this.editor;
    e.ui.componentFactory.add("alignment", (s) => {
      const r = Tt(s), o = s.uiLanguageDirection === "rtl" ? "w" : "e", a = s.t;
      Ma(r, () => t.map((u) => this._createButton(s, u.name, {
        tooltipPosition: o
      })), {
        enableActiveItemFocusOnDropdownOpen: !0,
        isVertical: !0,
        ariaLabel: a("Text alignment toolbar")
      }), r.buttonView.set({
        label: a("Text alignment"),
        tooltip: !0
      }), r.extendTemplate({
        attributes: {
          class: "ck-alignment-dropdown"
        }
      });
      const l = s.contentLanguageDirection === "rtl" ? vo.get("right") : vo.get("left"), c = e.commands.get("alignment");
      return r.buttonView.bind("icon").to(c, "value", (u) => vo.get(u) || l), r.bind("isEnabled").to(c, "isEnabled"), this.listenTo(r, "execute", () => {
        e.editing.view.focus();
      }), r;
    });
  }
  /**
  * Creates a menu for all alignment options to use either in menu bar.
  *
  * @param options Normalized alignment options from config.
  */
  _addMenuBarMenu(t) {
    const e = this.editor;
    e.ui.componentFactory.add("menuBar:alignment", (i) => {
      const s = e.commands.get("alignment"), r = i.t, o = new Yt(i), a = new kn(i);
      o.bind("isEnabled").to(s), a.set({
        ariaLabel: r("Text alignment"),
        role: "menu"
      }), o.buttonView.set({
        label: r("Text alignment")
      });
      for (const l of t) {
        const c = new en(i, o), u = new Fe(i);
        u.delegate("execute").to(o), u.set({
          label: this.localizedOptionTitles[l.name],
          icon: vo.get(l.name),
          role: "menuitemcheckbox",
          isToggleable: !0
        }), u.on("execute", () => {
          e.execute("alignment", {
            value: l.name
          }), e.editing.view.focus();
        }), u.bind("isOn").to(s, "value", (d) => d === l.name), u.bind("isEnabled").to(s, "isEnabled"), c.children.add(u), a.items.add(c);
      }
      return o.panelView.children.add(a), o;
    });
  }
}
class mB extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      lB,
      fB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Alignment";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class Yy {
  /**
  * Creates a new instance of the change buffer.
  *
  * @param limit The maximum number of atomic changes which can be contained in one batch.
  */
  constructor(t, e = 20) {
    /**
    * The model instance.
    */
    f(this, "model");
    /**
    * The maximum number of atomic changes which can be contained in one batch.
    */
    f(this, "limit");
    /**
    * Whether the buffer is locked. A locked buffer cannot be reset unless it gets unlocked.
    */
    f(this, "_isLocked");
    /**
    * The number of atomic changes in the buffer. Once it exceeds the {@link #limit},
    * the {@link #batch batch} is set to a new one.
    */
    f(this, "_size");
    /**
    * The current batch instance.
    */
    f(this, "_batch", null);
    /**
    * The callback to document the change event which later needs to be removed.
    */
    f(this, "_changeCallback");
    /**
    * The callback to document selection `change:attribute` and `change:range` events which resets the buffer.
    */
    f(this, "_selectionChangeCallback");
    this.model = t, this._size = 0, this.limit = e, this._isLocked = !1, this._changeCallback = (i, s) => {
      s.isLocal && s.isUndoable && s !== this._batch && this._reset(!0);
    }, this._selectionChangeCallback = () => {
      this._reset();
    }, this.model.document.on("change", this._changeCallback), this.model.document.selection.on("change:range", this._selectionChangeCallback), this.model.document.selection.on("change:attribute", this._selectionChangeCallback);
  }
  /**
  * The current batch to which a feature should add its operations. Once the {@link #size}
  * is reached or exceeds the {@link #limit}, the batch is set to a new instance and the size is reset.
  */
  get batch() {
    return this._batch || (this._batch = this.model.createBatch({
      isTyping: !0
    })), this._batch;
  }
  /**
  * The number of atomic changes in the buffer. Once it exceeds the {@link #limit},
  * the {@link #batch batch} is set to a new one.
  */
  get size() {
    return this._size;
  }
  /**
  * The input number of changes into the buffer. Once the {@link #size} is
  * reached or exceeds the {@link #limit}, the batch is set to a new instance and the size is reset.
  *
  * @param changeCount The number of atomic changes to input.
  */
  input(t) {
    this._size += t, this._size >= this.limit && this._reset(!0);
  }
  /**
  * Whether the buffer is locked. A locked buffer cannot be reset unless it gets unlocked.
  */
  get isLocked() {
    return this._isLocked;
  }
  /**
  * Locks the buffer.
  */
  lock() {
    this._isLocked = !0;
  }
  /**
  * Unlocks the buffer.
  */
  unlock() {
    this._isLocked = !1;
  }
  /**
  * Destroys the buffer.
  */
  destroy() {
    this.model.document.off("change", this._changeCallback), this.model.document.selection.off("change:range", this._selectionChangeCallback), this.model.document.selection.off("change:attribute", this._selectionChangeCallback);
  }
  /**
  * Resets the change buffer.
  *
  * @param ignoreLock Whether internal lock {@link #isLocked} should be ignored.
  */
  _reset(t = !1) {
    (!this.isLocked || t) && (this._batch = null, this._size = 0);
  }
}
class gB extends he {
  /**
  * Creates an instance of the command.
  *
  * @param undoStepSize The maximum number of atomic changes
  * which can be contained in one batch in the command buffer.
  */
  constructor(e, i) {
    super(e);
    /**
    * Typing's change buffer used to group subsequent changes into batches.
    */
    f(this, "_buffer");
    this._buffer = new Yy(e.model, i), this._isEnabledBasedOnSelection = !1;
  }
  /**
  * The current change buffer.
  */
  get buffer() {
    return this._buffer;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._buffer.destroy();
  }
  /**
  * Executes the input command. It replaces the content within the given range with the given text.
  * Replacing is a two step process, first the content within the range is removed and then the new text is inserted
  * at the beginning of the range (which after the removal is a collapsed range).
  *
  * @fires execute
  * @param options The command options.
  */
  execute(e = {}) {
    const i = this.editor.model, s = i.document, r = e.text || "", o = r.length;
    let a = s.selection;
    if (e.selection ? a = e.selection : e.range && (a = i.createSelection(e.range)), !i.canEditAt(a))
      return;
    const l = e.resultRange;
    i.enqueueChange(this._buffer.batch, (c) => {
      this._buffer.lock();
      const u = Array.from(s.selection.getAttributes());
      i.deleteContent(a), r && i.insertContent(c.createText(r, u), a), l ? c.setSelection(l) : a.is("documentSelection") || c.setSelection(a), this._buffer.unlock(), this._buffer.input(o);
    });
  }
}
const Jy = [
  // For collapsed range:
  //	- This one is a regular typing (all browsers, all systems).
  //	- This one is used by Chrome when typing accented letter – 2nd step when the user selects the accent (Mac).
  // For non-collapsed range:
  //	- This one is used by Chrome when typing accented letter – when the selection box first appears (Mac).
  //	- This one is used by Safari when accepting spell check suggestions from the context menu (Mac).
  "insertText",
  // This one is used by Safari when typing accented letter (Mac).
  // This one is used by Safari when accepting spell check suggestions from the autocorrection pop-up (Mac).
  "insertReplacementText"
], pB = [
  ...Jy,
  "insertCompositionText"
];
class bB extends sn {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Instance of the focus observer. Insert text observer calls
    * {@link module:engine/view/observer/focusobserver~FocusObserver#flush} to mark the latest focus change as complete.
    */
    f(this, "focusObserver");
    this.focusObserver = e.getObserver(Ql);
    const i = be.isAndroid ? pB : Jy, s = e.document;
    s.on("beforeinput", (r, o) => {
      if (!this.isEnabled)
        return;
      const { data: a, targetRanges: l, inputType: c, domEvent: u } = o;
      if (!i.includes(c))
        return;
      this.focusObserver.flush();
      const d = new jn(s, "insertText");
      s.fire(d, new zr(e, u, {
        text: a,
        selection: e.createSelection(l)
      })), d.stop.called && r.stop();
    }), be.isAndroid || s.on("compositionend", (r, { data: o, domEvent: a }) => {
      this.isEnabled && o && s.fire("insertText", new zr(e, a, {
        text: o
      }));
    }, {
      priority: "lowest"
    });
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
class Xy extends I {
  constructor() {
    super(...arguments);
    /**
    * The queue of `insertText` command executions that are waiting for the DOM to get updated after beforeinput event.
    */
    f(this, "_compositionQueue");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Input";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model, s = e.editing.view, r = e.editing.mapper, o = i.document.selection;
    this._compositionQueue = new wB(e), s.addObserver(bB);
    const a = new gB(e, e.config.get("typing.undoStep") || 20);
    e.commands.add("insertText", a), e.commands.add("input", a), this.listenTo(s.document, "insertText", (l, c) => {
      s.document.isComposing || c.preventDefault(), be.isAndroid && s.document.isComposing && this._compositionQueue.flush("next beforeinput");
      const { text: u, selection: d } = c;
      let h;
      d ? h = Array.from(d.getRanges()).map((p) => r.toModelRange(p)) : h = Array.from(o.getRanges());
      let m = u;
      if (be.isAndroid) {
        const p = Array.from(h[0].getItems()).reduce((w, y) => w + (y.is("$textProxy") ? y.data : ""), "");
        if (p && (p.length <= m.length ? m.startsWith(p) && (m = m.substring(p.length), h[0].start = h[0].start.getShiftedBy(p.length)) : p.startsWith(m) && (h[0].start = h[0].start.getShiftedBy(m.length), m = "")), m.length == 0 && h[0].isCollapsed)
          return;
      }
      const g = {
        text: m,
        selection: i.createSelection(h)
      };
      be.isAndroid && s.document.isComposing ? this._compositionQueue.push(g) : (e.execute("insertText", g), s.scrollToTheSelection());
    }), be.isAndroid ? this.listenTo(s.document, "keydown", (l, c) => {
      o.isCollapsed || c.keyCode != 229 || !s.document.isComposing || $b(i, a);
    }) : this.listenTo(s.document, "compositionstart", () => {
      o.isCollapsed || $b(i, a);
    }), be.isAndroid ? (this.listenTo(s.document, "mutations", (l, { mutations: c }) => {
      if (s.document.isComposing)
        for (const { node: u } of c) {
          const d = _B(u, r), h = r.toModelElement(d);
          if (this._compositionQueue.isComposedElement(h)) {
            this._compositionQueue.flush("mutations");
            return;
          }
        }
    }), this.listenTo(s.document, "compositionend", () => {
      this._compositionQueue.flush("composition end");
    }), this.listenTo(s.document, "compositionend", () => {
      const l = [];
      for (const c of this._compositionQueue.flushComposedElements()) {
        const u = r.toViewElement(c);
        u && l.push({
          type: "children",
          node: u
        });
      }
      l.length && s.document.fire("mutations", {
        mutations: l
      });
    }, {
      priority: "lowest"
    })) : this.listenTo(s.document, "compositionend", () => {
      s.document.fire("mutations", {
        mutations: []
      });
    }, {
      priority: "lowest"
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._compositionQueue.destroy();
  }
}
class wB {
  /**
  * @inheritDoc
  */
  constructor(t) {
    /**
    * The editor instance.
    */
    f(this, "editor");
    /**
    * Debounced queue flush as a safety mechanism for cases of mutation observer not triggering.
    */
    f(this, "flushDebounced", vi(() => this.flush("timeout"), 50));
    /**
    * The queue of `insertText` command executions that are waiting for the DOM to get updated after beforeinput event.
    */
    f(this, "_queue", []);
    /**
    * A set of model elements. The composition happened in those elements. It's used for mutations check.
    */
    f(this, "_compositionElements", /* @__PURE__ */ new Set());
    this.editor = t;
  }
  /**
  * Destroys the helper object.
  */
  destroy() {
    for (this.flushDebounced.cancel(), this._compositionElements.clear(); this._queue.length; )
      this.shift();
  }
  /**
  * Returns the size of the queue.
  */
  get length() {
    return this._queue.length;
  }
  /**
  * Push next insertText command data to the queue.
  */
  push(t) {
    const e = {
      text: t.text
    };
    if (t.selection) {
      e.selectionRanges = [];
      for (const i of t.selection.getRanges())
        e.selectionRanges.push(ni.fromRange(i)), this._compositionElements.add(i.start.parent);
    }
    this._queue.push(e), this.flushDebounced();
  }
  /**
  * Shift the first item from the insertText command data queue.
  */
  shift() {
    const t = this._queue.shift(), e = {
      text: t.text
    };
    if (t.selectionRanges) {
      const i = t.selectionRanges.map((s) => vB(s)).filter((s) => !!s);
      i.length && (e.selection = this.editor.model.createSelection(i));
    }
    return e;
  }
  /**
  * Applies all queued insertText command executions.
  *
  * @param reason Used only for debugging.
  */
  flush(t) {
    const e = this.editor, i = e.model, s = e.editing.view;
    if (this.flushDebounced.cancel(), !this._queue.length)
      return;
    const o = e.commands.get("insertText").buffer;
    i.enqueueChange(o.batch, () => {
      for (o.lock(); this._queue.length; ) {
        const a = this.shift();
        e.execute("insertText", a);
      }
      o.unlock();
    }), s.scrollToTheSelection();
  }
  /**
  * Returns `true` if the given model element is related to recent composition.
  */
  isComposedElement(t) {
    return this._compositionElements.has(t);
  }
  /**
  * Returns an array of composition-related elements and clears the internal list.
  */
  flushComposedElements() {
    const t = Array.from(this._compositionElements);
    return this._compositionElements.clear(), t;
  }
}
function $b(n, t) {
  if (!t.isEnabled)
    return;
  const e = t.buffer;
  e.lock(), n.enqueueChange(e.batch, () => {
    n.deleteContent(n.document.selection);
  }), e.unlock();
}
function vB(n) {
  const t = n.toRange();
  return n.detach(), t.root.rootName == "$graveyard" ? null : t;
}
function _B(n, t) {
  let e = n.is("$text") ? n.parent : n;
  for (; !t.toModelElement(e); )
    e = e.parent;
  return e;
}
class zb extends he {
  /**
  * Creates an instance of the command.
  *
  * @param direction The directionality of the delete describing in what direction it
  * should consume the content when the selection is collapsed.
  */
  constructor(e, i) {
    super(e);
    /**
    * The directionality of the delete describing in what direction it should
    * consume the content when the selection is collapsed.
    */
    f(this, "direction");
    /**
    * Delete's change buffer used to group subsequent changes into batches.
    */
    f(this, "_buffer");
    this.direction = i, this._buffer = new Yy(e.model, e.config.get("typing.undoStep")), this._isEnabledBasedOnSelection = !1;
  }
  /**
  * The current change buffer.
  */
  get buffer() {
    return this._buffer;
  }
  /**
  * Executes the delete command. Depending on whether the selection is collapsed or not, deletes its content
  * or a piece of content in the {@link #direction defined direction}.
  *
  * @fires execute
  * @param options The command options.
  * @param options.unit See {@link module:engine/model/utils/modifyselection~modifySelection}'s options.
  * @param options.sequence A number describing which subsequent delete event it is without the key being released.
  * See the {@link module:engine/view/document~Document#event:delete} event data.
  * @param options.selection Selection to remove. If not set, current model selection will be used.
  */
  execute(e = {}) {
    const i = this.editor.model, s = i.document;
    i.enqueueChange(this._buffer.batch, (r) => {
      this._buffer.lock();
      const o = r.createSelection(e.selection || s.selection);
      if (!i.canEditAt(o))
        return;
      const a = e.sequence || 1, l = o.isCollapsed;
      if (o.isCollapsed && i.modifySelection(o, {
        direction: this.direction,
        unit: e.unit,
        treatEmojiAsSingleUnit: !0
      }), this._shouldEntireContentBeReplacedWithParagraph(a)) {
        this._replaceEntireContentWithParagraph(r);
        return;
      }
      if (this._shouldReplaceFirstBlockWithParagraph(o, a)) {
        this.editor.execute("paragraph", {
          selection: o
        });
        return;
      }
      if (o.isCollapsed)
        return;
      let c = 0;
      o.getFirstRange().getMinimalFlatRanges().forEach((u) => {
        c += ih(u.getWalker({
          singleCharacters: !0,
          ignoreElementEnd: !0,
          shallow: !0
        }));
      }), i.deleteContent(o, {
        doNotResetEntireContent: l,
        direction: this.direction
      }), this._buffer.input(c), r.setSelection(o), this._buffer.unlock();
    });
  }
  /**
  * If the user keeps <kbd>Backspace</kbd> or <kbd>Delete</kbd> key pressed, the content of the current
  * editable will be cleared. However, this will not yet lead to resetting the remaining block to a paragraph
  * (which happens e.g. when the user does <kbd>Ctrl</kbd> + <kbd>A</kbd>, <kbd>Backspace</kbd>).
  *
  * But, if the user pressed the key in an empty editable for the first time,
  * we want to replace the entire content with a paragraph if:
  *
  * * the current limit element is empty,
  * * the paragraph is allowed in the limit element,
  * * the limit doesn't already have a paragraph inside.
  *
  * See https://github.com/ckeditor/ckeditor5-typing/issues/61.
  *
  * @param sequence A number describing which subsequent delete event it is without the key being released.
  */
  _shouldEntireContentBeReplacedWithParagraph(e) {
    if (e > 1)
      return !1;
    const i = this.editor.model, r = i.document.selection, o = i.schema.getLimitElement(r);
    if (!(r.isCollapsed && r.containsEntireContent(o)) || !i.schema.checkChild(o, "paragraph"))
      return !1;
    const l = o.getChild(0);
    return !(l && l.is("element", "paragraph"));
  }
  /**
  * The entire content is replaced with the paragraph. Selection is moved inside the paragraph.
  *
  * @param writer The model writer.
  */
  _replaceEntireContentWithParagraph(e) {
    const i = this.editor.model, r = i.document.selection, o = i.schema.getLimitElement(r), a = e.createElement("paragraph");
    e.remove(e.createRangeIn(o)), e.insert(a, o), e.setSelection(a, 0);
  }
  /**
  * Checks if the selection is inside an empty element that is the first child of the limit element
  * and should be replaced with a paragraph.
  *
  * @param selection The selection.
  * @param sequence A number describing which subsequent delete event it is without the key being released.
  */
  _shouldReplaceFirstBlockWithParagraph(e, i) {
    const s = this.editor.model;
    if (i > 1 || this.direction != "backward" || !e.isCollapsed)
      return !1;
    const r = e.getFirstPosition(), o = s.schema.getLimitElement(r), a = o.getChild(0);
    return !(r.parent != a || !e.containsEntireContent(a) || !s.schema.checkChild(o, "paragraph") || a.name == "paragraph");
  }
}
const yB = "character", Hb = "word", kB = "codePoint", us = "selection", Er = "backward", Vo = "forward", Qy = {
  // --------------------------------------- Backward delete types -----------------------------------------------------
  // This happens in Safari on Mac when some content is selected and Ctrl + K is pressed.
  deleteContent: {
    unit: us,
    // According to the Input Events Level 2 spec, this delete type has no direction
    // but to keep things simple, let's default to backward.
    direction: Er
  },
  // Chrome and Safari on Mac: Backspace or Ctrl + H
  deleteContentBackward: {
    // This kind of deletions must be done on the code point-level instead of target range provided by the DOM beforeinput event.
    // Take for instance "👨‍👩‍👧‍👧", it equals:
    //
    //	* [ "👨", "ZERO WIDTH JOINER", "👩", "ZERO WIDTH JOINER", "👧", "ZERO WIDTH JOINER", "👧" ]
    //	* or simply "\u{1F468}\u200D\u{1F469}\u200D\u{1F467}\u200D\u{1F467}"
    //
    // The range provided by the browser would cause the entire multi-byte grapheme to disappear while the user
    // intention when deleting backwards ("👨‍👩‍👧‍👧[]", then backspace) is gradual "decomposition" (first to "👨‍👩‍👧‍[]",
    // then to "👨‍👩‍[]", etc.).
    //
    //	* "👨‍👩‍👧‍👧[]" + backward delete (by code point)  -> results in "👨‍👩‍👧[]", removed the last "👧" 👍
    //	* "👨‍👩‍👧‍👧[]" + backward delete (by character)  -> results in "[]", removed the whole grapheme 👎
    //
    // Deleting by code-point is simply a better UX. See "deleteContentForward" to learn more.
    unit: kB,
    direction: Er
  },
  // On Mac: Option + Backspace.
  // On iOS: Hold the backspace for a while and the whole words will start to disappear.
  deleteWordBackward: {
    unit: Hb,
    direction: Er
  },
  // Safari on Mac: Cmd + Backspace
  deleteHardLineBackward: {
    unit: us,
    direction: Er
  },
  // Chrome on Mac: Cmd + Backspace.
  deleteSoftLineBackward: {
    unit: us,
    direction: Er
  },
  // --------------------------------------- Forward delete types -----------------------------------------------------
  // Chrome on Mac: Fn + Backspace or Ctrl + D
  // Safari on Mac: Ctrl + K or Ctrl + D
  deleteContentForward: {
    // Unlike backward delete, this delete must be performed by character instead of by code point, which
    // provides the best UX for working with accented letters.
    // Take, for example "b̂" ("\u0062\u0302", or [ "LATIN SMALL LETTER B", "COMBINING CIRCUMFLEX ACCENT" ]):
    //
    //	* "b̂[]" + backward delete (by code point)  -> results in "b[]", removed the combining mark 👍
    //	* "[]b̂" + forward delete (by code point)   -> results in "[]^", a bare combining mark does that not make sense when alone 👎
    //	* "[]b̂" + forward delete (by character)    -> results in "[]", removed both "b" and the combining mark 👍
    //
    // See: "deleteContentBackward" to learn more.
    unit: yB,
    direction: Vo
  },
  // On Mac: Fn + Option + Backspace.
  deleteWordForward: {
    unit: Hb,
    direction: Vo
  },
  // Chrome on Mac: Ctrl + K (you have to disable the Link plugin first, though, because it uses the same keystroke)
  // This is weird that it does not work in Safari on Mac despite being listed in the official shortcuts listing
  // on Apple's webpage.
  deleteHardLineForward: {
    unit: us,
    direction: Vo
  },
  // At this moment there is no known way to trigger this event type but let's keep it for the symmetry with
  // deleteSoftLineBackward.
  deleteSoftLineForward: {
    unit: us,
    direction: Vo
  }
};
class CB extends sn {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t);
    const e = t.document;
    let i = 0;
    e.on("keydown", () => {
      i++;
    }), e.on("keyup", () => {
      i = 0;
    }), e.on("beforeinput", (s, r) => {
      if (!this.isEnabled)
        return;
      const { targetRanges: o, domEvent: a, inputType: l } = r, c = Qy[l];
      if (!c)
        return;
      const u = {
        direction: c.direction,
        unit: c.unit,
        sequence: i
      };
      u.unit == us && (u.selectionToRemove = t.createSelection(o[0])), l === "deleteContentBackward" && (be.isAndroid && (u.sequence = 1), TB(o) && (u.unit = us, u.selectionToRemove = t.createSelection(o)));
      const d = new io(e, "delete", o[0]);
      e.fire(d, new zr(t, a, u)), d.stop.called && s.stop();
    }), be.isBlink && xB(this);
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
function xB(n) {
  const t = n.view, e = t.document;
  let i = null, s = !1;
  e.on("keydown", (a, { keyCode: l }) => {
    i = l, s = !1;
  }), e.on("keyup", (a, { keyCode: l, domEvent: c }) => {
    const u = e.selection, d = n.isEnabled && l == i && r(l) && !u.isCollapsed && !s;
    if (i = null, d) {
      const h = u.getFirstRange(), m = new io(e, "delete", h), g = {
        unit: us,
        direction: o(l),
        selectionToRemove: u
      };
      e.fire(m, new zr(t, c, g));
    }
  }), e.on("beforeinput", (a, { inputType: l }) => {
    const c = Qy[l];
    r(i) && c && c.direction == o(i) && (s = !0);
  }, {
    priority: "high"
  }), e.on("beforeinput", (a, { inputType: l, data: c }) => {
    i == Ie.delete && l == "insertText" && c == "" && a.stop();
  }, {
    priority: "high"
  });
  function r(a) {
    return a == Ie.backspace || a == Ie.delete;
  }
  function o(a) {
    return a == Ie.backspace ? Er : Vo;
  }
}
function TB(n) {
  if (n.length != 1 || n[0].isCollapsed)
    return !1;
  const t = n[0].getWalker({
    direction: "backward",
    singleCharacters: !0,
    ignoreElementEnd: !0
  });
  let e = 0;
  for (const { nextPosition: i, item: s } of t) {
    if (i.parent.is("$text")) {
      const r = i.parent.data, o = i.offset;
      if (kf(r, o) || Cf(r, o) || w_(r, o))
        continue;
      e++;
    } else (s.is("containerElement") || s.is("emptyElement")) && e++;
    if (e > 1)
      return !0;
  }
  return !1;
}
class Cn extends I {
  constructor() {
    super(...arguments);
    /**
    * Whether pressing backspace should trigger undo action
    */
    f(this, "_undoOnBackspace");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Delete";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.editing.view, s = i.document, r = e.model.document;
    i.addObserver(CB), this._undoOnBackspace = !1;
    const o = new zb(e, "forward");
    e.commands.add("deleteForward", o), e.commands.add("forwardDelete", o), e.commands.add("delete", new zb(e, "backward")), this.listenTo(s, "delete", (a, l) => {
      s.isComposing || l.preventDefault();
      const { direction: c, sequence: u, selectionToRemove: d, unit: h } = l, m = c === "forward" ? "deleteForward" : "delete", g = {
        sequence: u
      };
      if (h == "selection") {
        const p = Array.from(d.getRanges()).map((w) => e.editing.mapper.toModelRange(w));
        g.selection = e.model.createSelection(p);
      } else
        g.unit = h;
      e.execute(m, g), i.scrollToTheSelection();
    }, {
      priority: "low"
    }), this.listenTo(s, "keydown", (a, l) => {
      if (s.isComposing || l.keyCode != Ie.backspace || !r.selection.isCollapsed)
        return;
      const c = e.model.schema.getLimitElement(r.selection);
      e.model.createPositionAt(c, 0).isTouching(r.selection.getFirstPosition()) && l.preventDefault();
    }), this.editor.plugins.has("UndoEditing") && (this.listenTo(s, "delete", (a, l) => {
      this._undoOnBackspace && l.direction == "backward" && l.sequence == 1 && l.unit == "codePoint" && (this._undoOnBackspace = !1, e.execute("undo"), l.preventDefault(), a.stop());
    }, {
      context: "$capture"
    }), this.listenTo(r, "change", () => {
      this._undoOnBackspace = !1;
    }));
  }
  /**
  * If the next user action after calling this method is pressing backspace, it would undo the last change.
  *
  * Requires {@link module:undo/undoediting~UndoEditing} plugin. If not loaded, does nothing.
  */
  requestUndoOnBackspace() {
    this.editor.plugins.has("UndoEditing") && (this._undoOnBackspace = !0);
  }
}
class ek extends I {
  static get requires() {
    return [
      Xy,
      Cn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Typing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function tk(n, t) {
  let e = n.start;
  return {
    text: Array.from(n.getWalker({
      ignoreElementEnd: !1
    })).reduce((s, { item: r }) => r.is("$text") || r.is("$textProxy") ? s + r.data : (e = t.createPositionAfter(r), ""), ""),
    range: t.createRange(e, n.end)
  };
}
class Gf extends (/* @__PURE__ */ je()) {
  /**
  * Creates a text watcher instance.
  *
  * @param testCallback See {@link module:typing/textwatcher~TextWatcher#testCallback}.
  */
  constructor(e, i) {
    super();
    /**
    * The editor's model.
    */
    f(this, "model");
    /**
    * The function used to match the text.
    *
    * The test callback can return 3 values:
    *
    * * `false` if there is no match,
    * * `true` if there is a match,
    * * an object if there is a match and we want to pass some additional information to the {@link #event:matched:data} event.
    */
    f(this, "testCallback");
    /**
    * Whether there is a match currently.
    */
    f(this, "_hasMatch");
    this.model = e, this.testCallback = i, this._hasMatch = !1, this.set("isEnabled", !0), this.on("change:isEnabled", () => {
      this.isEnabled ? this._startListening() : (this.stopListening(e.document.selection), this.stopListening(e.document));
    }), this._startListening();
  }
  /**
  * Flag indicating whether there is a match currently.
  */
  get hasMatch() {
    return this._hasMatch;
  }
  /**
  * Starts listening to the editor for typing and selection events.
  */
  _startListening() {
    const i = this.model.document;
    this.listenTo(i.selection, "change:range", (s, { directChange: r }) => {
      if (r) {
        if (!i.selection.isCollapsed) {
          this.hasMatch && (this.fire("unmatched"), this._hasMatch = !1);
          return;
        }
        this._evaluateTextBeforeSelection("selection");
      }
    }), this.listenTo(i, "change:data", (s, r) => {
      r.isUndo || !r.isLocal || this._evaluateTextBeforeSelection("data", {
        batch: r
      });
    });
  }
  /**
  * Checks the editor content for matched text.
  *
  * @fires matched:data
  * @fires matched:selection
  * @fires unmatched
  *
  * @param suffix A suffix used for generating the event name.
  * @param data Data object for event.
  */
  _evaluateTextBeforeSelection(e, i = {}) {
    const s = this.model, o = s.document.selection, a = s.createRange(s.createPositionAt(o.focus.parent, 0), o.focus), { text: l, range: c } = tk(a, s), u = this.testCallback(l);
    if (!u && this.hasMatch && this.fire("unmatched"), this._hasMatch = !!u, u) {
      const d = Object.assign(i, {
        text: l,
        range: c
      });
      typeof u == "object" && Object.assign(d, u), this.fire(`matched:${e}`, d);
    }
  }
}
class sc extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A set of attributes to handle.
    */
    f(this, "attributes");
    /**
    * The current UID of the overridden gravity, as returned by
    * {@link module:engine/model/writer~Writer#overrideSelectionGravity}.
    */
    f(this, "_overrideUid");
    /**
    * A flag indicating that the automatic gravity restoration should not happen upon the next
    * gravity restoration.
    * {@link module:engine/model/selection~Selection#event:change:range} event.
    */
    f(this, "_isNextGravityRestorationSkipped", !1);
    this.attributes = /* @__PURE__ */ new Set(), this._overrideUid = null;
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TwoStepCaretMovement";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model, s = e.editing.view, r = e.locale, o = i.document.selection;
    this.listenTo(s.document, "arrowKey", (a, l) => {
      if (!o.isCollapsed || l.shiftKey || l.altKey || l.ctrlKey)
        return;
      const c = l.keyCode == Ie.arrowright, u = l.keyCode == Ie.arrowleft;
      if (!c && !u)
        return;
      const d = r.contentLanguageDirection;
      let h = !1;
      d === "ltr" && c || d === "rtl" && u ? h = this._handleForwardMovement(l) : h = this._handleBackwardMovement(l), h === !0 && a.stop();
    }, {
      context: "$text",
      priority: "highest"
    }), this.listenTo(o, "change:range", (a, l) => {
      if (this._isNextGravityRestorationSkipped) {
        this._isNextGravityRestorationSkipped = !1;
        return;
      }
      this._isGravityOverridden && (!l.directChange && Li(o.getFirstPosition(), this.attributes) || this._restoreGravity());
    }), this._enableClickingAfterNode(), this._enableInsertContentSelectionAttributesFixer(), this._handleDeleteContentAfterNode();
  }
  /**
  * Registers a given attribute for the two-step caret movement.
  *
  * @param attribute Name of the attribute to handle.
  */
  registerAttribute(e) {
    this.attributes.add(e);
  }
  /**
  * Updates the document selection and the view according to the two–step caret movement state
  * when moving **forwards**. Executed upon `keypress` in the {@link module:engine/view/view~View}.
  *
  * @param data Data of the key press.
  * @returns `true` when the handler prevented caret movement.
  */
  _handleForwardMovement(e) {
    const i = this.attributes, s = this.editor.model, r = s.document.selection, o = r.getFirstPosition();
    return this._isGravityOverridden || o.isAtStart && On(r, i) ? !1 : Li(o, i) ? (yo(e), On(r, i) && Li(o, i, !0) ? _o(s, i) : this._overrideGravity(), !0) : !1;
  }
  /**
  * Updates the document selection and the view according to the two–step caret movement state
  * when moving **backwards**. Executed upon `keypress` in the {@link module:engine/view/view~View}.
  *
  * @param data Data of the key press.
  * @returns `true` when the handler prevented caret movement
  */
  _handleBackwardMovement(e) {
    const i = this.attributes, s = this.editor.model, r = s.document.selection, o = r.getFirstPosition();
    return this._isGravityOverridden ? (yo(e), this._restoreGravity(), Li(o, i, !0) ? _o(s, i) : hl(s, i, o), !0) : o.isAtStart ? On(r, i) ? (yo(e), hl(s, i, o), !0) : !1 : !On(r, i) && Li(o, i, !0) ? (yo(e), hl(s, i, o), !0) : jb(o, i) ? o.isAtEnd && !On(r, i) && Li(o, i) ? (yo(e), hl(s, i, o), !0) : (this._isNextGravityRestorationSkipped = !0, this._overrideGravity(), !1) : !1;
  }
  /**
  * Starts listening to {@link module:engine/view/document~Document#event:mousedown} and
  * {@link module:engine/view/document~Document#event:selectionChange} and puts the selection before/after a 2-step node
  * if clicked at the beginning/ending of the 2-step node.
  *
  * The purpose of this action is to allow typing around the 2-step node directly after a click.
  *
  * See https://github.com/ckeditor/ckeditor5/issues/1016.
  */
  _enableClickingAfterNode() {
    const e = this.editor, i = e.model, s = i.document.selection, r = e.editing.view.document;
    e.editing.view.addObserver(Yc), e.editing.view.addObserver(jO);
    let o = !1, a = !1;
    this.listenTo(r, "touchstart", () => {
      a = !1, o = !0;
    }), this.listenTo(r, "mousedown", () => {
      a = !0;
    }), this.listenTo(r, "selectionChange", () => {
      const l = this.attributes;
      if (!a && !o || (a = !1, o = !1, !s.isCollapsed) || !On(s, l))
        return;
      const c = s.getFirstPosition();
      Li(c, l) && (c.isAtStart || Li(c, l, !0) ? _o(i, l) : this._isGravityOverridden || this._overrideGravity());
    });
  }
  /**
  * Starts listening to {@link module:engine/model/model~Model#event:insertContent} and corrects the model
  * selection attributes if the selection is at the end of a two-step node after inserting the content.
  *
  * The purpose of this action is to improve the overall UX because the user is no longer "trapped" by the
  * two-step attribute of the selection, and they can type a "clean" (`linkHref`–less) text right away.
  *
  * See https://github.com/ckeditor/ckeditor5/issues/6053.
  */
  _enableInsertContentSelectionAttributesFixer() {
    const i = this.editor.model, s = i.document.selection, r = this.attributes;
    this.listenTo(i, "insertContent", () => {
      const o = s.getFirstPosition();
      On(s, r) && Li(o, r) && _o(i, r);
    }, {
      priority: "low"
    });
  }
  /**
  * Starts listening to {@link module:engine/model/model~Model#deleteContent} and checks whether
  * removing a content right after the tow-step attribute.
  *
  * If so, the selection should not preserve the two-step attribute. However, if
  * the {@link module:typing/twostepcaretmovement~TwoStepCaretMovement} plugin is active and
  * the selection has the two-step attribute due to overridden gravity (at the end), the two-step attribute should stay untouched.
  *
  * The purpose of this action is to allow removing the link text and keep the selection outside the link.
  *
  * See https://github.com/ckeditor/ckeditor5/issues/7521.
  */
  _handleDeleteContentAfterNode() {
    const e = this.editor, i = e.model, s = i.document.selection, r = e.editing.view;
    let o = !1, a = !1;
    this.listenTo(r.document, "delete", (l, c) => {
      o = c.direction === "backward";
    }, {
      priority: "high"
    }), this.listenTo(i, "deleteContent", () => {
      if (!o)
        return;
      const l = s.getFirstPosition();
      a = On(s, this.attributes) && !jb(l, this.attributes);
    }, {
      priority: "high"
    }), this.listenTo(i, "deleteContent", () => {
      o && (o = !1, !a && e.model.enqueueChange(() => {
        const l = s.getFirstPosition();
        On(s, this.attributes) && Li(l, this.attributes) && (l.isAtStart || Li(l, this.attributes, !0) ? _o(i, this.attributes) : this._isGravityOverridden || this._overrideGravity());
      }));
    }, {
      priority: "low"
    });
  }
  /**
  * `true` when the gravity is overridden for the plugin.
  */
  get _isGravityOverridden() {
    return !!this._overrideUid;
  }
  /**
  * Overrides the gravity using the {@link module:engine/model/writer~Writer model writer}
  * and stores the information about this fact in the {@link #_overrideUid}.
  *
  * A shorthand for {@link module:engine/model/writer~Writer#overrideSelectionGravity}.
  */
  _overrideGravity() {
    this._overrideUid = this.editor.model.change((e) => e.overrideSelectionGravity());
  }
  /**
  * Restores the gravity using the {@link module:engine/model/writer~Writer model writer}.
  *
  * A shorthand for {@link module:engine/model/writer~Writer#restoreSelectionGravity}.
  */
  _restoreGravity() {
    this.editor.model.change((e) => {
      e.restoreSelectionGravity(this._overrideUid), this._overrideUid = null;
    });
  }
}
function On(n, t) {
  for (const e of t)
    if (n.hasAttribute(e))
      return !0;
  return !1;
}
function hl(n, t, e) {
  const i = e.nodeBefore;
  n.change((s) => {
    if (i) {
      const r = [], o = n.schema.isObject(i) && n.schema.isInline(i);
      for (const [a, l] of i.getAttributes())
        n.schema.checkAttribute("$text", a) && (!o || n.schema.getAttributeProperties(a).copyFromObject !== !1) && r.push([
          a,
          l
        ]);
      s.setSelectionAttribute(r);
    } else
      s.removeSelectionAttribute(t);
  });
}
function _o(n, t) {
  n.change((e) => {
    e.removeSelectionAttribute(t);
  });
}
function yo(n) {
  n.preventDefault();
}
function jb(n, t) {
  const e = n.getShiftedBy(-1);
  return Li(e, t);
}
function Li(n, t, e = !1) {
  const { nodeBefore: i, nodeAfter: s } = n;
  for (const r of t) {
    const o = i ? i.getAttribute(r) : void 0, a = s ? s.getAttribute(r) : void 0;
    if (!(e && (o === void 0 || a === void 0)) && a !== o)
      return !0;
  }
  return !1;
}
const Ub = {
  // Common symbols:
  copyright: {
    from: "(c)",
    to: "©"
  },
  registeredTrademark: {
    from: "(r)",
    to: "®"
  },
  trademark: {
    from: "(tm)",
    to: "™"
  },
  // Mathematical:
  oneHalf: {
    from: /(^|[^/a-z0-9])(1\/2)([^/a-z0-9])$/i,
    to: [
      null,
      "½",
      null
    ]
  },
  oneThird: {
    from: /(^|[^/a-z0-9])(1\/3)([^/a-z0-9])$/i,
    to: [
      null,
      "⅓",
      null
    ]
  },
  twoThirds: {
    from: /(^|[^/a-z0-9])(2\/3)([^/a-z0-9])$/i,
    to: [
      null,
      "⅔",
      null
    ]
  },
  oneForth: {
    from: /(^|[^/a-z0-9])(1\/4)([^/a-z0-9])$/i,
    to: [
      null,
      "¼",
      null
    ]
  },
  threeQuarters: {
    from: /(^|[^/a-z0-9])(3\/4)([^/a-z0-9])$/i,
    to: [
      null,
      "¾",
      null
    ]
  },
  lessThanOrEqual: {
    from: "<=",
    to: "≤"
  },
  greaterThanOrEqual: {
    from: ">=",
    to: "≥"
  },
  notEqual: {
    from: "!=",
    to: "≠"
  },
  arrowLeft: {
    from: "<-",
    to: "←"
  },
  arrowRight: {
    from: "->",
    to: "→"
  },
  // Typography:
  horizontalEllipsis: {
    from: "...",
    to: "…"
  },
  enDash: {
    from: /(^| )(--)( )$/,
    to: [
      null,
      "–",
      null
    ]
  },
  emDash: {
    from: /(^| )(---)( )$/,
    to: [
      null,
      "—",
      null
    ]
  },
  // Quotations:
  // English, US
  quotesPrimary: {
    from: fr('"'),
    to: [
      null,
      "“",
      null,
      "”"
    ]
  },
  quotesSecondary: {
    from: fr("'"),
    to: [
      null,
      "‘",
      null,
      "’"
    ]
  },
  // English, UK
  quotesPrimaryEnGb: {
    from: fr("'"),
    to: [
      null,
      "‘",
      null,
      "’"
    ]
  },
  quotesSecondaryEnGb: {
    from: fr('"'),
    to: [
      null,
      "“",
      null,
      "”"
    ]
  },
  // Polish
  quotesPrimaryPl: {
    from: fr('"'),
    to: [
      null,
      "„",
      null,
      "”"
    ]
  },
  quotesSecondaryPl: {
    from: fr("'"),
    to: [
      null,
      "‚",
      null,
      "’"
    ]
  }
}, Wb = {
  symbols: [
    "copyright",
    "registeredTrademark",
    "trademark"
  ],
  mathematical: [
    "oneHalf",
    "oneThird",
    "twoThirds",
    "oneForth",
    "threeQuarters",
    "lessThanOrEqual",
    "greaterThanOrEqual",
    "notEqual",
    "arrowLeft",
    "arrowRight"
  ],
  typography: [
    "horizontalEllipsis",
    "enDash",
    "emDash"
  ],
  quotes: [
    "quotesPrimary",
    "quotesSecondary"
  ]
}, EB = [
  "symbols",
  "mathematical",
  "typography",
  "quotes"
];
class SB extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      "Delete",
      "Input"
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TextTransformation";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("typing", {
      transformations: {
        include: EB
      }
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor.model.document.selection;
    e.on("change:range", () => {
      this.isEnabled = !e.anchor.parent.is("element", "codeBlock");
    }), this._enableTransformationWatchers();
  }
  /**
  * Create new TextWatcher listening to the editor for typing and selection events.
  */
  _enableTransformationWatchers() {
    const t = this.editor, e = t.model, i = t.plugins.get("Delete"), s = RB(t.config.get("typing.transformations")), r = (a) => {
      for (const l of s)
        if (l.from.test(a))
          return {
            normalizedTransformation: l
          };
    }, o = new Gf(t.model, r);
    o.on("matched:data", (a, l) => {
      if (!l.batch.isTyping)
        return;
      const { from: c, to: u } = l.normalizedTransformation, d = c.exec(l.text), h = u(d.slice(1)), m = l.range;
      let g = d.index;
      e.enqueueChange((p) => {
        for (let w = 1; w < d.length; w++) {
          const y = d[w], k = h[w - 1];
          if (k == null) {
            g += y.length;
            continue;
          }
          const C = m.start.getShiftedBy(g), E = e.createRange(C, C.getShiftedBy(y.length)), T = PB(C);
          e.insertContent(p.createText(k, T), E), g += k.length;
        }
        e.enqueueChange(() => {
          i.requestUndoOnBackspace();
        });
      });
    }), o.bind("isEnabled").to(this);
  }
}
function AB(n) {
  return typeof n == "string" ? new RegExp(`(${Uv(n)})$`) : n;
}
function IB(n) {
  return typeof n == "string" ? () => [
    n
  ] : n instanceof Array ? () => n : n;
}
function PB(n) {
  return (n.textNode ? n.textNode : n.nodeAfter).getAttributes();
}
function fr(n) {
  return new RegExp(`(^|\\s)(${n})([^${n}]*)(${n})$`);
}
function RB(n) {
  const t = n.extra || [], e = n.remove || [], i = (r) => !e.includes(r), s = n.include.concat(t).filter(i);
  return OB(s).filter(i).map((r) => typeof r == "string" && Ub[r] ? Ub[r] : r).filter((r) => typeof r == "object").map((r) => ({
    from: AB(r.from),
    to: IB(r.to)
  }));
}
function OB(n) {
  const t = /* @__PURE__ */ new Set();
  for (const e of n)
    if (typeof e == "string" && Wb[e])
      for (const i of Wb[e])
        t.add(i);
    else
      t.add(e);
  return Array.from(t);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function Na(n, t, e, i) {
  return i.createRange(yh(n, t, e, !0, i), yh(n, t, e, !1, i));
}
function yh(n, t, e, i, s) {
  let r = n.textNode || (i ? n.nodeBefore : n.nodeAfter), o = null;
  for (; r && r.getAttribute(t) == e; )
    o = r, r = i ? r.previousSibling : r.nextSibling;
  return o ? s.createPositionAt(o, i ? "before" : "after") : n;
}
function ik(n, t, e, i) {
  const s = n.editing.view, r = /* @__PURE__ */ new Set();
  s.document.registerPostFixer((o) => {
    const a = n.model.document.selection;
    let l = !1;
    if (a.hasAttribute(t)) {
      const c = Na(a.getFirstPosition(), t, a.getAttribute(t), n.model), u = n.editing.mapper.toViewRange(c);
      for (const d of u.getItems())
        d.is("element", e) && !d.hasClass(i) && (o.addClass(i, d), r.add(d), l = !0);
    }
    return l;
  }), n.conversion.for("editingDowncast").add((o) => {
    o.on("insert", a, {
      priority: "highest"
    }), o.on("remove", a, {
      priority: "highest"
    }), o.on("attribute", a, {
      priority: "highest"
    }), o.on("selection", a, {
      priority: "highest"
    });
    function a() {
      s.change((l) => {
        for (const c of r.values())
          l.removeClass(i, c), r.delete(c);
      });
    }
  });
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function ss(n, t, e, i) {
  let s, r = null;
  typeof i == "function" ? s = i : (r = n.commands.get(i), s = () => {
    n.execute(i);
  }), n.model.document.on("change:data", (o, a) => {
    if (r && !r.isEnabled || !t.isEnabled)
      return;
    const l = Xe(n.model.document.selection.getRanges());
    if (!l.isCollapsed || a.isUndo || !a.isLocal)
      return;
    const c = Array.from(n.model.document.differ.getChanges()), u = c[0];
    if (c.length != 1 || u.type !== "insert" || u.name != "$text" || u.length != 1)
      return;
    const d = u.position.parent;
    if (d.is("element", "codeBlock") || d.is("element", "listItem") && typeof i != "function" && ![
      "numberedList",
      "bulletedList",
      "todoList"
    ].includes(i) || r && r.value === !0)
      return;
    const h = d.getChild(0), m = n.model.createRangeOn(h);
    if (!m.containsRange(l) && !l.end.isEqual(m.end))
      return;
    const g = e.exec(h.data.substr(0, l.end.offset));
    g && n.model.enqueueChange((p) => {
      const w = p.createPositionAt(d, 0), y = p.createPositionAt(d, g[0].length), k = new ni(w, y);
      if (s({
        match: g
      }) !== !1) {
        p.remove(k);
        const E = n.model.document.selection.getFirstRange(), T = p.createRangeIn(d);
        d.isEmpty && !T.isEqual(E) && !T.containsRange(E, !0) && p.remove(d);
      }
      k.detach(), n.model.enqueueChange(() => {
        n.plugins.get("Delete").requestUndoOnBackspace();
      });
    });
  });
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function mr(n, t, e, i) {
  let s, r;
  e instanceof RegExp ? s = e : r = e, r = r || ((o) => {
    let a;
    const l = [], c = [];
    for (; (a = s.exec(o)) !== null && !(a && a.length < 4); ) {
      let { index: u, "1": d, "2": h, "3": m } = a;
      const g = d + h + m;
      u += a[0].length - g.length;
      const p = [
        u,
        u + d.length
      ], w = [
        u + d.length + h.length,
        u + d.length + h.length + m.length
      ];
      l.push(p), l.push(w), c.push([
        u + d.length,
        u + d.length + h.length
      ]);
    }
    return {
      remove: l,
      format: c
    };
  }), n.model.document.on("change:data", (o, a) => {
    if (a.isUndo || !a.isLocal || !t.isEnabled)
      return;
    const l = n.model, c = l.document.selection;
    if (!c.isCollapsed)
      return;
    const u = Array.from(l.document.differ.getChanges()), d = u[0];
    if (u.length != 1 || d.type !== "insert" || d.name != "$text" || d.length != 1)
      return;
    const h = c.focus, m = h.parent, { text: g, range: p } = VB(l.createRange(l.createPositionAt(m, 0), h), l), w = r(g), y = qb(p.start, w.format, l), k = qb(p.start, w.remove, l);
    y.length && k.length && l.enqueueChange((C) => {
      if (i(C, y) !== !1) {
        for (const T of k.reverse())
          C.remove(T);
        l.enqueueChange(() => {
          n.plugins.get("Delete").requestUndoOnBackspace();
        });
      }
    });
  });
}
function qb(n, t, e) {
  return t.filter((i) => i[0] !== void 0 && i[1] !== void 0).map((i) => e.createRange(n.getShiftedBy(i[0]), n.getShiftedBy(i[1])));
}
function VB(n, t) {
  let e = n.start;
  return {
    text: Array.from(n.getItems()).reduce((s, r) => !(r.is("$text") || r.is("$textProxy")) || r.getAttribute("code") ? (e = t.createPositionAfter(r), "") : s + r.data, ""),
    range: t.createRange(e, n.end)
  };
}
class LB extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Cn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Autoformat";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = this.editor.t;
    this._addListAutoformats(), this._addBasicStylesAutoformats(), this._addHeadingAutoformats(), this._addBlockQuoteAutoformats(), this._addCodeBlockAutoformats(), this._addHorizontalLineAutoformats(), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Revert autoformatting action"),
          keystroke: "Backspace"
        }
      ]
    });
  }
  /**
  * Adds autoformatting related to the {@link module:list/list~List}.
  *
  * When typed:
  * - `* ` or `- ` &ndash; A paragraph will be changed into a bulleted list.
  * - `1. ` or `1) ` &ndash; A paragraph will be changed into a numbered list ("1" can be any digit or a list of digits).
  * - `[] ` or `[ ] ` &ndash; A paragraph will be changed into a to-do list.
  * - `[x] ` or `[ x ] ` &ndash; A paragraph will be changed into a checked to-do list.
  */
  _addListAutoformats() {
    const t = this.editor.commands;
    t.get("bulletedList") && ss(this.editor, this, /^[*-]\s$/, "bulletedList"), t.get("numberedList") && ss(this.editor, this, /^1[.|)]\s$/, "numberedList"), t.get("todoList") && ss(this.editor, this, /^\[\s?\]\s$/, "todoList"), t.get("checkTodoList") && ss(this.editor, this, /^\[\s?x\s?\]\s$/, () => {
      this.editor.execute("todoList"), this.editor.execute("checkTodoList");
    });
  }
  /**
  * Adds autoformatting related to the {@link module:basic-styles/bold~Bold},
  * {@link module:basic-styles/italic~Italic}, {@link module:basic-styles/code~Code}
  * and {@link module:basic-styles/strikethrough~Strikethrough}
  *
  * When typed:
  * - `**foobar**` &ndash; `**` characters are removed and `foobar` is set to bold,
  * - `__foobar__` &ndash; `__` characters are removed and `foobar` is set to bold,
  * - `*foobar*` &ndash; `*` characters are removed and `foobar` is set to italic,
  * - `_foobar_` &ndash; `_` characters are removed and `foobar` is set to italic,
  * - ``` `foobar` &ndash; ``` ` ``` characters are removed and `foobar` is set to code,
  * - `~~foobar~~` &ndash; `~~` characters are removed and `foobar` is set to strikethrough.
  */
  _addBasicStylesAutoformats() {
    const t = this.editor.commands;
    if (t.get("bold")) {
      const e = fl(this.editor, "bold");
      mr(this.editor, this, /(?:^|\s)(\*\*)([^*]+)(\*\*)$/g, e), mr(this.editor, this, /(?:^|\s)(__)([^_]+)(__)$/g, e);
    }
    if (t.get("italic")) {
      const e = fl(this.editor, "italic");
      mr(this.editor, this, /(?:^|\s)(\*)([^*_]+)(\*)$/g, e), mr(this.editor, this, /(?:^|\s)(_)([^_]+)(_)$/g, e);
    }
    if (t.get("code")) {
      const e = fl(this.editor, "code");
      mr(this.editor, this, /(`)([^`]+)(`)$/g, e);
    }
    if (t.get("strikethrough")) {
      const e = fl(this.editor, "strikethrough");
      mr(this.editor, this, /(~~)([^~]+)(~~)$/g, e);
    }
  }
  /**
  * Adds autoformatting related to {@link module:heading/heading~Heading}.
  *
  * It is using a number at the end of the command name to associate it with the proper trigger:
  *
  * * `heading` with a `heading1` value will be executed when typing `#`,
  * * `heading` with a `heading2` value will be executed when typing `##`,
  * * ... up to `heading6` for `######`.
  */
  _addHeadingAutoformats() {
    const t = this.editor.commands.get("heading");
    t && t.modelElements.filter((e) => e.match(/^heading[1-6]$/)).forEach((e) => {
      const i = e[7], s = new RegExp(`^(#{${i}})\\s$`);
      ss(this.editor, this, s, () => {
        if (!t.isEnabled || t.value === e)
          return !1;
        this.editor.execute("heading", {
          value: e
        });
      });
    });
  }
  /**
  * Adds autoformatting related to {@link module:block-quote/blockquote~BlockQuote}.
  *
  * When typed:
  * * `> ` &ndash; A paragraph will be changed to a block quote.
  */
  _addBlockQuoteAutoformats() {
    this.editor.commands.get("blockQuote") && ss(this.editor, this, /^>\s$/, "blockQuote");
  }
  /**
  * Adds autoformatting related to {@link module:code-block/codeblock~CodeBlock}.
  *
  * When typed:
  * - `` ``` `` &ndash; A paragraph will be changed to a code block.
  */
  _addCodeBlockAutoformats() {
    const t = this.editor, e = t.model.document.selection;
    t.commands.get("codeBlock") && ss(t, this, /^```$/, () => {
      if (e.getFirstPosition().parent.is("element", "listItem"))
        return !1;
      this.editor.execute("codeBlock", {
        usePreviousLanguageChoice: !0
      });
    });
  }
  /**
  * Adds autoformatting related to {@link module:horizontal-line/horizontalline~HorizontalLine}.
  *
  * When typed:
  * - `` --- `` &ndash; Will be replaced with a horizontal line.
  */
  _addHorizontalLineAutoformats() {
    this.editor.commands.get("horizontalLine") && ss(this.editor, this, /^---$/, "horizontalLine");
  }
}
function fl(n, t) {
  return (e, i) => {
    if (!n.commands.get(t).isEnabled)
      return !1;
    const r = n.model.schema.getValidRanges(i, t);
    for (const o of r)
      e.setAttribute(t, !0, o);
    e.removeSelectionAttribute(t);
  };
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class BB extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The adapter is an object with a `save()` method. That method will be called whenever
    * the data changes. It might be called some time after the change,
    * since the event is throttled for performance reasons.
    */
    f(this, "adapter");
    /**
    * Debounced save method. The `save()` method is called the specified `waitingTime` after `debouncedSave()` is called,
    * unless a new action happens in the meantime.
    */
    f(this, "_debouncedSave");
    /**
    * The last saved document version.
    */
    f(this, "_lastDocumentVersion");
    /**
    * Promise used for asynchronous save calls.
    *
    * Created to handle the autosave call to an external data source. It resolves when that call is finished. It is re-used if
    * save is called before the promise has been resolved. It is set to `null` if there is no call in progress.
    */
    f(this, "_savePromise");
    /**
    * DOM emitter.
    */
    f(this, "_domEmitter");
    /**
    * The configuration of this plugins.
    */
    f(this, "_config");
    /**
    * Editor's pending actions manager.
    */
    f(this, "_pendingActions");
    /**
    * Informs whether there should be another autosave callback performed, immediately after current autosave callback finishes.
    *
    * This is set to `true` when there is a save request while autosave callback is already being processed
    * and the model has changed since the last save.
    */
    f(this, "_makeImmediateSave");
    /**
    * An action that will be added to the pending action manager for actions happening in that plugin.
    */
    f(this, "_action", null);
    const i = e.config.get("autosave") || {}, s = i.waitingTime || 1e3;
    this.set("state", "synchronized"), this._debouncedSave = vi(this._save.bind(this), s), this._lastDocumentVersion = e.model.document.version, this._savePromise = null, this._domEmitter = new (li())(), this._config = i, this._pendingActions = e.plugins.get(Hr), this._makeImmediateSave = !1;
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Autosave";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hr
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model.document;
    this.listenTo(e, "ready", () => {
      this.listenTo(i, "change:data", (s, r) => {
        this._saveCallbacks.length && r.isLocal && (this.state === "synchronized" && (this.state = "waiting", this._setPendingAction()), this.state === "waiting" && this._debouncedSave());
      });
    }), this.listenTo(e, "destroy", () => this._flush(), {
      priority: "highest"
    });
    /* istanbul ignore next -- @preserve */
    this._domEmitter.listenTo(window, "beforeunload", (s, r) => {
      this._pendingActions.hasAny && (r.returnValue = this._pendingActions.first.message);
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._domEmitter.stopListening(), super.destroy();
  }
  /**
  * Immediately calls autosave callback. All previously queued (debounced) callbacks are cleared. If there is already an autosave
  * callback in progress, then the requested save will be performed immediately after the current callback finishes.
  *
  * @returns A promise that will be resolved when the autosave callback is finished.
  */
  save() {
    return this._debouncedSave.cancel(), this._save();
  }
  /**
  * Invokes the remaining `_save()` method call.
  */
  _flush() {
    this._debouncedSave.flush();
  }
  /**
  * If the adapter is set and a new document version exists,
  * the `_save()` method creates a pending action and calls the `adapter.save()` method.
  * It waits for the result and then removes the created pending action.
  *
  * @returns A promise that will be resolved when the autosave callback is finished.
  */
  _save() {
    return this._savePromise ? (this._makeImmediateSave = this.editor.model.document.version > this._lastDocumentVersion, this._savePromise) : (this._setPendingAction(), this.state = "saving", this._lastDocumentVersion = this.editor.model.document.version, this._savePromise = Promise.resolve().then(() => Promise.all(this._saveCallbacks.map((e) => e(this.editor)))).finally(() => {
      this._savePromise = null;
    }).then(() => {
      if (this._makeImmediateSave)
        return this._makeImmediateSave = !1, this._save();
      this.editor.model.document.version > this._lastDocumentVersion ? (this.state = "waiting", this._debouncedSave()) : (this.state = "synchronized", this._pendingActions.remove(this._action), this._action = null);
    }).catch((e) => {
      throw this.state = "error", this.state = "saving", this._debouncedSave(), e;
    }), this._savePromise);
  }
  /**
  * Creates a pending action if it is not set already.
  */
  _setPendingAction() {
    const e = this.editor.t;
    this._action || (this._action = this._pendingActions.add(e("Saving changes")));
  }
  /**
  * Saves callbacks.
  */
  get _saveCallbacks() {
    const e = [];
    return this.adapter && this.adapter.save && e.push(this.adapter.save), this._config.save && e.push(this._config.save), e;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class Js extends he {
  /**
  * @param attributeKey Attribute that will be set by the command.
  */
  constructor(e, i) {
    super(e);
    /**
    * The attribute that will be set by the command.
    */
    f(this, "attributeKey");
    this.attributeKey = i;
  }
  /**
  * Updates the command's {@link #value} and {@link #isEnabled} based on the current selection.
  */
  refresh() {
    const e = this.editor.model, i = e.document;
    this.value = this._getValueFromFirstAllowedNode(), this.isEnabled = e.schema.checkAttributeInSelection(i.selection, this.attributeKey);
  }
  /**
  * Executes the command &ndash; applies the attribute to the selection or removes it from the selection.
  *
  * If the command is active (`value == true`), it will remove attributes. Otherwise, it will set attributes.
  *
  * The execution result differs, depending on the {@link module:engine/model/document~Document#selection}:
  *
  * * If the selection is on a range, the command applies the attribute to all nodes in that range
  * (if they are allowed to have this attribute by the {@link module:engine/model/schema~Schema schema}).
  * * If the selection is collapsed in a non-empty node, the command applies the attribute to the
  * {@link module:engine/model/document~Document#selection} itself (note that typed characters copy attributes from the selection).
  * * If the selection is collapsed in an empty node, the command applies the attribute to the parent node of the selection (note
  * that the selection inherits all attributes from a node if it is in an empty node).
  *
  * @fires execute
  * @param options Command options.
  * @param options.forceValue If set, it will force the command behavior. If `true`,
  * the command will apply the attribute, otherwise the command will remove the attribute.
  * If not set, the command will look for its current value to decide what it should do.
  */
  execute(e = {}) {
    const i = this.editor.model, r = i.document.selection, o = e.forceValue === void 0 ? !this.value : e.forceValue;
    i.change((a) => {
      if (r.isCollapsed)
        o ? a.setSelectionAttribute(this.attributeKey, !0) : a.removeSelectionAttribute(this.attributeKey);
      else {
        const l = i.schema.getValidRanges(r.getRanges(), this.attributeKey);
        for (const c of l)
          o ? a.setAttribute(this.attributeKey, o, c) : a.removeAttribute(this.attributeKey, c);
      }
    });
  }
  /**
  * Checks the attribute value of the first node in the selection that allows the attribute.
  * For the collapsed selection returns the selection attribute.
  *
  * @returns The attribute value.
  */
  _getValueFromFirstAllowedNode() {
    const e = this.editor.model, i = e.schema, s = e.document.selection;
    if (s.isCollapsed)
      return s.hasAttribute(this.attributeKey);
    for (const r of s.getRanges())
      for (const o of r.getItems())
        if (i.checkAttribute(o, this.attributeKey))
          return o.hasAttribute(this.attributeKey);
    return !1;
  }
}
const gr = "bold";
class MB extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BoldEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: gr
    }), t.model.schema.setAttributeProperties(gr, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: gr,
      view: "strong",
      upcastAlso: [
        "b",
        (i) => {
          const s = i.getStyle("font-weight");
          return s && (s == "bold" || Number(s) >= 600) ? {
            name: !0,
            styles: [
              "font-weight"
            ]
          } : null;
        }
      ]
    }), t.commands.add(gr, new Js(t, gr)), t.keystrokes.set("CTRL+B", gr), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Bold text"),
          keystroke: "CTRL+B"
        }
      ]
    });
  }
}
function Xs({ editor: n, commandName: t, plugin: e, icon: i, label: s, keystroke: r }) {
  return (o) => {
    const a = n.commands.get(t), l = new o(n.locale);
    return l.set({
      label: s,
      icon: i,
      keystroke: r,
      isToggleable: !0
    }), l.bind("isEnabled").to(a, "isEnabled"), l.bind("isOn").to(a, "value"), l instanceof Fe ? l.set({
      role: "menuitemcheckbox"
    }) : l.set({
      tooltip: !0
    }), e.listenTo(l, "execute", () => {
      n.execute(t), n.editing.view.focus();
    }), l;
  };
}
const od = "bold";
class NB extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BoldUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, i = Xs({
      editor: t,
      commandName: od,
      plugin: this,
      icon: Z.bold,
      label: e("Bold"),
      keystroke: "CTRL+B"
    });
    t.ui.componentFactory.add(od, () => i(le)), t.ui.componentFactory.add("menuBar:" + od, () => i(Fe));
  }
}
class FB extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      MB,
      NB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Bold";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const xs = "code", DB = "ck-code_selected";
class $B extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      sc
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: xs
    }), t.model.schema.setAttributeProperties(xs, {
      isFormatting: !0,
      copyOnEnter: !1
    }), t.conversion.attributeToElement({
      model: xs,
      view: "code"
    }), t.commands.add(xs, new Js(t, xs)), t.plugins.get(sc).registerAttribute(xs), ik(t, xs, "code", DB), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Move out of an inline code style"),
          keystroke: [
            [
              "arrowleft",
              "arrowleft"
            ],
            [
              "arrowright",
              "arrowright"
            ]
          ]
        }
      ]
    });
  }
}
var zB = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m12.5 5.7 5.2 3.9v1.3l-5.6 4c-.1.2-.3.2-.5.2-.3-.1-.6-.7-.6-1l.3-.4 4.7-3.5L11.5 7l-.2-.2c-.1-.3-.1-.6 0-.8.2-.2.5-.4.8-.4a.8.8 0 0 1 .4.1zm-5.2 0L2 9.6v1.3l5.6 4c.1.2.3.2.5.2.3-.1.7-.7.6-1 0-.1 0-.3-.2-.4l-5-3.5L8.2 7l.2-.2c.1-.3.1-.6 0-.8-.2-.2-.5-.4-.8-.4a.8.8 0 0 0-.3.1z"/></svg>';
const ad = "code";
class HB extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, i = Xs({
      editor: t,
      commandName: ad,
      plugin: this,
      icon: zB,
      label: e("Code")
    });
    t.ui.componentFactory.add(ad, () => i(le)), t.ui.componentFactory.add("menuBar:" + ad, () => i(Fe));
  }
}
class jB extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      $B,
      HB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Code";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const pr = "italic";
class UB extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ItalicEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: pr
    }), t.model.schema.setAttributeProperties(pr, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: pr,
      view: "i",
      upcastAlso: [
        "em",
        {
          styles: {
            "font-style": "italic"
          }
        }
      ]
    }), t.commands.add(pr, new Js(t, pr)), t.keystrokes.set("CTRL+I", pr), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Italic text"),
          keystroke: "CTRL+I"
        }
      ]
    });
  }
}
var WB = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m9.586 14.633.021.004c-.036.335.095.655.393.962.082.083.173.15.274.201h1.474a.6.6 0 1 1 0 1.2H5.304a.6.6 0 0 1 0-1.2h1.15c.474-.07.809-.182 1.005-.334.157-.122.291-.32.404-.597l2.416-9.55a1.053 1.053 0 0 0-.281-.823 1.12 1.12 0 0 0-.442-.296H8.15a.6.6 0 0 1 0-1.2h6.443a.6.6 0 1 1 0 1.2h-1.195c-.376.056-.65.155-.823.296-.215.175-.423.439-.623.79l-2.366 9.347z"/></svg>';
const ld = "italic";
class qB extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ItalicUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, i = Xs({
      editor: t,
      commandName: ld,
      plugin: this,
      icon: WB,
      keystroke: "CTRL+I",
      label: e("Italic")
    });
    t.ui.componentFactory.add(ld, () => i(le)), t.ui.componentFactory.add("menuBar:" + ld, () => i(Fe));
  }
}
class GB extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      UB,
      qB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Italic";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const ko = "strikethrough";
class KB extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "StrikethroughEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: ko
    }), t.model.schema.setAttributeProperties(ko, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: ko,
      view: "s",
      upcastAlso: [
        "del",
        "strike",
        {
          styles: {
            "text-decoration": "line-through"
          }
        }
      ]
    }), t.commands.add(ko, new Js(t, ko)), t.keystrokes.set("CTRL+SHIFT+X", "strikethrough"), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Strikethrough text"),
          keystroke: "CTRL+SHIFT+X"
        }
      ]
    });
  }
}
var ZB = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M7 16.4c-.8-.4-1.5-.9-2.2-1.5a.6.6 0 0 1-.2-.5l.3-.6h1c1 1.2 2.1 1.7 3.7 1.7 1 0 1.8-.3 2.3-.6.6-.4.6-1.2.6-1.3.2-1.2-.9-2.1-.9-2.1h2.1c.3.7.4 1.2.4 1.7v.8l-.6 1.2c-.6.8-1.1 1-1.6 1.2a6 6 0 0 1-2.4.6c-1 0-1.8-.3-2.5-.6zM6.8 9 6 8.3c-.4-.5-.5-.8-.5-1.6 0-.7.1-1.3.5-1.8.4-.6 1-1 1.6-1.3a6.3 6.3 0 0 1 4.7 0 4 4 0 0 1 1.7 1l.3.7c0 .1.2.4-.2.7-.4.2-.9.1-1 0a3 3 0 0 0-1.2-1c-.4-.2-1-.3-2-.4-.7 0-1.4.2-2 .6-.8.6-1 .8-1 1.5 0 .8.5 1 1.2 1.5.6.4 1.1.7 1.9 1H6.8z"/><path d="M3 10.5V9h14v1.5z"/></svg>';
const cd = "strikethrough";
class YB extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "StrikethroughUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, i = Xs({
      editor: t,
      commandName: cd,
      plugin: this,
      icon: ZB,
      keystroke: "CTRL+SHIFT+X",
      label: e("Strikethrough")
    });
    t.ui.componentFactory.add(cd, () => i(le)), t.ui.componentFactory.add("menuBar:" + cd, () => i(Fe));
  }
}
class JB extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      KB,
      YB
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Strikethrough";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const Co = "subscript";
class XB extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SubscriptEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.extend("$text", {
      allowAttributes: Co
    }), t.model.schema.setAttributeProperties(Co, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: Co,
      view: "sub",
      upcastAlso: [
        {
          styles: {
            "vertical-align": "sub"
          }
        }
      ]
    }), t.commands.add(Co, new Js(t, Co));
  }
}
var QB = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m7.03 10.349 3.818-3.819a.8.8 0 1 1 1.132 1.132L8.16 11.48l3.819 3.818a.8.8 0 1 1-1.132 1.132L7.03 12.61l-3.818 3.82a.8.8 0 1 1-1.132-1.132L5.9 11.48 2.08 7.662A.8.8 0 1 1 3.212 6.53l3.818 3.82zm8.147 7.829h2.549c.254 0 .447.05.58.152a.49.49 0 0 1 .201.413.54.54 0 0 1-.159.393c-.105.108-.266.162-.48.162h-3.594c-.245 0-.435-.066-.572-.197a.621.621 0 0 1-.205-.463c0-.114.044-.265.132-.453a1.62 1.62 0 0 1 .288-.444c.433-.436.824-.81 1.172-1.122.348-.312.597-.517.747-.615.267-.183.49-.368.667-.553.177-.185.312-.375.405-.57.093-.194.139-.384.139-.57a1.008 1.008 0 0 0-.554-.917 1.197 1.197 0 0 0-.56-.133c-.426 0-.761.182-1.005.546a2.332 2.332 0 0 0-.164.39 1.609 1.609 0 0 1-.258.488c-.096.114-.237.17-.423.17a.558.558 0 0 1-.405-.156.568.568 0 0 1-.161-.427c0-.218.05-.446.151-.683.101-.238.252-.453.452-.646s.454-.349.762-.467a2.998 2.998 0 0 1 1.081-.178c.498 0 .923.076 1.274.228a1.916 1.916 0 0 1 1.004 1.032 1.984 1.984 0 0 1-.156 1.794c-.2.32-.405.572-.613.754-.208.182-.558.468-1.048.857-.49.39-.826.691-1.008.906a2.703 2.703 0 0 0-.24.309z"/></svg>';
const ud = "subscript";
class eM extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SubscriptUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, i = Xs({
      editor: t,
      commandName: ud,
      plugin: this,
      icon: QB,
      label: e("Subscript")
    });
    t.ui.componentFactory.add(ud, () => i(le)), t.ui.componentFactory.add("menuBar:" + ud, () => i(Fe));
  }
}
class tM extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      XB,
      eM
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Subscript";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const xo = "superscript";
class iM extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SuperscriptEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.extend("$text", {
      allowAttributes: xo
    }), t.model.schema.setAttributeProperties(xo, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: xo,
      view: "sup",
      upcastAlso: [
        {
          styles: {
            "vertical-align": "super"
          }
        }
      ]
    }), t.commands.add(xo, new Js(t, xo));
  }
}
var nM = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M15.677 8.678h2.549c.254 0 .447.05.58.152a.49.49 0 0 1 .201.413.54.54 0 0 1-.159.393c-.105.108-.266.162-.48.162h-3.594c-.245 0-.435-.066-.572-.197a.621.621 0 0 1-.205-.463c0-.114.044-.265.132-.453a1.62 1.62 0 0 1 .288-.444c.433-.436.824-.81 1.172-1.122.348-.312.597-.517.747-.615.267-.183.49-.368.667-.553.177-.185.312-.375.405-.57.093-.194.139-.384.139-.57a1.008 1.008 0 0 0-.554-.917 1.197 1.197 0 0 0-.56-.133c-.426 0-.761.182-1.005.546a2.332 2.332 0 0 0-.164.39 1.609 1.609 0 0 1-.258.488c-.096.114-.237.17-.423.17a.558.558 0 0 1-.405-.156.568.568 0 0 1-.161-.427c0-.218.05-.446.151-.683.101-.238.252-.453.452-.646s.454-.349.762-.467a2.998 2.998 0 0 1 1.081-.178c.498 0 .923.076 1.274.228a1.916 1.916 0 0 1 1.004 1.032 1.984 1.984 0 0 1-.156 1.794c-.2.32-.405.572-.613.754-.208.182-.558.468-1.048.857-.49.39-.826.691-1.008.906a2.703 2.703 0 0 0-.24.309zM7.03 10.349l3.818-3.819a.8.8 0 1 1 1.132 1.132L8.16 11.48l3.819 3.818a.8.8 0 1 1-1.132 1.132L7.03 12.61l-3.818 3.82a.8.8 0 1 1-1.132-1.132L5.9 11.48 2.08 7.662A.8.8 0 1 1 3.212 6.53l3.818 3.82z"/></svg>';
const dd = "superscript";
class sM extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SuperscriptUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, i = Xs({
      editor: t,
      commandName: dd,
      plugin: this,
      icon: nM,
      label: e("Superscript")
    });
    t.ui.componentFactory.add(dd, () => i(le)), t.ui.componentFactory.add("menuBar:" + dd, () => i(Fe));
  }
}
class rM extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      iM,
      sM
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Superscript";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const To = "underline";
class oM extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "UnderlineEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.model.schema.extend("$text", {
      allowAttributes: To
    }), t.model.schema.setAttributeProperties(To, {
      isFormatting: !0,
      copyOnEnter: !0
    }), t.conversion.attributeToElement({
      model: To,
      view: "u",
      upcastAlso: {
        styles: {
          "text-decoration": "underline"
        }
      }
    }), t.commands.add(To, new Js(t, To)), t.keystrokes.set("CTRL+U", "underline"), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Underline text"),
          keystroke: "CTRL+U"
        }
      ]
    });
  }
}
var aM = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3 18v-1.5h14V18zm2.2-8V3.6c0-.4.4-.6.8-.6.3 0 .7.2.7.6v6.2c0 2 1.3 2.8 3.2 2.8 1.9 0 3.4-.9 3.4-2.9V3.6c0-.3.4-.5.8-.5.3 0 .7.2.7.5V10c0 2.7-2.2 4-4.9 4-2.6 0-4.7-1.2-4.7-4z"/></svg>';
const hd = "underline";
class lM extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "UnderlineUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale.t, i = Xs({
      editor: t,
      commandName: hd,
      plugin: this,
      icon: aM,
      label: e("Underline"),
      keystroke: "CTRL+U"
    });
    t.ui.componentFactory.add(hd, () => i(le)), t.ui.componentFactory.add("menuBar:" + hd, () => i(Fe));
  }
}
class cM extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      oM,
      lM
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Underline";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function* nk(n, t) {
  for (const e of t)
    e && n.getAttributeProperties(e[0]).copyOnEnter && (yield e);
}
class uM extends he {
  /**
  * @inheritDoc
  */
  execute() {
    this.editor.model.change((t) => {
      this.enterBlock(t), this.fire("afterExecute", {
        writer: t
      });
    });
  }
  /**
  * Splits a block where the document selection is placed, in the way how the <kbd>Enter</kbd> key is expected to work:
  *
  * ```
  * <p>Foo[]bar</p>   ->   <p>Foo</p><p>[]bar</p>
  * <p>Foobar[]</p>   ->   <p>Foobar</p><p>[]</p>
  * <p>Fo[ob]ar</p>   ->   <p>Fo</p><p>[]ar</p>
  * ```
  *
  * In some cases, the split will not happen:
  *
  * ```
  * // The selection parent is a limit element:
  * <figcaption>A[bc]d</figcaption>   ->   <figcaption>A[]d</figcaption>
  *
  * // The selection spans over multiple elements:
  * <h>x[x</h><p>y]y<p>   ->   <h>x</h><p>[]y</p>
  * ```
  *
  * @param writer Writer to use when performing the enter action.
  * @returns Boolean indicating if the block was split.
  */
  enterBlock(t) {
    const e = this.editor.model, i = e.document.selection, s = e.schema, r = i.isCollapsed, o = i.getFirstRange(), a = o.start.parent, l = o.end.parent;
    if (s.isLimit(a) || s.isLimit(l))
      return !r && a == l && e.deleteContent(i), !1;
    if (r) {
      const c = nk(t.model.schema, i.getAttributes());
      return Gb(t, o.start), t.setSelectionAttribute(c), !0;
    } else {
      const c = !(o.start.isAtStart && o.end.isAtEnd), u = a == l;
      if (e.deleteContent(i, {
        leaveUnmerged: c
      }), c) {
        if (u)
          return Gb(t, i.focus), !0;
        t.setSelection(l, 0);
      }
    }
    return !1;
  }
}
function Gb(n, t) {
  n.split(t), n.setSelection(t.parent.nextSibling, 0);
}
const dM = {
  insertParagraph: {
    isSoft: !1
  },
  insertLineBreak: {
    isSoft: !0
  }
};
class sk extends sn {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t);
    const e = this.document;
    let i = !1;
    e.on("keydown", (s, r) => {
      i = r.shiftKey;
    }), e.on("beforeinput", (s, r) => {
      if (!this.isEnabled)
        return;
      let o = r.inputType;
      be.isSafari && i && o == "insertParagraph" && (o = "insertLineBreak");
      const a = r.domEvent, l = dM[o];
      if (!l)
        return;
      const c = new io(e, "enter", r.targetRanges[0]);
      e.fire(c, new zr(t, a, {
        isSoft: l.isSoft
      })), c.stop.called && s.stop();
    });
  }
  /**
  * @inheritDoc
  */
  observe() {
  }
  /**
  * @inheritDoc
  */
  stopObserving() {
  }
}
class Fa extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Enter";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  init() {
    const t = this.editor, e = t.editing.view, i = e.document, s = this.editor.t;
    e.addObserver(sk), t.commands.add("enter", new uM(t)), this.listenTo(i, "enter", (r, o) => {
      i.isComposing || o.preventDefault(), !o.isSoft && (t.execute("enter"), e.scrollToTheSelection());
    }, {
      priority: "low"
    }), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: s("Insert a hard break (a new paragraph)"),
          keystroke: "Enter"
        }
      ]
    });
  }
}
class hM extends he {
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = t.document;
    t.change((i) => {
      mM(t, i, e.selection), this.fire("afterExecute", {
        writer: i
      });
    });
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document;
    this.isEnabled = fM(t.schema, e.selection);
  }
}
function fM(n, t) {
  if (t.rangeCount > 1)
    return !1;
  const e = t.anchor;
  if (!e || !n.checkChild(e, "softBreak"))
    return !1;
  const i = t.getFirstRange(), s = i.start.parent, r = i.end.parent;
  return !((kh(s, n) || kh(r, n)) && s !== r);
}
function mM(n, t, e) {
  const i = e.isCollapsed, s = e.getFirstRange(), r = s.start.parent, o = s.end.parent, a = r == o;
  if (i) {
    const l = nk(n.schema, e.getAttributes());
    Kb(n, t, s.end), t.removeSelectionAttribute(e.getAttributeKeys()), t.setSelectionAttribute(l);
  } else {
    const l = !(s.start.isAtStart && s.end.isAtEnd);
    n.deleteContent(e, {
      leaveUnmerged: l
    }), a ? Kb(n, t, e.focus) : l && t.setSelection(o, 0);
  }
}
function Kb(n, t, e) {
  const i = t.createElement("softBreak");
  n.insertContent(i, e), t.setSelection(i, "after");
}
function kh(n, t) {
  return n.is("rootElement") ? !1 : t.isLimit(n) || kh(n.parent, t);
}
class rk extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ShiftEnter";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  init() {
    const t = this.editor, e = t.model.schema, i = t.conversion, s = t.editing.view, r = s.document, o = this.editor.t;
    e.register("softBreak", {
      allowWhere: "$text",
      isInline: !0
    }), i.for("upcast").elementToElement({
      model: "softBreak",
      view: "br"
    }), i.for("downcast").elementToElement({
      model: "softBreak",
      view: (a, { writer: l }) => l.createEmptyElement("br")
    }), s.addObserver(sk), t.commands.add("shiftEnter", new hM(t)), this.listenTo(r, "enter", (a, l) => {
      r.isComposing || l.preventDefault(), l.isSoft && (t.execute("shiftEnter"), s.scrollToTheSelection());
    }, {
      priority: "low"
    }), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: o("Insert a soft break (a <code>&lt;br&gt;</code> element)"),
          keystroke: "Shift+Enter"
        }
      ]
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class gM extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    this.value = this._getValue(), this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the command. When the command {@link #value is on}, all top-most block quotes within
  * the selection will be removed. If it is off, all selected blocks will be wrapped with
  * a block quote.
  *
  * @fires execute
  * @param options Command options.
  * @param options.forceValue If set, it will force the command behavior. If `true`, the command will apply a block quote,
  * otherwise the command will remove the block quote. If not set, the command will act basing on its current value.
  */
  execute(t = {}) {
    const e = this.editor.model, i = e.schema, s = e.document.selection, r = Array.from(s.getSelectedBlocks()), o = t.forceValue === void 0 ? !this.value : t.forceValue;
    e.change((a) => {
      if (!o)
        this._removeQuote(a, r.filter(ml));
      else {
        const l = r.filter((c) => ml(c) || Yb(i, c));
        this._applyQuote(a, l);
      }
    });
  }
  /**
  * Checks the command's {@link #value}.
  */
  _getValue() {
    const t = this.editor.model.document.selection, e = Xe(t.getSelectedBlocks());
    return !!(e && ml(e));
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    if (this.value)
      return !0;
    const t = this.editor.model.document.selection, e = this.editor.model.schema, i = Xe(t.getSelectedBlocks());
    return i ? Yb(e, i) : !1;
  }
  /**
  * Removes the quote from given blocks.
  *
  * If blocks which are supposed to be "unquoted" are in the middle of a quote,
  * start it or end it, then the quote will be split (if needed) and the blocks
  * will be moved out of it, so other quoted blocks remained quoted.
  */
  _removeQuote(t, e) {
    Zb(t, e).reverse().forEach((i) => {
      if (i.start.isAtStart && i.end.isAtEnd) {
        t.unwrap(i.start.parent);
        return;
      }
      if (i.start.isAtStart) {
        const r = t.createPositionBefore(i.start.parent);
        t.move(i, r);
        return;
      }
      i.end.isAtEnd || t.split(i.end);
      const s = t.createPositionAfter(i.end.parent);
      t.move(i, s);
    });
  }
  /**
  * Applies the quote to given blocks.
  */
  _applyQuote(t, e) {
    const i = [];
    Zb(t, e).reverse().forEach((s) => {
      let r = ml(s.start);
      r || (r = t.createElement("blockQuote"), t.wrap(s, r)), i.push(r);
    }), i.reverse().reduce((s, r) => s.nextSibling == r ? (t.merge(t.createPositionAfter(s)), s) : r);
  }
}
function ml(n) {
  return n.parent.name == "blockQuote" ? n.parent : null;
}
function Zb(n, t) {
  let e, i = 0;
  const s = [];
  for (; i < t.length; ) {
    const r = t[i], o = t[i + 1];
    e || (e = n.createPositionBefore(r)), (!o || r.nextSibling != o) && (s.push(n.createRange(e, n.createPositionAfter(r))), e = null), i++;
  }
  return s;
}
function Yb(n, t) {
  const e = n.checkChild(t.parent, "blockQuote"), i = n.checkChild([
    "$root",
    "blockQuote"
  ], t);
  return e && i;
}
class pM extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BlockQuoteEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Fa,
      Cn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model.schema;
    t.commands.add("blockQuote", new gM(t)), e.register("blockQuote", {
      inheritAllFrom: "$container"
    }), t.conversion.elementToElement({
      model: "blockQuote",
      view: "blockquote"
    }), t.model.document.registerPostFixer((o) => {
      const a = t.model.document.differ.getChanges();
      for (const l of a)
        if (l.type == "insert") {
          const c = l.position.nodeAfter;
          if (!c)
            continue;
          if (c.is("element", "blockQuote") && c.isEmpty)
            return o.remove(c), !0;
          if (c.is("element", "blockQuote") && !e.checkChild(l.position, c))
            return o.unwrap(c), !0;
          if (c.is("element")) {
            const u = o.createRangeIn(c);
            for (const d of u.getItems())
              if (d.is("element", "blockQuote") && !e.checkChild(o.createPositionBefore(d), d))
                return o.unwrap(d), !0;
          }
        } else if (l.type == "remove") {
          const c = l.position.parent;
          if (c.is("element", "blockQuote") && c.isEmpty)
            return o.remove(c), !0;
        }
      return !1;
    });
    const i = this.editor.editing.view.document, s = t.model.document.selection, r = t.commands.get("blockQuote");
    this.listenTo(i, "enter", (o, a) => {
      if (!s.isCollapsed || !r.value)
        return;
      s.getLastPosition().parent.isEmpty && (t.execute("blockQuote"), t.editing.view.scrollToTheSelection(), a.preventDefault(), o.stop());
    }, {
      context: "blockquote"
    }), this.listenTo(i, "delete", (o, a) => {
      if (a.direction != "backward" || !s.isCollapsed || !r.value)
        return;
      const l = s.getLastPosition().parent;
      l.isEmpty && !l.previousSibling && (t.execute("blockQuote"), t.editing.view.scrollToTheSelection(), a.preventDefault(), o.stop());
    }, {
      context: "blockquote"
    });
  }
}
class bM extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BlockQuoteUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add("blockQuote", () => {
      const e = this._createButton(le);
      return e.set({
        tooltip: !0
      }), e;
    }), t.ui.componentFactory.add("menuBar:blockQuote", () => {
      const e = this._createButton(Fe);
      return e.set({
        role: "menuitemcheckbox"
      }), e;
    });
  }
  /**
  * Creates a button for block quote command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, i = e.locale, s = e.commands.get("blockQuote"), r = new t(e.locale), o = i.t;
    return r.set({
      label: o("Block quote"),
      icon: Z.quote,
      isToggleable: !0
    }), r.bind("isEnabled").to(s, "isEnabled"), r.bind("isOn").to(s, "value"), this.listenTo(r, "execute", () => {
      e.execute("blockQuote"), e.editing.view.focus();
    }), r;
  }
}
class wM extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pM,
      bM
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BlockQuote";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class vM extends (/* @__PURE__ */ Je()) {
  constructor() {
    super(...arguments);
    f(this, "_stack", []);
  }
  /**
  * Adds highlight descriptor to the stack.
  *
  * @fires change:top
  */
  add(e, i) {
    const s = this._stack, r = s[0];
    this._insertDescriptor(e);
    const o = s[0];
    r !== o && !fd(r, o) && this.fire("change:top", {
      oldDescriptor: r,
      newDescriptor: o,
      writer: i
    });
  }
  /**
  * Removes highlight descriptor from the stack.
  *
  * @fires change:top
  * @param id Id of the descriptor to remove.
  */
  remove(e, i) {
    const s = this._stack, r = s[0];
    this._removeDescriptor(e);
    const o = s[0];
    r !== o && !fd(r, o) && this.fire("change:top", {
      oldDescriptor: r,
      newDescriptor: o,
      writer: i
    });
  }
  /**
  * Inserts a given descriptor in correct place in the stack. It also takes care about updating information
  * when descriptor with same id is already present.
  */
  _insertDescriptor(e) {
    const i = this._stack, s = i.findIndex((o) => o.id === e.id);
    if (fd(e, i[s]))
      return;
    s > -1 && i.splice(s, 1);
    let r = 0;
    for (; i[r] && _M(i[r], e); )
      r++;
    i.splice(r, 0, e);
  }
  /**
  * Removes descriptor with given id from the stack.
  *
  * @param id Descriptor's id.
  */
  _removeDescriptor(e) {
    const i = this._stack, s = i.findIndex((r) => r.id === e);
    s > -1 && i.splice(s, 1);
  }
}
function fd(n, t) {
  return n && t && n.priority == t.priority && rc(n.classes) == rc(t.classes);
}
function _M(n, t) {
  return n.priority > t.priority ? !0 : n.priority < t.priority ? !1 : rc(n.classes) > rc(t.classes);
}
function rc(n) {
  return Array.isArray(n) ? n.sort().join(",") : n;
}
var yM = '<svg viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg"><path d="M4 0v1H1v3H0V.5A.5.5 0 0 1 .5 0H4zm8 0h3.5a.5.5 0 0 1 .5.5V4h-1V1h-3V0zM4 16H.5a.5.5 0 0 1-.5-.5V12h1v3h3v1zm8 0v-1h3v-3h1v3.5a.5.5 0 0 1-.5.5H12z"/><path fill-opacity=".256" d="M1 1h14v14H1z"/><g class="ck-icon__selected-indicator"><path d="M7 0h2v1H7V0zM0 7h1v2H0V7zm15 0h1v2h-1V7zm-8 8h2v1H7v-1z"/><path fill-opacity=".254" d="M1 1h14v14H1z"/></g></svg>';
const kM = "ck-widget", Jb = "ck-widget_selected";
function vt(n) {
  return n.is("element") ? !!n.getCustomProperty("widget") : !1;
}
function Qn(n, t, e = {}) {
  if (!n.is("containerElement"))
    throw new S("widget-to-widget-wrong-element-type", null, {
      element: n
    });
  return t.setAttribute("contenteditable", "false", n), t.addClass(kM, n), t.setCustomProperty("widget", !0, n), n.getFillerOffset = SM, t.setCustomProperty("widgetLabel", [], n), e.label && TM(n, e.label), e.hasSelectionHandle && AM(n, t), ok(n, t), n;
}
function CM(n, t, e) {
  if (t.classes && e.addClass(Ue(t.classes), n), t.attributes)
    for (const i in t.attributes)
      e.setAttribute(i, t.attributes[i], n);
}
function xM(n, t, e) {
  if (t.classes && e.removeClass(Ue(t.classes), n), t.attributes)
    for (const i in t.attributes)
      e.removeAttribute(i, n);
}
function ok(n, t, e = CM, i = xM) {
  const s = new vM();
  s.on("change:top", (a, l) => {
    l.oldDescriptor && i(n, l.oldDescriptor, l.writer), l.newDescriptor && e(n, l.newDescriptor, l.writer);
  });
  const r = (a, l, c) => s.add(l, c), o = (a, l, c) => s.remove(l, c);
  t.setCustomProperty("addHighlight", r, n), t.setCustomProperty("removeHighlight", o, n);
}
function TM(n, t) {
  n.getCustomProperty("widgetLabel").push(t);
}
function EM(n) {
  return n.getCustomProperty("widgetLabel").reduce((e, i) => typeof i == "function" ? e ? e + ". " + i() : i() : e ? e + ". " + i : i, "");
}
function Kf(n, t, e = {}) {
  return t.addClass([
    "ck-editor__editable",
    "ck-editor__nested-editable"
  ], n), t.setAttribute("role", "textbox", n), t.setAttribute("tabindex", "-1", n), e.label && t.setAttribute("aria-label", e.label, n), t.setAttribute("contenteditable", n.isReadOnly ? "false" : "true", n), n.on("change:isReadOnly", (i, s, r) => {
    t.setAttribute("contenteditable", r ? "false" : "true", n);
  }), n.on("change:isFocused", (i, s, r) => {
    r ? t.addClass("ck-editor__nested-editable_focused", n) : t.removeClass("ck-editor__nested-editable_focused", n);
  }), ok(n, t), n;
}
function Da(n, t) {
  const e = n.getSelectedElement();
  if (e) {
    const i = os(n);
    if (i)
      return t.createRange(t.createPositionAt(e, i));
  }
  return t.schema.findOptimalInsertionRange(n);
}
function SM() {
  return null;
}
function AM(n, t) {
  const e = t.createUIElement("div", {
    class: "ck ck-widget__selection-handle"
  }, function(i) {
    const s = this.toDomElement(i), r = new Qi();
    return r.set("content", yM), r.render(), s.appendChild(r.element), s;
  });
  t.insert(t.createPositionAt(n, 0), e), t.addClass([
    "ck-widget_with-selection-handle"
  ], n);
}
function Zf(n) {
  const t = (a) => {
    const { width: l, paddingLeft: c, paddingRight: u } = a.ownerDocument.defaultView.getComputedStyle(a);
    return parseFloat(l) - (parseFloat(c) || 0) - (parseFloat(u) || 0);
  }, e = n.parentElement;
  if (!e)
    return 0;
  let i = t(e);
  const s = 5;
  let r = 0, o = e;
  for (; isNaN(i); ) {
    if (o = o.parentElement, ++r > s)
      return 0;
    i = t(o);
  }
  return i;
}
function IM(n, t = new ke(n)) {
  const e = Zf(n);
  return e ? t.width / e * 100 : 0;
}
const Bn = "widget-type-around";
function Ts(n, t, e) {
  return !!n && vt(n) && !e.isInline(t);
}
function PM(n) {
  return n.closest(".ck-widget__type-around__button");
}
function RM(n) {
  return n.classList.contains("ck-widget__type-around__button_before") ? "before" : "after";
}
function OM(n, t) {
  const e = n.closest(".ck-widget");
  return t.mapDomToView(e);
}
function os(n) {
  return n.getAttribute(Bn);
}
var VM = '<svg viewBox="0 0 10 8" xmlns="http://www.w3.org/2000/svg"><path d="M9.055.263v3.972h-6.77M1 4.216l2-2.038m-2 2 2 2.038"/></svg>';
const ak = [
  "before",
  "after"
], LM = new DOMParser().parseFromString(VM, "image/svg+xml").firstChild, Xb = "ck-widget__type-around_disabled";
class BM extends I {
  constructor() {
    super(...arguments);
    /**
    * A reference to the model widget element that has the fake caret active
    * on either side of it. It is later used to remove CSS classes associated with the fake caret
    * when the widget no longer needs it.
    */
    f(this, "_currentFakeCaretModelElement", null);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "WidgetTypeAround";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Fa,
      Cn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.editing.view;
    this.on("change:isEnabled", (s, r, o) => {
      i.change((a) => {
        for (const l of i.document.roots)
          o ? a.removeClass(Xb, l) : a.addClass(Xb, l);
      }), o || e.model.change((a) => {
        a.removeSelectionAttribute(Bn);
      });
    }), this._enableTypeAroundUIInjection(), this._enableInsertingParagraphsOnButtonClick(), this._enableInsertingParagraphsOnEnterKeypress(), this._enableInsertingParagraphsOnTypingKeystroke(), this._enableTypeAroundFakeCaretActivationUsingKeyboardArrows(), this._enableDeleteIntegration(), this._enableInsertContentIntegration(), this._enableInsertObjectIntegration(), this._enableDeleteContentIntegration();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._currentFakeCaretModelElement = null;
  }
  /**
  * Inserts a new paragraph next to a widget element with the selection anchored in it.
  *
  * **Note**: This method is heavily user-oriented and will both focus the editing view and scroll
  * the viewport to the selection in the inserted paragraph.
  *
  * @param widgetModelElement The model widget element next to which a paragraph is inserted.
  * @param position The position where the paragraph is inserted. Either `'before'` or `'after'` the widget.
  */
  _insertParagraph(e, i) {
    const s = this.editor, r = s.editing.view, o = s.model.schema.getAttributesWithProperty(e, "copyOnReplace", !0);
    s.execute("insertParagraph", {
      position: s.model.createPositionAt(e, i),
      attributes: o
    }), r.focus(), r.scrollToTheSelection();
  }
  /**
  * A wrapper for the {@link module:utils/emittermixin~Emitter#listenTo} method that executes the callbacks only
  * when the plugin {@link #isEnabled is enabled}.
  *
  * @param emitter The object that fires the event.
  * @param event The name of the event.
  * @param callback The function to be called on event.
  * @param options Additional options.
  * @param options.priority The priority of this event callback. The higher the priority value the sooner
  * the callback will be fired. Events having the same priority are called in the order they were added.
  */
  _listenToIfEnabled(e, i, s, r) {
    this.listenTo(e, i, (...o) => {
      this.isEnabled && s(...o);
    }, r);
  }
  /**
  * Similar to {@link #_insertParagraph}, this method inserts a paragraph except that it
  * does not expect a position. Instead, it performs the insertion next to a selected widget
  * according to the `widget-type-around` model selection attribute value (fake caret position).
  *
  * Because this method requires the `widget-type-around` attribute to be set,
  * the insertion can only happen when the widget's fake caret is active (e.g. activated
  * using the keyboard).
  *
  * @returns Returns `true` when the paragraph was inserted (the attribute was present) and `false` otherwise.
  */
  _insertParagraphAccordingToFakeCaretPosition() {
    const s = this.editor.model.document.selection, r = os(s);
    if (!r)
      return !1;
    const o = s.getSelectedElement();
    return this._insertParagraph(o, r), !0;
  }
  /**
  * Creates a listener in the editing conversion pipeline that injects the widget type around
  * UI into every single widget instance created in the editor.
  *
  * The UI is delivered as a {@link module:engine/view/uielement~UIElement}
  * wrapper which renders DOM buttons that users can use to insert paragraphs.
  */
  _enableTypeAroundUIInjection() {
    const e = this.editor, i = e.model.schema, s = e.locale.t, r = {
      before: s("Insert paragraph before block"),
      after: s("Insert paragraph after block")
    };
    e.editing.downcastDispatcher.on("insert", (o, a, l) => {
      const c = l.mapper.toViewElement(a.item);
      c && Ts(c, a.item, i) && (MM(l.writer, r, c), c.getCustomProperty("widgetLabel").push(() => this.isEnabled ? s("Press Enter to type after or press Shift + Enter to type before the widget") : ""));
    }, {
      priority: "low"
    });
  }
  /**
  * Brings support for the fake caret that appears when either:
  *
  * * the selection moves to a widget from a position next to it using arrow keys,
  * * the arrow key is pressed when the widget is already selected.
  *
  * The fake caret lets the user know that they can start typing or just press
  * <kbd>Enter</kbd> to insert a paragraph at the position next to a widget as suggested by the fake caret.
  *
  * The fake caret disappears when the user changes the selection or the editor
  * gets blurred.
  *
  * The whole idea is as follows:
  *
  * 1. A user does one of the 2 scenarios described at the beginning.
  * 2. The "keydown" listener is executed and the decision is made whether to show or hide the fake caret.
  * 3. If it should show up, the `widget-type-around` model selection attribute is set indicating
  *    on which side of the widget it should appear.
  * 4. The selection dispatcher reacts to the selection attribute and sets CSS classes responsible for the
  *    fake caret on the view widget.
  * 5. If the fake caret should disappear, the selection attribute is removed and the dispatcher
  *    does the CSS class clean-up in the view.
  * 6. Additionally, `change:range` and `FocusTracker#isFocused` listeners also remove the selection
  *    attribute (the former also removes widget CSS classes).
  */
  _enableTypeAroundFakeCaretActivationUsingKeyboardArrows() {
    const e = this.editor, i = e.model, s = i.document.selection, r = i.schema, o = e.editing.view;
    this._listenToIfEnabled(o.document, "arrowKey", (l, c) => {
      this._handleArrowKeyPress(l, c);
    }, {
      context: [
        vt,
        "$text"
      ],
      priority: "high"
    }), this._listenToIfEnabled(s, "change:range", (l, c) => {
      c.directChange && e.model.change((u) => {
        u.removeSelectionAttribute(Bn);
      });
    }), this._listenToIfEnabled(i.document, "change:data", () => {
      const l = s.getSelectedElement();
      if (l) {
        const c = e.editing.mapper.toViewElement(l);
        if (Ts(c, l, r))
          return;
      }
      e.model.change((c) => {
        c.removeSelectionAttribute(Bn);
      });
    }), this._listenToIfEnabled(e.editing.downcastDispatcher, "selection", (l, c, u) => {
      const d = u.writer;
      if (this._currentFakeCaretModelElement) {
        const p = u.mapper.toViewElement(this._currentFakeCaretModelElement);
        p && (d.removeClass(ak.map(a), p), this._currentFakeCaretModelElement = null);
      }
      const h = c.selection.getSelectedElement();
      if (!h)
        return;
      const m = u.mapper.toViewElement(h);
      if (!Ts(m, h, r))
        return;
      const g = os(c.selection);
      g && (d.addClass(a(g), m), this._currentFakeCaretModelElement = h);
    }), this._listenToIfEnabled(e.ui.focusTracker, "change:isFocused", (l, c, u) => {
      u || e.model.change((d) => {
        d.removeSelectionAttribute(Bn);
      });
    });
    function a(l) {
      return `ck-widget_type-around_show-fake-caret_${l}`;
    }
  }
  /**
  * A listener executed on each "keydown" in the view document, a part of
  * {@link #_enableTypeAroundFakeCaretActivationUsingKeyboardArrows}.
  *
  * It decides whether the arrow keypress should activate the fake caret or not (also whether it should
  * be deactivated).
  *
  * The fake caret activation is done by setting the `widget-type-around` model selection attribute
  * in this listener, and stopping and preventing the event that would normally be handled by the widget
  * plugin that is responsible for the regular keyboard navigation near/across all widgets (that
  * includes inline widgets, which are ignored by the widget type around plugin).
  */
  _handleArrowKeyPress(e, i) {
    const s = this.editor, r = s.model, o = r.document.selection, a = r.schema, l = s.editing.view, c = i.keyCode, u = J4(c, s.locale.contentLanguageDirection), d = l.document.selection.getSelectedElement(), h = s.editing.mapper.toModelElement(d);
    let m;
    Ts(d, h, a) ? m = this._handleArrowKeyPressOnSelectedWidget(u) : o.isCollapsed ? m = this._handleArrowKeyPressWhenSelectionNextToAWidget(u) : i.shiftKey || (m = this._handleArrowKeyPressWhenNonCollapsedSelection(u)), m && (i.preventDefault(), e.stop());
  }
  /**
  * Handles the keyboard navigation on "keydown" when a widget is currently selected and activates or deactivates
  * the fake caret for that widget, depending on the current value of the `widget-type-around` model
  * selection attribute and the direction of the pressed arrow key.
  *
  * @param isForward `true` when the pressed arrow key was responsible for the forward model selection movement
  * as in {@link module:utils/keyboard~isForwardArrowKeyCode}.
  * @returns Returns `true` when the keypress was handled and no other keydown listener of the editor should
  * process the event any further. Returns `false` otherwise.
  */
  _handleArrowKeyPressOnSelectedWidget(e) {
    const s = this.editor.model, r = s.document.selection, o = os(r);
    return s.change((a) => {
      if (o) {
        if (!(o === (e ? "after" : "before")))
          return a.removeSelectionAttribute(Bn), !0;
      } else
        return a.setSelectionAttribute(Bn, e ? "after" : "before"), !0;
      return !1;
    });
  }
  /**
  * Handles the keyboard navigation on "keydown" when **no** widget is selected but the selection is **directly** next
  * to one and upon the fake caret should become active for this widget upon arrow keypress
  * (AKA entering/selecting the widget).
  *
  * **Note**: This code mirrors the implementation from the widget plugin but also adds the selection attribute.
  * Unfortunately, there is no safe way to let the widget plugin do the selection part first and then just set the
  * selection attribute here in the widget type around plugin. This is why this code must duplicate some from the widget plugin.
  *
  * @param isForward `true` when the pressed arrow key was responsible for the forward model selection movement
  * as in {@link module:utils/keyboard~isForwardArrowKeyCode}.
  * @returns Returns `true` when the keypress was handled and no other keydown listener of the editor should
  * process the event any further. Returns `false` otherwise.
  */
  _handleArrowKeyPressWhenSelectionNextToAWidget(e) {
    const i = this.editor, s = i.model, r = s.schema, o = i.plugins.get("Widget"), a = o._getObjectElementNextToSelection(e), l = i.editing.mapper.toViewElement(a);
    return Ts(l, a, r) ? (s.change((c) => {
      o._setSelectionOverElement(a), c.setSelectionAttribute(Bn, e ? "before" : "after");
    }), !0) : !1;
  }
  /**
  * Handles the keyboard navigation on "keydown" when a widget is currently selected (together with some other content)
  * and the widget is the first or last element in the selection. It activates or deactivates the fake caret for that widget.
  *
  * @param isForward `true` when the pressed arrow key was responsible for the forward model selection movement
  * as in {@link module:utils/keyboard~isForwardArrowKeyCode}.
  * @returns Returns `true` when the keypress was handled and no other keydown listener of the editor should
  * process the event any further. Returns `false` otherwise.
  */
  _handleArrowKeyPressWhenNonCollapsedSelection(e) {
    const i = this.editor, s = i.model, r = s.schema, o = i.editing.mapper, a = s.document.selection, l = e ? a.getLastPosition().nodeBefore : a.getFirstPosition().nodeAfter, c = o.toViewElement(l);
    return Ts(c, l, r) ? (s.change((u) => {
      u.setSelection(l, "on"), u.setSelectionAttribute(Bn, e ? "after" : "before");
    }), !0) : !1;
  }
  /**
  * Registers a `mousedown` listener for the view document which intercepts events
  * coming from the widget type around UI, which happens when a user clicks one of the buttons
  * that insert a paragraph next to a widget.
  */
  _enableInsertingParagraphsOnButtonClick() {
    const e = this.editor, i = e.editing.view;
    this._listenToIfEnabled(i.document, "mousedown", (s, r) => {
      const o = PM(r.domTarget);
      if (!o)
        return;
      const a = RM(o), l = OM(o, i.domConverter), c = e.editing.mapper.toModelElement(l);
      this._insertParagraph(c, a), r.preventDefault(), s.stop();
    });
  }
  /**
  * Creates the <kbd>Enter</kbd> key listener on the view document that allows the user to insert a paragraph
  * near the widget when either:
  *
  * * The fake caret was first activated using the arrow keys,
  * * The entire widget is selected in the model.
  *
  * In the first case, the new paragraph is inserted according to the `widget-type-around` selection
  * attribute (see {@link #_handleArrowKeyPress}).
  *
  * In the second case, the new paragraph is inserted based on whether a soft (<kbd>Shift</kbd>+<kbd>Enter</kbd>) keystroke
  * was pressed or not.
  */
  _enableInsertingParagraphsOnEnterKeypress() {
    const e = this.editor, i = e.model.document.selection, s = e.editing.view;
    this._listenToIfEnabled(s.document, "enter", (r, o) => {
      if (r.eventPhase != "atTarget")
        return;
      const a = i.getSelectedElement(), l = e.editing.mapper.toViewElement(a), c = e.model.schema;
      let u;
      this._insertParagraphAccordingToFakeCaretPosition() ? u = !0 : Ts(l, a, c) && (this._insertParagraph(a, o.isSoft ? "before" : "after"), u = !0), u && (o.preventDefault(), r.stop());
    }, {
      context: vt
    });
  }
  /**
  * Similar to the {@link #_enableInsertingParagraphsOnEnterKeypress}, it allows the user
  * to insert a paragraph next to a widget when the fake caret was activated using arrow
  * keys but it responds to typing instead of <kbd>Enter</kbd>.
  *
  * Listener enabled by this method will insert a new paragraph according to the `widget-type-around`
  * model selection attribute as the user simply starts typing, which creates the impression that the fake caret
  * behaves like a real one rendered by the browser (AKA your text appears where the caret was).
  *
  * **Note**: At the moment this listener creates 2 undo steps: one for the `insertParagraph` command
  * and another one for actual typing. It is not a disaster but this may need to be fixed
  * sooner or later.
  */
  _enableInsertingParagraphsOnTypingKeystroke() {
    const i = this.editor.editing.view.document;
    this._listenToIfEnabled(i, "insertText", (s, r) => {
      this._insertParagraphAccordingToFakeCaretPosition() && (r.selection = i.selection);
    }, {
      priority: "high"
    }), be.isAndroid ? this._listenToIfEnabled(i, "keydown", (s, r) => {
      r.keyCode == 229 && this._insertParagraphAccordingToFakeCaretPosition();
    }) : this._listenToIfEnabled(i, "compositionstart", () => {
      this._insertParagraphAccordingToFakeCaretPosition();
    }, {
      priority: "high"
    });
  }
  /**
  * It creates a "delete" event listener on the view document to handle cases when the <kbd>Delete</kbd> or <kbd>Backspace</kbd>
  * is pressed and the fake caret is currently active.
  *
  * The fake caret should create an illusion of a real browser caret so that when it appears before or after
  * a widget, pressing <kbd>Delete</kbd> or <kbd>Backspace</kbd> should remove a widget or delete the content
  * before or after a widget (depending on the content surrounding the widget).
  */
  _enableDeleteIntegration() {
    const e = this.editor, i = e.editing.view, s = e.model, r = s.schema;
    this._listenToIfEnabled(i.document, "delete", (o, a) => {
      if (o.eventPhase != "atTarget")
        return;
      const l = os(s.document.selection);
      if (!l)
        return;
      const c = a.direction, u = s.document.selection.getSelectedElement(), d = l === "before", h = c == "forward";
      if (d === h)
        e.execute("delete", {
          selection: s.createSelection(u, "on")
        });
      else {
        const g = r.getNearestSelectionRange(s.createPositionAt(u, l), c);
        if (g)
          if (!g.isCollapsed)
            s.change((p) => {
              p.setSelection(g), e.execute(h ? "deleteForward" : "delete");
            });
          else {
            const p = s.createSelection(g.start);
            if (s.modifySelection(p, {
              direction: c
            }), !p.focus.isEqual(g.start))
              s.change((w) => {
                w.setSelection(g), e.execute(h ? "deleteForward" : "delete");
              });
            else {
              const w = DM(r, g.start.parent);
              s.deleteContent(s.createSelection(w, "on"), {
                doNotAutoparagraph: !0
              });
            }
          }
      }
      a.preventDefault(), o.stop();
    }, {
      context: vt
    });
  }
  /**
  * Attaches the {@link module:engine/model/model~Model#event:insertContent} event listener that, for instance, allows the user to paste
  * content near a widget when the fake caret is first activated using the arrow keys.
  *
  * The content is inserted according to the `widget-type-around` selection attribute (see {@link #_handleArrowKeyPress}).
  */
  _enableInsertContentIntegration() {
    const e = this.editor, i = this.editor.model, s = i.document.selection;
    this._listenToIfEnabled(e.model, "insertContent", (r, [o, a]) => {
      if (a && !a.is("documentSelection"))
        return;
      const l = os(s);
      if (l)
        return r.stop(), i.change((c) => {
          const u = s.getSelectedElement(), d = i.createPositionAt(u, l), h = c.createSelection(d), m = i.insertContent(o, h);
          return c.setSelection(h), m;
        });
    }, {
      priority: "high"
    });
  }
  /**
  * Attaches the {@link module:engine/model/model~Model#event:insertObject} event listener that modifies the
  * `options.findOptimalPosition`parameter to position of fake caret in relation to selected element
  * to reflect user's intent of desired insertion position.
  *
  * The object is inserted according to the `widget-type-around` selection attribute (see {@link #_handleArrowKeyPress}).
  */
  _enableInsertObjectIntegration() {
    const e = this.editor, s = this.editor.model.document.selection;
    this._listenToIfEnabled(e.model, "insertObject", (r, o) => {
      const [, a, l = {}] = o;
      if (a && !a.is("documentSelection"))
        return;
      const c = os(s);
      c && (l.findOptimalPosition = c, o[3] = l);
    }, {
      priority: "high"
    });
  }
  /**
  * Attaches the {@link module:engine/model/model~Model#event:deleteContent} event listener to block the event when the fake
  * caret is active.
  *
  * This is required for cases that trigger {@link module:engine/model/model~Model#deleteContent `model.deleteContent()`}
  * before calling {@link module:engine/model/model~Model#insertContent `model.insertContent()`} like, for instance,
  * plain text pasting.
  */
  _enableDeleteContentIntegration() {
    const e = this.editor, s = this.editor.model.document.selection;
    this._listenToIfEnabled(e.model, "deleteContent", (r, [o]) => {
      if (o && !o.is("documentSelection"))
        return;
      os(s) && r.stop();
    }, {
      priority: "high"
    });
  }
}
function MM(n, t, e) {
  const i = n.createUIElement("div", {
    class: "ck ck-reset_all ck-widget__type-around"
  }, function(s) {
    const r = this.toDomElement(s);
    return NM(r, t), FM(r), r;
  });
  n.insert(n.createPositionAt(e, "end"), i);
}
function NM(n, t) {
  for (const e of ak) {
    const i = new ri({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-widget__type-around__button",
          `ck-widget__type-around__button_${e}`
        ],
        title: t[e],
        "aria-hidden": "true"
      },
      children: [
        n.ownerDocument.importNode(LM, !0)
      ]
    });
    n.appendChild(i.render());
  }
}
function FM(n) {
  const t = new ri({
    tag: "div",
    attributes: {
      class: [
        "ck",
        "ck-widget__type-around__fake-caret"
      ]
    }
  });
  n.appendChild(t.render());
}
function DM(n, t) {
  let e = t;
  for (const i of t.getAncestors({
    parentFirst: !0
  })) {
    if (i.childCount > 1 || n.isLimit(i))
      break;
    e = i;
  }
  return e;
}
function $M(n) {
  const t = n.model;
  return (e, i) => {
    const s = i.keyCode == Ie.arrowup, r = i.keyCode == Ie.arrowdown, o = i.shiftKey, a = t.document.selection;
    if (!s && !r)
      return;
    const l = r;
    if (o && jM(a, l))
      return;
    const c = zM(n, a, l);
    if (c) {
      if (c.isCollapsed) {
        if (a.isCollapsed)
          return;
        if (o)
          return;
      }
      (c.isCollapsed || HM(n, c, l)) && (t.change((u) => {
        const d = l ? c.end : c.start;
        if (o) {
          const h = t.createSelection(a.anchor);
          h.setFocus(d), u.setSelection(h);
        } else
          u.setSelection(d);
      }), e.stop(), i.preventDefault(), i.stopPropagation());
    }
  };
}
function zM(n, t, e) {
  const i = n.model;
  if (e) {
    const s = t.isCollapsed ? t.focus : t.getLastPosition(), r = Qb(i, s, "forward");
    if (!r)
      return null;
    const o = i.createRange(s, r), a = e1(i.schema, o, "backward");
    return a ? i.createRange(s, a) : null;
  } else {
    const s = t.isCollapsed ? t.focus : t.getFirstPosition(), r = Qb(i, s, "backward");
    if (!r)
      return null;
    const o = i.createRange(r, s), a = e1(i.schema, o, "forward");
    return a ? i.createRange(a, s) : null;
  }
}
function Qb(n, t, e) {
  const i = n.schema, s = n.createRangeIn(t.root), r = e == "forward" ? "elementStart" : "elementEnd";
  for (const { previousPosition: o, item: a, type: l } of s.getWalker({
    startPosition: t,
    direction: e
  })) {
    if (i.isLimit(a) && !i.isInline(a))
      return o;
    if (l == r && i.isBlock(a))
      return null;
  }
  return null;
}
function e1(n, t, e) {
  const i = e == "backward" ? t.end : t.start;
  if (n.checkChild(i, "$text"))
    return i;
  for (const { nextPosition: s } of t.getWalker({
    direction: e
  }))
    if (n.checkChild(s, "$text"))
      return s;
  return null;
}
function HM(n, t, e) {
  const i = n.model, s = n.view.domConverter;
  if (e) {
    const c = i.createSelection(t.start);
    i.modifySelection(c), !c.focus.isAtEnd && !t.start.isEqual(c.focus) && (t = i.createRange(c.focus, t.end));
  }
  const r = n.mapper.toViewRange(t), o = s.viewRangeToDom(r), a = ke.getDomRangeRects(o);
  let l;
  for (const c of a) {
    if (l === void 0) {
      l = Math.round(c.bottom);
      continue;
    }
    if (Math.round(c.top) >= l)
      return !1;
    l = Math.max(l, Math.round(c.bottom));
  }
  return !0;
}
function jM(n, t) {
  return !n.isCollapsed && n.isBackward == t;
}
class Sn extends I {
  constructor() {
    super(...arguments);
    /**
    * Holds previously selected widgets.
    */
    f(this, "_previouslySelected", /* @__PURE__ */ new Set());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Widget";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      BM,
      Cn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.editing.view, s = i.document, r = e.t;
    this.editor.editing.downcastDispatcher.on("selection", (o, a, l) => {
      const c = l.writer, u = a.selection;
      if (u.isCollapsed)
        return;
      const d = u.getSelectedElement();
      if (!d)
        return;
      const h = e.editing.mapper.toViewElement(d);
      vt(h) && l.consumable.consume(u, "selection") && c.setSelection(c.createRangeOn(h), {
        fake: !0,
        label: EM(h)
      });
    }), this.editor.editing.downcastDispatcher.on("selection", (o, a, l) => {
      this._clearPreviouslySelectedWidgets(l.writer);
      const c = l.writer, u = c.document.selection;
      let d = null;
      for (const h of u.getRanges())
        for (const m of h) {
          const g = m.item;
          vt(g) && !qM(g, d) && (c.addClass(Jb, g), this._previouslySelected.add(g), d = g);
        }
    }, {
      priority: "low"
    }), i.addObserver(Yc), this.listenTo(s, "mousedown", (...o) => this._onMousedown(...o)), this.listenTo(s, "arrowKey", (...o) => {
      this._handleSelectionChangeOnArrowKeyPress(...o);
    }, {
      context: [
        vt,
        "$text"
      ]
    }), this.listenTo(s, "arrowKey", (...o) => {
      this._preventDefaultOnArrowKeyPress(...o);
    }, {
      context: "$root"
    }), this.listenTo(s, "arrowKey", $M(this.editor.editing), {
      context: "$text"
    }), this.listenTo(s, "delete", (o, a) => {
      this._handleDelete(a.direction == "forward") && (a.preventDefault(), o.stop());
    }, {
      context: "$root"
    }), this.listenTo(s, "tab", (o, a) => {
      o.eventPhase == "atTarget" && (a.shiftKey || this._selectFirstNestedEditable() && (a.preventDefault(), o.stop()));
    }, {
      context: vt,
      priority: "low"
    }), this.listenTo(s, "tab", (o, a) => {
      a.shiftKey && this._selectAncestorWidget() && (a.preventDefault(), o.stop());
    }, {
      priority: "low"
    }), this.listenTo(s, "keydown", (o, a) => {
      a.keystroke == Ie.esc && this._selectAncestorWidget() && (a.preventDefault(), o.stop());
    }, {
      priority: "low"
    }), e.accessibility.addKeystrokeInfoGroup({
      id: "widget",
      label: r("Keystrokes that can be used when a widget is selected (for example: image, table, etc.)"),
      keystrokes: [
        {
          label: r("Move focus from an editable area back to the parent widget"),
          keystroke: "Esc"
        },
        {
          label: r("Insert a new paragraph directly after a widget"),
          keystroke: "Enter"
        },
        {
          label: r("Insert a new paragraph directly before a widget"),
          keystroke: "Shift+Enter"
        },
        {
          label: r("Move the caret to allow typing directly before a widget"),
          keystroke: [
            [
              "arrowup"
            ],
            [
              "arrowleft"
            ]
          ]
        },
        {
          label: r("Move the caret to allow typing directly after a widget"),
          keystroke: [
            [
              "arrowdown"
            ],
            [
              "arrowright"
            ]
          ]
        }
      ]
    });
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:mousedown mousedown} events on widget elements.
  */
  _onMousedown(e, i) {
    const s = this.editor, r = s.editing.view, o = r.document;
    let a = i.target;
    if (!a)
      return;
    if (i.domEvent.detail >= 3) {
      this._selectBlockContent(a) && i.preventDefault();
      return;
    }
    if (!vt(a)) {
      const c = UM(a);
      if (!c)
        return;
      if (vt(c))
        a = c;
      else {
        const u = WM(r, i);
        if (u && vt(u))
          a = u;
        else
          return;
      }
    }
    be.isAndroid && i.preventDefault(), o.isFocused || r.focus();
    const l = s.editing.mapper.toModelElement(a);
    this._setSelectionOverElement(l);
  }
  /**
  * Selects entire block content, e.g. on triple click it selects entire paragraph.
  */
  _selectBlockContent(e) {
    const i = this.editor, s = i.model, r = i.editing.mapper, o = s.schema, a = r.findMappedViewAncestor(this.editor.editing.view.createPositionAt(e, 0)), l = GM(r.toModelElement(a), s.schema);
    return l ? (s.change((c) => {
      const u = o.isLimit(l) ? null : KM(c.createPositionAfter(l), o), d = c.createPositionAt(l, 0), h = u ? c.createPositionAt(u, 0) : c.createPositionAt(l, "end");
      c.setSelection(c.createRange(d, h));
    }), !0) : !1;
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:keydown keydown} events and changes
  * the model selection when:
  *
  * * arrow key is pressed when the widget is selected,
  * * the selection is next to a widget and the widget should become selected upon the arrow key press.
  *
  * See {@link #_preventDefaultOnArrowKeyPress}.
  */
  _handleSelectionChangeOnArrowKeyPress(e, i) {
    const s = i.keyCode, r = this.editor.model, o = r.schema, a = r.document.selection, l = a.getSelectedElement(), c = Wc(s, this.editor.locale.contentLanguageDirection), u = c == "down" || c == "right", d = c == "up" || c == "down";
    if (l && o.isObject(l)) {
      const m = u ? a.getLastPosition() : a.getFirstPosition(), g = o.getNearestSelectionRange(m, u ? "forward" : "backward");
      g && (r.change((p) => {
        p.setSelection(g);
      }), i.preventDefault(), e.stop());
      return;
    }
    if (!a.isCollapsed && !i.shiftKey) {
      const m = a.getFirstPosition(), g = a.getLastPosition(), p = m.nodeAfter, w = g.nodeBefore;
      (p && o.isObject(p) || w && o.isObject(w)) && (r.change((y) => {
        y.setSelection(u ? g : m);
      }), i.preventDefault(), e.stop());
      return;
    }
    if (!a.isCollapsed)
      return;
    const h = this._getObjectElementNextToSelection(u);
    if (h && o.isObject(h)) {
      if (o.isInline(h) && d)
        return;
      this._setSelectionOverElement(h), i.preventDefault(), e.stop();
    }
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:keydown keydown} events and prevents
  * the default browser behavior to make sure the fake selection is not being moved from a fake selection
  * container.
  *
  * See {@link #_handleSelectionChangeOnArrowKeyPress}.
  */
  _preventDefaultOnArrowKeyPress(e, i) {
    const s = this.editor.model, r = s.schema, o = s.document.selection.getSelectedElement();
    o && r.isObject(o) && (i.preventDefault(), e.stop());
  }
  /**
  * Handles delete keys: backspace and delete.
  *
  * @param isForward Set to true if delete was performed in forward direction.
  * @returns Returns `true` if keys were handled correctly.
  */
  _handleDelete(e) {
    const s = this.editor.model.document.selection;
    if (!this.editor.model.canEditAt(s) || !s.isCollapsed)
      return;
    const r = this._getObjectElementNextToSelection(e);
    if (r)
      return this.editor.model.change((o) => {
        let a = s.anchor.parent;
        for (; a.isEmpty; ) {
          const l = a;
          a = l.parent, o.remove(l);
        }
        this._setSelectionOverElement(r);
      }), !0;
  }
  /**
  * Sets {@link module:engine/model/selection~Selection document's selection} over given element.
  *
  * @internal
  */
  _setSelectionOverElement(e) {
    this.editor.model.change((i) => {
      i.setSelection(i.createRangeOn(e));
    });
  }
  /**
  * Checks if {@link module:engine/model/element~Element element} placed next to the current
  * {@link module:engine/model/selection~Selection model selection} exists and is marked in
  * {@link module:engine/model/schema~Schema schema} as `object`.
  *
  * @internal
  * @param forward Direction of checking.
  */
  _getObjectElementNextToSelection(e) {
    const i = this.editor.model, s = i.schema, r = i.document.selection, o = i.createSelection(r);
    if (i.modifySelection(o, {
      direction: e ? "forward" : "backward"
    }), o.isEqual(r))
      return null;
    const a = e ? o.focus.nodeBefore : o.focus.nodeAfter;
    return a && s.isObject(a) ? a : null;
  }
  /**
  * Removes CSS class from previously selected widgets.
  */
  _clearPreviouslySelectedWidgets(e) {
    for (const i of this._previouslySelected)
      e.removeClass(Jb, i);
    this._previouslySelected.clear();
  }
  /**
  * Moves the document selection into the first nested editable.
  */
  _selectFirstNestedEditable() {
    const e = this.editor, s = this.editor.editing.view.document;
    for (const r of s.selection.getFirstRange().getItems())
      if (r.is("editableElement")) {
        const o = e.editing.mapper.toModelElement(r);
        /* istanbul ignore next -- @preserve */
        if (!o)
          continue;
        const a = e.model.createPositionAt(o, 0), l = e.model.schema.getNearestSelectionRange(a, "forward");
        return e.model.change((c) => {
          c.setSelection(l);
        }), !0;
      }
    return !1;
  }
  /**
  * Updates the document selection so that it selects first ancestor widget.
  */
  _selectAncestorWidget() {
    const e = this.editor, i = e.editing.mapper, r = e.editing.view.document.selection.getFirstPosition().parent, a = (r.is("$text") ? r.parent : r).findAncestor(vt);
    if (!a)
      return !1;
    const l = i.toModelElement(a);
    /* istanbul ignore next -- @preserve */
    return l ? (e.model.change((c) => {
      c.setSelection(l, "on");
    }), !0) : !1;
  }
}
function UM(n) {
  let t = n;
  for (; t; ) {
    if (t.is("editableElement") || vt(t))
      return t;
    t = t.parent;
  }
  return null;
}
function WM(n, t) {
  const e = a_(t.domEvent);
  let i = null;
  if (e ? i = n.domConverter.domRangeToView(e) : i = n.createRange(n.createPositionAt(t.target, 0)), !i)
    return null;
  const s = i.start;
  if (!s.parent)
    return null;
  let r = s.parent;
  return s.parent.is("editableElement") && (s.isAtEnd && s.nodeBefore ? r = s.nodeBefore : s.isAtStart && s.nodeAfter && (r = s.nodeAfter)), r.is("$text") ? r.parent : r;
}
function qM(n, t) {
  return t ? Array.from(n.getAncestors()).includes(t) : !1;
}
function GM(n, t) {
  for (const e of n.getAncestors({
    includeSelf: !0,
    parentFirst: !0
  })) {
    if (t.checkChild(e, "$text"))
      return e;
    if (t.isLimit(e) && !t.isObject(e))
      break;
  }
  return null;
}
function KM(n, t) {
  const e = new Fn({
    startPosition: n
  });
  for (const { item: i } of e) {
    if (t.isLimit(i) || !i.is("element"))
      return null;
    if (t.checkChild(i, "$text"))
      return i;
  }
  return null;
}
class oc extends I {
  constructor() {
    super(...arguments);
    /**
    * A map of toolbar definitions.
    */
    f(this, "_toolbarDefinitions", /* @__PURE__ */ new Map());
    f(this, "_balloon");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ai
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "WidgetToolbarRepository";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    if (e.plugins.has("BalloonToolbar")) {
      const i = e.plugins.get("BalloonToolbar");
      this.listenTo(i, "show", (s) => {
        ZM(e.editing.view.document.selection) && s.stop();
      }, {
        priority: "high"
      });
    }
    this._balloon = this.editor.plugins.get("ContextualBalloon"), this.on("change:isEnabled", () => {
      this._updateToolbarsVisibility();
    }), this.listenTo(e.ui, "update", () => {
      this._updateToolbarsVisibility();
    }), this.listenTo(e.ui.focusTracker, "change:isFocused", () => {
      this._updateToolbarsVisibility();
    }, {
      priority: "low"
    });
  }
  destroy() {
    super.destroy();
    for (const e of this._toolbarDefinitions.values())
      e.view.destroy();
  }
  /**
  * Registers toolbar in the WidgetToolbarRepository. It renders it in the `ContextualBalloon` based on the value of the invoked
  * `getRelatedElement` function. Toolbar items are gathered from `items` array.
  * The balloon's CSS class is by default `ck-toolbar-container` and may be override with the `balloonClassName` option.
  *
  * Note: This method should be called in the {@link module:core/plugin~PluginInterface#afterInit `Plugin#afterInit()`}
  * callback (or later) to make sure that the given toolbar items were already registered by other plugins.
  *
  * @param toolbarId An id for the toolbar. Used to
  * @param options.ariaLabel Label used by assistive technologies to describe this toolbar element.
  * @param options.items Array of toolbar items.
  * @param options.getRelatedElement Callback which returns an element the toolbar should be attached to.
  * @param options.balloonClassName CSS class for the widget balloon.
  */
  register(e, { ariaLabel: i, items: s, getRelatedElement: r, balloonClassName: o = "ck-toolbar-container" }) {
    if (!s.length) {
      Ve("widget-toolbar-no-items", {
        toolbarId: e
      });
      return;
    }
    const a = this.editor, l = a.t, c = new Us(a.locale);
    if (c.ariaLabel = i || l("Widget toolbar"), this._toolbarDefinitions.has(e))
      throw new S("widget-toolbar-duplicated", this, {
        toolbarId: e
      });
    const u = {
      view: c,
      getRelatedElement: r,
      balloonClassName: o,
      itemsConfig: s,
      initialized: !1
    };
    a.ui.addToolbar(c, {
      isContextual: !0,
      beforeFocus: () => {
        const d = r(a.editing.view.document.selection);
        d && this._showToolbar(u, d);
      },
      afterBlur: () => {
        this._hideToolbar(u);
      }
    }), this._toolbarDefinitions.set(e, u);
  }
  /**
  * Iterates over stored toolbars and makes them visible or hidden.
  */
  _updateToolbarsVisibility() {
    let e = 0, i = null, s = null;
    for (const r of this._toolbarDefinitions.values()) {
      const o = r.getRelatedElement(this.editor.editing.view.document.selection);
      if (!this.isEnabled || !o)
        this._isToolbarInBalloon(r) && this._hideToolbar(r);
      else if (!this.editor.ui.focusTracker.isFocused)
        this._isToolbarVisible(r) && this._hideToolbar(r);
      else {
        const a = o.getAncestors().length;
        a > e && (e = a, i = o, s = r);
      }
    }
    s && this._showToolbar(s, i);
  }
  /**
  * Hides the given toolbar.
  */
  _hideToolbar(e) {
    this._balloon.remove(e.view), this.stopListening(this._balloon, "change:visibleView");
  }
  /**
  * Shows up the toolbar if the toolbar is not visible.
  * Otherwise, repositions the toolbar's balloon when toolbar's view is the most top view in balloon stack.
  *
  * It might happen here that the toolbar's view is under another view. Then do nothing as the other toolbar view
  * should be still visible after the {@link module:ui/editorui/editorui~EditorUI#event:update}.
  */
  _showToolbar(e, i) {
    this._isToolbarVisible(e) ? t1(this.editor, i) : this._isToolbarInBalloon(e) || (e.initialized || (e.initialized = !0, e.view.fillFromConfig(e.itemsConfig, this.editor.ui.componentFactory)), this._balloon.add({
      view: e.view,
      position: lk(this.editor, i),
      balloonClassName: e.balloonClassName
    }), this.listenTo(this._balloon, "change:visibleView", () => {
      for (const s of this._toolbarDefinitions.values())
        if (this._isToolbarVisible(s)) {
          const r = s.getRelatedElement(this.editor.editing.view.document.selection);
          t1(this.editor, r);
        }
    }));
  }
  _isToolbarVisible(e) {
    return this._balloon.visibleView === e.view;
  }
  _isToolbarInBalloon(e) {
    return this._balloon.hasView(e.view);
  }
}
function t1(n, t) {
  const e = n.plugins.get("ContextualBalloon"), i = lk(n, t);
  e.updatePosition(i);
}
function lk(n, t) {
  const e = n.editing.view, i = Dt.defaultPositions;
  return {
    target: e.domConverter.mapViewToDom(t),
    positions: [
      i.northArrowSouth,
      i.northArrowSouthWest,
      i.northArrowSouthEast,
      i.southArrowNorth,
      i.southArrowNorthWest,
      i.southArrowNorthEast,
      i.viewportStickyNorth
    ]
  };
}
function ZM(n) {
  const t = n.getSelectedElement();
  return !!(t && vt(t));
}
class YM extends (/* @__PURE__ */ je()) {
  /**
  * @param options Resizer options.
  */
  constructor(e) {
    super();
    /**
    * The reference point of the resizer where the dragging started. It is used to measure the distance the user cursor
    * traveled, so how much the image should be enlarged.
    * This information is only known after the DOM was rendered, so it will be updated later.
    *
    * @internal
    */
    f(this, "_referenceCoordinates");
    /**
    * Resizer options.
    */
    f(this, "_options");
    /**
    * The original width (pixels) of the resized object when the resize process was started.
    *
    * @readonly
    */
    f(this, "_originalWidth");
    /**
    * The original height (pixels) of the resized object when the resize process was started.
    *
    * @readonly
    */
    f(this, "_originalHeight");
    /**
    * The original width (percents) of the resized object when the resize process was started.
    *
    * @readonly
    */
    f(this, "_originalWidthPercents");
    /**
    * A width to height ratio of the resized image.
    *
    * @readonly
    */
    f(this, "_aspectRatio");
    this.set("activeHandlePosition", null), this.set("proposedWidthPercents", null), this.set("proposedWidth", null), this.set("proposedHeight", null), this.set("proposedHandleHostWidth", null), this.set("proposedHandleHostHeight", null), this._options = e, this._referenceCoordinates = null;
  }
  /**
  * The original width (pixels) of the resized object when the resize process was started.
  */
  get originalWidth() {
    return this._originalWidth;
  }
  /**
  * The original height (pixels) of the resized object when the resize process was started.
  */
  get originalHeight() {
    return this._originalHeight;
  }
  /**
  * The original width (percents) of the resized object when the resize process was started.
  */
  get originalWidthPercents() {
    return this._originalWidthPercents;
  }
  /**
  * A width to height ratio of the resized image.
  */
  get aspectRatio() {
    return this._aspectRatio;
  }
  /**
  *
  * @param domResizeHandle The handle used to calculate the reference point.
  */
  begin(e, i, s) {
    const r = new ke(i);
    this.activeHandlePosition = QM(e), this._referenceCoordinates = JM(i, eN(this.activeHandlePosition)), this._originalWidth = r.width, this._originalHeight = r.height, this._aspectRatio = r.width / r.height;
    const o = s.style.width;
    o && o.match(/^\d+(\.\d*)?%$/) ? this._originalWidthPercents = parseFloat(o) : this._originalWidthPercents = IM(s, r);
  }
  update(e) {
    this.proposedWidth = e.width, this.proposedHeight = e.height, this.proposedWidthPercents = e.widthPercents, this.proposedHandleHostWidth = e.handleHostWidth, this.proposedHandleHostHeight = e.handleHostHeight;
  }
}
function JM(n, t) {
  const e = new ke(n), i = t.split("-"), s = {
    x: i[1] == "right" ? e.right : e.left,
    y: i[0] == "bottom" ? e.bottom : e.top
  };
  return s.x += n.ownerDocument.defaultView.scrollX, s.y += n.ownerDocument.defaultView.scrollY, s;
}
function XM(n) {
  return `ck-widget__resizer__handle-${n}`;
}
function QM(n) {
  const t = [
    "top-left",
    "top-right",
    "bottom-right",
    "bottom-left"
  ];
  for (const e of t)
    if (n.classList.contains(XM(e)))
      return e;
}
function eN(n) {
  const t = n.split("-"), e = {
    top: "bottom",
    bottom: "top",
    left: "right",
    right: "left"
  };
  return `${e[t[0]]}-${e[t[1]]}`;
}
class tN extends ee {
  constructor() {
    super();
    const t = this.bindTemplate;
    this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-size-view",
          t.to("_viewPosition", (e) => e ? `ck-orientation-${e}` : "")
        ],
        style: {
          display: t.if("_isVisible", "none", (e) => !e)
        }
      },
      children: [
        {
          text: t.to("_label")
        }
      ]
    });
  }
  /**
  * A method used for binding the `SizeView` instance properties to the `ResizeState` instance observable properties.
  *
  * @internal
  * @param options An object defining the resizer options, used for setting the proper size label.
  * @param resizeState The `ResizeState` class instance, used for keeping the `SizeView` state up to date.
  */
  _bindToState(t, e) {
    this.bind("_isVisible").to(e, "proposedWidth", e, "proposedHeight", (i, s) => i !== null && s !== null), this.bind("_label").to(e, "proposedHandleHostWidth", e, "proposedHandleHostHeight", e, "proposedWidthPercents", (i, s, r) => t.unit === "px" ? `${i}×${s}` : `${r}%`), this.bind("_viewPosition").to(
      e,
      "activeHandlePosition",
      e,
      "proposedHandleHostWidth",
      e,
      "proposedHandleHostHeight",
      // If the widget is too small to contain the size label, display the label above.
      (i, s, r) => s < 50 || r < 50 ? "above-center" : i
    );
  }
  /**
  * A method used for cleaning up. It removes the bindings and hides the view.
  *
  * @internal
  */
  _dismiss() {
    this.unbind(), this._isVisible = !1;
  }
}
class i1 extends (/* @__PURE__ */ je()) {
  /**
  * @param options Resizer options.
  */
  constructor(e) {
    super();
    /**
    * Stores the state of the resizable host geometry, such as the original width, the currently proposed height, etc.
    *
    * Note that a new state is created for each resize transaction.
    */
    f(this, "_state");
    /**
    * A view displaying the proposed new element size during the resizing.
    */
    f(this, "_sizeView");
    /**
    * Options passed to the {@link #constructor}.
    */
    f(this, "_options");
    /**
    * A wrapper that is controlled by the resizer. This is usually a widget element.
    */
    f(this, "_viewResizerWrapper", null);
    /**
    * The width of the resized {@link module:widget/widgetresize~ResizerOptions#viewElement viewElement} before the resizing started.
    */
    f(this, "_initialViewWidth");
    this._options = e, this.set("isEnabled", !0), this.set("isSelected", !1), this.bind("isVisible").to(this, "isEnabled", this, "isSelected", (i, s) => i && s), this.decorate("begin"), this.decorate("cancel"), this.decorate("commit"), this.decorate("updateSize"), this.on("commit", (i) => {
      !this.state.proposedWidth && !this.state.proposedWidthPercents && (this._cleanup(), i.stop());
    }, {
      priority: "high"
    });
  }
  /**
  * Stores the state of the resizable host geometry, such as the original width, the currently proposed height, etc.
  *
  * Note that a new state is created for each resize transaction.
  */
  get state() {
    return this._state;
  }
  /**
  * Makes resizer visible in the UI.
  */
  show() {
    this._options.editor.editing.view.change((i) => {
      i.removeClass("ck-hidden", this._viewResizerWrapper);
    });
  }
  /**
  * Hides resizer in the UI.
  */
  hide() {
    this._options.editor.editing.view.change((i) => {
      i.addClass("ck-hidden", this._viewResizerWrapper);
    });
  }
  /**
  * Attaches the resizer to the DOM.
  */
  attach() {
    const e = this, i = this._options.viewElement;
    this._options.editor.editing.view.change((r) => {
      const o = r.createUIElement("div", {
        class: "ck ck-reset_all ck-widget__resizer"
      }, function(a) {
        const l = this.toDomElement(a);
        return e._appendHandles(l), e._appendSizeUI(l), l;
      });
      r.insert(r.createPositionAt(i, "end"), o), r.addClass("ck-widget_with-resizer", i), this._viewResizerWrapper = o, this.isVisible || this.hide();
    }), this.on("change:isVisible", () => {
      this.isVisible ? (this.show(), this.redraw()) : this.hide();
    });
  }
  /**
  * Starts the resizing process.
  *
  * Creates a new {@link #state} for the current process.
  *
  * @fires begin
  * @param domResizeHandle Clicked handle.
  */
  begin(e) {
    this._state = new YM(this._options), this._sizeView._bindToState(this._options, this.state), this._initialViewWidth = this._options.viewElement.getStyle("width"), this.state.begin(e, this._getHandleHost(), this._getResizeHost());
  }
  /**
  * Updates the proposed size based on `domEventData`.
  *
  * @fires updateSize
  */
  updateSize(e) {
    const i = this._proposeNewSize(e);
    this._options.editor.editing.view.change((u) => {
      const d = this._options.unit || "%", h = (d === "%" ? i.widthPercents : i.width) + d;
      u.setStyle("width", h, this._options.viewElement);
    });
    const r = this._getHandleHost(), o = new ke(r), a = Math.round(o.width), l = Math.round(o.height), c = new ke(r);
    i.width = Math.round(c.width), i.height = Math.round(c.height), this.redraw(o), this.state.update({
      ...i,
      handleHostWidth: a,
      handleHostHeight: l
    });
  }
  /**
  * Applies the geometry proposed with the resizer.
  *
  * @fires commit
  */
  commit() {
    const e = this._options.unit || "%", i = (e === "%" ? this.state.proposedWidthPercents : this.state.proposedWidth) + e;
    this._options.editor.editing.view.change(() => {
      this._cleanup(), this._options.onCommit(i);
    });
  }
  /**
  * Cancels and rejects the proposed resize dimensions, hiding the UI.
  *
  * @fires cancel
  */
  cancel() {
    this._cleanup();
  }
  /**
  * Destroys the resizer.
  */
  destroy() {
    this.cancel();
  }
  /**
  * Redraws the resizer.
  *
  * @param handleHostRect Handle host rectangle might be given to improve performance.
  */
  redraw(e) {
    const i = this._domResizerWrapper;
    if (!sN(i))
      return;
    const s = i.parentElement, r = this._getHandleHost(), o = this._viewResizerWrapper, a = [
      o.getStyle("width"),
      o.getStyle("height"),
      o.getStyle("left"),
      o.getStyle("top")
    ];
    let l;
    if (s.isSameNode(r)) {
      const c = e || new ke(r);
      l = [
        c.width + "px",
        c.height + "px",
        void 0,
        void 0
      ];
    } else
      l = [
        r.offsetWidth + "px",
        r.offsetHeight + "px",
        r.offsetLeft + "px",
        r.offsetTop + "px"
      ];
    Zt(a, l) !== "same" && this._options.editor.editing.view.change((c) => {
      c.setStyle({
        width: l[0],
        height: l[1],
        left: l[2],
        top: l[3]
      }, o);
    });
  }
  containsHandle(e) {
    return this._domResizerWrapper.contains(e);
  }
  static isResizeHandle(e) {
    return e.classList.contains("ck-widget__resizer__handle");
  }
  /**
  * Cleans up the context state.
  */
  _cleanup() {
    this._sizeView._dismiss(), this._options.editor.editing.view.change((i) => {
      i.setStyle("width", this._initialViewWidth, this._options.viewElement);
    });
  }
  /**
  * Calculates the proposed size as the resize handles are dragged.
  *
  * @param domEventData Event data that caused the size update request. It should be used to calculate the proposed size.
  */
  _proposeNewSize(e) {
    const i = this.state, s = nN(e), r = this._options.isCentered ? this._options.isCentered(this) : !0, o = {
      x: i._referenceCoordinates.x - (s.x + i.originalWidth),
      y: s.y - i.originalHeight - i._referenceCoordinates.y
    };
    r && i.activeHandlePosition.endsWith("-right") && (o.x = s.x - (i._referenceCoordinates.x + i.originalWidth)), r && (o.x *= 2);
    let a = Math.abs(i.originalWidth + o.x), l = Math.abs(i.originalHeight + o.y);
    return (a / i.aspectRatio > l ? "width" : "height") == "width" ? l = a / i.aspectRatio : a = l * i.aspectRatio, {
      width: Math.round(a),
      height: Math.round(l),
      widthPercents: Math.min(Math.round(i.originalWidthPercents / i.originalWidth * a * 100) / 100, 100)
    };
  }
  /**
  * Obtains the resize host.
  *
  * Resize host is an object that receives dimensions which are the result of resizing.
  */
  _getResizeHost() {
    const e = this._domResizerWrapper.parentElement;
    return this._options.getResizeHost(e);
  }
  /**
  * Obtains the handle host.
  *
  * Handle host is an object that the handles are aligned to.
  *
  * Handle host will not always be an entire widget itself. Take an image as an example. The image widget
  * contains an image and a caption. Only the image should be surrounded with handles.
  */
  _getHandleHost() {
    const e = this._domResizerWrapper.parentElement;
    return this._options.getHandleHost(e);
  }
  /**
  * DOM container of the entire resize UI.
  *
  * Note that this property will have a value only after the element bound with the resizer is rendered
  * (otherwise `null`).
  */
  get _domResizerWrapper() {
    return this._options.editor.editing.view.domConverter.mapViewToDom(this._viewResizerWrapper);
  }
  /**
  * Renders the resize handles in the DOM.
  *
  * @param domElement The resizer wrapper.
  */
  _appendHandles(e) {
    const i = [
      "top-left",
      "top-right",
      "bottom-right",
      "bottom-left"
    ];
    for (const s of i)
      e.appendChild(new ri({
        tag: "div",
        attributes: {
          class: `ck-widget__resizer__handle ${iN(s)}`
        }
      }).render());
  }
  /**
  * Sets up the {@link #_sizeView} property and adds it to the passed `domElement`.
  */
  _appendSizeUI(e) {
    this._sizeView = new tN(), this._sizeView.render(), e.appendChild(this._sizeView.element);
  }
}
function iN(n) {
  return `ck-widget__resizer__handle-${n}`;
}
function nN(n) {
  return {
    x: n.pageX,
    y: n.pageY
  };
}
function sN(n) {
  return n && n.ownerDocument && n.ownerDocument.contains(n);
}
class md extends I {
  constructor() {
    super(...arguments);
    /**
    * A map of resizers created using this plugin instance.
    */
    f(this, "_resizers", /* @__PURE__ */ new Map());
    f(this, "_observer");
    f(this, "_redrawSelectedResizerThrottled");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "WidgetResize";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor.editing, i = ae.window.document;
    this.set("selectedResizer", null), this.set("_activeResizer", null), e.view.addObserver(Yc), this._observer = new (li())(), this.listenTo(e.view.document, "mousedown", this._mouseDownListener.bind(this), {
      priority: "high"
    }), this._observer.listenTo(i, "mousemove", this._mouseMoveListener.bind(this)), this._observer.listenTo(i, "mouseup", this._mouseUpListener.bind(this)), this._redrawSelectedResizerThrottled = $s(() => this.redrawSelectedResizer(), 200), this.editor.ui.on("update", this._redrawSelectedResizerThrottled), this.editor.model.document.on("change", () => {
      for (const [r, o] of this._resizers)
        r.isAttached() || (this._resizers.delete(r), o.destroy());
    }, {
      priority: "lowest"
    }), this._observer.listenTo(ae.window, "resize", this._redrawSelectedResizerThrottled);
    const s = this.editor.editing.view.document.selection;
    s.on("change", () => {
      const r = s.getSelectedElement(), o = this.getResizerByViewElement(r) || null;
      o ? this.select(o) : this.deselect();
    });
  }
  /**
  * Redraws the selected resizer if there is any selected resizer and if it is visible.
  */
  redrawSelectedResizer() {
    this.selectedResizer && this.selectedResizer.isVisible && this.selectedResizer.redraw();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._observer.stopListening();
    for (const e of this._resizers.values())
      e.destroy();
    this._redrawSelectedResizerThrottled.cancel();
  }
  /**
  * Marks resizer as selected.
  */
  select(e) {
    this.deselect(), this.selectedResizer = e, this.selectedResizer.isSelected = !0;
  }
  /**
  * Deselects currently set resizer.
  */
  deselect() {
    this.selectedResizer && (this.selectedResizer.isSelected = !1), this.selectedResizer = null;
  }
  /**
  * @param options Resizer options.
  */
  attachTo(e) {
    const i = new i1(e), s = this.editor.plugins;
    if (i.attach(), s.has("WidgetToolbarRepository")) {
      const a = s.get("WidgetToolbarRepository");
      i.on("begin", () => {
        a.forceDisabled("resize");
      }, {
        priority: "lowest"
      }), i.on("cancel", () => {
        a.clearForceDisabled("resize");
      }, {
        priority: "highest"
      }), i.on("commit", () => {
        a.clearForceDisabled("resize");
      }, {
        priority: "highest"
      });
    }
    this._resizers.set(e.viewElement, i);
    const o = this.editor.editing.view.document.selection.getSelectedElement();
    return this.getResizerByViewElement(o) == i && this.select(i), i;
  }
  /**
  * Returns a resizer created for a given view element (widget element).
  *
  * @param viewElement View element associated with the resizer.
  */
  getResizerByViewElement(e) {
    return this._resizers.get(e);
  }
  /**
  * Returns a resizer that contains a given resize handle.
  */
  _getResizerByHandle(e) {
    for (const i of this._resizers.values())
      if (i.containsHandle(e))
        return i;
  }
  /**
  * @param domEventData Native DOM event.
  */
  _mouseDownListener(e, i) {
    const s = i.domTarget;
    i1.isResizeHandle(s) && (this._activeResizer = this._getResizerByHandle(s) || null, this._activeResizer && (this._activeResizer.begin(s), e.stop(), i.preventDefault()));
  }
  /**
  * @param domEventData Native DOM event.
  */
  _mouseMoveListener(e, i) {
    this._activeResizer && this._activeResizer.updateSize(i);
  }
  _mouseUpListener() {
    this._activeResizer && (this._activeResizer.commit(), this._activeResizer = null);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class rN extends ee {
  /**
  * Creates an instance of the {@link module:bookmark/ui/bookmarkformview~BookmarkFormView} class.
  *
  * Also see {@link #render}.
  *
  * @param locale The localization services instance.
  * @param validators  Form validators used by {@link #isValid}.
  */
  constructor(e, i) {
    super(e);
    /**
    * Tracks information about DOM focus in the form.
    */
    f(this, "focusTracker", new ze());
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes", new Ze());
    /**
    * The ID input view.
    */
    f(this, "idInputView");
    /**
    * The Submit button view.
    */
    f(this, "buttonView");
    /**
    * A collection of form child views in the form.
    */
    f(this, "children");
    /**
    * An array of form validators used by {@link #isValid}.
    */
    f(this, "_validators");
    /**
    * A collection of views that can be focused in the form.
    */
    f(this, "_focusables", new Et());
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    f(this, "_focusCycler");
    const s = e.t;
    this._validators = i, this.idInputView = this._createIdInput(), this.buttonView = this._createButton(s("Insert"), "ck-button-action ck-button-bold"), this.buttonView.type = "submit", this.children = this._createViewChildren(), this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    });
    const r = [
      "ck",
      "ck-bookmark-view"
    ];
    this.setTemplate({
      tag: "form",
      attributes: {
        class: r,
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Tn({
      view: this
    }), [
      this.idInputView,
      this.buttonView
    ].forEach((i) => {
      this._focusables.add(i), this.focusTracker.add(i.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist {@link #_focusables} in the form.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Validates the form and returns `false` when some fields are invalid.
  */
  isValid() {
    this.resetFormStatus();
    for (const e of this._validators) {
      const i = e(this);
      if (i)
        return this.idInputView.errorText = i, !1;
    }
    return !0;
  }
  /**
  * Cleans up the supplementary error and information text of the {@link #idInputView}
  * bringing them back to the state when the form has been displayed for the first time.
  *
  * See {@link #isValid}.
  */
  resetFormStatus() {
    this.idInputView.errorText = null;
  }
  /**
  * Creates header and form view.
  */
  _createViewChildren() {
    const e = this.createCollection(), i = this.t;
    return e.add(new Va(this.locale, {
      label: i("Bookmark")
    })), e.add(this._createFormContentView()), e;
  }
  /**
  * Creates form content view with input and button.
  */
  _createFormContentView() {
    const e = new ee(this.locale), i = this.createCollection(), s = [
      "ck",
      "ck-bookmark-form",
      "ck-responsive-form"
    ];
    return i.add(this.idInputView), i.add(this.buttonView), e.setTemplate({
      tag: "div",
      attributes: {
        class: s
      },
      children: i
    }), e;
  }
  /**
  * Creates a labeled input view.
  *
  * @returns Labeled field view instance.
  */
  _createIdInput() {
    const e = this.locale.t, i = new gt(this.locale, pi);
    return i.label = e("Bookmark name"), i.infoText = e("Enter the bookmark name without spaces."), i;
  }
  /**
  * Creates a button view.
  *
  * @param label The button label.
  * @param className The additional button CSS class name.
  * @returns The button view instance.
  */
  _createButton(e, i) {
    const s = new le(this.locale);
    return s.set({
      label: e,
      withText: !0
    }), s.extendTemplate({
      attributes: {
        class: i
      }
    }), s;
  }
  /**
  * The native DOM `value` of the {@link #idInputView} element.
  *
  * **Note**: Do not confuse it with the {@link module:ui/inputtext/inputtextview~InputTextView#value}
  * which works one way only and may not represent the actual state of the component in the DOM.
  */
  get id() {
    const { element: e } = this.idInputView.fieldView;
    return e ? e.value.trim() : null;
  }
}
class oN extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Tracks information about DOM focus in the actions.
    */
    f(this, "focusTracker", new ze());
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes", new Ze());
    /**
    * The bookmark preview view.
    */
    f(this, "bookmarkPreviewView");
    /**
    * The remove button view.
    */
    f(this, "removeButtonView");
    /**
    * The edit bookmark button view.
    */
    f(this, "editButtonView");
    /**
    * A collection of views that can be focused in the view.
    */
    f(this, "_focusables", new Et());
    /**
    * Helps cycling over {@link #_focusables} in the view.
    */
    f(this, "_focusCycler");
    const i = e.t;
    this.bookmarkPreviewView = this._createBookmarkPreviewView(), this.removeButtonView = this._createButton(i("Remove bookmark"), Z.remove, "remove", this.bookmarkPreviewView), this.editButtonView = this._createButton(i("Edit bookmark"), Z.pencil, "edit", this.bookmarkPreviewView), this.set("id", void 0), this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-bookmark-actions",
          "ck-responsive-form"
        ],
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: [
        this.bookmarkPreviewView,
        this.editButtonView,
        this.removeButtonView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), [
      this.editButtonView,
      this.removeButtonView
    ].forEach((i) => {
      this._focusables.add(i), this.focusTracker.add(i.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist {@link #_focusables} in the actions.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Creates a button view.
  *
  * @param label The button label.
  * @param icon The button icon.
  * @param eventName An event name that the `ButtonView#execute` event will be delegated to.
  * @param additionalLabel An additional label outside the button.
  * @returns The button view instance.
  */
  _createButton(e, i, s, r) {
    const o = new le(this.locale);
    return o.set({
      label: e,
      icon: i,
      tooltip: !0
    }), o.delegate("execute").to(this, s), o.labelView.unbind("id"), o.labelView.bind("id").to(o, "ariaLabelledBy", (a) => aN(a)), o.ariaLabelledBy = `${o.ariaLabelledBy} ${r.id}`, o;
  }
  /**
  * Creates a bookmark name preview label.
  *
  * @returns The label view instance.
  */
  _createBookmarkPreviewView() {
    const e = new gi(this.locale);
    return e.extendTemplate({
      attributes: {
        class: [
          "ck",
          "ck-bookmark-actions__preview"
        ]
      }
    }), e.bind("text").to(this, "id"), e;
  }
}
function aN(n) {
  return n.split(" ")[0];
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function ck(n) {
  return !(!n || typeof n != "string" || /\s/.test(n));
}
class lN extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model.document.selection, i = this._getPositionToInsertBookmark(e);
    this.isEnabled = !!i;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options Command options.
  * @param options.bookmarkId The value of the `bookmarkId` attribute.
  */
  execute(t) {
    if (!t)
      return;
    const { bookmarkId: e } = t;
    if (!ck(e)) {
      Ve("insert-bookmark-command-executed-with-invalid-name");
      return;
    }
    const i = this.editor, s = i.model, r = s.document.selection;
    s.change((o) => {
      let a = this._getPositionToInsertBookmark(r);
      if (!s.schema.checkChild(a, "bookmark")) {
        const u = i.execute("insertParagraph", {
          position: a
        });
        if (!u)
          return;
        a = u;
      }
      const c = o.createElement("bookmark", {
        ...Object.fromEntries(r.getAttributes()),
        bookmarkId: e
      });
      s.insertObject(c, a, null, {
        setSelection: "on"
      });
    });
  }
  /**
  * Returns the position where the bookmark can be inserted. And if it is not possible to insert a bookmark,
  * check if it is possible to insert a paragraph.
  */
  _getPositionToInsertBookmark(t) {
    const e = this.editor.model, i = e.schema, s = t.getFirstRange(), r = s.start;
    if (gd(r, i))
      return r;
    for (const { previousPosition: o, item: a } of s) {
      if (a.is("element") && i.checkChild(a, "$text") && gd(a, i))
        return e.createPositionAt(a, 0);
      if (gd(o, i))
        return o;
    }
    return null;
  }
}
function gd(n, t) {
  return t.checkChild(n, "bookmark") ? !0 : t.checkChild(n, "paragraph") ? t.checkChild("paragraph", "bookmark") : !1;
}
class cN extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model.document.selection, i = n1(e);
    this.isEnabled = !!i, this.value = i ? i.getAttribute("bookmarkId") : void 0;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options Command options.
  * @param options.bookmarkId The new value of the `bookmarkId` attribute to set.
  */
  execute(t) {
    if (!t)
      return;
    const { bookmarkId: e } = t;
    if (!ck(e)) {
      Ve("update-bookmark-command-executed-with-invalid-name");
      return;
    }
    const i = this.editor.model, s = i.document.selection, r = n1(s);
    r && i.change((o) => {
      o.setAttribute("bookmarkId", e, r);
    });
  }
}
function n1(n) {
  const t = n.getSelectedElement();
  return t && t.is("element", "bookmark") ? t : null;
}
class Yf extends I {
  constructor() {
    super(...arguments);
    /**
    * A collection of bookmarks elements in the document.
    */
    f(this, "_bookmarkElements", /* @__PURE__ */ new Map());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BookmarkEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const { editor: e } = this;
    this._defineSchema(), this._defineConverters(), e.commands.add("insertBookmark", new lN(e)), e.commands.add("updateBookmark", new cN(e)), this.listenTo(e.model.document, "change:data", () => {
      this._trackBookmarkElements();
    });
  }
  /**
  * Returns the model element for the given bookmark ID if it exists.
  */
  getElementForBookmarkId(e) {
    for (const [i, s] of this._bookmarkElements)
      if (s == e)
        return i;
    return null;
  }
  /**
  * Defines the schema for the bookmark feature.
  */
  _defineSchema() {
    this.editor.model.schema.register("bookmark", {
      inheritAllFrom: "$inlineObject",
      allowAttributes: "bookmarkId",
      disallowAttributes: [
        "linkHref",
        "htmlA"
      ]
    });
  }
  /**
  * Defines the converters for the bookmark feature.
  */
  _defineConverters() {
    const { editor: e } = this, { conversion: i, t: s } = e;
    e.data.htmlProcessor.domConverter.registerInlineObjectMatcher((r) => Ch(r)), e.editing.view.domConverter.registerInlineObjectMatcher((r) => Ch(r, !1)), i.for("dataDowncast").elementToElement({
      model: {
        name: "bookmark",
        attributes: [
          "bookmarkId"
        ]
      },
      view: (r, { writer: o }) => o.createEmptyElement("a", {
        id: r.getAttribute("bookmarkId")
      })
    }), i.for("editingDowncast").elementToElement({
      model: {
        name: "bookmark",
        attributes: [
          "bookmarkId"
        ]
      },
      view: (r, { writer: o }) => {
        const a = r.getAttribute("bookmarkId"), l = o.createContainerElement("a", {
          id: a,
          class: "ck-bookmark"
        }, [
          this._createBookmarkUIElement(o)
        ]);
        return this._bookmarkElements.set(r, a), Qn(l, o, {
          label: () => `${a} ${s("bookmark widget")}`
        });
      }
    }), i.for("upcast").add((r) => r.on("element:a", uN(e)));
  }
  /**
  * Creates a UI element for the `bookmark` representation in editing view.
  */
  _createBookmarkUIElement(e) {
    return e.createUIElement("span", {
      class: "ck-bookmark__icon"
    }, function(i) {
      const s = this.toDomElement(i), r = new Qi();
      return r.set({
        content: Z.bookmarkInline,
        isColorInherited: !1
      }), r.render(), s.appendChild(r.element), s;
    });
  }
  /**
  * Tracking the added or removed bookmark elements.
  */
  _trackBookmarkElements() {
    this._bookmarkElements.forEach((e, i) => {
      i.root.rootName === "$graveyard" && this._bookmarkElements.delete(i);
    });
  }
}
function Ch(n, t = !0) {
  if (!(n.name === "a") || t && !n.isEmpty)
    return null;
  const i = n.hasAttribute("id"), s = n.hasAttribute("name"), r = n.hasAttribute("href");
  return i && !r ? {
    name: !0,
    attributes: [
      "id"
    ]
  } : s && !r ? {
    name: !0,
    attributes: [
      "name"
    ]
  } : null;
}
function uN(n) {
  return (t, e, i) => {
    const s = e.viewItem, r = Ch(s, !1);
    if (!r || !i.consumable.test(s, r) || !dN(n) && !s.isEmpty)
      return;
    const a = i.writer, l = s.getAttribute("id"), c = s.getAttribute("name"), u = l || c, d = a.createElement("bookmark", {
      bookmarkId: u
    });
    if (!i.safeInsert(d, e.modelCursor))
      return;
    i.consumable.consume(s, r), l === c && i.consumable.consume(s, {
      attributes: [
        "name"
      ]
    }), i.updateConversionResult(d, e);
    const { modelCursor: h, modelRange: m } = i.convertChildren(s, e.modelCursor);
    e.modelCursor = h, e.modelRange = a.createRange(e.modelRange.start, m.end);
  };
}
function dN(n) {
  const t = n.config.get("bookmark.enableNonEmptyAnchorConversion");
  return t !== void 0 ? t : !0;
}
const an = "bookmark-ui";
class hN extends I {
  constructor() {
    super(...arguments);
    /**
    * The actions view displayed inside of the balloon.
    */
    f(this, "actionsView", null);
    /**
    * The form view displayed inside the balloon.
    */
    f(this, "formView", null);
    /**
    * The contextual balloon plugin instance.
    */
    f(this, "_balloon");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Yf,
      ai
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "BookmarkUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.editing.view.addObserver(oy), this._balloon = e.plugins.get(ai), this._createToolbarBookmarkButton(), this._enableBalloonActivators(), e.conversion.for("editingDowncast").markerToHighlight({
      model: an,
      view: {
        classes: [
          "ck-fake-bookmark-selection"
        ]
      }
    }), e.conversion.for("editingDowncast").markerToElement({
      model: an,
      view: (i, { writer: s }) => {
        if (!i.markerRange.isCollapsed)
          return null;
        const r = s.createUIElement("span");
        return s.addClass([
          "ck-fake-bookmark-selection",
          "ck-fake-bookmark-selection_collapsed"
        ], r), r;
      }
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.formView && this.formView.destroy(), this.actionsView && this.actionsView.destroy();
  }
  /**
  * Creates views.
  */
  _createViews() {
    this.actionsView = this._createActionsView(), this.formView = this._createFormView(), this._enableUserBalloonInteractions();
  }
  /**
  * Creates the {@link module:bookmark/ui/bookmarkactionsview~BookmarkActionsView} instance.
  */
  _createActionsView() {
    const e = this.editor, i = new oN(e.locale), s = e.commands.get("updateBookmark"), r = e.commands.get("delete");
    return i.bind("id").to(s, "value"), i.editButtonView.bind("isEnabled").to(s), i.removeButtonView.bind("isEnabled").to(r), this.listenTo(i, "edit", () => {
      this._addFormView();
    }), this.listenTo(i, "remove", () => {
      this._hideUI(), e.execute("delete");
    }), i.keystrokes.set("Esc", (o, a) => {
      this._hideUI(), a();
    }), i;
  }
  /**
  * Creates the {@link module:bookmark/ui/bookmarkformview~BookmarkFormView} instance.
  */
  _createFormView() {
    const e = this.editor, i = e.locale, s = e.commands.get("insertBookmark"), r = e.commands.get("updateBookmark"), o = [
      s,
      r
    ], a = new (no(rN))(i, fN(e));
    return a.idInputView.fieldView.bind("value").to(r, "value"), a.idInputView.bind("isEnabled").toMany(o, "isEnabled", (...l) => l.some((c) => c)), a.buttonView.bind("isEnabled").toMany(o, "isEnabled", (...l) => l.some((c) => c)), this.listenTo(a, "submit", () => {
      if (a.isValid()) {
        const l = a.id;
        this._getSelectedBookmarkElement() ? e.execute("updateBookmark", {
          bookmarkId: l
        }) : e.execute("insertBookmark", {
          bookmarkId: l
        }), this._closeFormView();
      }
    }), this.listenTo(a.idInputView, "change:errorText", () => {
      e.ui.update();
    }), a.keystrokes.set("Esc", (l, c) => {
      this._closeFormView(), c();
    }), a;
  }
  /**
  * Creates a toolbar Bookmark button. Clicking this button will show
  * a {@link #_balloon} attached to the selection.
  */
  _createToolbarBookmarkButton() {
    const e = this.editor;
    e.ui.componentFactory.add("bookmark", () => {
      const i = this._createButton(le);
      return i.set({
        tooltip: !0
      }), i;
    }), e.ui.componentFactory.add("menuBar:bookmark", () => this._createButton(Fe));
  }
  /**
  * Creates a button for `bookmark` command to use either in toolbar or in menu bar.
  */
  _createButton(e) {
    const i = this.editor, s = i.locale, r = new e(s), o = i.commands.get("insertBookmark"), a = i.commands.get("updateBookmark"), l = s.t;
    return r.set({
      label: l("Bookmark"),
      icon: Z.bookmark
    }), this.listenTo(r, "execute", () => this._showUI(!0)), r.bind("isEnabled").toMany([
      o,
      a
    ], "isEnabled", (...c) => c.some((u) => u)), r.bind("isOn").to(a, "value", (c) => !!c), r;
  }
  /**
  * Attaches actions that control whether the balloon panel containing the
  * {@link #formView} should be displayed.
  */
  _enableBalloonActivators() {
    const i = this.editor.editing.view.document;
    this.listenTo(i, "click", () => {
      this._getSelectedBookmarkElement() && this._showUI();
    });
  }
  /**
  * Attaches actions that control whether the balloon panel containing the
  * {@link #formView} is visible or not.
  */
  _enableUserBalloonInteractions() {
    this.editor.keystrokes.set("Tab", (e, i) => {
      this._areActionsVisible && !this.actionsView.focusTracker.isFocused && (this.actionsView.focus(), i());
    }, {
      // Use the high priority because the bookmark UI navigation is more important
      // than other feature's actions, e.g. list indentation.
      priority: "high"
    }), this.editor.keystrokes.set("Esc", (e, i) => {
      this._isUIVisible && (this._hideUI(), i());
    }), Xn({
      emitter: this.formView,
      activator: () => this._isUIInPanel,
      contextElements: () => [
        this._balloon.view.element
      ],
      callback: () => this._hideUI()
    });
  }
  /**
  * Updates the button label. If bookmark is selected label is set to 'Update' otherwise
  * it is 'Insert'.
  */
  _updateFormButtonLabel(e) {
    const i = this.editor.locale.t;
    this.formView.buttonView.label = i(e ? "Update" : "Insert");
  }
  /**
  * Adds the {@link #actionsView} to the {@link #_balloon}.
  *
  * @internal
  */
  _addActionsView() {
    this.actionsView || this._createViews(), !this._areActionsInPanel && this._balloon.add({
      view: this.actionsView,
      position: this._getBalloonPositionData()
    });
  }
  /**
  * Adds the {@link #formView} to the {@link #_balloon}.
  */
  _addFormView() {
    if (this.formView || this._createViews(), this._isFormInPanel)
      return;
    const i = this.editor.commands.get("updateBookmark");
    this.formView.disableCssTransitions(), this.formView.resetFormStatus(), this._balloon.add({
      view: this.formView,
      position: this._getBalloonPositionData()
    }), this.formView.idInputView.fieldView.value = i.value || "", this._balloon.visibleView === this.formView && this.formView.idInputView.fieldView.select(), this.formView.enableCssTransitions();
  }
  /**
  * Closes the form view. Decides whether the balloon should be hidden completely.
  */
  _closeFormView() {
    this.editor.commands.get("updateBookmark").value !== void 0 ? this._removeFormView() : this._hideUI();
  }
  /**
  * Removes the {@link #formView} from the {@link #_balloon}.
  */
  _removeFormView() {
    this._isFormInPanel && (this.formView.buttonView.focus(), this.formView.idInputView.fieldView.reset(), this._balloon.remove(this.formView), this.editor.editing.view.focus(), this._hideFakeVisualSelection());
  }
  /**
  * Shows the correct UI type. It is either {@link #formView} or {@link #actionsView}.
  */
  _showUI(e = !1) {
    this.formView || this._createViews(), this._getSelectedBookmarkElement() ? (this._areActionsVisible ? this._addFormView() : this._addActionsView(), e && this._balloon.showStack("main")) : (this._showFakeVisualSelection(), this._addActionsView(), e && this._balloon.showStack("main"), this._addFormView()), this._startUpdatingUI();
  }
  /**
  * Removes the {@link #formView} from the {@link #_balloon}.
  *
  * See {@link #_addFormView}, {@link #_addActionsView}.
  */
  _hideUI() {
    if (!this._isUIInPanel)
      return;
    const e = this.editor;
    this.stopListening(e.ui, "update"), this.stopListening(this._balloon, "change:visibleView"), e.editing.view.focus(), this._removeFormView(), this._balloon.remove(this.actionsView), this._hideFakeVisualSelection();
  }
  /**
  * Makes the UI react to the {@link module:ui/editorui/editorui~EditorUI#event:update} event to
  * reposition itself when the editor UI should be refreshed.
  *
  * See: {@link #_hideUI} to learn when the UI stops reacting to the `update` event.
  */
  _startUpdatingUI() {
    const e = this.editor, i = e.editing.view.document;
    let s = this._getSelectedBookmarkElement(), r = a();
    this._updateFormButtonLabel(!!s);
    const o = () => {
      const l = this._getSelectedBookmarkElement(), c = a();
      s && !l || !s && c !== r ? this._hideUI() : this._isUIVisible && this._balloon.updatePosition(this._getBalloonPositionData()), this._updateFormButtonLabel(!!s), s = l, r = c;
    };
    function a() {
      return i.selection.focus.getAncestors().reverse().find((l) => l.is("element"));
    }
    this.listenTo(e.ui, "update", o), this.listenTo(this._balloon, "change:visibleView", o);
  }
  /**
  * Returns `true` when {@link #formView} is in the {@link #_balloon}.
  */
  get _isFormInPanel() {
    return !!this.formView && this._balloon.hasView(this.formView);
  }
  /**
  * Returns `true` when {@link #actionsView} is in the {@link #_balloon}.
  */
  get _areActionsInPanel() {
    return !!this.actionsView && this._balloon.hasView(this.actionsView);
  }
  /**
  * Returns `true` when {@link #actionsView} is in the {@link #_balloon} and it is
  * currently visible.
  */
  get _areActionsVisible() {
    return !!this.actionsView && this._balloon.visibleView === this.actionsView;
  }
  /**
  * Returns `true` when {@link #actionsView} or {@link #formView} is in the {@link #_balloon}.
  */
  get _isUIInPanel() {
    return this._isFormInPanel || this._areActionsInPanel;
  }
  /**
  * Returns `true` when {@link #actionsView} or {@link #formView} is in the {@link #_balloon} and it is
  * currently visible.
  */
  get _isUIVisible() {
    const e = this._balloon.visibleView;
    return !!this.formView && e == this.formView || this._areActionsVisible;
  }
  /**
  * Returns positioning options for the {@link #_balloon}. They control the way the balloon is attached
  * to the target element or selection.
  */
  _getBalloonPositionData() {
    const e = this.editor.editing.view, i = this.editor.model;
    let s;
    const r = this._getSelectedBookmarkElement();
    if (i.markers.has(an)) {
      const o = Array.from(this.editor.editing.mapper.markerNameToElements(an)), a = e.createRange(e.createPositionBefore(o[0]), e.createPositionAfter(o[o.length - 1]));
      s = e.domConverter.viewRangeToDom(a);
    } else r && (s = () => {
      const o = this.editor.editing.mapper, a = e.domConverter, l = o.toViewElement(r);
      return a.mapViewToDom(l);
    });
    return s && {
      target: s
    };
  }
  /**
  * Returns the bookmark {@link module:engine/view/attributeelement~AttributeElement} under
  * the {@link module:engine/view/document~Document editing view's} selection or `null`
  * if there is none.
  */
  _getSelectedBookmarkElement() {
    const i = this.editor.model.document.selection.getSelectedElement();
    return i && i.is("element", "bookmark") ? i : null;
  }
  /**
  * Displays a fake visual selection when the contextual balloon is displayed.
  *
  * This adds a 'bookmark-ui' marker into the document that is rendered as a highlight on selected text fragment.
  */
  _showFakeVisualSelection() {
    const e = this.editor.model;
    e.change((i) => {
      const s = e.document.selection.getFirstRange();
      if (e.markers.has(an))
        i.updateMarker(an, {
          range: s
        });
      else if (s.start.isAtEnd) {
        const r = s.start.getLastMatchingPosition(({ item: o }) => !e.schema.isContent(o), {
          boundaries: s
        });
        i.addMarker(an, {
          usingOperation: !1,
          affectsData: !1,
          range: i.createRange(r, s.end)
        });
      } else
        i.addMarker(an, {
          usingOperation: !1,
          affectsData: !1,
          range: s
        });
    });
  }
  /**
  * Hides the fake visual selection created in {@link #_showFakeVisualSelection}.
  */
  _hideFakeVisualSelection() {
    const e = this.editor.model;
    e.markers.has(an) && e.change((i) => {
      i.removeMarker(an);
    });
  }
}
function fN(n) {
  const { t } = n, e = n.plugins.get(Yf);
  return [
    (i) => {
      if (!i.id)
        return t("Bookmark must not be empty.");
    },
    (i) => {
      if (i.id && /\s/.test(i.id))
        return t("Bookmark name cannot contain space characters.");
    },
    (i) => {
      const s = n.model.document.selection.getSelectedElement(), r = e.getElementForBookmarkId(i.id);
      if (s !== r && r)
        return t("Bookmark name already exists.");
    }
  ];
}
class mN extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Bookmark";
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Yf,
      hN,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class ga extends xn {
  constructor(e) {
    super(e);
    f(this, "domEventType", [
      "paste",
      "copy",
      "cut",
      "drop",
      "dragover",
      "dragstart",
      "dragend",
      "dragenter",
      "dragleave"
    ]);
    const i = this.document;
    this.listenTo(i, "paste", s("clipboardInput"), {
      priority: "low"
    }), this.listenTo(i, "drop", s("clipboardInput"), {
      priority: "low"
    }), this.listenTo(i, "dragover", s("dragging"), {
      priority: "low"
    });
    function s(r) {
      return (o, a) => {
        a.preventDefault();
        const l = a.dropRange ? [
          a.dropRange
        ] : null, c = new jn(i, r);
        i.fire(c, {
          dataTransfer: a.dataTransfer,
          method: o.name,
          targetRanges: l,
          target: a.target,
          domEvent: a.domEvent
        }), c.stop.called && a.stopPropagation();
      };
    }
  }
  onDomEvent(e) {
    const i = "clipboardData" in e ? e.clipboardData : e.dataTransfer, s = e.type == "drop" || e.type == "paste", r = {
      dataTransfer: new P_(i, {
        cacheFiles: s
      })
    };
    if (e.type == "drop" || e.type == "dragover") {
      const o = a_(e);
      r.dropRange = o && this.view.domConverter.domRangeToView(o);
    }
    this.fire(e.type, e, r);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function gN(n) {
  return n = n.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\r?\n\r?\n/g, "</p><p>").replace(/\r?\n/g, "<br>").replace(/\t/g, "&nbsp;&nbsp;&nbsp;&nbsp;").replace(/^\s/, "&nbsp;").replace(/\s$/, "&nbsp;").replace(/\s\s/g, " &nbsp;"), (n.includes("</p><p>") || n.includes("<br>")) && (n = `<p>${n}</p>`), n;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function pN(n) {
  return n.replace(/<span(?: class="Apple-converted-space"|)>(\s+)<\/span>/g, (t, e) => e.length == 1 ? " " : e).replace(/<!--[\s\S]*?-->/g, "");
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const s1 = [
  "figcaption",
  "li"
], r1 = [
  "ol",
  "ul"
];
function uk(n, t) {
  if (t.is("$text") || t.is("$textProxy"))
    return t.data;
  if (t.is("element", "img") && t.hasAttribute("alt"))
    return t.getAttribute("alt");
  if (t.is("element", "br"))
    return `
`;
  let e = "", i = null;
  for (const s of t.getChildren())
    e += bN(s, i) + uk(n, s), i = s;
  if (t.is("rawElement")) {
    const s = document.createElement("div");
    t.render(s, n), e += dk(s);
  }
  return e;
}
function dk(n) {
  let t = "";
  if (n.nodeType === Node.TEXT_NODE)
    return n.textContent;
  if (n.tagName === "BR")
    return `
`;
  for (const e of n.childNodes)
    t += dk(e);
  return t;
}
function bN(n, t) {
  return t ? n.is("element", "li") && !n.isEmpty && n.getChild(0).is("containerElement") || r1.includes(n.name) && r1.includes(t.name) ? `

` : !n.is("containerElement") && !t.is("containerElement") ? "" : s1.includes(n.name) || s1.includes(t.name) ? `
` : n.is("element") && n.getCustomProperty("dataPipeline:transparentRendering") || t.is("element") && t.getCustomProperty("dataPipeline:transparentRendering") ? "" : `

` : "";
}
class Go extends I {
  constructor() {
    super(...arguments);
    /**
    * Map of marker names that can be copied.
    *
    * @internal
    */
    f(this, "_markersToCopy", /* @__PURE__ */ new Map());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ClipboardMarkersUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Registers marker name as copyable in clipboard pipeline.
  *
  * @param markerName Name of marker that can be copied.
  * @param config Configuration that describes what can be performed on specified marker.
  * @internal
  */
  _registerMarkerToCopy(e, i) {
    this._markersToCopy.set(e, i);
  }
  /**
  * Performs copy markers on provided selection and paste it to fragment returned from `getCopiedFragment`.
  *
  * 	1. Picks all markers in provided selection.
  * 	2. Inserts fake markers to document.
  * 	3. Gets copied selection fragment from document.
  * 	4. Removes fake elements from fragment and document.
  * 	5. Inserts markers in the place of removed fake markers.
  *
  * Due to selection modification, when inserting items, `getCopiedFragment` must *always* operate on `writer.model.document.selection'.
  * Do not use any other custom selection object within callback, as this will lead to out-of-bounds exceptions in rare scenarios.
  *
  * @param action Type of clipboard action.
  * @param writer An instance of the model writer.
  * @param selection Selection to be checked.
  * @param getCopiedFragment	Callback that performs copy of selection and returns it as fragment.
  * @internal
  */
  _copySelectedFragmentWithMarkers(e, i, s = (r) => r.model.getSelectedContent(r.model.document.selection)) {
    return this.editor.model.change((r) => {
      const o = r.model.document.selection;
      r.setSelection(i);
      const a = this._insertFakeMarkersIntoSelection(r, r.model.document.selection, e), l = s(r), c = this._removeFakeMarkersInsideElement(r, l);
      for (const [u, d] of Object.entries(a)) {
        c[u] || (c[u] = r.createRangeIn(l));
        for (const h of d)
          r.remove(h);
      }
      l.markers.clear();
      for (const [u, d] of Object.entries(c))
        l.markers.set(u, d);
      return r.setSelection(o), l;
    });
  }
  /**
  * Performs paste of markers on already pasted element.
  *
  * 	1. Inserts fake markers that are present in fragment element (such fragment will be processed in `getPastedDocumentElement`).
  * 	2. Calls `getPastedDocumentElement` and gets element that is inserted into root model.
  * 	3. Removes all fake markers present in transformed element.
  * 	4. Inserts new markers with removed fake markers ranges into pasted fragment.
  *
  * There are multiple edge cases that have to be considered before calling this function:
  *
  * 	* `markers` are inserted into the same element that must be later transformed inside `getPastedDocumentElement`.
  * 	* Fake marker elements inside `getPastedDocumentElement` can be cloned, but their ranges cannot overlap.
  * 	* If `duplicateOnPaste` is `true` in marker config then associated marker ID is regenerated before pasting.
  *
  * @param action Type of clipboard action.
  * @param markers Object that maps marker name to corresponding range.
  * @param getPastedDocumentElement Getter used to get target markers element.
  * @internal
  */
  _pasteMarkersIntoTransformedElement(e, i) {
    const s = this._getPasteMarkersFromRangeMap(e);
    return this.editor.model.change((r) => {
      const o = this._insertFakeMarkersElements(r, s), a = i(r), l = this._removeFakeMarkersInsideElement(r, a);
      for (const c of Object.values(o).flat())
        r.remove(c);
      for (const [c, u] of Object.entries(l))
        r.model.markers.has(c) || r.addMarker(c, {
          usingOperation: !0,
          affectsData: !0,
          range: u
        });
      return a;
    });
  }
  /**
  * Pastes document fragment with markers to document.
  * If `duplicateOnPaste` is `true` in marker config then associated markers IDs
  * are regenerated before pasting to avoid markers duplications in content.
  *
  * @param fragment Document fragment that should contain already processed by pipeline markers.
  * @internal
  */
  _pasteFragmentWithMarkers(e) {
    const i = this._getPasteMarkersFromRangeMap(e.markers);
    e.markers.clear();
    for (const s of i)
      e.markers.set(s.name, s.range);
    return this.editor.model.insertContent(e);
  }
  /**
  * In some situations we have to perform copy on selected fragment with certain markers. This function allows to temporarily bypass
  * restrictions on markers that we want to copy.
  *
  * This function executes `executor()` callback. For the duration of the callback, if the clipboard pipeline is used to copy
  * content, markers with the specified name will be copied to the clipboard as well.
  *
  * @param markerName Which markers should be copied.
  * @param executor Callback executed.
  * @param config Optional configuration flags used to copy (such like partial copy flag).
  * @internal
  */
  _forceMarkersCopy(e, i, s = {
    allowedActions: "all",
    copyPartiallySelected: !0,
    duplicateOnPaste: !0
  }) {
    const r = this._markersToCopy.get(e);
    this._markersToCopy.set(e, s), i(), r ? this._markersToCopy.set(e, r) : this._markersToCopy.delete(e);
  }
  /**
  * Checks if marker can be copied.
  *
  * @param markerName Name of checked marker.
  * @param action Type of clipboard action. If null then checks only if marker is registered as copyable.
  * @internal
  */
  _isMarkerCopyable(e, i) {
    const s = this._getMarkerClipboardConfig(e);
    if (!s)
      return !1;
    if (!i)
      return !0;
    const { allowedActions: r } = s;
    return r === "all" || r.includes(i);
  }
  /**
  * Checks if marker has any clipboard copy behavior configuration.
  *
  * @param markerName Name of checked marker.
  */
  _hasMarkerConfiguration(e) {
    return !!this._getMarkerClipboardConfig(e);
  }
  /**
  * Returns marker's configuration flags passed during registration.
  *
  * @param markerName Name of marker that should be returned.
  * @internal
  */
  _getMarkerClipboardConfig(e) {
    const [i] = e.split(":");
    return this._markersToCopy.get(i) || null;
  }
  /**
  * First step of copying markers. It looks for markers intersecting with given selection and inserts `$marker` elements
  * at positions where document markers start or end. This way `$marker` elements can be easily copied together with
  * the rest of the content of the selection.
  *
  * @param writer An instance of the model writer.
  * @param selection Selection to be checked.
  * @param action Type of clipboard action.
  */
  _insertFakeMarkersIntoSelection(e, i, s) {
    const r = this._getCopyableMarkersFromSelection(e, i, s);
    return this._insertFakeMarkersElements(e, r);
  }
  /**
  * Returns array of markers that can be copied in specified selection.
  *
  * If marker cannot be copied partially (according to `copyPartiallySelected` configuration flag) and
  * is not present entirely in any selection range then it will be skipped.
  *
  * @param writer An instance of the model writer.
  * @param selection  Selection which will be checked.
  * @param action Type of clipboard action. If null then checks only if marker is registered as copyable.
  */
  _getCopyableMarkersFromSelection(e, i, s) {
    const r = Array.from(i.getRanges()), o = new Set(r.flatMap((l) => Array.from(e.model.markers.getMarkersIntersectingRange(l)))), a = (l) => {
      if (!this._isMarkerCopyable(l.name, s))
        return !1;
      const { copyPartiallySelected: u } = this._getMarkerClipboardConfig(l.name);
      if (!u) {
        const d = l.getRange();
        return r.some((h) => h.containsRange(d, !0));
      }
      return !0;
    };
    return Array.from(o).filter(a).map((l) => ({
      name: s === "dragstart" ? this._getUniqueMarkerName(l.name) : l.name,
      range: l.getRange()
    }));
  }
  /**
  * Picks all markers from markers map that can be pasted.
  * If `duplicateOnPaste` is `true`, it regenerates their IDs to ensure uniqueness.
  * If marker is not registered, it will be kept in the array anyway.
  *
  * @param markers Object that maps marker name to corresponding range.
  * @param action Type of clipboard action. If null then checks only if marker is registered as copyable.
  */
  _getPasteMarkersFromRangeMap(e, i = null) {
    const { model: s } = this.editor;
    return (e instanceof Map ? Array.from(e.entries()) : Object.entries(e)).flatMap(([o, a]) => {
      if (!this._hasMarkerConfiguration(o))
        return [
          {
            name: o,
            range: a
          }
        ];
      if (this._isMarkerCopyable(o, i)) {
        const l = this._getMarkerClipboardConfig(o), c = s.markers.has(o) && s.markers.get(o).getRange().root.rootName === "$graveyard";
        return (l.duplicateOnPaste || c) && (o = this._getUniqueMarkerName(o)), [
          {
            name: o,
            range: a
          }
        ];
      }
      return [];
    });
  }
  /**
  * Inserts specified array of fake markers elements to document and assigns them `type` and `name` attributes.
  * Fake markers elements are used to calculate position of markers on pasted fragment that were transformed during
  * steps between copy and paste.
  *
  * @param writer An instance of the model writer.
  * @param markers Array of markers that will be inserted.
  */
  _insertFakeMarkersElements(e, i) {
    const s = {}, r = i.flatMap((o) => {
      const { start: a, end: l } = o.range;
      return [
        {
          position: a,
          marker: o,
          type: "start"
        },
        {
          position: l,
          marker: o,
          type: "end"
        }
      ];
    }).sort(({ position: o }, { position: a }) => o.isBefore(a) ? 1 : -1);
    for (const { position: o, marker: a, type: l } of r) {
      const c = e.createElement("$marker", {
        "data-name": a.name,
        "data-type": l
      });
      s[a.name] || (s[a.name] = []), s[a.name].push(c), e.insert(c, o);
    }
    return s;
  }
  /**
  * Removes all `$marker` elements from the given document fragment.
  *
  * Returns an object where keys are marker names, and values are ranges corresponding to positions
  * where `$marker` elements were inserted.
  *
  * If the document fragment had only one `$marker` element for given marker (start or end) the other boundary is set automatically
  * (to the end or start of the document fragment, respectively).
  *
  * @param writer An instance of the model writer.
  * @param rootElement The element to be checked.
  */
  _removeFakeMarkersInsideElement(e, i) {
    const r = this._getAllFakeMarkersFromElement(e, i).reduce((o, a) => {
      const l = a.markerElement && e.createPositionBefore(a.markerElement);
      let c = o[a.name], u = !1;
      return c && c.start && c.end && (this._getMarkerClipboardConfig(a.name).duplicateOnPaste ? o[this._getUniqueMarkerName(a.name)] = o[a.name] : u = !0, c = null), u || (o[a.name] = {
        ...c,
        [a.type]: l
      }), a.markerElement && e.remove(a.markerElement), o;
    }, {});
    return QP(r, (o) => new F(o.start || e.createPositionFromPath(i, [
      0
    ]), o.end || e.createPositionAt(i, "end")));
  }
  /**
  * Returns array that contains list of fake markers with corresponding `$marker` elements.
  *
  * For each marker, there can be two `$marker` elements or only one (if the document fragment contained
  * only the beginning or only the end of a marker).
  *
  * @param writer An instance of the model writer.
  * @param rootElement The element to be checked.
  */
  _getAllFakeMarkersFromElement(e, i) {
    const s = Array.from(e.createRangeIn(i)).flatMap(({ item: a }) => {
      if (!a.is("element", "$marker"))
        return [];
      const l = a.getAttribute("data-name"), c = a.getAttribute("data-type");
      return [
        {
          markerElement: a,
          name: l,
          type: c
        }
      ];
    }), r = [], o = [];
    for (const a of s)
      a.type === "end" && (s.some((c) => c.name === a.name && c.type === "start") || r.push({
        markerElement: null,
        name: a.name,
        type: "start"
      })), a.type === "start" && (s.some((c) => c.name === a.name && c.type === "end") || o.unshift({
        markerElement: null,
        name: a.name,
        type: "end"
      }));
    return [
      ...r,
      ...s,
      ...o
    ];
  }
  /**
  * When copy of markers occurs we have to make sure that pasted markers have different names
  * than source markers. This functions helps with assigning unique part to marker name to
  * prevent duplicated markers error.
  *
  * @param name Name of marker
  */
  _getUniqueMarkerName(e) {
    const i = e.split(":"), s = Bt().substring(1, 6);
    return i.length === 3 ? `${i.slice(0, 2).join(":")}:${s}` : `${i.join(":")}:${s}`;
  }
}
class _i extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ClipboardPipeline";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Go
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.editing.view.addObserver(ga), this._setupPasteDrop(), this._setupCopyCut();
  }
  /**
  * Fires Clipboard `'outputTransformation'` event for given parameters.
  *
  * @internal
  */
  _fireOutputTransformationEvent(t, e, i) {
    const s = this.editor.plugins.get("ClipboardMarkersUtils");
    this.editor.model.enqueueChange({
      isUndoable: i === "cut"
    }, () => {
      const r = s._copySelectedFragmentWithMarkers(i, e);
      this.fire("outputTransformation", {
        dataTransfer: t,
        content: r,
        method: i
      });
    });
  }
  /**
  * The clipboard paste pipeline.
  */
  _setupPasteDrop() {
    const t = this.editor, e = t.model, i = t.editing.view, s = i.document, r = this.editor.plugins.get("ClipboardMarkersUtils");
    this.listenTo(s, "clipboardInput", (o, a) => {
      a.method == "paste" && !t.model.canEditAt(t.model.document.selection) && o.stop();
    }, {
      priority: "highest"
    }), this.listenTo(s, "clipboardInput", (o, a) => {
      const l = a.dataTransfer;
      let c;
      if (a.content)
        c = a.content;
      else {
        let h = "";
        l.getData("text/html") ? h = pN(l.getData("text/html")) : l.getData("text/plain") && (h = gN(l.getData("text/plain"))), c = this.editor.data.htmlProcessor.toView(h);
      }
      const u = new jn(this, "inputTransformation"), d = l.getData("application/ckeditor5-editor-id") || null;
      this.fire(u, {
        content: c,
        dataTransfer: l,
        sourceEditorId: d,
        targetRanges: a.targetRanges,
        method: a.method
      }), u.stop.called && o.stop(), i.scrollToTheSelection();
    }, {
      priority: "low"
    }), this.listenTo(this, "inputTransformation", (o, a) => {
      if (a.content.isEmpty)
        return;
      const c = this.editor.data.toModel(a.content, "$clipboardHolder");
      c.childCount != 0 && (o.stop(), e.change(() => {
        this.fire("contentInsertion", {
          content: c,
          method: a.method,
          sourceEditorId: a.sourceEditorId,
          dataTransfer: a.dataTransfer,
          targetRanges: a.targetRanges
        });
      }));
    }, {
      priority: "low"
    }), this.listenTo(this, "contentInsertion", (o, a) => {
      a.resultRange = r._pasteFragmentWithMarkers(a.content);
    }, {
      priority: "low"
    });
  }
  /**
  * The clipboard copy/cut pipeline.
  */
  _setupCopyCut() {
    const t = this.editor, e = t.model.document, s = t.editing.view.document, r = (o, a) => {
      const l = a.dataTransfer;
      a.preventDefault(), this._fireOutputTransformationEvent(l, e.selection, o.name);
    };
    this.listenTo(s, "copy", r, {
      priority: "low"
    }), this.listenTo(s, "cut", (o, a) => {
      t.model.canEditAt(t.model.document.selection) ? r(o, a) : a.preventDefault();
    }, {
      priority: "low"
    }), this.listenTo(this, "outputTransformation", (o, a) => {
      const l = t.data.toView(a.content, {
        isClipboardPipeline: !0
      });
      s.fire("clipboardOutput", {
        dataTransfer: a.dataTransfer,
        content: l,
        method: a.method
      });
    }, {
      priority: "low"
    }), this.listenTo(s, "clipboardOutput", (o, a) => {
      a.content.isEmpty || (a.dataTransfer.setData("text/html", this.editor.data.htmlProcessor.toData(a.content)), a.dataTransfer.setData("text/plain", uk(t.data.htmlProcessor.domConverter, a.content)), a.dataTransfer.setData("application/ckeditor5-editor-id", this.editor.id)), a.method == "cut" && t.model.deleteContent(e.selection);
    }, {
      priority: "low"
    });
  }
}
const pd = /* @__PURE__ */ to("px");
class wN extends ee {
  /**
  * @inheritDoc
  */
  constructor() {
    super();
    const t = this.bindTemplate;
    this.set({
      isVisible: !1,
      left: null,
      top: null,
      width: null
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-clipboard-drop-target-line",
          t.if("isVisible", "ck-hidden", (e) => !e)
        ],
        style: {
          left: t.to("left", (e) => pd(e)),
          top: t.to("top", (e) => pd(e)),
          width: t.to("width", (e) => pd(e))
        }
      }
    });
  }
}
class gl extends I {
  constructor() {
    super(...arguments);
    /**
    * A delayed callback removing the drop marker.
    *
    * @internal
    */
    f(this, "removeDropMarkerDelayed", _f(() => this.removeDropMarker(), 40));
    /**
    * A throttled callback updating the drop marker.
    */
    f(this, "_updateDropMarkerThrottled", $s((e) => this._updateDropMarker(e), 40));
    /**
    * A throttled callback reconverting the drop parker.
    */
    f(this, "_reconvertMarkerThrottled", $s(() => {
      this.editor.model.markers.has("drop-target") && this.editor.editing.reconvertMarker("drop-target");
    }, 0));
    /**
    * The horizontal drop target line view.
    */
    f(this, "_dropTargetLineView", new wN());
    /**
    * DOM Emitter.
    */
    f(this, "_domEmitter", new (li())());
    /**
    * Map of document scrollable elements.
    */
    f(this, "_scrollables", /* @__PURE__ */ new Map());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DragDropTarget";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this._setupDropMarker();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._domEmitter.stopListening();
    for (const { resizeObserver: e } of this._scrollables.values())
      e.destroy();
    return this._updateDropMarkerThrottled.cancel(), this.removeDropMarkerDelayed.cancel(), this._reconvertMarkerThrottled.cancel(), super.destroy();
  }
  /**
  * Finds the drop target range and updates the drop marker.
  *
  * @internal
  */
  updateDropMarker(e, i, s, r, o, a) {
    this.removeDropMarkerDelayed.cancel();
    const l = o1(this.editor, e, i, s, r, o, a);
    /* istanbul ignore next -- @preserve */
    if (l) {
      if (a && a.containsRange(l))
        return this.removeDropMarker();
      this._updateDropMarkerThrottled(l);
    }
  }
  /**
  * Finds the final drop target range.
  *
  * @internal
  */
  getFinalDropRange(e, i, s, r, o, a) {
    const l = o1(this.editor, e, i, s, r, o, a);
    return this.removeDropMarker(), l;
  }
  /**
  * Removes the drop target marker.
  *
  * @internal
  */
  removeDropMarker() {
    const e = this.editor.model;
    this.removeDropMarkerDelayed.cancel(), this._updateDropMarkerThrottled.cancel(), this._dropTargetLineView.isVisible = !1, e.markers.has("drop-target") && e.change((i) => {
      i.removeMarker("drop-target");
    });
  }
  /**
  * Creates downcast conversion for the drop target marker.
  */
  _setupDropMarker() {
    const e = this.editor;
    e.ui.view.body.add(this._dropTargetLineView), e.conversion.for("editingDowncast").markerToHighlight({
      model: "drop-target",
      view: {
        classes: [
          "ck-clipboard-drop-target-range"
        ]
      }
    }), e.conversion.for("editingDowncast").markerToElement({
      model: "drop-target",
      view: (i, { writer: s }) => {
        if (e.model.schema.checkChild(i.markerRange.start, "$text"))
          return this._dropTargetLineView.isVisible = !1, this._createDropTargetPosition(s);
        i.markerRange.isCollapsed ? this._updateDropTargetLine(i.markerRange) : this._dropTargetLineView.isVisible = !1;
      }
    });
  }
  /**
  * Updates the drop target marker to the provided range.
  *
  * @param targetRange The range to set the marker to.
  */
  _updateDropMarker(e) {
    const i = this.editor, s = i.model.markers;
    i.model.change((r) => {
      s.has("drop-target") ? s.get("drop-target").getRange().isEqual(e) || r.updateMarker("drop-target", {
        range: e
      }) : r.addMarker("drop-target", {
        range: e,
        usingOperation: !1,
        affectsData: !1
      });
    });
  }
  /**
  * Creates the UI element for vertical (in-line) drop target.
  */
  _createDropTargetPosition(e) {
    return e.createUIElement("span", {
      class: "ck ck-clipboard-drop-target-position"
    }, function(i) {
      const s = this.toDomElement(i);
      return s.append("⁠", i.createElement("span"), "⁠"), s;
    });
  }
  /**
  * Updates the horizontal drop target line.
  */
  _updateDropTargetLine(e) {
    const i = this.editor.editing, s = e.start.nodeBefore, r = e.start.nodeAfter, o = e.start.parent, a = s ? i.mapper.toViewElement(s) : null, l = a ? i.view.domConverter.mapViewToDom(a) : null, c = r ? i.mapper.toViewElement(r) : null, u = c ? i.view.domConverter.mapViewToDom(c) : null, d = i.mapper.toViewElement(o);
    if (!d)
      return;
    const h = i.view.domConverter.mapViewToDom(d), m = this._getScrollableRect(d), { scrollX: g, scrollY: p } = ae.window, w = l ? new ke(l) : null, y = u ? new ke(u) : null, k = new ke(h).excludeScrollbarsAndBorders(), C = w ? w.bottom : k.top, E = y ? y.top : k.bottom, T = ae.window.getComputedStyle(h), L = C <= E ? (C + E) / 2 : E;
    if (m.top < L && L < m.bottom) {
      const O = k.left + parseFloat(T.paddingLeft), N = k.right - parseFloat(T.paddingRight), M = Math.max(O + g, m.left), J = Math.min(N + g, m.right);
      this._dropTargetLineView.set({
        isVisible: !0,
        left: M,
        top: L + p,
        width: J - M
      });
    } else
      this._dropTargetLineView.isVisible = !1;
  }
  /**
  * Finds the closest scrollable element rect for the given view element.
  */
  _getScrollableRect(e) {
    const i = e.root.rootName;
    let s;
    if (this._scrollables.has(i))
      s = this._scrollables.get(i).domElement;
    else {
      const r = this.editor.editing.view.domConverter.mapViewToDom(e);
      s = _N(r), this._domEmitter.listenTo(s, "scroll", this._reconvertMarkerThrottled, {
        usePassive: !0
      });
      const o = new $r(s, this._reconvertMarkerThrottled);
      this._scrollables.set(i, {
        domElement: s,
        resizeObserver: o
      });
    }
    return new ke(s).excludeScrollbarsAndBorders();
  }
}
function o1(n, t, e, i, s, r, o) {
  const a = n.model, l = n.editing.mapper;
  let u = a1(n, t);
  for (; u; ) {
    if (!r) {
      if (a.schema.checkChild(u, "$text")) {
        if (e) {
          const d = e[0].start, h = l.toModelPosition(d);
          if (!o || Array.from(o.getItems({
            shallow: !0
          })).every((g) => a.schema.checkChild(h, g))) {
            if (a.schema.checkChild(h, "$text"))
              return a.createRange(h);
            if (d)
              return pl(n, a1(n, d.parent), i, s);
          }
        }
      } else if (a.schema.isInline(u))
        return pl(n, u, i, s);
    }
    if (a.schema.isBlock(u))
      return pl(n, u, i, s);
    if (a.schema.checkChild(u, "$block")) {
      const d = Array.from(u.getChildren()).filter((g) => g.is("element") && !vN(n, g));
      let h = 0, m = d.length;
      if (m == 0)
        return a.createRange(a.createPositionAt(u, "end"));
      for (; h < m - 1; ) {
        const g = Math.floor((h + m) / 2);
        hk(n, d[g], i, s) == "before" ? m = g : h = g;
      }
      return pl(n, d[h], i, s);
    }
    u = u.parent;
  }
  return null;
}
function vN(n, t) {
  const e = n.editing.mapper, i = n.editing.view.domConverter, s = e.toViewElement(t);
  if (!s)
    return !0;
  const r = i.mapViewToDom(s);
  return ae.window.getComputedStyle(r).float != "none";
}
function pl(n, t, e, i) {
  const s = n.model;
  return s.createRange(s.createPositionAt(t, hk(n, t, e, i)));
}
function hk(n, t, e, i) {
  const s = n.editing.mapper, r = n.editing.view.domConverter, o = s.toViewElement(t), a = r.mapViewToDom(o), l = new ke(a);
  return n.model.schema.isInline(t) ? e < (l.left + l.right) / 2 ? "before" : "after" : i < (l.top + l.bottom) / 2 ? "before" : "after";
}
function a1(n, t) {
  const e = n.editing.mapper, i = n.editing.view, s = e.toModelElement(t);
  if (s)
    return s;
  const r = i.createPositionBefore(t), o = e.findMappedViewAncestor(r);
  return e.toModelElement(o);
}
function _N(n) {
  let t = n;
  do {
    t = t.parentElement;
    const e = ae.window.getComputedStyle(t).overflowY;
    if (e == "auto" || e == "scroll")
      break;
  } while (t.tagName != "BODY");
  return t;
}
class yN extends I {
  constructor() {
    super(...arguments);
    /**
    * Whether current dragging is started by block toolbar button dragging.
    */
    f(this, "_isBlockDragging", !1);
    /**
    * DOM Emitter.
    */
    f(this, "_domEmitter", new (li())());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DragDropBlockToolbar";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    if (this.listenTo(e, "change:isReadOnly", (i, s, r) => {
      r ? (this.forceDisabled("readOnlyMode"), this._isBlockDragging = !1) : this.clearForceDisabled("readOnlyMode");
    }), be.isAndroid && this.forceDisabled("noAndroidSupport"), e.plugins.has("BlockToolbar")) {
      const s = e.plugins.get("BlockToolbar").buttonView.element;
      this._domEmitter.listenTo(s, "dragstart", (r, o) => this._handleBlockDragStart(o)), this._domEmitter.listenTo(ae.document, "dragover", (r, o) => this._handleBlockDragging(o)), this._domEmitter.listenTo(ae.document, "drop", (r, o) => this._handleBlockDragging(o)), this._domEmitter.listenTo(ae.document, "dragend", () => this._handleBlockDragEnd(), {
        useCapture: !0
      }), this.isEnabled && s.setAttribute("draggable", "true"), this.on("change:isEnabled", (r, o, a) => {
        s.setAttribute("draggable", a ? "true" : "false");
      });
    }
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this._domEmitter.stopListening(), super.destroy();
  }
  /**
  * The `dragstart` event handler.
  */
  _handleBlockDragStart(e) {
    if (!this.isEnabled)
      return;
    const i = this.editor.model, s = i.document.selection, r = this.editor.editing.view, o = Array.from(s.getSelectedBlocks()), a = i.createRange(i.createPositionBefore(o[0]), i.createPositionAfter(o[o.length - 1]));
    i.change((l) => l.setSelection(a)), this._isBlockDragging = !0, r.focus(), r.getObserver(ga).onDomEvent(e);
  }
  /**
  * The `dragover` and `drop` event handler.
  */
  _handleBlockDragging(e) {
    if (!this.isEnabled || !this._isBlockDragging)
      return;
    const i = e.clientX + (this.editor.locale.contentLanguageDirection == "ltr" ? 100 : -100), s = e.clientY, r = document.elementFromPoint(i, s), o = this.editor.editing.view;
    !r || !r.closest(".ck-editor__editable") || o.getObserver(ga).onDomEvent({
      ...e,
      type: e.type,
      dataTransfer: e.dataTransfer,
      target: r,
      clientX: i,
      clientY: s,
      preventDefault: () => e.preventDefault(),
      stopPropagation: () => e.stopPropagation()
    });
  }
  /**
  * The `dragend` event handler.
  */
  _handleBlockDragEnd() {
    this._isBlockDragging = !1;
  }
}
class kN extends I {
  constructor() {
    super(...arguments);
    /**
    * The live range over the original content that is being dragged.
    */
    f(this, "_draggedRange");
    /**
    * The UID of current dragging that is used to verify if the drop started in the same editor as the drag start.
    *
    * **Note**: This is a workaround for broken 'dragend' events (they are not fired if the source text node got removed).
    */
    f(this, "_draggingUid");
    /**
    * The reference to the model element that currently has a `draggable` attribute set (it is set while dragging).
    */
    f(this, "_draggableElement");
    /**
    * A delayed callback removing draggable attributes.
    */
    f(this, "_clearDraggableAttributesDelayed", _f(() => this._clearDraggableAttributes(), 40));
    /**
    * Whether the dragged content can be dropped only in block context.
    */
    // TODO handle drag from other editor instance
    // TODO configure to use block, inline or both
    f(this, "_blockMode", !1);
    /**
    * DOM Emitter.
    */
    f(this, "_domEmitter", new (li())());
    /**
    * The DOM element used to generate dragged preview image.
    */
    f(this, "_previewContainer");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DragDrop";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      _i,
      Sn,
      gl,
      yN
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.editing.view;
    this._draggedRange = null, this._draggingUid = "", this._draggableElement = null, i.addObserver(ga), i.addObserver(Yc), this._setupDragging(), this._setupContentInsertionIntegration(), this._setupClipboardInputIntegration(), this._setupDraggableAttributeHandling(), this.listenTo(e, "change:isReadOnly", (s, r, o) => {
      o ? this.forceDisabled("readOnlyMode") : this.clearForceDisabled("readOnlyMode");
    }), this.on("change:isEnabled", (s, r, o) => {
      o || this._finalizeDragging(!1);
    }), be.isAndroid && this.forceDisabled("noAndroidSupport");
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this._draggedRange && (this._draggedRange.detach(), this._draggedRange = null), this._previewContainer && this._previewContainer.remove(), this._domEmitter.stopListening(), this._clearDraggableAttributesDelayed.cancel(), super.destroy();
  }
  /**
  * Drag and drop events handling.
  */
  _setupDragging() {
    const e = this.editor, i = e.model, s = e.editing.view, r = s.document, o = e.plugins.get(gl);
    this.listenTo(r, "dragstart", (a, l) => {
      if (l.target && l.target.is("editableElement")) {
        l.preventDefault();
        return;
      }
      if (this._prepareDraggedRange(l.target), !this._draggedRange) {
        l.preventDefault();
        return;
      }
      this._draggingUid = Bt(), l.dataTransfer.effectAllowed = this.isEnabled ? "copyMove" : "copy", l.dataTransfer.setData("application/ckeditor5-dragging-uid", this._draggingUid);
      const c = i.createSelection(this._draggedRange.toRange());
      this.editor.plugins.get("ClipboardPipeline")._fireOutputTransformationEvent(l.dataTransfer, c, "dragstart");
      const { dataTransfer: d, domTarget: h, domEvent: m } = l, { clientX: g } = m;
      this._updatePreview({
        dataTransfer: d,
        domTarget: h,
        clientX: g
      }), l.stopPropagation(), this.isEnabled || (this._draggedRange.detach(), this._draggedRange = null, this._draggingUid = "");
    }, {
      priority: "low"
    }), this.listenTo(r, "dragend", (a, l) => {
      this._finalizeDragging(!l.dataTransfer.isCanceled && l.dataTransfer.dropEffect == "move");
    }, {
      priority: "low"
    }), this._domEmitter.listenTo(ae.document, "dragend", () => {
      this._blockMode = !1;
    }, {
      useCapture: !0
    }), this.listenTo(r, "dragenter", () => {
      this.isEnabled && s.focus();
    }), this.listenTo(r, "dragleave", () => {
      o.removeDropMarkerDelayed();
    }), this.listenTo(r, "dragging", (a, l) => {
      if (!this.isEnabled) {
        l.dataTransfer.dropEffect = "none";
        return;
      }
      const { clientX: c, clientY: u } = l.domEvent;
      o.updateDropMarker(l.target, l.targetRanges, c, u, this._blockMode, this._draggedRange), this._draggedRange || (l.dataTransfer.dropEffect = "copy"), be.isGecko || (l.dataTransfer.effectAllowed == "copy" ? l.dataTransfer.dropEffect = "copy" : [
        "all",
        "copyMove"
      ].includes(l.dataTransfer.effectAllowed) && (l.dataTransfer.dropEffect = "move")), a.stop();
    }, {
      priority: "low"
    });
  }
  /**
  * Integration with the `clipboardInput` event.
  */
  _setupClipboardInputIntegration() {
    const e = this.editor, s = e.editing.view.document, r = e.plugins.get(gl);
    this.listenTo(s, "clipboardInput", (o, a) => {
      if (a.method != "drop")
        return;
      const { clientX: l, clientY: c } = a.domEvent, u = r.getFinalDropRange(a.target, a.targetRanges, l, c, this._blockMode, this._draggedRange);
      if (!u) {
        this._finalizeDragging(!1), o.stop();
        return;
      }
      if (this._draggedRange && this._draggingUid != a.dataTransfer.getData("application/ckeditor5-dragging-uid") && (this._draggedRange.detach(), this._draggedRange = null, this._draggingUid = ""), l1(a.dataTransfer) == "move" && this._draggedRange && this._draggedRange.containsRange(u, !0)) {
        this._finalizeDragging(!1), o.stop();
        return;
      }
      a.targetRanges = [
        e.editing.mapper.toViewRange(u)
      ];
    }, {
      priority: "high"
    });
  }
  /**
  * Integration with the `contentInsertion` event of the clipboard pipeline.
  */
  _setupContentInsertionIntegration() {
    const e = this.editor.plugins.get(_i);
    e.on("contentInsertion", (i, s) => {
      if (!this.isEnabled || s.method !== "drop")
        return;
      const r = s.targetRanges.map((o) => this.editor.editing.mapper.toModelRange(o));
      this.editor.model.change((o) => o.setSelection(r));
    }, {
      priority: "high"
    }), e.on("contentInsertion", (i, s) => {
      if (!this.isEnabled || s.method !== "drop")
        return;
      const r = l1(s.dataTransfer) == "move", o = !s.resultRange || !s.resultRange.isCollapsed;
      this._finalizeDragging(o && r);
    }, {
      priority: "lowest"
    });
  }
  /**
  * Adds listeners that add the `draggable` attribute to the elements while the mouse button is down so the dragging could start.
  */
  _setupDraggableAttributeHandling() {
    const e = this.editor, i = e.editing.view, s = i.document;
    this.listenTo(s, "mousedown", (r, o) => {
      if (be.isAndroid || !o)
        return;
      this._clearDraggableAttributesDelayed.cancel();
      let a = c1(o.target);
      if (be.isBlink && !e.isReadOnly && !a && !s.selection.isCollapsed) {
        const l = s.selection.getSelectedElement();
        (!l || !vt(l)) && (a = s.selection.editableElement);
      }
      a && (i.change((l) => {
        l.setAttribute("draggable", "true", a);
      }), this._draggableElement = e.editing.mapper.toModelElement(a));
    }), this.listenTo(s, "mouseup", () => {
      be.isAndroid || this._clearDraggableAttributesDelayed();
    });
  }
  /**
  * Removes the `draggable` attribute from the element that was used for dragging.
  */
  _clearDraggableAttributes() {
    const e = this.editor.editing;
    e.view.change((i) => {
      this._draggableElement && this._draggableElement.root.rootName != "$graveyard" && i.removeAttribute("draggable", e.mapper.toViewElement(this._draggableElement)), this._draggableElement = null;
    });
  }
  /**
  * Deletes the dragged content from its original range and clears the dragging state.
  *
  * @param moved Whether the move succeeded.
  */
  _finalizeDragging(e) {
    const i = this.editor, s = i.model;
    i.plugins.get(gl).removeDropMarker(), this._clearDraggableAttributes(), i.plugins.has("WidgetToolbarRepository") && i.plugins.get("WidgetToolbarRepository").clearForceDisabled("dragDrop"), this._draggingUid = "", this._previewContainer && (this._previewContainer.remove(), this._previewContainer = void 0), this._draggedRange && (e && this.isEnabled && s.change((o) => {
      const a = s.createSelection(this._draggedRange);
      s.deleteContent(a, {
        doNotAutoparagraph: !0
      });
      const l = a.getFirstPosition().parent;
      l.isEmpty && !s.schema.checkChild(l, "$text") && s.schema.checkChild(l, "paragraph") && o.insertElement("paragraph", l, 0);
    }), this._draggedRange.detach(), this._draggedRange = null);
  }
  /**
  * Sets the dragged source range based on event target and document selection.
  */
  _prepareDraggedRange(e) {
    const i = this.editor, s = i.model, r = s.document.selection, o = e ? c1(e) : null;
    if (o) {
      const u = i.editing.mapper.toModelElement(o);
      this._draggedRange = ni.fromRange(s.createRangeOn(u)), this._blockMode = s.schema.isBlock(u), i.plugins.has("WidgetToolbarRepository") && i.plugins.get("WidgetToolbarRepository").forceDisabled("dragDrop");
      return;
    }
    if (r.isCollapsed && !r.getFirstPosition().parent.isEmpty)
      return;
    const a = Array.from(r.getSelectedBlocks()), l = r.getFirstRange();
    if (a.length == 0) {
      this._draggedRange = ni.fromRange(l);
      return;
    }
    const c = fk(s, a);
    if (a.length > 1)
      this._draggedRange = ni.fromRange(c), this._blockMode = !0;
    else if (a.length == 1) {
      const u = l.start.isTouching(c.start) && l.end.isTouching(c.end);
      this._draggedRange = ni.fromRange(u ? c : l), this._blockMode = u;
    }
    s.change((u) => u.setSelection(this._draggedRange.toRange()));
  }
  /**
  * Updates the dragged preview image.
  */
  _updatePreview({ dataTransfer: e, domTarget: i, clientX: s }) {
    const r = this.editor.editing.view, o = r.document.selection.editableElement, a = r.domConverter.mapViewToDom(o), l = ae.window.getComputedStyle(a);
    this._previewContainer ? this._previewContainer.firstElementChild && this._previewContainer.removeChild(this._previewContainer.firstElementChild) : (this._previewContainer = Ft(ae.document, "div", {
      style: "position: fixed; left: -999999px;"
    }), ae.document.body.appendChild(this._previewContainer));
    const c = new ke(a);
    if (a.contains(i))
      return;
    const u = parseFloat(l.paddingLeft), d = Ft(ae.document, "div");
    d.className = "ck ck-content", d.style.width = l.width, d.style.paddingLeft = `${c.left - s + u}px`, be.isiOS && (d.style.backgroundColor = "white"), r.domConverter.setContentOf(d, e.getData("text/html")), e.setDragImage(d, 0, 0), this._previewContainer.appendChild(d);
  }
}
function l1(n) {
  return be.isGecko ? n.dropEffect : [
    "all",
    "copyMove"
  ].includes(n.effectAllowed) ? "move" : "copy";
}
function c1(n) {
  if (n.is("editableElement"))
    return null;
  if (n.hasClass("ck-widget__selection-handle"))
    return n.findAncestor(vt);
  if (vt(n))
    return n;
  const t = n.findAncestor((e) => vt(e) || e.is("editableElement"));
  return vt(t) ? t : null;
}
function fk(n, t) {
  const e = t[0], i = t[t.length - 1], s = e.getCommonAncestor(i), r = n.createPositionBefore(e), o = n.createPositionAfter(i);
  if (s && s.is("element") && !n.schema.isLimit(s)) {
    const a = n.createRangeOn(s), l = r.isTouching(a.start), c = o.isTouching(a.end);
    if (l && c)
      return fk(n, [
        s
      ]);
  }
  return n.createRange(r, o);
}
class CN extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PastePlainText";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      _i
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model, i = t.editing.view, s = e.document.selection;
    i.addObserver(ga), t.plugins.get(_i).on("contentInsertion", (r, o) => {
      xN(o.content, e) && e.change((a) => {
        const l = Array.from(s.getAttributes()).filter(([u]) => e.schema.getAttributeProperties(u).isFormatting);
        s.isCollapsed || e.deleteContent(s, {
          doNotAutoparagraph: !0
        }), l.push(...s.getAttributes());
        const c = a.createRangeIn(o.content);
        for (const u of c.getItems())
          for (const d of l)
            e.schema.checkAttribute(u, d[0]) && a.setAttribute(d[0], d[1], u);
      });
    });
  }
}
function xN(n, t) {
  let e = t.createRangeIn(n);
  if (n.childCount == 1) {
    const i = n.getChild(0);
    i.is("element") && t.schema.isBlock(i) && !t.schema.isObject(i) && !t.schema.isLimit(i) && (e = t.createRangeIn(i));
  }
  for (const i of e.getItems())
    if (!t.schema.isInline(i) || Array.from(i.getAttributeKeys()).find((r) => t.schema.getAttributeProperties(r).isFormatting))
      return !1;
  return !0;
}
class Jf extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Clipboard";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Go,
      _i,
      kN,
      CN
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t;
    t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Copy selected content"),
          keystroke: "CTRL+C"
        },
        {
          label: e("Paste content"),
          keystroke: "CTRL+V"
        },
        {
          label: e("Paste content as plain text"),
          keystroke: "CTRL+SHIFT+V"
        }
      ]
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function ac(n) {
  const t = n.t, e = n.config.get("codeBlock.languages");
  for (const i of e)
    i.label === "Plain text" && (i.label = t("Plain text")), i.class === void 0 && (i.class = `language-${i.language}`);
  return e;
}
function lc(n, t, e) {
  const i = {};
  for (const s of n)
    if (t === "class") {
      const r = s[t].split(" ").shift();
      i[r] = s[e];
    } else
      i[s[t]] = s[e];
  return i;
}
function Xf(n) {
  return n.data.match(/^(\s*)/)[0];
}
function TN(n, t) {
  const e = n.createDocumentFragment(), i = t.split(`
`), s = i.reduce((r, o, a) => (r.push(o), a < i.length - 1 && r.push(n.createElement("br")), r), []);
  return n.appendChild(s, e), e;
}
function xh(n) {
  const t = n.document.selection, e = [];
  if (t.isCollapsed)
    return [
      t.anchor
    ];
  const i = t.getFirstRange().getWalker({
    ignoreElementEnd: !0,
    direction: "backward"
  });
  for (const { item: s } of i) {
    let r = s.is("$textProxy") ? s.textNode : s;
    const o = r.parent;
    if (!o.is("element", "codeBlock") || r.is("element", "softBreak"))
      continue;
    for (; r.previousSibling && !r.previousSibling.is("element", "softBreak"); )
      r = r.previousSibling;
    const a = r.is("$text") ? r.startOffset + Xf(r).length : r.startOffset, l = n.createPositionAt(o, a);
    e.every((c) => !c.isEqual(l)) && e.push(l);
  }
  return e;
}
function mk(n) {
  const t = Xe(n.getSelectedBlocks());
  return !!t && t.is("element", "codeBlock");
}
function u1(n, t) {
  return t.is("rootElement") || n.isLimit(t) ? !1 : n.checkChild(t.parent, "codeBlock");
}
function d1(n, t, e, i) {
  const s = lc(t, "language", "label"), r = e.getAttribute("language");
  if (r in s) {
    const o = s[r];
    return n(i === "enter" ? "Entering %0 code snippet" : "Leaving %0 code snippet", o);
  }
  return n(i === "enter" ? "Entering code snippet" : "Leaving code snippet");
}
function gk(n, t) {
  for (n.textNode && (n = t.createPositionBefore(n.textNode)); n.nodeBefore && !n.nodeBefore.is("element", "softBreak"); )
    n = t.createPositionBefore(n.nodeBefore);
  const e = n.nodeAfter;
  return e && e.is("$text") ? e : null;
}
class EN extends he {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Contains the last used language.
    */
    f(this, "_lastLanguage");
    this._lastLanguage = null;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.value = this._getValue(), this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the command. When the command {@link #value is on}, all topmost code blocks within
  * the selection will be removed. If it is off, all selected blocks will be flattened and
  * wrapped by a code block.
  *
  * @fires execute
  * @param options Command options.
  * @param options.language The code block language.
  * @param options.forceValue If set, it will force the command behavior. If `true`, the command will apply a code block,
  * otherwise the command will remove the code block. If not set, the command will act basing on its current value.
  * @param options.usePreviousLanguageChoice If set on `true` and the `options.language` is not specified, the command
  * will apply the previous language (if the command was already executed) when inserting the `codeBlock` element.
  */
  execute(e = {}) {
    const i = this.editor, s = i.model, r = s.document.selection, a = ac(i)[0], l = Array.from(r.getSelectedBlocks()), c = e.forceValue == null ? !this.value : e.forceValue, u = SN(e, this._lastLanguage, a.language);
    s.change((d) => {
      c ? this._applyCodeBlock(d, l, u) : this._removeCodeBlock(d, l);
    });
  }
  /**
  * Checks the command's {@link #value}.
  *
  * @returns The current value.
  */
  _getValue() {
    const e = this.editor.model.document.selection, i = Xe(e.getSelectedBlocks());
    return !!(i && i.is("element", "codeBlock")) ? i.getAttribute("language") : !1;
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    if (this.value)
      return !0;
    const e = this.editor.model.document.selection, i = this.editor.model.schema, s = Xe(e.getSelectedBlocks());
    return s ? u1(i, s) : !1;
  }
  _applyCodeBlock(e, i, s) {
    this._lastLanguage = s;
    const r = this.editor.model.schema, o = i.filter((a) => u1(r, a));
    for (const a of o)
      e.rename(a, "codeBlock"), e.setAttribute("language", s, a), r.removeDisallowedAttributes([
        a
      ], e), Array.from(a.getChildren()).filter((l) => !r.checkChild(a, l)).forEach((l) => e.remove(l));
    o.reverse().forEach((a, l) => {
      const c = o[l + 1];
      a.previousSibling === c && (e.appendElement("softBreak", c), e.merge(e.createPositionBefore(a)));
    });
  }
  _removeCodeBlock(e, i) {
    const s = i.filter((r) => r.is("element", "codeBlock"));
    for (const r of s) {
      const o = e.createRangeOn(r);
      for (const a of Array.from(o.getItems()).reverse())
        if (a.is("element", "softBreak") && a.parent.is("element", "codeBlock")) {
          const { position: l } = e.split(e.createPositionBefore(a)), c = l.nodeAfter;
          e.rename(c, "paragraph"), e.removeAttribute("language", c), e.remove(a);
        }
      e.rename(r, "paragraph"), e.removeAttribute("language", r);
    }
  }
}
function SN(n, t, e) {
  return n.language ? n.language : n.usePreviousLanguageChoice && t ? t : e;
}
class AN extends he {
  constructor(e) {
    super(e);
    /**
    * A sequence of characters added to the line when the command is executed.
    */
    f(this, "_indentSequence");
    this._indentSequence = e.config.get("codeBlock.indentSequence");
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the command. When the command {@link #isEnabled is enabled}, the indentation of the
  * code lines in the selection will be increased.
  *
  * @fires execute
  */
  execute() {
    const i = this.editor.model;
    i.change((s) => {
      const r = xh(i);
      for (const o of r) {
        const a = s.createText(this._indentSequence);
        i.insertContent(a, o);
      }
    });
  }
  /**
  * Checks whether the command can be enabled in the current context.
  */
  _checkEnabled() {
    return this._indentSequence ? mk(this.editor.model.document.selection) : !1;
  }
}
class IN extends he {
  constructor(e) {
    super(e);
    /**
    * A sequence of characters removed from the line when the command is executed.
    */
    f(this, "_indentSequence");
    this._indentSequence = e.config.get("codeBlock.indentSequence");
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the command. When the command {@link #isEnabled is enabled}, the indentation of the
  * code lines in the selection will be decreased.
  *
  * @fires execute
  */
  execute() {
    const i = this.editor.model;
    i.change(() => {
      const s = xh(i);
      for (const r of s) {
        const o = h1(i, r, this._indentSequence);
        o && i.deleteContent(i.createSelection(o));
      }
    });
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @private
  * @returns {Boolean} Whether the command should be enabled.
  */
  _checkEnabled() {
    if (!this._indentSequence)
      return !1;
    const e = this.editor.model;
    return mk(e.document.selection) ? xh(e).some((i) => h1(e, i, this._indentSequence)) : !1;
  }
}
function h1(n, t, e) {
  const i = gk(t, n);
  if (!i)
    return null;
  const s = Xf(i), r = s.lastIndexOf(e);
  if (r + e.length !== s.length || r === -1)
    return null;
  const { parent: o, startOffset: a } = i;
  return n.createRange(n.createPositionAt(o, a + r), n.createPositionAt(o, a + r + e.length));
}
function f1(n, t, e = !1) {
  const i = lc(t, "language", "class"), s = lc(t, "language", "label");
  return (r, o, a) => {
    const { writer: l, mapper: c, consumable: u } = a;
    if (!u.consume(o.item, "insert"))
      return;
    const d = o.item.getAttribute("language"), h = c.toViewPosition(n.createPositionBefore(o.item)), m = {};
    e && (m["data-language"] = s[d], m.spellcheck = "false");
    const g = i[d] ? {
      class: i[d]
    } : void 0, p = l.createContainerElement("code", g), w = l.createContainerElement("pre", m, p);
    l.insert(h, w), c.bindElements(o.item, p);
  };
}
function PN(n) {
  return (t, e, i) => {
    if (e.item.parent.name !== "codeBlock")
      return;
    const { writer: s, mapper: r, consumable: o } = i;
    if (!o.consume(e.item, "insert"))
      return;
    const a = r.toViewPosition(n.createPositionBefore(e.item));
    s.insert(a, s.createText(`
`));
  };
}
function RN(n, t) {
  const e = lc(t, "class", "language"), i = t[0].language;
  return (s, r, o) => {
    const a = r.viewItem, l = a.parent;
    if (!l || !l.is("element", "pre") || r.modelCursor.findAncestor("codeBlock"))
      return;
    const { consumable: c, writer: u } = o;
    if (!c.test(a, {
      name: !0
    }))
      return;
    const d = u.createElement("codeBlock"), h = [
      ...a.getClassNames()
    ];
    h.length || h.push("");
    for (const m of h) {
      const g = e[m];
      if (g) {
        u.setAttribute("language", g, d);
        break;
      }
    }
    d.hasAttribute("language") || u.setAttribute("language", i, d), o.convertChildren(a, d), o.safeInsert(d, r.modelCursor) && (c.consume(a, {
      name: !0
    }), o.updateConversionResult(d, r));
  };
}
function ON() {
  return (n, t, { consumable: e, writer: i }) => {
    let s = t.modelCursor;
    if (!e.test(t.viewItem) || !s.findAncestor("codeBlock"))
      return;
    e.consume(t.viewItem);
    const o = t.viewItem.data.split(`
`).map((l) => i.createText(l)), a = o[o.length - 1];
    for (const l of o)
      if (i.insert(l, s), s = s.getShiftedBy(l.offsetSize), l !== a) {
        const c = i.createElement("softBreak");
        i.insert(c, s), s = i.createPositionAfter(c);
      }
    t.modelRange = i.createRange(t.modelCursor, s), t.modelCursor = s;
  };
}
function VN() {
  return (n, t, { consumable: e }) => {
    const i = t.viewItem;
    if (i.findAncestor("pre"))
      return;
    const s = Array.from(i.getChildren()), r = s.find((o) => o.is("element", "code"));
    if (r)
      for (const o of s)
        o === r || !o.is("$text") || e.consume(o, {
          name: !0
        });
  };
}
const pk = "paragraph";
class LN extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeBlockEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      rk
    ];
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("codeBlock", {
      languages: [
        {
          language: "plaintext",
          label: "Plain text"
        },
        {
          language: "c",
          label: "C"
        },
        {
          language: "cs",
          label: "C#"
        },
        {
          language: "cpp",
          label: "C++"
        },
        {
          language: "css",
          label: "CSS"
        },
        {
          language: "diff",
          label: "Diff"
        },
        {
          language: "html",
          label: "HTML"
        },
        {
          language: "java",
          label: "Java"
        },
        {
          language: "javascript",
          label: "JavaScript"
        },
        {
          language: "php",
          label: "PHP"
        },
        {
          language: "python",
          label: "Python"
        },
        {
          language: "ruby",
          label: "Ruby"
        },
        {
          language: "typescript",
          label: "TypeScript"
        },
        {
          language: "xml",
          label: "XML"
        }
      ],
      // A single tab.
      indentSequence: "	"
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model.schema, i = t.model, s = t.editing.view, r = ac(t);
    t.commands.add("codeBlock", new EN(t)), t.commands.add("indentCodeBlock", new AN(t)), t.commands.add("outdentCodeBlock", new IN(t)), this.listenTo(s.document, "tab", (o, a) => {
      const l = a.shiftKey ? "outdentCodeBlock" : "indentCodeBlock";
      t.commands.get(l).isEnabled && (t.execute(l), a.stopPropagation(), a.preventDefault(), o.stop());
    }, {
      context: "pre"
    }), e.register("codeBlock", {
      allowWhere: "$block",
      allowChildren: "$text",
      // Disallow `$inlineObject` and its derivatives like `inlineWidget` inside `codeBlock` to ensure that only text,
      // not other inline elements like inline images, are allowed. This maintains the semantic integrity of code blocks.
      disallowChildren: "$inlineObject",
      allowAttributes: [
        "language"
      ],
      allowAttributesOf: "$listItem",
      isBlock: !0
    }), e.addAttributeCheck((o, a) => {
      const l = o.getItem(o.length - 2);
      if (e.getAttributeProperties(a).isFormatting && l && l.name == "codeBlock")
        return !1;
    }), t.editing.downcastDispatcher.on("insert:codeBlock", f1(i, r, !0)), t.data.downcastDispatcher.on("insert:codeBlock", f1(i, r)), t.data.downcastDispatcher.on("insert:softBreak", PN(i), {
      priority: "high"
    }), t.data.upcastDispatcher.on("element:code", RN(s, r)), t.data.upcastDispatcher.on("text", ON()), t.data.upcastDispatcher.on("element:pre", VN(), {
      priority: "high"
    }), this.listenTo(t.editing.view.document, "clipboardInput", (o, a) => {
      let l = i.createRange(i.document.selection.anchor);
      if (a.targetRanges && (l = t.editing.mapper.toModelRange(a.targetRanges[0])), !l.start.parent.is("element", "codeBlock"))
        return;
      const c = a.dataTransfer.getData("text/plain"), u = new Pi(t.editing.view.document);
      a.content = TN(u, c);
    }), t.plugins.has("ClipboardPipeline") && t.plugins.get(_i).on("contentInsertion", (o, a) => {
      const l = t.model, c = l.document.selection;
      c.anchor.parent.is("element", "codeBlock") && l.change((u) => {
        const d = u.createRangeIn(a.content);
        for (const h of [
          ...d.getItems()
        ])
          h.is("node") && !e.checkChild(c.anchor, h) && u.remove(h);
      });
    }), this.listenTo(i, "getSelectedContent", (o, [a]) => {
      const l = a.anchor;
      a.isCollapsed || !l.parent.is("element", "codeBlock") || !l.hasSameParentAs(a.focus) || i.change((c) => {
        const u = o.return;
        if (l.parent.is("element") && (u.childCount > 1 || a.containsEntireContent(l.parent))) {
          const h = c.createElement("codeBlock", l.parent.getAttributes());
          c.append(u, h);
          const m = c.createDocumentFragment();
          c.append(h, m), o.return = m;
          return;
        }
        const d = u.getChild(0);
        e.checkAttribute(d, "code") && c.setAttribute("code", !0, d);
      });
    });
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.commands, i = e.get("indent"), s = e.get("outdent");
    i && i.registerChildCommand(e.get("indentCodeBlock"), {
      priority: "highest"
    }), s && s.registerChildCommand(e.get("outdentCodeBlock")), this.listenTo(t.editing.view.document, "enter", (r, o) => {
      t.model.document.selection.getLastPosition().parent.is("element", "codeBlock") && (!MN(t, o.isSoft) && !NN(t, o.isSoft) && BN(t), o.preventDefault(), r.stop());
    }, {
      context: "pre"
    }), this._initAriaAnnouncements();
  }
  /**
  * Observe when user enters or leaves code block and set proper aria value in global live announcer.
  * This allows screen readers to indicate when the user has entered and left the specified code block.
  *
  * @internal
  */
  _initAriaAnnouncements() {
    const { model: t, ui: e, t: i } = this.editor, s = ac(this.editor);
    let r = null;
    t.document.selection.on("change:range", () => {
      const o = t.document.selection.focus.parent;
      !e || r === o || !o.is("element") || (r && r.is("element", "codeBlock") && e.ariaLiveAnnouncer.announce(d1(i, s, r, "leave")), o.is("element", "codeBlock") && e.ariaLiveAnnouncer.announce(d1(i, s, o, "enter")), r = o);
    });
  }
}
function BN(n) {
  const t = n.model, e = t.document, i = e.selection.getLastPosition();
  let s;
  const r = gk(i, t);
  r && r.is("$text") && (s = Xf(r)), n.model.change((o) => {
    n.execute("shiftEnter"), s && o.insertText(s, e.selection.anchor);
  });
}
function MN(n, t) {
  const i = n.model.document, s = n.editing.view, r = i.selection.getLastPosition(), o = r.nodeAfter;
  return t || !i.selection.isCollapsed || !r.isAtStart || !As(o) ? !1 : (n.model.change((a) => {
    n.execute("enter");
    const l = i.selection.anchor.parent.previousSibling;
    a.rename(l, pk), a.setSelection(l, "in"), n.model.schema.removeDisallowedAttributes([
      l
    ], a), a.remove(o);
  }), s.scrollToTheSelection(), !0);
}
function NN(n, t) {
  const e = n.model, i = e.document, s = n.editing.view, r = i.selection.getLastPosition(), o = r.nodeBefore;
  let a;
  if (t || !i.selection.isCollapsed || !r.isAtEnd || !o || !o.previousSibling)
    return !1;
  if (As(o) && As(o.previousSibling))
    a = e.createRange(e.createPositionBefore(o.previousSibling), e.createPositionAfter(o));
  else if (bd(o) && As(o.previousSibling) && As(o.previousSibling.previousSibling))
    a = e.createRange(e.createPositionBefore(o.previousSibling.previousSibling), e.createPositionAfter(o));
  else if (bd(o) && As(o.previousSibling) && bd(o.previousSibling.previousSibling) && o.previousSibling.previousSibling && As(o.previousSibling.previousSibling.previousSibling))
    a = e.createRange(e.createPositionBefore(o.previousSibling.previousSibling.previousSibling), e.createPositionAfter(o));
  else
    return !1;
  return n.model.change((l) => {
    l.remove(a), n.execute("enter");
    const c = i.selection.anchor.parent;
    l.rename(c, pk), n.model.schema.removeDisallowedAttributes([
      c
    ], l);
  }), s.scrollToTheSelection(), !0;
}
function bd(n) {
  return n && n.is("$text") && !n.data.match(/\S/);
}
function As(n) {
  return n && n.is("element", "softBreak");
}
class FN extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeBlockUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t, i = t.ui.componentFactory, s = ac(t), r = this._getLanguageListItemDefinitions(s), o = t.commands.get("codeBlock");
    i.add("codeBlock", (a) => {
      const l = Tt(a, Ys), c = l.buttonView, u = e("Insert code block");
      return c.set({
        label: u,
        tooltip: !0,
        icon: Z.codeBlock,
        isToggleable: !0
      }), c.bind("isOn").to(o, "value", (d) => !!d), c.on("execute", () => {
        t.execute("codeBlock", {
          usePreviousLanguageChoice: !0
        }), t.editing.view.focus();
      }), l.on("execute", (d) => {
        t.execute("codeBlock", {
          language: d.source._codeBlockLanguage,
          forceValue: !0
        }), t.editing.view.focus();
      }), l.class = "ck-code-block-dropdown", l.bind("isEnabled").to(o), En(l, r, {
        role: "menu",
        ariaLabel: u
      }), l;
    }), i.add("menuBar:codeBlock", (a) => {
      const l = new Yt(a);
      l.buttonView.set({
        role: "menuitem",
        label: e("Code block"),
        icon: Z.codeBlock
      }), l.bind("isEnabled").to(o);
      const c = new kn(a);
      c.set({
        ariaLabel: e("Insert code block")
      });
      for (const u of r) {
        const d = new en(a, l), h = new Fe(a);
        h.bind(...Object.keys(u.model)).to(u.model), h.set({
          isToggleable: !0,
          role: "menuitemcheckbox"
        }), h.delegate("execute").to(l), h.on("execute", () => {
          t.execute("codeBlock", {
            language: u.model._codeBlockLanguage,
            forceValue: o.value != u.model._codeBlockLanguage
          }), t.editing.view.focus();
        }), d.children.add(h), c.items.add(d);
      }
      return l.panelView.children.add(c), l;
    });
  }
  /**
  * A helper returning a collection of the `codeBlock` dropdown items representing languages
  * available for the user to choose from.
  */
  _getLanguageListItemDefinitions(t) {
    const i = this.editor.commands.get("codeBlock"), s = new bt();
    for (const r of t) {
      const o = {
        type: "button",
        model: new tn({
          _codeBlockLanguage: r.language,
          label: r.label,
          role: "menuitemradio",
          withText: !0
        })
      };
      o.model.bind("isOn").to(i, "value", (a) => a === o.model._codeBlockLanguage), s.add(o);
    }
    return s;
  }
}
class DN extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      LN,
      FN
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeBlock";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class $N extends U9 {
  /**
  * Creates an instance of the classic editor UI class.
  *
  * @param editor The editor instance.
  * @param view The view of the UI.
  */
  constructor(e, i) {
    super(e);
    /**
    * The main (top–most) view of the editor UI.
    */
    f(this, "view");
    /**
    * A normalized `config.toolbar` object.
    */
    f(this, "_toolbarConfig");
    /**
    * The element replacer instance used to hide the editor's source element.
    */
    f(this, "_elementReplacer");
    this.view = i, this._toolbarConfig = Hf(e.config.get("toolbar")), this._elementReplacer = new i_(), this.listenTo(e.editing.view, "scrollToTheSelection", this._handleScrollToTheSelectionWithStickyPanel.bind(this));
  }
  /**
  * @inheritDoc
  */
  get element() {
    return this.view.element;
  }
  /**
  * Initializes the UI.
  *
  * @param replacementElement The DOM element that will be the source for the created editor.
  */
  init(e) {
    const i = this.editor, s = this.view, r = i.editing.view, o = s.editable, a = r.document.getRoot();
    o.name = a.rootName, s.render();
    const l = o.element;
    this.setEditableElement(o.name, l), s.editable.bind("isFocused").to(this.focusTracker), r.attachDomRoot(l), e && this._elementReplacer.replace(e, this.element), this._initPlaceholder(), this._initToolbar(), s.menuBarView && this._initMenuBar(s.menuBarView), this._initDialogPluginIntegration(), this._initContextualBalloonIntegration(), this.fire("ready");
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy();
    const e = this.view, i = this.editor.editing.view;
    this._elementReplacer.restore(), i.getDomRoot(e.editable.name) && i.detachDomRoot(e.editable.name), e.destroy();
  }
  /**
  * Initializes the editor toolbar.
  */
  _initToolbar() {
    const e = this.view;
    e.stickyPanel.bind("isActive").to(this.focusTracker, "isFocused"), e.stickyPanel.limiterElement = e.element, e.stickyPanel.bind("viewportTopOffset").to(this, "viewportOffset", ({ top: i }) => i || 0), e.toolbar.fillFromConfig(this._toolbarConfig, this.componentFactory), this.addToolbar(e.toolbar);
  }
  /**
  * Enable the placeholder text on the editing root.
  */
  _initPlaceholder() {
    const e = this.editor, i = e.editing.view, s = i.document.getRoot(), r = e.sourceElement;
    let o;
    const a = e.config.get("placeholder");
    a && (o = typeof a == "string" ? a : a[this.view.editable.name]), !o && r && r.tagName.toLowerCase() === "textarea" && (o = r.getAttribute("placeholder")), o && (s.placeholder = o), xf({
      view: i,
      element: s,
      isDirectHost: !1,
      keepOnFocus: !0
    });
  }
  /**
  * Provides an integration between the sticky toolbar and {@link module:ui/panel/balloon/contextualballoon contextual balloon plugin}.
  * It allows the contextual balloon to consider the height of the
  * {@link module:editor-classic/classiceditoruiview~ClassicEditorUIView#stickyPanel}. It prevents the balloon from overlapping
  * the sticky toolbar by adjusting the balloon's position using viewport offset configuration.
  */
  _initContextualBalloonIntegration() {
    if (!this.editor.plugins.has("ContextualBalloon"))
      return;
    const { stickyPanel: e } = this.view, i = this.editor.plugins.get("ContextualBalloon");
    i.on("getPositionOptions", (r) => {
      const o = r.return;
      if (!o || !e.isSticky || !e.element)
        return;
      const a = new ke(e.element).height, l = typeof o.target == "function" ? o.target() : o.target, c = typeof o.limiter == "function" ? o.limiter() : o.limiter;
      if (l && c && new ke(l).height >= new ke(c).height - a)
        return;
      const u = {
        ...o.viewportOffsetConfig
      }, d = (u.top || 0) + a;
      r.return = {
        ...o,
        viewportOffsetConfig: {
          ...u,
          top: d
        }
      };
    }, {
      priority: "low"
    });
    const s = () => {
      i.visibleView && i.updatePosition();
    };
    this.listenTo(e, "change:isSticky", s), this.listenTo(this.editor.ui, "change:viewportOffset", s);
  }
  /**
  * Provides an integration between the sticky toolbar and {@link module:utils/dom/scroll~scrollViewportToShowTarget}.
  * It allows the UI-agnostic engine method to consider the geometry of the
  * {@link module:editor-classic/classiceditoruiview~ClassicEditorUIView#stickyPanel} that pins to the
  * edge of the viewport and can obscure the user caret after scrolling the window.
  *
  * @param evt The `scrollToTheSelection` event info.
  * @param data The payload carried by the `scrollToTheSelection` event.
  * @param originalArgs The original arguments passed to `scrollViewportToShowTarget()` method (see implementation to learn more).
  */
  _handleScrollToTheSelectionWithStickyPanel(e, i, s) {
    const r = this.view.stickyPanel;
    if (r.isSticky) {
      const o = new ke(r.element).height;
      i.viewportOffset.top += o;
    } else {
      const o = () => {
        this.editor.editing.view.scrollToTheSelection(s);
      };
      this.listenTo(r, "change:isSticky", o), setTimeout(() => {
        this.stopListening(r, "change:isSticky", o);
      }, 20);
    }
  }
  /**
  * Provides an integration between the sticky toolbar and {@link module:ui/dialog/dialog the Dialog plugin}.
  *
  * It moves the dialog down to ensure that the
  * {@link module:editor-classic/classiceditoruiview~ClassicEditorUIView#stickyPanel sticky panel}
  * used by the editor UI will not get obscured by the dialog when the dialog uses one of its automatic positions.
  */
  _initDialogPluginIntegration() {
    if (!this.editor.plugins.has("Dialog"))
      return;
    const e = this.view.stickyPanel, i = this.editor.plugins.get("Dialog");
    i.on("show", () => {
      const s = i.view;
      s.on("moveTo", (r, o) => {
        if (!e.isSticky || s.wasMoved || s.isModal)
          return;
        const a = new ke(e.contentPanelElement);
        o[1] < a.bottom + ha.defaultOffset && (o[1] = a.bottom + ha.defaultOffset);
      }, {
        priority: "high"
      });
    }, {
      priority: "low"
    });
  }
}
class zN extends q9 {
  /**
  * Creates an instance of the classic editor UI view.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param editingView The editing view instance this view is related to.
  * @param options Configuration options for the view instance.
  * @param options.shouldToolbarGroupWhenFull When set `true` enables automatic items grouping
  * in the main {@link module:editor-classic/classiceditoruiview~ClassicEditorUIView#toolbar toolbar}.
  * See {@link module:ui/toolbar/toolbarview~ToolbarOptions#shouldGroupWhenFull} to learn more.
  * @param options.label When set, this value will be used as an accessible `aria-label` of the
  * {@link module:ui/editableui/editableuiview~EditableUIView editable view}.
  */
  constructor(e, i, s = {}) {
    super(e);
    /**
    * Sticky panel view instance. This is a parent view of a {@link #toolbar}
    * that makes toolbar sticky.
    */
    f(this, "stickyPanel");
    /**
    * Toolbar view instance.
    */
    f(this, "toolbar");
    /**
    * Editable UI view.
    */
    f(this, "editable");
    this.stickyPanel = new J9(e), this.toolbar = new Us(e, {
      shouldGroupWhenFull: s.shouldToolbarGroupWhenFull
    }), s.useMenuBar && (this.menuBarView = new sB(e)), this.editable = new K9(e, i, void 0, {
      label: s.label
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.menuBarView ? this.stickyPanel.content.addMany([
      this.menuBarView,
      this.toolbar
    ]) : this.stickyPanel.content.add(this.toolbar), this.top.add(this.stickyPanel), this.main.add(this.editable);
  }
}
class HN extends (/* @__PURE__ */ gh(Tr)) {
  /**
  * Creates an instance of the classic editor.
  *
  * **Note:** do not use the constructor to create editor instances. Use the static
  * {@link module:editor-classic/classiceditor~ClassicEditor.create `ClassicEditor.create()`} method instead.
  *
  * @param sourceElementOrData The DOM element that will be the source for the created editor
  * or the editor's initial data. For more information see
  * {@link module:editor-classic/classiceditor~ClassicEditor.create `ClassicEditor.create()`}.
  * @param config The editor configuration.
  */
  constructor(e, i = {}) {
    if (!Bl(e) && i.initialData !== void 0)
      throw new S("editor-create-initial-data", null);
    super(i);
    /**
    * @inheritDoc
    */
    f(this, "ui");
    this.config.define("menuBar.isVisible", !1), this.config.get("initialData") === void 0 && this.config.set("initialData", jN(e)), Bl(e) && (this.sourceElement = e), this.model.document.createRoot();
    const s = !this.config.get("toolbar.shouldNotGroupWhenFull"), r = this.config.get("menuBar"), o = new zN(this.locale, this.editing.view, {
      shouldToolbarGroupWhenFull: s,
      useMenuBar: r.isVisible,
      label: this.config.get("label")
    });
    this.ui = new $N(this, o), L8(this);
  }
  /**
  * @inheritDoc
  */
  static get editorName() {
    return "ClassicEditor";
  }
  /**
  * Destroys the editor instance, releasing all resources used by it.
  *
  * Updates the original editor element with the data if the
  * {@link module:core/editor/editorconfig~EditorConfig#updateSourceElementOnDestroy `updateSourceElementOnDestroy`}
  * configuration option is set to `true`.
  */
  destroy() {
    return this.sourceElement && this.updateSourceElement(), this.ui.destroy(), super.destroy();
  }
  /**
  * Creates a new classic editor instance.
  *
  * There are three ways how the editor can be initialized.
  *
  * # Replacing a DOM element (and loading data from it)
  *
  * You can initialize the editor using an existing DOM element:
  *
  * ```ts
  * ClassicEditor
  * 	.create( document.querySelector( '#editor' ) )
  * 	.then( editor => {
  * 		console.log( 'Editor was initialized', editor );
  * 	} )
  * 	.catch( err => {
  * 		console.error( err.stack );
  * 	} );
  * ```
  *
  * The element's content will be used as the editor data and the element will be replaced by the editor UI.
  *
  * # Creating a detached editor
  *
  * Alternatively, you can initialize the editor by passing the initial data directly as a string.
  * In this case, the editor will render an element that must be inserted into the DOM:
  *
  * ```ts
  * ClassicEditor
  * 	.create( '<p>Hello world!</p>' )
  * 	.then( editor => {
  * 		console.log( 'Editor was initialized', editor );
  *
  * 		// Initial data was provided so the editor UI element needs to be added manually to the DOM.
  * 		document.body.appendChild( editor.ui.element );
  * 	} )
  * 	.catch( err => {
  * 		console.error( err.stack );
  * 	} );
  * ```
  *
  * This lets you dynamically append the editor to your web page whenever it is convenient for you. You may use this method if your
  * web page content is generated on the client side and the DOM structure is not ready at the moment when you initialize the editor.
  *
  * # Replacing a DOM element (and data provided in `config.initialData`)
  *
  * You can also mix these two ways by providing a DOM element to be used and passing the initial data through the configuration:
  *
  * ```ts
  * ClassicEditor
  * 	.create( document.querySelector( '#editor' ), {
  * 		initialData: '<h2>Initial data</h2><p>Foo bar.</p>'
  * 	} )
  * 	.then( editor => {
  * 		console.log( 'Editor was initialized', editor );
  * 	} )
  * 	.catch( err => {
  * 		console.error( err.stack );
  * 	} );
  * ```
  *
  * This method can be used to initialize the editor on an existing element with the specified content in case if your integration
  * makes it difficult to set the content of the source element.
  *
  * Note that an error will be thrown if you pass the initial data both as the first parameter and also in the configuration.
  *
  * # Configuring the editor
  *
  * See the {@link module:core/editor/editorconfig~EditorConfig editor configuration documentation} to learn more about
  * customizing plugins, toolbar and more.
  *
  * @param sourceElementOrData The DOM element that will be the source for the created editor
  * or the editor's initial data.
  *
  * If a DOM element is passed, its content will be automatically loaded to the editor upon initialization
  * and the {@link module:editor-classic/classiceditorui~ClassicEditorUI#element editor element} will replace the passed element
  * in the DOM (the original one will be hidden and the editor will be injected next to it).
  *
  * If the {@link module:core/editor/editorconfig~EditorConfig#updateSourceElementOnDestroy updateSourceElementOnDestroy}
  * option is set to `true`, the editor data will be set back to the original element once the editor is destroyed and when a form,
  * in which this element is contained, is submitted (if the original element is a `<textarea>`). This ensures seamless integration
  * with native web forms.
  *
  * If the initial data is passed, a detached editor will be created. In this case you need to insert it into the DOM manually.
  * It is available under the {@link module:editor-classic/classiceditorui~ClassicEditorUI#element `editor.ui.element`} property.
  *
  * @param config The editor configuration.
  * @returns A promise resolved once the editor is ready. The promise resolves with the created editor instance.
  */
  static create(e, i = {}) {
    return new Promise((s) => {
      const r = new this(e, i);
      s(r.initPlugins().then(() => r.ui.init(Bl(e) ? e : null)).then(() => r.data.init(r.config.get("initialData"))).then(() => r.fire("ready")).then(() => r));
    });
  }
}
function jN(n) {
  return Bl(n) ? F4(n) : n;
}
function Bl(n) {
  return Hn(n);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class UN extends he {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.affectsData = !1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = t.document.selection;
    let i = t.schema.getLimitElement(e);
    if (e.containsEntireContent(i) || !m1(t.schema, i))
      do
        if (i = i.parent, !i)
          return;
      while (!m1(t.schema, i));
    t.change((s) => {
      s.setSelection(i, "in");
    });
  }
}
function m1(n, t) {
  return n.isLimit(t) && (n.checkChild(t, "$text") || n.checkChild(t, "paragraph"));
}
const WN = /* @__PURE__ */ Uc("Ctrl+A");
class qN extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SelectAllEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t, s = t.editing.view.document;
    t.commands.add("selectAll", new UN(t)), this.listenTo(s, "keydown", (r, o) => {
      zs(o) === WN && (t.execute("selectAll"), o.preventDefault());
    }), t.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: e("Select all"),
          keystroke: "CTRL+A"
        }
      ]
    });
  }
}
var GN = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20"><path d="M.75 15.5a.75.75 0 0 1 .75.75V18l.008.09A.5.5 0 0 0 2 18.5h1.75a.75.75 0 1 1 0 1.5H1.5l-.144-.007a1.5 1.5 0 0 1-1.35-1.349L0 18.5v-2.25a.75.75 0 0 1 .75-.75zm18.5 0a.75.75 0 0 1 .75.75v2.25l-.007.144a1.5 1.5 0 0 1-1.349 1.35L18.5 20h-2.25a.75.75 0 1 1 0-1.5H18a.5.5 0 0 0 .492-.41L18.5 18v-1.75a.75.75 0 0 1 .75-.75zm-10.45 3c.11 0 .2.09.2.2v1.1a.2.2 0 0 1-.2.2H7.2a.2.2 0 0 1-.2-.2v-1.1c0-.11.09-.2.2-.2h1.6zm4 0c.11 0 .2.09.2.2v1.1a.2.2 0 0 1-.2.2h-1.6a.2.2 0 0 1-.2-.2v-1.1c0-.11.09-.2.2-.2h1.6zm.45-5.5a.75.75 0 1 1 0 1.5h-8.5a.75.75 0 1 1 0-1.5h8.5zM1.3 11c.11 0 .2.09.2.2v1.6a.2.2 0 0 1-.2.2H.2a.2.2 0 0 1-.2-.2v-1.6c0-.11.09-.2.2-.2h1.1zm18.5 0c.11 0 .2.09.2.2v1.6a.2.2 0 0 1-.2.2h-1.1a.2.2 0 0 1-.2-.2v-1.6c0-.11.09-.2.2-.2h1.1zm-4.55-2a.75.75 0 1 1 0 1.5H4.75a.75.75 0 1 1 0-1.5h10.5zM1.3 7c.11 0 .2.09.2.2v1.6a.2.2 0 0 1-.2.2H.2a.2.2 0 0 1-.2-.2V7.2c0-.11.09-.2.2-.2h1.1zm18.5 0c.11 0 .2.09.2.2v1.6a.2.2 0 0 1-.2.2h-1.1a.2.2 0 0 1-.2-.2V7.2c0-.11.09-.2.2-.2h1.1zm-4.55-2a.75.75 0 1 1 0 1.5h-2.5a.75.75 0 1 1 0-1.5h2.5zm-5 0a.75.75 0 1 1 0 1.5h-5.5a.75.75 0 0 1 0-1.5h5.5zm-6.5-5a.75.75 0 0 1 0 1.5H2a.5.5 0 0 0-.492.41L1.5 2v1.75a.75.75 0 0 1-1.5 0V1.5l.007-.144A1.5 1.5 0 0 1 1.356.006L1.5 0h2.25zM18.5 0l.144.007a1.5 1.5 0 0 1 1.35 1.349L20 1.5v2.25a.75.75 0 1 1-1.5 0V2l-.008-.09A.5.5 0 0 0 18 1.5h-1.75a.75.75 0 1 1 0-1.5h2.25zM8.8 0c.11 0 .2.09.2.2v1.1a.2.2 0 0 1-.2.2H7.2a.2.2 0 0 1-.2-.2V.2c0-.11.09-.2.2-.2h1.6zm4 0c.11 0 .2.09.2.2v1.1a.2.2 0 0 1-.2.2h-1.6a.2.2 0 0 1-.2-.2V.2c0-.11.09-.2.2-.2h1.6z"/></svg>';
class KN extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SelectAllUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add("selectAll", () => {
      const e = this._createButton(le);
      return e.set({
        tooltip: !0
      }), e;
    }), t.ui.componentFactory.add("menuBar:selectAll", () => this._createButton(Fe));
  }
  /**
  * Creates a button for select all command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, i = e.locale, s = e.commands.get("selectAll"), r = new t(e.locale), o = i.t;
    return r.set({
      label: o("Select all"),
      icon: GN,
      keystroke: "Ctrl+A"
    }), r.bind("isEnabled").to(s, "isEnabled"), this.listenTo(r, "execute", () => {
      e.execute("selectAll"), e.editing.view.focus();
    }), r;
  }
}
class ZN extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      qN,
      KN
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SelectAll";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class bk extends he {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Stack of items stored by the command. These are pairs of:
    *
    * * {@link module:engine/model/batch~Batch batch} saved by the command,
    * * {@link module:engine/model/selection~Selection selection} state at the moment of saving the batch.
    */
    f(this, "_stack", []);
    /**
    * Stores all batches that were created by this command.
    *
    * @internal
    */
    f(this, "_createdBatches", /* @__PURE__ */ new WeakSet());
    this.refresh(), this._isEnabledBasedOnSelection = !1, this.listenTo(e.data, "set", (i, s) => {
      s[1] = {
        ...s[1]
      };
      const r = s[1];
      r.batchType || (r.batchType = {
        isUndoable: !1
      });
    }, {
      priority: "high"
    }), this.listenTo(e.data, "set", (i, s) => {
      s[1].batchType.isUndoable || this.clearStack();
    });
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._stack.length > 0;
  }
  /**
  * Returns all batches created by this command.
  */
  get createdBatches() {
    return this._createdBatches;
  }
  /**
  * Stores a batch in the command, together with the selection state of the {@link module:engine/model/document~Document document}
  * created by the editor which this command is registered to.
  *
  * @param batch The batch to add.
  */
  addBatch(e) {
    const i = this.editor.model.document.selection, s = {
      ranges: i.hasOwnRange ? Array.from(i.getRanges()) : [],
      isBackward: i.isBackward
    };
    this._stack.push({
      batch: e,
      selection: s
    }), this.refresh();
  }
  /**
  * Removes all items from the stack.
  */
  clearStack() {
    this._stack = [], this.refresh();
  }
  /**
  * Restores the {@link module:engine/model/document~Document#selection document selection} state after a batch was undone.
  *
  * @param ranges Ranges to be restored.
  * @param isBackward A flag describing whether the restored range was selected forward or backward.
  * @param operations Operations which has been applied since selection has been stored.
  */
  _restoreSelection(e, i, s) {
    const r = this.editor.model, o = r.document, a = [], l = e.map((u) => u.getTransformedByOperations(s)), c = l.flat();
    for (const u of l) {
      const d = u.filter((h) => h.root != o.graveyard).filter((h) => !JN(h, c));
      d.length && (YN(d), a.push(d[0]));
    }
    a.length && r.change((u) => {
      u.setSelection(a, {
        backward: i
      });
    });
  }
  /**
  * Undoes a batch by reversing that batch, transforming reversed batch and finally applying it.
  * This is a helper method for {@link #execute}.
  *
  * @param batchToUndo The batch to be undone.
  * @param undoingBatch The batch that will contain undoing changes.
  */
  _undo(e, i) {
    const s = this.editor.model, r = s.document;
    this._createdBatches.add(i);
    const o = e.operations.slice().filter((a) => a.isDocumentOperation);
    o.reverse();
    for (const a of o) {
      const l = a.baseVersion + 1, c = Array.from(r.history.getOperations(l)), d = rO([
        a.getReversed()
      ], c, {
        useRelations: !0,
        document: this.editor.model.document,
        forceWeakRemove: !0
      }).operationsA;
      for (let h of d) {
        const m = h.affectedSelectable;
        m && !s.canEditAt(m) && (h = new Lt(h.baseVersion)), i.addOperation(h), s.applyOperation(h), r.history.setOperationAsUndone(a, h);
      }
    }
  }
}
function YN(n) {
  n.sort((t, e) => t.start.isBefore(e.start) ? -1 : 1);
  for (let t = 1; t < n.length; t++) {
    const i = n[t - 1].getJoined(n[t], !0);
    i && (t--, n.splice(t, 2, i));
  }
}
function JN(n, t) {
  return t.some((e) => e !== n && e.containsRange(n, !0));
}
class XN extends bk {
  /**
  * Executes the command. This method reverts a {@link module:engine/model/batch~Batch batch} added to the command's stack, transforms
  * and applies the reverted version on the {@link module:engine/model/document~Document document} and removes the batch from the stack.
  * Then, it restores the {@link module:engine/model/document~Document#selection document selection}.
  *
  * @fires execute
  * @fires revert
  * @param batch A batch that should be undone. If not set, the last added batch will be undone.
  */
  execute(t = null) {
    const e = t ? this._stack.findIndex((r) => r.batch == t) : this._stack.length - 1, i = this._stack.splice(e, 1)[0], s = this.editor.model.createBatch({
      isUndo: !0
    });
    this.editor.model.enqueueChange(s, () => {
      this._undo(i.batch, s);
      const r = this.editor.model.document.history.getOperations(i.batch.baseVersion);
      this._restoreSelection(i.selection.ranges, i.selection.isBackward, r);
    }), this.fire("revert", i.batch, s), this.refresh();
  }
}
class QN extends bk {
  /**
  * Executes the command. This method reverts the last {@link module:engine/model/batch~Batch batch} added to
  * the command's stack, applies the reverted and transformed version on the
  * {@link module:engine/model/document~Document document} and removes the batch from the stack.
  * Then, it restores the {@link module:engine/model/document~Document#selection document selection}.
  *
  * @fires execute
  */
  execute() {
    const t = this._stack.pop(), e = this.editor.model.createBatch({
      isUndo: !0
    });
    this.editor.model.enqueueChange(e, () => {
      const s = t.batch.operations[t.batch.operations.length - 1].baseVersion + 1, r = this.editor.model.document.history.getOperations(s);
      this._restoreSelection(t.selection.ranges, t.selection.isBackward, r), this._undo(t.batch, e);
    }), this.refresh();
  }
}
class eF extends I {
  constructor() {
    super(...arguments);
    /**
    * The command that manages the undo {@link module:engine/model/batch~Batch batches} stack (history).
    * Created and registered during the {@link #init feature initialization}.
    */
    f(this, "_undoCommand");
    /**
    * The command that manages the redo {@link module:engine/model/batch~Batch batches} stack (history).
    * Created and registered during the {@link #init feature initialization}.
    */
    f(this, "_redoCommand");
    /**
    * Keeps track of which batches were registered in undo.
    */
    f(this, "_batchRegistry", /* @__PURE__ */ new WeakSet());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "UndoEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.t;
    this._undoCommand = new XN(e), this._redoCommand = new QN(e), e.commands.add("undo", this._undoCommand), e.commands.add("redo", this._redoCommand), this.listenTo(e.model, "applyOperation", (s, r) => {
      const o = r[0];
      if (!o.isDocumentOperation)
        return;
      const a = o.batch, l = this._redoCommand.createdBatches.has(a), c = this._undoCommand.createdBatches.has(a);
      this._batchRegistry.has(a) || (this._batchRegistry.add(a), a.isUndoable && (l ? this._undoCommand.addBatch(a) : c || (this._undoCommand.addBatch(a), this._redoCommand.clearStack())));
    }, {
      priority: "highest"
    }), this.listenTo(this._undoCommand, "revert", (s, r, o) => {
      this._redoCommand.addBatch(o);
    }), e.keystrokes.set("CTRL+Z", "undo"), e.keystrokes.set("CTRL+Y", "redo"), e.keystrokes.set("CTRL+SHIFT+Z", "redo"), e.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: i("Undo"),
          keystroke: "CTRL+Z"
        },
        {
          label: i("Redo"),
          keystroke: [
            [
              "CTRL+Y"
            ],
            [
              "CTRL+SHIFT+Z"
            ]
          ]
        }
      ]
    });
  }
}
class tF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "UndoUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale, i = t.t, s = e.uiLanguageDirection == "ltr" ? Z.undo : Z.redo, r = e.uiLanguageDirection == "ltr" ? Z.redo : Z.undo;
    this._addButtonsToFactory("undo", i("Undo"), "CTRL+Z", s), this._addButtonsToFactory("redo", i("Redo"), "CTRL+Y", r);
  }
  /**
  * Creates a button for the specified command.
  *
  * @param name Command name.
  * @param label Button label.
  * @param keystroke Command keystroke.
  * @param Icon Source of the icon.
  */
  _addButtonsToFactory(t, e, i, s) {
    const r = this.editor;
    r.ui.componentFactory.add(t, () => {
      const o = this._createButton(le, t, e, i, s);
      return o.set({
        tooltip: !0
      }), o;
    }), r.ui.componentFactory.add("menuBar:" + t, () => this._createButton(Fe, t, e, i, s));
  }
  /**
  * TODO
  */
  _createButton(t, e, i, s, r) {
    const o = this.editor, a = o.locale, l = o.commands.get(e), c = new t(a);
    return c.set({
      label: i,
      icon: r,
      keystroke: s
    }), c.bind("isEnabled").to(l, "isEnabled"), this.listenTo(c, "execute", () => {
      o.execute(e), o.editing.view.focus();
    }), c;
  }
}
class Qf extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      eF,
      tF
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Undo";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class iF extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      NL,
      Jf,
      Fa,
      ZN,
      rk,
      ek,
      Qf
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Essentials";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class nF extends ee {
  /**
  * Creates a view of find and replace form.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * A collection of child views.
    */
    f(this, "children");
    /**
    * The find in text input view that stores the searched string.
    *
    * @internal
    */
    f(this, "_findInputView");
    /**
    * The replace input view.
    */
    f(this, "_replaceInputView");
    /**
    * The find button view that initializes the search process.
    */
    f(this, "_findButtonView");
    /**
    * The find previous button view.
    */
    f(this, "_findPrevButtonView");
    /**
    * The find next button view.
    */
    f(this, "_findNextButtonView");
    /**
    * A collapsible view aggregating the advanced search options.
    */
    f(this, "_advancedOptionsCollapsibleView");
    /**
    * A switch button view controlling the "Match case" option.
    */
    f(this, "_matchCaseSwitchView");
    /**
    * A switch button view controlling the "Whole words only" option.
    */
    f(this, "_wholeWordsOnlySwitchView");
    /**
    * The replace button view.
    */
    f(this, "_replaceButtonView");
    /**
    * The replace all button view.
    */
    f(this, "_replaceAllButtonView");
    /**
    * The `div` aggregating the inputs.
    */
    f(this, "_inputsDivView");
    /**
    * The `div` aggregating the action buttons.
    */
    f(this, "_actionButtonsDivView");
    /**
    * Tracks information about the DOM focus in the form.
    */
    f(this, "_focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "_keystrokes");
    /**
    * A collection of views that can be focused in the form.
    */
    f(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    f(this, "focusCycler");
    const i = e.t;
    this.children = this.createCollection(), this.set("matchCount", 0), this.set("highlightOffset", 0), this.set("isDirty", !1), this.set("_areCommandsEnabled", {}), this.set("_resultsCounterText", ""), this.set("_matchCase", !1), this.set("_wholeWordsOnly", !1), this.bind("_searchResultsFound").to(this, "matchCount", this, "isDirty", (s, r) => s > 0 && !r), this._findInputView = this._createInputField(i("Find in text…")), this._findPrevButtonView = this._createButton({
      label: i("Previous result"),
      class: "ck-button-prev",
      icon: Z.previousArrow,
      keystroke: "Shift+F3",
      tooltip: !0
    }), this._findNextButtonView = this._createButton({
      label: i("Next result"),
      class: "ck-button-next",
      icon: Z.previousArrow,
      keystroke: "F3",
      tooltip: !0
    }), this._replaceInputView = this._createInputField(i("Replace with…"), "ck-labeled-field-replace"), this._inputsDivView = this._createInputsDiv(), this._matchCaseSwitchView = this._createMatchCaseSwitch(), this._wholeWordsOnlySwitchView = this._createWholeWordsOnlySwitch(), this._advancedOptionsCollapsibleView = this._createAdvancedOptionsCollapsible(), this._replaceAllButtonView = this._createButton({
      label: i("Replace all"),
      class: "ck-button-replaceall",
      withText: !0
    }), this._replaceButtonView = this._createButton({
      label: i("Replace"),
      class: "ck-button-replace",
      withText: !0
    }), this._findButtonView = this._createButton({
      label: i("Find"),
      class: "ck-button-find ck-button-action",
      withText: !0
    }), this._actionButtonsDivView = this._createActionButtonsDiv(), this._focusTracker = new ze(), this._keystrokes = new Ze(), this._focusables = new Et(), this.focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this._focusTracker,
      keystrokeHandler: this._keystrokes,
      actions: {
        // Navigate form fields backwards using the <kbd>Shift</kbd> + <kbd>Tab</kbd> keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the <kbd>Tab</kbd> key.
        focusNext: "tab"
      }
    }), this.children.addMany([
      this._inputsDivView,
      this._advancedOptionsCollapsibleView,
      this._actionButtonsDivView
    ]), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-find-and-replace-form"
        ],
        tabindex: "-1"
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Tn({
      view: this
    }), this._initFocusCycling(), this._initKeystrokeHandling();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._focusTracker.destroy(), this._keystrokes.destroy();
  }
  /**
  * @inheritDoc
  */
  focus(e) {
    e === -1 ? this.focusCycler.focusLast() : this.focusCycler.focusFirst();
  }
  /**
  * Resets the form before re-appearing.
  *
  * It clears error messages, hides the match counter and disables the replace feature
  * until the next hit of the "Find" button.
  *
  * **Note**: It does not reset inputs and options, though. This way the form works better in editors with
  * disappearing toolbar (e.g. BalloonEditor): hiding the toolbar by accident (together with the find and replace UI)
  * does not require filling the entire form again.
  */
  reset() {
    this._findInputView.errorText = null, this.isDirty = !0;
  }
  /**
  * Returns the value of the find input.
  */
  get _textToFind() {
    return this._findInputView.fieldView.element.value;
  }
  /**
  * Returns the value of the replace input.
  */
  get _textToReplace() {
    return this._replaceInputView.fieldView.element.value;
  }
  /**
  * Configures and returns the `<div>` aggregating all form inputs.
  */
  _createInputsDiv() {
    const e = this.locale, i = e.t, s = new ee(e);
    return this._findInputView.fieldView.on("input", () => {
      this.isDirty = !0;
    }), this._findPrevButtonView.delegate("execute").to(this, "findPrevious"), this._findNextButtonView.delegate("execute").to(this, "findNext"), this._findPrevButtonView.bind("isEnabled").to(this, "_areCommandsEnabled", ({ findPrevious: r }) => r), this._findNextButtonView.bind("isEnabled").to(this, "_areCommandsEnabled", ({ findNext: r }) => r), this._injectFindResultsCounter(), this._replaceInputView.bind("isEnabled").to(this, "_areCommandsEnabled", this, "_searchResultsFound", ({ replace: r }, o) => r && o), this._replaceInputView.bind("infoText").to(this._replaceInputView, "isEnabled", this._replaceInputView, "isFocused", (r, o) => r || !o ? "" : i("Tip: Find some text first in order to replace it.")), s.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-find-and-replace-form__inputs"
        ]
      },
      children: [
        this._findInputView,
        this._findPrevButtonView,
        this._findNextButtonView,
        this._replaceInputView
      ]
    }), s;
  }
  /**
  * The action performed when the {@link #_findButtonView} is pressed.
  */
  _onFindButtonExecute() {
    if (!this._textToFind) {
      const e = this.t;
      this._findInputView.errorText = e("Text to find must not be empty.");
      return;
    }
    this.isDirty = !1, this.fire("findNext", {
      searchText: this._textToFind,
      matchCase: this._matchCase,
      wholeWords: this._wholeWordsOnly
    });
  }
  /**
  * Configures an injects the find results counter displaying a "N of M" label of the {@link #_findInputView}.
  */
  _injectFindResultsCounter() {
    const e = this.locale, i = e.t, s = this.bindTemplate, r = new ee(this.locale);
    this.bind("_resultsCounterText").to(this, "highlightOffset", this, "matchCount", (a, l) => i("%0 of %1", [
      a,
      l
    ])), r.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-results-counter",
          // The counter only makes sense when the field text corresponds to search results in the editing.
          s.if("isDirty", "ck-hidden")
        ]
      },
      children: [
        {
          text: s.to("_resultsCounterText")
        }
      ]
    });
    const o = () => {
      const a = this._findInputView.fieldView.element;
      if (!a || !yn(a))
        return;
      const l = new ke(r.element).width, c = e.uiLanguageDirection === "ltr" ? "paddingRight" : "paddingLeft";
      l ? a.style[c] = `calc( 2 * var(--ck-spacing-standard) + ${l}px )` : a.style[c] = "";
    };
    this.on("change:_resultsCounterText", o, {
      priority: "low"
    }), this.on("change:isDirty", o, {
      priority: "low"
    }), this._findInputView.template.children[0].children.push(r);
  }
  /**
  * Creates the collapsible view aggregating the advanced search options.
  */
  _createAdvancedOptionsCollapsible() {
    const e = this.locale.t, i = new $f(this.locale, [
      this._matchCaseSwitchView,
      this._wholeWordsOnlySwitchView
    ]);
    return i.set({
      label: e("Advanced options"),
      isCollapsed: !0
    }), i;
  }
  /**
  * Configures and returns the `<div>` element aggregating all form action buttons.
  */
  _createActionButtonsDiv() {
    const e = new ee(this.locale);
    return this._replaceButtonView.bind("isEnabled").to(this, "_areCommandsEnabled", this, "_searchResultsFound", ({ replace: i }, s) => i && s), this._replaceAllButtonView.bind("isEnabled").to(this, "_areCommandsEnabled", this, "_searchResultsFound", ({ replaceAll: i }, s) => i && s), this._replaceButtonView.on("execute", () => {
      this.fire("replace", {
        searchText: this._textToFind,
        replaceText: this._textToReplace
      });
    }), this._replaceAllButtonView.on("execute", () => {
      this.fire("replaceAll", {
        searchText: this._textToFind,
        replaceText: this._textToReplace
      }), this.focus();
    }), this._findButtonView.on("execute", this._onFindButtonExecute.bind(this)), e.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-find-and-replace-form__actions"
        ]
      },
      children: [
        this._replaceAllButtonView,
        this._replaceButtonView,
        this._findButtonView
      ]
    }), e;
  }
  /**
  * Creates, configures and returns and instance of a dropdown allowing users to narrow
  * the search criteria down. The dropdown has a list with switch buttons for each option.
  */
  _createMatchCaseSwitch() {
    const e = this.locale.t, i = new js(this.locale);
    return i.set({
      label: e("Match case"),
      withText: !0
    }), i.bind("isOn").to(this, "_matchCase"), i.on("execute", () => {
      this._matchCase = !this._matchCase, this.isDirty = !0;
    }), i;
  }
  /**
  * Creates, configures and returns and instance of a dropdown allowing users to narrow
  * the search criteria down. The dropdown has a list with switch buttons for each option.
  */
  _createWholeWordsOnlySwitch() {
    const e = this.locale.t, i = new js(this.locale);
    return i.set({
      label: e("Whole words only"),
      withText: !0
    }), i.bind("isOn").to(this, "_wholeWordsOnly"), i.on("execute", () => {
      this._wholeWordsOnly = !this._wholeWordsOnly, this.isDirty = !0;
    }), i;
  }
  /**
  * Initializes the {@link #_focusables} and {@link #_focusTracker} to allow navigation
  * using <kbd>Tab</kbd> and <kbd>Shift</kbd>+<kbd>Tab</kbd> keystrokes in the right order.
  */
  _initFocusCycling() {
    [
      this._findInputView,
      this._findPrevButtonView,
      this._findNextButtonView,
      this._replaceInputView,
      this._advancedOptionsCollapsibleView.buttonView,
      this._matchCaseSwitchView,
      this._wholeWordsOnlySwitchView,
      this._replaceAllButtonView,
      this._replaceButtonView,
      this._findButtonView
    ].forEach((i) => {
      this._focusables.add(i), this._focusTracker.add(i.element);
    });
  }
  /**
  * Initializes the keystroke handling in the form.
  */
  _initKeystrokeHandling() {
    const e = (s) => s.stopPropagation(), i = (s) => {
      s.stopPropagation(), s.preventDefault();
    };
    this._keystrokes.listenTo(this.element), this._keystrokes.set("f3", (s) => {
      i(s), this._findNextButtonView.fire("execute");
    }), this._keystrokes.set("shift+f3", (s) => {
      i(s), this._findPrevButtonView.fire("execute");
    }), this._keystrokes.set("enter", (s) => {
      const r = s.target;
      r === this._findInputView.fieldView.element ? (this._areCommandsEnabled.findNext ? this._findNextButtonView.fire("execute") : this._findButtonView.fire("execute"), i(s)) : r === this._replaceInputView.fieldView.element && !this.isDirty && (this._replaceButtonView.fire("execute"), i(s));
    }), this._keystrokes.set("shift+enter", (s) => {
      s.target === this._findInputView.fieldView.element && (this._areCommandsEnabled.findPrevious ? this._findPrevButtonView.fire("execute") : this._findButtonView.fire("execute"), i(s));
    }), this._keystrokes.set("arrowright", e), this._keystrokes.set("arrowleft", e), this._keystrokes.set("arrowup", e), this._keystrokes.set("arrowdown", e);
  }
  /**
  * Creates a button view.
  *
  * @param options The properties of the `ButtonView`.
  * @returns The button view instance.
  */
  _createButton(e) {
    const i = new le(this.locale);
    return i.set(e), i;
  }
  /**
  * Creates a labeled input view.
  *
  * @param label The input label.
  * @returns The labeled input view instance.
  */
  _createInputField(e, i) {
    const s = new gt(this.locale, pi);
    return s.label = e, s.class = i, s;
  }
}
var g1 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m12.87 13.786 1.532-1.286 3.857 4.596a1 1 0 1 1-1.532 1.286l-3.857-4.596z"/><path d="M16.004 8.5a6.5 6.5 0 0 1-9.216 5.905c-1.154-.53-.863-1.415-.663-1.615.194-.194.564-.592 1.635-.141a4.5 4.5 0 0 0 5.89-5.904l-.104-.227 1.332-1.331c.045-.046.196-.041.224.007a6.47 6.47 0 0 1 .902 3.306zm-3.4-5.715c.562.305.742 1.106.354 1.494-.388.388-.995.414-1.476.178a4.5 4.5 0 0 0-6.086 5.882l.114.236-1.348 1.349c-.038.037-.17.022-.198-.023a6.5 6.5 0 0 1 5.54-9.9 6.469 6.469 0 0 1 3.1.784z"/><path d="M4.001 11.93.948 8.877a.2.2 0 0 1 .141-.341h6.106a.2.2 0 0 1 .141.341L4.283 11.93a.2.2 0 0 1-.282 0zm11.083-6.789 3.053 3.053a.2.2 0 0 1-.14.342H11.89a.2.2 0 0 1-.14-.342l3.052-3.053a.2.2 0 0 1 .282 0z"/></svg>';
class sF extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A reference to the find and replace form view.
    */
    f(this, "formView");
    e.config.define("findAndReplace.uiType", "dialog"), this.formView = null;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hs
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FindAndReplaceUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.config.get("findAndReplace.uiType") === "dropdown", s = e.commands.get("find"), r = this.editor.t;
    e.ui.componentFactory.add("findAndReplace", () => {
      let o;
      return i ? (o = this._createDropdown(), o.bind("isEnabled").to(s)) : o = this._createDialogButtonForToolbar(), e.keystrokes.set("Ctrl+F", (a, l) => {
        if (s.isEnabled) {
          if (o instanceof nc) {
            const c = o.buttonView;
            c.isOn || c.fire("execute");
          } else
            o.isOn ? e.plugins.get("Dialog").view.focus() : o.fire("execute");
          l();
        }
      }), o;
    }), i || e.ui.componentFactory.add("menuBar:findAndReplace", () => this._createDialogButtonForMenuBar()), e.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: r("Find in the document"),
          keystroke: "CTRL+F"
        }
      ]
    });
  }
  /**
  * Creates a dropdown containing the find and replace form.
  */
  _createDropdown() {
    const e = this.editor, i = e.locale.t, s = Tt(e.locale);
    return s.once("change:isOpen", () => {
      this.formView = this._createFormView(), this.formView.children.add(new Va(e.locale, {
        label: i("Find and replace")
      }), 0), s.panelView.children.add(this.formView);
    }), s.on("change:isOpen", (r, o, a) => {
      a ? this._setupFormView() : this.fire("searchReseted");
    }, {
      priority: "low"
    }), s.buttonView.set({
      icon: g1,
      label: i("Find and replace"),
      keystroke: "CTRL+F",
      tooltip: !0
    }), s;
  }
  /**
  * Creates a button that opens a dialog with the find and replace form.
  */
  _createDialogButtonForToolbar() {
    const e = this.editor, i = this._createButton(le), s = e.plugins.get("Dialog");
    return i.set({
      tooltip: !0
    }), i.bind("isOn").to(s, "id", (r) => r === "findAndReplace"), i.on("execute", () => {
      i.isOn ? s.hide() : this._showDialog();
    }), i;
  }
  /**
  * Creates a button for for menu bar that will show find and replace dialog.
  */
  _createDialogButtonForMenuBar() {
    const e = this._createButton(Fe), i = this.editor.plugins.get("Dialog"), s = this.editor.plugins.get("Dialog");
    return e.set({
      role: "menuitemcheckbox",
      isToggleable: !0
    }), e.bind("isOn").to(s, "id", (r) => r === "findAndReplace"), e.on("execute", () => {
      if (i.id === "findAndReplace") {
        i.hide();
        return;
      }
      this._showDialog();
    }), e;
  }
  /**
  * Creates a button for find and replace command to use either in toolbar or in menu bar.
  */
  _createButton(e) {
    const i = this.editor, s = i.commands.get("find"), r = new e(i.locale), o = i.locale.t;
    return r.bind("isEnabled").to(s), r.set({
      icon: g1,
      label: o("Find and replace"),
      keystroke: "CTRL+F"
    }), r;
  }
  /**
  * Shows the find and replace dialog.
  */
  _showDialog() {
    const e = this.editor, i = e.plugins.get("Dialog"), s = e.locale.t;
    this.formView || (this.formView = this._createFormView()), i.show({
      id: "findAndReplace",
      title: s("Find and replace"),
      content: this.formView,
      position: Ci.EDITOR_TOP_SIDE,
      onShow: () => {
        this._setupFormView();
      },
      onHide: () => {
        this.fire("searchReseted");
      }
    });
  }
  /**
  * Sets up the form view for the find and replace.
  *
  * @param formView A related form view.
  */
  _createFormView() {
    const e = this.editor, i = new (no(nF))(e.locale), s = e.commands, o = this.editor.plugins.get("FindAndReplaceEditing").state;
    i.bind("highlightOffset").to(o, "highlightedOffset"), i.listenTo(o.results, "change", () => {
      i.matchCount = o.results.length;
    });
    const a = s.get("findNext"), l = s.get("findPrevious"), c = s.get("replace"), u = s.get("replaceAll");
    return i.bind("_areCommandsEnabled").to(a, "isEnabled", l, "isEnabled", c, "isEnabled", u, "isEnabled", (d, h, m, g) => ({
      findNext: d,
      findPrevious: h,
      replace: m,
      replaceAll: g
    })), i.delegate("findNext", "findPrevious", "replace", "replaceAll").to(this), i.on("change:isDirty", (d, h, m) => {
      m && this.fire("searchReseted");
    }), i;
  }
  /**
  * Clears the find and replace form and focuses the search text field.
  */
  _setupFormView() {
    this.formView.disableCssTransitions(), this.formView.reset(), this.formView._findInputView.fieldView.select(), this.formView.enableCssTransitions();
  }
}
class rF extends he {
  /**
  * Creates a new `FindCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param state An object to hold plugin state.
  */
  constructor(e, i) {
    super(e);
    /**
    * The find and replace state object used for command operations.
    */
    f(this, "_state");
    this.isEnabled = !0, this.affectsData = !1, this._state = i;
  }
  /**
  * Executes the command.
  *
  * @param callbackOrText
  * @param options Options object.
  * @param options.matchCase If set to `true`, the letter case will be matched.
  * @param options.wholeWords If set to `true`, only whole words that match `callbackOrText` will be matched.
  *
  * @fires execute
  */
  execute(e, { matchCase: i, wholeWords: s } = {}) {
    const { editor: r } = this, { model: o } = r, a = r.plugins.get("FindAndReplaceUtils");
    let l, c = "";
    typeof e == "string" ? l = (...h) => ({
      results: a.findByTextCallback(e, {
        matchCase: i,
        wholeWords: s
      })(...h),
      searchText: e
    }) : l = e;
    const u = l;
    l = (...h) => {
      const m = u(...h);
      return m && "searchText" in m && (c = m.searchText), m;
    };
    const d = o.document.getRootNames().reduce((h, m) => a.updateFindResultFromRange(o.createRangeIn(o.document.getRoot(m)), o, l, h), null);
    return this._state.clear(o), this._state.results.addMany(d), this._state.highlightedResult = d.get(0), this._state.searchText = c, l && (this._state.lastSearchCallback = l), this._state.matchCase = !!i, this._state.matchWholeWords = !!s, {
      results: d,
      findCallback: l
    };
  }
}
class oF extends (/* @__PURE__ */ je()) {
  /**
  * Creates an instance of the state.
  */
  constructor(t) {
    super(), this.set("results", new bt()), this.set("highlightedResult", null), this.set("highlightedOffset", 0), this.set("searchText", ""), this.set("replaceText", ""), this.set("lastSearchCallback", null), this.set("matchCase", !1), this.set("matchWholeWords", !1), this.results.on("change", (e, { removed: i, index: s }) => {
      if (Array.from(i).length) {
        let r = !1;
        if (t.change((o) => {
          for (const a of i)
            this.highlightedResult === a && (r = !0), t.markers.has(a.marker.name) && o.removeMarker(a.marker);
        }), r) {
          const o = s >= this.results.length ? 0 : s;
          this.highlightedResult = this.results.get(o);
        }
      }
    }), this.on("change:highlightedResult", () => {
      this.refreshHighlightOffset(t);
    });
  }
  /**
  * Cleans the state up and removes markers from the model.
  */
  clear(t) {
    this.searchText = "", t.change((e) => {
      if (this.highlightedResult) {
        const i = this.highlightedResult.marker.name.split(":")[1], s = t.markers.get(`findResultHighlighted:${i}`);
        s && e.removeMarker(s);
      }
      [
        ...this.results
      ].forEach(({ marker: i }) => {
        e.removeMarker(i);
      });
    }), this.results.clear();
  }
  /**
  * Refreshes the highlight result offset based on it's index within the result list.
  */
  refreshHighlightOffset(t) {
    const { highlightedResult: e, results: i } = this;
    e ? this.highlightedOffset = wk(t, [
      ...i
    ]).indexOf(e) + 1 : this.highlightedOffset = 0;
  }
}
function wk(n, t) {
  const e = {
    before: -1,
    same: 0,
    after: 1,
    different: 1
  };
  return n.document.getRootNames().flatMap((i) => t.filter((s) => s.marker.getStart().root.rootName === i).sort((s, r) => e[s.marker.getStart().compareWith(r.marker.getStart())]));
}
class vk extends he {
  /**
  * Creates a new `ReplaceCommand` instance.
  *
  * @param editor Editor on which this command will be used.
  * @param state An object to hold plugin state.
  */
  constructor(e, i) {
    super(e);
    /**
    * The find and replace state object used for command operations.
    */
    f(this, "_state");
    this.isEnabled = !0, this._state = i, this._isEnabledBasedOnSelection = !1;
  }
  /**
  * Common logic for both `replace` commands.
  * Replace a given find result by a string or a callback.
  *
  * @param result A single result from the find command.
  */
  _replace(e, i) {
    const { model: s } = this.editor, r = i.marker.getRange();
    s.canEditAt(r) && s.change((o) => {
      if (r.root.rootName === "$graveyard") {
        this._state.results.remove(i);
        return;
      }
      let a = {};
      for (const l of r.getItems())
        if (l.is("$text") || l.is("$textProxy")) {
          a = l.getAttributes();
          break;
        }
      s.insertContent(o.createText(e, a), r), this._state.results.has(i) && this._state.results.remove(i);
    });
  }
}
class aF extends vk {
  /**
  * Replace a given find result by a string or a callback.
  *
  * @param result A single result from the find command.
  *
  * @fires execute
  */
  execute(t, e) {
    const i = Math.max(this._state.highlightedOffset - 1, 0);
    if (this._replace(t, e), this._state.results.length) {
      const s = wk(this.editor.model, [
        ...this._state.results
      ]);
      this._state.highlightedResult = s[i % s.length];
    }
  }
}
class lF extends vk {
  /**
  * Replaces all the occurrences of `textToReplace` with a given `newText` string.
  *
  * ```ts
  *	replaceAllCommand.execute( 'replaceAll', 'new text replacement', 'text to replace' );
  * ```
  *
  * Alternatively you can call it from editor instance:
  *
  * ```ts
  *	editor.execute( 'replaceAll', 'new text', 'old text' );
  * ```
  *
  * @param newText Text that will be inserted to the editor for each match.
  * @param textToReplace Text to be replaced or a collection of matches
  * as returned by the find command.
  *
  * @fires module:core/command~Command#event:execute
  */
  execute(t, e) {
    const { editor: i } = this, { model: s } = i, r = i.plugins.get("FindAndReplaceUtils"), o = e instanceof bt ? e : s.document.getRootNames().reduce((a, l) => r.updateFindResultFromRange(s.createRangeIn(s.document.getRoot(l)), s, r.findByTextCallback(e, this._state), a), null);
    o.length && s.change(() => {
      [
        ...o
      ].forEach((a) => {
        this._replace(t, a);
      });
    });
  }
}
class _k extends he {
  /**
  * Creates a new `FindNextCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param state An object to hold plugin state.
  */
  constructor(e, i) {
    super(e);
    /**
    * The find and replace state object used for command operations.
    */
    f(this, "_state");
    this.affectsData = !1, this._state = i, this.isEnabled = !1, this.listenTo(this._state.results, "change", () => {
      this.isEnabled = this._state.results.length > 1;
    });
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._state.results.length > 1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const e = this._state.results, i = e.getIndex(this._state.highlightedResult), s = i + 1 >= e.length ? 0 : i + 1;
    this._state.highlightedResult = this._state.results.get(s);
  }
}
class cF extends _k {
  /**
  * @inheritDoc
  */
  execute() {
    const e = this._state.results.getIndex(this._state.highlightedResult), i = e - 1 < 0 ? this._state.results.length - 1 : e - 1;
    this._state.highlightedResult = this._state.results.get(i);
  }
}
class uF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FindAndReplaceUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Executes findCallback and updates search results list.
  *
  * @param range The model range to scan for matches.
  * @param model The model.
  * @param findCallback The callback that should return `true` if provided text matches the search term.
  * @param startResults An optional collection of find matches that the function should
  * start with. This would be a collection returned by a previous `updateFindResultFromRange()` call.
  * @returns A collection of objects describing find match.
  *
  * An example structure:
  *
  * ```js
  * {
  *	id: resultId,
  *	label: foundItem.label,
  *	marker
  *	}
  * ```
  */
  updateFindResultFromRange(t, e, i, s) {
    const r = s || new bt(), o = (a) => r.find((l) => {
      const { marker: c } = l, u = c.getRange(), d = a.getRange();
      return u.isEqual(d);
    });
    return e.change((a) => {
      [
        ...t
      ].forEach(({ type: l, item: c }) => {
        if (l === "elementStart" && e.schema.checkChild(c, "$text")) {
          let u = i({
            item: c,
            text: this.rangeToText(e.createRangeIn(c))
          });
          if (!u)
            return;
          "results" in u && (u = u.results), u.forEach((d) => {
            const h = `findResult:${Bt()}`, m = a.addMarker(h, {
              usingOperation: !1,
              affectsData: !1,
              range: a.createRange(a.createPositionAt(c, d.start), a.createPositionAt(c, d.end))
            }), g = dF(r, m);
            o(m) || r.add({
              id: h,
              label: d.label,
              marker: m
            }, g);
          });
        }
      });
    }), r;
  }
  /**
  * Returns text representation of a range. The returned text length should be the same as range length.
  * In order to achieve this, this function will replace inline elements (text-line) as new line character ("\n").
  *
  * @param range The model range.
  * @returns The text content of the provided range.
  */
  rangeToText(t) {
    return Array.from(t.getItems({
      shallow: !0
    })).reduce((e, i) => i.is("$text") || i.is("$textProxy") ? e + i.data : `${e}
`, "");
  }
  /**
  * Creates a text matching callback for a specified search term and matching options.
  *
  * @param searchTerm The search term.
  * @param options Matching options.
  * 	- options.matchCase=false If set to `true` letter casing will be ignored.
  * 	- options.wholeWords=false If set to `true` only whole words that match `callbackOrText` will be matched.
  */
  findByTextCallback(t, e) {
    let i = "gu";
    e.matchCase || (i += "i");
    let s = `(${Uv(t)})`;
    if (e.wholeWords) {
      const a = "[^a-zA-ZÀ-ɏḀ-ỿ]";
      new RegExp("^" + a).test(t) || (s = `(^|${a}|_)${s}`), new RegExp(a + "$").test(t) || (s = `${s}(?=_|${a}|$)`);
    }
    const r = new RegExp(s, i);
    function o({ text: a }) {
      return [
        ...a.matchAll(r)
      ].map(hF);
    }
    return o;
  }
}
function dF(n, t) {
  const e = n.find(({ marker: i }) => t.getStart().isBefore(i.getStart()));
  return e ? n.getIndex(e) : n.length;
}
function hF(n) {
  const t = n.length - 1;
  let e = n.index;
  return n.length === 3 && (e += n[1].length), {
    label: n[t],
    start: e,
    end: e + n[t].length
  };
}
const fF = "ck-find-result_selected";
class mF extends I {
  constructor() {
    super(...arguments);
    /**
    * An object storing the find and replace state within a given editor instance.
    */
    f(this, "state");
    /**
    * Reacts to document changes in order to update search list.
    */
    f(this, "_onDocumentChange", () => {
      const e = /* @__PURE__ */ new Set(), i = /* @__PURE__ */ new Set(), s = this.editor.model, { results: r } = this.state, o = s.document.differ.getChanges(), a = s.document.differ.getChangedMarkers();
      o.forEach((u) => {
        u.position && (u.name === "$text" || u.position.nodeAfter && s.schema.isInline(u.position.nodeAfter) ? (e.add(u.position.parent), [
          ...s.markers.getMarkersAtPosition(u.position)
        ].forEach((d) => {
          i.add(d.name);
        })) : u.type === "insert" && u.position.nodeAfter && e.add(u.position.nodeAfter));
      }), a.forEach(({ name: u, data: { newRange: d } }) => {
        d && d.start.root.rootName === "$graveyard" && i.add(u);
      }), e.forEach((u) => {
        [
          ...s.markers.getMarkersIntersectingRange(s.createRangeIn(u))
        ].forEach((h) => i.add(h.name));
      }), i.forEach((u) => {
        r.has(u) && (r.get(u) === this.state.highlightedResult && (this.state.highlightedResult = null), r.remove(u));
      });
      const l = [], c = this.editor.plugins.get("FindAndReplaceUtils");
      e.forEach((u) => {
        const d = c.updateFindResultFromRange(s.createRangeOn(u), s, this.state.lastSearchCallback, r);
        l.push(...d);
      }), a.forEach((u) => {
        if (u.data.newRange) {
          const d = c.updateFindResultFromRange(u.data.newRange, s, this.state.lastSearchCallback, r);
          l.push(...d);
        }
      }), !this.state.highlightedResult && l.length ? this.state.highlightedResult = l[0] : this.state.refreshHighlightOffset(s);
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      uF
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FindAndReplaceEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.state = new oF(this.editor.model), this.set("_isSearchActive", !1), this._defineConverters(), this._defineCommands(), this.listenTo(this.state, "change:highlightedResult", (s, r, o, a) => {
      const { model: l } = this.editor;
      l.change((c) => {
        if (a) {
          const u = a.marker.name.split(":")[1], d = l.markers.get(`findResultHighlighted:${u}`);
          d && c.removeMarker(d);
        }
        if (o) {
          const u = o.marker.name.split(":")[1];
          c.addMarker(`findResultHighlighted:${u}`, {
            usingOperation: !1,
            affectsData: !1,
            range: o.marker.getRange()
          });
        }
      });
    });
    /* istanbul ignore next -- @preserve */
    const i = vi(((s, r, o) => {
      if (o) {
        const a = this.editor.editing.view.domConverter, l = this.editor.editing.mapper.toViewRange(o.marker.getRange());
        f_({
          target: a.viewRangeToDom(l),
          viewportOffset: 40
        });
      }
    }).bind(this), 32);
    this.listenTo(this.state, "change:highlightedResult", i, {
      priority: "low"
    }), this.listenTo(this.editor, "destroy", i.cancel), this.on("change:_isSearchActive", (s, r, o) => {
      o ? this.listenTo(this.editor.model.document, "change:data", this._onDocumentChange) : this.stopListening(this.editor.model.document, "change:data", this._onDocumentChange);
    });
  }
  /**
  * Initiate a search.
  */
  find(e, i) {
    return this._isSearchActive = !0, this.editor.execute("find", e, i), this.state.results;
  }
  /**
  * Stops active results from updating, and clears out the results.
  */
  stop() {
    this.state.clear(this.editor.model), this._isSearchActive = !1;
  }
  /**
  * Sets up the commands.
  */
  _defineCommands() {
    this.editor.commands.add("find", new rF(this.editor, this.state)), this.editor.commands.add("findNext", new _k(this.editor, this.state)), this.editor.commands.add("findPrevious", new cF(this.editor, this.state)), this.editor.commands.add("replace", new aF(this.editor, this.state)), this.editor.commands.add("replaceAll", new lF(this.editor, this.state));
  }
  /**
  * Sets up the marker downcast converters for search results highlighting.
  */
  _defineConverters() {
    const { editor: e } = this;
    e.conversion.for("editingDowncast").markerToHighlight({
      model: "findResult",
      view: ({ markerName: i }) => {
        const [, s] = i.split(":");
        return {
          name: "span",
          classes: [
            "ck-find-result"
          ],
          attributes: {
            // ...however, adding a unique attribute should be future-proof..
            "data-find-result": s
          }
        };
      }
    }), e.conversion.for("editingDowncast").markerToHighlight({
      model: "findResultHighlighted",
      view: ({ markerName: i }) => {
        const [, s] = i.split(":");
        return {
          name: "span",
          classes: [
            fF
          ],
          attributes: {
            // ...however, adding a unique attribute should be future-proof..
            "data-find-result": s
          }
        };
      }
    });
  }
}
class gF extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      mF,
      sF
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FindAndReplace";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins.get("FindAndReplaceUI"), e = this.editor.plugins.get("FindAndReplaceEditing"), i = e.state;
    t.on("findNext", (s, r) => {
      r ? (i.searchText = r.searchText, e.find(r.searchText, r)) : this.editor.execute("findNext");
    }), t.on("findPrevious", (s, r) => {
      r && i.searchText !== r.searchText ? e.find(r.searchText) : this.editor.execute("findPrevious");
    }), t.on("replace", (s, r) => {
      i.searchText !== r.searchText && e.find(r.searchText);
      const o = i.highlightedResult;
      o && this.editor.execute("replace", r.replaceText, o);
    }), t.on("replaceAll", (s, r) => {
      i.searchText !== r.searchText && e.find(r.searchText), this.editor.execute("replaceAll", r.replaceText, i.results);
    }), t.on("searchReseted", () => {
      i.clear(this.editor.model), e.stop();
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class tu extends he {
  /**
  * Creates an instance of the command.
  *
  * @param editor Editor instance.
  * @param attributeKey The name of a model attribute on which this command operates.
  */
  constructor(e, i) {
    super(e);
    /**
    * A model attribute on which this command operates.
    */
    f(this, "attributeKey");
    this.attributeKey = i;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model, i = e.document;
    this.value = i.selection.getAttribute(this.attributeKey), this.isEnabled = e.schema.checkAttributeInSelection(i.selection, this.attributeKey);
  }
  /**
  * Executes the command. Applies the `value` of the {@link #attributeKey} to the selection.
  * If no `value` is passed, it removes the attribute from the selection.
  *
  * @param options Options for the executed command.
  * @param options.value The value to apply.
  * @fires execute
  */
  execute(e = {}) {
    const i = this.editor.model, r = i.document.selection, o = e.value, a = e.batch, l = (c) => {
      if (r.isCollapsed)
        o ? c.setSelectionAttribute(this.attributeKey, o) : c.removeSelectionAttribute(this.attributeKey);
      else {
        const u = i.schema.getValidRanges(r.getRanges(), this.attributeKey);
        for (const d of u)
          o ? c.setAttribute(this.attributeKey, o, d) : c.removeAttribute(this.attributeKey, d);
      }
    };
    a ? i.enqueueChange(a, (c) => {
      l(c);
    }) : i.change((c) => {
      l(c);
    });
  }
}
const di = "fontSize", hi = "fontFamily", hn = "fontColor", Nn = "fontBackgroundColor";
function yk(n, t) {
  const e = {
    model: {
      key: n,
      values: []
    },
    view: {},
    upcastAlso: {}
  };
  for (const i of t)
    e.model.values.push(i.model), e.view[i.model] = i.view, i.upcastAlso && (e.upcastAlso[i.model] = i.upcastAlso);
  return e;
}
function kk(n) {
  return (t) => bF(t.getStyle(n));
}
function Ck(n) {
  return (t, { writer: e }) => e.createAttributeElement("span", {
    style: `${n}:${t}`
  }, {
    priority: 7
  });
}
function pF({ dropdownView: n, colors: t, columns: e, removeButtonLabel: i, colorPickerLabel: s, documentColorsLabel: r, documentColorsCount: o, colorPickerViewConfig: a }) {
  const l = n.locale, c = new Uf(l, {
    colors: t,
    columns: e,
    removeButtonLabel: i,
    colorPickerLabel: s,
    documentColorsLabel: r,
    documentColorsCount: o,
    colorPickerViewConfig: a
  });
  return n.colorSelectorView = c, n.panelView.children.add(c), c;
}
function bF(n) {
  return n.replace(/\s/g, "");
}
class wF extends tu {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t, hi);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function xk(n) {
  return n.map(vF).filter((t) => t !== void 0);
}
function vF(n) {
  if (typeof n == "object")
    return n;
  if (n === "default")
    return {
      title: "Default",
      model: void 0
    };
  if (typeof n == "string")
    return _F(n);
}
function _F(n) {
  const t = n.replace(/"|'/g, "").split(","), e = t[0], i = t.map(yF).join(", ");
  return {
    title: e,
    model: i,
    view: {
      name: "span",
      styles: {
        "font-family": i
      },
      priority: 7
    }
  };
}
function yF(n) {
  return n = n.trim(), n.indexOf(" ") > 0 && (n = `'${n}'`), n;
}
class kF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontFamilyEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define(hi, {
      options: [
        "default",
        "Arial, Helvetica, sans-serif",
        "Courier New, Courier, monospace",
        "Georgia, serif",
        "Lucida Sans Unicode, Lucida Grande, sans-serif",
        "Tahoma, Geneva, sans-serif",
        "Times New Roman, Times, serif",
        "Trebuchet MS, Helvetica, sans-serif",
        "Verdana, Geneva, sans-serif"
      ],
      supportAllValues: !1
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.extend("$text", {
      allowAttributes: hi
    }), t.model.schema.setAttributeProperties(hi, {
      isFormatting: !0,
      copyOnEnter: !0
    });
    const e = xk(t.config.get("fontFamily.options")).filter((s) => s.model), i = yk(hi, e);
    t.config.get("fontFamily.supportAllValues") ? (this._prepareAnyValueConverters(), this._prepareCompatibilityConverter()) : t.conversion.attributeToElement(i), t.commands.add(hi, new wF(t));
  }
  /**
  * These converters enable keeping any value found as `style="font-family: *"` as a value of an attribute on a text even
  * if it is not defined in the plugin configuration.
  */
  _prepareAnyValueConverters() {
    const t = this.editor;
    t.conversion.for("downcast").attributeToElement({
      model: hi,
      view: (e, { writer: i }) => i.createAttributeElement("span", {
        style: "font-family:" + e
      }, {
        priority: 7
      })
    }), t.conversion.for("upcast").elementToAttribute({
      model: {
        key: hi,
        value: (e) => e.getStyle("font-family")
      },
      view: {
        name: "span",
        styles: {
          "font-family": /.*/
        }
      }
    });
  }
  /**
  * Adds support for legacy `<font face="..">` formatting.
  */
  _prepareCompatibilityConverter() {
    this.editor.conversion.for("upcast").elementToAttribute({
      view: {
        name: "font",
        attributes: {
          face: /.*/
        }
      },
      model: {
        key: hi,
        value: (e) => e.getAttribute("face")
      }
    });
  }
}
var p1 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M11.03 3h6.149a.75.75 0 1 1 0 1.5h-5.514L11.03 3zm1.27 3h4.879a.75.75 0 1 1 0 1.5h-4.244L12.3 6zm1.27 3h3.609a.75.75 0 1 1 0 1.5h-2.973L13.57 9zm-2.754 2.5L8.038 4.785 5.261 11.5h5.555zm.62 1.5H4.641l-1.666 4.028H1.312l5.789-14h1.875l5.789 14h-1.663L11.436 13z"/></svg>';
class CF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontFamilyUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t, i = this._getLocalizedOptions(), s = t.commands.get(hi), r = e("Font Family"), o = xF(i, s);
    t.ui.componentFactory.add(hi, (a) => {
      const l = Tt(a);
      return En(l, o, {
        role: "menu",
        ariaLabel: r
      }), l.buttonView.set({
        label: r,
        icon: p1,
        tooltip: !0
      }), l.extendTemplate({
        attributes: {
          class: "ck-font-family-dropdown"
        }
      }), l.bind("isEnabled").to(s), this.listenTo(l, "execute", (c) => {
        t.execute(c.source.commandName, {
          value: c.source.commandParam
        }), t.editing.view.focus();
      }), l;
    }), t.ui.componentFactory.add(`menuBar:${hi}`, (a) => {
      const l = new Yt(a);
      l.buttonView.set({
        label: r,
        icon: p1
      }), l.bind("isEnabled").to(s);
      const c = new kn(a);
      for (const u of o) {
        const d = new en(a, l), h = new Fe(a);
        h.set({
          role: "menuitemradio",
          isToggleable: !0
        }), h.bind(...Object.keys(u.model)).to(u.model), h.delegate("execute").to(l), h.on("execute", () => {
          t.execute(u.model.commandName, {
            value: u.model.commandParam
          }), t.editing.view.focus();
        }), d.children.add(h), c.items.add(d);
      }
      return l.panelView.children.add(c), l;
    });
  }
  /**
  * Returns options as defined in `config.fontFamily.options` but processed to account for
  * editor localization, i.e. to display {@link module:font/fontconfig~FontFamilyOption}
  * in the correct language.
  *
  * Note: The reason behind this method is that there is no way to use {@link module:utils/locale~Locale#t}
  * when the user configuration is defined because the editor does not exist yet.
  */
  _getLocalizedOptions() {
    const t = this.editor, e = t.t;
    return xk(t.config.get(hi).options).map((s) => (s.title === "Default" && (s.title = e("Default")), s));
  }
}
function xF(n, t) {
  const e = new bt();
  for (const i of n) {
    const s = {
      type: "button",
      model: new tn({
        commandName: hi,
        commandParam: i.model,
        label: i.title,
        role: "menuitemradio",
        withText: !0
      })
    };
    s.model.bind("isOn").to(t, "value", (r) => r === i.model ? !0 : !r || !i.model ? !1 : r.split(",")[0].replace(/'/g, "").toLowerCase() === i.model.toLowerCase()), i.view && typeof i.view != "string" && i.view.styles && s.model.set("labelStyle", `font-family: ${i.view.styles["font-family"]}`), e.add(s);
  }
  return e;
}
class TF extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      kF,
      CF
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontFamily";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class EF extends tu {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t, di);
  }
}
function em(n) {
  return n.map((t) => SF(t)).filter((t) => t !== void 0);
}
const b1 = {
  get tiny() {
    return {
      title: "Tiny",
      model: "tiny",
      view: {
        name: "span",
        classes: "text-tiny",
        priority: 7
      }
    };
  },
  get small() {
    return {
      title: "Small",
      model: "small",
      view: {
        name: "span",
        classes: "text-small",
        priority: 7
      }
    };
  },
  get big() {
    return {
      title: "Big",
      model: "big",
      view: {
        name: "span",
        classes: "text-big",
        priority: 7
      }
    };
  },
  get huge() {
    return {
      title: "Huge",
      model: "huge",
      view: {
        name: "span",
        classes: "text-huge",
        priority: 7
      }
    };
  }
};
function SF(n) {
  if (typeof n == "number" && (n = String(n)), typeof n == "object" && PF(n))
    return Th(n);
  const t = IF(n);
  if (t)
    return Th(t);
  if (n === "default")
    return {
      model: void 0,
      title: "Default"
    };
  if (!RF(n))
    return AF(n);
}
function AF(n) {
  return typeof n == "string" && (n = {
    title: n,
    model: `${parseFloat(n)}px`
  }), n.view = {
    name: "span",
    styles: {
      "font-size": n.model
    }
  }, Th(n);
}
function Th(n) {
  return n.view && typeof n.view != "string" && !n.view.priority && (n.view.priority = 7), n;
}
function IF(n) {
  return typeof n == "string" ? b1[n] : b1[n.model];
}
function PF(n) {
  return n.title && n.model && n.view;
}
function RF(n) {
  let t;
  if (typeof n == "object")
    if (n.model)
      t = parseFloat(n.model);
    else
      throw new S("font-size-invalid-definition", null, n);
  else
    t = parseFloat(n);
  return isNaN(t);
}
const w1 = [
  "x-small",
  "x-small",
  "small",
  "medium",
  "large",
  "x-large",
  "xx-large",
  "xxx-large"
];
class OF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontSizeEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define(di, {
      options: [
        "tiny",
        "small",
        "default",
        "big",
        "huge"
      ],
      supportAllValues: !1
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.extend("$text", {
      allowAttributes: di
    }), t.model.schema.setAttributeProperties(di, {
      isFormatting: !0,
      copyOnEnter: !0
    });
    const e = t.config.get("fontSize.supportAllValues"), i = em(this.editor.config.get("fontSize.options")).filter((r) => r.model), s = yk(di, i);
    e ? (this._prepareAnyValueConverters(s), this._prepareCompatibilityConverter()) : t.conversion.attributeToElement(s), t.commands.add(di, new EF(t));
  }
  /**
  * These converters enable keeping any value found as `style="font-size: *"` as a value of an attribute on a text even
  * if it is not defined in the plugin configuration.
  *
  * @param definition Converter definition out of input data.
  */
  _prepareAnyValueConverters(t) {
    const e = this.editor, i = t.model.values.filter((s) => !Jc(String(s)) && !ly(String(s)));
    if (i.length)
      throw new S("font-size-invalid-use-of-named-presets", null, {
        presets: i
      });
    e.conversion.for("downcast").attributeToElement({
      model: di,
      view: (s, { writer: r }) => {
        if (s)
          return r.createAttributeElement("span", {
            style: "font-size:" + s
          }, {
            priority: 7
          });
      }
    }), e.conversion.for("upcast").elementToAttribute({
      model: {
        key: di,
        value: (s) => s.getStyle("font-size")
      },
      view: {
        name: "span",
        styles: {
          "font-size": /.*/
        }
      }
    });
  }
  /**
  * Adds support for legacy `<font size="..">` formatting.
  */
  _prepareCompatibilityConverter() {
    this.editor.conversion.for("upcast").elementToAttribute({
      view: {
        name: "font",
        attributes: {
          // Documentation mentions sizes from 1 to 7. To handle old content we support all values
          // up to 999 but clamp it to the valid range. Why 999? It should cover accidental values
          // similar to percentage, e.g. 100%, 200% which could be the usual mistake for font size.
          size: /^[+-]?\d{1,3}$/
        }
      },
      model: {
        key: di,
        value: (e) => {
          const i = e.getAttribute("size"), s = i[0] === "-" || i[0] === "+";
          let r = parseInt(i, 10);
          s && (r = 3 + r);
          const o = w1.length - 1, a = Math.min(Math.max(r, 0), o);
          return w1[a];
        }
      }
    });
  }
}
var v1 = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M9.816 11.5 7.038 4.785 4.261 11.5h5.555zm.62 1.5H3.641l-1.666 4.028H.312l5.789-14h1.875l5.789 14h-1.663L10.436 13zm7.55 2.279.779-.779.707.707-2.265 2.265-2.193-2.265.707-.707.765.765V4.825c0-.042 0-.083.002-.123l-.77.77-.707-.707L17.207 2.5l2.265 2.265-.707.707-.782-.782c.002.043.003.089.003.135v10.454z"/></svg>';
class VF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontSizeUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t, i = this._getLocalizedOptions(), s = t.commands.get(di), r = e("Font Size"), o = LF(i, s);
    t.ui.componentFactory.add(di, (a) => {
      const l = Tt(a);
      return En(l, o, {
        role: "menu",
        ariaLabel: r
      }), l.buttonView.set({
        label: r,
        icon: v1,
        tooltip: !0
      }), l.extendTemplate({
        attributes: {
          class: [
            "ck-font-size-dropdown"
          ]
        }
      }), l.bind("isEnabled").to(s), this.listenTo(l, "execute", (c) => {
        t.execute(c.source.commandName, {
          value: c.source.commandParam
        }), t.editing.view.focus();
      }), l;
    }), t.ui.componentFactory.add(`menuBar:${di}`, (a) => {
      const l = new Yt(a);
      l.buttonView.set({
        label: r,
        icon: v1
      }), l.bind("isEnabled").to(s);
      const c = new kn(a);
      for (const u of o) {
        const d = new en(a, l), h = new Fe(a);
        h.set({
          role: "menuitemradio",
          isToggleable: !0
        }), h.bind(...Object.keys(u.model)).to(u.model), h.delegate("execute").to(l), h.on("execute", () => {
          t.execute(u.model.commandName, {
            value: u.model.commandParam
          }), t.editing.view.focus();
        }), d.children.add(h), c.items.add(d);
      }
      return l.panelView.children.add(c), l;
    });
  }
  /**
  * Returns options as defined in `config.fontSize.options` but processed to account for
  * editor localization, i.e. to display {@link module:font/fontconfig~FontSizeOption}
  * in the correct language.
  *
  * Note: The reason behind this method is that there is no way to use {@link module:utils/locale~Locale#t}
  * when the user configuration is defined because the editor does not exist yet.
  */
  _getLocalizedOptions() {
    const t = this.editor, e = t.t, i = {
      Default: e("Default"),
      Tiny: e("Tiny"),
      Small: e("Small"),
      Big: e("Big"),
      Huge: e("Huge")
    };
    return em(t.config.get(di).options).map((r) => {
      const o = i[r.title];
      return o && o != r.title && (r = Object.assign({}, r, {
        title: o
      })), r;
    });
  }
}
function LF(n, t) {
  const e = new bt();
  for (const i of n) {
    const s = {
      type: "button",
      model: new tn({
        commandName: di,
        commandParam: i.model,
        label: i.title,
        class: "ck-fontsize-option",
        role: "menuitemradio",
        withText: !0
      })
    };
    i.view && typeof i.view != "string" && (i.view.styles && s.model.set("labelStyle", `font-size:${i.view.styles["font-size"]}`), i.view.classes && s.model.set("class", `${s.model.class} ${i.view.classes}`)), s.model.bind("isOn").to(t, "value", (r) => r === i.model), e.add(s);
  }
  return e;
}
class BF extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      OF,
      VF
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontSize";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Normalizes and translates the {@link module:font/fontconfig~FontSizeConfig#options configuration options}
  * to the {@link module:font/fontconfig~FontSizeOption} format.
  *
  * @param configuredOptions An array of options taken from the configuration.
  */
  normalizeSizeOptions(t) {
    return em(t);
  }
}
class MF extends tu {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t, hn);
  }
}
class NF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontColorEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define(hn, {
      colors: [
        {
          color: "hsl(0, 0%, 0%)",
          label: "Black"
        },
        {
          color: "hsl(0, 0%, 30%)",
          label: "Dim grey"
        },
        {
          color: "hsl(0, 0%, 60%)",
          label: "Grey"
        },
        {
          color: "hsl(0, 0%, 90%)",
          label: "Light grey"
        },
        {
          color: "hsl(0, 0%, 100%)",
          label: "White",
          hasBorder: !0
        },
        {
          color: "hsl(0, 75%, 60%)",
          label: "Red"
        },
        {
          color: "hsl(30, 75%, 60%)",
          label: "Orange"
        },
        {
          color: "hsl(60, 75%, 60%)",
          label: "Yellow"
        },
        {
          color: "hsl(90, 75%, 60%)",
          label: "Light green"
        },
        {
          color: "hsl(120, 75%, 60%)",
          label: "Green"
        },
        {
          color: "hsl(150, 75%, 60%)",
          label: "Aquamarine"
        },
        {
          color: "hsl(180, 75%, 60%)",
          label: "Turquoise"
        },
        {
          color: "hsl(210, 75%, 60%)",
          label: "Light blue"
        },
        {
          color: "hsl(240, 75%, 60%)",
          label: "Blue"
        },
        {
          color: "hsl(270, 75%, 60%)",
          label: "Purple"
        }
      ],
      columns: 5
    }), t.conversion.for("upcast").elementToAttribute({
      view: {
        name: "span",
        styles: {
          color: /[\s\S]+/
        }
      },
      model: {
        key: hn,
        value: kk("color")
      }
    }), t.conversion.for("upcast").elementToAttribute({
      view: {
        name: "font",
        attributes: {
          color: /^#?\w+$/
        }
      },
      model: {
        key: hn,
        value: (e) => e.getAttribute("color")
      }
    }), t.conversion.for("downcast").attributeToElement({
      model: hn,
      view: Ck("color")
    }), t.commands.add(hn, new MF(t)), t.model.schema.extend("$text", {
      allowAttributes: hn
    }), t.model.schema.setAttributeProperties(hn, {
      isFormatting: !0,
      copyOnEnter: !0
    });
  }
}
class Tk extends I {
  /**
  * Creates a plugin which introduces a dropdown with a pre–configured
  * {@link module:ui/colorselector/colorselectorview~ColorSelectorView}.
  *
  * @param config The configuration object.
  * @param config.commandName The name of the command which will be executed when a color tile is clicked.
  * @param config.componentName The name of the dropdown in the {@link module:ui/componentfactory~ComponentFactory}
  * and the configuration scope name in `editor.config`.
  * @param config.icon The SVG icon used by the dropdown.
  * @param config.dropdownLabel The label used by the dropdown.
  */
  constructor(e, { commandName: i, componentName: s, icon: r, dropdownLabel: o }) {
    super(e);
    /**
    * The name of the command which will be executed when a color tile is clicked.
    */
    f(this, "commandName");
    /**
    * The name of this component in the {@link module:ui/componentfactory~ComponentFactory}.
    * Also the configuration scope name in `editor.config`.
    */
    f(this, "componentName");
    /**
    * The SVG icon used by the dropdown.
    */
    f(this, "icon");
    /**
    * The label used by the dropdown.
    */
    f(this, "dropdownLabel");
    /**
    * The number of columns in the color grid.
    */
    f(this, "columns");
    this.commandName = i, this.componentName = s, this.icon = r, this.dropdownLabel = o, this.columns = e.config.get(`${this.componentName}.columns`);
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.locale, s = i.t, r = e.commands.get(this.commandName), o = e.config.get(this.componentName), a = ma(o.colors), l = fa(i, a), c = o.documentColors, u = o.colorPicker !== !1;
    e.ui.componentFactory.add(this.componentName, (d) => {
      const h = Tt(d);
      let m = !1;
      const g = pF({
        dropdownView: h,
        colors: l.map((p) => ({
          label: p.label,
          color: p.model,
          options: {
            hasBorder: p.hasBorder
          }
        })),
        columns: this.columns,
        removeButtonLabel: s("Remove color"),
        colorPickerLabel: s("Color picker"),
        documentColorsLabel: c !== 0 ? s("Document colors") : "",
        documentColorsCount: c === void 0 ? this.columns : c,
        colorPickerViewConfig: u ? o.colorPicker || {} : !1
      });
      return g.bind("selectedColor").to(r, "value"), h.buttonView.set({
        label: this.dropdownLabel,
        icon: this.icon,
        tooltip: !0
      }), h.extendTemplate({
        attributes: {
          class: "ck-color-ui-dropdown"
        }
      }), h.bind("isEnabled").to(r), g.on("execute", (p, w) => {
        h.isOpen && e.execute(this.commandName, {
          value: w.value,
          batch: this._undoStepBatch
        }), w.source !== "colorPicker" && e.editing.view.focus(), w.source === "colorPickerSaveButton" && (h.isOpen = !1);
      }), g.on("colorPicker:show", () => {
        this._undoStepBatch = e.model.createBatch();
      }), g.on("colorPicker:cancel", () => {
        this._undoStepBatch.operations.length && (h.isOpen = !1, e.execute("undo", this._undoStepBatch)), e.editing.view.focus();
      }), h.on("change:isOpen", (p, w, y) => {
        m || (m = !0, h.colorSelectorView.appendUI()), y && (c !== 0 && g.updateDocumentColors(e.model, this.componentName), g.updateSelectedColors(), g.showColorGridsFragment());
      }), eu(h, () => h.colorSelectorView.colorGridsFragmentView.staticColorsGrid.items.find((p) => p.isOn)), h;
    }), e.ui.componentFactory.add(`menuBar:${this.componentName}`, (d) => {
      const h = new Yt(d);
      h.buttonView.set({
        label: this.dropdownLabel,
        icon: this.icon
      }), h.bind("isEnabled").to(r);
      let m = !1;
      const g = new Uf(d, {
        colors: l.map((p) => ({
          label: p.label,
          color: p.model,
          options: {
            hasBorder: p.hasBorder
          }
        })),
        columns: this.columns,
        removeButtonLabel: s("Remove color"),
        colorPickerLabel: s("Color picker"),
        documentColorsLabel: c !== 0 ? s("Document colors") : "",
        documentColorsCount: c === void 0 ? this.columns : c,
        colorPickerViewConfig: !1
      });
      return g.bind("selectedColor").to(r, "value"), g.delegate("execute").to(h), g.on("execute", (p, w) => {
        e.execute(this.commandName, {
          value: w.value,
          batch: this._undoStepBatch
        }), e.editing.view.focus();
      }), h.on("change:isOpen", (p, w, y) => {
        m || (m = !0, g.appendUI()), y && (c !== 0 && g.updateDocumentColors(e.model, this.componentName), g.updateSelectedColors(), g.showColorGridsFragment());
      }), h.panelView.children.add(g), h;
    });
  }
}
var FF = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M12.4 10.3 10 4.5l-2.4 5.8h4.8zm.5 1.2H7.1L5.7 15H4.2l5-12h1.6l5 12h-1.5L13 11.5zm3.1 7H4a1 1 0 0 1 0-2h12a1 1 0 0 1 0 2z"/></svg>';
class DF extends Tk {
  /**
  * @inheritDoc
  */
  constructor(t) {
    const e = t.locale.t;
    super(t, {
      commandName: hn,
      componentName: hn,
      icon: FF,
      dropdownLabel: e("Font Color")
    });
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontColorUI";
  }
}
class $F extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      NF,
      DF
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontColor";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class zF extends tu {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t, Nn);
  }
}
class HF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontBackgroundColorEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define(Nn, {
      colors: [
        {
          color: "hsl(0, 0%, 0%)",
          label: "Black"
        },
        {
          color: "hsl(0, 0%, 30%)",
          label: "Dim grey"
        },
        {
          color: "hsl(0, 0%, 60%)",
          label: "Grey"
        },
        {
          color: "hsl(0, 0%, 90%)",
          label: "Light grey"
        },
        {
          color: "hsl(0, 0%, 100%)",
          label: "White",
          hasBorder: !0
        },
        {
          color: "hsl(0, 75%, 60%)",
          label: "Red"
        },
        {
          color: "hsl(30, 75%, 60%)",
          label: "Orange"
        },
        {
          color: "hsl(60, 75%, 60%)",
          label: "Yellow"
        },
        {
          color: "hsl(90, 75%, 60%)",
          label: "Light green"
        },
        {
          color: "hsl(120, 75%, 60%)",
          label: "Green"
        },
        {
          color: "hsl(150, 75%, 60%)",
          label: "Aquamarine"
        },
        {
          color: "hsl(180, 75%, 60%)",
          label: "Turquoise"
        },
        {
          color: "hsl(210, 75%, 60%)",
          label: "Light blue"
        },
        {
          color: "hsl(240, 75%, 60%)",
          label: "Blue"
        },
        {
          color: "hsl(270, 75%, 60%)",
          label: "Purple"
        }
      ],
      columns: 5
    }), t.data.addStyleProcessorRules(Mf), t.conversion.for("upcast").elementToAttribute({
      view: {
        name: "span",
        styles: {
          "background-color": /[\s\S]+/
        }
      },
      model: {
        key: Nn,
        value: kk("background-color")
      }
    }), t.conversion.for("downcast").attributeToElement({
      model: Nn,
      view: Ck("background-color")
    }), t.commands.add(Nn, new zF(t)), t.model.schema.extend("$text", {
      allowAttributes: Nn
    }), t.model.schema.setAttributeProperties(Nn, {
      isFormatting: !0,
      copyOnEnter: !0
    });
  }
}
var jF = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M4 2h12a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2zm8.38 9.262H7.62L10 5.506l2.38 5.756zm.532 1.285L14.34 16h1.426L10.804 4H9.196L4.234 16H5.66l1.428-3.453h5.824z"/></svg>';
class UF extends Tk {
  /**
  * @inheritDoc
  */
  constructor(t) {
    const e = t.locale.t;
    super(t, {
      commandName: Nn,
      componentName: Nn,
      icon: jF,
      dropdownLabel: e("Font Background Color")
    });
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontBackgroundColorUI";
  }
}
class WF extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      HF,
      UF
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FontBackgroundColor";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class qF extends he {
  constructor(t) {
    super(t), this._isEnabledBasedOnSelection = !1;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document, i = Xe(e.selection.getSelectedBlocks());
    this.value = !!i && i.is("element", "paragraph"), this.isEnabled = !!i && _1(i, t.schema);
  }
  /**
  * Executes the command. All the blocks (see {@link module:engine/model/schema~Schema}) in the selection
  * will be turned to paragraphs.
  *
  * @fires execute
  * @param options Options for the executed command.
  * @param options.selection The selection that the command should be applied to. By default,
  * if not provided, the command is applied to the {@link module:engine/model/document~Document#selection}.
  */
  execute(t = {}) {
    const e = this.editor.model, i = e.document, s = t.selection || i.selection;
    e.canEditAt(s) && e.change((r) => {
      const o = s.getSelectedBlocks();
      for (const a of o)
        !a.is("element", "paragraph") && _1(a, e.schema) && r.rename(a, "paragraph");
    });
  }
}
function _1(n, t) {
  return t.checkChild(n.parent, "paragraph") && !t.isObject(n);
}
class GF extends he {
  constructor(t) {
    super(t), this._isEnabledBasedOnSelection = !1;
  }
  /**
  * Executes the command.
  *
  * @param options Options for the executed command.
  * @param options.position The model position at which the new paragraph will be inserted.
  * @param options.attributes Attributes keys and values to set on a inserted paragraph.
  * @fires execute
  */
  execute(t) {
    const e = this.editor.model, i = t.attributes;
    let s = t.position;
    return e.canEditAt(s) ? e.change((r) => {
      if (s = this._findPositionToInsertParagraph(s, r), !s)
        return null;
      const o = r.createElement("paragraph");
      return i && e.schema.setAllowedAttributes(o, i, r), e.insertContent(o, s), r.setSelection(o, "in"), r.createPositionAt(o, 0);
    }) : null;
  }
  /**
  * Returns the best position to insert a new paragraph.
  */
  _findPositionToInsertParagraph(t, e) {
    const i = this.editor.model;
    if (i.schema.checkChild(t, "paragraph"))
      return t;
    const s = i.schema.findAllowedParent(t, "paragraph");
    if (!s)
      return null;
    const r = t.parent, o = i.schema.checkChild(r, "$text");
    return r.isEmpty || o && t.isAtEnd ? i.createPositionAfter(r) : !r.isEmpty && o && t.isAtStart ? i.createPositionBefore(r) : e.split(t, s).position;
  }
}
const Ac = class Ac extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Paragraph";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model;
    t.commands.add("paragraph", new qF(t)), t.commands.add("insertParagraph", new GF(t)), e.schema.register("paragraph", {
      inheritAllFrom: "$block"
    }), t.conversion.elementToElement({
      model: "paragraph",
      view: "p"
    }), t.conversion.for("upcast").elementToElement({
      model: (i, { writer: s }) => !Ac.paragraphLikeElements.has(i.name) || i.isEmpty ? null : s.createElement("paragraph"),
      view: /.+/,
      converterPriority: "low"
    });
  }
};
/**
* A list of element names which should be treated by the autoparagraphing algorithms as
* paragraph-like. This means that e.g. the following content:
*
* ```html
* <h1>Foo</h1>
* <table>
*   <tr>
*     <td>X</td>
*     <td>
*       <ul>
*         <li>Y</li>
*         <li>Z</li>
*       </ul>
*     </td>
*   </tr>
* </table>
* ```
*
* contains five paragraph-like elements: `<h1>`, two `<td>`s and two `<li>`s.
* Hence, if none of the features is going to convert those elements the above content will be automatically handled
* by the paragraph feature and converted to:
*
* ```html
* <p>Foo</p>
* <p>X</p>
* <p>Y</p>
* <p>Z</p>
* ```
*
* Note: The `<td>` containing two `<li>` elements was ignored as the innermost paragraph-like elements
* have a priority upon conversion.
*/
f(Ac, "paragraphLikeElements", /* @__PURE__ */ new Set([
  "blockquote",
  "dd",
  "div",
  "dt",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "li",
  "p",
  "td",
  "th"
]));
let cc = Ac;
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class KF extends he {
  /**
  * Creates an instance of the command.
  *
  * @param editor Editor instance.
  * @param modelElements Names of the element which this command can apply in the model.
  */
  constructor(e, i) {
    super(e);
    /**
    * Set of defined model's elements names that this command support.
    * See {@link module:heading/headingconfig~HeadingOption}.
    */
    f(this, "modelElements");
    this.modelElements = i;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = Xe(this.editor.model.document.selection.getSelectedBlocks());
    this.value = !!e && this.modelElements.includes(e.name) && e.name, this.isEnabled = !!e && this.modelElements.some((i) => y1(e, i, this.editor.model.schema));
  }
  /**
  * Executes the command. Applies the heading to the selected blocks or, if the first selected
  * block is a heading already, turns selected headings (of this level only) to paragraphs.
  *
  * @param options.value Name of the element which this command will apply in the model.
  * @fires execute
  */
  execute(e) {
    const i = this.editor.model, s = i.document, r = e.value;
    i.change((o) => {
      const a = Array.from(s.selection.getSelectedBlocks()).filter((l) => y1(l, r, i.schema));
      for (const l of a)
        l.is("element", r) || o.rename(l, r);
    });
  }
}
function y1(n, t, e) {
  return e.checkChild(n.parent, t) && !e.isObject(n);
}
const k1 = "paragraph";
class ZF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HeadingEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("heading", {
      options: [
        {
          model: "paragraph",
          title: "Paragraph",
          class: "ck-heading_paragraph"
        },
        {
          model: "heading1",
          view: "h2",
          title: "Heading 1",
          class: "ck-heading_heading1"
        },
        {
          model: "heading2",
          view: "h3",
          title: "Heading 2",
          class: "ck-heading_heading2"
        },
        {
          model: "heading3",
          view: "h4",
          title: "Heading 3",
          class: "ck-heading_heading3"
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      cc
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.config.get("heading.options"), i = [];
    for (const s of e)
      s.model !== "paragraph" && (t.model.schema.register(s.model, {
        inheritAllFrom: "$block"
      }), t.conversion.elementToElement(s), i.push(s.model));
    this._addDefaultH1Conversion(t), t.commands.add("heading", new KF(t, i));
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.commands.get("enter"), i = t.config.get("heading.options");
    e && this.listenTo(e, "afterExecute", (s, r) => {
      const o = t.model.document.selection.getFirstPosition().parent;
      i.some((l) => o.is("element", l.model)) && !o.is("element", k1) && o.childCount === 0 && r.writer.rename(o, k1);
    });
  }
  /**
  * Adds default conversion for `h1` -> `heading1` with a low priority.
  *
  * @param editor Editor instance on which to add the `h1` conversion.
  */
  _addDefaultH1Conversion(t) {
    t.conversion.for("upcast").elementToElement({
      model: "heading1",
      view: "h1",
      // With a `low` priority, `paragraph` plugin autoparagraphing mechanism is executed. Make sure
      // this listener is called before it. If not, `h1` will be transformed into a paragraph.
      converterPriority: Si.low + 1
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function YF(n) {
  const t = n.t, e = {
    Paragraph: t("Paragraph"),
    "Heading 1": t("Heading 1"),
    "Heading 2": t("Heading 2"),
    "Heading 3": t("Heading 3"),
    "Heading 4": t("Heading 4"),
    "Heading 5": t("Heading 5"),
    "Heading 6": t("Heading 6")
  };
  return n.config.get("heading.options").map((i) => {
    const s = e[i.title];
    return s && s != i.title && (i.title = s), i;
  });
}
class JF extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HeadingUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t, i = YF(t), s = e("Choose heading"), r = e("Heading");
    t.ui.componentFactory.add("heading", (o) => {
      const a = {}, l = new bt(), c = t.commands.get("heading"), u = t.commands.get("paragraph"), d = [
        c
      ];
      for (const m of i) {
        const g = {
          type: "button",
          model: new tn({
            label: m.title,
            class: m.class,
            role: "menuitemradio",
            withText: !0
          })
        };
        m.model === "paragraph" ? (g.model.bind("isOn").to(u, "value"), g.model.set("commandName", "paragraph"), d.push(u)) : (g.model.bind("isOn").to(c, "value", (p) => p === m.model), g.model.set({
          commandName: "heading",
          commandValue: m.model
        })), l.add(g), a[m.model] = m.title;
      }
      const h = Tt(o);
      return En(h, l, {
        ariaLabel: r,
        role: "menu"
      }), h.buttonView.set({
        ariaLabel: r,
        ariaLabelledBy: void 0,
        isOn: !1,
        withText: !0,
        tooltip: r
      }), h.extendTemplate({
        attributes: {
          class: [
            "ck-heading-dropdown"
          ]
        }
      }), h.bind("isEnabled").toMany(d, "isEnabled", (...m) => m.some((g) => g)), h.buttonView.bind("label").to(c, "value", u, "value", (m, g) => {
        const p = g ? "paragraph" : m;
        return typeof p == "boolean" || !a[p] ? s : a[p];
      }), h.buttonView.bind("ariaLabel").to(c, "value", u, "value", (m, g) => {
        const p = g ? "paragraph" : m;
        return typeof p == "boolean" || !a[p] ? r : `${a[p]}, ${r}`;
      }), this.listenTo(h, "execute", (m) => {
        const { commandName: g, commandValue: p } = m.source;
        t.execute(g, p ? {
          value: p
        } : void 0), t.editing.view.focus();
      }), h;
    }), t.ui.componentFactory.add("menuBar:heading", (o) => {
      const a = new Yt(o), l = t.commands.get("heading"), c = t.commands.get("paragraph"), u = [
        l
      ], d = new kn(o);
      a.set({
        class: "ck-heading-dropdown"
      }), d.set({
        ariaLabel: e("Heading"),
        role: "menu"
      }), a.buttonView.set({
        label: e("Heading")
      }), a.panelView.children.add(d);
      for (const h of i) {
        const m = new en(o, a), g = new Fe(o);
        m.children.add(g), d.items.add(m), g.set({
          isToggleable: !0,
          label: h.title,
          role: "menuitemradio",
          class: h.class
        }), g.delegate("execute").to(a), g.on("execute", () => {
          const p = h.model === "paragraph" ? "paragraph" : "heading";
          t.execute(p, {
            value: h.model
          }), t.editing.view.focus();
        }), h.model === "paragraph" ? (g.bind("isOn").to(c, "value"), u.push(c)) : g.bind("isOn").to(l, "value", (p) => p === h.model);
      }
      return a.bind("isEnabled").toMany(u, "isEnabled", (...h) => h.some((m) => m)), a;
    });
  }
}
class XF extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ZF,
      JF
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Heading";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class QF extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document;
    this.value = e.selection.getAttribute("highlight"), this.isEnabled = t.schema.checkAttributeInSelection(e.selection, "highlight");
  }
  /**
  * Executes the command.
  *
  * @param options Options for the executed command.
  * @param options.value The value to apply.
  *
  * @fires execute
  */
  execute(t = {}) {
    const e = this.editor.model, s = e.document.selection, r = t.value;
    e.change((o) => {
      if (s.isCollapsed) {
        const a = s.getFirstPosition();
        if (s.hasAttribute("highlight")) {
          const l = (h) => h.item.hasAttribute("highlight") && h.item.getAttribute("highlight") === this.value, c = a.getLastMatchingPosition(l, {
            direction: "backward"
          }), u = a.getLastMatchingPosition(l), d = o.createRange(c, u);
          !r || this.value === r ? (a.isEqual(u) || o.removeAttribute("highlight", d), o.removeSelectionAttribute("highlight")) : (a.isEqual(u) || o.setAttribute("highlight", r, d), o.setSelectionAttribute("highlight", r));
        } else r && o.setSelectionAttribute("highlight", r);
      } else {
        const a = e.schema.getValidRanges(s.getRanges(), "highlight");
        for (const l of a)
          r ? o.setAttribute("highlight", r, l) : o.removeAttribute("highlight", l);
      }
    });
  }
}
class eD extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HighlightEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("highlight", {
      options: [
        {
          model: "yellowMarker",
          class: "marker-yellow",
          title: "Yellow marker",
          color: "var(--ck-highlight-marker-yellow)",
          type: "marker"
        },
        {
          model: "greenMarker",
          class: "marker-green",
          title: "Green marker",
          color: "var(--ck-highlight-marker-green)",
          type: "marker"
        },
        {
          model: "pinkMarker",
          class: "marker-pink",
          title: "Pink marker",
          color: "var(--ck-highlight-marker-pink)",
          type: "marker"
        },
        {
          model: "blueMarker",
          class: "marker-blue",
          title: "Blue marker",
          color: "var(--ck-highlight-marker-blue)",
          type: "marker"
        },
        {
          model: "redPen",
          class: "pen-red",
          title: "Red pen",
          color: "var(--ck-highlight-pen-red)",
          type: "pen"
        },
        {
          model: "greenPen",
          class: "pen-green",
          title: "Green pen",
          color: "var(--ck-highlight-pen-green)",
          type: "pen"
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.extend("$text", {
      allowAttributes: "highlight"
    });
    const e = t.config.get("highlight.options");
    t.conversion.attributeToElement(tD(e)), t.commands.add("highlight", new QF(t));
  }
}
function tD(n) {
  const t = {
    model: {
      key: "highlight",
      values: []
    },
    view: {}
  };
  for (const e of n)
    t.model.values.push(e.model), t.view[e.model] = {
      name: "mark",
      classes: e.class
    };
  return t;
}
var iD = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path class="ck-icon__fill" d="M10.798 1.59 3.002 12.875l1.895 1.852 2.521 1.402 6.997-12.194z"/><path d="m2.556 16.727.234-.348c-.297-.151-.462-.293-.498-.426-.036-.137.002-.416.115-.837.094-.25.15-.449.169-.595a4.495 4.495 0 0 0 0-.725c-.209-.621-.303-1.041-.284-1.26.02-.218.178-.506.475-.862l6.77-9.414c.539-.91 1.605-.85 3.199.18 1.594 1.032 2.188 1.928 1.784 2.686l-5.877 10.36c-.158.412-.333.673-.526.782-.193.108-.604.179-1.232.21-.362.131-.608.237-.738.318-.13.081-.305.238-.526.47-.293.265-.504.397-.632.397-.096 0-.27-.075-.524-.226l-.31.41-1.6-1.12zm-.279.415 1.575 1.103-.392.515H1.19l1.087-1.618zm8.1-13.656-4.953 6.9L8.75 12.57l4.247-7.574c.175-.25-.188-.647-1.092-1.192-.903-.546-1.412-.652-1.528-.32zM8.244 18.5 9.59 17h9.406v1.5H8.245z"/></svg>', nD = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path class="ck-icon__fill" d="M10.126 2.268 2.002 13.874l1.895 1.852 2.521 1.402L14.47 5.481l-1.543-2.568-2.801-.645z"/><path d="m4.5 18.088-2.645-1.852-.04-2.95-.006-.005.006-.008v-.025l.011.008L8.73 2.97c.165-.233.356-.417.567-.557l-1.212.308L4.604 7.9l-.83-.558 3.694-5.495 2.708-.69 1.65 1.145.046.018.85-1.216 2.16 1.512-.856 1.222c.828.967 1.144 2.141.432 3.158L7.55 17.286l.006.005-3.055.797H4.5zm-.634.166-1.976.516-.026-1.918 2.002 1.402zM9.968 3.817l-.006-.004-6.123 9.184 3.277 2.294 6.108-9.162.005.003c.317-.452-.16-1.332-1.064-1.966-.891-.624-1.865-.776-2.197-.349zM8.245 18.5 9.59 17h9.406v1.5H8.245z"/></svg>';
class sD extends I {
  /**
  * Returns the localized option titles provided by the plugin.
  *
  * The following localized titles corresponding with default
  * {@link module:highlight/highlightconfig~HighlightConfig#options} are available:
  *
  * * `'Yellow marker'`,
  * * `'Green marker'`,
  * * `'Pink marker'`,
  * * `'Blue marker'`,
  * * `'Red pen'`,
  * * `'Green pen'`.
  */
  get localizedOptionTitles() {
    const t = this.editor.t;
    return {
      "Yellow marker": t("Yellow marker"),
      "Green marker": t("Green marker"),
      "Pink marker": t("Pink marker"),
      "Blue marker": t("Blue marker"),
      "Red pen": t("Red pen"),
      "Green pen": t("Green pen")
    };
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HighlightUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.config.get("highlight.options");
    for (const e of t)
      this._addHighlighterButton(e);
    this._addRemoveHighlightButton(), this._addDropdown(t), this._addMenuBarButton(t);
  }
  /**
  * Creates the "Remove highlight" button.
  */
  _addRemoveHighlightButton() {
    const t = this.editor.t, e = this.editor.commands.get("highlight");
    this._addButton("removeHighlight", t("Remove highlight"), Z.eraser, null, (i) => {
      i.bind("isEnabled").to(e, "isEnabled");
    });
  }
  /**
  * Creates a toolbar button from the provided highlight option.
  */
  _addHighlighterButton(t) {
    const e = this.editor.commands.get("highlight");
    this._addButton("highlight:" + t.model, t.title, bl(t.type), t.model, i);
    function i(s) {
      s.bind("isEnabled").to(e, "isEnabled"), s.bind("isOn").to(e, "value", (r) => r === t.model), s.iconView.fillColor = t.color, s.isToggleable = !0;
    }
  }
  /**
  * Internal method for creating highlight buttons.
  *
  * @param name The name of the button.
  * @param label The label for the button.
  * @param icon The button icon.
  * @param value The `value` property passed to the executed command.
  * @param decorateButton A callback getting ButtonView instance so that it can be further customized.
  */
  _addButton(t, e, i, s, r) {
    const o = this.editor;
    o.ui.componentFactory.add(t, (a) => {
      const l = new le(a), c = this.localizedOptionTitles[e] ? this.localizedOptionTitles[e] : e;
      return l.set({
        label: c,
        icon: i,
        tooltip: !0
      }), l.on("execute", () => {
        o.execute("highlight", {
          value: s
        }), o.editing.view.focus();
      }), r(l), l;
    });
  }
  /**
  * Creates the split button dropdown UI from the provided highlight options.
  */
  _addDropdown(t) {
    const e = this.editor, i = e.t, s = e.ui.componentFactory, r = t[0], o = t.reduce((a, l) => (a[l.model] = l, a), {});
    s.add("highlight", (a) => {
      const l = e.commands.get("highlight"), c = Tt(a, Ys), u = c.buttonView;
      u.set({
        label: i("Highlight"),
        tooltip: !0,
        // Holds last executed highlighter.
        lastExecuted: r.model,
        // Holds current highlighter to execute (might be different then last used).
        commandValue: r.model,
        isToggleable: !0
      }), u.bind("icon").to(l, "value", (m) => bl(h(m, "type"))), u.bind("color").to(l, "value", (m) => h(m, "color")), u.bind("commandValue").to(l, "value", (m) => h(m, "model")), u.bind("isOn").to(l, "value", (m) => !!m), u.delegate("execute").to(c);
      const d = () => {
        const m = t.map((g) => {
          const p = s.create("highlight:" + g.model);
          return this.listenTo(p, "execute", () => {
            c.buttonView.set({
              lastExecuted: g.model
            });
          }), p;
        });
        return m.push(new zf()), m.push(s.create("removeHighlight")), m;
      };
      c.bind("isEnabled").to(l, "isEnabled"), Ma(c, d, {
        enableActiveItemFocusOnDropdownOpen: !0,
        ariaLabel: i("Text highlight toolbar")
      }), rD(c), u.on("execute", () => {
        e.execute("highlight", {
          value: u.commandValue
        });
      }), this.listenTo(c, "execute", () => {
        e.editing.view.focus();
      });
      function h(m, g) {
        const p = !m || m === u.lastExecuted ? u.lastExecuted : m;
        return o[p][g];
      }
      return c;
    });
  }
  /**
  * Creates the menu bar button for highlight including submenu with available options.
  */
  _addMenuBarButton(t) {
    const e = this.editor, i = e.t, s = e.commands.get("highlight");
    e.ui.componentFactory.add("menuBar:highlight", (r) => {
      const o = new Yt(r);
      o.buttonView.set({
        label: i("Highlight"),
        icon: bl("marker")
      }), o.bind("isEnabled").to(s), o.buttonView.iconView.fillColor = "transparent";
      const a = new kn(r);
      for (const u of t) {
        const d = new en(r, o), h = new Fe(r);
        h.set({
          label: u.title,
          icon: bl(u.type),
          role: "menuitemradio",
          isToggleable: !0
        }), h.iconView.fillColor = u.color, h.delegate("execute").to(o), h.bind("isOn").to(s, "value", (m) => m === u.model), h.on("execute", () => {
          e.execute("highlight", {
            value: u.model
          }), e.editing.view.focus();
        }), d.children.add(h), a.items.add(d);
      }
      a.items.add(new Ba(r));
      const l = new en(r, o), c = new Fe(r);
      return c.set({
        label: i("Remove highlight"),
        icon: Z.eraser
      }), c.delegate("execute").to(o), c.on("execute", () => {
        e.execute("highlight", {
          value: null
        }), e.editing.view.focus();
      }), l.children.add(c), a.items.add(l), o.panelView.children.add(a), o;
    });
  }
}
function rD(n) {
  n.buttonView.actionView.iconView.bind("fillColor").to(n.buttonView, "color");
}
function bl(n) {
  return n === "marker" ? iD : nD;
}
class oD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      eD,
      sD
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Highlight";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class aD extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.schema, i = t.document.selection;
    this.isEnabled = lD(i, e, t);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  */
  execute() {
    const t = this.editor.model;
    t.change((e) => {
      const i = e.createElement("horizontalLine");
      t.insertObject(i, null, null, {
        setSelection: "after"
      });
    });
  }
}
function lD(n, t, e) {
  const i = cD(n, e);
  return t.checkChild(i, "horizontalLine");
}
function cD(n, t) {
  const i = Da(n, t).start.parent;
  return i.isEmpty && !i.is("element", "$root") ? i.parent : i;
}
class uD extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HorizontalLineEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model.schema, i = t.t, s = t.conversion;
    e.register("horizontalLine", {
      inheritAllFrom: "$blockObject"
    }), s.for("dataDowncast").elementToElement({
      model: "horizontalLine",
      view: (r, { writer: o }) => o.createEmptyElement("hr")
    }), s.for("editingDowncast").elementToStructure({
      model: "horizontalLine",
      view: (r, { writer: o }) => {
        const a = i("Horizontal line"), l = o.createContainerElement("div", null, o.createEmptyElement("hr"));
        return o.addClass("ck-horizontal-line", l), o.setCustomProperty("hr", !0, l), dD(l, o, a);
      }
    }), s.for("upcast").elementToElement({
      view: "hr",
      model: "horizontalLine"
    }), t.commands.add("horizontalLine", new aD(t));
  }
}
function dD(n, t, e) {
  return t.setCustomProperty("horizontalLine", !0, n), Qn(n, t, {
    label: e
  });
}
class hD extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HorizontalLineUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add("horizontalLine", () => {
      const e = this._createButton(le);
      return e.set({
        tooltip: !0
      }), e;
    }), t.ui.componentFactory.add("menuBar:horizontalLine", () => this._createButton(Fe));
  }
  /**
  * Creates a button for horizontal line command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, i = e.locale, s = e.commands.get("horizontalLine"), r = new t(e.locale), o = i.t;
    return r.set({
      label: o("Horizontal line"),
      icon: Z.horizontalLine
    }), r.bind("isEnabled").to(s, "isEnabled"), this.listenTo(r, "execute", () => {
      e.execute("horizontalLine"), e.editing.view.focus();
    }), r;
  }
}
class fD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      uD,
      hD,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HorizontalLine";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class mD extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.schema, i = t.document.selection, s = C1(i);
    this.isEnabled = gD(i, e, t), this.value = s ? s.getAttribute("value") || "" : null;
  }
  /**
  * Executes the command, which either:
  *
  * * creates and inserts a new HTML embed element if none was selected,
  * * updates the content of the HTML embed if one was selected.
  *
  * @fires execute
  * @param value When passed, the value (content) will be set on a new embed or a selected one.
  */
  execute(t) {
    const e = this.editor.model, i = e.document.selection;
    e.change((s) => {
      let r;
      this.value !== null ? r = C1(i) : (r = s.createElement("rawHtml"), e.insertObject(r, null, null, {
        setSelection: "on"
      })), s.setAttribute("value", t, r);
    });
  }
}
function gD(n, t, e) {
  const i = pD(n, e);
  return t.checkChild(i, "rawHtml");
}
function pD(n, t) {
  const i = Da(n, t).start.parent;
  return i.isEmpty && !i.is("rootElement") ? i.parent : i;
}
function C1(n) {
  const t = n.getSelectedElement();
  return t && t.is("element", "rawHtml") ? t : null;
}
class bD extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Keeps references to {@link module:ui/button/buttonview~ButtonView edit, save, and cancel} button instances created for
    * each widget so they can be destroyed if they are no longer in DOM after the editing view was re-rendered.
    */
    f(this, "_widgetButtonViewReferences", /* @__PURE__ */ new Set());
    e.config.define("htmlEmbed", {
      showPreviews: !1,
      sanitizeHtml: (i) => (Ve("html-embed-provide-sanitize-function"), {
        html: i,
        hasChanged: !1
      })
    });
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HtmlEmbedEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.model.schema.register("rawHtml", {
      inheritAllFrom: "$blockObject",
      allowAttributes: [
        "value"
      ]
    }), e.commands.add("htmlEmbed", new mD(e)), this._setupConversion();
  }
  /**
  * Prepares converters for the feature.
  */
  _setupConversion() {
    const e = this.editor, i = e.t, s = e.editing.view, r = this._widgetButtonViewReferences, o = e.config.get("htmlEmbed");
    this.editor.editing.view.on("render", () => {
      for (const d of r) {
        if (d.element && d.element.isConnected)
          return;
        d.destroy(), r.delete(d);
      }
    }, {
      priority: "lowest"
    }), e.data.registerRawContentMatcher({
      name: "div",
      classes: "raw-html-embed"
    }), e.conversion.for("upcast").elementToElement({
      view: {
        name: "div",
        classes: "raw-html-embed"
      },
      model: (d, { writer: h }) => h.createElement("rawHtml", {
        value: d.getCustomProperty("$rawContent")
      })
    }), e.conversion.for("dataDowncast").elementToElement({
      model: "rawHtml",
      view: (d, { writer: h }) => h.createRawElement("div", {
        class: "raw-html-embed"
      }, function(m) {
        m.innerHTML = d.getAttribute("value") || "";
      })
    }), e.conversion.for("editingDowncast").elementToStructure({
      model: {
        name: "rawHtml",
        attributes: [
          "value"
        ]
      },
      view: (d, { writer: h }) => {
        let m, g, p;
        const w = h.createRawElement("div", {
          class: "raw-html-embed__content-wrapper"
        }, function(C) {
          m = C, a({
            editor: e,
            domElement: C,
            state: g,
            props: p
          }), m.addEventListener("mousedown", () => {
            if (g.isEditable) {
              const E = e.model;
              E.document.selection.getSelectedElement() !== d && E.change((L) => L.setSelection(d, "on"));
            }
          }, !0);
        }), y = {
          makeEditable() {
            g = Object.assign({}, g, {
              isEditable: !0
            }), a({
              domElement: m,
              editor: e,
              state: g,
              props: p
            }), s.change((C) => {
              C.setAttribute("data-cke-ignore-events", "true", w);
            }), m.querySelector("textarea").focus();
          },
          save(C) {
            C !== g.getRawHtmlValue() ? (e.execute("htmlEmbed", C), e.editing.view.focus()) : this.cancel();
          },
          cancel() {
            g = Object.assign({}, g, {
              isEditable: !1
            }), a({
              domElement: m,
              editor: e,
              state: g,
              props: p
            }), e.editing.view.focus(), s.change((C) => {
              C.removeAttribute("data-cke-ignore-events", w);
            });
          }
        };
        g = {
          showPreviews: o.showPreviews,
          isEditable: !1,
          getRawHtmlValue: () => d.getAttribute("value") || ""
        }, p = {
          sanitizeHtml: o.sanitizeHtml,
          textareaPlaceholder: i("Paste raw HTML here..."),
          onEditClick() {
            y.makeEditable();
          },
          onSaveClick(C) {
            y.save(C);
          },
          onCancelClick() {
            y.cancel();
          }
        };
        const k = h.createContainerElement("div", {
          class: "raw-html-embed",
          "data-html-embed-label": i("HTML snippet"),
          dir: e.locale.uiLanguageDirection
        }, w);
        return h.setCustomProperty("rawHtmlApi", y, k), h.setCustomProperty("rawHtml", !0, k), Qn(k, h, {
          label: i("HTML snippet"),
          hasSelectionHandle: !0
        });
      }
    });
    function a({ editor: d, domElement: h, state: m, props: g }) {
      h.textContent = "";
      const p = h.ownerDocument;
      let w;
      if (m.isEditable) {
        const k = {
          isDisabled: !1,
          placeholder: g.textareaPlaceholder
        };
        w = c({
          domDocument: p,
          state: m,
          props: k
        }), h.append(w);
      } else if (m.showPreviews) {
        const k = {
          sanitizeHtml: g.sanitizeHtml
        };
        h.append(u({
          domDocument: p,
          state: m,
          props: k,
          editor: d
        }));
      } else {
        const k = {
          isDisabled: !0,
          placeholder: g.textareaPlaceholder
        };
        h.append(c({
          domDocument: p,
          state: m,
          props: k
        }));
      }
      const y = {
        onEditClick: g.onEditClick,
        onSaveClick: () => {
          g.onSaveClick(w.value);
        },
        onCancelClick: g.onCancelClick
      };
      h.prepend(l({
        editor: d,
        domDocument: p,
        state: m,
        props: y
      }));
    }
    function l({ editor: d, domDocument: h, state: m, props: g }) {
      const p = Ft(h, "div", {
        class: "raw-html-embed__buttons-wrapper"
      });
      if (m.isEditable) {
        const w = wd(d, "save", g.onSaveClick), y = wd(d, "cancel", g.onCancelClick);
        p.append(w.element, y.element), r.add(w).add(y);
      } else {
        const w = wd(d, "edit", g.onEditClick);
        p.append(w.element), r.add(w);
      }
      return p;
    }
    function c({ domDocument: d, state: h, props: m }) {
      const g = Ft(d, "textarea", {
        placeholder: m.placeholder,
        class: "ck ck-reset ck-input ck-input-text raw-html-embed__source"
      });
      return g.disabled = m.isDisabled, g.value = h.getRawHtmlValue(), g;
    }
    function u({ editor: d, domDocument: h, state: m, props: g }) {
      const p = g.sanitizeHtml(m.getRawHtmlValue()), w = m.getRawHtmlValue().length > 0 ? i("No preview available") : i("Empty snippet content"), y = Ft(h, "div", {
        class: "ck ck-reset_all raw-html-embed__preview-placeholder"
      }, w), k = Ft(h, "div", {
        class: "raw-html-embed__preview-content",
        dir: d.locale.contentLanguageDirection
      }), E = h.createRange().createContextualFragment(p.html);
      return k.appendChild(E), Ft(h, "div", {
        class: "raw-html-embed__preview"
      }, [
        y,
        k
      ]);
    }
  }
}
function wd(n, t, e) {
  const { t: i } = n.locale, s = new le(n.locale), r = n.commands.get("htmlEmbed");
  return s.set({
    class: `raw-html-embed__${t}-button`,
    icon: Z.pencil,
    tooltip: !0,
    tooltipPosition: n.locale.uiLanguageDirection === "rtl" ? "e" : "w"
  }), s.render(), t === "edit" ? (s.set({
    icon: Z.pencil,
    label: i("Edit source")
  }), s.bind("isEnabled").to(r)) : t === "save" ? (s.set({
    icon: Z.check,
    label: i("Save changes")
  }), s.bind("isEnabled").to(r)) : s.set({
    icon: Z.cancel,
    label: i("Cancel")
  }), s.on("execute", e), s;
}
class wD extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HtmlEmbedUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, i = t.locale.t;
    t.ui.componentFactory.add("htmlEmbed", () => {
      const s = this._createButton(le);
      return s.set({
        tooltip: !0,
        label: i("Insert HTML")
      }), s;
    }), t.ui.componentFactory.add("menuBar:htmlEmbed", () => {
      const s = this._createButton(Fe);
      return s.set({
        label: i("HTML snippet")
      }), s;
    });
  }
  /**
  * Creates a button for html embed command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, i = e.commands.get("htmlEmbed"), s = new t(e.locale);
    return s.set({
      icon: Z.html
    }), s.bind("isEnabled").to(i, "isEnabled"), this.listenTo(s, "execute", () => {
      e.execute("htmlEmbed"), e.editing.view.focus(), e.editing.view.document.selection.getSelectedElement().getCustomProperty("rawHtmlApi").makeEditable();
    }), s;
  }
}
class vD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      bD,
      wD,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HtmlEmbed";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function ms(n, t, e, i) {
  t && _D(n, t, i), e && wn(n, e, i);
}
function wn(n, t, e) {
  if (t.attributes)
    for (const [i, s] of Object.entries(t.attributes))
      n.setAttribute(i, s, e);
  t.styles && n.setStyle(t.styles, e), t.classes && n.addClass(t.classes, e);
}
function _D(n, t, e) {
  if (t.attributes)
    for (const [i] of Object.entries(t.attributes))
      n.removeAttribute(i, e);
  if (t.styles)
    for (const i of Object.keys(t.styles))
      n.removeStyle(i, e);
  t.classes && n.removeClass(t.classes, e);
}
function yD(n, t) {
  const e = Di(n);
  let i = "attributes";
  for (i in t)
    i == "classes" ? e[i] = Array.from(/* @__PURE__ */ new Set([
      ...n[i] || [],
      ...t[i]
    ])) : e[i] = {
      ...n[i],
      ...t[i]
    };
  return e;
}
function br(n, t, e, i, s) {
  const r = t.getAttribute(e), o = {};
  for (const a of [
    "attributes",
    "styles",
    "classes"
  ]) {
    if (a != i) {
      r && r[a] && (o[a] = r[a]);
      continue;
    }
    if (i == "classes") {
      const c = new Set(r && r.classes || []);
      s(c), c.size && (o[a] = Array.from(c));
      continue;
    }
    const l = new Map(Object.entries(r && r[a] || {}));
    s(l), l.size && (o[a] = Object.fromEntries(l));
  }
  Object.keys(o).length ? t.is("documentSelection") ? n.setSelectionAttribute(e, o) : n.setAttribute(e, o, t) : r && (t.is("documentSelection") ? n.removeSelectionAttribute(e) : n.removeAttribute(e, t));
}
function kD(n) {
  return i4(n).replace(/ /g, "");
}
function Mi(n) {
  return `html${kD(n)}Attributes`;
}
function tm({ model: n }) {
  return (t, e) => e.writer.createElement(n, {
    htmlContent: t.getCustomProperty("$rawContent")
  });
}
function CD(n, { view: t, isInline: e }) {
  const i = n.t;
  return (s, { writer: r }) => {
    const o = i("HTML object"), a = iu(t, s, r), l = s.getAttribute(Mi(t));
    r.addClass("html-object-embed__content", a), l && wn(r, l, a);
    const c = r.createContainerElement(e ? "span" : "div", {
      class: "html-object-embed",
      "data-html-object-embed-label": o
    }, a);
    return Qn(c, r, {
      label: o
    });
  };
}
function iu(n, t, e) {
  return e.createRawElement(n, null, (i, s) => {
    s.setContentOf(i, t.getAttribute("htmlContent"));
  });
}
function xD({ view: n, model: t, allowEmpty: e }, i) {
  return (r) => {
    r.on(`element:${n}`, (o, a, l) => {
      let c = i.processViewAttributes(a.viewItem, l);
      if (!(!c && !l.consumable.test(a.viewItem, {
        name: !0
      }))) {
        if (c = c || {}, l.consumable.consume(a.viewItem, {
          name: !0
        }), a.modelRange || (a = Object.assign(a, l.convertChildren(a.viewItem, a.modelCursor))), e && a.modelRange.isCollapsed && Object.keys(c).length) {
          const u = l.writer.createElement("htmlEmptyElement");
          if (!l.safeInsert(u, a.modelCursor))
            return;
          const d = l.getSplitParts(u);
          a.modelRange = l.writer.createRange(a.modelRange.start, l.writer.createPositionAfter(d[d.length - 1])), l.updateConversionResult(u, a), s(u, c, l);
          return;
        }
        for (const u of a.modelRange.getItems())
          s(u, c, l);
      }
    }, {
      priority: "low"
    });
  };
  function s(r, o, a) {
    if (a.schema.checkAttribute(r, t)) {
      const l = r.getAttribute(t), c = yD(o, l || {});
      a.writer.setAttribute(t, c, r);
    }
  }
}
function x1({ model: n, view: t }, e) {
  return (i, { writer: s, consumable: r }) => {
    if (!i.hasAttribute(n))
      return null;
    const o = s.createContainerElement(t), a = i.getAttribute(n);
    return r.consume(i, `attribute:${n}`), wn(s, a, o), o.getFillerOffset = () => null, e ? Qn(o, s) : o;
  };
}
function TD({ priority: n, view: t }) {
  return (e, i) => {
    if (!e)
      return;
    const { writer: s } = i, r = s.createAttributeElement(t, null, {
      priority: n
    });
    return wn(s, e, r), r;
  };
}
function Wr({ view: n }, t) {
  return (e) => {
    e.on(`element:${n}`, (i, s, r) => {
      if (!s.modelRange || s.modelRange.isCollapsed)
        return;
      const o = t.processViewAttributes(s.viewItem, r);
      o && r.writer.setAttribute(Mi(s.viewItem.name), o, s.modelRange);
    }, {
      priority: "low"
    });
  };
}
function pa({ view: n, model: t }) {
  return (e) => {
    e.on(`attribute:${Mi(n)}:${t}`, (i, s, r) => {
      if (!r.consumable.consume(s.item, i.name))
        return;
      const { attributeOldValue: o, attributeNewValue: a } = s, l = r.writer, c = r.mapper.toViewElement(s.item);
      ms(l, o, a, c);
    });
  };
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
var T1 = {
  block: [
    // Existing features.
    {
      model: "codeBlock",
      view: "pre"
    },
    {
      model: "paragraph",
      view: "p"
    },
    {
      model: "blockQuote",
      view: "blockquote"
    },
    {
      model: "listItem",
      view: "li"
    },
    {
      model: "pageBreak",
      view: "div"
    },
    {
      model: "rawHtml",
      view: "div"
    },
    {
      model: "table",
      view: "table"
    },
    {
      model: "tableRow",
      view: "tr"
    },
    {
      model: "tableCell",
      view: "td"
    },
    {
      model: "tableCell",
      view: "th"
    },
    {
      model: "tableColumnGroup",
      view: "colgroup"
    },
    {
      model: "tableColumn",
      view: "col"
    },
    {
      model: "caption",
      view: "caption"
    },
    {
      model: "caption",
      view: "figcaption"
    },
    {
      model: "imageBlock",
      view: "img"
    },
    {
      model: "imageInline",
      view: "img"
    },
    {
      model: "horizontalLine",
      view: "hr"
    },
    // Compatibility features.
    {
      model: "htmlP",
      view: "p",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlBlockquote",
      view: "blockquote",
      modelSchema: {
        inheritAllFrom: "$container"
      }
    },
    {
      model: "htmlTable",
      view: "table",
      modelSchema: {
        allowWhere: "$block",
        isBlock: !0
      }
    },
    {
      model: "htmlTbody",
      view: "tbody",
      modelSchema: {
        allowIn: "htmlTable",
        isBlock: !1
      }
    },
    {
      model: "htmlThead",
      view: "thead",
      modelSchema: {
        allowIn: "htmlTable",
        isBlock: !1
      }
    },
    {
      model: "htmlTfoot",
      view: "tfoot",
      modelSchema: {
        allowIn: "htmlTable",
        isBlock: !1
      }
    },
    {
      model: "htmlCaption",
      view: "caption",
      modelSchema: {
        allowIn: "htmlTable",
        allowChildren: "$text",
        isBlock: !1
      }
    },
    {
      model: "htmlColgroup",
      view: "colgroup",
      modelSchema: {
        allowIn: "htmlTable",
        allowChildren: "col",
        isBlock: !1
      }
    },
    {
      model: "htmlCol",
      view: "col",
      modelSchema: {
        allowIn: "htmlColgroup",
        isBlock: !1
      }
    },
    {
      model: "htmlTr",
      view: "tr",
      modelSchema: {
        allowIn: [
          "htmlTable",
          "htmlThead",
          "htmlTbody"
        ],
        isLimit: !0
      }
    },
    // TODO can also include text.
    {
      model: "htmlTd",
      view: "td",
      modelSchema: {
        allowIn: "htmlTr",
        allowContentOf: "$container",
        isLimit: !0,
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlTh",
      view: "th",
      modelSchema: {
        allowIn: "htmlTr",
        allowContentOf: "$container",
        isLimit: !0,
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlFigure",
      view: "figure",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include other block elements.
    {
      model: "htmlFigcaption",
      view: "figcaption",
      modelSchema: {
        allowIn: "htmlFigure",
        allowChildren: "$text",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlAddress",
      view: "address",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlAside",
      view: "aside",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlMain",
      view: "main",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlDetails",
      view: "details",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    {
      model: "htmlSummary",
      view: "summary",
      modelSchema: {
        allowChildren: [
          "htmlH1",
          "htmlH2",
          "htmlH3",
          "htmlH4",
          "htmlH5",
          "htmlH6",
          "$text"
        ],
        allowIn: "htmlDetails",
        isBlock: !1
      }
    },
    {
      model: "htmlDiv",
      view: "div",
      paragraphLikeModel: "htmlDivParagraph",
      modelSchema: {
        inheritAllFrom: "$container"
      }
    },
    // TODO can also include text.
    {
      model: "htmlFieldset",
      view: "fieldset",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include h1-h6.
    {
      model: "htmlLegend",
      view: "legend",
      modelSchema: {
        allowIn: "htmlFieldset",
        allowChildren: "$text"
      }
    },
    // TODO can also include text.
    {
      model: "htmlHeader",
      view: "header",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlFooter",
      view: "footer",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlForm",
      view: "form",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !0
      }
    },
    {
      model: "htmlHgroup",
      view: "hgroup",
      modelSchema: {
        allowIn: [
          "$root",
          "$container"
        ],
        allowChildren: [
          "paragraph",
          "htmlP",
          "htmlH1",
          "htmlH2",
          "htmlH3",
          "htmlH4",
          "htmlH5",
          "htmlH6"
        ],
        isBlock: !1
      }
    },
    {
      model: "htmlH1",
      view: "h1",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH2",
      view: "h2",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH3",
      view: "h3",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH4",
      view: "h4",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH5",
      view: "h5",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlH6",
      view: "h6",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "$htmlList",
      modelSchema: {
        allowWhere: "$container",
        allowChildren: [
          "$htmlList",
          "htmlLi"
        ],
        isBlock: !1
      }
    },
    {
      model: "htmlDir",
      view: "dir",
      modelSchema: {
        inheritAllFrom: "$htmlList"
      }
    },
    {
      model: "htmlMenu",
      view: "menu",
      modelSchema: {
        inheritAllFrom: "$htmlList"
      }
    },
    {
      model: "htmlUl",
      view: "ul",
      modelSchema: {
        inheritAllFrom: "$htmlList"
      }
    },
    {
      model: "htmlOl",
      view: "ol",
      modelSchema: {
        inheritAllFrom: "$htmlList"
      }
    },
    // TODO can also include other block elements.
    {
      model: "htmlLi",
      view: "li",
      modelSchema: {
        allowIn: "$htmlList",
        allowChildren: "$text",
        isBlock: !1
      }
    },
    {
      model: "htmlPre",
      view: "pre",
      modelSchema: {
        inheritAllFrom: "$block"
      }
    },
    {
      model: "htmlArticle",
      view: "article",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    {
      model: "htmlSection",
      view: "section",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    // TODO can also include text.
    {
      model: "htmlNav",
      view: "nav",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    {
      model: "htmlDivDl",
      view: "div",
      modelSchema: {
        allowChildren: [
          "htmlDt",
          "htmlDd"
        ],
        allowIn: "htmlDl"
      }
    },
    {
      model: "htmlDl",
      view: "dl",
      modelSchema: {
        allowWhere: "$container",
        allowChildren: [
          "htmlDt",
          "htmlDd",
          "htmlDivDl"
        ],
        isBlock: !1
      }
    },
    {
      model: "htmlDt",
      view: "dt",
      modelSchema: {
        allowChildren: "$block",
        isBlock: !1
      }
    },
    {
      model: "htmlDd",
      view: "dd",
      modelSchema: {
        allowChildren: "$block",
        isBlock: !1
      }
    },
    {
      model: "htmlCenter",
      view: "center",
      modelSchema: {
        inheritAllFrom: "$container",
        isBlock: !1
      }
    },
    {
      model: "htmlHr",
      view: "hr",
      isEmpty: !0,
      modelSchema: {
        inheritAllFrom: "$blockObject"
      }
    }
  ],
  inline: [
    // Existing features (attribute set on an existing model element).
    {
      model: "htmlLiAttributes",
      view: "li",
      appliesToBlock: !0,
      coupledAttribute: "listItemId"
    },
    {
      model: "htmlOlAttributes",
      view: "ol",
      appliesToBlock: !0,
      coupledAttribute: "listItemId"
    },
    {
      model: "htmlUlAttributes",
      view: "ul",
      appliesToBlock: !0,
      coupledAttribute: "listItemId"
    },
    {
      model: "htmlFigureAttributes",
      view: "figure",
      appliesToBlock: "table"
    },
    {
      model: "htmlTheadAttributes",
      view: "thead",
      appliesToBlock: "table"
    },
    {
      model: "htmlTbodyAttributes",
      view: "tbody",
      appliesToBlock: "table"
    },
    {
      model: "htmlFigureAttributes",
      view: "figure",
      appliesToBlock: "imageBlock"
    },
    // Compatibility features.
    {
      model: "htmlAcronym",
      view: "acronym",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlTt",
      view: "tt",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlFont",
      view: "font",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlTime",
      view: "time",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlVar",
      view: "var",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlBig",
      view: "big",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSmall",
      view: "small",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSamp",
      view: "samp",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlQ",
      view: "q",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlOutput",
      view: "output",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlKbd",
      view: "kbd",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlBdi",
      view: "bdi",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlBdo",
      view: "bdo",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlAbbr",
      view: "abbr",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlA",
      view: "a",
      priority: 5,
      coupledAttribute: "linkHref"
    },
    {
      model: "htmlStrong",
      view: "strong",
      coupledAttribute: "bold",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlB",
      view: "b",
      coupledAttribute: "bold",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlI",
      view: "i",
      coupledAttribute: "italic",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlEm",
      view: "em",
      coupledAttribute: "italic",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlS",
      view: "s",
      coupledAttribute: "strikethrough",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    // TODO According to HTML-spec can behave as div-like element, although CKE4 only handles it as an inline element.
    {
      model: "htmlDel",
      view: "del",
      coupledAttribute: "strikethrough",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    // TODO According to HTML-spec can behave as div-like element, although CKE4 only handles it as an inline element.
    {
      model: "htmlIns",
      view: "ins",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlU",
      view: "u",
      coupledAttribute: "underline",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSub",
      view: "sub",
      coupledAttribute: "subscript",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSup",
      view: "sup",
      coupledAttribute: "superscript",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlCode",
      view: "code",
      coupledAttribute: "code",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlMark",
      view: "mark",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlSpan",
      view: "span",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlCite",
      view: "cite",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlLabel",
      view: "label",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    {
      model: "htmlDfn",
      view: "dfn",
      attributeProperties: {
        copyOnEnter: !0,
        isFormatting: !0
      }
    },
    // Objects.
    {
      model: "htmlObject",
      view: "object",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlIframe",
      view: "iframe",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlInput",
      view: "input",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlButton",
      view: "button",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlTextarea",
      view: "textarea",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlSelect",
      view: "select",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlVideo",
      view: "video",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlEmbed",
      view: "embed",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlOembed",
      view: "oembed",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlAudio",
      view: "audio",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlImg",
      view: "img",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlCanvas",
      view: "canvas",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    // TODO it could be probably represented as non-object element, although it has graphical representation,
    // so probably makes more sense to keep it as an object.
    {
      model: "htmlMeter",
      view: "meter",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    // TODO it could be probably represented as non-object element, although it has graphical representation,
    // so probably makes more sense to keep it as an object.
    {
      model: "htmlProgress",
      view: "progress",
      isObject: !0,
      modelSchema: {
        inheritAllFrom: "$inlineObject"
      }
    },
    {
      model: "htmlScript",
      view: "script",
      modelSchema: {
        allowWhere: [
          "$text",
          "$block"
        ],
        isInline: !0
      }
    },
    {
      model: "htmlStyle",
      view: "style",
      modelSchema: {
        allowWhere: [
          "$text",
          "$block"
        ],
        isInline: !0
      }
    },
    {
      model: "htmlCustomElement",
      view: "$customElement",
      modelSchema: {
        allowWhere: [
          "$text",
          "$block"
        ],
        allowAttributesOf: "$inlineObject",
        isInline: !0
      }
    }
  ]
};
class qr extends I {
  constructor() {
    super(...arguments);
    /**
    * A map of registered data schema definitions.
    */
    f(this, "_definitions", []);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DataSchema";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    for (const e of T1.block)
      this.registerBlockElement(e);
    for (const e of T1.inline)
      this.registerInlineElement(e);
  }
  /**
  * Add new data schema definition describing block element.
  */
  registerBlockElement(e) {
    this._definitions.push({
      ...e,
      isBlock: !0
    });
  }
  /**
  * Add new data schema definition describing inline element.
  */
  registerInlineElement(e) {
    this._definitions.push({
      ...e,
      isInline: !0
    });
  }
  /**
  * Updates schema definition describing block element with new properties.
  *
  * Creates new scheme if it doesn't exist.
  * Array properties are concatenated with original values.
  *
  * @param definition Definition update.
  */
  extendBlockElement(e) {
    this._extendDefinition({
      ...e,
      isBlock: !0
    });
  }
  /**
  * Updates schema definition describing inline element with new properties.
  *
  * Creates new scheme if it doesn't exist.
  * Array properties are concatenated with original values.
  *
  * @param definition Definition update.
  */
  extendInlineElement(e) {
    this._extendDefinition({
      ...e,
      isInline: !0
    });
  }
  /**
  * Returns all definitions matching the given view name.
  *
  * @param includeReferences Indicates if this method should also include definitions of referenced models.
  */
  getDefinitionsForView(e, i = !1) {
    const s = /* @__PURE__ */ new Set();
    for (const r of this._getMatchingViewDefinitions(e)) {
      if (i)
        for (const o of this._getReferences(r.model))
          s.add(o);
      s.add(r);
    }
    return s;
  }
  /**
  * Returns definitions matching the given model name.
  */
  getDefinitionsForModel(e) {
    return this._definitions.filter((i) => i.model == e);
  }
  /**
  * Returns definitions matching the given view name.
  */
  _getMatchingViewDefinitions(e) {
    return this._definitions.filter((i) => i.view && ED(e, i.view));
  }
  /**
  * Resolves all definition references registered for the given data schema definition.
  *
  * @param modelName Data schema model name.
  */
  *_getReferences(e) {
    const i = [
      "inheritAllFrom",
      "inheritTypesFrom",
      "allowWhere",
      "allowContentOf",
      "allowAttributesOf"
    ], s = this._definitions.filter((r) => r.model == e);
    for (const { modelSchema: r } of s)
      if (r)
        for (const o of i)
          for (const a of Ue(r[o] || [])) {
            const l = this._definitions.filter((c) => c.model == a);
            for (const c of l)
              a !== e && (yield* this._getReferences(c.model), yield c);
          }
  }
  /**
  * Updates schema definition with new properties.
  *
  * Creates new scheme if it doesn't exist.
  * Array properties are concatenated with original values.
  *
  * @param definition Definition update.
  */
  _extendDefinition(e) {
    const i = Array.from(this._definitions.entries()).filter(([, s]) => s.model == e.model);
    if (i.length == 0) {
      this._definitions.push(e);
      return;
    }
    for (const [s, r] of i)
      this._definitions[s] = WP({}, r, e, (o, a) => Array.isArray(o) ? o.concat(a) : void 0);
  }
}
function ED(n, t) {
  return typeof n == "string" ? n === t : n instanceof RegExp ? n.test(t) : !1;
}
class pt extends I {
  constructor(e) {
    super(e);
    /**
    * An instance of the {@link module:html-support/dataschema~DataSchema}.
    */
    f(this, "_dataSchema");
    /**
    * {@link module:engine/view/matcher~Matcher Matcher} instance describing rules upon which
    * content attributes should be allowed.
    */
    f(this, "_allowedAttributes");
    /**
    * {@link module:engine/view/matcher~Matcher Matcher} instance describing rules upon which
    * content attributes should be disallowed.
    */
    f(this, "_disallowedAttributes");
    /**
     * Allowed element definitions by {@link module:html-support/datafilter~DataFilter#allowElement} method.
    */
    f(this, "_allowedElements");
    /**
    * Disallowed element names by {@link module:html-support/datafilter~DataFilter#disallowElement} method.
    */
    f(this, "_disallowedElements");
    /**
     * Indicates if {@link module:engine/controller/datacontroller~DataController editor's data controller}
     * data has been already initialized.
    */
    f(this, "_dataInitialized");
    /**
    * Cached map of coupled attributes. Keys are the feature attributes names
    * and values are arrays with coupled GHS attributes names.
    */
    f(this, "_coupledAttributes");
    this._dataSchema = e.plugins.get("DataSchema"), this._allowedAttributes = new oi(), this._disallowedAttributes = new oi(), this._allowedElements = /* @__PURE__ */ new Set(), this._disallowedElements = /* @__PURE__ */ new Set(), this._dataInitialized = !1, this._coupledAttributes = null, this._registerElementsAfterInit(), this._registerElementHandlers(), this._registerCoupledAttributesPostFixer(), this._registerAssociatedHtmlAttributesPostFixer();
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DataFilter";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      qr,
      Sn
    ];
  }
  /**
  * Load a configuration of one or many elements, where their attributes should be allowed.
  *
  * **Note**: Rules will be applied just before next data pipeline data init or set.
  *
  * @param config Configuration of elements that should have their attributes accepted in the editor.
  */
  loadAllowedConfig(e) {
    for (const i of e) {
      const s = i.name || /[\s\S]+/, r = S1(i);
      this.allowElement(s), r.forEach((o) => this.allowAttributes(o));
    }
  }
  /**
  * Load a configuration of one or many elements, where their attributes should be disallowed.
  *
  * **Note**: Rules will be applied just before next data pipeline data init or set.
  *
  * @param config Configuration of elements that should have their attributes rejected from the editor.
  */
  loadDisallowedConfig(e) {
    for (const i of e) {
      const s = i.name || /[\s\S]+/, r = S1(i);
      r.length == 0 ? this.disallowElement(s) : r.forEach((o) => this.disallowAttributes(o));
    }
  }
  /**
  * Load a configuration of one or many elements, where when empty should be allowed.
  *
  * **Note**: It modifies DataSchema so must be loaded before registering filtering rules.
  *
  * @param config Configuration of elements that should be preserved even if empty.
  */
  loadAllowedEmptyElementsConfig(e) {
    for (const i of e)
      this.allowEmptyElement(i);
  }
  /**
  * Allow the given element in the editor context.
  *
  * This method will only allow elements described by the {@link module:html-support/dataschema~DataSchema} used
  * to create data filter.
  *
  * **Note**: Rules will be applied just before next data pipeline data init or set.
  *
  * @param viewName String or regular expression matching view name.
  */
  allowElement(e) {
    for (const i of this._dataSchema.getDefinitionsForView(e, !0))
      this._addAllowedElement(i), this._coupledAttributes = null;
  }
  /**
  * Disallow the given element in the editor context.
  *
  * This method will only disallow elements described by the {@link module:html-support/dataschema~DataSchema} used
  * to create data filter.
  *
  * @param viewName String or regular expression matching view name.
  */
  disallowElement(e) {
    for (const i of this._dataSchema.getDefinitionsForView(e, !1))
      this._disallowedElements.add(i.view);
  }
  /**
  * Allow the given empty element in the editor context.
  *
  * This method will only allow elements described by the {@link module:html-support/dataschema~DataSchema} used
  * to create data filter.
  *
  * **Note**: It modifies DataSchema so must be called before registering filtering rules.
  *
  * @param viewName String or regular expression matching view name.
  */
  allowEmptyElement(e) {
    for (const i of this._dataSchema.getDefinitionsForView(e, !0))
      i.isInline && this._dataSchema.extendInlineElement({
        ...i,
        allowEmpty: !0
      });
  }
  /**
  * Allow the given attributes for view element allowed by {@link #allowElement} method.
  *
  * @param config Pattern matching all attributes which should be allowed.
  */
  allowAttributes(e) {
    this._allowedAttributes.add(e);
  }
  /**
  * Disallow the given attributes for view element allowed by {@link #allowElement} method.
  *
  * @param config Pattern matching all attributes which should be disallowed.
  */
  disallowAttributes(e) {
    this._disallowedAttributes.add(e);
  }
  /**
  * Processes all allowed and disallowed attributes on the view element by consuming them and returning the allowed ones.
  *
  * This method applies the configuration set up by {@link #allowAttributes `allowAttributes()`}
  * and {@link #disallowAttributes `disallowAttributes()`} over the given view element by consuming relevant attributes.
  * It returns the allowed attributes that were found on the given view element for further processing by integration code.
  *
  * ```ts
  * dispatcher.on( 'element:myElement', ( evt, data, conversionApi ) => {
  * 	// Get rid of disallowed and extract all allowed attributes from a viewElement.
  * 	const viewAttributes = dataFilter.processViewAttributes( data.viewItem, conversionApi );
  * 	// Do something with them, i.e. store inside a model as a dictionary.
  * 	if ( viewAttributes ) {
  * 		conversionApi.writer.setAttribute( 'htmlAttributesOfMyElement', viewAttributes, data.modelRange );
  * 	}
  * } );
  * ```
  *
  * @see module:engine/conversion/viewconsumable~ViewConsumable#consume
  *
  * @returns Object with following properties:
  * - attributes Set with matched attribute names.
  * - styles Set with matched style names.
  * - classes Set with matched class names.
  */
  processViewAttributes(e, i) {
    const { consumable: s } = i;
    return E1(e, this._disallowedAttributes, s), SD(e, E1(e, this._allowedAttributes, s));
  }
  /**
  * Adds allowed element definition and fires registration event.
  */
  _addAllowedElement(e) {
    if (!this._allowedElements.has(e)) {
      if (this._allowedElements.add(e), "appliesToBlock" in e && typeof e.appliesToBlock == "string")
        for (const i of this._dataSchema.getDefinitionsForModel(e.appliesToBlock))
          i.isBlock && this._addAllowedElement(i);
      this._dataInitialized && this.editor.data.once("set", () => {
        this._fireRegisterEvent(e);
      }, {
        // With the highest priority listener we are able to register elements right before
        // running data conversion.
        priority: Si.highest + 1
      });
    }
  }
  /**
   * Registers elements allowed by {@link module:html-support/datafilter~DataFilter#allowElement} method
   * once {@link module:engine/controller/datacontroller~DataController editor's data controller} is initialized.
  */
  _registerElementsAfterInit() {
    this.editor.data.on("init", () => {
      this._dataInitialized = !0;
      for (const e of this._allowedElements)
        this._fireRegisterEvent(e);
    }, {
      // With highest priority listener we are able to register elements right before
      // running data conversion. Also:
      // * Make sure that priority is higher than the one used by `RealTimeCollaborationClient`,
      // as RTC is stopping event propagation.
      // * Make sure no other features hook into this event before GHS because otherwise the
      // downcast conversion (for these features) could run before GHS registered its converters
      // (https://github.com/ckeditor/ckeditor5/issues/11356).
      priority: Si.highest + 1
    });
  }
  /**
  * Registers default element handlers.
  */
  _registerElementHandlers() {
    this.on("register", (e, i) => {
      const s = this.editor.model.schema;
      if (i.isObject && !s.isRegistered(i.model))
        this._registerObjectElement(i);
      else if (i.isBlock)
        this._registerBlockElement(i);
      else if (i.isInline)
        this._registerInlineElement(i);
      else
        throw new S("data-filter-invalid-definition", null, i);
      e.stop();
    }, {
      priority: "lowest"
    });
  }
  /**
  * Registers a model post-fixer that is removing coupled GHS attributes of inline elements. Those attributes
  * are removed if a coupled feature attribute is removed.
  *
  * For example, consider following HTML:
  *
  * ```html
  * <a href="foo.html" id="myId">bar</a>
  * ```
  *
  * Which would be upcasted to following text node in the model:
  *
  * ```html
  * <$text linkHref="foo.html" htmlA="{ attributes: { id: 'myId' } }">bar</$text>
  * ```
  *
  * When the user removes the link from that text (using UI), only `linkHref` attribute would be removed:
  *
  * ```html
  * <$text htmlA="{ attributes: { id: 'myId' } }">bar</$text>
  * ```
  *
  * The `htmlA` attribute would stay in the model and would cause GHS to generate an `<a>` element.
  * This is incorrect from UX point of view, as the user wanted to remove the whole link (not only `href`).
  */
  _registerCoupledAttributesPostFixer() {
    const e = this.editor.model, i = e.document.selection;
    e.document.registerPostFixer((s) => {
      const r = e.document.differ.getChanges();
      let o = !1;
      const a = this._getCoupledAttributesMap();
      for (const l of r) {
        if (l.type != "attribute" || l.attributeNewValue !== null)
          continue;
        const c = a.get(l.attributeKey);
        if (c)
          for (const { item: u } of l.range.getWalker())
            for (const d of c)
              u.hasAttribute(d) && (s.removeAttribute(d, u), o = !0);
      }
      return o;
    }), this.listenTo(i, "change:attribute", (s, { attributeKeys: r }) => {
      const o = /* @__PURE__ */ new Set(), a = this._getCoupledAttributesMap();
      for (const l of r) {
        if (i.hasAttribute(l))
          continue;
        const c = a.get(l);
        if (c)
          for (const u of c)
            i.hasAttribute(u) && o.add(u);
      }
      o.size != 0 && e.change((l) => {
        for (const c of o)
          l.removeSelectionAttribute(c);
      });
    });
  }
  /**
  * Removes `html*Attributes` attributes from incompatible elements.
  *
  * For example, consider the following HTML:
  *
  * ```html
  * <heading2 htmlH2Attributes="...">foobar[]</heading2>
  * ```
  *
  * Pressing `enter` creates a new `paragraph` element that inherits
  * the `htmlH2Attributes` attribute from `heading2`.
  *
  * ```html
  * <heading2 htmlH2Attributes="...">foobar</heading2>
  * <paragraph htmlH2Attributes="...">[]</paragraph>
  * ```
  *
  * This postfixer ensures that this doesn't happen, and that elements can
  * only have `html*Attributes` associated with them,
  * e.g.: `htmlPAttributes` for `<p>`, `htmlDivAttributes` for `<div>`, etc.
  *
  * With it enabled, pressing `enter` at the end of `<heading2>` will create
  * a new paragraph without the `htmlH2Attributes` attribute.
  *
  * ```html
  * <heading2 htmlH2Attributes="...">foobar</heading2>
  * <paragraph>[]</paragraph>
  * ```
  */
  _registerAssociatedHtmlAttributesPostFixer() {
    const e = this.editor.model;
    e.document.registerPostFixer((i) => {
      const s = e.document.differ.getChanges();
      let r = !1;
      for (const o of s)
        if (!(o.type !== "insert" || o.name === "$text"))
          for (const a of o.attributes.keys())
            !a.startsWith("html") || !a.endsWith("Attributes") || e.schema.checkAttribute(o.name, a) || (i.removeAttribute(a, o.position.nodeAfter), r = !0);
      return r;
    });
  }
  /**
  * Collects the map of coupled attributes. The returned map is keyed by the feature attribute name
  * and coupled GHS attribute names are stored in the value array.
  */
  _getCoupledAttributesMap() {
    if (this._coupledAttributes)
      return this._coupledAttributes;
    this._coupledAttributes = /* @__PURE__ */ new Map();
    for (const e of this._allowedElements)
      if (e.coupledAttribute && e.model) {
        const i = this._coupledAttributes.get(e.coupledAttribute);
        i ? i.push(e.model) : this._coupledAttributes.set(e.coupledAttribute, [
          e.model
        ]);
      }
    return this._coupledAttributes;
  }
  /**
  * Fires `register` event for the given element definition.
  */
  _fireRegisterEvent(e) {
    e.view && this._disallowedElements.has(e.view) || this.fire(e.view ? `register:${e.view}` : "register", e);
  }
  /**
  * Registers object element and attribute converters for the given data schema definition.
  */
  _registerObjectElement(e) {
    const i = this.editor, s = i.model.schema, r = i.conversion, { view: o, model: a } = e;
    s.register(a, e.modelSchema);
    /* istanbul ignore next: paranoid check -- @preserve */
    o && (s.extend(e.model, {
      allowAttributes: [
        Mi(o),
        "htmlContent"
      ]
    }), i.data.registerRawContentMatcher({
      name: o
    }), r.for("upcast").elementToElement({
      view: o,
      model: tm(e),
      // With a `low` priority, `paragraph` plugin auto-paragraphing mechanism is executed. Make sure
      // this listener is called before it. If not, some elements will be transformed into a paragraph.
      // `+ 2` is used to take priority over `_addDefaultH1Conversion` in the Heading plugin.
      converterPriority: Si.low + 2
    }), r.for("upcast").add(Wr(e, this)), r.for("editingDowncast").elementToStructure({
      model: {
        name: a,
        attributes: [
          Mi(o)
        ]
      },
      view: CD(i, e)
    }), r.for("dataDowncast").elementToElement({
      model: a,
      view: (l, { writer: c }) => iu(o, l, c)
    }), r.for("dataDowncast").add(pa(e)));
  }
  /**
  * Registers block element and attribute converters for the given data schema definition.
  */
  _registerBlockElement(e) {
    const i = this.editor, s = i.model.schema, r = i.conversion, { view: o, model: a } = e;
    if (!s.isRegistered(e.model)) {
      if (!e.modelSchema || (s.register(e.model, e.modelSchema), !o))
        return;
      r.for("upcast").elementToElement({
        model: a,
        view: o,
        // With a `low` priority, `paragraph` plugin auto-paragraphing mechanism is executed. Make sure
        // this listener is called before it. If not, some elements will be transformed into a paragraph.
        // `+ 2` is used to take priority over `_addDefaultH1Conversion` in the Heading plugin.
        converterPriority: Si.low + 2
      }), r.for("downcast").elementToElement({
        model: a,
        view: (l, { writer: c }) => e.isEmpty ? c.createEmptyElement(o) : c.createContainerElement(o)
      });
    }
    o && (s.extend(e.model, {
      allowAttributes: Mi(o)
    }), r.for("upcast").add(Wr(e, this)), r.for("downcast").add(pa(e)));
  }
  /**
  * Registers inline element and attribute converters for the given data schema definition.
  *
  * Extends `$text` model schema to allow the given definition model attribute and its properties.
  */
  _registerInlineElement(e) {
    const i = this.editor, s = i.model.schema, r = i.conversion, o = e.model;
    e.appliesToBlock || (s.extend("$text", {
      allowAttributes: o
    }), e.attributeProperties && s.setAttributeProperties(o, e.attributeProperties), r.for("upcast").add(xD(e, this)), r.for("downcast").attributeToElement({
      model: o,
      view: TD(e)
    }), e.allowEmpty && (s.setAttributeProperties(o, {
      copyFromObject: !1
    }), s.isRegistered("htmlEmptyElement") || s.register("htmlEmptyElement", {
      inheritAllFrom: "$inlineObject"
    }), i.data.htmlProcessor.domConverter.registerInlineObjectMatcher((a) => a.name == e.view && a.isEmpty && Array.from(a.getAttributeKeys()).length ? {
      name: !0
    } : null), r.for("editingDowncast").elementToElement({
      model: "htmlEmptyElement",
      view: x1(e, !0)
    }), r.for("dataDowncast").elementToElement({
      model: "htmlEmptyElement",
      view: x1(e)
    })));
  }
}
function E1(n, t, e) {
  const i = t.matchAll(n) || [], s = n.document.stylesProcessor;
  return i.reduce((r, { match: o }) => {
    for (const [a, l] of o.attributes || [])
      if (a == "style") {
        const c = l, u = s.getRelatedStyles(c).filter((d) => d.split("-").length > c.split("-").length).sort((d, h) => h.split("-").length - d.split("-").length);
        for (const d of u)
          e.consume(n, {
            styles: [
              d
            ]
          }) && r.styles.push(d);
        e.consume(n, {
          styles: [
            c
          ]
        }) && r.styles.push(c);
      } else if (a == "class") {
        const c = l;
        e.consume(n, {
          classes: [
            c
          ]
        }) && r.classes.push(c);
      } else
        e.consume(n, {
          attributes: [
            a
          ]
        }) && r.attributes.push(a);
    return r;
  }, {
    attributes: [],
    classes: [],
    styles: []
  });
}
function SD(n, { attributes: t, classes: e, styles: i }) {
  return !t.length && !e.length && !i.length ? null : {
    ...t.length && {
      attributes: AD(n, t)
    },
    ...i.length && {
      styles: ID(n, i)
    },
    ...e.length && {
      classes: e
    }
  };
}
function AD(n, t) {
  const e = {};
  for (const i of t) {
    const s = n.getAttribute(i);
    s !== void 0 && d_(i) && (e[i] = s);
  }
  return e;
}
function ID(n, t) {
  const e = new Br(n.document.stylesProcessor);
  for (const i of t) {
    const s = n.getStyle(i);
    s !== void 0 && e.set(i, s);
  }
  return Object.fromEntries(e.getStylesEntries());
}
function vd(n, t) {
  const { name: e } = n, i = n[t];
  return qi(i) ? Object.entries(i).map(([s, r]) => ({
    name: e,
    [t]: {
      [s]: r
    }
  })) : Array.isArray(i) ? i.map((s) => ({
    name: e,
    [t]: [
      s
    ]
  })) : [
    n
  ];
}
function S1(n) {
  const { name: t, attributes: e, classes: i, styles: s } = n, r = [];
  return e && r.push(...vd({
    name: t,
    attributes: e
  }, "attributes")), i && r.push(...vd({
    name: t,
    classes: i
  }, "classes")), s && r.push(...vd({
    name: t,
    styles: s
  }, "styles")), r;
}
class PD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CodeBlockElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    if (!this.editor.plugins.has("CodeBlockEditing"))
      return;
    const t = this.editor.plugins.get(pt);
    t.on("register:pre", (e, i) => {
      if (i.model !== "codeBlock")
        return;
      const s = this.editor, r = s.model.schema, o = s.conversion;
      r.extend("codeBlock", {
        allowAttributes: [
          "htmlPreAttributes",
          "htmlContentAttributes"
        ]
      }), o.for("upcast").add(RD(t)), o.for("downcast").add(OD()), e.stop();
    });
  }
}
function RD(n) {
  return (t) => {
    t.on("element:code", (e, i, s) => {
      const r = i.viewItem, o = r.parent;
      if (!o || !o.is("element", "pre"))
        return;
      a(o, "htmlPreAttributes"), a(r, "htmlContentAttributes");
      function a(l, c) {
        const u = n.processViewAttributes(l, s);
        u && s.writer.setAttribute(c, u, i.modelRange);
      }
    }, {
      priority: "low"
    });
  };
}
function OD() {
  return (n) => {
    n.on("attribute:htmlPreAttributes:codeBlock", (t, e, i) => {
      if (!i.consumable.consume(e.item, t.name))
        return;
      const { attributeOldValue: s, attributeNewValue: r } = e, a = i.mapper.toViewElement(e.item).parent;
      ms(i.writer, s, r, a);
    }), n.on("attribute:htmlContentAttributes:codeBlock", (t, e, i) => {
      if (!i.consumable.consume(e.item, t.name))
        return;
      const { attributeOldValue: s, attributeNewValue: r } = e, o = i.mapper.toViewElement(e.item);
      ms(i.writer, s, r, o);
    });
  };
}
class VD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "DualContentModelElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.plugins.get(pt).on("register", (e, i) => {
      const s = i, r = this.editor, o = r.model.schema, a = r.conversion;
      if (!s.paragraphLikeModel || o.isRegistered(s.model) || o.isRegistered(s.paragraphLikeModel))
        return;
      const l = {
        model: s.paragraphLikeModel,
        view: s.view
      };
      o.register(s.model, s.modelSchema), o.register(l.model, {
        inheritAllFrom: "$block"
      }), a.for("upcast").elementToElement({
        view: s.view,
        model: (c, { writer: u }) => this._hasBlockContent(c) ? u.createElement(s.model) : u.createElement(l.model),
        // With a `low` priority, `paragraph` plugin auto-paragraphing mechanism is executed. Make sure
        // this listener is called before it. If not, some elements will be transformed into a paragraph.
        converterPriority: Si.low + 0.5
      }), a.for("downcast").elementToElement({
        view: s.view,
        model: s.model
      }), this._addAttributeConversion(s), a.for("downcast").elementToElement({
        view: l.view,
        model: l.model
      }), this._addAttributeConversion(l), e.stop();
    });
  }
  /**
  * Checks whether the given view element includes any other block element.
  */
  _hasBlockContent(t) {
    const e = this.editor.editing.view, i = e.domConverter.blockElements;
    for (const s of e.createRangeIn(t).getItems())
      if (s.is("element") && i.includes(s.name))
        return !0;
    return !1;
  }
  /**
  * Adds attribute filtering conversion for the given data schema.
  */
  _addAttributeConversion(t) {
    const e = this.editor, i = e.conversion, s = e.plugins.get(pt);
    e.model.schema.extend(t.model, {
      allowAttributes: Mi(t.view)
    }), i.for("upcast").add(Wr(t, s)), i.for("downcast").add(pa(t));
  }
}
class LD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      qr,
      Fa
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HeadingElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("HeadingEditing"))
      return;
    const e = t.config.get("heading.options");
    this.registerHeadingElements(t, e);
  }
  /**
  * Registers all elements supported by HeadingEditing to enable custom attributes for those elements.
  */
  registerHeadingElements(t, e) {
    const i = t.plugins.get(qr), s = [];
    for (const r of e)
      "model" in r && "view" in r && (i.registerBlockElement({
        view: r.view,
        model: r.model
      }), s.push(r.model));
    i.extendBlockElement({
      model: "htmlHgroup",
      modelSchema: {
        allowChildren: s
      }
    }), i.extendBlockElement({
      model: "htmlSummary",
      modelSchema: {
        allowChildren: s
      }
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function ba(n, t, e) {
  const i = n.createRangeOn(t);
  for (const { item: s } of i.getWalker())
    if (s.is("element", e))
      return s;
}
class BD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("ImageInlineEditing") && !t.plugins.has("ImageBlockEditing"))
      return;
    const e = t.model.schema, i = t.conversion, s = t.plugins.get(pt);
    s.on("register:figure", () => {
      i.for("upcast").add(FD(s));
    }), s.on("register:img", (r, o) => {
      o.model !== "imageBlock" && o.model !== "imageInline" || (e.isRegistered("imageBlock") && e.extend("imageBlock", {
        allowAttributes: [
          "htmlImgAttributes",
          // Figure and Link don't have model counterpart.
          // We will preserve attributes on image model element using these attribute keys.
          "htmlFigureAttributes",
          "htmlLinkAttributes"
        ]
      }), e.isRegistered("imageInline") && e.extend("imageInline", {
        allowAttributes: [
          // `htmlA` is needed for standard GHS link integration.
          "htmlA",
          "htmlImgAttributes"
        ]
      }), i.for("upcast").add(MD(s)), i.for("downcast").add(DD()), t.plugins.has("LinkImage") && i.for("upcast").add(ND(s, t)), r.stop());
    });
  }
}
function MD(n) {
  return (t) => {
    t.on("element:img", (e, i, s) => {
      if (!i.modelRange)
        return;
      const r = i.viewItem, o = n.processViewAttributes(r, s);
      o && s.writer.setAttribute("htmlImgAttributes", o, i.modelRange);
    }, {
      priority: "low"
    });
  };
}
function ND(n, t) {
  const e = t.plugins.get("ImageUtils");
  return (i) => {
    i.on("element:a", (s, r, o) => {
      const a = r.viewItem;
      if (!e.findViewImgElement(a))
        return;
      const c = r.modelCursor.parent;
      if (!c.is("element", "imageBlock"))
        return;
      const u = n.processViewAttributes(a, o);
      u && o.writer.setAttribute("htmlLinkAttributes", u, c);
    }, {
      priority: "low"
    });
  };
}
function FD(n) {
  return (t) => {
    t.on("element:figure", (e, i, s) => {
      const r = i.viewItem;
      if (!i.modelRange || !r.hasClass("image"))
        return;
      const o = n.processViewAttributes(r, s);
      o && s.writer.setAttribute("htmlFigureAttributes", o, i.modelRange);
    }, {
      priority: "low"
    });
  };
}
function DD() {
  return (n) => {
    t("htmlImgAttributes"), e("img", "htmlImgAttributes"), e("figure", "htmlFigureAttributes"), e("a", "htmlLinkAttributes");
    function t(i) {
      n.on(`attribute:${i}:imageInline`, (s, r, o) => {
        if (!o.consumable.consume(r.item, s.name))
          return;
        const { attributeOldValue: a, attributeNewValue: l } = r, c = o.mapper.toViewElement(r.item);
        ms(o.writer, a, l, c);
      }, {
        priority: "low"
      });
    }
    function e(i, s) {
      n.on(`attribute:${s}:imageBlock`, (r, o, a) => {
        if (!a.consumable.test(o.item, r.name))
          return;
        const { attributeOldValue: l, attributeNewValue: c } = o, u = a.mapper.toViewElement(o.item), d = ba(a.writer, u, i);
        d && (ms(a.writer, l, c, d), a.consumable.consume(o.item, r.name));
      }, {
        priority: "low"
      }), i === "a" && n.on("attribute:linkHref:imageBlock", (r, o, a) => {
        if (!a.consumable.consume(o.item, "attribute:htmlLinkAttributes:imageBlock"))
          return;
        const l = a.mapper.toViewElement(o.item), c = ba(a.writer, l, "a");
        wn(a.writer, o.item.getAttribute("htmlLinkAttributes"), c);
      }, {
        priority: "low"
      });
    }
  };
}
class $D extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MediaEmbedElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("MediaEmbed") || t.config.get("mediaEmbed.previewsInData"))
      return;
    const e = t.model.schema, i = t.conversion, s = this.editor.plugins.get(pt), r = this.editor.plugins.get(qr), o = t.config.get("mediaEmbed.elementName");
    r.registerBlockElement({
      model: "media",
      view: o
    }), s.on("register:figure", () => {
      i.for("upcast").add(HD(s));
    }), s.on(`register:${o}`, (a, l) => {
      l.model === "media" && (e.extend("media", {
        allowAttributes: [
          Mi(o),
          "htmlFigureAttributes"
        ]
      }), i.for("upcast").add(zD(s, o)), i.for("dataDowncast").add(jD(o)), a.stop());
    });
  }
}
function zD(n, t) {
  const e = (i, s, r) => {
    const o = s.viewItem;
    a(o, Mi(t));
    function a(l, c) {
      const u = n.processViewAttributes(l, r);
      u && r.writer.setAttribute(c, u, s.modelRange);
    }
  };
  return (i) => {
    i.on(`element:${t}`, e, {
      priority: "low"
    });
  };
}
function HD(n) {
  return (t) => {
    t.on("element:figure", (e, i, s) => {
      const r = i.viewItem;
      if (!i.modelRange || !r.hasClass("media"))
        return;
      const o = n.processViewAttributes(r, s);
      o && s.writer.setAttribute("htmlFigureAttributes", o, i.modelRange);
    }, {
      priority: "low"
    });
  };
}
function jD(n) {
  return (t) => {
    e(n, Mi(n)), e("figure", "htmlFigureAttributes");
    function e(i, s) {
      t.on(`attribute:${s}:media`, (r, o, a) => {
        if (!a.consumable.consume(o.item, r.name))
          return;
        const { attributeOldValue: l, attributeNewValue: c } = o, u = a.mapper.toViewElement(o.item), d = ba(a.writer, u, i);
        ms(a.writer, l, c, d);
      });
    }
  };
}
class UD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ScriptElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins.get(pt);
    t.on("register:script", (e, i) => {
      const s = this.editor, r = s.model.schema, o = s.conversion;
      r.register("htmlScript", i.modelSchema), r.extend("htmlScript", {
        allowAttributes: [
          "htmlScriptAttributes",
          "htmlContent"
        ],
        isContent: !0
      }), s.data.registerRawContentMatcher({
        name: "script"
      }), o.for("upcast").elementToElement({
        view: "script",
        model: tm(i)
      }), o.for("upcast").add(Wr(i, t)), o.for("downcast").elementToElement({
        model: "htmlScript",
        view: (a, { writer: l }) => iu("script", a, l)
      }), o.for("downcast").add(pa(i)), e.stop();
    });
  }
}
class WD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("TableEditing"))
      return;
    const e = t.model.schema, i = t.conversion, s = t.plugins.get(pt), r = t.plugins.get("TableUtils");
    s.on("register:figure", () => {
      i.for("upcast").add(KD(s));
    }), s.on("register:table", (o, a) => {
      a.model === "table" && (e.extend("table", {
        allowAttributes: [
          "htmlTableAttributes",
          // Figure, thead and tbody elements don't have model counterparts.
          // We will be preserving attributes on table element using these attribute keys.
          "htmlFigureAttributes",
          "htmlTheadAttributes",
          "htmlTbodyAttributes"
        ]
      }), i.for("upcast").add(GD(s)), i.for("downcast").add(ZD()), t.model.document.registerPostFixer(qD(t.model, r)), o.stop());
    });
  }
}
function qD(n, t) {
  return (e) => {
    const i = n.document.differ.getChanges();
    let s = !1;
    for (const r of i) {
      if (r.type != "attribute" || r.attributeKey != "headingRows")
        continue;
      const o = r.range.start.nodeAfter, a = o.getAttribute("htmlTheadAttributes"), l = o.getAttribute("htmlTbodyAttributes");
      a && !r.attributeNewValue ? (e.removeAttribute("htmlTheadAttributes", o), s = !0) : l && r.attributeNewValue == t.getRows(o) && (e.removeAttribute("htmlTbodyAttributes", o), s = !0);
    }
    return s;
  };
}
function GD(n) {
  return (t) => {
    t.on("element:table", (e, i, s) => {
      if (!i.modelRange)
        return;
      const r = i.viewItem;
      o(r, "htmlTableAttributes");
      for (const a of r.getChildren())
        a.is("element", "thead") && o(a, "htmlTheadAttributes"), a.is("element", "tbody") && o(a, "htmlTbodyAttributes");
      function o(a, l) {
        const c = n.processViewAttributes(a, s);
        c && s.writer.setAttribute(l, c, i.modelRange);
      }
    }, {
      priority: "low"
    });
  };
}
function KD(n) {
  return (t) => {
    t.on("element:figure", (e, i, s) => {
      const r = i.viewItem;
      if (!i.modelRange || !r.hasClass("table"))
        return;
      const o = n.processViewAttributes(r, s);
      o && s.writer.setAttribute("htmlFigureAttributes", o, i.modelRange);
    }, {
      priority: "low"
    });
  };
}
function ZD() {
  return (n) => {
    t("table", "htmlTableAttributes"), t("figure", "htmlFigureAttributes"), t("thead", "htmlTheadAttributes"), t("tbody", "htmlTbodyAttributes");
    function t(e, i) {
      n.on(`attribute:${i}:table`, (s, r, o) => {
        if (!o.consumable.test(r.item, s.name))
          return;
        const a = o.mapper.toViewElement(r.item), l = ba(o.writer, a, e);
        l && (o.consumable.consume(r.item, s.name), ms(o.writer, r.attributeOldValue, r.attributeNewValue, l));
      });
    }
  };
}
class YD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "StyleElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins.get(pt);
    t.on("register:style", (e, i) => {
      const s = this.editor, r = s.model.schema, o = s.conversion;
      r.register("htmlStyle", i.modelSchema), r.extend("htmlStyle", {
        allowAttributes: [
          "htmlStyleAttributes",
          "htmlContent"
        ],
        isContent: !0
      }), s.data.registerRawContentMatcher({
        name: "style"
      }), o.for("upcast").elementToElement({
        view: "style",
        model: tm(i)
      }), o.for("upcast").add(Wr(i, t)), o.for("downcast").elementToElement({
        model: "htmlStyle",
        view: (a, { writer: l }) => iu("style", a, l)
      }), o.for("downcast").add(pa(i)), e.stop();
    });
  }
}
class JD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("ListEditing"))
      return;
    const e = t.model.schema, i = t.conversion, s = t.plugins.get(pt), r = t.plugins.get("ListEditing"), o = t.plugins.get("ListUtils"), a = [
      "ul",
      "ol",
      "li"
    ];
    r.registerDowncastStrategy({
      scope: "item",
      attributeName: "htmlLiAttributes",
      setAttributeOnDowncast: wn
    }), r.registerDowncastStrategy({
      scope: "list",
      attributeName: "htmlUlAttributes",
      setAttributeOnDowncast: wn
    }), r.registerDowncastStrategy({
      scope: "list",
      attributeName: "htmlOlAttributes",
      setAttributeOnDowncast: wn
    }), s.on("register", (l, c) => {
      if (!a.includes(c.view) || (l.stop(), e.checkAttribute("$block", "htmlLiAttributes")))
        return;
      const u = a.map((d) => Mi(d));
      e.extend("$listItem", {
        allowAttributes: u
      }), i.for("upcast").add((d) => {
        d.on("element:ul", _d("htmlUlAttributes", s), {
          priority: "low"
        }), d.on("element:ol", _d("htmlOlAttributes", s), {
          priority: "low"
        }), d.on("element:li", _d("htmlLiAttributes", s), {
          priority: "low"
        });
      });
    }), r.on("postFixer", (l, { listNodes: c, writer: u }) => {
      for (const { node: d, previousNodeInList: h } of c)
        if (h) {
          if (h.getAttribute("listType") == d.getAttribute("listType")) {
            const m = A1(h.getAttribute("listType")), g = h.getAttribute(m);
            !Dr(d.getAttribute(m), g) && u.model.schema.checkAttribute(d, m) && (u.setAttribute(m, g, d), l.return = !0);
          }
          if (h.getAttribute("listItemId") == d.getAttribute("listItemId")) {
            const m = h.getAttribute("htmlLiAttributes");
            !Dr(d.getAttribute("htmlLiAttributes"), m) && u.model.schema.checkAttribute(d, "htmlLiAttributes") && (u.setAttribute("htmlLiAttributes", m, d), l.return = !0);
          }
        }
    }), r.on("postFixer", (l, { listNodes: c, writer: u }) => {
      for (const { node: d } of c) {
        const h = d.getAttribute("listType");
        !o.isNumberedListType(h) && d.getAttribute("htmlOlAttributes") && (u.removeAttribute("htmlOlAttributes", d), l.return = !0), o.isNumberedListType(h) && d.getAttribute("htmlUlAttributes") && (u.removeAttribute("htmlUlAttributes", d), l.return = !0);
      }
    });
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor;
    if (!t.commands.get("indentList"))
      return;
    const e = t.commands.get("indentList");
    this.listenTo(e, "afterExecute", (i, s) => {
      t.model.change((r) => {
        for (const o of s) {
          const a = A1(o.getAttribute("listType"));
          t.model.schema.checkAttribute(o, a) && r.setAttribute(a, {}, o);
        }
      });
    });
  }
}
function _d(n, t) {
  return (e, i, s) => {
    const r = i.viewItem;
    i.modelRange || Object.assign(i, s.convertChildren(i.viewItem, i.modelCursor));
    const o = t.processViewAttributes(r, s);
    for (const a of i.modelRange.getItems({
      shallow: !0
    }))
      a.hasAttribute("listItemId") && (a.hasAttribute("htmlUlAttributes") || a.hasAttribute("htmlOlAttributes") || s.writer.model.schema.checkAttribute(a, n) && s.writer.setAttribute(n, o || {}, a));
  };
}
function A1(n) {
  return n === "numbered" || n == "customNumbered" ? "htmlOlAttributes" : "htmlUlAttributes";
}
class XD extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HorizontalLineElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    if (!t.plugins.has("HorizontalLineEditing"))
      return;
    const e = t.model.schema, i = t.conversion, s = t.plugins.get(pt);
    s.on("register:hr", (r, o) => {
      o.model === "horizontalLine" && (e.extend("horizontalLine", {
        allowAttributes: [
          "htmlHrAttributes"
        ]
      }), i.for("upcast").add(Wr(o, s)), i.for("downcast").add(QD()), r.stop());
    });
  }
}
function QD() {
  return (n) => {
    n.on("attribute:htmlHrAttributes:horizontalLine", (t, e, i) => {
      if (!i.consumable.test(e.item, t.name))
        return;
      const { attributeOldValue: s, attributeNewValue: r } = e, o = i.mapper.toViewElement(e.item), a = ba(i.writer, o, "hr");
      a && (ms(i.writer, s, r, a), i.consumable.consume(e.item, t.name));
    }, {
      priority: "low"
    });
  };
}
class e$ extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt,
      qr
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "CustomElementSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins.get(pt), e = this.editor.plugins.get(qr);
    t.on("register:$customElement", (i, s) => {
      i.stop();
      const r = this.editor, o = r.model.schema, a = r.conversion, l = r.editing.view.domConverter.unsafeElements, c = r.data.htmlProcessor.domConverter.preElements;
      o.register(s.model, s.modelSchema), o.extend(s.model, {
        allowAttributes: [
          "htmlElementName",
          "htmlCustomElementAttributes",
          "htmlContent"
        ],
        isContent: !0
      }), r.data.htmlProcessor.domConverter.registerRawContentMatcher({
        name: "template"
      }), a.for("upcast").elementToElement({
        view: /.*/,
        model: (u, d) => {
          if (u.name == "$comment" || !t$(u.name) || e.getDefinitionsForView(u.name).size)
            return null;
          l.includes(u.name) || l.push(u.name), c.includes(u.name) || c.push(u.name);
          const h = d.writer.createElement(s.model, {
            htmlElementName: u.name
          }), m = t.processViewAttributes(u, d);
          m && d.writer.setAttribute("htmlCustomElementAttributes", m, h);
          let g;
          if (u.is("element", "template") && u.getCustomProperty("$rawContent"))
            g = u.getCustomProperty("$rawContent");
          else {
            const w = new Pi(u.document).createDocumentFragment(u), y = r.data.htmlProcessor.domConverter.viewToDom(w), k = y.firstChild;
            for (; k.firstChild; )
              y.appendChild(k.firstChild);
            k.remove(), g = r.data.htmlProcessor.htmlWriter.getHtml(y);
          }
          d.writer.setAttribute("htmlContent", g, h);
          for (const { item: p } of r.editing.view.createRangeIn(u))
            d.consumable.consume(p, {
              name: !0
            });
          return h;
        },
        converterPriority: "low"
      }), a.for("editingDowncast").elementToElement({
        model: {
          name: s.model,
          attributes: [
            "htmlElementName",
            "htmlCustomElementAttributes",
            "htmlContent"
          ]
        },
        view: (u, { writer: d }) => {
          const h = u.getAttribute("htmlElementName"), m = d.createRawElement(h);
          return u.hasAttribute("htmlCustomElementAttributes") && wn(d, u.getAttribute("htmlCustomElementAttributes"), m), m;
        }
      }), a.for("dataDowncast").elementToElement({
        model: {
          name: s.model,
          attributes: [
            "htmlElementName",
            "htmlCustomElementAttributes",
            "htmlContent"
          ]
        },
        view: (u, { writer: d }) => {
          const h = u.getAttribute("htmlElementName"), m = u.getAttribute("htmlContent"), g = d.createRawElement(h, null, (p, w) => {
            w.setContentOf(p, m);
          });
          return u.hasAttribute("htmlCustomElementAttributes") && wn(d, u.getAttribute("htmlCustomElementAttributes"), g), g;
        }
      });
    });
  }
}
function t$(n) {
  try {
    document.createElement(n);
  } catch {
    return !1;
  }
  return !0;
}
class i$ extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "GeneralHtmlSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      pt,
      PD,
      VD,
      LD,
      BD,
      $D,
      UD,
      WD,
      YD,
      JD,
      XD,
      e$
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.plugins.get(pt);
    e.loadAllowedEmptyElementsConfig(t.config.get("htmlSupport.allowEmpty") || []), e.loadAllowedConfig(t.config.get("htmlSupport.allow") || []), e.loadDisallowedConfig(t.config.get("htmlSupport.disallow") || []);
  }
  /**
  * Returns a GHS model attribute name related to a given view element name.
  *
  * @internal
  * @param viewElementName A view element name.
  */
  getGhsAttributeNameForElement(t) {
    const e = this.editor.plugins.get("DataSchema"), i = Array.from(e.getDefinitionsForView(t, !1)), s = i.find((r) => r.isInline && !i[0].isObject);
    return s ? s.model : Mi(t);
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it includes the given class name.
  *
  * @internal
  * @param viewElementName A view element name.
  * @param className The css class to add.
  * @param selectable The selection or element to update.
  */
  addModelHtmlClass(t, e, i) {
    const s = this.editor.model, r = this.getGhsAttributeNameForElement(t);
    s.change((o) => {
      for (const a of wr(s, i, r))
        br(o, a, r, "classes", (l) => {
          for (const c of Ue(e))
            l.add(c);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it does not include the given class name.
  *
  * @internal
  * @param viewElementName A view element name.
  * @param className The css class to remove.
  * @param selectable The selection or element to update.
  */
  removeModelHtmlClass(t, e, i) {
    const s = this.editor.model, r = this.getGhsAttributeNameForElement(t);
    s.change((o) => {
      for (const a of wr(s, i, r))
        br(o, a, r, "classes", (l) => {
          for (const c of Ue(e))
            l.delete(c);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it includes the given attribute.
  *
  * @param viewElementName A view element name.
  * @param attributes The object with attributes to set.
  * @param selectable The selection or element to update.
  */
  setModelHtmlAttributes(t, e, i) {
    const s = this.editor.model, r = this.getGhsAttributeNameForElement(t);
    s.change((o) => {
      for (const a of wr(s, i, r))
        br(o, a, r, "attributes", (l) => {
          for (const [c, u] of Object.entries(e))
            l.set(c, u);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it does not include the given attribute.
  *
  * @param viewElementName A view element name.
  * @param attributeName The attribute name (or names) to remove.
  * @param selectable The selection or element to update.
  */
  removeModelHtmlAttributes(t, e, i) {
    const s = this.editor.model, r = this.getGhsAttributeNameForElement(t);
    s.change((o) => {
      for (const a of wr(s, i, r))
        br(o, a, r, "attributes", (l) => {
          for (const c of Ue(e))
            l.delete(c);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it includes a given style.
  *
  * @param viewElementName A view element name.
  * @param styles The object with styles to set.
  * @param selectable The selection or element to update.
  */
  setModelHtmlStyles(t, e, i) {
    const s = this.editor.model, r = this.getGhsAttributeNameForElement(t);
    s.change((o) => {
      for (const a of wr(s, i, r))
        br(o, a, r, "styles", (l) => {
          for (const [c, u] of Object.entries(e))
            l.set(c, u);
        });
    });
  }
  /**
  * Updates GHS model attribute for a specified view element name, so it does not include a given style.
  *
  * @param viewElementName A view element name.
  * @param properties The style (or styles list) to remove.
  * @param selectable The selection or element to update.
  */
  removeModelHtmlStyles(t, e, i) {
    const s = this.editor.model, r = this.getGhsAttributeNameForElement(t);
    s.change((o) => {
      for (const a of wr(s, i, r))
        br(o, a, r, "styles", (l) => {
          for (const c of Ue(e))
            l.delete(c);
        });
    });
  }
}
function* wr(n, t, e) {
  if (t)
    if (!(Symbol.iterator in t) && t.is("documentSelection") && t.isCollapsed)
      n.schema.checkAttributeInSelection(t, e) && (yield t);
    else
      for (const i of n$(n, t, e))
        yield* i.getItems({
          shallow: !0
        });
}
function n$(n, t, e) {
  return !(Symbol.iterator in t) && (t.is("node") || t.is("$text") || t.is("$textProxy")) ? n.schema.checkAttribute(t, e) ? [
    n.createRangeOn(t)
  ] : [] : n.schema.getValidRanges(n.createSelection(t).getRanges(), e);
}
class s$ extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "HtmlComment";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = /* @__PURE__ */ new Map();
    t.data.processor.skipComments = !1, t.model.schema.addAttributeCheck((i, s) => {
      if (i.endsWith("$root") && s.startsWith("$comment"))
        return !0;
    }), t.conversion.for("upcast").elementToMarker({
      view: "$comment",
      model: (i) => {
        const r = `$comment:${Bt()}`, o = i.getCustomProperty("$rawContent");
        return e.set(r, o), r;
      }
    }), t.conversion.for("dataDowncast").markerToElement({
      model: "$comment",
      view: (i, { writer: s }) => {
        let r;
        for (const c of this.editor.model.document.getRootNames())
          if (r = this.editor.model.document.getRoot(c), r.hasAttribute(i.markerName))
            break;
        const o = i.markerName, a = r.getAttribute(o), l = s.createUIElement("$comment");
        return s.setCustomProperty("$rawContent", a, l), l;
      }
    }), t.model.document.registerPostFixer((i) => {
      let s = !1;
      const r = t.model.document.differ.getChangedMarkers().filter((o) => o.name.startsWith("$comment:"));
      for (const o of r) {
        const { oldRange: a, newRange: l } = o.data;
        if (!(a && l && a.root == l.root)) {
          if (a) {
            const c = a.root;
            c.hasAttribute(o.name) && (i.removeAttribute(o.name, c), s = !0);
          }
          if (l) {
            const c = l.root;
            c.rootName == "$graveyard" ? (i.removeMarker(o.name), s = !0) : c.hasAttribute(o.name) || (i.setAttribute(o.name, e.get(o.name) || "", c), s = !0);
          }
        }
      }
      return s;
    }), t.data.on("set", () => {
      for (const i of t.model.markers.getMarkersGroup("$comment"))
        this.removeHtmlComment(i.name);
    }, {
      priority: "high"
    }), t.model.on("deleteContent", (i, [s]) => {
      for (const r of s.getRanges()) {
        const o = t.model.schema.getLimitElement(r), a = t.model.createPositionAt(o, 0), l = t.model.createPositionAt(o, "end");
        let c;
        a.isTouching(r.start) && l.isTouching(r.end) ? c = this.getHtmlCommentsInRange(t.model.createRange(a, l)) : c = this.getHtmlCommentsInRange(r, {
          skipBoundaries: !0
        });
        for (const u of c)
          this.removeHtmlComment(u);
      }
    }, {
      priority: "high"
    });
  }
  /**
  * Creates an HTML comment on the specified position and returns its ID.
  *
  * *Note*: If two comments are created at the same position, the second comment will be inserted before the first one.
  *
  * @returns Comment ID. This ID can be later used to e.g. remove the comment from the content.
  */
  createHtmlComment(t, e) {
    const i = Bt(), r = this.editor.model, o = r.document.getRoot(t.root.rootName), a = `$comment:${i}`;
    return r.change((l) => {
      const c = l.createRange(t);
      return l.addMarker(a, {
        usingOperation: !0,
        affectsData: !0,
        range: c
      }), l.setAttribute(a, e, o), a;
    });
  }
  /**
  * Removes an HTML comment with the given comment ID.
  *
  * It does nothing and returns `false` if the comment with the given ID does not exist.
  * Otherwise it removes the comment and returns `true`.
  *
  * Note that a comment can be removed also by removing the content around the comment.
  *
  * @param commentID The ID of the comment to be removed.
  * @returns `true` when the comment with the given ID was removed, `false` otherwise.
  */
  removeHtmlComment(t) {
    const e = this.editor, i = e.model.markers.get(t);
    return i ? (e.model.change((s) => {
      s.removeMarker(i);
    }), !0) : !1;
  }
  /**
  * Gets the HTML comment data for the comment with a given ID.
  *
  * Returns `null` if the comment does not exist.
  */
  getHtmlCommentData(t) {
    const i = this.editor.model.markers.get(t);
    if (!i)
      return null;
    let s = "";
    for (const r of this.editor.model.document.getRoots())
      if (r.hasAttribute(t)) {
        s = r.getAttribute(t);
        break;
      }
    return {
      content: s,
      position: i.getStart()
    };
  }
  /**
  * Gets all HTML comments in the given range.
  *
  * By default, it includes comments at the range boundaries.
  *
  * @param range
  * @param options.skipBoundaries When set to `true` the range boundaries will be skipped.
  * @returns HTML comment IDs
  */
  getHtmlCommentsInRange(t, { skipBoundaries: e = !1 } = {}) {
    const i = !e;
    return Array.from(this.editor.model.markers.getMarkersGroup("$comment")).filter((r) => s(r, t)).map((r) => r.name);
    function s(r, o) {
      const a = r.getRange().start;
      return (a.isAfter(o.start) || i && a.isEqual(o.start)) && (a.isBefore(o.end) || i && a.isEqual(o.end));
    }
  }
}
class r$ extends Of {
  /**
  * @inheritDoc
  */
  toView(t) {
    if (!/<(?:html|body|head|meta)(?:\s[^>]*)?>/i.test(t.trim().slice(0, 1e4)))
      return super.toView(t);
    let e = "", i = "";
    t = t.trim().replace(/<\?xml\s[^?]*\?>/i, (a) => (i = a, "")), t = t.trim().replace(/^<!DOCTYPE\s[^>]*?>/i, (a) => (e = a, ""));
    const s = this._toDom(t), r = this.domConverter.domToView(s, {
      skipComments: this.skipComments
    }), o = new Pi(r.document);
    return o.setCustomProperty("$fullPageDocument", s.ownerDocument.documentElement.outerHTML, r), e && o.setCustomProperty("$fullPageDocType", e, r), i && o.setCustomProperty("$fullPageXmlDeclaration", i, r), r;
  }
  /**
  * @inheritDoc
  */
  toData(t) {
    let e = super.toData(t);
    const i = t.getCustomProperty("$fullPageDocument"), s = t.getCustomProperty("$fullPageDocType"), r = t.getCustomProperty("$fullPageXmlDeclaration");
    return i && (e = i.replace(/<\/body\s*>/, e + "$&"), s && (e = s + `
` + e), r && (e = r + `
` + e)), e;
  }
}
class o$ extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "FullPage";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = [
      "$fullPageDocument",
      "$fullPageDocType",
      "$fullPageXmlDeclaration"
    ];
    t.data.processor = new r$(t.data.viewDocument), t.model.schema.extend("$root", {
      allowAttributes: e
    }), t.data.on("toModel", (i, [s]) => {
      const r = t.model.document.getRoot();
      t.model.change((o) => {
        for (const a of e) {
          const l = s.getCustomProperty(a);
          l && o.setAttribute(a, l, r);
        }
      });
    }, {
      priority: "low"
    }), t.data.on("toView", (i, [s]) => {
      if (!s.is("rootElement"))
        return;
      const r = s, o = i.return;
      if (!r.hasAttribute("$fullPageDocument"))
        return;
      const a = new Pi(o.document);
      for (const l of e) {
        const c = r.getAttribute(l);
        c && a.setCustomProperty(l, c, o);
      }
    }, {
      priority: "low"
    }), t.data.on("set", () => {
      const i = t.model.document.getRoot();
      t.model.change((s) => {
        for (const r of e)
          i.hasAttribute(r) && s.removeAttribute(r, i);
      });
    }, {
      priority: "high"
    }), t.data.on("get", (i, s) => {
      s[0] || (s[0] = {}), s[0].trim = !1;
    }, {
      priority: "high"
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function a$(n) {
  return n.createContainerElement("span", {
    class: "image-inline"
  }, n.createEmptyElement("img"));
}
function I1(n) {
  return n.createContainerElement("figure", {
    class: "image"
  }, [
    n.createEmptyElement("img"),
    n.createSlot("children")
  ]);
}
function Ek(n, t) {
  const e = n.plugins.get("ImageUtils"), i = n.plugins.has("ImageInlineEditing") && n.plugins.has("ImageBlockEditing");
  return (r) => e.isInlineImageView(r) ? i && (r.getStyle("display") == "block" || r.findAncestor(e.isBlockImageView) ? "imageBlock" : "imageInline") !== t ? null : s(r) : null;
  function s(r) {
    const o = {
      name: !0
    };
    return r.hasAttribute("src") && (o.attributes = [
      "src"
    ]), o;
  }
}
function im(n, t) {
  const e = Xe(t.getSelectedBlocks());
  return !e || n.isObject(e) || e.isEmpty && e.name != "listItem" ? "imageBlock" : "imageInline";
}
function uc(n) {
  return n && n.endsWith("px") ? parseInt(n) : null;
}
function dc(n) {
  const t = uc(n.getStyle("width")), e = uc(n.getStyle("height"));
  return !!(t && e);
}
const l$ = /^(image|image-inline)$/;
class jt extends I {
  constructor() {
    super(...arguments);
    /**
    * DOM Emitter.
    */
    f(this, "_domEmitter", new (li())());
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Checks if the provided model element is an `image` or `imageInline`.
  */
  isImage(e) {
    return this.isInlineImage(e) || this.isBlockImage(e);
  }
  /**
  * Checks if the provided view element represents an inline image.
  *
  * Also, see {@link module:image/imageutils~ImageUtils#isImageWidget}.
  */
  isInlineImageView(e) {
    return !!e && e.is("element", "img");
  }
  /**
  * Checks if the provided view element represents a block image.
  *
  * Also, see {@link module:image/imageutils~ImageUtils#isImageWidget}.
  */
  isBlockImageView(e) {
    return !!e && e.is("element", "figure") && e.hasClass("image");
  }
  /**
  * Handles inserting single file. This method unifies image insertion using {@link module:widget/utils~findOptimalInsertionRange}
  * method.
  *
  * ```ts
  * const imageUtils = editor.plugins.get( 'ImageUtils' );
  *
  * imageUtils.insertImage( { src: 'path/to/image.jpg' } );
  * ```
  *
  * @param attributes Attributes of the inserted image.
  * This method filters out the attributes which are disallowed by the {@link module:engine/model/schema~Schema}.
  * @param selectable Place to insert the image. If not specified,
  * the {@link module:widget/utils~findOptimalInsertionRange} logic will be applied for the block images
  * and `model.document.selection` for the inline images.
  *
  * **Note**: If `selectable` is passed, this helper will not be able to set selection attributes (such as `linkHref`)
  * and apply them to the new image. In this case, make sure all selection attributes are passed in `attributes`.
  *
  * @param imageType Image type of inserted image. If not specified,
  * it will be determined automatically depending of editor config or place of the insertion.
  * @param options.setImageSizes Specifies whether the image `width` and `height` attributes should be set automatically.
  * The default is `true`.
  * @return The inserted model image element.
  */
  insertImage(e = {}, i = null, s = null, r = {}) {
    const o = this.editor, a = o.model, l = a.document.selection, c = Sk(o, i || l, s);
    e = {
      ...Object.fromEntries(l.getAttributes()),
      ...e
    };
    for (const u in e)
      a.schema.checkAttribute(c, u) || delete e[u];
    return a.change((u) => {
      const { setImageSizes: d = !0 } = r, h = u.createElement(c, e);
      return a.insertObject(h, i, null, {
        setSelection: "on",
        // If we want to insert a block image (for whatever reason) then we don't want to split text blocks.
        // This applies only when we don't have the selectable specified (i.e., we insert multiple block images at once).
        findOptimalPosition: !i && c != "imageInline" ? "auto" : void 0
      }), h.parent ? (d && this.setImageNaturalSizeAttributes(h), h) : null;
    });
  }
  /**
  * Reads original image sizes and sets them as `width` and `height`.
  *
  * The `src` attribute may not be available if the user is using an upload adapter. In such a case,
  * this method is called again after the upload process is complete and the `src` attribute is available.
  */
  setImageNaturalSizeAttributes(e) {
    const i = e.getAttribute("src");
    i && (e.getAttribute("width") || e.getAttribute("height") || this.editor.model.change((s) => {
      const r = new ae.window.Image();
      this._domEmitter.listenTo(r, "load", () => {
        !e.getAttribute("width") && !e.getAttribute("height") && this.editor.model.enqueueChange(s.batch, (o) => {
          o.setAttribute("width", r.naturalWidth, e), o.setAttribute("height", r.naturalHeight, e);
        }), this._domEmitter.stopListening(r, "load");
      }), r.src = i;
    }));
  }
  /**
  * Returns an image widget editing view element if one is selected or is among the selection's ancestors.
  */
  getClosestSelectedImageWidget(e) {
    const i = e.getFirstPosition();
    if (!i)
      return null;
    const s = e.getSelectedElement();
    if (s && this.isImageWidget(s))
      return s;
    let r = i.parent;
    for (; r; ) {
      if (r.is("element") && this.isImageWidget(r))
        return r;
      r = r.parent;
    }
    return null;
  }
  /**
  * Returns a image model element if one is selected or is among the selection's ancestors.
  */
  getClosestSelectedImageElement(e) {
    const i = e.getSelectedElement();
    return this.isImage(i) ? i : e.getFirstPosition().findAncestor("imageBlock");
  }
  /**
  * Returns an image widget editing view based on the passed image view.
  */
  getImageWidgetFromImageView(e) {
    return e.findAncestor({
      classes: l$
    });
  }
  /**
  * Checks if image can be inserted at current model selection.
  *
  * @internal
  */
  isImageAllowed() {
    const i = this.editor.model.document.selection;
    return c$(this.editor, i) && u$(i);
  }
  /**
  * Converts a given {@link module:engine/view/element~Element} to an image widget:
  * * Adds a {@link module:engine/view/element~Element#_setCustomProperty custom property} allowing to recognize the image widget
  * element.
  * * Calls the {@link module:widget/utils~toWidget} function with the proper element's label creator.
  *
  * @param writer An instance of the view writer.
  * @param label The element's label. It will be concatenated with the image `alt` attribute if one is present.
  */
  toImageWidget(e, i, s) {
    return i.setCustomProperty("image", !0, e), Qn(e, i, {
      label: () => {
        const a = this.findViewImgElement(e).getAttribute("alt");
        return a ? `${a} ${s}` : s;
      }
    });
  }
  /**
  * Checks if a given view element is an image widget.
  */
  isImageWidget(e) {
    return !!e.getCustomProperty("image") && vt(e);
  }
  /**
  * Checks if the provided model element is an `image`.
  */
  isBlockImage(e) {
    return !!e && e.is("element", "imageBlock");
  }
  /**
  * Checks if the provided model element is an `imageInline`.
  */
  isInlineImage(e) {
    return !!e && e.is("element", "imageInline");
  }
  /**
  * Get the view `<img>` from another view element, e.g. a widget (`<figure class="image">`), a link (`<a>`).
  *
  * The `<img>` can be located deep in other elements, so this helper performs a deep tree search.
  */
  findViewImgElement(e) {
    if (this.isInlineImageView(e))
      return e;
    const i = this.editor.editing.view;
    for (const { item: s } of i.createRangeIn(e))
      if (this.isInlineImageView(s))
        return s;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    return this._domEmitter.stopListening(), super.destroy();
  }
}
function c$(n, t) {
  if (Sk(n, t, null) == "imageBlock") {
    const i = d$(t, n.model);
    if (n.model.schema.checkChild(i, "imageBlock"))
      return !0;
  } else if (n.model.schema.checkChild(t.focus, "imageInline"))
    return !0;
  return !1;
}
function u$(n) {
  return [
    ...n.focus.getAncestors()
  ].every((t) => !t.is("element", "imageBlock"));
}
function d$(n, t) {
  const i = Da(n, t).start.parent;
  return i.isEmpty && !i.is("element", "$root") ? i.parent : i;
}
function Sk(n, t, e) {
  const i = n.model.schema, s = n.config.get("image.insert.type");
  return n.plugins.has("ImageBlockEditing") ? n.plugins.has("ImageInlineEditing") ? e || (s === "inline" ? "imageInline" : s !== "auto" ? "imageBlock" : t.is("selection") ? im(i, t) : i.checkChild(t, "imageInline") ? "imageInline" : "imageBlock") : "imageBlock" : "imageInline";
}
const h$ = new RegExp(String(/^(http(s)?:\/\/)?[\w-]+\.[\w.~:/[\]@!$&'()*+,;=%-]+/.source + /\.(jpg|jpeg|png|gif|ico|webp|JPG|JPEG|PNG|GIF|ICO|WEBP)/.source + /(\?[\w.~:/[\]@!$&'()*+,;=%-]*)?/.source + /(#[\w.~:/[\]@!$&'()*+,;=%-]*)?$/.source));
class f$ extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The paste–to–embed `setTimeout` ID. Stored as a property to allow
    * cleaning of the timeout.
    */
    f(this, "_timeoutId");
    /**
    * The position where the `<imageBlock>` element will be inserted after the timeout,
    * determined each time a new content is pasted into the document.
    */
    f(this, "_positionToInsert");
    this._timeoutId = null, this._positionToInsert = null;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Jf,
      jt,
      Qf,
      Cn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AutoImage";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model.document, s = e.plugins.get("ClipboardPipeline");
    this.listenTo(s, "inputTransformation", () => {
      const r = i.selection.getFirstRange(), o = At.fromPosition(r.start);
      o.stickiness = "toPrevious";
      const a = At.fromPosition(r.end);
      a.stickiness = "toNext", i.once("change:data", () => {
        this._embedImageBetweenPositions(o, a), o.detach(), a.detach();
      }, {
        priority: "high"
      });
    }), e.commands.get("undo").on("execute", () => {
      this._timeoutId && (ae.window.clearTimeout(this._timeoutId), this._positionToInsert.detach(), this._timeoutId = null, this._positionToInsert = null);
    }, {
      priority: "high"
    });
  }
  /**
  * Analyzes the part of the document between provided positions in search for a URL representing an image.
  * When the URL is found, it is automatically converted into an image.
  *
  * @param leftPosition Left position of the selection.
  * @param rightPosition Right position of the selection.
  */
  _embedImageBetweenPositions(e, i) {
    const s = this.editor, r = new ni(e, i), o = r.getWalker({
      ignoreElementEnd: !0
    }), a = Object.fromEntries(s.model.document.selection.getAttributes()), l = this.editor.plugins.get("ImageUtils");
    let c = "";
    for (const u of o)
      u.item.is("$textProxy") && (c += u.item.data);
    if (c = c.trim(), !c.match(h$)) {
      r.detach();
      return;
    }
    this._positionToInsert = At.fromPosition(e), this._timeoutId = setTimeout(() => {
      if (!s.commands.get("insertImage").isEnabled) {
        r.detach();
        return;
      }
      s.model.change((h) => {
        this._timeoutId = null, h.remove(r), r.detach();
        let m;
        this._positionToInsert.root.rootName !== "$graveyard" && (m = this._positionToInsert.toPosition()), l.insertImage({
          ...a,
          src: c
        }, m), this._positionToInsert.detach(), this._positionToInsert = null;
      }), s.plugins.get("Delete").requestUndoOnBackspace();
    }, 100);
  }
}
class m$ extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const i = this.editor.plugins.get("ImageUtils").getClosestSelectedImageElement(this.editor.model.document.selection);
    this.isEnabled = !!i, this.isEnabled && i.hasAttribute("alt") ? this.value = i.getAttribute("alt") : this.value = !1;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options
  * @param options.newValue The new value of the `alt` attribute to set.
  */
  execute(t) {
    const e = this.editor, i = e.plugins.get("ImageUtils"), s = e.model, r = i.getClosestSelectedImageElement(s.document.selection);
    s.change((o) => {
      o.setAttribute("alt", t.newValue, r);
    });
  }
}
class g$ extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageTextAlternativeEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.commands.add("imageTextAlternative", new m$(this.editor));
  }
}
class p$ extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Tracks information about the DOM focus in the form.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * An input with a label.
    */
    f(this, "labeledInput");
    /**
    * A button used to submit the form.
    */
    f(this, "saveButtonView");
    /**
    * A button used to cancel the form.
    */
    f(this, "cancelButtonView");
    /**
    * A collection of views which can be focused in the form.
    */
    f(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    f(this, "_focusCycler");
    const i = this.locale.t;
    this.focusTracker = new ze(), this.keystrokes = new Ze(), this.labeledInput = this._createLabeledInputView(), this.saveButtonView = this._createButton(i("Save"), Z.check, "ck-button-save"), this.saveButtonView.type = "submit", this.cancelButtonView = this._createButton(i("Cancel"), Z.cancel, "ck-button-cancel", "cancel"), this._focusables = new Et(), this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-text-alternative-form",
          "ck-responsive-form"
        ],
        // https://github.com/ckeditor/ckeditor5-image/issues/40
        tabindex: "-1"
      },
      children: [
        this.labeledInput,
        this.saveButtonView,
        this.cancelButtonView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.listenTo(this.element), Tn({
      view: this
    }), [
      this.labeledInput,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((e) => {
      this._focusables.add(e), this.focusTracker.add(e.element);
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates the button view.
  *
  * @param label The button label
  * @param icon The button's icon.
  * @param className The additional button CSS class name.
  * @param eventName The event name that the ButtonView#execute event will be delegated to.
  * @returns The button view instance.
  */
  _createButton(e, i, s, r) {
    const o = new le(this.locale);
    return o.set({
      label: e,
      icon: i,
      tooltip: !0
    }), o.extendTemplate({
      attributes: {
        class: s
      }
    }), r && o.delegate("execute").to(this, r), o;
  }
  /**
  * Creates an input with a label.
  *
  * @returns Labeled field view instance.
  */
  _createLabeledInputView() {
    const e = this.locale.t, i = new gt(this.locale, pi);
    return i.label = e("Text alternative"), i;
  }
}
function b$(n) {
  const t = n.plugins.get("ContextualBalloon");
  if (n.plugins.get("ImageUtils").getClosestSelectedImageWidget(n.editing.view.document.selection)) {
    const i = nm(n);
    t.updatePosition(i);
  }
}
function nm(n) {
  const t = n.editing.view, e = Dt.defaultPositions, i = n.plugins.get("ImageUtils");
  return {
    target: t.domConverter.mapViewToDom(i.getClosestSelectedImageWidget(t.document.selection)),
    positions: [
      e.northArrowSouth,
      e.northArrowSouthWest,
      e.northArrowSouthEast,
      e.southArrowNorth,
      e.southArrowNorthWest,
      e.southArrowNorthEast,
      e.viewportStickyNorth
    ]
  };
}
class w$ extends I {
  constructor() {
    super(...arguments);
    /**
    * The contextual balloon plugin instance.
    */
    f(this, "_balloon");
    /**
    * A form containing a textarea and buttons, used to change the `alt` text value.
    */
    f(this, "_form");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ai
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageTextAlternativeUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this._createButton();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._form && this._form.destroy();
  }
  /**
  * Creates a button showing the balloon panel for changing the image text alternative and
  * registers it in the editor {@link module:ui/componentfactory~ComponentFactory ComponentFactory}.
  */
  _createButton() {
    const e = this.editor, i = e.t;
    e.ui.componentFactory.add("imageTextAlternative", (s) => {
      const r = e.commands.get("imageTextAlternative"), o = new le(s);
      return o.set({
        label: i("Change image text alternative"),
        icon: Z.textAlternative,
        tooltip: !0
      }), o.bind("isEnabled").to(r, "isEnabled"), o.bind("isOn").to(r, "value", (a) => !!a), this.listenTo(o, "execute", () => {
        this._showForm();
      }), o;
    });
  }
  /**
  * Creates the {@link module:image/imagetextalternative/ui/textalternativeformview~TextAlternativeFormView}
  * form.
  */
  _createForm() {
    const e = this.editor, s = e.editing.view.document, r = e.plugins.get("ImageUtils");
    this._balloon = this.editor.plugins.get("ContextualBalloon"), this._form = new (no(p$))(e.locale), this._form.render(), this.listenTo(this._form, "submit", () => {
      e.execute("imageTextAlternative", {
        newValue: this._form.labeledInput.fieldView.element.value
      }), this._hideForm(!0);
    }), this.listenTo(this._form, "cancel", () => {
      this._hideForm(!0);
    }), this._form.keystrokes.set("Esc", (o, a) => {
      this._hideForm(!0), a();
    }), this.listenTo(e.ui, "update", () => {
      r.getClosestSelectedImageWidget(s.selection) ? this._isVisible && b$(e) : this._hideForm(!0);
    }), Xn({
      emitter: this._form,
      activator: () => this._isVisible,
      contextElements: () => [
        this._balloon.view.element
      ],
      callback: () => this._hideForm()
    });
  }
  /**
  * Shows the {@link #_form} in the {@link #_balloon}.
  */
  _showForm() {
    if (this._isVisible)
      return;
    this._form || this._createForm();
    const e = this.editor, i = e.commands.get("imageTextAlternative"), s = this._form.labeledInput;
    this._form.disableCssTransitions(), this._isInBalloon || this._balloon.add({
      view: this._form,
      position: nm(e)
    }), s.fieldView.value = s.fieldView.element.value = i.value || "", this._form.labeledInput.fieldView.select(), this._form.enableCssTransitions();
  }
  /**
  * Removes the {@link #_form} from the {@link #_balloon}.
  *
  * @param focusEditable Controls whether the editing view is focused afterwards.
  */
  _hideForm(e = !1) {
    this._isInBalloon && (this._form.focusTracker.isFocused && this._form.saveButtonView.focus(), this._balloon.remove(this._form), e && this.editor.editing.view.focus());
  }
  /**
  * Returns `true` when the {@link #_form} is the visible view in the {@link #_balloon}.
  */
  get _isVisible() {
    return !!this._balloon && this._balloon.visibleView === this._form;
  }
  /**
  * Returns `true` when the {@link #_form} is in the {@link #_balloon}.
  */
  get _isInBalloon() {
    return !!this._balloon && this._balloon.hasView(this._form);
  }
}
class sm extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      g$,
      w$
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageTextAlternative";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
function v$(n) {
  const t = (e, i, s) => {
    if (!s.consumable.test(i.viewItem, {
      name: !0,
      classes: "image"
    }))
      return;
    const r = n.findViewImgElement(i.viewItem);
    if (!r || !s.consumable.test(r, {
      name: !0
    }))
      return;
    s.consumable.consume(i.viewItem, {
      name: !0,
      classes: "image"
    });
    const o = s.convertItem(r, i.modelCursor), a = Xe(o.modelRange.getItems());
    if (!a) {
      s.consumable.revert(i.viewItem, {
        name: !0,
        classes: "image"
      });
      return;
    }
    s.convertChildren(i.viewItem, a), s.updateConversionResult(a, i);
  };
  return (e) => {
    e.on("element:figure", t);
  };
}
function _$(n) {
  const t = [
    "srcset",
    "media",
    "type",
    "sizes"
  ], e = (i, s, r) => {
    const o = s.viewItem;
    if (!r.consumable.test(o, {
      name: !0
    }))
      return;
    const a = /* @__PURE__ */ new Map();
    for (const u of o.getChildren())
      if (u.is("element", "source")) {
        const d = {};
        for (const h of t)
          u.hasAttribute(h) && r.consumable.test(u, {
            attributes: h
          }) && (d[h] = u.getAttribute(h));
        Object.keys(d).length && a.set(u, d);
      }
    const l = n.findViewImgElement(o);
    if (!l)
      return;
    let c = s.modelCursor.parent;
    if (!c.is("element", "imageBlock")) {
      const u = r.convertItem(l, s.modelCursor);
      s.modelRange = u.modelRange, s.modelCursor = u.modelCursor, c = Xe(u.modelRange.getItems());
    }
    r.consumable.consume(o, {
      name: !0
    });
    for (const [u, d] of a)
      r.consumable.consume(u, {
        attributes: Object.keys(d)
      });
    a.size && r.writer.setAttribute("sources", Array.from(a.values()), c), r.convertChildren(o, c);
  };
  return (i) => {
    i.on("element:picture", e);
  };
}
function Ak(n, t) {
  const e = (i, s, r) => {
    if (!r.consumable.consume(s.item, i.name))
      return;
    const o = r.writer, a = r.mapper.toViewElement(s.item), l = n.findViewImgElement(a);
    s.attributeNewValue === null ? (o.removeAttribute("srcset", l), o.removeAttribute("sizes", l)) : s.attributeNewValue && (o.setAttribute("srcset", s.attributeNewValue, l), o.setAttribute("sizes", "100vw", l));
  };
  return (i) => {
    i.on(`attribute:srcset:${t}`, e);
  };
}
function y$(n) {
  const t = (e, i, s) => {
    if (!s.consumable.consume(i.item, e.name))
      return;
    const r = s.writer, o = s.mapper.toViewElement(i.item), a = n.findViewImgElement(o), l = i.attributeNewValue;
    if (l && l.length) {
      const c = [];
      let u = a.parent;
      for (; u && u.is("attributeElement"); ) {
        const m = u.parent;
        r.unwrap(r.createRangeOn(a), u), c.unshift(u), u = m;
      }
      const d = a.parent.is("element", "picture"), h = d ? a.parent : r.createContainerElement("picture", null);
      d || r.insert(r.createPositionBefore(a), h), r.remove(r.createRangeIn(h)), r.insert(r.createPositionAt(h, "end"), l.map((m) => r.createEmptyElement("source", m))), r.move(r.createRangeOn(a), r.createPositionAt(h, "end"));
      for (const m of c)
        r.wrap(r.createRangeOn(h), m);
    } else if (a.parent.is("element", "picture")) {
      const c = a.parent;
      r.move(r.createRangeOn(a), r.createPositionBefore(c)), r.remove(c);
    }
  };
  return (e) => {
    e.on("attribute:sources:imageBlock", t), e.on("attribute:sources:imageInline", t);
  };
}
function hc(n, t, e) {
  const i = (s, r, o) => {
    if (!o.consumable.consume(r.item, s.name))
      return;
    const a = o.writer, l = o.mapper.toViewElement(r.item), c = n.findViewImgElement(l);
    a.setAttribute(r.attributeKey, r.attributeNewValue || "", c);
  };
  return (s) => {
    s.on(`attribute:${e}:${t}`, i);
  };
}
class rm extends sn {
  /**
  * @inheritDoc
  */
  observe(t) {
    this.listenTo(t, "load", (e, i) => {
      const s = i.target;
      this.checkShouldIgnoreEventFromTarget(s) || s.tagName == "IMG" && this._fireEvents(i);
    }, {
      useCapture: !0
    });
  }
  /**
  * @inheritDoc
  */
  stopObserving(t) {
    this.stopListening(t);
  }
  /**
  * Fires {@link module:engine/view/document~Document#event:layoutChanged} and
  * {@link module:engine/view/document~Document#event:imageLoaded}
  * if observer {@link #isEnabled is enabled}.
  *
  * @param domEvent The DOM event.
  */
  _fireEvents(t) {
    this.isEnabled && (this.document.fire("layoutChanged"), this.document.fire("imageLoaded", t));
  }
}
class k$ extends he {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t);
    const e = t.config.get("image.insert.type");
    t.plugins.has("ImageBlockEditing") || e === "block" && Ve("image-block-plugin-required"), t.plugins.has("ImageInlineEditing") || e === "inline" && Ve("image-inline-plugin-required");
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get("ImageUtils");
    this.isEnabled = t.isImageAllowed();
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options Options for the executed command.
  * @param options.imageType The type of the image to insert. If not specified, the type will be determined automatically.
  * @param options.source The image source or an array of image sources to insert.
  * @param options.breakBlock If set to `true`, the block at the selection start will be broken before inserting the image.
  * See the documentation of the command to learn more about accepted formats.
  */
  execute(t) {
    const e = Ue(t.source), i = this.editor.model.document.selection, s = this.editor.plugins.get("ImageUtils"), r = Object.fromEntries(i.getAttributes());
    e.forEach((o, a) => {
      const l = i.getSelectedElement();
      if (typeof o == "string" && (o = {
        src: o
      }), a && l && s.isImage(l)) {
        const c = this.editor.model.createPositionAfter(l);
        s.insertImage({
          ...o,
          ...r
        }, c, t.imageType);
      } else t.breakBlock ? s.insertImage({
        ...o,
        ...r
      }, i.getFirstPosition(), t.imageType) : s.insertImage({
        ...o,
        ...r
      }, null, t.imageType);
    });
  }
}
class C$ extends he {
  constructor(t) {
    super(t), this.decorate("cleanupImage");
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.plugins.get("ImageUtils"), i = this.editor.model.document.selection.getSelectedElement();
    this.isEnabled = e.isImage(i), this.value = this.isEnabled ? i.getAttribute("src") : null;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options Options for the executed command.
  * @param options.source The image source to replace.
  */
  execute(t) {
    const e = this.editor.model.document.selection.getSelectedElement(), i = this.editor.plugins.get("ImageUtils");
    this.editor.model.change((s) => {
      s.setAttribute("src", t.source, e), this.cleanupImage(s, e), i.setImageNaturalSizeAttributes(e);
    });
  }
  /**
  * Cleanup image attributes that are not relevant to the new source.
  *
  * Removed attributes are: 'srcset', 'sizes', 'sources', 'width', 'height', 'alt'.
  *
  * This method is decorated, to allow custom cleanup logic.
  * For example, to remove 'myImageId' attribute after 'src' has changed:
  *
  * ```ts
  * replaceImageSourceCommand.on( 'cleanupImage', ( eventInfo, [ writer, image ] ) => {
  * 	writer.removeAttribute( 'myImageId', image );
  * } );
  * ```
  */
  cleanupImage(t, e) {
    t.removeAttribute("srcset", e), t.removeAttribute("sizes", e), t.removeAttribute("sources", e), t.removeAttribute("width", e), t.removeAttribute("height", e), t.removeAttribute("alt", e);
  }
}
class om extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.conversion;
    t.editing.view.addObserver(rm), e.for("upcast").attributeToAttribute({
      view: {
        name: "img",
        key: "alt"
      },
      model: "alt"
    }).attributeToAttribute({
      view: {
        name: "img",
        key: "srcset"
      },
      model: "srcset"
    });
    const i = new k$(t), s = new C$(t);
    t.commands.add("insertImage", i), t.commands.add("replaceImageSource", s), t.commands.add("imageInsert", i);
  }
}
class Ik extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageSizeAttributes";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._registerSchema(), this._registerConverters("imageBlock"), this._registerConverters("imageInline");
  }
  /**
  * Registers the `width` and `height` attributes for inline and block images.
  */
  _registerSchema() {
    this.editor.plugins.has("ImageBlockEditing") && this.editor.model.schema.extend("imageBlock", {
      allowAttributes: [
        "width",
        "height"
      ]
    }), this.editor.plugins.has("ImageInlineEditing") && this.editor.model.schema.extend("imageInline", {
      allowAttributes: [
        "width",
        "height"
      ]
    });
  }
  /**
  * Registers converters for `width` and `height` attributes.
  */
  _registerConverters(t) {
    const e = this.editor, i = e.plugins.get("ImageUtils"), s = t === "imageBlock" ? "figure" : "img";
    e.conversion.for("upcast").attributeToAttribute({
      view: {
        name: s,
        styles: {
          width: /.+/
        }
      },
      model: {
        key: "width",
        value: (o) => dc(o) ? uc(o.getStyle("width")) : null
      }
    }).attributeToAttribute({
      view: {
        name: s,
        key: "width"
      },
      model: "width"
    }).attributeToAttribute({
      view: {
        name: s,
        styles: {
          height: /.+/
        }
      },
      model: {
        key: "height",
        value: (o) => dc(o) ? uc(o.getStyle("height")) : null
      }
    }).attributeToAttribute({
      view: {
        name: s,
        key: "height"
      },
      model: "height"
    }), e.conversion.for("editingDowncast").add((o) => {
      r(o, "width", "width", !0, !0), r(o, "height", "height", !0, !0);
    }), e.conversion.for("dataDowncast").add((o) => {
      r(o, "width", "width", !1), r(o, "height", "height", !1);
    });
    function r(o, a, l, c, u = !1) {
      o.on(`attribute:${a}:${t}`, (d, h, m) => {
        if (!m.consumable.consume(h.item, d.name))
          return;
        const g = m.writer, p = m.mapper.toViewElement(h.item), w = i.findViewImgElement(p);
        h.attributeNewValue !== null ? g.setAttribute(l, h.attributeNewValue, w) : g.removeAttribute(l, w);
        const y = h.item.getAttribute("width"), k = h.item.getAttribute("height"), C = y && k;
        if (C && u && g.setAttribute("loading", "lazy", w), h.item.hasAttribute("sources"))
          return;
        const E = h.item.hasAttribute("resizedWidth");
        t === "imageInline" && !E && !c || C && g.setStyle("aspect-ratio", `${y}/${k}`, w);
      });
    }
  }
}
class Pk extends he {
  /**
  * @inheritDoc
  *
  * @param modelElementName Model element name the command converts to.
  */
  constructor(e, i) {
    super(e);
    /**
    * Model element name the command converts to.
    */
    f(this, "_modelElementName");
    this._modelElementName = i;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const i = this.editor.plugins.get("ImageUtils"), s = i.getClosestSelectedImageElement(this.editor.model.document.selection);
    this._modelElementName === "imageBlock" ? this.isEnabled = i.isInlineImage(s) : this.isEnabled = i.isBlockImage(s);
  }
  /**
  * Executes the command and changes the type of a selected image.
  *
  * @fires execute
  * @param options.setImageSizes Specifies whether the image `width` and `height` attributes should be set automatically.
  * The default is `true`.
  * @returns An object containing references to old and new model image elements
  * (for before and after the change) so external integrations can hook into the decorated
  * `execute` event and handle this change. `null` if the type change failed.
  */
  execute(e = {}) {
    const i = this.editor, s = this.editor.model, r = i.plugins.get("ImageUtils"), o = r.getClosestSelectedImageElement(s.document.selection), a = Object.fromEntries(o.getAttributes());
    return !a.src && !a.uploadId ? null : s.change((l) => {
      const { setImageSizes: c = !0 } = e, u = Array.from(s.markers).filter((m) => m.getRange().containsItem(o)), d = r.insertImage(a, s.createSelection(o, "on"), this._modelElementName, {
        setImageSizes: c
      });
      if (!d)
        return null;
      const h = l.createRangeOn(d);
      for (const m of u) {
        const g = m.getRange(), p = g.root.rootName != "$graveyard" ? g.getJoined(h, !0) : h;
        l.updateMarker(m, {
          range: p
        });
      }
      return {
        oldElement: o,
        newElement: d
      };
    });
  }
}
class Rk extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImagePlaceholder";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._setupSchema(), this._setupConversion(), this._setupLoadListener();
  }
  /**
  * Extends model schema.
  */
  _setupSchema() {
    const t = this.editor.model.schema;
    t.isRegistered("imageBlock") && t.extend("imageBlock", {
      allowAttributes: [
        "placeholder"
      ]
    }), t.isRegistered("imageInline") && t.extend("imageInline", {
      allowAttributes: [
        "placeholder"
      ]
    });
  }
  /**
  * Registers converters.
  */
  _setupConversion() {
    const t = this.editor, e = t.conversion, i = t.plugins.get("ImageUtils");
    e.for("editingDowncast").add((s) => {
      s.on("attribute:placeholder", (r, o, a) => {
        if (!a.consumable.test(o.item, r.name) || !o.item.is("element", "imageBlock") && !o.item.is("element", "imageInline"))
          return;
        a.consumable.consume(o.item, r.name);
        const l = a.writer, c = a.mapper.toViewElement(o.item), u = i.findViewImgElement(c);
        o.attributeNewValue ? (l.addClass("image_placeholder", u), l.setStyle("background-image", `url(${o.attributeNewValue})`, u), l.setCustomProperty("editingPipeline:doNotReuseOnce", !0, u)) : (l.removeClass("image_placeholder", u), l.removeStyle("background-image", u));
      });
    });
  }
  /**
  * Prepares listener for image load.
  */
  _setupLoadListener() {
    const t = this.editor, e = t.model, i = t.editing, s = i.view, r = t.plugins.get("ImageUtils");
    s.addObserver(rm), this.listenTo(s.document, "imageLoaded", (o, a) => {
      const l = s.domConverter.mapDomToView(a.target);
      if (!l)
        return;
      const c = r.getImageWidgetFromImageView(l);
      if (!c)
        return;
      const u = i.mapper.toModelElement(c);
      !u || !u.hasAttribute("placeholder") || e.enqueueChange({
        isUndoable: !1
      }, (d) => {
        d.removeAttribute("placeholder", u);
      });
    });
  }
}
class Ok extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      om,
      Ik,
      jt,
      Rk,
      _i
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageBlockEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.register("imageBlock", {
      inheritAllFrom: "$blockObject",
      allowAttributes: [
        "alt",
        "src",
        "srcset"
      ]
    }), this._setupConversion(), t.plugins.has("ImageInlineEditing") && (t.commands.add("imageTypeBlock", new Pk(this.editor, "imageBlock")), this._setupClipboardIntegration());
  }
  /**
  * Configures conversion pipelines to support upcasting and downcasting
  * block images (block image widgets) and their attributes.
  */
  _setupConversion() {
    const t = this.editor, e = t.t, i = t.conversion, s = t.plugins.get("ImageUtils");
    i.for("dataDowncast").elementToStructure({
      model: "imageBlock",
      view: (r, { writer: o }) => I1(o)
    }), i.for("editingDowncast").elementToStructure({
      model: "imageBlock",
      view: (r, { writer: o }) => s.toImageWidget(I1(o), o, e("image widget"))
    }), i.for("downcast").add(hc(s, "imageBlock", "src")).add(hc(s, "imageBlock", "alt")).add(Ak(s, "imageBlock")), i.for("upcast").elementToElement({
      view: Ek(t, "imageBlock"),
      model: (r, { writer: o }) => o.createElement("imageBlock", r.hasAttribute("src") ? {
        src: r.getAttribute("src")
      } : void 0)
    }).add(v$(s));
  }
  /**
  * Integrates the plugin with the clipboard pipeline.
  *
  * Idea is that the feature should recognize the user's intent when an **inline** image is
  * pasted or dropped. If such an image is pasted/dropped:
  *
  * * into an empty block (e.g. an empty paragraph),
  * * on another object (e.g. some block widget).
  *
  * it gets converted into a block image on the fly. We assume this is the user's intent
  * if they decided to put their image there.
  *
  * See the `ImageInlineEditing` for the similar integration that works in the opposite direction.
  *
  * The feature also sets image `width` and `height` attributes on paste.
  */
  _setupClipboardIntegration() {
    const t = this.editor, e = t.model, i = t.editing.view, s = t.plugins.get("ImageUtils"), r = t.plugins.get("ClipboardPipeline");
    this.listenTo(r, "inputTransformation", (o, a) => {
      const l = Array.from(a.content.getChildren());
      let c;
      if (!l.every(s.isInlineImageView))
        return;
      a.targetRanges ? c = t.editing.mapper.toModelRange(a.targetRanges[0]) : c = e.document.selection.getFirstRange();
      const u = e.createSelection(c);
      if (im(e.schema, u) === "imageBlock") {
        const d = new Pi(i.document), h = l.map((m) => d.createElement("figure", {
          class: "image"
        }, m));
        a.content = d.createDocumentFragment(h);
      }
    }), this.listenTo(r, "contentInsertion", (o, a) => {
      a.method === "paste" && e.change((l) => {
        const c = l.createRangeIn(a.content);
        for (const u of c.getItems())
          u.is("element", "imageBlock") && s.setImageNaturalSizeAttributes(u);
      });
    });
  }
}
class x$ extends ee {
  /**
  * Creates a view for the dropdown panel of {@link module:image/imageinsert/imageinsertui~ImageInsertUI}.
  *
  * @param locale The localization services instance.
  * @param integrations An integrations object that contains components (or tokens for components) to be shown in the panel view.
  */
  constructor(e, i = []) {
    super(e);
    /**
    * Tracks information about DOM focus in the form.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * A collection of views that can be focused in the form.
    */
    f(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    f(this, "_focusCycler");
    /**
    * A collection of the defined integrations for inserting the images.
    */
    f(this, "children");
    this.focusTracker = new ze(), this.keystrokes = new Ze(), this._focusables = new Et(), this.children = this.createCollection(), this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    });
    for (const s of i)
      this.children.add(s), this._focusables.add(s), s instanceof $f && this._focusables.addMany(s.children);
    this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-image-insert-form"
        ],
        tabindex: -1
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Tn({
      view: this
    });
    for (const i of this._focusables)
      this.focusTracker.add(i.element);
    this.keystrokes.listenTo(this.element);
    const e = (i) => i.stopPropagation();
    this.keystrokes.set("arrowright", e), this.keystrokes.set("arrowleft", e), this.keystrokes.set("arrowup", e), this.keystrokes.set("arrowdown", e);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the first {@link #_focusables focusable} in the form.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
}
class $a extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The dropdown view responsible for displaying the image insert UI.
    */
    f(this, "dropdownView");
    /**
    * Registered integrations map.
    */
    f(this, "_integrations", /* @__PURE__ */ new Map());
    e.config.define("image.insert.integrations", [
      "upload",
      "assetManager",
      "url"
    ]);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInsertUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model.document.selection, s = e.plugins.get("ImageUtils");
    this.set("isImageSelected", !1), this.listenTo(e.model.document, "change", () => {
      this.isImageSelected = s.isImage(i.getSelectedElement());
    });
    const r = (a) => this._createToolbarComponent(a), o = (a) => this._createMenuBarComponent(a);
    e.ui.componentFactory.add("insertImage", r), e.ui.componentFactory.add("imageInsert", r), e.ui.componentFactory.add("menuBar:insertImage", o);
  }
  /**
  * Registers the insert image dropdown integration.
  */
  registerIntegration({ name: e, observable: i, buttonViewCreator: s, formViewCreator: r, menuBarButtonViewCreator: o, requiresForm: a = !1, override: l = !1 }) {
    this._integrations.has(e) && !l && Ve("image-insert-integration-exists", {
      name: e
    }), this._integrations.set(e, {
      observable: i,
      buttonViewCreator: s,
      menuBarButtonViewCreator: o,
      formViewCreator: r,
      requiresForm: a
    });
  }
  /**
  * Creates the toolbar component.
  */
  _createToolbarComponent(e) {
    const i = this.editor, s = e.t, r = this._prepareIntegrations();
    if (!r.length)
      return null;
    let o;
    const a = r[0];
    if (r.length == 1) {
      if (!a.requiresForm)
        return a.buttonViewCreator(!0);
      o = a.buttonViewCreator(!0);
    } else {
      const u = a.buttonViewCreator(!1);
      o = new Ys(e, u), o.tooltip = !0, o.bind("label").to(this, "isImageSelected", (d) => s(d ? "Replace image" : "Insert image"));
    }
    const l = this.dropdownView = Tt(e, o), c = r.map(({ observable: u }) => typeof u == "function" ? u() : u);
    return l.bind("isEnabled").toMany(c, "isEnabled", (...u) => u.some((d) => d)), l.once("change:isOpen", () => {
      const u = r.flatMap(({ formViewCreator: h }) => h(r.length == 1)), d = new x$(i.locale, u);
      l.panelView.children.add(d);
    }), l;
  }
  /**
  * Creates the menu bar component.
  */
  _createMenuBarComponent(e) {
    const i = e.t, s = this._prepareIntegrations();
    if (!s.length)
      return null;
    const r = s.flatMap(({ menuBarButtonViewCreator: l }) => l(s.length == 1)), o = new Yt(e), a = new kn(e);
    o.panelView.children.add(a), o.buttonView.set({
      icon: Z.image,
      label: i("Image")
    });
    for (const l of r) {
      const c = new en(e, o);
      c.children.add(l), a.items.add(c), l.delegate("execute").to(o);
    }
    return o;
  }
  /**
  * Validates the integrations list.
  */
  _prepareIntegrations() {
    const i = this.editor.config.get("image.insert.integrations"), s = [];
    if (!i.length)
      return Ve("image-insert-integrations-not-specified"), s;
    for (const r of i) {
      if (!this._integrations.has(r)) {
        [
          "upload",
          "assetManager",
          "url"
        ].includes(r) || Ve("image-insert-unknown-integration", {
          item: r
        });
        continue;
      }
      s.push(this._integrations.get(r));
    }
    return s.length || Ve("image-insert-integrations-not-registered"), s;
  }
}
class T$ extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Ok,
      Sn,
      sm,
      $a
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageBlock";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class E$ extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      om,
      Ik,
      jt,
      Rk,
      _i
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInlineEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.register("imageInline", {
      inheritAllFrom: "$inlineObject",
      allowAttributes: [
        "alt",
        "src",
        "srcset"
      ],
      // Disallow inline images in captions (at least for now).
      // This is the best spot to do that because independent packages can introduce captions (ImageCaption, TableCaption, etc.).
      disallowIn: [
        "caption"
      ]
    }), this._setupConversion(), t.plugins.has("ImageBlockEditing") && (t.commands.add("imageTypeInline", new Pk(this.editor, "imageInline")), this._setupClipboardIntegration());
  }
  /**
  * Configures conversion pipelines to support upcasting and downcasting
  * inline images (inline image widgets) and their attributes.
  */
  _setupConversion() {
    const t = this.editor, e = t.t, i = t.conversion, s = t.plugins.get("ImageUtils");
    i.for("dataDowncast").elementToElement({
      model: "imageInline",
      view: (r, { writer: o }) => o.createEmptyElement("img")
    }), i.for("editingDowncast").elementToStructure({
      model: "imageInline",
      view: (r, { writer: o }) => s.toImageWidget(a$(o), o, e("image widget"))
    }), i.for("downcast").add(hc(s, "imageInline", "src")).add(hc(s, "imageInline", "alt")).add(Ak(s, "imageInline")), i.for("upcast").elementToElement({
      view: Ek(t, "imageInline"),
      model: (r, { writer: o }) => o.createElement("imageInline", r.hasAttribute("src") ? {
        src: r.getAttribute("src")
      } : void 0)
    });
  }
  /**
  * Integrates the plugin with the clipboard pipeline.
  *
  * Idea is that the feature should recognize the user's intent when an **block** image is
  * pasted or dropped. If such an image is pasted/dropped into a non-empty block
  * (e.g. a paragraph with some text) it gets converted into an inline image on the fly.
  *
  * We assume this is the user's intent if they decided to put their image there.
  *
  * **Note**: If a block image has a caption, it will not be converted to an inline image
  * to avoid the confusion. Captions are added on purpose and they should never be lost
  * in the clipboard pipeline.
  *
  * See the `ImageBlockEditing` for the similar integration that works in the opposite direction.
  *
  * The feature also sets image `width` and `height` attributes when pasting.
  */
  _setupClipboardIntegration() {
    const t = this.editor, e = t.model, i = t.editing.view, s = t.plugins.get("ImageUtils"), r = t.plugins.get("ClipboardPipeline");
    this.listenTo(r, "inputTransformation", (o, a) => {
      const l = Array.from(a.content.getChildren());
      let c;
      if (!l.every(s.isBlockImageView))
        return;
      a.targetRanges ? c = t.editing.mapper.toModelRange(a.targetRanges[0]) : c = e.document.selection.getFirstRange();
      const u = e.createSelection(c);
      if (im(e.schema, u) === "imageInline") {
        const d = new Pi(i.document), h = l.map((m) => m.childCount === 1 ? (Array.from(m.getAttributes()).forEach((g) => d.setAttribute(...g, s.findViewImgElement(m))), m.getChild(0)) : m);
        a.content = d.createDocumentFragment(h);
      }
    }), this.listenTo(r, "contentInsertion", (o, a) => {
      a.method === "paste" && e.change((l) => {
        const c = l.createRangeIn(a.content);
        for (const u of c.getItems())
          u.is("element", "imageInline") && s.setImageNaturalSizeAttributes(u);
      });
    });
  }
}
class S$ extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      E$,
      Sn,
      sm,
      $a
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInline";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class Vk extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCaptionUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jt
    ];
  }
  /**
  * Returns the caption model element from a given image element. Returns `null` if no caption is found.
  */
  getCaptionFromImageModelElement(t) {
    for (const e of t.getChildren())
      if (e && e.is("element", "caption"))
        return e;
    return null;
  }
  /**
  * Returns the caption model element for a model selection. Returns `null` if the selection has no caption element ancestor.
  */
  getCaptionFromModelSelection(t) {
    const e = this.editor.plugins.get("ImageUtils"), i = t.getFirstPosition().findAncestor("caption");
    return i && e.isBlockImage(i.parent) ? i : null;
  }
  /**
  * {@link module:engine/view/matcher~Matcher} pattern. Checks if a given element is a `<figcaption>` element that is placed
  * inside the image `<figure>` element.
  * @returns Returns the object accepted by {@link module:engine/view/matcher~Matcher} or `null` if the element
  * cannot be matched.
  */
  matchImageCaptionViewElement(t) {
    const e = this.editor.plugins.get("ImageUtils");
    return t.name == "figcaption" && e.isBlockImageView(t.parent) ? {
      name: !0
    } : null;
  }
}
class A$ extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor, e = t.plugins.get("ImageCaptionUtils"), i = t.plugins.get("ImageUtils");
    if (!t.plugins.has(Ok)) {
      this.isEnabled = !1, this.value = !1;
      return;
    }
    const s = t.model.document.selection, r = s.getSelectedElement();
    if (!r) {
      const o = e.getCaptionFromModelSelection(s);
      this.isEnabled = !!o, this.value = !!o;
      return;
    }
    this.isEnabled = i.isImage(r), this.isEnabled ? this.value = !!e.getCaptionFromImageModelElement(r) : this.value = !1;
  }
  /**
  * Executes the command.
  *
  * ```ts
  * editor.execute( 'toggleImageCaption' );
  * ```
  *
  * @param options Options for the executed command.
  * @param options.focusCaptionOnShow When true and the caption shows up, the selection will be moved into it straight away.
  * @fires execute
  */
  execute(t = {}) {
    const { focusCaptionOnShow: e } = t;
    this.editor.model.change((i) => {
      this.value ? this._hideImageCaption(i) : this._showImageCaption(i, e);
    });
  }
  /**
  * Shows the caption of the `<imageBlock>` or `<imageInline>`. Also:
  *
  * * it converts `<imageInline>` to `<imageBlock>` to show the caption,
  * * it attempts to restore the caption content from the `ImageCaptionEditing` caption registry,
  * * it moves the selection to the caption right away, it the `focusCaptionOnShow` option was set.
  */
  _showImageCaption(t, e) {
    const s = this.editor.model.document.selection, r = this.editor.plugins.get("ImageCaptionEditing"), o = this.editor.plugins.get("ImageUtils");
    let a = s.getSelectedElement();
    const l = r._getSavedCaption(a);
    o.isInlineImage(a) && (this.editor.execute("imageTypeBlock"), a = s.getSelectedElement());
    const c = l || t.createElement("caption");
    t.append(c, a), e && t.setSelection(c, "in");
  }
  /**
  * Hides the caption of a selected image (or an image caption the selection is anchored to).
  *
  * The content of the caption is stored in the `ImageCaptionEditing` caption registry to make this
  * a reversible action.
  */
  _hideImageCaption(t) {
    const e = this.editor, i = e.model.document.selection, s = e.plugins.get("ImageCaptionEditing"), r = e.plugins.get("ImageCaptionUtils");
    let o = i.getSelectedElement(), a;
    o ? a = r.getCaptionFromImageModelElement(o) : (a = r.getCaptionFromModelSelection(i), o = a.parent), s._saveCaption(o, a), t.setSelection(o, "on"), t.remove(a);
  }
}
class I$ extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A map that keeps saved JSONified image captions and image model elements they are
    * associated with.
    *
    * To learn more about this system, see {@link #_saveCaption}.
    */
    f(this, "_savedCaptionsMap");
    this._savedCaptionsMap = /* @__PURE__ */ new WeakMap();
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jt,
      Vk
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCaptionEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model.schema;
    i.isRegistered("caption") ? i.extend("caption", {
      allowIn: "imageBlock"
    }) : i.register("caption", {
      allowIn: "imageBlock",
      allowContentOf: "$block",
      isLimit: !0
    }), e.commands.add("toggleImageCaption", new A$(this.editor)), this._setupConversion(), this._setupImageTypeCommandsIntegration(), this._registerCaptionReconversion();
  }
  /**
  * Configures conversion pipelines to support upcasting and downcasting
  * image captions.
  */
  _setupConversion() {
    const e = this.editor, i = e.editing.view, s = e.plugins.get("ImageUtils"), r = e.plugins.get("ImageCaptionUtils"), o = e.t;
    e.conversion.for("upcast").elementToElement({
      view: (a) => r.matchImageCaptionViewElement(a),
      model: "caption"
    }), e.conversion.for("dataDowncast").elementToElement({
      model: "caption",
      view: (a, { writer: l }) => s.isBlockImage(a.parent) ? l.createContainerElement("figcaption") : null
    }), e.conversion.for("editingDowncast").elementToElement({
      model: "caption",
      view: (a, { writer: l }) => {
        if (!s.isBlockImage(a.parent))
          return null;
        const c = l.createEditableElement("figcaption");
        l.setCustomProperty("imageCaption", !0, c), c.placeholder = o("Enter image caption"), xf({
          view: i,
          element: c,
          keepOnFocus: !0
        });
        const u = a.parent.getAttribute("alt"), d = u ? o("Caption for image: %0", [
          u
        ]) : o("Caption for the image");
        return Kf(c, l, {
          label: d
        });
      }
    });
  }
  /**
  * Integrates with {@link module:image/image/imagetypecommand~ImageTypeCommand image type commands}
  * to make sure the caption is preserved when the type of an image changes so it can be restored
  * in the future if the user decides they want their caption back.
  */
  _setupImageTypeCommandsIntegration() {
    const e = this.editor, i = e.plugins.get("ImageUtils"), s = e.plugins.get("ImageCaptionUtils"), r = e.commands.get("imageTypeInline"), o = e.commands.get("imageTypeBlock"), a = (l) => {
      if (!l.return)
        return;
      const { oldElement: c, newElement: u } = l.return;
      /* istanbul ignore if: paranoid check -- @preserve */
      if (!c)
        return;
      if (i.isBlockImage(c)) {
        const h = s.getCaptionFromImageModelElement(c);
        if (h) {
          this._saveCaption(u, h);
          return;
        }
      }
      const d = this._getSavedCaption(c);
      d && this._saveCaption(u, d);
    };
    r && this.listenTo(r, "execute", a, {
      priority: "low"
    }), o && this.listenTo(o, "execute", a, {
      priority: "low"
    });
  }
  /**
  * Returns the saved {@link module:engine/model/element~Element#toJSON JSONified} caption
  * of an image model element.
  *
  * See {@link #_saveCaption}.
  *
  * @internal
  * @param imageModelElement The model element the caption should be returned for.
  * @returns The model caption element or `null` if there is none.
  */
  _getSavedCaption(e) {
    const i = this._savedCaptionsMap.get(e);
    return i ? et.fromJSON(i) : null;
  }
  /**
  * Saves a {@link module:engine/model/element~Element#toJSON JSONified} caption for
  * an image element to allow restoring it in the future.
  *
  * A caption is saved every time it gets hidden and/or the type of an image changes. The
  * user should be able to restore it on demand.
  *
  * **Note**: The caption cannot be stored in the image model element attribute because,
  * for instance, when the model state propagates to collaborators, the attribute would get
  * lost (mainly because it does not convert to anything when the caption is hidden) and
  * the states of collaborators' models would de-synchronize causing numerous issues.
  *
  * See {@link #_getSavedCaption}.
  *
  * @internal
  * @param imageModelElement The model element the caption is saved for.
  * @param caption The caption model element to be saved.
  */
  _saveCaption(e, i) {
    this._savedCaptionsMap.set(e, i.toJSON());
  }
  /**
  * Reconverts image caption when image alt attribute changes.
  * The change of alt attribute is reflected in caption's aria-label attribute.
  */
  _registerCaptionReconversion() {
    const e = this.editor, i = e.model, s = e.plugins.get("ImageUtils"), r = e.plugins.get("ImageCaptionUtils");
    i.document.on("change:data", () => {
      const o = i.document.differ.getChanges();
      for (const a of o) {
        if (a.attributeKey !== "alt")
          continue;
        const l = a.range.start.nodeAfter;
        if (s.isBlockImage(l)) {
          const c = r.getCaptionFromImageModelElement(l);
          if (!c)
            return;
          e.editing.reconvertItem(c);
        }
      }
    });
  }
}
class P$ extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Vk
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCaptionUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.editing.view, i = t.plugins.get("ImageCaptionUtils"), s = t.t;
    t.ui.componentFactory.add("toggleImageCaption", (r) => {
      const o = t.commands.get("toggleImageCaption"), a = new le(r);
      return a.set({
        icon: Z.caption,
        tooltip: !0,
        isToggleable: !0
      }), a.bind("isOn", "isEnabled").to(o, "value", "isEnabled"), a.bind("label").to(o, "value", (l) => s(l ? "Toggle caption off" : "Toggle caption on")), this.listenTo(a, "execute", () => {
        t.execute("toggleImageCaption", {
          focusCaptionOnShow: !0
        });
        const l = i.getCaptionFromModelSelection(t.model.document.selection);
        if (l) {
          const c = t.editing.mapper.toViewElement(l);
          e.scrollToTheSelection(), e.change((u) => {
            u.addClass("image__caption_highlighted", c);
          });
        }
        t.editing.view.focus();
      }), a;
    });
  }
}
class R$ extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      I$,
      P$
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCaption";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
function Lk(n) {
  const t = n.map((e) => e.replace("+", "\\+"));
  return new RegExp(`^image\\/(${t.join("|")})$`);
}
function O$(n) {
  return new Promise((t, e) => {
    const i = n.getAttribute("src");
    fetch(i).then((s) => s.blob()).then((s) => {
      const r = Bk(s, i), a = `image.${r.replace("image/", "")}`, l = new File([
        s
      ], a, {
        type: r
      });
      t(l);
    }).catch((s) => s && s.name === "TypeError" ? L$(i).then(t).catch(e) : e(s));
  });
}
function V$(n, t) {
  return !n.isInlineImageView(t) || !t.getAttribute("src") ? !1 : !!t.getAttribute("src").match(/^data:image\/\w+;base64,/g) || !!t.getAttribute("src").match(/^blob:/g);
}
function Bk(n, t) {
  return n.type ? n.type : t.match(/data:(image\/\w+);base64/) ? t.match(/data:(image\/\w+);base64/)[1].toLowerCase() : "image/jpeg";
}
function L$(n) {
  return B$(n).then((t) => {
    const e = Bk(t, n), s = `image.${e.replace("image/", "")}`;
    return new File([
      t
    ], s, {
      type: e
    });
  });
}
function B$(n) {
  return new Promise((t, e) => {
    const i = ae.document.createElement("img");
    i.addEventListener("load", () => {
      const s = ae.document.createElement("canvas");
      s.width = i.width, s.height = i.height, s.getContext("2d").drawImage(i, 0, 0), s.toBlob((o) => o ? t(o) : e());
    }), i.addEventListener("error", () => e()), i.src = n;
  });
}
class M$ extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageUploadUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add("uploadImage", () => this._createToolbarButton()), t.ui.componentFactory.add("imageUpload", () => this._createToolbarButton()), t.ui.componentFactory.add("menuBar:uploadImage", () => this._createMenuBarButton("standalone")), t.plugins.has("ImageInsertUI") && t.plugins.get("ImageInsertUI").registerIntegration({
      name: "upload",
      observable: () => t.commands.get("uploadImage"),
      buttonViewCreator: () => this._createToolbarButton(),
      formViewCreator: () => this._createDropdownButton(),
      menuBarButtonViewCreator: (e) => this._createMenuBarButton(e ? "insertOnly" : "insertNested")
    });
  }
  /**
  * Creates the base for various kinds of the button component provided by this feature.
  */
  _createButton(t) {
    const e = this.editor, i = e.locale, s = e.commands.get("uploadImage"), r = e.config.get("image.upload.types"), o = Lk(r), a = new t(e.locale), l = i.t;
    return a.set({
      acceptedType: r.map((c) => `image/${c}`).join(","),
      allowMultipleFiles: !0,
      label: l("Upload from computer"),
      icon: Z.imageUpload
    }), a.bind("isEnabled").to(s), a.on("done", (c, u) => {
      const d = Array.from(u).filter((h) => o.test(h.type));
      d.length && (e.execute("uploadImage", {
        file: d
      }), e.editing.view.focus());
    }), a;
  }
  /**
  * Creates a simple toolbar button, with an icon and a tooltip.
  */
  _createToolbarButton() {
    const t = this.editor.locale.t, e = this.editor.plugins.get("ImageInsertUI"), i = this.editor.commands.get("uploadImage"), s = this._createButton(Eb);
    return s.tooltip = !0, s.bind("label").to(e, "isImageSelected", i, "isAccessAllowed", (r, o) => t(o ? r ? "Replace image from computer" : "Upload image from computer" : "You have no image upload permissions.")), s;
  }
  /**
  * Creates a button for the dropdown view, with an icon, text and no tooltip.
  */
  _createDropdownButton() {
    const t = this.editor.locale.t, e = this.editor.plugins.get("ImageInsertUI"), i = this._createButton(Eb);
    return i.withText = !0, i.bind("label").to(e, "isImageSelected", (s) => t(s ? "Replace from computer" : "Upload from computer")), i.on("execute", () => {
      e.dropdownView.isOpen = !1;
    }), i;
  }
  /**
  * Creates a button for the menu bar.
  */
  _createMenuBarButton(t) {
    const e = this.editor.locale.t, i = this._createButton(qy);
    switch (i.withText = !0, t) {
      case "standalone":
        i.label = e("Image from computer");
        break;
      case "insertOnly":
        i.label = e("Image");
        break;
      case "insertNested":
        i.label = e("From computer");
        break;
    }
    return i;
  }
}
class N$ extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The image placeholder that is displayed before real image data can be accessed.
    *
    * For the record, this image is a 1x1 px GIF with an aspect ratio set by CSS.
    */
    f(this, "placeholder");
    /**
    * This method is called each time the image `uploadStatus` attribute is changed.
    *
    * @param evt An object containing information about the fired event.
    * @param data Additional information about the change.
    */
    f(this, "uploadStatusChange", (e, i, s) => {
      const r = this.editor, o = i.item, a = o.getAttribute("uploadId");
      if (!s.consumable.consume(i.item, e.name))
        return;
      const l = r.plugins.get("ImageUtils"), c = r.plugins.get(Rr), u = a ? i.attributeNewValue : null, d = this.placeholder, h = r.editing.mapper.toViewElement(o), m = s.writer;
      if (u == "reading") {
        P1(h, m), R1(l, d, h, m);
        return;
      }
      if (u == "uploading") {
        const g = c.loaders.get(a);
        P1(h, m), g ? (O1(h, m), D$(h, m, g, r.editing.view), U$(l, h, m, g)) : R1(l, d, h, m);
        return;
      }
      u == "complete" && c.loaders.get(a) && z$(h, m, r.editing.view), $$(h, m), O1(h, m), F$(h, m);
    });
    this.placeholder = "data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==";
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageUploadProgress";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.plugins.has("ImageBlockEditing") && e.editing.downcastDispatcher.on("attribute:uploadStatus:imageBlock", this.uploadStatusChange), e.plugins.has("ImageInlineEditing") && e.editing.downcastDispatcher.on("attribute:uploadStatus:imageInline", this.uploadStatusChange);
  }
}
function P1(n, t) {
  n.hasClass("ck-appear") || t.addClass("ck-appear", n);
}
function F$(n, t) {
  t.removeClass("ck-appear", n);
}
function R1(n, t, e, i) {
  e.hasClass("ck-image-upload-placeholder") || i.addClass("ck-image-upload-placeholder", e);
  const s = n.findViewImgElement(e);
  s.getAttribute("src") !== t && i.setAttribute("src", t, s), Mk(e, "placeholder") || i.insert(i.createPositionAfter(s), j$(i));
}
function O1(n, t) {
  n.hasClass("ck-image-upload-placeholder") && t.removeClass("ck-image-upload-placeholder", n), Nk(n, t, "placeholder");
}
function D$(n, t, e, i) {
  const s = H$(t);
  t.insert(t.createPositionAt(n, "end"), s), e.on("change:uploadedPercent", (r, o, a) => {
    i.change((l) => {
      l.setStyle("width", a + "%", s);
    });
  });
}
function $$(n, t) {
  Nk(n, t, "progressBar");
}
function z$(n, t, e) {
  const i = t.createUIElement("div", {
    class: "ck-image-upload-complete-icon"
  });
  t.insert(t.createPositionAt(n, "end"), i), setTimeout(() => {
    e.change((s) => s.remove(s.createRangeOn(i)));
  }, 3e3);
}
function H$(n) {
  const t = n.createUIElement("div", {
    class: "ck-progress-bar"
  });
  return n.setCustomProperty("progressBar", !0, t), t;
}
function j$(n) {
  const t = n.createUIElement("div", {
    class: "ck-upload-placeholder-loader"
  });
  return n.setCustomProperty("placeholder", !0, t), t;
}
function Mk(n, t) {
  for (const e of n.getChildren())
    if (e.getCustomProperty(t))
      return e;
}
function Nk(n, t, e) {
  const i = Mk(n, e);
  i && t.remove(t.createRangeOn(i));
}
function U$(n, t, e, i) {
  if (i.data) {
    const s = n.findViewImgElement(t);
    e.setAttribute("src", i.data, s);
  }
}
class W$ extends he {
  /**
  * Creates an instance of the `imageUlpoad` command. When executed, the command upload one of
  * the currently selected image from computer.
  *
  * @param editor The editor instance.
  */
  constructor(t) {
    super(t), this.set("isAccessAllowed", !0);
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor, e = t.plugins.get("ImageUtils"), i = t.model.document.selection.getSelectedElement();
    this.isEnabled = e.isImageAllowed() || e.isImage(i);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options Options for the executed command.
  * @param options.file The image file or an array of image files to upload.
  */
  execute(t) {
    const e = Ue(t.file), i = this.editor.model.document.selection, s = this.editor.plugins.get("ImageUtils"), r = Object.fromEntries(i.getAttributes());
    e.forEach((o, a) => {
      const l = i.getSelectedElement();
      if (a && l && s.isImage(l)) {
        const c = this.editor.model.createPositionAfter(l);
        this._uploadImage(o, r, c);
      } else
        this._uploadImage(o, r);
    });
  }
  /**
  * Handles uploading single file.
  */
  _uploadImage(t, e, i) {
    const s = this.editor, o = s.plugins.get(Rr).createLoader(t), a = s.plugins.get("ImageUtils");
    o && a.insertImage({
      ...e,
      uploadId: o.id
    }, i);
  }
}
class q$ extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * An internal mapping of {@link module:upload/filerepository~FileLoader#id file loader UIDs} and
    * model elements during the upload.
    *
    * Model element of the uploaded image can change, for instance, when {@link module:image/image/imagetypecommand~ImageTypeCommand}
    * is executed as a result of adding caption or changing image style. As a result, the upload logic must keep track of the model
    * element (reference) and resolve the upload for the correct model element (instead of the one that landed in the `$graveyard`
    * after image type changed).
    */
    f(this, "_uploadImageElements");
    /**
    * An internal mapping of {@link module:upload/filerepository~FileLoader#id file loader UIDs} and
    * upload responses for handling images dragged during their upload process. When such images are later
    * dropped, their original upload IDs no longer exist in the registry (as the original upload completed).
    * This map preserves the upload responses to properly handle such cases.
    */
    f(this, "_uploadedImages", /* @__PURE__ */ new Map());
    e.config.define("image", {
      upload: {
        types: [
          "jpeg",
          "png",
          "gif",
          "bmp",
          "webp",
          "tiff"
        ]
      }
    }), this._uploadImageElements = /* @__PURE__ */ new Map();
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Rr,
      Nb,
      _i,
      jt
    ];
  }
  static get pluginName() {
    return "ImageUploadEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model.document, s = e.conversion, r = e.plugins.get(Rr), o = e.plugins.get("ImageUtils"), a = e.plugins.get("ClipboardPipeline"), l = Lk(e.config.get("image.upload.types")), c = new W$(e);
    e.commands.add("uploadImage", c), e.commands.add("imageUpload", c), s.for("upcast").attributeToAttribute({
      view: {
        name: "img",
        key: "uploadId"
      },
      model: "uploadId"
    }).add((u) => u.on("element:img", (d, h, m) => {
      if (!m.consumable.test(h.viewItem, {
        attributes: [
          "data-ck-upload-id"
        ]
      }))
        return;
      const g = h.viewItem.getAttribute("data-ck-upload-id");
      if (!g)
        return;
      const [p] = Array.from(h.modelRange.getItems({
        shallow: !0
      })), w = r.loaders.get(g);
      p && (m.writer.setAttribute("uploadId", g, p), m.consumable.consume(h.viewItem, {
        attributes: [
          "data-ck-upload-id"
        ]
      }), w && w.data && m.writer.setAttribute("uploadStatus", w.status, p));
    }, {
      priority: "low"
    })), this.listenTo(e.editing.view.document, "clipboardInput", (u, d) => {
      if (G$(d.dataTransfer))
        return;
      const h = Array.from(d.dataTransfer.files).filter((g) => g ? l.test(g.type) : !1);
      if (!h.length)
        return;
      if (u.stop(), e.model.change((g) => {
        d.targetRanges && g.setSelection(d.targetRanges.map((p) => e.editing.mapper.toModelRange(p))), e.execute("uploadImage", {
          file: h
        });
      }), !e.commands.get("uploadImage").isAccessAllowed) {
        const g = e.plugins.get("Notification"), p = e.locale.t;
        g.showWarning(p("You have no image upload permissions."), {
          namespace: "image"
        });
      }
    }), this.listenTo(a, "inputTransformation", (u, d) => {
      const h = Array.from(e.editing.view.createRangeIn(d.content)).map((g) => g.item).filter((g) => V$(o, g) && !g.getAttribute("uploadProcessed")).map((g) => ({
        promise: O$(g),
        imageElement: g
      }));
      if (!h.length)
        return;
      const m = new Pi(e.editing.view.document);
      for (const g of h) {
        m.setAttribute("uploadProcessed", !0, g.imageElement);
        const p = r.createLoader(g.promise);
        p && (m.setAttribute("src", "", g.imageElement), m.setAttribute("uploadId", p.id, g.imageElement));
      }
    }), e.editing.view.document.on("dragover", (u, d) => {
      d.preventDefault();
    }), i.on("change", () => {
      const u = i.differ.getChanges({
        includeChangesInGraveyard: !0
      }).reverse(), d = /* @__PURE__ */ new Set();
      for (const h of u)
        if (h.type == "insert" && h.name != "$text") {
          const m = h.position.nodeAfter, g = h.position.root.rootName == "$graveyard";
          for (const p of K$(e, m)) {
            const w = p.getAttribute("uploadId");
            if (!w)
              continue;
            const y = r.loaders.get(w);
            if (!y) {
              !g && this._uploadedImages.has(w) && e.model.enqueueChange({
                isUndoable: !1
              }, (k) => {
                k.setAttribute("uploadStatus", "complete", p), this.fire("uploadComplete", {
                  data: this._uploadedImages.get(w),
                  imageElement: p
                });
              });
              continue;
            }
            g ? d.has(w) || Array.from(this._uploadImageElements.get(w)).every((C) => C.root.rootName == "$graveyard") && y.abort() : (d.add(w), this._uploadImageElements.has(w) ? this._uploadImageElements.get(w).add(p) : this._uploadImageElements.set(w, /* @__PURE__ */ new Set([
              p
            ])), y.status == "idle" && this._readAndUpload(y));
          }
        }
    }), this.on("uploadComplete", (u, { imageElement: d, data: h }) => {
      const m = h.urls ? h.urls : h;
      this.editor.model.change((g) => {
        g.setAttribute("src", m.default, d), this._parseAndSetSrcsetAttributeOnImage(m, d, g), o.setImageNaturalSizeAttributes(d);
      });
    }, {
      priority: "low"
    });
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const e = this.editor.model.schema;
    this.editor.plugins.has("ImageBlockEditing") && (e.extend("imageBlock", {
      allowAttributes: [
        "uploadId",
        "uploadStatus"
      ]
    }), this._registerConverters("imageBlock")), this.editor.plugins.has("ImageInlineEditing") && (e.extend("imageInline", {
      allowAttributes: [
        "uploadId",
        "uploadStatus"
      ]
    }), this._registerConverters("imageInline"));
  }
  /**
  * Reads and uploads an image.
  *
  * The image is read from the disk and as a Base64-encoded string it is set temporarily to
  * `image[src]`. When the image is successfully uploaded, the temporary data is replaced with the target
  * image's URL (the URL to the uploaded image on the server).
  */
  _readAndUpload(e) {
    const i = this.editor, s = i.model, r = i.locale.t, o = i.plugins.get(Rr), a = i.plugins.get(Nb), l = i.plugins.get("ImageUtils"), c = this._uploadImageElements;
    return s.enqueueChange({
      isUndoable: !1
    }, (d) => {
      const h = c.get(e.id);
      for (const m of h)
        d.setAttribute("uploadStatus", "reading", m);
    }), e.read().then(() => {
      const d = e.upload();
      i.ui && i.ui.ariaLiveAnnouncer.announce(r("Uploading image"));
      for (const h of c.get(e.id)) {
        /* istanbul ignore next -- @preserve */
        if (be.isSafari) {
          const m = i.editing.mapper.toViewElement(h), g = l.findViewImgElement(m);
          i.editing.view.once("render", () => {
            if (!g.parent)
              return;
            const p = i.editing.view.domConverter.mapViewToDom(g.parent);
            if (!p)
              return;
            const w = p.style.display;
            p.style.display = "none", p._ckHack = p.offsetHeight, p.style.display = w;
          });
        }
        s.enqueueChange({
          isUndoable: !1
        }, (m) => {
          m.setAttribute("uploadStatus", "uploading", h);
        });
      }
      return d;
    }).then((d) => {
      s.enqueueChange({
        isUndoable: !1
      }, (h) => {
        for (const m of c.get(e.id))
          h.setAttribute("uploadStatus", "complete", m), this.fire("uploadComplete", {
            data: d,
            imageElement: m
          });
        i.ui && i.ui.ariaLiveAnnouncer.announce(r("Image upload complete")), this._uploadedImages.set(e.id, d);
      }), u();
    }).catch((d) => {
      if (i.ui && i.ui.ariaLiveAnnouncer.announce(r("Error during image upload")), e.status !== "error" && e.status !== "aborted")
        throw d;
      e.status == "error" && d && a.showWarning(d, {
        title: r("Upload failed"),
        namespace: "upload"
      }), s.enqueueChange({
        isUndoable: !1
      }, (h) => {
        for (const m of c.get(e.id))
          m.root.rootName !== "$graveyard" && h.remove(m);
      }), u();
    });
    function u() {
      s.enqueueChange({
        isUndoable: !1
      }, (d) => {
        for (const h of c.get(e.id))
          d.removeAttribute("uploadId", h), d.removeAttribute("uploadStatus", h);
        c.delete(e.id);
      }), o.destroyLoader(e);
    }
  }
  /**
  * Creates the `srcset` attribute based on a given file upload response and sets it as an attribute to a specific image element.
  *
  * @param data Data object from which `srcset` will be created.
  * @param image The image element on which the `srcset` attribute will be set.
  */
  _parseAndSetSrcsetAttributeOnImage(e, i, s) {
    let r = 0;
    const o = Object.keys(e).filter((a) => {
      const l = parseInt(a, 10);
      if (!isNaN(l))
        return r = Math.max(r, l), !0;
    }).map((a) => `${e[a]} ${a}w`).join(", ");
    if (o != "") {
      const a = {
        srcset: o
      };
      !i.hasAttribute("width") && !i.hasAttribute("height") && (a.width = r), s.setAttributes(a, i);
    }
  }
  /**
  * Registers image upload converters.
  *
  * @param imageType The type of the image.
  */
  _registerConverters(e) {
    const { conversion: i, plugins: s } = this.editor, r = s.get(Rr), o = s.get(jt);
    i.for("dataDowncast").add((a) => {
      a.on(`attribute:uploadId:${e}`, (l, c, u) => {
        if (!u.consumable.test(c.item, l.name))
          return;
        const d = r.loaders.get(c.attributeNewValue);
        if (!d || !d.data)
          return null;
        const h = u.mapper.toViewElement(c.item), m = o.findViewImgElement(h);
        m && (u.consumable.consume(c.item, l.name), u.writer.setAttribute("data-ck-upload-id", d.id, m));
      });
    });
  }
}
function G$(n) {
  return Array.from(n.types).includes("text/html") && n.getData("text/html") !== "";
}
function K$(n, t) {
  const e = n.plugins.get("ImageUtils");
  return Array.from(n.model.createRangeOn(t)).filter((i) => e.isImage(i.item)).map((i) => i.item);
}
class Fk extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageUpload";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      q$,
      M$,
      N$
    ];
  }
}
class Z$ extends ee {
  /**
  * Creates a view for the dropdown panel of {@link module:image/imageinsert/imageinsertui~ImageInsertUI}.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * The URL input field view.
    */
    f(this, "urlInputView");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    this.set("imageURLInputValue", ""), this.set("isImageSelected", !1), this.set("isEnabled", !0), this.keystrokes = new Ze(), this.urlInputView = this._createUrlInputView(), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-image-insert-url"
        ],
        tabindex: "-1"
      },
      children: [
        this.urlInputView,
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-image-insert-url__action-row"
            ]
          }
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Tn({
      view: this
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates the {@link #urlInputView}.
  */
  _createUrlInputView() {
    const e = this.locale, i = e.t, s = new gt(e, pi);
    return s.bind("label").to(this, "isImageSelected", (r) => i(r ? "Update image URL" : "Insert image via URL")), s.bind("isEnabled").to(this), s.fieldView.inputMode = "url", s.fieldView.placeholder = "https://example.com/image.png", s.fieldView.bind("value").to(this, "imageURLInputValue", (r) => r || ""), s.fieldView.on("input", () => {
      this.imageURLInputValue = s.fieldView.element.value.trim();
    }), s;
  }
  /**
  * Focuses the view.
  */
  focus() {
    this.urlInputView.focus();
  }
}
class Y$ extends I {
  constructor() {
    super(...arguments);
    f(this, "_imageInsertUI");
    f(this, "_formView");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInsertViaUrlUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      $a,
      Hs
    ];
  }
  init() {
    this.editor.ui.componentFactory.add("insertImageViaUrl", () => this._createToolbarButton()), this.editor.ui.componentFactory.add("menuBar:insertImageViaUrl", () => this._createMenuBarButton("standalone"));
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._imageInsertUI = this.editor.plugins.get("ImageInsertUI"), this._imageInsertUI.registerIntegration({
      name: "url",
      observable: () => this.editor.commands.get("insertImage"),
      buttonViewCreator: () => this._createToolbarButton(),
      formViewCreator: () => this._createDropdownButton(),
      menuBarButtonViewCreator: (e) => this._createMenuBarButton(e ? "insertOnly" : "insertNested")
    });
  }
  /**
  * Creates the base for various kinds of the button component provided by this feature.
  */
  _createInsertUrlButton(e) {
    const i = new e(this.editor.locale);
    return i.icon = Z.imageUrl, i.on("execute", () => {
      this._showModal();
    }), i;
  }
  /**
  * Creates a simple toolbar button, with an icon and a tooltip.
  */
  _createToolbarButton() {
    const e = this.editor.locale.t, i = this._createInsertUrlButton(le);
    return i.tooltip = !0, i.bind("label").to(this._imageInsertUI, "isImageSelected", (s) => e(s ? "Update image URL" : "Insert image via URL")), i;
  }
  /**
  * Creates a button for the dropdown view, with an icon, text and no tooltip.
  */
  _createDropdownButton() {
    const e = this.editor.locale.t, i = this._createInsertUrlButton(le);
    return i.withText = !0, i.bind("label").to(this._imageInsertUI, "isImageSelected", (s) => e(s ? "Update image URL" : "Insert via URL")), i;
  }
  /**
  * Creates a button for the menu bar.
  */
  _createMenuBarButton(e) {
    const i = this.editor.locale.t, s = this._createInsertUrlButton(Fe);
    switch (s.withText = !0, e) {
      case "standalone":
        s.label = i("Image via URL");
        break;
      case "insertOnly":
        s.label = i("Image");
        break;
      case "insertNested":
        s.label = i("Via URL");
        break;
    }
    return s;
  }
  /**
  * Creates the form view used to submit the image URL.
  */
  _createInsertUrlView() {
    const e = this.editor, i = e.locale, s = e.commands.get("replaceImageSource"), r = e.commands.get("insertImage"), o = new Z$(i);
    return o.bind("isImageSelected").to(this._imageInsertUI), o.bind("isEnabled").toMany([
      r,
      s
    ], "isEnabled", (...a) => a.some((l) => l)), o;
  }
  /**
  * Shows the insert image via URL form view in a modal.
  */
  _showModal() {
    const e = this.editor, s = e.locale.t, r = e.plugins.get("Dialog");
    this._formView || (this._formView = this._createInsertUrlView(), this._formView.on("submit", () => this._handleSave()));
    const o = e.commands.get("replaceImageSource");
    this._formView.imageURLInputValue = o.value || "", r.show({
      id: "insertImageViaUrl",
      title: this._imageInsertUI.isImageSelected ? s("Update image URL") : s("Insert image via URL"),
      isModal: !0,
      content: this._formView,
      actionButtons: [
        {
          label: s("Cancel"),
          withText: !0,
          onExecute: () => r.hide()
        },
        {
          label: s("Accept"),
          class: "ck-button-action",
          withText: !0,
          onExecute: () => this._handleSave()
        }
      ]
    });
  }
  /**
  * Executes appropriate command depending on selection and form value.
  */
  _handleSave() {
    this.editor.commands.get("replaceImageSource").isEnabled ? this.editor.execute("replaceImageSource", {
      source: this._formView.imageURLInputValue
    }) : this.editor.execute("insertImage", {
      source: this._formView.imageURLInputValue
    }), this.editor.plugins.get("Dialog").hide();
  }
}
class Dk extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInsertViaUrl";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Y$,
      $a
    ];
  }
}
class J$ extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageInsert";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Fk,
      Dk,
      $a
    ];
  }
}
class X$ extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor, i = t.plugins.get("ImageUtils").getClosestSelectedImageElement(t.model.document.selection);
    this.isEnabled = !!i, !i || !i.hasAttribute("resizedWidth") ? this.value = null : this.value = {
      width: i.getAttribute("resizedWidth"),
      height: null
    };
  }
  /**
  * Executes the command.
  *
  * ```ts
  * // Sets the width to 50%:
  * editor.execute( 'resizeImage', { width: '50%' } );
  *
  * // Removes the width attribute:
  * editor.execute( 'resizeImage', { width: null } );
  * ```
  *
  * @param options
  * @param options.width The new width of the image.
  * @fires execute
  */
  execute(t) {
    const e = this.editor, i = e.model, s = e.plugins.get("ImageUtils"), r = s.getClosestSelectedImageElement(i.document.selection);
    this.value = {
      width: t.width,
      height: null
    }, r && i.change((o) => {
      o.setAttribute("resizedWidth", t.width, r), o.removeAttribute("resizedHeight", r), s.setImageNaturalSizeAttributes(r);
    });
  }
}
class $k extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageResizeEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("image", {
      resizeUnit: "%",
      resizeOptions: [
        {
          name: "resizeImage:original",
          value: null,
          icon: "original"
        },
        {
          name: "resizeImage:custom",
          value: "custom",
          icon: "custom"
        },
        {
          name: "resizeImage:25",
          value: "25",
          icon: "small"
        },
        {
          name: "resizeImage:50",
          value: "50",
          icon: "medium"
        },
        {
          name: "resizeImage:75",
          value: "75",
          icon: "large"
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = new X$(t);
    this._registerConverters("imageBlock"), this._registerConverters("imageInline"), t.commands.add("resizeImage", e), t.commands.add("imageResize", e);
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._registerSchema();
  }
  _registerSchema() {
    this.editor.plugins.has("ImageBlockEditing") && this.editor.model.schema.extend("imageBlock", {
      allowAttributes: [
        "resizedWidth",
        "resizedHeight"
      ]
    }), this.editor.plugins.has("ImageInlineEditing") && this.editor.model.schema.extend("imageInline", {
      allowAttributes: [
        "resizedWidth",
        "resizedHeight"
      ]
    });
  }
  /**
  * Registers image resize converters.
  *
  * @param imageType The type of the image.
  */
  _registerConverters(t) {
    const e = this.editor, i = e.plugins.get("ImageUtils");
    e.conversion.for("downcast").add((s) => s.on(`attribute:resizedWidth:${t}`, (r, o, a) => {
      if (!a.consumable.consume(o.item, r.name))
        return;
      const l = a.writer, c = a.mapper.toViewElement(o.item);
      o.attributeNewValue !== null ? (l.setStyle("width", o.attributeNewValue, c), l.addClass("image_resized", c)) : (l.removeStyle("width", c), l.removeClass("image_resized", c));
    })), e.conversion.for("dataDowncast").attributeToAttribute({
      model: {
        name: t,
        key: "resizedHeight"
      },
      view: (s) => ({
        key: "style",
        value: {
          height: s
        }
      })
    }), e.conversion.for("editingDowncast").add((s) => s.on(`attribute:resizedHeight:${t}`, (r, o, a) => {
      if (!a.consumable.consume(o.item, r.name))
        return;
      const l = a.writer, c = a.mapper.toViewElement(o.item), u = t === "imageInline" ? i.findViewImgElement(c) : c;
      o.attributeNewValue !== null ? l.setStyle("height", o.attributeNewValue, u) : l.removeStyle("height", u);
    })), e.conversion.for("upcast").attributeToAttribute({
      view: {
        name: t === "imageBlock" ? "figure" : "img",
        styles: {
          width: /.+/
        }
      },
      model: {
        key: "resizedWidth",
        value: (s) => dc(s) ? null : s.getStyle("width")
      }
    }), e.conversion.for("upcast").attributeToAttribute({
      view: {
        name: t === "imageBlock" ? "figure" : "img",
        styles: {
          height: /.+/
        }
      },
      model: {
        key: "resizedHeight",
        value: (s) => dc(s) ? null : s.getStyle("height")
      }
    });
  }
}
const yd = {
  small: Z.objectSizeSmall,
  medium: Z.objectSizeMedium,
  large: Z.objectSizeLarge,
  custom: Z.objectSizeCustom,
  original: Z.objectSizeFull
};
class Q$ extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The resize unit.
    * @default '%'
    */
    f(this, "_resizeUnit");
    this._resizeUnit = e.config.get("image.resizeUnit");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      $k
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageResizeButtons";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.config.get("image.resizeOptions"), s = e.commands.get("resizeImage");
    this.bind("isEnabled").to(s);
    for (const r of i)
      this._registerImageResizeButton(r);
    this._registerImageResizeDropdown(i);
  }
  /**
  * A helper function that creates a standalone button component for the plugin.
  *
  * @param resizeOption A model of the resize option.
  */
  _registerImageResizeButton(e) {
    const i = this.editor, { name: s, value: r, icon: o } = e;
    i.ui.componentFactory.add(s, (a) => {
      const l = new le(a), c = i.commands.get("resizeImage"), u = this._getOptionLabelValue(e, !0);
      if (!yd[o])
        throw new S("imageresizebuttons-missing-icon", i, e);
      if (l.set({
        // Use the `label` property for a verbose description (because of ARIA).
        label: u,
        icon: yd[o],
        tooltip: u,
        isToggleable: !0
      }), l.bind("isEnabled").to(this), i.plugins.has("ImageCustomResizeUI") && Eo(e)) {
        const d = i.plugins.get("ImageCustomResizeUI");
        this.listenTo(l, "execute", () => {
          d._showForm(this._resizeUnit);
        });
      } else {
        const d = r ? r + this._resizeUnit : null;
        l.bind("isOn").to(c, "value", Eh(d)), this.listenTo(l, "execute", () => {
          i.execute("resizeImage", {
            width: d
          });
        });
      }
      return l;
    });
  }
  /**
  * A helper function that creates a dropdown component for the plugin containing all the resize options defined in
  * the editor configuration.
  *
  * @param options An array of configured options.
  */
  _registerImageResizeDropdown(e) {
    const i = this.editor, s = i.t, r = e.find((a) => !a.value), o = (a) => {
      const l = i.commands.get("resizeImage"), c = Tt(a, My), u = c.buttonView, d = s("Resize image");
      return u.set({
        tooltip: d,
        commandValue: r.value,
        icon: yd.medium,
        isToggleable: !0,
        label: this._getOptionLabelValue(r),
        withText: !0,
        class: "ck-resize-image-button",
        ariaLabel: d,
        ariaLabelledBy: void 0
      }), u.bind("label").to(l, "value", (h) => h && h.width ? h.width : this._getOptionLabelValue(r)), c.bind("isEnabled").to(this), En(c, () => this._getResizeDropdownListItemDefinitions(e, l), {
        ariaLabel: s("Image resize list"),
        role: "menu"
      }), this.listenTo(c, "execute", (h) => {
        "onClick" in h.source ? h.source.onClick() : (i.execute(h.source.commandName, {
          width: h.source.commandValue
        }), i.editing.view.focus());
      }), c;
    };
    i.ui.componentFactory.add("resizeImage", o), i.ui.componentFactory.add("imageResize", o);
  }
  /**
  * A helper function for creating an option label value string.
  *
  * @param option A resize option object.
  * @param forTooltip An optional flag for creating a tooltip label.
  * @returns A user-defined label combined from the numeric value and the resize unit or the default label
  * for reset options (`Original`).
  */
  _getOptionLabelValue(e, i = !1) {
    const s = this.editor.t;
    return e.label ? e.label : i ? Eo(e) ? s("Custom image size") : e.value ? s("Resize image to %0", e.value + this._resizeUnit) : s("Resize image to the original size") : Eo(e) ? s("Custom") : e.value ? e.value + this._resizeUnit : s("Original");
  }
  /**
  * A helper function that parses the resize options and returns list item definitions ready for use in the dropdown.
  *
  * @param options The resize options.
  * @param command The resize image command.
  * @returns Dropdown item definitions.
  */
  _getResizeDropdownListItemDefinitions(e, i) {
    const { editor: s } = this, r = new bt(), o = e.map((a) => Eo(a) ? {
      ...a,
      valueWithUnits: "custom"
    } : a.value ? {
      ...a,
      valueWithUnits: `${a.value}${this._resizeUnit}`
    } : {
      ...a,
      valueWithUnits: null
    });
    for (const a of o) {
      let l = null;
      if (s.plugins.has("ImageCustomResizeUI") && Eo(a)) {
        const c = s.plugins.get("ImageCustomResizeUI");
        l = {
          type: "button",
          model: new tn({
            label: this._getOptionLabelValue(a),
            role: "menuitemradio",
            withText: !0,
            icon: null,
            onClick: () => {
              c._showForm(this._resizeUnit);
            }
          })
        };
        const u = ZP(o, "valueWithUnits");
        l.model.bind("isOn").to(i, "value", ez(u));
      } else
        l = {
          type: "button",
          model: new tn({
            commandName: "resizeImage",
            commandValue: a.valueWithUnits,
            label: this._getOptionLabelValue(a),
            role: "menuitemradio",
            withText: !0,
            icon: null
          })
        }, l.model.bind("isOn").to(i, "value", Eh(a.valueWithUnits));
      l.model.bind("isEnabled").to(i, "isEnabled"), r.add(l);
    }
    return r;
  }
}
function Eo(n) {
  return n.value === "custom";
}
function Eh(n) {
  return (t) => {
    const e = t;
    return n === null && e === n ? !0 : e !== null && e.width === n;
  };
}
function ez(n) {
  return (t) => !n.some((e) => Eh(e)(t));
}
const tz = "figure.image.ck-widget > img,figure.image.ck-widget > picture > img,figure.image.ck-widget > a > img,figure.image.ck-widget > a > picture > img,span.image-inline.ck-widget > img,span.image-inline.ck-widget > picture > img", kd = "image_resized";
class iz extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      md,
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageResizeHandles";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.commands.get("resizeImage");
    this.bind("isEnabled").to(t), this._setupResizerCreator();
  }
  /**
  * Attaches the listeners responsible for creating a resizer for each image, except for images inside the HTML embed preview.
  */
  _setupResizerCreator() {
    const t = this.editor, e = t.editing.view, i = t.plugins.get("ImageUtils");
    e.addObserver(rm), this.listenTo(e.document, "imageLoaded", (s, r) => {
      if (!r.target.matches(tz))
        return;
      const o = t.editing.view.domConverter, a = o.domToView(r.target), l = i.getImageWidgetFromImageView(a);
      let c = this.editor.plugins.get(md).getResizerByViewElement(l);
      if (c) {
        c.redraw();
        return;
      }
      const u = t.editing.mapper, d = u.toModelElement(l);
      c = t.plugins.get(md).attachTo({
        unit: t.config.get("image.resizeUnit"),
        modelElement: d,
        viewElement: l,
        editor: t,
        getHandleHost(h) {
          return h.querySelector("img");
        },
        getResizeHost() {
          return o.mapViewToDom(u.toViewElement(d));
        },
        isCentered() {
          return d.getAttribute("imageStyle") == "alignCenter";
        },
        onCommit(h) {
          e.change((m) => {
            m.removeClass(kd, l);
          }), t.execute("resizeImage", {
            width: h
          });
        }
      }), c.on("updateSize", () => {
        l.hasClass(kd) || e.change((m) => {
          m.addClass(kd, l);
        });
        const h = d.name === "imageInline" ? a : l;
        h.getStyle("height") && e.change((m) => {
          m.removeStyle("height", h);
        });
      }), c.bind("isEnabled").to(this);
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function zk(n) {
  if (!n)
    return null;
  const [, t, e] = n.trim().match(/([.,\d]+)(%|px)$/) || [], i = Number.parseFloat(t);
  return Number.isNaN(i) ? null : {
    value: i,
    unit: e
  };
}
function Hk(n, t, e) {
  return e === "px" ? {
    value: t.value,
    unit: "px"
  } : {
    value: t.value / n * 100,
    unit: "%"
  };
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function jk(n) {
  const { editing: t } = n, i = n.plugins.get("ImageUtils").getClosestSelectedImageElement(n.model.document.selection);
  if (!i)
    return null;
  const s = t.mapper.toViewElement(i), r = t.view.domConverter.mapViewToDom(s);
  return {
    model: i,
    view: s,
    dom: r
  };
}
function nz(n, t) {
  const e = jk(n);
  if (!e)
    return null;
  const i = zk(e.model.getAttribute("resizedWidth") || null);
  if (!i)
    return null;
  if (i.unit === t)
    return i;
  const s = Zf(e.dom), r = {
    value: new ke(e.dom).width
  };
  return Hk(s, r, t);
}
class sz extends ee {
  /**
  * @inheritDoc
  */
  constructor(e, i, s) {
    super(e);
    /**
    * Tracks information about the DOM focus in the form.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * Resize unit shortcut.
    */
    f(this, "unit");
    /**
    * An input with a label.
    */
    f(this, "labeledInput");
    /**
    * A button used to submit the form.
    */
    f(this, "saveButtonView");
    /**
    * A button used to cancel the form.
    */
    f(this, "cancelButtonView");
    /**
    * A collection of views which can be focused in the form.
    */
    f(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    f(this, "_focusCycler");
    /**
    * An array of form validators used by {@link #isValid}.
    */
    f(this, "_validators");
    const r = this.locale.t;
    this.focusTracker = new ze(), this.keystrokes = new Ze(), this.unit = i, this.labeledInput = this._createLabeledInputView(), this.saveButtonView = this._createButton(r("Save"), Z.check, "ck-button-save"), this.saveButtonView.type = "submit", this.cancelButtonView = this._createButton(r("Cancel"), Z.cancel, "ck-button-cancel", "cancel"), this._focusables = new Et(), this._validators = s, this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-image-custom-resize-form",
          "ck-responsive-form"
        ],
        // https://github.com/ckeditor/ckeditor5-image/issues/40
        tabindex: "-1"
      },
      children: [
        this.labeledInput,
        this.saveButtonView,
        this.cancelButtonView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.keystrokes.listenTo(this.element), Tn({
      view: this
    }), [
      this.labeledInput,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((e) => {
      this._focusables.add(e), this.focusTracker.add(e.element);
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates the button view.
  *
  * @param label The button label
  * @param icon The button's icon.
  * @param className The additional button CSS class name.
  * @param eventName The event name that the ButtonView#execute event will be delegated to.
  * @returns The button view instance.
  */
  _createButton(e, i, s, r) {
    const o = new le(this.locale);
    return o.set({
      label: e,
      icon: i,
      tooltip: !0
    }), o.extendTemplate({
      attributes: {
        class: s
      }
    }), r && o.delegate("execute").to(this, r), o;
  }
  /**
  * Creates an input with a label.
  *
  * @returns Labeled field view instance.
  */
  _createLabeledInputView() {
    const e = this.locale.t, i = new gt(this.locale, Dy);
    return i.label = e("Resize image (in %0)", this.unit), i.fieldView.set({
      step: 0.1
    }), i;
  }
  /**
  * Validates the form and returns `false` when some fields are invalid.
  */
  isValid() {
    this.resetFormStatus();
    for (const e of this._validators) {
      const i = e(this);
      if (i)
        return this.labeledInput.errorText = i, !1;
    }
    return !0;
  }
  /**
  * Cleans up the supplementary error and information text of the {@link #labeledInput}
  * bringing them back to the state when the form has been displayed for the first time.
  *
  * See {@link #isValid}.
  */
  resetFormStatus() {
    this.labeledInput.errorText = null;
  }
  /**
  * The native DOM `value` of the input element of {@link #labeledInput}.
  */
  get rawSize() {
    const { element: e } = this.labeledInput.fieldView;
    return e ? e.value : null;
  }
  /**
  * Get numeric value of size. Returns `null` if value of size input element in {@link #labeledInput}.is not a number.
  */
  get parsedSize() {
    const { rawSize: e } = this;
    if (e === null)
      return null;
    const i = Number.parseFloat(e);
    return Number.isNaN(i) ? null : i;
  }
  /**
  * Returns serialized image input size with unit.
  * Returns `null` if value of size input element in {@link #labeledInput}.is not a number.
  */
  get sizeWithUnits() {
    const { parsedSize: e, unit: i } = this;
    return e === null ? null : `${e}${i}`;
  }
}
function rz(n, t) {
  const e = jk(n);
  if (!e)
    return null;
  const i = Zf(e.dom), s = zk(window.getComputedStyle(e.dom).minWidth) || {
    value: 1
  }, r = Math.max(0.1, Hk(i, s, t).value);
  return {
    unit: t,
    lower: r,
    upper: t === "px" ? i : 100
  };
}
class oz extends I {
  constructor() {
    super(...arguments);
    /**
    * The contextual balloon plugin instance.
    */
    f(this, "_balloon");
    /**
    * A form containing a textarea and buttons, used to change the `alt` text value.
    */
    f(this, "_form");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ai
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageCustomResizeUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._form && this._form.destroy();
  }
  /**
  * Creates the {@link module:image/imageresize/ui/imagecustomresizeformview~ImageCustomResizeFormView}
  * form.
  */
  _createForm(e) {
    const i = this.editor;
    this._balloon = this.editor.plugins.get("ContextualBalloon"), this._form = new (no(sz))(i.locale, e, az(i)), this._form.render(), this.listenTo(this._form, "submit", () => {
      this._form.isValid() && (i.execute("resizeImage", {
        width: this._form.sizeWithUnits
      }), this._hideForm(!0));
    }), this.listenTo(this._form.labeledInput, "change:errorText", () => {
      i.ui.update();
    }), this.listenTo(this._form, "cancel", () => {
      this._hideForm(!0);
    }), this._form.keystrokes.set("Esc", (s, r) => {
      this._hideForm(!0), r();
    }), Xn({
      emitter: this._form,
      activator: () => this._isVisible,
      contextElements: () => [
        this._balloon.view.element
      ],
      callback: () => this._hideForm()
    });
  }
  /**
  * Shows the {@link #_form} in the {@link #_balloon}.
  *
  * @internal
  */
  _showForm(e) {
    if (this._isVisible)
      return;
    this._form || this._createForm(e);
    const i = this.editor, s = this._form.labeledInput;
    this._form.disableCssTransitions(), this._form.resetFormStatus(), this._isInBalloon || this._balloon.add({
      view: this._form,
      position: nm(i)
    });
    const r = nz(i, e), o = r ? r.value.toFixed(1) : "", a = rz(i, e);
    s.fieldView.value = s.fieldView.element.value = o, a && Object.assign(s.fieldView, {
      min: a.lower.toFixed(1),
      max: Math.ceil(a.upper).toFixed(1)
    }), this._form.labeledInput.fieldView.select(), this._form.enableCssTransitions();
  }
  /**
  * Removes the {@link #_form} from the {@link #_balloon}.
  *
  * @param focusEditable Controls whether the editing view is focused afterwards.
  */
  _hideForm(e = !1) {
    this._isInBalloon && (this._form.focusTracker.isFocused && this._form.saveButtonView.focus(), this._balloon.remove(this._form), e && this.editor.editing.view.focus());
  }
  /**
  * Returns `true` when the {@link #_form} is the visible view in the {@link #_balloon}.
  */
  get _isVisible() {
    return !!this._balloon && this._balloon.visibleView === this._form;
  }
  /**
  * Returns `true` when the {@link #_form} is in the {@link #_balloon}.
  */
  get _isInBalloon() {
    return !!this._balloon && this._balloon.hasView(this._form);
  }
}
function az(n) {
  const t = n.t;
  return [
    (e) => {
      if (e.rawSize.trim() === "")
        return t("The value must not be empty.");
      if (e.parsedSize === null)
        return t("The value should be a plain number.");
    }
  ];
}
class lz extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      $k,
      iz,
      oz,
      Q$
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageResize";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class cz extends he {
  /**
  * Creates an instance of the image style command. When executed, the command applies one of
  * {@link module:image/imageconfig~ImageStyleConfig#options style options} to the currently selected image.
  *
  * @param editor The editor instance.
  * @param styles The style options that this command supports.
  */
  constructor(e, i) {
    super(e);
    /**
    * An object containing names of default style options for the inline and block images.
    * If there is no default style option for the given image type in the configuration,
    * the name will be `false`.
    */
    f(this, "_defaultStyles");
    /**
    * The styles handled by this command.
    */
    f(this, "_styles");
    this._defaultStyles = {
      imageBlock: !1,
      imageInline: !1
    }, this._styles = new Map(i.map((s) => {
      if (s.isDefault)
        for (const r of s.modelElements)
          this._defaultStyles[r] = s.name;
      return [
        s.name,
        s
      ];
    }));
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const s = this.editor.plugins.get("ImageUtils").getClosestSelectedImageElement(this.editor.model.document.selection);
    this.isEnabled = !!s, this.isEnabled ? s.hasAttribute("imageStyle") ? this.value = s.getAttribute("imageStyle") : this.value = this._defaultStyles[s.name] : this.value = !1;
  }
  /**
  * Executes the command and applies the style to the currently selected image:
  *
  * ```ts
  * editor.execute( 'imageStyle', { value: 'side' } );
  * ```
  *
  * **Note**: Executing this command may change the image model element if the desired style requires an image
  * of a different type. Learn more about {@link module:image/imageconfig~ImageStyleOptionDefinition#modelElements model element}
  * configuration for the style option.
  *
  * @param options.value The name of the style (as configured in {@link module:image/imageconfig~ImageStyleConfig#options}).
  * @param options.setImageSizes Specifies whether the image `width` and `height` attributes should be set automatically.
  * The default is `true`.
  * @fires execute
  */
  execute(e = {}) {
    const i = this.editor, s = i.model, r = i.plugins.get("ImageUtils");
    s.change((o) => {
      const a = e.value, { setImageSizes: l = !0 } = e;
      let c = r.getClosestSelectedImageElement(s.document.selection);
      a && this.shouldConvertImageType(a, c) && (this.editor.execute(r.isBlockImage(c) ? "imageTypeInline" : "imageTypeBlock", {
        setImageSizes: l
      }), c = r.getClosestSelectedImageElement(s.document.selection)), !a || this._styles.get(a).isDefault ? o.removeAttribute("imageStyle", c) : o.setAttribute("imageStyle", a, c), l && r.setImageNaturalSizeAttributes(c);
    });
  }
  /**
  * Returns `true` if requested style change would trigger the image type change.
  *
  * @param requestedStyle The name of the style (as configured in {@link module:image/imageconfig~ImageStyleConfig#options}).
  * @param imageElement The image model element.
  */
  shouldConvertImageType(e, i) {
    return !this._styles.get(e).modelElements.includes(i.name);
  }
}
const Ml = {
  // This style represents an image placed in the line of text.
  get inline() {
    return {
      name: "inline",
      title: "In line",
      icon: Z.objectInline,
      modelElements: [
        "imageInline"
      ],
      isDefault: !0
    };
  },
  // This style represents an image aligned to the left and wrapped with text.
  get alignLeft() {
    return {
      name: "alignLeft",
      title: "Left aligned image",
      icon: Z.objectLeft,
      modelElements: [
        "imageBlock",
        "imageInline"
      ],
      className: "image-style-align-left"
    };
  },
  // This style represents an image aligned to the left.
  get alignBlockLeft() {
    return {
      name: "alignBlockLeft",
      title: "Left aligned image",
      icon: Z.objectBlockLeft,
      modelElements: [
        "imageBlock"
      ],
      className: "image-style-block-align-left"
    };
  },
  // This style represents a centered image.
  get alignCenter() {
    return {
      name: "alignCenter",
      title: "Centered image",
      icon: Z.objectCenter,
      modelElements: [
        "imageBlock"
      ],
      className: "image-style-align-center"
    };
  },
  // This style represents an image aligned to the right and wrapped with text.
  get alignRight() {
    return {
      name: "alignRight",
      title: "Right aligned image",
      icon: Z.objectRight,
      modelElements: [
        "imageBlock",
        "imageInline"
      ],
      className: "image-style-align-right"
    };
  },
  // This style represents an image aligned to the right.
  get alignBlockRight() {
    return {
      name: "alignBlockRight",
      title: "Right aligned image",
      icon: Z.objectBlockRight,
      modelElements: [
        "imageBlock"
      ],
      className: "image-style-block-align-right"
    };
  },
  // This option is equal to the situation when no style is applied.
  get block() {
    return {
      name: "block",
      title: "Centered image",
      icon: Z.objectCenter,
      modelElements: [
        "imageBlock"
      ],
      isDefault: !0
    };
  },
  // This represents a side image.
  get side() {
    return {
      name: "side",
      title: "Side image",
      icon: Z.objectRight,
      modelElements: [
        "imageBlock"
      ],
      className: "image-style-side"
    };
  }
}, Uk = {
  full: Z.objectFullWidth,
  left: Z.objectBlockLeft,
  right: Z.objectBlockRight,
  center: Z.objectCenter,
  inlineLeft: Z.objectLeft,
  inlineRight: Z.objectRight,
  inline: Z.objectInline
}, Wk = [
  {
    name: "imageStyle:wrapText",
    title: "Wrap text",
    defaultItem: "imageStyle:alignLeft",
    items: [
      "imageStyle:alignLeft",
      "imageStyle:alignRight"
    ]
  },
  {
    name: "imageStyle:breakText",
    title: "Break text",
    defaultItem: "imageStyle:block",
    items: [
      "imageStyle:alignBlockLeft",
      "imageStyle:block",
      "imageStyle:alignBlockRight"
    ]
  }
];
function uz(n) {
  return (n.configuredStyles.options || []).map((i) => fz(i)).filter((i) => mz(i, n));
}
function dz(n, t) {
  return n && t ? {
    options: [
      "inline",
      "alignLeft",
      "alignRight",
      "alignCenter",
      "alignBlockLeft",
      "alignBlockRight",
      "block",
      "side"
    ]
  } : n ? {
    options: [
      "block",
      "side"
    ]
  } : t ? {
    options: [
      "inline",
      "alignLeft",
      "alignRight"
    ]
  } : {};
}
function hz(n) {
  return n.has("ImageBlockEditing") && n.has("ImageInlineEditing") ? [
    ...Wk
  ] : [];
}
function fz(n) {
  return typeof n == "string" ? Ml[n] ? n = {
    ...Ml[n]
  } : n = {
    name: n
  } : n = gz(Ml[n.name], n), typeof n.icon == "string" && (n.icon = Uk[n.icon] || n.icon), n;
}
function mz(n, { isBlockPluginLoaded: t, isInlinePluginLoaded: e }) {
  const { modelElements: i, name: s } = n;
  if (!i || !i.length || !s)
    return qk({
      style: n
    }), !1;
  {
    const r = [
      t ? "imageBlock" : null,
      e ? "imageInline" : null
    ];
    if (!i.some((o) => r.includes(o)))
      return Ve("image-style-missing-dependency", {
        style: n,
        missingPlugins: i.map((o) => o === "imageBlock" ? "ImageBlockEditing" : "ImageInlineEditing")
      }), !1;
  }
  return !0;
}
function gz(n, t) {
  const e = {
    ...t
  };
  for (const i in n)
    Object.prototype.hasOwnProperty.call(t, i) || (e[i] = n[i]);
  return e;
}
function qk(n) {
  Ve("image-style-configuration-definition-invalid", n);
}
var Sh = {
  normalizeStyles: uz,
  getDefaultStylesConfiguration: dz,
  getDefaultDropdownDefinitions: hz,
  warnInvalidStyle: qk,
  DEFAULT_OPTIONS: Ml,
  DEFAULT_ICONS: Uk,
  DEFAULT_DROPDOWN_DEFINITIONS: Wk
};
function pz(n) {
  return (t, e, i) => {
    if (!i.consumable.consume(e.item, t.name))
      return;
    const s = V1(e.attributeNewValue, n), r = V1(e.attributeOldValue, n), o = i.mapper.toViewElement(e.item), a = i.writer;
    r && a.removeClass(r.className, o), s && a.addClass(s.className, o);
  };
}
function bz(n) {
  const t = {
    imageInline: n.filter((e) => !e.isDefault && e.modelElements.includes("imageInline")),
    imageBlock: n.filter((e) => !e.isDefault && e.modelElements.includes("imageBlock"))
  };
  return (e, i, s) => {
    if (!i.modelRange)
      return;
    const r = i.viewItem, o = Xe(i.modelRange.getItems());
    if (o && s.schema.checkAttribute(o, "imageStyle"))
      for (const a of t[o.name])
        s.consumable.consume(r, {
          classes: a.className
        }) && s.writer.setAttribute("imageStyle", a.name, o);
  };
}
function V1(n, t) {
  for (const e of t)
    if (e.name === n)
      return e;
}
class Gk extends I {
  constructor() {
    super(...arguments);
    /**
    * It contains a list of the normalized and validated style options.
    *
    * * Each option contains a complete icon markup.
    * * The style options not supported by any of the loaded image editing plugins (
    * {@link module:image/image/imageinlineediting~ImageInlineEditing `ImageInlineEditing`} or
    * {@link module:image/image/imageblockediting~ImageBlockEditing `ImageBlockEditing`}) are filtered out.
    *
    * @internal
    * @readonly
    */
    f(this, "normalizedStyles");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageStyleEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const { normalizeStyles: e, getDefaultStylesConfiguration: i } = Sh, s = this.editor, r = s.plugins.has("ImageBlockEditing"), o = s.plugins.has("ImageInlineEditing");
    s.config.define("image.styles", i(r, o)), this.normalizedStyles = e({
      configuredStyles: s.config.get("image.styles"),
      isBlockPluginLoaded: r,
      isInlinePluginLoaded: o
    }), this._setupConversion(r, o), this._setupPostFixer(), s.commands.add("imageStyle", new cz(s, this.normalizedStyles));
  }
  /**
  * Sets the editor conversion taking the presence of
  * {@link module:image/image/imageinlineediting~ImageInlineEditing `ImageInlineEditing`}
  * and {@link module:image/image/imageblockediting~ImageBlockEditing `ImageBlockEditing`} plugins into consideration.
  */
  _setupConversion(e, i) {
    const s = this.editor, r = s.model.schema, o = pz(this.normalizedStyles), a = bz(this.normalizedStyles);
    s.editing.downcastDispatcher.on("attribute:imageStyle", o), s.data.downcastDispatcher.on("attribute:imageStyle", o), e && (r.extend("imageBlock", {
      allowAttributes: "imageStyle"
    }), s.data.upcastDispatcher.on("element:figure", a, {
      priority: "low"
    })), i && (r.extend("imageInline", {
      allowAttributes: "imageStyle"
    }), s.data.upcastDispatcher.on("element:img", a, {
      priority: "low"
    }));
  }
  /**
  * Registers a post-fixer that will make sure that the style attribute value is correct for a specific image type (block vs inline).
  */
  _setupPostFixer() {
    const e = this.editor, i = e.model.document, s = e.plugins.get(jt), r = new Map(this.normalizedStyles.map((o) => [
      o.name,
      o
    ]));
    i.registerPostFixer((o) => {
      let a = !1;
      for (const l of i.differ.getChanges())
        if (l.type == "insert" || l.type == "attribute" && l.attributeKey == "imageStyle") {
          let c = l.type == "insert" ? l.position.nodeAfter : l.range.start.nodeAfter;
          if (c && c.is("element", "paragraph") && c.childCount > 0 && (c = c.getChild(0)), !s.isImage(c))
            continue;
          const u = c.getAttribute("imageStyle");
          if (!u)
            continue;
          const d = r.get(u);
          (!d || !d.modelElements.includes(c.name)) && (o.removeAttribute("imageStyle", c), a = !0);
        }
      return a;
    });
  }
}
class wz extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Gk
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageStyleUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Returns the default localized style titles provided by the plugin.
  *
  * The following localized titles corresponding with
  * {@link module:image/imagestyle/utils#DEFAULT_OPTIONS} are available:
  *
  * * `'Wrap text'`,
  * * `'Break text'`,
  * * `'In line'`,
  * * `'Full size image'`,
  * * `'Side image'`,
  * * `'Left aligned image'`,
  * * `'Centered image'`,
  * * `'Right aligned image'`
  */
  get localizedDefaultStylesTitles() {
    const t = this.editor.t;
    return {
      "Wrap text": t("Wrap text"),
      "Break text": t("Break text"),
      "In line": t("In line"),
      "Full size image": t("Full size image"),
      "Side image": t("Side image"),
      "Left aligned image": t("Left aligned image"),
      "Centered image": t("Centered image"),
      "Right aligned image": t("Right aligned image")
    };
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.plugins, e = this.editor.config.get("image.toolbar") || [], i = t.get("ImageStyleEditing"), s = L1(i.normalizedStyles, this.localizedDefaultStylesTitles);
    for (const o of s)
      this._createButton(o);
    const r = L1([
      ...e.filter(nt),
      ...Sh.getDefaultDropdownDefinitions(t)
    ], this.localizedDefaultStylesTitles);
    for (const o of r)
      this._createDropdown(o, s);
  }
  /**
  * Creates a dropdown and stores it in the editor {@link module:ui/componentfactory~ComponentFactory}.
  */
  _createDropdown(t, e) {
    const i = this.editor.ui.componentFactory;
    i.add(t.name, (s) => {
      let r;
      const { defaultItem: o, items: a, title: l } = t, c = a.filter((m) => e.find(({ name: g }) => B1(g) === m)).map((m) => {
        const g = i.create(m);
        return m === o && (r = g), g;
      });
      a.length !== c.length && Sh.warnInvalidStyle({
        dropdown: t
      });
      const u = Tt(s, Ys), d = u.buttonView, h = d.arrowView;
      return Ma(u, c, {
        enableActiveItemFocusOnDropdownOpen: !0
      }), d.set({
        label: M1(l, r.label),
        class: null,
        tooltip: !0
      }), h.unbind("label"), h.set({
        label: l
      }), d.bind("icon").toMany(c, "isOn", (...m) => {
        const g = m.findIndex(cs);
        return g < 0 ? r.icon : c[g].icon;
      }), d.bind("label").toMany(c, "isOn", (...m) => {
        const g = m.findIndex(cs);
        return M1(l, g < 0 ? r.label : c[g].label);
      }), d.bind("isOn").toMany(c, "isOn", (...m) => m.some(cs)), d.bind("class").toMany(c, "isOn", (...m) => m.some(cs) ? "ck-splitbutton_flatten" : void 0), d.on("execute", () => {
        c.some(({ isOn: m }) => m) ? u.isOpen = !u.isOpen : r.fire("execute");
      }), u.bind("isEnabled").toMany(c, "isEnabled", (...m) => m.some(cs)), this.listenTo(u, "execute", () => {
        this.editor.editing.view.focus();
      }), u;
    });
  }
  /**
  * Creates a button and stores it in the editor {@link module:ui/componentfactory~ComponentFactory}.
  */
  _createButton(t) {
    const e = t.name;
    this.editor.ui.componentFactory.add(B1(e), (i) => {
      const s = this.editor.commands.get("imageStyle"), r = new le(i);
      return r.set({
        label: t.title,
        icon: t.icon,
        tooltip: !0,
        isToggleable: !0
      }), r.bind("isEnabled").to(s, "isEnabled"), r.bind("isOn").to(s, "value", (o) => o === e), r.on("execute", this._executeCommand.bind(this, e)), r;
    });
  }
  _executeCommand(t) {
    this.editor.execute("imageStyle", {
      value: t
    }), this.editor.editing.view.focus();
  }
}
function L1(n, t) {
  for (const e of n)
    t[e.title] && (e.title = t[e.title]);
  return n;
}
function B1(n) {
  return `imageStyle:${n}`;
}
function M1(n, t) {
  return (n ? n + ": " : "") + t;
}
class vz extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Gk,
      wz
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageStyle";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class _z extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      oc,
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ImageToolbar";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.t, i = t.plugins.get(oc), s = t.plugins.get("ImageUtils");
    i.register("image", {
      ariaLabel: e("Image toolbar"),
      items: yz(t.config.get("image.toolbar") || []),
      getRelatedElement: (r) => s.getClosestSelectedImageWidget(r)
    });
  }
}
function yz(n) {
  return n.map((t) => nt(t) ? t.name : t);
}
class kz extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      om,
      jt
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PictureEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor;
    t.plugins.has("ImageBlockEditing") && t.model.schema.extend("imageBlock", {
      allowAttributes: [
        "sources"
      ]
    }), t.plugins.has("ImageInlineEditing") && t.model.schema.extend("imageInline", {
      allowAttributes: [
        "sources"
      ]
    }), this._setupConversion(), this._setupImageUploadEditingIntegration();
  }
  /**
  * Configures conversion pipelines to support upcasting and downcasting images using the `<picture>` view element
  * and the model `sources` attribute.
  */
  _setupConversion() {
    const t = this.editor, e = t.conversion, i = t.plugins.get("ImageUtils");
    e.for("upcast").add(_$(i)), e.for("downcast").add(y$(i));
  }
  /**
  * Makes it possible for uploaded images to get the `sources` model attribute and the `<picture>...</picture>`
  * view structure out-of-the-box if relevant data is provided along the
  * {@link module:image/imageupload/imageuploadediting~ImageUploadEditing#event:uploadComplete} event.
  */
  _setupImageUploadEditingIntegration() {
    const t = this.editor;
    if (!t.plugins.has("ImageUploadEditing"))
      return;
    const e = t.plugins.get("ImageUploadEditing");
    this.listenTo(e, "uploadComplete", (i, { imageElement: s, data: r }) => {
      const o = r.sources;
      o && t.model.change((a) => {
        a.setAttributes({
          sources: o
        }, s);
      });
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class Cz extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "IndentEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.commands.add("indent", new tb(t)), t.commands.add("outdent", new tb(t));
  }
}
class xz extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "IndentUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.locale, i = t.t, s = e.uiLanguageDirection == "ltr" ? Z.indent : Z.outdent, r = e.uiLanguageDirection == "ltr" ? Z.outdent : Z.indent;
    this._defineButton("indent", i("Increase indent"), s), this._defineButton("outdent", i("Decrease indent"), r);
  }
  /**
  * Defines UI buttons for both toolbar and menu bar.
  */
  _defineButton(t, e, i) {
    const s = this.editor;
    s.ui.componentFactory.add(t, () => {
      const r = this._createButton(le, t, e, i);
      return r.set({
        tooltip: !0
      }), r;
    }), s.ui.componentFactory.add("menuBar:" + t, () => this._createButton(Fe, t, e, i));
  }
  /**
  * Creates a button to use either in toolbar or in menu bar.
  */
  _createButton(t, e, i, s) {
    const r = this.editor, o = r.commands.get(e), a = new t(r.locale);
    return a.set({
      label: i,
      icon: s
    }), a.bind("isEnabled").to(o, "isEnabled"), this.listenTo(a, "execute", () => {
      r.execute(e), r.editing.view.focus();
    }), a;
  }
}
class Tz extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Indent";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Cz,
      xz
    ];
  }
}
class wl extends he {
  /**
  * Creates an instance of the command.
  */
  constructor(e, i) {
    super(e);
    /**
    * The command's indentation behavior.
    */
    f(this, "_indentBehavior");
    this._indentBehavior = i;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const i = this.editor.model, s = Xe(i.document.selection.getSelectedBlocks());
    if (!s || !this._isIndentationChangeAllowed(s)) {
      this.isEnabled = !1;
      return;
    }
    this.isEnabled = this._indentBehavior.checkEnabled(s.getAttribute("blockIndent"));
  }
  /**
  * @inheritDoc
  */
  execute() {
    const e = this.editor.model, i = this._getBlocksToChange();
    e.change((s) => {
      for (const r of i) {
        const o = r.getAttribute("blockIndent"), a = this._indentBehavior.getNextIndent(o);
        a ? s.setAttribute("blockIndent", a, r) : s.removeAttribute("blockIndent", r);
      }
    });
  }
  /**
  * Returns blocks from selection that should have blockIndent selection set.
  */
  _getBlocksToChange() {
    const i = this.editor.model.document.selection;
    return Array.from(i.getSelectedBlocks()).filter((r) => this._isIndentationChangeAllowed(r));
  }
  /**
  * Returns false if indentation cannot be applied, i.e.:
  * - for blocks disallowed by schema declaration
  * - for blocks in Document Lists (disallowed forward indentation only). See https://github.com/ckeditor/ckeditor5/issues/14155.
  * Otherwise returns true.
  */
  _isIndentationChangeAllowed(e) {
    const i = this.editor;
    return i.model.schema.checkAttribute(e, "blockIndent") ? !i.plugins.has("ListUtils") || !this._indentBehavior.isForward ? !0 : !i.plugins.get("ListUtils").isListItemBlock(e) : !1;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class N1 {
  /**
  * Creates an instance of the indentation behavior.
  *
  * @param config.direction The direction of indentation.
  * @param config.offset The offset of the next indentation step.
  * @param config.unit Indentation unit.
  */
  constructor(t) {
    /**
    * The direction of indentation.
    */
    f(this, "isForward");
    /**
    * The offset of the next indentation step.
    */
    f(this, "offset");
    /**
    * Indentation unit.
    */
    f(this, "unit");
    this.isForward = t.direction === "forward", this.offset = t.offset, this.unit = t.unit;
  }
  /**
  * @inheritDoc
  */
  checkEnabled(t) {
    const e = parseFloat(t || "0");
    return this.isForward || e > 0;
  }
  /**
  * @inheritDoc
  */
  getNextIndent(t) {
    const e = parseFloat(t || "0");
    if (!(!t || t.endsWith(this.unit)))
      return this.isForward ? this.offset + this.unit : void 0;
    const s = this.isForward ? this.offset : -this.offset, r = e + s;
    return r > 0 ? r + this.unit : void 0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class F1 {
  /**
  * Creates an instance of the indentation behavior.
  *
  * @param config.direction The direction of indentation.
  * @param config.classes A list of classes used for indentation.
  */
  constructor(t) {
    /**
    * The direction of indentation.
    */
    f(this, "isForward");
    /**
    * A list of classes used for indentation.
    */
    f(this, "classes");
    this.isForward = t.direction === "forward", this.classes = t.classes;
  }
  /**
  * @inheritDoc
  */
  checkEnabled(t) {
    const e = this.classes.indexOf(t);
    return this.isForward ? e < this.classes.length - 1 : e >= 0;
  }
  /**
  * @inheritDoc
  */
  getNextIndent(t) {
    const e = this.classes.indexOf(t), i = this.isForward ? 1 : -1;
    return this.classes[e + i];
  }
}
const Ez = [
  "paragraph",
  "heading1",
  "heading2",
  "heading3",
  "heading4",
  "heading5",
  "heading6"
];
class Sz extends I {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("indentBlock", {
      offset: 40,
      unit: "px"
    });
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "IndentBlock";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.config.get("indentBlock");
    e.classes && e.classes.length ? (this._setupConversionUsingClasses(e.classes), t.commands.add("indentBlock", new wl(t, new F1({
      direction: "forward",
      classes: e.classes
    }))), t.commands.add("outdentBlock", new wl(t, new F1({
      direction: "backward",
      classes: e.classes
    })))) : (t.data.addStyleProcessorRules(p8), this._setupConversionUsingOffset(), t.commands.add("indentBlock", new wl(t, new N1({
      direction: "forward",
      offset: e.offset,
      unit: e.unit
    }))), t.commands.add("outdentBlock", new wl(t, new N1({
      direction: "backward",
      offset: e.offset,
      unit: e.unit
    }))));
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.model.schema, i = t.commands.get("indent"), s = t.commands.get("outdent"), r = t.config.get("heading.options");
    (r && r.map((l) => l.model) || Ez).forEach((l) => {
      e.isRegistered(l) && e.extend(l, {
        allowAttributes: "blockIndent"
      });
    }), e.setAttributeProperties("blockIndent", {
      isFormatting: !0
    }), i.registerChildCommand(t.commands.get("indentBlock")), s.registerChildCommand(t.commands.get("outdentBlock"));
  }
  /**
  * Setups conversion for using offset indents.
  */
  _setupConversionUsingOffset() {
    const t = this.editor.conversion, i = this.editor.locale.contentLanguageDirection === "rtl" ? "margin-right" : "margin-left";
    t.for("upcast").attributeToAttribute({
      view: {
        styles: {
          [i]: /[\s\S]+/
        }
      },
      model: {
        key: "blockIndent",
        value: (s) => {
          if (!s.is("element", "li"))
            return s.getStyle(i);
        }
      }
    }), t.for("downcast").attributeToAttribute({
      model: "blockIndent",
      view: (s) => ({
        key: "style",
        value: {
          [i]: s
        }
      })
    });
  }
  /**
  * Setups conversion for using classes.
  */
  _setupConversionUsingClasses(t) {
    const e = {
      model: {
        key: "blockIndent",
        values: []
      },
      view: {}
    };
    for (const i of t)
      e.model.values.push(i), e.view[i] = {
        key: "class",
        value: [
          i
        ]
      };
    this.editor.conversion.attributeToAttribute(e);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function am(n, t) {
  return t = t || sh(n), `${n}:${t}`;
}
function Az(n) {
  const [t, e] = n.split(":");
  return {
    languageCode: t,
    textDirection: e
  };
}
class Iz extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document;
    this.value = this._getValueFromFirstAllowedNode(), this.isEnabled = t.schema.checkAttributeInSelection(e.selection, "language");
  }
  /**
  * Executes the command. Applies the attribute to the selection or removes it from the selection.
  *
  * If `languageCode` is set to `false` or a `null` value, it will remove attributes. Otherwise, it will set
  * the attribute in the `{@link #value value}` format.
  *
  * The execution result differs, depending on the {@link module:engine/model/document~Document#selection}:
  *
  * * If the selection is on a range, the command applies the attribute to all nodes in that range
  * (if they are allowed to have this attribute by the {@link module:engine/model/schema~Schema schema}).
  * * If the selection is collapsed in a non-empty node, the command applies the attribute to the
  * {@link module:engine/model/document~Document#selection} itself (note that typed characters copy attributes from the selection).
  * * If the selection is collapsed in an empty node, the command applies the attribute to the parent node of the selection (note
  * that the selection inherits all attributes from a node if it is in an empty node).
  *
  * @fires execute
  * @param options Command options.
  * @param options.languageCode The language code to be applied to the model.
  * @param options.textDirection The language text direction.
  */
  execute({ languageCode: t, textDirection: e } = {}) {
    const i = this.editor.model, r = i.document.selection, o = t ? am(t, e) : !1;
    i.change((a) => {
      if (r.isCollapsed)
        o ? a.setSelectionAttribute("language", o) : a.removeSelectionAttribute("language");
      else {
        const l = i.schema.getValidRanges(r.getRanges(), "language");
        for (const c of l)
          o ? a.setAttribute("language", o, c) : a.removeAttribute("language", c);
      }
    });
  }
  /**
  * Returns the attribute value of the first node in the selection that allows the attribute.
  * For a collapsed selection it returns the selection attribute.
  *
  * @returns The attribute value.
  */
  _getValueFromFirstAllowedNode() {
    const t = this.editor.model, e = t.schema, i = t.document.selection;
    if (i.isCollapsed)
      return i.getAttribute("language") || !1;
    for (const s of i.getRanges())
      for (const r of s.getItems())
        if (e.checkAttribute(r, "language"))
          return r.getAttribute("language") || !1;
    return !1;
  }
}
class Pz extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TextPartLanguageEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("language", {
      textPartLanguage: [
        {
          title: "Arabic",
          languageCode: "ar"
        },
        {
          title: "French",
          languageCode: "fr"
        },
        {
          title: "Spanish",
          languageCode: "es"
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.model.schema.extend("$text", {
      allowAttributes: "language"
    }), t.model.schema.setAttributeProperties("language", {
      copyOnEnter: !0
    }), this._defineConverters(), t.commands.add("textPartLanguage", new Iz(t));
  }
  /**
  * @private
  */
  _defineConverters() {
    const t = this.editor.conversion;
    t.for("upcast").elementToAttribute({
      model: {
        key: "language",
        value: (e) => {
          const i = e.getAttribute("lang"), s = e.getAttribute("dir");
          return am(i, s);
        }
      },
      view: {
        name: "span",
        attributes: {
          lang: /[\s\S]+/
        }
      }
    }), t.for("downcast").attributeToElement({
      model: "language",
      view: (e, { writer: i }, s) => {
        if (!e || !s.item.is("$textProxy") && !s.item.is("documentSelection"))
          return;
        const { languageCode: r, textDirection: o } = Az(e);
        return i.createAttributeElement("span", {
          lang: r,
          dir: o
        });
      }
    });
  }
}
class Rz extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TextPartLanguageUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t, i = e("Choose language"), s = e("Language");
    t.ui.componentFactory.add("textPartLanguage", (r) => {
      const { definitions: o, titles: a } = this._getItemMetadata(), l = t.commands.get("textPartLanguage"), c = Tt(r);
      return En(c, o, {
        ariaLabel: s,
        role: "menu"
      }), c.buttonView.set({
        ariaLabel: s,
        ariaLabelledBy: void 0,
        isOn: !1,
        withText: !0,
        tooltip: s
      }), c.extendTemplate({
        attributes: {
          class: [
            "ck-text-fragment-language-dropdown"
          ]
        }
      }), c.bind("isEnabled").to(l, "isEnabled"), c.buttonView.bind("label").to(l, "value", (u) => u && a[u] || i), c.buttonView.bind("ariaLabel").to(l, "value", (u) => {
        const d = u && a[u];
        return d ? `${d}, ${s}` : s;
      }), this.listenTo(c, "execute", (u) => {
        l.execute({
          languageCode: u.source.languageCode,
          textDirection: u.source.textDirection
        }), t.editing.view.focus();
      }), c;
    }), t.ui.componentFactory.add("menuBar:textPartLanguage", (r) => {
      const { definitions: o } = this._getItemMetadata(), a = t.commands.get("textPartLanguage"), l = new Yt(r);
      l.buttonView.set({
        label: s
      });
      const c = new kn(r);
      c.set({
        ariaLabel: e("Language"),
        role: "menu"
      });
      for (const u of o) {
        if (u.type != "button") {
          c.items.add(new Ba(r));
          continue;
        }
        const d = new en(r, l), h = new Fe(r);
        h.set({
          role: "menuitemradio",
          isToggleable: !0
        }), h.bind(...Object.keys(u.model)).to(u.model), h.delegate("execute").to(l), d.children.add(h), c.items.add(d);
      }
      return l.bind("isEnabled").to(a, "isEnabled"), l.panelView.children.add(c), l.on("execute", (u) => {
        a.execute({
          languageCode: u.source.languageCode,
          textDirection: u.source.textDirection
        }), t.editing.view.focus();
      }), l;
    });
  }
  /**
  * Returns metadata for dropdown and menu items.
  */
  _getItemMetadata() {
    const t = this.editor, e = new bt(), i = {}, s = t.commands.get("textPartLanguage"), r = t.config.get("language.textPartLanguage"), o = t.locale.t, a = o("Remove language");
    e.add({
      type: "button",
      model: new tn({
        label: a,
        languageCode: !1,
        withText: !0
      })
    }), e.add({
      type: "separator"
    });
    for (const l of r) {
      const c = {
        type: "button",
        model: new tn({
          label: l.title,
          languageCode: l.languageCode,
          role: "menuitemradio",
          textDirection: l.textDirection,
          withText: !0
        })
      }, u = am(l.languageCode, l.textDirection);
      c.model.bind("isOn").to(s, "value", (d) => d === u), e.add(c), i[u] = l.title;
    }
    return {
      definitions: e,
      titles: i
    };
  }
}
class Oz extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Pz,
      Rz
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TextPartLanguage";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class Vz {
  constructor() {
    /**
    * Stores the definition of {@link module:link/linkconfig~LinkDecoratorAutomaticDefinition automatic decorators}.
    * This data is used as a source for a downcast dispatcher to create a proper conversion to output data.
    */
    f(this, "_definitions", /* @__PURE__ */ new Set());
  }
  /**
  * Gives information about the number of decorators stored in the {@link module:link/utils/automaticdecorators~AutomaticDecorators}
  * instance.
  */
  get length() {
    return this._definitions.size;
  }
  /**
  * Adds automatic decorator objects or an array with them to be used during downcasting.
  *
  * @param item A configuration object of automatic rules for decorating links. It might also be an array of such objects.
  */
  add(t) {
    Array.isArray(t) ? t.forEach((e) => this._definitions.add(e)) : this._definitions.add(t);
  }
  /**
  * Provides the conversion helper used in the {@link module:engine/conversion/downcasthelpers~DowncastHelpers#add} method.
  *
  * @returns A dispatcher function used as conversion helper in {@link module:engine/conversion/downcasthelpers~DowncastHelpers#add}.
  */
  getDispatcher() {
    return (t) => {
      t.on("attribute:linkHref", (e, i, s) => {
        if (!s.consumable.test(i.item, "attribute:linkHref") || !(i.item.is("selection") || s.schema.isInline(i.item)))
          return;
        const r = s.writer, o = r.document.selection;
        for (const a of this._definitions) {
          const l = r.createAttributeElement("a", a.attributes, {
            priority: 5
          });
          a.classes && r.addClass(a.classes, l);
          for (const c in a.styles)
            r.setStyle(c, a.styles[c], l);
          r.setCustomProperty("link", !0, l), a.callback(i.attributeNewValue) ? i.item.is("selection") ? r.wrap(o.getFirstRange(), l) : r.wrap(s.mapper.toViewRange(i.range), l) : r.unwrap(s.mapper.toViewRange(i.range), l);
        }
      }, {
        priority: "high"
      });
    };
  }
  /**
  * Provides the conversion helper used in the {@link module:engine/conversion/downcasthelpers~DowncastHelpers#add} method
  * when linking images.
  *
  * @returns A dispatcher function used as conversion helper in {@link module:engine/conversion/downcasthelpers~DowncastHelpers#add}.
  */
  getDispatcherForLinkedImage() {
    return (t) => {
      t.on("attribute:linkHref:imageBlock", (e, i, { writer: s, mapper: r }) => {
        const o = r.toViewElement(i.item), a = Array.from(o.getChildren()).find((l) => l.is("element", "a"));
        if (a)
          for (const l of this._definitions) {
            const c = Ji(l.attributes);
            if (l.callback(i.attributeNewValue)) {
              for (const [u, d] of c)
                u === "class" ? s.addClass(d, a) : s.setAttribute(u, d, a);
              l.classes && s.addClass(l.classes, a);
              for (const u in l.styles)
                s.setStyle(u, l.styles[u], a);
            } else {
              for (const [u, d] of c)
                u === "class" ? s.removeClass(d, a) : s.removeAttribute(u, a);
              l.classes && s.removeClass(l.classes, a);
              for (const u in l.styles)
                s.removeStyle(u, a);
            }
          }
      });
    };
  }
}
const Lz = /[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205f\u3000]/g, Bz = "^(?:(?:<protocols>):|[^a-z]|[a-z+.-]+(?:[^a-z+.:-]|$))", Mz = /^[\S]+@((?![-_])(?:[-\w\u00a1-\uffff]{0,63}[^-_]\.))+(?:[a-z\u00a1-\uffff]{2,})$/i, Nz = /^((\w+:(\/{2,})?)|(\W))/i, Fz = [
  "https?",
  "ftps?",
  "mailto"
], Lo = "Ctrl+K";
function Dz(n) {
  return n.is("attributeElement") && !!n.getCustomProperty("link");
}
function D1(n, { writer: t }) {
  const e = t.createAttributeElement("a", {
    href: n
  }, {
    priority: 5
  });
  return t.setCustomProperty("link", !0, e), e;
}
function Kk(n, t = Fz) {
  const e = String(n), i = t.join("|"), s = new RegExp(`${Bz.replace("<protocols>", i)}`, "i");
  return $z(e, s) ? e : "#";
}
function $z(n, t) {
  return !!n.replace(Lz, "").match(t);
}
function zz(n, t) {
  const e = {
    "Open in a new tab": n("Open in a new tab"),
    Downloadable: n("Downloadable")
  };
  return t.forEach((i) => ("label" in i && e[i.label] && (i.label = e[i.label]), i)), t;
}
function Hz(n) {
  const t = [];
  if (n)
    for (const [e, i] of Object.entries(n)) {
      const s = Object.assign({}, i, {
        id: `link${hv(e)}`
      });
      t.push(s);
    }
  return t;
}
function Ah(n, t) {
  return n ? t.checkAttribute(n.name, "linkHref") : !1;
}
function jz(n) {
  return Mz.test(n);
}
function lm(n, t) {
  const e = jz(n) ? "mailto:" : t, i = !!e && !Zk(n);
  return n && i ? e + n : n;
}
function Zk(n) {
  return Nz.test(n);
}
function Yk(n) {
  window.open(n, "_blank", "noopener");
}
function Jk(n) {
  const t = n.plugins.has("BookmarkEditing") ? n.plugins.get("BookmarkEditing") : null;
  function e(s) {
    return !!s && s.startsWith("#") && !!t && !!t.getElementForBookmarkId(s.slice(1));
  }
  function i(s) {
    const r = s.slice(1), o = t.getElementForBookmarkId(r);
    n.model.change((a) => {
      a.setSelection(o, "on");
    }), n.editing.view.scrollToTheSelection({
      alignToTop: !0,
      forceScroll: !0
    });
  }
  return {
    isScrollableToTarget: e,
    scrollToTarget: i
  };
}
class Uz extends he {
  constructor() {
    super(...arguments);
    /**
    * A collection of {@link module:link/utils/manualdecorator~ManualDecorator manual decorators}
    * corresponding to the {@link module:link/linkconfig~LinkConfig#decorators decorator configuration}.
    *
    * You can consider it a model with states of manual decorators added to the currently selected link.
    */
    f(this, "manualDecorators", new bt());
    /**
    * An instance of the helper that ties together all {@link module:link/linkconfig~LinkDecoratorAutomaticDefinition}
    * that are used by the {@glink features/link link} and the {@glink features/images/images-linking linking images} features.
    */
    f(this, "automaticDecorators", new Vz());
  }
  /**
  * Synchronizes the state of {@link #manualDecorators} with the currently present elements in the model.
  */
  restoreManualDecoratorStates() {
    for (const e of this.manualDecorators)
      e.value = this._getDecoratorStateFromModel(e.id);
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model, i = e.document.selection, s = i.getSelectedElement() || Xe(i.getSelectedBlocks());
    Ah(s, e.schema) ? (this.value = s.getAttribute("linkHref"), this.isEnabled = e.schema.checkAttribute(s, "linkHref")) : (this.value = i.getAttribute("linkHref"), this.isEnabled = e.schema.checkAttributeInSelection(i, "linkHref"));
    for (const r of this.manualDecorators)
      r.value = this._getDecoratorStateFromModel(r.id);
  }
  /**
  * Executes the command.
  *
  * When the selection is non-collapsed, the `linkHref` attribute will be applied to nodes inside the selection, but only to
  * those nodes where the `linkHref` attribute is allowed (disallowed nodes will be omitted).
  *
  * When the selection is collapsed and is not inside the text with the `linkHref` attribute, a
  * new {@link module:engine/model/text~Text text node} with the `linkHref` attribute will be inserted in place of the caret, but
  * only if such element is allowed in this place. The `_data` of the inserted text will equal the `href` parameter.
  * The selection will be updated to wrap the just inserted text node.
  *
  * When the selection is collapsed and inside the text with the `linkHref` attribute, the attribute value will be updated.
  *
  * # Decorators and model attribute management
  *
  * There is an optional argument to this command that applies or removes model
  * {@glink framework/architecture/editing-engine#text-attributes text attributes} brought by
  * {@link module:link/utils/manualdecorator~ManualDecorator manual link decorators}.
  *
  * Text attribute names in the model correspond to the entries in the {@link module:link/linkconfig~LinkConfig#decorators
  * configuration}.
  * For every decorator configured, a model text attribute exists with the "link" prefix. For example, a `'linkMyDecorator'` attribute
  * corresponds to `'myDecorator'` in the configuration.
  *
  * To learn more about link decorators, check out the {@link module:link/linkconfig~LinkConfig#decorators `config.link.decorators`}
  * documentation.
  *
  * Here is how to manage decorator attributes with the link command:
  *
  * ```ts
  * const linkCommand = editor.commands.get( 'link' );
  *
  * // Adding a new decorator attribute.
  * linkCommand.execute( 'http://example.com', {
  * 	linkIsExternal: true
  * } );
  *
  * // Removing a decorator attribute from the selection.
  * linkCommand.execute( 'http://example.com', {
  * 	linkIsExternal: false
  * } );
  *
  * // Adding multiple decorator attributes at the same time.
  * linkCommand.execute( 'http://example.com', {
  * 	linkIsExternal: true,
  * 	linkIsDownloadable: true,
  * } );
  *
  * // Removing and adding decorator attributes at the same time.
  * linkCommand.execute( 'http://example.com', {
  * 	linkIsExternal: false,
  * 	linkFoo: true,
  * 	linkIsDownloadable: false,
  * } );
  * ```
  *
  * **Note**: If the decorator attribute name is not specified, its state remains untouched.
  *
  * **Note**: {@link module:link/unlinkcommand~UnlinkCommand#execute `UnlinkCommand#execute()`} removes all
  * decorator attributes.
  *
  * @fires execute
  * @param href Link destination.
  * @param manualDecoratorIds The information about manual decorator attributes to be applied or removed upon execution.
  */
  execute(e, i = {}) {
    const s = this.editor.model, r = s.document.selection, o = [], a = [];
    for (const l in i)
      i[l] ? o.push(l) : a.push(l);
    s.change((l) => {
      if (r.isCollapsed) {
        const c = r.getFirstPosition();
        if (r.hasAttribute("linkHref")) {
          const u = $1(r);
          let d = Na(c, "linkHref", r.getAttribute("linkHref"), s);
          r.getAttribute("linkHref") === u && (d = this._updateLinkContent(s, l, d, e)), l.setAttribute("linkHref", e, d), o.forEach((h) => {
            l.setAttribute(h, !0, d);
          }), a.forEach((h) => {
            l.removeAttribute(h, d);
          }), l.setSelection(l.createPositionAfter(d.end.nodeBefore));
        } else if (e !== "") {
          const u = Ji(r.getAttributes());
          u.set("linkHref", e), o.forEach((h) => {
            u.set(h, !0);
          });
          const { end: d } = s.insertContent(l.createText(e, u), c);
          l.setSelection(d);
        }
        [
          "linkHref",
          ...o,
          ...a
        ].forEach((u) => {
          l.removeSelectionAttribute(u);
        });
      } else {
        const c = s.schema.getValidRanges(r.getRanges(), "linkHref"), u = [];
        for (const h of r.getSelectedBlocks())
          s.schema.checkAttribute(h, "linkHref") && u.push(l.createRangeOn(h));
        const d = u.slice();
        for (const h of c)
          this._isRangeToUpdate(h, u) && d.push(h);
        for (const h of d) {
          let m = h;
          if (d.length === 1) {
            const g = $1(r);
            r.getAttribute("linkHref") === g && (m = this._updateLinkContent(s, l, h, e), l.setSelection(l.createSelection(m)));
          }
          l.setAttribute("linkHref", e, m), o.forEach((g) => {
            l.setAttribute(g, !0, m);
          }), a.forEach((g) => {
            l.removeAttribute(g, m);
          });
        }
      }
    });
  }
  /**
  * Provides information whether a decorator with a given name is present in the currently processed selection.
  *
  * @param decoratorName The name of the manual decorator used in the model
  * @returns The information whether a given decorator is currently present in the selection.
  */
  _getDecoratorStateFromModel(e) {
    const i = this.editor.model, s = i.document.selection, r = s.getSelectedElement();
    return Ah(r, i.schema) ? r.getAttribute(e) : s.getAttribute(e);
  }
  /**
  * Checks whether specified `range` is inside an element that accepts the `linkHref` attribute.
  *
  * @param range A range to check.
  * @param allowedRanges An array of ranges created on elements where the attribute is accepted.
  */
  _isRangeToUpdate(e, i) {
    for (const s of i)
      if (s.containsRange(e))
        return !1;
    return !0;
  }
  /**
  * Updates selected link with a new value as its content and as its href attribute.
  *
  * @param model Model is need to insert content.
  * @param writer Writer is need to create text element in model.
  * @param range A range where should be inserted content.
  * @param href A link value which should be in the href attribute and in the content.
  */
  _updateLinkContent(e, i, s, r) {
    const o = i.createText(r, {
      linkHref: r
    });
    return e.insertContent(o, s);
  }
}
function $1(n) {
  if (n.isCollapsed) {
    const t = n.getFirstPosition();
    return t.textNode && t.textNode.data;
  } else {
    const t = Array.from(n.getFirstRange().getItems());
    if (t.length > 1)
      return null;
    const e = t[0];
    return e.is("$text") || e.is("$textProxy") ? e.data : null;
  }
}
class Wz extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document.selection, i = e.getSelectedElement();
    Ah(i, t.schema) ? this.isEnabled = t.schema.checkAttribute(i, "linkHref") : this.isEnabled = t.schema.checkAttributeInSelection(e, "linkHref");
  }
  /**
  * Executes the command.
  *
  * When the selection is collapsed, it removes the `linkHref` attribute from each node with the same `linkHref` attribute value.
  * When the selection is non-collapsed, it removes the `linkHref` attribute from each node in selected ranges.
  *
  * # Decorators
  *
  * If {@link module:link/linkconfig~LinkConfig#decorators `config.link.decorators`} is specified,
  * all configured decorators are removed together with the `linkHref` attribute.
  *
  * @fires execute
  */
  execute() {
    const t = this.editor, e = this.editor.model, i = e.document.selection, s = t.commands.get("link");
    e.change((r) => {
      const o = i.isCollapsed ? [
        Na(i.getFirstPosition(), "linkHref", i.getAttribute("linkHref"), e)
      ] : e.schema.getValidRanges(i.getRanges(), "linkHref");
      for (const a of o)
        if (r.removeAttribute("linkHref", a), s)
          for (const l of s.manualDecorators)
            r.removeAttribute(l.id, a);
    });
  }
}
class qz extends (/* @__PURE__ */ je()) {
  /**
  * Creates a new instance of {@link module:link/utils/manualdecorator~ManualDecorator}.
  *
  * @param config.id The name of the attribute used in the model that represents a given manual decorator.
  * For example: `'linkIsExternal'`.
  * @param config.label The label used in the user interface to toggle the manual decorator.
  * @param config.attributes A set of attributes added to output data when the decorator is active for a specific link.
  * Attributes should keep the format of attributes defined in {@link module:engine/view/elementdefinition~ElementDefinition}.
  * @param [config.defaultValue] Controls whether the decorator is "on" by default.
  */
  constructor({ id: e, label: i, attributes: s, classes: r, styles: o, defaultValue: a }) {
    super();
    /**
    * An ID of a manual decorator which is the name of the attribute in the model, for example: 'linkManualDecorator0'.
    */
    f(this, "id");
    /**
    * The default value of manual decorator.
    */
    f(this, "defaultValue");
    /**
    * The label used in the user interface to toggle the manual decorator.
    */
    f(this, "label");
    /**
    * A set of attributes added to downcasted data when the decorator is activated for a specific link.
    * Attributes should be added in a form of attributes defined in {@link module:engine/view/elementdefinition~ElementDefinition}.
    */
    f(this, "attributes");
    /**
    * A set of classes added to downcasted data when the decorator is activated for a specific link.
    * Classes should be added in a form of classes defined in {@link module:engine/view/elementdefinition~ElementDefinition}.
    */
    f(this, "classes");
    /**
    * A set of styles added to downcasted data when the decorator is activated for a specific link.
    * Styles should be added in a form of styles defined in {@link module:engine/view/elementdefinition~ElementDefinition}.
    */
    f(this, "styles");
    this.id = e, this.set("value", void 0), this.defaultValue = a, this.label = i, this.attributes = s, this.classes = r, this.styles = o;
  }
  /**
  * Returns {@link module:engine/view/matcher~MatcherPattern} with decorator attributes.
  *
  * @internal
  */
  _createPattern() {
    return {
      attributes: this.attributes,
      classes: this.classes,
      styles: this.styles
    };
  }
}
const Gz = "ck-link_selected", z1 = "automatic", Kz = "manual", Zz = /^(https?:)?\/\//;
class nu extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      sc,
      Xy,
      _i
    ];
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("link", {
      allowCreatingEmptyLinks: !1,
      addTargetToExternalLinks: !1
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.config.get("link.allowedProtocols");
    t.model.schema.extend("$text", {
      allowAttributes: "linkHref"
    }), t.conversion.for("dataDowncast").attributeToElement({
      model: "linkHref",
      view: D1
    }), t.conversion.for("editingDowncast").attributeToElement({
      model: "linkHref",
      view: (r, o) => D1(Kk(r, e), o)
    }), t.conversion.for("upcast").elementToAttribute({
      view: {
        name: "a",
        attributes: {
          href: !0
        }
      },
      model: {
        key: "linkHref",
        value: (r) => r.getAttribute("href")
      }
    }), t.commands.add("link", new Uz(t)), t.commands.add("unlink", new Wz(t));
    const i = zz(t.t, Hz(t.config.get("link.decorators")));
    this._enableAutomaticDecorators(i.filter((r) => r.mode === z1)), this._enableManualDecorators(i.filter((r) => r.mode === Kz)), t.plugins.get(sc).registerAttribute("linkHref"), ik(t, "linkHref", "a", Gz), this._enableLinkOpen(), this._enableSelectionAttributesFixer(), this._enableClipboardIntegration();
  }
  /**
  * Processes an array of configured {@link module:link/linkconfig~LinkDecoratorAutomaticDefinition automatic decorators}
  * and registers a {@link module:engine/conversion/downcastdispatcher~DowncastDispatcher downcast dispatcher}
  * for each one of them. Downcast dispatchers are obtained using the
  * {@link module:link/utils/automaticdecorators~AutomaticDecorators#getDispatcher} method.
  *
  * **Note**: This method also activates the automatic external link decorator if enabled with
  * {@link module:link/linkconfig~LinkConfig#addTargetToExternalLinks `config.link.addTargetToExternalLinks`}.
  */
  _enableAutomaticDecorators(t) {
    const e = this.editor, s = e.commands.get("link").automaticDecorators;
    e.config.get("link.addTargetToExternalLinks") && s.add({
      id: "linkIsExternal",
      mode: z1,
      callback: (r) => !!r && Zz.test(r),
      attributes: {
        target: "_blank",
        rel: "noopener noreferrer"
      }
    }), s.add(t), s.length && e.conversion.for("downcast").add(s.getDispatcher());
  }
  /**
  * Processes an array of configured {@link module:link/linkconfig~LinkDecoratorManualDefinition manual decorators},
  * transforms them into {@link module:link/utils/manualdecorator~ManualDecorator} instances and stores them in the
  * {@link module:link/linkcommand~LinkCommand#manualDecorators} collection (a model for manual decorators state).
  *
  * Also registers an {@link module:engine/conversion/downcasthelpers~DowncastHelpers#attributeToElement attribute-to-element}
  * converter for each manual decorator and extends the {@link module:engine/model/schema~Schema model's schema}
  * with adequate model attributes.
  */
  _enableManualDecorators(t) {
    if (!t.length)
      return;
    const e = this.editor, s = e.commands.get("link").manualDecorators;
    t.forEach((r) => {
      e.model.schema.extend("$text", {
        allowAttributes: r.id
      });
      const o = new qz(r);
      s.add(o), e.conversion.for("downcast").attributeToElement({
        model: o.id,
        view: (a, { writer: l, schema: c }, { item: u }) => {
          if ((u.is("selection") || c.isInline(u)) && a) {
            const d = l.createAttributeElement("a", o.attributes, {
              priority: 5
            });
            o.classes && l.addClass(o.classes, d);
            for (const h in o.styles)
              l.setStyle(h, o.styles[h], d);
            return l.setCustomProperty("link", !0, d), d;
          }
        }
      }), e.conversion.for("upcast").elementToAttribute({
        view: {
          name: "a",
          ...o._createPattern()
        },
        model: {
          key: o.id
        }
      });
    });
  }
  /**
  * Attaches handlers for {@link module:engine/view/document~Document#event:enter} and
  * {@link module:engine/view/document~Document#event:click} to enable link following.
  */
  _enableLinkOpen() {
    const t = this.editor, i = t.editing.view.document, s = Jk(t);
    function r(o) {
      s.isScrollableToTarget(o) ? s.scrollToTarget(o) : Yk(o);
    }
    this.listenTo(i, "click", (o, a) => {
      if (!(be.isMac ? a.domEvent.metaKey : a.domEvent.ctrlKey))
        return;
      let c = a.domTarget;
      if (c.tagName.toLowerCase() != "a" && (c = c.closest("a")), !c)
        return;
      const u = c.getAttribute("href");
      u && (o.stop(), a.preventDefault(), r(u));
    }, {
      context: "$capture"
    }), this.listenTo(i, "keydown", (o, a) => {
      const c = t.commands.get("link").value;
      c && a.keyCode === Ie.enter && a.altKey && (o.stop(), r(c));
    });
  }
  /**
  * Watches the DocumentSelection attribute changes and removes link decorator attributes when the linkHref attribute is removed.
  *
  * This is to ensure that there is no left-over link decorator attributes on the document selection that is no longer in a link.
  */
  _enableSelectionAttributesFixer() {
    const e = this.editor.model, i = e.document.selection;
    this.listenTo(i, "change:attribute", (s, { attributeKeys: r }) => {
      !r.includes("linkHref") || i.hasAttribute("linkHref") || e.change((o) => {
        Yz(o, Jz(e.schema));
      });
    });
  }
  /**
  * Enables URL fixing on pasting.
  */
  _enableClipboardIntegration() {
    const t = this.editor, e = t.model, i = this.editor.config.get("link.defaultProtocol");
    i && this.listenTo(t.plugins.get("ClipboardPipeline"), "contentInsertion", (s, r) => {
      e.change((o) => {
        const a = o.createRangeIn(r.content);
        for (const l of a.getItems())
          if (l.hasAttribute("linkHref")) {
            const c = lm(l.getAttribute("linkHref"), i);
            o.setAttribute("linkHref", c, l);
          }
      });
    });
  }
}
function Yz(n, t) {
  n.removeSelectionAttribute("linkHref");
  for (const e of t)
    n.removeSelectionAttribute(e);
}
function Jz(n) {
  return n.getDefinition("$text").allowAttributes.filter((e) => e.startsWith("link"));
}
class Xz extends ee {
  /**
  * Creates an instance of the {@link module:link/ui/linkformview~LinkFormView} class.
  *
  * Also see {@link #render}.
  *
  * @param locale The localization services instance.
  * @param linkCommand Reference to {@link module:link/linkcommand~LinkCommand}.
  * @param validators  Form validators used by {@link #isValid}.
  */
  constructor(e, i, s) {
    super(e);
    /**
    * Tracks information about DOM focus in the form.
    */
    f(this, "focusTracker", new ze());
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes", new Ze());
    /**
    * The URL input view.
    */
    f(this, "urlInputView");
    /**
    * The Save button view.
    */
    f(this, "saveButtonView");
    /**
    * The Cancel button view.
    */
    f(this, "cancelButtonView");
    /**
    * A collection of {@link module:ui/button/switchbuttonview~SwitchButtonView},
    * which corresponds to {@link module:link/linkcommand~LinkCommand#manualDecorators manual decorators}
    * configured in the editor.
    */
    f(this, "_manualDecoratorSwitches");
    /**
    * A collection of child views in the form.
    */
    f(this, "children");
    /**
    * An array of form validators used by {@link #isValid}.
    */
    f(this, "_validators");
    /**
    * A collection of views that can be focused in the form.
    */
    f(this, "_focusables", new Et());
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    f(this, "_focusCycler");
    const r = e.t;
    this._validators = s, this.urlInputView = this._createUrlInput(), this.saveButtonView = this._createButton(r("Save"), Z.check, "ck-button-save"), this.saveButtonView.type = "submit", this.cancelButtonView = this._createButton(r("Cancel"), Z.cancel, "ck-button-cancel", "cancel"), this._manualDecoratorSwitches = this._createManualDecoratorSwitches(i), this.children = this._createFormChildren(i.manualDecorators), this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    });
    const o = [
      "ck",
      "ck-link-form",
      "ck-responsive-form"
    ];
    i.manualDecorators.length && o.push("ck-link-form_layout-vertical", "ck-vertical-form"), this.setTemplate({
      tag: "form",
      attributes: {
        class: o,
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: this.children
    });
  }
  /**
  * Obtains the state of the {@link module:ui/button/switchbuttonview~SwitchButtonView switch buttons} representing
  * {@link module:link/linkcommand~LinkCommand#manualDecorators manual link decorators}
  * in the {@link module:link/ui/linkformview~LinkFormView}.
  *
  * @returns Key-value pairs, where the key is the name of the decorator and the value is its state.
  */
  getDecoratorSwitchesState() {
    return Array.from(this._manualDecoratorSwitches).reduce((e, i) => (e[i.name] = i.isOn, e), {});
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Tn({
      view: this
    }), [
      this.urlInputView,
      ...this._manualDecoratorSwitches,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((i) => {
      this._focusables.add(i), this.focusTracker.add(i.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist {@link #_focusables} in the form.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Validates the form and returns `false` when some fields are invalid.
  */
  isValid() {
    this.resetFormStatus();
    for (const e of this._validators) {
      const i = e(this);
      if (i)
        return this.urlInputView.errorText = i, !1;
    }
    return !0;
  }
  /**
  * Cleans up the supplementary error and information text of the {@link #urlInputView}
  * bringing them back to the state when the form has been displayed for the first time.
  *
  * See {@link #isValid}.
  */
  resetFormStatus() {
    this.urlInputView.errorText = null;
  }
  /**
  * Creates a labeled input view.
  *
  * @returns Labeled field view instance.
  */
  _createUrlInput() {
    const e = this.locale.t, i = new gt(this.locale, pi);
    return i.fieldView.inputMode = "url", i.label = e("Link URL"), i;
  }
  /**
  * Creates a button view.
  *
  * @param label The button label.
  * @param icon The button icon.
  * @param className The additional button CSS class name.
  * @param eventName An event name that the `ButtonView#execute` event will be delegated to.
  * @returns The button view instance.
  */
  _createButton(e, i, s, r) {
    const o = new le(this.locale);
    return o.set({
      label: e,
      icon: i,
      tooltip: !0
    }), o.extendTemplate({
      attributes: {
        class: s
      }
    }), r && o.delegate("execute").to(this, r), o;
  }
  /**
  * Populates {@link module:ui/viewcollection~ViewCollection} of {@link module:ui/button/switchbuttonview~SwitchButtonView}
  * made based on {@link module:link/linkcommand~LinkCommand#manualDecorators}.
  *
  * @param linkCommand A reference to the link command.
  * @returns ViewCollection of switch buttons.
  */
  _createManualDecoratorSwitches(e) {
    const i = this.createCollection();
    for (const s of e.manualDecorators) {
      const r = new js(this.locale);
      r.set({
        name: s.id,
        label: s.label,
        withText: !0
      }), r.bind("isOn").toMany([
        s,
        e
      ], "value", (o, a) => a === void 0 && o === void 0 ? !!s.defaultValue : !!o), r.on("execute", () => {
        s.set("value", !r.isOn);
      }), i.add(r);
    }
    return i;
  }
  /**
  * Populates the {@link #children} collection of the form.
  *
  * If {@link module:link/linkcommand~LinkCommand#manualDecorators manual decorators} are configured in the editor, it creates an
  * additional `View` wrapping all {@link #_manualDecoratorSwitches} switch buttons corresponding
  * to these decorators.
  *
  * @param manualDecorators A reference to
  * the collection of manual decorators stored in the link command.
  * @returns The children of link form view.
  */
  _createFormChildren(e) {
    const i = this.createCollection();
    if (i.add(this.urlInputView), e.length) {
      const s = new ee();
      s.setTemplate({
        tag: "ul",
        children: this._manualDecoratorSwitches.map((r) => ({
          tag: "li",
          children: [
            r
          ],
          attributes: {
            class: [
              "ck",
              "ck-list__item"
            ]
          }
        })),
        attributes: {
          class: [
            "ck",
            "ck-reset",
            "ck-list"
          ]
        }
      }), i.add(s);
    }
    return i.add(this.saveButtonView), i.add(this.cancelButtonView), i;
  }
  /**
  * The native DOM `value` of the {@link #urlInputView} element.
  *
  * **Note**: Do not confuse it with the {@link module:ui/inputtext/inputtextview~InputTextView#value}
  * which works one way only and may not represent the actual state of the component in the DOM.
  */
  get url() {
    const { element: e } = this.urlInputView.fieldView;
    return e ? e.value.trim() : null;
  }
}
var Qz = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.077 15 .991-1.416a.75.75 0 1 1 1.229.86l-1.148 1.64a.748.748 0 0 1-.217.206 5.251 5.251 0 0 1-8.503-5.955.741.741 0 0 1 .12-.274l1.147-1.639a.75.75 0 1 1 1.228.86L4.933 10.7l.006.003a3.75 3.75 0 0 0 6.132 4.294l.006.004zm5.494-5.335a.748.748 0 0 1-.12.274l-1.147 1.639a.75.75 0 1 1-1.228-.86l.86-1.23a3.75 3.75 0 0 0-6.144-4.301l-.86 1.229a.75.75 0 0 1-1.229-.86l1.148-1.64a.748.748 0 0 1 .217-.206 5.251 5.251 0 0 1 8.503 5.955zm-4.563-2.532a.75.75 0 0 1 .184 1.045l-3.155 4.505a.75.75 0 1 1-1.229-.86l3.155-4.506a.75.75 0 0 1 1.045-.184zm4.919 10.562-1.414 1.414a.75.75 0 1 1-1.06-1.06l1.414-1.415-1.415-1.414a.75.75 0 0 1 1.061-1.06l1.414 1.414 1.414-1.415a.75.75 0 0 1 1.061 1.061l-1.414 1.414 1.414 1.415a.75.75 0 0 1-1.06 1.06l-1.415-1.414z"/></svg>';
class eH extends ee {
  /**
  * @inheritDoc
  */
  constructor(e, i = {}, s) {
    super(e);
    /**
    * Tracks information about DOM focus in the actions.
    */
    f(this, "focusTracker", new ze());
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes", new Ze());
    /**
    * The href preview view.
    */
    f(this, "previewButtonView");
    /**
    * The unlink button view.
    */
    f(this, "unlinkButtonView");
    /**
    * The edit link button view.
    */
    f(this, "editButtonView");
    /**
    * A collection of views that can be focused in the view.
    */
    f(this, "_focusables", new Et());
    /**
    * Helps cycling over {@link #_focusables} in the view.
    */
    f(this, "_focusCycler");
    f(this, "_linkConfig");
    f(this, "_options");
    const r = e.t;
    this._options = s, this.previewButtonView = this._createPreviewButton(), this.unlinkButtonView = this._createButton(r("Unlink"), Qz, "unlink"), this.editButtonView = this._createButton(r("Edit link"), Z.pencil, "edit"), this.set("href", void 0), this._linkConfig = i, this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-link-actions",
          "ck-responsive-form"
        ],
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: [
        this.previewButtonView,
        this.editButtonView,
        this.unlinkButtonView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), [
      this.previewButtonView,
      this.editButtonView,
      this.unlinkButtonView
    ].forEach((i) => {
      this._focusables.add(i), this.focusTracker.add(i.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist {@link #_focusables} in the actions.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Creates a button view.
  *
  * @param label The button label.
  * @param icon The button icon.
  * @param eventName An event name that the `ButtonView#execute` event will be delegated to.
  * @returns The button view instance.
  */
  _createButton(e, i, s) {
    const r = new le(this.locale);
    return r.set({
      label: e,
      icon: i,
      tooltip: !0
    }), r.delegate("execute").to(this, s), r;
  }
  /**
  * Creates a link href preview button.
  *
  * @returns The button view instance.
  */
  _createPreviewButton() {
    const e = new le(this.locale), i = this.bindTemplate, s = this.t;
    return e.set({
      withText: !0
    }), e.extendTemplate({
      attributes: {
        class: [
          "ck",
          "ck-link-actions__preview"
        ],
        href: i.to("href", (r) => r && Kk(r, this._linkConfig.allowedProtocols)),
        target: "_blank",
        rel: "noopener noreferrer"
      },
      on: {
        click: i.to((r) => {
          this._options && this._options.isScrollableToTarget(this.href) ? (r.preventDefault(), this._options.scrollToTarget(this.href)) : Yk(this.href);
        })
      }
    }), e.bind("tooltip").to(this, "href", (r) => this._options && this._options.isScrollableToTarget(r) ? s("Scroll to target") : s("Open link in new tab")), e.bind("label").to(this, "href", (r) => r || s("This link has no URL")), e.bind("isEnabled").to(this, "href", (r) => !!r), e.template.tag = "a", e;
  }
}
var Xk = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.077 15 .991-1.416a.75.75 0 1 1 1.229.86l-1.148 1.64a.748.748 0 0 1-.217.206 5.251 5.251 0 0 1-8.503-5.955.741.741 0 0 1 .12-.274l1.147-1.639a.75.75 0 1 1 1.228.86L4.933 10.7l.006.003a3.75 3.75 0 0 0 6.132 4.294l.006.004zm5.494-5.335a.748.748 0 0 1-.12.274l-1.147 1.639a.75.75 0 1 1-1.228-.86l.86-1.23a3.75 3.75 0 0 0-6.144-4.301l-.86 1.229a.75.75 0 0 1-1.229-.86l1.148-1.64a.748.748 0 0 1 .217-.206 5.251 5.251 0 0 1 8.503 5.955zm-4.563-2.532a.75.75 0 0 1 .184 1.045l-3.155 4.505a.75.75 0 1 1-1.229-.86l3.155-4.506a.75.75 0 0 1 1.045-.184z"/></svg>';
const ln = "link-ui";
class Qk extends I {
  constructor() {
    super(...arguments);
    /**
    * The actions view displayed inside of the balloon.
    */
    f(this, "actionsView", null);
    /**
    * The form view displayed inside the balloon.
    */
    f(this, "formView", null);
    /**
    * The contextual balloon plugin instance.
    */
    f(this, "_balloon");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ai
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = this.editor.t;
    e.editing.view.addObserver(oy), this._balloon = e.plugins.get(ai), this._createToolbarLinkButton(), this._enableBalloonActivators(), e.conversion.for("editingDowncast").markerToHighlight({
      model: ln,
      view: {
        classes: [
          "ck-fake-link-selection"
        ]
      }
    }), e.conversion.for("editingDowncast").markerToElement({
      model: ln,
      view: (s, { writer: r }) => {
        if (!s.markerRange.isCollapsed)
          return null;
        const o = r.createUIElement("span");
        return r.addClass([
          "ck-fake-link-selection",
          "ck-fake-link-selection_collapsed"
        ], o), o;
      }
    }), e.accessibility.addKeystrokeInfos({
      keystrokes: [
        {
          label: i("Create link"),
          keystroke: Lo
        },
        {
          label: i("Move out of a link"),
          keystroke: [
            [
              "arrowleft",
              "arrowleft"
            ],
            [
              "arrowright",
              "arrowright"
            ]
          ]
        }
      ]
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.formView && this.formView.destroy(), this.actionsView && this.actionsView.destroy();
  }
  /**
  * Creates views.
  */
  _createViews() {
    this.actionsView = this._createActionsView(), this.formView = this._createFormView(), this._enableUserBalloonInteractions();
  }
  /**
  * Creates the {@link module:link/ui/linkactionsview~LinkActionsView} instance.
  */
  _createActionsView() {
    const e = this.editor, i = new eH(e.locale, e.config.get("link"), Jk(e)), s = e.commands.get("link"), r = e.commands.get("unlink");
    return i.bind("href").to(s, "value"), i.editButtonView.bind("isEnabled").to(s), i.unlinkButtonView.bind("isEnabled").to(r), this.listenTo(i, "edit", () => {
      this._addFormView();
    }), this.listenTo(i, "unlink", () => {
      e.execute("unlink"), this._hideUI();
    }), i.keystrokes.set("Esc", (o, a) => {
      this._hideUI(), a();
    }), i.keystrokes.set(Lo, (o, a) => {
      this._addFormView(), a();
    }), i;
  }
  /**
  * Creates the {@link module:link/ui/linkformview~LinkFormView} instance.
  */
  _createFormView() {
    const e = this.editor, i = e.commands.get("link"), s = e.config.get("link.defaultProtocol"), r = new (no(Xz))(e.locale, i, tH(e));
    return r.urlInputView.fieldView.bind("value").to(i, "value"), r.urlInputView.bind("isEnabled").to(i, "isEnabled"), r.saveButtonView.bind("isEnabled").to(i, "isEnabled"), this.listenTo(r, "submit", () => {
      if (r.isValid()) {
        const { value: o } = r.urlInputView.fieldView.element, a = lm(o, s);
        e.execute("link", a, r.getDecoratorSwitchesState()), this._closeFormView();
      }
    }), this.listenTo(r.urlInputView, "change:errorText", () => {
      e.ui.update();
    }), this.listenTo(r, "cancel", () => {
      this._closeFormView();
    }), r.keystrokes.set("Esc", (o, a) => {
      this._closeFormView(), a();
    }), r;
  }
  /**
  * Creates a toolbar Link button. Clicking this button will show
  * a {@link #_balloon} attached to the selection.
  */
  _createToolbarLinkButton() {
    const e = this.editor;
    e.ui.componentFactory.add("link", () => {
      const i = this._createButton(le);
      return i.set({
        tooltip: !0
      }), i;
    }), e.ui.componentFactory.add("menuBar:link", () => {
      const i = this._createButton(Fe);
      return i.set({
        role: "menuitemcheckbox"
      }), i;
    });
  }
  /**
  * Creates a button for link command to use either in toolbar or in menu bar.
  */
  _createButton(e) {
    const i = this.editor, s = i.locale, r = i.commands.get("link"), o = new e(i.locale), a = s.t;
    return o.set({
      label: a("Link"),
      icon: Xk,
      keystroke: Lo,
      isToggleable: !0
    }), o.bind("isEnabled").to(r, "isEnabled"), o.bind("isOn").to(r, "value", (l) => !!l), this.listenTo(o, "execute", () => this._showUI(!0)), o;
  }
  /**
  * Attaches actions that control whether the balloon panel containing the
  * {@link #formView} should be displayed.
  */
  _enableBalloonActivators() {
    const e = this.editor, i = e.editing.view.document;
    this.listenTo(i, "click", () => {
      this._getSelectedLinkElement() && this._showUI();
    }), e.keystrokes.set(Lo, (s, r) => {
      r(), e.commands.get("link").isEnabled && this._showUI(!0);
    });
  }
  /**
  * Attaches actions that control whether the balloon panel containing the
  * {@link #formView} is visible or not.
  */
  _enableUserBalloonInteractions() {
    this.editor.keystrokes.set("Tab", (e, i) => {
      this._areActionsVisible && !this.actionsView.focusTracker.isFocused && (this.actionsView.focus(), i());
    }, {
      // Use the high priority because the link UI navigation is more important
      // than other feature's actions, e.g. list indentation.
      // https://github.com/ckeditor/ckeditor5-link/issues/146
      priority: "high"
    }), this.editor.keystrokes.set("Esc", (e, i) => {
      this._isUIVisible && (this._hideUI(), i());
    }), Xn({
      emitter: this.formView,
      activator: () => this._isUIInPanel,
      contextElements: () => [
        this._balloon.view.element
      ],
      callback: () => this._hideUI()
    });
  }
  /**
  * Adds the {@link #actionsView} to the {@link #_balloon}.
  *
  * @internal
  */
  _addActionsView() {
    this.actionsView || this._createViews(), !this._areActionsInPanel && this._balloon.add({
      view: this.actionsView,
      position: this._getBalloonPositionData()
    });
  }
  /**
  * Adds the {@link #formView} to the {@link #_balloon}.
  */
  _addFormView() {
    if (this.formView || this._createViews(), this._isFormInPanel)
      return;
    const i = this.editor.commands.get("link");
    this.formView.disableCssTransitions(), this.formView.resetFormStatus(), this._balloon.add({
      view: this.formView,
      position: this._getBalloonPositionData()
    }), this.formView.urlInputView.fieldView.value = i.value || "", this._balloon.visibleView === this.formView && this.formView.urlInputView.fieldView.select(), this.formView.enableCssTransitions();
  }
  /**
  * Closes the form view. Decides whether the balloon should be hidden completely or if the action view should be shown. This is
  * decided upon the link command value (which has a value if the document selection is in the link).
  *
  * Additionally, if any {@link module:link/linkconfig~LinkConfig#decorators} are defined in the editor configuration, the state of
  * switch buttons responsible for manual decorator handling is restored.
  */
  _closeFormView() {
    const e = this.editor.commands.get("link");
    e.restoreManualDecoratorStates(), e.value !== void 0 ? this._removeFormView() : this._hideUI();
  }
  /**
  * Removes the {@link #formView} from the {@link #_balloon}.
  */
  _removeFormView() {
    this._isFormInPanel && (this.formView.saveButtonView.focus(), this.formView.urlInputView.fieldView.reset(), this._balloon.remove(this.formView), this.editor.editing.view.focus(), this._hideFakeVisualSelection());
  }
  /**
  * Shows the correct UI type. It is either {@link #formView} or {@link #actionsView}.
  *
  * @internal
  */
  _showUI(e = !1) {
    this.formView || this._createViews(), this._getSelectedLinkElement() ? (this._areActionsVisible ? this._addFormView() : this._addActionsView(), e && this._balloon.showStack("main")) : (this._showFakeVisualSelection(), this._addActionsView(), e && this._balloon.showStack("main"), this._addFormView()), this._startUpdatingUI();
  }
  /**
  * Removes the {@link #formView} from the {@link #_balloon}.
  *
  * See {@link #_addFormView}, {@link #_addActionsView}.
  */
  _hideUI() {
    if (!this._isUIInPanel)
      return;
    const e = this.editor;
    this.stopListening(e.ui, "update"), this.stopListening(this._balloon, "change:visibleView"), e.editing.view.focus(), this._removeFormView(), this._balloon.remove(this.actionsView), this._hideFakeVisualSelection();
  }
  /**
  * Makes the UI react to the {@link module:ui/editorui/editorui~EditorUI#event:update} event to
  * reposition itself when the editor UI should be refreshed.
  *
  * See: {@link #_hideUI} to learn when the UI stops reacting to the `update` event.
  */
  _startUpdatingUI() {
    const e = this.editor, i = e.editing.view.document;
    let s = this._getSelectedLinkElement(), r = a();
    const o = () => {
      const l = this._getSelectedLinkElement(), c = a();
      s && !l || !s && c !== r ? this._hideUI() : this._isUIVisible && this._balloon.updatePosition(this._getBalloonPositionData()), s = l, r = c;
    };
    function a() {
      return i.selection.focus.getAncestors().reverse().find((l) => l.is("element"));
    }
    this.listenTo(e.ui, "update", o), this.listenTo(this._balloon, "change:visibleView", o);
  }
  /**
  * Returns `true` when {@link #formView} is in the {@link #_balloon}.
  */
  get _isFormInPanel() {
    return !!this.formView && this._balloon.hasView(this.formView);
  }
  /**
  * Returns `true` when {@link #actionsView} is in the {@link #_balloon}.
  */
  get _areActionsInPanel() {
    return !!this.actionsView && this._balloon.hasView(this.actionsView);
  }
  /**
  * Returns `true` when {@link #actionsView} is in the {@link #_balloon} and it is
  * currently visible.
  */
  get _areActionsVisible() {
    return !!this.actionsView && this._balloon.visibleView === this.actionsView;
  }
  /**
  * Returns `true` when {@link #actionsView} or {@link #formView} is in the {@link #_balloon}.
  */
  get _isUIInPanel() {
    return this._isFormInPanel || this._areActionsInPanel;
  }
  /**
  * Returns `true` when {@link #actionsView} or {@link #formView} is in the {@link #_balloon} and it is
  * currently visible.
  */
  get _isUIVisible() {
    const e = this._balloon.visibleView;
    return !!this.formView && e == this.formView || this._areActionsVisible;
  }
  /**
  * Returns positioning options for the {@link #_balloon}. They control the way the balloon is attached
  * to the target element or selection.
  *
  * If the selection is collapsed and inside a link element, the panel will be attached to the
  * entire link element. Otherwise, it will be attached to the selection.
  */
  _getBalloonPositionData() {
    const e = this.editor.editing.view, i = this.editor.model, s = e.document;
    let r;
    if (i.markers.has(ln)) {
      const o = Array.from(this.editor.editing.mapper.markerNameToElements(ln)), a = e.createRange(e.createPositionBefore(o[0]), e.createPositionAfter(o[o.length - 1]));
      r = e.domConverter.viewRangeToDom(a);
    } else
      r = () => {
        const o = this._getSelectedLinkElement();
        return o ? (
          // When selection is inside link element, then attach panel to this element.
          e.domConverter.mapViewToDom(o)
        ) : (
          // Otherwise attach panel to the selection.
          e.domConverter.viewRangeToDom(s.selection.getFirstRange())
        );
      };
    return {
      target: r
    };
  }
  /**
  * Returns the link {@link module:engine/view/attributeelement~AttributeElement} under
  * the {@link module:engine/view/document~Document editing view's} selection or `null`
  * if there is none.
  *
  * **Note**: For a non–collapsed selection, the link element is returned when **fully**
  * selected and the **only** element within the selection boundaries, or when
  * a linked widget is selected.
  */
  _getSelectedLinkElement() {
    const e = this.editor.editing.view, i = e.document.selection, s = i.getSelectedElement();
    if (i.isCollapsed || s && vt(s))
      return Cd(i.getFirstPosition());
    {
      const r = i.getFirstRange().getTrimmed(), o = Cd(r.start), a = Cd(r.end);
      return !o || o != a ? null : e.createRangeIn(o).getTrimmed().isEqual(r) ? o : null;
    }
  }
  /**
  * Displays a fake visual selection when the contextual balloon is displayed.
  *
  * This adds a 'link-ui' marker into the document that is rendered as a highlight on selected text fragment.
  */
  _showFakeVisualSelection() {
    const e = this.editor.model;
    e.change((i) => {
      const s = e.document.selection.getFirstRange();
      if (e.markers.has(ln))
        i.updateMarker(ln, {
          range: s
        });
      else if (s.start.isAtEnd) {
        const r = s.start.getLastMatchingPosition(({ item: o }) => !e.schema.isContent(o), {
          boundaries: s
        });
        i.addMarker(ln, {
          usingOperation: !1,
          affectsData: !1,
          range: i.createRange(r, s.end)
        });
      } else
        i.addMarker(ln, {
          usingOperation: !1,
          affectsData: !1,
          range: s
        });
    });
  }
  /**
  * Hides the fake visual selection created in {@link #_showFakeVisualSelection}.
  */
  _hideFakeVisualSelection() {
    const e = this.editor.model;
    e.markers.has(ln) && e.change((i) => {
      i.removeMarker(ln);
    });
  }
}
function Cd(n) {
  return n.getAncestors().find((t) => Dz(t)) || null;
}
function tH(n) {
  const t = n.t, e = n.config.get("link.allowCreatingEmptyLinks");
  return [
    (i) => {
      if (!e && !i.url.length)
        return t("Link URL must not be empty.");
    }
  ];
}
const iH = 4, eC = new RegExp(
  // Group 1: Line start or after a space.
  "(^|\\s)(((?:(?:(?:https?|ftp):)?\\/\\/)(?:\\S+(?::\\S*)?@)?(?:(?:[1-9]\\d?|1\\d\\d|2[01]\\d|22[0-3])(?:\\.(?:1?\\d{1,2}|2[0-4]\\d|25[0-5])){2}(?:\\.(?:[1-9]\\d?|1\\d\\d|2[0-4]\\d|25[0-4]))|(((?!www\\.)|(www\\.))(?![-_])(?:[-_a-z0-9\\u00a1-\\uffff]{1,63}\\.)+(?:[a-z\\u00a1-\\uffff]{2,63})))(?::\\d{2,5})?(?:[/?#]\\S*)?)|((www.|(\\S+@))((?![-_])(?:[-_a-z0-9\\u00a1-\\uffff]{1,63}\\.))+(?:[a-z\\u00a1-\\uffff]{2,63})))$",
  "i"
), nH = 2;
class tC extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Cn,
      nu
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AutoLink";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor.model.document.selection;
    e.on("change:range", () => {
      this.isEnabled = !e.anchor.parent.is("element", "codeBlock");
    }), this._enableTypingHandling();
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    this._enableEnterHandling(), this._enableShiftEnterHandling(), this._enablePasteLinking();
  }
  /**
  * For given position, returns a range that includes the whole link that contains the position.
  *
  * If position is not inside a link, returns `null`.
  */
  _expandLinkRange(t, e) {
    return e.textNode && e.textNode.hasAttribute("linkHref") ? Na(e, "linkHref", e.textNode.getAttribute("linkHref"), t) : null;
  }
  /**
  * Extends the document selection to includes all links that intersects with given `selectedRange`.
  */
  _selectEntireLinks(t, e) {
    const s = this.editor.model, r = s.document.selection, o = r.getFirstPosition(), a = r.getLastPosition();
    let l = e.getJoined(this._expandLinkRange(s, o) || e);
    l && (l = l.getJoined(this._expandLinkRange(s, a) || e)), l && (l.start.isBefore(o) || l.end.isAfter(a)) && t.setSelection(l);
  }
  /**
  * Enables autolinking on pasting a URL when some content is selected.
  */
  _enablePasteLinking() {
    const t = this.editor, e = t.model, i = e.document.selection, s = t.plugins.get("ClipboardPipeline"), r = t.commands.get("link");
    s.on("inputTransformation", (o, a) => {
      if (!this.isEnabled || !r.isEnabled || i.isCollapsed || a.method !== "paste" || i.rangeCount > 1)
        return;
      const l = i.getFirstRange(), c = a.dataTransfer.getData("text/plain");
      if (!c)
        return;
      const u = c.match(eC);
      u && u[2] === c && (e.change((d) => {
        this._selectEntireLinks(d, l), r.execute(c);
      }), o.stop());
    }, {
      priority: "high"
    });
  }
  /**
  * Enables autolinking on typing.
  */
  _enableTypingHandling() {
    const t = this.editor, e = new Gf(t.model, (i) => {
      let s = i;
      if (!sH(s))
        return;
      s = s.slice(0, -1), "!.:,;?".includes(s[s.length - 1]) && (s = s.slice(0, -1));
      const r = H1(s);
      if (r)
        return {
          url: r,
          removedTrailingCharacters: i.length - s.length
        };
    });
    e.on("matched:data", (i, s) => {
      const { batch: r, range: o, url: a, removedTrailingCharacters: l } = s;
      if (!r.isTyping)
        return;
      const c = o.end.getShiftedBy(-l), u = c.getShiftedBy(-a.length), d = t.model.createRange(u, c);
      this._applyAutoLink(a, d);
    }), e.bind("isEnabled").to(this);
  }
  /**
  * Enables autolinking on the <kbd>Enter</kbd> key.
  */
  _enableEnterHandling() {
    const t = this.editor, e = t.model, i = t.commands.get("enter");
    i && i.on("execute", () => {
      const s = e.document.selection.getFirstPosition();
      if (!s.parent.previousSibling)
        return;
      const r = e.createRangeIn(s.parent.previousSibling);
      this._checkAndApplyAutoLinkOnRange(r);
    });
  }
  /**
  * Enables autolinking on the <kbd>Shift</kbd>+<kbd>Enter</kbd> keyboard shortcut.
  */
  _enableShiftEnterHandling() {
    const t = this.editor, e = t.model, i = t.commands.get("shiftEnter");
    i && i.on("execute", () => {
      const s = e.document.selection.getFirstPosition(), r = e.createRange(e.createPositionAt(s.parent, 0), s.getShiftedBy(-1));
      this._checkAndApplyAutoLinkOnRange(r);
    });
  }
  /**
  * Checks if the passed range contains a linkable text.
  */
  _checkAndApplyAutoLinkOnRange(t) {
    const e = this.editor.model, { text: i, range: s } = tk(t, e), r = H1(i);
    if (r) {
      const o = e.createRange(s.end.getShiftedBy(-r.length), s.end);
      this._applyAutoLink(r, o);
    }
  }
  /**
  * Applies a link on a given range if the link should be applied.
  *
  * @param url The URL to link.
  * @param range The text range to apply the link attribute to.
  */
  _applyAutoLink(t, e) {
    const i = this.editor.model, s = this.editor.config.get("link.defaultProtocol"), r = lm(t, s);
    !this.isEnabled || !rH(e, i) || !Zk(r) || oH(e) || this._persistAutoLink(r, e);
  }
  /**
  * Enqueues autolink changes in the model.
  *
  * @param url The URL to link.
  * @param range The text range to apply the link attribute to.
  */
  _persistAutoLink(t, e) {
    const i = this.editor.model, s = this.editor.plugins.get("Delete");
    i.enqueueChange((r) => {
      r.setAttribute("linkHref", t, e), i.enqueueChange(() => {
        s.requestUndoOnBackspace();
      });
    });
  }
}
function sH(n) {
  return n.length > iH && n[n.length - 1] === " " && n[n.length - 2] !== " ";
}
function H1(n) {
  const t = eC.exec(n);
  return t ? t[nH] : null;
}
function rH(n, t) {
  return t.schema.checkAttributeInSelection(t.createSelection(n), "linkHref");
}
function oH(n) {
  const t = n.start.nodeAfter;
  return !!t && t.hasAttribute("linkHref");
}
class aH extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nu,
      Qk,
      tC
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Link";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class lH extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      "ImageEditing",
      "ImageUtils",
      nu
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkImageEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.model.schema;
    t.plugins.has("ImageBlockEditing") && e.extend("imageBlock", {
      allowAttributes: [
        "linkHref"
      ]
    }), t.conversion.for("upcast").add(cH(t)), t.conversion.for("downcast").add(uH(t)), this._enableAutomaticDecorators(), this._enableManualDecorators();
  }
  /**
  * Processes {@link module:link/linkconfig~LinkDecoratorAutomaticDefinition automatic decorators} definitions and
  * attaches proper converters that will work when linking an image.`
  */
  _enableAutomaticDecorators() {
    const t = this.editor, i = t.commands.get("link").automaticDecorators;
    i.length && t.conversion.for("downcast").add(i.getDispatcherForLinkedImage());
  }
  /**
  * Processes transformed {@link module:link/utils/manualdecorator~ManualDecorator} instances and attaches proper converters
  * that will work when linking an image.
  */
  _enableManualDecorators() {
    const t = this.editor, e = t.commands.get("link");
    for (const i of e.manualDecorators)
      t.plugins.has("ImageBlockEditing") && t.model.schema.extend("imageBlock", {
        allowAttributes: i.id
      }), t.plugins.has("ImageInlineEditing") && t.model.schema.extend("imageInline", {
        allowAttributes: i.id
      }), t.conversion.for("downcast").add(dH(i)), t.conversion.for("upcast").add(hH(t, i));
  }
}
function cH(n) {
  const t = n.plugins.has("ImageInlineEditing"), e = n.plugins.get("ImageUtils");
  return (i) => {
    i.on("element:a", (s, r, o) => {
      const a = r.viewItem, l = e.findViewImgElement(a);
      if (!l)
        return;
      const c = l.findAncestor((m) => e.isBlockImageView(m));
      if (t && !c)
        return;
      const u = {
        attributes: [
          "href"
        ]
      };
      if (!o.consumable.consume(a, u))
        return;
      const d = a.getAttribute("href");
      if (!d)
        return;
      let h = r.modelCursor.parent;
      if (!h.is("element", "imageBlock")) {
        const m = o.convertItem(l, r.modelCursor);
        r.modelRange = m.modelRange, r.modelCursor = m.modelCursor, h = r.modelCursor.nodeBefore;
      }
      h && h.is("element", "imageBlock") && o.writer.setAttribute("linkHref", d, h);
    }, {
      priority: "high"
    });
  };
}
function uH(n) {
  const t = n.plugins.get("ImageUtils");
  return (e) => {
    e.on("attribute:linkHref:imageBlock", (i, s, r) => {
      if (!r.consumable.consume(s.item, i.name))
        return;
      const o = r.mapper.toViewElement(s.item), a = r.writer, l = Array.from(o.getChildren()).find((d) => d.is("element", "a")), c = t.findViewImgElement(o), u = c.parent.is("element", "picture") ? c.parent : c;
      if (l)
        s.attributeNewValue ? a.setAttribute("href", s.attributeNewValue, l) : (a.move(a.createRangeOn(u), a.createPositionAt(o, 0)), a.remove(l));
      else {
        const d = a.createContainerElement("a", {
          href: s.attributeNewValue
        });
        a.insert(a.createPositionAt(o, 0), d), a.move(a.createRangeOn(u), a.createPositionAt(d, 0));
      }
    }, {
      priority: "high"
    });
  };
}
function dH(n) {
  return (t) => {
    t.on(`attribute:${n.id}:imageBlock`, (e, i, s) => {
      const r = s.mapper.toViewElement(i.item), o = Array.from(r.getChildren()).find((a) => a.is("element", "a"));
      if (o) {
        for (const [a, l] of Ji(n.attributes))
          s.writer.setAttribute(a, l, o);
        n.classes && s.writer.addClass(n.classes, o);
        for (const a in n.styles)
          s.writer.setStyle(a, n.styles[a], o);
      }
    });
  };
}
function hH(n, t) {
  const e = n.plugins.has("ImageInlineEditing"), i = n.plugins.get("ImageUtils");
  return (s) => {
    s.on("element:a", (r, o, a) => {
      const l = o.viewItem, c = i.findViewImgElement(l);
      if (!c)
        return;
      const u = c.findAncestor((g) => i.isBlockImageView(g));
      if (e && !u)
        return;
      const h = new oi(t._createPattern()).match(l);
      if (!h || !a.consumable.consume(l, h.match))
        return;
      const m = o.modelCursor.nodeBefore || o.modelCursor.parent;
      a.writer.setAttribute(t.id, !0, m);
    }, {
      priority: "high"
    });
  };
}
class fH extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nu,
      Qk,
      "ImageBlockEditing"
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkImageUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.editing.view.document;
    this.listenTo(e, "click", (i, s) => {
      this._isSelectedLinkedImage(t.model.document.selection) && (s.preventDefault(), i.stop());
    }, {
      priority: "high"
    }), this._createToolbarLinkImageButton();
  }
  /**
  * Creates a `LinkImageUI` button view.
  *
  * Clicking this button shows a {@link module:link/linkui~LinkUI#_balloon} attached to the selection.
  * When an image is already linked, the view shows {@link module:link/linkui~LinkUI#actionsView} or
  * {@link module:link/linkui~LinkUI#formView} if it is not.
  */
  _createToolbarLinkImageButton() {
    const t = this.editor, e = t.t;
    t.ui.componentFactory.add("linkImage", (i) => {
      const s = new le(i), r = t.plugins.get("LinkUI"), o = t.commands.get("link");
      return s.set({
        isEnabled: !0,
        label: e("Link image"),
        icon: Xk,
        keystroke: Lo,
        tooltip: !0,
        isToggleable: !0
      }), s.bind("isEnabled").to(o, "isEnabled"), s.bind("isOn").to(o, "value", (a) => !!a), this.listenTo(s, "execute", () => {
        this._isSelectedLinkedImage(t.model.document.selection) ? r._addActionsView() : r._showUI(!0);
      }), s;
    });
  }
  /**
  * Returns true if a linked image (either block or inline) is the only selected element
  * in the model document.
  */
  _isSelectedLinkedImage(t) {
    const e = t.getSelectedElement();
    return this.editor.plugins.get("ImageUtils").isImage(e) && e.hasAttribute("linkHref");
  }
}
class mH extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      lH,
      fH
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkImage";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class Fi {
  /**
  * Creates a document list iterator.
  *
  * @param startElement The start list item block element.
  * @param options.direction The iterating direction.
  * @param options.includeSelf Whether start block should be included in the result (if it's matching other criteria).
  * @param options.sameAttributes Additional attributes that must be the same for each block.
  * @param options.sameIndent Whether blocks with the same indent level as the start block should be included
  * in the result.
  * @param options.lowerIndent Whether blocks with a lower indent level than the start block should be included
  * in the result.
  * @param options.higherIndent Whether blocks with a higher indent level than the start block should be included
  * in the result.
  */
  constructor(t, e) {
    /**
    * The start list item block element.
    */
    f(this, "_startElement");
    /**
    * The reference indent. Initialized by the indent of the start block.
    */
    f(this, "_referenceIndent");
    /**
    * The iterating direction.
    */
    f(this, "_isForward");
    /**
    * Whether start block should be included in the result (if it's matching other criteria).
    */
    f(this, "_includeSelf");
    /**
    * Additional attributes that must be the same for each block.
    */
    f(this, "_sameAttributes");
    /**
    * Whether blocks with the same indent level as the start block should be included in the result.
    */
    f(this, "_sameIndent");
    /**
    * Whether blocks with a lower indent level than the start block should be included in the result.
    */
    f(this, "_lowerIndent");
    /**
    * Whether blocks with a higher indent level than the start block should be included in the result.
    */
    f(this, "_higherIndent");
    this._startElement = t, this._referenceIndent = t.getAttribute("listIndent"), this._isForward = e.direction == "forward", this._includeSelf = !!e.includeSelf, this._sameAttributes = Ue(e.sameAttributes || []), this._sameIndent = !!e.sameIndent, this._lowerIndent = !!e.lowerIndent, this._higherIndent = !!e.higherIndent;
  }
  /**
  * Performs only first step of iteration and returns the result.
  *
  * @param startElement The start list item block element.
  * @param options.direction The iterating direction.
  * @param options.includeSelf Whether start block should be included in the result (if it's matching other criteria).
  * @param options.sameAttributes Additional attributes that must be the same for each block.
  * @param options.sameIndent Whether blocks with the same indent level as the start block should be included
  * in the result.
  * @param options.lowerIndent Whether blocks with a lower indent level than the start block should be included
  * in the result.
  * @param options.higherIndent Whether blocks with a higher indent level than the start block should be included
  * in the result.
  */
  static first(t, e) {
    const s = new this(t, e)[Symbol.iterator]();
    return Xe(s);
  }
  /**
  * Iterable interface.
  */
  *[Symbol.iterator]() {
    const t = [];
    for (const { node: e } of new za(this._getStartNode(), this._isForward ? "forward" : "backward")) {
      const i = e.getAttribute("listIndent");
      if (i < this._referenceIndent) {
        if (!this._lowerIndent)
          break;
        this._referenceIndent = i;
      } else if (i > this._referenceIndent) {
        if (!this._higherIndent)
          continue;
        if (!this._isForward) {
          t.push(e);
          continue;
        }
      } else {
        if (!this._sameIndent) {
          if (this._higherIndent) {
            t.length && (yield* t, t.length = 0);
            break;
          }
          continue;
        }
        if (this._sameAttributes.some((s) => e.getAttribute(s) !== this._startElement.getAttribute(s)))
          break;
      }
      t.length && (yield* t, t.length = 0), yield e;
    }
  }
  /**
  * Returns the model element to start iterating.
  */
  _getStartNode() {
    return this._includeSelf ? this._startElement : this._isForward ? this._startElement.nextSibling : this._startElement.previousSibling;
  }
}
class za {
  /**
  * @param node The model node.
  * @param direction Iteration direction.
  */
  constructor(t, e = "forward") {
    f(this, "_node");
    f(this, "_isForward");
    f(this, "_previousNodesByIndent", []);
    f(this, "_previous", null);
    f(this, "_previousNodeIndent", null);
    this._node = t, this._isForward = e === "forward";
  }
  [Symbol.iterator]() {
    return this;
  }
  next() {
    if (!it(this._node))
      return {
        done: !0,
        value: void 0
      };
    const t = this._node.getAttribute("listIndent");
    let e = null;
    if (this._previous) {
      const s = this._previousNodeIndent;
      t > s ? this._previousNodesByIndent[s] = this._previous : t < s ? (e = this._previousNodesByIndent[t] || null, this._previousNodesByIndent.length = t) : e = this._previous;
    }
    const i = {
      node: this._node,
      previous: this._previous,
      previousNodeInList: e
    };
    return this._previous = this._node, this._previousNodeIndent = t, this._node = this._isForward ? this._node.nextSibling : this._node.previousSibling, {
      value: i,
      done: !1
    };
  }
}
class gH {
  /**
  * @param listHead The head element of a list.
  */
  constructor(t) {
    f(this, "_listHead");
    this._listHead = t;
  }
  /**
  * List blocks iterator.
  *
  * Iterates over all blocks of a list.
  */
  [Symbol.iterator]() {
    return new za(this._listHead);
  }
}
class Gr {
  /**
  * Returns the next ID.
  *
  * @internal
  */
  /* istanbul ignore next: static function definition -- @preserve */
  static next() {
    return Bt();
  }
}
function it(n) {
  return !!n && n.is("element") && n.hasAttribute("listItemId");
}
function Kr(n, t = {}) {
  return [
    ...gs(n, {
      ...t,
      direction: "backward"
    }),
    ...gs(n, {
      ...t,
      direction: "forward"
    })
  ];
}
function gs(n, t = {}) {
  const e = t.direction == "forward", i = Array.from(new Fi(n, {
    ...t,
    includeSelf: e,
    sameIndent: !0,
    sameAttributes: "listItemId"
  }));
  return e ? i : i.reverse();
}
function pH(n) {
  return Array.from(new Fi(n, {
    direction: "forward",
    higherIndent: !0
  }));
}
function iC(n, t) {
  const e = new Fi(n, {
    sameIndent: !0,
    sameAttributes: "listType",
    ...t
  }), i = new Fi(n, {
    sameIndent: !0,
    sameAttributes: "listType",
    includeSelf: !0,
    direction: "forward",
    ...t
  });
  return [
    ...Array.from(e).reverse(),
    ...i
  ];
}
function qn(n) {
  return !Fi.first(n, {
    sameIndent: !0,
    sameAttributes: "listItemId"
  });
}
function j1(n) {
  return !Fi.first(n, {
    direction: "forward",
    sameIndent: !0,
    sameAttributes: "listItemId"
  });
}
function Ha(n, t = {}) {
  n = Ue(n);
  const e = t.withNested !== !1, i = /* @__PURE__ */ new Set();
  for (const s of n)
    for (const r of Kr(s, {
      higherIndent: e
    }))
      i.add(r);
  return Qs(i);
}
function su(n) {
  n = Ue(n);
  const t = /* @__PURE__ */ new Set();
  for (const e of n)
    for (const i of iC(e))
      t.add(i);
  return Qs(t);
}
function cm(n, t) {
  const e = gs(n, {
    direction: "forward"
  }), i = Gr.next();
  for (const s of e)
    t.setAttribute("listItemId", i, s);
  return e;
}
function Ih(n, t, e) {
  const i = {};
  for (const [r, o] of t.getAttributes())
    r.startsWith("list") && (i[r] = o);
  const s = gs(n, {
    direction: "forward"
  });
  for (const r of s)
    e.setAttributes(i, r);
  return s;
}
function Ph(n, t, { expand: e, indentBy: i = 1 } = {}) {
  n = Ue(n);
  const s = e ? Ha(n) : n;
  for (const r of s) {
    const o = r.getAttribute("listIndent") + i;
    o < 0 ? ru(r, t) : t.setAttribute("listIndent", o, r);
  }
  return s;
}
function bH(n, t) {
  n = Ue(n);
  const e = Ha(n), i = /* @__PURE__ */ new Set(), s = Math.min(...e.map((o) => o.getAttribute("listIndent"))), r = /* @__PURE__ */ new Map();
  for (const o of e)
    r.set(o, Fi.first(o, {
      lowerIndent: !0
    }));
  for (const o of e) {
    if (i.has(o))
      continue;
    i.add(o);
    const a = o.getAttribute("listIndent") - 1;
    if (a < 0) {
      ru(o, t);
      continue;
    }
    if (o.getAttribute("listIndent") == s) {
      const l = vH(o, r.get(o), t);
      for (const c of l)
        i.add(c);
      if (l.length)
        continue;
    }
    t.setAttribute("listIndent", a, o);
  }
  return Qs(i);
}
function ru(n, t) {
  n = Ue(n);
  for (const e of n)
    e.is("element", "listItem") && t.rename(e, "paragraph");
  for (const e of n)
    for (const i of e.getAttributeKeys())
      i.startsWith("list") && t.removeAttribute(i, e);
  return n;
}
function wa(n) {
  if (!n.length)
    return !1;
  const t = n[0].getAttribute("listItemId");
  return t ? !n.some((e) => e.getAttribute("listItemId") != t) : !1;
}
function wH(n, t) {
  const e = [];
  let i = Number.POSITIVE_INFINITY;
  for (const { node: s } of new za(n.nextSibling)) {
    const r = s.getAttribute("listIndent");
    if (r == 0)
      break;
    r < i && (i = r);
    const o = r - i;
    t.setAttribute("listIndent", o, s), e.push(s);
  }
  return e;
}
function Qs(n) {
  return Array.from(n).filter((t) => t.root.rootName !== "$graveyard").sort((t, e) => t.index - e.index);
}
function va(n) {
  const t = n.document.selection.getSelectedElement();
  return t && n.schema.isObject(t) && n.schema.isBlock(t) ? t : null;
}
function xd(n, t) {
  return t.checkChild(n.parent, "listItem") && t.checkChild(n, "$text") && !t.isObject(n);
}
function fc(n) {
  return n == "numbered" || n == "customNumbered";
}
function vH(n, t, e) {
  return gs(t, {
    direction: "forward"
  }).pop().index > n.index ? Ih(n, t, e) : [];
}
class U1 extends he {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param indentDirection The direction of indent. If it is equal to `backward`, the command
  * will outdent a list item.
  */
  constructor(e, i) {
    super(e);
    /**
    * Determines by how much the command will change the list item's indent attribute.
    */
    f(this, "_direction");
    this._direction = i;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Indents or outdents (depending on the {@link #constructor}'s `indentDirection` parameter) selected list items.
  *
  * @fires execute
  * @fires afterExecute
  */
  execute() {
    const e = this.editor.model, i = W1(e.document.selection);
    e.change((s) => {
      const r = [];
      wa(i) && !qn(i[0]) ? (this._direction == "forward" && r.push(...Ph(i, s)), r.push(...cm(i[0], s))) : this._direction == "forward" ? r.push(...Ph(i, s, {
        expand: !0
      })) : r.push(...bH(i, s));
      for (const o of r) {
        if (!o.hasAttribute("listType"))
          continue;
        const a = Fi.first(o, {
          sameIndent: !0
        });
        a && s.setAttribute("listType", a.getAttribute("listType"), o);
      }
      this._fireAfterExecute(r);
    });
  }
  /**
  * Fires the `afterExecute` event.
  *
  * @param changedBlocks The changed list elements.
  */
  _fireAfterExecute(e) {
    this.fire("afterExecute", Qs(new Set(e)));
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    let e = W1(this.editor.model.document.selection), i = e[0];
    if (!i)
      return !1;
    if (this._direction == "backward" || wa(e) && !qn(e[0]))
      return !0;
    e = Ha(e), i = e[0];
    const s = Fi.first(i, {
      sameIndent: !0
    });
    return s ? s.getAttribute("listType") == i.getAttribute("listType") : !1;
  }
}
function W1(n) {
  const t = Array.from(n.getSelectedBlocks()), e = t.findIndex((i) => !it(i));
  return e != -1 && (t.length = e), t;
}
class Bo extends he {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param type List type that will be handled by this command.
  */
  constructor(e, i, s = {}) {
    super(e);
    /**
    * The type of the list created by the command.
    */
    f(this, "type");
    /**
    * List Walker options that change the range of the list items to be changed when the selection is collapsed within a list item.
    *
    * In a multi-level list, when the selection is collapsed within a list item, instead of changing only the list items of the same list
    * type and current indent level, the entire list structure is changed (all list items at all indent levels of any list type).
    */
    f(this, "_listWalkerOptions");
    this.type = i, this._listWalkerOptions = s.multiLevel ? {
      higherIndent: !0,
      lowerIndent: !0,
      sameAttributes: []
    } : void 0;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.value = this._getValue(), this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the list command.
  *
  * @fires execute
  * @fires afterExecute
  * @param options Command options.
  * @param options.forceValue If set, it will force the command behavior. If `true`, the command will try to convert the
  * selected items and potentially the neighbor elements to the proper list items. If set to `false` it will convert selected elements
  * to paragraphs. If not set, the command will toggle selected elements to list items or paragraphs, depending on the selection.
  * @param options.additionalAttributes Additional attributes that are set for list items when the command is executed.
  */
  execute(e = {}) {
    const i = this.editor.model, s = i.document, r = va(i), o = Array.from(s.selection.getSelectedBlocks()).filter((l) => i.schema.checkAttribute(l, "listType") || xd(l, i.schema)), a = e.forceValue !== void 0 ? !e.forceValue : this.value;
    i.change((l) => {
      if (a) {
        const c = o[o.length - 1], u = gs(c, {
          direction: "forward"
        }), d = [];
        u.length > 1 && d.push(...cm(u[1], l)), d.push(...ru(o, l)), d.push(...wH(c, l)), this._fireAfterExecute(d);
      } else if ((r || s.selection.isCollapsed) && it(o[0])) {
        const c = iC(r || o[0], this._listWalkerOptions);
        for (const u of c)
          l.setAttributes({
            ...e.additionalAttributes,
            listType: this.type
          }, u);
        this._fireAfterExecute(c);
      } else {
        const c = [];
        for (const u of o)
          if (!u.hasAttribute("listType"))
            !u.is("element", "listItem") && xd(u, i.schema) && l.rename(u, "listItem"), l.setAttributes({
              ...e.additionalAttributes,
              listIndent: 0,
              listItemId: Gr.next(),
              listType: this.type
            }, u), c.push(u);
          else
            for (const d of Ha(u, {
              withNested: !1
            }))
              d.getAttribute("listType") != this.type && (l.setAttributes({
                ...e.additionalAttributes,
                listType: this.type
              }, d), c.push(d));
        this._fireAfterExecute(c);
      }
    });
  }
  /**
  * Fires the `afterExecute` event.
  *
  * @param changedBlocks The changed list elements.
  */
  _fireAfterExecute(e) {
    this.fire("afterExecute", Qs(new Set(e)));
  }
  /**
  * Checks the command's {@link #value}.
  *
  * @returns The current value.
  */
  _getValue() {
    const e = this.editor.model.document.selection, i = Array.from(e.getSelectedBlocks());
    if (!i.length)
      return !1;
    for (const s of i)
      if (s.getAttribute("listType") != this.type)
        return !1;
    return !0;
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    const e = this.editor.model, i = e.schema, s = e.document.selection, r = Array.from(s.getSelectedBlocks());
    if (!r.length)
      return !1;
    if (this.value)
      return !0;
    for (const o of r)
      if (i.checkAttribute(o, "listType") || xd(o, i))
        return !0;
    return !1;
  }
}
class q1 extends he {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param direction Whether list item should be merged before or after the selected block.
  */
  constructor(e, i) {
    super(e);
    /**
    * Whether list item should be merged before or after the selected block.
    */
    f(this, "_direction");
    this._direction = i;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Merges list blocks together (depending on the {@link #constructor}'s `direction` parameter).
  *
  * @fires execute
  * @fires afterExecute
  * @param options Command options.
  * @param options.shouldMergeOnBlocksContentLevel When set `true`, merging will be performed together
  * with {@link module:engine/model/model~Model#deleteContent} to get rid of the inline content in the selection or take advantage
  * of the heuristics in `deleteContent()` that helps convert lists into paragraphs in certain cases.
  */
  execute({ shouldMergeOnBlocksContentLevel: e = !1 } = {}) {
    const i = this.editor.model, s = i.document.selection, r = [];
    i.change((o) => {
      const { firstElement: a, lastElement: l } = this._getMergeSubjectElements(s, e), c = a.getAttribute("listIndent") || 0, u = l.getAttribute("listIndent"), d = l.getAttribute("listItemId");
      if (c != u) {
        const h = pH(l);
        r.push(...Ph([
          l,
          ...h
        ], o, {
          indentBy: c - u,
          // If outdenting, the entire sub-tree that follows must be included.
          expand: c < u
        }));
      }
      if (e) {
        let h = s;
        s.isCollapsed && (h = o.createSelection(o.createRange(o.createPositionAt(a, "end"), o.createPositionAt(l, 0)))), i.deleteContent(h, {
          doNotResetEntireContent: s.isCollapsed
        });
        const m = h.getLastPosition().parent, g = m.nextSibling;
        r.push(m), g && g !== l && g.getAttribute("listItemId") == d && r.push(...Ih(g, m, o));
      } else
        r.push(...Ih(l, a, o));
      this._fireAfterExecute(r);
    });
  }
  /**
  * Fires the `afterExecute` event.
  *
  * @param changedBlocks The changed list elements.
  */
  _fireAfterExecute(e) {
    this.fire("afterExecute", Qs(new Set(e)));
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    const e = this.editor.model, i = e.document.selection, s = va(e);
    if (i.isCollapsed || s) {
      const r = s || i.getFirstPosition().parent;
      if (!it(r))
        return !1;
      const o = this._direction == "backward" ? r.previousSibling : r.nextSibling;
      if (!o || wa([
        r,
        o
      ]))
        return !1;
    } else {
      const r = i.getLastPosition(), o = i.getFirstPosition();
      if (r.parent === o.parent || !it(r.parent))
        return !1;
    }
    return !0;
  }
  /**
  * Returns the boundary elements the merge should be executed for. These are not necessarily selection's first
  * and last position parents but sometimes sibling or even further blocks depending on the context.
  *
  * @param selection The selection the merge is executed for.
  * @param shouldMergeOnBlocksContentLevel When `true`, merge is performed together with
  * {@link module:engine/model/model~Model#deleteContent} to remove the inline content within the selection.
  */
  _getMergeSubjectElements(e, i) {
    const s = this.editor.model, r = va(s);
    let o, a;
    if (e.isCollapsed || r) {
      const l = r || e.getFirstPosition().parent, c = qn(l);
      this._direction == "backward" ? (a = l, c && !i ? o = Fi.first(l, {
        sameIndent: !0,
        lowerIndent: !0
      }) : o = l.previousSibling) : (o = l, a = l.nextSibling);
    } else
      o = e.getFirstPosition().parent, a = e.getLastPosition().parent;
    return {
      firstElement: o,
      lastElement: a
    };
  }
}
class G1 extends he {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param direction Whether list item should be split before or after the selected block.
  */
  constructor(e, i) {
    super(e);
    /**
    * Whether list item should be split before or after the selected block.
    */
    f(this, "_direction");
    this._direction = i;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = this._checkEnabled();
  }
  /**
  * Splits the list item at the selection.
  *
  * @fires execute
  * @fires afterExecute
  */
  execute() {
    this.editor.model.change((i) => {
      const s = cm(this._getStartBlock(), i);
      this._fireAfterExecute(s);
    });
  }
  /**
  * Fires the `afterExecute` event.
  *
  * @param changedBlocks The changed list elements.
  */
  _fireAfterExecute(e) {
    this.fire("afterExecute", Qs(new Set(e)));
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    const e = this.editor.model.document.selection, i = this._getStartBlock();
    return e.isCollapsed && it(i) && !qn(i);
  }
  /**
  * Returns the model element that is the main focus of the command (according to the current selection and command direction).
  */
  _getStartBlock() {
    const i = this.editor.model.document.selection.getFirstPosition().parent;
    return this._direction == "before" ? i : i.nextSibling;
  }
}
class _H extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Expands the given list of selected blocks to include all the items of the lists they're in.
  *
  * @param blocks The list of selected blocks.
  */
  expandListBlocksToCompleteList(t) {
    return su(t);
  }
  /**
  * Check if the given block is the first in the list item.
  *
  * @param listBlock The list block element.
  */
  isFirstBlockOfListItem(t) {
    return qn(t);
  }
  /**
  * Returns true if the given model node is a list item block.
  *
  * @param node A model node.
  */
  isListItemBlock(t) {
    return it(t);
  }
  /**
  * Expands the given list of selected blocks to include the leading and tailing blocks of partially selected list items.
  *
  * @param blocks The list of selected blocks.
  * @param options.withNested Whether should include nested list items.
  */
  expandListBlocksToCompleteItems(t, e = {}) {
    return Ha(t, e);
  }
  /**
  * Returns true if listType is of type `numbered` or `customNumbered`.
  */
  isNumberedListType(t) {
    return fc(t);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function yH(n) {
  return n.is("element", "ol") || n.is("element", "ul");
}
function Rh(n) {
  return n.is("element", "li");
}
function kH(n) {
  let t = 0, e = n.parent;
  for (; e; ) {
    if (Rh(e))
      t++;
    else {
      const i = e.previousSibling;
      i && Rh(i) && t++;
    }
    e = e.parent;
  }
  return t;
}
function CH(n, t, e, i = sC(e, t)) {
  return n.createAttributeElement(nC(e), null, {
    priority: 2 * t / 100 - 100,
    id: i
  });
}
function xH(n, t, e) {
  return n.createAttributeElement("li", null, {
    priority: (2 * t + 1) / 100 - 100,
    id: e
  });
}
function nC(n) {
  return n == "numbered" || n == "customNumbered" ? "ol" : "ul";
}
function sC(n, t) {
  return `list-${n}-${t}`;
}
function Ui(n, t, e) {
  const i = n.nodeBefore;
  if (it(i)) {
    let s = i;
    for (const { node: r } of new za(s, "backward"))
      if (s = r, e.has(s) || (e.add(s), t.has(i)))
        return;
    t.add(s);
  } else {
    const s = n.nodeAfter;
    it(s) && t.add(s);
  }
}
function TH(n, t) {
  let e = 0, i = -1, s = null, r = !1;
  for (const { node: o } of n) {
    const a = o.getAttribute("listIndent");
    if (a > e) {
      let l;
      s === null ? (s = a - e, l = e) : (s > a && (s = a), l = a - s), l > i + 1 && (l = i + 1), t.setAttribute("listIndent", l, o), r = !0, i = l;
    } else
      s = null, e = a + 1, i = a;
  }
  return r;
}
function EH(n, t, e) {
  const i = /* @__PURE__ */ new Set();
  let s = !1;
  for (const { node: r } of n) {
    if (i.has(r))
      continue;
    let o = r.getAttribute("listType"), a = r.getAttribute("listItemId");
    if (t.has(a) && (a = Gr.next()), t.add(a), r.is("element", "listItem")) {
      r.getAttribute("listItemId") != a && (e.setAttribute("listItemId", a, r), s = !0);
      continue;
    }
    for (const l of gs(r, {
      direction: "forward"
    }))
      i.add(l), l.getAttribute("listType") != o && (a = Gr.next(), o = l.getAttribute("listType")), l.getAttribute("listItemId") != a && (e.setAttribute("listItemId", a, l), s = !0);
  }
  return s;
}
function SH() {
  return (n, t, e) => {
    const { writer: i, schema: s } = e;
    if (!t.modelRange)
      return;
    const r = Array.from(t.modelRange.getItems({
      shallow: !0
    })).filter((d) => s.checkAttribute(d, "listItemId"));
    if (!r.length)
      return;
    const o = Gr.next(), a = kH(t.viewItem);
    let l = t.viewItem.parent && t.viewItem.parent.is("element", "ol") ? "numbered" : "bulleted";
    const c = r[0].getAttribute("listType");
    c && (l = c);
    const u = {
      listItemId: o,
      listIndent: a,
      listType: l
    };
    for (const d of r)
      d.hasAttribute("listItemId") || i.setAttributes(u, d);
    r.length > 1 && r[1].getAttribute("listItemId") != u.listItemId && e.keepEmptyElement(r[0]);
  };
}
function AH(n, t, e, i) {
  return () => {
    const a = n.document.differ.getChanges(), l = [], c = /* @__PURE__ */ new Set(), u = /* @__PURE__ */ new Set(), d = /* @__PURE__ */ new Set();
    for (const h of a)
      if (h.type == "insert" && h.name != "$text")
        Ui(h.position, c, d), h.attributes.has("listItemId") ? u.add(h.position.nodeAfter) : Ui(h.position.getShiftedBy(h.length), c, d);
      else if (h.type == "remove" && h.attributes.has("listItemId"))
        Ui(h.position, c, d);
      else if (h.type == "attribute") {
        const m = h.range.start.nodeAfter;
        e.includes(h.attributeKey) ? (Ui(h.range.start, c, d), h.attributeNewValue === null ? (Ui(h.range.start.getShiftedBy(1), c, d), r(m) && l.push(m)) : u.add(m)) : it(m) && r(m) && l.push(m);
      }
    for (const h of c.values())
      l.push(...s(h, u));
    for (const h of new Set(l))
      t.reconvertItem(h);
  };
  function s(a, l) {
    const c = [], u = /* @__PURE__ */ new Set(), d = [];
    for (const { node: h, previous: m } of new za(a)) {
      if (u.has(h))
        continue;
      const g = h.getAttribute("listIndent");
      m && g < m.getAttribute("listIndent") && (d.length = g + 1), d[g] = Object.fromEntries(Array.from(h.getAttributes()).filter(([w]) => e.includes(w)));
      const p = gs(h, {
        direction: "forward"
      });
      for (const w of p)
        u.add(w), (r(w, p) || o(w, d, l)) && c.push(w);
    }
    return c;
  }
  function r(a, l) {
    const c = t.mapper.toViewElement(a);
    if (!c)
      return !1;
    if (i.fire("checkElement", {
      modelElement: a,
      viewElement: c
    }))
      return !0;
    if (!a.is("element", "paragraph") && !a.is("element", "listItem"))
      return !1;
    const d = oC(a, e, l);
    return d && c.is("element", "p") ? !0 : !!(!d && c.is("element", "span"));
  }
  function o(a, l, c) {
    if (c.has(a))
      return !1;
    const u = t.mapper.toViewElement(a);
    let d = l.length - 1;
    for (let h = u.parent; !h.is("editableElement"); h = h.parent) {
      const m = Rh(h), g = yH(h);
      if (!g && !m)
        continue;
      const p = `checkAttributes:${m ? "item" : "list"}`;
      if (i.fire(p, {
        viewElement: h,
        modelAttributes: l[d]
      }))
        break;
      if (g && (d--, d < 0))
        return !1;
    }
    return !0;
  }
}
function K1(n, t, e, { dataPipeline: i } = {}) {
  const s = BH(n);
  return (r, o, a) => {
    const { writer: l, mapper: c, consumable: u } = a, d = o.item;
    if (!n.includes(o.attributeKey) || !s(d, u))
      return;
    const h = PH(d, c, e);
    rC(h, l, c), VH(h, l);
    const m = OH(d, h, t, l, {
      dataPipeline: i
    });
    LH(d, m, t, l);
  };
}
function IH(n) {
  return (t, e, i) => {
    const { writer: s, mapper: r } = i, o = t.name.split(":")[1];
    if (!n.checkAttribute(o, "listItemId"))
      return;
    const a = r.toViewPosition(e.position), l = e.position.getShiftedBy(e.length), c = r.toViewPosition(l, {
      isPhantom: !0
    }), d = s.createRange(a, c).getTrimmed().end.nodeBefore;
    /* istanbul ignore next -- @preserve */
    d && rC(d, s, r);
  };
}
function Z1(n, { dataPipeline: t } = {}) {
  return (e, { writer: i }) => {
    if (!oC(e, n))
      return null;
    if (!t)
      return i.createContainerElement("span", {
        class: "ck-list-bogus-paragraph"
      });
    const s = i.createContainerElement("p");
    return i.setCustomProperty("dataPipeline:transparentRendering", !0, s), s;
  };
}
function PH(n, t, e) {
  const i = e.createRangeOn(n);
  return t.toViewRange(i).getTrimmed().end.nodeBefore;
}
function RH(n, t) {
  return (e, i) => {
    if (i.modelPosition.offset > 0)
      return;
    const s = i.modelPosition.parent;
    if (!it(s) || !n.some((c) => c.scope == "itemMarker" && c.canInjectMarkerIntoElement && c.canInjectMarkerIntoElement(s)))
      return;
    const r = i.mapper.toViewElement(s), o = t.createRangeIn(r), a = o.getWalker();
    let l = o.start;
    for (const { item: c } of a) {
      if (c.is("element") && i.mapper.toModelElement(c) || c.is("$textProxy"))
        break;
      c.is("element") && c.getCustomProperty("listItemMarker") && (l = t.createPositionAfter(c), a.skip(({ previousPosition: u }) => !u.isEqual(l)));
    }
    i.viewPosition = l;
  };
}
function rC(n, t, e) {
  for (; n.parent.is("attributeElement") && n.parent.getCustomProperty("listItemWrapper"); )
    t.unwrap(t.createRangeOn(n), n.parent);
  const i = [];
  s(t.createPositionBefore(n).getWalker({
    direction: "backward"
  })), s(t.createRangeIn(n).getWalker());
  for (const r of i)
    t.remove(r);
  function s(r) {
    for (const { item: o } of r) {
      if (o.is("element") && e.toModelElement(o))
        break;
      o.is("element") && o.getCustomProperty("listItemMarker") && i.push(o);
    }
  }
}
function OH(n, t, e, i, { dataPipeline: s }) {
  let r = i.createRangeOn(t);
  if (!qn(n))
    return r;
  for (const o of e) {
    if (o.scope != "itemMarker")
      continue;
    const a = o.createElement(i, n, {
      dataPipeline: s
    });
    if (!a || (i.setCustomProperty("listItemMarker", !0, a), o.canInjectMarkerIntoElement && o.canInjectMarkerIntoElement(n) ? i.insert(i.createPositionAt(t, 0), a) : (i.insert(r.start, a), r = i.createRange(i.createPositionBefore(a), i.createPositionAfter(t))), !o.createWrapperElement || !o.canWrapElement))
      continue;
    const l = o.createWrapperElement(i, n, {
      dataPipeline: s
    });
    i.setCustomProperty("listItemWrapper", !0, l), o.canWrapElement(n) ? r = i.wrap(r, l) : (r = i.wrap(i.createRangeOn(a), l), r = i.createRange(r.start, i.createPositionAfter(t)));
  }
  return r;
}
function VH(n, t) {
  let e = n.parent;
  for (; e.is("attributeElement") && [
    "ul",
    "ol",
    "li"
  ].includes(e.name); ) {
    const i = e.parent;
    t.unwrap(t.createRangeOn(n), e), e = i;
  }
}
function LH(n, t, e, i) {
  if (!n.hasAttribute("listIndent"))
    return;
  const s = n.getAttribute("listIndent");
  let r = n;
  for (let o = s; o >= 0; o--) {
    const a = xH(i, o, r.getAttribute("listItemId")), l = CH(i, o, r.getAttribute("listType"));
    for (const c of e)
      (c.scope == "list" || c.scope == "item") && r.hasAttribute(c.attributeName) && c.setAttributeOnDowncast(i, r.getAttribute(c.attributeName), c.scope == "list" ? l : a);
    if (t = i.wrap(t, a), t = i.wrap(t, l), o == 0 || (r = Fi.first(r, {
      lowerIndent: !0
    }), !r))
      break;
  }
}
function BH(n) {
  return (t, e) => {
    const i = [];
    for (const s of n)
      t.hasAttribute(s) && i.push(`attribute:${s}`);
    return i.every((s) => e.test(t, s) !== !1) ? (i.forEach((s) => e.consume(t, s)), !0) : !1;
  };
}
function oC(n, t, e = Kr(n)) {
  if (!it(n))
    return !1;
  for (const i of n.getAttributeKeys())
    if (!(i.startsWith("selection:") || i == "htmlEmptyBlock") && !t.includes(i))
      return !1;
  return e.length < 2;
}
const Nl = [
  "listType",
  "listIndent",
  "listItemId"
];
class _a extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The list of registered downcast strategies.
    */
    f(this, "_downcastStrategies", []);
    e.config.define("list.multiBlock", !0);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Fa,
      Cn,
      _H,
      _i
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model, s = e.config.get("list.multiBlock");
    if (e.plugins.has("LegacyListEditing"))
      throw new S("list-feature-conflict", this, {
        conflictPlugin: "LegacyListEditing"
      });
    i.schema.register("$listItem", {
      allowAttributes: Nl
    }), s ? (i.schema.extend("$container", {
      allowAttributesOf: "$listItem"
    }), i.schema.extend("$block", {
      allowAttributesOf: "$listItem"
    }), i.schema.extend("$blockObject", {
      allowAttributesOf: "$listItem"
    })) : i.schema.register("listItem", {
      inheritAllFrom: "$block",
      allowAttributesOf: "$listItem"
    });
    for (const r of Nl)
      i.schema.setAttributeProperties(r, {
        copyOnReplace: !0
      });
    e.commands.add("numberedList", new Bo(e, "numbered")), e.commands.add("bulletedList", new Bo(e, "bulleted")), e.commands.add("customNumberedList", new Bo(e, "customNumbered", {
      multiLevel: !0
    })), e.commands.add("customBulletedList", new Bo(e, "customBulleted", {
      multiLevel: !0
    })), e.commands.add("indentList", new U1(e, "forward")), e.commands.add("outdentList", new U1(e, "backward")), e.commands.add("splitListItemBefore", new G1(e, "before")), e.commands.add("splitListItemAfter", new G1(e, "after")), s && (e.commands.add("mergeListItemBackward", new q1(e, "backward")), e.commands.add("mergeListItemForward", new q1(e, "forward"))), this._setupDeleteIntegration(), this._setupEnterIntegration(), this._setupTabIntegration(), this._setupClipboardIntegration(), this._setupAccessibilityIntegration();
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const i = this.editor.commands, s = i.get("indent"), r = i.get("outdent");
    s && s.registerChildCommand(i.get("indentList"), {
      priority: "high"
    }), r && r.registerChildCommand(i.get("outdentList"), {
      priority: "lowest"
    }), this._setupModelPostFixing(), this._setupConversion();
  }
  /**
  * Registers a downcast strategy.
  *
  * **Note**: Strategies must be registered in the `Plugin#init()` phase so that it can be applied
  * in the `ListEditing#afterInit()`.
  *
  * @param strategy The downcast strategy to register.
  */
  registerDowncastStrategy(e) {
    this._downcastStrategies.push(e);
  }
  /**
  * Returns list of model attribute names that should affect downcast conversion.
  */
  getListAttributeNames() {
    return [
      ...Nl,
      ...this._downcastStrategies.map((e) => e.attributeName)
    ];
  }
  /**
  * Attaches the listener to the {@link module:engine/view/document~Document#event:delete} event and handles backspace/delete
  * keys in and around document lists.
  */
  _setupDeleteIntegration() {
    const e = this.editor, i = e.commands.get("mergeListItemBackward"), s = e.commands.get("mergeListItemForward");
    this.listenTo(e.editing.view.document, "delete", (r, o) => {
      const a = e.model.document.selection;
      va(e.model) || e.model.change(() => {
        const l = a.getFirstPosition();
        if (a.isCollapsed && o.direction == "backward") {
          if (!l.isAtStart)
            return;
          const c = l.parent;
          if (!it(c))
            return;
          if (!Fi.first(c, {
            sameAttributes: "listType",
            sameIndent: !0
          }) && c.getAttribute("listIndent") === 0)
            j1(c) || e.execute("splitListItemAfter"), e.execute("outdentList");
          else {
            if (!i || !i.isEnabled)
              return;
            i.execute({
              shouldMergeOnBlocksContentLevel: Y1(e.model, "backward")
            });
          }
          o.preventDefault(), r.stop();
        } else {
          if (a.isCollapsed && !a.getLastPosition().isAtEnd || !s || !s.isEnabled)
            return;
          s.execute({
            shouldMergeOnBlocksContentLevel: Y1(e.model, "forward")
          }), o.preventDefault(), r.stop();
        }
      });
    }, {
      context: "li"
    });
  }
  /**
  * Attaches a listener to the {@link module:engine/view/document~Document#event:enter} event and handles enter key press
  * in document lists.
  */
  _setupEnterIntegration() {
    const e = this.editor, i = e.model, s = e.commands, r = s.get("enter");
    this.listenTo(e.editing.view.document, "enter", (o, a) => {
      const l = i.document, c = l.selection.getFirstPosition().parent;
      if (l.selection.isCollapsed && it(c) && c.isEmpty && !a.isSoft) {
        const u = qn(c), d = j1(c);
        u && d ? (e.execute("outdentList"), a.preventDefault(), o.stop()) : u && !d ? (e.execute("splitListItemAfter"), a.preventDefault(), o.stop()) : d && (e.execute("splitListItemBefore"), a.preventDefault(), o.stop());
      }
    }, {
      context: "li"
    }), this.listenTo(r, "afterExecute", () => {
      const o = s.get("splitListItemBefore");
      if (o.refresh(), !o.isEnabled)
        return;
      const l = e.model.document.selection.getLastPosition().parent;
      Kr(l).length === 2 && o.execute();
    });
  }
  /**
  * Attaches a listener to the {@link module:engine/view/document~Document#event:tab} event and handles tab key and tab+shift keys
  * presses in document lists.
  */
  _setupTabIntegration() {
    const e = this.editor;
    this.listenTo(e.editing.view.document, "tab", (i, s) => {
      const r = s.shiftKey ? "outdentList" : "indentList";
      this.editor.commands.get(r).isEnabled && (e.execute(r), s.stopPropagation(), s.preventDefault(), i.stop());
    }, {
      context: "li"
    });
  }
  /**
  * Registers the conversion helpers for the document-list feature.
  */
  _setupConversion() {
    const e = this.editor, i = e.model, s = this.getListAttributeNames(), r = e.config.get("list.multiBlock"), o = r ? "paragraph" : "listItem";
    e.conversion.for("upcast").elementToElement({
      view: "li",
      model: (l, { writer: c }) => c.createElement(o, {
        listType: ""
      })
    }).elementToElement({
      view: "p",
      model: (l, { writer: c }) => l.parent && l.parent.is("element", "li") ? c.createElement(o, {
        listType: ""
      }) : null,
      converterPriority: "high"
    }).add((l) => {
      l.on("element:li", SH());
    }), r || e.conversion.for("downcast").elementToElement({
      model: "listItem",
      view: "p"
    }), e.conversion.for("editingDowncast").elementToElement({
      model: o,
      view: Z1(s),
      converterPriority: "high"
    }).add((l) => {
      l.on("attribute", K1(s, this._downcastStrategies, i)), l.on("remove", IH(i.schema));
    }), e.conversion.for("dataDowncast").elementToElement({
      model: o,
      view: Z1(s, {
        dataPipeline: !0
      }),
      converterPriority: "high"
    }).add((l) => {
      l.on("attribute", K1(s, this._downcastStrategies, i, {
        dataPipeline: !0
      }));
    });
    const a = RH(this._downcastStrategies, e.editing.view);
    e.editing.mapper.on("modelToViewPosition", a), e.data.mapper.on("modelToViewPosition", a), this.listenTo(i.document, "change:data", AH(i, e.editing, s, this), {
      priority: "high"
    }), this.on("checkAttributes:item", (l, { viewElement: c, modelAttributes: u }) => {
      c.id != u.listItemId && (l.return = !0, l.stop());
    }), this.on("checkAttributes:list", (l, { viewElement: c, modelAttributes: u }) => {
      (c.name != nC(u.listType) || c.id != sC(u.listType, u.listIndent)) && (l.return = !0, l.stop());
    });
  }
  /**
  * Registers model post-fixers.
  */
  _setupModelPostFixing() {
    const e = this.editor.model, i = this.getListAttributeNames();
    e.document.registerPostFixer((s) => MH(e, s, i, this)), this.on("postFixer", (s, { listNodes: r, writer: o }) => {
      s.return = TH(r, o) || s.return;
    }, {
      priority: "high"
    }), this.on("postFixer", (s, { listNodes: r, writer: o, seenIds: a }) => {
      s.return = EH(r, a, o) || s.return;
    }, {
      priority: "high"
    });
  }
  /**
  * Integrates the feature with the clipboard via {@link module:engine/model/model~Model#insertContent} and
  * {@link module:engine/model/model~Model#getSelectedContent}.
  */
  _setupClipboardIntegration() {
    const e = this.editor.model, i = this.editor.plugins.get("ClipboardPipeline");
    this.listenTo(e, "insertContent", NH(e), {
      priority: "high"
    }), this.listenTo(i, "outputTransformation", (s, r) => {
      e.change((o) => {
        const a = Array.from(r.content.getChildren()), l = a[a.length - 1];
        if (a.length > 1 && l.is("element") && l.isEmpty && a.slice(0, -1).every(it) && o.remove(l), r.method == "copy" || r.method == "cut") {
          const c = Array.from(r.content.getChildren());
          wa(c) && ru(c, o);
        }
      });
    });
  }
  /**
  * Informs editor accessibility features about keystrokes brought by the plugin.
  */
  _setupAccessibilityIntegration() {
    const e = this.editor, i = e.t;
    e.accessibility.addKeystrokeInfoGroup({
      id: "list",
      label: i("Keystrokes that can be used in a list"),
      keystrokes: [
        {
          label: i("Increase list item indent"),
          keystroke: "Tab"
        },
        {
          label: i("Decrease list item indent"),
          keystroke: "Shift+Tab"
        }
      ]
    });
  }
}
function MH(n, t, e, i) {
  const s = n.document.differ.getChanges(), r = /* @__PURE__ */ new Set(), o = /* @__PURE__ */ new Set(), a = i.editor.config.get("list.multiBlock");
  let l = !1;
  for (const u of s) {
    if (u.type == "insert" && u.name != "$text") {
      const d = u.position.nodeAfter;
      if (!n.schema.checkAttribute(d, "listItemId"))
        for (const h of Array.from(d.getAttributeKeys()))
          e.includes(h) && (t.removeAttribute(h, d), l = !0);
      Ui(u.position, o, r), u.attributes.has("listItemId") || Ui(u.position.getShiftedBy(u.length), o, r);
      for (const { item: h, previousPosition: m } of n.createRangeIn(d))
        it(h) && Ui(m, o, r);
    } else u.type == "remove" ? Ui(u.position, o, r) : u.type == "attribute" && e.includes(u.attributeKey) && (Ui(u.range.start, o, r), u.attributeNewValue === null && Ui(u.range.start.getShiftedBy(1), o, r));
    if (!a && u.type == "attribute" && Nl.includes(u.attributeKey)) {
      const d = u.range.start.nodeAfter;
      u.attributeNewValue === null && d && d.is("element", "listItem") ? (t.rename(d, "paragraph"), l = !0) : u.attributeOldValue === null && d && d.is("element") && d.name != "listItem" && (t.rename(d, "listItem"), l = !0);
    }
  }
  const c = /* @__PURE__ */ new Set();
  for (const u of o.values())
    l = i.fire("postFixer", {
      listNodes: new gH(u),
      listHead: u,
      writer: t,
      seenIds: c
    }) || l;
  return l;
}
function NH(n) {
  return (t, [e, i]) => {
    const s = e.is("documentFragment") ? Array.from(e.getChildren()) : [
      e
    ];
    if (!s.length)
      return;
    const o = (i ? n.createSelection(i) : n.document.selection).getFirstPosition();
    let a;
    if (it(o.parent))
      a = o.parent;
    else if (it(o.nodeBefore) && it(o.nodeAfter))
      a = o.nodeBefore;
    else
      return;
    n.change((l) => {
      const c = a.getAttribute("listType"), u = a.getAttribute("listIndent"), d = s[0].getAttribute("listIndent") || 0, h = Math.max(u - d, 0);
      for (const m of s) {
        const g = it(m);
        a.is("element", "listItem") && m.is("element", "paragraph") && l.rename(m, "listItem"), l.setAttributes({
          listIndent: (g ? m.getAttribute("listIndent") : 0) + h,
          listItemId: g ? m.getAttribute("listItemId") : Gr.next(),
          listType: c
        }, m);
      }
    });
  };
}
function Y1(n, t) {
  const e = n.document.selection;
  if (!e.isCollapsed)
    return !va(n);
  if (t === "forward")
    return !0;
  const s = e.getFirstPosition().parent, r = s.previousSibling;
  return n.schema.isObject(r) ? !1 : r.isEmpty ? !0 : wa([
    s,
    r
  ]);
}
function Oh(n, t, e, i) {
  n.ui.componentFactory.add(t, () => {
    const s = J1(le, n, t, e, i);
    return s.set({
      tooltip: !0,
      isToggleable: !0
    }), s;
  }), n.ui.componentFactory.add(`menuBar:${t}`, () => {
    const s = J1(Fe, n, t, e, i);
    return s.set({
      role: "menuitemcheckbox",
      isToggleable: !0
    }), s;
  });
}
function J1(n, t, e, i, s) {
  const r = t.commands.get(e), o = new n(t.locale);
  return o.set({
    label: i,
    icon: s
  }), o.bind("isOn", "isEnabled").to(r, "value", "isEnabled"), o.on("execute", () => {
    t.execute(e), t.editing.view.focus();
  }), o;
}
class FH extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.t;
    this.editor.ui.componentFactory.has("numberedList") || Oh(this.editor, "numberedList", t("Numbered List"), Z.numberedList), this.editor.ui.componentFactory.has("bulletedList") || Oh(this.editor, "bulletedList", t("Bulleted List"), Z.bulletedList);
  }
}
class DH extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      _a,
      FH
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "List";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class $H extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this._getValue();
    this.value = t, this.isEnabled = t != null;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.startIndex The list start index.
  */
  execute({ startIndex: t = 1 } = {}) {
    const e = this.editor.model, i = e.document;
    let s = Array.from(i.selection.getSelectedBlocks()).filter((r) => it(r) && fc(r.getAttribute("listType")));
    s = su(s), e.change((r) => {
      for (const o of s)
        r.setAttribute("listStart", t >= 0 ? t : 1, o);
    });
  }
  /**
  * Checks the command's {@link #value}.
  *
  * @returns The current value.
  */
  _getValue() {
    const e = this.editor.model.document, i = Xe(e.selection.getSelectedBlocks());
    return i && it(i) && fc(i.getAttribute("listType")) ? i.getAttribute("listStart") : null;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const aC = {}, lC = {}, cC = {}, uC = [
  {
    listStyle: "disc",
    typeAttribute: "disc",
    listType: "bulleted"
  },
  {
    listStyle: "circle",
    typeAttribute: "circle",
    listType: "bulleted"
  },
  {
    listStyle: "square",
    typeAttribute: "square",
    listType: "bulleted"
  },
  {
    listStyle: "decimal",
    typeAttribute: "1",
    listType: "numbered"
  },
  {
    listStyle: "decimal-leading-zero",
    typeAttribute: null,
    listType: "numbered"
  },
  {
    listStyle: "lower-roman",
    typeAttribute: "i",
    listType: "numbered"
  },
  {
    listStyle: "upper-roman",
    typeAttribute: "I",
    listType: "numbered"
  },
  {
    listStyle: "lower-alpha",
    typeAttribute: "a",
    listType: "numbered"
  },
  {
    listStyle: "upper-alpha",
    typeAttribute: "A",
    listType: "numbered"
  },
  {
    listStyle: "lower-latin",
    typeAttribute: "a",
    listType: "numbered"
  },
  {
    listStyle: "upper-latin",
    typeAttribute: "A",
    listType: "numbered"
  }
];
for (const { listStyle: n, typeAttribute: t, listType: e } of uC)
  aC[n] = e, lC[n] = t, t && (cC[t] = n);
function dC() {
  return uC.map((n) => n.listStyle);
}
function um(n) {
  return aC[n] || null;
}
function hC(n) {
  return cC[n] || null;
}
function Vh(n) {
  return lC[n] || null;
}
function zH(n) {
  switch (n) {
    case "lower-alpha":
      return "lower-latin";
    case "upper-alpha":
      return "upper-latin";
    default:
      return n;
  }
}
class HH extends he {
  /**
  * Creates an instance of the command.
  *
  * @param editor The editor instance.
  * @param defaultType The list type that will be used by default if the value was not specified during
  * the command execution.
  * @param supportedTypes The list of supported style types by this command.
  */
  constructor(e, i, s) {
    super(e);
    /**
    * The default type of the list style.
    */
    f(this, "defaultType");
    /**
    * The list of supported style types by this command.
    */
    f(this, "_supportedTypes");
    this.defaultType = i, this._supportedTypes = s;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    this.value = this._getValue(), this.isEnabled = this._checkEnabled();
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.type The type of the list style, e.g. `'disc'` or `'square'`. If `null` is specified, the default
  * style will be applied.
  */
  execute(e = {}) {
    const i = this.editor.model, s = i.document;
    i.change((r) => {
      this._tryToConvertItemsToList(e);
      let o = Array.from(s.selection.getSelectedBlocks()).filter((a) => a.hasAttribute("listType"));
      if (o.length) {
        o = su(o);
        for (const a of o)
          r.setAttribute("listStyle", e.type || this.defaultType, a);
      }
    });
  }
  /**
  * Checks if the given style type is supported by this plugin.
  */
  isStyleTypeSupported(e) {
    return this._supportedTypes ? this._supportedTypes.includes(e) : !0;
  }
  /**
  * Checks the command's {@link #value}.
  *
  * @returns The current value.
  */
  _getValue() {
    const e = Xe(this.editor.model.document.selection.getSelectedBlocks());
    return it(e) ? e.getAttribute("listStyle") : null;
  }
  /**
  * Checks whether the command can be enabled in the current context.
  *
  * @returns Whether the command should be enabled.
  */
  _checkEnabled() {
    const e = this.editor, i = e.commands.get("numberedList"), s = e.commands.get("bulletedList");
    return i.isEnabled || s.isEnabled;
  }
  /**
   * Check if the provided list style is valid. Also change the selection to a list if it's not set yet.
   *
   * @param options.type The type of the list style. If `null` is specified, the function does nothing.
  */
  _tryToConvertItemsToList(e) {
    if (!e.type)
      return;
    const i = um(e.type);
    if (!i)
      return;
    const s = this.editor, r = `${i}List`;
    s.commands.get(r).value || s.execute(r);
  }
}
class jH extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this._getValue();
    this.value = t, this.isEnabled = t != null;
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.reversed Whether the list should be reversed.
  */
  execute(t = {}) {
    const e = this.editor.model, i = e.document;
    let s = Array.from(i.selection.getSelectedBlocks()).filter((r) => it(r) && r.getAttribute("listType") == "numbered");
    s = su(s), e.change((r) => {
      for (const o of s)
        r.setAttribute("listReversed", !!t.reversed, o);
    });
  }
  /**
  * Checks the command's {@link #value}.
  */
  _getValue() {
    const e = this.editor.model.document, i = Xe(e.selection.getSelectedBlocks());
    return it(i) && i.getAttribute("listType") == "numbered" ? i.getAttribute("listReversed") : null;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function X1(n) {
  return (t, e, i) => {
    const { writer: s, schema: r, consumable: o } = i;
    if (o.test(e.viewItem, n.viewConsumables) === !1)
      return;
    e.modelRange || Object.assign(e, i.convertChildren(e.viewItem, e.modelCursor));
    let a = !1;
    for (const l of e.modelRange.getItems({
      shallow: !0
    }))
      r.checkAttribute(l, n.attributeName) && n.appliesToListItem(l) && (l.hasAttribute(n.attributeName) || (s.setAttribute(n.attributeName, n.getAttributeOnUpcast(e.viewItem), l), a = !0));
    a && o.consume(e.viewItem, n.viewConsumables);
  };
}
class UH extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListPropertiesUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * Gets all the style types supported by given list type.
  */
  getAllSupportedStyleTypes() {
    return dC();
  }
  /**
  * Checks whether the given list-style-type is supported by numbered or bulleted list.
  */
  getListTypeFromListStyleType(t) {
    return um(t);
  }
  /**
  * Converts `type` attribute of `<ul>` or `<ol>` elements to `list-style-type` equivalent.
  */
  getListStyleTypeFromTypeAttribute(t) {
    return hC(t);
  }
  /**
  * Converts `list-style-type` style to `type` attribute of `<ul>` or `<ol>` elements.
  */
  getTypeAttributeFromListStyleType(t) {
    return Vh(t);
  }
}
function fC(n) {
  const { startIndex: t, reversed: e, styles: i } = n;
  return {
    styles: WH(i),
    startIndex: t || !1,
    reversed: e || !1
  };
}
function WH(n) {
  const t = {
    listTypes: [
      "bulleted",
      "numbered"
    ],
    useAttribute: !1
  };
  return n === !0 || (n ? Array.isArray(n) || typeof n == "string" ? t.listTypes = Ue(n) : (t.listTypes = n.listTypes ? Ue(n.listTypes) : t.listTypes, t.useAttribute = !!n.useAttribute, n.listStyleTypes && (t.listStyleTypes = n.listStyleTypes)) : t.listTypes = []), t;
}
const So = "default";
class qH extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      _a,
      UH
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListPropertiesEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.config.define("list.properties", {
      styles: !0,
      startIndex: !1,
      reversed: !1
    });
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model, i = t.plugins.get(_a), s = t.config.get("list.properties"), r = GH(s);
    for (const o of r)
      o.addCommand(t), e.schema.extend("$listItem", {
        allowAttributes: o.attributeName
      }), i.registerDowncastStrategy({
        scope: "list",
        attributeName: o.attributeName,
        setAttributeOnDowncast(a, l, c) {
          o.setAttributeOnDowncast(a, l, c);
        }
      });
    t.conversion.for("upcast").add((o) => {
      for (const a of r)
        o.on("element:ol", X1(a)), o.on("element:ul", X1(a));
    }), i.on("checkAttributes:list", (o, { viewElement: a, modelAttributes: l }) => {
      for (const c of r)
        c.getAttributeOnUpcast(a) != l[c.attributeName] && (o.return = !0, o.stop());
    }), this.listenTo(t.commands.get("indentList"), "afterExecute", (o, a) => {
      e.change((l) => {
        for (const c of a)
          for (const u of r)
            u.appliesToListItem(c) && l.setAttribute(u.attributeName, u.defaultValue, c);
      });
    }), i.on("postFixer", (o, { listNodes: a, writer: l }) => {
      for (const { node: c } of a)
        for (const u of r)
          u.hasValidAttribute(c) || (u.appliesToListItem(c) ? l.setAttribute(u.attributeName, u.defaultValue, c) : l.removeAttribute(u.attributeName, c), o.return = !0);
    }), i.on("postFixer", (o, { listNodes: a, writer: l }) => {
      for (const { node: c, previousNodeInList: u } of a)
        if (u && u.getAttribute("listType") == c.getAttribute("listType"))
          for (const d of r) {
            const { attributeName: h } = d;
            if (!d.appliesToListItem(c))
              continue;
            const m = u.getAttribute(h);
            c.getAttribute(h) != m && (l.setAttribute(h, m, c), o.return = !0);
          }
    });
  }
}
function GH(n) {
  const t = [], e = fC(n);
  if (n.styles) {
    const i = e.styles.useAttribute;
    t.push({
      attributeName: "listStyle",
      defaultValue: So,
      viewConsumables: {
        styles: "list-style-type"
      },
      addCommand(s) {
        let r = dC();
        i && (r = r.filter((o) => !!Vh(o))), s.commands.add("listStyle", new HH(s, So, r));
      },
      appliesToListItem(s) {
        return s.getAttribute("listType") == "numbered" || s.getAttribute("listType") == "bulleted";
      },
      hasValidAttribute(s) {
        if (!this.appliesToListItem(s))
          return !s.hasAttribute("listStyle");
        if (!s.hasAttribute("listStyle"))
          return !1;
        const r = s.getAttribute("listStyle");
        return r == So ? !0 : um(r) == s.getAttribute("listType");
      },
      setAttributeOnDowncast(s, r, o) {
        if (r && r !== So)
          if (i) {
            const a = Vh(r);
            if (a) {
              s.setAttribute("type", a, o);
              return;
            }
          } else {
            s.setStyle("list-style-type", r, o);
            return;
          }
        s.removeStyle("list-style-type", o), s.removeAttribute("type", o);
      },
      getAttributeOnUpcast(s) {
        const r = s.getStyle("list-style-type");
        if (r)
          return zH(r);
        const o = s.getAttribute("type");
        return o ? hC(o) : So;
      }
    });
  }
  return n.reversed && t.push({
    attributeName: "listReversed",
    defaultValue: !1,
    viewConsumables: {
      attributes: "reversed"
    },
    addCommand(i) {
      i.commands.add("listReversed", new jH(i));
    },
    appliesToListItem(i) {
      return i.getAttribute("listType") == "numbered";
    },
    hasValidAttribute(i) {
      return this.appliesToListItem(i) == i.hasAttribute("listReversed");
    },
    setAttributeOnDowncast(i, s, r) {
      s ? i.setAttribute("reversed", "reversed", r) : i.removeAttribute("reversed", r);
    },
    getAttributeOnUpcast(i) {
      return i.hasAttribute("reversed");
    }
  }), n.startIndex && t.push({
    attributeName: "listStart",
    defaultValue: 1,
    viewConsumables: {
      attributes: "start"
    },
    addCommand(i) {
      i.commands.add("listStart", new $H(i));
    },
    appliesToListItem(i) {
      return fc(i.getAttribute("listType"));
    },
    hasValidAttribute(i) {
      return this.appliesToListItem(i) == i.hasAttribute("listStart");
    },
    setAttributeOnDowncast(i, s, r) {
      s == 0 || s > 1 ? i.setAttribute("start", s, r) : i.removeAttribute("start", r);
    },
    getAttributeOnUpcast(i) {
      const s = i.getAttribute("start");
      return s >= 0 ? s : 1;
    }
  }), t;
}
class mC extends ee {
  /**
  * Creates an instance of the list properties view.
  *
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param options Options of the view.
  * @param options.enabledProperties An object containing the configuration of enabled list property names.
  * Allows conditional rendering the sub-components of the properties view.
  * @param options.styleButtonViews A list of style buttons to be rendered
  * inside the styles grid. The grid will not be rendered when `enabledProperties` does not include the `'styles'` key.
  * @param options.styleGridAriaLabel An assistive technologies label set on the grid of styles (if the grid is rendered).
  */
  constructor(e, { enabledProperties: i, styleButtonViews: s, styleGridAriaLabel: r }) {
    super(e);
    /**
    * A collection of the child views.
    */
    f(this, "children");
    /**
    * A view that renders the grid of list styles.
    */
    f(this, "stylesView", null);
    /**
    * A collapsible view that hosts additional list property fields ({@link #startIndexFieldView} and
    * {@link #reversedSwitchButtonView}) to visually separate them from the {@link #stylesView grid of styles}.
    *
    * **Note**: Only present when:
    * * the view represents **numbered** list properties,
    * * and the {@link #stylesView} is rendered,
    * * and either {@link #startIndexFieldView} or {@link #reversedSwitchButtonView} is rendered.
    *
    * @readonly
    */
    f(this, "additionalPropertiesCollapsibleView", null);
    /**
    * A labeled number field allowing the user to set the start index of the list.
    *
    * **Note**: Only present when the view represents **numbered** list properties.
    *
    * @readonly
    */
    f(this, "startIndexFieldView", null);
    /**
    * A switch button allowing the user to make the edited list reversed.
    *
    * **Note**: Only present when the view represents **numbered** list properties.
    *
    * @readonly
    */
    f(this, "reversedSwitchButtonView", null);
    /**
    * Tracks information about the DOM focus in the view.
    */
    f(this, "focusTracker", new ze());
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes", new Ze());
    /**
    * A collection of views that can be focused in the properties view.
    */
    f(this, "focusables", new Et());
    /**
    * Helps cycling over {@link #focusables} in the view.
    */
    f(this, "focusCycler");
    const o = [
      "ck",
      "ck-list-properties"
    ];
    this.children = this.createCollection(), this.focusCycler = new Pt({
      focusables: this.focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate #children backwards using the <kbd>Shift</kbd> + <kbd>Tab</kbd> keystroke.
        focusPrevious: "shift + tab",
        // Navigate #children forwards using the <kbd>Tab</kbd> key.
        focusNext: "tab"
      }
    }), s && s.length ? (this.stylesView = this._createStylesView(s, r), this.children.add(this.stylesView)) : o.push("ck-list-properties_without-styles"), (i.startIndex || i.reversed) && (this._addNumberedListPropertyViews(i), o.push("ck-list-properties_with-numbered-properties")), this.setTemplate({
      tag: "div",
      attributes: {
        class: o
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    if (super.render(), this.stylesView) {
      this.focusables.add(this.stylesView), this.focusTracker.add(this.stylesView.element), (this.startIndexFieldView || this.reversedSwitchButtonView) && (this.focusables.add(this.children.last.buttonView), this.focusTracker.add(this.children.last.buttonView.element));
      for (const e of this.stylesView.children)
        this.stylesView.focusTracker.add(e.element);
      Oa({
        keystrokeHandler: this.stylesView.keystrokes,
        focusTracker: this.stylesView.focusTracker,
        gridItems: this.stylesView.children,
        // Note: The styles view has a different number of columns depending on whether the other properties
        // are enabled in the dropdown or not (https://github.com/ckeditor/ckeditor5/issues/12340)
        numberOfColumns: () => ae.window.getComputedStyle(this.stylesView.element).getPropertyValue("grid-template-columns").split(" ").length,
        uiLanguageDirection: this.locale && this.locale.uiLanguageDirection
      });
    }
    if (this.startIndexFieldView) {
      this.focusables.add(this.startIndexFieldView), this.focusTracker.add(this.startIndexFieldView.element);
      const e = (i) => i.stopPropagation();
      this.keystrokes.set("arrowright", e), this.keystrokes.set("arrowleft", e), this.keystrokes.set("arrowup", e), this.keystrokes.set("arrowdown", e);
    }
    this.reversedSwitchButtonView && (this.focusables.add(this.reversedSwitchButtonView), this.focusTracker.add(this.reversedSwitchButtonView.element)), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  focus() {
    this.focusCycler.focusFirst();
  }
  /**
  * @inheritDoc
  */
  focusLast() {
    this.focusCycler.focusLast();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates the list styles grid.
  *
  * @param styleButtons Buttons to be placed in the grid.
  * @param styleGridAriaLabel The assistive technology label of the grid.
  */
  _createStylesView(e, i) {
    const s = new ee(this.locale);
    return s.children = s.createCollection(), s.children.addMany(e), s.setTemplate({
      tag: "div",
      attributes: {
        "aria-label": i,
        class: [
          "ck",
          "ck-list-styles-list"
        ]
      },
      children: s.children
    }), s.children.delegate("execute").to(this), s.focus = function() {
      for (const r of this.children)
        if (r instanceof le && r.isOn) {
          r.focus();
          return;
        }
      this.children.first.focus();
    }, s.focusTracker = new ze(), s.keystrokes = new Ze(), s.render(), s.keystrokes.listenTo(s.element), s;
  }
  /**
  * Renders {@link #startIndexFieldView} and/or {@link #reversedSwitchButtonView} depending on the configuration of the properties view.
  *
  * @param enabledProperties An object containing the configuration of enabled list property names
  * (see {@link #constructor}).
  */
  _addNumberedListPropertyViews(e) {
    const i = this.locale.t, s = [];
    e.startIndex && (this.startIndexFieldView = this._createStartIndexField(), s.push(this.startIndexFieldView)), e.reversed && (this.reversedSwitchButtonView = this._createReversedSwitchButton(), s.push(this.reversedSwitchButtonView)), this.stylesView ? (this.additionalPropertiesCollapsibleView = new $f(this.locale, s), this.additionalPropertiesCollapsibleView.set({
      label: i("List properties"),
      isCollapsed: !0
    }), this.additionalPropertiesCollapsibleView.buttonView.bind("isEnabled").toMany(s, "isEnabled", (...r) => r.some((o) => o)), this.additionalPropertiesCollapsibleView.buttonView.on("change:isEnabled", (r, o, a) => {
      a || (this.additionalPropertiesCollapsibleView.isCollapsed = !0);
    }), this.children.add(this.additionalPropertiesCollapsibleView)) : this.children.addMany(s);
  }
  /**
  * Creates the list start index labeled field.
  */
  _createStartIndexField() {
    const e = this.locale.t, i = new gt(this.locale, Dy);
    return i.set({
      label: e("Start at"),
      class: "ck-numbered-list-properties__start-index"
    }), i.fieldView.set({
      min: 0,
      step: 1,
      value: 1,
      inputMode: "numeric"
    }), i.fieldView.on("input", () => {
      const s = i.fieldView.element, r = s.valueAsNumber;
      if (Number.isNaN(r)) {
        i.errorText = e("Invalid start index value.");
        return;
      }
      s.checkValidity() ? this.fire("listStart", {
        startIndex: r
      }) : i.errorText = e("Start index must be greater than 0.");
    }), i;
  }
  /**
  * Creates the reversed list switch button.
  */
  _createReversedSwitchButton() {
    const e = this.locale.t, i = new js(this.locale);
    return i.set({
      withText: !0,
      label: e("Reversed order"),
      class: "ck-numbered-list-properties__reversed-order"
    }), i.delegate("execute").to(this, "listReversed"), i;
  }
}
var KH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M11 27a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0-9a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0-9a3 3 0 1 1 0 6 3 3 0 0 1 0-6z"/></svg>', ZH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M11 27a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0 1a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm0-10a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0 1a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm0-10a3 3 0 1 1 0 6 3 3 0 0 1 0-6zm0 1a2 2 0 1 0 0 4 2 2 0 0 0 0-4z"/></svg>', YH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M14 27v6H8v-6h6zm0-9v6H8v-6h6zm0-9v6H8V9h6z"/></svg>', JH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M10.29 15V8.531H9.286c-.14.393-.4.736-.778 1.03-.378.295-.728.495-1.05.6v1.121a4.257 4.257 0 0 0 1.595-.936V15h1.235zm3.343 0v-1.235h-1.235V15h1.235zM11.3 24v-1.147H8.848c.064-.111.148-.226.252-.343.104-.117.351-.354.74-.712.39-.357.66-.631.81-.821.225-.288.39-.562.494-.824.104-.263.156-.539.156-.829 0-.51-.182-.936-.545-1.279-.363-.342-.863-.514-1.499-.514-.58 0-1.063.148-1.45.444-.387.296-.617.784-.69 1.463l1.23.124c.024-.36.112-.619.264-.774.153-.155.358-.233.616-.233.26 0 .465.074.613.222.148.148.222.36.222.635 0 .25-.085.501-.255.756-.126.185-.468.536-1.024 1.055-.692.641-1.155 1.156-1.389 1.544-.234.389-.375.8-.422 1.233H11.3zm2.333 0v-1.235h-1.235V24h1.235zM9.204 34.11c.615 0 1.129-.2 1.542-.598.413-.398.62-.88.62-1.446 0-.39-.11-.722-.332-.997a1.5 1.5 0 0 0-.886-.532c.619-.337.928-.788.928-1.353 0-.399-.151-.756-.453-1.073-.366-.386-.852-.58-1.459-.58a2.25 2.25 0 0 0-.96.2 1.617 1.617 0 0 0-.668.55c-.16.232-.28.544-.358.933l1.138.194c.032-.282.123-.495.272-.642.15-.146.33-.22.54-.22.215 0 .386.065.515.194s.193.302.193.518c0 .255-.087.46-.263.613-.176.154-.43.227-.765.218l-.136 1.006c.22-.061.409-.092.567-.092.24 0 .444.09.61.272.168.182.251.428.251.739 0 .328-.087.589-.261.782a.833.833 0 0 1-.644.29.841.841 0 0 1-.607-.242c-.167-.16-.27-.394-.307-.698l-1.196.145c.062.542.285.98.668 1.316.384.335.868.503 1.45.503zm4.43-.11v-1.235h-1.236V34h1.235z"/></svg>', XH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M5.714 15.11c.624 0 1.11-.22 1.46-.66.421-.533.632-1.408.632-2.627 0-1.222-.21-2.096-.629-2.624-.351-.445-.839-.668-1.463-.668-.624 0-1.11.22-1.459.66-.422.533-.633 1.406-.633 2.619 0 1.236.192 2.095.576 2.577.384.482.89.723 1.516.723zm0-1.024a.614.614 0 0 1-.398-.14c-.115-.094-.211-.283-.287-.565-.077-.283-.115-.802-.115-1.558s.043-1.294.128-1.613c.064-.246.155-.417.272-.512a.617.617 0 0 1 .4-.143.61.61 0 0 1 .398.143c.116.095.211.284.288.567.076.283.114.802.114 1.558s-.043 1.292-.128 1.608c-.064.246-.155.417-.272.512a.617.617 0 0 1-.4.143zm6.078.914V8.531H10.79c-.14.393-.4.736-.778 1.03-.378.295-.728.495-1.05.6v1.121a4.257 4.257 0 0 0 1.595-.936V15h1.235zm3.344 0v-1.235h-1.235V15h1.235zm-9.422 9.11c.624 0 1.11-.22 1.46-.66.421-.533.632-1.408.632-2.627 0-1.222-.21-2.096-.629-2.624-.351-.445-.839-.668-1.463-.668-.624 0-1.11.22-1.459.66-.422.533-.633 1.406-.633 2.619 0 1.236.192 2.095.576 2.577.384.482.89.723 1.516.723zm0-1.024a.614.614 0 0 1-.398-.14c-.115-.094-.211-.283-.287-.565-.077-.283-.115-.802-.115-1.558s.043-1.294.128-1.613c.064-.246.155-.417.272-.512a.617.617 0 0 1 .4-.143.61.61 0 0 1 .398.143c.116.095.211.284.288.567.076.283.114.802.114 1.558s-.043 1.292-.128 1.608c-.064.246-.155.417-.272.512a.617.617 0 0 1-.4.143zm7.088.914v-1.147H10.35c.065-.111.149-.226.253-.343.104-.117.35-.354.74-.712.39-.357.66-.631.81-.821.225-.288.39-.562.493-.824.104-.263.156-.539.156-.829 0-.51-.181-.936-.544-1.279-.364-.342-.863-.514-1.499-.514-.58 0-1.063.148-1.45.444-.387.296-.617.784-.69 1.463l1.23.124c.024-.36.112-.619.264-.774.152-.155.357-.233.615-.233.261 0 .465.074.613.222.148.148.222.36.222.635 0 .25-.085.501-.255.756-.126.185-.467.536-1.024 1.055-.691.641-1.154 1.156-1.388 1.544-.235.389-.375.8-.422 1.233h4.328zm2.334 0v-1.235h-1.235V24h1.235zM5.714 34.11c.624 0 1.11-.22 1.46-.66.421-.533.632-1.408.632-2.627 0-1.222-.21-2.096-.629-2.624-.351-.445-.839-.668-1.463-.668-.624 0-1.11.22-1.459.66-.422.533-.633 1.406-.633 2.619 0 1.236.192 2.095.576 2.577.384.482.89.723 1.516.723zm0-1.024a.614.614 0 0 1-.398-.14c-.115-.094-.211-.283-.287-.565-.077-.283-.115-.802-.115-1.558s.043-1.294.128-1.613c.064-.246.155-.417.272-.512a.617.617 0 0 1 .4-.143.61.61 0 0 1 .398.143c.116.095.211.284.288.567.076.283.114.802.114 1.558s-.043 1.292-.128 1.608c-.064.246-.155.417-.272.512a.617.617 0 0 1-.4.143zm4.992 1.024c.616 0 1.13-.2 1.543-.598.413-.398.62-.88.62-1.446 0-.39-.111-.722-.332-.997a1.5 1.5 0 0 0-.886-.532c.618-.337.927-.788.927-1.353 0-.399-.15-.756-.452-1.073-.366-.386-.853-.58-1.46-.58a2.25 2.25 0 0 0-.96.2 1.617 1.617 0 0 0-.667.55c-.16.232-.28.544-.359.933l1.139.194c.032-.282.123-.495.272-.642.15-.146.33-.22.54-.22.214 0 .386.065.515.194s.193.302.193.518c0 .255-.088.46-.264.613-.175.154-.43.227-.764.218l-.136 1.006c.22-.061.408-.092.566-.092.24 0 .444.09.611.272.167.182.25.428.25.739 0 .328-.086.589-.26.782a.833.833 0 0 1-.644.29.841.841 0 0 1-.607-.242c-.167-.16-.27-.394-.308-.698l-1.195.145c.062.542.284.98.668 1.316.384.335.867.503 1.45.503zm4.43-.11v-1.235h-1.235V34h1.235z"/></svg>', QH = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M11.88 8.7V7.558h-1.234V8.7h1.234zm0 5.3V9.333h-1.234V14h1.234zm2.5 0v-1.235h-1.234V14h1.235zm-4.75 4.7v-1.142H8.395V18.7H9.63zm0 5.3v-4.667H8.395V24H9.63zm2.5-5.3v-1.142h-1.234V18.7h1.235zm0 5.3v-4.667h-1.234V24h1.235zm2.501 0v-1.235h-1.235V24h1.235zM7.38 28.7v-1.142H6.145V28.7H7.38zm0 5.3v-4.667H6.145V34H7.38zm2.5-5.3v-1.142H8.646V28.7H9.88zm0 5.3v-4.667H8.646V34H9.88zm2.5-5.3v-1.142h-1.234V28.7h1.235zm0 5.3v-4.667h-1.234V34h1.235zm2.501 0v-1.235h-1.235V34h1.235z"/></svg>', ej = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M11.916 15V8.558h-1.301V15h1.3zm2.465 0v-1.235h-1.235V15h1.235zM9.665 25v-6.442h-1.3V25h1.3zm2.5 0v-6.442h-1.3V25h1.3zm2.466 0v-1.235h-1.235V25h1.235zm-7.216 9v-6.442h-1.3V34h1.3zm2.5 0v-6.442h-1.3V34h1.3zm2.501 0v-6.442h-1.3V34h1.3zm2.465 0v-1.235h-1.235V34h1.235z"/></svg>', tj = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="M9.62 14.105c.272 0 .528-.05.768-.153s.466-.257.677-.462c.009.024.023.072.044.145.047.161.086.283.119.365h1.221a2.649 2.649 0 0 1-.222-.626c-.04-.195-.059-.498-.059-.908l.013-1.441c0-.536-.055-.905-.165-1.105-.11-.201-.3-.367-.569-.497-.27-.13-.68-.195-1.23-.195-.607 0-1.064.108-1.371.325-.308.217-.525.55-.65 1.002l1.12.202c.076-.217.176-.369.299-.455.123-.086.294-.13.514-.13.325 0 .546.05.663.152.118.101.176.27.176.508v.123c-.222.093-.622.194-1.2.303-.427.082-.755.178-.982.288-.227.11-.403.268-.53.474a1.327 1.327 0 0 0-.188.706c0 .398.138.728.415.988.277.261.656.391 1.136.391zm.368-.87a.675.675 0 0 1-.492-.189.606.606 0 0 1-.193-.448c0-.176.08-.32.241-.435.106-.07.33-.142.673-.215a7.19 7.19 0 0 0 .751-.19v.247c0 .296-.016.496-.048.602a.773.773 0 0 1-.295.409 1.07 1.07 0 0 1-.637.22zm4.645.765v-1.235h-1.235V14h1.235zM10.2 25.105c.542 0 1.003-.215 1.382-.646.38-.43.57-1.044.57-1.84 0-.771-.187-1.362-.559-1.774a1.82 1.82 0 0 0-1.41-.617c-.522 0-.973.216-1.354.65v-2.32H7.594V25h1.147v-.686a1.9 1.9 0 0 0 .67.592c.26.133.523.2.79.2zm-.299-.975c-.354 0-.638-.164-.852-.492-.153-.232-.229-.59-.229-1.073 0-.468.098-.818.295-1.048a.93.93 0 0 1 .738-.345c.302 0 .55.118.743.354.193.236.29.62.29 1.154 0 .5-.096.868-.288 1.1-.192.233-.424.35-.697.35zm4.478.87v-1.235h-1.234V25h1.234zm-4.017 9.105c.6 0 1.08-.142 1.437-.426.357-.284.599-.704.725-1.261l-1.213-.207c-.061.326-.167.555-.316.688a.832.832 0 0 1-.576.2.916.916 0 0 1-.75-.343c-.185-.228-.278-.62-.278-1.173 0-.498.091-.853.274-1.066.183-.212.429-.318.736-.318.232 0 .42.061.565.184.145.123.238.306.28.55l1.216-.22c-.146-.501-.387-.874-.722-1.119-.336-.244-.788-.366-1.356-.366-.695 0-1.245.214-1.653.643-.407.43-.61 1.03-.61 1.8 0 .762.202 1.358.608 1.788.406.431.95.646 1.633.646zM14.633 34v-1.235h-1.235V34h1.235z"/></svg>', ij = '<svg viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg"><path d="M35 29a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17zm0-9a1 1 0 0 1 1 1v1a1 1 0 0 1-1 1H18a1 1 0 0 1-1-1v-1a1 1 0 0 1 1-1h17z" fill-opacity=".163"/><path d="m7.88 15 .532-1.463h2.575L11.549 15h1.415l-2.58-6.442H9.01L6.5 15h1.38zm2.69-2.549H8.811l.87-2.39.887 2.39zM14.88 15v-1.235h-1.234V15h1.234zM9.352 25c.83-.006 1.352-.02 1.569-.044.346-.038.636-.14.872-.305.236-.166.422-.387.558-.664.137-.277.205-.562.205-.855 0-.372-.106-.695-.317-.97-.21-.276-.512-.471-.905-.585a1.51 1.51 0 0 0 .661-.567 1.5 1.5 0 0 0 .244-.83c0-.28-.066-.53-.197-.754a1.654 1.654 0 0 0-.495-.539 1.676 1.676 0 0 0-.672-.266c-.25-.042-.63-.063-1.14-.063H7.158V25h2.193zm.142-3.88H8.46v-1.49h.747c.612 0 .983.007 1.112.022.217.026.38.102.49.226.11.125.165.287.165.486a.68.68 0 0 1-.192.503.86.86 0 0 1-.525.23 11.47 11.47 0 0 1-.944.023h.18zm.17 2.795H8.46v-1.723h1.05c.592 0 .977.03 1.154.092.177.062.313.16.406.295a.84.84 0 0 1 .14.492c0 .228-.06.41-.181.547a.806.806 0 0 1-.473.257c-.126.026-.423.04-.892.04zM14.88 25v-1.235h-1.234V25h1.234zm-5.018 9.11c.691 0 1.262-.17 1.711-.512.45-.341.772-.864.965-1.567l-1.261-.4c-.109.472-.287.818-.536 1.037-.25.22-.547.33-.892.33-.47 0-.85-.173-1.143-.519-.293-.345-.44-.925-.44-1.74 0-.767.15-1.322.447-1.665.297-.343.684-.514 1.162-.514.346 0 .64.096.881.29.242.193.4.457.477.79l1.288-.307c-.147-.516-.367-.911-.66-1.187-.492-.465-1.132-.698-1.92-.698-.902 0-1.63.296-2.184.89-.554.593-.83 1.426-.83 2.498 0 1.014.275 1.813.825 2.397.551.585 1.254.877 2.11.877zM14.88 34v-1.235h-1.234V34h1.234z"/></svg>';
class nj extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListPropertiesUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  init() {
    const t = this.editor, e = t.locale.t, i = t.config.get("list.properties"), s = fC(i), r = s.styles.listTypes;
    if (r.includes("bulleted")) {
      const o = [
        {
          label: e("Toggle the disc list style"),
          tooltip: e("Disc"),
          type: "disc",
          icon: KH
        },
        {
          label: e("Toggle the circle list style"),
          tooltip: e("Circle"),
          type: "circle",
          icon: ZH
        },
        {
          label: e("Toggle the square list style"),
          tooltip: e("Square"),
          type: "square",
          icon: YH
        }
      ], a = e("Bulleted List"), l = e("Bulleted list styles toolbar"), c = "bulletedList";
      t.ui.componentFactory.add(c, Q1({
        editor: t,
        normalizedConfig: s,
        parentCommandName: c,
        buttonLabel: a,
        buttonIcon: Z.bulletedList,
        styleGridAriaLabel: l,
        styleDefinitions: o
      })), t.ui.componentFactory.add(`menuBar:${c}`, ew({
        editor: t,
        normalizedConfig: s,
        parentCommandName: c,
        buttonLabel: a,
        styleGridAriaLabel: l,
        styleDefinitions: o
      }));
    }
    if (r.includes("numbered") || i.startIndex || i.reversed) {
      const o = [
        {
          label: e("Toggle the decimal list style"),
          tooltip: e("Decimal"),
          type: "decimal",
          icon: JH
        },
        {
          label: e("Toggle the decimal with leading zero list style"),
          tooltip: e("Decimal with leading zero"),
          type: "decimal-leading-zero",
          icon: XH
        },
        {
          label: e("Toggle the lower–roman list style"),
          tooltip: e("Lower–roman"),
          type: "lower-roman",
          icon: QH
        },
        {
          label: e("Toggle the upper–roman list style"),
          tooltip: e("Upper-roman"),
          type: "upper-roman",
          icon: ej
        },
        {
          label: e("Toggle the lower–latin list style"),
          tooltip: e("Lower-latin"),
          type: "lower-latin",
          icon: tj
        },
        {
          label: e("Toggle the upper–latin list style"),
          tooltip: e("Upper-latin"),
          type: "upper-latin",
          icon: ij
        }
      ], a = e("Numbered List"), l = e("Numbered list styles toolbar"), c = "numberedList";
      t.ui.componentFactory.add(c, Q1({
        editor: t,
        normalizedConfig: s,
        parentCommandName: c,
        buttonLabel: a,
        buttonIcon: Z.numberedList,
        styleGridAriaLabel: l,
        styleDefinitions: o
      })), r.includes("numbered") && t.ui.componentFactory.add(`menuBar:${c}`, ew({
        editor: t,
        normalizedConfig: s,
        parentCommandName: c,
        buttonLabel: a,
        styleGridAriaLabel: l,
        styleDefinitions: o
      }));
    }
  }
}
function Q1({ editor: n, normalizedConfig: t, parentCommandName: e, buttonLabel: i, buttonIcon: s, styleGridAriaLabel: r, styleDefinitions: o }) {
  const a = n.commands.get(e);
  return (l) => {
    const c = Tt(l, Ys), u = c.buttonView;
    return c.bind("isEnabled").to(a), c.class = "ck-list-styles-dropdown", u.on("execute", () => {
      n.execute(e), n.editing.view.focus();
    }), u.set({
      label: i,
      icon: s,
      tooltip: !0,
      isToggleable: !0
    }), u.bind("isOn").to(a, "value", (d) => !!d), c.once("change:isOpen", () => {
      const d = sj({
        editor: n,
        normalizedConfig: t,
        dropdownView: c,
        parentCommandName: e,
        styleGridAriaLabel: r,
        styleDefinitions: o
      });
      c.panelView.children.add(d);
    }), c.on("execute", () => {
      n.editing.view.focus();
    }), c;
  };
}
function gC({ editor: n, listStyleCommand: t, parentCommandName: e }) {
  const i = n.locale, s = n.commands.get(e);
  return ({ label: r, type: o, icon: a, tooltip: l }) => {
    const c = new le(i);
    return c.set({
      label: r,
      icon: a,
      tooltip: l
    }), c.bind("isOn").to(t, "value", (u) => u === o), c.on("execute", () => {
      s.value ? t.value === o ? n.execute(e) : t.value !== o && n.execute("listStyle", {
        type: o
      }) : n.model.change(() => {
        n.execute("listStyle", {
          type: o
        });
      });
    }), c;
  };
}
function sj({ editor: n, normalizedConfig: t, dropdownView: e, parentCommandName: i, styleDefinitions: s, styleGridAriaLabel: r }) {
  const o = n.locale, a = {
    ...t,
    ...i != "numberedList" ? {
      startIndex: !1,
      reversed: !1
    } : null
  }, l = i.replace("List", "");
  let c = null;
  if (t.styles.listTypes.includes(l)) {
    const d = n.commands.get("listStyle"), h = gC({
      editor: n,
      parentCommandName: i,
      listStyleCommand: d
    }), m = t.styles.listStyleTypes;
    let g = s;
    if (m) {
      const w = m[l];
      w && (g = s.filter((y) => w.includes(y.type)));
    }
    const p = pC(d);
    c = g.filter(p).map(h);
  }
  const u = new mC(o, {
    styleGridAriaLabel: r,
    enabledProperties: a,
    styleButtonViews: c
  });
  if (t.styles.listTypes.includes(l) && eu(e, () => u.stylesView.children.find((d) => d.isOn)), a.startIndex) {
    const d = n.commands.get("listStart");
    u.startIndexFieldView.bind("isEnabled").to(d), u.startIndexFieldView.fieldView.bind("value").to(d), u.on("listStart", (h, m) => n.execute("listStart", m));
  }
  if (a.reversed) {
    const d = n.commands.get("listReversed");
    u.reversedSwitchButtonView.bind("isEnabled").to(d), u.reversedSwitchButtonView.bind("isOn").to(d, "value", (h) => !!h), u.on("listReversed", () => {
      const h = d.value;
      n.execute("listReversed", {
        reversed: !h
      });
    });
  }
  return u.delegate("execute").to(e), u;
}
function ew({ editor: n, normalizedConfig: t, parentCommandName: e, buttonLabel: i, styleGridAriaLabel: s, styleDefinitions: r }) {
  return (o) => {
    const a = new Yt(o), l = n.commands.get(e), c = n.commands.get("listStyle"), u = pC(c), d = gC({
      editor: n,
      parentCommandName: e,
      listStyleCommand: c
    }), h = t.styles.listStyleTypes;
    let m = r;
    if (h) {
      const w = l.type, y = h[w];
      y && (m = r.filter((k) => y.includes(k.type)));
    }
    const g = m.filter(u).map(d), p = new mC(o, {
      styleGridAriaLabel: s,
      enabledProperties: {
        ...t,
        // Disable list start index and reversed in the menu bar.
        startIndex: !1,
        reversed: !1
      },
      styleButtonViews: g
    });
    return p.delegate("execute").to(a), a.buttonView.set({
      label: i,
      icon: Z[e]
    }), a.panelView.children.add(p), a.bind("isEnabled").to(l, "isEnabled"), a.on("execute", () => {
      n.editing.view.focus();
    }), a;
  };
}
function pC(n) {
  return typeof n.isStyleTypeSupported == "function" ? (t) => n.isStyleTypeSupported(t.type) : () => !0;
}
class rj extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      qH,
      nj
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListProperties";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
class oj extends he {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.on("execute", () => {
      this.refresh();
    }, {
      priority: "highest"
    });
  }
  /**
  * Updates the command's {@link #value} and {@link #isEnabled} properties based on the current selection.
  */
  refresh() {
    const t = this._getSelectedItems();
    this.value = this._getValue(t), this.isEnabled = !!t.length;
  }
  /**
  * Executes the command.
  *
  * @param options.forceValue If set, it will force the command behavior. If `true`, the command will apply
  * the attribute. Otherwise, the command will remove the attribute. If not set, the command will look for its current
  * value to decide what it should do.
  */
  execute(t = {}) {
    this.editor.model.change((e) => {
      const i = this._getSelectedItems(), s = t.forceValue === void 0 ? !this._getValue(i) : t.forceValue;
      for (const r of i)
        s ? e.setAttribute("todoListChecked", !0, r) : e.removeAttribute("todoListChecked", r);
    });
  }
  /**
  * Returns a value for the command.
  */
  _getValue(t) {
    return t.every((e) => e.getAttribute("todoListChecked"));
  }
  /**
  * Gets all to-do list items selected by the {@link module:engine/model/selection~Selection}.
  */
  _getSelectedItems() {
    const t = this.editor.model, e = t.schema, i = t.document.selection.getFirstRange(), s = i.start.parent, r = [];
    e.checkAttribute(s, "todoListChecked") && r.push(...Kr(s));
    for (const o of i.getItems({
      shallow: !0
    }))
      e.checkAttribute(o, "todoListChecked") && !r.includes(o) && r.push(...Kr(o));
    return r;
  }
}
class aj extends xn {
  constructor() {
    super(...arguments);
    /**
    * @inheritDoc
    */
    f(this, "domEventType", [
      "change"
    ]);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    if (e.target) {
      const i = this.view.domConverter.mapDomToView(e.target);
      i && i.is("element", "input") && i.getAttribute("type") == "checkbox" && i.findAncestor({
        classes: "todo-list__label"
      }) && this.fire("todoCheckboxChange", e);
    }
  }
}
const lj = /* @__PURE__ */ Uc("Ctrl+Enter");
class cj extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TodoListEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      _a
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model, i = t.editing, s = t.plugins.get(_a), o = t.config.get("list.multiBlock") ? "paragraph" : "listItem";
    t.commands.add("todoList", new Bo(t, "todo")), t.commands.add("checkTodoList", new oj(t)), i.view.addObserver(aj), e.schema.extend("$listItem", {
      allowAttributes: "todoListChecked"
    }), e.schema.addAttributeCheck((a) => {
      const l = a.last;
      if (!l.getAttribute("listItemId") || l.getAttribute("listType") != "todo")
        return !1;
    }, "todoListChecked"), t.conversion.for("upcast").add((a) => {
      a.on("element:input", dj()), a.on("element:li", uj(), {
        priority: "low"
      }), a.on("element:label", Td({
        name: "label",
        classes: "todo-list__label"
      })), a.on("element:label", Td({
        name: "label",
        classes: [
          "todo-list__label",
          "todo-list__label_without-description"
        ]
      })), a.on("element:span", Td({
        name: "span",
        classes: "todo-list__label__description"
      })), a.on("element:ul", hj({
        name: "ul",
        classes: "todo-list"
      }));
    }), t.conversion.for("downcast").elementToElement({
      model: o,
      view: (a, { writer: l }) => {
        if (vl(a, s.getListAttributeNames()))
          return l.createContainerElement("span", {
            class: "todo-list__label__description"
          });
      },
      converterPriority: "highest"
    }), s.registerDowncastStrategy({
      scope: "list",
      attributeName: "listType",
      setAttributeOnDowncast(a, l, c) {
        l == "todo" ? a.addClass("todo-list", c) : a.removeClass("todo-list", c);
      }
    }), s.registerDowncastStrategy({
      scope: "itemMarker",
      attributeName: "todoListChecked",
      createElement(a, l, { dataPipeline: c }) {
        if (l.getAttribute("listType") != "todo")
          return null;
        const u = a.createUIElement("input", {
          type: "checkbox",
          ...l.getAttribute("todoListChecked") ? {
            checked: "checked"
          } : null,
          ...c ? {
            disabled: "disabled"
          } : {
            tabindex: "-1"
          }
        });
        if (c)
          return u;
        const d = a.createContainerElement("span", {
          contenteditable: "false"
        }, u);
        return d.getFillerOffset = () => null, d;
      },
      canWrapElement(a) {
        return vl(a, s.getListAttributeNames());
      },
      createWrapperElement(a, l, { dataPipeline: c }) {
        const u = [
          "todo-list__label"
        ];
        return vl(l, s.getListAttributeNames()) || u.push("todo-list__label_without-description"), a.createAttributeElement(c ? "label" : "span", {
          class: u.join(" ")
        });
      }
    }), s.on("checkElement", (a, { modelElement: l, viewElement: c }) => {
      const u = vl(l, s.getListAttributeNames());
      c.hasClass("todo-list__label__description") != u && (a.return = !0, a.stop());
    }), s.on("checkElement", (a, { modelElement: l, viewElement: c }) => {
      const u = l.getAttribute("listType") == "todo" && qn(l);
      let d = !1;
      const h = t.editing.view.createPositionBefore(c).getWalker({
        direction: "backward"
      });
      for (const { item: m } of h) {
        if (m.is("element") && t.editing.mapper.toModelElement(m))
          break;
        m.is("element", "input") && m.getAttribute("type") == "checkbox" && (d = !0);
      }
      d != u && (a.return = !0, a.stop());
    }), s.on("postFixer", (a, { listNodes: l, writer: c }) => {
      for (const { node: u, previousNodeInList: d } of l) {
        if (!d || d.getAttribute("listItemId") != u.getAttribute("listItemId"))
          continue;
        const h = d.hasAttribute("todoListChecked"), m = u.hasAttribute("todoListChecked");
        m && !h ? (c.removeAttribute("todoListChecked", u), a.return = !0) : !m && h && (c.setAttribute("todoListChecked", !0, u), a.return = !0);
      }
    }), e.document.registerPostFixer((a) => {
      const l = e.document.differ.getChanges();
      let c = !1;
      for (const u of l)
        if (u.type == "attribute" && u.attributeKey == "listType") {
          const d = u.range.start.nodeAfter;
          u.attributeOldValue == "todo" && d.hasAttribute("todoListChecked") && (a.removeAttribute("todoListChecked", d), c = !0);
        } else if (u.type == "insert" && u.name != "$text")
          for (const { item: d } of a.createRangeOn(u.position.nodeAfter))
            d.is("element") && d.getAttribute("listType") != "todo" && d.hasAttribute("todoListChecked") && (a.removeAttribute("todoListChecked", d), c = !0);
      return c;
    }), this.listenTo(i.view.document, "keydown", (a, l) => {
      zs(l) === lj && (t.execute("checkTodoList"), a.stop());
    }, {
      priority: "high"
    }), this.listenTo(i.view.document, "todoCheckboxChange", (a, l) => {
      const c = l.target;
      if (!c || !c.is("element", "input"))
        return;
      const u = i.view.createPositionAfter(c), h = i.mapper.toModelPosition(u).parent;
      h && it(h) && h.getAttribute("listType") == "todo" && this._handleCheckmarkChange(h);
    }), this.listenTo(i.view.document, "arrowKey", mj(e, t.locale), {
      context: "$text"
    }), this.listenTo(i.mapper, "viewToModelPosition", (a, l) => {
      const c = l.viewPosition.parent, u = c.is("attributeElement", "li") && l.viewPosition.offset == 0, d = tw(c) && l.viewPosition.offset <= 1, h = c.is("element", "span") && c.getAttribute("contenteditable") == "false" && tw(c.parent);
      if (!u && !d && !h)
        return;
      const m = l.modelPosition.nodeAfter;
      m && m.getAttribute("listType") == "todo" && (l.modelPosition = e.createPositionAt(m, 0));
    }, {
      priority: "low"
    }), this._initAriaAnnouncements();
  }
  /**
  * Handles the checkbox element change, moves the selection to the corresponding model item to make it possible
  * to toggle the `todoListChecked` attribute using the command, and restores the selection position.
  *
  * Some say it's a hack :) Moving the selection only for executing the command on a certain node and restoring it after,
  * is not a clear solution. We need to design an API for using commands beyond the selection range.
  * See https://github.com/ckeditor/ckeditor5/issues/1954.
  */
  _handleCheckmarkChange(t) {
    const e = this.editor, i = e.model, s = Array.from(i.document.selection.getRanges());
    i.change((r) => {
      r.setSelection(t, "end"), e.execute("checkTodoList"), r.setSelection(s);
    });
  }
  /**
  * Observe when user enters or leaves todo list and set proper aria value in global live announcer.
  * This allows screen readers to indicate when the user has entered and left the specified todo list.
  *
  * @internal
  */
  _initAriaAnnouncements() {
    const { model: t, ui: e, t: i } = this.editor;
    let s = null;
    e && t.document.selection.on("change:range", () => {
      const r = t.document.selection.focus.parent, o = iw(s), a = iw(r);
      o && !a ? e.ariaLiveAnnouncer.announce(i("Leaving a to-do list")) : !o && a && e.ariaLiveAnnouncer.announce(i("Entering a to-do list")), s = r;
    });
  }
}
function uj() {
  return (n, t, e) => {
    const { writer: i, schema: s } = e;
    if (!t.modelRange)
      return;
    const r = Array.from(t.modelRange.getItems({
      shallow: !0
    })).filter((o) => o.getAttribute("listType") === "todo" && s.checkAttribute(o, "listItemId")).reduce((o, a) => {
      const l = a.getAttribute("listItemId");
      return o.has(l) || o.set(l, Kr(a)), o;
    }, /* @__PURE__ */ new Map());
    for (const [, o] of r.entries())
      if (o.some((a) => a.getAttribute("todoListChecked")))
        for (const a of o)
          i.setAttribute("todoListChecked", !0, a);
  };
}
function dj() {
  return (n, t, e) => {
    const i = t.modelCursor, s = i.parent, r = t.viewItem;
    if (!e.consumable.test(r, {
      name: !0
    }) || r.getAttribute("type") != "checkbox" || !i.isAtStart || !s.hasAttribute("listType"))
      return;
    e.consumable.consume(r, {
      name: !0
    });
    const o = e.writer;
    o.setAttribute("listType", "todo", s), t.viewItem.hasAttribute("checked") && o.setAttribute("todoListChecked", !0, s), t.modelRange = o.createRange(i);
  };
}
function Td(n) {
  const t = new oi(n);
  return (e, i, s) => {
    const r = t.match(i.viewItem);
    r && s.consumable.consume(i.viewItem, r.match) && Object.assign(i, s.convertChildren(i.viewItem, i.modelCursor));
  };
}
function hj(n) {
  const t = new oi(n);
  return (e, i, s) => {
    const r = t.match(i.viewItem);
    if (!r)
      return;
    const o = r.match;
    o.name = !1, s.consumable.consume(i.viewItem, o);
  };
}
function vl(n, t) {
  return (n.is("element", "paragraph") || n.is("element", "listItem")) && n.getAttribute("listType") == "todo" && qn(n) && fj(n, t);
}
function fj(n, t) {
  for (const e of n.getAttributeKeys())
    if (!e.startsWith("selection:") && !t.includes(e))
      return !1;
  return !0;
}
function mj(n, t) {
  return (e, i) => {
    const s = Wc(i.keyCode, t.contentLanguageDirection), r = n.schema, o = n.document.selection;
    if (!o.isCollapsed)
      return;
    const a = o.getFirstPosition(), l = a.parent;
    if (s == "right" && a.isAtEnd) {
      const c = r.getNearestSelectionRange(n.createPositionAfter(l), "forward");
      if (!c)
        return;
      const u = c.start.parent;
      u && it(u) && u.getAttribute("listType") == "todo" && (n.change((d) => d.setSelection(c)), i.preventDefault(), i.stopPropagation(), e.stop());
    } else if (s == "left" && a.isAtStart && it(l) && l.getAttribute("listType") == "todo") {
      const c = r.getNearestSelectionRange(n.createPositionBefore(l), "backward");
      if (!c)
        return;
      n.change((u) => u.setSelection(c)), i.preventDefault(), i.stopPropagation(), e.stop();
    }
  };
}
function tw(n) {
  return !!n && n.is("attributeElement") && n.hasClass("todo-list__label");
}
function iw(n) {
  return !n || !n.is("element", "paragraph") && !n.is("element", "listItem") ? !1 : n.getAttribute("listType") == "todo";
}
class gj extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TodoListUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor.t;
    Oh(this.editor, "todoList", t("To-do List"), Z.todoList);
  }
}
class pj extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      cj,
      gj
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TodoList";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
function bC() {
  return {
    baseUrl: null,
    breaks: !1,
    extensions: null,
    gfm: !0,
    headerIds: !0,
    headerPrefix: "",
    highlight: null,
    langPrefix: "language-",
    mangle: !0,
    pedantic: !1,
    renderer: null,
    sanitize: !1,
    sanitizer: null,
    silent: !1,
    smartLists: !1,
    smartypants: !1,
    tokenizer: null,
    walkTokens: null,
    xhtml: !1
  };
}
let ro = bC();
function bj(n) {
  ro = n;
}
const wj = /[&<>"']/, vj = /[&<>"']/g, _j = /[<>"']|&(?!#?\w+;)/, yj = /[<>"']|&(?!#?\w+;)/g, kj = {
  "&": "&amp;",
  "<": "&lt;",
  ">": "&gt;",
  '"': "&quot;",
  "'": "&#39;"
}, nw = (n) => kj[n];
function Nt(n, t) {
  if (t) {
    if (wj.test(n))
      return n.replace(vj, nw);
  } else if (_j.test(n))
    return n.replace(yj, nw);
  return n;
}
const Cj = /&(#(?:\d+)|(?:#x[0-9A-Fa-f]+)|(?:\w+));?/ig;
function wC(n) {
  return n.replace(Cj, (t, e) => (e = e.toLowerCase(), e === "colon" ? ":" : e.charAt(0) === "#" ? e.charAt(1) === "x" ? String.fromCharCode(parseInt(e.substring(2), 16)) : String.fromCharCode(+e.substring(1)) : ""));
}
const xj = /(^|[^\[])\^/g;
function ot(n, t) {
  n = n.source || n, t = t || "";
  const e = {
    replace: (i, s) => (s = s.source || s, s = s.replace(xj, "$1"), n = n.replace(i, s), e),
    getRegex: () => new RegExp(n, t)
  };
  return e;
}
const Tj = /[^\w:]/g, Ej = /^$|^[a-z][a-z0-9+.-]*:|^[?#]/i;
function sw(n, t, e) {
  if (n) {
    let i;
    try {
      i = decodeURIComponent(wC(e)).replace(Tj, "").toLowerCase();
    } catch {
      return null;
    }
    if (i.indexOf("javascript:") === 0 || i.indexOf("vbscript:") === 0 || i.indexOf("data:") === 0)
      return null;
  }
  t && !Ej.test(e) && (e = Pj(t, e));
  try {
    e = encodeURI(e).replace(/%25/g, "%");
  } catch {
    return null;
  }
  return e;
}
const _l = {}, Sj = /^[^:]+:\/*[^/]*$/, Aj = /^([^:]+:)[\s\S]*$/, Ij = /^([^:]+:\/*[^/]*)[\s\S]*$/;
function Pj(n, t) {
  _l[" " + n] || (Sj.test(n) ? _l[" " + n] = n + "/" : _l[" " + n] = Fl(n, "/", !0)), n = _l[" " + n];
  const e = n.indexOf(":") === -1;
  return t.substring(0, 2) === "//" ? e ? t : n.replace(Aj, "$1") + t : t.charAt(0) === "/" ? e ? t : n.replace(Ij, "$1") + t : n + t;
}
const mc = { exec: function() {
} };
function rn(n) {
  let t = 1, e, i;
  for (; t < arguments.length; t++) {
    e = arguments[t];
    for (i in e)
      Object.prototype.hasOwnProperty.call(e, i) && (n[i] = e[i]);
  }
  return n;
}
function rw(n, t) {
  const e = n.replace(/\|/g, (r, o, a) => {
    let l = !1, c = o;
    for (; --c >= 0 && a[c] === "\\"; ) l = !l;
    return l ? "|" : " |";
  }), i = e.split(/ \|/);
  let s = 0;
  if (i[0].trim() || i.shift(), i.length > 0 && !i[i.length - 1].trim() && i.pop(), i.length > t)
    i.splice(t);
  else
    for (; i.length < t; ) i.push("");
  for (; s < i.length; s++)
    i[s] = i[s].trim().replace(/\\\|/g, "|");
  return i;
}
function Fl(n, t, e) {
  const i = n.length;
  if (i === 0)
    return "";
  let s = 0;
  for (; s < i; ) {
    const r = n.charAt(i - s - 1);
    if (r === t && !e)
      s++;
    else if (r !== t && e)
      s++;
    else
      break;
  }
  return n.substr(0, i - s);
}
function Rj(n, t) {
  if (n.indexOf(t[1]) === -1)
    return -1;
  const e = n.length;
  let i = 0, s = 0;
  for (; s < e; s++)
    if (n[s] === "\\")
      s++;
    else if (n[s] === t[0])
      i++;
    else if (n[s] === t[1] && (i--, i < 0))
      return s;
  return -1;
}
function vC(n) {
  n && n.sanitize && !n.silent && console.warn("marked(): sanitize and sanitizer parameters are deprecated since version 0.7.0, should not be used and will be removed in the future. Read more here: https://marked.js.org/#/USING_ADVANCED.md#options");
}
function ow(n, t) {
  if (t < 1)
    return "";
  let e = "";
  for (; t > 1; )
    t & 1 && (e += n), t >>= 1, n += n;
  return e + n;
}
function aw(n, t, e, i) {
  const s = t.href, r = t.title ? Nt(t.title) : null, o = n[1].replace(/\\([\[\]])/g, "$1");
  if (n[0].charAt(0) !== "!") {
    i.state.inLink = !0;
    const a = {
      type: "link",
      raw: e,
      href: s,
      title: r,
      text: o,
      tokens: i.inlineTokens(o, [])
    };
    return i.state.inLink = !1, a;
  } else
    return {
      type: "image",
      raw: e,
      href: s,
      title: r,
      text: Nt(o)
    };
}
function Oj(n, t) {
  const e = n.match(/^(\s+)(?:```)/);
  if (e === null)
    return t;
  const i = e[1];
  return t.split(`
`).map((s) => {
    const r = s.match(/^\s+/);
    if (r === null)
      return s;
    const [o] = r;
    return o.length >= i.length ? s.slice(i.length) : s;
  }).join(`
`);
}
class dm {
  constructor(t) {
    this.options = t || ro;
  }
  space(t) {
    const e = this.rules.block.newline.exec(t);
    if (e && e[0].length > 0)
      return {
        type: "space",
        raw: e[0]
      };
  }
  code(t) {
    const e = this.rules.block.code.exec(t);
    if (e) {
      const i = e[0].replace(/^ {1,4}/gm, "");
      return {
        type: "code",
        raw: e[0],
        codeBlockStyle: "indented",
        text: this.options.pedantic ? i : Fl(i, `
`)
      };
    }
  }
  fences(t) {
    const e = this.rules.block.fences.exec(t);
    if (e) {
      const i = e[0], s = Oj(i, e[3] || "");
      return {
        type: "code",
        raw: i,
        lang: e[2] ? e[2].trim() : e[2],
        text: s
      };
    }
  }
  heading(t) {
    const e = this.rules.block.heading.exec(t);
    if (e) {
      let i = e[2].trim();
      if (/#$/.test(i)) {
        const r = Fl(i, "#");
        (this.options.pedantic || !r || / $/.test(r)) && (i = r.trim());
      }
      const s = {
        type: "heading",
        raw: e[0],
        depth: e[1].length,
        text: i,
        tokens: []
      };
      return this.lexer.inline(s.text, s.tokens), s;
    }
  }
  hr(t) {
    const e = this.rules.block.hr.exec(t);
    if (e)
      return {
        type: "hr",
        raw: e[0]
      };
  }
  blockquote(t) {
    const e = this.rules.block.blockquote.exec(t);
    if (e) {
      const i = e[0].replace(/^ *> ?/gm, "");
      return {
        type: "blockquote",
        raw: e[0],
        tokens: this.lexer.blockTokens(i, []),
        text: i
      };
    }
  }
  list(t) {
    let e = this.rules.block.list.exec(t);
    if (e) {
      let i, s, r, o, a, l, c, u, d, h, m, g, p = e[1].trim();
      const w = p.length > 1, y = {
        type: "list",
        raw: "",
        ordered: w,
        start: w ? +p.slice(0, -1) : "",
        loose: !1,
        items: []
      };
      p = w ? `\\d{1,9}\\${p.slice(-1)}` : `\\${p}`, this.options.pedantic && (p = w ? p : "[*+-]");
      const k = new RegExp(`^( {0,3}${p})((?: [^\\n]*)?(?:\\n|$))`);
      for (; t && (g = !1, !(!(e = k.exec(t)) || this.rules.block.hr.test(t))); ) {
        if (i = e[0], t = t.substring(i.length), u = e[2].split(`
`, 1)[0], d = t.split(`
`, 1)[0], this.options.pedantic ? (o = 2, m = u.trimLeft()) : (o = e[2].search(/[^ ]/), o = o > 4 ? 1 : o, m = u.slice(o), o += e[1].length), l = !1, !u && /^ *$/.test(d) && (i += d + `
`, t = t.substring(d.length + 1), g = !0), !g) {
          const E = new RegExp(`^ {0,${Math.min(3, o - 1)}}(?:[*+-]|\\d{1,9}[.)])`);
          for (; t && (h = t.split(`
`, 1)[0], u = h, this.options.pedantic && (u = u.replace(/^ {1,4}(?=( {4})*[^ ])/g, "  ")), !E.test(u)); ) {
            if (u.search(/[^ ]/) >= o || !u.trim())
              m += `
` + u.slice(o);
            else if (!l)
              m += `
` + u;
            else
              break;
            !l && !u.trim() && (l = !0), i += h + `
`, t = t.substring(h.length + 1);
          }
        }
        y.loose || (c ? y.loose = !0 : /\n *\n *$/.test(i) && (c = !0)), this.options.gfm && (s = /^\[[ xX]\] /.exec(m), s && (r = s[0] !== "[ ] ", m = m.replace(/^\[[ xX]\] +/, ""))), y.items.push({
          type: "list_item",
          raw: i,
          task: !!s,
          checked: r,
          loose: !1,
          text: m
        }), y.raw += i;
      }
      y.items[y.items.length - 1].raw = i.trimRight(), y.items[y.items.length - 1].text = m.trimRight(), y.raw = y.raw.trimRight();
      const C = y.items.length;
      for (a = 0; a < C; a++) {
        this.lexer.state.top = !1, y.items[a].tokens = this.lexer.blockTokens(y.items[a].text, []);
        const E = y.items[a].tokens.filter((L) => L.type === "space"), T = E.every((L) => {
          const O = L.raw.split("");
          let N = 0;
          for (const M of O)
            if (M === `
` && (N += 1), N > 1)
              return !0;
          return !1;
        });
        !y.loose && E.length && T && (y.loose = !0, y.items[a].loose = !0);
      }
      return y;
    }
  }
  html(t) {
    const e = this.rules.block.html.exec(t);
    if (e) {
      const i = {
        type: "html",
        raw: e[0],
        pre: !this.options.sanitizer && (e[1] === "pre" || e[1] === "script" || e[1] === "style"),
        text: e[0]
      };
      return this.options.sanitize && (i.type = "paragraph", i.text = this.options.sanitizer ? this.options.sanitizer(e[0]) : Nt(e[0]), i.tokens = [], this.lexer.inline(i.text, i.tokens)), i;
    }
  }
  def(t) {
    const e = this.rules.block.def.exec(t);
    if (e)
      return e[3] && (e[3] = e[3].substring(1, e[3].length - 1)), {
        type: "def",
        tag: e[1].toLowerCase().replace(/\s+/g, " "),
        raw: e[0],
        href: e[2],
        title: e[3]
      };
  }
  table(t) {
    const e = this.rules.block.table.exec(t);
    if (e) {
      const i = {
        type: "table",
        header: rw(e[1]).map((s) => ({ text: s })),
        align: e[2].replace(/^ *|\| *$/g, "").split(/ *\| */),
        rows: e[3] && e[3].trim() ? e[3].replace(/\n[ \t]*$/, "").split(`
`) : []
      };
      if (i.header.length === i.align.length) {
        i.raw = e[0];
        let s = i.align.length, r, o, a, l;
        for (r = 0; r < s; r++)
          /^ *-+: *$/.test(i.align[r]) ? i.align[r] = "right" : /^ *:-+: *$/.test(i.align[r]) ? i.align[r] = "center" : /^ *:-+ *$/.test(i.align[r]) ? i.align[r] = "left" : i.align[r] = null;
        for (s = i.rows.length, r = 0; r < s; r++)
          i.rows[r] = rw(i.rows[r], i.header.length).map((c) => ({ text: c }));
        for (s = i.header.length, o = 0; o < s; o++)
          i.header[o].tokens = [], this.lexer.inlineTokens(i.header[o].text, i.header[o].tokens);
        for (s = i.rows.length, o = 0; o < s; o++)
          for (l = i.rows[o], a = 0; a < l.length; a++)
            l[a].tokens = [], this.lexer.inlineTokens(l[a].text, l[a].tokens);
        return i;
      }
    }
  }
  lheading(t) {
    const e = this.rules.block.lheading.exec(t);
    if (e) {
      const i = {
        type: "heading",
        raw: e[0],
        depth: e[2].charAt(0) === "=" ? 1 : 2,
        text: e[1],
        tokens: []
      };
      return this.lexer.inline(i.text, i.tokens), i;
    }
  }
  paragraph(t) {
    const e = this.rules.block.paragraph.exec(t);
    if (e) {
      const i = {
        type: "paragraph",
        raw: e[0],
        text: e[1].charAt(e[1].length - 1) === `
` ? e[1].slice(0, -1) : e[1],
        tokens: []
      };
      return this.lexer.inline(i.text, i.tokens), i;
    }
  }
  text(t) {
    const e = this.rules.block.text.exec(t);
    if (e) {
      const i = {
        type: "text",
        raw: e[0],
        text: e[0],
        tokens: []
      };
      return this.lexer.inline(i.text, i.tokens), i;
    }
  }
  escape(t) {
    const e = this.rules.inline.escape.exec(t);
    if (e)
      return {
        type: "escape",
        raw: e[0],
        text: Nt(e[1])
      };
  }
  tag(t) {
    const e = this.rules.inline.tag.exec(t);
    if (e)
      return !this.lexer.state.inLink && /^<a /i.test(e[0]) ? this.lexer.state.inLink = !0 : this.lexer.state.inLink && /^<\/a>/i.test(e[0]) && (this.lexer.state.inLink = !1), !this.lexer.state.inRawBlock && /^<(pre|code|kbd|script)(\s|>)/i.test(e[0]) ? this.lexer.state.inRawBlock = !0 : this.lexer.state.inRawBlock && /^<\/(pre|code|kbd|script)(\s|>)/i.test(e[0]) && (this.lexer.state.inRawBlock = !1), {
        type: this.options.sanitize ? "text" : "html",
        raw: e[0],
        inLink: this.lexer.state.inLink,
        inRawBlock: this.lexer.state.inRawBlock,
        text: this.options.sanitize ? this.options.sanitizer ? this.options.sanitizer(e[0]) : Nt(e[0]) : e[0]
      };
  }
  link(t) {
    const e = this.rules.inline.link.exec(t);
    if (e) {
      const i = e[2].trim();
      if (!this.options.pedantic && /^</.test(i)) {
        if (!/>$/.test(i))
          return;
        const o = Fl(i.slice(0, -1), "\\");
        if ((i.length - o.length) % 2 === 0)
          return;
      } else {
        const o = Rj(e[2], "()");
        if (o > -1) {
          const l = (e[0].indexOf("!") === 0 ? 5 : 4) + e[1].length + o;
          e[2] = e[2].substring(0, o), e[0] = e[0].substring(0, l).trim(), e[3] = "";
        }
      }
      let s = e[2], r = "";
      if (this.options.pedantic) {
        const o = /^([^'"]*[^\s])\s+(['"])(.*)\2/.exec(s);
        o && (s = o[1], r = o[3]);
      } else
        r = e[3] ? e[3].slice(1, -1) : "";
      return s = s.trim(), /^</.test(s) && (this.options.pedantic && !/>$/.test(i) ? s = s.slice(1) : s = s.slice(1, -1)), aw(e, {
        href: s && s.replace(this.rules.inline._escapes, "$1"),
        title: r && r.replace(this.rules.inline._escapes, "$1")
      }, e[0], this.lexer);
    }
  }
  reflink(t, e) {
    let i;
    if ((i = this.rules.inline.reflink.exec(t)) || (i = this.rules.inline.nolink.exec(t))) {
      let s = (i[2] || i[1]).replace(/\s+/g, " ");
      if (s = e[s.toLowerCase()], !s || !s.href) {
        const r = i[0].charAt(0);
        return {
          type: "text",
          raw: r,
          text: r
        };
      }
      return aw(i, s, i[0], this.lexer);
    }
  }
  emStrong(t, e, i = "") {
    let s = this.rules.inline.emStrong.lDelim.exec(t);
    if (!s || s[3] && i.match(/[\p{L}\p{N}]/u)) return;
    const r = s[1] || s[2] || "";
    if (!r || r && (i === "" || this.rules.inline.punctuation.exec(i))) {
      const o = s[0].length - 1;
      let a, l, c = o, u = 0;
      const d = s[0][0] === "*" ? this.rules.inline.emStrong.rDelimAst : this.rules.inline.emStrong.rDelimUnd;
      for (d.lastIndex = 0, e = e.slice(-1 * t.length + o); (s = d.exec(e)) != null; ) {
        if (a = s[1] || s[2] || s[3] || s[4] || s[5] || s[6], !a) continue;
        if (l = a.length, s[3] || s[4]) {
          c += l;
          continue;
        } else if ((s[5] || s[6]) && o % 3 && !((o + l) % 3)) {
          u += l;
          continue;
        }
        if (c -= l, c > 0) continue;
        if (l = Math.min(l, l + c + u), Math.min(o, l) % 2) {
          const m = t.slice(1, o + s.index + l);
          return {
            type: "em",
            raw: t.slice(0, o + s.index + l + 1),
            text: m,
            tokens: this.lexer.inlineTokens(m, [])
          };
        }
        const h = t.slice(2, o + s.index + l - 1);
        return {
          type: "strong",
          raw: t.slice(0, o + s.index + l + 1),
          text: h,
          tokens: this.lexer.inlineTokens(h, [])
        };
      }
    }
  }
  codespan(t) {
    const e = this.rules.inline.code.exec(t);
    if (e) {
      let i = e[2].replace(/\n/g, " ");
      const s = /[^ ]/.test(i), r = /^ /.test(i) && / $/.test(i);
      return s && r && (i = i.substring(1, i.length - 1)), i = Nt(i, !0), {
        type: "codespan",
        raw: e[0],
        text: i
      };
    }
  }
  br(t) {
    const e = this.rules.inline.br.exec(t);
    if (e)
      return {
        type: "br",
        raw: e[0]
      };
  }
  del(t) {
    const e = this.rules.inline.del.exec(t);
    if (e)
      return {
        type: "del",
        raw: e[0],
        text: e[2],
        tokens: this.lexer.inlineTokens(e[2], [])
      };
  }
  autolink(t, e) {
    const i = this.rules.inline.autolink.exec(t);
    if (i) {
      let s, r;
      return i[2] === "@" ? (s = Nt(this.options.mangle ? e(i[1]) : i[1]), r = "mailto:" + s) : (s = Nt(i[1]), r = s), {
        type: "link",
        raw: i[0],
        text: s,
        href: r,
        tokens: [
          {
            type: "text",
            raw: s,
            text: s
          }
        ]
      };
    }
  }
  url(t, e) {
    let i;
    if (i = this.rules.inline.url.exec(t)) {
      let s, r;
      if (i[2] === "@")
        s = Nt(this.options.mangle ? e(i[0]) : i[0]), r = "mailto:" + s;
      else {
        let o;
        do
          o = i[0], i[0] = this.rules.inline._backpedal.exec(i[0])[0];
        while (o !== i[0]);
        s = Nt(i[0]), i[1] === "www." ? r = "http://" + s : r = s;
      }
      return {
        type: "link",
        raw: i[0],
        text: s,
        href: r,
        tokens: [
          {
            type: "text",
            raw: s,
            text: s
          }
        ]
      };
    }
  }
  inlineText(t, e) {
    const i = this.rules.inline.text.exec(t);
    if (i) {
      let s;
      return this.lexer.state.inRawBlock ? s = this.options.sanitize ? this.options.sanitizer ? this.options.sanitizer(i[0]) : Nt(i[0]) : i[0] : s = Nt(this.options.smartypants ? e(i[0]) : i[0]), {
        type: "text",
        raw: i[0],
        text: s
      };
    }
  }
}
const Ce = {
  newline: /^(?: *(?:\n|$))+/,
  code: /^( {4}[^\n]+(?:\n(?: *(?:\n|$))*)?)+/,
  fences: /^ {0,3}(`{3,}(?=[^`\n]*\n)|~{3,})([^\n]*)\n(?:|([\s\S]*?)\n)(?: {0,3}\1[~`]* *(?=\n|$)|$)/,
  hr: /^ {0,3}((?:- *){3,}|(?:_ *){3,}|(?:\* *){3,})(?:\n+|$)/,
  heading: /^ {0,3}(#{1,6})(?=\s|$)(.*)(?:\n+|$)/,
  blockquote: /^( {0,3}> ?(paragraph|[^\n]*)(?:\n|$))+/,
  list: /^( {0,3}bull)( [^\n]+?)?(?:\n|$)/,
  html: "^ {0,3}(?:<(script|pre|style|textarea)[\\s>][\\s\\S]*?(?:</\\1>[^\\n]*\\n+|$)|comment[^\\n]*(\\n+|$)|<\\?[\\s\\S]*?(?:\\?>\\n*|$)|<![A-Z][\\s\\S]*?(?:>\\n*|$)|<!\\[CDATA\\[[\\s\\S]*?(?:\\]\\]>\\n*|$)|</?(tag)(?: +|\\n|/?>)[\\s\\S]*?(?:(?:\\n *)+\\n|$)|<(?!script|pre|style|textarea)([a-z][\\w-]*)(?:attribute)*? */?>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$)|</(?!script|pre|style|textarea)[a-z][\\w-]*\\s*>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$))",
  def: /^ {0,3}\[(label)\]: *(?:\n *)?<?([^\s>]+)>?(?:(?: +(?:\n *)?| *\n *)(title))? *(?:\n+|$)/,
  table: mc,
  lheading: /^([^\n]+)\n {0,3}(=+|-+) *(?:\n+|$)/,
  // regex template, placeholders will be replaced according to different paragraph
  // interruption rules of commonmark and the original markdown spec:
  _paragraph: /^([^\n]+(?:\n(?!hr|heading|lheading|blockquote|fences|list|html|table| +\n)[^\n]+)*)/,
  text: /^[^\n]+/
};
Ce._label = /(?!\s*\])(?:\\.|[^\[\]\\])+/;
Ce._title = /(?:"(?:\\"?|[^"\\])*"|'[^'\n]*(?:\n[^'\n]+)*\n?'|\([^()]*\))/;
Ce.def = ot(Ce.def).replace("label", Ce._label).replace("title", Ce._title).getRegex();
Ce.bullet = /(?:[*+-]|\d{1,9}[.)])/;
Ce.listItemStart = ot(/^( *)(bull) */).replace("bull", Ce.bullet).getRegex();
Ce.list = ot(Ce.list).replace(/bull/g, Ce.bullet).replace("hr", "\\n+(?=\\1?(?:(?:- *){3,}|(?:_ *){3,}|(?:\\* *){3,})(?:\\n+|$))").replace("def", "\\n+(?=" + Ce.def.source + ")").getRegex();
Ce._tag = "address|article|aside|base|basefont|blockquote|body|caption|center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|footer|form|frame|frameset|h[1-6]|head|header|hr|html|iframe|legend|li|link|main|menu|menuitem|meta|nav|noframes|ol|optgroup|option|p|param|section|source|summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul";
Ce._comment = /<!--(?!-?>)[\s\S]*?(?:-->|$)/;
Ce.html = ot(Ce.html, "i").replace("comment", Ce._comment).replace("tag", Ce._tag).replace("attribute", / +[a-zA-Z:_][\w.:-]*(?: *= *"[^"\n]*"| *= *'[^'\n]*'| *= *[^\s"'=<>`]+)?/).getRegex();
Ce.paragraph = ot(Ce._paragraph).replace("hr", Ce.hr).replace("heading", " {0,3}#{1,6} ").replace("|lheading", "").replace("|table", "").replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", Ce._tag).getRegex();
Ce.blockquote = ot(Ce.blockquote).replace("paragraph", Ce.paragraph).getRegex();
Ce.normal = rn({}, Ce);
Ce.gfm = rn({}, Ce.normal, {
  table: "^ *([^\\n ].*\\|.*)\\n {0,3}(?:\\| *)?(:?-+:? *(?:\\| *:?-+:? *)*)(?:\\| *)?(?:\\n((?:(?! *\\n|hr|heading|blockquote|code|fences|list|html).*(?:\\n|$))*)\\n*|$)"
  // Cells
});
Ce.gfm.table = ot(Ce.gfm.table).replace("hr", Ce.hr).replace("heading", " {0,3}#{1,6} ").replace("blockquote", " {0,3}>").replace("code", " {4}[^\\n]").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", Ce._tag).getRegex();
Ce.gfm.paragraph = ot(Ce._paragraph).replace("hr", Ce.hr).replace("heading", " {0,3}#{1,6} ").replace("|lheading", "").replace("table", Ce.gfm.table).replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", Ce._tag).getRegex();
Ce.pedantic = rn({}, Ce.normal, {
  html: ot(
    `^ *(?:comment *(?:\\n|\\s*$)|<(tag)[\\s\\S]+?</\\1> *(?:\\n{2,}|\\s*$)|<tag(?:"[^"]*"|'[^']*'|\\s[^'"/>\\s]*)*?/?> *(?:\\n{2,}|\\s*$))`
  ).replace("comment", Ce._comment).replace(/tag/g, "(?!(?:a|em|strong|small|s|cite|q|dfn|abbr|data|time|code|var|samp|kbd|sub|sup|i|b|u|mark|ruby|rt|rp|bdi|bdo|span|br|wbr|ins|del|img)\\b)\\w+(?!:|[^\\w\\s@]*@)\\b").getRegex(),
  def: /^ *\[([^\]]+)\]: *<?([^\s>]+)>?(?: +(["(][^\n]+[")]))? *(?:\n+|$)/,
  heading: /^(#{1,6})(.*)(?:\n+|$)/,
  fences: mc,
  // fences not supported
  paragraph: ot(Ce.normal._paragraph).replace("hr", Ce.hr).replace("heading", ` *#{1,6} *[^
]`).replace("lheading", Ce.lheading).replace("blockquote", " {0,3}>").replace("|fences", "").replace("|list", "").replace("|html", "").getRegex()
});
const fe = {
  escape: /^\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/,
  autolink: /^<(scheme:[^\s\x00-\x1f<>]*|email)>/,
  url: mc,
  tag: "^comment|^</[a-zA-Z][\\w:-]*\\s*>|^<[a-zA-Z][\\w-]*(?:attribute)*?\\s*/?>|^<\\?[\\s\\S]*?\\?>|^<![a-zA-Z]+\\s[\\s\\S]*?>|^<!\\[CDATA\\[[\\s\\S]*?\\]\\]>",
  // CDATA section
  link: /^!?\[(label)\]\(\s*(href)(?:\s+(title))?\s*\)/,
  reflink: /^!?\[(label)\]\[(ref)\]/,
  nolink: /^!?\[(ref)\](?:\[\])?/,
  reflinkSearch: "reflink|nolink(?!\\()",
  emStrong: {
    lDelim: /^(?:\*+(?:([punct_])|[^\s*]))|^_+(?:([punct*])|([^\s_]))/,
    //        (1) and (2) can only be a Right Delimiter. (3) and (4) can only be Left.  (5) and (6) can be either Left or Right.
    //        () Skip orphan delim inside strong    (1) #***                (2) a***#, a***                   (3) #***a, ***a                 (4) ***#              (5) #***#                 (6) a***a
    rDelimAst: /^[^_*]*?\_\_[^_*]*?\*[^_*]*?(?=\_\_)|[punct_](\*+)(?=[\s]|$)|[^punct*_\s](\*+)(?=[punct_\s]|$)|[punct_\s](\*+)(?=[^punct*_\s])|[\s](\*+)(?=[punct_])|[punct_](\*+)(?=[punct_])|[^punct*_\s](\*+)(?=[^punct*_\s])/,
    rDelimUnd: /^[^_*]*?\*\*[^_*]*?\_[^_*]*?(?=\*\*)|[punct*](\_+)(?=[\s]|$)|[^punct*_\s](\_+)(?=[punct*\s]|$)|[punct*\s](\_+)(?=[^punct*_\s])|[\s](\_+)(?=[punct*])|[punct*](\_+)(?=[punct*])/
    // ^- Not allowed for _
  },
  code: /^(`+)([^`]|[^`][\s\S]*?[^`])\1(?!`)/,
  br: /^( {2,}|\\)\n(?!\s*$)/,
  del: mc,
  text: /^(`+|[^`])(?:(?= {2,}\n)|[\s\S]*?(?:(?=[\\<!\[`*_]|\b_|$)|[^ ](?= {2,}\n)))/,
  punctuation: /^([\spunctuation])/
};
fe._punctuation = "!\"#$%&'()+\\-.,/:;<=>?@\\[\\]`^{|}~";
fe.punctuation = ot(fe.punctuation).replace(/punctuation/g, fe._punctuation).getRegex();
fe.blockSkip = /\[[^\]]*?\]\([^\)]*?\)|`[^`]*?`|<[^>]*?>/g;
fe.escapedEmSt = /\\\*|\\_/g;
fe._comment = ot(Ce._comment).replace("(?:-->|$)", "-->").getRegex();
fe.emStrong.lDelim = ot(fe.emStrong.lDelim).replace(/punct/g, fe._punctuation).getRegex();
fe.emStrong.rDelimAst = ot(fe.emStrong.rDelimAst, "g").replace(/punct/g, fe._punctuation).getRegex();
fe.emStrong.rDelimUnd = ot(fe.emStrong.rDelimUnd, "g").replace(/punct/g, fe._punctuation).getRegex();
fe._escapes = /\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/g;
fe._scheme = /[a-zA-Z][a-zA-Z0-9+.-]{1,31}/;
fe._email = /[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+(@)[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+(?![-_])/;
fe.autolink = ot(fe.autolink).replace("scheme", fe._scheme).replace("email", fe._email).getRegex();
fe._attribute = /\s+[a-zA-Z:_][\w.:-]*(?:\s*=\s*"[^"]*"|\s*=\s*'[^']*'|\s*=\s*[^\s"'=<>`]+)?/;
fe.tag = ot(fe.tag).replace("comment", fe._comment).replace("attribute", fe._attribute).getRegex();
fe._label = /(?:\[(?:\\.|[^\[\]\\])*\]|\\.|`[^`]*`|[^\[\]\\`])*?/;
fe._href = /<(?:\\.|[^\n<>\\])+>|[^\s\x00-\x1f]*/;
fe._title = /"(?:\\"?|[^"\\])*"|'(?:\\'?|[^'\\])*'|\((?:\\\)?|[^)\\])*\)/;
fe.link = ot(fe.link).replace("label", fe._label).replace("href", fe._href).replace("title", fe._title).getRegex();
fe.reflink = ot(fe.reflink).replace("label", fe._label).replace("ref", Ce._label).getRegex();
fe.nolink = ot(fe.nolink).replace("ref", Ce._label).getRegex();
fe.reflinkSearch = ot(fe.reflinkSearch, "g").replace("reflink", fe.reflink).replace("nolink", fe.nolink).getRegex();
fe.normal = rn({}, fe);
fe.pedantic = rn({}, fe.normal, {
  strong: {
    start: /^__|\*\*/,
    middle: /^__(?=\S)([\s\S]*?\S)__(?!_)|^\*\*(?=\S)([\s\S]*?\S)\*\*(?!\*)/,
    endAst: /\*\*(?!\*)/g,
    endUnd: /__(?!_)/g
  },
  em: {
    start: /^_|\*/,
    middle: /^()\*(?=\S)([\s\S]*?\S)\*(?!\*)|^_(?=\S)([\s\S]*?\S)_(?!_)/,
    endAst: /\*(?!\*)/g,
    endUnd: /_(?!_)/g
  },
  link: ot(/^!?\[(label)\]\((.*?)\)/).replace("label", fe._label).getRegex(),
  reflink: ot(/^!?\[(label)\]\s*\[([^\]]*)\]/).replace("label", fe._label).getRegex()
});
fe.gfm = rn({}, fe.normal, {
  escape: ot(fe.escape).replace("])", "~|])").getRegex(),
  _extended_email: /[A-Za-z0-9._+-]+(@)[a-zA-Z0-9-_]+(?:\.[a-zA-Z0-9-_]*[a-zA-Z0-9])+(?![-_])/,
  url: /^((?:ftp|https?):\/\/|www\.)(?:[a-zA-Z0-9\-]+\.?)+[^\s<]*|^email/,
  _backpedal: /(?:[^?!.,:;*_~()&]+|\([^)]*\)|&(?![a-zA-Z0-9]+;$)|[?!.,:;*_~)]+(?!$))+/,
  del: /^(~~?)(?=[^\s~])([\s\S]*?[^\s~])\1(?=[^~]|$)/,
  text: /^([`~]+|[^`~])(?:(?= {2,}\n)|(?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)|[\s\S]*?(?:(?=[\\<!\[`*~_]|\b_|https?:\/\/|ftp:\/\/|www\.|$)|[^ ](?= {2,}\n)|[^a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-](?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)))/
});
fe.gfm.url = ot(fe.gfm.url, "i").replace("email", fe.gfm._extended_email).getRegex();
fe.breaks = rn({}, fe.gfm, {
  br: ot(fe.br).replace("{2,}", "*").getRegex(),
  text: ot(fe.gfm.text).replace("\\b_", "\\b_| {2,}\\n").replace(/\{2,\}/g, "*").getRegex()
});
function Vj(n) {
  return n.replace(/---/g, "—").replace(/--/g, "–").replace(/(^|[-\u2014/(\[{"\s])'/g, "$1‘").replace(/'/g, "’").replace(/(^|[-\u2014/(\[{\u2018\s])"/g, "$1“").replace(/"/g, "”").replace(/\.{3}/g, "…");
}
function lw(n) {
  let t = "", e, i;
  const s = n.length;
  for (e = 0; e < s; e++)
    i = n.charCodeAt(e), Math.random() > 0.5 && (i = "x" + i.toString(16)), t += "&#" + i + ";";
  return t;
}
class Gn {
  constructor(t) {
    this.tokens = [], this.tokens.links = /* @__PURE__ */ Object.create(null), this.options = t || ro, this.options.tokenizer = this.options.tokenizer || new dm(), this.tokenizer = this.options.tokenizer, this.tokenizer.options = this.options, this.tokenizer.lexer = this, this.inlineQueue = [], this.state = {
      inLink: !1,
      inRawBlock: !1,
      top: !0
    };
    const e = {
      block: Ce.normal,
      inline: fe.normal
    };
    this.options.pedantic ? (e.block = Ce.pedantic, e.inline = fe.pedantic) : this.options.gfm && (e.block = Ce.gfm, this.options.breaks ? e.inline = fe.breaks : e.inline = fe.gfm), this.tokenizer.rules = e;
  }
  /**
   * Expose Rules
   */
  static get rules() {
    return {
      block: Ce,
      inline: fe
    };
  }
  /**
   * Static Lex Method
   */
  static lex(t, e) {
    return new Gn(e).lex(t);
  }
  /**
   * Static Lex Inline Method
   */
  static lexInline(t, e) {
    return new Gn(e).inlineTokens(t);
  }
  /**
   * Preprocessing
   */
  lex(t) {
    t = t.replace(/\r\n|\r/g, `
`).replace(/\t/g, "    "), this.blockTokens(t, this.tokens);
    let e;
    for (; e = this.inlineQueue.shift(); )
      this.inlineTokens(e.src, e.tokens);
    return this.tokens;
  }
  /**
   * Lexing
   */
  blockTokens(t, e = []) {
    this.options.pedantic && (t = t.replace(/^ +$/gm, ""));
    let i, s, r, o;
    for (; t; )
      if (!(this.options.extensions && this.options.extensions.block && this.options.extensions.block.some((a) => (i = a.call({ lexer: this }, t, e)) ? (t = t.substring(i.raw.length), e.push(i), !0) : !1))) {
        if (i = this.tokenizer.space(t)) {
          t = t.substring(i.raw.length), i.raw.length === 1 && e.length > 0 ? e[e.length - 1].raw += `
` : e.push(i);
          continue;
        }
        if (i = this.tokenizer.code(t)) {
          t = t.substring(i.raw.length), s = e[e.length - 1], s && (s.type === "paragraph" || s.type === "text") ? (s.raw += `
` + i.raw, s.text += `
` + i.text, this.inlineQueue[this.inlineQueue.length - 1].src = s.text) : e.push(i);
          continue;
        }
        if (i = this.tokenizer.fences(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.heading(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.hr(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.blockquote(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.list(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.html(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.def(t)) {
          t = t.substring(i.raw.length), s = e[e.length - 1], s && (s.type === "paragraph" || s.type === "text") ? (s.raw += `
` + i.raw, s.text += `
` + i.raw, this.inlineQueue[this.inlineQueue.length - 1].src = s.text) : this.tokens.links[i.tag] || (this.tokens.links[i.tag] = {
            href: i.href,
            title: i.title
          });
          continue;
        }
        if (i = this.tokenizer.table(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.lheading(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (r = t, this.options.extensions && this.options.extensions.startBlock) {
          let a = 1 / 0;
          const l = t.slice(1);
          let c;
          this.options.extensions.startBlock.forEach(function(u) {
            c = u.call({ lexer: this }, l), typeof c == "number" && c >= 0 && (a = Math.min(a, c));
          }), a < 1 / 0 && a >= 0 && (r = t.substring(0, a + 1));
        }
        if (this.state.top && (i = this.tokenizer.paragraph(r))) {
          s = e[e.length - 1], o && s.type === "paragraph" ? (s.raw += `
` + i.raw, s.text += `
` + i.text, this.inlineQueue.pop(), this.inlineQueue[this.inlineQueue.length - 1].src = s.text) : e.push(i), o = r.length !== t.length, t = t.substring(i.raw.length);
          continue;
        }
        if (i = this.tokenizer.text(t)) {
          t = t.substring(i.raw.length), s = e[e.length - 1], s && s.type === "text" ? (s.raw += `
` + i.raw, s.text += `
` + i.text, this.inlineQueue.pop(), this.inlineQueue[this.inlineQueue.length - 1].src = s.text) : e.push(i);
          continue;
        }
        if (t) {
          const a = "Infinite loop on byte: " + t.charCodeAt(0);
          if (this.options.silent) {
            console.error(a);
            break;
          } else
            throw new Error(a);
        }
      }
    return this.state.top = !0, e;
  }
  inline(t, e) {
    this.inlineQueue.push({ src: t, tokens: e });
  }
  /**
   * Lexing/Compiling
   */
  inlineTokens(t, e = []) {
    let i, s, r, o = t, a, l, c;
    if (this.tokens.links) {
      const u = Object.keys(this.tokens.links);
      if (u.length > 0)
        for (; (a = this.tokenizer.rules.inline.reflinkSearch.exec(o)) != null; )
          u.includes(a[0].slice(a[0].lastIndexOf("[") + 1, -1)) && (o = o.slice(0, a.index) + "[" + ow("a", a[0].length - 2) + "]" + o.slice(this.tokenizer.rules.inline.reflinkSearch.lastIndex));
    }
    for (; (a = this.tokenizer.rules.inline.blockSkip.exec(o)) != null; )
      o = o.slice(0, a.index) + "[" + ow("a", a[0].length - 2) + "]" + o.slice(this.tokenizer.rules.inline.blockSkip.lastIndex);
    for (; (a = this.tokenizer.rules.inline.escapedEmSt.exec(o)) != null; )
      o = o.slice(0, a.index) + "++" + o.slice(this.tokenizer.rules.inline.escapedEmSt.lastIndex);
    for (; t; )
      if (l || (c = ""), l = !1, !(this.options.extensions && this.options.extensions.inline && this.options.extensions.inline.some((u) => (i = u.call({ lexer: this }, t, e)) ? (t = t.substring(i.raw.length), e.push(i), !0) : !1))) {
        if (i = this.tokenizer.escape(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.tag(t)) {
          t = t.substring(i.raw.length), s = e[e.length - 1], s && i.type === "text" && s.type === "text" ? (s.raw += i.raw, s.text += i.text) : e.push(i);
          continue;
        }
        if (i = this.tokenizer.link(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.reflink(t, this.tokens.links)) {
          t = t.substring(i.raw.length), s = e[e.length - 1], s && i.type === "text" && s.type === "text" ? (s.raw += i.raw, s.text += i.text) : e.push(i);
          continue;
        }
        if (i = this.tokenizer.emStrong(t, o, c)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.codespan(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.br(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.del(t)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (i = this.tokenizer.autolink(t, lw)) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (!this.state.inLink && (i = this.tokenizer.url(t, lw))) {
          t = t.substring(i.raw.length), e.push(i);
          continue;
        }
        if (r = t, this.options.extensions && this.options.extensions.startInline) {
          let u = 1 / 0;
          const d = t.slice(1);
          let h;
          this.options.extensions.startInline.forEach(function(m) {
            h = m.call({ lexer: this }, d), typeof h == "number" && h >= 0 && (u = Math.min(u, h));
          }), u < 1 / 0 && u >= 0 && (r = t.substring(0, u + 1));
        }
        if (i = this.tokenizer.inlineText(r, Vj)) {
          t = t.substring(i.raw.length), i.raw.slice(-1) !== "_" && (c = i.raw.slice(-1)), l = !0, s = e[e.length - 1], s && s.type === "text" ? (s.raw += i.raw, s.text += i.text) : e.push(i);
          continue;
        }
        if (t) {
          const u = "Infinite loop on byte: " + t.charCodeAt(0);
          if (this.options.silent) {
            console.error(u);
            break;
          } else
            throw new Error(u);
        }
      }
    return e;
  }
}
class hm {
  constructor(t) {
    this.options = t || ro;
  }
  code(t, e, i) {
    const s = (e || "").match(/\S*/)[0];
    if (this.options.highlight) {
      const r = this.options.highlight(t, s);
      r != null && r !== t && (i = !0, t = r);
    }
    return t = t.replace(/\n$/, "") + `
`, s ? '<pre><code class="' + this.options.langPrefix + Nt(s, !0) + '">' + (i ? t : Nt(t, !0)) + `</code></pre>
` : "<pre><code>" + (i ? t : Nt(t, !0)) + `</code></pre>
`;
  }
  blockquote(t) {
    return `<blockquote>
` + t + `</blockquote>
`;
  }
  html(t) {
    return t;
  }
  heading(t, e, i, s) {
    return this.options.headerIds ? "<h" + e + ' id="' + this.options.headerPrefix + s.slug(i) + '">' + t + "</h" + e + `>
` : "<h" + e + ">" + t + "</h" + e + `>
`;
  }
  hr() {
    return this.options.xhtml ? `<hr/>
` : `<hr>
`;
  }
  list(t, e, i) {
    const s = e ? "ol" : "ul", r = e && i !== 1 ? ' start="' + i + '"' : "";
    return "<" + s + r + `>
` + t + "</" + s + `>
`;
  }
  listitem(t) {
    return "<li>" + t + `</li>
`;
  }
  checkbox(t) {
    return "<input " + (t ? 'checked="" ' : "") + 'disabled="" type="checkbox"' + (this.options.xhtml ? " /" : "") + "> ";
  }
  paragraph(t) {
    return "<p>" + t + `</p>
`;
  }
  table(t, e) {
    return e && (e = "<tbody>" + e + "</tbody>"), `<table>
<thead>
` + t + `</thead>
` + e + `</table>
`;
  }
  tablerow(t) {
    return `<tr>
` + t + `</tr>
`;
  }
  tablecell(t, e) {
    const i = e.header ? "th" : "td";
    return (e.align ? "<" + i + ' align="' + e.align + '">' : "<" + i + ">") + t + "</" + i + `>
`;
  }
  // span level renderer
  strong(t) {
    return "<strong>" + t + "</strong>";
  }
  em(t) {
    return "<em>" + t + "</em>";
  }
  codespan(t) {
    return "<code>" + t + "</code>";
  }
  br() {
    return this.options.xhtml ? "<br/>" : "<br>";
  }
  del(t) {
    return "<del>" + t + "</del>";
  }
  link(t, e, i) {
    if (t = sw(this.options.sanitize, this.options.baseUrl, t), t === null)
      return i;
    let s = '<a href="' + Nt(t) + '"';
    return e && (s += ' title="' + e + '"'), s += ">" + i + "</a>", s;
  }
  image(t, e, i) {
    if (t = sw(this.options.sanitize, this.options.baseUrl, t), t === null)
      return i;
    let s = '<img src="' + t + '" alt="' + i + '"';
    return e && (s += ' title="' + e + '"'), s += this.options.xhtml ? "/>" : ">", s;
  }
  text(t) {
    return t;
  }
}
class _C {
  // no need for block level renderers
  strong(t) {
    return t;
  }
  em(t) {
    return t;
  }
  codespan(t) {
    return t;
  }
  del(t) {
    return t;
  }
  html(t) {
    return t;
  }
  text(t) {
    return t;
  }
  link(t, e, i) {
    return "" + i;
  }
  image(t, e, i) {
    return "" + i;
  }
  br() {
    return "";
  }
}
class yC {
  constructor() {
    this.seen = {};
  }
  serialize(t) {
    return t.toLowerCase().trim().replace(/<[!\/a-z].*?>/ig, "").replace(/[\u2000-\u206F\u2E00-\u2E7F\\'!"#$%&()*+,./:;<=>?@[\]^`{|}~]/g, "").replace(/\s/g, "-");
  }
  /**
   * Finds the next safe (unique) slug to use
   */
  getNextSafeSlug(t, e) {
    let i = t, s = 0;
    if (this.seen.hasOwnProperty(i)) {
      s = this.seen[t];
      do
        s++, i = t + "-" + s;
      while (this.seen.hasOwnProperty(i));
    }
    return e || (this.seen[t] = s, this.seen[i] = 0), i;
  }
  /**
   * Convert string to unique id
   * @param {object} options
   * @param {boolean} options.dryrun Generates the next unique slug without updating the internal accumulator.
   */
  slug(t, e = {}) {
    const i = this.serialize(t);
    return this.getNextSafeSlug(i, e.dryrun);
  }
}
class Kn {
  constructor(t) {
    this.options = t || ro, this.options.renderer = this.options.renderer || new hm(), this.renderer = this.options.renderer, this.renderer.options = this.options, this.textRenderer = new _C(), this.slugger = new yC();
  }
  /**
   * Static Parse Method
   */
  static parse(t, e) {
    return new Kn(e).parse(t);
  }
  /**
   * Static Parse Inline Method
   */
  static parseInline(t, e) {
    return new Kn(e).parseInline(t);
  }
  /**
   * Parse Loop
   */
  parse(t, e = !0) {
    let i = "", s, r, o, a, l, c, u, d, h, m, g, p, w, y, k, C, E, T, L;
    const O = t.length;
    for (s = 0; s < O; s++) {
      if (m = t[s], this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[m.type] && (L = this.options.extensions.renderers[m.type].call({ parser: this }, m), L !== !1 || !["space", "hr", "heading", "code", "table", "blockquote", "list", "html", "paragraph", "text"].includes(m.type))) {
        i += L || "";
        continue;
      }
      switch (m.type) {
        case "space":
          continue;
        case "hr": {
          i += this.renderer.hr();
          continue;
        }
        case "heading": {
          i += this.renderer.heading(
            this.parseInline(m.tokens),
            m.depth,
            wC(this.parseInline(m.tokens, this.textRenderer)),
            this.slugger
          );
          continue;
        }
        case "code": {
          i += this.renderer.code(
            m.text,
            m.lang,
            m.escaped
          );
          continue;
        }
        case "table": {
          for (d = "", u = "", a = m.header.length, r = 0; r < a; r++)
            u += this.renderer.tablecell(
              this.parseInline(m.header[r].tokens),
              { header: !0, align: m.align[r] }
            );
          for (d += this.renderer.tablerow(u), h = "", a = m.rows.length, r = 0; r < a; r++) {
            for (c = m.rows[r], u = "", l = c.length, o = 0; o < l; o++)
              u += this.renderer.tablecell(
                this.parseInline(c[o].tokens),
                { header: !1, align: m.align[o] }
              );
            h += this.renderer.tablerow(u);
          }
          i += this.renderer.table(d, h);
          continue;
        }
        case "blockquote": {
          h = this.parse(m.tokens), i += this.renderer.blockquote(h);
          continue;
        }
        case "list": {
          for (g = m.ordered, p = m.start, w = m.loose, a = m.items.length, h = "", r = 0; r < a; r++)
            k = m.items[r], C = k.checked, E = k.task, y = "", k.task && (T = this.renderer.checkbox(C), w ? k.tokens.length > 0 && k.tokens[0].type === "paragraph" ? (k.tokens[0].text = T + " " + k.tokens[0].text, k.tokens[0].tokens && k.tokens[0].tokens.length > 0 && k.tokens[0].tokens[0].type === "text" && (k.tokens[0].tokens[0].text = T + " " + k.tokens[0].tokens[0].text)) : k.tokens.unshift({
              type: "text",
              text: T
            }) : y += T), y += this.parse(k.tokens, w), h += this.renderer.listitem(y, E, C);
          i += this.renderer.list(h, g, p);
          continue;
        }
        case "html": {
          i += this.renderer.html(m.text);
          continue;
        }
        case "paragraph": {
          i += this.renderer.paragraph(this.parseInline(m.tokens));
          continue;
        }
        case "text": {
          for (h = m.tokens ? this.parseInline(m.tokens) : m.text; s + 1 < O && t[s + 1].type === "text"; )
            m = t[++s], h += `
` + (m.tokens ? this.parseInline(m.tokens) : m.text);
          i += e ? this.renderer.paragraph(h) : h;
          continue;
        }
        default: {
          const N = 'Token with "' + m.type + '" type was not found.';
          if (this.options.silent) {
            console.error(N);
            return;
          } else
            throw new Error(N);
        }
      }
    }
    return i;
  }
  /**
   * Parse Inline Tokens
   */
  parseInline(t, e) {
    e = e || this.renderer;
    let i = "", s, r, o;
    const a = t.length;
    for (s = 0; s < a; s++) {
      if (r = t[s], this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[r.type] && (o = this.options.extensions.renderers[r.type].call({ parser: this }, r), o !== !1 || !["escape", "html", "link", "image", "strong", "em", "codespan", "br", "del", "text"].includes(r.type))) {
        i += o || "";
        continue;
      }
      switch (r.type) {
        case "escape": {
          i += e.text(r.text);
          break;
        }
        case "html": {
          i += e.html(r.text);
          break;
        }
        case "link": {
          i += e.link(r.href, r.title, this.parseInline(r.tokens, e));
          break;
        }
        case "image": {
          i += e.image(r.href, r.title, r.text);
          break;
        }
        case "strong": {
          i += e.strong(this.parseInline(r.tokens, e));
          break;
        }
        case "em": {
          i += e.em(this.parseInline(r.tokens, e));
          break;
        }
        case "codespan": {
          i += e.codespan(r.text);
          break;
        }
        case "br": {
          i += e.br();
          break;
        }
        case "del": {
          i += e.del(this.parseInline(r.tokens, e));
          break;
        }
        case "text": {
          i += e.text(r.text);
          break;
        }
        default: {
          const l = 'Token with "' + r.type + '" type was not found.';
          if (this.options.silent) {
            console.error(l);
            return;
          } else
            throw new Error(l);
        }
      }
    }
    return i;
  }
}
function xe(n, t, e) {
  if (typeof n > "u" || n === null)
    throw new Error("marked(): input parameter is undefined or null");
  if (typeof n != "string")
    throw new Error("marked(): input parameter is of type " + Object.prototype.toString.call(n) + ", string expected");
  if (typeof t == "function" && (e = t, t = null), t = rn({}, xe.defaults, t || {}), vC(t), e) {
    const i = t.highlight;
    let s;
    try {
      s = Gn.lex(n, t);
    } catch (a) {
      return e(a);
    }
    const r = function(a) {
      let l;
      if (!a)
        try {
          t.walkTokens && xe.walkTokens(s, t.walkTokens), l = Kn.parse(s, t);
        } catch (c) {
          a = c;
        }
      return t.highlight = i, a ? e(a) : e(null, l);
    };
    if (!i || i.length < 3 || (delete t.highlight, !s.length)) return r();
    let o = 0;
    xe.walkTokens(s, function(a) {
      a.type === "code" && (o++, setTimeout(() => {
        i(a.text, a.lang, function(l, c) {
          if (l)
            return r(l);
          c != null && c !== a.text && (a.text = c, a.escaped = !0), o--, o === 0 && r();
        });
      }, 0));
    }), o === 0 && r();
    return;
  }
  try {
    const i = Gn.lex(n, t);
    return t.walkTokens && xe.walkTokens(i, t.walkTokens), Kn.parse(i, t);
  } catch (i) {
    if (i.message += `
Please report this to https://github.com/markedjs/marked.`, t.silent)
      return "<p>An error occurred:</p><pre>" + Nt(i.message + "", !0) + "</pre>";
    throw i;
  }
}
xe.options = xe.setOptions = function(n) {
  return rn(xe.defaults, n), bj(xe.defaults), xe;
};
xe.getDefaults = bC;
xe.defaults = ro;
xe.use = function(...n) {
  const t = rn({}, ...n), e = xe.defaults.extensions || { renderers: {}, childTokens: {} };
  let i;
  n.forEach((s) => {
    if (s.extensions && (i = !0, s.extensions.forEach((r) => {
      if (!r.name)
        throw new Error("extension name required");
      if (r.renderer) {
        const o = e.renderers ? e.renderers[r.name] : null;
        o ? e.renderers[r.name] = function(...a) {
          let l = r.renderer.apply(this, a);
          return l === !1 && (l = o.apply(this, a)), l;
        } : e.renderers[r.name] = r.renderer;
      }
      if (r.tokenizer) {
        if (!r.level || r.level !== "block" && r.level !== "inline")
          throw new Error("extension level must be 'block' or 'inline'");
        e[r.level] ? e[r.level].unshift(r.tokenizer) : e[r.level] = [r.tokenizer], r.start && (r.level === "block" ? e.startBlock ? e.startBlock.push(r.start) : e.startBlock = [r.start] : r.level === "inline" && (e.startInline ? e.startInline.push(r.start) : e.startInline = [r.start]));
      }
      r.childTokens && (e.childTokens[r.name] = r.childTokens);
    })), s.renderer) {
      const r = xe.defaults.renderer || new hm();
      for (const o in s.renderer) {
        const a = r[o];
        r[o] = (...l) => {
          let c = s.renderer[o].apply(r, l);
          return c === !1 && (c = a.apply(r, l)), c;
        };
      }
      t.renderer = r;
    }
    if (s.tokenizer) {
      const r = xe.defaults.tokenizer || new dm();
      for (const o in s.tokenizer) {
        const a = r[o];
        r[o] = (...l) => {
          let c = s.tokenizer[o].apply(r, l);
          return c === !1 && (c = a.apply(r, l)), c;
        };
      }
      t.tokenizer = r;
    }
    if (s.walkTokens) {
      const r = xe.defaults.walkTokens;
      t.walkTokens = function(o) {
        s.walkTokens.call(this, o), r && r.call(this, o);
      };
    }
    i && (t.extensions = e), xe.setOptions(t);
  });
};
xe.walkTokens = function(n, t) {
  for (const e of n)
    switch (t.call(xe, e), e.type) {
      case "table": {
        for (const i of e.header)
          xe.walkTokens(i.tokens, t);
        for (const i of e.rows)
          for (const s of i)
            xe.walkTokens(s.tokens, t);
        break;
      }
      case "list": {
        xe.walkTokens(e.items, t);
        break;
      }
      default:
        xe.defaults.extensions && xe.defaults.extensions.childTokens && xe.defaults.extensions.childTokens[e.type] ? xe.defaults.extensions.childTokens[e.type].forEach(function(i) {
          xe.walkTokens(e[i], t);
        }) : e.tokens && xe.walkTokens(e.tokens, t);
    }
};
xe.parseInline = function(n, t) {
  if (typeof n > "u" || n === null)
    throw new Error("marked.parseInline(): input parameter is undefined or null");
  if (typeof n != "string")
    throw new Error("marked.parseInline(): input parameter is of type " + Object.prototype.toString.call(n) + ", string expected");
  t = rn({}, xe.defaults, t || {}), vC(t);
  try {
    const e = Gn.lexInline(n, t);
    return t.walkTokens && xe.walkTokens(e, t.walkTokens), Kn.parseInline(e, t);
  } catch (e) {
    if (e.message += `
Please report this to https://github.com/markedjs/marked.`, t.silent)
      return "<p>An error occurred:</p><pre>" + Nt(e.message + "", !0) + "</pre>";
    throw e;
  }
};
xe.Parser = Kn;
xe.parser = Kn.parse;
xe.Renderer = hm;
xe.TextRenderer = _C;
xe.Lexer = Gn;
xe.lexer = Gn.lex;
xe.Tokenizer = dm;
xe.Slugger = yC;
xe.parse = xe;
xe.options;
xe.setOptions;
xe.use;
xe.walkTokens;
xe.parseInline;
Kn.parse;
Gn.lex;
function Lj(n) {
  for (var t = 1; t < arguments.length; t++) {
    var e = arguments[t];
    for (var i in e)
      e.hasOwnProperty(i) && (n[i] = e[i]);
  }
  return n;
}
function Lh(n, t) {
  return Array(t + 1).join(n);
}
function Bj(n) {
  return n.replace(/^\n*/, "");
}
function Mj(n) {
  for (var t = n.length; t > 0 && n[t - 1] === `
`; ) t--;
  return n.substring(0, t);
}
var Nj = [
  "ADDRESS",
  "ARTICLE",
  "ASIDE",
  "AUDIO",
  "BLOCKQUOTE",
  "BODY",
  "CANVAS",
  "CENTER",
  "DD",
  "DIR",
  "DIV",
  "DL",
  "DT",
  "FIELDSET",
  "FIGCAPTION",
  "FIGURE",
  "FOOTER",
  "FORM",
  "FRAMESET",
  "H1",
  "H2",
  "H3",
  "H4",
  "H5",
  "H6",
  "HEADER",
  "HGROUP",
  "HR",
  "HTML",
  "ISINDEX",
  "LI",
  "MAIN",
  "MENU",
  "NAV",
  "NOFRAMES",
  "NOSCRIPT",
  "OL",
  "OUTPUT",
  "P",
  "PRE",
  "SECTION",
  "TABLE",
  "TBODY",
  "TD",
  "TFOOT",
  "TH",
  "THEAD",
  "TR",
  "UL"
];
function fm(n) {
  return mm(n, Nj);
}
var kC = [
  "AREA",
  "BASE",
  "BR",
  "COL",
  "COMMAND",
  "EMBED",
  "HR",
  "IMG",
  "INPUT",
  "KEYGEN",
  "LINK",
  "META",
  "PARAM",
  "SOURCE",
  "TRACK",
  "WBR"
];
function CC(n) {
  return mm(n, kC);
}
function Fj(n) {
  return TC(n, kC);
}
var xC = [
  "A",
  "TABLE",
  "THEAD",
  "TBODY",
  "TFOOT",
  "TH",
  "TD",
  "IFRAME",
  "SCRIPT",
  "AUDIO",
  "VIDEO"
];
function Dj(n) {
  return mm(n, xC);
}
function $j(n) {
  return TC(n, xC);
}
function mm(n, t) {
  return t.indexOf(n.nodeName) >= 0;
}
function TC(n, t) {
  return n.getElementsByTagName && t.some(function(e) {
    return n.getElementsByTagName(e).length;
  });
}
var Jt = {};
Jt.paragraph = {
  filter: "p",
  replacement: function(n) {
    return `

` + n + `

`;
  }
};
Jt.lineBreak = {
  filter: "br",
  replacement: function(n, t, e) {
    return e.br + `
`;
  }
};
Jt.heading = {
  filter: ["h1", "h2", "h3", "h4", "h5", "h6"],
  replacement: function(n, t, e) {
    var i = Number(t.nodeName.charAt(1));
    if (e.headingStyle === "setext" && i < 3) {
      var s = Lh(i === 1 ? "=" : "-", n.length);
      return `

` + n + `
` + s + `

`;
    } else
      return `

` + Lh("#", i) + " " + n + `

`;
  }
};
Jt.blockquote = {
  filter: "blockquote",
  replacement: function(n) {
    return n = n.replace(/^\n+|\n+$/g, ""), n = n.replace(/^/gm, "> "), `

` + n + `

`;
  }
};
Jt.list = {
  filter: ["ul", "ol"],
  replacement: function(n, t) {
    var e = t.parentNode;
    return e.nodeName === "LI" && e.lastElementChild === t ? `
` + n : `

` + n + `

`;
  }
};
Jt.listItem = {
  filter: "li",
  replacement: function(n, t, e) {
    n = n.replace(/^\n+/, "").replace(/\n+$/, `
`).replace(/\n/gm, `
    `);
    var i = e.bulletListMarker + "   ", s = t.parentNode;
    if (s.nodeName === "OL") {
      var r = s.getAttribute("start"), o = Array.prototype.indexOf.call(s.children, t);
      i = (r ? Number(r) + o : o + 1) + ".  ";
    }
    return i + n + (t.nextSibling && !/\n$/.test(n) ? `
` : "");
  }
};
Jt.indentedCodeBlock = {
  filter: function(n, t) {
    return t.codeBlockStyle === "indented" && n.nodeName === "PRE" && n.firstChild && n.firstChild.nodeName === "CODE";
  },
  replacement: function(n, t, e) {
    return `

    ` + t.firstChild.textContent.replace(/\n/g, `
    `) + `

`;
  }
};
Jt.fencedCodeBlock = {
  filter: function(n, t) {
    return t.codeBlockStyle === "fenced" && n.nodeName === "PRE" && n.firstChild && n.firstChild.nodeName === "CODE";
  },
  replacement: function(n, t, e) {
    for (var i = t.firstChild.getAttribute("class") || "", s = (i.match(/language-(\S+)/) || [null, ""])[1], r = t.firstChild.textContent, o = e.fence.charAt(0), a = 3, l = new RegExp("^" + o + "{3,}", "gm"), c; c = l.exec(r); )
      c[0].length >= a && (a = c[0].length + 1);
    var u = Lh(o, a);
    return `

` + u + s + `
` + r.replace(/\n$/, "") + `
` + u + `

`;
  }
};
Jt.horizontalRule = {
  filter: "hr",
  replacement: function(n, t, e) {
    return `

` + e.hr + `

`;
  }
};
Jt.inlineLink = {
  filter: function(n, t) {
    return t.linkStyle === "inlined" && n.nodeName === "A" && n.getAttribute("href");
  },
  replacement: function(n, t) {
    var e = t.getAttribute("href");
    e && (e = e.replace(/([()])/g, "\\$1"));
    var i = gc(t.getAttribute("title"));
    return i && (i = ' "' + i.replace(/"/g, '\\"') + '"'), "[" + n + "](" + e + i + ")";
  }
};
Jt.referenceLink = {
  filter: function(n, t) {
    return t.linkStyle === "referenced" && n.nodeName === "A" && n.getAttribute("href");
  },
  replacement: function(n, t, e) {
    var i = t.getAttribute("href"), s = gc(t.getAttribute("title"));
    s && (s = ' "' + s + '"');
    var r, o;
    switch (e.linkReferenceStyle) {
      case "collapsed":
        r = "[" + n + "][]", o = "[" + n + "]: " + i + s;
        break;
      case "shortcut":
        r = "[" + n + "]", o = "[" + n + "]: " + i + s;
        break;
      default:
        var a = this.references.length + 1;
        r = "[" + n + "][" + a + "]", o = "[" + a + "]: " + i + s;
    }
    return this.references.push(o), r;
  },
  references: [],
  append: function(n) {
    var t = "";
    return this.references.length && (t = `

` + this.references.join(`
`) + `

`, this.references = []), t;
  }
};
Jt.emphasis = {
  filter: ["em", "i"],
  replacement: function(n, t, e) {
    return n.trim() ? e.emDelimiter + n + e.emDelimiter : "";
  }
};
Jt.strong = {
  filter: ["strong", "b"],
  replacement: function(n, t, e) {
    return n.trim() ? e.strongDelimiter + n + e.strongDelimiter : "";
  }
};
Jt.code = {
  filter: function(n) {
    var t = n.previousSibling || n.nextSibling, e = n.parentNode.nodeName === "PRE" && !t;
    return n.nodeName === "CODE" && !e;
  },
  replacement: function(n) {
    if (!n) return "";
    n = n.replace(/\r?\n|\r/g, " ");
    for (var t = /^`|^ .*?[^ ].* $|`$/.test(n) ? " " : "", e = "`", i = n.match(/`+/gm) || []; i.indexOf(e) !== -1; ) e = e + "`";
    return e + t + n + t + e;
  }
};
Jt.image = {
  filter: "img",
  replacement: function(n, t) {
    var e = gc(t.getAttribute("alt")), i = t.getAttribute("src") || "", s = gc(t.getAttribute("title")), r = s ? ' "' + s + '"' : "";
    return i ? "![" + e + "](" + i + r + ")" : "";
  }
};
function gc(n) {
  return n ? n.replace(/(\n+\s*)+/g, `
`) : "";
}
function EC(n) {
  this.options = n, this._keep = [], this._remove = [], this.blankRule = {
    replacement: n.blankReplacement
  }, this.keepReplacement = n.keepReplacement, this.defaultRule = {
    replacement: n.defaultReplacement
  }, this.array = [];
  for (var t in n.rules) this.array.push(n.rules[t]);
}
EC.prototype = {
  add: function(n, t) {
    this.array.unshift(t);
  },
  keep: function(n) {
    this._keep.unshift({
      filter: n,
      replacement: this.keepReplacement
    });
  },
  remove: function(n) {
    this._remove.unshift({
      filter: n,
      replacement: function() {
        return "";
      }
    });
  },
  forNode: function(n) {
    if (n.isBlank) return this.blankRule;
    var t;
    return (t = Ed(this.array, n, this.options)) || (t = Ed(this._keep, n, this.options)) || (t = Ed(this._remove, n, this.options)) ? t : this.defaultRule;
  },
  forEach: function(n) {
    for (var t = 0; t < this.array.length; t++) n(this.array[t], t);
  }
};
function Ed(n, t, e) {
  for (var i = 0; i < n.length; i++) {
    var s = n[i];
    if (zj(s, t, e)) return s;
  }
}
function zj(n, t, e) {
  var i = n.filter;
  if (typeof i == "string") {
    if (i === t.nodeName.toLowerCase()) return !0;
  } else if (Array.isArray(i)) {
    if (i.indexOf(t.nodeName.toLowerCase()) > -1) return !0;
  } else if (typeof i == "function") {
    if (i.call(n, t, e)) return !0;
  } else
    throw new TypeError("`filter` needs to be a string, array, or function");
}
function Hj(n) {
  var t = n.element, e = n.isBlock, i = n.isVoid, s = n.isPre || function(d) {
    return d.nodeName === "PRE";
  };
  if (!(!t.firstChild || s(t))) {
    for (var r = null, o = !1, a = null, l = cw(a, t, s); l !== t; ) {
      if (l.nodeType === 3 || l.nodeType === 4) {
        var c = l.data.replace(/[ \r\n\t]+/g, " ");
        if ((!r || / $/.test(r.data)) && !o && c[0] === " " && (c = c.substr(1)), !c) {
          l = Sd(l);
          continue;
        }
        l.data = c, r = l;
      } else if (l.nodeType === 1)
        e(l) || l.nodeName === "BR" ? (r && (r.data = r.data.replace(/ $/, "")), r = null, o = !1) : i(l) || s(l) ? (r = null, o = !0) : r && (o = !1);
      else {
        l = Sd(l);
        continue;
      }
      var u = cw(a, l, s);
      a = l, l = u;
    }
    r && (r.data = r.data.replace(/ $/, ""), r.data || Sd(r));
  }
}
function Sd(n) {
  var t = n.nextSibling || n.parentNode;
  return n.parentNode.removeChild(n), t;
}
function cw(n, t, e) {
  return n && n.parentNode === t || e(t) ? t.nextSibling || t.parentNode : t.firstChild || t.nextSibling || t.parentNode;
}
var gm = typeof window < "u" ? window : {};
function jj() {
  var n = gm.DOMParser, t = !1;
  try {
    new n().parseFromString("", "text/html") && (t = !0);
  } catch {
  }
  return t;
}
function Uj() {
  var n = function() {
  };
  return Wj() ? n.prototype.parseFromString = function(t) {
    var e = new window.ActiveXObject("htmlfile");
    return e.designMode = "on", e.open(), e.write(t), e.close(), e;
  } : n.prototype.parseFromString = function(t) {
    var e = document.implementation.createHTMLDocument("");
    return e.open(), e.write(t), e.close(), e;
  }, n;
}
function Wj() {
  var n = !1;
  try {
    document.implementation.createHTMLDocument("").open();
  } catch {
    gm.ActiveXObject && (n = !0);
  }
  return n;
}
var qj = jj() ? gm.DOMParser : Uj();
function Gj(n, t) {
  var e;
  if (typeof n == "string") {
    var i = Kj().parseFromString(
      // DOM parsers arrange elements in the <head> and <body>.
      // Wrapping in a custom element ensures elements are reliably arranged in
      // a single element.
      '<x-turndown id="turndown-root">' + n + "</x-turndown>",
      "text/html"
    );
    e = i.getElementById("turndown-root");
  } else
    e = n.cloneNode(!0);
  return Hj({
    element: e,
    isBlock: fm,
    isVoid: CC,
    isPre: t.preformattedCode ? Zj : null
  }), e;
}
var Ad;
function Kj() {
  return Ad = Ad || new qj(), Ad;
}
function Zj(n) {
  return n.nodeName === "PRE" || n.nodeName === "CODE";
}
function Yj(n, t) {
  return n.isBlock = fm(n), n.isCode = n.nodeName === "CODE" || n.parentNode.isCode, n.isBlank = Jj(n), n.flankingWhitespace = Xj(n, t), n;
}
function Jj(n) {
  return !CC(n) && !Dj(n) && /^\s*$/i.test(n.textContent) && !Fj(n) && !$j(n);
}
function Xj(n, t) {
  if (n.isBlock || t.preformattedCode && n.isCode)
    return { leading: "", trailing: "" };
  var e = Qj(n.textContent);
  return e.leadingAscii && uw("left", n, t) && (e.leading = e.leadingNonAscii), e.trailingAscii && uw("right", n, t) && (e.trailing = e.trailingNonAscii), { leading: e.leading, trailing: e.trailing };
}
function Qj(n) {
  var t = n.match(/^(([ \t\r\n]*)(\s*))(?:(?=\S)[\s\S]*\S)?((\s*?)([ \t\r\n]*))$/);
  return {
    leading: t[1],
    // whole string for whitespace-only strings
    leadingAscii: t[2],
    leadingNonAscii: t[3],
    trailing: t[4],
    // empty for whitespace-only strings
    trailingNonAscii: t[5],
    trailingAscii: t[6]
  };
}
function uw(n, t, e) {
  var i, s, r;
  return n === "left" ? (i = t.previousSibling, s = / $/) : (i = t.nextSibling, s = /^ /), i && (i.nodeType === 3 ? r = s.test(i.nodeValue) : e.preformattedCode && i.nodeName === "CODE" ? r = !1 : i.nodeType === 1 && !fm(i) && (r = s.test(i.textContent))), r;
}
var eU = Array.prototype.reduce, tU = [
  [/\\/g, "\\\\"],
  [/\*/g, "\\*"],
  [/^-/g, "\\-"],
  [/^\+ /g, "\\+ "],
  [/^(=+)/g, "\\$1"],
  [/^(#{1,6}) /g, "\\$1 "],
  [/`/g, "\\`"],
  [/^~~~/g, "\\~~~"],
  [/\[/g, "\\["],
  [/\]/g, "\\]"],
  [/^>/g, "\\>"],
  [/_/g, "\\_"],
  [/^(\d+)\. /g, "$1\\. "]
];
function pc(n) {
  if (!(this instanceof pc)) return new pc(n);
  var t = {
    rules: Jt,
    headingStyle: "setext",
    hr: "* * *",
    bulletListMarker: "*",
    codeBlockStyle: "indented",
    fence: "```",
    emDelimiter: "_",
    strongDelimiter: "**",
    linkStyle: "inlined",
    linkReferenceStyle: "full",
    br: "  ",
    preformattedCode: !1,
    blankReplacement: function(e, i) {
      return i.isBlock ? `

` : "";
    },
    keepReplacement: function(e, i) {
      return i.isBlock ? `

` + i.outerHTML + `

` : i.outerHTML;
    },
    defaultReplacement: function(e, i) {
      return i.isBlock ? `

` + e + `

` : e;
    }
  };
  this.options = Lj({}, t, n), this.rules = new EC(this.options);
}
pc.prototype = {
  /**
   * The entry point for converting a string or DOM node to Markdown
   * @public
   * @param {String|HTMLElement} input The string or DOM node to convert
   * @returns A Markdown representation of the input
   * @type String
   */
  turndown: function(n) {
    if (!sU(n))
      throw new TypeError(
        n + " is not a string, or an element/document/fragment node."
      );
    if (n === "") return "";
    var t = SC.call(this, new Gj(n, this.options));
    return iU.call(this, t);
  },
  /**
   * Add one or more plugins
   * @public
   * @param {Function|Array} plugin The plugin or array of plugins to add
   * @returns The Turndown instance for chaining
   * @type Object
   */
  use: function(n) {
    if (Array.isArray(n))
      for (var t = 0; t < n.length; t++) this.use(n[t]);
    else if (typeof n == "function")
      n(this);
    else
      throw new TypeError("plugin must be a Function or an Array of Functions");
    return this;
  },
  /**
   * Adds a rule
   * @public
   * @param {String} key The unique key of the rule
   * @param {Object} rule The rule
   * @returns The Turndown instance for chaining
   * @type Object
   */
  addRule: function(n, t) {
    return this.rules.add(n, t), this;
  },
  /**
   * Keep a node (as HTML) that matches the filter
   * @public
   * @param {String|Array|Function} filter The unique key of the rule
   * @returns The Turndown instance for chaining
   * @type Object
   */
  keep: function(n) {
    return this.rules.keep(n), this;
  },
  /**
   * Remove a node that matches the filter
   * @public
   * @param {String|Array|Function} filter The unique key of the rule
   * @returns The Turndown instance for chaining
   * @type Object
   */
  remove: function(n) {
    return this.rules.remove(n), this;
  },
  /**
   * Escapes Markdown syntax
   * @public
   * @param {String} string The string to escape
   * @returns A string with Markdown syntax escaped
   * @type String
   */
  escape: function(n) {
    return tU.reduce(function(t, e) {
      return t.replace(e[0], e[1]);
    }, n);
  }
};
function SC(n) {
  var t = this;
  return eU.call(n.childNodes, function(e, i) {
    i = new Yj(i, t.options);
    var s = "";
    return i.nodeType === 3 ? s = i.isCode ? i.nodeValue : t.escape(i.nodeValue) : i.nodeType === 1 && (s = nU.call(t, i)), AC(e, s);
  }, "");
}
function iU(n) {
  var t = this;
  return this.rules.forEach(function(e) {
    typeof e.append == "function" && (n = AC(n, e.append(t.options)));
  }), n.replace(/^[\t\r\n]+/, "").replace(/[\t\r\n\s]+$/, "");
}
function nU(n) {
  var t = this.rules.forNode(n), e = SC.call(this, n), i = n.flankingWhitespace;
  return (i.leading || i.trailing) && (e = e.trim()), i.leading + t.replacement(e, n, this.options) + i.trailing;
}
function AC(n, t) {
  var e = Mj(n), i = Bj(t), s = Math.max(n.length - e.length, t.length - i.length), r = `

`.substring(0, s);
  return e + r + i;
}
function sU(n) {
  return n != null && (typeof n == "string" || n.nodeType && (n.nodeType === 1 || n.nodeType === 9 || n.nodeType === 11));
}
var dw = /highlight-(?:text|source)-([a-z0-9]+)/;
function rU(n) {
  n.addRule("highlightedCodeBlock", {
    filter: function(t) {
      var e = t.firstChild;
      return t.nodeName === "DIV" && dw.test(t.className) && e && e.nodeName === "PRE";
    },
    replacement: function(t, e, i) {
      var s = e.className || "", r = (s.match(dw) || [null, ""])[1];
      return `

` + i.fence + r + `
` + e.firstChild.textContent + `
` + i.fence + `

`;
    }
  });
}
function oU(n) {
  n.addRule("strikethrough", {
    filter: ["del", "s", "strike"],
    replacement: function(t) {
      return "~" + t + "~";
    }
  });
}
var aU = Array.prototype.indexOf, lU = Array.prototype.every, Zr = {};
Zr.tableCell = {
  filter: ["th", "td"],
  replacement: function(n, t) {
    return IC(n, t);
  }
};
Zr.tableRow = {
  filter: "tr",
  replacement: function(n, t) {
    var e = "", i = { left: ":--", right: "--:", center: ":-:" };
    if (pm(t))
      for (var s = 0; s < t.childNodes.length; s++) {
        var r = "---", o = (t.childNodes[s].getAttribute("align") || "").toLowerCase();
        o && (r = i[o] || r), e += IC(r, t.childNodes[s]);
      }
    return `
` + n + (e ? `
` + e : "");
  }
};
Zr.table = {
  // Only convert tables with a heading row.
  // Tables with no heading row are kept using `keep` (see below).
  filter: function(n) {
    return n.nodeName === "TABLE" && pm(n.rows[0]);
  },
  replacement: function(n) {
    return n = n.replace(`

`, `
`), `

` + n + `

`;
  }
};
Zr.tableSection = {
  filter: ["thead", "tbody", "tfoot"],
  replacement: function(n) {
    return n;
  }
};
function pm(n) {
  var t = n.parentNode;
  return t.nodeName === "THEAD" || t.firstChild === n && (t.nodeName === "TABLE" || cU(t)) && lU.call(n.childNodes, function(e) {
    return e.nodeName === "TH";
  });
}
function cU(n) {
  var t = n.previousSibling;
  return n.nodeName === "TBODY" && (!t || t.nodeName === "THEAD" && /^\s*$/i.test(t.textContent));
}
function IC(n, t) {
  var e = aU.call(t.parentNode.childNodes, t), i = " ";
  return e === 0 && (i = "| "), i + n + " |";
}
function uU(n) {
  n.keep(function(e) {
    return e.nodeName === "TABLE" && !pm(e.rows[0]);
  });
  for (var t in Zr) n.addRule(t, Zr[t]);
}
function dU(n) {
  n.addRule("taskListItems", {
    filter: function(t) {
      return t.type === "checkbox" && t.parentNode.nodeName === "LI";
    },
    replacement: function(t, e) {
      return (e.checked ? "[x]" : "[ ]") + " ";
    }
  });
}
function hU(n) {
  n.use([
    rU,
    oU,
    uU,
    dU
  ]);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class fU {
  constructor() {
    f(this, "_parser");
    f(this, "_options", {
      gfm: !0,
      breaks: !0,
      tables: !0,
      xhtml: !0,
      headerIds: !1
    });
    xe.use({
      tokenizer: {
        // Disable the autolink rule in the lexer.
        autolink: () => null,
        url: () => null
      },
      renderer: {
        checkbox(...t) {
          return Object.getPrototypeOf(this).checkbox.call(this, ...t).trimRight();
        },
        code(...t) {
          return Object.getPrototypeOf(this).code.call(this, ...t).replace(`
</code>`, "</code>");
        }
      }
    }), this._parser = xe;
  }
  parse(t) {
    return this._parser.parse(t, this._options);
  }
}
const mU = /* @__PURE__ */ new RegExp(
  // Prefix.
  /\b(?:(?:https?|ftp):\/\/|www\.)/.source + // Domain name.
  /(?![-_])(?:[-_a-z0-9\u00a1-\uffff]{1,63}\.)+(?:[a-z\u00a1-\uffff]{2,63})/.source + // The rest.
  /(?:[^\s<>]*)/.source,
  "gi"
);
class gU extends pc {
  escape(t) {
    const e = super.escape;
    function i(o) {
      return o = e(o), o = o.replace(/</g, "\\<"), o;
    }
    let s = "", r = 0;
    for (const o of this._matchAutolink(t)) {
      const a = o.index;
      a > r && (s += i(t.substring(r, a)));
      const l = o[0];
      s += l, r = a + l.length;
    }
    return r < t.length && (s += i(t.substring(r, t.length))), s;
  }
  /**
  * Trimming end of link.
  * https://github.github.com/gfm/#autolinks-extension-
  */
  *_matchAutolink(t) {
    for (const e of t.matchAll(mU)) {
      const i = e[0], s = this._autolinkFindEnd(i);
      yield Object.assign([
        i.substring(0, s)
      ], {
        index: e.index
      });
    }
  }
  /**
  * Returns the new length of the link (after it would trim trailing characters).
  */
  _autolinkFindEnd(t) {
    let e = t.length;
    for (; e > 0; ) {
      const i = t[e - 1];
      if (`?!.,:*_~'"`.includes(i))
        e--;
      else if (i == ")") {
        let s = 0;
        for (let r = 0; r < e; r++)
          t[r] == "(" ? s++ : t[r] == ")" && s--;
        if (s < 0)
          e--;
        else
          break;
      } else
        break;
    }
    return e;
  }
}
class pU {
  constructor() {
    f(this, "_parser");
    this._parser = this._createParser();
  }
  parse(t) {
    return this._parser.turndown(t);
  }
  keep(t) {
    this._parser.keep(t);
  }
  _createParser() {
    const t = new gU({
      codeBlockStyle: "fenced",
      hr: "---",
      headingStyle: "atx"
    });
    return t.use([
      hU,
      this._todoList
    ]), t;
  }
  // This is a copy of the original taskListItems rule from turndown-plugin-gfm, with minor changes.
  _todoList(t) {
    t.addRule("taskListItems", {
      filter(e) {
        return e.type === "checkbox" && // Changes here as CKEditor outputs a deeper structure.
        (e.parentNode.nodeName === "LI" || e.parentNode.parentNode.nodeName === "LI");
      },
      replacement(e, i) {
        return (i.checked ? "[x]" : "[ ]") + " ";
      }
    });
  }
}
class PC {
  /**
  * Creates a new instance of the Markdown data processor class.
  */
  constructor(t) {
    /**
    * HTML data processor used to process HTML produced by the Markdown-to-HTML converter and the other way.
    */
    f(this, "_htmlDP");
    /**
    * Helper for converting Markdown to HTML.
    */
    f(this, "_markdown2html");
    /**
    * Helper for converting HTML to Markdown.
    */
    f(this, "_html2markdown");
    this._htmlDP = new Of(t), this._markdown2html = new fU(), this._html2markdown = new pU();
  }
  /**
  * Keeps the specified element in the output as HTML. This is useful if the editor contains
  * features producing HTML that is not a part of the Markdown standard.
  *
  * By default, all HTML tags are removed.
  *
  * @param element The element name to be kept.
  */
  keepHtml(t) {
    this._html2markdown.keep([
      t
    ]);
  }
  /**
  * Converts the provided Markdown string to a view tree.
  *
  * @param data A Markdown string.
  * @returns The converted view element.
  */
  toView(t) {
    const e = this._markdown2html.parse(t);
    return this._htmlDP.toView(e);
  }
  /**
  * Converts the provided {@link module:engine/view/documentfragment~DocumentFragment} to data format &ndash; in this
  * case to a Markdown string.
  *
  * @returns Markdown string.
  */
  toData(t) {
    const e = this._htmlDP.toData(t);
    return this._html2markdown.parse(e);
  }
  /**
  * Registers a {@link module:engine/view/matcher~MatcherPattern} for view elements whose content should be treated as raw data
  * and not processed during the conversion from Markdown to view elements.
  *
  * The raw data can be later accessed by a
  * {@link module:engine/view/element~Element#getCustomProperty custom property of a view element} called `"$rawContent"`.
  *
  * @param pattern The pattern matching all view elements whose content should
  * be treated as raw data.
  */
  registerRawContentMatcher(t) {
    this._htmlDP.registerRawContentMatcher(t);
  }
  /**
  * This method does not have any effect on the data processor result. It exists for compatibility with the
  * {@link module:engine/dataprocessor/dataprocessor~DataProcessor `DataProcessor` interface}.
  */
  useFillerType() {
  }
}
class bU extends I {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), t.data.processor = new PC(t.data.viewDocument);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Markdown";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
const wU = [
  "SPAN",
  "BR",
  "PRE",
  "CODE"
];
class vU extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * @internal
    */
    f(this, "_gfmDataProcessor");
    this._gfmDataProcessor = new PC(e.data.viewDocument);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PasteFromMarkdownExperimental";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      _i
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, s = e.editing.view.document, r = e.plugins.get("ClipboardPipeline");
    let o = !1;
    this.listenTo(s, "keydown", (a, l) => {
      o = l.shiftKey;
    }), this.listenTo(r, "inputTransformation", (a, l) => {
      if (o)
        return;
      const c = l.dataTransfer.getData("text/html");
      if (!c) {
        const d = l.dataTransfer.getData("text/plain");
        l.content = this._gfmDataProcessor.toView(d);
        return;
      }
      const u = this._parseMarkdownFromHtml(c);
      u && (l.content = this._gfmDataProcessor.toView(u));
    });
  }
  /**
  * Determines if the code copied from a website in the `text/html` type can be parsed as Markdown.
  * It removes any OS-specific HTML tags, for example, <meta> on macOS and <!--StartFragment--> on Windows.
  * Then removes a single wrapper HTML tag or wrappers for sibling tags, and if there are no more tags left,
  * returns the remaining text. Returns null if there are any remaining HTML tags detected.
  *
  * @param htmlString Clipboard content in the `text/html` type format.
  */
  _parseMarkdownFromHtml(e) {
    const i = this._removeOsSpecificTags(e);
    if (!this._containsOnlyAllowedFirstLevelTags(i))
      return null;
    const s = this._removeFirstLevelWrapperTagsAndBrs(i);
    return this._containsAnyRemainingHtmlTags(s) ? null : this._replaceHtmlReservedEntitiesWithCharacters(s);
  }
  /**
  * Removes OS-specific tags.
  *
  * @param htmlString Clipboard content in the `text/html` type format.
  */
  _removeOsSpecificTags(e) {
    return e.replace(/^<meta\b[^>]*>/, "").trim().replace(/^<html>/, "").replace(/<\/html>$/, "").trim().replace(/^<body>/, "").replace(/<\/body>$/, "").trim().replace(/^<!--StartFragment-->/, "").replace(/<!--EndFragment-->$/, "").trim();
  }
  /**
  * If the input HTML string contains any first-level formatting tags
  * like <b>, <strong>, or <i>, we should not treat it as Markdown.
  *
  * @param htmlString Clipboard content.
  */
  _containsOnlyAllowedFirstLevelTags(e) {
    const i = new DOMParser(), { body: s } = i.parseFromString(e, "text/html");
    return Array.from(s.children).map((o) => o.tagName).every((o) => wU.includes(o));
  }
  /**
  * Removes multiple HTML wrapper tags from a list of sibling HTML tags.
  *
  * @param htmlString Clipboard content without any OS-specific tags.
  */
  _removeFirstLevelWrapperTagsAndBrs(e) {
    const i = new DOMParser(), { body: s } = i.parseFromString(e, "text/html"), r = s.querySelectorAll("br");
    for (const a of r)
      a.replaceWith(`
`);
    const o = s.querySelectorAll(":scope > *");
    for (const a of o) {
      const l = a.cloneNode(!0);
      a.replaceWith(...l.childNodes);
    }
    return s.innerHTML;
  }
  /**
  * Determines if a string contains any HTML tags.
  */
  _containsAnyRemainingHtmlTags(e) {
    return e.includes("<");
  }
  /**
  * Replaces the reserved HTML entities with the actual characters.
  *
  * @param htmlString Clipboard content without any tags.
  */
  _replaceHtmlReservedEntitiesWithCharacters(e) {
    return e.replace(/&gt;/g, ">").replace(/&lt;/g, "<").replace(/&nbsp;/g, " ");
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function hw(n, t) {
  const e = (i, s, r) => {
    if (!r.consumable.consume(s.item, i.name))
      return;
    const o = s.attributeNewValue, a = r.writer, l = r.mapper.toViewElement(s.item), c = [
      ...l.getChildren()
    ].find((d) => d.getCustomProperty("media-content"));
    a.remove(c);
    const u = n.getMediaViewElement(a, o, t);
    a.insert(a.createPositionAt(l, 0), u);
  };
  return (i) => {
    i.on("attribute:url:media", e);
  };
}
function _U(n, t, e) {
  return t.setCustomProperty("media", !0, n), Qn(n, t, {
    label: e
  });
}
function fw(n, t, e, i) {
  return n.createContainerElement("figure", {
    class: "media"
  }, [
    t.getMediaViewElement(n, e, i),
    n.createSlot()
  ]);
}
function mw(n) {
  const t = n.getSelectedElement();
  return t && t.is("element", "media") ? t : null;
}
function RC(n, t, e, i) {
  n.change((s) => {
    const r = s.createElement("media", {
      url: t
    });
    n.insertObject(r, e, null, {
      setSelection: "on",
      findOptimalPosition: i ? "auto" : void 0
    });
  });
}
class yU extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document.selection, i = mw(e);
    this.value = i ? i.getAttribute("url") : void 0, this.isEnabled = CU(e) || kU(e, t);
  }
  /**
  * Executes the command, which either:
  *
  * * updates the URL of the selected media,
  * * inserts the new media into the editor and puts the selection around it.
  *
  * @fires execute
  * @param url The URL of the media.
  */
  execute(t) {
    const e = this.editor.model, i = e.document.selection, s = mw(i);
    s ? e.change((r) => {
      r.setAttribute("url", t, s);
    }) : RC(e, t, i, !0);
  }
}
function kU(n, t) {
  let i = Da(n, t).start.parent;
  return i.isEmpty && !t.schema.isLimit(i) && (i = i.parent), t.schema.checkChild(i, "media");
}
function CU(n) {
  const t = n.getSelectedElement();
  return !!t && t.name === "media";
}
var xU = '<svg viewBox="0 0 64 42" xmlns="http://www.w3.org/2000/svg"><path d="M47.426 17V3.713L63.102 0v19.389h-.001l.001.272c0 1.595-2.032 3.43-4.538 4.098-2.506.668-4.538-.083-4.538-1.678 0-1.594 2.032-3.43 4.538-4.098.914-.244 2.032-.565 2.888-.603V4.516L49.076 7.447v9.556A1.014 1.014 0 0 0 49 17h-1.574zM29.5 17h-8.343a7.073 7.073 0 1 0-4.657 4.06v3.781H3.3a2.803 2.803 0 0 1-2.8-2.804V8.63a2.803 2.803 0 0 1 2.8-2.805h4.082L8.58 2.768A1.994 1.994 0 0 1 10.435 1.5h8.985c.773 0 1.477.448 1.805 1.149l1.488 3.177H26.7c1.546 0 2.8 1.256 2.8 2.805V17zm-11.637 0H17.5a1 1 0 0 0-1 1v.05A4.244 4.244 0 1 1 17.863 17zm29.684 2c.97 0 .953-.048.953.889v20.743c0 .953.016.905-.953.905H19.453c-.97 0-.953.048-.953-.905V19.89c0-.937-.016-.889.97-.889h28.077zm-4.701 19.338V22.183H24.154v16.155h18.692zM20.6 21.375v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616v-1.616H20.6zm0 3.231v1.616h1.616V37.53H20.6zm24.233-16.155v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615v-1.616h-1.615zm0 3.231v1.616h1.615V37.53h-1.615zM29.485 25.283a.4.4 0 0 1 .593-.35l9.05 4.977a.4.4 0 0 1 0 .701l-9.05 4.978a.4.4 0 0 1-.593-.35v-9.956z"/></svg>';
const TU = "0 0 64 42";
class EU {
  /**
  * Creates an instance of the {@link module:media-embed/mediaregistry~MediaRegistry} class.
  *
  * @param locale The localization services instance.
  * @param config The configuration of the media embed feature.
  */
  constructor(t, e) {
    /**
    * The {@link module:utils/locale~Locale} instance.
    */
    f(this, "locale");
    /**
    * The media provider definitions available for the registry. Usually corresponding with the
    * {@link module:media-embed/mediaembedconfig~MediaEmbedConfig media configuration}.
    */
    f(this, "providerDefinitions");
    const i = e.providers, s = e.extraProviders || [], r = new Set(e.removeProviders), o = i.concat(s).filter((a) => {
      const l = a.name;
      return l ? !r.has(l) : (Ve("media-embed-no-provider-name", {
        provider: a
      }), !1);
    });
    this.locale = t, this.providerDefinitions = o;
  }
  /**
  * Checks whether the passed URL is representing a certain media type allowed in the editor.
  *
  * @param url The URL to be checked
  */
  hasMedia(t) {
    return !!this._getMedia(t);
  }
  /**
  * For the given media URL string and options, it returns the {@link module:engine/view/element~Element view element}
  * representing that media.
  *
  * **Note:** If no URL is specified, an empty view element is returned.
  *
  * @param writer The view writer used to produce a view element.
  * @param url The URL to be translated into a view element.
  */
  getMediaViewElement(t, e, i) {
    return this._getMedia(e).getViewElement(t, i);
  }
  /**
  * Returns a `Media` instance for the given URL.
  *
  * @param url The URL of the media.
  * @returns The `Media` instance or `null` when there is none.
  */
  _getMedia(t) {
    if (!t)
      return new gw(this.locale);
    t = t.trim();
    for (const e of this.providerDefinitions) {
      const i = e.html, s = Ue(e.url);
      for (const r of s) {
        const o = this._getUrlMatches(t, r);
        if (o)
          return new gw(this.locale, t, o, i);
      }
    }
    return null;
  }
  /**
  * Tries to match `url` to `pattern`.
  *
  * @param url The URL of the media.
  * @param pattern The pattern that should accept the media URL.
  */
  _getUrlMatches(t, e) {
    let i = t.match(e);
    if (i)
      return i;
    let s = t.replace(/^https?:\/\//, "");
    return i = s.match(e), i || (s = s.replace(/^www\./, ""), i = s.match(e), i) ? i : null;
  }
}
class gw {
  constructor(t, e, i, s) {
    /**
    * The URL this Media instance represents.
    */
    f(this, "url");
    /**
    * Shorthand for {@link module:utils/locale~Locale#t}.
    *
    * @see module:utils/locale~Locale#t
    */
    f(this, "_locale");
    /**
    * The output of the `RegExp.match` which validated the {@link #url} of this media.
    */
    f(this, "_match");
    /**
    * The function returning the HTML string preview of this media.
    */
    f(this, "_previewRenderer");
    this.url = this._getValidUrl(e), this._locale = t, this._match = i, this._previewRenderer = s;
  }
  /**
  * Returns the view element representation of the media.
  *
  * @param writer The view writer used to produce a view element.
  */
  getViewElement(t, e) {
    const i = {};
    let s;
    if (e.renderForEditingView || e.renderMediaPreview && this.url && this._previewRenderer) {
      this.url && (i["data-oembed-url"] = this.url), e.renderForEditingView && (i.class = "ck-media__wrapper");
      const r = this._getPreviewHtml(e);
      s = t.createRawElement("div", i, (o, a) => {
        a.setContentOf(o, r);
      });
    } else
      this.url && (i.url = this.url), s = t.createEmptyElement(e.elementName, i);
    return t.setCustomProperty("media-content", !0, s), s;
  }
  /**
  * Returns the HTML string of the media content preview.
  */
  _getPreviewHtml(t) {
    return this._previewRenderer ? this._previewRenderer(this._match) : this.url && t.renderForEditingView ? this._getPlaceholderHtml() : "";
  }
  /**
  * Returns the placeholder HTML when the media has no content preview.
  */
  _getPlaceholderHtml() {
    const t = new Qi(), e = this._locale.t;
    return t.content = xU, t.viewBox = TU, new ri({
      tag: "div",
      attributes: {
        class: "ck ck-reset_all ck-media__placeholder"
      },
      children: [
        {
          tag: "div",
          attributes: {
            class: "ck-media__placeholder__icon"
          },
          children: [
            t
          ]
        },
        {
          tag: "a",
          attributes: {
            class: "ck-media__placeholder__url",
            target: "_blank",
            rel: "noopener noreferrer",
            href: this.url,
            "data-cke-tooltip-text": e("Open media in new tab")
          },
          children: [
            {
              tag: "span",
              attributes: {
                class: "ck-media__placeholder__url__text"
              },
              children: [
                this.url
              ]
            }
          ]
        }
      ]
    }).render().outerHTML;
  }
  /**
  * Returns the full URL to the specified media.
  *
  * @param url The URL of the media.
  */
  _getValidUrl(t) {
    return t ? t.match(/^https?/) ? t : "https://" + t : null;
  }
}
class bc extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The media registry managing the media providers in the editor.
    */
    f(this, "registry");
    e.config.define("mediaEmbed", {
      elementName: "oembed",
      providers: [
        {
          name: "dailymotion",
          url: [
            /^dailymotion\.com\/video\/(\w+)/,
            /^dai.ly\/(\w+)/
          ],
          html: (i) => `<div style="position: relative; padding-bottom: 100%; height: 0; "><iframe src="https://www.dailymotion.com/embed/video/${i[1]}" style="position: absolute; width: 100%; height: 100%; top: 0; left: 0;" frameborder="0" width="480" height="270" allowfullscreen allow="autoplay"></iframe></div>`
        },
        {
          name: "spotify",
          url: [
            /^open\.spotify\.com\/(artist\/\w+)/,
            /^open\.spotify\.com\/(album\/\w+)/,
            /^open\.spotify\.com\/(track\/\w+)/
          ],
          html: (i) => `<div style="position: relative; padding-bottom: 100%; height: 0; padding-bottom: 126%;"><iframe src="https://open.spotify.com/embed/${i[1]}" style="position: absolute; width: 100%; height: 100%; top: 0; left: 0;" frameborder="0" allowtransparency="true" allow="encrypted-media"></iframe></div>`
        },
        {
          name: "youtube",
          url: [
            /^(?:m\.)?youtube\.com\/watch\?v=([\w-]+)(?:&t=(\d+))?/,
            /^(?:m\.)?youtube\.com\/shorts\/([\w-]+)(?:\?t=(\d+))?/,
            /^(?:m\.)?youtube\.com\/v\/([\w-]+)(?:\?t=(\d+))?/,
            /^youtube\.com\/embed\/([\w-]+)(?:\?start=(\d+))?/,
            /^youtu\.be\/([\w-]+)(?:\?t=(\d+))?/
          ],
          html: (i) => {
            const s = i[1], r = i[2];
            return `<div style="position: relative; padding-bottom: 100%; height: 0; padding-bottom: 56.2493%;"><iframe src="https://www.youtube.com/embed/${s}${r ? `?start=${r}` : ""}" style="position: absolute; width: 100%; height: 100%; top: 0; left: 0;" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe></div>`;
          }
        },
        {
          name: "vimeo",
          url: [
            /^vimeo\.com\/(\d+)/,
            /^vimeo\.com\/[^/]+\/[^/]+\/video\/(\d+)/,
            /^vimeo\.com\/album\/[^/]+\/video\/(\d+)/,
            /^vimeo\.com\/channels\/[^/]+\/(\d+)/,
            /^vimeo\.com\/groups\/[^/]+\/videos\/(\d+)/,
            /^vimeo\.com\/ondemand\/[^/]+\/(\d+)/,
            /^player\.vimeo\.com\/video\/(\d+)/
          ],
          html: (i) => `<div style="position: relative; padding-bottom: 100%; height: 0; padding-bottom: 56.2493%;"><iframe src="https://player.vimeo.com/video/${i[1]}" style="position: absolute; width: 100%; height: 100%; top: 0; left: 0;" frameborder="0" webkitallowfullscreen mozallowfullscreen allowfullscreen></iframe></div>`
        },
        {
          name: "instagram",
          url: [
            /^instagram\.com\/p\/(\w+)/,
            /^instagram\.com\/reel\/(\w+)/
          ]
        },
        {
          name: "twitter",
          url: [
            /^twitter\.com/,
            /^x\.com/
          ]
        },
        {
          name: "googleMaps",
          url: [
            /^google\.com\/maps/,
            /^goo\.gl\/maps/,
            /^maps\.google\.com/,
            /^maps\.app\.goo\.gl/
          ]
        },
        {
          name: "flickr",
          url: /^flickr\.com/
        },
        {
          name: "facebook",
          url: /^facebook\.com/
        }
      ]
    }), this.registry = new EU(e.locale, e.config.get("mediaEmbed"));
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MediaEmbedEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model.schema, s = e.t, r = e.conversion, o = e.config.get("mediaEmbed.previewsInData"), a = e.config.get("mediaEmbed.elementName"), l = this.registry;
    e.commands.add("mediaEmbed", new yU(e)), i.register("media", {
      inheritAllFrom: "$blockObject",
      allowAttributes: [
        "url"
      ]
    }), r.for("dataDowncast").elementToStructure({
      model: "media",
      view: (c, { writer: u }) => {
        const d = c.getAttribute("url");
        return fw(u, l, d, {
          elementName: a,
          renderMediaPreview: !!d && o
        });
      }
    }), r.for("dataDowncast").add(hw(l, {
      elementName: a,
      renderMediaPreview: o
    })), r.for("editingDowncast").elementToStructure({
      model: "media",
      view: (c, { writer: u }) => {
        const d = c.getAttribute("url"), h = fw(u, l, d, {
          elementName: a,
          renderForEditingView: !0
        });
        return _U(h, u, s("media widget"));
      }
    }), r.for("editingDowncast").add(hw(l, {
      elementName: a,
      renderForEditingView: !0
    })), r.for("upcast").elementToElement({
      view: (c) => [
        "oembed",
        a
      ].includes(c.name) && c.getAttribute("url") ? {
        name: !0
      } : null,
      model: (c, { writer: u }) => {
        const d = c.getAttribute("url");
        return l.hasMedia(d) ? u.createElement("media", {
          url: d
        }) : null;
      }
    }).elementToElement({
      view: {
        name: "div",
        attributes: {
          "data-oembed-url": !0
        }
      },
      model: (c, { writer: u }) => {
        const d = c.getAttribute("data-oembed-url");
        return l.hasMedia(d) ? u.createElement("media", {
          url: d
        }) : null;
      }
    }).add((c) => {
      const u = (d, h, m) => {
        if (!m.consumable.consume(h.viewItem, {
          name: !0,
          classes: "media"
        }))
          return;
        const { modelRange: g, modelCursor: p } = m.convertChildren(h.viewItem, h.modelCursor);
        h.modelRange = g, h.modelCursor = p, Xe(g.getItems()) || m.consumable.revert(h.viewItem, {
          name: !0,
          classes: "media"
        });
      };
      c.on("element:figure", u);
    });
  }
}
const SU = /^(?:http(s)?:\/\/)?[\w-]+\.[\w-.~:/?#[\]@!$&'()*+,;=%]+$/;
class AU extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The paste–to–embed `setTimeout` ID. Stored as a property to allow
    * cleaning of the timeout.
    */
    f(this, "_timeoutId");
    /**
    * The position where the `<media>` element will be inserted after the timeout,
    * determined each time the new content is pasted into the document.
    */
    f(this, "_positionToInsert");
    this._timeoutId = null, this._positionToInsert = null;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Jf,
      Cn,
      Qf
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "AutoMediaEmbed";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model.document, s = e.plugins.get("ClipboardPipeline");
    this.listenTo(s, "inputTransformation", () => {
      const o = i.selection.getFirstRange(), a = At.fromPosition(o.start);
      a.stickiness = "toPrevious";
      const l = At.fromPosition(o.end);
      l.stickiness = "toNext", i.once("change:data", () => {
        this._embedMediaBetweenPositions(a, l), a.detach(), l.detach();
      }, {
        priority: "high"
      });
    }), e.commands.get("undo").on("execute", () => {
      this._timeoutId && (ae.window.clearTimeout(this._timeoutId), this._positionToInsert.detach(), this._timeoutId = null, this._positionToInsert = null);
    }, {
      priority: "high"
    });
  }
  /**
  * Analyzes the part of the document between provided positions in search for a URL representing media.
  * When the URL is found, it is automatically converted into media.
  *
  * @param leftPosition Left position of the selection.
  * @param rightPosition Right position of the selection.
  */
  _embedMediaBetweenPositions(e, i) {
    const s = this.editor, r = s.plugins.get(bc).registry, o = new ni(e, i), a = o.getWalker({
      ignoreElementEnd: !0
    });
    let l = "";
    for (const u of a)
      u.item.is("$textProxy") && (l += u.item.data);
    if (l = l.trim(), !l.match(SU)) {
      o.detach();
      return;
    }
    if (!r.hasMedia(l)) {
      o.detach();
      return;
    }
    if (!s.commands.get("mediaEmbed").isEnabled) {
      o.detach();
      return;
    }
    this._positionToInsert = At.fromPosition(e), this._timeoutId = ae.window.setTimeout(() => {
      s.model.change((u) => {
        this._timeoutId = null, u.remove(o), o.detach();
        let d = null;
        this._positionToInsert.root.rootName !== "$graveyard" && (d = this._positionToInsert), RC(s.model, l, d, !1), this._positionToInsert.detach(), this._positionToInsert = null;
      }), s.plugins.get(Cn).requestUndoOnBackspace();
    }, 100);
  }
}
class IU extends ee {
  /**
  * @param validators Form validators used by {@link #isValid}.
  * @param locale The localization services instance.
  */
  constructor(e, i) {
    super(i);
    /**
    * Tracks information about the DOM focus in the form.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * The URL input view.
    */
    f(this, "urlInputView");
    /**
    * An array of form validators used by {@link #isValid}.
    */
    f(this, "_validators");
    /**
    * The default info text for the {@link #urlInputView}.
    */
    f(this, "_urlInputViewInfoDefault");
    /**
    * The info text with an additional tip for the {@link #urlInputView},
    * displayed when the input has some value.
    */
    f(this, "_urlInputViewInfoTip");
    this.focusTracker = new ze(), this.keystrokes = new Ze(), this.set("mediaURLInputValue", ""), this.urlInputView = this._createUrlInput(), this._validators = e, this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-media-form",
          "ck-responsive-form"
        ],
        tabindex: "-1"
      },
      children: [
        this.urlInputView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Tn({
      view: this
    }), this.focusTracker.add(this.urlInputView.element), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the {@link #urlInputView}.
  */
  focus() {
    this.urlInputView.focus();
  }
  /**
  * The native DOM `value` of the {@link #urlInputView} element.
  *
  * **Note**: Do not confuse it with the {@link module:ui/inputtext/inputtextview~InputTextView#value}
  * which works one way only and may not represent the actual state of the component in the DOM.
  */
  get url() {
    return this.urlInputView.fieldView.element.value.trim();
  }
  set url(e) {
    this.urlInputView.fieldView.value = e.trim();
  }
  /**
  * Validates the form and returns `false` when some fields are invalid.
  */
  isValid() {
    this.resetFormStatus();
    for (const e of this._validators) {
      const i = e(this);
      if (i)
        return this.urlInputView.errorText = i, !1;
    }
    return !0;
  }
  /**
  * Cleans up the supplementary error and information text of the {@link #urlInputView}
  * bringing them back to the state when the form has been displayed for the first time.
  *
  * See {@link #isValid}.
  */
  resetFormStatus() {
    this.urlInputView.errorText = null, this.urlInputView.infoText = this._urlInputViewInfoDefault;
  }
  /**
  * Creates a labeled input view.
  *
  * @returns Labeled input view instance.
  */
  _createUrlInput() {
    const e = this.locale.t, i = new gt(this.locale, pi), s = i.fieldView;
    return this._urlInputViewInfoDefault = e("Paste the media URL in the input."), this._urlInputViewInfoTip = e("Tip: Paste the URL into the content to embed faster."), i.label = e("Media URL"), i.infoText = this._urlInputViewInfoDefault, s.inputMode = "url", s.on("input", () => {
      i.infoText = s.element.value ? this._urlInputViewInfoTip : this._urlInputViewInfoDefault, this.mediaURLInputValue = s.element.value.trim();
    }), i;
  }
}
var PU = '<svg viewBox="0 0 22 20" xmlns="http://www.w3.org/2000/svg"><path d="M1.587 1.5c-.612 0-.601-.029-.601.551v14.84c0 .59-.01.559.591.559h18.846c.602 0 .591.03.591-.56V2.052c0-.58.01-.55-.591-.55H1.587Zm.701.971h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-14.24 1h13.008v12H4.467l.029-12Zm-2.208 1h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h1.003v1H2.288v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h1.003l-.029 1h-.974v-1Zm16.448 0h1.003v1h-1.003v-1Zm-16.448 2h.974v1h-.974v-1Zm16.448 0h1.003v1h-1.003v-1Z"/><path d="M8.374 6.648a.399.399 0 0 1 .395-.4.402.402 0 0 1 .2.049l5.148 2.824a.4.4 0 0 1 0 .7l-5.148 2.824a.403.403 0 0 1-.595-.35V6.648Z"/></svg>';
class RU extends I {
  constructor() {
    super(...arguments);
    f(this, "_formView");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      bc,
      Hs
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MediaEmbedUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.ui.componentFactory.add("mediaEmbed", () => {
      const i = this.editor.locale.t, s = this._createDialogButton(le);
      return s.tooltip = !0, s.label = i("Insert media"), s;
    }), e.ui.componentFactory.add("menuBar:mediaEmbed", () => {
      const i = this.editor.locale.t, s = this._createDialogButton(Fe);
      return s.label = i("Media"), s;
    });
  }
  /**
  * Creates a button for menu bar that will show media embed dialog.
  */
  _createDialogButton(e) {
    const i = this.editor, s = new e(i.locale), r = i.commands.get("mediaEmbed"), o = this.editor.plugins.get("Dialog");
    return s.icon = PU, s.bind("isEnabled").to(r, "isEnabled"), s.on("execute", () => {
      o.id === "mediaEmbed" ? o.hide() : this._showDialog();
    }), s;
  }
  _showDialog() {
    const e = this.editor, i = e.plugins.get("Dialog"), s = e.commands.get("mediaEmbed"), r = e.locale.t;
    if (!this._formView) {
      const o = e.plugins.get(bc).registry;
      this._formView = new (no(IU))(OU(e.t, o), e.locale), this._formView.on("submit", () => this._handleSubmitForm());
    }
    i.show({
      id: "mediaEmbed",
      title: r("Insert media"),
      content: this._formView,
      isModal: !0,
      onShow: () => {
        this._formView.url = s.value || "", this._formView.resetFormStatus(), this._formView.urlInputView.fieldView.select();
      },
      actionButtons: [
        {
          label: r("Cancel"),
          withText: !0,
          onExecute: () => i.hide()
        },
        {
          label: r("Accept"),
          class: "ck-button-action",
          withText: !0,
          onExecute: () => this._handleSubmitForm()
        }
      ]
    });
  }
  _handleSubmitForm() {
    const e = this.editor, i = e.plugins.get("Dialog");
    this._formView.isValid() && (e.execute("mediaEmbed", this._formView.url), i.hide(), e.editing.view.focus());
  }
}
function OU(n, t) {
  return [
    (e) => {
      if (!e.url.length)
        return n("The URL must not be empty.");
    },
    (e) => {
      if (!t.hasMedia(e.url))
        return n("This media URL is not supported.");
    }
  ];
}
class VU extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      bc,
      RU,
      AU,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MediaEmbed";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const pw = {
  "(": ")",
  "[": "]",
  "{": "}"
};
class LU extends he {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this._isEnabledBasedOnSelection = !1;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document;
    this.isEnabled = t.schema.checkAttributeInSelection(e.selection, "mention");
  }
  /**
  * Executes the command.
  *
  * @param options Options for the executed command.
  * @param options.mention The mention object to insert. When a string is passed, it will be used to create a plain
  * object with the name attribute that equals the passed string.
  * @param options.marker The marker character (e.g. `'@'`).
  * @param options.text The text of the inserted mention. Defaults to the full mention string composed from `marker` and
  * `mention` string or `mention.id` if an object is passed.
  * @param options.range The range to replace.
  * Note that the replaced range might be shorter than the inserted text with the mention attribute.
  * @fires execute
  */
  execute(t) {
    const e = this.editor.model, s = e.document.selection, r = typeof t.mention == "string" ? {
      id: t.mention
    } : t.mention, o = r.id, a = t.range || s.getFirstRange();
    if (!e.canEditAt(a))
      return;
    const l = t.text || o, c = OC({
      _text: l,
      id: o
    }, r);
    if (!o.startsWith(t.marker))
      throw new S("mentioncommand-incorrect-id", this);
    e.change((u) => {
      const d = Ji(s.getAttributes()), h = new Map(d.entries());
      h.set("mention", c);
      const m = e.insertContent(u.createText(l, h), a), g = m.start.nodeBefore, p = m.end.nodeAfter, w = p && p.is("$text") && p.data.startsWith(" ");
      let y = !1;
      if (g && p && g.is("$text") && p.is("$text")) {
        const k = g.data.slice(-1), C = k in pw, E = C && p.data.startsWith(pw[k]);
        y = C && E;
      }
      !y && !w && e.insertContent(u.createText(" ", d), a.start.getShiftedBy(l.length));
    });
  }
}
class BU extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MentionEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model, i = e.document;
    e.schema.extend("$text", {
      allowAttributes: "mention"
    }), t.conversion.for("upcast").elementToAttribute({
      view: {
        name: "span",
        attributes: "data-mention",
        classes: "mention"
      },
      model: {
        key: "mention",
        value: (s) => VC(s)
      }
    }), t.conversion.for("downcast").attributeToElement({
      model: "mention",
      view: NU
    }), t.conversion.for("downcast").add(MU), i.registerPostFixer((s) => $U(s, i, e.schema)), i.registerPostFixer((s) => zU(s, i)), i.registerPostFixer((s) => FU(s, i)), t.commands.add("mention", new LU(t));
  }
}
function OC(n, t) {
  return Object.assign({
    uid: Bt()
  }, n, t || {});
}
function VC(n, t) {
  const e = n.getAttribute("data-mention"), i = n.getChild(0);
  if (!i)
    return;
  const s = {
    id: e,
    _text: i.data
  };
  return OC(s, t);
}
function MU(n) {
  n.on("attribute:mention", (t, e, i) => {
    const s = e.attributeNewValue;
    if (!e.item.is("$textProxy") || !s)
      return;
    const r = e.range.start;
    (r.textNode || r.nodeAfter).data != s._text && i.consumable.consume(e.item, t.name);
  }, {
    priority: "highest"
  });
}
function NU(n, { writer: t }) {
  if (!n)
    return;
  const e = {
    class: "mention",
    "data-mention": n.id
  }, i = {
    id: n.uid,
    priority: 20
  };
  return t.createAttributeElement("span", e, i);
}
function FU(n, t) {
  const e = t.selection, i = e.focus;
  return e.isCollapsed && e.hasAttribute("mention") && DU(i) ? (n.removeSelectionAttribute("mention"), !0) : !1;
}
function DU(n) {
  const t = n.isAtStart;
  return n.nodeBefore && n.nodeBefore.is("$text") || t;
}
function $U(n, t, e) {
  const i = t.differ.getChanges();
  let s = !1;
  for (const r of i) {
    if (r.type == "attribute")
      continue;
    const o = r.position;
    if (r.name == "$text") {
      const a = o.textNode && o.textNode.nextSibling;
      s = Es(o.textNode, n) || s, s = Es(a, n) || s, s = Es(o.nodeBefore, n) || s, s = Es(o.nodeAfter, n) || s;
    }
    if (r.name != "$text" && r.type == "insert") {
      const a = o.nodeAfter;
      for (const l of n.createRangeIn(a).getItems())
        s = Es(l, n) || s;
    }
    if (r.type == "insert" && e.isInline(r.name)) {
      const a = o.nodeAfter && o.nodeAfter.nextSibling;
      s = Es(o.nodeBefore, n) || s, s = Es(a, n) || s;
    }
  }
  return s;
}
function zU(n, t) {
  const e = t.differ.getChanges();
  let i = !1;
  for (const s of e)
    if (s.type === "attribute" && s.attributeKey != "mention") {
      const r = s.range.start.nodeBefore, o = s.range.end.nodeAfter;
      for (const a of [
        r,
        o
      ])
        LC(a) && a.getAttribute(s.attributeKey) != s.attributeNewValue && (n.setAttribute(s.attributeKey, s.attributeNewValue, a), i = !0);
    }
  return i;
}
function LC(n) {
  if (!n || !(n.is("$text") || n.is("$textProxy")) || !n.hasAttribute("mention"))
    return !1;
  const t = n.data, i = n.getAttribute("mention")._text;
  return t != i;
}
function Es(n, t) {
  return LC(n) ? (t.removeAttribute("mention", n), !0) : !1;
}
class HU extends Qc {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    f(this, "selected");
    f(this, "position");
    this.extendTemplate({
      attributes: {
        class: [
          "ck-mentions"
        ],
        tabindex: "-1"
      }
    });
  }
  /**
  * {@link #select Selects} the first item.
  */
  selectFirst() {
    this.select(0);
  }
  /**
  * Selects next item to the currently {@link #select selected}.
  *
  * If the last item is already selected, it will select the first item.
  */
  selectNext() {
    const e = this.selected, i = this.items.getIndex(e);
    this.select(i + 1);
  }
  /**
  * Selects previous item to the currently {@link #select selected}.
  *
  * If the first item is already selected, it will select the last item.
  */
  selectPrevious() {
    const e = this.selected, i = this.items.getIndex(e);
    this.select(i - 1);
  }
  /**
  * Marks item at a given index as selected.
  *
  * Handles selection cycling when passed index is out of bounds:
  * - if the index is lower than 0, it will select the last item,
  * - if the index is higher than the last item index, it will select the first item.
  *
  * @param index Index of an item to be marked as selected.
  */
  select(e) {
    let i = 0;
    e > 0 && e < this.items.length ? i = e : e < 0 && (i = this.items.length - 1);
    const s = this.items.get(i);
    this.selected !== s && (this.selected && this.selected.removeHighlight(), s.highlight(), this.selected = s, this._isItemVisibleInScrolledArea(s) || (this.element.scrollTop = s.element.offsetTop));
  }
  /**
  * Triggers the `execute` event on the {@link #select selected} item.
  */
  executeSelected() {
    this.selected.fire("execute");
  }
  /**
  * Checks if an item is visible in the scrollable area.
  *
  * The item is considered visible when:
  * - its top boundary is inside the scrollable rect
  * - its bottom boundary is inside the scrollable rect (the whole item must be visible)
  */
  _isItemVisibleInScrolledArea(e) {
    return new ke(this.element).contains(new ke(e.element));
  }
}
class jU extends ee {
  /**
  * Creates an instance of {@link module:mention/ui/domwrapperview~DomWrapperView} class.
  *
  * Also see {@link #render}.
  */
  constructor(e, i) {
    super(e);
    /**
    * The DOM element for which wrapper was created.
    */
    f(this, "domElement");
    this.template = void 0, this.domElement = i, this.domElement.classList.add("ck-button"), this.set("isOn", !1), this.on("change:isOn", (s, r, o) => {
      o ? (this.domElement.classList.add("ck-on"), this.domElement.classList.remove("ck-off")) : (this.domElement.classList.add("ck-off"), this.domElement.classList.remove("ck-on"));
    }), this.listenTo(this.domElement, "click", () => {
      this.fire("execute");
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.element = this.domElement;
  }
  /**
  * Focuses the DOM element.
  */
  focus() {
    this.domElement.focus();
  }
}
class UU extends Wn {
  constructor() {
    super(...arguments);
    f(this, "item");
    f(this, "marker");
  }
  highlight() {
    const e = this.children.first;
    e.isOn = !0;
  }
  removeHighlight() {
    const e = this.children.first;
    e.isOn = !1;
  }
}
const yl = 3, WU = [
  Ie.arrowup,
  Ie.arrowdown,
  Ie.esc
], qU = [
  Ie.enter,
  Ie.tab
];
class GU extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The mention view.
    */
    f(this, "_mentionsView");
    /**
    * Stores mention feeds configurations.
    */
    f(this, "_mentionsConfigurations");
    /**
    * The contextual balloon plugin instance.
    */
    f(this, "_balloon");
    f(this, "_items", new bt());
    f(this, "_lastRequested");
    /**
    * Debounced feed requester. It uses `lodash#debounce` method to delay function call.
    */
    f(this, "_requestFeedDebounced");
    this._mentionsView = this._createMentionView(), this._mentionsConfigurations = /* @__PURE__ */ new Map(), this._requestFeedDebounced = vi(this._requestFeed, 100), e.config.define("mention", {
      feeds: []
    });
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "MentionUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ai
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.config.get("mention.commitKeys") || qU, s = WU.concat(i);
    this._balloon = e.plugins.get(ai), e.editing.view.document.on("keydown", (a, l) => {
      o(l.keyCode) && this._isUIVisible && (l.preventDefault(), a.stop(), l.keyCode == Ie.arrowdown && this._mentionsView.selectNext(), l.keyCode == Ie.arrowup && this._mentionsView.selectPrevious(), i.includes(l.keyCode) && this._mentionsView.executeSelected(), l.keyCode == Ie.esc && this._hideUIAndRemoveMarker());
    }, {
      priority: "highest"
    }), Xn({
      emitter: this._mentionsView,
      activator: () => this._isUIVisible,
      contextElements: () => [
        this._balloon.view.element
      ],
      callback: () => this._hideUIAndRemoveMarker()
    });
    const r = e.config.get("mention.feeds");
    for (const a of r) {
      const { feed: l, marker: c, dropdownLimit: u } = a;
      if (!eW(c))
        throw new S("mentionconfig-incorrect-marker", null, {
          marker: c
        });
      const d = typeof l == "function" ? l.bind(this.editor) : JU(l), h = a.itemRenderer, m = {
        marker: c,
        feedCallback: d,
        itemRenderer: h,
        dropdownLimit: u
      };
      this._mentionsConfigurations.set(c, m);
    }
    this._setupTextWatcher(r), this.listenTo(e, "change:isReadOnly", () => {
      this._hideUIAndRemoveMarker();
    }), this.on("requestFeed:response", (a, l) => this._handleFeedResponse(l)), this.on("requestFeed:error", () => this._hideUIAndRemoveMarker());
    function o(a) {
      return s.includes(a);
    }
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this._mentionsView.destroy();
  }
  /**
  * Returns true when {@link #_mentionsView} is in the {@link module:ui/panel/balloon/contextualballoon~ContextualBalloon} and it is
  * currently visible.
  */
  get _isUIVisible() {
    return this._balloon.visibleView === this._mentionsView;
  }
  /**
  * Creates the {@link #_mentionsView}.
  */
  _createMentionView() {
    const e = this.editor.locale, i = new HU(e);
    return i.items.bindTo(this._items).using((s) => {
      const { item: r, marker: o } = s, { dropdownLimit: a } = this._mentionsConfigurations.get(o), l = a || this.editor.config.get("mention.dropdownLimit") || 10;
      if (i.items.length >= l)
        return null;
      const c = new UU(e), u = this._renderItem(r, o);
      return u.delegate("execute").to(c), c.children.add(u), c.item = r, c.marker = o, c.on("execute", () => {
        i.fire("execute", {
          item: r,
          marker: o
        });
      }), c;
    }), i.on("execute", (s, r) => {
      const o = this.editor, a = o.model, l = r.item, c = r.marker, u = o.model.markers.get("mention"), d = a.createPositionAt(a.document.selection.focus), h = a.createPositionAt(u.getStart()), m = a.createRange(h, d);
      this._hideUIAndRemoveMarker(), o.execute("mention", {
        mention: l,
        text: l.text,
        marker: c,
        range: m
      }), o.editing.view.focus();
    }), i;
  }
  /**
  * Returns item renderer for the marker.
  */
  _getItemRenderer(e) {
    const { itemRenderer: i } = this._mentionsConfigurations.get(e);
    return i;
  }
  /**
  * Requests a feed from a configured callbacks.
  */
  _requestFeed(e, i) {
    this._lastRequested = i;
    const { feedCallback: s } = this._mentionsConfigurations.get(e), r = s(i);
    if (!(r instanceof Promise)) {
      this.fire("requestFeed:response", {
        feed: r,
        marker: e,
        feedText: i
      });
      return;
    }
    r.then((a) => {
      this._lastRequested == i ? this.fire("requestFeed:response", {
        feed: a,
        marker: e,
        feedText: i
      }) : this.fire("requestFeed:discarded", {
        feed: a,
        marker: e,
        feedText: i
      });
    }).catch((a) => {
      this.fire("requestFeed:error", {
        error: a
      }), Ve("mention-feed-callback-error", {
        marker: e
      });
    });
  }
  /**
  * Registers a text watcher for the marker.
  */
  _setupTextWatcher(e) {
    const i = this.editor, s = e.map((a) => ({
      ...a,
      pattern: MC(a.marker, a.minimumCharacters || 0)
    })), r = new Gf(i.model, ZU(s));
    r.on("matched", (a, l) => {
      const c = BC(s, l.text), d = i.model.document.selection.focus, h = i.model.createPositionAt(d.parent, c.position);
      if (XU(d) || QU(h)) {
        this._hideUIAndRemoveMarker();
        return;
      }
      const m = YU(c, l.text), g = c.marker.length + m.length, p = d.getShiftedBy(-g), w = d.getShiftedBy(-m.length), y = i.model.createRange(p, w);
      if (Id(i)) {
        const k = i.model.markers.get("mention");
        i.model.change((C) => {
          C.updateMarker(k, {
            range: y
          });
        });
      } else
        i.model.change((k) => {
          k.addMarker("mention", {
            range: y,
            usingOperation: !1,
            affectsData: !1
          });
        });
      this._requestFeedDebounced(c.marker, m);
    }), r.on("unmatched", () => {
      this._hideUIAndRemoveMarker();
    });
    const o = i.commands.get("mention");
    return r.bind("isEnabled").to(o), r;
  }
  /**
  * Handles the feed response event data.
  */
  _handleFeedResponse(e) {
    const { feed: i, marker: s } = e;
    if (!Id(this.editor))
      return;
    this._items.clear();
    for (const o of i) {
      const a = typeof o != "object" ? {
        id: o,
        text: o
      } : o;
      this._items.add({
        item: a,
        marker: s
      });
    }
    const r = this.editor.model.markers.get("mention");
    this._items.length ? this._showOrUpdateUI(r) : this._hideUIAndRemoveMarker();
  }
  /**
  * Shows the mentions balloon. If the panel is already visible, it will reposition it.
  */
  _showOrUpdateUI(e) {
    this._isUIVisible ? this._balloon.updatePosition(this._getBalloonPanelPositionData(e, this._mentionsView.position)) : this._balloon.add({
      view: this._mentionsView,
      position: this._getBalloonPanelPositionData(e, this._mentionsView.position),
      singleViewMode: !0
    }), this._mentionsView.position = this._balloon.view.position, this._mentionsView.selectFirst();
  }
  /**
  * Hides the mentions balloon and removes the 'mention' marker from the markers collection.
  */
  _hideUIAndRemoveMarker() {
    this._balloon.hasView(this._mentionsView) && this._balloon.remove(this._mentionsView), Id(this.editor) && this.editor.model.change((e) => e.removeMarker("mention")), this._mentionsView.position = void 0;
  }
  /**
  * Renders a single item in the autocomplete list.
  */
  _renderItem(e, i) {
    const s = this.editor;
    let r, o = e.id;
    const a = this._getItemRenderer(i);
    if (a) {
      const l = a(e);
      typeof l != "string" ? r = new jU(s.locale, l) : o = l;
    }
    if (!r) {
      const l = new le(s.locale);
      l.label = o, l.withText = !0, r = l;
    }
    return r;
  }
  /**
  * Creates a position options object used to position the balloon panel.
  *
  * @param mentionMarker
  * @param preferredPosition The name of the last matched position name.
  */
  _getBalloonPanelPositionData(e, i) {
    const s = this.editor, r = s.editing, o = r.view.domConverter, a = r.mapper, l = s.locale.uiLanguageDirection;
    return {
      target: () => {
        let c = e.getRange();
        c.start.root.rootName == "$graveyard" && (c = s.model.document.selection.getFirstRange());
        const u = a.toViewRange(c);
        return ke.getDomRangeRects(o.viewRangeToDom(u)).pop();
      },
      limiter: () => {
        const c = this.editor.editing.view, d = c.document.selection.editableElement;
        return d ? c.domConverter.mapViewToDom(d.root) : null;
      },
      positions: KU(i, l)
    };
  }
}
function KU(n, t) {
  const e = {
    // Positions the panel to the southeast of the caret rectangle.
    caret_se: (i) => ({
      top: i.bottom + yl,
      left: i.right,
      name: "caret_se",
      config: {
        withArrow: !1
      }
    }),
    // Positions the panel to the northeast of the caret rectangle.
    caret_ne: (i, s) => ({
      top: i.top - s.height - yl,
      left: i.right,
      name: "caret_ne",
      config: {
        withArrow: !1
      }
    }),
    // Positions the panel to the southwest of the caret rectangle.
    caret_sw: (i, s) => ({
      top: i.bottom + yl,
      left: i.right - s.width,
      name: "caret_sw",
      config: {
        withArrow: !1
      }
    }),
    // Positions the panel to the northwest of the caret rect.
    caret_nw: (i, s) => ({
      top: i.top - s.height - yl,
      left: i.right - s.width,
      name: "caret_nw",
      config: {
        withArrow: !1
      }
    })
  };
  return Object.prototype.hasOwnProperty.call(e, n) ? [
    e[n]
  ] : t !== "rtl" ? [
    e.caret_se,
    e.caret_sw,
    e.caret_ne,
    e.caret_nw
  ] : [
    e.caret_sw,
    e.caret_se,
    e.caret_nw,
    e.caret_ne
  ];
}
function BC(n, t) {
  let e;
  for (const i of n) {
    const s = t.lastIndexOf(i.marker);
    s > 0 && !t.substring(s - 1).match(i.pattern) || (!e || s >= e.position) && (e = {
      marker: i.marker,
      position: s,
      minimumCharacters: i.minimumCharacters,
      pattern: i.pattern
    });
  }
  return e;
}
function MC(n, t) {
  const e = t == 0 ? "*" : `{${t},}`, i = be.features.isRegExpUnicodePropertySupported ? `\\p{Ps}\\p{Pi}"'` : `\\(\\[{"'`, s = ".";
  n = n.replace(/[.*+?^${}()\-|[\]\\]/g, "\\$&");
  const r = `(?:^|[ ${i}])(${n})(${s}${e})$`;
  return new RegExp(r, "u");
}
function ZU(n) {
  return (e) => {
    const i = BC(n, e);
    if (!i)
      return !1;
    let s = 0;
    i.position !== 0 && (s = i.position - 1);
    const r = e.substring(s);
    return i.pattern.test(r);
  };
}
function YU(n, t) {
  let e = 0;
  n.position !== 0 && (e = n.position - 1);
  const i = MC(n.marker, 0);
  return t.substring(e).match(i)[2];
}
function JU(n) {
  return (t) => n.filter((i) => (typeof i == "string" ? i : String(i.id)).toLowerCase().includes(t.toLowerCase()));
}
function XU(n) {
  const t = n.textNode && n.textNode.hasAttribute("mention"), e = n.nodeBefore;
  return t || e && e.is("$text") && e.hasAttribute("mention");
}
function QU(n) {
  const t = n.nodeAfter;
  return t && t.is("$text") && t.hasAttribute("mention");
}
function eW(n) {
  return !!n;
}
function Id(n) {
  return n.model.markers.has("mention");
}
class tW extends I {
  toMentionAttribute(t, e) {
    return VC(t, e);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Mention";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      BU,
      GU
    ];
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class iW extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.schema, i = t.document.selection;
    this.isEnabled = nW(i, e, t);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  */
  execute() {
    const t = this.editor.model;
    t.change((e) => {
      const i = e.createElement("pageBreak");
      t.insertObject(i, null, null, {
        setSelection: "after"
      });
    });
  }
}
function nW(n, t, e) {
  const i = sW(n, e);
  return t.checkChild(i, "pageBreak");
}
function sW(n, t) {
  const i = Da(n, t).start.parent;
  return i.isEmpty && !i.is("element", "$root") ? i.parent : i;
}
class rW extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PageBreakEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model.schema, i = t.t, s = t.conversion;
    e.register("pageBreak", {
      inheritAllFrom: "$blockObject"
    }), s.for("dataDowncast").elementToStructure({
      model: "pageBreak",
      view: (r, { writer: o }) => o.createContainerElement(
        "div",
        {
          class: "page-break",
          // If user has no `.ck-content` styles, it should always break a page during print.
          style: "page-break-after: always"
        },
        // For a rationale of using span inside a div see:
        // https://github.com/ckeditor/ckeditor5-page-break/pull/1#discussion_r328934062.
        o.createContainerElement("span", {
          style: "display: none"
        })
      )
    }), s.for("editingDowncast").elementToStructure({
      model: "pageBreak",
      view: (r, { writer: o }) => {
        const a = i("Page break"), l = o.createContainerElement("div"), c = o.createRawElement("span", {
          class: "page-break__label"
        }, function(u) {
          u.innerText = i("Page break");
        });
        return o.addClass("page-break", l), o.insert(o.createPositionAt(l, 0), c), oW(l, o, a);
      }
    }), s.for("upcast").elementToElement({
      view: (r) => {
        const o = r.getStyle("page-break-before") == "always", a = r.getStyle("page-break-after") == "always";
        if (!o && !a)
          return null;
        if (r.childCount == 1) {
          const l = r.getChild(0);
          if (!l.is("element", "span") || l.getStyle("display") != "none")
            return null;
        } else if (r.childCount > 1)
          return null;
        return {
          name: !0
        };
      },
      model: "pageBreak",
      // This conversion must be checked before <br> conversion because some editors use
      // <br style="page-break-before:always"> as a page break marker.
      converterPriority: "high"
    }), t.commands.add("pageBreak", new iW(t));
  }
}
function oW(n, t, e) {
  return t.setCustomProperty("pageBreak", !0, n), Qn(n, t, {
    label: e
  });
}
var aW = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M3.598.687h1.5v5h-1.5zm14.5 0h1.5v5h-1.5z"/><path d="M19.598 4.187v1.5h-16v-1.5zm-16 14.569h1.5v-5h-1.5zm14.5 0h1.5v-5h-1.5z"/><path d="M19.598 15.256v-1.5h-16v1.5zM5.081 9h6v2h-6zm8 0h6v2h-6zm-9.483 1L0 12.5v-5z"/></svg>';
class lW extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PageBreakUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add("pageBreak", () => {
      const e = this._createButton(le);
      return e.set({
        tooltip: !0
      }), e;
    }), t.ui.componentFactory.add("menuBar:pageBreak", () => this._createButton(Fe));
  }
  /**
  * Creates a button for page break command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, i = e.locale, s = e.commands.get("pageBreak"), r = new t(e.locale), o = i.t;
    return r.set({
      label: o("Page break"),
      icon: aW
    }), r.bind("isEnabled").to(s, "isEnabled"), this.listenTo(r, "execute", () => {
      e.execute("pageBreak"), e.editing.view.focus();
    }), r;
  }
}
class cW extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      rW,
      lW,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PageBreak";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function uW(n, t) {
  const e = [];
  for (const i of t.createRangeIn(n)) {
    const s = i.item;
    s.is("element", "a") && !s.hasAttribute("href") && (s.hasAttribute("id") || s.hasAttribute("name")) && e.push(s);
  }
  for (const i of e) {
    const s = i.parent.getChildIndex(i) + 1, r = i.getChildren();
    t.insertChild(s, r, i.parent);
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function dW(n) {
  const t = parseFloat(n);
  return n.endsWith("pt") ? Sr(t * 96 / 72) : n.endsWith("pc") ? Sr(t * 12 * 96 / 72) : n.endsWith("in") ? Sr(t * 96) : n.endsWith("cm") ? Sr(t * 96 / 2.54) : n.endsWith("mm") ? Sr(t / 10 * 96 / 2.54) : n;
}
function bw(n) {
  return n !== void 0 && n.endsWith("px");
}
function Sr(n) {
  return n.toFixed(2).replace(/\.?0+$/, "") + "px";
}
function hW(n, t, e) {
  if (!n.childCount)
    return;
  const i = new Pi(n.document), s = mW(n, i);
  if (!s.length)
    return;
  const r = {}, o = [];
  for (const a of s)
    if (a.indent !== void 0) {
      gW(a) || (o.length = 0);
      const l = `${a.id}:${a.indent}`, c = Math.min(a.indent - 1, o.length);
      if (c < o.length && o[c].id !== a.id && (o.length = c), c < o.length - 1)
        o.length = c + 1;
      else {
        const d = pW(a, t);
        if (c > o.length - 1 || o[c].listElement.name != d.type) {
          c == 0 && d.type == "ol" && a.id !== void 0 && r[l] && (d.startIndex = r[l]);
          const h = _W(d, i, e);
          if (bw(a.marginLeft) && (c == 0 || bw(o[c - 1].marginLeft))) {
            let m = a.marginLeft;
            c > 0 && (m = Sr(parseFloat(m) - parseFloat(o[c - 1].marginLeft))), i.setStyle("padding-left", m, h);
          }
          if (o.length == 0) {
            const m = a.element.parent, g = m.getChildIndex(a.element) + 1;
            i.insertChild(g, h, m);
          } else {
            const m = o[c - 1].listItemElements;
            i.appendChild(h, m[m.length - 1]);
          }
          o[c] = {
            ...a,
            listElement: h,
            listItemElements: []
          }, c == 0 && a.id !== void 0 && (r[l] = d.startIndex || 1);
        }
      }
      const u = a.element.name == "li" ? a.element : i.createElement("li");
      i.appendChild(u, o[c].listElement), o[c].listItemElements.push(u), c == 0 && a.id !== void 0 && r[l]++, a.element != u && i.appendChild(a.element, u), kW(a.element, i), i.removeStyle("text-indent", a.element), i.removeStyle("margin-left", a.element);
    } else {
      const l = o.find((c) => c.marginLeft == a.marginLeft);
      if (l) {
        const c = l.listItemElements;
        i.appendChild(a.element, c[c.length - 1]), i.removeStyle("margin-left", a.element);
      } else
        o.length = 0;
    }
}
function fW(n, t) {
  for (const e of t.createRangeIn(n)) {
    const i = e.item;
    if (i.is("element", "li")) {
      const s = i.getChild(0);
      s && s.is("element", "p") && t.unwrapElement(s);
    }
  }
}
function mW(n, t) {
  const e = t.createRangeIn(n), i = [], s = /* @__PURE__ */ new Set();
  for (const r of e.getItems()) {
    if (!r.is("element") || !r.name.match(/^(p|h\d+|li|div)$/))
      continue;
    let o = CW(r);
    if (o !== void 0 && parseFloat(o) == 0 && !Array.from(r.getClassNames()).find((a) => a.startsWith("MsoList")) && (o = void 0), r.hasStyle("mso-list") || o !== void 0 && s.has(o)) {
      const a = yW(r);
      i.push({
        element: r,
        id: a.id,
        order: a.order,
        indent: a.indent,
        marginLeft: o
      }), o !== void 0 && s.add(o);
    } else
      s.clear();
  }
  return i;
}
function gW(n) {
  const t = n.element.previousSibling;
  return ww(t || n.element.parent);
}
function ww(n) {
  return n.is("element", "ol") || n.is("element", "ul");
}
function pW(n, t) {
  const e = new RegExp(`@list l${n.id}:level${n.indent}\\s*({[^}]*)`, "gi"), i = /mso-level-number-format:([^;]{0,100});/gi, s = /mso-level-start-at:\s{0,100}([0-9]{0,10})\s{0,100};/gi, r = new RegExp(`@list\\s+l${n.id}:level\\d\\s*{[^{]*mso-level-text:"%\\d\\\\.`, "gi"), o = new RegExp(`@list l${n.id}:level\\d\\s*{[^{]*mso-level-number-format:`, "gi"), a = r.exec(t), l = o.exec(t), c = a && !l, u = e.exec(t);
  let d = "decimal", h = "ol", m = null;
  if (u && u[1]) {
    const g = i.exec(u[1]);
    if (g && g[1] && (d = g[1].trim(), h = d !== "bullet" && d !== "image" ? "ol" : "ul"), d === "bullet") {
      const p = bW(n.element);
      p && (d = p);
    } else {
      const p = s.exec(u[1]);
      p && p[1] && (m = parseInt(p[1]));
    }
    c && (h = "ol");
  }
  return {
    type: h,
    startIndex: m,
    style: vW(d),
    isLegalStyleList: c
  };
}
function bW(n) {
  if (n.name == "li" && n.parent.name == "ul" && n.parent.hasAttribute("type"))
    return n.parent.getAttribute("type");
  const t = wW(n);
  if (!t)
    return null;
  const e = t._data;
  return e === "o" ? "circle" : e === "·" ? "disc" : e === "§" ? "square" : null;
}
function wW(n) {
  if (n.getChild(0).is("$text"))
    return null;
  for (const t of n.getChildren()) {
    if (!t.is("element", "span"))
      continue;
    const e = t.getChild(0);
    if (e)
      return e.is("$text") ? e : e.getChild(0);
  }
  /* istanbul ignore next -- @preserve */
  return null;
}
function vW(n) {
  if (n.startsWith("arabic-leading-zero"))
    return "decimal-leading-zero";
  switch (n) {
    case "alpha-upper":
      return "upper-alpha";
    case "alpha-lower":
      return "lower-alpha";
    case "roman-upper":
      return "upper-roman";
    case "roman-lower":
      return "lower-roman";
    case "circle":
    case "disc":
    case "square":
      return n;
    default:
      return null;
  }
}
function _W(n, t, e) {
  const i = t.createElement(n.type);
  return n.style && t.setStyle("list-style-type", n.style, i), n.startIndex && n.startIndex > 1 && t.setAttribute("start", n.startIndex, i), n.isLegalStyleList && e && t.addClass("legal-list", i), i;
}
function yW(n) {
  const t = n.getStyle("mso-list");
  if (t === void 0)
    return {};
  const e = t.match(/(^|\s{1,100})l(\d+)/i), i = t.match(/\s{0,100}lfo(\d+)/i), s = t.match(/\s{0,100}level(\d+)/i);
  return e && i && s ? {
    id: e[2],
    order: i[1],
    indent: parseInt(s[1])
  } : {
    indent: 1
    // Handle empty mso-list style as a marked for default list item.
  };
}
function kW(n, t) {
  const e = new oi({
    name: "span",
    styles: {
      "mso-list": "Ignore"
    }
  }), i = t.createRangeIn(n);
  for (const s of i)
    s.type === "elementStart" && e.match(s.item) && t.remove(s.item);
}
function CW(n) {
  const t = n.getStyle("margin-left");
  return t === void 0 || t.endsWith("px") ? t : dW(t);
}
function xW(n, t) {
  if (!n.childCount)
    return;
  const e = new Pi(n.document), i = EW(n, e);
  SW(i, n, e), IW(i, n, e), AW(n, e);
  const s = PW(n, e);
  s.length && OW(s, RW(t), e);
}
function TW(n) {
  return btoa(n.match(/\w{2}/g).map((t) => String.fromCharCode(parseInt(t, 16))).join(""));
}
function EW(n, t) {
  const e = t.createRangeIn(n), i = new oi({
    name: /v:(.+)/
  }), s = [];
  for (const r of e) {
    if (r.type != "elementStart")
      continue;
    const o = r.item, a = o.previousSibling, l = a && a.is("element") ? a.name : null, c = [
      "Chart"
    ], u = i.match(o), d = o.getAttribute("o:gfxdata"), h = l === "v:shapetype", m = d && c.some((g) => o.getAttribute("id").includes(g));
    u && d && !h && !m && s.push(r.item.getAttribute("id"));
  }
  return s;
}
function SW(n, t, e) {
  const i = e.createRangeIn(t), s = new oi({
    name: "img"
  }), r = [];
  for (const o of i)
    if (o.item.is("element") && s.match(o.item)) {
      const a = o.item, l = a.getAttribute("v:shapes") ? a.getAttribute("v:shapes").split(" ") : [];
      l.length && l.every((c) => n.indexOf(c) > -1) ? r.push(a) : a.getAttribute("src") || r.push(a);
    }
  for (const o of r)
    e.remove(o);
}
function AW(n, t) {
  const e = t.createRangeIn(n), i = new oi({
    name: /v:(.+)/
  }), s = [];
  for (const r of e)
    r.type == "elementStart" && i.match(r.item) && s.push(r.item);
  for (const r of s)
    t.remove(r);
}
function IW(n, t, e) {
  const i = e.createRangeIn(t), s = [];
  for (const a of i)
    if (a.type == "elementStart" && a.item.is("element", "v:shape")) {
      const l = a.item.getAttribute("id");
      if (n.includes(l))
        continue;
      r(a.item.parent.getChildren(), l) || s.push(a.item);
    }
  for (const a of s) {
    const l = {
      src: o(a)
    };
    a.hasAttribute("alt") && (l.alt = a.getAttribute("alt"));
    const c = e.createElement("img", l);
    e.insertChild(a.index + 1, c, a.parent);
  }
  function r(a, l) {
    for (const c of a) {
      /* istanbul ignore else -- @preserve */
      if (c.is("element") && (c.name == "img" && c.getAttribute("v:shapes") == l || r(c.getChildren(), l)))
        return !0;
    }
    return !1;
  }
  function o(a) {
    for (const l of a.getChildren()) {
      /* istanbul ignore else -- @preserve */
      if (l.is("element") && l.getAttribute("src"))
        return l.getAttribute("src");
    }
  }
}
function PW(n, t) {
  const e = t.createRangeIn(n), i = new oi({
    name: "img"
  }), s = [];
  for (const r of e)
    r.item.is("element") && i.match(r.item) && r.item.getAttribute("src").startsWith("file://") && s.push(r.item);
  return s;
}
function RW(n) {
  if (!n)
    return [];
  const t = /{\\pict[\s\S]+?\\bliptag-?\d+(\\blipupi-?\d+)?({\\\*\\blipuid\s?[\da-fA-F]+)?[\s}]*?/, e = new RegExp("(?:(" + t.source + "))([\\da-fA-F\\s]+)\\}", "g"), i = n.match(e), s = [];
  if (i)
    for (const r of i) {
      let o = !1;
      r.includes("\\pngblip") ? o = "image/png" : r.includes("\\jpegblip") && (o = "image/jpeg"), o && s.push({
        hex: r.replace(t, "").replace(/[^\da-fA-F]/g, ""),
        type: o
      });
    }
  return s;
}
function OW(n, t, e) {
  if (n.length === t.length)
    for (let i = 0; i < n.length; i++) {
      const s = `data:${t[i].type};base64,${TW(t[i].hex)}`;
      e.setAttribute("src", s, n[i]);
    }
}
function VW(n) {
  const t = [], e = new Pi(n.document);
  for (const { item: i } of e.createRangeIn(n))
    if (i.is("element")) {
      for (const s of i.getClassNames())
        /\bmso/gi.exec(s) && e.removeClass(s, i);
      for (const s of i.getStyleNames())
        /\bmso/gi.exec(s) && e.removeStyle(s, i);
      (i.is("element", "w:sdt") || i.is("element", "w:sdtpr") && i.isEmpty || i.is("element", "o:p") && i.isEmpty) && t.push(i);
    }
  for (const i of t) {
    const s = i.parent, r = s.getChildIndex(i);
    e.insertChild(r, i.getChildren(), s), e.remove(i);
  }
}
const LW = /<meta\s*name="?generator"?\s*content="?microsoft\s*word\s*\d+"?\/?>/i, BW = /xmlns:o="urn:schemas-microsoft-com/i;
class MW {
  /**
  * Creates a new `MSWordNormalizer` instance.
  *
  * @param document View document.
  */
  constructor(t, e = !1) {
    f(this, "document");
    f(this, "hasMultiLevelListPlugin");
    this.document = t, this.hasMultiLevelListPlugin = e;
  }
  /**
  * @inheritDoc
  */
  isActive(t) {
    return LW.test(t) || BW.test(t);
  }
  /**
  * @inheritDoc
  */
  execute(t) {
    const e = new Pi(this.document), { body: i, stylesString: s } = t._parsedData;
    uW(i, e), hW(i, s, this.hasMultiLevelListPlugin), xW(i, t.dataTransfer.getData("text/rtf")), VW(i), t.content = i;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function NW(n, t) {
  for (const e of n.getChildren())
    if (e.is("element", "b") && e.getStyle("font-weight") === "normal") {
      const i = n.getChildIndex(e);
      t.remove(e), t.insertChild(i, e.getChildren(), n);
    }
}
function FW(n, t) {
  const e = new Gc(t.document.stylesProcessor), i = new Zc(e, {
    renderingMode: "data"
  }), s = i.blockElements, r = i.inlineObjectElements, o = [];
  for (const a of t.createRangeIn(n)) {
    const l = a.item;
    if (l.is("element", "br")) {
      const c = vw(l, "forward", t, {
        blockElements: s,
        inlineObjectElements: r
      }), u = vw(l, "backward", t, {
        blockElements: s,
        inlineObjectElements: r
      }), d = _w(c, s);
      (_w(u, s) || d) && o.push(l);
    }
  }
  for (const a of o)
    a.hasClass("Apple-interchange-newline") ? t.remove(a) : t.replace(a, t.createElement("p"));
}
function vw(n, t, e, { blockElements: i, inlineObjectElements: s }) {
  let r = e.createPositionAt(n, t == "forward" ? "after" : "before");
  return r = r.getLastMatchingPosition(({ item: o }) => o.is("element") && !i.includes(o.name) && !s.includes(o.name), {
    direction: t
  }), t == "forward" ? r.nodeAfter : r.nodeBefore;
}
function _w(n, t) {
  return !!n && n.is("element") && t.includes(n.name);
}
const DW = /id=("|')docs-internal-guid-[-0-9a-f]+("|')/i;
class $W {
  /**
  * Creates a new `GoogleDocsNormalizer` instance.
  *
  * @param document View document.
  */
  constructor(t) {
    f(this, "document");
    this.document = t;
  }
  /**
  * @inheritDoc
  */
  isActive(t) {
    return DW.test(t);
  }
  /**
  * @inheritDoc
  */
  execute(t) {
    const e = new Pi(this.document), { body: i } = t._parsedData;
    NW(i, e), fW(i, e), FW(i, e), t.content = i;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function zW(n, t) {
  for (const e of n.getChildren())
    e.is("element", "table") && e.hasAttribute("xmlns") && t.removeAttribute("xmlns", e);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function HW(n, t) {
  for (const e of n.getChildren())
    if (e.is("element", "google-sheets-html-origin")) {
      const i = n.getChildIndex(e);
      t.remove(e), t.insertChild(i, e.getChildren(), n);
    }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function jW(n, t) {
  for (const e of n.getChildren())
    e.is("element", "table") && e.getStyle("width") === "0px" && t.removeStyle("width", e);
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function UW(n, t) {
  for (const e of Array.from(n.getChildren()))
    e.is("element", "style") && t.remove(e);
}
const WW = /<google-sheets-html-origin/i;
class qW {
  /**
  * Creates a new `GoogleSheetsNormalizer` instance.
  *
  * @param document View document.
  */
  constructor(t) {
    f(this, "document");
    this.document = t;
  }
  /**
  * @inheritDoc
  */
  isActive(t) {
    return WW.test(t);
  }
  /**
  * @inheritDoc
  */
  execute(t) {
    const e = new Pi(this.document), { body: i } = t._parsedData;
    HW(i, e), zW(i, e), jW(i, e), UW(i, e), t.content = i;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function GW(n) {
  return yw(yw(n)).replace(/(<span\s+style=['"]mso-spacerun:yes['"]>[^\S\r\n]*?)[\r\n]+([^\S\r\n]*<\/span>)/g, "$1$2").replace(/<span\s+style=['"]mso-spacerun:yes['"]><\/span>/g, "").replace(/(<span\s+style=['"]letter-spacing:[^'"]+?['"]>)[\r\n]+(<\/span>)/g, "$1 $2").replace(/ <\//g, " </").replace(/ <o:p><\/o:p>/g, " <o:p></o:p>").replace(/<o:p>(&nbsp;|\u00A0)<\/o:p>/g, "").replace(/>([^\S\r\n]*[\r\n]\s*)</g, "><");
}
function KW(n) {
  n.querySelectorAll("span[style*=spacerun]").forEach((t) => {
    const e = t, i = e.innerText.length || 0;
    e.innerText = Array(i + 1).join("  ").substr(0, i);
  });
}
function yw(n) {
  return n.replace(/<span(?: class="Apple-converted-space"|)>(\s+)<\/span>/g, (t, e) => e.length === 1 ? " " : Array(e.length + 1).join("  ").substr(0, e.length));
}
function ZW(n, t) {
  const e = new DOMParser();
  n = n.replace(/<!--\[if gte vml 1]>/g, ""), n = n.replace(/<o:SmartTagType(?:\s+[^\s>=]+(?:="[^"]*")?)*\s*\/?>/gi, "");
  const i = GW(XW(n)), s = e.parseFromString(i, "text/html");
  KW(s);
  const r = s.body.innerHTML, o = YW(s, t), a = JW(s);
  return {
    body: o,
    bodyString: r,
    styles: a.styles,
    stylesString: a.stylesString
  };
}
function YW(n, t) {
  const e = new Gc(t), i = new Zc(e, {
    renderingMode: "data"
  }), s = n.createDocumentFragment(), r = n.body.childNodes;
  for (; r.length > 0; )
    s.appendChild(r[0]);
  return i.domToView(s, {
    skipComments: !0
  });
}
function JW(n) {
  const t = [], e = [], i = Array.from(n.getElementsByTagName("style"));
  for (const s of i)
    s.sheet && s.sheet.cssRules && s.sheet.cssRules.length && (t.push(s.sheet), e.push(s.innerHTML));
  return {
    styles: t,
    stylesString: e.join(" ")
  };
}
function XW(n) {
  const t = "</body>", e = "</html>", i = n.indexOf(t);
  if (i < 0)
    return n;
  const s = n.indexOf(e, i + t.length);
  return n.substring(0, i + t.length) + (s >= 0 ? n.substring(s) : "");
}
class QW extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "PasteFromOffice";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      _i
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.plugins.get("ClipboardPipeline"), i = t.editing.view.document, s = [], r = this.editor.plugins.has("MultiLevelList");
    s.push(new MW(i, r)), s.push(new $W(i)), s.push(new qW(i)), e.on("inputTransformation", (o, a) => {
      if (a._isTransformedWithPasteFromOffice || t.model.document.selection.getFirstPosition().parent.is("element", "codeBlock"))
        return;
      const c = a.dataTransfer.getData("text/html"), u = s.find((d) => d.isActive(c));
      u && (a._parsedData || (a._parsedData = ZW(c, i.stylesProcessor)), u.execute(a), a._isTransformedWithPasteFromOffice = !0);
    }, {
      priority: "high"
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
var eq = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M8.69 14.915c.053.052.173.083.36.093a.366.366 0 0 1 .345.485l-.003.01a.738.738 0 0 1-.697.497h-2.67a.374.374 0 0 1-.353-.496l.013-.038a.681.681 0 0 1 .644-.458c.197-.012.325-.043.386-.093a.28.28 0 0 0 .072-.11L9.592 4.5H6.269c-.359-.017-.609.013-.75.09-.142.078-.289.265-.442.563-.192.29-.516.464-.864.464H4.17a.43.43 0 0 1-.407-.569L4.46 3h13.08l-.62 2.043a.81.81 0 0 1-.775.574h-.114a.486.486 0 0 1-.486-.486c.001-.284-.054-.464-.167-.54-.112-.076-.367-.106-.766-.091h-3.28l-2.68 10.257c-.006.074.007.127.038.158zM3 17h8a.5.5 0 1 1 0 1H3a.5.5 0 1 1 0-1zm11.299 1.17a.75.75 0 1 1-1.06-1.06l1.414-1.415-1.415-1.414a.75.75 0 0 1 1.06-1.06l1.415 1.414 1.414-1.415a.75.75 0 1 1 1.06 1.06l-1.413 1.415 1.414 1.415a.75.75 0 0 1-1.06 1.06l-1.415-1.414-1.414 1.414z"/></svg>';
const kl = "removeFormat";
class tq extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "RemoveFormatUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add(kl, () => {
      const e = this._createButton(le);
      return e.set({
        tooltip: !0
      }), e;
    }), t.ui.componentFactory.add(`menuBar:${kl}`, () => this._createButton(Fe));
  }
  /**
  * Creates a button for remove format command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, i = e.locale, s = e.commands.get(kl), r = new t(e.locale), o = i.t;
    return r.set({
      label: o("Remove Format"),
      icon: eq
    }), r.bind("isEnabled").to(s, "isEnabled"), this.listenTo(r, "execute", () => {
      e.execute(kl), e.editing.view.focus();
    }), r;
  }
}
class iq extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model;
    this.isEnabled = !!Xe(this._getFormattingItems(t.document.selection, t.schema));
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = t.schema;
    t.change((i) => {
      for (const s of this._getFormattingItems(t.document.selection, e))
        if (s.is("selection"))
          for (const r of this._getFormattingAttributes(s, e))
            i.removeSelectionAttribute(r);
        else {
          const r = i.createRangeOn(s);
          for (const o of this._getFormattingAttributes(s, e))
            i.removeAttribute(o, r);
        }
    });
  }
  /**
  * Returns an iterable of items in a selection (including the selection itself) that have formatting model
  * attributes to be removed by the feature.
  *
  * @param schema The schema describing the item.
  */
  *_getFormattingItems(t, e) {
    const i = (s) => !!Xe(this._getFormattingAttributes(s, e));
    for (const s of t.getRanges())
      for (const r of s.getItems())
        !e.isBlock(r) && i(r) && (yield r);
    for (const s of t.getSelectedBlocks())
      i(s) && (yield s);
    i(t) && (yield t);
  }
  /**
  * Returns an iterable of formatting attributes of a given model item.
  *
  * **Note:** Formatting items have the `isFormatting` property set to `true`.
  *
  * @param schema The schema describing the item.
  * @returns The names of formatting attributes found in a given item.
  */
  *_getFormattingAttributes(t, e) {
    for (const [i] of t.getAttributes()) {
      const s = e.getAttributeProperties(i);
      s && s.isFormatting && (yield i);
    }
  }
}
class nq extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "RemoveFormatEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.commands.add("removeFormat", new iq(t));
  }
}
class sq extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      nq,
      tq
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "RemoveFormat";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class rq extends he {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.affectsData = !1, this.value = !1;
  }
  /**
  * Toggles the visibility of content blocks.
  */
  execute() {
    const t = "ck-show-blocks", e = this.editor.editing.view;
    e.change((i) => {
      for (const s of e.document.roots)
        s.hasClass(t) ? (i.removeClass(t, s), this.value = !1) : (i.addClass(t, s), this.value = !0);
    });
  }
}
class oq extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ShowBlocksEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const { editor: t } = this;
    t.commands.add("showBlocks", new rq(t));
  }
}
var aq = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m6.395 9.196 2.545-.007V6.498a.598.598 0 0 1 .598-.598h.299a.598.598 0 0 1 .598.598v6.877a.598.598 0 0 1-.598.598h-.299a.598.598 0 0 1-.598-.598v-2.691l-2.545.007v2.691a.598.598 0 0 1-.598.598h-.299a.598.598 0 0 1-.598-.598V6.505a.598.598 0 0 1 .598-.598h.299a.598.598 0 0 1 .598.598v2.691Z"/><path d="M15.094 13.417V6.462a.562.562 0 0 0-.562-.562h-.782a1 1 0 0 0-.39.08l-1.017.43a.562.562 0 0 0-.343.517v.197c0 .4.406.67.775.519l.819-.337v6.111c0 .31.251.562.561.562h.377c.31 0 .562-.251.562-.562Z"/><path d="M0 15.417v1.5h1.5v-1.5H0Z"/><path d="M18.5 15.417v1.5H20v-1.5h-1.5Z"/><path d="M18.5 12.333v1.5H20v-1.5h-1.5Z"/><path d="M18.5 9.25v1.5H20v-1.5h-1.5Z"/><path d="M18.5 6.167v1.5H20v-1.5h-1.5Z"/><path d="M0 18.5v.5a1 1 0 0 0 1 1h.5v-1.5H0Z"/><path d="M3.083 18.5V20h1.5v-1.5h-1.5Z"/><path d="M6.167 18.5V20h1.5v-1.5h-1.5Z"/><path d="M9.25 18.5V20h1.5v-1.5h-1.5Z"/><path d="M12.333 18.5V20h1.5v-1.5h-1.5Z"/><path d="M15.417 18.5V20h1.5v-1.5h-1.5Z"/><path d="M18.5 18.5V20h.5a1 1 0 0 0 1-1v-.5h-1.5Z"/><path clip-rule="evenodd" d="M0 1a1 1 0 0 1 1-1h18a1 1 0 0 1 1 1v3.583h-1.5V1.5h-17v12.333H0V1Z"/></svg>';
class lq extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ShowBlocksUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor;
    t.ui.componentFactory.add("showBlocks", () => {
      const e = this._createButton(le);
      return e.set({
        tooltip: !0,
        icon: aq
      }), e;
    }), t.ui.componentFactory.add("menuBar:showBlocks", () => this._createButton(Fe));
  }
  /**
  * Creates a button for show blocks command to use either in toolbar or in menu bar.
  */
  _createButton(t) {
    const e = this.editor, i = e.locale, s = e.commands.get("showBlocks"), r = new t(i), o = i.t;
    return r.set({
      label: o("Show blocks"),
      isToggleable: !0,
      role: "menuitemcheckbox"
    }), r.bind("isEnabled").to(s), r.bind("isOn").to(s, "value", s, "isEnabled", (a, l) => a && l), this.listenTo(r, "execute", () => {
      e.execute("showBlocks"), e.editing.view.focus();
    }), r;
  }
}
class cq extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ShowBlocks";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      oq,
      lq
    ];
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function uq(n) {
  const t = [
    {
      name: "address",
      isVoid: !1
    },
    {
      name: "article",
      isVoid: !1
    },
    {
      name: "aside",
      isVoid: !1
    },
    {
      name: "blockquote",
      isVoid: !1
    },
    {
      name: "details",
      isVoid: !1
    },
    {
      name: "dialog",
      isVoid: !1
    },
    {
      name: "dd",
      isVoid: !1
    },
    {
      name: "div",
      isVoid: !1
    },
    {
      name: "dl",
      isVoid: !1
    },
    {
      name: "dt",
      isVoid: !1
    },
    {
      name: "fieldset",
      isVoid: !1
    },
    {
      name: "figcaption",
      isVoid: !1
    },
    {
      name: "figure",
      isVoid: !1
    },
    {
      name: "footer",
      isVoid: !1
    },
    {
      name: "form",
      isVoid: !1
    },
    {
      name: "h1",
      isVoid: !1
    },
    {
      name: "h2",
      isVoid: !1
    },
    {
      name: "h3",
      isVoid: !1
    },
    {
      name: "h4",
      isVoid: !1
    },
    {
      name: "h5",
      isVoid: !1
    },
    {
      name: "h6",
      isVoid: !1
    },
    {
      name: "header",
      isVoid: !1
    },
    {
      name: "hgroup",
      isVoid: !1
    },
    {
      name: "hr",
      isVoid: !0
    },
    {
      name: "li",
      isVoid: !1
    },
    {
      name: "main",
      isVoid: !1
    },
    {
      name: "nav",
      isVoid: !1
    },
    {
      name: "ol",
      isVoid: !1
    },
    {
      name: "p",
      isVoid: !1
    },
    {
      name: "section",
      isVoid: !1
    },
    {
      name: "table",
      isVoid: !1
    },
    {
      name: "tbody",
      isVoid: !1
    },
    {
      name: "td",
      isVoid: !1
    },
    {
      name: "th",
      isVoid: !1
    },
    {
      name: "thead",
      isVoid: !1
    },
    {
      name: "tr",
      isVoid: !1
    },
    {
      name: "ul",
      isVoid: !1
    }
  ], e = t.map((o) => o.name).join("|"), i = n.replace(new RegExp(`</?(${e})( .*?)?>`, "g"), `
$&
`).replace(/<br[^>]*>/g, `$&
`).split(`
`);
  let s = 0, r = !1;
  return i.filter((o) => o.length).map((o) => (r = fq(o, r), dq(o, t) ? Pd(o, s++) : hq(o, t) ? Pd(o, --s) : r === "middle" || r === "last" ? o : Pd(o, s))).join(`
`);
}
function dq(n, t) {
  return t.some((e) => !(e.isVoid || !new RegExp(`<${e.name}( .*?)?>`).test(n)));
}
function hq(n, t) {
  return t.some((e) => new RegExp(`</${e.name}>`).test(n));
}
function Pd(n, t, e = "    ") {
  return `${e.repeat(Math.max(0, t))}${n}`;
}
function fq(n, t) {
  return new RegExp("<pre( .*?)?>").test(n) ? "first" : new RegExp("</pre>").test(n) ? "last" : t === "first" || t === "middle" ? "middle" : !1;
}
const Cl = "SourceEditingMode";
class mq extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The element replacer instance used to replace the editing roots with the wrapper elements containing the document source.
    */
    f(this, "_elementReplacer");
    /**
    * Maps all root names to wrapper elements containing the document source.
    */
    f(this, "_replacedRoots");
    /**
    * Maps all root names to their document data.
    */
    f(this, "_dataFromRoots");
    this.set("isSourceEditingMode", !1), this._elementReplacer = new i_(), this._replacedRoots = /* @__PURE__ */ new Map(), this._dataFromRoots = /* @__PURE__ */ new Map(), e.config.define("sourceEditing.allowCollaborationFeatures", !1);
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SourceEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Hr
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    this._checkCompatibility();
    const e = this.editor, i = e.locale.t;
    e.ui.componentFactory.add("sourceEditing", () => {
      const s = this._createButton(le);
      return s.set({
        label: i("Source"),
        icon: Z.source,
        tooltip: !0,
        class: "ck-source-editing-button"
      }), s;
    }), e.ui.componentFactory.add("menuBar:sourceEditing", () => {
      const s = this._createButton(Fe);
      return s.set({
        label: i("Show source"),
        role: "menuitemcheckbox"
      }), s;
    }), this._isAllowedToHandleSourceEditingMode() && (this.on("change:isSourceEditingMode", (s, r, o) => {
      o ? (this._hideVisibleDialog(), this._showSourceEditing(), this._disableCommands()) : (this._hideSourceEditing(), this._enableCommands());
    }), this.on("change:isEnabled", (s, r, o) => this._handleReadOnlyMode(!o)), this.listenTo(e, "change:isReadOnly", (s, r, o) => this._handleReadOnlyMode(o))), e.data.on("get", () => {
      this.isSourceEditingMode && this.updateEditorData();
    }, {
      priority: "high"
    });
  }
  /**
  * Updates the source data in all hidden editing roots.
  */
  updateEditorData() {
    const e = this.editor, i = {};
    for (const [s, r] of this._replacedRoots) {
      const o = this._dataFromRoots.get(s), a = r.dataset.value;
      o !== a && (i[s] = a, this._dataFromRoots.set(s, a));
    }
    Object.keys(i).length && e.data.set(i, {
      batchType: {
        isUndoable: !0
      },
      suppressErrorInCollaboration: !0
    });
  }
  _checkCompatibility() {
    const e = this.editor, i = e.config.get("sourceEditing.allowCollaborationFeatures");
    if (!i && e.plugins.has("RealTimeCollaborativeEditing"))
      throw new S("source-editing-incompatible-with-real-time-collaboration", null);
    !i && [
      "CommentsEditing",
      "TrackChangesEditing",
      "RevisionHistory"
    ].some((r) => e.plugins.has(r)) && console.warn("You initialized the editor with the source editing feature and at least one of the collaboration features. Please be advised that the source editing feature may not work, and be careful when editing document source that contains markers created by the collaboration features."), e.plugins.has("RestrictedEditingModeEditing") && console.warn("You initialized the editor with the source editing feature and restricted editing feature. Please be advised that the source editing feature may not work, and be careful when editing document source that contains markers created by the restricted editing feature.");
  }
  /**
  * Creates source editing wrappers that replace each editing root. Each wrapper contains the document source from the corresponding
  * root.
  *
  * The wrapper element contains a textarea and it solves the problem, that the textarea element cannot auto expand its height based on
  * the content it contains. The solution is to make the textarea more like a plain div element, which expands in height as much as it
  * needs to, in order to display the whole document source without scrolling. The wrapper element is a parent for the textarea and for
  * the pseudo-element `::after`, that replicates the look, content, and position of the textarea. The pseudo-element replica is hidden,
  * but it is styled to be an identical visual copy of the textarea with the same content. Then, the wrapper is a grid container and both
  * of its children (the textarea and the `::after` pseudo-element) are positioned within a CSS grid to occupy the same grid cell. The
  * content in the pseudo-element `::after` is set in CSS and it stretches the grid to the appropriate size based on the textarea value.
  * Since both children occupy the same grid cell, both have always the same height.
  */
  _showSourceEditing() {
    const e = this.editor, i = e.editing.view, s = e.model;
    s.change((r) => {
      r.setSelection(null), r.removeSelectionAttribute(s.document.selection.getAttributeKeys());
    });
    for (const [r, o] of i.domRoots) {
      const a = gq(e.data.get({
        rootName: r
      })), l = Ft(o.ownerDocument, "textarea", {
        rows: "1",
        "aria-label": "Source code editing area"
      }), c = Ft(o.ownerDocument, "div", {
        class: "ck-source-editing-area",
        "data-value": a
      }, [
        l
      ]);
      l.value = a, l.setSelectionRange(0, 0), l.addEventListener("input", () => {
        c.dataset.value = l.value, e.ui.update();
      }), i.change((u) => {
        const d = i.document.getRoot(r);
        u.addClass("ck-hidden", d);
      }), e.ui.setEditableElement("sourceEditing:" + r, l), this._replacedRoots.set(r, c), this._elementReplacer.replace(o, c), this._dataFromRoots.set(r, a);
    }
    this._focusSourceEditing();
  }
  /**
  * Restores all hidden editing roots and sets the source data in them.
  */
  _hideSourceEditing() {
    const i = this.editor.editing.view;
    this.updateEditorData(), i.change((s) => {
      for (const [r] of this._replacedRoots)
        s.removeClass("ck-hidden", i.document.getRoot(r));
    }), this._elementReplacer.restore(), this._replacedRoots.clear(), this._dataFromRoots.clear(), i.focus();
  }
  /**
  * Focuses the textarea containing document source from the first editing root.
  */
  _focusSourceEditing() {
    const e = this.editor, [i] = this._replacedRoots.values(), s = i.querySelector("textarea");
    e.editing.view.document.isFocused = !1, s.focus();
  }
  /**
  * Disables all commands.
  */
  _disableCommands() {
    const e = this.editor;
    for (const i of e.commands.commands())
      i.forceDisabled(Cl);
    e.plugins.has("CommentsArchiveUI") && e.plugins.get("CommentsArchiveUI").forceDisabled(Cl);
  }
  /**
  * Clears forced disable for all commands, that was previously set through {@link #_disableCommands}.
  */
  _enableCommands() {
    const e = this.editor;
    for (const i of e.commands.commands())
      i.clearForceDisabled(Cl);
    e.plugins.has("CommentsArchiveUI") && e.plugins.get("CommentsArchiveUI").clearForceDisabled(Cl);
  }
  /**
  * Adds or removes the `readonly` attribute from the textarea from all roots, if document source mode is active.
  *
  * @param isReadOnly Indicates whether all textarea elements should be read-only.
  */
  _handleReadOnlyMode(e) {
    if (this.isSourceEditingMode)
      for (const [, i] of this._replacedRoots)
        i.querySelector("textarea").readOnly = e;
  }
  /**
  * Checks, if the plugin is allowed to handle the source editing mode by itself. Currently, the source editing mode is supported only
  * for the {@link module:editor-classic/classiceditor~ClassicEditor classic editor}.
  */
  _isAllowedToHandleSourceEditingMode() {
    const i = this.editor.ui.view.editable;
    return i && !i.hasExternalElement;
  }
  /**
  * If any {@link module:ui/dialog/dialogview~DialogView editor dialog} is currently visible, hide it.
  */
  _hideVisibleDialog() {
    if (this.editor.plugins.has("Dialog")) {
      const e = this.editor.plugins.get("Dialog");
      e.isOpen && e.hide();
    }
  }
  _createButton(e) {
    const i = this.editor, s = new e(i.locale);
    return s.set({
      withText: !0,
      isToggleable: !0
    }), s.bind("isOn").to(this, "isSourceEditingMode"), s.bind("isEnabled").to(this, "isEnabled", i, "isReadOnly", i.plugins.get(Hr), "hasAny", (r, o, a) => !(!r || o || a)), this.listenTo(s, "execute", () => {
      this.isSourceEditingMode = !this.isSourceEditingMode;
    }), s;
  }
}
function gq(n) {
  return pq(n) ? uq(n) : n;
}
function pq(n) {
  return n.startsWith("<");
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class bq extends ee {
  /**
  * Creates an instance of a character grid containing tiles representing special characters.
  *
  * @param locale The localization services instance.
  */
  constructor(e) {
    super(e);
    /**
    * A collection of the child tile views. Each tile represents a particular character.
    */
    f(this, "tiles");
    /**
    * Tracks information about the DOM focus in the grid.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    this.tiles = this.createCollection(), this.setTemplate({
      tag: "div",
      children: [
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-character-grid__tiles"
            ]
          },
          children: this.tiles
        }
      ],
      attributes: {
        class: [
          "ck",
          "ck-character-grid"
        ]
      }
    }), this.focusTracker = new ze(), this.keystrokes = new Ze(), Oa({
      keystrokeHandler: this.keystrokes,
      focusTracker: this.focusTracker,
      gridItems: this.tiles,
      numberOfColumns: () => ae.window.getComputedStyle(this.element.firstChild).getPropertyValue("grid-template-columns").split(" ").length,
      uiLanguageDirection: this.locale && this.locale.uiLanguageDirection
    });
  }
  /**
  * Creates a new tile for the grid.
  *
  * @param character A human-readable character displayed as the label (e.g. "ε").
  * @param name The name of the character (e.g. "greek small letter epsilon").
  */
  createTile(e, i) {
    const s = new le(this.locale);
    return s.set({
      label: e,
      withText: !0,
      class: "ck-character-grid__tile"
    }), s.extendTemplate({
      attributes: {
        title: i
      },
      on: {
        mouseover: s.bindTemplate.to("mouseover"),
        focus: s.bindTemplate.to("focus")
      }
    }), s.on("mouseover", () => {
      this.fire("tileHover", {
        name: i,
        character: e
      });
    }), s.on("focus", () => {
      this.fire("tileFocus", {
        name: i,
        character: e
      });
    }), s.on("execute", () => {
      this.fire("execute", {
        name: i,
        character: e
      });
    }), s;
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    for (const e of this.tiles)
      this.focusTracker.add(e.element);
    this.tiles.on("change", (e, { added: i, removed: s }) => {
      if (i.length > 0)
        for (const r of i)
          this.focusTracker.add(r.element);
      if (s.length > 0)
        for (const r of s)
          this.focusTracker.remove(r.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the first focusable in {@link ~CharacterGridView#tiles}.
  */
  focus() {
    this.tiles.first.focus();
  }
}
class wq extends ee {
  constructor(t) {
    super(t);
    const e = this.bindTemplate;
    this.set("character", null), this.set("name", null), this.bind("code").to(this, "character", vq), this.setTemplate({
      tag: "div",
      children: [
        {
          tag: "span",
          attributes: {
            class: [
              "ck-character-info__name"
            ]
          },
          children: [
            {
              // Note: ZWSP to prevent vertical collapsing.
              text: e.to("name", (i) => i || "​")
            }
          ]
        },
        {
          tag: "span",
          attributes: {
            class: [
              "ck-character-info__code"
            ]
          },
          children: [
            {
              text: e.to("code")
            }
          ]
        }
      ],
      attributes: {
        class: [
          "ck",
          "ck-character-info"
        ]
      }
    });
  }
}
function vq(n) {
  return n === null ? "" : "U+" + ("0000" + n.codePointAt(0).toString(16)).slice(-4);
}
class _q extends ee {
  /**
  * Creates an instance of the `SpecialCharactersView`.
  */
  constructor(e, i, s, r) {
    super(e);
    /**
    * A collection of the focusable children of the view.
    */
    f(this, "items");
    /**
    * Tracks information about the DOM focus in the view.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * Helps cycling over focusable {@link #items} in the view.
    */
    f(this, "_focusCycler");
    /**
    * An instance of the `SpecialCharactersCategoriesView`.
    */
    f(this, "categoriesView");
    /**
    * An instance of the `CharacterGridView`.
    */
    f(this, "gridView");
    /**
    * An instance of the `CharacterInfoView`.
    */
    f(this, "infoView");
    this.categoriesView = i, this.gridView = s, this.infoView = r, this.items = this.createCollection(), this.focusTracker = new ze(), this.keystrokes = new Ze(), this._focusCycler = new Pt({
      focusables: this.items,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        focusPrevious: "shift + tab",
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "div",
      children: [
        this.categoriesView,
        this.gridView,
        this.infoView
      ],
      attributes: {
        // Avoid focus loss when the user clicks the area of the grid that is not a button.
        // https://github.com/ckeditor/ckeditor5/pull/12319#issuecomment-1231779819
        tabindex: "-1"
      }
    }), this.items.add(this.categoriesView), this.items.add(this.gridView);
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this.focusTracker.add(this.categoriesView.element), this.focusTracker.add(this.gridView.element), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the first focusable in {@link #items}.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
}
var yq = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M10 2.5a7.47 7.47 0 0 1 4.231 1.31 7.268 7.268 0 0 1 2.703 3.454 7.128 7.128 0 0 1 .199 4.353c-.39 1.436-1.475 2.72-2.633 3.677h2.013c0-.226.092-.443.254-.603a.876.876 0 0 1 1.229 0c.163.16.254.377.254.603v.853c0 .209-.078.41-.22.567a.873.873 0 0 1-.547.28l-.101.006h-4.695a.517.517 0 0 1-.516-.518v-1.265c0-.21.128-.398.317-.489a5.601 5.601 0 0 0 2.492-2.371 5.459 5.459 0 0 0 .552-3.693 5.53 5.53 0 0 0-1.955-3.2A5.71 5.71 0 0 0 10 4.206 5.708 5.708 0 0 0 6.419 5.46 5.527 5.527 0 0 0 4.46 8.663a5.457 5.457 0 0 0 .554 3.695 5.6 5.6 0 0 0 2.497 2.37.55.55 0 0 1 .317.49v1.264c0 .286-.23.518-.516.518H2.618a.877.877 0 0 1-.614-.25.845.845 0 0 1-.254-.603v-.853c0-.226.091-.443.254-.603a.876.876 0 0 1 1.228 0c.163.16.255.377.255.603h1.925c-1.158-.958-2.155-2.241-2.545-3.678a7.128 7.128 0 0 1 .199-4.352 7.268 7.268 0 0 1 2.703-3.455A7.475 7.475 0 0 1 10 2.5z"/></svg>';
class kq extends ee {
  /**
  * Creates an instance of the {@link module:special-characters/ui/specialcharacterscategoriesview~SpecialCharactersCategoriesView}
  * class.
  *
  * @param locale The localization services instance.
  * @param groupNames The names of the character groups.
  */
  constructor(e, i) {
    super(e);
    f(this, "_groupNames");
    f(this, "_dropdownView");
    this.set("currentGroupName", Array.from(i.entries())[0][0]), this._groupNames = i, this._dropdownView = new gt(e, jf), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-character-categories"
        ]
      },
      children: [
        this._dropdownView
      ]
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this._setupDropdown();
  }
  /**
  * @inheritDoc
  */
  focus() {
    this._dropdownView.focus();
  }
  /**
  * Creates dropdown item list, sets up bindings and fills properties.
  */
  _setupDropdown() {
    const e = new bt();
    for (const [r, o] of this._groupNames) {
      const a = {
        type: "button",
        model: new tn({
          name: r,
          label: o,
          role: "menuitemradio",
          withText: !0
        })
      };
      a.model.bind("isOn").to(this, "currentGroupName", (l) => l === r), e.add(a);
    }
    const i = this.locale.t, s = i("Category");
    this._dropdownView.set({
      label: s,
      isEmpty: !1
    }), this._dropdownView.fieldView.panelPosition = this.locale.uiLanguageDirection === "rtl" ? "se" : "sw", this._dropdownView.fieldView.buttonView.set({
      withText: !0,
      tooltip: s,
      ariaLabel: s,
      ariaLabelledBy: void 0,
      isOn: !1
    }), this._dropdownView.fieldView.buttonView.bind("label").to(this, "currentGroupName", (r) => this._groupNames.get(r)), this._dropdownView.fieldView.on("execute", ({ source: r }) => {
      this.currentGroupName = r.name;
    }), En(this._dropdownView.fieldView, e, {
      ariaLabel: s,
      role: "menu"
    });
  }
}
const Rd = "All";
class Cq extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Registered characters. A pair of a character name and its symbol.
    */
    f(this, "_characters");
    /**
    * Registered groups. Each group contains a displayed label and a collection with symbol names.
    */
    f(this, "_groups");
    /**
    * A label describing the "All" special characters category.
    */
    f(this, "_allSpecialCharactersGroupLabel");
    const i = e.t;
    this._characters = /* @__PURE__ */ new Map(), this._groups = /* @__PURE__ */ new Map(), this._allSpecialCharactersGroupLabel = i("All");
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ek,
      Hs
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SpecialCharacters";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.ui.componentFactory.add("specialCharacters", () => {
      const i = this._createDialogButton(le);
      return i.set({
        tooltip: !0
      }), i;
    }), e.ui.componentFactory.add("menuBar:specialCharacters", () => this._createDialogButton(Fe));
  }
  /**
  * Adds a collection of special characters to the specified group. The title of a special character must be unique.
  *
  * **Note:** The "All" category name is reserved by the plugin and cannot be used as a new name for a special
  * characters category.
  */
  addItems(e, i, s = {
    label: e
  }) {
    if (e === Rd)
      throw new S("special-character-invalid-group-name", null);
    const r = this._getGroup(e, s.label);
    for (const o of i)
      r.items.add(o.title), this._characters.set(o.title, o.character);
  }
  /**
  * Returns special character groups in an order determined based on configuration and registration sequence.
  */
  getGroups() {
    const e = Array.from(this._groups.keys()), i = this.editor.config.get("specialCharacters.order") || [], s = i.find((r) => !e.includes(r));
    if (s)
      throw new S("special-character-invalid-order-group-name", null, {
        invalidGroup: s
      });
    return /* @__PURE__ */ new Set([
      ...i,
      ...e
    ]);
  }
  /**
  * Returns a collection of special characters symbol names (titles).
  */
  getCharactersForGroup(e) {
    if (e === Rd)
      return new Set(this._characters.keys());
    const i = this._groups.get(e);
    if (i)
      return i.items;
  }
  /**
  * Returns the symbol of a special character for the specified name. If the special character could not be found, `undefined`
  * is returned.
  *
  * @param title The title of a special character.
  */
  getCharacter(e) {
    return this._characters.get(e);
  }
  /**
  * Returns a group of special characters. If the group with the specified name does not exist, it will be created.
  *
  * @param groupName The name of the group to create.
  * @param label The label describing the new group.
  */
  _getGroup(e, i) {
    return this._groups.has(e) || this._groups.set(e, {
      items: /* @__PURE__ */ new Set(),
      label: i
    }), this._groups.get(e);
  }
  /**
  * Updates the symbol grid depending on the currently selected character group.
  */
  _updateGrid(e, i) {
    i.tiles.clear();
    const s = this.getCharactersForGroup(e);
    for (const r of s) {
      const o = this.getCharacter(r);
      i.tiles.add(i.createTile(o, r));
    }
  }
  /**
  * Initializes the dropdown, used for lazy loading.
  *
  * @returns An object with `categoriesView`, `gridView` and `infoView` properties, containing UI parts.
  */
  _createDropdownPanelContent(e) {
    const i = Array.from(this.getGroups()).map((l) => [
      l,
      this._groups.get(l).label
    ]), s = new Map([
      // Add a special group that shows all available special characters.
      [
        Rd,
        this._allSpecialCharactersGroupLabel
      ],
      ...i
    ]), r = new kq(e, s), o = new bq(e), a = new wq(e);
    return o.on("tileHover", (l, c) => {
      a.set(c);
    }), o.on("tileFocus", (l, c) => {
      a.set(c);
    }), r.on("change:currentGroupName", (l, c, u) => {
      this._updateGrid(u, o);
    }), this._updateGrid(r.currentGroupName, o), {
      categoriesView: r,
      gridView: o,
      infoView: a
    };
  }
  /**
  * Creates a button for toolbar and menu bar that will show special characters dialog.
  */
  _createDialogButton(e) {
    const i = this.editor, s = i.locale, r = new e(i.locale), o = i.commands.get("insertText"), a = s.t, l = this.editor.plugins.get("Dialog");
    return r.set({
      label: a("Special characters"),
      icon: yq,
      isToggleable: !0
    }), r.bind("isOn").to(l, "id", (c) => c === "specialCharacters"), r.bind("isEnabled").to(o, "isEnabled"), r.on("execute", () => {
      if (l.id === "specialCharacters") {
        l.hide();
        return;
      }
      this._showDialog();
    }), r;
  }
  _showDialog() {
    const e = this.editor, i = e.plugins.get("Dialog"), s = e.locale, r = s.t, { categoriesView: o, gridView: a, infoView: l } = this._createDropdownPanelContent(s), c = new _q(s, o, a, l);
    a.on("execute", (u, d) => {
      e.execute("insertText", {
        text: d.character
      });
    }), i.show({
      id: "specialCharacters",
      title: r("Special characters"),
      className: "ck-special-characters",
      content: c,
      position: Ci.EDITOR_TOP_SIDE
    });
  }
}
class NC extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SpecialCharactersArrows";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t;
    t.plugins.get("SpecialCharacters").addItems("Arrows", [
      {
        title: e("leftwards simple arrow"),
        character: "←"
      },
      {
        title: e("rightwards simple arrow"),
        character: "→"
      },
      {
        title: e("upwards simple arrow"),
        character: "↑"
      },
      {
        title: e("downwards simple arrow"),
        character: "↓"
      },
      {
        title: e("leftwards double arrow"),
        character: "⇐"
      },
      {
        title: e("rightwards double arrow"),
        character: "⇒"
      },
      {
        title: e("upwards double arrow"),
        character: "⇑"
      },
      {
        title: e("downwards double arrow"),
        character: "⇓"
      },
      {
        title: e("leftwards dashed arrow"),
        character: "⇠"
      },
      {
        title: e("rightwards dashed arrow"),
        character: "⇢"
      },
      {
        title: e("upwards dashed arrow"),
        character: "⇡"
      },
      {
        title: e("downwards dashed arrow"),
        character: "⇣"
      },
      {
        title: e("leftwards arrow to bar"),
        character: "⇤"
      },
      {
        title: e("rightwards arrow to bar"),
        character: "⇥"
      },
      {
        title: e("upwards arrow to bar"),
        character: "⤒"
      },
      {
        title: e("downwards arrow to bar"),
        character: "⤓"
      },
      {
        title: e("up down arrow with base"),
        character: "↨"
      },
      {
        title: e("back with leftwards arrow above"),
        character: "🔙"
      },
      {
        title: e("end with leftwards arrow above"),
        character: "🔚"
      },
      {
        title: e("on with exclamation mark with left right arrow above"),
        character: "🔛"
      },
      {
        title: e("soon with rightwards arrow above"),
        character: "🔜"
      },
      {
        title: e("top with upwards arrow above"),
        character: "🔝"
      }
    ], {
      label: e("Arrows")
    });
  }
}
class FC extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SpecialCharactersText";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t;
    t.plugins.get("SpecialCharacters").addItems("Text", [
      {
        character: "‹",
        title: e("Single left-pointing angle quotation mark")
      },
      {
        character: "›",
        title: e("Single right-pointing angle quotation mark")
      },
      {
        character: "«",
        title: e("Left-pointing double angle quotation mark")
      },
      {
        character: "»",
        title: e("Right-pointing double angle quotation mark")
      },
      {
        character: "‘",
        title: e("Left single quotation mark")
      },
      {
        character: "’",
        title: e("Right single quotation mark")
      },
      {
        character: "“",
        title: e("Left double quotation mark")
      },
      {
        character: "”",
        title: e("Right double quotation mark")
      },
      {
        character: "‚",
        title: e("Single low-9 quotation mark")
      },
      {
        character: "„",
        title: e("Double low-9 quotation mark")
      },
      {
        character: "¡",
        title: e("Inverted exclamation mark")
      },
      {
        character: "¿",
        title: e("Inverted question mark")
      },
      {
        character: "‥",
        title: e("Two dot leader")
      },
      {
        character: "…",
        title: e("Horizontal ellipsis")
      },
      {
        character: "‡",
        title: e("Double dagger")
      },
      {
        character: "‰",
        title: e("Per mille sign")
      },
      {
        character: "‱",
        title: e("Per ten thousand sign")
      },
      {
        character: "‼",
        title: e("Double exclamation mark")
      },
      {
        character: "⁈",
        title: e("Question exclamation mark")
      },
      {
        character: "⁉",
        title: e("Exclamation question mark")
      },
      {
        character: "⁇",
        title: e("Double question mark")
      },
      {
        character: "©",
        title: e("Copyright sign")
      },
      {
        character: "®",
        title: e("Registered sign")
      },
      {
        character: "™",
        title: e("Trade mark sign")
      },
      {
        character: "§",
        title: e("Section sign")
      },
      {
        character: "¶",
        title: e("Paragraph sign")
      },
      {
        character: "⁋",
        title: e("Reversed paragraph sign")
      }
    ], {
      label: e("Text")
    });
  }
}
class DC extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SpecialCharactersMathematical";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t;
    t.plugins.get("SpecialCharacters").addItems("Mathematical", [
      {
        character: "<",
        title: e("Less-than sign")
      },
      {
        character: ">",
        title: e("Greater-than sign")
      },
      {
        character: "≤",
        title: e("Less-than or equal to")
      },
      {
        character: "≥",
        title: e("Greater-than or equal to")
      },
      {
        character: "–",
        title: e("En dash")
      },
      {
        character: "—",
        title: e("Em dash")
      },
      {
        character: "¯",
        title: e("Macron")
      },
      {
        character: "‾",
        title: e("Overline")
      },
      {
        character: "°",
        title: e("Degree sign")
      },
      {
        character: "−",
        title: e("Minus sign")
      },
      {
        character: "±",
        title: e("Plus-minus sign")
      },
      {
        character: "÷",
        title: e("Division sign")
      },
      {
        character: "⁄",
        title: e("Fraction slash")
      },
      {
        character: "×",
        title: e("Multiplication sign")
      },
      {
        character: "ƒ",
        title: e("Latin small letter f with hook")
      },
      {
        character: "∫",
        title: e("Integral")
      },
      {
        character: "∑",
        title: e("N-ary summation")
      },
      {
        character: "∞",
        title: e("Infinity")
      },
      {
        character: "√",
        title: e("Square root")
      },
      {
        character: "∼",
        title: e("Tilde operator")
      },
      {
        character: "≅",
        title: e("Approximately equal to")
      },
      {
        character: "≈",
        title: e("Almost equal to")
      },
      {
        character: "≠",
        title: e("Not equal to")
      },
      {
        character: "≡",
        title: e("Identical to")
      },
      {
        character: "∈",
        title: e("Element of")
      },
      {
        character: "∉",
        title: e("Not an element of")
      },
      {
        character: "∋",
        title: e("Contains as member")
      },
      {
        character: "∏",
        title: e("N-ary product")
      },
      {
        character: "∧",
        title: e("Logical and")
      },
      {
        character: "∨",
        title: e("Logical or")
      },
      {
        character: "¬",
        title: e("Not sign")
      },
      {
        character: "∩",
        title: e("Intersection")
      },
      {
        character: "∪",
        title: e("Union")
      },
      {
        character: "∂",
        title: e("Partial differential")
      },
      {
        character: "∀",
        title: e("For all")
      },
      {
        character: "∃",
        title: e("There exists")
      },
      {
        character: "∅",
        title: e("Empty set")
      },
      {
        character: "∇",
        title: e("Nabla")
      },
      {
        character: "∗",
        title: e("Asterisk operator")
      },
      {
        character: "∝",
        title: e("Proportional to")
      },
      {
        character: "∠",
        title: e("Angle")
      },
      {
        character: "¼",
        title: e("Vulgar fraction one quarter")
      },
      {
        character: "½",
        title: e("Vulgar fraction one half")
      },
      {
        character: "¾",
        title: e("Vulgar fraction three quarters")
      }
    ], {
      label: e("Mathematical")
    });
  }
}
class $C extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SpecialCharactersLatin";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t;
    t.plugins.get("SpecialCharacters").addItems("Latin", [
      {
        character: "Ā",
        title: e("Latin capital letter a with macron")
      },
      {
        character: "ā",
        title: e("Latin small letter a with macron")
      },
      {
        character: "Ă",
        title: e("Latin capital letter a with breve")
      },
      {
        character: "ă",
        title: e("Latin small letter a with breve")
      },
      {
        character: "Ą",
        title: e("Latin capital letter a with ogonek")
      },
      {
        character: "ą",
        title: e("Latin small letter a with ogonek")
      },
      {
        character: "Ć",
        title: e("Latin capital letter c with acute")
      },
      {
        character: "ć",
        title: e("Latin small letter c with acute")
      },
      {
        character: "Ĉ",
        title: e("Latin capital letter c with circumflex")
      },
      {
        character: "ĉ",
        title: e("Latin small letter c with circumflex")
      },
      {
        character: "Ċ",
        title: e("Latin capital letter c with dot above")
      },
      {
        character: "ċ",
        title: e("Latin small letter c with dot above")
      },
      {
        character: "Č",
        title: e("Latin capital letter c with caron")
      },
      {
        character: "č",
        title: e("Latin small letter c with caron")
      },
      {
        character: "Ď",
        title: e("Latin capital letter d with caron")
      },
      {
        character: "ď",
        title: e("Latin small letter d with caron")
      },
      {
        character: "Đ",
        title: e("Latin capital letter d with stroke")
      },
      {
        character: "đ",
        title: e("Latin small letter d with stroke")
      },
      {
        character: "Ē",
        title: e("Latin capital letter e with macron")
      },
      {
        character: "ē",
        title: e("Latin small letter e with macron")
      },
      {
        character: "Ĕ",
        title: e("Latin capital letter e with breve")
      },
      {
        character: "ĕ",
        title: e("Latin small letter e with breve")
      },
      {
        character: "Ė",
        title: e("Latin capital letter e with dot above")
      },
      {
        character: "ė",
        title: e("Latin small letter e with dot above")
      },
      {
        character: "Ę",
        title: e("Latin capital letter e with ogonek")
      },
      {
        character: "ę",
        title: e("Latin small letter e with ogonek")
      },
      {
        character: "Ě",
        title: e("Latin capital letter e with caron")
      },
      {
        character: "ě",
        title: e("Latin small letter e with caron")
      },
      {
        character: "Ĝ",
        title: e("Latin capital letter g with circumflex")
      },
      {
        character: "ĝ",
        title: e("Latin small letter g with circumflex")
      },
      {
        character: "Ğ",
        title: e("Latin capital letter g with breve")
      },
      {
        character: "ğ",
        title: e("Latin small letter g with breve")
      },
      {
        character: "Ġ",
        title: e("Latin capital letter g with dot above")
      },
      {
        character: "ġ",
        title: e("Latin small letter g with dot above")
      },
      {
        character: "Ģ",
        title: e("Latin capital letter g with cedilla")
      },
      {
        character: "ģ",
        title: e("Latin small letter g with cedilla")
      },
      {
        character: "Ĥ",
        title: e("Latin capital letter h with circumflex")
      },
      {
        character: "ĥ",
        title: e("Latin small letter h with circumflex")
      },
      {
        character: "Ħ",
        title: e("Latin capital letter h with stroke")
      },
      {
        character: "ħ",
        title: e("Latin small letter h with stroke")
      },
      {
        character: "Ĩ",
        title: e("Latin capital letter i with tilde")
      },
      {
        character: "ĩ",
        title: e("Latin small letter i with tilde")
      },
      {
        character: "Ī",
        title: e("Latin capital letter i with macron")
      },
      {
        character: "ī",
        title: e("Latin small letter i with macron")
      },
      {
        character: "Ĭ",
        title: e("Latin capital letter i with breve")
      },
      {
        character: "ĭ",
        title: e("Latin small letter i with breve")
      },
      {
        character: "Į",
        title: e("Latin capital letter i with ogonek")
      },
      {
        character: "į",
        title: e("Latin small letter i with ogonek")
      },
      {
        character: "İ",
        title: e("Latin capital letter i with dot above")
      },
      {
        character: "ı",
        title: e("Latin small letter dotless i")
      },
      {
        character: "Ĳ",
        title: e("Latin capital ligature ij")
      },
      {
        character: "ĳ",
        title: e("Latin small ligature ij")
      },
      {
        character: "Ĵ",
        title: e("Latin capital letter j with circumflex")
      },
      {
        character: "ĵ",
        title: e("Latin small letter j with circumflex")
      },
      {
        character: "Ķ",
        title: e("Latin capital letter k with cedilla")
      },
      {
        character: "ķ",
        title: e("Latin small letter k with cedilla")
      },
      {
        character: "ĸ",
        title: e("Latin small letter kra")
      },
      {
        character: "Ĺ",
        title: e("Latin capital letter l with acute")
      },
      {
        character: "ĺ",
        title: e("Latin small letter l with acute")
      },
      {
        character: "Ļ",
        title: e("Latin capital letter l with cedilla")
      },
      {
        character: "ļ",
        title: e("Latin small letter l with cedilla")
      },
      {
        character: "Ľ",
        title: e("Latin capital letter l with caron")
      },
      {
        character: "ľ",
        title: e("Latin small letter l with caron")
      },
      {
        character: "Ŀ",
        title: e("Latin capital letter l with middle dot")
      },
      {
        character: "ŀ",
        title: e("Latin small letter l with middle dot")
      },
      {
        character: "Ł",
        title: e("Latin capital letter l with stroke")
      },
      {
        character: "ł",
        title: e("Latin small letter l with stroke")
      },
      {
        character: "Ń",
        title: e("Latin capital letter n with acute")
      },
      {
        character: "ń",
        title: e("Latin small letter n with acute")
      },
      {
        character: "Ņ",
        title: e("Latin capital letter n with cedilla")
      },
      {
        character: "ņ",
        title: e("Latin small letter n with cedilla")
      },
      {
        character: "Ň",
        title: e("Latin capital letter n with caron")
      },
      {
        character: "ň",
        title: e("Latin small letter n with caron")
      },
      {
        character: "ŉ",
        title: e("Latin small letter n preceded by apostrophe")
      },
      {
        character: "Ŋ",
        title: e("Latin capital letter eng")
      },
      {
        character: "ŋ",
        title: e("Latin small letter eng")
      },
      {
        character: "Ō",
        title: e("Latin capital letter o with macron")
      },
      {
        character: "ō",
        title: e("Latin small letter o with macron")
      },
      {
        character: "Ŏ",
        title: e("Latin capital letter o with breve")
      },
      {
        character: "ŏ",
        title: e("Latin small letter o with breve")
      },
      {
        character: "Ő",
        title: e("Latin capital letter o with double acute")
      },
      {
        character: "ő",
        title: e("Latin small letter o with double acute")
      },
      {
        character: "Œ",
        title: e("Latin capital ligature oe")
      },
      {
        character: "œ",
        title: e("Latin small ligature oe")
      },
      {
        character: "Ŕ",
        title: e("Latin capital letter r with acute")
      },
      {
        character: "ŕ",
        title: e("Latin small letter r with acute")
      },
      {
        character: "Ŗ",
        title: e("Latin capital letter r with cedilla")
      },
      {
        character: "ŗ",
        title: e("Latin small letter r with cedilla")
      },
      {
        character: "Ř",
        title: e("Latin capital letter r with caron")
      },
      {
        character: "ř",
        title: e("Latin small letter r with caron")
      },
      {
        character: "Ś",
        title: e("Latin capital letter s with acute")
      },
      {
        character: "ś",
        title: e("Latin small letter s with acute")
      },
      {
        character: "Ŝ",
        title: e("Latin capital letter s with circumflex")
      },
      {
        character: "ŝ",
        title: e("Latin small letter s with circumflex")
      },
      {
        character: "Ş",
        title: e("Latin capital letter s with cedilla")
      },
      {
        character: "ş",
        title: e("Latin small letter s with cedilla")
      },
      {
        character: "Š",
        title: e("Latin capital letter s with caron")
      },
      {
        character: "š",
        title: e("Latin small letter s with caron")
      },
      {
        character: "Ţ",
        title: e("Latin capital letter t with cedilla")
      },
      {
        character: "ţ",
        title: e("Latin small letter t with cedilla")
      },
      {
        character: "Ť",
        title: e("Latin capital letter t with caron")
      },
      {
        character: "ť",
        title: e("Latin small letter t with caron")
      },
      {
        character: "Ŧ",
        title: e("Latin capital letter t with stroke")
      },
      {
        character: "ŧ",
        title: e("Latin small letter t with stroke")
      },
      {
        character: "Ũ",
        title: e("Latin capital letter u with tilde")
      },
      {
        character: "ũ",
        title: e("Latin small letter u with tilde")
      },
      {
        character: "Ū",
        title: e("Latin capital letter u with macron")
      },
      {
        character: "ū",
        title: e("Latin small letter u with macron")
      },
      {
        character: "Ŭ",
        title: e("Latin capital letter u with breve")
      },
      {
        character: "ŭ",
        title: e("Latin small letter u with breve")
      },
      {
        character: "Ů",
        title: e("Latin capital letter u with ring above")
      },
      {
        character: "ů",
        title: e("Latin small letter u with ring above")
      },
      {
        character: "Ű",
        title: e("Latin capital letter u with double acute")
      },
      {
        character: "ű",
        title: e("Latin small letter u with double acute")
      },
      {
        character: "Ų",
        title: e("Latin capital letter u with ogonek")
      },
      {
        character: "ų",
        title: e("Latin small letter u with ogonek")
      },
      {
        character: "Ŵ",
        title: e("Latin capital letter w with circumflex")
      },
      {
        character: "ŵ",
        title: e("Latin small letter w with circumflex")
      },
      {
        character: "Ŷ",
        title: e("Latin capital letter y with circumflex")
      },
      {
        character: "ŷ",
        title: e("Latin small letter y with circumflex")
      },
      {
        character: "Ÿ",
        title: e("Latin capital letter y with diaeresis")
      },
      {
        character: "Ź",
        title: e("Latin capital letter z with acute")
      },
      {
        character: "ź",
        title: e("Latin small letter z with acute")
      },
      {
        character: "Ż",
        title: e("Latin capital letter z with dot above")
      },
      {
        character: "ż",
        title: e("Latin small letter z with dot above")
      },
      {
        character: "Ž",
        title: e("Latin capital letter z with caron")
      },
      {
        character: "ž",
        title: e("Latin small letter z with caron")
      },
      {
        character: "ſ",
        title: e("Latin small letter long s")
      }
    ], {
      label: e("Latin")
    });
  }
}
class zC extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SpecialCharactersCurrency";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.t;
    t.plugins.get("SpecialCharacters").addItems("Currency", [
      {
        character: "$",
        title: e("Dollar sign")
      },
      {
        character: "€",
        title: e("Euro sign")
      },
      {
        character: "¥",
        title: e("Yen sign")
      },
      {
        character: "£",
        title: e("Pound sign")
      },
      {
        character: "¢",
        title: e("Cent sign")
      },
      {
        character: "₠",
        title: e("Euro-currency sign")
      },
      {
        character: "₡",
        title: e("Colon sign")
      },
      {
        character: "₢",
        title: e("Cruzeiro sign")
      },
      {
        character: "₣",
        title: e("French franc sign")
      },
      {
        character: "₤",
        title: e("Lira sign")
      },
      {
        character: "¤",
        title: e("Currency sign")
      },
      {
        character: "₿",
        title: e("Bitcoin sign")
      },
      {
        character: "₥",
        title: e("Mill sign")
      },
      {
        character: "₦",
        title: e("Naira sign")
      },
      {
        character: "₧",
        title: e("Peseta sign")
      },
      {
        character: "₨",
        title: e("Rupee sign")
      },
      {
        character: "₩",
        title: e("Won sign")
      },
      {
        character: "₪",
        title: e("New sheqel sign")
      },
      {
        character: "₫",
        title: e("Dong sign")
      },
      {
        character: "₭",
        title: e("Kip sign")
      },
      {
        character: "₮",
        title: e("Tugrik sign")
      },
      {
        character: "₯",
        title: e("Drachma sign")
      },
      {
        character: "₰",
        title: e("German penny sign")
      },
      {
        character: "₱",
        title: e("Peso sign")
      },
      {
        character: "₲",
        title: e("Guarani sign")
      },
      {
        character: "₳",
        title: e("Austral sign")
      },
      {
        character: "₴",
        title: e("Hryvnia sign")
      },
      {
        character: "₵",
        title: e("Cedi sign")
      },
      {
        character: "₶",
        title: e("Livre tournois sign")
      },
      {
        character: "₷",
        title: e("Spesmilo sign")
      },
      {
        character: "₸",
        title: e("Tenge sign")
      },
      {
        character: "₹",
        title: e("Indian rupee sign")
      },
      {
        character: "₺",
        title: e("Turkish lira sign")
      },
      {
        character: "₻",
        title: e("Nordic mark sign")
      },
      {
        character: "₼",
        title: e("Manat sign")
      },
      {
        character: "₽",
        title: e("Ruble sign")
      }
    ], {
      label: e("Currency")
    });
  }
}
class xq extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "SpecialCharactersEssentials";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      zC,
      FC,
      DC,
      NC,
      $C
    ];
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class Tq extends le {
  /**
  * Creates an instance of the {@link module:style/ui/stylegridbuttonview~StyleGridButtonView} class.
  *
  * @param locale The localization services instance.
  * @param styleDefinition Definition of the style.
  */
  constructor(e, i) {
    super(e);
    /**
    * Definition of the style the button will apply when executed.
    */
    f(this, "styleDefinition");
    /**
    * The view rendering the preview of the style.
    */
    f(this, "previewView");
    this.styleDefinition = i, this.previewView = this._createPreview(), this.set({
      label: i.name,
      class: "ck-style-grid__button",
      withText: !0
    }), this.extendTemplate({
      attributes: {
        role: "option"
      }
    }), this.children.add(this.previewView, 0);
  }
  /**
  * Creates the view representing the preview of the style.
  */
  _createPreview() {
    const e = new ee(this.locale);
    return e.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-reset_all-excluded",
          "ck-style-grid__button__preview",
          "ck-content"
        ],
        // The preview "AaBbCcDdEeFfGgHhIiJj" should not be read by screen readers because it is purely presentational.
        "aria-hidden": "true"
      },
      children: [
        this.styleDefinition.previewTemplate
      ]
    }), e;
  }
}
class Eq extends ee {
  /**
  * Creates an instance of the {@link module:style/ui/stylegridview~StyleGridView} class.
  *
  * @param locale The localization services instance.
  * @param styleDefinitions Definitions of the styles.
  */
  constructor(e, i) {
    super(e);
    /**
    * Tracks information about the DOM focus in the view.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * A collection of style {@link module:style/ui/stylegridbuttonview~StyleGridButtonView buttons}.
    */
    f(this, "children");
    this.focusTracker = new ze(), this.keystrokes = new Ze(), this.set("activeStyles", []), this.set("enabledStyles", []), this.children = this.createCollection(), this.children.delegate("execute").to(this);
    for (const s of i) {
      const r = new Tq(e, s);
      this.children.add(r);
    }
    this.on("change:activeStyles", () => {
      for (const s of this.children)
        s.isOn = this.activeStyles.includes(s.styleDefinition.name);
    }), this.on("change:enabledStyles", () => {
      for (const s of this.children)
        s.isEnabled = this.enabledStyles.includes(s.styleDefinition.name);
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-style-grid"
        ],
        role: "listbox"
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render();
    for (const e of this.children)
      this.focusTracker.add(e.element);
    Oa({
      keystrokeHandler: this.keystrokes,
      focusTracker: this.focusTracker,
      gridItems: this.children,
      numberOfColumns: 3,
      uiLanguageDirection: this.locale && this.locale.uiLanguageDirection
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * Focuses the first style button in the grid.
  */
  focus() {
    this.children.first.focus();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
}
class kw extends ee {
  /**
  * Creates an instance of the {@link module:style/ui/stylegroupview~StyleGroupView} class.
  *
  * @param locale The localization services instance.
  * @param label The localized label of the group.
  * @param styleDefinitions Definitions of the styles in the group.
  */
  constructor(e, i, s) {
    super(e);
    /**
    * The styles grid of the group.
    */
    f(this, "gridView");
    /**
    * The label of the group.
    */
    f(this, "labelView");
    this.labelView = new gi(e), this.labelView.text = i, this.gridView = new Eq(e, s), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-style-panel__style-group"
        ],
        role: "group",
        "aria-labelledby": this.labelView.id
      },
      children: [
        this.labelView,
        this.gridView
      ]
    });
  }
}
class Sq extends ee {
  /**
  * Creates an instance of the {@link module:style/ui/stylegroupview~StyleGroupView} class.
  *
  * @param locale The localization services instance.
  * @param styleDefinitions Normalized definitions of the styles.
  */
  constructor(e, i) {
    super(e);
    /**
    * Tracks information about DOM focus in the panel.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * A collection of panel children.
    */
    f(this, "children");
    /**
    * A view representing block styles group.
    */
    f(this, "blockStylesGroupView");
    /**
    * A view representing inline styles group
    */
    f(this, "inlineStylesGroupView");
    /**
    * A collection of views that can be focused in the panel.
    */
    f(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the panel.
    */
    f(this, "_focusCycler");
    const s = e.t;
    this.focusTracker = new ze(), this.keystrokes = new Ze(), this.children = this.createCollection(), this.blockStylesGroupView = new kw(e, s("Block styles"), i.block), this.inlineStylesGroupView = new kw(e, s("Text styles"), i.inline), this.set("activeStyles", []), this.set("enabledStyles", []), this._focusables = new Et(), this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate style groups backwards using the <kbd>Shift</kbd> + <kbd>Tab</kbd> keystroke.
        focusPrevious: [
          "shift + tab"
        ],
        // Navigate style groups forward using the <kbd>Tab</kbd> key.
        focusNext: [
          "tab"
        ]
      }
    }), i.block.length && this.children.add(this.blockStylesGroupView), i.inline.length && this.children.add(this.inlineStylesGroupView), this.blockStylesGroupView.gridView.delegate("execute").to(this), this.inlineStylesGroupView.gridView.delegate("execute").to(this), this.blockStylesGroupView.gridView.bind("activeStyles", "enabledStyles").to(this, "activeStyles", "enabledStyles"), this.inlineStylesGroupView.gridView.bind("activeStyles", "enabledStyles").to(this, "activeStyles", "enabledStyles"), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-style-panel"
        ]
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), this._focusables.add(this.blockStylesGroupView.gridView), this._focusables.add(this.inlineStylesGroupView.gridView), this.focusTracker.add(this.blockStylesGroupView.gridView.element), this.focusTracker.add(this.inlineStylesGroupView.gridView.element), this.keystrokes.listenTo(this.element);
  }
  /**
  * Focuses the first focusable element in the panel.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Focuses the last focusable element in the panel.
  */
  focusLast() {
    this._focusCycler.focusLast();
  }
}
const Aq = [
  "caption",
  "colgroup",
  "dd",
  "dt",
  "figcaption",
  "legend",
  "li",
  "optgroup",
  "option",
  "rp",
  "rt",
  "summary",
  "tbody",
  "td",
  "tfoot",
  "th",
  "thead",
  "tr"
];
class Zn extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    f(this, "_htmlSupport");
    this.decorate("isStyleEnabledForBlock"), this.decorate("isStyleActiveForBlock"), this.decorate("getAffectedBlocks"), this.decorate("isStyleEnabledForInlineSelection"), this.decorate("isStyleActiveForInlineSelection"), this.decorate("getAffectedInlineSelectable"), this.decorate("getStylePreview"), this.decorate("configureGHSDataFilter");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "StyleUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this._htmlSupport = this.editor.plugins.get("GeneralHtmlSupport");
  }
  /**
  * Normalizes {@link module:style/styleconfig~StyleConfig#definitions} in the configuration of the styles feature.
  * The structure of normalized styles looks as follows:
  *
  * ```ts
  * {
  * 	block: [
  * 		<module:style/style~StyleDefinition>,
  * 		<module:style/style~StyleDefinition>,
  * 		...
  * 	],
  * 	inline: [
  * 		<module:style/style~StyleDefinition>,
  * 		<module:style/style~StyleDefinition>,
  * 		...
  * 	]
  * }
  * ```
  *
  * @returns An object with normalized style definitions grouped into `block` and `inline` categories (arrays).
  */
  normalizeConfig(e, i = []) {
    const s = {
      block: [],
      inline: []
    };
    for (const r of i) {
      const o = [], a = [];
      for (const c of e.getDefinitionsForView(r.element)) {
        const u = "appliesToBlock" in c ? c.appliesToBlock : !1;
        if (c.isBlock || u) {
          if (typeof u == "string")
            o.push(u);
          else if (c.isBlock) {
            const d = c;
            o.push(c.model), d.paragraphLikeModel && o.push(d.paragraphLikeModel);
          }
        } else
          a.push(c.model);
      }
      const l = this.getStylePreview(r, [
        {
          text: "AaBbCcDdEeFfGgHhIiJj"
        }
      ]);
      o.length ? s.block.push({
        ...r,
        previewTemplate: l,
        modelElements: o,
        isBlock: !0
      }) : s.inline.push({
        ...r,
        previewTemplate: l,
        ghsAttributes: a
      });
    }
    return s;
  }
  /**
  * Verifies if the given style is applicable to the provided block element.
  *
  * @internal
  */
  isStyleEnabledForBlock(e, i) {
    const s = this.editor.model, r = this._htmlSupport.getGhsAttributeNameForElement(e.element);
    return s.schema.checkAttribute(i, r) ? e.modelElements.includes(i.name) : !1;
  }
  /**
  * Returns true if the given style is applied to the specified block element.
  *
  * @internal
  */
  isStyleActiveForBlock(e, i) {
    const s = this._htmlSupport.getGhsAttributeNameForElement(e.element), r = i.getAttribute(s);
    return this.hasAllClasses(r, e.classes);
  }
  /**
  * Returns an array of block elements that style should be applied to.
  *
  * @internal
  */
  getAffectedBlocks(e, i) {
    return e.modelElements.includes(i.name) ? [
      i
    ] : null;
  }
  /**
  * Verifies if the given style is applicable to the provided document selection.
  *
  * @internal
  */
  isStyleEnabledForInlineSelection(e, i) {
    const s = this.editor.model;
    for (const r of e.ghsAttributes)
      if (s.schema.checkAttributeInSelection(i, r))
        return !0;
    return !1;
  }
  /**
  * Returns true if the given style is applied to the specified document selection.
  *
  * @internal
  */
  isStyleActiveForInlineSelection(e, i) {
    for (const s of e.ghsAttributes) {
      const r = this._getValueFromFirstAllowedNode(i, s);
      if (this.hasAllClasses(r, e.classes))
        return !0;
    }
    return !1;
  }
  /**
  * Returns a selectable that given style should be applied to.
  *
  * @internal
  */
  getAffectedInlineSelectable(e, i) {
    return i;
  }
  /**
  * Returns the `TemplateDefinition` used by styles dropdown to render style preview.
  *
  * @internal
  */
  getStylePreview(e, i) {
    const { element: s, classes: r } = e;
    return {
      tag: Pq(s) ? s : "div",
      attributes: {
        class: r
      },
      children: i
    };
  }
  /**
  * Verifies if all classes are present in the given GHS attribute.
  *
  * @internal
  */
  hasAllClasses(e, i) {
    return nt(e) && Iq(e) && i.every((s) => e.classes.includes(s));
  }
  /**
  * This is where the styles feature configures the GHS feature. This method translates normalized
  * {@link module:style/styleconfig~StyleDefinition style definitions} to
  * {@link module:engine/view/matcher~MatcherObjectPattern matcher patterns} and feeds them to the GHS
  * {@link module:html-support/datafilter~DataFilter} plugin.
  *
  * @internal
  */
  configureGHSDataFilter({ block: e, inline: i }) {
    const s = this.editor.plugins.get("DataFilter");
    s.loadAllowedConfig(e.map(Cw)), s.loadAllowedConfig(i.map(Cw));
  }
  /**
  * Checks the attribute value of the first node in the selection that allows the attribute.
  * For the collapsed selection, returns the selection attribute.
  *
  * @param selection The document selection.
  * @param attributeName Name of the GHS attribute.
  * @returns The attribute value.
  */
  _getValueFromFirstAllowedNode(e, i) {
    const r = this.editor.model.schema;
    if (e.isCollapsed)
      return e.getAttribute(i);
    for (const o of e.getRanges())
      for (const a of o.getItems())
        if (r.checkAttribute(a, i))
          return a.getAttribute(i);
    return null;
  }
}
function Iq(n) {
  return !!n.classes && Array.isArray(n.classes);
}
function Pq(n) {
  return !Aq.includes(n);
}
function Cw({ element: n, classes: t }) {
  return {
    name: n,
    classes: t
  };
}
class Rq extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "StyleUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Zn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.plugins.get("DataSchema"), i = t.plugins.get("StyleUtils"), s = t.config.get("style.definitions"), r = i.normalizeConfig(e, s);
    t.ui.componentFactory.add("style", (o) => {
      const a = o.t, l = Tt(o), c = t.commands.get("style");
      return l.once("change:isOpen", () => {
        const u = new Sq(o, r);
        l.panelView.children.add(u), u.delegate("execute").to(l), u.bind("activeStyles").to(c, "value"), u.bind("enabledStyles").to(c, "enabledStyles");
      }), l.bind("isEnabled").to(c), l.buttonView.withText = !0, l.buttonView.bind("label").to(c, "value", (u) => u.length > 1 ? a("Multiple styles") : u.length === 1 ? u[0] : a("Styles")), l.bind("class").to(c, "value", (u) => {
        const d = [
          "ck-style-dropdown"
        ];
        return u.length > 1 && d.push("ck-style-dropdown_multiple-active"), d.join(" ");
      }), l.on("execute", (u) => {
        t.execute("style", {
          styleName: u.source.styleDefinition.name
        }), t.editing.view.focus();
      }), l;
    });
  }
}
class Oq extends he {
  /**
  * Creates an instance of the command.
  *
  * @param editor Editor on which this command will be used.
  * @param styleDefinitions Normalized definitions of the styles.
  */
  constructor(e, i) {
    super(e);
    /**
    * Normalized definitions of the styles.
    */
    f(this, "_styleDefinitions");
    /**
    * The StyleUtils plugin.
    */
    f(this, "_styleUtils");
    this.set("value", []), this.set("enabledStyles", []), this._styleDefinitions = i, this._styleUtils = this.editor.plugins.get(Zn);
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model, i = e.document.selection, s = /* @__PURE__ */ new Set(), r = /* @__PURE__ */ new Set();
    for (const a of this._styleDefinitions.inline)
      this._styleUtils.isStyleEnabledForInlineSelection(a, i) && r.add(a.name), this._styleUtils.isStyleActiveForInlineSelection(a, i) && s.add(a.name);
    const o = Xe(i.getSelectedBlocks()) || i.getFirstPosition().parent;
    if (o) {
      const a = o.getAncestors({
        includeSelf: !0,
        parentFirst: !0
      });
      for (const l of a) {
        if (l.is("rootElement"))
          break;
        for (const c of this._styleDefinitions.block)
          this._styleUtils.isStyleEnabledForBlock(c, l) && (r.add(c.name), this._styleUtils.isStyleActiveForBlock(c, l) && s.add(c.name));
        if (e.schema.isObject(l))
          break;
      }
    }
    this.enabledStyles = Array.from(r).sort(), this.isEnabled = this.enabledStyles.length > 0, this.value = this.isEnabled ? Array.from(s).sort() : [];
  }
  /**
  * Executes the command &ndash; applies the style classes to the selection or removes it from the selection.
  *
  * If the command value already contains the requested style, it will remove the style classes. Otherwise, it will set it.
  *
  * The execution result differs, depending on the {@link module:engine/model/document~Document#selection} and the
  * style type (inline or block):
  *
  * * When applying inline styles:
  *   * If the selection is on a range, the command applies the style classes to all nodes in that range.
  *   * If the selection is collapsed in a non-empty node, the command applies the style classes to the
  * {@link module:engine/model/document~Document#selection}.
  *
  * * When applying block styles:
  *   * If the selection is on a range, the command applies the style classes to the nearest block parent element.
  *
  * @fires execute
  * @param options Command options.
  * @param options.styleName Style name matching the one defined in the
  * {@link module:style/styleconfig~StyleConfig#definitions configuration}.
  * @param options.forceValue Whether the command should add given style (`true`) or remove it (`false`) from the selection.
  * If not set (default), the command will toggle the style basing on the first selected node. Note, that this will not force
  * setting a style on an element that cannot receive given style.
  */
  execute({ styleName: e, forceValue: i }) {
    if (!this.enabledStyles.includes(e)) {
      Ve("style-command-executed-with-incorrect-style-name");
      return;
    }
    const s = this.editor.model, r = s.document.selection, o = this.editor.plugins.get("GeneralHtmlSupport"), a = [
      ...this._styleDefinitions.inline,
      ...this._styleDefinitions.block
    ], l = a.filter(({ name: d }) => this.value.includes(d)), c = a.find(({ name: d }) => d == e), u = i === void 0 ? !this.value.includes(c.name) : i;
    s.change(() => {
      let d;
      Lq(c) ? d = this._findAffectedBlocks(Bq(r), c) : d = [
        this._styleUtils.getAffectedInlineSelectable(c, r)
      ];
      for (const h of d)
        u ? o.addModelHtmlClass(c.element, c.classes, h) : o.removeModelHtmlClass(c.element, Vq(l, c), h);
    });
  }
  /**
  * Returns a set of elements that should be affected by the block-style change.
  */
  _findAffectedBlocks(e, i) {
    const s = /* @__PURE__ */ new Set();
    for (const r of e) {
      const o = r.getAncestors({
        includeSelf: !0,
        parentFirst: !0
      });
      for (const a of o) {
        if (a.is("rootElement"))
          break;
        const l = this._styleUtils.getAffectedBlocks(i, a);
        if (l) {
          for (const c of l)
            s.add(c);
          break;
        }
      }
    }
    return s;
  }
}
function Vq(n, t) {
  return n.reduce((e, i) => i.name === t.name ? e : e.filter((s) => !i.classes.includes(s)), t.classes);
}
function Lq(n) {
  return "isBlock" in n;
}
function Bq(n) {
  const t = Array.from(n.getSelectedBlocks());
  return t.length ? t : [
    n.getFirstPosition().parent
  ];
}
class Mq extends I {
  constructor() {
    super(...arguments);
    f(this, "_listUtils");
    f(this, "_styleUtils");
    f(this, "_htmlSupport");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "ListStyleSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Zn,
      "GeneralHtmlSupport"
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.plugins.has("ListEditing") && (this._styleUtils = e.plugins.get(Zn), this._listUtils = this.editor.plugins.get("ListUtils"), this._htmlSupport = this.editor.plugins.get("GeneralHtmlSupport"), this.listenTo(this._styleUtils, "isStyleEnabledForBlock", (i, [s, r]) => {
      this._isStyleEnabledForBlock(s, r) && (i.return = !0, i.stop());
    }, {
      priority: "high"
    }), this.listenTo(this._styleUtils, "isStyleActiveForBlock", (i, [s, r]) => {
      this._isStyleActiveForBlock(s, r) && (i.return = !0, i.stop());
    }, {
      priority: "high"
    }), this.listenTo(this._styleUtils, "getAffectedBlocks", (i, [s, r]) => {
      const o = this._getAffectedBlocks(s, r);
      o && (i.return = o, i.stop());
    }, {
      priority: "high"
    }), this.listenTo(this._styleUtils, "getStylePreview", (i, [s, r]) => {
      const o = this._getStylePreview(s, r);
      o && (i.return = o, i.stop());
    }, {
      priority: "high"
    }));
  }
  /**
  * Verifies if the given style is applicable to the provided block element.
  */
  _isStyleEnabledForBlock(e, i) {
    const s = this.editor.model;
    if (![
      "ol",
      "ul",
      "li"
    ].includes(e.element) || !this._listUtils.isListItemBlock(i))
      return !1;
    const r = this._htmlSupport.getGhsAttributeNameForElement(e.element);
    if (e.element == "ol" || e.element == "ul") {
      if (!s.schema.checkAttribute(i, r))
        return !1;
      const a = this._listUtils.isNumberedListType(i.getAttribute("listType")) ? "ol" : "ul";
      return e.element == a;
    } else
      return s.schema.checkAttribute(i, r);
  }
  /**
  * Returns true if the given style is applied to the specified block element.
  */
  _isStyleActiveForBlock(e, i) {
    const s = this._htmlSupport.getGhsAttributeNameForElement(e.element), r = i.getAttribute(s);
    return this._styleUtils.hasAllClasses(r, e.classes);
  }
  /**
  * Returns an array of block elements that style should be applied to.
  */
  _getAffectedBlocks(e, i) {
    return this._isStyleEnabledForBlock(e, i) ? e.element == "li" ? this._listUtils.expandListBlocksToCompleteItems(i, {
      withNested: !1
    }) : this._listUtils.expandListBlocksToCompleteList(i) : null;
  }
  /**
  * Returns a view template definition for the style preview.
  */
  _getStylePreview(e, i) {
    const { element: s, classes: r } = e;
    return s == "ol" || s == "ul" ? {
      tag: s,
      attributes: {
        class: r
      },
      children: [
        {
          tag: "li",
          children: i
        }
      ]
    } : s == "li" ? {
      tag: "ol",
      children: [
        {
          tag: s,
          attributes: {
            class: r
          },
          children: i
        }
      ]
    } : null;
  }
}
class Nq extends I {
  constructor() {
    super(...arguments);
    f(this, "_tableUtils");
    f(this, "_styleUtils");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableStyleSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Zn
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.plugins.has("TableEditing") && (this._styleUtils = e.plugins.get(Zn), this._tableUtils = this.editor.plugins.get("TableUtils"), this.listenTo(this._styleUtils, "isStyleEnabledForBlock", (i, [s, r]) => {
      this._isApplicable(s, r) && (i.return = this._isStyleEnabledForBlock(s, r), i.stop());
    }, {
      priority: "high"
    }), this.listenTo(this._styleUtils, "getAffectedBlocks", (i, [s, r]) => {
      this._isApplicable(s, r) && (i.return = this._getAffectedBlocks(s, r), i.stop());
    }, {
      priority: "high"
    }), this.listenTo(this._styleUtils, "configureGHSDataFilter", (i, [{ block: s }]) => {
      this.editor.plugins.get("DataFilter").loadAllowedConfig(s.filter((o) => o.element == "figcaption").map((o) => ({
        name: "caption",
        classes: o.classes
      })));
    }));
  }
  /**
  * Checks if this plugin's custom logic should be applied for defintion-block pair.
  *
  * @param definition Style definition that is being considered.
  * @param block Block element to check if should be styled.
  * @returns True if the defintion-block pair meet the plugin criteria, false otherwise.
  */
  _isApplicable(e, i) {
    return [
      "td",
      "th"
    ].includes(e.element) ? i.name == "tableCell" : [
      "thead",
      "tbody"
    ].includes(e.element) ? i.name == "table" : !1;
  }
  /**
  * Checks if the style definition should be applied to selected block.
  *
  * @param definition Style definition that is being considered.
  * @param block Block element to check if should be styled.
  * @returns True if the block should be style with the style description, false otherwise.
  */
  _isStyleEnabledForBlock(e, i) {
    if ([
      "td",
      "th"
    ].includes(e.element)) {
      const s = this._tableUtils.getCellLocation(i), o = i.parent.parent, a = o.getAttribute("headingRows") || 0, l = o.getAttribute("headingColumns") || 0, c = s.row < a || s.column < l;
      return e.element == "th" ? c : !c;
    }
    if ([
      "thead",
      "tbody"
    ].includes(e.element)) {
      const s = i.getAttribute("headingRows") || 0;
      return e.element == "thead" ? s > 0 : s < this._tableUtils.getRows(i);
    }
    /* istanbul ignore next -- @preserve */
    return !1;
  }
  /**
  * Gets all blocks that the style should be applied to.
  *
  * @param definition Style definition that is being considered.
  * @param block A block element from selection.
  * @returns An array with the block that was passed as an argument if meets the criteria, null otherwise.
  */
  _getAffectedBlocks(e, i) {
    return this._isStyleEnabledForBlock(e, i) ? [
      i
    ] : null;
  }
}
class Fq extends I {
  constructor() {
    super(...arguments);
    f(this, "_styleUtils");
    f(this, "_htmlSupport");
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "LinkStyleSupport";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Zn,
      "GeneralHtmlSupport"
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor;
    e.plugins.has("LinkEditing") && (this._styleUtils = e.plugins.get(Zn), this._htmlSupport = this.editor.plugins.get("GeneralHtmlSupport"), this.listenTo(this._styleUtils, "isStyleEnabledForInlineSelection", (i, [s, r]) => {
      s.element == "a" && (i.return = this._isStyleEnabled(s, r), i.stop());
    }, {
      priority: "high"
    }), this.listenTo(this._styleUtils, "isStyleActiveForInlineSelection", (i, [s, r]) => {
      s.element == "a" && (i.return = this._isStyleActive(s, r), i.stop());
    }, {
      priority: "high"
    }), this.listenTo(this._styleUtils, "getAffectedInlineSelectable", (i, [s, r]) => {
      if (s.element != "a")
        return;
      const o = this._getAffectedSelectable(s, r);
      o && (i.return = o, i.stop());
    }, {
      priority: "high"
    }));
  }
  /**
  * Verifies if the given style is applicable to the provided document selection.
  */
  _isStyleEnabled(e, i) {
    const s = this.editor.model;
    if (i.isCollapsed)
      return i.hasAttribute("linkHref");
    for (const r of i.getRanges())
      for (const o of r.getItems())
        if ((o.is("$textProxy") || s.schema.isInline(o)) && o.hasAttribute("linkHref"))
          return !0;
    return !1;
  }
  /**
  * Returns true if the given style is applied to the specified document selection.
  */
  _isStyleActive(e, i) {
    const s = this.editor.model, r = this._htmlSupport.getGhsAttributeNameForElement(e.element);
    if (i.isCollapsed) {
      if (i.hasAttribute("linkHref")) {
        const o = i.getAttribute(r);
        if (this._styleUtils.hasAllClasses(o, e.classes))
          return !0;
      }
      return !1;
    }
    for (const o of i.getRanges())
      for (const a of o.getItems())
        if ((a.is("$textProxy") || s.schema.isInline(a)) && a.hasAttribute("linkHref")) {
          const l = a.getAttribute(r);
          return this._styleUtils.hasAllClasses(l, e.classes);
        }
    return !1;
  }
  /**
  * Returns a selectable that given style should be applied to.
  */
  _getAffectedSelectable(e, i) {
    const s = this.editor.model;
    if (i.isCollapsed) {
      const o = i.getAttribute("linkHref");
      return Na(i.getFirstPosition(), "linkHref", o, s);
    }
    const r = [];
    for (const o of i.getRanges()) {
      const a = s.createRange(xw(o.start, "linkHref", !0, s), xw(o.end, "linkHref", !1, s));
      for (const l of a.getItems())
        (l.is("$textProxy") || s.schema.isInline(l)) && l.hasAttribute("linkHref") && r.push(this.editor.model.createRangeOn(l));
    }
    return Dq(r);
  }
}
function xw(n, t, e, i) {
  const s = n.textNode || (e ? n.nodeAfter : n.nodeBefore);
  if (!s || !s.hasAttribute(t))
    return n;
  const r = s.getAttribute(t);
  return yh(n, t, r, e, i);
}
function Dq(n) {
  for (let t = 1; t < n.length; t++) {
    const e = n[t - 1].getJoined(n[t]);
    e && n.splice(--t, 2, e);
  }
  return n;
}
class $q extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "StyleEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      "GeneralHtmlSupport",
      Zn,
      Mq,
      Nq,
      Fq
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.plugins.get("DataSchema"), i = t.plugins.get("StyleUtils"), s = t.config.get("style.definitions"), r = i.normalizeConfig(e, s);
    t.commands.add("style", new Oq(t, r)), i.configureGHSDataFilter(r);
  }
}
class zq extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Style";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      $q,
      Rq
    ];
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function bm(n, t) {
  const { modelAttribute: e, styleName: i, viewElement: s, defaultValue: r, reduceBoxSides: o = !1, shouldUpcast: a = () => !0 } = t;
  n.for("upcast").attributeToAttribute({
    view: {
      name: s,
      styles: {
        [i]: /[\s\S]+/
      }
    },
    model: {
      key: e,
      value: (l) => {
        if (!a(l))
          return;
        const c = l.getNormalizedStyle(i), u = o ? $l(c) : c;
        if (r !== u)
          return u;
      }
    }
  });
}
function Bh(n, t, e, i) {
  n.for("upcast").add((s) => s.on("element:" + t, (r, o, a) => {
    if (!o.modelRange)
      return;
    const l = [
      "border-top-width",
      "border-top-color",
      "border-top-style",
      "border-bottom-width",
      "border-bottom-color",
      "border-bottom-style",
      "border-right-width",
      "border-right-color",
      "border-right-style",
      "border-left-width",
      "border-left-color",
      "border-left-style"
    ].filter((m) => o.viewItem.hasStyle(m));
    if (!l.length)
      return;
    const c = {
      styles: l
    };
    if (!a.consumable.test(o.viewItem, c))
      return;
    const u = [
      ...o.modelRange.getItems({
        shallow: !0
      })
    ].pop();
    a.consumable.consume(o.viewItem, c);
    const d = {
      style: o.viewItem.getNormalizedStyle("border-style"),
      color: o.viewItem.getNormalizedStyle("border-color"),
      width: o.viewItem.getNormalizedStyle("border-width")
    }, h = {
      style: $l(d.style),
      color: $l(d.color),
      width: $l(d.width)
    };
    h.style !== i.style && a.writer.setAttribute(e.style, h.style, u), h.color !== i.color && a.writer.setAttribute(e.color, h.color, u), h.width !== i.width && a.writer.setAttribute(e.width, h.width, u);
  }));
}
function Ko(n, t) {
  const { modelElement: e, modelAttribute: i, styleName: s } = t;
  n.for("downcast").attributeToAttribute({
    model: {
      name: e,
      key: i
    },
    view: (r) => ({
      key: "style",
      value: {
        [s]: r
      }
    })
  });
}
function Dl(n, t) {
  const { modelAttribute: e, styleName: i } = t;
  n.for("downcast").add((s) => s.on(`attribute:${e}:table`, (r, o, a) => {
    const { item: l, attributeNewValue: c } = o, { mapper: u, writer: d } = a;
    if (!a.consumable.consume(o.item, r.name))
      return;
    const h = [
      ...u.toViewElement(l).getChildren()
    ].find((m) => m.is("element", "table"));
    c ? d.setStyle(i, c, h) : d.removeStyle(i, h);
  }));
}
function $l(n) {
  if (!n)
    return;
  const t = [
    "top",
    "right",
    "bottom",
    "left"
  ];
  if (!t.every((r) => n[r]))
    return n;
  const i = n.top;
  return t.every((r) => n[r] === i) ? i : n;
}
function wt(n, t, e, i, s = 1) {
  t != null && s !== void 0 && s !== null && t > s ? i.setAttribute(n, t, e) : i.removeAttribute(n, e);
}
function er(n, t, e = {}) {
  const i = n.createElement("tableCell", e);
  return n.insertElement("paragraph", i), n.insert(i, t), i;
}
function Mh(n, t) {
  const e = t.parent.parent, i = parseInt(e.getAttribute("headingColumns") || "0"), { column: s } = n.getCellLocation(t);
  return !!i && s < i;
}
function zl(n, t, e) {
  const { modelAttribute: i } = e;
  n.extend("tableCell", {
    allowAttributes: [
      i
    ]
  }), bm(t, {
    viewElement: /^(td|th)$/,
    ...e
  }), Ko(t, {
    modelElement: "tableCell",
    ...e
  });
}
function Bs(n) {
  const t = n.getSelectedElement();
  return t && t.is("element", "table") ? t : n.getFirstPosition().findAncestor("table");
}
function Hq() {
  return (n) => {
    n.on("element:figure", (t, e, i) => {
      if (!i.consumable.test(e.viewItem, {
        name: !0,
        classes: "table"
      }))
        return;
      const s = Wq(e.viewItem);
      if (!s || !i.consumable.test(s, {
        name: !0
      }))
        return;
      i.consumable.consume(e.viewItem, {
        name: !0,
        classes: "table"
      });
      const r = i.convertItem(s, e.modelCursor), o = Xe(r.modelRange.getItems());
      if (!o) {
        i.consumable.revert(e.viewItem, {
          name: !0,
          classes: "table"
        });
        return;
      }
      i.convertChildren(e.viewItem, i.writer.createPositionAt(o, "end")), i.updateConversionResult(o, e);
    });
  };
}
function jq() {
  return (n) => {
    n.on("element:table", (t, e, i) => {
      const s = e.viewItem;
      if (!i.consumable.test(s, {
        name: !0
      }))
        return;
      const { rows: r, headingRows: o, headingColumns: a } = qq(s), l = {};
      a && (l.headingColumns = a), o && (l.headingRows = o);
      const c = i.writer.createElement("table", l);
      if (i.safeInsert(c, e.modelCursor)) {
        if (i.consumable.consume(s, {
          name: !0
        }), r.forEach((u) => i.convertItem(u, i.writer.createPositionAt(c, "end"))), i.convertChildren(s, i.writer.createPositionAt(c, "end")), c.isEmpty) {
          const u = i.writer.createElement("tableRow");
          i.writer.insert(u, i.writer.createPositionAt(c, "end")), er(i.writer, i.writer.createPositionAt(u, "end"));
        }
        i.updateConversionResult(c, e);
      }
    });
  };
}
function Uq() {
  return (n) => {
    n.on("element:tr", (t, e) => {
      e.viewItem.isEmpty && e.modelCursor.index == 0 && t.stop();
    }, {
      priority: "high"
    });
  };
}
function Tw(n) {
  return (t) => {
    t.on(`element:${n}`, (e, i, { writer: s }) => {
      if (!i.modelRange)
        return;
      const r = i.modelRange.start.nodeAfter, o = s.createPositionAt(r, 0);
      if (i.viewItem.isEmpty) {
        s.insertElement("paragraph", o);
        return;
      }
      const a = Array.from(r.getChildren());
      if (a.every((l) => l.is("element", "$marker"))) {
        const l = s.createElement("paragraph");
        s.insert(l, s.createPositionAt(r, 0));
        for (const c of a)
          s.move(s.createRangeOn(c), s.createPositionAt(l, "end"));
      }
    }, {
      priority: "low"
    });
  };
}
function Wq(n) {
  for (const t of n.getChildren())
    if (t.is("element", "table"))
      return t;
}
function qq(n) {
  let t = 0, e;
  const i = [], s = [];
  let r;
  for (const o of Array.from(n.getChildren())) {
    if (o.name !== "tbody" && o.name !== "thead" && o.name !== "tfoot")
      continue;
    o.name === "thead" && !r && (r = o);
    const a = Array.from(o.getChildren()).filter((c) => c.is("element", "tr"));
    let l = null;
    for (const c of a) {
      const u = Array.from(c.getChildren()).filter((d) => d.is("element", "td") || d.is("element", "th"));
      if (r && o === r || o.name === "tbody" && u.length > 0 && // These conditions handles the case when the first column is a <th> element and it's the only column in the row.
      // This case is problematic because it's not clear if this row should be a heading row or not, as it may be result
      // of the cell span from the previous row.
      // Issue: https://github.com/ckeditor/ckeditor5/issues/17556
      (l === null || u.length === l) && u.every((d) => d.is("element", "th")))
        t++, i.push(c);
      else {
        s.push(c);
        const d = Gq(c);
        (!e || d < e) && (e = d);
      }
      l = Math.max(l || 0, u.length);
    }
  }
  return {
    headingRows: t,
    headingColumns: e || 0,
    rows: [
      ...i,
      ...s
    ]
  };
}
function Gq(n) {
  let t = 0, e = 0;
  const i = Array.from(n.getChildren()).filter((s) => s.name === "th" || s.name === "td");
  for (; e < i.length && i[e].name === "th"; ) {
    const s = i[e], r = parseInt(s.getAttribute("colspan") || "1");
    t = t + r, e++;
  }
  return t;
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
class Ke {
  /**
  * Creates an instance of the table walker.
  *
  * The table walker iterates internally by traversing the table from row index = 0 and column index = 0.
  * It walks row by row and column by column in order to output values defined in the constructor.
  * By default it will output only the locations that are occupied by a cell. To include also spanned rows and columns,
  * pass the `includeAllSlots` option to the constructor.
  *
  * The most important values of the iterator are column and row indexes of a cell.
  *
  * See {@link module:table/tablewalker~TableSlot} what values are returned by the table walker.
  *
  * To iterate over a given row:
  *
  * ```ts
  * const tableWalker = new TableWalker( table, { startRow: 1, endRow: 2 } );
  *
  * for ( const tableSlot of tableWalker ) {
  *   console.log( 'A cell at row', tableSlot.row, 'and column', tableSlot.column );
  * }
  * ```
  *
  * For instance the code above for the following table:
  *
  *  +----+----+----+----+----+----+
  *  | 00      | 02 | 03 | 04 | 05 |
  *  |         +----+----+----+----+
  *  |         | 12      | 14 | 15 |
  *  |         +----+----+----+    +
  *  |         | 22           |    |
  *  |----+----+----+----+----+    +
  *  | 30 | 31 | 32 | 33 | 34 |    |
  *  +----+----+----+----+----+----+
  *
  * will log in the console:
  *
  *  'A cell at row 1 and column 2'
  *  'A cell at row 1 and column 4'
  *  'A cell at row 1 and column 5'
  *  'A cell at row 2 and column 2'
  *
  * To also iterate over spanned cells:
  *
  * ```ts
  * const tableWalker = new TableWalker( table, { row: 1, includeAllSlots: true } );
  *
  * for ( const tableSlot of tableWalker ) {
  *   console.log( 'Slot at', tableSlot.row, 'x', tableSlot.column, ':', tableSlot.isAnchor ? 'is anchored' : 'is spanned' );
  * }
  * ```
  *
  * will log in the console for the table from the previous example:
  *
  *  'Cell at 1 x 0 : is spanned'
  *  'Cell at 1 x 1 : is spanned'
  *  'Cell at 1 x 2 : is anchored'
  *  'Cell at 1 x 3 : is spanned'
  *  'Cell at 1 x 4 : is anchored'
  *  'Cell at 1 x 5 : is anchored'
  *
  * **Note**: Option `row` is a shortcut that sets both `startRow` and `endRow` to the same row.
  * (Use either `row` or `startRow` and `endRow` but never together). Similarly the `column` option sets both `startColumn`
  * and `endColumn` to the same column (Use either `column` or `startColumn` and `endColumn` but never together).
  *
  * @param table A table over which the walker iterates.
  * @param options An object with configuration.
  * @param options.row A row index for which this iterator will output cells. Can't be used together with `startRow` and `endRow`.
  * @param options.startRow A row index from which this iterator should start. Can't be used together with `row`. Default value is 0.
  * @param options.endRow A row index at which this iterator should end. Can't be used together with `row`.
  * @param options.column A column index for which this iterator will output cells.
  * Can't be used together with `startColumn` and `endColumn`.
  * @param options.startColumn A column index from which this iterator should start.
  * Can't be used together with `column`. Default value is 0.
  * @param options.endColumn A column index at which this iterator should end. Can't be used together with `column`.
  * @param options.includeAllSlots Also return values for spanned cells. Default value is "false".
  */
  constructor(t, e = {}) {
    /**
    * The walker's table element.
    *
    * @internal
    */
    f(this, "_table");
    /**
    * A row index from which this iterator will start.
    */
    f(this, "_startRow");
    /**
    * A row index at which this iterator will end.
    */
    f(this, "_endRow");
    /**
    * If set, the table walker will only output cells from a given column and following ones or cells that overlap them.
    */
    f(this, "_startColumn");
    /**
    * If set, the table walker will only output cells up to a given column.
    */
    f(this, "_endColumn");
    /**
    * Enables output of spanned cells that are normally not yielded.
    */
    f(this, "_includeAllSlots");
    /**
    * Row indexes to skip from the iteration.
    */
    f(this, "_skipRows");
    /**
    * The current row index.
    *
    * @internal
    */
    f(this, "_row");
    /**
    * The index of the current row element in the table.
    *
    * @internal
    */
    f(this, "_rowIndex");
    /**
    * The current column index.
    *
    * @internal
    */
    f(this, "_column");
    /**
    * The cell index in a parent row. For spanned cells when {@link #_includeAllSlots} is set to `true`,
    * this represents the index of the next table cell.
    *
    * @internal
    */
    f(this, "_cellIndex");
    /**
    * Holds a map of spanned cells in a table.
    */
    f(this, "_spannedCells");
    /**
    * Index of the next column where a cell is anchored.
    */
    f(this, "_nextCellAtColumn");
    /**
    * Indicates whether the iterator jumped to (or close to) the start row, ignoring rows that don't need to be traversed.
    */
    f(this, "_jumpedToStartRow", !1);
    this._table = t, this._startRow = e.row !== void 0 ? e.row : e.startRow || 0, this._endRow = e.row !== void 0 ? e.row : e.endRow, this._startColumn = e.column !== void 0 ? e.column : e.startColumn || 0, this._endColumn = e.column !== void 0 ? e.column : e.endColumn, this._includeAllSlots = !!e.includeAllSlots, this._skipRows = /* @__PURE__ */ new Set(), this._row = 0, this._rowIndex = 0, this._column = 0, this._cellIndex = 0, this._spannedCells = /* @__PURE__ */ new Map(), this._nextCellAtColumn = -1;
  }
  /**
  * Iterable interface.
  */
  [Symbol.iterator]() {
    return this;
  }
  /**
  * Gets the next table walker's value.
  *
  * @returns The next table walker's value.
  */
  next() {
    this._canJumpToStartRow() && this._jumpToNonSpannedRowClosestToStartRow();
    const t = this._table.getChild(this._rowIndex);
    if (!t || this._isOverEndRow())
      return {
        done: !0,
        value: void 0
      };
    if (!t.is("element", "tableRow"))
      return this._rowIndex++, this.next();
    if (this._isOverEndColumn())
      return this._advanceToNextRow();
    let e = null;
    const i = this._getSpanned();
    if (i)
      this._includeAllSlots && !this._shouldSkipSlot() && (e = this._formatOutValue(i.cell, i.row, i.column));
    else {
      const s = t.getChild(this._cellIndex);
      if (!s)
        return this._advanceToNextRow();
      const r = parseInt(s.getAttribute("colspan") || "1"), o = parseInt(s.getAttribute("rowspan") || "1");
      (r > 1 || o > 1) && this._recordSpans(s, o, r), this._shouldSkipSlot() || (e = this._formatOutValue(s)), this._nextCellAtColumn = this._column + r;
    }
    return this._column++, this._column == this._nextCellAtColumn && this._cellIndex++, e || this.next();
  }
  /**
  * Marks a row to skip in the next iteration. It will also skip cells from the current row if there are any cells from the current row
  * to output.
  *
  * @param row The row index to skip.
  */
  skipRow(t) {
    this._skipRows.add(t);
  }
  /**
  * Advances internal cursor to the next row.
  */
  _advanceToNextRow() {
    return this._row++, this._rowIndex++, this._column = 0, this._cellIndex = 0, this._nextCellAtColumn = -1, this.next();
  }
  /**
  * Checks if the current row is over {@link #_endRow}.
  */
  _isOverEndRow() {
    return this._endRow !== void 0 && this._row > this._endRow;
  }
  /**
  * Checks if the current cell is over {@link #_endColumn}
  */
  _isOverEndColumn() {
    return this._endColumn !== void 0 && this._column > this._endColumn;
  }
  /**
  * A common method for formatting the iterator's output value.
  *
  * @param cell The table cell to output.
  * @param anchorRow The row index of a cell anchor slot.
  * @param anchorColumn The column index of a cell anchor slot.
  */
  _formatOutValue(t, e = this._row, i = this._column) {
    return {
      done: !1,
      value: new Kq(this, t, e, i)
    };
  }
  /**
  * Checks if the current slot should be skipped.
  */
  _shouldSkipSlot() {
    const t = this._skipRows.has(this._row), e = this._row < this._startRow, i = this._column < this._startColumn, s = this._endColumn !== void 0 && this._column > this._endColumn;
    return t || e || i || s;
  }
  /**
  * Returns the cell element that is spanned over the current cell location.
  */
  _getSpanned() {
    const t = this._spannedCells.get(this._row);
    return t && t.get(this._column) || null;
  }
  /**
  * Updates spanned cells map relative to the current cell location and its span dimensions.
  *
  * @param cell A cell that is spanned.
  * @param rowspan Cell height.
  * @param colspan Cell width.
  */
  _recordSpans(t, e, i) {
    const s = {
      cell: t,
      row: this._row,
      column: this._column
    };
    for (let r = this._row; r < this._row + e; r++)
      for (let o = this._column; o < this._column + i; o++)
        (r != this._row || o != this._column) && this._markSpannedCell(r, o, s);
  }
  /**
  * Marks the cell location as spanned by another cell.
  *
  * @param row The row index of the cell location.
  * @param column The column index of the cell location.
  * @param data A spanned cell details (cell element, anchor row and column).
  */
  _markSpannedCell(t, e, i) {
    this._spannedCells.has(t) || this._spannedCells.set(t, /* @__PURE__ */ new Map()), this._spannedCells.get(t).set(e, i);
  }
  /**
  * Checks if part of the table can be skipped.
  */
  _canJumpToStartRow() {
    return !!this._startRow && this._startRow > 0 && !this._jumpedToStartRow;
  }
  /**
  * Sets the current row to `this._startRow` or the first row before it that has the number of cells
  * equal to the number of columns in the table.
  *
  * Example:
  * 	+----+----+----+
  *  | 00 | 01 | 02 |
  *  |----+----+----+
  *  | 10      | 12 |
  *  |         +----+
  *  |         | 22 |
  *  |         +----+
  *  |         | 32 | <--- Start row
  *  +----+----+----+
  *  | 40 | 41 | 42 |
  *  +----+----+----+
  *
  * If the 4th row is a `this._startRow`, this method will:
  * 1.) Count the number of columns this table has based on the first row (3 columns in this case).
  * 2.) Check if the 4th row contains 3 cells. It doesn't, so go to the row before it.
  * 3.) Check if the 3rd row contains 3 cells. It doesn't, so go to the row before it.
  * 4.) Check if the 2nd row contains 3 cells. It does, so set the current row to that row.
  *
  * Setting the current row this way is necessary to let the `next()`  method loop over the cells
  * spanning multiple rows or columns and update the `this._spannedCells` property.
  */
  _jumpToNonSpannedRowClosestToStartRow() {
    const t = this._getRowLength(0);
    for (let e = this._startRow; !this._jumpedToStartRow; e--)
      t === this._getRowLength(e) && (this._row = e, this._rowIndex = e, this._jumpedToStartRow = !0);
  }
  /**
  * Returns a number of columns in a row taking `colspan` into consideration.
  */
  _getRowLength(t) {
    return [
      ...this._table.getChild(t).getChildren()
    ].reduce((i, s) => i + parseInt(s.getAttribute("colspan") || "1"), 0);
  }
}
class Kq {
  /**
  * Creates an instance of the table walker value.
  *
  * @param tableWalker The table walker instance.
  * @param cell The current table cell.
  * @param anchorRow The row index of a cell anchor slot.
  * @param anchorColumn The column index of a cell anchor slot.
  */
  constructor(t, e, i, s) {
    /**
    * The current table cell.
    */
    f(this, "cell");
    /**
    * The row index of a table slot.
    */
    f(this, "row");
    /**
    * The column index of a table slot.
    */
    f(this, "column");
    /**
    * The row index of a cell anchor slot.
    */
    f(this, "cellAnchorRow");
    /**
    * The column index of a cell anchor slot.
    */
    f(this, "cellAnchorColumn");
    /**
    * The index of the current cell in the parent row.
    */
    f(this, "_cellIndex");
    /**
    * The index of the current row element in the table.
    */
    f(this, "_rowIndex");
    /**
    * The table element.
    */
    f(this, "_table");
    this.cell = e, this.row = t._row, this.column = t._column, this.cellAnchorRow = i, this.cellAnchorColumn = s, this._cellIndex = t._cellIndex, this._rowIndex = t._rowIndex, this._table = t._table;
  }
  // @if CK_DEBUG // public get isSpanned(): unknown { return throwMissingGetterError( 'isSpanned' ); }
  // @if CK_DEBUG // public get colspan(): unknown { return throwMissingGetterError( 'colspan' ); }
  // @if CK_DEBUG // public get rowspan(): unknown { return throwMissingGetterError( 'rowspan' ); }
  // @if CK_DEBUG // public get cellIndex(): unknown { return throwMissingGetterError( 'cellIndex' ); }
  /**
  * Whether the cell is anchored in the current slot.
  */
  get isAnchor() {
    return this.row === this.cellAnchorRow && this.column === this.cellAnchorColumn;
  }
  /**
  * The width of a cell defined by a `colspan` attribute. If the model attribute is not present, it is set to `1`.
  */
  get cellWidth() {
    return parseInt(this.cell.getAttribute("colspan") || "1");
  }
  /**
  * The height of a cell defined by a `rowspan` attribute. If the model attribute is not present, it is set to `1`.
  */
  get cellHeight() {
    return parseInt(this.cell.getAttribute("rowspan") || "1");
  }
  /**
  * The index of the current row element in the table.
  */
  get rowIndex() {
    return this._rowIndex;
  }
  /**
  * Returns the {@link module:engine/model/position~Position} before the table slot.
  */
  getPositionBefore() {
    return this._table.root.document.model.createPositionAt(this._table.getChild(this.row), this._cellIndex);
  }
}
function Ew(n, t) {
  return (e, { writer: i }) => {
    const s = e.getAttribute("headingRows") || 0, r = i.createContainerElement("table", null, []), o = i.createContainerElement("figure", {
      class: "table"
    }, r);
    s > 0 && i.insert(i.createPositionAt(r, "end"), i.createContainerElement("thead", null, i.createSlot((a) => a.is("element", "tableRow") && a.index < s))), s < n.getRows(e) && i.insert(i.createPositionAt(r, "end"), i.createContainerElement("tbody", null, i.createSlot((a) => a.is("element", "tableRow") && a.index >= s)));
    for (const { positionOffset: a, filter: l } of t.additionalSlots)
      i.insert(i.createPositionAt(r, a), i.createSlot(l));
    return i.insert(i.createPositionAt(r, "after"), i.createSlot((a) => a.is("element", "tableRow") ? !1 : !t.additionalSlots.some(({ filter: l }) => l(a)))), t.asWidget ? Yq(o, i) : o;
  };
}
function Zq() {
  return (n, { writer: t }) => n.isEmpty ? t.createEmptyElement("tr") : t.createContainerElement("tr");
}
function Sw(n = {}) {
  return (t, { writer: e }) => {
    const i = t.parent, s = i.parent, r = s.getChildIndex(i), o = new Ke(s, {
      row: r
    }), a = s.getAttribute("headingRows") || 0, l = s.getAttribute("headingColumns") || 0;
    let c = null;
    for (const u of o)
      if (u.cell == t) {
        const h = u.row < a || u.column < l ? "th" : "td";
        c = n.asWidget ? Kf(e.createEditableElement(h), e) : e.createContainerElement(h);
        break;
      }
    return c;
  };
}
function Aw(n = {}) {
  return (t, { writer: e }) => {
    if (!t.parent.is("element", "tableCell") || !HC(t))
      return null;
    if (n.asWidget)
      return e.createContainerElement("span", {
        class: "ck-table-bogus-paragraph"
      });
    {
      const i = e.createContainerElement("p");
      return e.setCustomProperty("dataPipeline:transparentRendering", !0, i), i;
    }
  };
}
function HC(n) {
  return n.parent.childCount == 1 && !Jq(n);
}
function Yq(n, t) {
  return t.setCustomProperty("table", !0, n), Qn(n, t, {
    hasSelectionHandle: !0
  });
}
function Jq(n) {
  for (const t of n.getAttributeKeys())
    if (!(t.startsWith("selection:") || t == "htmlEmptyBlock"))
      return !0;
  return !1;
}
class Xq extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = t.document.selection, i = t.schema;
    this.isEnabled = Qq(e, i);
  }
  /**
  * Executes the command.
  *
  * Inserts a table with the given number of rows and columns into the editor.
  *
  * @param options.rows The number of rows to create in the inserted table. Default value is 2.
  * @param options.columns The number of columns to create in the inserted table. Default value is 2.
  * @param options.headingRows The number of heading rows. If not provided it will default to
  * {@link module:table/tableconfig~TableConfig#defaultHeadings `config.table.defaultHeadings.rows`} table config.
  * @param options.headingColumns The number of heading columns. If not provided it will default to
  * {@link module:table/tableconfig~TableConfig#defaultHeadings `config.table.defaultHeadings.columns`} table config.
  * @fires execute
  */
  execute(t = {}) {
    const e = this.editor, i = e.model, s = e.plugins.get("TableUtils"), r = e.config.get("table.defaultHeadings.rows"), o = e.config.get("table.defaultHeadings.columns");
    t.headingRows === void 0 && r && (t.headingRows = r), t.headingColumns === void 0 && o && (t.headingColumns = o), i.change((a) => {
      const l = s.createTable(a, t);
      i.insertObject(l, null, null, {
        findOptimalPosition: "auto"
      }), a.setSelection(a.createPositionAt(l.getNodeByPath([
        0,
        0,
        0
      ]), 0));
    });
  }
}
function Qq(n, t) {
  const e = n.getFirstPosition().parent, i = e === e.root ? e : e.parent;
  return t.checkChild(i, "table");
}
class Iw extends he {
  /**
  * Creates a new `InsertRowCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param options.order The order of insertion relative to the row in which the caret is located.
  * Possible values: `"above"` and `"below"`. Default value is "below"
  */
  constructor(e, i = {}) {
    super(e);
    /**
    * The order of insertion relative to the row in which the caret is located.
    */
    f(this, "order");
    this.order = i.order || "below";
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model.document.selection, s = !!this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(e).length;
    this.isEnabled = s;
  }
  /**
  * Executes the command.
  *
  * Depending on the command's {@link #order} value, it inserts a row `'below'` or `'above'` the row in which selection is set.
  *
  * @fires execute
  */
  execute() {
    const e = this.editor, i = e.model.document.selection, s = e.plugins.get("TableUtils"), r = this.order === "above", o = s.getSelectionAffectedTableCells(i), a = s.getRowIndexes(o), l = r ? a.first : a.last, c = o[0].findAncestor("table");
    s.insertRows(c, {
      at: r ? l : l + 1,
      copyStructureFromAbove: !r
    });
  }
}
class Pw extends he {
  /**
  * Creates a new `InsertColumnCommand` instance.
  *
  * @param editor An editor on which this command will be used.
  * @param options.order The order of insertion relative to the column in which the caret is located.
  * Possible values: `"left"` and `"right"`. Default value is "right".
  */
  constructor(e, i = {}) {
    super(e);
    /**
    * The order of insertion relative to the column in which the caret is located.
    */
    f(this, "order");
    this.order = i.order || "right";
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.model.document.selection, s = !!this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(e).length;
    this.isEnabled = s;
  }
  /**
  * Executes the command.
  *
  * Depending on the command's {@link #order} value, it inserts a column to the `'left'` or `'right'` of the column
  * in which the selection is set.
  *
  * @fires execute
  */
  execute() {
    const e = this.editor, i = e.model.document.selection, s = e.plugins.get("TableUtils"), r = this.order === "left", o = s.getSelectionAffectedTableCells(i), a = s.getColumnIndexes(o), l = r ? a.first : a.last, c = o[0].findAncestor("table");
    s.insertColumns(c, {
      columns: 1,
      at: r ? l : l + 1
    });
  }
}
class Rw extends he {
  /**
  * Creates a new `SplitCellCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param options.direction Indicates whether the command should split cells `'horizontally'` or `'vertically'`.
  */
  constructor(e, i = {}) {
    super(e);
    /**
    * The direction that indicates which cell will be split.
    */
    f(this, "direction");
    this.direction = i.direction || "horizontally";
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const i = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(this.editor.model.document.selection);
    this.isEnabled = i.length === 1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const e = this.editor.plugins.get("TableUtils"), i = e.getSelectionAffectedTableCells(this.editor.model.document.selection)[0];
    this.direction === "horizontally" ? e.splitCellHorizontally(i, 2) : e.splitCellVertically(i, 2);
  }
}
function jC(n, t, e) {
  const { startRow: i, startColumn: s, endRow: r, endColumn: o } = t, a = e.createElement("table"), l = r - i + 1;
  for (let u = 0; u < l; u++)
    e.insertElement("tableRow", a, "end");
  const c = [
    ...new Ke(n, {
      startRow: i,
      endRow: r,
      startColumn: s,
      endColumn: o,
      includeAllSlots: !0
    })
  ];
  for (const { row: u, column: d, cell: h, isAnchor: m, cellAnchorRow: g, cellAnchorColumn: p } of c) {
    const w = u - i, y = a.getChild(w);
    if (!m)
      (g < i || p < s) && er(e, e.createPositionAt(y, "end"));
    else {
      const k = e.cloneElement(h);
      e.append(k, y), KC(k, u, d, r, o, e);
    }
  }
  return eG(a, n, i, s, e), a;
}
function UC(n, t, e = 0) {
  const i = [], s = new Ke(n, {
    startRow: e,
    endRow: t - 1
  });
  for (const r of s) {
    const { row: o, cellHeight: a } = r, l = o + a - 1;
    o < t && t <= l && i.push(r);
  }
  return i;
}
function WC(n, t, e) {
  const i = n.parent, s = i.parent, r = i.index, o = parseInt(n.getAttribute("rowspan")), a = t - r, l = {}, c = o - a;
  c > 1 && (l.rowspan = c);
  const u = parseInt(n.getAttribute("colspan") || "1");
  u > 1 && (l.colspan = u);
  const d = r, h = d + a, m = [
    ...new Ke(s, {
      startRow: d,
      endRow: h,
      includeAllSlots: !0
    })
  ];
  let g = null, p;
  for (const w of m) {
    const { row: y, column: k, cell: C } = w;
    C === n && p === void 0 && (p = k), p !== void 0 && p === k && y === h && (g = er(e, w.getPositionBefore(), l));
  }
  return wt("rowspan", a, n, e), g;
}
function qC(n, t) {
  const e = [], i = new Ke(n);
  for (const s of i) {
    const { column: r, cellWidth: o } = s, a = r + o - 1;
    r < t && t <= a && e.push(s);
  }
  return e;
}
function GC(n, t, e, i) {
  const s = parseInt(n.getAttribute("colspan")), r = e - t, o = {}, a = s - r;
  a > 1 && (o.colspan = a);
  const l = parseInt(n.getAttribute("rowspan") || "1");
  l > 1 && (o.rowspan = l);
  const c = er(i, i.createPositionAfter(n), o);
  return wt("colspan", r, n, i), c;
}
function KC(n, t, e, i, s, r) {
  const o = parseInt(n.getAttribute("colspan") || "1"), a = parseInt(n.getAttribute("rowspan") || "1");
  if (e + o - 1 > s) {
    const u = s - e + 1;
    wt("colspan", u, n, r, 1);
  }
  if (t + a - 1 > i) {
    const u = i - t + 1;
    wt("rowspan", u, n, r, 1);
  }
}
function eG(n, t, e, i, s) {
  const r = parseInt(t.getAttribute("headingRows") || "0");
  if (r > 0) {
    const a = r - e;
    wt("headingRows", a, n, s, 0);
  }
  const o = parseInt(t.getAttribute("headingColumns") || "0");
  if (o > 0) {
    const a = o - i;
    wt("headingColumns", a, n, s, 0);
  }
}
function Nh(n, t) {
  const e = t.getColumns(n), i = new Array(e).fill(0);
  for (const { column: r } of new Ke(n))
    i[r]++;
  const s = i.reduce((r, o, a) => o ? r : [
    ...r,
    a
  ], []);
  if (s.length > 0) {
    const r = s[s.length - 1];
    return t.removeColumns(n, {
      at: r
    }), !0;
  }
  return !1;
}
function Fh(n, t) {
  const e = [], i = t.getRows(n);
  for (let s = 0; s < i; s++)
    n.getChild(s).isEmpty && e.push(s);
  if (e.length > 0) {
    const s = e[e.length - 1];
    return t.removeRows(n, {
      at: s
    }), !0;
  }
  return !1;
}
function wm(n, t) {
  Nh(n, t) || Fh(n, t);
}
function ZC(n, t) {
  const e = Array.from(new Ke(n, {
    startColumn: t.firstColumn,
    endColumn: t.lastColumn,
    row: t.lastRow
  }));
  if (e.every(({ cellHeight: r }) => r === 1))
    return t.lastRow;
  const s = e[0].cellHeight - 1;
  return t.lastRow + s;
}
function YC(n, t) {
  const e = Array.from(new Ke(n, {
    startRow: t.firstRow,
    endRow: t.lastRow,
    column: t.lastColumn
  }));
  if (e.every(({ cellWidth: r }) => r === 1))
    return t.lastColumn;
  const s = e[0].cellWidth - 1;
  return t.lastColumn + s;
}
class xl extends he {
  /**
  * Creates a new `MergeCellCommand` instance.
  *
  * @param editor The editor on which this command will be used.
  * @param options.direction Indicates which cell to merge with the currently selected one.
  * Possible values are: `'left'`, `'right'`, `'up'` and `'down'`.
  */
  constructor(e, i) {
    super(e);
    /**
    * The direction that indicates which cell will be merged with the currently selected one.
    */
    f(this, "direction");
    /**
    * Whether the merge is horizontal (left/right) or vertical (up/down).
    */
    f(this, "isHorizontal");
    this.direction = i.direction, this.isHorizontal = this.direction == "right" || this.direction == "left";
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this._getMergeableCell();
    this.value = e, this.isEnabled = !!e;
  }
  /**
  * Executes the command.
  *
  * Depending on the command's {@link #direction} value, it will merge the cell that is to the `'left'`, `'right'`, `'up'` or `'down'`.
  *
  * @fires execute
  */
  execute() {
    const e = this.editor.model, i = e.document, r = this.editor.plugins.get("TableUtils").getTableCellsContainingSelection(i.selection)[0], o = this.value, a = this.direction;
    e.change((l) => {
      const c = a == "right" || a == "down", u = c ? r : o, d = c ? o : r, h = d.parent;
      nG(d, u, l);
      const m = this.isHorizontal ? "colspan" : "rowspan", g = parseInt(r.getAttribute(m) || "1"), p = parseInt(o.getAttribute(m) || "1");
      l.setAttribute(m, g + p, u), l.setSelection(l.createRangeIn(u));
      const w = this.editor.plugins.get("TableUtils"), y = h.findAncestor("table");
      wm(y, w);
    });
  }
  /**
  * Returns a cell that can be merged with the current cell depending on the command's direction.
  */
  _getMergeableCell() {
    const i = this.editor.model.document, s = this.editor.plugins.get("TableUtils"), r = s.getTableCellsContainingSelection(i.selection)[0];
    if (!r)
      return;
    const o = this.isHorizontal ? tG(r, this.direction, s) : iG(r, this.direction, s);
    if (!o)
      return;
    const a = this.isHorizontal ? "rowspan" : "colspan", l = parseInt(r.getAttribute(a) || "1");
    if (parseInt(o.getAttribute(a) || "1") === l)
      return o;
  }
}
function tG(n, t, e) {
  const s = n.parent.parent, r = t == "right" ? n.nextSibling : n.previousSibling, o = (s.getAttribute("headingColumns") || 0) > 0;
  if (!r)
    return;
  const a = t == "right" ? n : r, l = t == "right" ? r : n, { column: c } = e.getCellLocation(a), { column: u } = e.getCellLocation(l), d = parseInt(a.getAttribute("colspan") || "1"), h = Mh(e, a), m = Mh(e, l);
  return o && h != m ? void 0 : c + d === u ? r : void 0;
}
function iG(n, t, e) {
  const i = n.parent, s = i.parent, r = s.getChildIndex(i);
  if (t == "down" && r === e.getRows(s) - 1 || t == "up" && r === 0)
    return null;
  const o = parseInt(n.getAttribute("rowspan") || "1"), a = s.getAttribute("headingRows") || 0, l = t == "down" && r + o === a, c = t == "up" && r === a;
  if (a && (l || c))
    return null;
  const u = parseInt(n.getAttribute("rowspan") || "1"), d = t == "down" ? r + u : r, h = [
    ...new Ke(s, {
      endRow: d
    })
  ], g = h.find((w) => w.cell === n).column, p = h.find(({ row: w, cellHeight: y, column: k }) => k !== g ? !1 : t == "down" ? w === d : d === w + y);
  return p && p.cell ? p.cell : null;
}
function nG(n, t, e) {
  Ow(n) || (Ow(t) && e.remove(e.createRangeIn(t)), e.move(e.createRangeIn(n), e.createPositionAt(t, "end"))), e.remove(n);
}
function Ow(n) {
  const t = n.getChild(0);
  return n.childCount == 1 && t.is("element", "paragraph") && t.isEmpty;
}
class sG extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get("TableUtils"), e = t.getSelectionAffectedTableCells(this.editor.model.document.selection), i = e[0];
    if (i) {
      const s = i.findAncestor("table"), o = t.getRows(s) - 1, a = t.getRowIndexes(e), l = a.first === 0 && a.last === o;
      this.isEnabled = !l;
    } else
      this.isEnabled = !1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = this.editor.plugins.get("TableUtils"), i = e.getSelectionAffectedTableCells(t.document.selection), s = e.getRowIndexes(i), r = i[0], o = r.findAncestor("table"), a = e.getCellLocation(r).column;
    t.change((l) => {
      const c = s.last - s.first + 1;
      e.removeRows(o, {
        at: s.first,
        rows: c
      });
      const u = rG(o, s.first, a, e.getRows(o));
      l.setSelection(l.createPositionAt(u, 0));
    });
  }
}
function rG(n, t, e, i) {
  const s = n.getChild(Math.min(t, i - 1));
  let r = s.getChild(0), o = 0;
  for (const a of s.getChildren()) {
    if (o > e)
      return r;
    r = a, o += parseInt(a.getAttribute("colspan") || "1");
  }
  return r;
}
class oG extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get("TableUtils"), e = t.getSelectionAffectedTableCells(this.editor.model.document.selection), i = e[0];
    if (i) {
      const s = i.findAncestor("table"), r = t.getColumns(s), { first: o, last: a } = t.getColumnIndexes(e);
      this.isEnabled = a - o < r - 1;
    } else
      this.isEnabled = !1;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.plugins.get("TableUtils"), [e, i] = lG(this.editor.model.document.selection, t), s = e.parent.parent, r = [
      ...new Ke(s)
    ], o = {
      first: r.find((l) => l.cell === e).column,
      last: r.find((l) => l.cell === i).column
    }, a = aG(r, e, i, o);
    this.editor.model.change((l) => {
      const c = o.last - o.first + 1;
      t.removeColumns(s, {
        at: o.first,
        columns: c
      }), l.setSelection(l.createPositionAt(a, 0));
    });
  }
}
function aG(n, t, e, i) {
  return parseInt(e.getAttribute("colspan") || "1") > 1 ? e : t.previousSibling || e.nextSibling ? e.nextSibling || t.previousSibling : i.first ? n.reverse().find(({ column: r }) => r < i.first).cell : n.reverse().find(({ column: r }) => r > i.last).cell;
}
function lG(n, t) {
  const e = t.getSelectionAffectedTableCells(n), i = e[0], s = e.pop(), r = [
    i,
    s
  ];
  return i.isBefore(s) ? r : r.reverse();
}
class cG extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get("TableUtils"), e = this.editor.model, i = t.getSelectionAffectedTableCells(e.document.selection), s = i.length > 0;
    this.isEnabled = s, this.value = s && i.every((r) => this._isInHeading(r, r.parent.parent));
  }
  /**
  * Executes the command.
  *
  * When the selection is in a non-header row, the command will set the `headingRows` table attribute to cover that row.
  *
  * When the selection is already in a header row, it will set `headingRows` so the heading section will end before that row.
  *
  * @fires execute
  * @param options.forceValue If set, the command will set (`true`) or unset (`false`) the header rows according to
  * the `forceValue` parameter instead of the current model state.
  */
  execute(t = {}) {
    if (t.forceValue === this.value)
      return;
    const e = this.editor.plugins.get("TableUtils"), i = this.editor.model, s = e.getSelectionAffectedTableCells(i.document.selection), r = s[0].findAncestor("table"), { first: o, last: a } = e.getRowIndexes(s), l = this.value ? o : a + 1, c = r.getAttribute("headingRows") || 0;
    i.change((u) => {
      if (l) {
        const d = l > c ? c : 0, h = UC(r, l, d);
        for (const { cell: m } of h)
          WC(m, l, u);
      }
      wt("headingRows", l, r, u, 0);
    });
  }
  /**
  * Checks if a table cell is in the heading section.
  */
  _isInHeading(t, e) {
    const i = parseInt(e.getAttribute("headingRows") || "0");
    return !!i && t.parent.index < i;
  }
}
class uG extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.model, e = this.editor.plugins.get("TableUtils"), i = e.getSelectionAffectedTableCells(t.document.selection), s = i.length > 0;
    this.isEnabled = s, this.value = s && i.every((r) => Mh(e, r));
  }
  /**
  * Executes the command.
  *
  * When the selection is in a non-header column, the command will set the `headingColumns` table attribute to cover that column.
  *
  * When the selection is already in a header column, it will set `headingColumns` so the heading section will end before that column.
  *
  * @fires execute
  * @param options.forceValue If set, the command will set (`true`) or unset (`false`) the header columns according to
  * the `forceValue` parameter instead of the current model state.
  */
  execute(t = {}) {
    if (t.forceValue === this.value)
      return;
    const e = this.editor.plugins.get("TableUtils"), i = this.editor.model, s = e.getSelectionAffectedTableCells(i.document.selection), r = s[0].findAncestor("table"), { first: o, last: a } = e.getColumnIndexes(s), l = this.value ? o : a + 1;
    i.change((c) => {
      if (l) {
        const u = qC(r, l);
        for (const { cell: d, column: h } of u)
          GC(d, h, l, c);
      }
      wt("headingColumns", l, r, c, 0);
    });
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
const dG = 5, Dh = 40, hG = 2;
function fG(n) {
  const t = /* @__PURE__ */ new Set();
  for (const e of n.document.differ.getChanges()) {
    let i = null;
    switch (e.type) {
      case "insert":
        i = [
          "table",
          "tableRow",
          "tableCell"
        ].includes(e.name) ? e.position : null;
        break;
      case "remove":
        i = [
          "tableRow",
          "tableCell"
        ].includes(e.name) ? e.position : null;
        break;
      case "attribute":
        e.range.start.nodeAfter && (i = [
          "table",
          "tableRow",
          "tableCell"
        ].includes(e.range.start.nodeAfter.name) ? e.range.start : null);
        break;
    }
    if (!i)
      continue;
    const s = i.nodeAfter && i.nodeAfter.is("element", "table") ? i.nodeAfter : i.findAncestor("table");
    for (const r of n.createRangeOn(s).getItems())
      r.is("element", "table") && ja(r) && t.add(r);
  }
  return t;
}
function mG(n, t) {
  return Dh * 100 / JC(n, t);
}
function JC(n, t) {
  const e = Vw(n, "tbody", t) || Vw(n, "thead", t), i = t.editing.view.domConverter.mapViewToDom(e);
  return $h(i);
}
function Vw(n, t, e) {
  return [
    ...[
      ...e.editing.mapper.toViewElement(n).getChildren()
    ].find((r) => r.is("element", "table")).getChildren()
  ].find((r) => r.is("element", t));
}
function $h(n) {
  const t = ae.window.getComputedStyle(n);
  return t.boxSizing === "border-box" ? parseFloat(t.width) - parseFloat(t.paddingLeft) - parseFloat(t.paddingRight) - parseFloat(t.borderLeftWidth) - parseFloat(t.borderRightWidth) : parseFloat(t.width);
}
function gG(n, t) {
  const e = t.getCellLocation(n).column, i = n.getAttribute("colspan") || 1;
  return {
    leftEdge: e,
    rightEdge: e + i - 1
  };
}
function mi(n) {
  const t = Math.pow(10, hG), e = typeof n == "number" ? n : parseFloat(n);
  return Math.round(e * t) / t;
}
function pG(n, t, e) {
  return n <= t ? mi(t) : n >= e ? mi(e) : mi(n);
}
function bG(n, t) {
  return Array(n).fill(t);
}
function ya(n) {
  return n.map((t) => typeof t == "number" ? t : parseFloat(t)).filter((t) => !Number.isNaN(t)).reduce((t, e) => t + e, 0);
}
function vm(n) {
  const t = n.map((s) => s === "auto" ? s : parseFloat(s.replace("%", "")));
  let e = wG(t);
  const i = ya(e);
  return i !== 100 && (e = e.map((s) => mi(s * 100 / i)).map((s, r, o) => {
    if (!(r === o.length - 1))
      return s;
    const l = ya(o);
    return mi(s + 100 - l);
  })), e.map((s) => s + "%");
}
function wG(n) {
  const t = n.filter((s) => s === "auto").length;
  if (t === 0)
    return n.map((s) => mi(s));
  const e = ya(n), i = Math.max((100 - e) / t, dG);
  return n.map((s) => s === "auto" ? i : s).map((s) => mi(s));
}
function vG(n) {
  const t = ae.window.getComputedStyle(n);
  return t.boxSizing === "border-box" ? parseInt(t.width) : parseFloat(t.width) + parseFloat(t.paddingLeft) + parseFloat(t.paddingRight) + parseFloat(t.borderWidth);
}
function XC(n, t, e, i) {
  for (let s = 0; s < Math.max(e.length, n.length); s++) {
    const r = n[s], o = e[s];
    o ? r ? i.setAttribute("columnWidth", o, r) : i.appendElement("tableColumn", {
      columnWidth: o
    }, t) : i.remove(r);
  }
}
function ja(n) {
  if (n.is("element", "tableColumnGroup"))
    return n;
  const t = n.getChildren();
  return Array.from(t).find((e) => e.is("element", "tableColumnGroup"));
}
function Ua(n) {
  const t = ja(n);
  return t ? Array.from(t.getChildren()) : [];
}
function _G(n) {
  return Ua(n).map((t) => t.getAttribute("columnWidth"));
}
function yG(n, t) {
  return Ua(n).reduce((i, s) => {
    const r = s.getAttribute("columnWidth"), o = s.getAttribute("colSpan");
    if (!o)
      return i.push(r), i;
    for (let a = 0; a < o; a++)
      i.push(r);
    return t.removeAttribute("colSpan", s), i;
  }, []);
}
class $t extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableUtils";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.decorate("insertColumns"), this.decorate("insertRows");
  }
  /**
  * Returns the table cell location as an object with table row and table column indexes.
  *
  * For instance, in the table below:
  *
  *      0   1   2   3
  *    +---+---+---+---+
  *  0 | a     | b | c |
  *    +       +   +---+
  *  1 |       |   | d |
  *    +---+---+   +---+
  *  2 | e     |   | f |
  *    +---+---+---+---+
  *
  * the method will return:
  *
  * ```ts
  * const cellA = table.getNodeByPath( [ 0, 0 ] );
  * editor.plugins.get( 'TableUtils' ).getCellLocation( cellA );
  * // will return { row: 0, column: 0 }
  *
  * const cellD = table.getNodeByPath( [ 1, 0 ] );
  * editor.plugins.get( 'TableUtils' ).getCellLocation( cellD );
  * // will return { row: 1, column: 3 }
  * ```
  *
  * @returns Returns a `{row, column}` object.
  */
  getCellLocation(t) {
    const e = t.parent, i = e.parent, s = i.getChildIndex(e), r = new Ke(i, {
      row: s
    });
    for (const { cell: o, row: a, column: l } of r)
      if (o === t)
        return {
          row: a,
          column: l
        };
    /* istanbul ignore next -- @preserve */
  }
  /**
  * Creates an empty table with a proper structure. The table needs to be inserted into the model,
  * for example, by using the {@link module:engine/model/model~Model#insertContent} function.
  *
  * ```ts
  * model.change( ( writer ) => {
  *   // Create a table of 2 rows and 7 columns:
  *   const table = tableUtils.createTable( writer, { rows: 2, columns: 7 } );
  *
  *   // Insert a table to the model at the best position taking the current selection:
  *   model.insertContent( table );
  * }
  * ```
  *
  * @param writer The model writer.
  * @param options.rows The number of rows to create. Default value is 2.
  * @param options.columns The number of columns to create. Default value is 2.
  * @param options.headingRows The number of heading rows. Default value is 0.
  * @param options.headingColumns The number of heading columns. Default value is 0.
  * @returns The created table element.
  */
  createTable(t, e) {
    const i = t.createElement("table"), s = e.rows || 2, r = e.columns || 2;
    return Od(t, i, 0, s, r), e.headingRows && wt("headingRows", Math.min(e.headingRows, s), i, t, 0), e.headingColumns && wt("headingColumns", Math.min(e.headingColumns, r), i, t, 0), i;
  }
  /**
  * Inserts rows into a table.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).insertRows( table, { at: 1, rows: 2 } );
  * ```
  *
  * Assuming the table on the left, the above code will transform it to the table on the right:
  *
  *  row index
  *    0 +---+---+---+       `at` = 1,      +---+---+---+ 0
  *      | a | b | c |       `rows` = 2,    | a | b | c |
  *    1 +   +---+---+   <-- insert here    +   +---+---+ 1
  *      |   | d | e |                      |   |   |   |
  *    2 +   +---+---+       will give:     +   +---+---+ 2
  *      |   | f | g |                      |   |   |   |
  *    3 +---+---+---+                      +   +---+---+ 3
  *                                         |   | d | e |
  *                                         +   +---+---+ 4
  *                                         +   + f | g |
  *                                         +---+---+---+ 5
  *
  * @param table The table model element where the rows will be inserted.
  * @param options.at The row index at which the rows will be inserted.  Default value is 0.
  * @param options.rows The number of rows to insert.  Default value is 1.
  * @param options.copyStructureFromAbove The flag for copying row structure. Note that
  * the row structure will not be copied if this option is not provided.
  */
  insertRows(t, e = {}) {
    const i = this.editor.model, s = e.at || 0, r = e.rows || 1, o = e.copyStructureFromAbove !== void 0, a = e.copyStructureFromAbove ? s - 1 : s, l = this.getRows(t), c = this.getColumns(t);
    if (s > l)
      throw new S("tableutils-insertrows-insert-out-of-range", this, {
        options: e
      });
    i.change((u) => {
      const d = t.getAttribute("headingRows") || 0;
      if (d > s && wt("headingRows", d + r, t, u, 0), !o && (s === 0 || s === l)) {
        Od(u, t, s, r, c);
        return;
      }
      const h = o ? Math.max(s, a) : s, m = new Ke(t, {
        endRow: h
      }), g = new Array(c).fill(1);
      for (const { row: p, column: w, cellHeight: y, cellWidth: k, cell: C } of m) {
        const E = p + y - 1, T = p < s && s <= E, L = p <= a && a <= E;
        T ? (u.setAttribute("rowspan", y + r, C), g[w] = -k) : o && L && (g[w] = k);
      }
      for (let p = 0; p < r; p++) {
        const w = u.createElement("tableRow");
        u.insert(w, t, s);
        for (let y = 0; y < g.length; y++) {
          const k = g[y], C = u.createPositionAt(w, "end");
          k > 0 && er(u, C, k > 1 ? {
            colspan: k
          } : void 0), y += Math.abs(k) - 1;
        }
      }
    });
  }
  /**
  * Inserts columns into a table.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).insertColumns( table, { at: 1, columns: 2 } );
  * ```
  *
  * Assuming the table on the left, the above code will transform it to the table on the right:
  *
  *  0   1   2   3                   0   1   2   3   4   5
  *  +---+---+---+                   +---+---+---+---+---+
  *  | a     | b |                   | a             | b |
  *  +       +---+                   +               +---+
  *  |       | c |                   |               | c |
  *  +---+---+---+     will give:    +---+---+---+---+---+
  *  | d | e | f |                   | d |   |   | e | f |
  *  +---+   +---+                   +---+---+---+   +---+
  *  | g |   | h |                   | g |   |   |   | h |
  *  +---+---+---+                   +---+---+---+---+---+
  *  | i         |                   | i                 |
  *  +---+---+---+                   +---+---+---+---+---+
  *      ^---- insert here, `at` = 1, `columns` = 2
  *
  * @param table The table model element where the columns will be inserted.
  * @param options.at The column index at which the columns will be inserted. Default value is 0.
  * @param options.columns The number of columns to insert. Default value is 1.
  */
  insertColumns(t, e = {}) {
    const i = this.editor.model, s = e.at || 0, r = e.columns || 1;
    i.change((o) => {
      const a = t.getAttribute("headingColumns");
      s < a && o.setAttribute("headingColumns", a + r, t);
      const l = this.getColumns(t);
      if (s === 0 || l === s) {
        for (const u of t.getChildren())
          u.is("element", "tableRow") && Ar(r, o, o.createPositionAt(u, s ? "end" : 0));
        return;
      }
      const c = new Ke(t, {
        column: s,
        includeAllSlots: !0
      });
      for (const u of c) {
        const { row: d, cell: h, cellAnchorColumn: m, cellAnchorRow: g, cellWidth: p, cellHeight: w } = u;
        if (m < s) {
          o.setAttribute("colspan", p + r, h);
          const y = g + w - 1;
          for (let k = d; k <= y; k++)
            c.skipRow(k);
        } else
          Ar(r, o, u.getPositionBefore());
      }
    });
  }
  /**
  * Removes rows from the given `table`.
  *
  * This method re-calculates the table geometry including `rowspan` attribute of table cells overlapping removed rows
  * and table headings values.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).removeRows( table, { at: 1, rows: 2 } );
  * ```
  *
  * Executing the above code in the context of the table on the left will transform its structure as presented on the right:
  *
  *  row index
  *      ┌───┬───┬───┐        `at` = 1        ┌───┬───┬───┐
  *    0 │ a │ b │ c │        `rows` = 2      │ a │ b │ c │ 0
  *      │   ├───┼───┤                        │   ├───┼───┤
  *    1 │   │ d │ e │  <-- remove from here  │   │ d │ g │ 1
  *      │   │   ├───┤        will give:      ├───┼───┼───┤
  *    2 │   │   │ f │                        │ h │ i │ j │ 2
  *      │   │   ├───┤                        └───┴───┴───┘
  *    3 │   │   │ g │
  *      ├───┼───┼───┤
  *    4 │ h │ i │ j │
  *      └───┴───┴───┘
  *
  * @param options.at The row index at which the removing rows will start.
  * @param options.rows The number of rows to remove. Default value is 1.
  */
  removeRows(t, e) {
    const i = this.editor.model, s = e.rows || 1, r = this.getRows(t), o = e.at, a = o + s - 1;
    if (a > r - 1)
      throw new S("tableutils-removerows-row-index-out-of-range", this, {
        table: t,
        options: e
      });
    i.change((l) => {
      const c = {
        first: o,
        last: a
      }, { cellsToMove: u, cellsToTrim: d } = xG(t, c);
      if (u.size) {
        const h = a + 1;
        TG(t, h, u, l);
      }
      for (let h = a; h >= o; h--)
        l.remove(t.getChild(h));
      for (const { rowspan: h, cell: m } of d)
        wt("rowspan", h, m, l);
      CG(t, c, l), Nh(t, this) || Fh(t, this);
    });
  }
  /**
  * Removes columns from the given `table`.
  *
  * This method re-calculates the table geometry including the `colspan` attribute of table cells overlapping removed columns
  * and table headings values.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).removeColumns( table, { at: 1, columns: 2 } );
  * ```
  *
  * Executing the above code in the context of the table on the left will transform its structure as presented on the right:
  *
  *    0   1   2   3   4                       0   1   2
  *  ┌───────────────┬───┐                   ┌───────┬───┐
  *  │ a             │ b │                   │ a     │ b │
  *  │               ├───┤                   │       ├───┤
  *  │               │ c │                   │       │ c │
  *  ├───┬───┬───┬───┼───┤     will give:    ├───┬───┼───┤
  *  │ d │ e │ f │ g │ h │                   │ d │ g │ h │
  *  ├───┼───┼───┤   ├───┤                   ├───┤   ├───┤
  *  │ i │ j │ k │   │ l │                   │ i │   │ l │
  *  ├───┴───┴───┴───┴───┤                   ├───┴───┴───┤
  *  │ m                 │                   │ m         │
  *  └───────────────────┘                   └───────────┘
  *        ^---- remove from here, `at` = 1, `columns` = 2
  *
  * @param options.at The row index at which the removing columns will start.
  * @param options.columns The number of columns to remove.
  */
  removeColumns(t, e) {
    const i = this.editor.model, s = e.at, r = e.columns || 1, o = e.at + r - 1;
    i.change((a) => {
      kG(t, {
        first: s,
        last: o
      }, a);
      const l = Ua(t);
      for (let c = o; c >= s; c--) {
        for (const { cell: u, column: d, cellWidth: h } of [
          ...new Ke(t)
        ])
          d <= c && h > 1 && d + h > c ? wt("colspan", h - 1, u, a) : d === c && a.remove(u);
        if (l[c]) {
          const u = c === 0 ? l[1] : l[c - 1], d = parseFloat(l[c].getAttribute("columnWidth")), h = parseFloat(u.getAttribute("columnWidth"));
          a.remove(l[c]), a.setAttribute("columnWidth", d + h + "%", u);
        }
      }
      Fh(t, this) || Nh(t, this);
    });
  }
  /**
  * Divides a table cell vertically into several ones.
  *
  * The cell will be visually split into more cells by updating colspans of other cells in a column
  * and inserting cells (columns) after that cell.
  *
  * In the table below, if cell "a" is split into 3 cells:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +---+---+---+
  *  | d | e | f |
  *  +---+---+---+
  *
  * it will result in the table below:
  *
  *  +---+---+---+---+---+
  *  | a |   |   | b | c |
  *  +---+---+---+---+---+
  *  | d         | e | f |
  *  +---+---+---+---+---+
  *
  * So cell "d" will get its `colspan` updated to `3` and 2 cells will be added (2 columns will be created).
  *
  * Splitting a cell that already has a `colspan` attribute set will distribute the cell `colspan` evenly and the remainder
  * will be left to the original cell:
  *
  *  +---+---+---+
  *  | a         |
  *  +---+---+---+
  *  | b | c | d |
  *  +---+---+---+
  *
  * Splitting cell "a" with `colspan=3` into 2 cells will create 1 cell with a `colspan=a` and cell "a" that will have `colspan=2`:
  *
  *  +---+---+---+
  *  | a     |   |
  *  +---+---+---+
  *  | b | c | d |
  *  +---+---+---+
  */
  splitCellVertically(t, e = 2) {
    const i = this.editor.model, r = t.parent.parent, o = parseInt(t.getAttribute("rowspan") || "1"), a = parseInt(t.getAttribute("colspan") || "1");
    i.change((l) => {
      if (a > 1) {
        const { newCellsSpan: c, updatedSpan: u } = Lw(a, e);
        wt("colspan", u, t, l);
        const d = {};
        c > 1 && (d.colspan = c), o > 1 && (d.rowspan = o);
        const h = a > e ? e - 1 : a - 1;
        Ar(h, l, l.createPositionAfter(t), d);
      }
      if (a < e) {
        const c = e - a, u = [
          ...new Ke(r)
        ], { column: d } = u.find(({ cell: p }) => p === t), h = u.filter(({ cell: p, cellWidth: w, column: y }) => {
          const k = p !== t && y === d, C = y < d && y + w > d;
          return k || C;
        });
        for (const { cell: p, cellWidth: w } of h)
          l.setAttribute("colspan", w + c, p);
        const m = {};
        o > 1 && (m.rowspan = o), Ar(c, l, l.createPositionAfter(t), m);
        const g = r.getAttribute("headingColumns") || 0;
        g > d && wt("headingColumns", g + c, r, l);
      }
    });
  }
  /**
  * Divides a table cell horizontally into several ones.
  *
  * The cell will be visually split into more cells by updating rowspans of other cells in the row and inserting rows with a single cell
  * below.
  *
  * If in the table below cell "b" is split into 3 cells:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +---+---+---+
  *  | d | e | f |
  *  +---+---+---+
  *
  * It will result in the table below:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +   +---+   +
  *  |   |   |   |
  *  +   +---+   +
  *  |   |   |   |
  *  +---+---+---+
  *  | d | e | f |
  *  +---+---+---+
  *
  * So cells "a" and "b" will get their `rowspan` updated to `3` and 2 rows with a single cell will be added.
  *
  * Splitting a cell that already has a `rowspan` attribute set will distribute the cell `rowspan` evenly and the remainder
  * will be left to the original cell:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +   +---+---+
  *  |   | d | e |
  *  +   +---+---+
  *  |   | f | g |
  *  +   +---+---+
  *  |   | h | i |
  *  +---+---+---+
  *
  * Splitting cell "a" with `rowspan=4` into 3 cells will create 2 cells with a `rowspan=1` and cell "a" will have `rowspan=2`:
  *
  *  +---+---+---+
  *  | a | b | c |
  *  +   +---+---+
  *  |   | d | e |
  *  +---+---+---+
  *  |   | f | g |
  *  +---+---+---+
  *  |   | h | i |
  *  +---+---+---+
  */
  splitCellHorizontally(t, e = 2) {
    const i = this.editor.model, s = t.parent, r = s.parent, o = r.getChildIndex(s), a = parseInt(t.getAttribute("rowspan") || "1"), l = parseInt(t.getAttribute("colspan") || "1");
    i.change((c) => {
      if (a > 1) {
        const u = [
          ...new Ke(r, {
            startRow: o,
            endRow: o + a - 1,
            includeAllSlots: !0
          })
        ], { newCellsSpan: d, updatedSpan: h } = Lw(a, e);
        wt("rowspan", h, t, c);
        const { column: m } = u.find(({ cell: w }) => w === t), g = {};
        d > 1 && (g.rowspan = d), l > 1 && (g.colspan = l);
        let p = 0;
        for (const w of u) {
          const { column: y, row: k } = w, C = k >= o + h, E = y === m;
          p >= d && E && (p = 0), C && E && (p || Ar(1, c, w.getPositionBefore(), g), p++);
        }
      }
      if (a < e) {
        const u = e - a, d = [
          ...new Ke(r, {
            startRow: 0,
            endRow: o
          })
        ];
        for (const { cell: g, cellHeight: p, row: w } of d)
          if (g !== t && w + p > o) {
            const y = p + u;
            c.setAttribute("rowspan", y, g);
          }
        const h = {};
        l > 1 && (h.colspan = l), Od(c, r, o + 1, u, 1, h);
        const m = r.getAttribute("headingRows") || 0;
        m > o && wt("headingRows", m + u, r, c);
      }
    });
  }
  /**
  * Returns the number of columns for a given table.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).getColumns( table );
  * ```
  *
  * @param table The table to analyze.
  */
  getColumns(t) {
    return [
      ...t.getChild(0).getChildren()
    ].filter((i) => i.is("element", "tableCell")).reduce((i, s) => {
      const r = parseInt(s.getAttribute("colspan") || "1");
      return i + r;
    }, 0);
  }
  /**
  * Returns the number of rows for a given table. Any other element present in the table model is omitted.
  *
  * ```ts
  * editor.plugins.get( 'TableUtils' ).getRows( table );
  * ```
  *
  * @param table The table to analyze.
  */
  getRows(t) {
    return Array.from(t.getChildren()).reduce((e, i) => i.is("element", "tableRow") ? e + 1 : e, 0);
  }
  /**
  * Creates an instance of the table walker.
  *
  * The table walker iterates internally by traversing the table from row index = 0 and column index = 0.
  * It walks row by row and column by column in order to output values defined in the options.
  * By default it will output only the locations that are occupied by a cell. To include also spanned rows and columns,
  * pass the `includeAllSlots` option.
  *
  * @internal
  * @param table A table over which the walker iterates.
  * @param options An object with configuration.
  */
  createTableWalker(t, e = {}) {
    return new Ke(t, e);
  }
  /**
  * Returns all model table cells that are fully selected (from the outside)
  * within the provided model selection's ranges.
  *
  * To obtain the cells selected from the inside, use
  * {@link #getTableCellsContainingSelection}.
  */
  getSelectedTableCells(t) {
    const e = [];
    for (const i of this.sortRanges(t.getRanges())) {
      const s = i.getContainedElement();
      s && s.is("element", "tableCell") && e.push(s);
    }
    return e;
  }
  /**
  * Returns all model table cells that the provided model selection's ranges
  * {@link module:engine/model/range~Range#start} inside.
  *
  * To obtain the cells selected from the outside, use
  * {@link #getSelectedTableCells}.
  */
  getTableCellsContainingSelection(t) {
    const e = [];
    for (const i of t.getRanges()) {
      const s = i.start.findAncestor("tableCell");
      s && e.push(s);
    }
    return e;
  }
  /**
  * Returns all model table cells that are either completely selected
  * by selection ranges or host selection range
  * {@link module:engine/model/range~Range#start start positions} inside them.
  *
  * Combines {@link #getTableCellsContainingSelection} and
  * {@link #getSelectedTableCells}.
  */
  getSelectionAffectedTableCells(t) {
    const e = this.getSelectedTableCells(t);
    return e.length ? e : this.getTableCellsContainingSelection(t);
  }
  /**
  * Returns an object with the `first` and `last` row index contained in the given `tableCells`.
  *
  * ```ts
  * const selectedTableCells = getSelectedTableCells( editor.model.document.selection );
  *
  * const { first, last } = getRowIndexes( selectedTableCells );
  *
  * console.log( `Selected rows: ${ first } to ${ last }` );
  * ```
  *
  * @returns Returns an object with the `first` and `last` table row indexes.
  */
  getRowIndexes(t) {
    const e = t.map((i) => i.parent.index);
    return this._getFirstLastIndexesObject(e);
  }
  /**
  * Returns an object with the `first` and `last` column index contained in the given `tableCells`.
  *
  * ```ts
  * const selectedTableCells = getSelectedTableCells( editor.model.document.selection );
  *
  * const { first, last } = getColumnIndexes( selectedTableCells );
  *
  * console.log( `Selected columns: ${ first } to ${ last }` );
  * ```
  *
  * @returns Returns an object with the `first` and `last` table column indexes.
  */
  getColumnIndexes(t) {
    const e = t[0].findAncestor("table"), s = [
      ...new Ke(e)
    ].filter((r) => t.includes(r.cell)).map((r) => r.column);
    return this._getFirstLastIndexesObject(s);
  }
  /**
  * Checks if the selection contains cells that do not exceed rectangular selection.
  *
  * In a table below:
  *
  *  ┌───┬───┬───┬───┐
  *  │ a │ b │ c │ d │
  *  ├───┴───┼───┤   │
  *  │ e     │ f │   │
  *  │       ├───┼───┤
  *  │       │ g │ h │
  *  └───────┴───┴───┘
  *
  * Valid selections are these which create a solid rectangle (without gaps), such as:
  *   - a, b (two horizontal cells)
  *   - c, f (two vertical cells)
  *   - a, b, e (cell "e" spans over four cells)
  *   - c, d, f (cell d spans over a cell in the row below)
  *
  * While an invalid selection would be:
  *   - a, c (the unselected cell "b" creates a gap)
  *   - f, g, h (cell "d" spans over a cell from the row of "f" cell - thus creates a gap)
  */
  isSelectionRectangular(t) {
    if (t.length < 2 || !this._areCellInTheSameTableSection(t))
      return !1;
    const e = /* @__PURE__ */ new Set(), i = /* @__PURE__ */ new Set();
    let s = 0;
    for (const o of t) {
      const { row: a, column: l } = this.getCellLocation(o), c = parseInt(o.getAttribute("rowspan")) || 1, u = parseInt(o.getAttribute("colspan")) || 1;
      e.add(a), i.add(l), c > 1 && e.add(a + c - 1), u > 1 && i.add(l + u - 1), s += c * u;
    }
    return SG(e, i) == s;
  }
  /**
  * Returns array of sorted ranges.
  */
  sortRanges(t) {
    return Array.from(t).sort(EG);
  }
  /**
  * Helper method to get an object with `first` and `last` indexes from an unsorted array of indexes.
  */
  _getFirstLastIndexesObject(t) {
    const e = t.sort((r, o) => r - o), i = e[0], s = e[e.length - 1];
    return {
      first: i,
      last: s
    };
  }
  /**
  * Checks if the selection does not mix a header (column or row) with other cells.
  *
  * For instance, in the table below valid selections consist of cells with the same letter only.
  * So, a-a (same heading row and column) or d-d (body cells) are valid while c-d or a-b are not.
  *
  * header columns
  *    ↓   ↓
  *  ┌───┬───┬───┬───┐
  *  │ a │ a │ b │ b │  ← header row
  *  ├───┼───┼───┼───┤
  *  │ c │ c │ d │ d │
  *  ├───┼───┼───┼───┤
  *  │ c │ c │ d │ d │
  *  └───┴───┴───┴───┘
  */
  _areCellInTheSameTableSection(t) {
    const e = t[0].findAncestor("table"), i = this.getRowIndexes(t), s = parseInt(e.getAttribute("headingRows")) || 0;
    if (!this._areIndexesInSameSection(i, s))
      return !1;
    const r = this.getColumnIndexes(t), o = parseInt(e.getAttribute("headingColumns")) || 0;
    return this._areIndexesInSameSection(r, o);
  }
  /**
  * Unified check if table rows/columns indexes are in the same heading/body section.
  */
  _areIndexesInSameSection({ first: t, last: e }, i) {
    const s = t < i, r = e < i;
    return s === r;
  }
}
function Od(n, t, e, i, s, r = {}) {
  for (let o = 0; o < i; o++) {
    const a = n.createElement("tableRow");
    n.insert(a, t, e), Ar(s, n, n.createPositionAt(a, "end"), r);
  }
}
function Ar(n, t, e, i = {}) {
  for (let s = 0; s < n; s++)
    er(t, e, i);
}
function Lw(n, t) {
  if (n < t)
    return {
      newCellsSpan: 1,
      updatedSpan: 1
    };
  const e = Math.floor(n / t), i = n - e * t + e;
  return {
    newCellsSpan: e,
    updatedSpan: i
  };
}
function kG(n, t, e) {
  const i = n.getAttribute("headingColumns") || 0;
  if (i && t.first < i) {
    const s = Math.min(i - 1, t.last) - t.first + 1;
    e.setAttribute("headingColumns", i - s, n);
  }
}
function CG(n, { first: t, last: e }, i) {
  const s = n.getAttribute("headingRows") || 0;
  if (t < s) {
    const r = e < s ? s - (e - t + 1) : t;
    wt("headingRows", r, n, i, 0);
  }
}
function xG(n, { first: t, last: e }) {
  const i = /* @__PURE__ */ new Map(), s = [];
  for (const { row: r, column: o, cellHeight: a, cell: l } of new Ke(n, {
    endRow: e
  })) {
    const c = r + a - 1;
    if (r >= t && r <= e && c > e) {
      const h = e - r + 1, m = a - h;
      i.set(o, {
        cell: l,
        rowspan: m
      });
    }
    if (r < t && c >= t) {
      let h;
      c >= e ? h = e - t + 1 : h = c - t + 1, s.push({
        cell: l,
        rowspan: a - h
      });
    }
  }
  return {
    cellsToMove: i,
    cellsToTrim: s
  };
}
function TG(n, t, e, i) {
  const r = [
    ...new Ke(n, {
      includeAllSlots: !0,
      row: t
    })
  ], o = n.getChild(t);
  let a;
  for (const { column: l, cell: c, isAnchor: u } of r)
    if (e.has(l)) {
      const { cell: d, rowspan: h } = e.get(l), m = a ? i.createPositionAfter(a) : i.createPositionAt(o, 0);
      i.move(i.createRangeOn(d), m), wt("rowspan", h, d, i), a = d;
    } else u && (a = c);
}
function EG(n, t) {
  const e = n.start, i = t.start;
  return e.isBefore(i) ? -1 : 1;
}
function SG(n, t) {
  const e = Array.from(n.values()), i = Array.from(t.values()), s = Math.max(...e), r = Math.min(...e), o = Math.max(...i), a = Math.min(...i);
  return (s - r + 1) * (o - a + 1);
}
class AG extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor.plugins.get($t), e = t.getSelectedTableCells(this.editor.model.document.selection);
    this.isEnabled = t.isSelectionRectangular(e);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  */
  execute() {
    const t = this.editor.model, e = this.editor.plugins.get($t);
    t.change((i) => {
      const s = e.getSelectedTableCells(t.document.selection), r = s.shift(), { mergeWidth: o, mergeHeight: a } = PG(r, s, e);
      wt("colspan", o, r, i), wt("rowspan", a, r, i);
      for (const c of s)
        IG(c, r, i);
      const l = r.findAncestor("table");
      wm(l, e), i.setSelection(r, "in");
    });
  }
}
function IG(n, t, e) {
  Bw(n) || (Bw(t) && e.remove(e.createRangeIn(t)), e.move(e.createRangeIn(n), e.createPositionAt(t, "end"))), e.remove(n);
}
function Bw(n) {
  const t = n.getChild(0);
  return n.childCount == 1 && t.is("element", "paragraph") && t.isEmpty;
}
function PG(n, t, e) {
  let i = 0, s = 0;
  for (const c of t) {
    const { row: u, column: d } = e.getCellLocation(c);
    i = Mw(c, d, i, "colspan"), s = Mw(c, u, s, "rowspan");
  }
  const { row: r, column: o } = e.getCellLocation(n), a = i - o, l = s - r;
  return {
    mergeWidth: a,
    mergeHeight: l
  };
}
function Mw(n, t, e, i) {
  const s = parseInt(n.getAttribute(i) || "1");
  return Math.max(e, t + s);
}
class RG extends he {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.affectsData = !1;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(this.editor.model.document.selection);
    this.isEnabled = e.length > 0;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.model, e = this.editor.plugins.get("TableUtils"), i = e.getSelectionAffectedTableCells(t.document.selection), s = e.getRowIndexes(i), r = i[0].findAncestor("table"), o = [];
    for (let a = s.first; a <= s.last; a++)
      for (const l of r.getChild(a).getChildren())
        o.push(t.createRangeOn(l));
    t.change((a) => {
      a.setSelection(o);
    });
  }
}
class OG extends he {
  /**
  * @inheritDoc
  */
  constructor(t) {
    super(t), this.affectsData = !1;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(this.editor.model.document.selection);
    this.isEnabled = e.length > 0;
  }
  /**
  * @inheritDoc
  */
  execute() {
    const t = this.editor.plugins.get("TableUtils"), e = this.editor.model, i = t.getSelectionAffectedTableCells(e.document.selection), s = i[0], r = i.pop(), o = s.findAncestor("table"), a = t.getCellLocation(s), l = t.getCellLocation(r), c = Math.min(a.column, l.column), u = Math.max(a.column, l.column), d = [];
    for (const h of new Ke(o, {
      startColumn: c,
      endColumn: u
    }))
      d.push(e.createRangeOn(h.cell));
    e.change((h) => {
      h.setSelection(d);
    });
  }
}
function VG(n) {
  n.document.registerPostFixer((t) => LG(t, n));
}
function LG(n, t) {
  const e = t.document.differ.getChanges();
  let i = !1;
  const s = /* @__PURE__ */ new Set();
  for (const r of e) {
    let o = null;
    r.type == "insert" && r.name == "table" && (o = r.position.nodeAfter), (r.type == "insert" || r.type == "remove") && (r.name == "tableRow" || r.name == "tableCell") && (o = r.position.findAncestor("table")), DG(r) && (o = r.range.start.findAncestor("table")), o && !s.has(o) && (i = BG(o, n) || i, i = MG(o, n) || i, s.add(o));
  }
  return i;
}
function BG(n, t) {
  let e = !1;
  const i = NG(n);
  if (i.length) {
    e = !0;
    for (const s of i)
      wt("rowspan", s.rowspan, s.cell, t, 1);
  }
  return e;
}
function MG(n, t) {
  let e = !1;
  const i = FG(n), s = [];
  for (const [l, c] of i.entries())
    !c && n.getChild(l).is("element", "tableRow") && s.push(l);
  if (s.length) {
    e = !0;
    for (const l of s.reverse())
      t.remove(n.getChild(l)), i.splice(l, 1);
  }
  const r = i.filter((l, c) => n.getChild(c).is("element", "tableRow")), o = r[0];
  if (!r.every((l) => l === o)) {
    const l = r.reduce((c, u) => u > c ? u : c, 0);
    for (const [c, u] of r.entries()) {
      const d = l - u;
      if (d) {
        for (let h = 0; h < d; h++)
          er(t, t.createPositionAt(n.getChild(c), "end"));
        e = !0;
      }
    }
  }
  return e;
}
function NG(n) {
  const t = parseInt(n.getAttribute("headingRows") || "0"), e = Array.from(n.getChildren()).reduce((s, r) => r.is("element", "tableRow") ? s + 1 : s, 0), i = [];
  for (const { row: s, cell: r, cellHeight: o } of new Ke(n)) {
    if (o < 2)
      continue;
    const l = s < t ? t : e;
    if (s + o > l) {
      const c = l - s;
      i.push({
        cell: r,
        rowspan: c
      });
    }
  }
  return i;
}
function FG(n) {
  const t = new Array(n.childCount).fill(0);
  for (const { rowIndex: e } of new Ke(n, {
    includeAllSlots: !0
  }))
    t[e]++;
  return t;
}
function DG(n) {
  if (n.type !== "attribute")
    return !1;
  const t = n.attributeKey;
  return t === "headingRows" || t === "colspan" || t === "rowspan";
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function $G(n) {
  n.document.registerPostFixer((t) => zG(t, n));
}
function zG(n, t) {
  const e = t.document.differ.getChanges();
  let i = !1;
  for (const s of e)
    s.type == "insert" && s.name == "table" && (i = HG(s.position.nodeAfter, n) || i), s.type == "insert" && s.name == "tableRow" && (i = QC(s.position.nodeAfter, n) || i), s.type == "insert" && s.name == "tableCell" && (i = zh(s.position.nodeAfter, n) || i), (s.type == "remove" || s.type == "insert") && jG(s) && (i = zh(s.position.parent, n) || i);
  return i;
}
function HG(n, t) {
  let e = !1;
  for (const i of n.getChildren())
    i.is("element", "tableRow") && (e = QC(i, t) || e);
  return e;
}
function QC(n, t) {
  let e = !1;
  for (const i of n.getChildren())
    e = zh(i, t) || e;
  return e;
}
function zh(n, t) {
  if (n.childCount == 0)
    return t.insertElement("paragraph", n), !0;
  const e = Array.from(n.getChildren()).filter((i) => i.is("$text"));
  for (const i of e)
    t.wrap(t.createRangeOn(i), "paragraph");
  return !!e.length;
}
function jG(n) {
  return n.position.parent.is("element", "tableCell") ? n.type == "insert" && n.name == "$text" || n.type == "remove" : !1;
}
function UG(n, t) {
  const e = n.document.differ;
  for (const i of e.getChanges()) {
    let s, r = !1;
    if (i.type == "attribute") {
      const c = i.range.start.nodeAfter;
      if (!c || !c.is("element", "table") || i.attributeKey != "headingRows" && i.attributeKey != "headingColumns")
        continue;
      s = c, r = i.attributeKey == "headingRows";
    } else (i.name == "tableRow" || i.name == "tableCell") && (s = i.position.findAncestor("table"), r = i.name == "tableRow");
    if (!s)
      continue;
    const o = s.getAttribute("headingRows") || 0, a = s.getAttribute("headingColumns") || 0, l = new Ke(s);
    for (const c of l) {
      const d = c.row < o || c.column < a ? "th" : "td", h = t.mapper.toViewElement(c.cell);
      h && h.is("element") && h.name != d && t.reconvertItem(r ? c.cell.parent : c.cell);
    }
  }
}
function WG(n, t) {
  const e = n.document.differ, i = /* @__PURE__ */ new Set();
  for (const s of e.getChanges()) {
    const r = s.type == "attribute" ? s.range.start.parent : s.position.parent;
    r.is("element", "tableCell") && i.add(r);
  }
  for (const s of i.values()) {
    const r = Array.from(s.getChildren()).filter((o) => qG(o, t.mapper));
    for (const o of r)
      t.reconvertItem(o);
  }
}
function qG(n, t) {
  if (!n.is("element", "paragraph"))
    return !1;
  const e = t.toViewElement(n);
  return e ? HC(n) !== e.is("element", "span") : !1;
}
class Wa extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * Handlers for creating additional slots in the table.
    */
    f(this, "_additionalSlots");
    this._additionalSlots = [];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      $t
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model, s = i.schema, r = e.conversion, o = e.plugins.get($t);
    s.register("table", {
      inheritAllFrom: "$blockObject",
      allowAttributes: [
        "headingRows",
        "headingColumns"
      ]
    }), s.register("tableRow", {
      allowIn: "table",
      isLimit: !0
    }), s.register("tableCell", {
      allowContentOf: "$container",
      allowIn: "tableRow",
      allowAttributes: [
        "colspan",
        "rowspan"
      ],
      isLimit: !0,
      isSelectable: !0
    }), r.for("upcast").add(Hq()), r.for("upcast").add(jq()), r.for("editingDowncast").elementToStructure({
      model: {
        name: "table",
        attributes: [
          "headingRows"
        ]
      },
      view: Ew(o, {
        asWidget: !0,
        additionalSlots: this._additionalSlots
      })
    }), r.for("dataDowncast").elementToStructure({
      model: {
        name: "table",
        attributes: [
          "headingRows"
        ]
      },
      view: Ew(o, {
        additionalSlots: this._additionalSlots
      })
    }), r.for("upcast").elementToElement({
      model: "tableRow",
      view: "tr"
    }), r.for("upcast").add(Uq()), r.for("downcast").elementToElement({
      model: "tableRow",
      view: Zq()
    }), r.for("upcast").elementToElement({
      model: "tableCell",
      view: "td"
    }), r.for("upcast").elementToElement({
      model: "tableCell",
      view: "th"
    }), r.for("upcast").add(Tw("td")), r.for("upcast").add(Tw("th")), r.for("editingDowncast").elementToElement({
      model: "tableCell",
      view: Sw({
        asWidget: !0
      })
    }), r.for("dataDowncast").elementToElement({
      model: "tableCell",
      view: Sw()
    }), r.for("editingDowncast").elementToElement({
      model: "paragraph",
      view: Aw({
        asWidget: !0
      }),
      converterPriority: "high"
    }), r.for("dataDowncast").elementToElement({
      model: "paragraph",
      view: Aw(),
      converterPriority: "high"
    }), r.for("downcast").attributeToAttribute({
      model: "colspan",
      view: "colspan"
    }), r.for("upcast").attributeToAttribute({
      model: {
        key: "colspan",
        value: Nw("colspan")
      },
      view: "colspan"
    }), r.for("downcast").attributeToAttribute({
      model: "rowspan",
      view: "rowspan"
    }), r.for("upcast").attributeToAttribute({
      model: {
        key: "rowspan",
        value: Nw("rowspan")
      },
      view: "rowspan"
    }), e.config.define("table.defaultHeadings.rows", 0), e.config.define("table.defaultHeadings.columns", 0), e.commands.add("insertTable", new Xq(e)), e.commands.add("insertTableRowAbove", new Iw(e, {
      order: "above"
    })), e.commands.add("insertTableRowBelow", new Iw(e, {
      order: "below"
    })), e.commands.add("insertTableColumnLeft", new Pw(e, {
      order: "left"
    })), e.commands.add("insertTableColumnRight", new Pw(e, {
      order: "right"
    })), e.commands.add("removeTableRow", new sG(e)), e.commands.add("removeTableColumn", new oG(e)), e.commands.add("splitTableCellVertically", new Rw(e, {
      direction: "vertically"
    })), e.commands.add("splitTableCellHorizontally", new Rw(e, {
      direction: "horizontally"
    })), e.commands.add("mergeTableCells", new AG(e)), e.commands.add("mergeTableCellRight", new xl(e, {
      direction: "right"
    })), e.commands.add("mergeTableCellLeft", new xl(e, {
      direction: "left"
    })), e.commands.add("mergeTableCellDown", new xl(e, {
      direction: "down"
    })), e.commands.add("mergeTableCellUp", new xl(e, {
      direction: "up"
    })), e.commands.add("setTableColumnHeader", new uG(e)), e.commands.add("setTableRowHeader", new cG(e)), e.commands.add("selectTableRow", new RG(e)), e.commands.add("selectTableColumn", new OG(e)), VG(i), $G(i), this.listenTo(i.document, "change:data", () => {
      UG(i, e.editing), WG(i, e.editing);
    });
  }
  /**
  * Registers downcast handler for the additional table slot.
  */
  registerAdditionalSlot(e) {
    this._additionalSlots.push(e);
  }
}
function Nw(n) {
  return (t) => {
    const e = parseInt(t.getAttribute(n));
    return Number.isNaN(e) || e <= 0 ? null : e;
  };
}
class Fw extends ee {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A collection of table size box items.
    */
    f(this, "items");
    /**
    * Listen to `keydown` events fired in this view's main element.
    */
    f(this, "keystrokes");
    /**
    * Tracks information about the DOM focus in the grid.
    */
    f(this, "focusTracker");
    const i = this.bindTemplate;
    this.items = this._createGridCollection(), this.keystrokes = new Ze(), this.focusTracker = new ze(), this.set("rows", 0), this.set("columns", 0), this.bind("label").to(this, "columns", this, "rows", (s, r) => `${r} × ${s}`), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck"
        ]
      },
      children: [
        {
          tag: "div",
          attributes: {
            class: [
              "ck-insert-table-dropdown__grid"
            ]
          },
          on: {
            "mouseover@.ck-insert-table-dropdown-grid-box": i.to("boxover")
          },
          children: this.items
        },
        {
          tag: "div",
          attributes: {
            class: [
              "ck",
              "ck-insert-table-dropdown__label"
            ],
            "aria-hidden": !0
          },
          children: [
            {
              text: i.to("label")
            }
          ]
        }
      ],
      on: {
        mousedown: i.to((s) => {
          s.preventDefault();
        }),
        click: i.to(() => {
          this.fire("execute");
        })
      }
    }), this.on("boxover", (s, r) => {
      const { row: o, column: a } = r.target.dataset;
      this.items.get((parseInt(o, 10) - 1) * 10 + (parseInt(a, 10) - 1)).focus();
    }), this.focusTracker.on("change:focusedElement", (s, r, o) => {
      if (!o)
        return;
      const { row: a, column: l } = o.dataset;
      this.set({
        rows: parseInt(a),
        columns: parseInt(l)
      });
    }), this.on("change:columns", () => this._highlightGridBoxes()), this.on("change:rows", () => this._highlightGridBoxes());
  }
  render() {
    super.render(), Oa({
      keystrokeHandler: this.keystrokes,
      focusTracker: this.focusTracker,
      gridItems: this.items,
      numberOfColumns: 10,
      uiLanguageDirection: this.locale && this.locale.uiLanguageDirection
    });
    for (const e of this.items)
      this.focusTracker.add(e.element);
    this.keystrokes.listenTo(this.element);
  }
  /**
  * Resets the rows and columns selection.
  */
  reset() {
    this.set({
      rows: 1,
      columns: 1
    });
  }
  /**
  * @inheritDoc
  */
  focus() {
    this.items.get(0).focus();
  }
  /**
  * @inheritDoc
  */
  focusLast() {
    this.items.get(0).focus();
  }
  /**
  * Highlights grid boxes depending on rows and columns selected.
  */
  _highlightGridBoxes() {
    const e = this.rows, i = this.columns;
    this.items.map((s, r) => {
      const o = Math.floor(r / 10), a = r % 10, l = o < e && a < i;
      s.set("isOn", l);
    });
  }
  /**
  * Creates a new Button for the grid.
  *
  * @param locale The locale instance.
  * @param row Row number.
  * @param column Column number.
  * @param label The grid button label.
  */
  _createGridButton(e, i, s, r) {
    const o = new le(e);
    return o.set({
      label: r,
      class: "ck-insert-table-dropdown-grid-box"
    }), o.extendTemplate({
      attributes: {
        "data-row": i,
        "data-column": s
      }
    }), o;
  }
  /**
  * @returns A view collection containing boxes to be placed in a table grid.
  */
  _createGridCollection() {
    const e = [];
    for (let i = 0; i < 100; i++) {
      const s = Math.floor(i / 10), r = i % 10, o = `${s + 1} × ${r + 1}`;
      e.push(this._createGridButton(this.locale, s + 1, r + 1, o));
    }
    return this.createCollection(e);
  }
}
var GG = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 1h15A1.5 1.5 0 0 1 19 2.5v15a1.5 1.5 0 0 1-1.5 1.5h-15A1.5 1.5 0 0 1 1 17.5v-15A1.5 1.5 0 0 1 2.5 1zM2 2v16h16V2H2z" opacity=".6"/><path d="M18 7v1H2V7h16zm0 5v1H2v-1h16z" opacity=".6"/><path d="M14 1v18a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1V1a1 1 0 0 1 1-1h6a1 1 0 0 1 1 1zm-2 1H8v4h4V2zm0 6H8v4h4V8zm0 6H8v4h4v-4z"/></svg>', KG = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 1h15A1.5 1.5 0 0 1 19 2.5v15a1.5 1.5 0 0 1-1.5 1.5h-15A1.5 1.5 0 0 1 1 17.5v-15A1.5 1.5 0 0 1 2.5 1zM2 2v16h16V2H2z" opacity=".6"/><path d="M7 2h1v16H7V2zm5 0h1v16h-1V2z" opacity=".6"/><path d="M1 6h18a1 1 0 0 1 1 1v6a1 1 0 0 1-1 1H1a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1zm1 2v4h4V8H2zm6 0v4h4V8H8zm6 0v4h4V8h-4z"/></svg>', ZG = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M2.5 1h15A1.5 1.5 0 0 1 19 2.5v15a1.5 1.5 0 0 1-1.5 1.5h-15A1.5 1.5 0 0 1 1 17.5v-15A1.5 1.5 0 0 1 2.5 1zM2 2v16h16V2H2z" opacity=".6"/><path d="M7 2h1v16H7V2zm5 0h1v7h-1V2zm6 5v1H2V7h16zM8 12v1H2v-1h6z" opacity=".6"/><path d="M7 7h12a1 1 0 0 1 1 1v11a1 1 0 0 1-1 1H7a1 1 0 0 1-1-1V8a1 1 0 0 1 1-1zm1 2v9h10V9H8z"/></svg>';
class YG extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = this.editor.t, s = t.locale.contentLanguageDirection === "ltr";
    t.ui.componentFactory.add("insertTable", (r) => {
      const o = t.commands.get("insertTable"), a = Tt(r);
      a.bind("isEnabled").to(o), a.buttonView.set({
        icon: Z.table,
        label: e("Insert table"),
        tooltip: !0
      });
      let l;
      return a.on("change:isOpen", () => {
        l || (l = new Fw(r), a.panelView.children.add(l), l.delegate("execute").to(a), a.on("execute", () => {
          t.execute("insertTable", {
            rows: l.rows,
            columns: l.columns
          }), t.editing.view.focus();
        }));
      }), a;
    }), t.ui.componentFactory.add("menuBar:insertTable", (r) => {
      const o = t.commands.get("insertTable"), a = new Yt(r), l = new Fw(r);
      return l.delegate("execute").to(a), a.on("change:isOpen", (c, u, d) => {
        d || l.reset();
      }), l.on("execute", () => {
        t.execute("insertTable", {
          rows: l.rows,
          columns: l.columns
        }), t.editing.view.focus();
      }), a.buttonView.set({
        label: e("Table"),
        icon: Z.table
      }), a.panelView.children.add(l), a.bind("isEnabled").to(o), a;
    }), t.ui.componentFactory.add("tableColumn", (r) => {
      const o = [
        {
          type: "switchbutton",
          model: {
            commandName: "setTableColumnHeader",
            label: e("Header column"),
            bindIsOn: !0
          }
        },
        {
          type: "separator"
        },
        {
          type: "button",
          model: {
            commandName: s ? "insertTableColumnLeft" : "insertTableColumnRight",
            label: e("Insert column left")
          }
        },
        {
          type: "button",
          model: {
            commandName: s ? "insertTableColumnRight" : "insertTableColumnLeft",
            label: e("Insert column right")
          }
        },
        {
          type: "button",
          model: {
            commandName: "removeTableColumn",
            label: e("Delete column")
          }
        },
        {
          type: "button",
          model: {
            commandName: "selectTableColumn",
            label: e("Select column")
          }
        }
      ];
      return this._prepareDropdown(e("Column"), GG, o, r);
    }), t.ui.componentFactory.add("tableRow", (r) => {
      const o = [
        {
          type: "switchbutton",
          model: {
            commandName: "setTableRowHeader",
            label: e("Header row"),
            bindIsOn: !0
          }
        },
        {
          type: "separator"
        },
        {
          type: "button",
          model: {
            commandName: "insertTableRowAbove",
            label: e("Insert row above")
          }
        },
        {
          type: "button",
          model: {
            commandName: "insertTableRowBelow",
            label: e("Insert row below")
          }
        },
        {
          type: "button",
          model: {
            commandName: "removeTableRow",
            label: e("Delete row")
          }
        },
        {
          type: "button",
          model: {
            commandName: "selectTableRow",
            label: e("Select row")
          }
        }
      ];
      return this._prepareDropdown(e("Row"), KG, o, r);
    }), t.ui.componentFactory.add("mergeTableCells", (r) => {
      const o = [
        {
          type: "button",
          model: {
            commandName: "mergeTableCellUp",
            label: e("Merge cell up")
          }
        },
        {
          type: "button",
          model: {
            commandName: s ? "mergeTableCellRight" : "mergeTableCellLeft",
            label: e("Merge cell right")
          }
        },
        {
          type: "button",
          model: {
            commandName: "mergeTableCellDown",
            label: e("Merge cell down")
          }
        },
        {
          type: "button",
          model: {
            commandName: s ? "mergeTableCellLeft" : "mergeTableCellRight",
            label: e("Merge cell left")
          }
        },
        {
          type: "separator"
        },
        {
          type: "button",
          model: {
            commandName: "splitTableCellVertically",
            label: e("Split cell vertically")
          }
        },
        {
          type: "button",
          model: {
            commandName: "splitTableCellHorizontally",
            label: e("Split cell horizontally")
          }
        }
      ];
      return this._prepareMergeSplitButtonDropdown(e("Merge cells"), ZG, o, r);
    });
  }
  /**
  * Creates a dropdown view from a set of options.
  *
  * @param label The dropdown button label.
  * @param icon An icon for the dropdown button.
  * @param options The list of options for the dropdown.
  */
  _prepareDropdown(t, e, i, s) {
    const r = this.editor, o = Tt(s), a = this._fillDropdownWithListOptions(o, i);
    return o.buttonView.set({
      label: t,
      icon: e,
      tooltip: !0
    }), o.bind("isEnabled").toMany(a, "isEnabled", (...l) => l.some((c) => c)), this.listenTo(o, "execute", (l) => {
      r.execute(l.source.commandName), l.source instanceof js || r.editing.view.focus();
    }), o;
  }
  /**
  * Creates a dropdown view with a {@link module:ui/dropdown/button/splitbuttonview~SplitButtonView} for
  * merge (and split)–related commands.
  *
  * @param label The dropdown button label.
  * @param icon An icon for the dropdown button.
  * @param options The list of options for the dropdown.
  */
  _prepareMergeSplitButtonDropdown(t, e, i, s) {
    const r = this.editor, o = Tt(s, Ys), a = "mergeTableCells", l = r.commands.get(a), c = this._fillDropdownWithListOptions(o, i);
    return o.buttonView.set({
      label: t,
      icon: e,
      tooltip: !0,
      isEnabled: !0
    }), o.bind("isEnabled").toMany([
      l,
      ...c
    ], "isEnabled", (...u) => u.some((d) => d)), this.listenTo(o.buttonView, "execute", () => {
      r.execute(a), r.editing.view.focus();
    }), this.listenTo(o, "execute", (u) => {
      r.execute(u.source.commandName), r.editing.view.focus();
    }), o;
  }
  /**
  * Injects a {@link module:ui/list/listview~ListView} into the passed dropdown with buttons
  * which execute editor commands as configured in passed options.
  *
  * @param options The list of options for the dropdown.
  * @returns Commands the list options are interacting with.
  */
  _fillDropdownWithListOptions(t, e) {
    const i = this.editor, s = [], r = new bt();
    for (const o of e)
      JG(o, i, s, r);
    return En(t, r), s;
  }
}
function JG(n, t, e, i) {
  if (n.type === "button" || n.type === "switchbutton") {
    const s = n.model = new tn(n.model), { commandName: r, bindIsOn: o } = n.model, a = t.commands.get(r);
    e.push(a), s.set({
      commandName: r
    }), s.bind("isEnabled").to(a), o && s.bind("isOn").to(a, "value"), s.set({
      withText: !0
    });
  }
  i.add(n);
}
class Ms extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableSelection";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      $t,
      $t
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model, i = t.editing.view;
    this.listenTo(e, "deleteContent", (s, r) => this._handleDeleteContent(s, r), {
      priority: "high"
    }), this.listenTo(i.document, "insertText", (s, r) => this._handleInsertTextEvent(s, r), {
      priority: "high"
    }), this._defineSelectionConverter(), this._enablePluginDisabling();
  }
  /**
  * Returns the currently selected table cells or `null` if it is not a table cells selection.
  */
  getSelectedTableCells() {
    const t = this.editor.plugins.get($t), e = this.editor.model.document.selection, i = t.getSelectedTableCells(e);
    return i.length == 0 ? null : i;
  }
  /**
  * Returns the selected table fragment as a document fragment.
  */
  getSelectionAsFragment() {
    const t = this.editor.plugins.get($t), e = this.getSelectedTableCells();
    return e ? this.editor.model.change((i) => {
      const s = i.createDocumentFragment(), { first: r, last: o } = t.getColumnIndexes(e), { first: a, last: l } = t.getRowIndexes(e), c = e[0].findAncestor("table");
      let u = l, d = o;
      if (t.isSelectionRectangular(e)) {
        const g = {
          firstColumn: r,
          lastColumn: o,
          firstRow: a,
          lastRow: l
        };
        u = ZC(c, g), d = YC(c, g);
      }
      const m = jC(c, {
        startRow: a,
        startColumn: r,
        endRow: u,
        endColumn: d
      }, i);
      return i.insert(m, s, 0), s;
    }) : null;
  }
  /**
  * Sets the model selection based on given anchor and target cells (can be the same cell).
  * Takes care of setting the backward flag.
  *
  * ```ts
  * const modelRoot = editor.model.document.getRoot();
  * const firstCell = modelRoot.getNodeByPath( [ 0, 0, 0 ] );
  * const lastCell = modelRoot.getNodeByPath( [ 0, 0, 1 ] );
  *
  * const tableSelection = editor.plugins.get( 'TableSelection' );
  * tableSelection.setCellSelection( firstCell, lastCell );
  * ```
  */
  setCellSelection(t, e) {
    const i = this._getCellsToSelect(t, e);
    this.editor.model.change((s) => {
      s.setSelection(i.cells.map((r) => s.createRangeOn(r)), {
        backward: i.backward
      });
    });
  }
  /**
  * Returns the focus cell from the current selection.
  */
  getFocusCell() {
    const i = [
      ...this.editor.model.document.selection.getRanges()
    ].pop().getContainedElement();
    return i && i.is("element", "tableCell") ? i : null;
  }
  /**
  * Returns the anchor cell from the current selection.
  */
  getAnchorCell() {
    const t = this.editor.model.document.selection, i = Xe(t.getRanges()).getContainedElement();
    return i && i.is("element", "tableCell") ? i : null;
  }
  /**
  * Defines a selection converter which marks the selected cells with a specific class.
  *
  * The real DOM selection is put in the last cell. Since the order of ranges is dependent on whether the
  * selection is backward or not, the last cell will usually be close to the "focus" end of the selection
  * (a selection has anchor and focus).
  *
  * The real DOM selection is then hidden with CSS.
  */
  _defineSelectionConverter() {
    const t = this.editor, e = /* @__PURE__ */ new Set();
    t.conversion.for("editingDowncast").add((s) => s.on("selection", (r, o, a) => {
      const l = a.writer;
      i(l);
      const c = this.getSelectedTableCells();
      if (!c)
        return;
      for (const d of c) {
        const h = a.mapper.toViewElement(d);
        l.addClass("ck-editor__editable_selected", h), e.add(h);
      }
      const u = a.mapper.toViewElement(c[c.length - 1]);
      l.setSelection(u, 0);
    }, {
      priority: "lowest"
    }));
    function i(s) {
      for (const r of e)
        s.removeClass("ck-editor__editable_selected", r);
      e.clear();
    }
  }
  /**
  * Creates a listener that reacts to changes in {@link #isEnabled} and, if the plugin was disabled,
  * it collapses the multi-cell selection to a regular selection placed inside a table cell.
  *
  * This listener helps features that disable the table selection plugin bring the selection
  * to a clear state they can work with (for instance, because they don't support multiple cell selection).
  */
  _enablePluginDisabling() {
    const t = this.editor;
    this.on("change:isEnabled", () => {
      if (!this.isEnabled) {
        const e = this.getSelectedTableCells();
        if (!e)
          return;
        t.model.change((i) => {
          const s = i.createPositionAt(e[0], 0), r = t.model.schema.getNearestSelectionRange(s);
          i.setSelection(r);
        });
      }
    });
  }
  /**
  * Overrides the default `model.deleteContent()` behavior over a selected table fragment.
  *
  * @param args Delete content method arguments.
  */
  _handleDeleteContent(t, e) {
    const i = this.editor.plugins.get($t), s = e[0], r = e[1], o = this.editor.model, a = !r || r.direction == "backward", l = i.getSelectedTableCells(s);
    l.length && (t.stop(), o.change((c) => {
      const u = l[a ? l.length - 1 : 0];
      o.change((h) => {
        for (const m of l)
          o.deleteContent(h.createSelection(m, "in"));
      });
      const d = o.schema.getNearestSelectionRange(c.createPositionAt(u, 0));
      s.is("documentSelection") ? c.setSelection(d) : s.setTo(d);
    }));
  }
  /**
  * This handler makes it possible to remove the content of all selected cells by starting to type.
  * If you take a look at {@link #_defineSelectionConverter} you will find out that despite the multi-cell selection being set
  * in the model, the view selection is collapsed in the last cell (because most browsers are unable to render multi-cell selections;
  * yes, it's a hack).
  *
  * When multiple cells are selected in the model and the user starts to type, the
  * {@link module:engine/view/document~Document#event:insertText} event carries information provided by the
  * beforeinput DOM  event, that in turn only knows about this collapsed DOM selection in the last cell.
  *
  * As a result, the selected cells have no chance to be cleaned up. To fix this, this listener intercepts
  * the event and injects the custom view selection in the data that translates correctly to the actual state
  * of the multi-cell selection in the model.
  *
  * @param data Insert text event data.
  */
  _handleInsertTextEvent(t, e) {
    const i = this.editor, s = this.getSelectedTableCells();
    if (!s)
      return;
    const r = i.editing.view, o = i.editing.mapper, a = s.map((l) => r.createRangeOn(o.toViewElement(l)));
    e.selection = r.createSelection(a);
  }
  /**
  * Returns an array of table cells that should be selected based on the
  * given anchor cell and target (focus) cell.
  *
  * The cells are returned in a reverse direction if the selection is backward.
  */
  _getCellsToSelect(t, e) {
    const i = this.editor.plugins.get("TableUtils"), s = i.getCellLocation(t), r = i.getCellLocation(e), o = Math.min(s.row, r.row), a = Math.max(s.row, r.row), l = Math.min(s.column, r.column), c = Math.max(s.column, r.column), u = new Array(a - o + 1).fill(null).map(() => []), d = {
      startRow: o,
      endRow: a,
      startColumn: l,
      endColumn: c
    };
    for (const { row: g, cell: p } of new Ke(t.findAncestor("table"), d))
      u[g - o].push(p);
    const h = r.row < s.row, m = r.column < s.column;
    return h && u.reverse(), m && u.forEach((g) => g.reverse()), {
      cells: u.flat(),
      backward: h || m
    };
  }
}
class XG extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableClipboard";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Go,
      _i,
      Ms,
      $t
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.editing.view.document;
    this.listenTo(e, "copy", (i, s) => this._onCopyCut(i, s)), this.listenTo(e, "cut", (i, s) => this._onCopyCut(i, s)), this.listenTo(t.model, "insertContent", (i, [s, r]) => this._onInsertContent(i, s, r), {
      priority: "high"
    }), this.decorate("_replaceTableSlotCell");
  }
  /**
  * Copies table content to a clipboard on "copy" & "cut" events.
  *
  * @param evt An object containing information about the handled event.
  * @param data Clipboard event data.
  */
  _onCopyCut(t, e) {
    const i = this.editor.editing.view, s = this.editor.plugins.get(Ms), r = this.editor.plugins.get(Go);
    s.getSelectedTableCells() && (t.name == "cut" && !this.editor.model.canEditAt(this.editor.model.document.selection) || (e.preventDefault(), t.stop(), this.editor.model.enqueueChange({
      isUndoable: t.name === "cut"
    }, () => {
      const o = r._copySelectedFragmentWithMarkers(t.name, this.editor.model.document.selection, () => s.getSelectionAsFragment());
      i.document.fire("clipboardOutput", {
        dataTransfer: e.dataTransfer,
        content: this.editor.data.toView(o),
        method: t.name
      });
    })));
  }
  /**
  * Overrides default {@link module:engine/model/model~Model#insertContent `model.insertContent()`} method to handle pasting table inside
  * selected table fragment.
  *
  * Depending on selected table fragment:
  * - If a selected table fragment is smaller than paste table it will crop pasted table to match dimensions.
  * - If dimensions are equal it will replace selected table fragment with a pasted table contents.
  *
  * @param content The content to insert.
  * @param selectable The selection into which the content should be inserted.
  * If not provided the current model document selection will be used.
  */
  _onInsertContent(t, e, i) {
    if (i && !i.is("documentSelection"))
      return;
    const s = this.editor.model, r = this.editor.plugins.get($t), o = this.editor.plugins.get(Go), a = this.getTableIfOnlyTableInContent(e, s);
    if (!a)
      return;
    const l = r.getSelectionAffectedTableCells(s.document.selection);
    if (!l.length) {
      wm(a, r);
      return;
    }
    t.stop(), e.is("documentFragment") ? o._pasteMarkersIntoTransformedElement(e.markers, (c) => this._replaceSelectedCells(a, l, c)) : this.editor.model.change((c) => {
      this._replaceSelectedCells(a, l, c);
    });
  }
  /**
  * Inserts provided `selectedTableCells` into `pastedTable`.
  */
  _replaceSelectedCells(t, e, i) {
    const s = this.editor.plugins.get($t), r = {
      width: s.getColumns(t),
      height: s.getRows(t)
    }, o = QG(e, r, i, s), a = o.lastRow - o.firstRow + 1, l = o.lastColumn - o.firstColumn + 1, c = {
      startRow: 0,
      startColumn: 0,
      endRow: Math.min(a, r.height) - 1,
      endColumn: Math.min(l, r.width) - 1
    };
    t = jC(t, c, i);
    const u = e[0].findAncestor("table"), d = this._replaceSelectedCellsWithPasted(t, r, u, o, i);
    if (this.editor.plugins.get("TableSelection").isEnabled) {
      const h = s.sortRanges(d.map((m) => i.createRangeOn(m)));
      i.setSelection(h);
    } else
      i.setSelection(d[0], 0);
    return u;
  }
  /**
  * Replaces the part of selectedTable with pastedTable.
  */
  _replaceSelectedCellsWithPasted(t, e, i, s, r) {
    const { width: o, height: a } = e, l = tK(t, o, a), c = [
      ...new Ke(i, {
        startRow: s.firstRow,
        endRow: s.lastRow,
        startColumn: s.firstColumn,
        endColumn: s.lastColumn,
        includeAllSlots: !0
      })
    ], u = [];
    let d;
    for (const w of c) {
      const { row: y, column: k } = w;
      k === s.firstColumn && (d = w.getPositionBefore());
      const C = y - s.firstRow, E = k - s.firstColumn, T = l[C % a][E % o], L = T ? r.cloneElement(T) : null, O = this._replaceTableSlotCell(w, L, d, r);
      O && (KC(O, y, k, s.lastRow, s.lastColumn, r), u.push(O), d = r.createPositionAfter(O));
    }
    const h = parseInt(i.getAttribute("headingRows") || "0"), m = parseInt(i.getAttribute("headingColumns") || "0"), g = s.firstRow < h && h <= s.lastRow, p = s.firstColumn < m && m <= s.lastColumn;
    if (g) {
      const w = {
        first: s.firstColumn,
        last: s.lastColumn
      }, y = Hh(i, h, w, r, s.firstRow);
      u.push(...y);
    }
    if (p) {
      const w = {
        first: s.firstRow,
        last: s.lastRow
      }, y = jh(i, m, w, r);
      u.push(...y);
    }
    return u;
  }
  /**
  * Replaces a single table slot.
  *
  * @returns Inserted table cell or null if slot should remain empty.
  * @private
  */
  _replaceTableSlotCell(t, e, i, s) {
    const { cell: r, isAnchor: o } = t;
    return o && s.remove(r), e ? (s.insert(e, i), e) : null;
  }
  /**
  * Extracts the table for pasting into a table.
  *
  * @param content The content to insert.
  * @param model The editor model.
  */
  getTableIfOnlyTableInContent(t, e) {
    if (!t.is("documentFragment") && !t.is("element"))
      return null;
    if (t.is("element", "table"))
      return t;
    if (t.childCount == 1 && t.getChild(0).is("element", "table"))
      return t.getChild(0);
    const i = e.createRangeIn(t);
    for (const s of i.getItems())
      if (s.is("element", "table")) {
        const r = e.createRange(i.start, e.createPositionBefore(s));
        if (e.hasContent(r, {
          ignoreWhitespaces: !0
        }))
          return null;
        const o = e.createRange(e.createPositionAfter(s), i.end);
        return e.hasContent(o, {
          ignoreWhitespaces: !0
        }) ? null : s;
      }
    return null;
  }
}
function QG(n, t, e, i) {
  const s = n[0].findAncestor("table"), r = i.getColumnIndexes(n), o = i.getRowIndexes(n), a = {
    firstColumn: r.first,
    lastColumn: r.last,
    firstRow: o.first,
    lastRow: o.last
  }, l = n.length === 1;
  return l && (a.lastRow += t.height - 1, a.lastColumn += t.width - 1, eK(s, a.lastRow + 1, a.lastColumn + 1, i)), l || !i.isSelectionRectangular(n) ? iK(s, a, e) : (a.lastRow = ZC(s, a), a.lastColumn = YC(s, a)), a;
}
function eK(n, t, e, i) {
  const s = i.getColumns(n), r = i.getRows(n);
  e > s && i.insertColumns(n, {
    at: s,
    columns: e - s
  }), t > r && i.insertRows(n, {
    at: r,
    rows: t - r
  });
}
function tK(n, t, e) {
  const i = new Array(e).fill(null).map(() => new Array(t).fill(null));
  for (const { column: s, row: r, cell: o } of new Ke(n))
    i[r][s] = o;
  return i;
}
function iK(n, t, e) {
  const { firstRow: i, lastRow: s, firstColumn: r, lastColumn: o } = t, a = {
    first: i,
    last: s
  }, l = {
    first: r,
    last: o
  };
  jh(n, r, a, e), jh(n, o + 1, a, e), Hh(n, i, l, e), Hh(n, s + 1, l, e, i);
}
function Hh(n, t, e, i, s = 0) {
  return t < 1 ? void 0 : UC(n, t, s).filter(({ column: a, cellWidth: l }) => e2(a, l, e)).map(({ cell: a }) => WC(a, t, i));
}
function jh(n, t, e, i) {
  return t < 1 ? void 0 : qC(n, t).filter(({ row: o, cellHeight: a }) => e2(o, a, e)).map(({ cell: o, column: a }) => GC(o, a, t, i));
}
function e2(n, t, e) {
  const i = n + t - 1, { first: s, last: r } = e, o = n >= s && n <= r, a = n < s && i >= s;
  return o || a;
}
class nK extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableKeyboard";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Ms,
      $t
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, i = t.editing.view.document, s = t.t;
    this.listenTo(i, "arrowKey", (...r) => this._onArrowKey(...r), {
      context: "table"
    }), this.listenTo(i, "tab", (...r) => this._handleTabOnSelectedTable(...r), {
      context: "figure"
    }), this.listenTo(i, "tab", (...r) => this._handleTab(...r), {
      context: [
        "th",
        "td"
      ]
    }), t.accessibility.addKeystrokeInfoGroup({
      id: "table",
      label: s("Keystrokes that can be used in a table cell"),
      keystrokes: [
        {
          label: s("Move the selection to the next cell"),
          keystroke: "Tab"
        },
        {
          label: s("Move the selection to the previous cell"),
          keystroke: "Shift+Tab"
        },
        {
          label: s("Insert a new table row (when in the last cell of a table)"),
          keystroke: "Tab"
        },
        {
          label: s("Navigate through the table"),
          keystroke: [
            [
              "arrowup"
            ],
            [
              "arrowright"
            ],
            [
              "arrowdown"
            ],
            [
              "arrowleft"
            ]
          ]
        }
      ]
    });
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:tab tab} events for the <kbd>Tab</kbd> key executed
  * when the table widget is selected.
  */
  _handleTabOnSelectedTable(t, e) {
    const i = this.editor, r = i.model.document.selection.getSelectedElement();
    !r || !r.is("element", "table") || (e.preventDefault(), e.stopPropagation(), t.stop(), i.model.change((o) => {
      o.setSelection(o.createRangeIn(r.getChild(0).getChild(0)));
    }));
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:tab tab} events for the <kbd>Tab</kbd> key executed
  * inside table cells.
  */
  _handleTab(t, e) {
    const i = this.editor, s = this.editor.plugins.get($t), r = this.editor.plugins.get("TableSelection"), o = i.model.document.selection, a = !e.shiftKey;
    let l = s.getTableCellsContainingSelection(o)[0];
    if (l || (l = r.getFocusCell()), !l)
      return;
    e.preventDefault(), e.stopPropagation(), t.stop();
    const c = l.parent, u = c.parent, d = u.getChildIndex(c), h = c.getChildIndex(l), m = h === 0;
    if (!a && m && d === 0) {
      i.model.change((y) => {
        y.setSelection(y.createRangeOn(u));
      });
      return;
    }
    const g = h === c.childCount - 1, p = d === s.getRows(u) - 1;
    if (a && p && g && (i.execute("insertTableRowBelow"), d === s.getRows(u) - 1)) {
      i.model.change((y) => {
        y.setSelection(y.createRangeOn(u));
      });
      return;
    }
    let w;
    if (a && g)
      w = u.getChild(d + 1).getChild(0);
    else if (!a && m) {
      const y = u.getChild(d - 1);
      w = y.getChild(y.childCount - 1);
    } else
      w = c.getChild(h + (a ? 1 : -1));
    i.model.change((y) => {
      y.setSelection(y.createRangeIn(w));
    });
  }
  /**
  * Handles {@link module:engine/view/document~Document#event:keydown keydown} events.
  */
  _onArrowKey(t, e) {
    const i = this.editor, s = e.keyCode, r = Wc(s, i.locale.contentLanguageDirection);
    this._handleArrowKeys(r, e.shiftKey) && (e.preventDefault(), e.stopPropagation(), t.stop());
  }
  /**
  * Handles arrow keys to move the selection around the table.
  *
  * @param direction The direction of the arrow key.
  * @param expandSelection If the current selection should be expanded.
  * @returns Returns `true` if key was handled.
  */
  _handleArrowKeys(t, e) {
    const i = this.editor.plugins.get($t), s = this.editor.plugins.get("TableSelection"), r = this.editor.model, o = r.document.selection, a = [
      "right",
      "down"
    ].includes(t), l = i.getSelectedTableCells(o);
    if (l.length) {
      let u;
      return e ? u = s.getFocusCell() : u = a ? l[l.length - 1] : l[0], this._navigateFromCellInDirection(u, t, e), !0;
    }
    const c = o.focus.findAncestor("tableCell");
    /* istanbul ignore if: paranoid check -- @preserve */
    if (!c)
      return !1;
    if (!o.isCollapsed)
      if (e) {
        if (o.isBackward == a && !o.containsEntireContent(c))
          return !1;
      } else {
        const u = o.getSelectedElement();
        if (!u || !r.schema.isObject(u))
          return !1;
      }
    return this._isSelectionAtCellEdge(o, c, a) ? (this._navigateFromCellInDirection(c, t, e), !0) : !1;
  }
  /**
  * Returns `true` if the selection is at the boundary of a table cell according to the navigation direction.
  *
  * @param selection The current selection.
  * @param tableCell The current table cell element.
  * @param isForward The expected navigation direction.
  */
  _isSelectionAtCellEdge(t, e, i) {
    const s = this.editor.model, r = this.editor.model.schema, o = i ? t.getLastPosition() : t.getFirstPosition();
    if (!r.getLimitElement(o).is("element", "tableCell"))
      return s.createPositionAt(e, i ? "end" : 0).isTouching(o);
    const a = s.createSelection(o);
    return s.modifySelection(a, {
      direction: i ? "forward" : "backward"
    }), o.isEqual(a.focus);
  }
  /**
  * Moves the selection from the given table cell in the specified direction.
  *
  * @param focusCell The table cell that is current multi-cell selection focus.
  * @param direction Direction in which selection should move.
  * @param expandSelection If the current selection should be expanded. Default value is false.
  */
  _navigateFromCellInDirection(t, e, i = !1) {
    const s = this.editor.model, r = t.findAncestor("table"), o = [
      ...new Ke(r, {
        includeAllSlots: !0
      })
    ], { row: a, column: l } = o[o.length - 1], c = o.find(({ cell: k }) => k == t);
    let { row: u, column: d } = c;
    switch (e) {
      case "left":
        d--;
        break;
      case "up":
        u--;
        break;
      case "right":
        d += c.cellWidth;
        break;
      case "down":
        u += c.cellHeight;
        break;
    }
    const h = u < 0 || u > a, m = d < 0 && u <= 0, g = d > l && u >= a;
    if (h || m || g) {
      s.change((k) => {
        k.setSelection(k.createRangeOn(r));
      });
      return;
    }
    d < 0 ? (d = i ? 0 : l, u--) : d > l && (d = i ? l : 0, u++);
    const p = o.find((k) => k.row == u && k.column == d).cell, w = [
      "right",
      "down"
    ].includes(e), y = this.editor.plugins.get("TableSelection");
    if (i && y.isEnabled) {
      const k = y.getAnchorCell() || t;
      y.setCellSelection(k, p);
    } else {
      const k = s.createPositionAt(p, w ? 0 : "end");
      s.change((C) => {
        C.setSelection(k);
      });
    }
  }
}
class t2 extends xn {
  constructor() {
    super(...arguments);
    f(this, "domEventType", [
      "mousemove",
      "mouseleave"
    ]);
  }
  /**
  * @inheritDoc
  */
  onDomEvent(e) {
    this.fire(e.type, e);
  }
}
class sK extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableMouse";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Ms,
      $t
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.editing.view.addObserver(t2), this._enableShiftClickSelection(), this._enableMouseDragSelection();
  }
  /**
  * Enables making cells selection by <kbd>Shift</kbd>+click. Creates a selection from the cell which previously held
  * the selection to the cell which was clicked. It can be the same cell, in which case it selects a single cell.
  */
  _enableShiftClickSelection() {
    const t = this.editor, e = t.plugins.get($t);
    let i = !1;
    const s = t.plugins.get(Ms);
    this.listenTo(t.editing.view.document, "mousedown", (r, o) => {
      const a = t.model.document.selection;
      if (!this.isEnabled || !s.isEnabled || !o.domEvent.shiftKey)
        return;
      const l = s.getAnchorCell() || e.getTableCellsContainingSelection(a)[0];
      if (!l)
        return;
      const c = this._getModelTableCellFromDomEvent(o);
      c && Dw(l, c) && (i = !0, s.setCellSelection(l, c), o.preventDefault());
    }), this.listenTo(t.editing.view.document, "mouseup", () => {
      i = !1;
    }), this.listenTo(t.editing.view.document, "selectionChange", (r) => {
      i && r.stop();
    }, {
      priority: "highest"
    });
  }
  /**
  * Enables making cells selection by dragging.
  *
  * The selection is made only on mousemove. Mouse tracking is started on mousedown.
  * However, the cells selection is enabled only after the mouse cursor left the anchor cell.
  * Thanks to that normal text selection within one cell works just fine. However, you can still select
  * just one cell by leaving the anchor cell and moving back to it.
  */
  _enableMouseDragSelection() {
    const t = this.editor;
    let e, i, s = !1, r = !1;
    const o = t.plugins.get(Ms);
    this.listenTo(t.editing.view.document, "mousedown", (a, l) => {
      !this.isEnabled || !o.isEnabled || l.domEvent.shiftKey || l.domEvent.ctrlKey || l.domEvent.altKey || (e = this._getModelTableCellFromDomEvent(l));
    }), this.listenTo(t.editing.view.document, "mousemove", (a, l) => {
      if (!l.domEvent.buttons || !e)
        return;
      const c = this._getModelTableCellFromDomEvent(l);
      c && Dw(e, c) && (i = c, !s && i != e && (s = !0)), s && (r = !0, o.setCellSelection(e, i), l.preventDefault());
    }), this.listenTo(t.editing.view.document, "mouseup", () => {
      s = !1, r = !1, e = null, i = null;
    }), this.listenTo(t.editing.view.document, "selectionChange", (a) => {
      r && a.stop();
    }, {
      priority: "highest"
    });
  }
  /**
  * Returns the model table cell element based on the target element of the passed DOM event.
  *
  * @returns Returns the table cell or `undefined`.
  */
  _getModelTableCellFromDomEvent(t) {
    const e = t.target, i = this.editor.editing.view.createPositionAt(e, 0);
    return this.editor.editing.mapper.toModelPosition(i).parent.findAncestor("tableCell", {
      includeSelf: !0
    });
  }
}
function Dw(n, t) {
  return n.parent.parent == t.parent.parent;
}
class rK extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Wa,
      YG,
      Ms,
      sK,
      nK,
      XG,
      Sn
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "Table";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
function i2(n) {
  const t = n2(n);
  return t || ou(n);
}
function n2(n) {
  const t = n.getSelectedElement();
  return t && s2(t) ? t : null;
}
function ou(n) {
  const t = n.getFirstPosition();
  if (!t)
    return null;
  let e = t.parent;
  for (; e; ) {
    if (e.is("element") && s2(e))
      return e;
    e = e.parent;
  }
  return null;
}
function s2(n) {
  return !!n.getCustomProperty("table") && vt(n);
}
class oK extends I {
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      oc
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableToolbar";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  afterInit() {
    const t = this.editor, e = t.t, i = t.plugins.get(oc), s = t.config.get("table.contentToolbar"), r = t.config.get("table.tableToolbar");
    s && i.register("tableContent", {
      ariaLabel: e("Table toolbar"),
      items: s,
      getRelatedElement: ou
    }), r && i.register("table", {
      ariaLabel: e("Table toolbar"),
      items: r,
      getRelatedElement: n2
    });
  }
}
class aK extends ee {
  /**
  * Creates an instance of the color input view.
  *
  * @param locale The locale instance.
  * @param options The input options.
  * @param options.colorDefinitions The colors to be displayed in the palette inside the input's dropdown.
  * @param options.columns The number of columns in which the colors will be displayed.
  * @param options.defaultColorValue If specified, the color input view will replace the "Remove color" button with
  * the "Restore default" button. Instead of clearing the input field, the default color value will be set.
  */
  constructor(e, i) {
    super(e);
    /**
    * A cached reference to the options passed to the constructor.
    */
    f(this, "options");
    /**
    * Tracks information about the DOM focus in the view.
    */
    f(this, "focusTracker");
    /**
    * Helps cycling over focusable children in the input view.
    */
    f(this, "focusCycler");
    /**
    * A collection of views that can be focused in the view.
    */
    f(this, "_focusables");
    /**
    * An instance of the dropdown allowing to select a color from a grid.
    */
    f(this, "dropdownView");
    /**
    * An instance of the input allowing the user to type a color value.
    */
    f(this, "inputView");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * The flag that indicates whether the user is still typing.
    * If set to true, it means that the text input field ({@link #inputView}) still has the focus.
    * So, we should interrupt the user by replacing the input's value.
    */
    f(this, "_stillTyping");
    this.set("value", ""), this.set("isReadOnly", !1), this.set("isFocused", !1), this.set("isEmpty", !0), this.options = i, this.focusTracker = new ze(), this._focusables = new Et(), this.dropdownView = this._createDropdownView(), this.inputView = this._createInputTextView(), this.keystrokes = new Ze(), this._stillTyping = !1, this.focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate items backwards using the <kbd>Shift</kbd> + <kbd>Tab</kbd> keystroke.
        focusPrevious: "shift + tab",
        // Navigate items forwards using the <kbd>Tab</kbd> key.
        focusNext: "tab"
      }
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-input-color"
        ]
      },
      children: [
        this.dropdownView,
        this.inputView
      ]
    }), this.on("change:value", (s, r, o) => this._setInputValue(o));
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), [
      this.inputView,
      this.dropdownView.buttonView
    ].forEach((e) => {
      this.focusTracker.add(e.element), this._focusables.add(e);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * Focuses the view.
  */
  focus(e) {
    e === -1 ? this.focusCycler.focusLast() : this.focusCycler.focusFirst();
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Creates and configures the {@link #dropdownView}.
  */
  _createDropdownView() {
    const e = this.locale, i = e.t, s = this.bindTemplate, r = this._createColorSelector(e), o = Tt(e), a = new ee();
    return a.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck",
          "ck-input-color__button__preview"
        ],
        style: {
          backgroundColor: s.to("value")
        }
      },
      children: [
        {
          tag: "span",
          attributes: {
            class: [
              "ck",
              "ck-input-color__button__preview__no-color-indicator",
              s.if("value", "ck-hidden", (l) => l != "")
            ]
          }
        }
      ]
    }), o.buttonView.extendTemplate({
      attributes: {
        class: "ck-input-color__button"
      }
    }), o.buttonView.children.add(a), o.buttonView.label = i("Color picker"), o.buttonView.tooltip = !0, o.panelPosition = e.uiLanguageDirection === "rtl" ? "se" : "sw", o.panelView.children.add(r), o.bind("isEnabled").to(this, "isReadOnly", (l) => !l), o.on("change:isOpen", (l, c, u) => {
      u && (r.updateSelectedColors(), r.showColorGridsFragment());
    }), o;
  }
  /**
  * Creates and configures an instance of {@link module:ui/inputtext/inputtextview~InputTextView}.
  *
  * @returns A configured instance to be set as {@link #inputView}.
  */
  _createInputTextView() {
    const e = this.locale, i = new By(e);
    return i.extendTemplate({
      on: {
        blur: i.bindTemplate.to("blur")
      }
    }), i.value = this.value, i.bind("isReadOnly", "hasError").to(this), this.bind("isFocused", "isEmpty").to(i), i.on("input", () => {
      const s = i.element.value, r = this.options.colorDefinitions.find((o) => s === o.label);
      this._stillTyping = !0, this.value = r && r.color || s;
    }), i.on("blur", () => {
      this._stillTyping = !1, this._setInputValue(i.element.value);
    }), i.delegate("input").to(this), i;
  }
  /**
  * Creates and configures the panel with "color grid" and "color picker" inside the {@link #dropdownView}.
  */
  _createColorSelector(e) {
    const i = e.t, s = this.options.defaultColorValue || "", r = i(s ? "Restore default" : "Remove color"), o = new Uf(e, {
      colors: this.options.colorDefinitions,
      columns: this.options.columns,
      removeButtonLabel: r,
      colorPickerLabel: i("Color picker"),
      colorPickerViewConfig: this.options.colorPickerConfig === !1 ? !1 : {
        ...this.options.colorPickerConfig,
        hideInput: !0
      }
    });
    o.appendUI(), o.on("execute", (l, c) => {
      if (c.source === "colorPickerSaveButton") {
        this.dropdownView.isOpen = !1;
        return;
      }
      this.value = c.value || s, this.fire("input"), c.source !== "colorPicker" && (this.dropdownView.isOpen = !1);
    });
    let a = this.value;
    return o.on("colorPicker:cancel", () => {
      this.value = a, this.fire("input"), this.dropdownView.isOpen = !1;
    }), o.colorGridsFragmentView.colorPickerButtonView.on("execute", () => {
      a = this.value;
    }), o.bind("selectedColor").to(this, "value"), o;
  }
  /**
  * Sets {@link #inputView}'s value property to the color value or color label,
  * if there is one and the user is not typing.
  *
  * Handles cases like:
  *
  * * Someone picks the color in the grid.
  * * The color is set from the plugin level.
  *
  * @param inputValue Color value to be set.
  */
  _setInputValue(e) {
    if (!this._stillTyping) {
      const i = $w(e), s = this.options.colorDefinitions.find((r) => i === $w(r.color));
      s ? this.inputView.value = s.label : this.inputView.value = e || "";
    }
  }
}
function $w(n) {
  return n.replace(/([(,])\s+/g, "$1").replace(/^\s+|\s+(?=[),\s]|$)/g, "").replace(/,|\s/g, " ");
}
const _m = (n) => n === "";
function ym(n) {
  return {
    none: n("None"),
    solid: n("Solid"),
    dotted: n("Dotted"),
    dashed: n("Dashed"),
    double: n("Double"),
    groove: n("Groove"),
    ridge: n("Ridge"),
    inset: n("Inset"),
    outset: n("Outset")
  };
}
function r2(n) {
  return n('The color is invalid. Try "#FF0000" or "rgb(255,0,0)" or "red".');
}
function o2(n) {
  return n('The value is invalid. Try "10px" or "2em" or simply "2".');
}
function wc(n) {
  return n = n.trim().toLowerCase(), _m(n) || ay(n);
}
function Zo(n) {
  return n = n.trim(), _m(n) || c2(n) || Jc(n) || ly(n);
}
function a2(n) {
  return n = n.trim(), _m(n) || c2(n) || Jc(n);
}
function l2(n, t) {
  const e = new bt(), i = ym(n.t);
  for (const s in i) {
    const r = {
      type: "button",
      model: new tn({
        _borderStyleValue: s,
        label: i[s],
        role: "menuitemradio",
        withText: !0
      })
    };
    s === "none" ? r.model.bind("isOn").to(n, "borderStyle", (o) => t === "none" ? !o : o === s) : r.model.bind("isOn").to(n, "borderStyle", (o) => o === s), e.add(r);
  }
  return e;
}
function Uh(n) {
  const { view: t, icons: e, toolbar: i, labels: s, propertyName: r, nameToValue: o, defaultValue: a } = n;
  for (const l in s) {
    const c = new le(t.locale);
    c.set({
      role: "radio",
      isToggleable: !0,
      label: s[l],
      icon: e[l],
      tooltip: s[l]
    });
    const u = o ? o(l) : l;
    c.bind("isOn").to(t, r, (d) => {
      let h = d;
      return d === "" && a && (h = a), u === h;
    }), c.on("execute", () => {
      t[r] = u;
    }), i.items.add(c);
  }
}
const vc = [
  {
    color: "hsl(0, 0%, 0%)",
    label: "Black"
  },
  {
    color: "hsl(0, 0%, 30%)",
    label: "Dim grey"
  },
  {
    color: "hsl(0, 0%, 60%)",
    label: "Grey"
  },
  {
    color: "hsl(0, 0%, 90%)",
    label: "Light grey"
  },
  {
    color: "hsl(0, 0%, 100%)",
    label: "White",
    hasBorder: !0
  },
  {
    color: "hsl(0, 75%, 60%)",
    label: "Red"
  },
  {
    color: "hsl(30, 75%, 60%)",
    label: "Orange"
  },
  {
    color: "hsl(60, 75%, 60%)",
    label: "Yellow"
  },
  {
    color: "hsl(90, 75%, 60%)",
    label: "Light green"
  },
  {
    color: "hsl(120, 75%, 60%)",
    label: "Green"
  },
  {
    color: "hsl(150, 75%, 60%)",
    label: "Aquamarine"
  },
  {
    color: "hsl(180, 75%, 60%)",
    label: "Turquoise"
  },
  {
    color: "hsl(210, 75%, 60%)",
    label: "Light blue"
  },
  {
    color: "hsl(240, 75%, 60%)",
    label: "Blue"
  },
  {
    color: "hsl(270, 75%, 60%)",
    label: "Purple"
  }
];
function _c(n) {
  return (t, e, i) => {
    const s = new aK(t.locale, {
      colorDefinitions: lK(n.colorConfig),
      columns: n.columns,
      defaultColorValue: n.defaultColorValue,
      colorPickerConfig: n.colorPickerConfig
    });
    return s.inputView.set({
      id: e,
      ariaDescribedById: i
    }), s.bind("isReadOnly").to(t, "isEnabled", (r) => !r), s.bind("hasError").to(t, "errorText", (r) => !!r), s.on("input", () => {
      t.errorText = null;
    }), t.bind("isEmpty", "isFocused").to(s), s;
  };
}
function c2(n) {
  const t = parseFloat(n);
  return !Number.isNaN(t) && n === String(t);
}
function lK(n) {
  return n.map((t) => ({
    color: t.model,
    label: t.label,
    options: {
      hasBorder: t.hasBorder
    }
  }));
}
class xi extends ee {
  /**
  * Creates an instance of the form row class.
  *
  * @param locale The locale instance.
  * @param options.labelView When passed, the row gets the `group` and `aria-labelledby`
  * DOM attributes and gets described by the label.
  */
  constructor(e, i = {}) {
    super(e);
    /**
    * A collection of row items (buttons, dropdowns, etc.).
    */
    f(this, "children");
    const s = this.bindTemplate;
    this.set("class", i.class || null), this.children = this.createCollection(), i.children && i.children.forEach((r) => this.children.add(r)), this.set("_role", null), this.set("_ariaLabelledBy", null), i.labelView && this.set({
      _role: "group",
      _ariaLabelledBy: i.labelView.id
    }), this.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-form__row",
          s.to("class")
        ],
        role: s.to("_role"),
        "aria-labelledby": s.to("_ariaLabelledBy")
      },
      children: this.children
    });
  }
}
class cK extends ee {
  /**
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param options Additional configuration of the view.
  * @param options.borderColors A configuration of the border color palette used by the
  * {@link module:table/tablecellproperties/ui/tablecellpropertiesview~TableCellPropertiesView#borderColorInput}.
  * @param options.backgroundColors A configuration of the background color palette used by the
  * {@link module:table/tablecellproperties/ui/tablecellpropertiesview~TableCellPropertiesView#backgroundInput}.
  * @param options.defaultTableCellProperties The default table cell properties.
  */
  constructor(e, i) {
    super(e);
    /**
    * Options passed to the view. See {@link #constructor} to learn more.
    */
    f(this, "options");
    /**
    * Tracks information about the DOM focus in the form.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * A collection of child views in the form.
    */
    f(this, "children");
    /**
    * A dropdown that allows selecting the style of the table cell border.
    */
    f(this, "borderStyleDropdown");
    /**
    * An input that allows specifying the width of the table cell border.
    */
    f(this, "borderWidthInput");
    /**
    * An input that allows specifying the color of the table cell border.
    */
    f(this, "borderColorInput");
    /**
    * An input that allows specifying the table cell background color.
    */
    f(this, "backgroundInput");
    /**
    * An input that allows specifying the table cell padding.
    */
    f(this, "paddingInput");
    /**
    * An input that allows specifying the table cell width.
    */
    f(this, "widthInput");
    /**
    * An input that allows specifying the table cell height.
    */
    f(this, "heightInput");
    /**
    * A toolbar with buttons that allow changing the horizontal text alignment in a table cell.
    */
    f(this, "horizontalAlignmentToolbar");
    /**
    * A toolbar with buttons that allow changing the vertical text alignment in a table cell.
    */
    f(this, "verticalAlignmentToolbar");
    /**
    * The "Save" button view.
    */
    f(this, "saveButtonView");
    /**
    * The "Cancel" button view.
    */
    f(this, "cancelButtonView");
    /**
    * A collection of views that can be focused in the form.
    */
    f(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    f(this, "_focusCycler");
    this.set({
      borderStyle: "",
      borderWidth: "",
      borderColor: "",
      padding: "",
      backgroundColor: "",
      width: "",
      height: "",
      horizontalAlignment: "",
      verticalAlignment: ""
    }), this.options = i;
    const { borderStyleDropdown: s, borderWidthInput: r, borderColorInput: o, borderRowLabel: a } = this._createBorderFields(), { backgroundRowLabel: l, backgroundInput: c } = this._createBackgroundFields(), { widthInput: u, operatorLabel: d, heightInput: h, dimensionsLabel: m } = this._createDimensionFields(), { horizontalAlignmentToolbar: g, verticalAlignmentToolbar: p, alignmentLabel: w } = this._createAlignmentFields();
    this.focusTracker = new ze(), this.keystrokes = new Ze(), this.children = this.createCollection(), this.borderStyleDropdown = s, this.borderWidthInput = r, this.borderColorInput = o, this.backgroundInput = c, this.paddingInput = this._createPaddingField(), this.widthInput = u, this.heightInput = h, this.horizontalAlignmentToolbar = g, this.verticalAlignmentToolbar = p;
    const { saveButtonView: y, cancelButtonView: k } = this._createActionButtons();
    this.saveButtonView = y, this.cancelButtonView = k, this._focusables = new Et(), this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.children.add(new Va(e, {
      label: this.t("Cell properties")
    })), this.children.add(new xi(e, {
      labelView: a,
      children: [
        a,
        s,
        o,
        r
      ],
      class: "ck-table-form__border-row"
    })), this.children.add(new xi(e, {
      labelView: l,
      children: [
        l,
        c
      ],
      class: "ck-table-form__background-row"
    })), this.children.add(new xi(e, {
      children: [
        // Dimensions row.
        new xi(e, {
          labelView: m,
          children: [
            m,
            u,
            d,
            h
          ],
          class: "ck-table-form__dimensions-row"
        }),
        // Padding row.
        new xi(e, {
          children: [
            this.paddingInput
          ],
          class: "ck-table-cell-properties-form__padding-row"
        })
      ]
    })), this.children.add(new xi(e, {
      labelView: w,
      children: [
        w,
        g,
        p
      ],
      class: "ck-table-cell-properties-form__alignment-row"
    })), this.children.add(new xi(e, {
      children: [
        this.saveButtonView,
        this.cancelButtonView
      ],
      class: "ck-table-form__action-row"
    })), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-form",
          "ck-table-form",
          "ck-table-cell-properties-form"
        ],
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Tn({
      view: this
    }), [
      this.borderColorInput,
      this.backgroundInput
    ].forEach((e) => {
      this._focusCycler.chain(e.fieldView.focusCycler);
    }), [
      this.borderStyleDropdown,
      this.borderColorInput,
      this.borderWidthInput,
      this.backgroundInput,
      this.widthInput,
      this.heightInput,
      this.paddingInput,
      this.horizontalAlignmentToolbar,
      this.verticalAlignmentToolbar,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((e) => {
      this._focusables.add(e), this.focusTracker.add(e.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist focusable field in the form.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #borderStyleDropdown},
  * * {@link #borderWidthInput},
  * * {@link #borderColorInput}.
  */
  _createBorderFields() {
    const e = this.options.defaultTableCellProperties, i = {
      style: e.borderStyle,
      width: e.borderWidth,
      color: e.borderColor
    }, s = _c({
      colorConfig: this.options.borderColors,
      columns: 5,
      defaultColorValue: i.color,
      colorPickerConfig: this.options.colorPickerConfig
    }), r = this.locale, o = this.t, a = o("Style"), l = new gi(r);
    l.text = o("Border");
    const c = ym(o), u = new gt(r, jf);
    u.set({
      label: a,
      class: "ck-table-form__border-style"
    }), u.fieldView.buttonView.set({
      ariaLabel: a,
      ariaLabelledBy: void 0,
      isOn: !1,
      withText: !0,
      tooltip: a
    }), u.fieldView.buttonView.bind("label").to(this, "borderStyle", (m) => c[m || "none"]), u.fieldView.on("execute", (m) => {
      this.borderStyle = m.source._borderStyleValue;
    }), u.bind("isEmpty").to(this, "borderStyle", (m) => !m), En(u.fieldView, l2(this, i.style), {
      role: "menu",
      ariaLabel: a
    });
    const d = new gt(r, pi);
    d.set({
      label: o("Width"),
      class: "ck-table-form__border-width"
    }), d.fieldView.bind("value").to(this, "borderWidth"), d.bind("isEnabled").to(this, "borderStyle", Tl), d.fieldView.on("input", () => {
      this.borderWidth = d.fieldView.element.value;
    });
    const h = new gt(r, s);
    return h.set({
      label: o("Color"),
      class: "ck-table-form__border-color"
    }), h.fieldView.bind("value").to(this, "borderColor"), h.bind("isEnabled").to(this, "borderStyle", Tl), h.fieldView.on("input", () => {
      this.borderColor = h.fieldView.value;
    }), this.on("change:borderStyle", (m, g, p, w) => {
      Tl(p) || (this.borderColor = "", this.borderWidth = ""), Tl(w) || (this.borderColor = i.color, this.borderWidth = i.width);
    }), {
      borderRowLabel: l,
      borderStyleDropdown: u,
      borderColorInput: h,
      borderWidthInput: d
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #backgroundInput}.
  */
  _createBackgroundFields() {
    const e = this.locale, i = this.t, s = new gi(e);
    s.text = i("Background");
    const r = _c({
      colorConfig: this.options.backgroundColors,
      columns: 5,
      defaultColorValue: this.options.defaultTableCellProperties.backgroundColor,
      colorPickerConfig: this.options.colorPickerConfig
    }), o = new gt(e, r);
    return o.set({
      label: i("Color"),
      class: "ck-table-cell-properties-form__background"
    }), o.fieldView.bind("value").to(this, "backgroundColor"), o.fieldView.on("input", () => {
      this.backgroundColor = o.fieldView.value;
    }), {
      backgroundRowLabel: s,
      backgroundInput: o
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #widthInput}.
  * * {@link #heightInput}.
  */
  _createDimensionFields() {
    const e = this.locale, i = this.t, s = new gi(e);
    s.text = i("Dimensions");
    const r = new gt(e, pi);
    r.set({
      label: i("Width"),
      class: "ck-table-form__dimensions-row__width"
    }), r.fieldView.bind("value").to(this, "width"), r.fieldView.on("input", () => {
      this.width = r.fieldView.element.value;
    });
    const o = new ee(e);
    o.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck-table-form__dimension-operator"
        ]
      },
      children: [
        {
          text: "×"
        }
      ]
    });
    const a = new gt(e, pi);
    return a.set({
      label: i("Height"),
      class: "ck-table-form__dimensions-row__height"
    }), a.fieldView.bind("value").to(this, "height"), a.fieldView.on("input", () => {
      this.height = a.fieldView.element.value;
    }), {
      dimensionsLabel: s,
      widthInput: r,
      operatorLabel: o,
      heightInput: a
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #paddingInput}.
  */
  _createPaddingField() {
    const e = this.locale, i = this.t, s = new gt(e, pi);
    return s.set({
      label: i("Padding"),
      class: "ck-table-cell-properties-form__padding"
    }), s.fieldView.bind("value").to(this, "padding"), s.fieldView.on("input", () => {
      this.padding = s.fieldView.element.value;
    }), s;
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #horizontalAlignmentToolbar},
  * * {@link #verticalAlignmentToolbar}.
  */
  _createAlignmentFields() {
    const e = this.locale, i = this.t, s = new gi(e), r = {
      left: Z.alignLeft,
      center: Z.alignCenter,
      right: Z.alignRight,
      justify: Z.alignJustify,
      top: Z.alignTop,
      middle: Z.alignMiddle,
      bottom: Z.alignBottom
    };
    s.text = i("Table cell text alignment");
    const o = new Us(e), a = e.contentLanguageDirection === "rtl";
    o.set({
      isCompact: !0,
      role: "radiogroup",
      ariaLabel: i("Horizontal text alignment toolbar")
    }), Uh({
      view: this,
      icons: r,
      toolbar: o,
      labels: this._horizontalAlignmentLabels,
      propertyName: "horizontalAlignment",
      nameToValue: (c) => {
        if (a) {
          if (c === "left")
            return "right";
          if (c === "right")
            return "left";
        }
        return c;
      },
      defaultValue: this.options.defaultTableCellProperties.horizontalAlignment
    });
    const l = new Us(e);
    return l.set({
      isCompact: !0,
      role: "radiogroup",
      ariaLabel: i("Vertical text alignment toolbar")
    }), Uh({
      view: this,
      icons: r,
      toolbar: l,
      labels: this._verticalAlignmentLabels,
      propertyName: "verticalAlignment",
      defaultValue: this.options.defaultTableCellProperties.verticalAlignment
    }), {
      horizontalAlignmentToolbar: o,
      verticalAlignmentToolbar: l,
      alignmentLabel: s
    };
  }
  /**
  * Creates the following form controls:
  *
  * * {@link #saveButtonView},
  * * {@link #cancelButtonView}.
  */
  _createActionButtons() {
    const e = this.locale, i = this.t, s = new le(e), r = new le(e), o = [
      this.borderWidthInput,
      this.borderColorInput,
      this.backgroundInput,
      this.paddingInput
    ];
    return s.set({
      label: i("Save"),
      icon: Z.check,
      class: "ck-button-save",
      type: "submit",
      withText: !0
    }), s.bind("isEnabled").toMany(o, "errorText", (...a) => a.every((l) => !l)), r.set({
      label: i("Cancel"),
      icon: Z.cancel,
      class: "ck-button-cancel",
      withText: !0
    }), r.delegate("execute").to(this, "cancel"), {
      saveButtonView: s,
      cancelButtonView: r
    };
  }
  /**
  * Provides localized labels for {@link #horizontalAlignmentToolbar} buttons.
  */
  get _horizontalAlignmentLabels() {
    const e = this.locale, i = this.t, s = i("Align cell text to the left"), r = i("Align cell text to the center"), o = i("Align cell text to the right"), a = i("Justify cell text");
    return e.uiLanguageDirection === "rtl" ? {
      right: o,
      center: r,
      left: s,
      justify: a
    } : {
      left: s,
      center: r,
      right: o,
      justify: a
    };
  }
  /**
  * Provides localized labels for {@link #verticalAlignmentToolbar} buttons.
  */
  get _verticalAlignmentLabels() {
    const e = this.t;
    return {
      top: e("Align cell text to the top"),
      middle: e("Align cell text to the middle"),
      bottom: e("Align cell text to the bottom")
    };
  }
}
function Tl(n) {
  return n !== "none";
}
const Wh = [
  Dt.defaultPositions.northArrowSouth,
  Dt.defaultPositions.northArrowSouthWest,
  Dt.defaultPositions.northArrowSouthEast,
  Dt.defaultPositions.southArrowNorth,
  Dt.defaultPositions.southArrowNorthWest,
  Dt.defaultPositions.southArrowNorthEast,
  Dt.defaultPositions.viewportStickyNorth
];
function u2(n, t) {
  const e = n.plugins.get("ContextualBalloon"), i = n.editing.view.document.selection;
  let s;
  t === "cell" ? ou(i) && (s = h2(n)) : i2(i) && (s = d2(n)), s && e.updatePosition(s);
}
function d2(n) {
  const t = n.model.document.selection, e = Bs(t), i = n.editing.mapper.toViewElement(e);
  return {
    target: n.editing.view.domConverter.mapViewToDom(i),
    positions: Wh
  };
}
function h2(n) {
  const t = n.editing.mapper, e = n.editing.view.domConverter, i = n.model.document.selection;
  if (i.rangeCount > 1)
    return {
      target: () => uK(i.getRanges(), n),
      positions: Wh
    };
  const s = f2(i.getFirstPosition()), r = t.toViewElement(s);
  return {
    target: e.mapViewToDom(r),
    positions: Wh
  };
}
function f2(n) {
  return n.nodeAfter && n.nodeAfter.is("element", "tableCell") ? n.nodeAfter : n.findAncestor("tableCell");
}
function uK(n, t) {
  const e = t.editing.mapper, i = t.editing.view.domConverter, s = Array.from(n).map((r) => {
    const o = f2(r.start), a = e.toViewElement(o);
    return new ke(i.mapViewToDom(a));
  });
  return ke.getBoundingRect(s);
}
var dK = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="m11.105 18-.17 1H2.5A1.5 1.5 0 0 1 1 17.5v-15A1.5 1.5 0 0 1 2.5 1h15A1.5 1.5 0 0 1 19 2.5v9.975l-.85-.124-.15-.302V8h-5v4h.021l-.172.351-1.916.28-.151.027c-.287.063-.54.182-.755.341L8 13v5h3.105zM2 12h5V8H2v4zm10-4H8v4h4V8zM2 2v5h5V2H2zm0 16h5v-5H2v5zM13 7h5V2h-5v5zM8 2v5h4V2H8z" opacity=".6"/><path d="m15.5 11.5 1.323 2.68 2.957.43-2.14 2.085.505 2.946L15.5 18.25l-2.645 1.39.505-2.945-2.14-2.086 2.957-.43L15.5 11.5zM13 6a1 1 0 0 1 1 1v3.172a2.047 2.047 0 0 0-.293.443l-.858 1.736-1.916.28-.151.027A1.976 1.976 0 0 0 9.315 14H7a1 1 0 0 1-1-1V7a1 1 0 0 1 1-1h6zm-1 2H8v4h4V8z"/></svg>';
function tr(n) {
  if (!n || !nt(n))
    return n;
  const { top: t, right: e, bottom: i, left: s } = n;
  if (t == e && e == i && i == s)
    return t;
}
function ir(n, t) {
  const e = parseFloat(n);
  return Number.isNaN(e) || String(e) !== String(n) ? n : `${e}${t}`;
}
function m2(n, t = {}) {
  const e = {
    borderStyle: "none",
    borderWidth: "",
    borderColor: "",
    backgroundColor: "",
    width: "",
    height: "",
    ...n
  };
  return t.includeAlignmentProperty && !e.alignment && (e.alignment = "center"), t.includePaddingProperty && !e.padding && (e.padding = ""), t.includeVerticalAlignmentProperty && !e.verticalAlignment && (e.verticalAlignment = "middle"), t.includeHorizontalAlignmentProperty && !e.horizontalAlignment && (e.horizontalAlignment = t.isRightToLeftContent ? "right" : "left"), e;
}
function g2(n, t) {
  return m2({
    // It adds support for border none in the table element, keep it in sync with the content styles
    // See more: https://github.com/ckeditor/ckeditor5/issues/6841#issuecomment-1959195608
    borderStyle: "double",
    borderColor: "hsl(0, 0%, 70%)",
    borderWidth: "1px",
    ...n
  }, t);
}
function km(n, t) {
  return m2({
    // It adds support for border none in the table element, keep it in sync with the content styles
    // See more: https://github.com/ckeditor/ckeditor5/issues/6841#issuecomment-1959195608
    borderStyle: "solid",
    borderColor: "hsl(0, 0%, 75%)",
    borderWidth: "1px",
    ...n
  }, t);
}
const hK = 500, zw = {
  borderStyle: "tableCellBorderStyle",
  borderColor: "tableCellBorderColor",
  borderWidth: "tableCellBorderWidth",
  height: "tableCellHeight",
  width: "tableCellWidth",
  padding: "tableCellPadding",
  backgroundColor: "tableCellBackgroundColor",
  horizontalAlignment: "tableCellHorizontalAlignment",
  verticalAlignment: "tableCellVerticalAlignment"
};
class fK extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The default table cell properties.
    */
    f(this, "_defaultTableCellProperties");
    /**
    * The contextual balloon plugin instance.
    */
    f(this, "_balloon");
    /**
    * The cell properties form view displayed inside the balloon.
    */
    f(this, "view");
    /**
    * The batch used to undo all changes made by the form (which are live, as the user types)
    * when "Cancel" was pressed. Each time the view is shown, a new batch is created.
    */
    f(this, "_undoStepBatch");
    /**
    * Flag used to indicate whether view is ready to execute update commands
    * (it finished loading initial data).
    */
    f(this, "_isReady");
    e.config.define("table.tableCellProperties", {
      borderColors: vc,
      backgroundColors: vc
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ai
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCellPropertiesUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.t;
    this._defaultTableCellProperties = km(e.config.get("table.tableCellProperties.defaultProperties"), {
      includeVerticalAlignmentProperty: !0,
      includeHorizontalAlignmentProperty: !0,
      includePaddingProperty: !0,
      isRightToLeftContent: e.locale.contentLanguageDirection === "rtl"
    }), this._balloon = e.plugins.get(ai), this.view = null, this._isReady = !1, e.ui.componentFactory.add("tableCellProperties", (s) => {
      const r = new le(s);
      r.set({
        label: i("Cell properties"),
        icon: dK,
        tooltip: !0
      }), this.listenTo(r, "execute", () => this._showView());
      const o = Object.values(zw).map((a) => e.commands.get(a));
      return r.bind("isEnabled").toMany(o, "isEnabled", (...a) => a.some((l) => l)), r;
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.view && this.view.destroy();
  }
  /**
  * Creates the {@link module:table/tablecellproperties/ui/tablecellpropertiesview~TableCellPropertiesView} instance.
  *
  * @returns The cell properties form view instance.
  */
  _createPropertiesView() {
    const e = this.editor, i = e.config.get("table.tableCellProperties"), s = ma(i.borderColors), r = fa(e.locale, s), o = ma(i.backgroundColors), a = fa(e.locale, o), l = i.colorPicker !== !1, c = new cK(e.locale, {
      borderColors: r,
      backgroundColors: a,
      defaultTableCellProperties: this._defaultTableCellProperties,
      colorPickerConfig: l ? i.colorPicker || {} : !1
    }), u = e.t;
    c.render(), this.listenTo(c, "submit", () => {
      this._hideView();
    }), this.listenTo(c, "cancel", () => {
      this._undoStepBatch.operations.length && e.execute("undo", this._undoStepBatch), this._hideView();
    }), c.keystrokes.set("Esc", (m, g) => {
      this._hideView(), g();
    }), Xn({
      emitter: c,
      activator: () => this._isViewInBalloon,
      contextElements: [
        this._balloon.view.element
      ],
      callback: () => this._hideView()
    });
    const d = r2(u), h = o2(u);
    return c.on("change:borderStyle", this._getPropertyChangeCallback("tableCellBorderStyle")), c.on("change:borderColor", this._getValidatedPropertyChangeCallback({
      viewField: c.borderColorInput,
      commandName: "tableCellBorderColor",
      errorText: d,
      validator: wc
    })), c.on("change:borderWidth", this._getValidatedPropertyChangeCallback({
      viewField: c.borderWidthInput,
      commandName: "tableCellBorderWidth",
      errorText: h,
      validator: a2
    })), c.on("change:padding", this._getValidatedPropertyChangeCallback({
      viewField: c.paddingInput,
      commandName: "tableCellPadding",
      errorText: h,
      validator: Zo
    })), c.on("change:width", this._getValidatedPropertyChangeCallback({
      viewField: c.widthInput,
      commandName: "tableCellWidth",
      errorText: h,
      validator: Zo
    })), c.on("change:height", this._getValidatedPropertyChangeCallback({
      viewField: c.heightInput,
      commandName: "tableCellHeight",
      errorText: h,
      validator: Zo
    })), c.on("change:backgroundColor", this._getValidatedPropertyChangeCallback({
      viewField: c.backgroundInput,
      commandName: "tableCellBackgroundColor",
      errorText: d,
      validator: wc
    })), c.on("change:horizontalAlignment", this._getPropertyChangeCallback("tableCellHorizontalAlignment")), c.on("change:verticalAlignment", this._getPropertyChangeCallback("tableCellVerticalAlignment")), c;
  }
  /**
  * In this method the "editor data -> UI" binding is happening.
  *
  * When executed, this method obtains selected cell property values from various table commands
  * and passes them to the {@link #view}.
  *
  * This way, the UI stays up–to–date with the editor data.
  */
  _fillViewFormFromCommandValues() {
    const e = this.editor.commands, i = e.get("tableCellBorderStyle");
    Object.entries(zw).map(([s, r]) => {
      const o = this._defaultTableCellProperties[s] || "";
      return [
        s,
        e.get(r).value || o
      ];
    }).forEach(([s, r]) => {
      (s === "borderColor" || s === "borderWidth") && i.value === "none" || this.view.set(s, r);
    }), this._isReady = !0;
  }
  /**
  * Shows the {@link #view} in the {@link #_balloon}.
  *
  * **Note**: Each time a view is shown, a new {@link #_undoStepBatch} is created. It contains
  * all changes made to the document when the view is visible, allowing a single undo step
  * for all of them.
  */
  _showView() {
    const e = this.editor;
    this.view || (this.view = this._createPropertiesView()), this.listenTo(e.ui, "update", () => {
      this._updateView();
    }), this._fillViewFormFromCommandValues(), this._balloon.add({
      view: this.view,
      position: h2(e)
    }), this._undoStepBatch = e.model.createBatch(), this.view.focus();
  }
  /**
  * Removes the {@link #view} from the {@link #_balloon}.
  */
  _hideView() {
    const e = this.editor;
    this.stopListening(e.ui, "update"), this._isReady = !1, this.view.saveButtonView.focus(), this._balloon.remove(this.view), this.editor.editing.view.focus();
  }
  /**
  * Repositions the {@link #_balloon} or hides the {@link #view} if a table cell is no longer selected.
  */
  _updateView() {
    const e = this.editor, i = e.editing.view.document;
    ou(i.selection) ? this._isViewVisible && u2(e, "cell") : this._hideView();
  }
  /**
  * Returns `true` when the {@link #view} is visible in the {@link #_balloon}.
  */
  get _isViewVisible() {
    return !!this.view && this._balloon.visibleView === this.view;
  }
  /**
  * Returns `true` when the {@link #view} is in the {@link #_balloon}.
  */
  get _isViewInBalloon() {
    return !!this.view && this._balloon.hasView(this.view);
  }
  /**
  * Creates a callback that when executed upon the {@link #view view's} property change
  * executes a related editor command with the new property value.
  *
  * @param defaultValue The default value of the command.
  */
  _getPropertyChangeCallback(e) {
    return (i, s, r) => {
      this._isReady && this.editor.execute(e, {
        value: r,
        batch: this._undoStepBatch
      });
    };
  }
  /**
  * Creates a callback that when executed upon the {@link #view view's} property change:
  * * Executes a related editor command with the new property value if the value is valid,
  * * Or sets the error text next to the invalid field, if the value did not pass the validation.
  */
  _getValidatedPropertyChangeCallback(e) {
    const { commandName: i, viewField: s, validator: r, errorText: o } = e, a = vi(() => {
      s.errorText = o;
    }, hK);
    return (l, c, u) => {
      a.cancel(), this._isReady && (r(u) ? (this.editor.execute(i, {
        value: u,
        batch: this._undoStepBatch
      }), s.errorText = null) : a());
    };
  }
}
class es extends he {
  /**
  * Creates a new `TableCellPropertyCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param attributeName Table cell attribute name.
  * @param defaultValue The default value of the attribute.
  */
  constructor(e, i, s) {
    super(e);
    /**
    * The attribute that will be set by the command.
    */
    f(this, "attributeName");
    /**
    * The default value for the attribute.
    */
    f(this, "_defaultValue");
    this.attributeName = i, this._defaultValue = s;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const e = this.editor, s = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(e.model.document.selection);
    this.isEnabled = !!s.length, this.value = this._getSingleValue(s);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.value If set, the command will set the attribute on selected table cells.
  * If it is not set, the command will remove the attribute from the selected table cells.
  * @param options.batch Pass the model batch instance to the command to aggregate changes,
  * for example to allow a single undo step for multiple executions.
  */
  execute(e = {}) {
    const { value: i, batch: s } = e, r = this.editor.model, a = this.editor.plugins.get("TableUtils").getSelectionAffectedTableCells(r.document.selection), l = this._getValueToSet(i);
    r.enqueueChange(s, (c) => {
      l ? a.forEach((u) => c.setAttribute(this.attributeName, l, u)) : a.forEach((u) => c.removeAttribute(this.attributeName, u));
    });
  }
  /**
  * Returns the attribute value for a table cell.
  */
  _getAttribute(e) {
    if (!e)
      return;
    const i = e.getAttribute(this.attributeName);
    if (i !== this._defaultValue)
      return i;
  }
  /**
  * Returns the proper model value. It can be used to add a default unit to numeric values.
  */
  _getValueToSet(e) {
    if (e !== this._defaultValue)
      return e;
  }
  /**
  * Returns a single value for all selected table cells. If the value is the same for all cells,
  * it will be returned (`undefined` otherwise).
  */
  _getSingleValue(e) {
    const i = this._getAttribute(e[0]);
    return e.every((r) => this._getAttribute(r) === i) ? i : void 0;
  }
}
class mK extends es {
  /**
  * Creates a new `TableCellWidthCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellWidth", e);
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    if (t = ir(t, "px"), t !== this._defaultValue)
      return t;
  }
}
class p2 extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCellWidthEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Wa
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = km(t.config.get("table.tableCellProperties.defaultProperties"));
    zl(t.model.schema, t.conversion, {
      modelAttribute: "tableCellWidth",
      styleName: "width",
      defaultValue: e.width
    }), t.commands.add("tableCellWidth", new mK(t, e.width));
  }
}
class gK extends es {
  /**
  * Creates a new `TableCellPaddingCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellPadding", e);
  }
  /**
  * @inheritDoc
  */
  _getAttribute(t) {
    if (!t)
      return;
    const e = tr(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    const e = ir(t, "px");
    if (e !== this._defaultValue)
      return e;
  }
}
class pK extends es {
  /**
  * Creates a new `TableCellHeightCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellHeight", e);
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    const e = ir(t, "px");
    if (e !== this._defaultValue)
      return e;
  }
}
class bK extends es {
  /**
  * Creates a new `TableCellBackgroundColorCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellBackgroundColor", e);
  }
}
class wK extends es {
  /**
  * Creates a new `TableCellVerticalAlignmentCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value for the "alignment" attribute.
  */
  constructor(t, e) {
    super(t, "tableCellVerticalAlignment", e);
  }
}
class vK extends es {
  /**
  * Creates a new `TableCellHorizontalAlignmentCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value for the "alignment" attribute.
  */
  constructor(t, e) {
    super(t, "tableCellHorizontalAlignment", e);
  }
}
class _K extends es {
  /**
  * Creates a new `TableCellBorderStyleCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellBorderStyle", e);
  }
  /**
  * @inheritDoc
  */
  _getAttribute(t) {
    if (!t)
      return;
    const e = tr(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
}
class yK extends es {
  /**
  * Creates a new `TableCellBorderColorCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellBorderColor", e);
  }
  /**
  * @inheritDoc
  */
  _getAttribute(t) {
    if (!t)
      return;
    const e = tr(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
}
class kK extends es {
  /**
  * Creates a new `TableCellBorderWidthCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableCellBorderWidth", e);
  }
  /**
  * @inheritDoc
  */
  _getAttribute(t) {
    if (!t)
      return;
    const e = tr(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    const e = ir(t, "px");
    if (e !== this._defaultValue)
      return e;
  }
}
const Hw = /^(top|middle|bottom)$/, jw = /^(left|center|right|justify)$/;
class CK extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCellPropertiesEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Wa,
      p2
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model.schema, i = t.conversion;
    t.config.define("table.tableCellProperties.defaultProperties", {});
    const s = km(t.config.get("table.tableCellProperties.defaultProperties"), {
      includeVerticalAlignmentProperty: !0,
      includeHorizontalAlignmentProperty: !0,
      includePaddingProperty: !0,
      isRightToLeftContent: t.locale.contentLanguageDirection === "rtl"
    });
    t.data.addStyleProcessorRules(uy), xK(e, i, {
      color: s.borderColor,
      style: s.borderStyle,
      width: s.borderWidth
    }), t.commands.add("tableCellBorderStyle", new _K(t, s.borderStyle)), t.commands.add("tableCellBorderColor", new yK(t, s.borderColor)), t.commands.add("tableCellBorderWidth", new kK(t, s.borderWidth)), zl(e, i, {
      modelAttribute: "tableCellHeight",
      styleName: "height",
      defaultValue: s.height
    }), t.commands.add("tableCellHeight", new pK(t, s.height)), t.data.addStyleProcessorRules(b8), zl(e, i, {
      modelAttribute: "tableCellPadding",
      styleName: "padding",
      reduceBoxSides: !0,
      defaultValue: s.padding
    }), t.commands.add("tableCellPadding", new gK(t, s.padding)), t.data.addStyleProcessorRules(Mf), zl(e, i, {
      modelAttribute: "tableCellBackgroundColor",
      styleName: "background-color",
      defaultValue: s.backgroundColor
    }), t.commands.add("tableCellBackgroundColor", new bK(t, s.backgroundColor)), TK(e, i, s.horizontalAlignment), t.commands.add("tableCellHorizontalAlignment", new vK(t, s.horizontalAlignment)), EK(e, i, s.verticalAlignment), t.commands.add("tableCellVerticalAlignment", new wK(t, s.verticalAlignment));
  }
}
function xK(n, t, e) {
  const i = {
    width: "tableCellBorderWidth",
    color: "tableCellBorderColor",
    style: "tableCellBorderStyle"
  };
  n.extend("tableCell", {
    allowAttributes: Object.values(i)
  }), Bh(t, "td", i, e), Bh(t, "th", i, e), Ko(t, {
    modelElement: "tableCell",
    modelAttribute: i.style,
    styleName: "border-style"
  }), Ko(t, {
    modelElement: "tableCell",
    modelAttribute: i.color,
    styleName: "border-color"
  }), Ko(t, {
    modelElement: "tableCell",
    modelAttribute: i.width,
    styleName: "border-width"
  });
}
function TK(n, t, e) {
  n.extend("tableCell", {
    allowAttributes: [
      "tableCellHorizontalAlignment"
    ]
  }), t.for("downcast").attributeToAttribute({
    model: {
      name: "tableCell",
      key: "tableCellHorizontalAlignment"
    },
    view: (i) => ({
      key: "style",
      value: {
        "text-align": i
      }
    })
  }), t.for("upcast").attributeToAttribute({
    view: {
      name: /^(td|th)$/,
      styles: {
        "text-align": jw
      }
    },
    model: {
      key: "tableCellHorizontalAlignment",
      value: (i) => {
        const s = i.getStyle("text-align");
        return s === e ? null : s;
      }
    }
  }).attributeToAttribute({
    view: {
      name: /^(td|th)$/,
      attributes: {
        align: jw
      }
    },
    model: {
      key: "tableCellHorizontalAlignment",
      value: (i) => {
        const s = i.getAttribute("align");
        return s === e ? null : s;
      }
    }
  });
}
function EK(n, t, e) {
  n.extend("tableCell", {
    allowAttributes: [
      "tableCellVerticalAlignment"
    ]
  }), t.for("downcast").attributeToAttribute({
    model: {
      name: "tableCell",
      key: "tableCellVerticalAlignment"
    },
    view: (i) => ({
      key: "style",
      value: {
        "vertical-align": i
      }
    })
  }), t.for("upcast").attributeToAttribute({
    view: {
      name: /^(td|th)$/,
      styles: {
        "vertical-align": Hw
      }
    },
    model: {
      key: "tableCellVerticalAlignment",
      value: (i) => {
        const s = i.getStyle("vertical-align");
        return s === e ? null : s;
      }
    }
  }).attributeToAttribute({
    view: {
      name: /^(td|th)$/,
      attributes: {
        valign: Hw
      }
    },
    model: {
      key: "tableCellVerticalAlignment",
      value: (i) => {
        const s = i.getAttribute("valign");
        return s === e ? null : s;
      }
    }
  });
}
class SK extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCellProperties";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      CK,
      fK
    ];
  }
}
class nr extends he {
  /**
  * Creates a new `TablePropertyCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param attributeName Table cell attribute name.
  * @param defaultValue The default value of the attribute.
  */
  constructor(e, i, s) {
    super(e);
    /**
    * The attribute that will be set by the command.
    */
    f(this, "attributeName");
    /**
    * The default value for the attribute.
    */
    f(this, "_defaultValue");
    this.attributeName = i, this._defaultValue = s;
  }
  /**
  * @inheritDoc
  */
  refresh() {
    const i = this.editor.model.document.selection, s = Bs(i);
    this.isEnabled = !!s, this.value = this._getValue(s);
  }
  /**
  * Executes the command.
  *
  * @fires execute
  * @param options.value If set, the command will set the attribute on the selected table.
  * If not set, the command will remove the attribute from the selected table.
  * @param options.batch Pass the model batch instance to the command to aggregate changes,
  * for example, to allow a single undo step for multiple executions.
  */
  execute(e = {}) {
    const i = this.editor.model, s = i.document.selection, { value: r, batch: o } = e, a = Bs(s), l = this._getValueToSet(r);
    i.enqueueChange(o, (c) => {
      l ? c.setAttribute(this.attributeName, l, a) : c.removeAttribute(this.attributeName, a);
    });
  }
  /**
  * Returns the attribute value for a table.
  */
  _getValue(e) {
    if (!e)
      return;
    const i = e.getAttribute(this.attributeName);
    if (i !== this._defaultValue)
      return i;
  }
  /**
  * Returns the proper model value. It can be used to add a default unit to numeric values.
  */
  _getValueToSet(e) {
    if (e !== this._defaultValue)
      return e;
  }
}
class AK extends nr {
  /**
  * Creates a new `TableBackgroundColorCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableBackgroundColor", e);
  }
}
class IK extends nr {
  /**
  * Creates a new `TableBorderColorCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableBorderColor", e);
  }
  /**
  * @inheritDoc
  */
  _getValue(t) {
    if (!t)
      return;
    const e = tr(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
}
class PK extends nr {
  /**
  * Creates a new `TableBorderStyleCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableBorderStyle", e);
  }
  /**
  * @inheritDoc
  */
  _getValue(t) {
    if (!t)
      return;
    const e = tr(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
}
class RK extends nr {
  /**
  * Creates a new `TableBorderWidthCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableBorderWidth", e);
  }
  /**
  * @inheritDoc
  */
  _getValue(t) {
    if (!t)
      return;
    const e = tr(t.getAttribute(this.attributeName));
    if (e !== this._defaultValue)
      return e;
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    const e = ir(t, "px");
    if (e !== this._defaultValue)
      return e;
  }
}
class OK extends nr {
  /**
  * Creates a new `TableWidthCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableWidth", e);
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    if (t = ir(t, "px"), t !== this._defaultValue)
      return t;
  }
}
class VK extends nr {
  /**
  * Creates a new `TableHeightCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value of the attribute.
  */
  constructor(t, e) {
    super(t, "tableHeight", e);
  }
  /**
  * @inheritDoc
  */
  _getValueToSet(t) {
    if (t = ir(t, "px"), t !== this._defaultValue)
      return t;
  }
}
class LK extends nr {
  /**
  * Creates a new `TableAlignmentCommand` instance.
  *
  * @param editor An editor in which this command will be used.
  * @param defaultValue The default value for the "alignment" attribute.
  */
  constructor(t, e) {
    super(t, "tableAlignment", e);
  }
}
const BK = /^(left|center|right)$/, MK = /^(left|none|right)$/;
class NK extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TablePropertiesEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Wa
    ];
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.model.schema, i = t.conversion;
    t.config.define("table.tableProperties.defaultProperties", {});
    const s = g2(t.config.get("table.tableProperties.defaultProperties"), {
      includeAlignmentProperty: !0
    });
    t.data.addStyleProcessorRules(uy), FK(e, i, {
      color: s.borderColor,
      style: s.borderStyle,
      width: s.borderWidth
    }), t.commands.add("tableBorderColor", new IK(t, s.borderColor)), t.commands.add("tableBorderStyle", new PK(t, s.borderStyle)), t.commands.add("tableBorderWidth", new RK(t, s.borderWidth)), DK(e, i, s.alignment), t.commands.add("tableAlignment", new LK(t, s.alignment)), Uw(e, i, {
      modelAttribute: "tableWidth",
      styleName: "width",
      defaultValue: s.width
    }), t.commands.add("tableWidth", new OK(t, s.width)), Uw(e, i, {
      modelAttribute: "tableHeight",
      styleName: "height",
      defaultValue: s.height
    }), t.commands.add("tableHeight", new VK(t, s.height)), t.data.addStyleProcessorRules(Mf), $K(e, i, {
      modelAttribute: "tableBackgroundColor",
      styleName: "background-color",
      defaultValue: s.backgroundColor
    }), t.commands.add("tableBackgroundColor", new AK(t, s.backgroundColor));
  }
}
function FK(n, t, e) {
  const i = {
    width: "tableBorderWidth",
    color: "tableBorderColor",
    style: "tableBorderStyle"
  };
  n.extend("table", {
    allowAttributes: Object.values(i)
  }), Bh(t, "table", i, e), Dl(t, {
    modelAttribute: i.color,
    styleName: "border-color"
  }), Dl(t, {
    modelAttribute: i.style,
    styleName: "border-style"
  }), Dl(t, {
    modelAttribute: i.width,
    styleName: "border-width"
  });
}
function DK(n, t, e) {
  n.extend("table", {
    allowAttributes: [
      "tableAlignment"
    ]
  }), t.for("downcast").attributeToAttribute({
    model: {
      name: "table",
      key: "tableAlignment"
    },
    view: (i) => ({
      key: "style",
      value: {
        // Model: `alignment:center` => CSS: `float:none`.
        float: i === "center" ? "none" : i
      }
    }),
    converterPriority: "high"
  }), t.for("upcast").attributeToAttribute({
    view: {
      name: /^(table|figure)$/,
      styles: {
        float: MK
      }
    },
    model: {
      key: "tableAlignment",
      value: (i) => {
        let s = i.getStyle("float");
        return s === "none" && (s = "center"), s === e ? null : s;
      }
    }
  }).attributeToAttribute({
    view: {
      attributes: {
        align: BK
      }
    },
    model: {
      name: "table",
      key: "tableAlignment",
      value: (i) => {
        const s = i.getAttribute("align");
        return s === e ? null : s;
      }
    }
  });
}
function $K(n, t, e) {
  const { modelAttribute: i } = e;
  n.extend("table", {
    allowAttributes: [
      i
    ]
  }), bm(t, {
    viewElement: "table",
    ...e
  }), Dl(t, e);
}
function Uw(n, t, e) {
  const { modelAttribute: i } = e;
  n.extend("table", {
    allowAttributes: [
      i
    ]
  }), bm(t, {
    viewElement: /^(table|figure)$/,
    shouldUpcast: (s) => !(s.name == "table" && s.parent.name == "figure"),
    ...e
  }), Ko(t, {
    modelElement: "table",
    ...e
  });
}
class zK extends ee {
  /**
  * @param locale The {@link module:core/editor/editor~Editor#locale} instance.
  * @param options Additional configuration of the view.
  */
  constructor(e, i) {
    super(e);
    /**
    * Options passed to the view. See {@link #constructor} to learn more.
    */
    f(this, "options");
    /**
    * Tracks information about the DOM focus in the form.
    */
    f(this, "focusTracker");
    /**
    * An instance of the {@link module:utils/keystrokehandler~KeystrokeHandler}.
    */
    f(this, "keystrokes");
    /**
    * A collection of child views in the form.
    */
    f(this, "children");
    /**
    * A dropdown that allows selecting the style of the table border.
    */
    f(this, "borderStyleDropdown");
    /**
    * An input that allows specifying the width of the table border.
    */
    f(this, "borderWidthInput");
    /**
    * An input that allows specifying the color of the table border.
    */
    f(this, "borderColorInput");
    /**
    * An input that allows specifying the table background color.
    */
    f(this, "backgroundInput");
    /**
    * An input that allows specifying the table width.
    */
    f(this, "widthInput");
    /**
    * An input that allows specifying the table height.
    */
    f(this, "heightInput");
    /**
    * A toolbar with buttons that allow changing the alignment of an entire table.
    */
    f(this, "alignmentToolbar");
    /**
    * The "Save" button view.
    */
    f(this, "saveButtonView");
    /**
    * The "Cancel" button view.
    */
    f(this, "cancelButtonView");
    /**
    * A collection of views that can be focused in the form.
    */
    f(this, "_focusables");
    /**
    * Helps cycling over {@link #_focusables} in the form.
    */
    f(this, "_focusCycler");
    this.set({
      borderStyle: "",
      borderWidth: "",
      borderColor: "",
      backgroundColor: "",
      width: "",
      height: "",
      alignment: ""
    }), this.options = i;
    const { borderStyleDropdown: s, borderWidthInput: r, borderColorInput: o, borderRowLabel: a } = this._createBorderFields(), { backgroundRowLabel: l, backgroundInput: c } = this._createBackgroundFields(), { widthInput: u, operatorLabel: d, heightInput: h, dimensionsLabel: m } = this._createDimensionFields(), { alignmentToolbar: g, alignmentLabel: p } = this._createAlignmentFields();
    this.focusTracker = new ze(), this.keystrokes = new Ze(), this.children = this.createCollection(), this.borderStyleDropdown = s, this.borderWidthInput = r, this.borderColorInput = o, this.backgroundInput = c, this.widthInput = u, this.heightInput = h, this.alignmentToolbar = g;
    const { saveButtonView: w, cancelButtonView: y } = this._createActionButtons();
    this.saveButtonView = w, this.cancelButtonView = y, this._focusables = new Et(), this._focusCycler = new Pt({
      focusables: this._focusables,
      focusTracker: this.focusTracker,
      keystrokeHandler: this.keystrokes,
      actions: {
        // Navigate form fields backwards using the Shift + Tab keystroke.
        focusPrevious: "shift + tab",
        // Navigate form fields forwards using the Tab key.
        focusNext: "tab"
      }
    }), this.children.add(new Va(e, {
      label: this.t("Table properties")
    })), this.children.add(new xi(e, {
      labelView: a,
      children: [
        a,
        s,
        o,
        r
      ],
      class: "ck-table-form__border-row"
    })), this.children.add(new xi(e, {
      labelView: l,
      children: [
        l,
        c
      ],
      class: "ck-table-form__background-row"
    })), this.children.add(new xi(e, {
      children: [
        // Dimensions row.
        new xi(e, {
          labelView: m,
          children: [
            m,
            u,
            d,
            h
          ],
          class: "ck-table-form__dimensions-row"
        }),
        // Alignment row.
        new xi(e, {
          labelView: p,
          children: [
            p,
            g
          ],
          class: "ck-table-properties-form__alignment-row"
        })
      ]
    })), this.children.add(new xi(e, {
      children: [
        this.saveButtonView,
        this.cancelButtonView
      ],
      class: "ck-table-form__action-row"
    })), this.setTemplate({
      tag: "form",
      attributes: {
        class: [
          "ck",
          "ck-form",
          "ck-table-form",
          "ck-table-properties-form"
        ],
        // https://github.com/ckeditor/ckeditor5-link/issues/90
        tabindex: "-1"
      },
      children: this.children
    });
  }
  /**
  * @inheritDoc
  */
  render() {
    super.render(), Tn({
      view: this
    }), [
      this.borderColorInput,
      this.backgroundInput
    ].forEach((e) => {
      this._focusCycler.chain(e.fieldView.focusCycler);
    }), [
      this.borderStyleDropdown,
      this.borderColorInput,
      this.borderWidthInput,
      this.backgroundInput,
      this.widthInput,
      this.heightInput,
      this.alignmentToolbar,
      this.saveButtonView,
      this.cancelButtonView
    ].forEach((e) => {
      this._focusables.add(e), this.focusTracker.add(e.element);
    }), this.keystrokes.listenTo(this.element);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.focusTracker.destroy(), this.keystrokes.destroy();
  }
  /**
  * Focuses the fist focusable field in the form.
  */
  focus() {
    this._focusCycler.focusFirst();
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #borderStyleDropdown},
  * * {@link #borderWidthInput},
  * * {@link #borderColorInput}.
  */
  _createBorderFields() {
    const e = this.options.defaultTableProperties, i = {
      style: e.borderStyle,
      width: e.borderWidth,
      color: e.borderColor
    }, s = _c({
      colorConfig: this.options.borderColors,
      columns: 5,
      defaultColorValue: i.color,
      colorPickerConfig: this.options.colorPickerConfig
    }), r = this.locale, o = this.t, a = o("Style"), l = new gi(r);
    l.text = o("Border");
    const c = ym(o), u = new gt(r, jf);
    u.set({
      label: a,
      class: "ck-table-form__border-style"
    }), u.fieldView.buttonView.set({
      ariaLabel: a,
      ariaLabelledBy: void 0,
      isOn: !1,
      withText: !0,
      tooltip: a
    }), u.fieldView.buttonView.bind("label").to(this, "borderStyle", (m) => c[m || "none"]), u.fieldView.on("execute", (m) => {
      this.borderStyle = m.source._borderStyleValue;
    }), u.bind("isEmpty").to(this, "borderStyle", (m) => !m), En(u.fieldView, l2(this, i.style), {
      role: "menu",
      ariaLabel: a
    });
    const d = new gt(r, pi);
    d.set({
      label: o("Width"),
      class: "ck-table-form__border-width"
    }), d.fieldView.bind("value").to(this, "borderWidth"), d.bind("isEnabled").to(this, "borderStyle", El), d.fieldView.on("input", () => {
      this.borderWidth = d.fieldView.element.value;
    });
    const h = new gt(r, s);
    return h.set({
      label: o("Color"),
      class: "ck-table-form__border-color"
    }), h.fieldView.bind("value").to(this, "borderColor"), h.bind("isEnabled").to(this, "borderStyle", El), h.fieldView.on("input", () => {
      this.borderColor = h.fieldView.value;
    }), this.on("change:borderStyle", (m, g, p, w) => {
      El(p) || (this.borderColor = "", this.borderWidth = ""), El(w) || (this.borderColor = i.color, this.borderWidth = i.width);
    }), {
      borderRowLabel: l,
      borderStyleDropdown: u,
      borderColorInput: h,
      borderWidthInput: d
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #backgroundInput}.
  */
  _createBackgroundFields() {
    const e = this.locale, i = this.t, s = new gi(e);
    s.text = i("Background");
    const r = _c({
      colorConfig: this.options.backgroundColors,
      columns: 5,
      defaultColorValue: this.options.defaultTableProperties.backgroundColor,
      colorPickerConfig: this.options.colorPickerConfig
    }), o = new gt(e, r);
    return o.set({
      label: i("Color"),
      class: "ck-table-properties-form__background"
    }), o.fieldView.bind("value").to(this, "backgroundColor"), o.fieldView.on("input", () => {
      this.backgroundColor = o.fieldView.value;
    }), {
      backgroundRowLabel: s,
      backgroundInput: o
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #widthInput},
  * * {@link #heightInput}.
  */
  _createDimensionFields() {
    const e = this.locale, i = this.t, s = new gi(e);
    s.text = i("Dimensions");
    const r = new gt(e, pi);
    r.set({
      label: i("Width"),
      class: "ck-table-form__dimensions-row__width"
    }), r.fieldView.bind("value").to(this, "width"), r.fieldView.on("input", () => {
      this.width = r.fieldView.element.value;
    });
    const o = new ee(e);
    o.setTemplate({
      tag: "span",
      attributes: {
        class: [
          "ck-table-form__dimension-operator"
        ]
      },
      children: [
        {
          text: "×"
        }
      ]
    });
    const a = new gt(e, pi);
    return a.set({
      label: i("Height"),
      class: "ck-table-form__dimensions-row__height"
    }), a.fieldView.bind("value").to(this, "height"), a.fieldView.on("input", () => {
      this.height = a.fieldView.element.value;
    }), {
      dimensionsLabel: s,
      widthInput: r,
      operatorLabel: o,
      heightInput: a
    };
  }
  /**
  * Creates the following form fields:
  *
  * * {@link #alignmentToolbar}.
  */
  _createAlignmentFields() {
    const e = this.locale, i = this.t, s = new gi(e);
    s.text = i("Alignment");
    const r = new Us(e);
    return r.set({
      role: "radiogroup",
      isCompact: !0,
      ariaLabel: i("Table alignment toolbar")
    }), Uh({
      view: this,
      icons: {
        left: Z.objectLeft,
        center: Z.objectCenter,
        right: Z.objectRight
      },
      toolbar: r,
      labels: this._alignmentLabels,
      propertyName: "alignment",
      defaultValue: this.options.defaultTableProperties.alignment
    }), {
      alignmentLabel: s,
      alignmentToolbar: r
    };
  }
  /**
  * Creates the following form controls:
  *
  * * {@link #saveButtonView},
  * * {@link #cancelButtonView}.
  */
  _createActionButtons() {
    const e = this.locale, i = this.t, s = new le(e), r = new le(e), o = [
      this.borderWidthInput,
      this.borderColorInput,
      this.backgroundInput,
      this.widthInput,
      this.heightInput
    ];
    return s.set({
      label: i("Save"),
      icon: Z.check,
      class: "ck-button-save",
      type: "submit",
      withText: !0
    }), s.bind("isEnabled").toMany(o, "errorText", (...a) => a.every((l) => !l)), r.set({
      label: i("Cancel"),
      icon: Z.cancel,
      class: "ck-button-cancel",
      withText: !0
    }), r.delegate("execute").to(this, "cancel"), {
      saveButtonView: s,
      cancelButtonView: r
    };
  }
  /**
  * Provides localized labels for {@link #alignmentToolbar} buttons.
  */
  get _alignmentLabels() {
    const e = this.locale, i = this.t, s = i("Align table to the left"), r = i("Center table"), o = i("Align table to the right");
    return e.uiLanguageDirection === "rtl" ? {
      right: o,
      center: r,
      left: s
    } : {
      left: s,
      center: r,
      right: o
    };
  }
}
function El(n) {
  return n !== "none";
}
var HK = '<svg viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M8 2v5h4V2h1v5h5v1h-5v4h.021l-.172.351-1.916.28-.151.027c-.287.063-.54.182-.755.341L8 13v5H7v-5H2v-1h5V8H2V7h5V2h1zm4 6H8v4h4V8z" opacity=".6"/><path d="m15.5 11.5 1.323 2.68 2.957.43-2.14 2.085.505 2.946L15.5 18.25l-2.645 1.39.505-2.945-2.14-2.086 2.957-.43L15.5 11.5zM17 1a2 2 0 0 1 2 2v9.475l-.85-.124-.857-1.736a2.048 2.048 0 0 0-.292-.44L17 3H3v14h7.808l.402.392L10.935 19H3a2 2 0 0 1-2-2V3a2 2 0 0 1 2-2h14z"/></svg>';
const jK = 500, Ww = {
  borderStyle: "tableBorderStyle",
  borderColor: "tableBorderColor",
  borderWidth: "tableBorderWidth",
  backgroundColor: "tableBackgroundColor",
  width: "tableWidth",
  height: "tableHeight",
  alignment: "tableAlignment"
};
class UK extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The default table properties.
    */
    f(this, "_defaultTableProperties");
    /**
    * The contextual balloon plugin instance.
    */
    f(this, "_balloon");
    /**
    * The properties form view displayed inside the balloon.
    */
    f(this, "view", null);
    /**
    * The batch used to undo all changes made by the form (which are live, as the user types)
    * when "Cancel" was pressed. Each time the view is shown, a new batch is created.
    */
    f(this, "_undoStepBatch");
    /**
    * Flag used to indicate whether view is ready to execute update commands
    * (it finished loading initial data).
    */
    f(this, "_isReady");
    e.config.define("table.tableProperties", {
      borderColors: vc,
      backgroundColors: vc
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      ai
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TablePropertiesUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.t;
    this._defaultTableProperties = g2(e.config.get("table.tableProperties.defaultProperties"), {
      includeAlignmentProperty: !0
    }), this._balloon = e.plugins.get(ai), e.ui.componentFactory.add("tableProperties", (s) => {
      const r = new le(s);
      r.set({
        label: i("Table properties"),
        icon: HK,
        tooltip: !0
      }), this.listenTo(r, "execute", () => this._showView());
      const o = Object.values(Ww).map((a) => e.commands.get(a));
      return r.bind("isEnabled").toMany(o, "isEnabled", (...a) => a.some((l) => l)), r;
    });
  }
  /**
  * @inheritDoc
  */
  destroy() {
    super.destroy(), this.view && this.view.destroy();
  }
  /**
  * Creates the {@link module:table/tableproperties/ui/tablepropertiesview~TablePropertiesView} instance.
  *
  * @returns The table properties form view instance.
  */
  _createPropertiesView() {
    const e = this.editor, i = e.config.get("table.tableProperties"), s = ma(i.borderColors), r = fa(e.locale, s), o = ma(i.backgroundColors), a = fa(e.locale, o), l = i.colorPicker !== !1, c = new zK(e.locale, {
      borderColors: r,
      backgroundColors: a,
      defaultTableProperties: this._defaultTableProperties,
      colorPickerConfig: l ? i.colorPicker || {} : !1
    }), u = e.t;
    c.render(), this.listenTo(c, "submit", () => {
      this._hideView();
    }), this.listenTo(c, "cancel", () => {
      this._undoStepBatch.operations.length && e.execute("undo", this._undoStepBatch), this._hideView();
    }), c.keystrokes.set("Esc", (m, g) => {
      this._hideView(), g();
    }), Xn({
      emitter: c,
      activator: () => this._isViewInBalloon,
      contextElements: [
        this._balloon.view.element
      ],
      callback: () => this._hideView()
    });
    const d = r2(u), h = o2(u);
    return c.on("change:borderStyle", this._getPropertyChangeCallback("tableBorderStyle")), c.on("change:borderColor", this._getValidatedPropertyChangeCallback({
      viewField: c.borderColorInput,
      commandName: "tableBorderColor",
      errorText: d,
      validator: wc
    })), c.on("change:borderWidth", this._getValidatedPropertyChangeCallback({
      viewField: c.borderWidthInput,
      commandName: "tableBorderWidth",
      errorText: h,
      validator: a2
    })), c.on("change:backgroundColor", this._getValidatedPropertyChangeCallback({
      viewField: c.backgroundInput,
      commandName: "tableBackgroundColor",
      errorText: d,
      validator: wc
    })), c.on("change:width", this._getValidatedPropertyChangeCallback({
      viewField: c.widthInput,
      commandName: "tableWidth",
      errorText: h,
      validator: Zo
    })), c.on("change:height", this._getValidatedPropertyChangeCallback({
      viewField: c.heightInput,
      commandName: "tableHeight",
      errorText: h,
      validator: Zo
    })), c.on("change:alignment", this._getPropertyChangeCallback("tableAlignment")), c;
  }
  /**
  * In this method the "editor data -> UI" binding is happening.
  *
  * When executed, this method obtains selected table property values from various table commands
  * and passes them to the {@link #view}.
  *
  * This way, the UI stays up–to–date with the editor data.
  */
  _fillViewFormFromCommandValues() {
    const e = this.editor.commands, i = e.get("tableBorderStyle");
    Object.entries(Ww).map(([s, r]) => {
      const o = s, a = this._defaultTableProperties[o] || "";
      return [
        o,
        e.get(r).value || a
      ];
    }).forEach(([s, r]) => {
      (s === "borderColor" || s === "borderWidth") && i.value === "none" || this.view.set(s, r);
    }), this._isReady = !0;
  }
  /**
  * Shows the {@link #view} in the {@link #_balloon}.
  *
  * **Note**: Each time a view is shown, the new {@link #_undoStepBatch} is created that contains
  * all changes made to the document when the view is visible, allowing a single undo step
  * for all of them.
  */
  _showView() {
    const e = this.editor;
    this.view || (this.view = this._createPropertiesView()), this.listenTo(e.ui, "update", () => {
      this._updateView();
    }), this._fillViewFormFromCommandValues(), this._balloon.add({
      view: this.view,
      position: d2(e)
    }), this._undoStepBatch = e.model.createBatch(), this.view.focus();
  }
  /**
  * Removes the {@link #view} from the {@link #_balloon}.
  */
  _hideView() {
    const e = this.editor;
    this.stopListening(e.ui, "update"), this._isReady = !1, this.view.saveButtonView.focus(), this._balloon.remove(this.view), this.editor.editing.view.focus();
  }
  /**
  * Repositions the {@link #_balloon} or hides the {@link #view} if a table is no longer selected.
  */
  _updateView() {
    const e = this.editor, i = e.editing.view.document;
    i2(i.selection) ? this._isViewVisible && u2(e, "table") : this._hideView();
  }
  /**
  * Returns `true` when the {@link #view} is the visible in the {@link #_balloon}.
  */
  get _isViewVisible() {
    return !!this.view && this._balloon.visibleView === this.view;
  }
  /**
  * Returns `true` when the {@link #view} is in the {@link #_balloon}.
  */
  get _isViewInBalloon() {
    return !!this.view && this._balloon.hasView(this.view);
  }
  /**
  * Creates a callback that when executed upon {@link #view view's} property change
  * executes a related editor command with the new property value.
  *
  * If new value will be set to the default value, the command will not be executed.
  *
  * @param commandName The command that will be executed.
  */
  _getPropertyChangeCallback(e) {
    return (i, s, r) => {
      this._isReady && this.editor.execute(e, {
        value: r,
        batch: this._undoStepBatch
      });
    };
  }
  /**
  * Creates a callback that when executed upon {@link #view view's} property change:
  * * executes a related editor command with the new property value if the value is valid,
  * * or sets the error text next to the invalid field, if the value did not pass the validation.
  */
  _getValidatedPropertyChangeCallback(e) {
    const { commandName: i, viewField: s, validator: r, errorText: o } = e, a = vi(() => {
      s.errorText = o;
    }, jK);
    return (l, c, u) => {
      a.cancel(), this._isReady && (r(u) ? (this.editor.execute(i, {
        value: u,
        batch: this._undoStepBatch
      }), s.errorText = null) : a());
    };
  }
}
class WK extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableProperties";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      NK,
      UK
    ];
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function qK(n) {
  n.document.registerPostFixer((t) => GK(t, n));
}
function GK(n, t) {
  const e = t.document.differ.getChanges();
  let i = !1;
  for (const s of e) {
    if (s.type != "insert")
      continue;
    const r = s.position.parent;
    if (r.is("element", "table") || s.name == "table") {
      const o = s.name == "table" ? s.position.nodeAfter : r, a = Array.from(o.getChildren()).filter((c) => c.is("element", "caption")), l = a.shift();
      if (!l)
        continue;
      for (const c of a)
        n.move(n.createRangeIn(c), l, "end"), n.remove(c);
      l.nextSibling && (n.move(n.createRangeOn(l), o, "end"), i = !0), i = !!a.length || i;
    }
  }
  return i;
}
function qw(n) {
  return !!n && n.is("element", "table");
}
function qh(n) {
  for (const t of n.getChildren())
    if (t.is("element", "caption"))
      return t;
  return null;
}
function KK(n) {
  const t = Bs(n);
  return t ? qh(t) : null;
}
function ZK(n) {
  const t = n.parent;
  return n.name == "figcaption" && t && t.is("element", "figure") && t.hasClass("table") ? {
    name: !0
  } : n.name == "caption" && t && t.is("element", "table") ? {
    name: !0
  } : null;
}
class YK extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    const t = this.editor, e = Bs(t.model.document.selection);
    this.isEnabled = !!e, this.isEnabled ? this.value = !!qh(e) : this.value = !1;
  }
  /**
  * Executes the command.
  *
  * ```ts
  * editor.execute( 'toggleTableCaption' );
  * ```
  *
  * @param options Options for the executed command.
  * @param options.focusCaptionOnShow When true and the caption shows up, the selection will be moved into it straight away.
  * @fires execute
  */
  execute({ focusCaptionOnShow: t = !1 } = {}) {
    this.editor.model.change((e) => {
      this.value ? this._hideTableCaption(e) : this._showTableCaption(e, t);
    });
  }
  /**
  * Shows the table caption. Also:
  *
  * * it attempts to restore the caption content from the `TableCaptionEditing` caption registry,
  * * it moves the selection to the caption right away, it the `focusCaptionOnShow` option was set.
  *
  * @param focusCaptionOnShow Default focus behavior when showing the caption.
  */
  _showTableCaption(t, e) {
    const i = this.editor.model, s = Bs(i.document.selection), a = this.editor.plugins.get("TableCaptionEditing")._getSavedCaption(s) || t.createElement("caption");
    i.insertContent(a, s, "end"), e && t.setSelection(a, "in");
  }
  /**
  * Hides the caption of a selected table (or an table caption the selection is anchored to).
  *
  * The content of the caption is stored in the `TableCaptionEditing` caption registry to make this
  * a reversible action.
  */
  _hideTableCaption(t) {
    const e = this.editor.model, i = Bs(e.document.selection), s = this.editor.plugins.get("TableCaptionEditing"), r = qh(i);
    s._saveCaption(i, r), e.deleteContent(t.createSelection(r, "on"));
  }
}
class JK extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A map that keeps saved JSONified table captions and table model elements they are
    * associated with.
    *
    * To learn more about this system, see {@link #_saveCaption}.
    */
    f(this, "_savedCaptionsMap");
    this._savedCaptionsMap = /* @__PURE__ */ new WeakMap();
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCaptionEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const e = this.editor, i = e.model.schema, s = e.editing.view, r = e.t;
    i.isRegistered("caption") ? i.extend("caption", {
      allowIn: "table"
    }) : i.register("caption", {
      allowIn: "table",
      allowContentOf: "$block",
      isLimit: !0
    }), e.commands.add("toggleTableCaption", new YK(this.editor)), e.conversion.for("upcast").elementToElement({
      view: ZK,
      model: "caption"
    }), e.conversion.for("dataDowncast").elementToElement({
      model: "caption",
      view: (o, { writer: a }) => qw(o.parent) ? a.createContainerElement("figcaption") : null
    }), e.conversion.for("editingDowncast").elementToElement({
      model: "caption",
      view: (o, { writer: a }) => {
        if (!qw(o.parent))
          return null;
        const l = a.createEditableElement("figcaption");
        return a.setCustomProperty("tableCaption", !0, l), l.placeholder = r("Enter table caption"), xf({
          view: s,
          element: l,
          keepOnFocus: !0
        }), Kf(l, a);
      }
    }), qK(e.model);
  }
  /**
  * Returns the saved {@link module:engine/model/element~Element#toJSON JSONified} caption
  * of a table model element.
  *
  * See {@link #_saveCaption}.
  *
  * @internal
  * @param tableModelElement The model element the caption should be returned for.
  * @returns The model caption element or `null` if there is none.
  */
  _getSavedCaption(e) {
    const i = this._savedCaptionsMap.get(e);
    return i ? et.fromJSON(i) : null;
  }
  /**
  * Saves a {@link module:engine/model/element~Element#toJSON JSONified} caption for
  * a table element to allow restoring it in the future.
  *
  * A caption is saved every time it gets hidden. The
  * user should be able to restore it on demand.
  *
  * **Note**: The caption cannot be stored in the table model element attribute because,
  * for instance, when the model state propagates to collaborators, the attribute would get
  * lost (mainly because it does not convert to anything when the caption is hidden) and
  * the states of collaborators' models would de-synchronize causing numerous issues.
  *
  * See {@link #_getSavedCaption}.
  *
  * @internal
  * @param tableModelElement The model element the caption is saved for.
  * @param caption The caption model element to be saved.
  */
  _saveCaption(e, i) {
    this._savedCaptionsMap.set(e, i.toJSON());
  }
}
class XK extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCaptionUI";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    const t = this.editor, e = t.editing.view, i = t.t;
    t.ui.componentFactory.add("toggleTableCaption", (s) => {
      const r = t.commands.get("toggleTableCaption"), o = new le(s);
      return o.set({
        icon: Z.caption,
        tooltip: !0,
        isToggleable: !0
      }), o.bind("isOn", "isEnabled").to(r, "value", "isEnabled"), o.bind("label").to(r, "value", (a) => i(a ? "Toggle caption off" : "Toggle caption on")), this.listenTo(o, "execute", () => {
        if (t.execute("toggleTableCaption", {
          focusCaptionOnShow: !0
        }), r.value) {
          const a = KK(t.model.document.selection), l = t.editing.mapper.toViewElement(a);
          if (!l)
            return;
          e.scrollToTheSelection(), e.change((c) => {
            c.addClass("table__caption_highlighted", l);
          });
        }
        t.editing.view.focus();
      }), o;
    });
  }
}
class QK extends I {
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableCaption";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      JK,
      XK
    ];
  }
}
class eZ extends he {
  /**
  * @inheritDoc
  */
  refresh() {
    this.isEnabled = !0;
  }
  /**
  * Updated the `tableWidth` attribute of the table and the `columnWidth` attribute of the columns of that table.
  */
  execute(t = {}) {
    const { model: e, plugins: i } = this.editor;
    let { table: s = e.document.selection.getSelectedElement(), columnWidths: r, tableWidth: o } = t;
    r && (r = Array.isArray(r) ? r : r.split(",")), e.change((a) => {
      o ? a.setAttribute("tableWidth", o, s) : a.removeAttribute("tableWidth", s);
      const l = i.get("TableColumnResizeEditing").getColumnGroupElement(s);
      if (!r && !l)
        return;
      if (!r)
        return a.remove(l);
      const c = vm(r);
      if (l)
        Array.from(l.getChildren()).forEach((u, d) => a.setAttribute("columnWidth", c[d], u));
      else {
        const u = a.createElement("tableColumnGroup");
        c.forEach((d) => a.appendElement("tableColumn", {
          columnWidth: d
        }, u)), a.append(u, s);
      }
    });
  }
}
function tZ(n) {
  return (t) => t.on("element:colgroup", (e, i, s) => {
    const r = i.modelCursor.findAncestor("table"), o = ja(r);
    if (!o)
      return;
    const a = Ua(o), l = n.getColumns(r);
    let c = yG(o, s.writer);
    c = Array.from({
      length: l
    }, (u, d) => c[d] || "auto"), (c.length != a.length || c.includes("auto")) && XC(a, o, vm(c), s.writer);
  }, {
    priority: "low"
  });
}
function iZ() {
  return (n) => n.on("insert:table", (t, e, i) => {
    const s = i.writer, r = e.item, o = i.mapper.toViewElement(r), a = o.is("element", "table") ? o : Array.from(o.getChildren()).find((c) => c.is("element", "table"));
    ja(r) ? s.addClass("ck-table-resized", a) : s.removeClass("ck-table-resized", a);
  }, {
    priority: "low"
  });
}
class nZ extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * A flag indicating if the column resizing is in progress.
    */
    f(this, "_isResizingActive");
    /**
    * A temporary storage for the required data needed to correctly calculate the widths of the resized columns. This storage is
    * initialized when column resizing begins, and is purged upon completion.
    */
    f(this, "_resizingData");
    /**
    * DOM emitter.
    */
    f(this, "_domEmitter");
    /**
    * A local reference to the {@link module:table/tableutils~TableUtils} plugin.
    */
    f(this, "_tableUtilsPlugin");
    this._isResizingActive = !1, this.set("_isResizingAllowed", !0), this._resizingData = null, this._domEmitter = new (li())(), this._tableUtilsPlugin = e.plugins.get("TableUtils"), this.on("change:_isResizingAllowed", (i, s, r) => {
      const o = r ? "removeClass" : "addClass";
      e.editing.view.change((a) => {
        for (const l of e.editing.view.document.roots)
          a[o]("ck-column-resize_disabled", e.editing.view.document.getRoot(l.rootName));
      });
    });
  }
  /**
  * @inheritDoc
  */
  static get requires() {
    return [
      Wa,
      $t
    ];
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "TableColumnResizeEditing";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this._extendSchema(), this._registerPostFixer(), this._registerConverters(), this._registerResizingListeners(), this._registerResizerInserter();
    const e = this.editor, i = e.plugins.get("TableColumnResize");
    e.plugins.get("TableEditing").registerAdditionalSlot({
      filter: (o) => o.is("element", "tableColumnGroup"),
      positionOffset: 0
    });
    const r = new eZ(e);
    e.commands.add("resizeTableWidth", r), e.commands.add("resizeColumnWidths", r), this.bind("_isResizingAllowed").to(e, "isReadOnly", i, "isEnabled", r, "isEnabled", (o, a, l) => !o && a && l);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._domEmitter.stopListening(), super.destroy();
  }
  /**
  * Returns a 'tableColumnGroup' element from the 'table'.
  *
  * @param element A 'table' or 'tableColumnGroup' element.
  * @returns A 'tableColumnGroup' element.
  */
  getColumnGroupElement(e) {
    return ja(e);
  }
  /**
  * Returns an array of 'tableColumn' elements.
  *
  * @param element A 'table' or 'tableColumnGroup' element.
  * @returns An array of 'tableColumn' elements.
  */
  getTableColumnElements(e) {
    return Ua(e);
  }
  /**
  * Returns an array of table column widths.
  *
  * @param element A 'table' or 'tableColumnGroup' element.
  * @returns An array of table column widths.
  */
  getTableColumnsWidths(e) {
    return _G(e);
  }
  /**
  * Registers new attributes for a table model element.
  */
  _extendSchema() {
    this.editor.model.schema.extend("table", {
      allowAttributes: [
        "tableWidth"
      ]
    }), this.editor.model.schema.register("tableColumnGroup", {
      allowIn: "table",
      isLimit: !0
    }), this.editor.model.schema.register("tableColumn", {
      allowIn: "tableColumnGroup",
      allowAttributes: [
        "columnWidth",
        "colSpan"
      ],
      isLimit: !0
    });
  }
  /**
  * Registers table column resize post-fixer.
  *
  * It checks if the change from the differ concerns a table-related element or attribute. For detected changes it:
  *  * Adjusts the `columnWidths` attribute to guarantee that the sum of the widths from all columns is 100%.
  *  * Checks if the `columnWidths` attribute gets updated accordingly after columns have been added or removed.
  */
  _registerPostFixer() {
    const i = this.editor.model;
    i.document.registerPostFixer((o) => {
      let a = !1;
      for (const l of fG(i)) {
        const c = this.getColumnGroupElement(l), u = this.getTableColumnElements(c), d = this.getTableColumnsWidths(c);
        let h = vm(d);
        h = s(h, l, this), !Dr(d, h) && (XC(u, c, h, o), a = !0);
      }
      return a;
    });
    function s(o, a, l) {
      const c = l._tableUtilsPlugin.getColumns(a);
      if (c - o.length === 0)
        return o;
      const d = o.map((m) => Number(m.replace("%", ""))), h = r(l.editor.model.document.differ, a);
      for (const m of h) {
        const g = c - d.length;
        if (g === 0)
          continue;
        const p = g > 0, w = l._tableUtilsPlugin.getCellLocation(m).column;
        if (p) {
          const y = mG(a, l.editor), k = bG(g, y);
          d.splice(w, 0, ...k);
        } else {
          const y = d.splice(w, Math.abs(g));
          d[w] += ya(y);
        }
      }
      return d.map((m) => m + "%");
    }
    function r(o, a) {
      const l = /* @__PURE__ */ new Set();
      for (const c of o.getChanges())
        if (c.type == "insert" && c.position.nodeAfter && c.position.nodeAfter.name == "tableCell" && c.position.nodeAfter.getAncestors().includes(a))
          l.add(c.position.nodeAfter);
        else if (c.type == "remove") {
          const u = c.position.nodeBefore || c.position.nodeAfter;
          u.name == "tableCell" && u.getAncestors().includes(a) && l.add(u);
        }
      return l;
    }
  }
  /**
  * Registers table column resize converters.
  */
  _registerConverters() {
    const i = this.editor.conversion;
    i.for("upcast").attributeToAttribute({
      view: {
        name: "figure",
        key: "style",
        value: {
          width: /[\s\S]+/
        }
      },
      model: {
        name: "table",
        key: "tableWidth",
        value: (s) => s.getStyle("width")
      }
    }), i.for("downcast").attributeToAttribute({
      model: {
        name: "table",
        key: "tableWidth"
      },
      view: (s) => ({
        name: "figure",
        key: "style",
        value: {
          width: s
        }
      })
    }), i.elementToElement({
      model: "tableColumnGroup",
      view: "colgroup"
    }), i.elementToElement({
      model: "tableColumn",
      view: "col"
    }), i.for("downcast").add(iZ()), i.for("upcast").add(tZ(this._tableUtilsPlugin)), i.for("upcast").attributeToAttribute({
      view: {
        name: "col",
        styles: {
          width: /.*/
        }
      },
      model: {
        key: "columnWidth",
        value: (s) => {
          const r = s.getStyle("width");
          return !r || !r.endsWith("%") && !r.endsWith("pt") ? "auto" : r;
        }
      }
    }), i.for("upcast").attributeToAttribute({
      view: {
        name: "col",
        key: "span"
      },
      model: "colSpan"
    }), i.for("downcast").attributeToAttribute({
      model: {
        name: "tableColumn",
        key: "columnWidth"
      },
      view: (s) => ({
        key: "style",
        value: {
          width: s
        }
      })
    });
  }
  /**
  * Registers listeners to handle resizing process.
  */
  _registerResizingListeners() {
    const e = this.editor.editing.view;
    e.addObserver(t2), e.document.on("mousedown", this._onMouseDownHandler.bind(this), {
      priority: "high"
    }), this._domEmitter.listenTo(ae.window.document, "mousemove", $s(this._onMouseMoveHandler.bind(this), 50)), this._domEmitter.listenTo(ae.window.document, "mouseup", this._onMouseUpHandler.bind(this));
  }
  /**
  * Handles the `mousedown` event on column resizer element:
  *  * calculates the initial column pixel widths,
  *  * inserts the `<colgroup>` element if it is not present in the `<table>`,
  *  * puts the necessary data in the temporary storage,
  *  * applies the attributes to the `<table>` view element.
  *
  * @param eventInfo An object containing information about the fired event.
  * @param domEventData The data related to the DOM event.
  */
  _onMouseDownHandler(e, i) {
    const s = i.target;
    if (!s.hasClass("ck-table-column-resizer") || !this._isResizingAllowed)
      return;
    const r = this.editor, o = r.editing.mapper.toModelElement(s.findAncestor("figure"));
    if (!r.model.canEditAt(o))
      return;
    i.preventDefault(), e.stop();
    const a = u(o, this._tableUtilsPlugin, r), l = s.findAncestor("table"), c = r.editing.view;
    Array.from(l.getChildren()).find((m) => m.is("element", "colgroup")) || c.change((m) => {
      d(m, a, l);
    }), this._isResizingActive = !0, this._resizingData = this._getResizingData(i, a), c.change((m) => h(m, l, this._resizingData));
    function u(m, g, p) {
      const w = Array(g.getColumns(m)), y = new Ke(m);
      for (const k of y) {
        const C = p.editing.mapper.toViewElement(k.cell), E = p.editing.view.domConverter.mapViewToDom(C), T = vG(E);
        (!w[k.column] || T < w[k.column]) && (w[k.column] = mi(T));
      }
      return w;
    }
    function d(m, g, p) {
      const w = m.createContainerElement("colgroup");
      for (let y = 0; y < g.length; y++) {
        const k = m.createEmptyElement("col"), C = `${mi(g[y] / ya(g) * 100)}%`;
        m.setStyle("width", C, k), m.insert(m.createPositionAt(w, "end"), k);
      }
      m.insert(m.createPositionAt(p, 0), w);
    }
    function h(m, g, p) {
      const w = p.widths.viewFigureWidth / p.widths.viewFigureParentWidth;
      m.addClass("ck-table-resized", g), m.addClass("ck-table-column-resizer__active", p.elements.viewResizer), m.setStyle("width", `${mi(w * 100)}%`, g.findAncestor("figure"));
    }
  }
  /**
  * Handles the `mousemove` event.
  *  * If resizing process is not in progress, it does nothing.
  *  * If resizing is active but not allowed, it stops the resizing process instantly calling the `mousedown` event handler.
  *  * Otherwise it dynamically updates the widths of the resized columns.
  *
  * @param eventInfo An object containing information about the fired event.
  * @param mouseEventData The native DOM event.
  */
  _onMouseMoveHandler(e, i) {
    if (!this._isResizingActive)
      return;
    if (!this._isResizingAllowed) {
      this._onMouseUpHandler();
      return;
    }
    const { columnPosition: s, flags: { isRightEdge: r, isTableCentered: o, isLtrContent: a }, elements: { viewFigure: l, viewLeftColumn: c, viewRightColumn: u }, widths: { viewFigureParentWidth: d, tableWidth: h, leftColumnWidth: m, rightColumnWidth: g } } = this._resizingData, p = -m + Dh, w = r ? d - h : g - Dh, y = (a ? 1 : -1) * (r && o ? 2 : 1), k = pG((i.clientX - s) * y, Math.min(p, 0), Math.max(w, 0));
    k !== 0 && this.editor.editing.view.change((C) => {
      const E = mi((m + k) * 100 / h);
      if (C.setStyle("width", `${E}%`, c), r) {
        const T = mi((h + k) * 100 / d);
        C.setStyle("width", `${T}%`, l);
      } else {
        const T = mi((g - k) * 100 / h);
        C.setStyle("width", `${T}%`, u);
      }
    });
  }
  /**
  * Handles the `mouseup` event.
  *  * If resizing process is not in progress, it does nothing.
  *  * If resizing is active but not allowed, it cancels the resizing process restoring the original widths.
  *  * Otherwise it propagates the changes from view to the model by executing the adequate commands.
  */
  _onMouseUpHandler() {
    if (!this._isResizingActive)
      return;
    const { viewResizer: e, modelTable: i, viewFigure: s, viewColgroup: r } = this._resizingData.elements, o = this.editor, a = o.editing.view, l = this.getColumnGroupElement(i), c = Array.from(r.getChildren()).filter((w) => w.is("view:element")), u = l ? this.getTableColumnsWidths(l) : null, d = c.map((w) => w.getStyle("width")), h = !Dr(u, d), m = i.getAttribute("tableWidth"), g = s.getStyle("width"), p = m !== g;
    (h || p) && (this._isResizingAllowed ? o.execute("resizeTableWidth", {
      table: i,
      tableWidth: `${mi(g)}%`,
      columnWidths: d
    }) : a.change((w) => {
      if (u)
        for (const y of c)
          w.setStyle("width", u.shift(), y);
      else
        w.remove(r);
      p && (m ? w.setStyle("width", m, s) : w.removeStyle("width", s)), !u && !m && w.removeClass("ck-table-resized", [
        ...s.getChildren()
      ].find((y) => y.name === "table"));
    })), a.change((w) => {
      w.removeClass("ck-table-column-resizer__active", e);
    }), this._isResizingActive = !1, this._resizingData = null;
  }
  /**
  * Retrieves and returns required data needed for the resizing process.
  *
  * @param domEventData The data of the `mousedown` event.
  * @param columnWidths The current widths of the columns.
  * @returns The data needed for the resizing process.
  */
  _getResizingData(e, i) {
    const s = this.editor, r = e.domEvent.clientX, o = e.target, a = o.findAncestor("td") || o.findAncestor("th"), l = s.editing.mapper.toModelElement(a), c = l.findAncestor("table"), u = gG(l, this._tableUtilsPlugin).rightEdge, d = this._tableUtilsPlugin.getColumns(c) - 1, h = u === d, m = !c.hasAttribute("tableAlignment"), g = s.locale.contentLanguageDirection !== "rtl", p = a.findAncestor("table"), w = p.findAncestor("figure"), y = [
      ...p.getChildren()
    ].find((M) => M.is("element", "colgroup")), k = y.getChild(u), C = h ? void 0 : y.getChild(u + 1), E = $h(s.editing.view.domConverter.mapViewToDom(w.parent)), T = $h(s.editing.view.domConverter.mapViewToDom(w)), L = JC(c, s), O = i[u], N = h ? void 0 : i[u + 1];
    return {
      columnPosition: r,
      flags: {
        isRightEdge: h,
        isTableCentered: m,
        isLtrContent: g
      },
      elements: {
        viewResizer: o,
        modelTable: c,
        viewFigure: w,
        viewColgroup: y,
        viewLeftColumn: k,
        viewRightColumn: C
      },
      widths: {
        viewFigureParentWidth: E,
        viewFigureWidth: T,
        tableWidth: L,
        leftColumnWidth: O,
        rightColumnWidth: N
      }
    };
  }
  /**
  * Registers a listener ensuring that each resizable cell have a resizer handle.
  */
  _registerResizerInserter() {
    this.editor.conversion.for("editingDowncast").add((e) => {
      e.on("insert:tableCell", (i, s, r) => {
        const o = s.item, a = r.mapper.toViewElement(o), l = r.writer;
        l.insert(l.createPositionAt(a, "end"), l.createUIElement("div", {
          class: "ck-table-column-resizer"
        }));
      }, {
        priority: "lowest"
      });
    });
  }
}
class sZ extends I {
  /**
  * @inheritDoc
   */
  static get requires() {
    return [
      nZ,
      p2
    ];
  }
  /**
  * @inheritDoc
   */
  static get pluginName() {
    return "TableColumnResize";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
}
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
/**
 * @license Copyright (c) 2003-2025, CKSource Holding sp. z o.o. All rights reserved.
 * For licensing, see LICENSE.md or https://ckeditor.com/legal/ckeditor-licensing-options
 */
function b2(n) {
  if (n.is("$text") || n.is("$textProxy"))
    return n.data;
  const t = n;
  let e = "", i = null;
  for (const s of t.getChildren()) {
    const r = b2(s);
    i && i.is("element") && (e += `
`), e += r, i = s;
  }
  return e;
}
class rZ extends I {
  /**
  * @inheritDoc
  */
  constructor(e) {
    super(e);
    /**
    * The configuration of this plugin.
    */
    f(this, "_config");
    /**
    * The reference to a {@link module:ui/view~View view object} that contains the self-updating HTML container.
    */
    f(this, "_outputView");
    /**
    * A regular expression used to recognize words in the editor's content.
    */
    f(this, "_wordsMatchRegExp");
    this.set("characters", 0), this.set("words", 0), Object.defineProperties(this, {
      characters: {
        get() {
          return this.characters = this._getCharacters(this._getText());
        }
      },
      words: {
        get() {
          return this.words = this._getWords(this._getText());
        }
      }
    }), this.set("_wordsLabel", void 0), this.set("_charactersLabel", void 0), this._config = e.config.get("wordCount") || {}, this._outputView = void 0, this._wordsMatchRegExp = be.features.isRegExpUnicodePropertySupported ? (
      // Usage of regular expression literal cause error during build (ckeditor/ckeditor5-dev#534).
      // Groups:
      // {L} - Any kind of letter from any language.
      // {N} - Any kind of numeric character in any script.
      new RegExp("([\\p{L}\\p{N}]+\\S?)+", "gu")
    ) : /([a-zA-Z0-9À-ž]+\S?)+/gu;
  }
  /**
  * @inheritDoc
  */
  static get pluginName() {
    return "WordCount";
  }
  /**
  * @inheritDoc
  */
  static get isOfficialPlugin() {
    return !0;
  }
  /**
  * @inheritDoc
  */
  init() {
    this.editor.model.document.on("change:data", $s(this._refreshStats.bind(this), 250)), typeof this._config.onUpdate == "function" && this.on("update", (i, s) => {
      this._config.onUpdate(s);
    }), Hn(this._config.container) && this._config.container.appendChild(this.wordCountContainer);
  }
  /**
  * @inheritDoc
  */
  destroy() {
    this._outputView && (this._outputView.element.remove(), this._outputView.destroy()), super.destroy();
  }
  /**
  * Creates a self-updating HTML element. Repeated executions return the same element.
  * The returned element has the following HTML structure:
  *
  * ```html
  * <div class="ck ck-word-count">
  * 	<div class="ck-word-count__words">Words: 4</div>
  * 	<div class="ck-word-count__characters">Characters: 28</div>
  * </div>
  * ```
  */
  get wordCountContainer() {
    const e = this.editor, i = e.t, s = e.config.get("wordCount.displayWords"), r = e.config.get("wordCount.displayCharacters"), o = ri.bind(this, this), a = [];
    return this._outputView || (this._outputView = new ee(), (s || s === void 0) && (this.bind("_wordsLabel").to(this, "words", (l) => i("Words: %0", l)), a.push({
      tag: "div",
      children: [
        {
          text: [
            o.to("_wordsLabel")
          ]
        }
      ],
      attributes: {
        class: "ck-word-count__words"
      }
    })), (r || r === void 0) && (this.bind("_charactersLabel").to(this, "characters", (l) => i("Characters: %0", l)), a.push({
      tag: "div",
      children: [
        {
          text: [
            o.to("_charactersLabel")
          ]
        }
      ],
      attributes: {
        class: "ck-word-count__characters"
      }
    })), this._outputView.setTemplate({
      tag: "div",
      attributes: {
        class: [
          "ck",
          "ck-word-count"
        ]
      },
      children: a
    }), this._outputView.render()), this._outputView.element;
  }
  _getText() {
    let e = "";
    for (const i of this.editor.model.document.getRoots())
      e !== "" && (e += `
`), e += b2(i);
    return e;
  }
  /**
  * Determines the number of characters in the current editor's model.
  */
  _getCharacters(e) {
    return e.replace(/\n/g, "").length;
  }
  /**
  * Determines the number of words in the current editor's model.
  */
  _getWords(e) {
    return (e.match(this._wordsMatchRegExp) || []).length;
  }
  /**
  * Determines the number of words and characters in the current editor's model and assigns it to {@link #characters} and {@link #words}.
  * It also fires the {@link #event:update}.
  *
  * @fires update
  */
  _refreshStats() {
    const e = this._getText(), i = this.words = this._getWords(e), s = this.characters = this._getCharacters(e);
    this.fire("update", {
      words: i,
      characters: s
    });
  }
}
const oZ = "GPL";
function gY(n) {
  var a;
  const t = yt(null), e = yt(null), [i, s] = zt(!1), { i18n: r } = Yr();
  ht(() => (s(!0), () => s(!1)), []);
  const { editorConfig: o } = Ca(() => i ? {
    editorConfig: {
      toolbar: {
        items: [
          "heading",
          "|",
          // 'sourceEditing',
          // 'showBlocks',
          // 'findAndReplace',
          // 'textPartLanguage',
          "fontSize",
          "fontFamily",
          "fontColor",
          "fontBackgroundColor",
          "|",
          "bold",
          "italic",
          "underline",
          "strikethrough",
          // 'subscript',
          // 'superscript',
          // 'code',
          // 'removeFormat',
          "|",
          "specialCharacters",
          "insertImage",
          "horizontalLine",
          "pageBreak",
          "link",
          // 'bookmark',
          // 'insertImageViaUrl',
          // 'ckbox',
          "mediaEmbed",
          "insertTable",
          "highlight",
          // 'blockQuote',
          // 'codeBlock',
          "htmlEmbed",
          "|",
          "alignment",
          "|",
          "bulletedList",
          "numberedList",
          "todoList",
          "outdent",
          "indent"
        ],
        shouldNotGroupWhenFull: !1
      },
      plugins: [
        mB,
        LB,
        f$,
        tC,
        BB,
        Q9,
        wM,
        FB,
        mN,
        jB,
        DN,
        iF,
        gF,
        WF,
        $F,
        TF,
        BF,
        o$,
        i$,
        XF,
        oD,
        fD,
        s$,
        vD,
        T$,
        R$,
        S$,
        J$,
        Dk,
        lz,
        vz,
        sm,
        _z,
        Fk,
        Tz,
        Sz,
        GB,
        aH,
        mH,
        DH,
        rj,
        bU,
        VU,
        tW,
        cW,
        cc,
        vU,
        QW,
        kz,
        sq,
        cq,
        mq,
        Cq,
        NC,
        zC,
        xq,
        $C,
        DC,
        FC,
        JB,
        zq,
        tM,
        rM,
        rK,
        QK,
        SK,
        sZ,
        WK,
        oK,
        Oz,
        SB,
        // Title,
        pj,
        cM,
        rZ
      ],
      balloonToolbar: ["bold", "italic", "|", "link", "insertImage", "|", "bulletedList", "numberedList"],
      extraPlugins: n.extraPlugins,
      mediaEmbed: {
        previewsInData: !0,
        providers: [
          {
            name: "youtube",
            url: /^https:\/\/www\.youtube\.com\/watch\?v=([\w-]+)/,
            html: (l) => `<div style="position: relative; padding-bottom: 56.25%; height: 0;"><iframe src="https://www.youtube.com/embed/${l[1]}" style="position: absolute; width: 100%; height: 100%; left: 0;" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe></div>`
          }
        ]
      },
      fontFamily: {
        options: n.fontFamily || ["Default", "Arial", "Courier New", "Inter", "Roboto", "Times New Roman", "Source Serif 4"],
        supportAllValues: !0
      },
      fontSize: {
        options: n.fontSize || [10, 12, 14, "default", 18, 20, 22, 24],
        supportAllValues: !0
      },
      fontColor: {
        columns: 6,
        colors: n.fontColors || [
          {
            color: "var(--neutral-text-title-color)",
            label: "title"
          },
          {
            color: "var(--neutral-text-subtitle-color)",
            label: "subtitle"
          },
          {
            color: "var(--neutral-text-body-color)",
            label: "body"
          },
          {
            color: "var(--neutral-text-placeholder-color)",
            label: "placeholder"
          },
          {
            color: "var(--neutral-text-disabled-color)",
            label: "disabled"
          },
          {
            color: "var(--neutral-text-stable-color)",
            label: "stable"
          },
          {
            color: "var(--primary-main-color)",
            label: "primay"
          },
          {
            color: "var(--warning-main-color)",
            label: "warning"
          },
          {
            color: "var(--success-main-color)",
            label: "success"
          },
          {
            color: "var(--secondary3-main-color)",
            label: "secondary3"
          },
          {
            color: "var(--secondary4-main-color)",
            label: "secondary4"
          },
          {
            color: "var(--secondary5-main-color)",
            label: "secondary5"
          },
          {
            color: "var(--infor-main-color)",
            label: "infor"
          },
          {
            color: "var(--error-main-color)",
            label: "error"
          }
        ]
      },
      fontBackgroundColor: {
        columns: 6,
        colors: n.fontBgColors || [
          {
            color: "var(--neutral-main-background-color)",
            label: "main"
          },
          {
            color: "var(--neutral-main-reverse-background-color)",
            label: "main-reverse"
          },
          {
            color: "var(--neutral-absolute-background-color)",
            label: "absolute"
          },
          {
            color: "var(--neutral-absolute-reverse-background-color)",
            label: "absolute-reverse"
          },
          {
            color: "var(--primary-background)",
            label: "primay-bg"
          },
          {
            color: "var(--warning-background)",
            label: "warning-bg"
          },
          {
            color: "var(--success-background)",
            label: "success-bg"
          },
          {
            color: "var(--secondary3-background)",
            label: "secondary3-bg"
          },
          {
            color: "var(--secondary4-background)",
            label: "secondary4-bg"
          },
          {
            color: "var(--secondary5-background)",
            label: "secondary5-bg"
          },
          {
            color: "var(--infor-background)",
            label: "infor-bg"
          },
          {
            color: "var(--error-background)",
            label: "error-bg"
          }
        ]
      },
      heading: {
        options: [
          {
            model: "paragraph",
            title: "Paragraph",
            class: "ck-heading_paragraph"
          },
          {
            model: "heading1",
            view: "h1",
            title: "Heading 1",
            class: "ck-heading_heading1"
          },
          {
            model: "heading2",
            view: "h2",
            title: "Heading 2",
            class: "ck-heading_heading2"
          },
          {
            model: "heading3",
            view: "h3",
            title: "Heading 3",
            class: "ck-heading_heading3"
          },
          {
            model: "heading4",
            view: "h4",
            title: "Heading 4",
            class: "ck-heading_heading4"
          },
          {
            model: "heading5",
            view: "h5",
            title: "Heading 5",
            class: "ck-heading_heading5"
          },
          {
            model: "heading6",
            view: "h6",
            title: "Heading 6",
            class: "ck-heading_heading6"
          }
        ]
      },
      htmlSupport: {
        allow: [
          {
            name: /^.*$/,
            styles: !0,
            attributes: !0,
            classes: !0
          }
        ]
      },
      image: {
        toolbar: [
          "toggleImageCaption",
          "imageTextAlternative",
          "|",
          "imageStyle:inline",
          "imageStyle:wrapText",
          "imageStyle:breakText",
          "|",
          "resizeImage"
        ]
      },
      language: "vi",
      licenseKey: oZ,
      link: {
        addTargetToExternalLinks: !0,
        defaultProtocol: "https://",
        decorators: {
          toggleDownloadable: {
            mode: "manual",
            label: "Downloadable",
            attributes: {
              download: "file"
            }
          }
        }
      },
      menuBar: { isVisible: n.menuBar },
      placeholder: n.placeholder,
      table: {
        contentToolbar: ["tableColumn", "tableRow", "mergeTableCells", "tableProperties", "tableCellProperties"]
      }
    }
  } : {}, [i, r.language]);
  return /* @__PURE__ */ v.jsx(
    "div",
    {
      ref: t,
      className: `col editor-container editor-container_classic-editor editor-container_include-style ${n.className ?? ""} ${(a = n.helperText) != null && a.length ? "helper-text" : ""}`,
      "helper-text": n.helperText,
      style: n.style ? { "--helper-text-color": n.helperTextColor ?? "#e14337", ...n.style } : { "--helper-text-color": n.helperTextColor ?? "#e14337" },
      children: /* @__PURE__ */ v.jsx("div", { className: "editor-container__editor", children: /* @__PURE__ */ v.jsx("div", { ref: e, children: o && /* @__PURE__ */ v.jsx(
        j0,
        {
          onReady: n.onReady,
          onAfterDestroy: n.onAfterDestroy,
          onFocus: n.onFocus,
          onChange: n.onChange,
          onBlur: n.onBlur,
          editor: HN,
          onError: n.onError,
          config: o,
          data: n.value,
          disabled: n.disabled
        }
      ) }) })
    }
  );
}
const Ut = {
  "slider-container": "_slider-container_15o51_1",
  "range-bar": "_range-bar_15o51_21",
  "value-bar": "_value-bar_15o51_41",
  "thumb-dot": "_thumb-dot_15o51_69",
  "tooltip-container": "_tooltip-container_15o51_135",
  "tooltip-message": "_tooltip-message_15o51_149",
  "mark-point": "_mark-point_15o51_167",
  "mark-text": "_mark-text_15o51_189"
};
function pY({ min: n = 0, max: t = 100, formatter: e, className: i, rangeBarWidth: s, defaultValue: r, disabled: o, marks: a, onChange: l, onChangeComplete: c, range: u, step: d = 1, style: h, tooltip: m }) {
  const [g, p] = zt(u ? [n, t] : (n + t) / 2), w = yt(null), [y, k] = zt(null), [C, E] = zt(null), T = Ca(() => m ? {
    onFocus: (O) => {
      E(O.target);
    },
    onMouseOver: (O) => {
      E(O.target);
    },
    onMouseOut: (O) => {
      document.activeElement !== O.target && E(null);
    },
    onBlur: () => {
      var O;
      (O = w.current) != null && O.contains(document.activeElement) || E(null);
    }
  } : {}, [m, w]), L = D2(C && g ? { message: C.getAttribute("slider-value") ?? "", position: "top" } : null);
  return ht(() => {
    let O = g;
    const N = (J) => {
      if (!y || !w.current) return;
      const { left: D, width: B } = w.current.getBoundingClientRect();
      if (O = (J.clientX - D) / B * (t - n) + n, O = Math.round(O / d) * d, u && Array.isArray(g)) {
        let $ = [...g];
        y === "min" ? $[0] = Math.max(n, Math.min(O, $[1] - d)) : y === "max" && ($[1] = Math.min(t, Math.max(O, $[0] + d))), p($), l && l($);
      } else !u && typeof g == "number" && (O = Math.max(n, Math.min(t, O)), p(O), l && l(O));
    }, M = () => {
      k(null), c && c(O), document.removeEventListener("mousemove", N), document.removeEventListener("mouseup", M);
    };
    return y && (document.addEventListener("mousemove", N), document.addEventListener("mouseup", M)), () => {
      document.removeEventListener("mousemove", N), document.removeEventListener("mouseup", M);
    };
  }, [n, t, d, g, u, l, c, y]), ht(() => {
    r != null && (typeof r == "number" ? (u && (r > t || r < n ? p([n, t]) : p([n, r])), p(r)) : r[0] > t || r[0] < n || r[1] > t || r[1] < n ? p([n, t]) : p(r));
  }, [r]), /* @__PURE__ */ v.jsxs("div", { ref: w, className: `${Ut["slider-container"]} ${i ?? "subtitle-3"} ${u ? Ut.range : ""}`, style: h ? { "--range-width": s, ...h } : { "--range-width": s }, children: [
    /* @__PURE__ */ v.jsx("div", { className: Ut["range-bar"], children: /* @__PURE__ */ v.jsx(
      "div",
      {
        className: Ut["value-bar"],
        style: { marginLeft: u ? `${g[0] / (t - n) * 100}%` : "none", width: `calc(${(u ? g[1] - g[0] : g - n) / (t - n) * 100}%)` }
      }
    ) }),
    m && L && C ? Zh.createPortal(/* @__PURE__ */ v.jsx(aZ, { rect: C.getBoundingClientRect(), tooltip: L }), document.body) : /* @__PURE__ */ v.jsx(v.Fragment, {}),
    a && a.map((O) => O.value < n || O.value > t ? null : /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
      /* @__PURE__ */ v.jsx("div", { className: Ut["mark-point"], style: { left: `${(O.value - n) / (t - n) * 100}%` } }),
      /* @__PURE__ */ v.jsx(Ae, { className: `${Ut["mark-text"]}`, style: { left: `${(O.value - n) / (t - n) * 100}%` }, children: O.label ?? (e ? e(O.value) : O.value) })
    ] })),
    u ? /* @__PURE__ */ v.jsxs(v.Fragment, { children: [
      /* @__PURE__ */ v.jsx(
        "button",
        {
          type: "button",
          disabled: o,
          className: Ut["thumb-dot"],
          ...T,
          "slider-value": e ? e(g[0]) : g[0],
          onPointerDown: o ? void 0 : () => {
            k("min");
          },
          style: { left: `${(g[0] - n) / (t - n) * 100}%` }
        }
      ),
      /* @__PURE__ */ v.jsx(
        "button",
        {
          type: "button",
          disabled: o,
          className: Ut["thumb-dot"],
          ...T,
          "slider-value": e ? e(g[1]) : g[1],
          onPointerDown: o ? void 0 : () => {
            k("max");
          },
          style: { left: `${(g[1] - n) / (t - n) * 100}%` }
        }
      )
    ] }) : /* @__PURE__ */ v.jsx(
      "button",
      {
        type: "button",
        disabled: o,
        className: Ut["thumb-dot"],
        ...T,
        "slider-value": e ? e(g) : g,
        onPointerDown: o ? void 0 : () => {
          k("single");
        },
        style: { left: `${(g - n) / (t - n) * 100}%` }
      }
    )
  ] });
}
const aZ = ({ rect: n, tooltip: t }) => {
  switch (Ca(() => {
    if (!n) return null;
    let i = t.position ?? "bottom";
    return document.body.offsetHeight - n.bottom < 100 && i === "bottom" ? i = "top" : n.top < 100 && i === "top" && (i = "bottom"), document.body.offsetWidth - n.right < 100 && i === "right" ? i = "left" : n.left < 100 && i === "left" && (i = "right"), i;
  }, [n, t.position])) {
    case "top":
      return /* @__PURE__ */ v.jsxs("div", { ref: (i) => {
        if (i) {
          const s = i.getBoundingClientRect();
          s.x < 0 ? (i.style.left = n.x + n.width / 2 + "px", i.style.transform = "translateX(-1.8rem)", i.style.alignItems = "start") : s.right > document.body.offsetWidth && (i.style.left = "unset", i.style.right = document.body.offsetWidth - n.right - n.width / 2 + "px", i.style.transform = "translateX(-1.4rem)", i.style.alignItems = "end");
        }
      }, className: `col ${Ut["tooltip-container"]}`, style: { alignItems: "center", bottom: document.body.offsetHeight - n.top - 4, left: n.left + n.width / 2, transform: "translateX(-50%)" }, children: [
        /* @__PURE__ */ v.jsx(Ae, { className: `body-3 ${Ut["tooltip-message"]}`, maxLine: 2, children: t.message }),
        /* @__PURE__ */ v.jsx("div", { className: "row", style: { padding: "0 1.2rem", transform: "translateY(-0.2rem)" }, children: /* @__PURE__ */ v.jsx("div", { style: { borderLeft: "0.6rem solid transparent", borderRight: "0.6rem solid transparent", borderTop: "0.8rem solid var(--neutral-main-reverse-background-color)", borderRadius: 2 } }) })
      ] }, t.message);
    case "bottom":
      return /* @__PURE__ */ v.jsxs("div", { ref: (i) => {
        if (i) {
          const s = i.getBoundingClientRect();
          s.x < 0 ? (i.style.left = n.x + n.width / 2 + "px", i.style.transform = "translateX(-1.8rem)", i.style.alignItems = "start") : s.right > document.body.offsetWidth && (i.style.left = "unset", i.style.right = document.body.offsetWidth - n.right - n.width / 2 + "px", i.style.transform = "translateX(-1.4rem)", i.style.alignItems = "end");
        }
      }, className: `col ${Ut["tooltip-container"]}`, style: { alignItems: "center", top: n.bottom + 4, left: n.left + n.width / 2, transform: "translateX(-50%)" }, children: [
        /* @__PURE__ */ v.jsx("div", { className: "row", style: { padding: "0 1.2rem", transform: "translateY(0.2rem)" }, children: /* @__PURE__ */ v.jsx("div", { style: { borderLeft: "0.6rem solid transparent", borderRight: "0.6rem solid transparent", borderBottom: "0.8rem solid var(--neutral-main-reverse-background-color)", borderRadius: 2 } }) }),
        /* @__PURE__ */ v.jsx(Ae, { className: `body-3 ${Ut["tooltip-message"]}`, maxLine: 2, children: t.message })
      ] }, t.message);
    case "left":
      return /* @__PURE__ */ v.jsxs("div", { ref: (i) => {
        if (i) {
          const s = i.getBoundingClientRect();
          s.y < 0 ? (i.style.top = n.y + n.height / 2 + "px", i.style.transform = "translateY(-1.8rem)", i.style.alignItems = "start") : s.bottom > document.body.offsetHeight && (i.style.top = "unset", i.style.bottom = document.body.offsetHeight - n.bottom - n.height / 2 + "px", i.style.transform = "translateY(-1.4rem)", i.style.alignItems = "end");
        }
      }, className: `row ${Ut["tooltip-container"]}`, style: { top: n.top + n.height / 2, right: document.body.offsetWidth - n.left - 4, transform: "translateY(-50%)" }, children: [
        /* @__PURE__ */ v.jsx(Ae, { className: `body-3 ${Ut["tooltip-message"]}`, maxLine: 2, children: t.message }),
        /* @__PURE__ */ v.jsx("div", { className: "row", style: { padding: "1.2rem 0", transform: "translateX(-0.2rem)" }, children: /* @__PURE__ */ v.jsx("div", { style: { borderTop: "0.6rem solid transparent", borderBottom: "0.6rem solid transparent", borderLeft: "0.8rem solid var(--neutral-main-reverse-background-color)", borderRadius: 2 } }) })
      ] }, t.message);
    case "right":
      return /* @__PURE__ */ v.jsxs("div", { ref: (i) => {
        if (i) {
          const s = i.getBoundingClientRect();
          s.y < 0 ? (i.style.top = n.y + n.height / 2 + "px", i.style.transform = "translateY(-1.8rem)", i.style.alignItems = "start") : s.bottom > document.body.offsetHeight && (i.style.top = "unset", i.style.bottom = document.body.offsetHeight - n.bottom - n.height / 2 + "px", i.style.transform = "translateY(-1.4rem)", i.style.alignItems = "end");
        }
      }, className: `row ${Ut["tooltip-container"]}`, style: { top: n.top + n.height / 2, left: n.right + 4, transform: "translateY(-50%)" }, children: [
        /* @__PURE__ */ v.jsx("div", { className: "row", style: { padding: "1.2rem 0", transform: "translateX(0.2rem)" }, children: /* @__PURE__ */ v.jsx("div", { style: { borderTop: "0.6rem solid transparent", borderBottom: "0.6rem solid transparent", borderRight: "0.8rem solid var(--neutral-main-reverse-background-color)", borderRadius: 2 } }) }),
        /* @__PURE__ */ v.jsx(Ae, { className: `body-3 ${Ut["tooltip-message"]}`, maxLine: 2, children: t.message })
      ] }, t.message);
    default:
      return /* @__PURE__ */ v.jsx(v.Fragment, {});
  }
}, Se = (n) => typeof n == "string", Ao = () => {
  let n, t;
  const e = new Promise((i, s) => {
    n = i, t = s;
  });
  return e.resolve = n, e.reject = t, e;
}, Gw = (n) => n == null ? "" : "" + n, lZ = (n, t, e) => {
  n.forEach((i) => {
    t[i] && (e[i] = t[i]);
  });
}, cZ = /###/g, Kw = (n) => n && n.indexOf("###") > -1 ? n.replace(cZ, ".") : n, Zw = (n) => !n || Se(n), Yo = (n, t, e) => {
  const i = Se(t) ? t.split(".") : t;
  let s = 0;
  for (; s < i.length - 1; ) {
    if (Zw(n)) return {};
    const r = Kw(i[s]);
    !n[r] && e && (n[r] = new e()), Object.prototype.hasOwnProperty.call(n, r) ? n = n[r] : n = {}, ++s;
  }
  return Zw(n) ? {} : {
    obj: n,
    k: Kw(i[s])
  };
}, Yw = (n, t, e) => {
  const {
    obj: i,
    k: s
  } = Yo(n, t, Object);
  if (i !== void 0 || t.length === 1) {
    i[s] = e;
    return;
  }
  let r = t[t.length - 1], o = t.slice(0, t.length - 1), a = Yo(n, o, Object);
  for (; a.obj === void 0 && o.length; )
    r = `${o[o.length - 1]}.${r}`, o = o.slice(0, o.length - 1), a = Yo(n, o, Object), a != null && a.obj && typeof a.obj[`${a.k}.${r}`] < "u" && (a.obj = void 0);
  a.obj[`${a.k}.${r}`] = e;
}, uZ = (n, t, e, i) => {
  const {
    obj: s,
    k: r
  } = Yo(n, t, Object);
  s[r] = s[r] || [], s[r].push(e);
}, yc = (n, t) => {
  const {
    obj: e,
    k: i
  } = Yo(n, t);
  if (e && Object.prototype.hasOwnProperty.call(e, i))
    return e[i];
}, dZ = (n, t, e) => {
  const i = yc(n, e);
  return i !== void 0 ? i : yc(t, e);
}, w2 = (n, t, e) => {
  for (const i in t)
    i !== "__proto__" && i !== "constructor" && (i in n ? Se(n[i]) || n[i] instanceof String || Se(t[i]) || t[i] instanceof String ? e && (n[i] = t[i]) : w2(n[i], t[i], e) : n[i] = t[i]);
  return n;
}, vr = (n) => n.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
var hZ = {
  "&": "&amp;",
  "<": "&lt;",
  ">": "&gt;",
  '"': "&quot;",
  "'": "&#39;",
  "/": "&#x2F;"
};
const fZ = (n) => Se(n) ? n.replace(/[&<>"'\/]/g, (t) => hZ[t]) : n;
class mZ {
  constructor(t) {
    this.capacity = t, this.regExpMap = /* @__PURE__ */ new Map(), this.regExpQueue = [];
  }
  getRegExp(t) {
    const e = this.regExpMap.get(t);
    if (e !== void 0)
      return e;
    const i = new RegExp(t);
    return this.regExpQueue.length === this.capacity && this.regExpMap.delete(this.regExpQueue.shift()), this.regExpMap.set(t, i), this.regExpQueue.push(t), i;
  }
}
const gZ = [" ", ",", "?", "!", ";"], pZ = new mZ(20), bZ = (n, t, e) => {
  t = t || "", e = e || "";
  const i = gZ.filter((o) => t.indexOf(o) < 0 && e.indexOf(o) < 0);
  if (i.length === 0) return !0;
  const s = pZ.getRegExp(`(${i.map((o) => o === "?" ? "\\?" : o).join("|")})`);
  let r = !s.test(n);
  if (!r) {
    const o = n.indexOf(e);
    o > 0 && !s.test(n.substring(0, o)) && (r = !0);
  }
  return r;
}, Gh = function(n, t) {
  let e = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : ".";
  if (!n) return;
  if (n[t])
    return Object.prototype.hasOwnProperty.call(n, t) ? n[t] : void 0;
  const i = t.split(e);
  let s = n;
  for (let r = 0; r < i.length; ) {
    if (!s || typeof s != "object")
      return;
    let o, a = "";
    for (let l = r; l < i.length; ++l)
      if (l !== r && (a += e), a += i[l], o = s[a], o !== void 0) {
        if (["string", "number", "boolean"].indexOf(typeof o) > -1 && l < i.length - 1)
          continue;
        r += l - r + 1;
        break;
      }
    s = o;
  }
  return s;
}, kc = (n) => n == null ? void 0 : n.replace("_", "-"), wZ = {
  type: "logger",
  log(n) {
    this.output("log", n);
  },
  warn(n) {
    this.output("warn", n);
  },
  error(n) {
    this.output("error", n);
  },
  output(n, t) {
    var e, i;
    (i = (e = console == null ? void 0 : console[n]) == null ? void 0 : e.apply) == null || i.call(e, console, t);
  }
};
class Cc {
  constructor(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    this.init(t, e);
  }
  init(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    this.prefix = e.prefix || "i18next:", this.logger = t || wZ, this.options = e, this.debug = e.debug;
  }
  log() {
    for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
      e[i] = arguments[i];
    return this.forward(e, "log", "", !0);
  }
  warn() {
    for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
      e[i] = arguments[i];
    return this.forward(e, "warn", "", !0);
  }
  error() {
    for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
      e[i] = arguments[i];
    return this.forward(e, "error", "");
  }
  deprecate() {
    for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
      e[i] = arguments[i];
    return this.forward(e, "warn", "WARNING DEPRECATED: ", !0);
  }
  forward(t, e, i, s) {
    return s && !this.debug ? null : (Se(t[0]) && (t[0] = `${i}${this.prefix} ${t[0]}`), this.logger[e](t));
  }
  create(t) {
    return new Cc(this.logger, {
      prefix: `${this.prefix}:${t}:`,
      ...this.options
    });
  }
  clone(t) {
    return t = t || this.options, t.prefix = t.prefix || this.prefix, new Cc(this.logger, t);
  }
}
var pn = new Cc();
class au {
  constructor() {
    this.observers = {};
  }
  on(t, e) {
    return t.split(" ").forEach((i) => {
      this.observers[i] || (this.observers[i] = /* @__PURE__ */ new Map());
      const s = this.observers[i].get(e) || 0;
      this.observers[i].set(e, s + 1);
    }), this;
  }
  off(t, e) {
    if (this.observers[t]) {
      if (!e) {
        delete this.observers[t];
        return;
      }
      this.observers[t].delete(e);
    }
  }
  emit(t) {
    for (var e = arguments.length, i = new Array(e > 1 ? e - 1 : 0), s = 1; s < e; s++)
      i[s - 1] = arguments[s];
    this.observers[t] && Array.from(this.observers[t].entries()).forEach((o) => {
      let [a, l] = o;
      for (let c = 0; c < l; c++)
        a(...i);
    }), this.observers["*"] && Array.from(this.observers["*"].entries()).forEach((o) => {
      let [a, l] = o;
      for (let c = 0; c < l; c++)
        a.apply(a, [t, ...i]);
    });
  }
}
class Jw extends au {
  constructor(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
      ns: ["translation"],
      defaultNS: "translation"
    };
    super(), this.data = t || {}, this.options = e, this.options.keySeparator === void 0 && (this.options.keySeparator = "."), this.options.ignoreJSONStructure === void 0 && (this.options.ignoreJSONStructure = !0);
  }
  addNamespaces(t) {
    this.options.ns.indexOf(t) < 0 && this.options.ns.push(t);
  }
  removeNamespaces(t) {
    const e = this.options.ns.indexOf(t);
    e > -1 && this.options.ns.splice(e, 1);
  }
  getResource(t, e, i) {
    var c, u;
    let s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {};
    const r = s.keySeparator !== void 0 ? s.keySeparator : this.options.keySeparator, o = s.ignoreJSONStructure !== void 0 ? s.ignoreJSONStructure : this.options.ignoreJSONStructure;
    let a;
    t.indexOf(".") > -1 ? a = t.split(".") : (a = [t, e], i && (Array.isArray(i) ? a.push(...i) : Se(i) && r ? a.push(...i.split(r)) : a.push(i)));
    const l = yc(this.data, a);
    return !l && !e && !i && t.indexOf(".") > -1 && (t = a[0], e = a[1], i = a.slice(2).join(".")), l || !o || !Se(i) ? l : Gh((u = (c = this.data) == null ? void 0 : c[t]) == null ? void 0 : u[e], i, r);
  }
  addResource(t, e, i, s) {
    let r = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : {
      silent: !1
    };
    const o = r.keySeparator !== void 0 ? r.keySeparator : this.options.keySeparator;
    let a = [t, e];
    i && (a = a.concat(o ? i.split(o) : i)), t.indexOf(".") > -1 && (a = t.split("."), s = e, e = a[1]), this.addNamespaces(e), Yw(this.data, a, s), r.silent || this.emit("added", t, e, i, s);
  }
  addResources(t, e, i) {
    let s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {
      silent: !1
    };
    for (const r in i)
      (Se(i[r]) || Array.isArray(i[r])) && this.addResource(t, e, r, i[r], {
        silent: !0
      });
    s.silent || this.emit("added", t, e, i);
  }
  addResourceBundle(t, e, i, s, r) {
    let o = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : {
      silent: !1,
      skipCopy: !1
    }, a = [t, e];
    t.indexOf(".") > -1 && (a = t.split("."), s = i, i = e, e = a[1]), this.addNamespaces(e);
    let l = yc(this.data, a) || {};
    o.skipCopy || (i = JSON.parse(JSON.stringify(i))), s ? w2(l, i, r) : l = {
      ...l,
      ...i
    }, Yw(this.data, a, l), o.silent || this.emit("added", t, e, i);
  }
  removeResourceBundle(t, e) {
    this.hasResourceBundle(t, e) && delete this.data[t][e], this.removeNamespaces(e), this.emit("removed", t, e);
  }
  hasResourceBundle(t, e) {
    return this.getResource(t, e) !== void 0;
  }
  getResourceBundle(t, e) {
    return e || (e = this.options.defaultNS), this.getResource(t, e);
  }
  getDataByLanguage(t) {
    return this.data[t];
  }
  hasLanguageSomeTranslations(t) {
    const e = this.getDataByLanguage(t);
    return !!(e && Object.keys(e) || []).find((s) => e[s] && Object.keys(e[s]).length > 0);
  }
  toJSON() {
    return this.data;
  }
}
var v2 = {
  processors: {},
  addPostProcessor(n) {
    this.processors[n.name] = n;
  },
  handle(n, t, e, i, s) {
    return n.forEach((r) => {
      var o;
      t = ((o = this.processors[r]) == null ? void 0 : o.process(t, e, i, s)) ?? t;
    }), t;
  }
};
const Xw = {}, Qw = (n) => !Se(n) && typeof n != "boolean" && typeof n != "number";
class xc extends au {
  constructor(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    super(), lZ(["resourceStore", "languageUtils", "pluralResolver", "interpolator", "backendConnector", "i18nFormat", "utils"], t, this), this.options = e, this.options.keySeparator === void 0 && (this.options.keySeparator = "."), this.logger = pn.create("translator");
  }
  changeLanguage(t) {
    t && (this.language = t);
  }
  exists(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
      interpolation: {}
    };
    if (t == null)
      return !1;
    const i = this.resolve(t, e);
    return (i == null ? void 0 : i.res) !== void 0;
  }
  extractFromKey(t, e) {
    let i = e.nsSeparator !== void 0 ? e.nsSeparator : this.options.nsSeparator;
    i === void 0 && (i = ":");
    const s = e.keySeparator !== void 0 ? e.keySeparator : this.options.keySeparator;
    let r = e.ns || this.options.defaultNS || [];
    const o = i && t.indexOf(i) > -1, a = !this.options.userDefinedKeySeparator && !e.keySeparator && !this.options.userDefinedNsSeparator && !e.nsSeparator && !bZ(t, i, s);
    if (o && !a) {
      const l = t.match(this.interpolator.nestingRegexp);
      if (l && l.length > 0)
        return {
          key: t,
          namespaces: Se(r) ? [r] : r
        };
      const c = t.split(i);
      (i !== s || i === s && this.options.ns.indexOf(c[0]) > -1) && (r = c.shift()), t = c.join(s);
    }
    return {
      key: t,
      namespaces: Se(r) ? [r] : r
    };
  }
  translate(t, e, i) {
    if (typeof e != "object" && this.options.overloadTranslationOptionHandler && (e = this.options.overloadTranslationOptionHandler(arguments)), typeof e == "object" && (e = {
      ...e
    }), e || (e = {}), t == null) return "";
    Array.isArray(t) || (t = [String(t)]);
    const s = e.returnDetails !== void 0 ? e.returnDetails : this.options.returnDetails, r = e.keySeparator !== void 0 ? e.keySeparator : this.options.keySeparator, {
      key: o,
      namespaces: a
    } = this.extractFromKey(t[t.length - 1], e), l = a[a.length - 1], c = e.lng || this.language, u = e.appendNamespaceToCIMode || this.options.appendNamespaceToCIMode;
    if ((c == null ? void 0 : c.toLowerCase()) === "cimode") {
      if (u) {
        const D = e.nsSeparator || this.options.nsSeparator;
        return s ? {
          res: `${l}${D}${o}`,
          usedKey: o,
          exactUsedKey: o,
          usedLng: c,
          usedNS: l,
          usedParams: this.getUsedParamsDetails(e)
        } : `${l}${D}${o}`;
      }
      return s ? {
        res: o,
        usedKey: o,
        exactUsedKey: o,
        usedLng: c,
        usedNS: l,
        usedParams: this.getUsedParamsDetails(e)
      } : o;
    }
    const d = this.resolve(t, e);
    let h = d == null ? void 0 : d.res;
    const m = (d == null ? void 0 : d.usedKey) || o, g = (d == null ? void 0 : d.exactUsedKey) || o, p = ["[object Number]", "[object Function]", "[object RegExp]"], w = e.joinArrays !== void 0 ? e.joinArrays : this.options.joinArrays, y = !this.i18nFormat || this.i18nFormat.handleAsObject, k = e.count !== void 0 && !Se(e.count), C = xc.hasDefaultValue(e), E = k ? this.pluralResolver.getSuffix(c, e.count, e) : "", T = e.ordinal && k ? this.pluralResolver.getSuffix(c, e.count, {
      ordinal: !1
    }) : "", L = k && !e.ordinal && e.count === 0, O = L && e[`defaultValue${this.options.pluralSeparator}zero`] || e[`defaultValue${E}`] || e[`defaultValue${T}`] || e.defaultValue;
    let N = h;
    y && !h && C && (N = O);
    const M = Qw(N), J = Object.prototype.toString.apply(N);
    if (y && N && M && p.indexOf(J) < 0 && !(Se(w) && Array.isArray(N))) {
      if (!e.returnObjects && !this.options.returnObjects) {
        this.options.returnedObjectHandler || this.logger.warn("accessing an object - but returnObjects options is not enabled!");
        const D = this.options.returnedObjectHandler ? this.options.returnedObjectHandler(m, N, {
          ...e,
          ns: a
        }) : `key '${o} (${this.language})' returned an object instead of string.`;
        return s ? (d.res = D, d.usedParams = this.getUsedParamsDetails(e), d) : D;
      }
      if (r) {
        const D = Array.isArray(N), B = D ? [] : {}, $ = D ? g : m;
        for (const W in N)
          if (Object.prototype.hasOwnProperty.call(N, W)) {
            const Y = `${$}${r}${W}`;
            C && !h ? B[W] = this.translate(Y, {
              ...e,
              defaultValue: Qw(O) ? O[W] : void 0,
              joinArrays: !1,
              ns: a
            }) : B[W] = this.translate(Y, {
              ...e,
              joinArrays: !1,
              ns: a
            }), B[W] === Y && (B[W] = N[W]);
          }
        h = B;
      }
    } else if (y && Se(w) && Array.isArray(h))
      h = h.join(w), h && (h = this.extendTranslation(h, t, e, i));
    else {
      let D = !1, B = !1;
      !this.isValidLookup(h) && C && (D = !0, h = O), this.isValidLookup(h) || (B = !0, h = o);
      const W = (e.missingKeyNoValueFallbackToKey || this.options.missingKeyNoValueFallbackToKey) && B ? void 0 : h, Y = C && O !== h && this.options.updateMissing;
      if (B || D || Y) {
        if (this.logger.log(Y ? "updateKey" : "missingKey", c, l, o, Y ? O : h), r) {
          const te = this.resolve(o, {
            ...e,
            keySeparator: !1
          });
          te && te.res && this.logger.warn("Seems the loaded translations were in flat JSON format instead of nested. Either set keySeparator: false on init or make sure your translations are published in nested format.");
        }
        let ye = [];
        const Pe = this.languageUtils.getFallbackCodes(this.options.fallbackLng, e.lng || this.language);
        if (this.options.saveMissingTo === "fallback" && Pe && Pe[0])
          for (let te = 0; te < Pe.length; te++)
            ye.push(Pe[te]);
        else this.options.saveMissingTo === "all" ? ye = this.languageUtils.toResolveHierarchy(e.lng || this.language) : ye.push(e.lng || this.language);
        const Be = (te, R, K) => {
          var ie;
          const X = C && K !== h ? K : W;
          this.options.missingKeyHandler ? this.options.missingKeyHandler(te, l, R, X, Y, e) : (ie = this.backendConnector) != null && ie.saveMissing && this.backendConnector.saveMissing(te, l, R, X, Y, e), this.emit("missingKey", te, l, R, h);
        };
        this.options.saveMissing && (this.options.saveMissingPlurals && k ? ye.forEach((te) => {
          const R = this.pluralResolver.getSuffixes(te, e);
          L && e[`defaultValue${this.options.pluralSeparator}zero`] && R.indexOf(`${this.options.pluralSeparator}zero`) < 0 && R.push(`${this.options.pluralSeparator}zero`), R.forEach((K) => {
            Be([te], o + K, e[`defaultValue${K}`] || O);
          });
        }) : Be(ye, o, O));
      }
      h = this.extendTranslation(h, t, e, d, i), B && h === o && this.options.appendNamespaceToMissingKey && (h = `${l}:${o}`), (B || D) && this.options.parseMissingKeyHandler && (h = this.options.parseMissingKeyHandler(this.options.appendNamespaceToMissingKey ? `${l}:${o}` : o, D ? h : void 0));
    }
    return s ? (d.res = h, d.usedParams = this.getUsedParamsDetails(e), d) : h;
  }
  extendTranslation(t, e, i, s, r) {
    var c, u;
    var o = this;
    if ((c = this.i18nFormat) != null && c.parse)
      t = this.i18nFormat.parse(t, {
        ...this.options.interpolation.defaultVariables,
        ...i
      }, i.lng || this.language || s.usedLng, s.usedNS, s.usedKey, {
        resolved: s
      });
    else if (!i.skipInterpolation) {
      i.interpolation && this.interpolator.init({
        ...i,
        interpolation: {
          ...this.options.interpolation,
          ...i.interpolation
        }
      });
      const d = Se(t) && (((u = i == null ? void 0 : i.interpolation) == null ? void 0 : u.skipOnVariables) !== void 0 ? i.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables);
      let h;
      if (d) {
        const g = t.match(this.interpolator.nestingRegexp);
        h = g && g.length;
      }
      let m = i.replace && !Se(i.replace) ? i.replace : i;
      if (this.options.interpolation.defaultVariables && (m = {
        ...this.options.interpolation.defaultVariables,
        ...m
      }), t = this.interpolator.interpolate(t, m, i.lng || this.language || s.usedLng, i), d) {
        const g = t.match(this.interpolator.nestingRegexp), p = g && g.length;
        h < p && (i.nest = !1);
      }
      !i.lng && s && s.res && (i.lng = this.language || s.usedLng), i.nest !== !1 && (t = this.interpolator.nest(t, function() {
        for (var g = arguments.length, p = new Array(g), w = 0; w < g; w++)
          p[w] = arguments[w];
        return (r == null ? void 0 : r[0]) === p[0] && !i.context ? (o.logger.warn(`It seems you are nesting recursively key: ${p[0]} in key: ${e[0]}`), null) : o.translate(...p, e);
      }, i)), i.interpolation && this.interpolator.reset();
    }
    const a = i.postProcess || this.options.postProcess, l = Se(a) ? [a] : a;
    return t != null && (l != null && l.length) && i.applyPostProcessor !== !1 && (t = v2.handle(l, t, e, this.options && this.options.postProcessPassResolved ? {
      i18nResolved: {
        ...s,
        usedParams: this.getUsedParamsDetails(i)
      },
      ...i
    } : i, this)), t;
  }
  resolve(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, i, s, r, o, a;
    return Se(t) && (t = [t]), t.forEach((l) => {
      if (this.isValidLookup(i)) return;
      const c = this.extractFromKey(l, e), u = c.key;
      s = u;
      let d = c.namespaces;
      this.options.fallbackNS && (d = d.concat(this.options.fallbackNS));
      const h = e.count !== void 0 && !Se(e.count), m = h && !e.ordinal && e.count === 0, g = e.context !== void 0 && (Se(e.context) || typeof e.context == "number") && e.context !== "", p = e.lngs ? e.lngs : this.languageUtils.toResolveHierarchy(e.lng || this.language, e.fallbackLng);
      d.forEach((w) => {
        var y, k;
        this.isValidLookup(i) || (a = w, !Xw[`${p[0]}-${w}`] && ((y = this.utils) != null && y.hasLoadedNamespace) && !((k = this.utils) != null && k.hasLoadedNamespace(a)) && (Xw[`${p[0]}-${w}`] = !0, this.logger.warn(`key "${s}" for languages "${p.join(", ")}" won't get resolved as namespace "${a}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!")), p.forEach((C) => {
          var L;
          if (this.isValidLookup(i)) return;
          o = C;
          const E = [u];
          if ((L = this.i18nFormat) != null && L.addLookupKeys)
            this.i18nFormat.addLookupKeys(E, u, C, w, e);
          else {
            let O;
            h && (O = this.pluralResolver.getSuffix(C, e.count, e));
            const N = `${this.options.pluralSeparator}zero`, M = `${this.options.pluralSeparator}ordinal${this.options.pluralSeparator}`;
            if (h && (E.push(u + O), e.ordinal && O.indexOf(M) === 0 && E.push(u + O.replace(M, this.options.pluralSeparator)), m && E.push(u + N)), g) {
              const J = `${u}${this.options.contextSeparator}${e.context}`;
              E.push(J), h && (E.push(J + O), e.ordinal && O.indexOf(M) === 0 && E.push(J + O.replace(M, this.options.pluralSeparator)), m && E.push(J + N));
            }
          }
          let T;
          for (; T = E.pop(); )
            this.isValidLookup(i) || (r = T, i = this.getResource(C, w, T, e));
        }));
      });
    }), {
      res: i,
      usedKey: s,
      exactUsedKey: r,
      usedLng: o,
      usedNS: a
    };
  }
  isValidLookup(t) {
    return t !== void 0 && !(!this.options.returnNull && t === null) && !(!this.options.returnEmptyString && t === "");
  }
  getResource(t, e, i) {
    var r;
    let s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {};
    return (r = this.i18nFormat) != null && r.getResource ? this.i18nFormat.getResource(t, e, i, s) : this.resourceStore.getResource(t, e, i, s);
  }
  getUsedParamsDetails() {
    let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    const e = ["defaultValue", "ordinal", "context", "replace", "lng", "lngs", "fallbackLng", "ns", "keySeparator", "nsSeparator", "returnObjects", "returnDetails", "joinArrays", "postProcess", "interpolation"], i = t.replace && !Se(t.replace);
    let s = i ? t.replace : t;
    if (i && typeof t.count < "u" && (s.count = t.count), this.options.interpolation.defaultVariables && (s = {
      ...this.options.interpolation.defaultVariables,
      ...s
    }), !i) {
      s = {
        ...s
      };
      for (const r of e)
        delete s[r];
    }
    return s;
  }
  static hasDefaultValue(t) {
    const e = "defaultValue";
    for (const i in t)
      if (Object.prototype.hasOwnProperty.call(t, i) && e === i.substring(0, e.length) && t[i] !== void 0)
        return !0;
    return !1;
  }
}
class e0 {
  constructor(t) {
    this.options = t, this.supportedLngs = this.options.supportedLngs || !1, this.logger = pn.create("languageUtils");
  }
  getScriptPartFromCode(t) {
    if (t = kc(t), !t || t.indexOf("-") < 0) return null;
    const e = t.split("-");
    return e.length === 2 || (e.pop(), e[e.length - 1].toLowerCase() === "x") ? null : this.formatLanguageCode(e.join("-"));
  }
  getLanguagePartFromCode(t) {
    if (t = kc(t), !t || t.indexOf("-") < 0) return t;
    const e = t.split("-");
    return this.formatLanguageCode(e[0]);
  }
  formatLanguageCode(t) {
    if (Se(t) && t.indexOf("-") > -1) {
      let e;
      try {
        e = Intl.getCanonicalLocales(t)[0];
      } catch {
      }
      return e && this.options.lowerCaseLng && (e = e.toLowerCase()), e || (this.options.lowerCaseLng ? t.toLowerCase() : t);
    }
    return this.options.cleanCode || this.options.lowerCaseLng ? t.toLowerCase() : t;
  }
  isSupportedCode(t) {
    return (this.options.load === "languageOnly" || this.options.nonExplicitSupportedLngs) && (t = this.getLanguagePartFromCode(t)), !this.supportedLngs || !this.supportedLngs.length || this.supportedLngs.indexOf(t) > -1;
  }
  getBestMatchFromCodes(t) {
    if (!t) return null;
    let e;
    return t.forEach((i) => {
      if (e) return;
      const s = this.formatLanguageCode(i);
      (!this.options.supportedLngs || this.isSupportedCode(s)) && (e = s);
    }), !e && this.options.supportedLngs && t.forEach((i) => {
      if (e) return;
      const s = this.getLanguagePartFromCode(i);
      if (this.isSupportedCode(s)) return e = s;
      e = this.options.supportedLngs.find((r) => {
        if (r === s) return r;
        if (!(r.indexOf("-") < 0 && s.indexOf("-") < 0) && (r.indexOf("-") > 0 && s.indexOf("-") < 0 && r.substring(0, r.indexOf("-")) === s || r.indexOf(s) === 0 && s.length > 1))
          return r;
      });
    }), e || (e = this.getFallbackCodes(this.options.fallbackLng)[0]), e;
  }
  getFallbackCodes(t, e) {
    if (!t) return [];
    if (typeof t == "function" && (t = t(e)), Se(t) && (t = [t]), Array.isArray(t)) return t;
    if (!e) return t.default || [];
    let i = t[e];
    return i || (i = t[this.getScriptPartFromCode(e)]), i || (i = t[this.formatLanguageCode(e)]), i || (i = t[this.getLanguagePartFromCode(e)]), i || (i = t.default), i || [];
  }
  toResolveHierarchy(t, e) {
    const i = this.getFallbackCodes(e || this.options.fallbackLng || [], t), s = [], r = (o) => {
      o && (this.isSupportedCode(o) ? s.push(o) : this.logger.warn(`rejecting language code not found in supportedLngs: ${o}`));
    };
    return Se(t) && (t.indexOf("-") > -1 || t.indexOf("_") > -1) ? (this.options.load !== "languageOnly" && r(this.formatLanguageCode(t)), this.options.load !== "languageOnly" && this.options.load !== "currentOnly" && r(this.getScriptPartFromCode(t)), this.options.load !== "currentOnly" && r(this.getLanguagePartFromCode(t))) : Se(t) && r(this.formatLanguageCode(t)), i.forEach((o) => {
      s.indexOf(o) < 0 && r(this.formatLanguageCode(o));
    }), s;
  }
}
const t0 = {
  zero: 0,
  one: 1,
  two: 2,
  few: 3,
  many: 4,
  other: 5
}, i0 = {
  select: (n) => n === 1 ? "one" : "other",
  resolvedOptions: () => ({
    pluralCategories: ["one", "other"]
  })
};
class vZ {
  constructor(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    this.languageUtils = t, this.options = e, this.logger = pn.create("pluralResolver"), this.pluralRulesCache = {};
  }
  addRule(t, e) {
    this.rules[t] = e;
  }
  clearCache() {
    this.pluralRulesCache = {};
  }
  getRule(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    const i = kc(t === "dev" ? "en" : t), s = e.ordinal ? "ordinal" : "cardinal", r = JSON.stringify({
      cleanedCode: i,
      type: s
    });
    if (r in this.pluralRulesCache)
      return this.pluralRulesCache[r];
    let o;
    try {
      o = new Intl.PluralRules(i, {
        type: s
      });
    } catch {
      if (!Intl)
        return this.logger.error("No Intl support, please use an Intl polyfill!"), i0;
      if (!t.match(/-|_/)) return i0;
      const l = this.languageUtils.getLanguagePartFromCode(t);
      o = this.getRule(l, e);
    }
    return this.pluralRulesCache[r] = o, o;
  }
  needsPlural(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, i = this.getRule(t, e);
    return i || (i = this.getRule("dev", e)), (i == null ? void 0 : i.resolvedOptions().pluralCategories.length) > 1;
  }
  getPluralFormsOfKey(t, e) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    return this.getSuffixes(t, i).map((s) => `${e}${s}`);
  }
  getSuffixes(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, i = this.getRule(t, e);
    return i || (i = this.getRule("dev", e)), i ? i.resolvedOptions().pluralCategories.sort((s, r) => t0[s] - t0[r]).map((s) => `${this.options.prepend}${e.ordinal ? `ordinal${this.options.prepend}` : ""}${s}`) : [];
  }
  getSuffix(t, e) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    const s = this.getRule(t, i);
    return s ? `${this.options.prepend}${i.ordinal ? `ordinal${this.options.prepend}` : ""}${s.select(e)}` : (this.logger.warn(`no plural rule found for: ${t}`), this.getSuffix("dev", e, i));
  }
}
const n0 = function(n, t, e) {
  let i = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : ".", s = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : !0, r = dZ(n, t, e);
  return !r && s && Se(e) && (r = Gh(n, e, i), r === void 0 && (r = Gh(t, e, i))), r;
}, Vd = (n) => n.replace(/\$/g, "$$$$");
class _Z {
  constructor() {
    var e;
    let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    this.logger = pn.create("interpolator"), this.options = t, this.format = ((e = t == null ? void 0 : t.interpolation) == null ? void 0 : e.format) || ((i) => i), this.init(t);
  }
  init() {
    let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    t.interpolation || (t.interpolation = {
      escapeValue: !0
    });
    const {
      escape: e,
      escapeValue: i,
      useRawValueToEscape: s,
      prefix: r,
      prefixEscaped: o,
      suffix: a,
      suffixEscaped: l,
      formatSeparator: c,
      unescapeSuffix: u,
      unescapePrefix: d,
      nestingPrefix: h,
      nestingPrefixEscaped: m,
      nestingSuffix: g,
      nestingSuffixEscaped: p,
      nestingOptionsSeparator: w,
      maxReplaces: y,
      alwaysFormat: k
    } = t.interpolation;
    this.escape = e !== void 0 ? e : fZ, this.escapeValue = i !== void 0 ? i : !0, this.useRawValueToEscape = s !== void 0 ? s : !1, this.prefix = r ? vr(r) : o || "{{", this.suffix = a ? vr(a) : l || "}}", this.formatSeparator = c || ",", this.unescapePrefix = u ? "" : d || "-", this.unescapeSuffix = this.unescapePrefix ? "" : u || "", this.nestingPrefix = h ? vr(h) : m || vr("$t("), this.nestingSuffix = g ? vr(g) : p || vr(")"), this.nestingOptionsSeparator = w || ",", this.maxReplaces = y || 1e3, this.alwaysFormat = k !== void 0 ? k : !1, this.resetRegExp();
  }
  reset() {
    this.options && this.init(this.options);
  }
  resetRegExp() {
    const t = (e, i) => (e == null ? void 0 : e.source) === i ? (e.lastIndex = 0, e) : new RegExp(i, "g");
    this.regexp = t(this.regexp, `${this.prefix}(.+?)${this.suffix}`), this.regexpUnescape = t(this.regexpUnescape, `${this.prefix}${this.unescapePrefix}(.+?)${this.unescapeSuffix}${this.suffix}`), this.nestingRegexp = t(this.nestingRegexp, `${this.nestingPrefix}(.+?)${this.nestingSuffix}`);
  }
  interpolate(t, e, i, s) {
    var m;
    let r, o, a;
    const l = this.options && this.options.interpolation && this.options.interpolation.defaultVariables || {}, c = (g) => {
      if (g.indexOf(this.formatSeparator) < 0) {
        const k = n0(e, l, g, this.options.keySeparator, this.options.ignoreJSONStructure);
        return this.alwaysFormat ? this.format(k, void 0, i, {
          ...s,
          ...e,
          interpolationkey: g
        }) : k;
      }
      const p = g.split(this.formatSeparator), w = p.shift().trim(), y = p.join(this.formatSeparator).trim();
      return this.format(n0(e, l, w, this.options.keySeparator, this.options.ignoreJSONStructure), y, i, {
        ...s,
        ...e,
        interpolationkey: w
      });
    };
    this.resetRegExp();
    const u = (s == null ? void 0 : s.missingInterpolationHandler) || this.options.missingInterpolationHandler, d = ((m = s == null ? void 0 : s.interpolation) == null ? void 0 : m.skipOnVariables) !== void 0 ? s.interpolation.skipOnVariables : this.options.interpolation.skipOnVariables;
    return [{
      regex: this.regexpUnescape,
      safeValue: (g) => Vd(g)
    }, {
      regex: this.regexp,
      safeValue: (g) => this.escapeValue ? Vd(this.escape(g)) : Vd(g)
    }].forEach((g) => {
      for (a = 0; r = g.regex.exec(t); ) {
        const p = r[1].trim();
        if (o = c(p), o === void 0)
          if (typeof u == "function") {
            const y = u(t, r, s);
            o = Se(y) ? y : "";
          } else if (s && Object.prototype.hasOwnProperty.call(s, p))
            o = "";
          else if (d) {
            o = r[0];
            continue;
          } else
            this.logger.warn(`missed to pass in variable ${p} for interpolating ${t}`), o = "";
        else !Se(o) && !this.useRawValueToEscape && (o = Gw(o));
        const w = g.safeValue(o);
        if (t = t.replace(r[0], w), d ? (g.regex.lastIndex += o.length, g.regex.lastIndex -= r[0].length) : g.regex.lastIndex = 0, a++, a >= this.maxReplaces)
          break;
      }
    }), t;
  }
  nest(t, e) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, s, r, o;
    const a = (l, c) => {
      const u = this.nestingOptionsSeparator;
      if (l.indexOf(u) < 0) return l;
      const d = l.split(new RegExp(`${u}[ ]*{`));
      let h = `{${d[1]}`;
      l = d[0], h = this.interpolate(h, o);
      const m = h.match(/'/g), g = h.match(/"/g);
      (((m == null ? void 0 : m.length) ?? 0) % 2 === 0 && !g || g.length % 2 !== 0) && (h = h.replace(/'/g, '"'));
      try {
        o = JSON.parse(h), c && (o = {
          ...c,
          ...o
        });
      } catch (p) {
        return this.logger.warn(`failed parsing options string in nesting for key ${l}`, p), `${l}${u}${h}`;
      }
      return o.defaultValue && o.defaultValue.indexOf(this.prefix) > -1 && delete o.defaultValue, l;
    };
    for (; s = this.nestingRegexp.exec(t); ) {
      let l = [];
      o = {
        ...i
      }, o = o.replace && !Se(o.replace) ? o.replace : o, o.applyPostProcessor = !1, delete o.defaultValue;
      let c = !1;
      if (s[0].indexOf(this.formatSeparator) !== -1 && !/{.*}/.test(s[1])) {
        const u = s[1].split(this.formatSeparator).map((d) => d.trim());
        s[1] = u.shift(), l = u, c = !0;
      }
      if (r = e(a.call(this, s[1].trim(), o), o), r && s[0] === t && !Se(r)) return r;
      Se(r) || (r = Gw(r)), r || (this.logger.warn(`missed to resolve ${s[1]} for nesting ${t}`), r = ""), c && (r = l.reduce((u, d) => this.format(u, d, i.lng, {
        ...i,
        interpolationkey: s[1].trim()
      }), r.trim())), t = t.replace(s[0], r), this.regexp.lastIndex = 0;
    }
    return t;
  }
}
const yZ = (n) => {
  let t = n.toLowerCase().trim();
  const e = {};
  if (n.indexOf("(") > -1) {
    const i = n.split("(");
    t = i[0].toLowerCase().trim();
    const s = i[1].substring(0, i[1].length - 1);
    t === "currency" && s.indexOf(":") < 0 ? e.currency || (e.currency = s.trim()) : t === "relativetime" && s.indexOf(":") < 0 ? e.range || (e.range = s.trim()) : s.split(";").forEach((o) => {
      if (o) {
        const [a, ...l] = o.split(":"), c = l.join(":").trim().replace(/^'+|'+$/g, ""), u = a.trim();
        e[u] || (e[u] = c), c === "false" && (e[u] = !1), c === "true" && (e[u] = !0), isNaN(c) || (e[u] = parseInt(c, 10));
      }
    });
  }
  return {
    formatName: t,
    formatOptions: e
  };
}, _r = (n) => {
  const t = {};
  return (e, i, s) => {
    let r = s;
    s && s.interpolationkey && s.formatParams && s.formatParams[s.interpolationkey] && s[s.interpolationkey] && (r = {
      ...r,
      [s.interpolationkey]: void 0
    });
    const o = i + JSON.stringify(r);
    let a = t[o];
    return a || (a = n(kc(i), s), t[o] = a), a(e);
  };
};
class kZ {
  constructor() {
    let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    this.logger = pn.create("formatter"), this.options = t, this.formats = {
      number: _r((e, i) => {
        const s = new Intl.NumberFormat(e, {
          ...i
        });
        return (r) => s.format(r);
      }),
      currency: _r((e, i) => {
        const s = new Intl.NumberFormat(e, {
          ...i,
          style: "currency"
        });
        return (r) => s.format(r);
      }),
      datetime: _r((e, i) => {
        const s = new Intl.DateTimeFormat(e, {
          ...i
        });
        return (r) => s.format(r);
      }),
      relativetime: _r((e, i) => {
        const s = new Intl.RelativeTimeFormat(e, {
          ...i
        });
        return (r) => s.format(r, i.range || "day");
      }),
      list: _r((e, i) => {
        const s = new Intl.ListFormat(e, {
          ...i
        });
        return (r) => s.format(r);
      })
    }, this.init(t);
  }
  init(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
      interpolation: {}
    };
    this.formatSeparator = e.interpolation.formatSeparator || ",";
  }
  add(t, e) {
    this.formats[t.toLowerCase().trim()] = e;
  }
  addCached(t, e) {
    this.formats[t.toLowerCase().trim()] = _r(e);
  }
  format(t, e, i) {
    let s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {};
    const r = e.split(this.formatSeparator);
    if (r.length > 1 && r[0].indexOf("(") > 1 && r[0].indexOf(")") < 0 && r.find((a) => a.indexOf(")") > -1)) {
      const a = r.findIndex((l) => l.indexOf(")") > -1);
      r[0] = [r[0], ...r.splice(1, a)].join(this.formatSeparator);
    }
    return r.reduce((a, l) => {
      var d;
      const {
        formatName: c,
        formatOptions: u
      } = yZ(l);
      if (this.formats[c]) {
        let h = a;
        try {
          const m = ((d = s == null ? void 0 : s.formatParams) == null ? void 0 : d[s.interpolationkey]) || {}, g = m.locale || m.lng || s.locale || s.lng || i;
          h = this.formats[c](a, g, {
            ...u,
            ...s,
            ...m
          });
        } catch (m) {
          this.logger.warn(m);
        }
        return h;
      } else
        this.logger.warn(`there was no format function for ${c}`);
      return a;
    }, t);
  }
}
const CZ = (n, t) => {
  n.pending[t] !== void 0 && (delete n.pending[t], n.pendingCount--);
};
class xZ extends au {
  constructor(t, e, i) {
    var r, o;
    let s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : {};
    super(), this.backend = t, this.store = e, this.services = i, this.languageUtils = i.languageUtils, this.options = s, this.logger = pn.create("backendConnector"), this.waitingReads = [], this.maxParallelReads = s.maxParallelReads || 10, this.readingCalls = 0, this.maxRetries = s.maxRetries >= 0 ? s.maxRetries : 5, this.retryTimeout = s.retryTimeout >= 1 ? s.retryTimeout : 350, this.state = {}, this.queue = [], (o = (r = this.backend) == null ? void 0 : r.init) == null || o.call(r, i, s.backend, s);
  }
  queueLoad(t, e, i, s) {
    const r = {}, o = {}, a = {}, l = {};
    return t.forEach((c) => {
      let u = !0;
      e.forEach((d) => {
        const h = `${c}|${d}`;
        !i.reload && this.store.hasResourceBundle(c, d) ? this.state[h] = 2 : this.state[h] < 0 || (this.state[h] === 1 ? o[h] === void 0 && (o[h] = !0) : (this.state[h] = 1, u = !1, o[h] === void 0 && (o[h] = !0), r[h] === void 0 && (r[h] = !0), l[d] === void 0 && (l[d] = !0)));
      }), u || (a[c] = !0);
    }), (Object.keys(r).length || Object.keys(o).length) && this.queue.push({
      pending: o,
      pendingCount: Object.keys(o).length,
      loaded: {},
      errors: [],
      callback: s
    }), {
      toLoad: Object.keys(r),
      pending: Object.keys(o),
      toLoadLanguages: Object.keys(a),
      toLoadNamespaces: Object.keys(l)
    };
  }
  loaded(t, e, i) {
    const s = t.split("|"), r = s[0], o = s[1];
    e && this.emit("failedLoading", r, o, e), !e && i && this.store.addResourceBundle(r, o, i, void 0, void 0, {
      skipCopy: !0
    }), this.state[t] = e ? -1 : 2, e && i && (this.state[t] = 0);
    const a = {};
    this.queue.forEach((l) => {
      uZ(l.loaded, [r], o), CZ(l, t), e && l.errors.push(e), l.pendingCount === 0 && !l.done && (Object.keys(l.loaded).forEach((c) => {
        a[c] || (a[c] = {});
        const u = l.loaded[c];
        u.length && u.forEach((d) => {
          a[c][d] === void 0 && (a[c][d] = !0);
        });
      }), l.done = !0, l.errors.length ? l.callback(l.errors) : l.callback());
    }), this.emit("loaded", a), this.queue = this.queue.filter((l) => !l.done);
  }
  read(t, e, i) {
    let s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 0, r = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : this.retryTimeout, o = arguments.length > 5 ? arguments[5] : void 0;
    if (!t.length) return o(null, {});
    if (this.readingCalls >= this.maxParallelReads) {
      this.waitingReads.push({
        lng: t,
        ns: e,
        fcName: i,
        tried: s,
        wait: r,
        callback: o
      });
      return;
    }
    this.readingCalls++;
    const a = (c, u) => {
      if (this.readingCalls--, this.waitingReads.length > 0) {
        const d = this.waitingReads.shift();
        this.read(d.lng, d.ns, d.fcName, d.tried, d.wait, d.callback);
      }
      if (c && u && s < this.maxRetries) {
        setTimeout(() => {
          this.read.call(this, t, e, i, s + 1, r * 2, o);
        }, r);
        return;
      }
      o(c, u);
    }, l = this.backend[i].bind(this.backend);
    if (l.length === 2) {
      try {
        const c = l(t, e);
        c && typeof c.then == "function" ? c.then((u) => a(null, u)).catch(a) : a(null, c);
      } catch (c) {
        a(c);
      }
      return;
    }
    return l(t, e, a);
  }
  prepareLoading(t, e) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, s = arguments.length > 3 ? arguments[3] : void 0;
    if (!this.backend)
      return this.logger.warn("No backend was added via i18next.use. Will not load resources."), s && s();
    Se(t) && (t = this.languageUtils.toResolveHierarchy(t)), Se(e) && (e = [e]);
    const r = this.queueLoad(t, e, i, s);
    if (!r.toLoad.length)
      return r.pending.length || s(), null;
    r.toLoad.forEach((o) => {
      this.loadOne(o);
    });
  }
  load(t, e, i) {
    this.prepareLoading(t, e, {}, i);
  }
  reload(t, e, i) {
    this.prepareLoading(t, e, {
      reload: !0
    }, i);
  }
  loadOne(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "";
    const i = t.split("|"), s = i[0], r = i[1];
    this.read(s, r, "read", void 0, void 0, (o, a) => {
      o && this.logger.warn(`${e}loading namespace ${r} for language ${s} failed`, o), !o && a && this.logger.log(`${e}loaded namespace ${r} for language ${s}`, a), this.loaded(t, o, a);
    });
  }
  saveMissing(t, e, i, s, r) {
    var l, c, u, d, h;
    let o = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : {}, a = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : () => {
    };
    if ((c = (l = this.services) == null ? void 0 : l.utils) != null && c.hasLoadedNamespace && !((d = (u = this.services) == null ? void 0 : u.utils) != null && d.hasLoadedNamespace(e))) {
      this.logger.warn(`did not save key "${i}" as the namespace "${e}" was not yet loaded`, "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!");
      return;
    }
    if (!(i == null || i === "")) {
      if ((h = this.backend) != null && h.create) {
        const m = {
          ...o,
          isUpdate: r
        }, g = this.backend.create.bind(this.backend);
        if (g.length < 6)
          try {
            let p;
            g.length === 5 ? p = g(t, e, i, s, m) : p = g(t, e, i, s), p && typeof p.then == "function" ? p.then((w) => a(null, w)).catch(a) : a(null, p);
          } catch (p) {
            a(p);
          }
        else
          g(t, e, i, s, a, m);
      }
      !t || !t[0] || this.store.addResource(t[0], e, i, s);
    }
  }
}
const s0 = () => ({
  debug: !1,
  initAsync: !0,
  ns: ["translation"],
  defaultNS: ["translation"],
  fallbackLng: ["dev"],
  fallbackNS: !1,
  supportedLngs: !1,
  nonExplicitSupportedLngs: !1,
  load: "all",
  preload: !1,
  simplifyPluralSuffix: !0,
  keySeparator: ".",
  nsSeparator: ":",
  pluralSeparator: "_",
  contextSeparator: "_",
  partialBundledLanguages: !1,
  saveMissing: !1,
  updateMissing: !1,
  saveMissingTo: "fallback",
  saveMissingPlurals: !0,
  missingKeyHandler: !1,
  missingInterpolationHandler: !1,
  postProcess: !1,
  postProcessPassResolved: !1,
  returnNull: !1,
  returnEmptyString: !0,
  returnObjects: !1,
  joinArrays: !1,
  returnedObjectHandler: !1,
  parseMissingKeyHandler: !1,
  appendNamespaceToMissingKey: !1,
  appendNamespaceToCIMode: !1,
  overloadTranslationOptionHandler: (n) => {
    let t = {};
    if (typeof n[1] == "object" && (t = n[1]), Se(n[1]) && (t.defaultValue = n[1]), Se(n[2]) && (t.tDescription = n[2]), typeof n[2] == "object" || typeof n[3] == "object") {
      const e = n[3] || n[2];
      Object.keys(e).forEach((i) => {
        t[i] = e[i];
      });
    }
    return t;
  },
  interpolation: {
    escapeValue: !0,
    format: (n) => n,
    prefix: "{{",
    suffix: "}}",
    formatSeparator: ",",
    unescapePrefix: "-",
    nestingPrefix: "$t(",
    nestingSuffix: ")",
    nestingOptionsSeparator: ",",
    maxReplaces: 1e3,
    skipOnVariables: !0
  }
}), r0 = (n) => {
  var t, e;
  return Se(n.ns) && (n.ns = [n.ns]), Se(n.fallbackLng) && (n.fallbackLng = [n.fallbackLng]), Se(n.fallbackNS) && (n.fallbackNS = [n.fallbackNS]), ((e = (t = n.supportedLngs) == null ? void 0 : t.indexOf) == null ? void 0 : e.call(t, "cimode")) < 0 && (n.supportedLngs = n.supportedLngs.concat(["cimode"])), typeof n.initImmediate == "boolean" && (n.initAsync = n.initImmediate), n;
}, Sl = () => {
}, TZ = (n) => {
  Object.getOwnPropertyNames(Object.getPrototypeOf(n)).forEach((e) => {
    typeof n[e] == "function" && (n[e] = n[e].bind(n));
  });
};
class ka extends au {
  constructor() {
    let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, e = arguments.length > 1 ? arguments[1] : void 0;
    if (super(), this.options = r0(t), this.services = {}, this.logger = pn, this.modules = {
      external: []
    }, TZ(this), e && !this.isInitialized && !t.isClone) {
      if (!this.options.initAsync)
        return this.init(t, e), this;
      setTimeout(() => {
        this.init(t, e);
      }, 0);
    }
  }
  init() {
    var t = this;
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, i = arguments.length > 1 ? arguments[1] : void 0;
    this.isInitializing = !0, typeof e == "function" && (i = e, e = {}), e.defaultNS == null && e.ns && (Se(e.ns) ? e.defaultNS = e.ns : e.ns.indexOf("translation") < 0 && (e.defaultNS = e.ns[0]));
    const s = s0();
    this.options = {
      ...s,
      ...this.options,
      ...r0(e)
    }, this.options.interpolation = {
      ...s.interpolation,
      ...this.options.interpolation
    }, e.keySeparator !== void 0 && (this.options.userDefinedKeySeparator = e.keySeparator), e.nsSeparator !== void 0 && (this.options.userDefinedNsSeparator = e.nsSeparator);
    const r = (u) => u ? typeof u == "function" ? new u() : u : null;
    if (!this.options.isClone) {
      this.modules.logger ? pn.init(r(this.modules.logger), this.options) : pn.init(null, this.options);
      let u;
      this.modules.formatter ? u = this.modules.formatter : u = kZ;
      const d = new e0(this.options);
      this.store = new Jw(this.options.resources, this.options);
      const h = this.services;
      h.logger = pn, h.resourceStore = this.store, h.languageUtils = d, h.pluralResolver = new vZ(d, {
        prepend: this.options.pluralSeparator,
        simplifyPluralSuffix: this.options.simplifyPluralSuffix
      }), u && (!this.options.interpolation.format || this.options.interpolation.format === s.interpolation.format) && (h.formatter = r(u), h.formatter.init(h, this.options), this.options.interpolation.format = h.formatter.format.bind(h.formatter)), h.interpolator = new _Z(this.options), h.utils = {
        hasLoadedNamespace: this.hasLoadedNamespace.bind(this)
      }, h.backendConnector = new xZ(r(this.modules.backend), h.resourceStore, h, this.options), h.backendConnector.on("*", function(m) {
        for (var g = arguments.length, p = new Array(g > 1 ? g - 1 : 0), w = 1; w < g; w++)
          p[w - 1] = arguments[w];
        t.emit(m, ...p);
      }), this.modules.languageDetector && (h.languageDetector = r(this.modules.languageDetector), h.languageDetector.init && h.languageDetector.init(h, this.options.detection, this.options)), this.modules.i18nFormat && (h.i18nFormat = r(this.modules.i18nFormat), h.i18nFormat.init && h.i18nFormat.init(this)), this.translator = new xc(this.services, this.options), this.translator.on("*", function(m) {
        for (var g = arguments.length, p = new Array(g > 1 ? g - 1 : 0), w = 1; w < g; w++)
          p[w - 1] = arguments[w];
        t.emit(m, ...p);
      }), this.modules.external.forEach((m) => {
        m.init && m.init(this);
      });
    }
    if (this.format = this.options.interpolation.format, i || (i = Sl), this.options.fallbackLng && !this.services.languageDetector && !this.options.lng) {
      const u = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
      u.length > 0 && u[0] !== "dev" && (this.options.lng = u[0]);
    }
    !this.services.languageDetector && !this.options.lng && this.logger.warn("init: no languageDetector is used and no lng is defined"), ["getResource", "hasResourceBundle", "getResourceBundle", "getDataByLanguage"].forEach((u) => {
      this[u] = function() {
        return t.store[u](...arguments);
      };
    }), ["addResource", "addResources", "addResourceBundle", "removeResourceBundle"].forEach((u) => {
      this[u] = function() {
        return t.store[u](...arguments), t;
      };
    });
    const l = Ao(), c = () => {
      const u = (d, h) => {
        this.isInitializing = !1, this.isInitialized && !this.initializedStoreOnce && this.logger.warn("init: i18next is already initialized. You should call init just once!"), this.isInitialized = !0, this.options.isClone || this.logger.log("initialized", this.options), this.emit("initialized", this.options), l.resolve(h), i(d, h);
      };
      if (this.languages && !this.isInitialized) return u(null, this.t.bind(this));
      this.changeLanguage(this.options.lng, u);
    };
    return this.options.resources || !this.options.initAsync ? c() : setTimeout(c, 0), l;
  }
  loadResources(t) {
    var r, o;
    let i = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : Sl;
    const s = Se(t) ? t : this.language;
    if (typeof t == "function" && (i = t), !this.options.resources || this.options.partialBundledLanguages) {
      if ((s == null ? void 0 : s.toLowerCase()) === "cimode" && (!this.options.preload || this.options.preload.length === 0)) return i();
      const a = [], l = (c) => {
        if (!c || c === "cimode") return;
        this.services.languageUtils.toResolveHierarchy(c).forEach((d) => {
          d !== "cimode" && a.indexOf(d) < 0 && a.push(d);
        });
      };
      s ? l(s) : this.services.languageUtils.getFallbackCodes(this.options.fallbackLng).forEach((u) => l(u)), (o = (r = this.options.preload) == null ? void 0 : r.forEach) == null || o.call(r, (c) => l(c)), this.services.backendConnector.load(a, this.options.ns, (c) => {
        !c && !this.resolvedLanguage && this.language && this.setResolvedLanguage(this.language), i(c);
      });
    } else
      i(null);
  }
  reloadResources(t, e, i) {
    const s = Ao();
    return typeof t == "function" && (i = t, t = void 0), typeof e == "function" && (i = e, e = void 0), t || (t = this.languages), e || (e = this.options.ns), i || (i = Sl), this.services.backendConnector.reload(t, e, (r) => {
      s.resolve(), i(r);
    }), s;
  }
  use(t) {
    if (!t) throw new Error("You are passing an undefined module! Please check the object you are passing to i18next.use()");
    if (!t.type) throw new Error("You are passing a wrong module! Please check the object you are passing to i18next.use()");
    return t.type === "backend" && (this.modules.backend = t), (t.type === "logger" || t.log && t.warn && t.error) && (this.modules.logger = t), t.type === "languageDetector" && (this.modules.languageDetector = t), t.type === "i18nFormat" && (this.modules.i18nFormat = t), t.type === "postProcessor" && v2.addPostProcessor(t), t.type === "formatter" && (this.modules.formatter = t), t.type === "3rdParty" && this.modules.external.push(t), this;
  }
  setResolvedLanguage(t) {
    if (!(!t || !this.languages) && !(["cimode", "dev"].indexOf(t) > -1))
      for (let e = 0; e < this.languages.length; e++) {
        const i = this.languages[e];
        if (!(["cimode", "dev"].indexOf(i) > -1) && this.store.hasLanguageSomeTranslations(i)) {
          this.resolvedLanguage = i;
          break;
        }
      }
  }
  changeLanguage(t, e) {
    var i = this;
    this.isLanguageChangingTo = t;
    const s = Ao();
    this.emit("languageChanging", t);
    const r = (l) => {
      this.language = l, this.languages = this.services.languageUtils.toResolveHierarchy(l), this.resolvedLanguage = void 0, this.setResolvedLanguage(l);
    }, o = (l, c) => {
      c ? (r(c), this.translator.changeLanguage(c), this.isLanguageChangingTo = void 0, this.emit("languageChanged", c), this.logger.log("languageChanged", c)) : this.isLanguageChangingTo = void 0, s.resolve(function() {
        return i.t(...arguments);
      }), e && e(l, function() {
        return i.t(...arguments);
      });
    }, a = (l) => {
      var u, d;
      !t && !l && this.services.languageDetector && (l = []);
      const c = Se(l) ? l : this.services.languageUtils.getBestMatchFromCodes(l);
      c && (this.language || r(c), this.translator.language || this.translator.changeLanguage(c), (d = (u = this.services.languageDetector) == null ? void 0 : u.cacheUserLanguage) == null || d.call(u, c)), this.loadResources(c, (h) => {
        o(h, c);
      });
    };
    return !t && this.services.languageDetector && !this.services.languageDetector.async ? a(this.services.languageDetector.detect()) : !t && this.services.languageDetector && this.services.languageDetector.async ? this.services.languageDetector.detect.length === 0 ? this.services.languageDetector.detect().then(a) : this.services.languageDetector.detect(a) : a(t), s;
  }
  getFixedT(t, e, i) {
    var s = this;
    const r = function(o, a) {
      let l;
      if (typeof a != "object") {
        for (var c = arguments.length, u = new Array(c > 2 ? c - 2 : 0), d = 2; d < c; d++)
          u[d - 2] = arguments[d];
        l = s.options.overloadTranslationOptionHandler([o, a].concat(u));
      } else
        l = {
          ...a
        };
      l.lng = l.lng || r.lng, l.lngs = l.lngs || r.lngs, l.ns = l.ns || r.ns, l.keyPrefix !== "" && (l.keyPrefix = l.keyPrefix || i || r.keyPrefix);
      const h = s.options.keySeparator || ".";
      let m;
      return l.keyPrefix && Array.isArray(o) ? m = o.map((g) => `${l.keyPrefix}${h}${g}`) : m = l.keyPrefix ? `${l.keyPrefix}${h}${o}` : o, s.t(m, l);
    };
    return Se(t) ? r.lng = t : r.lngs = t, r.ns = e, r.keyPrefix = i, r;
  }
  t() {
    var s;
    for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
      e[i] = arguments[i];
    return (s = this.translator) == null ? void 0 : s.translate(...e);
  }
  exists() {
    var s;
    for (var t = arguments.length, e = new Array(t), i = 0; i < t; i++)
      e[i] = arguments[i];
    return (s = this.translator) == null ? void 0 : s.exists(...e);
  }
  setDefaultNamespace(t) {
    this.options.defaultNS = t;
  }
  hasLoadedNamespace(t) {
    let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    if (!this.isInitialized)
      return this.logger.warn("hasLoadedNamespace: i18next was not initialized", this.languages), !1;
    if (!this.languages || !this.languages.length)
      return this.logger.warn("hasLoadedNamespace: i18n.languages were undefined or empty", this.languages), !1;
    const i = e.lng || this.resolvedLanguage || this.languages[0], s = this.options ? this.options.fallbackLng : !1, r = this.languages[this.languages.length - 1];
    if (i.toLowerCase() === "cimode") return !0;
    const o = (a, l) => {
      const c = this.services.backendConnector.state[`${a}|${l}`];
      return c === -1 || c === 0 || c === 2;
    };
    if (e.precheck) {
      const a = e.precheck(this, o);
      if (a !== void 0) return a;
    }
    return !!(this.hasResourceBundle(i, t) || !this.services.backendConnector.backend || this.options.resources && !this.options.partialBundledLanguages || o(i, t) && (!s || o(r, t)));
  }
  loadNamespaces(t, e) {
    const i = Ao();
    return this.options.ns ? (Se(t) && (t = [t]), t.forEach((s) => {
      this.options.ns.indexOf(s) < 0 && this.options.ns.push(s);
    }), this.loadResources((s) => {
      i.resolve(), e && e(s);
    }), i) : (e && e(), Promise.resolve());
  }
  loadLanguages(t, e) {
    const i = Ao();
    Se(t) && (t = [t]);
    const s = this.options.preload || [], r = t.filter((o) => s.indexOf(o) < 0 && this.services.languageUtils.isSupportedCode(o));
    return r.length ? (this.options.preload = s.concat(r), this.loadResources((o) => {
      i.resolve(), e && e(o);
    }), i) : (e && e(), Promise.resolve());
  }
  dir(t) {
    var s, r;
    if (t || (t = this.resolvedLanguage || (((s = this.languages) == null ? void 0 : s.length) > 0 ? this.languages[0] : this.language)), !t) return "rtl";
    const e = ["ar", "shu", "sqr", "ssh", "xaa", "yhd", "yud", "aao", "abh", "abv", "acm", "acq", "acw", "acx", "acy", "adf", "ads", "aeb", "aec", "afb", "ajp", "apc", "apd", "arb", "arq", "ars", "ary", "arz", "auz", "avl", "ayh", "ayl", "ayn", "ayp", "bbz", "pga", "he", "iw", "ps", "pbt", "pbu", "pst", "prp", "prd", "ug", "ur", "ydd", "yds", "yih", "ji", "yi", "hbo", "men", "xmn", "fa", "jpr", "peo", "pes", "prs", "dv", "sam", "ckb"], i = ((r = this.services) == null ? void 0 : r.languageUtils) || new e0(s0());
    return e.indexOf(i.getLanguagePartFromCode(t)) > -1 || t.toLowerCase().indexOf("-arab") > 1 ? "rtl" : "ltr";
  }
  static createInstance() {
    let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, e = arguments.length > 1 ? arguments[1] : void 0;
    return new ka(t, e);
  }
  cloneInstance() {
    let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : Sl;
    const i = t.forkResourceStore;
    i && delete t.forkResourceStore;
    const s = {
      ...this.options,
      ...t,
      isClone: !0
    }, r = new ka(s);
    if ((t.debug !== void 0 || t.prefix !== void 0) && (r.logger = r.logger.clone(t)), ["store", "services", "language"].forEach((a) => {
      r[a] = this[a];
    }), r.services = {
      ...this.services
    }, r.services.utils = {
      hasLoadedNamespace: r.hasLoadedNamespace.bind(r)
    }, i) {
      const a = Object.keys(this.store.data).reduce((l, c) => (l[c] = {
        ...this.store.data[c]
      }, Object.keys(l[c]).reduce((u, d) => (u[d] = {
        ...l[c][d]
      }, u), {})), {});
      r.store = new Jw(a, s), r.services.resourceStore = r.store;
    }
    return r.translator = new xc(r.services, s), r.translator.on("*", function(a) {
      for (var l = arguments.length, c = new Array(l > 1 ? l - 1 : 0), u = 1; u < l; u++)
        c[u - 1] = arguments[u];
      r.emit(a, ...c);
    }), r.init(s, e), r.translator.options = s, r.translator.backendConnector.services.utils = {
      hasLoadedNamespace: r.hasLoadedNamespace.bind(r)
    }, r;
  }
  toJSON() {
    return {
      options: this.options,
      store: this.store,
      language: this.language,
      languages: this.languages,
      resolvedLanguage: this.resolvedLanguage
    };
  }
}
const Xt = ka.createInstance();
Xt.createInstance = ka.createInstance;
Xt.createInstance;
Xt.dir;
Xt.init;
Xt.loadResources;
Xt.reloadResources;
Xt.use;
Xt.changeLanguage;
Xt.getFixedT;
Xt.t;
Xt.exists;
Xt.setDefaultNamespace;
Xt.hasLoadedNamespace;
Xt.loadNamespaces;
Xt.loadLanguages;
const EZ = {
  en: {
    translation: {
      su: "Su",
      mo: "Mo",
      tu: "Tu",
      we: "We",
      th: "Th",
      fr: "Fr",
      sa: "Sa",
      yesterday: "Yesterday",
      lastWeek: "Last week",
      lastMonth: "Last month",
      lastYear: "Last year",
      january: "January",
      february: "February",
      march: "March",
      april: "April",
      may: "May",
      june: "June",
      july: "July",
      august: "August",
      september: "September",
      october: "October",
      november: "November",
      december: "December",
      today: "Today",
      apply: "Apply",
      cancel: "Cancel",
      submit: "Submit",
      limitFileError: "File {{file.name}} exceeds the maximum size of {{sizeTitle}}",
      limitFileWarning: "File size should not exceed {{sizeTitle}}.",
      uploadFileAction: "Click or drag and drop to upload file",
      file: "File",
      remove: "Remove",
      choose: "Choose",
      select: "Select",
      add: "Add",
      all: "All",
      noResultFound: "No result found",
      ofItems: "of {{totalItem}} items",
      page: "Page",
      go: "Go to",
      or: "Or",
      username: "Username",
      password: "Password",
      dontHaveAccount: "Don't have an account?",
      forgotPassword: "Forgot your password?",
      login: "Log in",
      loginTo: "Log in to",
      signup: "Sign up",
      signupFor: "Sign up for",
      "start-date": "Start date",
      "end-date": "End date",
      reset: "Reset",
      date: "Date",
      on: "On",
      daily: "Daily",
      weekly: "Weekly",
      monthly: "Monthly",
      last: "Last"
    }
  },
  vi: {
    translation: {
      su: "CN",
      mo: "T2",
      tu: "T3",
      we: "T4",
      th: "T5",
      fr: "T6",
      sa: "T7",
      yesterday: "Hôm qua",
      lastWeek: "Tuần trước",
      lastMonth: "Tháng trước",
      lastYear: "Năm trước",
      january: "Tháng 1",
      february: "Tháng 2",
      march: "Tháng 3",
      april: "Tháng 4",
      may: "Tháng 5",
      june: "Tháng 6",
      july: "Tháng 7",
      august: "Tháng 8",
      september: "Tháng 9",
      october: "Tháng 10",
      november: "Tháng 11",
      december: "Tháng 12",
      today: "Hôm nay",
      apply: "Áp dụng",
      cancel: "Hủy",
      submit: "Xác nhận",
      limitFileError: "Tệp {{name}} vượt quá dung lượng tối đa {{sizeTitle}}",
      limitFileWarning: "Tệp không được vượt quá {{sizeTitle}}.",
      uploadFileAction: "Nhấn hoặc kéo vào để tải tệp lên",
      file: "Tệp",
      remove: "Xóa",
      choose: "Chọn",
      select: "Chọn",
      add: "Thêm",
      all: "Tất cả",
      noResultFound: "Không tìm thấy kết quả",
      ofItems: "trên {{totalItem}} kết quả",
      page: "Trang",
      go: "Đi tới",
      or: "Hoặc",
      username: "Tên đăng nhập",
      password: "Mật khẩu",
      dontHaveAccount: "Chưa có tài khoản?",
      forgotPassword: "Quên mật khẩu?",
      login: "Đăng nhập",
      loginTo: "Đăng nhập vào",
      signup: "Đăng ký",
      signupFor: "Đăng ký",
      "start-date": "Ngày bắt đầu",
      "end-date": "Ngày kết thúc",
      reset: "Mặc định",
      date: "Ngày",
      on: "Vào",
      daily: "Hàng ngày",
      weekly: "Hàng tuần",
      monthly: "Hàng tháng",
      last: "Cuối tháng"
    }
  }
};
Xt.use(ox).init({
  resources: EZ,
  lng: "en",
  // Default language
  fallbackLng: "en",
  // Fallback language
  // backend: {loadPath: "somevariables"},
  interpolation: {
    escapeValue: !1
    // React already escapes values
  }
});
export {
  mn as Button,
  jx as Calendar,
  YZ as Carousel,
  s5 as CellAlignItems,
  a0 as Checkbox,
  Xo as ComponentStatus,
  gY as CustomCkEditor5,
  LZ as DateTimePicker,
  OZ as Dialog,
  mx as DialogAlignment,
  WZ as ImportFile,
  GZ as InfiniteScroll,
  QZ as InputOtp,
  XZ as NumberPicker,
  NZ as Pagination,
  d0 as Popup,
  MZ as ProgressBar,
  ZZ as ProgressCircle,
  jZ as RadioButton,
  KZ as Rating,
  hx as Select1,
  BZ as SelectMultiple,
  pY as Slider,
  RZ as Switch,
  zZ as Table,
  JZ as Tag,
  $Z as TbBody,
  T0 as TbCell,
  DZ as TbHeader,
  FZ as TbRow,
  Ae as Text,
  UZ as TextArea,
  Ps as TextField,
  qZ as ToastContainer,
  j5 as ToastMessage,
  eY as WLoginView,
  rt as Winicon,
  Ss as closePopup,
  x0 as getStatusIcon,
  Xt as i18n,
  VZ as showDialog,
  Io as showPopup
};
//# sourceMappingURL=index.es.js.map
